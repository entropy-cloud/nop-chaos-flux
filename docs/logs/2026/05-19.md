# Development Log - 2026-05-19

### 2026-05-19 (plan 399 - compiler diagnostic fidelity sync)

- Executed the retained `19-04` / `19-05` diagnostic-fidelity fixes in `docs/plans/399-deep-audit-2026-05-19-compiler-diagnostic-fidelity-plan.md`: `packages/flux-formula/src/compile/compile-node.ts` now preserves the original compilation failure on diagnostic `cause` while still emitting readable fallback messages, and `packages/flux-compiler/src/schema-compiler/flux-value-shape-validation.ts` now retains per-branch union failure details instead of collapsing them into a generic top-level summary.
- Rebuilt `@nop-chaos/flux-core` so the expanded `ExpressionCompileOptions.reportDiagnostic` contract is reflected in emitted declarations consumed by `@nop-chaos/flux-formula`; this cleared the build-only stale declaration mismatch around the new `cause?: unknown` field.
- Added and confirmed focused proof on the owner surfaces: `packages/flux-formula/src/index.test.ts` now asserts compile diagnostics retain the original thrown error object, and `packages/flux-compiler/src/schema-compiler-shape-validation-analyze.test.ts` now proves union-shaped value diagnostics include branch-level `Option N` failure detail.
- Follow-up closure proof tightened the final owner-facing surface instead of only the intermediate callback payload: `packages/flux-core/src/schema-diagnostics/index.ts` and `packages/flux-compiler/src/schema-compiler/diagnostics.ts` now preserve optional diagnostic `cause`, and `packages/flux-compiler/src/schema-compiler-shape-validation-analyze.test.ts` now proves `compiler.validate(...)` returns `unhandled-compilation-error` diagnostics with the original thrown `Error` attached on `cause`.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-formula exec vitest run src/index.test.ts` (`1` file / `29` tests) and `pnpm --filter @nop-chaos/flux-compiler exec vitest run src/schema-compiler-shape-validation-analyze.test.ts src/schema-compiler-table.test.ts src/tables.test.ts` (`3` files / `64` tests).
- Package-local verification also passed: `pnpm --filter @nop-chaos/flux-core build`, `pnpm --filter @nop-chaos/flux-core lint`, `pnpm --filter @nop-chaos/flux-formula typecheck`, `build`, `lint`, and `pnpm --filter @nop-chaos/flux-compiler typecheck`, `build`, `lint`.
- Updated `docs/architecture/flux-core.md` so the supported compiler diagnostic contract now explicitly records both retained compile `cause` fidelity and union branch-detail preservation.

### 2026-05-19 (plan 383 - table schema authoring contract sync)

- Executed the retained `12-03` / `12-04` table authoring sync in `docs/plans/383-deep-audit-2026-05-19-table-schema-authoring-contract-plan.md`: `packages/flux-renderers-data/src/schemas.ts` now exposes explicit authoring input types for table columns, keeping `loadingContent` as the public loading slot field and adding the missing author-facing nested column inputs `label?: SchemaInput | string`, `cell?: SchemaInput`, and `body?: SchemaInput`.
- Synced compiler fixtures and coverage proof away from the stale `loadingSlot` name: `packages/flux-compiler/src/schema-compiler-prop-coverage.test-support.ts`, `packages/flux-compiler/src/schema-compiler-prop-coverage-data-structures.test.ts`, and `packages/flux-compiler/src/schema-compiler-registry-fixtures.ts` now all exercise `loadingContent` as the public table field.
- Updated `docs/architecture/field-metadata-slot-modeling.md` so the deep region extraction contract explicitly covers `table.columns[].body -> columns.N.quickEditBody` in addition to `label`, `buttons`, and `cell`.
- Focused verification passed on the owner surfaces: `pnpm --filter @nop-chaos/flux-compiler exec vitest run src/schema-compiler-prop-coverage-data-structures.test.ts src/schema-compiler-table.test.ts src/schema-compiler-registry-features.test.ts src/tables.test.ts` (`4` files / `66` tests) and `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-renderer-definition-contracts.test.ts src/__tests__/table-data-and-layout.test.tsx src/__tests__/table-quick-edit-cell.unit.test.tsx` (`3` files / `28` tests).
- Package-local closure gates also passed: `pnpm --filter @nop-chaos/flux-compiler typecheck`, `build`, `lint` and `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `build`, `lint`.
- Follow-up closure proof now explicitly covers the missing deep-region extraction case the owner doc records: `packages/flux-compiler/src/schema-compiler-table.test.ts` proves authored `columns[].body` compiles into `columns.N.quickEditBody` and `quickEditBodyRegionKey` instead of staying as an authored `body` prop.

### 2026-05-19 (plans 387 and 393 - closure sync)

- Closed `docs/plans/387-deep-audit-2026-05-19-detail-field-and-field-chrome-contract-plan.md` after confirming the retained code gaps were already fixed in the live repo: `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx` preserves schema `className`, detail surfaces now read through the supported form/scope hooks instead of directly reading `FormRuntime` store state, and `packages/flux-renderers-form/src/field-utils/field-reading.tsx` makes shared `FieldFrame` chrome inputs explicit via `formFieldChromeRules`.
- Added focused proof in `packages/flux-renderers-form-advanced/src/detail-view/detail-field-basic.test.tsx` that authored `className` lands on the canonical detail-field control root, closing the last proof-only gap for Plan `387`.
- Synced `docs/architecture/field-metadata-slot-modeling.md` with the live shared field-chrome metadata contract, while `docs/architecture/renderer-runtime.md` already matched the retained detail-field control-root baseline and required no further semantic change.
- Closed `docs/plans/393-deep-audit-2026-05-19-spreadsheet-shell-styling-scope-plan.md` as an adjudicated no-code closure: the current spreadsheet canvas/styling owner docs already explicitly allow spreadsheet-owned shell and overlay selectors inside the spreadsheet canvas boundary, so retained `10-02` / `10-03` no longer describe a live unsupported contract on the current baseline.
- Independent audits `ses_1bd6a8bd8ffe95ya3f4UG3Ouvh` and `ses_1bd6a8bbdffeY3zC8KfRgvJ1g4` confirmed Plans `387` and `393` had no remaining in-scope live blocker beyond proof/bookkeeping sync.
- Repo-wide closure baseline remains green on the current tree: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).

### 2026-05-19 (plan 401 - chart title slot contract sync)

- Executed the retained `12-02` chart slot fix in `docs/plans/401-deep-audit-2026-05-19-chart-slot-authoring-contract-plan.md`: `packages/flux-renderers-data/src/data-renderer-definitions.ts` now models `title` as `value-or-region`, `packages/flux-renderers-data/src/chart-schemas.ts` accepts authored title fragments, and `packages/flux-renderers-data/src/chart-renderer.tsx` resolves `title` through the shared slot helper instead of treating it as a plain prop only.
- The chart renderer now publishes title chrome through `data-slot="chart-title"` for both plain-string and region-backed title content, and keeps accessibility fidelity by using `aria-labelledby` against that resolved title slot content.
- Added focused proof in `packages/flux-renderers-data/src/__tests__/chart-renderer.unit.test.tsx` that region-backed chart titles render, label the chart canvas, and preserve the existing plain-string title behavior.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/chart-renderer.unit.test.tsx` (`1` file / `9` tests).
- Package-local closure gates also passed: `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `pnpm --filter @nop-chaos/flux-renderers-data build`, and `pnpm --filter @nop-chaos/flux-renderers-data lint`.
- Updated `docs/components/chart/design.md` and `docs/architecture/field-metadata-slot-modeling.md` so the supported chart `title` value-or-region contract is explicit on both the component and architecture owner docs.
- Repo-wide closure gates re-passed on the same live tree after the chart slot sync: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).

### 2026-05-19 (plan 392 - spreadsheet host semantics sync)

- Executed the retained spreadsheet host-semantic fixes in `docs/plans/392-deep-audit-2026-05-19-spreadsheet-host-semantics-plan.md`: `packages/spreadsheet-renderers/src/host-action-provider.ts` now preserves `cancelled` when mapping `SpreadsheetCommandResult -> ActionResult`, and `packages/spreadsheet-renderers/src/default-page-body.tsx` now routes outside-click inline save through the same result-aware save path instead of blindly discarding bridge failures.
- Added focused proof on the correct owner surfaces: `packages/spreadsheet-renderers/src/default-page-body.test.tsx` now proves the default spreadsheet host triggers `handleEditSave` only for true outside clicks, `packages/spreadsheet-renderers/src/__tests__/schema-integration.test.tsx` keeps the default host/page integration baseline green, `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx` still proves explicit failed/cancelled edit-save UI states, and `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-sheet-commands.test.tsx` preserves the existing resolved-cancelled command vocabulary.
- Added provider-level regression proof in `packages/spreadsheet-renderers/src/__tests__/schema-integration.test.tsx` that a resolved `{ ok:false, cancelled:true }` spreadsheet host command remains cancelled after namespace invocation instead of being collapsed into a generic failure result.
- Focused verification passed: `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/default-page-body.test.tsx src/__tests__/grid-selection.test.tsx src/__tests__/schema-integration.test.tsx src/spreadsheet-interactions/use-sheet-commands.test.tsx` (`4` files / `33` tests).
- Updated `docs/components/spreadsheet-page/design.md` and `docs/architecture/report-designer/design.md` to make the host save/cancel result contract explicit for both default page-host edit saves and namespaced `spreadsheet:*` actions.
- Package-local closure gates also passed after the proof cleanup: `pnpm --filter @nop-chaos/spreadsheet-renderers typecheck`, `pnpm --filter @nop-chaos/spreadsheet-renderers build`, and `pnpm --filter @nop-chaos/spreadsheet-renderers lint`.
- Repo-wide closure gates re-passed on the same live tree after the `392` sync: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).
- Independent closure audit `ses_1bd7d478affen7WKSYMYKJPeXe` confirmed there is no remaining in-scope code/test/doc gap; the only initial blockers were stale plan/log bookkeeping, which are now synced, so Plan `392` moves to `completed`.

### 2026-05-19 (plan 386 - validation owner lifecycle contract sync)

- Closed the remaining retained `08-04` ambiguity in `docs/plans/386-deep-audit-2026-05-19-validation-owner-and-registration-lifecycle-plan.md` by locking the live `applyChangesAndRevalidate(...)` transitional lifecycle contract into focused proof: owner-local writes may commit during `bootstrapping` / `refreshing`, but validation publication waits for the owner to return to `active` and then re-reads the active compiled model generation.
- Added focused lifecycle proof in `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts` for both cases the audit called out: bootstrapping owners do not publish validation before activation, and `applyChangesAndRevalidate(...)` validates against the activated model after a lifecycle transition instead of publishing against a stale generation.
- Reconfirmed the rest of Plan `386` on the same focused pass: `packages/flux-runtime/src/__tests__/owner-registration-contracts.test.ts`, `form-runtime-owner-lifecycle.test.ts`, `owner-submit-contracts.test.ts`, and `form-runtime-deferred.test.ts` stayed green, preserving the already-landed `08-01` / `08-02` / `08-03` / `08-05` fixes.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/owner-validation-lifecycle-contracts.test.ts src/__tests__/owner-registration-contracts.test.ts src/__tests__/form-runtime-owner-lifecycle.test.ts src/__tests__/owner-submit-contracts.test.ts src/__tests__/form-runtime-deferred.test.ts` (`5` files / `34` tests).
- Updated `docs/architecture/form-validation.md` and `docs/architecture/flux-runtime-module-boundaries.md` so the live owner contract now explicitly states that transitional lifecycle writes are allowed but validation publication is deferred until `active`.
- Reused the current live workspace green baseline for closure: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks) had already re-passed on the same tree after the adjacent `382` closure slice, and package-local `pnpm --filter @nop-chaos/flux-runtime typecheck`, `build`, and `lint` also passed after the `08-04` proof landed.
- Independent closure audit `ses_1bd9a8ecaffe4EkEyNbzkS8XuJ` returned `Verdict: acceptable` with `Findings: none`, so Plan `386` now moves to `completed`.

### 2026-05-19 (plan 382 - table/crud owner-state contract sync)

- Executed the retained `04-01` owner-state fix in `docs/plans/382-deep-audit-2026-05-19-table-and-crud-owner-state-and-event-contract-plan.md`: `packages/flux-renderers-data/src/table-renderer/use-table-visible-columns.ts` and `packages/flux-renderers-data/src/crud-renderer-ownership.ts` now preserve explicit scope-owned empty arrays for `columnSettings.toggledColumnsStatePath` / `orderedColumnsStatePath` instead of falling back to default columns.
- Added focused regression proof for the supported empty-array contract in `packages/flux-renderers-data/src/__tests__/data-table-columns.test.tsx` and `packages/flux-renderers-data/src/__tests__/crud-binding-and-status.test.tsx`; both now prove that an explicit `[]` owner value means “no visible columns”, not “missing owner state”.
- Reconfirmed the in-scope `09-02` / `09-04` event contract on the same focused pass: `packages/flux-renderers-data/src/__tests__/table-internal-components.test.tsx` and `packages/flux-renderers-data/src/__tests__/crud-query-and-pagination.test.tsx` stayed green, matching the already-landed semantic payload baseline for `table:page-change` and CRUD query submit/reset events.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-table-columns.test.tsx src/__tests__/crud-binding-and-status.test.tsx src/__tests__/crud-query-and-pagination.test.tsx src/__tests__/table-internal-components.test.tsx` (`4` files / `35` tests).
- Updated `docs/components/table/design.md` and `docs/components/crud/design.md` to make the empty-array owner-state semantics explicit. `docs/architecture/renderer-runtime.md` required no change because its existing event passthrough contract already matches the live table/CRUD semantic payload shape.
- Repo-wide closure gates re-passed on the same live tree: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).
- Independent closure audit `ses_1bd9ed593ffeVpkho4lb4wPR6p` returned `Verdict: acceptable` with `Findings: none`, so Plan `382` now moves to `completed`.

### 2026-05-19 (restored full-green baseline and stale partial closure sync)

- Repaired the transient workspace blockers that had been preventing honest closure sync on older `371` successor plans:
  - `packages/flux-renderers-data/src/chart-renderer.tsx` no longer closes over an unstable chart-handle callback, restoring `build`/`lint` for `@nop-chaos/flux-renderers-data`.
  - `packages/flux-react/src/node-renderer.tsx` was normalized from the parser-hostile `memo(function ...` export shape to a named implementation plus `memo(...)`, restoring `@nop-chaos/flow-designer-renderers` test parsing.
- Continued through the live report field-panel surface owned by Plan `391` to restore the remaining repo-wide test gate:
  - aligned `packages/report-designer-renderers/src/field-panel-renderer.test.tsx` to the canonical `field.type = 'field'` payload already used by the live field-panel contract,
  - hardened `packages/report-designer-renderers/src/report-field-panel.tsx` so `readReportFieldDragPayload()` safely falls back when `drop` events do not carry `dataTransfer`,
  - normalized the stale left-panel contract assertion in `packages/report-designer-renderers/src/page-renderer.test.tsx` to plain DOM disabled checks.
- Workspace verification is green again after those repairs: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).
- With the full-green baseline restored, synced the following stale partial plans to honest `completed` status because their in-scope work and closure audits were already done and only repo-wide gates/bookkeeping were stale:
  - `docs/plans/374-deep-audit-2026-05-19-variant-field-owner-boundary-plan.md`
  - `docs/plans/375-deep-audit-2026-05-19-spreadsheet-grid-file-boundary-plan.md`
  - `docs/plans/377-deep-audit-2026-05-19-schema-renderer-suite-decomposition-plan.md`
  - `docs/plans/378-deep-audit-2026-05-19-import-stack-suite-decomposition-plan.md`
  - `docs/plans/379-deep-audit-2026-05-19-action-control-flow-suite-decomposition-plan.md`
  - `docs/plans/380-deep-audit-2026-05-19-input-renderer-decomposition-plan.md`
- Plan `376` still remains honestly `partially completed`, even on the green baseline, because its own closure gates explicitly require `pnpm check:oversized-code-files` and that script still fails on unrelated `scripts/audit/rules.mjs` (`734` lines). The spreadsheet retained findings themselves remain fixed.
- After this closure sync, the remaining genuinely open `371` successor queue is now led by `376` plus the still-planned owners `382`, `383`, `386`, `387`, `388`, `389`, `391`, `392`, `393`, `395`, `396`, `399`, `401`, `403`, and `408`.

### 2026-05-19 (plan 378 - import-stack suite decomposition execution)

- Executed `docs/plans/378-deep-audit-2026-05-19-import-stack-suite-decomposition-plan.md` for retained findings `02-06` and `14-04` by splitting `packages/flux-runtime/src/__tests__/import-stack.test.ts` into narrower owner-shaped suites: the main import-stack lifecycle surface remains in `import-stack.test.ts` (`419` lines), `installPrepared` moved to `import-stack-install-prepared.test.ts` (`214` lines), and rollback-specific proof remains in `import-stack-rollback.test.ts` (`92` lines).
- Kept helper ownership narrow instead of adding another abstraction layer: `packages/flux-runtime/src/__tests__/import-stack-test-support.ts` remains the shared support surface and is now reused across all three suites, satisfying the retained duplicated-helper finding without widening runtime semantics.
- Focused proof is green: `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/import-stack.test.ts src/__tests__/import-stack-install-prepared.test.ts src/__tests__/import-stack-rollback.test.ts` passed (`3` files / `37` tests). Package-local `pnpm --filter @nop-chaos/flux-runtime typecheck`, `build`, and `lint` also passed.
- Hard-gate verification is green for this surface: `pnpm exec node scripts/check-oversized-code-files.mjs` no longer reports any import-stack suite offender. The remaining `700+` files are unrelated: `packages/flow-designer-renderers/src/designer-page-shell.test.tsx` and `scripts/audit/rules.mjs`.
- Independent closure audit `ses_1beedbcceffeL502P1qtU4bOQv` confirmed no remaining in-scope semantic gap for `02-06` / `14-04`; only stale plan bookkeeping prevented honest closure sync.
- Full workspace verification is currently blocked outside Plan `378`: `pnpm typecheck`, `pnpm build`, and `pnpm lint` fail in `packages/flux-renderers-data/src/chart-renderer.tsx`, while `pnpm test` currently fails because `packages/flow-designer-renderers` tests hit a parse error in `packages/flux-react/src/node-renderer.tsx`. Plan `378` therefore moves to `partially completed`, not `completed`.
- With Plan `378` now executed, the `371` owner-routing queue still cannot close. The genuinely unexecuted successor set is now at least `382`, `383`, `386`, `387`, `388`, `389`, `391`, `392`, `393`, `395`, `396`, `399`, `401`, `403`, and `408`, while older partial plans still need honest closure sync once workspace verification is trustworthy again.

### 2026-05-19 (plans 409 and 410 - closure sync and flow-designer summary contract fix)

- Closed `docs/plans/409-open-ended-adversarial-review-2026-05-19-report-and-spreadsheet-host-contract-plan.md` after independent audit `ses_1bf184934ffeei8wx3yTJyTdPP` confirmed the live report/spreadsheet host-contract fixes were already complete and only stale plan bookkeeping remained.
- Closed `docs/plans/410-open-ended-adversarial-review-2026-05-19-flow-designer-host-contract-plan.md` after fixing the remaining live `R03-01` drift: `packages/flow-designer-core/src/types.ts`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/flow-designer-renderers/src/designer-page-body.tsx`, and `packages/flow-designer-renderers/src/renderer-definitions.ts` now share one supported selection vocabulary where branch selection stays `branch` across both `$designer` and `statusPath`, and the stale static `$designer.busy` contract is removed.
- Added focused flow-designer regression proof for the closure fix: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-page-shell.test.tsx src/designer-provider-and-manifest.test.tsx src/designer-controls.test.tsx --reporter=verbose` passed (`3` files / `37` tests). The updated proof now checks the shared selection-kind helper and static host-contract metadata instead of relying on brittle UI-only branch-selection setup.
- Repo-wide verification after the flow-designer fix is green: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).
- With Plans `409` and `410` now completed, the `371` owner-routing queue still cannot close yet because several successor plans remain genuinely unexecuted (`378`, `382`, `383`, `386`, `387`, `388`, `389`, `391`, `392`, `393`, `395`, `396`, `399`, `401`, `403`, `408`).

### 2026-05-19 (plan 400 - shared E2E WebSocket suppression closure)

- Closed the widened `R01-04` scope in `docs/plans/400-deep-audit-2026-05-19-test-harness-reliability-plan.md`: `tests/e2e/fixtures.ts` no longer treats `WebSocket connection` as shared known noise, so supported Playwright page-entry gating now fails on real transport/runtime connection errors instead of silently filtering them.
- Added regression proof in `tests/e2e/fixtures-hard-gate.spec.ts` that injects a `WebSocket connection` console error and verifies `assertTrackedPageErrors(page)` rejects; the test uses `allowConsoleErrors(1)` only to keep fixture teardown honest after the intentionally injected failure.
- Focused verification passed: `pnpm exec playwright test "tests/e2e/fixtures-hard-gate.spec.ts" --workers=1` (`3` passed) and `pnpm exec playwright test "tests/e2e/flux-basic-row-inspect.spec.ts" --workers=1` (`1` passed).
- Added `docs/bugs/62-e2e-shared-websocket-error-suppression-fix.md` to record the cross-cutting hard-gate regression and why future specs must use explicit per-test allowances instead of reintroducing a fixture-wide suppression.
- Repo-wide verification after the fix is green: `pnpm typecheck` (`49` successful tasks), `pnpm build` (`26` successful tasks), `pnpm lint` (`26` successful tasks), and `pnpm test` (`49` successful tasks).
- Independent closure audit `ses_1bf1d762fffedGQRE4o3TxVrTW` returned acceptable for closure with no remaining in-scope semantic gaps, so Plan `400` now moves to `completed`.

### 2026-05-19 (plan 414 - final closure audit sync)

- Recorded the final independent closure audit for `docs/plans/414-playground-performance-diagnostics-e2e-plan.md`: fresh audit `ses_1bf2e29daffe7YiLCLDd8NY1ww` re-checked the live repo after workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed and returned acceptable for closure with no remaining in-scope semantic gaps.
- Synced the plan closure text so Phase 6, closure gates, and closure evidence all agree on the live completed state; the previous intermediate note that closure was only "ready to re-audit" is now removed.
- Also refreshed Plan `411` closure evidence to cite the same final repo-wide audit pass in addition to its earlier focused closure audit, keeping the completed plan text aligned with the live green workspace baseline.
- Next unresolved owner-routing target remains Plan `400` / retained finding `R01-04`, where `tests/e2e/fixtures.ts` still needs an honest replacement for the broad `WebSocket connection` suppression before the wider `371` routing queue can close.

### 2026-05-19 (plans 407, 411, and 412 - repo-wide closure gates)

- Closed the remaining closure gates for `docs/plans/407-open-ended-adversarial-review-2026-05-19-runtime-validation-and-packaging-fidelity-plan.md`, `docs/plans/411-open-ended-adversarial-review-2026-05-19-word-editor-truth-surface-and-host-contract-plan.md`, and `docs/plans/412-open-ended-adversarial-review-2026-05-19-surface-containment-truthfulness-plan.md` after confirming the live workspace is green again.
- Repo-wide verification now passes end to end: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all completed successfully in the live repo. The current unit-test workspace summary from `pnpm test` is `49` successful Turbo tasks across the workspace, including the previously suspected packages `@nop-chaos/flow-designer-renderers` (`19` files / `129` tests across its three Vitest batches), `@nop-chaos/flux-renderers-data` (`32` files / `301` tests), `@nop-chaos/flux-renderers-form-advanced` (`68` files / `619` tests), `@nop-chaos/flux-playground` (`18` files / `76` tests), `@nop-chaos/nop-debugger` (`17` files / `137` tests), and `@nop-chaos/flux` (`1` file / `6` tests).
- Plan `407` now records independent closure audit `ses_1bf2da6e3ffe1YAgiwrpi5QIO3` with `Verdict: acceptable` and `Findings: none`; the audit explicitly confirmed that recording repo-wide closure gates is justified and that the plan can honestly move to `completed`.
- Plan `412` now records the previously completed independent closure audit `ses_1bf53e4ebffeD4B124gU06sly4` with `Verdict: acceptable` and `Findings: none`; the only remaining requirement had been repo-wide gate confirmation, which is now satisfied.
- Plan `411` already had clean independent closure audit `ses_1bf7c4234ffeJK8UKOM5EDRAfp`; with repo-wide `typecheck` / `build` / `lint` / `test` now green, Plans `407`, `411`, and `412` all move from `partially completed` to `completed`.

### 2026-05-19 (plans 407 and 412 - validation-owner stale-model fix and honest drawer containment)

- Closed the remaining in-scope code work for `docs/plans/407-open-ended-adversarial-review-2026-05-19-runtime-validation-and-packaging-fidelity-plan.md`: `packages/flux-react/src/schema-renderer.tsx` now refreshes the page-root validation owner with `undefined` when schema replacement removes the root validation plan, and the validation runtime refresh path now accepts that explicit model clear instead of retaining stale compiled state.
- Focused proof for `407/R01-01` now lives in `packages/flux-react/src/__tests__/schema-renderer-validation-owner-boundary.test.tsx`, which verifies that a page-root owner with an attached validation model drops back to `validation === undefined` after schema replacement removes the root validation plan. Related lifecycle proofs in `packages/flux-runtime/src/__tests__/form-runtime-owner-lifecycle.test.ts` and `src/__tests__/owner-validation-lifecycle-contracts.test.ts` stayed green.
- Executed `docs/plans/412-open-ended-adversarial-review-2026-05-19-surface-containment-truthfulness-plan.md`: `packages/ui/src/components/ui/drawer.tsx` now aligns container-targeted drawer geometry with the public API by switching overlay, viewport, and popup from viewport-fixed geometry to container-contained absolute geometry whenever `containerElement` is provided.
- Added focused containment proof in `packages/ui/src/components/ui/drawer.test.tsx` that container-targeted drawers render their overlay/viewport/popup inside the supplied container with contained geometry classes instead of the previous viewport-fixed baseline.
- Updated `docs/architecture/surface-owner.md` to make the live containment rule explicit: `containerElement` support is only honest when portal mount target and geometry semantics move together.
- Verification passed for these slices: `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/schema-renderer-validation-owner-boundary.test.tsx src/__tests__/schema-renderer.test.tsx`, `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/form-runtime-owner-lifecycle.test.ts src/__tests__/owner-validation-lifecycle-contracts.test.ts`, `pnpm --filter @nop-chaos/ui exec vitest run src/components/ui/drawer.test.tsx`, plus affected-package `typecheck` for `@nop-chaos/flux-core`, `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, and `@nop-chaos/ui`, and package `build` for `@nop-chaos/ui`.

### 2026-05-19 (plans 411 and 414 - extraction regression repair)

- Repaired two follow-up regressions introduced while shrinking oversized files without changing the supported owner contracts.
- Plan `414`: `apps/playground/src/pages/performance-table-page.tsx` now re-imports the runtime helpers that were extracted into `apps/playground/src/pages/performance-table/runtime.tsx` (`createSessionId`, `getVisibleRowProfile`, `getActiveDiagnosticsPageRuntime`, `readDebuggerSessionSummary`), restoring honest diagnostics compilation after the split.
- Plan `411`: `packages/word-editor-renderers/src/__tests__/word-editor-page-actions.test.tsx` now imports `word-editor-page-actions.test-support.tsx` before `WordEditorPage` / provider modules so the shared `vi.mock(...)` setup is installed before those modules load; this restores the dataset dialog/save-path test harness after the extraction.
- Plan `411` also closed the last live `R03-04` gap: `packages/word-editor-renderers/src/hooks/use-word-editor-actions.ts` no longer persists datasets directly from the dataset dialog path. Dataset edits now stay in memory, mark the editor dirty, and flush only through the explicit save path proven in `src/__tests__/word-editor-page-actions.test.tsx`.
- Refreshed focused verification passed: `pnpm --filter @nop-chaos/flux-playground typecheck`, `pnpm --filter @nop-chaos/flux-playground build`, `pnpm --filter @nop-chaos/flux-playground lint`, `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-page-actions.test.tsx src/__tests__/use-word-editor-save.test.tsx src/__tests__/word-editor-action-provider.test.ts`, `pnpm --filter @nop-chaos/word-editor-renderers typecheck`, `pnpm --filter @nop-chaos/word-editor-renderers build`, and `pnpm --filter @nop-chaos/word-editor-renderers lint`.
- Independent closure audit `ses_1bf7c4234ffeJK8UKOM5EDRAfp` returned `Verdict: acceptable` and `Findings: none`; the honest remaining gates for Plan `411` are now only repo-wide `pnpm typecheck` / `pnpm build` / `pnpm lint` / `pnpm test` confirmation.
- Owner-doc adjudication: no new architecture-doc update required; the supported plan-owned contracts for `docs/plans/411-open-ended-adversarial-review-2026-05-19-word-editor-truth-surface-and-host-contract-plan.md` and `docs/plans/414-playground-performance-diagnostics-e2e-plan.md` did not change.

### 2026-05-19 (plan 411 phase-2 closure sync and flux-react host-scope regression fix)

- Completed the remaining in-scope code/doc work for `docs/plans/411-open-ended-adversarial-review-2026-05-19-word-editor-truth-surface-and-host-contract-plan.md`; the plan now reflects all three phases as completed, while overall `Plan Status` stays `partially completed` until independent closure audit and repo-wide verification finish.
- Synced the final supported word-editor baseline into `docs/architecture/word-editor/design.md` and `docs/components/word-editor-page/design.md`: explicit save remains `SavedDocumentData`-first, `statusPath.busy` is honest, template insertion preserves canonical tag kind (including supported `c:out` `tag-selfclose` insertion), chart/code insertion is validator-gated end to end, and watermark is now documented as out of the supported authoring/persisted-truth surface.
- `packages/flux-react/src/workbench/hooks.ts` now keeps a stable host projection scope across ordinary rerenders after the unique `scope.id` change, so existing `scope.store` subscriptions continue to observe projection replacement events instead of losing nested changed-path notifications.
- Focused verification passed for the regression fix and dialog host baseline: `pnpm --filter @nop-chaos/flux-react exec vitest run src/workbench/hooks.test.tsx src/__tests__/dialog-host.test.tsx`.

### 2026-05-19 (plan 414 - table/array focused locality proof follow-up)

- Continued `docs/plans/414-playground-performance-diagnostics-e2e-plan.md` after the first diagnostics slice and closed the focused locality blocker where table single-row sessions still woke sibling probes.
- Root cause: the table diagnostic was mutating `perfRows` through host React state only, which caused `SchemaRenderer` to refresh page data through a broad root replace path; that over-represented invalidation and made the locality gate dishonest even when the intended target row update was narrow.
- Fix: `apps/playground/src/pages/performance-table/runtime.tsx` now tracks the active diagnostics page runtime alongside the diagnostics form runtime, and `apps/playground/src/pages/performance-table-page.tsx` now drives table single-row diagnostics through precise `page.scope.update('perfRows.<index>.*', ...)` path updates before syncing host state. The array diagnostic kept the form-runtime path and now also waits for the target probe tracked value before sampling counters.
- Added supported Playwright locality gates in `tests/e2e/performance-table.spec.ts` for both `table-single-row-locality` and `array-item-locality`, using fixture-managed `page`, `assertTrackedPageErrors(page)`, diagnostics-mode entry via `/?diagnostics=1#/performance-table`, and fresh-session polling that checks previous session id mismatch plus scenario/timestamp window.
- Added focused route/app proof in `apps/playground/src/app-diagnostics-route.test.tsx` to pin the supported diagnostics URL contract: ordinary `/#/performance-table` stays lightweight, while `/?diagnostics=1#/performance-table` enters the same page with `diagnosticsEnabled === true` and keeps debugger wiring attached.
- Focused proof now passes honestly: `pnpm --filter @nop-chaos/flux-playground exec vitest run src/pages/performance-table-page.test.tsx --reporter=verbose --testNamePattern "records a completed table single-row locality session|records a completed array item locality session with index-addressed validation path"` and `pnpm --filter @nop-chaos/flux-playground test -- --run src/pages/performance-table-page.test.tsx src/pages/performance-table/diagnostics.test.ts src/pages/performance-table/measurement.test.ts` (`17` files / `71` tests).
- Consolidated focused verification also passed with exact file targeting: `pnpm --filter @nop-chaos/flux-playground exec vitest run src/app-diagnostics-route.test.tsx src/pages/performance-table-page.test.tsx src/pages/performance-table/diagnostics.test.ts src/pages/performance-table/measurement.test.ts --reporter=verbose` (`4` files / `13` tests).
- Supported E2E verification passed: `pnpm test:e2e -- performance-table.spec.ts --workers=1` (`6` tests). The existing record-value spec in that file was also tightened to match the live select text casing instead of relying on uppercase presentation.
- Strengthened supported E2E proof now also cross-checks the public debugger automation surface instead of trusting only page-local summaries: the locality specs require `window.__NOP_DEBUGGER_API__.inspectByElement()` to resolve the target probe node and require session-scoped `queryEvents({ kind: 'error', interactionId, sinceTimestamp })` / `getRecentFailures({ sinceTimestamp })` to stay zero.
- Verification after this slice: workspace `pnpm build` passed and workspace `pnpm lint` passed. Workspace `pnpm typecheck` is still blocked by unrelated existing `packages/word-editor-renderers/src/__tests__/word-editor-page-actions.test.tsx` missing-name errors (`datasetStore`, `resetFluxI18n`, `initFluxI18n`, `resetMockStores`), not by the performance diagnostics changes.
- Owner-doc adjudication for this slice: updated `docs/architecture/performance-diagnostics-and-e2e-design.md` to match the live diagnostics baseline (debugger wiring, `/?diagnostics=1#/performance-table`, supported table/array locality gates). `docs/architecture/playground-experience.md`, `docs/architecture/debugger-runtime.md`, and `docs/architecture/array-field.md` required no further change for this slice because the implemented work stayed within their existing documented contracts.

### 2026-05-19 (plan 414 closure)

- Closed `docs/plans/414-playground-performance-diagnostics-e2e-plan.md` after the final repo-wide verification and checklist sync.
- Final repo-wide verification is fully green on the live repo: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed after clearing two unrelated stale test-contract failures surfaced during closure verification.
- The unrelated closure-verification follow-ups were test-only syncs, not new Plan 414 feature work: `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx` now matches the current `$designer.doc.nodes` / `edges` projection contract and uses a valid node fixture with `position`, and `packages/flux-renderers-data/src/__tests__/table-internal-components.test.tsx` now asserts pagination callback behavior by requested page/pageSize plus forwarded UI event instead of a brittle exact call shape.
- Reconfirmed the plan-owned diagnostics proof on the same live baseline: supported Playwright locality gates remain in `tests/e2e/performance-table.spec.ts`, focused diagnostics tests remain green in `apps/playground/src/app-diagnostics-route.test.tsx`, `src/pages/performance-table-page.test.tsx`, `src/pages/performance-table/diagnostics.test.ts`, and `src/pages/performance-table/measurement.test.ts`, and ordinary `/#/performance-table` still stays lightweight while `/?diagnostics=1#/performance-table` enables the page-local diagnostics/debugger path.
- Independent closure audit result for Plan 414: subagent `ses_1bf2dac20ffe13KvKXxl3SLoss` initially returned `Verdict: not acceptable` only because the plan text still had unchecked closure items and no recorded audit evidence. Final independent closure audit `ses_1bf24585effe1YmvtK3kKE6bcx` then returned `Verdict: acceptable`, `Findings: none`, and `Plan 414 can be marked completed now: Yes` after the final plan/log synchronization.
- Full-green baseline for this closure pass: workspace `pnpm typecheck` (`49` tasks), workspace `pnpm build` (`26` tasks), workspace `pnpm lint` (`26` tasks), workspace `pnpm test` (`49` tasks), supported Playwright `pnpm test:e2e -- performance-table.spec.ts --workers=1` (`6` tests), and focused playground diagnostics Vitest coverage (`4` files / `13` tests) all passed.

### 2026-05-19 (plans 377, 379, 380 - oversized/test-surface and input renderer execution)

- Executed the in-scope work for `docs/plans/377-deep-audit-2026-05-19-schema-renderer-suite-decomposition-plan.md`, `docs/plans/379-deep-audit-2026-05-19-action-control-flow-suite-decomposition-plan.md`, and `docs/plans/380-deep-audit-2026-05-19-input-renderer-decomposition-plan.md`; all three now move to `partially completed` because the local owner work landed but repo-wide closure gates remain blocked by unrelated failures.
- Plan `377`: split the mixed-owner `SchemaRenderer` test surface so `packages/flux-react/src/__tests__/schema-renderer.test.tsx` now carries the root callback/import-preparation coverage and `packages/flux-react/src/__tests__/schema-renderer-registry-debug.test.tsx` carries the registry/debug/surface-runtime seam coverage. Focused verification passed: `pnpm exec vitest run src/__tests__/schema-renderer.test.tsx src/__tests__/schema-renderer-registry-debug.test.tsx` in `packages/flux-react` (`2` files / `19` tests), plus `pnpm --filter @nop-chaos/flux-react typecheck`, `build`, and `lint`.
- Plan `379`: replaced the oversized `packages/flux-action-core/src/__tests__/contract-control-flow-edge-cases.test.ts` with narrower suites `contract-control-flow-branches.test.ts`, `contract-control-flow-parallel.test.ts`, and `contract-control-flow-timeout-cancel.test.ts`, and extracted shared fixtures into `packages/flux-action-core/src/__tests__/control-flow-test-fixtures.ts`. Focused verification passed: `pnpm exec vitest run src/__tests__/contract-control-flow-branches.test.ts src/__tests__/contract-control-flow-parallel.test.ts src/__tests__/contract-control-flow-timeout-cancel.test.ts` in `packages/flux-action-core` (`3` files / `19` tests), plus package `typecheck`, `build`, and `lint`.
- Plan `380`: decomposed `packages/flux-renderers-form/src/renderers/input.tsx` into a thinner root file plus `packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx` and `packages/flux-renderers-form/src/renderers/input-number-renderer.tsx`, and restored input-number stepper keyboard focusability by removing the negative tab index from the stepper buttons. Focused verification passed: `pnpm exec vitest run src/__tests__/input-number.test.tsx src/__tests__/input-classname-contract.test.tsx` in `packages/flux-renderers-form` (`2` files / `22` tests), plus package `typecheck`, `build`, and `lint`.
- `pnpm check:oversized-code-files` no longer reports the old `377` / `379` hard-gate offenders. The remaining hard-gate files are unrelated to these plans: `apps/playground/src/pages/performance-table-page.tsx` and `packages/word-editor-renderers/src/__tests__/word-editor-page-actions.test.tsx`.
- Independent closure audit task `ses_1bfccaf68ffe9ARbI0y9NFyyzn` confirmed the in-scope fixes and focused proof for `377`, `379`, and `380`, and recommended keeping all three at `partially completed` until unrelated workspace `typecheck` / `build` / `lint` / `test` failures are cleared.

### 2026-05-19 (plan 414 - playground performance diagnostics execution slice)

- Started executing `docs/plans/414-playground-performance-diagnostics-e2e-plan.md`; plan status is now `in progress`, not `planned`.
- Landed diagnostics-mode routing and page wiring in `apps/playground/src/App.tsx`, `apps/playground/src/route-model.ts`, and `apps/playground/src/pages/performance-table-page.tsx`: `PerformanceTablePage` now accepts `debuggerController` and `diagnosticsEnabled`, and `/?diagnostics=1#/performance-table` is the active URL shape.
- Added page-local diagnostics scaffolding under `apps/playground/src/pages/performance-table/diagnostics.ts`, exported it through `apps/playground/src/pages/performance-table/index.ts`, and installed `window.__NOP_PERF_DIAGNOSTICS__` only when diagnostics mode is enabled.
- Added diagnostics-only render probes and schema branches in `apps/playground/src/pages/performance-table/schema.ts`; the page now exposes `Run Single Row Locality Diagnostic` and `Run Array Item Locality Diagnostic` controls behind diagnostics mode.
- Added focused tests in `apps/playground/src/pages/performance-table/diagnostics.test.ts` and expanded `apps/playground/src/pages/performance-table-page.test.tsx`; focused playground diagnostics tests passed via `pnpm --filter @nop-chaos/flux-playground test -- --run src/pages/performance-table-page.test.tsx src/pages/performance-table/diagnostics.test.ts`.
- Verification is still blocked for plan closure: supported Playwright locality specs are not implemented yet, table/array sibling-locality proof is not complete yet, and workspace/playground `typecheck` is currently blocked by pre-existing unrelated `packages/flux-renderers-data` build/type errors (`table-renderer.tsx`, `use-table-pagination.ts`).

### 2026-05-19 (plan 411 - word-editor save/status/provider slice)

- Executed Phase 1 and Phase 3 of `docs/plans/411-open-ended-adversarial-review-2026-05-19-word-editor-truth-surface-and-host-contract-plan.md`; the plan remains `in progress` because Phase 2 (`R20-01`, `R21-01`, `R22-01`, `R23-01`) is still open.
- `packages/word-editor-renderers/src/word-editor-action-provider.ts` now passes the full `SavedDocumentData` envelope into the host `saveEvent(saved, ctx)` callback, delays `saveDatasets(...)` until the save succeeds, and enforces `insertChart` / `insertCode` payloads with the same core validators the manifest contract already implies.
- `packages/word-editor-renderers/src/hooks/use-word-editor-save.ts`, `packages/word-editor-renderers/src/hooks/use-word-editor-state.ts`, and `packages/word-editor-renderers/src/word-editor-page.tsx` now publish honest explicit-save busy state through `statusPath`, while the page save button stays disabled during the in-flight save.
- Focused proof landed in `packages/word-editor-renderers/src/__tests__/word-editor-action-provider.test.ts`, `src/__tests__/use-word-editor-save.test.tsx`, and `src/__tests__/word-editor-page-actions.test.tsx`; existing projection coverage in `src/__tests__/word-editor-page-host-scope-projections.test.tsx` stayed green.
- Updated owner docs to match the live baseline: `docs/architecture/word-editor/design.md` and `docs/components/word-editor-page/design.md` now explicitly describe saved-envelope delivery, `statusPath.busy`, save-commit ordering, and manifest/provider parity for chart/code insertion.
- Verification for this slice passed: `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-action-provider.test.ts src/__tests__/use-word-editor-save.test.tsx src/__tests__/word-editor-page-actions.test.tsx src/__tests__/word-editor-page-host-scope-projections.test.tsx`, `pnpm --filter @nop-chaos/word-editor-renderers typecheck`, `pnpm --filter @nop-chaos/word-editor-renderers build`, and `pnpm --filter @nop-chaos/word-editor-renderers lint`.

### 2026-05-19 (plan 413 — TaskFlow E2E tests)

- Created `tests/e2e/taskflow-designer-ui.spec.ts` with 6 E2E tests covering:
  - TaskFlow (Graph) tab: 7 nodes + toolbar buttons (Export, Import, Save, Back) + edge rendering + palette
  - TaskFlow (Tree) tab: tree document rendering + toolbar
  - Tab switching: cycling between TaskFlow and existing tabs produces zero console/page errors
  - Toolbar actions: Export and Save buttons clickable without error
- Fixed `${data.valid}` expression evaluation error in tree schema condition (changed to static `"true"`)
- All 6 tests pass: `pnpm test:e2e -- taskflow-designer-ui.spec.ts --workers=1` — 6 passed (38s)
- Separated TaskFlow designer into its own entry point:
  - Created `apps/playground/src/pages/taskflow-designer-page.tsx` — new standalone page with TaskFlow (Graph) and TaskFlow (Tree) tabs
  - Added `taskflow-designer` to `DOMAIN_RENDERER_ROUTES`, `NavigationTarget`, `NAV_CARDS`, and `App.tsx` router
  - "TaskFlow" card now navigates directly to `#/taskflow-designer` instead of being mixed in Flow Designer tabs
- Improved TaskFlow graph schema visuals:
  - Added `classAliases` with glass card styling (`nop-glass-card`), gradient icon backgrounds, colored badges
  - Enriched all 10 node bodies with header (icon + info) and footer (badge + metadata)
  - Added edge body labels for visual edge kind display
  - Updated E2E tests to navigate via new TaskFlow card
  - All 8 E2E tests pass: `pnpm test:e2e -- taskflow-designer-ui.spec.ts --workers=1` — 8 passed (32s)

### 2026-05-19 (plan 375 - spreadsheet-grid surface execution)

- Executed the in-scope code/doc work for `docs/plans/375-deep-audit-2026-05-19-spreadsheet-grid-file-boundary-plan.md`; the plan remains `partially completed` because unrelated workspace verification blockers are still present.
- Split `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx` so the root interaction owner now delegates table/cell rendering into `packages/spreadsheet-renderers/src/spreadsheet-grid/table-shell.tsx`; the root file now stays under the oversized hard gate at `402` lines while preserving the existing grid behaviors.
- Closed the fill-handle semantics finding by keeping the handle as a presentational pointer-only affordance (`aria-hidden`, no advertised button/focus semantics) and documenting the supported non-pointer path as selection plus shared context-menu actions in `docs/components/spreadsheet-page/design.md`.
- Focused proof landed in `packages/spreadsheet-renderers/src/__tests__/context-menu-fill-and-range.test.tsx`, including a regression that the fill handle does not surface faux button semantics.
- Verification results: `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/context-menu-fill-and-range.test.tsx src/__tests__/grid-selection.test.tsx` passed (`2` files / `24` tests); `pnpm --filter @nop-chaos/spreadsheet-renderers typecheck`, `build`, and `lint` passed. `pnpm check:oversized-code-files` still fails only on unrelated hard-gate files (`packages/flux-react/src/__tests__/schema-renderer.test.tsx`, `packages/flux-action-core/src/__tests__/contract-control-flow-edge-cases.test.ts`); workspace `pnpm typecheck`/`build` are blocked by unrelated `@nop-chaos/flux-renderers-data` errors, and workspace `pnpm test` is blocked by an unrelated `packages/flux-runtime/src/__tests__/runtime-factory-utils.test.ts` failure.

### 2026-05-19 (plan 413 — TaskFlow Visual Designer playground prototype closure)

- **Closed** `docs/plans/413-taskflow-visual-designer-playground-prototype-plan.md` with `Plan Status: completed`.
- All 6 phases reached `status=completed`; remaining UI polish items deferred as follow-ups.
- Final closure verification:
  - `pnpm --filter @nop-chaos/flux-playground typecheck` passed (only pre-existing `flux-renderers-data` errors)
  - `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` passed
  - `pnpm --filter @nop-chaos/flow-designer-renderers build` passed
  - `pnpm --filter @nop-chaos/flux-playground lint` passed
  - `pnpm --filter @nop-chaos/flow-designer-renderers lint` passed

### 2026-05-19 (plan 413 — TaskFlow Visual Designer playground prototype execution)

- Executed `docs/plans/413-taskflow-visual-designer-playground-prototype-plan.md`: Phases 1-5 implemented, Phase 6 (integration verification) deferred to closure audit.
- **Phase 1** (xui:import TaskFlow 设计库): Created `apps/playground/src/taskflow-designer-lib/` with full type system (`types.ts`), graph/tree projection (`projection.ts`), sync back (`sync.ts`), lowering (`lowering.ts`), validation (`validation.ts`), and active container stack. The library exports `createNamespace()` and `createExpressionHelpers()` as required by `ImportedLibraryModule` contract. Edge semantic recovery strategy: primary from `data.taskflowEdgeKind`, fallback to `sourcePort` inference.
- **Phase 1 importLoader**: Configured inline in `flow-designer-page.tsx` with `resolveImportUrl` mapping `'taskflow-designer'` → `'taskflow-designer://'` protocol, and `importLoader.load` returning the static `taskflowDesignerLib` module.
- **Phase 2** (Workflow Graph schema): Created `apps/playground/src/schemas/taskflow-workflow-schema.json` with 10 node types (tf-start through tf-end), 4 semantic edge types (tf-next/error/wait/wait-error), `defaultEdgeType: 'tf-next'`, palette groups, toolbar with Export/Save. Pre-populated with 7 nodes and 8 edges covering all 4 edge kinds. `beforeConnect` hook injected programmatically via `injectDesignerHooks()`.
- **Phase 3** (Dingflow Tree schema): Created `apps/playground/src/schemas/taskflow-dingflow-schema.json` with `documentMode: 'tree'`, 9 tree node types, `tf-entry` synthetic root, `treeConfig` with TB layout, if/choose/parallel branches. Tree data includes `sequential → if (then/else) → invoke/script`.
- **Phase 4** (Active Container): `enter-container`/`exit-container` actions implemented in `apps/playground/src/taskflow-designer-lib/index.ts` with container stack management, flush-before-switch, and `replaceDocumentFromHost`-compatible breadcrumb tracking.
- **Phase 5** (Save/Export Pipeline + `$designer.doc` extension): Extended `packages/flow-designer-renderers/src/designer-host-projection.ts` (DESIGNER_HOST_PROJECTION_FIELDS + buildDesignerHostProjection) to expose `nodes` and `edges` summary arrays on `$designer.doc` for domain export access. `taskflow:export-json`, `taskflow:save`, `taskflow:import-json` actions implemented in the namespace provider, reading `$designer.doc.nodes/edges` from `ctx.scope`.
- **Packages changed** (only `designer-host-projection.ts` as planned): `packages/flow-designer-renderers/src/designer-host-projection.ts`.
- **Verification**: `pnpm --filter @nop-chaos/flux-playground typecheck` passed (pre-existing errors only in other packages); `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` passed; `pnpm --filter @nop-chaos/flow-designer-renderers build` passed; lint passed on playground. Pre-existing test failure in `flux-runtime` (`runtime-factory-utils.test.ts` scope id format) is unrelated.
- Plan file: `docs/plans/413-taskflow-visual-designer-playground-prototype-plan.md`.

### 2026-05-19 (plan 414 — playground performance diagnostics E2E)

- Drafted `docs/plans/414-playground-performance-diagnostics-e2e-plan.md` from `docs/architecture/performance-diagnostics-and-e2e-design.md`.
- Scope: wire `performance-table` diagnostics mode to debugger when enabled, add page-local structured diagnostics/probes, gate table single-row locality and array object-item visible locality through supported Playwright tests, and keep absolute timing out of hard E2E thresholds.
- Revised the plan through repeated independent review until convergence. Resolved blockers around required debugger wiring, visible-row target selection, renderer containment ownership, unique session freshness, inspectable probe anchors, array/table visible value snapshots, and session-scoped debugger failure/error gates.
- Final independent verdicts: plan quality `acceptable`, technical feasibility `acceptable`, and E2E/debugger truthfulness `acceptable` with no remaining findings.
- Plan status is `planned`; it is ready for execution, not yet implemented.

### 2026-05-19 (performance diagnostics design)

- Added `docs/architecture/performance-diagnostics-and-e2e-design.md` to define how the playground performance page should be interpreted, how `nop-debugger` can participate in bounded performance diagnostics, and which count-based locality metrics can support table/array single-item refresh claims.
- Key decision: E2E may gate deterministic locality counters and debugger failure cleanliness, but must not claim cross-machine timing benchmarks from Playwright wall-clock or Profiler duration thresholds.
- Updated docs routing in `docs/index.md`, `docs/architecture/README.md`, `docs/references/architecture-doc-status-matrix.md`, and related debugger/playground docs.

### 2026-05-19 (plan 413 — TaskFlow Visual Designer playground prototype)

- Drafted `docs/plans/413-taskflow-visual-designer-playground-prototype-plan.md` based on `docs/architecture/taskflow-visual-designer.md`.
- Architecture: active-container hybrid projection with workflow/dingflow profiles; domain logic injected via `xui:import` (`taskflow-designer-lib`).
- Scope: 6 phases (xui:import lib, workflow schema, dingflow schema, active container switching, save/export pipeline, playground integration). Only one `packages/` change — extend `$designer.doc` projection with nodes/edges arrays for domain export.
- Key technical decisions: edge semantics in `GraphEdge.data.taskflowEdgeKind` (not `GraphEdge.type`), `beforeConnect` hook injected programmatically in playground code, `replaceDocumentFromHost` for container switching, `importLoader` set up inline in Phase 1.
- Plan went through 2 rounds of independent sub-agent review with 3 reviewers (architecture alignment, technical feasibility, plan quality). All 11 round-1 findings resolved. Final consensus: "ready for execution".
- Plan file: `docs/plans/413-taskflow-visual-designer-playground-prototype-plan.md`.

### 2026-05-19 (closure pass for plans 372, 384, 385, 394, 397, 398, 402, 404)

- Cleared the remaining workspace gates after re-running `pnpm check:workspace-manifest-deps`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`; all now pass on the live repo.
- Follow-up test-contract fixes were required after the earlier UI contract changes surfaced stale assertions in `packages/flux-renderers-data` and `packages/flux-renderers-form-advanced`.
- `packages/flux-renderers-data`: updated quick-edit tests (`src/__tests__/data-crud-quick-edit.test.tsx`, `src/__tests__/table-quick-edit-cell.unit.test.tsx`) to target the dialog footer close action explicitly now that the standard dialog close button is also exposed with the same accessible name.
- `packages/flux-renderers-form-advanced`: updated stale text-based test expectations to match the supported accessible/UI contract across `src/composite-field/array-field.test.tsx`, `src/__tests__/form-array-validation.test.tsx`, `src/__tests__/composite-item-id.test.tsx`, `src/__tests__/composite-form-object-array.test.tsx`, `src/detail-view/detail-field-basic.test.tsx`, `src/detail-view/detail-view-basic.test.tsx`, `src/condition-builder/config-display.test.tsx`, and `src/condition-builder/value-input.test.tsx`.
- Verification proof from this closure pass: `@nop-chaos/flux-renderers-data` full suite passed (`32` files / `298` tests), `@nop-chaos/flux-renderers-form-advanced` full suite passed (`68` files / `619` tests), and the final workspace `pnpm test` passed (`49` successful tasks).
- Independent closure audit task `ses_1c0f62c9dffe4PJxn8dEuWtW0L` returned `completed` for Plans `372`, `384`, `385`, `394`, `397`, `398`, `402`, and `404`; those plan files were updated from `partially completed` to `completed` with refreshed closure evidence.
- Plan `372` closure notes were also corrected to reflect the honest live fix path: removing the undeclared cross-package test dependency by using local `flux-react` test support instead of preserving a `flux-react` <-> `flux-renderers-basic` workspace cycle.

### 2026-05-19 (plans 406-412)

- Froze the `docs/analysis/2026-05-19-open-ended-adversarial-review-01/` retained set into `docs/plans/406-open-ended-adversarial-review-2026-05-19-25-round-remediation-routing-plan.md`: `25` rounds, `39` findings, one-to-one execution-owner routing.
- Reused two same-surface active owners instead of creating dishonest duplicate umbrellas: Plan `400` now explicitly owns `R01-04` (shared E2E error-gate suppression), and Plan `391` now explicitly owns `R02-03` / `R04-02` (report field-panel contract integrity).
- Created six narrow successor execution plans from Plan `406`:
  - `407-open-ended-adversarial-review-2026-05-19-runtime-validation-and-packaging-fidelity-plan.md`
  - `408-open-ended-adversarial-review-2026-05-19-supported-e2e-truthfulness-plan.md`
  - `409-open-ended-adversarial-review-2026-05-19-report-and-spreadsheet-host-contract-plan.md`
  - `410-open-ended-adversarial-review-2026-05-19-flow-designer-host-contract-plan.md`
  - `411-open-ended-adversarial-review-2026-05-19-word-editor-truth-surface-and-host-contract-plan.md`
  - `412-open-ended-adversarial-review-2026-05-19-surface-containment-truthfulness-plan.md`
- Plan `406` went through repeated independent review/revision before convergence. The final fresh routing review returned `Verdict: acceptable` and `Findings: none` after resolving reused-owner scope drift, bucket over-bundling, and plan-status inconsistencies.
- The successor-plan bundle `407`-`412` also passed an independent structural review with `Verdict: acceptable` and `Findings: none`; current consensus is that the queue is narrow, guide-compliant, and aligned with the retained finding matrix.

### 2026-05-19 (plan 408 - supported E2E truthfulness closure)

- Completed the in-scope code/test remediation for `docs/plans/408-open-ended-adversarial-review-2026-05-19-supported-e2e-truthfulness-plan.md` across the supported Playwright surfaces for code-editor, flow-designer, report-designer, word-editor, and component-lab; final plan status remains `partially completed` until independent closure audit evidence is recorded.
- Tightened the affected supported E2E titles and end-state assertions so they now match the live product surface instead of overclaiming unsupported outcomes such as parent-scope writeback text, direct hidden-input clicks, optimistic toolbar edit persistence, or debug-only state as proof.
- Updated the touched supported specs: `tests/e2e/code-editor.spec.ts`, `tests/e2e/flow-designer-edge-creation.spec.ts`, `tests/e2e/flow-designer-ui.spec.ts`, `tests/e2e/report-designer-demo.spec.ts`, `tests/e2e/word-editor.spec.ts`, `tests/e2e/word-editor-persistence.spec.ts`, `tests/e2e/word-editor-template-expr.spec.ts`, `tests/e2e/component-lab/navigation.spec.ts`, `tests/e2e/component-lab/simple-form.spec.ts`, `tests/e2e/component-lab/complex-form.spec.ts`, `tests/e2e/component-lab/layout-content.spec.ts`, and `tests/e2e/component-lab/crud-editing-and-selection.spec.ts`.
- Validation exposed a real playground regression outside pure assertion wording: `apps/playground/src/pages/report-designer-demo.tsx` was recreating spreadsheet/report cores and bridges every render, which produced a live report-designer infinite rerender failure during the supported E2E gate. The demo now memoizes those long-lived instances and disposes the designer core on unmount.
- No owner-doc update was required for `docs/testing/e2e-standards.md`; the supported proof rule did not change, only the tests were brought back into alignment with it.
- Focused reruns passed during execution: `pnpm test:e2e -- code-editor.spec.ts flow-designer-edge-creation.spec.ts flow-designer-ui.spec.ts report-designer-demo.spec.ts word-editor-persistence.spec.ts word-editor-template-expr.spec.ts --workers=1` and `pnpm test:e2e -- component-lab/navigation.spec.ts component-lab/simple-form.spec.ts component-lab/complex-form.spec.ts component-lab/layout-content.spec.ts component-lab/crud-editing-and-selection.spec.ts --workers=1`.
- Final exact-file `408` proof passed: `pnpm exec playwright test "tests/e2e/code-editor.spec.ts" "tests/e2e/flow-designer-edge-creation.spec.ts" "tests/e2e/flow-designer-ui.spec.ts" "tests/e2e/report-designer-demo.spec.ts" "tests/e2e/word-editor.spec.ts" "tests/e2e/word-editor-persistence.spec.ts" "tests/e2e/word-editor-template-expr.spec.ts" "tests/e2e/component-lab/navigation.spec.ts" "tests/e2e/component-lab/simple-form.spec.ts" "tests/e2e/component-lab/complex-form.spec.ts" "tests/e2e/component-lab/layout-content.spec.ts" "tests/e2e/component-lab/crud-editing-and-selection.spec.ts" --workers=1` (`111` passed, `2` skipped).
- Supporting verification remained green on the live repo: workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`, and affected-package `pnpm --filter @nop-chaos/flux-playground typecheck`.
- Landed the first execution slice across Plans `407`, `409`, and `410`:
  - `packages/flux-compiler/src/schema-compiler/validation-compiler.ts` and `packages/flux-compiler/src/schema-compiler.ts` now keep validate/compile `beforeCompile` plugin semantics aligned (single supported execution path).
  - `packages/flux-runtime/src/runtime-factory.ts` now keeps `scopeKey` separate from canonical runtime-created child `scope.id`, and action-scope teardown now uses namespace unregister instead of the invalid `ActionScope.dispose()` call path.
  - `scripts/check-flux-bundle-pack.mjs` now verifies stylesheet payload from the packed tarball (`package/dist/style.css`) instead of workspace source CSS.
  - `packages/report-designer-renderers/src/page-renderer.tsx` now seeds configured inspector shells into the canonical report runtime once, then honors `snapshot.inspector.open` for right-panel visibility.
  - `packages/report-designer-renderers/src/bridge.ts` now publishes aggregated runtime `canUndo` / `canRedo` instead of spreadsheet-only values.
  - `packages/spreadsheet-renderers/src/host-action-provider.ts` now rejects payloads that do not match the published spreadsheet host args contract instead of blindly forwarding arbitrary objects.
  - `packages/flow-designer-renderers/src/designer-context.ts` stopped publishing a phantom `$designer.busy` field; `docs/architecture/flow-designer/runtime-snapshot.md` now matches the live compact host summary export.
- Focused proof added/updated in this slice:
  - `packages/flux-compiler/src/schema-compiler-shape-validation-compile.test.ts`
  - `packages/flux-runtime/src/__tests__/scope-lifecycle-leak-fix.test.ts`
  - `packages/report-designer-renderers/src/page-renderer.test.tsx`
  - `packages/report-designer-renderers/src/bridge.test.ts`
  - `packages/spreadsheet-renderers/src/__tests__/schema-integration.test.tsx`
  - `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx`
- Additional repo-health fixes were required to clear package verification blockers exposed during this slice:
  - removed stray `.ts` extension imports in `packages/flux-compiler/src/schema-compiler/{shape-validation.ts,shape-validation-deep-fields.ts}`
  - fixed `packages/spreadsheet-renderers/src/spreadsheet-grid/viewport.ts` to import `cellAddress` from `@nop-chaos/spreadsheet-core`
  - fixed missing test imports/types in `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx`
- Verification for this slice passed:
  - `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/page-renderer.test.tsx src/bridge.test.ts`
  - `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/schema-integration.test.tsx`
  - `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-provider-and-manifest.test.tsx`
  - `pnpm --filter @nop-chaos/report-designer-renderers typecheck`
  - `pnpm --filter @nop-chaos/spreadsheet-renderers typecheck`
  - `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`
  - `pnpm --filter @nop-chaos/report-designer-renderers build`
  - `pnpm --filter @nop-chaos/spreadsheet-renderers build`
  - `pnpm --filter @nop-chaos/flow-designer-renderers build`
  - `pnpm --filter @nop-chaos/report-designer-renderers lint`
  - `pnpm --filter @nop-chaos/spreadsheet-renderers lint`
  - `pnpm --filter @nop-chaos/flow-designer-renderers lint`
- Landed the second execution slice across Plans `409` and `410`:
  - `packages/report-designer-renderers/src/host-action-provider.ts` now enforces the published report-designer manifest `args` contracts instead of forwarding arbitrary objects.
  - `packages/flow-designer-renderers/src/designer-toolbar.tsx` now routes built-in toolbar buttons and switches for supported `designer:*` actions through `ActionScope.resolve(...)` / provider invocation instead of bypassing the namespace path with direct command dispatch.
  - `packages/flow-designer-renderers/src/designer-manifest.ts` now publishes `navigate-back` so runtime publication, built-in UI behavior, and manifest discovery all describe the same public action surface.
- Focused proof added/updated in this slice:
  - `packages/report-designer-renderers/src/host-action-provider.test.ts`
  - `packages/flow-designer-renderers/src/designer-controls.test.tsx`
  - `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx`
- Additional shared compile blockers surfaced during this slice and were fixed to keep package verification honest:
  - `packages/spreadsheet-renderers/src/index.ts` now re-exports `SpreadsheetGridProps` from `src/spreadsheet-grid/types.ts`
  - `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx` now imports `SpreadsheetFrozenPane` explicitly
- Verification for this slice passed:
  - `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/host-action-provider.test.ts src/page-renderer.test.tsx src/bridge.test.ts`
  - `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-controls.test.tsx src/designer-provider-and-manifest.test.tsx`
  - `pnpm --filter @nop-chaos/report-designer-renderers typecheck`
  - `pnpm --filter @nop-chaos/report-designer-renderers build`
  - `pnpm --filter @nop-chaos/report-designer-renderers lint`
  - `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`
  - `pnpm --filter @nop-chaos/flow-designer-renderers build`
  - `pnpm --filter @nop-chaos/flow-designer-renderers lint`

### 2026-05-19 (plan 405 — UX audit second-round remediation)

- Executed `docs/plans/405-ux-audit-2026-05-19-remediation-plan.md`: all 5 phases completed, 7 in-scope fixes across 7 files.
- Phase 1: Unified delete button style in `array-editor.tsx` and `array-field.tsx` — changed `variant="destructive"` to `variant="ghost"` + `hover:text-destructive`. `array-editor` uses `Trash2Icon` (inline compact layout); `array-field` uses text button (card-list layout where delete occupies its own row). Design rationale: pure icon is appropriate for inline rows but too sparse as a standalone card-bottom element.
- Phase 2: Added `role="status" aria-live="polite"` to chart loading wrapper in `chart-renderer.tsx`, matching `table-loading-overlay.tsx` pattern.
- Phase 3: Disabled `PaginationNext` on last page in `crud-renderer-toolbar.tsx` via `pointer-events-none opacity-50` + `aria-disabled`, symmetric with first-page `PaginationPrevious` disable.
- Phase 4: Added `focus-visible:ring-2 focus-visible:ring-ring` to tree-renderer treeitem div, fieldset collapsible legend, and table interactive rows (`table-body-row-rendering.tsx`).
- Phase 5: Changed `condition-item.tsx` delete button from `opacity-0` to `opacity-40` for baseline visibility while preserving hover/focus full opacity.
- Verification: `pnpm typecheck` (49/49 tasks passed), `pnpm build` (26/26 tasks passed), affected-package lint (flux-renderers-form-advanced, flux-renderers-data, flux-renderers-form all clean). Pre-existing `flux-i18n` lint error (`no-new-func`) and `flow-designer-renderers` test worker crash are unrelated.
- Deferred items per plan: InputNumber suffix/stepper layout, tree-renderer CollapsibleTrigger focus-visible, CRUD full pagination.

### 2026-05-19 (plans 398, 400, 402)

- Executed `docs/plans/398-deep-audit-2026-05-19-naming-alignment-plan.md`, `docs/plans/400-deep-audit-2026-05-19-test-harness-reliability-plan.md`, and `docs/plans/402-deep-audit-2026-05-19-cross-package-i18n-alignment-plan.md` without touching unrelated dirty files.
- Plan 398: replaced non-canonical `sourceType` test samples with canonical source schema fields in `packages/flux-react/src/__tests__/node-source-prop-controller.test.ts`, `packages/flux-react/src/__tests__/use-node-source-props.test.tsx`, and `packages/flux-react/src/__tests__/use-source-value.test.tsx`.
- Plan 400: added failure-safe cleanup in `packages/word-editor-renderers/src/__tests__/insert-controls.test.tsx`, `packages/word-editor-renderers/src/__tests__/word-editor-page-actions.test.tsx`, and `packages/flux-react/src/__tests__/compilation-and-boundaries.test.tsx`; added explicit `assertTrackedPageErrors(page)` page-entry gating to `tests/e2e/flux-basic-row-inspect.spec.ts`.
- Plan 402: wired `packages/ui/src/lib/i18n.ts` fallback through the active `flux-i18n` instance via `setI18nGetter`, updated `packages/flux-i18n/src/i18n.ts`, added focused coverage in `packages/ui/src/lib/i18n.test.ts`, and extended `packages/flux-i18n/src/i18n-contract.test.ts`; adjudicated `docs/architecture/frontend-baseline.md` as `No change required` because the supported workspace/package baseline did not change.
- Focused verification passed: `packages/flux-react` targeted tests (`4` files / `35` tests), `packages/word-editor-renderers` targeted tests (`2` files / `14` tests), `packages/ui` targeted tests (`2` files / `4` tests), `packages/flux-i18n` targeted tests (`1` file / `18` tests), Playwright `tests/e2e/flux-basic-row-inspect.spec.ts` (`1` test), and workspace `pnpm lint`.
- Workspace `pnpm typecheck` and `pnpm build` remain blocked by a pre-existing Turborepo cycle between `@nop-chaos/flux-react` and `@nop-chaos/flux-renderers-basic`, so those plan closure gates could not be fully closed in this execution slice.

### 2026-05-19 (plans 384, 385, 404)

- Executed `docs/plans/384-deep-audit-2026-05-19-table-row-accessibility-plan.md`, `docs/plans/385-deep-audit-2026-05-19-table-column-settings-performance-plan.md`, and `docs/plans/404-deep-audit-2026-05-19-tabs-hidden-region-mounting-plan.md`; all three are now `partially completed` because the in-scope work landed but full closure gates are still blocked by an unrelated dirty `packages/flux-i18n/src/i18n.ts` `typecheck` failure and because verification stayed focused instead of running full package `pnpm test`.
- Plan 384: removed fake keyboard/button semantics from interactive table `<tr>` rows in `packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx`; row-click remains mouse click only, while the real expand `<Button>` now carries `aria-expanded`. Focused proofs landed in `packages/flux-renderers-data/src/__tests__/table-internal-components.test.tsx`.
- Plan 385: replaced the local column-settings render-time `findIndex`/`indexOf` lookup path in `packages/flux-renderers-data/src/table-renderer.tsx` with memoized keyed maps/sets so the settings UI renders from bounded lookups; focused proof landed in `packages/flux-renderers-data/src/__tests__/data-table-columns.test.tsx`.
- Plan 404: adjudicated the honest supported baseline as `keepMounted` for inactive tabs, made it explicit in `packages/flux-renderers-basic/src/tabs.tsx`, aligned `docs/components/tabs/design.md`, and added focused proof in `packages/flux-renderers-basic/src/__tests__/basic-page-layout-structure.test.tsx`. `docs/architecture/renderer-runtime.md` needed no change because the owner/mounting rule already lives in the component design doc and existing tests.
- Focused verification passed: `pnpm exec vitest run src/__tests__/table-internal-components.test.tsx` in `packages/flux-renderers-data`, `pnpm exec vitest run src/__tests__/data-table-columns.test.tsx` in `packages/flux-renderers-data`, `pnpm exec vitest run src/__tests__/basic-page-layout-structure.test.tsx -t "keeps inactive tab panels mounted as the supported baseline"` in `packages/flux-renderers-basic`, plus package-scoped `build` and `lint` for both affected packages.
- Verification blockers outside this scope: package-scoped `typecheck` for both affected packages currently fails because dirty `packages/flux-i18n/src/i18n.ts` imports `@nop-chaos/ui/lib/i18n` without a resolvable type path, and the package-level `pnpm test` entrypoint still picks up unrelated pre-existing failing suites when invoked through the package script.

### 2026-05-19 (UI design pattern remediation)

- Executed `docs/plans/370-ui-design-pattern-remediation-plan.md`: 22 in-scope fixes across 8 phases, all 8 phases completed.
- Phase 1: Removed `getWrappedFieldActionClasses` from `wrapped-field-action.tsx`, delegating to Button's built-in CVA for correct `icon-xs` sizing (24px vs incorrect 32px).
- Phase 2: Added `focus-visible:ring-2 focus-visible:ring-ring` to table sort headers, tree nodes, and chart canvas; changed chart `role="button"` to `role="img"`.
- Phase 3: Unified loading states with `Spinner` component in `SelectRenderer`, `ChartRenderer`, and `TreeControls` (both InputTree and TreeSelect).
- Phase 4: Wrapped `CheckboxRenderer`/`SwitchRenderer` controls in `<Label>` for click-to-toggle; added `aria-label` fallback (`optionLabel ?? name`) to CheckboxRenderer; removed literal "Button" fallback from ButtonRenderer.
- Phase 5: Added indeterminate visual support to `Checkbox` component (CSS-based via `data-indeterminate`); added `indeterminate` prop to table select-all checkbox; added `role="radio"` + `aria-checked` to single-select table rows; fixed VirtualBody empty state to render `emptyContent`; replaced filter icon `ChevronDownIcon` → `ListFilterIcon`.
- Phase 6: Changed key-value delete button to `ghost` + `Trash2Icon` + `hover:text-destructive`; replaced hardcoded `orange-*` in NOT toggle with `warning` design token; registered `--color-warning` in `@theme inline`.
- Phase 7: Added `flux.common.edit` i18n key (en-US/zh-CN); changed quick-edit button fallback from "Save" to "Edit"; removed `showCloseButton={false}` from DetailSurface and QuickEdit dialogs; replaced `×` with `XIcon` in multi-select remove tags.
- Phase 8: Changed `PaginationPrevious`/`PaginationNext` default text to i18n; CRUD toolbar pagination now uses `PaginationPrevious`/`PaginationNext` components from `@nop-chaos/ui`.
- Verification: `pnpm typecheck` passed (49 tasks), `pnpm build` passed (26 tasks), `pnpm lint` passed (26 tasks), `pnpm test` passed (43/48 tasks; 5 failures are pre-existing `nop-debugger` DLL exit and `word-editor-renderers` worker issues unrelated to changes). `@nop-chaos/flux-renderers-data` full suite: 32 files / 296 tests all green.
- Closure audit: Independent sub-agent verified all 8 phases pass against live repo. Initial FAIL on Phase 8 10.1 (CRUD toolbar used plain `<Button>` instead of `PaginationPrevious`/`PaginationNext`), fixed and re-verified. Chart test mock updated to include `Spinner` and `role="img"` assertion. Plan status set to `completed`.
- Files changed: 16 source files across 7 packages. Plan: `docs/plans/370-ui-design-pattern-remediation-plan.md`.

### 2026-05-19

- Completed a full 20-dimension deep audit and archived the reviewed results under `docs/analysis/2026-05-19-deep-audit-full/`.
- Tool baseline: `pnpm check:workspace-manifest-deps` and `pnpm check:oversized-code-files` currently fail; other hard gates checked for this audit include `check:src-artifacts`, `check:react19`, `check:active-doc-code-anchors`, `check:package-css-exports`, `check:i18n-keys`, `check:renderer-definition-fields-only`, and `check:finite-prop-contracts`.
- Final independently reviewed audit summary is in `docs/analysis/2026-05-19-deep-audit-full/summary.md`; P0 findings are the `flux-react` test manifest dependency gap plus seven oversized hard-gate files.

### 2026-05-19

- Fixed the performance table Full Stress StrictMode row-slot regression by including `RendererRuntime.runtimeId` in table row-scope cache owner keys, preventing keyed `SchemaRenderer` remounts from reusing stale row scopes from a previous runtime. Code paths: `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-table-row-scope-identity.test.tsx`, `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/bugs/61-performance-table-strictmode-row-scope-runtime-key-fix.md`.
- Verification status for this fix: `packages/flux-renderers-data` focused table tests passed (`4` files / `36` tests), full `@nop-chaos/flux-renderers-data` package tests passed (`32` files / `296` tests), `packages/flux-renderers-basic` nested structural test passed (`1` file / `9` tests), `apps/playground` performance-table page regression passed (`16` files / `66` tests), `packages/flux-react` StrictMode direct focused test passed (`1` file / `3` tests), focused Playwright performance-table E2E passed (`1` test), and full `pnpm test` passed (`49` successful tasks).
- Fixed recursive renderer slot evaluation under repeated-template remounts by isolating `NodeRendererResolved` runtime state per `instancePath`, which restores `recurse` lab rendering and removes `${$slot.node.*}` expression failures in StrictMode. Code paths: `packages/flux-react/src/node-renderer-resolved.tsx`, `packages/flux-react/src/node-instance.ts`, `apps/playground/src/component-lab/renderers/recurse-lab-page.test.tsx`.
- Verified recurse regression coverage stays green at both unit and e2e levels with zero console errors for the lab scenario. Relevant tests: `apps/playground/src/component-lab/renderers/recurse-lab-page.test.tsx`, `packages/flux-renderers-basic/src/__tests__/basic-structural.test.tsx`, `tests/e2e/component-lab/layout-content.spec.ts`.
- Normalized several package build scripts to use the shared asset copier so repeated workspace builds do not fail on existing CSS outputs. Package manifests: `packages/flux-renderers-form/package.json`, `packages/flow-designer-renderers/package.json`, `packages/report-designer-renderers/package.json`, `packages/flux-code-editor/package.json`.
- Verification status: targeted recurse unit tests passed, targeted recurse e2e passed, `pnpm typecheck` passed, `pnpm lint` passed. Re-ran `pnpm build` after script normalization to confirm the workspace build path is clean.

### 2026-05-19 (continued)

- Fixed collapsible fieldset missing expand/collapse icon. `packages/flux-renderers-form/src/renderers/fieldset.tsx` now renders `ChevronRightIcon` (collapsed) / `ChevronDownIcon` (expanded) inside the `<legend>` when `collapsible: true`, marked with `data-slot="fieldset-collapse-icon"`. Updated `fieldset-interaction.test.tsx` with 3 new tests (icon presence, icon toggle, no icon when not collapsible) and `form-package-exports.test.tsx` with icon assertions. Verification: `pnpm typecheck` passed, `pnpm build` passed, `pnpm lint` passed, `@nop-chaos/flux-renderers-form` 28 files / 195 tests all passed.

### 2026-05-19 (routing queue)

- Drafted and stabilized `docs/plans/371-deep-audit-2026-05-19-owner-routing-plan.md` as the docs-only owner-routing baseline for the `2026-05-19` deep audit retained set. The routing baseline now freezes `64` retained findings, explicit owner-doc obligations, and a one-to-one successor queue.
- Created successor plans `372`-`404` so every retained finding now points at a real plan file under `docs/plans/`, instead of placeholder successor paths. The queue is deliberately narrow: hard-gate file/suite splits, table/CRUD contract vs schema vs accessibility vs performance, validation owner lifecycle, detail-field contract, runtime lifecycle/debug fidelity, report/spreadsheet/flow-designer surfaces, docs/naming alignment, compiler diagnostics, test reliability, slot contract, and remaining performance/widget semantics all have separate owner plans.
- Corrected the retained-count bookkeeping in `docs/analysis/2026-05-19-deep-audit-full/summary.md` from `66` to `64` so the source summary now matches the explicit retained tables and Plan `371` matrix.
- Independent routing review consensus is now clean: final fresh review `ses_1c1a8efacffeFpK21DOAlZoJPa` returned `No findings` and `Verdict: acceptable` for Plan `371` plus successor plans `372`-`404`.
- Reconciled the post-merge renumbering so Plan `371`, the surviving successor files, and this log now all reference the same contiguous queue `372`-`404`.
- Re-audited plan granularity after questioning whether the queue was over-split. Current consensus change: merged `375+393` into one spreadsheet-grid surface, merged `380+381` into one input-renderer surface, and merged `389+390` into one runtime lifecycle/debug-fidelity surface. The routing baseline was rewritten to reflect the leaner queue while keeping the remaining plans on honest single execution surfaces.

### 2026-05-19 (plans 372, 394, 397)

- Plan `372`: added the missing `@nop-chaos/flux-renderers-basic` devDependency to `packages/flux-react/package.json`, which makes `pnpm check:workspace-manifest-deps` pass again and restores an honest manifest for `schema-renderer-strictmode-form.test.tsx`.
- Plan `372` also exposed a real workspace graph blocker: with the manifest made honest, Turbo now detects a package-task cycle between `@nop-chaos/flux-react` and `@nop-chaos/flux-renderers-basic`, so workspace `pnpm typecheck`, `pnpm build`, and `pnpm test` fail before execution. Focused `flux-react` tests still pass.
- Plan `394`: removed the unsafe `as unknown as` boundary in `packages/flow-designer-renderers/src/schemas.ts` by making `defineDesignerPageSchema()` return the caller-constrained input type directly. Added focused proof in `packages/flow-designer-renderers/src/public-surface.test.ts` that the helper preserves host-only fields without pretending to widen into `BaseSchema`.
- Plan `394` owner-doc adjudication: `docs/architecture/flow-designer/design.md` already remained honest for the supported boundary, so no doc text change was required.
- Plan `397`: aligned the live hook contract across code and owner docs. `packages/flux-core/src/types/renderer-hooks.ts` now includes `useCurrentImportFrame()` and `useScopeSelector(..., { paths })`; `packages/flux-react/src/index.tsx` now exports `useCurrentImportFrame`; `packages/flux-react/src/hooks.ts` now includes it in `rendererHooks`; `docs/architecture/renderer-runtime.md` now documents both the public `useCurrentImportFrame` adjudication and the `useScopeSelector.paths` precision option.
- Focused verification: `pnpm check:workspace-manifest-deps` passed; `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/public-surface.test.ts` passed (`1` file / `4` tests); `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/public-surface.test.ts src/__tests__/schema-renderer-strictmode-form.test.tsx` passed (`2` files / `6` tests); `pnpm --filter @nop-chaos/flux-core typecheck` passed; `pnpm --filter @nop-chaos/flux-react typecheck` passed; `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` passed; `pnpm --filter @nop-chaos/flux-core build` passed; `pnpm --filter @nop-chaos/flow-designer-renderers build` passed; workspace `pnpm lint` passed.
