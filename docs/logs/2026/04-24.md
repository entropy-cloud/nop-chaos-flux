# Development Log — 2026-04-24

### 2026-04-24

- Closed Plan 136 after a final fresh independent closure audit passed. The CRUD workflow baseline is now fully aligned across live renderer behavior, focused CRUD/table/compiler tests, playground CRUD examples, canonical docs/examples, and full workspace verification. The last blocking issues ended up being a mix of Plan 136 debt and unrelated workspace hygiene drift; after fixing the remaining CRUD i18n key, playground route inventory/registry drift, and pre-existing hook/lint issues uncovered during full verification, the final independent audit confirmed there was no remaining plan-owned CRUD gap.
- Link to doc or code path: `docs/plans/136-crud-workflow-completion-plan.md`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-compiler/src/schema-compiler-registry.test.ts`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `docs/components/crud/design.md`, `docs/components/crud/example.json`, `docs/components/crud/migration-example.json`
- Key decision: only mark the plan completed after a brand-new independent audit (`task_id: ses_23f9e92abffe671s4w7pEVjVCo`) re-verified the updated live repo and after the whole workspace was green on `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`.
- Next step: treat remaining CRUD work strictly as successor-plan scope only: richer table-heavy parity, editing/runtime extensions, and later AMIS full-parity / migration audit.

### 2026-04-24

- Aligned the live `recurse` runtime with its design contract: when a `recurse` node does not explicitly override `itemName` / `indexName` / `keyName`, it now inherits those binding names from the nearest enclosing structural loop context instead of silently falling back to default `item` / `index`. Added a focused structural renderer test that proves recursive levels keep the parent loop's custom binding names and stable keys, then re-ran the targeted Component Lab Playwright checks for `loop` and `recurse` to confirm the playground examples now work without extra recurse-local binding overrides.
- Link to doc or code path: `packages/flux-renderers-basic/src/recurse.tsx`, `packages/flux-renderers-basic/src/__tests__/basic-structural.test.tsx`, `apps/playground/src/component-lab/renderers/recurse-lab-page.tsx`, `tests/e2e/component-lab/layout-content.spec.ts`, `docs/components/recurse/design.md`
- Key decision: make the runtime honor the documented lexical-inheritance rule rather than keeping the playground example-specific workaround (`itemName: 'node'` on every recurse node); examples should stay minimal and reflect the intended authoring model.
- Next step: if similar drift exists for recurse inheritance of other loop-local semantics beyond binding names, audit `itemData` / `keyBy` / any future repeated-scope metadata against the same lexical inheritance contract.

### 2026-04-24

- Followed through on the Component Lab `loop` / `recurse` fix with end-to-end verification. Two additional live mismatches surfaced during Playwright validation: the playground examples were still authoring `badge` content via `label` even though the live badge renderer reads `text`, and the current recurse runtime does not automatically carry the enclosing loop's `itemName`, so recursive lab examples needed explicit `itemName: 'node'` on the `recurse` nodes to keep nested levels rendering against `$slot.node`. Also widened the shared Component Lab helper timeouts because a cold-start local Vite boot can take ~19s before `component-lab` becomes visible, which was causing false e2e failures before the page finished compiling.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/loop-lab-page.tsx`, `apps/playground/src/component-lab/renderers/recurse-lab-page.tsx`, `tests/e2e/component-lab/helpers.ts`, `tests/e2e/component-lab/layout-content.spec.ts`
- Key decision: keep the runtime unchanged for this slice and make the examples/tests honest consumers of the current live contracts (`badge.text`, explicit recurse `itemName`, larger cold-start wait budget) rather than layering compatibility behavior into renderers just to satisfy playground fixtures.
- Next step: if recurse is intended to inherit the nearest loop bindings by default, align the runtime with `docs/components/recurse/design.md` in a separate slice and then simplify the playground example back down.

### 2026-04-24

- Fixed the Component Lab `loop` / `recurse` playground examples so they render against the current structural-loop runtime contract again. The previous examples used direct `item` / `node` bindings instead of the live `$slot.*` bindings, and the `recurse` examples were authored at page root even though `recurse` only works inside the nearest enclosing `loop.body`. Updated the examples to use `$slot` bindings, wrapped recursion in a lexical `loop`, and tightened the e2e assertions to check real rendered labels instead of only checking that the stage exists.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/loop-lab-page.tsx`, `apps/playground/src/component-lab/renderers/recurse-lab-page.tsx`, `tests/e2e/component-lab/layout-content.spec.ts`
- Key decision: treat the playground examples as consumers of the already-landed structural rendering contract instead of adding compatibility behavior in runtime; the minimal fix is to author the examples the same way the live renderer tests already do.
- Next step: fix the separate playground dev-server import-resolution blocker in `@nop-chaos/flux-renderers-data` (`packages/flux-renderers-data/src/index.tsx` -> `./crud-renderer`) so Playwright can boot the Component Lab again and the strengthened `loop` / `recurse` e2e checks can run end to end.

### 2026-04-24

- Ran an independent closure audit for Plan 136 and treated the `not completed` result as binding feedback rather than closing from self-audit. The audit found two remaining plan-owned gaps: CRUD docs/examples still overstated API-backed/server-side workflow semantics beyond the current array-source baseline, and CRUD-specific proof was still missing for `$crud.pagination/$crud.sort/$crud.filters` publication plus CRUD-context operation-column interaction. Follow-up landed in the same session: docs were narrowed to the actual live baseline, focused CRUD tests were added for summary publication and operation-button interaction, and `@nop-chaos/flux-renderers-data` verification returned to green afterward.
- Link to doc or code path: `docs/plans/136-crud-workflow-completion-plan.md`, `docs/components/crud/design.md`, `docs/components/crud/migration-example.json`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`
- Key decision: use the first independent audit as real closure gating instead of a formality; fix the remaining plan-owned gaps immediately, then require one more fresh independent audit before moving the plan from `partially completed` to `completed`.
- Next step: run a final fresh closure audit for Plan 136 against the updated repo and, if it passes, mark the plan completed with successor-plan boundaries only.

### 2026-04-24

- Fixed the playground home navigation card layout regression: the home page cards were rendered with `@nop-chaos/ui` `Button` base inline-flex behavior but without explicitly restoring the card-specific vertical layout, so card eyebrow/title/description no longer stacked correctly after the shared button baseline change. The home page now pins those entry buttons to `flex-col`, `items-start`, and `w-full` so the visual card layout is stable again.
- Link to doc or code path: `apps/playground/src/pages/home-page.tsx`
- Key decision: keep the existing button-based interactive card pattern and make the page-local layout explicit instead of attributing the regression to `Card` `data-slot` styling, because the page was not using the shared `Card` component at all.
- Next step: if similar regressions appear elsewhere, audit pages that use `Button` as a card container and make their intended layout explicit instead of depending on shared button defaults.

### 2026-04-24

- Synced CRUD documentation examples to the current live baseline: the canonical example now shows scope-backed `columnSettings` ownership and `$crud.query` summary usage, while the AMIS migration example now includes a stable CRUD `id`, explicit `columnSettings`, and selection-aware `listActions` disabling so the target Flux example matches currently tested runtime behavior instead of only field-name mapping.
- Link to doc or code path: `docs/components/crud/example.json`, `docs/components/crud/migration-example.json`, `docs/components/crud/design.md`
- Key decision: prefer examples that reflect the verified live baseline over minimal field-rename demonstrations; migration docs should show the canonical target shape users should actually author after transform, not just the alias correspondence table.
- Next step: re-audit Plan 136 remaining closure criteria and decide whether the next slice should be plan closure evidence, successor-plan extraction, or any remaining CRUD doc wording cleanup.

### 2026-04-24

- Extended Plan 136 Phase 4 compiler evidence: `flux-compiler` now has focused CRUD authoring-transform tests proving `bulkActions -> listActions` canonicalization at compile time and proving canonical fields win when legacy aliases (`filter`, `primaryField`, `perPageField`) are present alongside `queryForm`, `rowKey`, and `pageSizeField`. While landing that evidence, also fixed two package-verification blockers: `flux-compiler` build now excludes test sources from `dist`, and table duplicate-row-key dev warnings no longer depend on untyped `import.meta.env` access.
- Link to doc or code path: `packages/flux-compiler/src/schema-compiler-registry.test.ts`, `packages/flux-compiler/tsconfig.build.json`, `packages/flux-renderers-data/src/table-renderer/table-data.ts`
- Key decision: keep the compiler evidence focused on canonicalization semantics instead of widening into migration-doc rewrites first; when a verification blocker surfaced on the touched path, fix it immediately so both touched packages end the slice fully green.
- Next step: return to remaining Plan 136 closure items, most likely syncing CRUD example/migration-example docs to the now-tested live alias/runtime baseline or auditing whether any workflow-focused tests are still missing.

### 2026-04-24

- Strengthened Plan 136 CRUD workflow evidence with two focused runtime tests: one now proves query submit/reset state becomes the next `onRefresh` input payload (`query`, `params`, `refreshCount`), and another proves selection-driven `listActions` disabled state turns on with selection and clears again on refresh when `autoClearSelectionOnRefresh` is enabled.
- Link to doc or code path: `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-data/src/crud-renderer.tsx`
- Key decision: validate the refresh contract through a host probe namespace instead of indirect `setValue` text assertions, so the test checks the real `onRefresh` payload shape rather than a secondary rendering side effect.
- Next step: keep closing Plan 136 evidence gaps around compiler alias coverage or any remaining CRUD workflow assertions that still lack direct focused proof.

### 2026-04-24

- Advanced the table/CRUD `columnSettings` live baseline from visibility-only to visibility plus ordered-column ownership: `orderedColumnsStatePath` now drives effective column order, the table renderer applies that order consistently to header/body/fixed-column layout, and the column settings menu exposes minimal move-up/move-down controls with focused tests for both scope-backed order state and local menu-driven reordering.
- Link to doc or code path: `packages/flux-renderers-data/src/table-renderer/use-table-controls.ts`, `packages/flux-renderers-data/src/table-renderer/column-settings-state.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-i18n/src/locales/en-US.ts`, `packages/flux-i18n/src/locales/zh-CN.ts`, `docs/components/crud/design.md`
- Key decision: keep `columnSettings` on one owner surface and land the smallest usable ordering behavior first; use ordered state paths plus explicit move actions now, while leaving drag/drop, overlay behavior, and fuller AMIS parity for a later successor slice.
- Next step: continue Plan 136 follow-up evidence and doc sync around CRUD workflow parity, then decide whether the next highest-value gap is stronger CRUD query/reset proof, selection-driven list action coverage, or compiler alias coverage.

### 2026-04-24

- Closed the `@nop-chaos/flux-renderers-data` verification loop after the CRUD/table follow-up: added the missing build-time dependency on `@nop-chaos/flux-renderers-form`, excluded test-only files from package build output, normalized newly introduced toolbar/button assertions to i18n-driven labels, and cleaned up existing package lint debt in chart/runtime helper code so package-local `typecheck`, `build`, `lint`, and `test` all pass again.
- Link to doc or code path: `packages/flux-renderers-data/package.json`, `packages/flux-renderers-data/tsconfig.build.json`, `packages/flux-renderers-data/src/chart-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/use-table-controls.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/crud-renderer-toolbar.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`
- Key decision: treat the package verification breakage as part of the same landing slice instead of leaving the CRUD/table work in a half-green state; keep build outputs restricted to production sources, while tests and test-support stay package-local but excluded from `dist` compilation.
- Next step: return to Plan 136 feature work from a green `flux-renderers-data` baseline, with the next highest-value gaps still in richer column settings, quick edit, and remaining AMIS CRUD parity deltas.

### 2026-04-24

- Tightened the new CRUD/table regression coverage to target stable slots/roles, fixed table header-search rendering for `searchable` columns, fixed body rendering to honor column-settings visibility, and added a CRUD-local query fallback so external search/reset controls keep working even when the nested query form lives behind its own component registry. Also updated form input/textarea renderers to publish `name` and `aria-label`, which makes CRUD query controls and tests resolve live field values reliably.
- Link to doc or code path: `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx`, `packages/flux-renderers-data/src/table-renderer/table-header-row.tsx`, `packages/flux-renderers-form/src/renderers/input.tsx`
- Key decision: keep the last query-submit fix local to CRUD rather than weakening form renderer registry isolation globally; the query form remains a normal form owner, while CRUD reads live query controls directly when it cannot resolve the nested form handle from the parent registry.
- Next step: decide whether to clean up the package-level pre-existing verification debt in `@nop-chaos/flux-renderers-data` (`build` missing `@nop-chaos/flux-renderers-form` dependency for `src/test-support.tsx`, plus existing unrelated lint failures in chart/toolbar/control files) as part of this CRUD stream or in a separate hygiene pass.

### 2026-04-24

- Fixed the report designer spreadsheet drag-selection preview regression: dragging across cells now shows the selection highlight during mouse move via local interaction preview state, while the committed spreadsheet selection still updates on mouseup. Added a focused regression test that asserts multi-cell range highlight appears before mouse release.
- Link to doc or code path: `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-selection.ts`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`
- Key decision: keep high-frequency drag preview out of the shared spreadsheet/report host scope and model it as renderer-local transient state, so mousemove feedback stays responsive without forcing global runtime updates on every pointer event.
- Next step: if more spreadsheet interactions gain preview overlays (resize ghost, drag-drop target bands, autofill preview), keep them on the same transient interaction layer instead of promoting them into committed runtime state too early.

### 2026-04-24

- Migrated chart renderer from ECharts to Recharts, unifying the chart library across the project (Recharts was already used in `@nop-chaos/ui` and `word-editor-renderers`). Removed `echarts` dependency from `flux-renderers-data`, now uses `recharts` via `ChartContainer`/`ChartTooltip`/`ChartLegend` from `@nop-chaos/ui`. ComponentHandle simplified to `resize` only (no longer exposes imperative `setOption`/`getDataURL`).
- Files: `packages/flux-renderers-data/src/chart-renderer.tsx` (full rewrite), `packages/flux-renderers-data/package.json` (echarts→recharts), `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx` (removed ECharts mock, updated handle assertions)
- Key decision: Recharts chosen over ECharts for React-native component model, unified tech stack (already used in ui/word-editor), and sufficient chart type coverage (bar, line, pie, scatter). Recharts v3.8.1 is actively maintained (27k stars, releases every 3-6 weeks).
- Pre-existing issues found: `index.tsx:287` type error in `transformCrudAuthoringSchema` return type; CRUD tests all failing with `Cannot read properties of undefined (reading 'action')` — both unrelated to chart migration.

### 2026-04-24

- Advanced Plan 136 from CRUD shell parity toward live workflow behavior: split the oversized `crud-renderer` into focused modules, wired CRUD-owned query/summary/toolbar state through scope-visible paths, added toolbar block rendering plus footer toolbar support, landed sticky left/right fixed columns in the table renderer, and added focused regression coverage for fixed operation columns and CRUD workflow markers. Validation is still blocked by pre-existing `packages/flux-renderers-data/src/chart-renderer.tsx` baseline issues (`recharts` missing and old chart typing errors), not by the new CRUD/table changes.
- Link to doc or code path: `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/crud-renderer-state.ts`, `packages/flux-renderers-data/src/crud-renderer-toolbar.tsx`, `packages/flux-renderers-data/src/table-renderer/fixed-columns.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/table-header-row.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-data/src/index.tsx`, `packages/flux-renderers-data/src/schema-validator.test.ts`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `docs/components/crud/design.md`, `docs/plans/136-crud-workflow-completion-plan.md`
- Key decision: keep CRUD as a thin workflow orchestrator over canonical table/form behavior, reduce scope subscriptions to one consolidated CRUD runtime selector over the actual configured paths, and treat fixed columns as a table-level live dependency that must land inside Plan 136 because CRUD usability depends on it.
- Next step: finish CRUD query submit/reset evidence with stronger focused tests, sync examples/migration docs to the live alias set, keep Plan 136 explicitly scoped as workflow baseline rather than pretending single-plan full AMIS parity, run remaining verification once the unrelated chart package baseline is unblocked, and then record an independent closure audit for Plan 136.

- Added a route-inventory-backed Playwright smoke suite for every playground entry page and re-ran the oversized-file audit script to verify current repo hygiene status.
- Link to doc or code path: `tests/e2e/playground-entry-pages.spec.ts`, `apps/playground/src/route-model.ts`, `scripts/check-oversized-code-files.mjs`, `package.json`
- Key decision: keep entry-page coverage driven by `DOMAIN_RENDERER_ROUTES` so any new playground landing page must add a matching e2e assertion instead of silently shipping uncovered.
- Next step: split the four code files still above the hard 700-line limit: `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-formula/src/compile.ts`, `packages/flux-action-core/src/action-dispatcher.ts`.

### 2026-04-24

- Fixed Bug #19 regression: commit `d5ae387` (migrate to @nop-chaos/ui components) had reverted the original `<span role="button">` fix by replacing all code-editor toolbar/header buttons with `<Button>`, re-introducing label click forwarding inside `wrap: true` renderer. Created `ToolbarButton` component that renders `<span role="button" tabIndex={0}>` with same visual appearance as `<Button variant="ghost">`. Updated all 5 affected files.
- Files: `packages/flux-code-editor/src/code-editor-renderer/toolbar-button.tsx` (new), `code-editor-renderer.tsx`, `code-editor-renderer/code-editor-toolbar.tsx`, `variable-panel.tsx`, `sql-result-panel.tsx`, `extensions/snippet-panel.tsx`
- Key decision: use `<span role="button">` for ALL interactive elements inside `wrap: true` renderers since `<label>` forwards clicks to the first labelable descendant per HTML spec. PopoverContent items exempt (rendered in portal, outside `<label>`).
- Bug doc: `docs/bugs/19-code-editor-label-click-forwarding-triggers-fullscreen-fix.md`

### 2026-04-24

- 对比 AMIS FormItem/Group/FieldSet 的表单分组与标签布局机制，更新 Flux 表单设计文档。新增 fieldset 独立组件设计、表单 mode/labelAlign/labelWidth 两级配置（form全局 + 字段FieldFrame覆盖）、字段提示信息（description/hint/remark/labelRemark）schema 定义。编写完整 AMIS-to-Flux 迁移 JSON 示例。
- 文件: `docs/components/form/design.md` §12-§15, `docs/components/fieldset/design.md` (新建), `docs/components/form/amis-migration-example.json` (新建), `docs/architecture/field-frame.md`, `docs/components/amis-baseline-matrix.md` (fieldset 从 notRetained 改为 targetContract)
- 关键决策: (1) 三层分离——FieldFrame=单字段chrome, fieldset=分组容器, flex=行布局；(2) 布局配置只有两级（form全局 → FieldFrame），通过 React context 自动穿透中间层，fieldset/flex 不参与；(3) AMIS `desc`/`caption` 不保留，统一用 `description`/`hint`

### 2026-04-24

- Added a new owner plan for CRUD workflow completion, narrowed it away from table-heavy and editing-heavy parity work, and iterated it through multiple independent subagent review passes until the plan converged on an execution-ready scope.
- Link to doc or code path: `docs/plans/136-crud-workflow-completion-plan.md`, `docs/plans/114-crud-component-implementation-plan.md`, `docs/components/crud/design.md`
- Key decision: keep one owner plan focused on CRUD workflow completion only, and explicitly defer table-heavy parity (`fixed columns`, `columnSettings`, `responsive expansion`, `header search/filter`) plus editing/runtime extensions (`quickEdit`, `quickSave*`, `loadDataOnce`, `syncLocation`) to successor plans instead of reopening one oversized umbrella plan.
- Next step: execute plan 136 phase by phase and open successor plans only when the workflow plan lands or a blocked dependency proves the split is needed earlier.

### 2026-04-24

- Landed renderer-local `authoringTransform` as a first-class compile-time schema-to-schema canonicalization hook on `RendererDefinition`, wired it into validation and compile before renderer-local closed-prop diagnostics, and used `crud.bulkActions -> listActions` as the first live lowering path.
- Link to doc or code path: `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/schema-diagnostics/index.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/schema-compiler-registry.test.ts`, `packages/flux-compiler/src/schema-compiler-renderer-contracts.test.ts`, `packages/flux-renderers-data/src/index.tsx`, `packages/flux-renderers-data/src/schema-validator.test.ts`, `docs/architecture/node-level-compile-time-transforms.md`
- Key decision: define `authoringTransform` as a pure renderer-local schema-to-schema canonicalization stage, not a field-level mapper and not a runtime normalization escape hatch.
- Next step: migrate more CRUD aliases such as AMIS-style query/filter sugar through the same canonicalization layer and keep runtime renderers consuming canonical props only.

### 2026-04-24

- Reworked the `crud` contract toward canonical `listActions` instead of a dedicated `bulkActions` field, expanded the AMIS parity design around fixed columns, query/filter, column settings, and responsive expansion, and added a dedicated architecture doc for renderer-local node-level compile-time transforms.
- Link to doc or code path: `docs/components/crud/design.md`, `docs/components/crud/example.json`, `docs/components/crud/migration-example.json`, `docs/architecture/node-level-compile-time-transforms.md`, `docs/index.md`, `packages/flux-renderers-data/src/crud-schema.ts`, `packages/flux-renderers-data/src/index.tsx`, `packages/flux-renderers-data/src/crud-renderer.tsx`
- Key decision: treat selection-driven batch behavior as ordinary `listActions` over `$crud` summary state rather than as a standalone schema field, and put future migration/canonicalization logic at renderer-node compile time instead of runtime normalization or field-by-field ad hoc transforms.
- Next step: if the compiler contract is extended next, add a renderer-level `authoringTransform` hook on `RendererDefinition` so `crud.bulkActions -> listActions` and similar migration aliases lower before renderer schema validation.

### 2026-04-24

- Added minimal historical notes to the highest-risk `docs/analysis/` reports that still mentioned `useNodeImports` as if it were part of the live architecture, and updated the comparison pipeline sketch to point at prepared import-boundary installation instead of the retired hook.
- Link to doc or code path: `docs/analysis/2026-04-20-tooljet-vs-nop-chaos-flux-deep-comparison.md`, `docs/analysis/2026-04-20-deep-audit-runtime/07-lifecycle.md`, `docs/analysis/2026-04-18-deep-audit-kernel/07-lifecycle.md`, `docs/analysis/2026-04-17-deep-audit-3/07-lifecycle.md`, `docs/analysis/2026-04-17-deep-audit/14-test-coverage-quality.md`
- Key decision: prefer lightweight historical framing on old analysis documents over invasive rewrites, so readers are warned away from stale lifecycle details without destroying the original audit record.
- Next step: if needed, repeat the same treatment for the remaining lower-value audit docs that only mention `useNodeImports` incidentally.

### 2026-04-24

- Marked the most misleading active-plan references to retired `use-node-imports` behavior as historical/outdated instead of current baseline: Plan 133 now points readers to the compile-time prepared-import model, Plan 116 records that its old node-local async path was later superseded, Plan 123 no longer states the retired React-side import loader as live baseline, and Plan 126 now references the remaining import failure reporting seam in terms of schema-prepare/import-stack ownership.
- Link to doc or code path: `docs/plans/133-node-renderer-runtime-stack-and-import-boundary-refactor-plan.md`, `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md`, `docs/plans/123-flux-runtime-split-and-boundary-hardening-plan.md`, `docs/plans/126-runtime-effect-channel-convergence-plan.md`
- Key decision: preserve historical plan records, but add explicit outdated/superseded framing wherever an old import-loading path would otherwise look like the current implementation baseline.
- Next step: if repo navigation still feels noisy, continue the same treatment for older analysis docs and archival logs rather than rewriting them wholesale.

### 2026-04-24

- Deleted the retired `packages/flux-react/src/use-node-imports.ts` hook after confirming it had no remaining source imports, exports, or test dependencies; the old async node-local import loading path is now only preserved in historical docs/plans, not in live code.
- Link to doc or code path: `packages/flux-react/src/use-node-imports.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `docs/plans/134-node-renderer-compile-time-execution-plan-convergence-plan.md`
- Key decision: remove the dead file once the compile-time preload + synchronous `importsPlan` runtime path was fully verified, while leaving superseded historical docs intact as records rather than rewriting them as if the old path never existed.
- Next step: if we want deeper repo hygiene, audit old plans/analysis docs that still reference `useNodeImports` and mark them more explicitly as historical or superseded where that improves navigation.

### 2026-04-24

- Removed the last dead `NodeRenderer` raw-schema helper remnants by deleting unused `getNodeImports()` and `getNodeClassAliases()` utilities now that live execution paths only consume compiled `importsPlan` / `classAliasesPlan` data.
- Link to doc or code path: `packages/flux-react/src/node-renderer-utils.ts`, `packages/flux-react/src/node-renderer.tsx`, `docs/plans/134-node-renderer-compile-time-execution-plan-convergence-plan.md`
- Key decision: keep the cleanup minimal and mechanical; remove only the dead helper remnants that no longer have live call sites, while leaving broader historical import-path cleanup to separate work if needed.
- Next step: if we choose to continue historical cleanup, the next candidate is the retired `use-node-imports.ts` file and any superseded plans/docs that still describe it as current baseline.

### 2026-04-24

- Closed Plan 134 after finishing the remaining compile-time execution-plan convergence gaps: `NodeRenderer` now has explicit no-import and no-class-alias fast-path tests, imported helper/member/function diagnostics are wired through static import meta, architecture docs describe schema-level preload as the only standard import-loading baseline, and full workspace `typecheck` / `build` / `lint` / `test` all passed.
- Link to doc or code path: `docs/plans/134-node-renderer-compile-time-execution-plan-convergence-plan.md`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-formula/src/compile.ts`, `packages/flux-compiler/src/schema-compiler-diagnostics.test.ts`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/module-cache-and-import-stack.md`
- Key decision: treat the remaining work as closure-quality hardening of the landed execution model rather than reopening runtime architecture; node-local optional boundaries now resolve entirely from compiled plan data plus prepared imports, and future richer import type contracts belong in a successor plan instead of this convergence plan.
- Next step: if import static metadata grows beyond current helper/member/function arity validation into richer typing or IDE-facing contracts, open a new successor plan instead of extending Plan 134.

### 2026-04-24

- Re-verified the previously suspected `flux-runtime` source/reaction recursion blocker and found it no longer reproducible: focused async-data suites, package-local `pnpm test`, and workspace-root `pnpm test` all passed without additional runtime code changes, so bug 15 was closed as a stale verification artifact rather than fixed through new implementation work.
- Link to doc or code path: `docs/bugs/15-flux-runtime-source-reaction-recursion-and-dependency-guard-regression.md`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-runtime/src/async-data/source-registry.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`
- Key decision: do not make speculative `flux-runtime` edits when the reported recursion failure cannot be reproduced; prefer evidence-based closure of the stale bug note over risky changes in a now-green subsystem.
- Next step: continue Plan 134 closure work from the now-green workspace baseline, and only reopen the runtime bug line if a concrete failing test resurfaces.

### 2026-04-24

- Closed Plan 135 after adding the representative page-root validation-owner example, enforcing the embedded `parentScope` non-goal in live code, and recording an independent audit that confirmed the landed slice is complete for the scoped owner families.
- Link to doc or code path: `docs/plans/135-non-form-validation-scope-and-owner-boundary-implementation-plan.md`, `docs/examples/page-root-validation-owner.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/contexts.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/__tests__/schema-renderer-validation-owner-boundary.test.tsx`
- Key decision: close the plan on focused implementation evidence and an explicit independent audit, while leaving broader repo-wide test cleanup and future non-form owner families such as filter/search panels to successor plans instead of silently expanding Plan 135.
- Next step: if Phase 3 continues, open a successor plan for the next concrete non-form owner family rather than reopening page-root scope.

### 2026-04-24

- Landed the first concrete non-form validation owner slice: `SchemaRenderer` page-owned root now provisions a validation owner, page root schemas compile a `validationPlan`, and bound fields outside `<form>` validate and show blur-triggered errors without regressing existing form validation UI behavior.
- Link to doc or code path: `docs/plans/135-non-form-validation-scope-and-owner-boundary-implementation-plan.md`, `docs/architecture/form-validation.md`, `docs/architecture/data-domain-owner.md`, `docs/examples/page-root-validation-owner.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/__tests__/schema-renderer-validation-owner-boundary.test.tsx`, `packages/flux-renderers-form/src/field-utils.tsx`, `packages/flux-renderers-form/src/__tests__/form-validation-ui.test.tsx`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-compiler/src/schema-compiler.ts`
- Key decision: keep the shipped slice narrow by treating only `SchemaRenderer` page-owned root as the first non-form owner family; embedded `parentScope` schema renders stay parent-owned, and the generic owner path continues to reuse the existing form-backed validation substrate instead of introducing a second incompatible field-state model.
- Next step: run an independent closure audit before marking Plan 135 complete.

### 2026-04-24

- Tightened the failing Playwright suite around contract-level assertions instead of implementation detail checks: code-editor and component-lab direct hash-route helpers now use lighter navigation waits plus page-owned visibility contracts, and the report-designer e2e file was rewritten away from brittle computed-style/layout assertions toward surface, grid, inspector, and sheet-tab behavior contracts.
- Link to doc or code path: `tests/e2e/code-editor.spec.ts`, `tests/e2e/component-lab/helpers.ts`, `tests/e2e/report-designer-demo.spec.ts`, `tests/e2e/debug-collapsible2.spec.ts`
- Key decision: keep E2E focused on externally observable page contracts (route entry, visible surfaces, interactive affordances, inspector updates, add-sheet behavior) rather than binding tests to CSS internals, exact display modes, or other incidental implementation details.
- Next step: separately audit the remaining non-task workspace blockers currently failing full repo verification in `flux-runtime` typecheck/build, `flux-compiler` lint/unit tests, and a few unrelated high-parallel Playwright cases before treating the whole repo as fully green.

### 2026-04-24

- During full-workspace verification for the NodeRenderer/import-plan convergence work, isolated a separate `flux-runtime` blocker in the async data / reaction dependency refresh subsystem and recorded it as an explicit bug instead of silently widening Plan 134.
- Link to doc or code path: `docs/bugs/15-flux-runtime-source-reaction-recursion-and-dependency-guard-regression.md`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-runtime/src/async-data/source-registry.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`
- Key decision: treat the remaining `pnpm test` failures as a distinct runtime bug line; do not mask or conflate them with the compile-time import / NodeRenderer execution-plan migration.
- Next step: create a dedicated execution plan for the `flux-runtime` source/reaction dependency guard path and fix that subsystem independently.

### 2026-04-24

- Started executing Plan 134 and landed the first real compile-time execution-plan convergence slice for `NodeRenderer`: `SchemaRenderer` now preloads all schema `xui:imports` before compilation, `TemplateNode` carries `classAliasesPlan` / `importsPlan`, and `NodeRenderer` installs import-owned boundaries synchronously from prepared imports instead of rediscovering `classAliases` / `xui:imports` from raw schema at render time.
- Link to doc or code path: `docs/plans/134-node-renderer-compile-time-execution-plan-convergence-plan.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-runtime/src/import-stack.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-core/src/types/compilation.ts`
- Key decision: remove the temporary raw-`XuiImportSpec` runtime compatibility path and keep the execution model strict: authoring schema imports are collected and preloaded before compile, runtime execution only consumes `PreparedImportSpec` / compiled node plans.
- Next step: finish the remaining Plan 134 work by wiring static import meta into compile-time member/function validation, adding explicit zero-overhead absence-path tests, and updating owner architecture docs to make the preloaded compile-time import model the normative baseline.

### 2026-04-24

- Added a new execution plan for Phase 3 owner-scope work covering reusable non-form validation owners, page/root fallback ownership, and standalone field validation outside `<form>`.
- Link to doc: `docs/plans/135-non-form-validation-scope-and-owner-boundary-implementation-plan.md`
- Key decision: do not reopen Phase 3 as one broad "owner tree rewrite"; instead, target a narrower complete feature slice that first lands reusable `ValidationScopeRuntime` substrate plus concrete non-form owner provisioning, while keeping row-local staged owners and generalized draft-owner automation out of scope.
- Next step: run independent review rounds against the new plan, tighten any scope or contract drift, then use the plan as the execution baseline when implementation begins.

### 2026-04-24

- Updated the AI methodology article with a new section on large-scale Agent Team governance and renumbered the affected later sections.
- Link to doc: `docs/articles/ai-assisted-framework-development-methodology.md`
- Key decision: treat high-governance workflows as machine-executable operating rules for `agent team + human arbitration`, not only as human-heavy process overhead; emphasize role separation, evidence isolation, and automatic candidate淘汰 before human review, while avoiding language that implies the original article had already fixed a simpler collaboration topology.
- Next step: if the article continues evolving, extract the Agent Team operating model into a dedicated follow-up article with concrete orchestration protocols and evidence schemas.
### 2026-04-24

- Updated action-field naming guidance to distinguish event-style `onXX` fields from owner semantic pipeline slots such as `submitAction` and `transformOutAction`.
- Link to doc or code path: `docs/architecture/field-metadata-slot-modeling.md`, `docs/components/form/design.md`, `packages/flux-renderers-form/src/renderers/form.tsx`
- Key decision: do not normalize all action-bearing schema fields to `onXX`; keep `xxxAction` for owner-controlled primary lifecycle or value-adaptation entry points, while using `onXX` for event-like hooks and follow-up result branches.
- Next step: apply the same naming rule when adding new owner-level action entry points in future component contracts.
