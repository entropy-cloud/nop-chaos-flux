### 2026-05-08

- continued `docs/plans/230-renderer-slot-and-type-contract-cleanup-plan.md` with a live renderer-contract repair for retained `readOnly` drift.
- shared form write blocking now lands in `packages/flux-renderers-form/src/field-utils/field-handlers.tsx`: `useFormFieldController(...)` ignores `onChange` writes while `readOnly` is true instead of only making widgets look visually inert.
- retained advanced-control propagation now lands in `packages/flux-renderers-form-advanced/src/{condition-builder/condition-builder.tsx,key-value.tsx,array-editor.tsx,tree-controls.tsx,tag-list.tsx}`; direct mutation paths in `tag-list`, `key-value`, `array-editor`, and `tree-select` now respect the shared `readOnly` contract instead of mutating state behind the field shell.
- focused proof passed: `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/field-utils.unit.test.tsx src/__tests__/input-source-state.test.tsx src/__tests__/input-number.test.tsx` and `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/tag-list.test.tsx src/condition-builder/condition-builder-renderer.test.tsx src/__tests__/tree-values.test.tsx src/key-value.test.tsx`.
- verification: package `typecheck` for `@nop-chaos/flux-renderers-form` and `@nop-chaos/flux-renderers-form-advanced` passed, and workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all remained green after the readOnly contract repair.

### 2026-05-08

- advanced `docs/plans/230-renderer-slot-and-type-contract-cleanup-plan.md` with live renderer/slot contract cleanup across owner pages and advanced controls.
- slot-model cleanup landed in `packages/{flow-designer-renderers,report-designer-renderers,word-editor-renderers,spreadsheet-renderers,flux-renderers-basic}/src/**`: owner pages and `fragment` now render compiled child regions through `RenderRegionHandle.render(...)` instead of re-rendering `region.templateNode` through `helpers.render(...)`.
- renderer-contract cleanup landed in `packages/flow-designer-renderers/src/designer-page.tsx` and `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder.tsx`: `designer-page` now reads `config` from resolved props instead of raw schema, and condition-builder picker mode now honors root `props.meta.className/testid/cid` passthrough consistently with embedded mode.
- focused proof passed: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-page-shell.test.tsx src/designer-page.tree.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/condition-builder/condition-builder-renderer.test.tsx src/__tests__/widget-classname-contract.test.tsx`, `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/schema-integration.test.tsx`, `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/renderers.integration.test.tsx`, `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-page-host-scope.test.tsx`.
- verification: workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all remained green after the slot/meta cleanup.

### 2026-05-08

- closed the final workspace verification blocker for the `222`-`230` closure pass by making `packages/flux-code-editor/src/code-editor-renderer/use-code-editor-binding.ts` tolerate missing `props.meta`; this unblocked `packages/flux-code-editor/src/code-editor-renderer/use-code-editor-binding.test.tsx` and removed the last red `pnpm test` failure.
- final workspace verification is green for this pass: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- independent closure audit verdict for the active successor set: `docs/plans/222-large-file-and-owner-boundary-successor-plan.md` can now close as `completed`; `223`, `224`, `226`, `227`, `229`, and `230` remain active with partial/in-progress status; `225` remains `planned` because no plan-owned hardening slice has landed yet.

### 2026-05-08

- completed `docs/plans/228-styling-and-css-surface-cleanup-plan.md` in the live repo. Moved `packages/flux-react/src/node-error-boundary.tsx` fallback visuals off inline styles onto `@nop-chaos/ui` `Alert` plus `packages/flux-react/src/default-spacing.css`; added `WorkbenchShell` density ownership in `packages/flux-react/src/workbench/workbench-shell.tsx` and removed the word-editor-specific `style={{ padding: 0 }}` override from `packages/word-editor-renderers/src/word-editor-page.tsx`.
- consolidated non-geometry debugger inline styling in `packages/nop-debugger/src/panel/{node-tab.tsx,styles-css.ts}`; narrowed spreadsheet CSS ownership in `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,canvas-styles.css}` by anchoring shell/header selectors to stable package-owned `data-slot="spreadsheet-*"` surfaces while retaining `ss-*` canvas classes; updated focused tests and `docs/architecture/report-designer/spreadsheet-canvas-css.md` accordingly.
- code-editor decision: `packages/flux-code-editor/src/code-editor-styles.css` now uses token-based fullscreen/error colors where straightforward, but keeps the package-owned `#1e1e1e` dark fullscreen surface as an intentional Monaco-aligned residual rather than pretending the live widget is fully tokenized.
- verification: focused `vitest` targets plus workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed during this plan closure pass.

### 2026-05-08

- advanced `docs/plans/226-accessibility-follow-up-plan.md` with live retained accessibility fixes across `packages/{flux-renderers-data,spreadsheet-renderers,flux-renderers-form,flux-renderers-form-advanced,flow-designer-renderers,word-editor-renderers,nop-debugger,ui}`.
- keyboard repairs landed for table row activation, chart canvas activation, spreadsheet corner/column headers, and flow inspector branch-card selection; label/association repairs landed for pagination page-size select, spreadsheet find/cell editor labels, condition-builder between inputs and selected-value badge removal, ArrayEditor/KeyValue child error wiring, checkbox-group source errors, table loading fallback text, debugger JSON toggles, word-editor dialogs, and carousel region naming.
- focused proof passed: `pnpm exec vitest run src/__tests__/table-internal-components.test.tsx src/__tests__/chart-renderer.unit.test.tsx` in `packages/flux-renderers-data`; `pnpm exec vitest run src/__tests__/grid-selection.test.tsx` in `packages/spreadsheet-renderers`; `pnpm exec vitest run src/designer-controls.test.tsx` in `packages/flow-designer-renderers`; `pnpm exec vitest run src/panel.test.tsx` in `packages/nop-debugger`; `pnpm exec vitest run src/__tests__/input-source-state.test.tsx` in `packages/flux-renderers-form`; `pnpm exec vitest run src/__tests__/form-array-validation.test.tsx src/condition-builder/value-input.a11y.test.tsx` in `packages/flux-renderers-form-advanced`; `pnpm exec vitest run src/__tests__/dialog-accessibility.test.tsx src/__tests__/expr-insert-dialog.test.tsx` in `packages/word-editor-renderers`.
- owner-doc decision: `No owner-doc update required` for the landed slice because the stable baseline changed only through DOM semantics inside existing widget contracts.
- residual owner decision: the retained report-designer non-drag alternative is still blocked in live code because the field panel only starts drag state and the canvas only consumes drop state; no existing keyboard-command path can target `report-designer:dropFieldToTarget` from the field list without extending renderer/host plumbing.

- continued `docs/plans/223-reactive-and-async-follow-up-closure-plan.md` closure work across `packages/flux-{core,runtime,react,renderers-basic,renderers-data}` plus retained host consumers in `packages/{flow-designer-core,report-designer-renderers,flux-renderers-form-advanced}`.
- implemented narrower reactive subscriptions: `packages/flux-react/src/{hook-subscriptions.ts,hooks.ts}` now support path-scoped `useScopeSelector(...)`, `useDataSourceStatus(...)` subscribes only to its status path, and `useCurrentFormModelGeneration()` uses the new optional `subscribeToModelGeneration()` contract added through `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/{form-runtime.ts,form-runtime-owner-lifecycle.ts,form-runtime-types.ts,runtime-owned-factories.ts}`, and projected form/validation runtimes.
- tightened controlled/runtime honesty and stale-state handling: `packages/flux-renderers-data/src/table-renderer/{use-table-sort.ts,use-table-filter.ts}` now honor controlled inputs; `packages/flux-renderers-basic/src/dynamic-renderer.tsx` clears stale loaded schema while a new load is in flight; `packages/flow-designer-core/src/{core.ts,core/shell-controls.ts}` keeps host replace from creating dirty/history user-edit state.
- hardened declarative surface behavior in `packages/flux-renderers-basic/src/use-surface-renderer.ts`: close/onClose handling is once-only across local close plus unmount cleanup, cleanup churn is stabilized, and declarative status tests now target the active dialog instance instead of stale portal nodes.
- focused proof passed: `pnpm exec vitest run src/__tests__/scope-and-reactivity.test.tsx src/__tests__/data-source-and-node-identity.test.tsx` in `packages/flux-react`; `pnpm exec vitest run src/__tests__/use-table-controls.test.tsx` in `packages/flux-renderers-data`; `pnpm exec vitest run src/__tests__/core-graph.test.ts` in `packages/flow-designer-core`; `pnpm exec vitest run src/__tests__/basic-dynamic-renderer.test.tsx src/__tests__/basic-page-layout.test.tsx` in `packages/flux-renderers-basic`.
- package verification status: `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, and `@nop-chaos/flux-renderers-data` now pass `typecheck`; `@nop-chaos/flux-react`, `@nop-chaos/flux-runtime`, and `@nop-chaos/flux-renderers-data` also pass `build` + `lint`; `@nop-chaos/flux-renderers-basic` was brought to passing focused tests, `typecheck`, and `lint`, with final build verification rerun after this log update.

### 2026-05-08

- substantially completed the plan-owned validation follow-up slice from `docs/plans/224-validation-subtree-follow-up-plan.md`: hidden parent transitions now invalidate descendant async validation and cascade descendant `clearValueWhenHidden`; owner-local external errors now survive ordinary validation overlay, remap with array mutations, and clear through `applyChangesAndRevalidate(...)`; `refreshCompiledModel(...)` clears `hiddenFields`; submit snapshots active child contracts per attempt; projected `applyChangesAndRevalidate(...)` now prefixes writes as well as `changedPaths`.
- code/doc paths: `packages/flux-runtime/src/{form-runtime-validation.ts,form-runtime-owner.ts,form-runtime-owner-external-errors.ts,form-runtime-array.ts,form-runtime-field-ops.ts,form-runtime-owner-lifecycle.ts,form-runtime-submit-flow.ts}`, `packages/flux-renderers-form-advanced/src/detail-view/{projected-form-runtime.ts,projected-validation-runtime.ts}`, `docs/architecture/form-validation.md`.
- focused proof: exact changed-file Vitest runs passed for `packages/flux-runtime/src/__tests__/{hidden-field-policy.test.ts,plan-68-69-remaining-behaviors.test.ts,form-runtime-array.test.ts,form-runtime-owner-lifecycle.test.ts,form-runtime-submit-flow.test.ts,runtime-dialogs-scope.test.ts,action-adapter.unit.test.ts}` and `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.test.ts`.
- validation verification follow-up: workspace `pnpm typecheck` and `pnpm build` now pass; workspace `pnpm lint` is blocked by an unrelated pre-existing `@typescript-eslint/no-unused-vars` failure at `packages/spreadsheet-core/src/core/cell-operations.ts:9`, and workspace `pnpm test` is blocked by unrelated failures in `packages/flow-designer-renderers/src/{designer-command-adapter.test.ts,designer-page.tree.test.tsx}`. `docs/plans/224-validation-subtree-follow-up-plan.md` now reflects partial completion instead of the stale all-planned state.

- 收口 `docs/plans/227-safety-and-performance-redlines-plan.md` 的主体实现：`packages/flux-core/src/value-adapter.ts` 在 transform failure 时抛出明确错误；`packages/ui/src/components/ui/chart.tsx` 对 chart id / CSS custom property key / color 值做白名单清洗；`packages/flux-runtime/src/async-data/api-cache.ts` 为 `stableStringify` 增加 cycle/depth/node-budget guards；`packages/flux-{compiler,runtime}/src/{validation-lowering.ts,validation/validators.ts}` 把 invalid/unsafe pattern 从 fail-open 改为 explicit validation error；`packages/spreadsheet-core/src/core/{document-access.ts,cell-operations.ts,search-operations.ts}` 为 batch cell writes 改成单次 cells clone，并拒绝 empty query / unsafe regex；`packages/flow-designer-core/src/tree-layout.ts` 去掉 retained `queue.shift` / repeated merge-target scan 热点。
- Focused proof: 新增/更新 `packages/{flux-core,ui,flux-compiler,flux-runtime,spreadsheet-core,flow-designer-core}/src/**/*.test.ts*` 中对应 redline 回归用例。
- Owner-doc decision: `docs/architecture/form-validation.md` 已同步 regex contract 变更；其余本次 hardening 属于 internal baseline tightening，`No owner-doc update required`.

- 推进 `docs/plans/229-async-lifecycle-and-error-integrity-plan.md` 的 retained closure。落地 same-tick async guards：`packages/flow-designer-renderers/src/designer-page-body.tsx` 为 create confirm 增加 ref latch，`packages/flux-renderers-data/src/table-renderer/table-quick-edit-controller.ts` 为 quick-edit save 增加 ref latch；`packages/report-designer-core/src/{adapters.ts,runtime/field-sources.ts,core.ts}` 为 field-source refresh 传递 `AbortSignal`；`packages/flux-react/src/{schema-renderer.tsx,use-node-scopes.ts}` 补上 locally owned registries dispose；`packages/flux-runtime/src/{runtime-action-helpers.ts,async-data/reaction-runtime.ts,action-adapter.ts}` 修正 `ok:false` / cause fidelity；`packages/flux-formula/src/compile/formula-compiler.ts` 去掉 template false-success；`packages/flux-compiler/src/schema-compiler.ts` 关闭 continueOnError unknown-renderer residual；`packages/flux-code-editor/src/source-resolvers.ts` 保留 resolver 原始错误对象。
- Focused proof: 新增/更新 `packages/{flow-designer-renderers,flux-renderers-data,report-designer-core,flux-react,flux-runtime,flux-formula,flux-compiler}/src/**/*test*`，覆盖 quick-edit same-tick guard、field-source signal handoff、registry dispose、reaction/validation `ok:false`、submitForm registry resolve cause、compileTemplate throw、compile continueOnError unknown-renderer diagnostics。
- Key decision: report designer field-source provider 的 live contract 现在支持 `load(context, { signal })` cancellation；这是代码层稳定契约扩展，但当前没有单独 owner-doc，暂记 `No owner-doc update required`。
- Verification: focused `pnpm vitest` 目标集通过；workspace verification pending below.

- 推进 `docs/plans/222-large-file-and-owner-boundary-successor-plan.md` 的落地与收尾。完成 retained owner-hotspot 拆分：`packages/flux-compiler/src/schema-compiler.ts` 拆到 `schema-compiler/{node-compiler.ts,validation-compiler.ts}`；`packages/word-editor-renderers/src/word-editor-page.tsx` 拆到 `src/hooks/{use-word-editor-state.ts,use-word-editor-save.ts,use-word-editor-actions.ts}`；`packages/flow-designer-renderers/src/designer-page.tsx` 拆到 `designer-{page-body,page-inner,tree-mode}.tsx`；`packages/flow-designer-renderers/src/designer-xyflow-canvas/designer-xyflow-canvas.tsx` 拆到 `use-{minimap-navigation,xyflow-sync,xyflow-interactions}.ts`；`packages/flux-react/src/{hooks.ts,node-renderer.tsx}` 分别拆到 `hooks/use-form-hooks.ts` 与 `node-renderer-resolved.tsx`。
- 收口 module-boundary defects：`packages/spreadsheet-renderers/src/index.ts` 去掉 root CSS side effect，显式样式入口改为 `packages/spreadsheet-renderers/src/renderers.tsx`、`packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`、`apps/playground/src/pages/report-designer-demo.tsx`；`packages/flux-i18n/src/i18n.ts` 增加 `initFluxI18n({ react?: boolean })` headless 开关；`packages/word-editor-core/src/canvas-editor-types.ts` 引入 package-owned canvas-editor type aliases，并同步 `src/{index.ts,canvas-editor-bridge.ts,template-model.ts}` 及下游 word-editor consumers。
- 代码/文档路径: `packages/{flux-compiler,word-editor-renderers,flow-designer-renderers,flux-react,spreadsheet-renderers,report-designer-renderers,flux-i18n,word-editor-core,flux-renderers-data}/src/**`, `vite.workspace-alias.ts`, `tsconfig.base.json`, `docs/plans/222-large-file-and-owner-boundary-successor-plan.md`.
- Key decision: `spreadsheet-renderers` 的 `canvas-styles.css` 不再通过 root barrel 隐式加载；chart UI contract 继续只通过 `@nop-chaos/ui/chart` subpath 消费，相关测试也必须 mock 这个 subpath，而不是旧的 `@nop-chaos/ui` root barrel。
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint` 全部通过；`pnpm --filter @nop-chaos/flux-renderers-data test -- --run src/__tests__/chart-renderer.unit.test.tsx` 通过；全量 `pnpm test` 在修复 `packages/flux-renderers-data/src/__tests__/chart-renderer.unit.test.tsx` 的 chart mock 后，再次只剩 `packages/flux-renderers-form-advanced/src/detail-view/{detail-view-owner-updates.test.tsx,detail-view.test.tsx,detail-view-transform.test.tsx}` 中 10 个 pre-existing failures。
- Next step: 对 Plan 222 做独立 closure audit，并决定在 workspace 仍有非本计划 owner 的 pre-existing `detail-view` 失败时，是否把计划状态从 `partially completed` 收口为 `completed`，或保留到相关 successor/owner 处理完测试 baseline 为止。
