# Development Log - 2026-06-23

### 2026-06-23 (移动端渲染器 UX/a11y/样式卫生 — 执行，plan 3 收口 MA-05/06/07/10/20-other/21/22/23/24 + OA-04/07/08/09/12)

- **`flux-renderers-mobile` 的可达性、几何正确性、手势所有权与样式系统卫生全部收敛**，Plan Status → completed。Plan: `docs/plans/2026-06-23-0655-3-mobile-ux-a11y-and-styling-hygiene-plan.md`（4 Phase，`必须自动化` tier — Proof 先于 Fix）。在 plan 1（状态机）+ plan 2（契约/markers）之后，于干净 data-slot 基线上做交互层打磨。15 条 in-scope finding 全部修复且有 focused 程序化验证（getBoundingClientRect / getComputedStyle / 焦点断言，无截图诊断）。M5 audit remediation 三 plan 全部收口。
- **Phase 1（a11y + 几何，最高 urgency）** — OA-08 / OA-04 / OA-09 / OA-12：
  - **OA-08**：`swipe-cell.tsx` 非 open 侧的左/右 wrapper 设 `inert`（React 19 boolean prop），把屏幕外 Delete/Archive 按钮移出 Tab 序列与可访问性树；`overflow:hidden` 只裁绘制不足以隐藏 AT/焦点。Proof：closed 态两侧 `inert` + open-left 时左清右留。
  - **OA-04**：`notice-bar.tsx` 角色按用途分流——绑 `onClick` 时 `role="button"`+`tabIndex{0}`+Enter/Space 激活；未绑时 `role="status"`（advisory，不聚焦）。删 `role="alert"` 的 assertive 误用。Proof ×2。
  - **OA-09**：`pull-refresh.tsx` indicator 改 `position:absolute; transform:translateY(-100%)` 出文档流，body 成为 translated track 唯一 in-flow 子，屏幕位移 === 根 translate === 手指位移（修复 indicator 堆叠高度造成的 ~2× 过冲）。Proof：断 indicator `position:absolute` + body 无自有 transform + 几何比例。
  - **OA-12**：`swipe-cell.tsx` 用 `ResizeObserver` 观察左/右 wrapper 重测宽度，替换只在 `[hasLeft,hasRight]` 触发的一次性 `useLayoutEffect`（region 内容/locale/异步图标变化后开位偏移不再失效）。Proof：mock ResizeObserver 驱动宽度 80→220，断开位 translateX 同步增长。
- **Phase 2（手势所有权 + 渲染期派生）** — MA-07 / MA-10：
  - **MA-07（语义修正）**：原 plan 给出 `pull-refresh=pan-y / swipe-cell=pan-x`，与 `touch-action` 语义相反（`touch-action` 命名的是**浏览器可 pan 的轴**）。e2e 证实 `pull-refresh` 带 `pan-y` 时浏览器消费垂直手势、渲染器收不到 `touchmove`、`data-status` 永停 `normal`。落地为语义正确的 `pull-refresh=pan-x`（保留垂直给渲染器 JS）+ `overscroll-behavior-y: contain`，`swipe-cell=pan-y`（保留水平给渲染器 JS、垂直交页面滚动）。`mobile-responsive-baseline.md §5` 增 touch-action 契约表 + 语义提醒。Proof：`getComputedStyle` 断两根 touch-action/overscroll。
  - **MA-10**：`pull-refresh.tsx` 删镜像 `useEffect+setStatus`，`resolvedStatus` 渲染期派生（`isBusy ? status : (state.isTouching && directionalDelta>0 ? (reachedThreshold?'loosing':'pulling') : 'normal')`），消除 touchmove(60–120Hz) 每帧双渲染。`isTouching &&` 门控防 release-without-commit 后 stale deltaY 残留 pulling/loosing（Draft Review Minor 1）。Proof：touchmove 派生 + release 回 normal 不残留 ×2。
- **Phase 3（样式系统卫生）** — MA-05 / MA-06 / MA-21 / MA-22 / MA-23 / MA-24：
  - 新增 `packages/flux-renderers-mobile/src/styles.css`（包 CSS）：迁入 `@keyframes nop-notice-bar-marquee`（**MA-05**，删 `ensureMarqueeKeyframes()` + `document.head` 注入 effect，无 cleanup/CSP/iframe 不友好）；变体配色走 token（**MA-06**，删 `VARIANT_CLASS_MAP`，`[data-slot="notice-bar"][data-variant="..."]` 选 `--nop-notice-bar-*-bg/-fg`，带 `:root` + `.dark`/`:root[data-mode='dark']` 暗色变体）。`package.json` 加 `sideEffects:["*.css"]` + `./styles.css` export + build 复制；`vite.workspace-alias.ts` + playground `styles.css` `@import` 接入。
  - **MA-22**：notice-bar 稳定布局 `flex items-center gap-2 overflow-hidden px-3 py-2` Tailwind 类，仅 `animationDuration` 留 inline。**MA-23**：countdown `tabular-nums` 类替换 inline `fontVariantNumeric`。
  - **MA-24**：swipe-cell 拖拽中（`isTouching || openState!=='closed'`）content pane 加 `select-none` + `userSelect:none`，baseline §5 增条款。
  - **MA-21**：notice-bar 测试与字面类解耦——改断 `data-variant` 协议（不再绑 `bg-amber-50` 等，token 迁移不假红）。
- **Phase 4（测试盲区 + playground demo）** — MA-20 / OA-07：
  - **MA-20 marquee**：notice-bar 测试用 `vi.spyOn(HTMLElement.prototype,'scrollWidth'/'clientWidth','get')` mock 溢出，覆盖 `shouldScroll=true` true-branch（happy-dom 默认返回 0 不可达）。
  - **MA-20 e2e**：`tests/e2e/mobile-components.spec.ts` pull-refresh 状态白名单收紧为 `[/pulling|loosing|loading/]`（删 `'normal'`，原白名单等同 no-op）；infinite-scroll 状态正则（`:84`）不动。
  - **OA-07**：`mobile-components-demo.tsx` 把静态 `hasMore:true,loading:false` 的 infinite-scroll 换成 `InfiniteScrollDemoHost` React host（渲染真实 `InfiniteScrollRenderer`，可控 `loading/hasMore/items/page`，setTimeout 模拟异步，3 页后 `hasMore:false` 停止，消除失控循环）。**附带修复 pre-existing 缺陷**：demo 构建 `registry` 却未透传给 `SchemaRenderer`（对比 `m4-data-display-demo.tsx:133`），导致 schema 驱动的四个移动端渲染器整页不渲染；补 `registry={registry}`。e2e 还补 `hasTouch:true`（`touchscreen.tap`/触控合成前置）+ 真实 press-move-release；当前 Playwright/Chromium 无头环境无法合成触控 drag，pull-refresh drag 用例诚实 `test.skip`（状态机由单测覆盖，触控合成属 Non-Blocking Follow-up）。
- **owner-doc 同步**：`mobile-responsive-baseline.md §5`（touch-action 契约表 + 语义提醒 + select-none 条款）；`notice-bar/design.md` §6（role=status/button + token 变体 + 包 CSS keyframes，删 role=alert 与 `bg-*-50` 字面量）；`swipe-cell/design.md`（inert a11y + pan-y + select-none + ResizeObserver 测量）；`pull-refresh/design.md` §5（1:1 几何 + 渲染期派生 + pan-x）。
- **验证**：`pnpm --filter @nop-chaos/flux-renderers-mobile test` 119→128 passed；`typecheck`/`lint`/`build`（含 `dist/styles.css` 复制）通过；仓库级 `pnpm typecheck` 51/51、`pnpm lint` 27/27、`pnpm build` 27/27、`pnpm test` 51/51；`npx playwright test mobile-components` 7 passed / 1 honest-skip。独立 fresh-session closure audit `approved`（round 1 唯一 issue 为 notice-bar design.md doc-only 残留，已同步修复）。

### 2026-06-23 (移动端渲染器契约诚实性 + markers 门禁 — 执行，plan 2 收口 MA-03/04/08/09/11/17/18/19/25 + OA-01/02/03/06/11)

- **`flux-renderers-mobile` 的“schema 类型层 ↔ field-rule 层 ↔ design.md 层 ↔ 运行时实现”四方收敛到一致，markers 契约门禁锁死回退**，Plan Status → completed。Plan: `docs/plans/2026-06-23-0655-2-mobile-contract-honesty-and-markers-gating-plan.md`（4 Phase，`必须自动化` tier — Proof 先于 Fix）。在 plan 1 稳住状态机之后、plan 3 改 a11y/样式之前，先收口“契约说了什么”。14 条 in-scope contract drift 全部收敛且有 focused 回归测试。
- **Phase 1（markers 契约门禁 + BEM 清理）** — MA-03 / MA-25：
  - 新增 `packages/flux-renderers-mobile/src/__tests__/mobile-markers-contract.test.tsx`，镜像 sibling 包门禁：扫描全 5 渲染器断言无 `.nop-(notice-bar|pull-refresh|infinite-scroll|swipe-cell|countdown)__*` region 类、无 `.nop-notice-bar--*` modifier，正向断言 `data-slot`/`data-status`/`data-variant` 存在（先写失败 Proof，再清理）。
  - 删除四渲染器全部死 BEM 类（共 16 region + 1 `notice-bar--variant` modifier，全仓零 CSS 命中）+ notice-bar `--variant` modifier；结构身份仅靠 `data-slot`，variant 靠 `data-variant`。
- **Phase 2（schema/field-rule/类型表面对齐）** — MA-08 / MA-17 / MA-11 / MA-18 / MA-19：
  - **MA-08**：`InfiniteScrollSchema` 补 `hasMore?/loading?/error?`（与 field-rule 一致），删 `infinite-scroll.tsx` 本地 `InfiniteScrollRuntimeProps` 与 `as` 强转。
  - **MA-17**：`NoticeBarSchema.icon?: SchemaValue` → `icon?: string`（与实现一致），清未用 import。
  - **MA-11**：`use-touch.ts` 接口 `onTouchEnd: () => void`（实现本就不取参），`pull-refresh.tsx`/`swipe-cell.tsx`/`use-touch.test.ts` 去掉 `({} as React.TouchEvent)`。
  - **MA-18**：`countdown.tsx` 给 `CountdownTimerOptions`/`CountdownTimerResult` 加 `export`，`index.ts` 增 type re-export（半导出 API 收口）。
  - **MA-19**：`package.json` 删 4 个零 import 幽灵依赖（deps `flux-react`、devDeps `flux-compiler`/`flux-formula`/`flux-runtime`），保留实际使用的 `flux-core`/`ui` 与 `flux-i18n`（见 OA-03）。
- **Phase 3（事件直通契约 MA-04）**：
  - DOM 入口转发原生事件：`notice-bar.tsx` 的 `onClick`/`onClose`（close 按钮先 `stopPropagation` 再 `props.events.onClose?.(event)`）、键盘激活转发 KeyboardEvent。
  - 语义事件传结构化 `{type,...}` 载荷：`onRefresh {type:'refresh',direction,threshold}`、`onOpen {type:'open',side}`、`onClose {type:'close',side}`、`onFinish {type:'finish'}`。
  - Proof 用例（capture 阶段读 `event.type`/`currentTarget`，避开 React 事件池 null 化）：notice-bar DOM 转发 ×2、pull-refresh/swipe-cell/countdown 语义 payload ×4。
- **Phase 4（design.md ↔ 代码契约对齐）** — OA-01/02/06/11 + OA-03 i18n：
  - **OA-11**：countdown format 单一真相源 = 实现 `DD HH mm ss SSS`；`countdown/design.md` docstring 收窄、`:92` 示例 `"ss"→"1845"` 修正为 `"45"`、`schemas.ts` format 补 docstring；Proof 增“未支持 token（YYYY/MM）字面透传”用例。
  - **OA-01**：notice-bar 多文本轮播死代码修复——`onAnimationIteration` 守卫 `!loop`（恒不可达）改 `loop`，`loop:true` 下每轮播推进 `currentIndex` 并 wrap-around；Proof 增轮播推进 + `loop:false` 不推进用例。
  - **MA-09 + OA-02**：swipe-cell `onAction` + close-after-action 实装——操作区**按钮**点击（capture-phase ref 监听，仅响应 `button/a/[role=button]` 等交互元素，不给容器加假 role，a11y 全套留 plan 3）派发 `onAction {type:'action',side}` 并自动回弹关闭；design.md §8/§11 已有契约，代码对齐。Proof ×2（左/右操作区点击派发 + 关闭）。
  - **OA-06**：useTouch preventDefault 契约收紧——`use-touch`/`swipe-cell`/`pull-refresh` design.md 改为“渲染器层不 preventDefault；手势所有权由 CSS `touch-action` 提供（见 plan 3 MA-07）”（不与 plan 3 耦合 JS preventDefault）。
  - **OA-03 i18n**：裁定引入最小 `t()` seam（保留 `flux-i18n`，默认中文资源就地）。落地调用点 `t('flux.mobile.*', { defaultValue })`（pull-refresh/infinite-scroll/notice-bar），并在 `flux-i18n` locale 包 `zh-CN.ts`/`en-US.ts` 补 `mobile` 命名空间 8 键（满足 `check-i18n-keys.mjs` 硬门禁）。
  - owner-doc 同步：notice-bar/swipe-cell design.md 的 DOM-marker 示例删 BEM 类、改 `data-slot`/`data-variant`/`data-state`，与代码一致。
- **Focused 单测（+18 case，全绿；包总计 101 → 119）**：markers 门禁 ×6、notice-bar MA-04 ×2 + OA-01 ×2、countdown OA-11 ×1、pull-refresh MA-04 ×1、swipe-cell MA-04 ×2 + MA-09/OA-02 ×2、use-touch.test 签名同步。
- **仓库级验证（执行 session，full-green）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（含 `check-i18n-keys` 硬门禁，0 errors）、`pnpm test` 51/51，全绿。
- **Closure Gates**：audit 条目由独立 fresh-session sub-agent 审计（见 plan Closure Audit Evidence 段），执行 session 不自审。

### 2026-06-23 (移动端渲染器异步与状态机正确性 — 执行，plan 1 收口 MA-01/02/12/13/14/15/16 + MA-20 子项 + OA-05/10/13)

- **`flux-renderers-mobile` 四个交互类渲染器（pull-refresh / infinite-scroll / swipe-cell / countdown）的异步链路与状态机收敛完成**，Plan Status → completed。Plan: `docs/plans/2026-06-23-0655-1-mobile-async-and-state-machine-correctness-plan.md`（3 Phase，`必须自动化` tier — Proof 先于 Fix）。这是 plan 2/3 在同批文件上做契约/UX 改动前必须先稳住的地基。in-scope 12 条 finding 全部修复且有 focused 回归测试覆盖。
- **Phase 1（pull-refresh 异步与卸载正确性）** — `packages/flux-renderers-mobile/src/pull-refresh.tsx`：
  - **MA-01**：`handleTouchEnd` 的 `void Promise.resolve(...)` 链补 `.catch(() => setStatus('normal'))`——reject 不再永久 spinner。
  - **MA-12**：引入 `isMountedRef`（mount effect 置 true、cleanup 置 false 并清 `successTimerRef`）；`.then`/`.catch` 内先判活再 `setState`——in-flight 期间卸载不再对已卸载实例写状态、不再遗留 timer。
  - **MA-02 (pull-refresh)**：`onRefresh` dispatch 与 `setStatus('loading')` 副作用**移出 `setStatus` updater**，改在 handler 体里 dispatch；新增 `statusRef`（effect 同步 + handler 内同步写）镜像当前 status，保留原 `current==='loading'||'success'` re-entrancy 守卫。
  - **OA-05 (pull-refresh)**：`onTouchCancel` 与 `onTouchEnd` **拆分为两条独立路径**（不触碰 use-touch）；cancel 不提交、恢复 `normal`、不触发 onRefresh；loading/success 态的 cancel 不偷走 in-flight 刷新。
- **Phase 2（infinite-scroll in-flight guard 与 error 挂起）** — `packages/flux-renderers-mobile/src/infinite-scroll.tsx`：
  - **MA-13**：引入本地 `isLoadingRef`，`onLoadMore` 触发时同步置位、随 host `loading` prop 转换（`[loading]` effect）清除——IO 回调与 immediateCheck 在 host `loading` 延迟置位时不再双发；保守 over-lock 优于重复请求。`hasMore === false` 显式布尔语义（implicit-truthy 允许加载）写入用例。
  - **OA-10**：observer 回调 guard 增 `if (error === true || typeof error === 'string') return;`，immediateCheck effect 同步加 error guard；两个 effect dep 数组纳入 `error`（经 `hasError` 派生）——error 态完全挂起自动加载，仅 retry `<Button>` 解除。
  - **MA-14**：抽 `fireLoadMore` useCallback，三处 `onLoadMoreRef.current?.()` 统一 `try { void Promise.resolve(...).catch(...) } catch {}`——reject / sync-throw 不再崩溃渲染器（host 经 `error` prop 上报）。调用保持同步以保留原时序契约。
  - 抽 `isFinished`/`hasError` 派生布尔，observer effect 重建时 armed 正确 guard。
- **Phase 3（swipe-cell / countdown 状态机与生命周期）** — `swipe-cell.tsx` / `countdown.tsx`：
  - **MA-02 (swipe-cell)**：`closeCell`/`openCell` 的 `onClose`/`onOpen` dispatch **移出 `setOpenState` updater**；新增 `openStateRef`（effect 同步 + handler 内同步写）保留原 `current !== target` re-entrancy 守卫，快速连续调用不漏判。
  - **MA-02 (countdown)**：`onFinishRef.current()` **移出 `setRemaining` updater**，改由独立 finish-detection effect（deps `[isFinished, started, paused]`，`finishedRef` 守卫）派发一次——updater 现纯函数，StrictMode 双调无害。
  - **OA-05 (swipe-cell)**：`onTouchCancel` 拆分为独立 `handleTouchCancel = reset()`——cancel 不提交、回弹到手势前 `openState`、不触发 onOpen/onClose。
  - **OA-13 裁定 + 实现**：默认裁定 `reset()` → `remaining` 回初始 + `started=false`（停止并等待显式 `start()`），与 `autoStart:false` 语义对齐、最小惊讶（不沿用"started 恢复为 autoStart"，默认 autoStart 下会静默重启，正是所述缺陷）。`start()` 恢复计时。reset/start 行为注释写入导出接口（接口 `export` 归 plan 2 的 MA-18）。
  - **MA-16**：`targetTime` 分支 `next = Math.max(0, targetTime - Date.now())`（`computeInitialRemaining` + tick 双侧 clamp）+ finish 后经 `isFinished` dep 让 tick effect cleanup `clearInterval`——消除毫秒模式下"到期后每 30ms 重渲染"storm。
- **owner docs 最小同步**（reset/touchcancel 裁定改变公开行为）：`countdown/design.md` §6（结束即停 tick + clamp + reset 契约）、§8（autoStart=false 等 3 行措辞修订）；`swipe-cell/design.md` §8（touchcancel 不提交行）；`pull-refresh/design.md` §8（touchcancel 不提交 + onRefresh reject 回 normal 行）。其余 design.md 契约对齐归 plan 2。
- **Focused 单测（+23 case，全绿；包总计 78 → 101）**：
  - `pull-refresh.test.tsx` +6：reject 回 normal（MA-01）、卸载 in-flight 不 setState（MA-12）、StrictMode onRefresh 单派发（MA-02）、loading 态二次 touchEnd 不重派（re-entrancy）、touchCancel 不提交（OA-05）、loading 态 touchCancel 不偷走刷新。
  - `infinite-scroll.test.tsx` +8：host loading 延迟不双发（MA-13）、loading 转换释放 guard、error=true/string 挂起 + retry 恢复（OA-10）、error 态 mount 不 immediateCheck、hasMore implicit-truthy、onLoadMore reject 不崩溃（MA-14）、observer rebuild on distance/disabled/hasMore/loading/error（MA-20 子项，MockIntersectionObserver 增 `disconnected` 标记）。
  - `swipe-cell.test.tsx` +3：StrictMode onOpen 单派发（MA-02）、touchCancel 不提交留 closed（OA-05）、open 态 touchCancel 不误触发 onClose。
  - `countdown.test.tsx` +6：targetTime ss 严格断言 `30`（MA-15）、millisecond 30ms 粒度（MA-15）、elapsed targetTime clamp 0 + onFinish 一次（MA-16）、`useCountdownTimer` harness 验 reset 停/start 续（OA-13）、targetTime 分支 finish 后 commit 计数稳定（MA-16，**经 revert 验证为真回归测试**：还原 clamp + isFinished 守卫后用例如期 fail）。
- **MA-16 回归测试真阳性验证**：临时同时还原 `targetTime` 不 clamp + 移除 `if (isFinished) return` tick guard → `does not keep re-rendering after finish` 用例如期 fail（commit 数 3 ≠ 2），恢复 fix 后 pass。确认该用例真正锁住 storm 根因（clamp + clearInterval 任一缺失即被原 bug 双重路径绕过，故测试须同时覆盖两者）。
- **仓库级验证（执行 session，full-green）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（0 errors）、`pnpm test` 51/51，全绿。
- **Closure Gates**：9 项中 8 项 `[x]`（defect 修复/无静默降级/focused verification/design.md 同步/typecheck/build/lint/test）。**唯一 `[ ]`：第 5 条独立 fresh-session closure-audit**——`AGENTS.md` Collaboration Discipline 明确「执行 session 不得自审勾选本项」，已 spawn 独立 fresh-session sub-agent 审计，证据见 plan Closure Audit Evidence 段。

### 2026-06-23 (M4 数据展示响应式 — 执行，M4a/M4c 收口；M4b 延后 W1c/W2a)

- **M4 数据展示响应式（crud + chart；cards/list 延后）执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-23-0410-2-m4-data-display-responsive-plan.md`（2 Phase，`建议有测` tier）。Mission: mobile，Work Item: M4。M4a（crud）+ M4c（chart）收口；M4b（cards/list）因 renderer 未落地显式延后主 roadmap W1c/W2a successor。
- **Phase 1（M4a crud 小屏响应式）**：
  - `packages/flux-renderers-data/src/crud-renderer.tsx` 消费 `useIsMobile()`；新增 `resolveToolbarBlocks(blocks, mode)` 复用既有 headerBlocks 过滤机制——mobile 隐藏 `switch-per-page`（infinite 模式的 pagination/switch-per-page 过滤优先级不变，mobile 过滤与之叠加）；`.nop-crud` 根增 `data-responsive="narrow"` marker。
  - `crud-renderer-toolbar.tsx` `CrudToolbarBlocks` 消费 `useIsMobile()`（hook 调用置于早返回之前，rules-of-hooks），小屏 layout 由 `flex-wrap items-center justify-between` 切到 `flex-col items-stretch`（header/footer 双侧块均纵列堆叠）；`[data-slot="<slot>-toolbar-layout"]` 增 `data-responsive="narrow"`。
  - `crud-query-region.tsx` `CrudQueryRegion` 新增 `isMobile?` prop，初始 `collapsed = isMobile || config.defaultCollapsed`（小屏即便 `defaultCollapsed:false` 仍强制折叠；用户可手动展开；未启用 `filterTogglable` 的查询区不受影响）。
  - **保留**：listActions / statistics / pagination（上一页-下一页+页码指示）/ polling-toggle；**隐藏**：switch-per-page。pagination 无 jumper，故「页码简化」即保留最简形态。缺省（桌面）行为完全不变。
- **Phase 2（M4c chart 小屏自适应 + 收口）**：
  - `packages/flux-renderers-data/src/chart-renderer.tsx` 用 `ResizeObserver` 测 `chart-canvas` 容器宽度（**优先容器宽度**，不用 `useIsMobile` 视口宽度——chart 常嵌非满宽容器）：`containerWidth < 768` → 数值型 `height` clamp 到 `Math.min(height, 300)`；图例 `ChartLegendContent` 增 `flex-wrap gap-x-3 gap-y-1`（多项换行而非挤压绘图区）；字符串型 height 原样透传。`ResizeObserver` 缺席（`typeof === 'undefined'`）或空态（canvas 不渲染）时 effect 早返回，`containerWidth=null` → 固定 height 缺省 400px（**无报错**，Failure Path）。`.nop-chart` 根增 `data-responsive="narrow"` + `data-responsive-supported="true"` marker。
  - playground：`apps/playground/src/pages/m4-data-display-demo.tsx`（路由 `/m4-data`，crud + chart 双示例，经 `App.tsx` + `route-model.ts` 注册 + DOMAIN_RENDERER_ROUTES 增条目）。
  - e2e：`tests/e2e/m4-data.spec.ts`（移动/桌面双视口 `test.use({viewport})` + 程序化断言：crud `data-responsive`/switch-per-page 隐藏/查询 `data-collapsed`/toolbar `flex-col`、chart `data-responsive` + `style.height` 300px vs 400px，**不靠截图**）。
  - **M0.1 deferred 收口**：M0.1 源 plan「业务 renderer border→hairline 迁移归 M4b」段加 `Closure Note (2026-06-23)`——cards/list/cell 未落地，随组件转 W1c/W2a successor。
  - **附带修复 playground-entry-pages.spec.ts 既有不一致**：该 spec 的 `domain route coverage matches playground route inventory` 严格相等断言此前因 m2-touch/m3-layout 缺 assertion 而失败；补 m2-touch/m3-touch/m4-data 三条 assertion 使其恢复一致（m4-data 为本 plan 必需）。
- **根因修复（跨包）**：`packages/ui/src/hooks/use-mobile.ts` 由 `useState(undefined)` + effect 内 `setIsMobile` 改为 `useState(readIsMobile)` 懒初始化——消除所有消费者的 `undefined→value` 多余重渲染。该多余重渲染此前打破 `crud-query-and-pagination.test.tsx`「notifies when query submit capability rejects」的 component-handle spy 时序（re-render 触发 query form 重新注册未 spy 的新 handle）。仓库无 SSR（`renderToString`/`hydrateRoot` grep 无命中），懒初始化安全且更优（桌面无 mobile 闪烁、移动端无首帧桌面假象）。消费者（dialog-host/page/button/select/tree/input-checkbox 等）单测全绿确认无回归。
- **owner docs 同步**：`crud/design.md` §14 响应式行为（toolbar 简化/查询折叠/DOM marker/触摸适配表）；`chart/design.md` §13 响应式行为（高度自适应/图例位置/ResizeObserver 缺席回退 Failure Path/DOM marker）。
- **roadmap 同步**：`docs/components/mobile-roadmap.md` —— M4 Phase Status `todo`→`done`（M4a/M4c ✅，M4b 延后 W1c/W2a），Current Baseline 表 + M4 work item 表 + blockquote 执行说明，Last Updated → 2026-06-23（M4a/M4c 收口）。
- **Focused 单测（13 case，全绿）**：`crud-responsive.test.tsx`（8：toolbar switch-per-page 桌面可见/移动隐藏、layout 纵列/横排、查询区桌面展开/移动强制折叠/可手动展开、infinite 模式回归）、`chart-responsive.test.tsx`（5：宽屏 authored height + 无 narrow、窄屏 height clamp 300 + 图例 flex-wrap、窄屏小 height 不放大、ResizeObserver 缺席回退 400 无报错、字符串 height 透传）。均用 `vi.mock`/`vi.stubGlobal('ResizeObserver', Fake)` 控制分支。
- **仓库级验证（执行 session）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27、`pnpm test` 51/51 全绿。e2e `pnpm test:e2e`（含 m4-data.spec.ts）待独立审计/CI 浏览器环境实跑确认。
- **Closure Gates**：11 项中 10 项 `[x]`（M4a/M4c 落地无回归、design.md 同步、M0.1 successor 注记、无静默降级 defect、typecheck/build/lint/test）。**唯一 `[ ]`：第 6 条独立 fresh-session closure-audit**——gate 文本与 `AGENTS.md` Collaboration Discipline 明确「执行 session 不得自审勾选本项」，需独立 fresh-session 审计后补勾。

### 2026-06-23 (M3 容器与布局响应式 — 执行 + closure audit，plan 收口)

- **M3 容器与布局响应式（page / flex / container；grid 延后）执行完成，独立 fresh-session closure audit `approved`（2 轮：首轮 1 Blocker + 2 Major，二轮 1 Major doc inconsistency，三轮 narrow re-audit 全绿），Plan Status → completed。** Plan: `docs/plans/2026-06-23-0410-1-m3-container-and-layout-responsive-plan.md`（3 Phase，`建议有测` tier）。Mission: mobile，Work Item: M3。
- **Phase 1（M3a page 响应式）**：`packages/flux-renderers-basic/src/page.tsx` 消费 `useIsMobile()` —— (1) aside 小屏折叠为触发式 Sheet 滑出（`data-slot="page-aside-toggle"` + `@nop-chaos/ui` Sheet，side=`asidePosition`，**非纯 hidden**）；(2) toolbar 小屏 `flex flex-col` 纵列堆叠；(3) footer fixed VisualViewport hook（新文件 `packages/flux-renderers-basic/src/use-fixed-footer-visual-viewport.ts`），mobile + `footerClassName.includes('fixed')` + 软键盘弹起时注入 `style.bottom: ${offset}px` + `data-keyboard-offset` marker；无 `window.visualViewport` 早返回（Failure Path）。收口 M2 plan + M0.1 plan 路由到 M3a 的 VisualViewport deferred 项。
- **Phase 2（M3a §14 骨架模式 playground + e2e + deferred 收口）**：`apps/playground/src/pages/m3-layout-demo.tsx`（路由 `/m3-layout`，5 类骨架模板各独立 SchemaRenderer，env.navigate → toast 可观测），`tests/e2e/m3-layout.spec.ts`（7 用例：移动视口下 Tabbar navigate toast、NavBar back、ActionBar/SubmitBar/Sticky 结构、VisualViewport `data-keyboard-offset` 注入、桌面无 mobile-only artifact）。**§14 模板修正**（Decision）：`flex.direction: "col"` → `"column"`、`button.variant: "solid"` → `"default"`、`fixed bottom-0 ...` 提升至 `footerClassName`（VisualViewport hook 检测点，非 inner flex）、NavBar `sticky top-0 ...` 提升至 `toolbarClassName`（非 `headerClassName`——`page-header` wrapper 仅在 title/subTitle/remark/mobile-aside-toggle 时渲染，无 title 的 NavBar schema 走 `page-toolbar`）。M1/M2/M0.1 源 plan Deferred 段加 `Closure Note (2026-06-23)`。
- **Phase 3（M3b flex/container 断点响应式）**：`packages/flux-renderers-basic/src/schemas.ts` 新增 `ResponsiveBreakpoint`/`ResponsiveFlexDirection`/`ResponsiveContainerDirection`/`ResponsiveWrap` 类型，`FlexSchema`/`ContainerSchema` 加 `responsiveDirection?` / `responsiveWrap?` 字段；`packages/flux-renderers-basic/src/utils.ts` 加 `resolveResponsiveDirection` / `resolveResponsiveWrap`（输出 `sm:` / `md:` / `lg:` / `xl:` / `2xl:` Tailwind 类前缀）；`flex.tsx` + `container.tsx` 消费，container `useFlexChild` 扩 `hasResponsive` 触发 flex body。缺省（无字段）输出完全不变（无回归）。
- **owner docs 同步**：`page/design.md` §13 重写（断点表对齐 live `useIsMobile()` 阈值 768px、aside Decision「折叠为触发式 Sheet 滑出」非纯 hidden、新增「软键盘 VisualViewport 处理（footer fixed 栏）」小节）；§14.1-§14.4 模板按上述修正；`flex/design.md` 新增 §14 响应式行为小节；`container/design.md` 新增 §16 响应式行为小节（与 flex 共享 per-breakpoint 字段约定）。
- **roadmap 同步**：`docs/components/mobile-roadmap.md` —— M3 Phase Status 标 `done`，Current Baseline 表更新（M3 done 含 grid 延后 W3a successor），M3 work item 表 M3a/M3b → ✅，Last Updated → 2026-06-23。
- **Focused 单测（23 case）**：`page-responsive.test.tsx`（11：aside 内联 vs mobile toggle、toolbar 堆叠、VisualViewport 早返回 + offset 注入）、`flex-responsive.test.tsx`（12：flex + container 的 direction/wrap 断点切换 + 缺省回退 + 共享断点约定）。均采用 `vi.mock('@nop-chaos/ui', ... useIsMobile)` 控制 mobile/desktop 分支。
- **Closure audit 证据**：独立 fresh-session subagent `ses_10eefec68ffet5HHhTo5SuuNtR`（首轮，发现 BLOCKER：`/m3-layout` 未传 `registry` prop 导致渲染空；MAJOR：VisualViewport e2e 替换 vv 而非原地 mutate；MAJOR：fixed 提升至 footerClassName）→ 修复 → `ses_10ee1dc6dffe44r7Oeqaea2oDH`（二轮，runtime/e2e 全绿，但发现 §14.2 NavBar `headerClassName` vs `toolbarClassName` doc 不一致 MAJOR）→ 修复 → `ses_10edec846ffeBhm4bOdrZb2IGX`（narrow re-audit，`approved`，关闭 plan）。**3 轮 audit 全部由独立 fresh-session subagent 完成**，执行 session 不自审。
- **仓库级验证（执行 session + audit session 双重确认）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（1 pre-existing TanStack Virtual warning in flux-renderers-form-advanced 非 M3）、`pnpm test` 51/51、`npx playwright test tests/e2e/m3-layout.spec.ts` 7/7，全绿（**full-green verification**：unit + e2e 均通过）。
- **Closure 裁定**：grid 响应式延后主 roadmap W3a successor（grid renderer 未落地，非 M3 结果面内 defect）；container query 体系化（baseline §7 标「未来」）+ page aside 折叠态增强交互（可拖拽宽度）归 Non-Blocking Follow-ups。无 in-scope live defect 被静默降级。Plan Status `active`→`completed`，11 项 Closure Gates 全 `[x]`。

### 2026-06-23 (M2 表单控件触摸适配 — 执行 + closure audit，plan 收口)

- **M2 表单控件触摸适配（input / textarea / input-number / checkbox / radio / switch / button）执行完成，独立 fresh-session closure audit `approved`，Plan Status → completed。** Plan: `docs/plans/2026-06-22-2335-2-m2-form-controls-touch-adaptation-plan.md`（4 Phase，`建议有测` tier）。Mission: mobile，Work Item: M2。
- **新增共享工具** `packages/flux-renderers-form/src/renderers/mobile-touch-utils.ts`：`resolveInputMode()`（inputType→inputmode 映射 email/tel/search/url + decimal）、`scrollRefIntoViewOnMobile()`（mobile-only scrollIntoView）、`shouldStackChoicesVertically()` + `MOBILE_CHOICE_STACK_THRESHOLD=3`、`InputModeValue` 类型（对齐 React inputmode union）。
- **M2a（input/textarea/input-number）代码落地**：
  - `packages/flux-renderers-form/src/schemas.ts` — `InputSchema` + `InputNumberSchema` 加 `inputMode?: string`。
  - `input.tsx` — `useIsMobile` + `resolveInputMode(inputType, props.props.inputMode)` 透传 `inputMode` 到 sharedInputProps + `scrollRefIntoViewOnMobile` 于 onFocus；`inputEnhancementFieldRules` 注册 `{ key: 'inputMode', kind: 'prop' }`。
  - `textarea-renderer.tsx` / `input-number-renderer.tsx` — `useIsMobile` + mobile-only scrollIntoView；input-number 默认 `inputMode='decimal'`（schema 可覆盖）。
  - font-size ≥16px 防 iOS 缩放：**复用** `@nop-chaos/ui` `Input`/`Textarea` 已有 `text-base md:text-sm`（mobile=16px），非新增 class。
- **M2b（checkbox/radio/switch）代码落地**：`input-choice-renderers.tsx` CheckboxRenderer/SwitchRenderer/RadioGroupRenderer + `checkbox-group-renderer.tsx` — `useIsMobile`；item Label 条件加 `min-h-11 py-2`（mobile，min-height 44px 优先于 size height）+ 无条件 `nop-haptic`；`shouldStackChoicesVertically(isMobile, options.length)` 为真时 wrapper/RadioGroup 加 `flex flex-col gap-1` + `data-mobile-stack="true"` marker。
- **M2c（button）代码落地**：`packages/flux-renderers-basic/src/button.tsx` — `useIsMobile`；`MOBILE_TOUCH_TARGET_SIZES=['default','lg']`，mobile 且 size 属该集合时 className 加 `min-h-11`；schema `block`（`block && 'w-full'`）不变（Decision A：不 auto-block，避免 button-group 多按钮堆叠）。`nop-haptic` 由 `@nop-chaos/ui` button cva 基础串无条件提供（M0.1c）。
- **owner docs 同步**：6 份 design.md 各补响应式小节 —— `input-text/design.md` §13、`textarea/design.md` §13、`input-number/design.md` §15、`checkbox/design.md` §13（含 radio-group，radio/design.md 未单列）、`switch/design.md` §13、`button/design.md` §13。
- **playground / e2e**：`apps/playground/src/pages/m2-touch-demo.tsx`（路由 `/m2-touch`，经 `route-model.ts` + `App.tsx` 注册）；`tests/e2e/m2-touch.spec.ts`（程序化断言：inputmode、font-size、min-height、flex-col，非截图）。
- **roadmap 同步**：`docs/components/mobile-roadmap.md` Phase Status L75 + Current Baseline L23 + M2 work item 表 L155-157 均标 `done`（M2a~M2c → ✅）。
- **Focused 单测（30 case，全绿）**：`input-touch-adaptation.test.tsx`（14）、`choice-touch-adaptation.test.tsx`（9）、`button-touch-adaptation.test.tsx`（7）。均采用 `vi.mock('@nop-chaos/ui', ... useIsMobile)` 控制 mobile/desktop 分支。
- **仓库级验证（执行 session）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（0 errors，1 pre-existing warning in select-combobox-lists.ts useVirtualizer 非 M2）、`pnpm test` 51/51，全绿。
- **Closure audit**：独立 fresh-session subagent `ses_10f14948bffeAuvE0516c8r3vP` verdict `approved`（Phase exit criteria 全 PASS，3 个不可自审 Closure Gates 全 PASS，focused 单测独立 re-run 30/30 PASS，interface-vs-semantics 抽查 text-base=16px/min-h-11=44px/flex-col 真实生效）。e2e 受 pre-existing dev server SchemaRenderer 渲染问题阻塞（与 M1 plan `m1-responsive.spec.ts` 先例一致：静态 heading 渲染、schema body 不渲染；clean repo mobile-components smoke 同样失败），focused 单测为主验证路径。
- **Closure 裁定**：无 in-scope live defect 被静默降级；两项 deferred（page footer VisualViewport→M3a、input suggest mobile→M1a 延伸）均为合法 out-of-scope improvement。Plan Status `active`→`completed`，14 项 Closure Gates 全 `[x]`。

### 2026-06-23 (M1 高频交互控件响应式 closure audit — plan 收口)

- **M1 高频交互控件响应式（select / tree-select / table / dialog / drawer / tabs）closure audit 通过，Plan Status → completed。** Plan: `docs/plans/2026-06-22-2335-1-m1-high-frequency-controls-responsive-plan.md`（5 Phase，`建议有测` tier）。本日由独立 fresh-session closure audit subagent（CLOSURE_AUDIT step，不复用执行 session 上下文）对照 live repo 逐项核对全部 5 Phase + Closure Gates。
- **代码落地核对（对照 live repo，非信任 checklist）**：
  - M1a：`packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx:236` + `packages/flux-renderers-form-advanced/src/tree-controls.tsx:182` 消费 `useIsMobile()`，mobile 走 `@nop-chaos/ui` Sheet（side=bottom）。
  - M1c：`packages/flux-react/src/dialog-host.tsx:189,367`（DialogView/DrawerView）消费 `useIsMobile()`；L416 drawer mobile `effectiveSide = isMobile && schemaSide !== 'bottom' ? 'bottom' : schemaSide` + `data-mobile-side-overridden` marker。
  - M1d：`packages/flux-renderers-basic/src/tabs.tsx:141,235-253,279,380-396`（swipeStateRef + 内联 `onTouchStart`/`onTouchMove`/`onTouchEnd` + `overflow-x-auto` + `nop-scrollbar-hide`，仅 mobile 注入）。
  - M1b：`packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx:653,672`（`nop-safe-bottom` + `nop-hairline--bottom`）+ `table-renderer.tsx:383`（`data-responsive-expand` marker）；裁定 A（现有 expand 满足，不做 `responsive.mode: 'card'`）。
- **Focused 单测核对**：5 个 responsive 测试文件存在（select-responsive / tree-select-responsive / dialog-host-responsive / table-responsive / tabs-responsive），断言覆盖 mobile/desktop 分支、Sheet/Popover 切换、swipe 方向/阈值/边界、drawer mobile side override、table expand marker。
- **design.md 同步核对**：6 份均含响应式小节引用 M0 baseline —— `select/design.md` §13、`tree-select/design.md` §12、`table/design.md` §13（M1b 裁定 + 视觉增强）、`dialog/design.md` §14、`drawer/design.md` §14、`tabs/design.md` §21（M1d Decision 裁定 A）。
- **roadmap 同步核对**：`docs/components/mobile-roadmap.md:23,74,138,144-147`（M1 → `done`，M1a~M1d → ✅）。
- **playground / e2e 核对**：`apps/playground/src/pages/m1-responsive-demo.tsx` + `tests/e2e/m1-responsive.spec.ts`（程序化断言，非截图）。
- **deferred 诚实性核对**：三项 deferred 均为合法 non-blocking —— bottom-sheet 拖拽/下滑关闭（optimization candidate）；surface runtime sheet-kind branch（out-of-scope improvement）；tabs sidebar 移动端（out-of-scope improvement + Successor: M3a）。e2e spec 受 pre-existing dev server SchemaRenderer 渲染问题阻塞（clean repo `mobile-components` smoke 同样失败，与本 plan 无关），focused 单测（28 case across 5 packages）作为主验证路径，属 `建议有测` 档可接受。
- **仓库级验证（本审计会话独立 re-run，full-green）**：`pnpm typecheck` = 51/51、`pnpm build` = 27/27、`pnpm lint` = 27/27、`pnpm test` = 51/51，全绿（cached green baseline）。
- **Closure 裁定**：无 in-scope live defect 被静默降级；Plan Status 由 `active` → `completed`，Last Reviewed → 2026-06-23，15 项 Closure Gates 全 `[x]`，Closure Audit Evidence 写入真实证据（live code path/行号 + 测试文件 + design.md 章节 + roadmap 行号 + 验证输出）。
