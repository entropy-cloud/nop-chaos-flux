# Development Log - 2026-06-30

### 2026-06-30 (晚) — 合并恢复被 reset 丢弃的 4 天工作（线A → master）

**背景**：一次 `git reset` 把 master 从 `529d3a69`（线A tip，6/28）倒退回 `c52a96f3`（6/24 共同祖先），丢弃了 6/24–6/28 共 114 个 commit 的 amis-bug-driven-improvements 工作。之后从 `c52a96f3` 重新分支产生线B（4 commit，含 plan-448 CRUD + skills）。本次把线A（`529d3a69`）合并回 master，融合两条线的全部工作。

**两轮独立子 agent 审核**（fresh context）：

- 第一轮 REJECT：纠正"取 529"桶整体反了（mtime 显示 master 6/29 > 529 6/27），抓出 table-renderer 重复 `const totalPages`、crud-design.md 漏判。
- 第二轮 REJECT：抓出 table-renderer TDZ（resolvedCurrentPage 在 totalPages 之前引用）、crud-renderer-state.unit.test 括号嵌套失衡。

**22 个冲突的最终解决**：

- 14 个取 master（contract-honesty 全家 + crud-renderer-state.ts + date-range.test + node-fs + 多个 docs）。
- 4 个 hand-merge：
  - `crud-renderer.tsx`：保留 auto-merge body（master loadAction + 529 listMode 两套正交特性），hunk 改为 `if (!useLoadAction) return handleRefresh()`。
  - `table-renderer.tsx`：删旧 totalPages + 把 `effectiveTotalRows`/`totalPages`(useMemo)/`resolvedCurrentPage` 依赖链整体上移到 `processedData` 之前（解决 TDZ），block1 用 529 的 `resolvedCurrentPage`（clamp 修复）+ master 的 `paginationEnabled && !serverPaged`。
  - `crud/design.md`：保留 master 的 loadAction 章节 + 529 的 listMode 载体边界章节。
  - `crud-renderer-state.unit.test.tsx`：重组 describe 结构（master 的 createCrudNormalizedSourceContext + 529 的 nullish/falsy-0 测试同 describe，createCrudEvaluationBindings 独立 describe）。
- **关键修正**：master 的 `useCrudHandle` 缺第5参数 `toggleSelection`（529 的 listMode 需要）→ 给 master 的 state 文件补上 `toggleSelection` capability。
- **关键发现**：master 的 `key-value.tsx`/`input-table-renderer.tsx`/`picker-renderer.tsx` 是**broken 的半成品重构**（用了不存在的 `toKeyValuePairs`/`EMPTY_KEY_VALUE_PAIRS`/`compatRemoveAt` 等符号，master HEAD 本就无法 typecheck）。实测后改为**这三个组件实现+测试统一取 529 版**（自洽），删除 master 引入的孤儿文件：`key-value-row.tsx`、`form-array-validation-h25-h27.test.tsx`、`h24-input-table-remove-when.test.tsx`，picker 测试取 529 版。
- lint 修复：`mall-mobile/src/layouts/TabShell.tsx` → `tab-shell.tsx`（kebab-case）。

**验证全绿**（full-green verification）：

- `pnpm typecheck`：55/55 tasks ✓
- `pnpm build`：29/29 tasks ✓
- `pnpm test`：56/56 tasks ✓（form-advanced 906 tests 全过）
- `pnpm lint`：通过 ✓
- 规模：556 文件改动，+36807 / −5118。

**教训**：

1. 审核 Finding 1 指出 master key-value 有 duplicate 特性（H25）建议取 master，但实测发现 master 源码 broken（符号不存在）——**行数/mtime/特性存在性都不如"能否真正编译"可靠**。最终以 typecheck 实测为准。
2. 两轮独立审核均发现人工分析遗漏（TDZ、括号嵌套、重复声明），证明 AGENTS.md "plan 必须独立 fresh-context 审核" 的价值。

### 2026-06-30 (早)

- **合并 `stash-pre-sync` → master（fast-forward）**：master 旧 tip `a0e1be2c` 是 `stash-pre-sync`（`4729e054` WIP: save pre-sync local changes）的直接祖先，`git merge --ff-only` 零冲突落地，56 文件 / +7244 −332 全部保留。
- **⚠️ 纠错：合并的 flux-guide 是残缺快照，已从 `529d3a69` 恢复完整版**。`stash-pre-sync` 只含 3 个 flux-guide 文件（`02-reference.md`、`flux-types/index.ts`、`flux-types/schema.d.ts`），但 reflog/fsck 显示完整版应是 22 文件，源于 dangling commit `529d3a69`（6/28 "feat: add confirm and alert + flux-guide fixes"）及其父 `d968c7a6`（"docs: add flux-guide with 22 files"）。这些 commit 因某次 `reset` 变成 unreachable。
  - 缺失的 18 个文件已用 `git checkout 529d3a69 -- flux-guide/` 全部恢复：`01-quickstart.md`、`README.md`、`design-patterns/`（cards/cascading-select/conditional/crud/custom/data-source/file-upload/form/tabs）、`mobile/`（countdown/infinite-scroll/notice-bar/pull-refresh/swipe-cell）。
  - `02-reference.md`、`index.ts`、`schema.d.ts` 三文件同步为 529d3a69 的最新版本。
- **撤销一次误判**：曾误以为 `flux-types/index.ts` 的 `import from './common'` 是 broken import 并新建了 `common.ts`。实际上原作者用的是 `flux-guide/flux-types/common.d.ts`（265 行，含 SchemaClassName/ApiSchema 联合类型/Option.children 等，比我手写的更完整地道）。已删除误加的 `common.ts`，恢复原 `common.d.ts`。
- **flux-guide 独立可编译**：新增 `flux-guide/tsconfig.json`（extends base，noEmit，Bundler 解析），`npx tsc -p flux-guide/tsconfig.json` 退出码 0。flux-guide 不在根 tsconfig references 内（根 `files: []` + references 仅指向 packages/apps）。
- **已知既有问题（与本次无关）**：`pnpm typecheck` 在 `packages/spreadsheet-renderers/src/canvas-styles.test.ts:4` 报 `TS2578 Unused '@ts-expect-error' directive`，源于 commit `1aebc41b`（合并前既有）。
- 新增/恢复文件：flux-guide 完整 22 文件 + `flux-guide/tsconfig.json`。
- 未推送：master 合并仅为本地 fast-forward + 本次恢复，未 `git push`。
