# Development Log - 2026-08-02

## Plan 2026-08-02-3 执行：ajax 校验迁移进 action definition

执行 `docs/plans/2026-08-02-3-ajax-validation-migration.md`（2 Phase 全绿）：

### Phase 1（ajax 硬编码分支迁移）

- **删除硬编码分支**：`validateActionShape`（`shape-validation-rules.ts`）中 `value.action === 'ajax'` 的 args 缺失报错 + `validateApiSchemaShape` 调用分支整体删除（grep 零命中）；`validateApiSchemaShape` 调用点仅剩定义 + source（`validateSourceShape`）+ data-source（`shape-validation-node-fields.ts`）。
- **definition 接管**：`validateBuiltInActionArgsByDefinition` 补齐 `argsRequired` 消费——ajax 缺失 args 发射 'ajax actions require args payload.'（保留迁移前语义，`schema-compiler-shape-validation-compile.test.ts:259` 锁定的 code/path 不变）；args 存在但非对象 → 不再二次发射（非对象早退），单诊断 'Action args must be an object when provided.'（收敛既有双发射：旧 'api must be an object.' 消除）。
- **约束消费**：url required/string/nonEmpty、method string、data/params object 全部由 `BUILT_IN_ACTION_DEFINITIONS.ajax` 的 fieldRules 消费（表达式字符串 `${...}` 豁免，`isDynamicallyAuthoredSchemaValue`）。
- **契约测试**（新增 `schema-compiler-ajax-validation-migration.test.ts`，+12）：args 缺失 / url 缺失 / url 非字符串 / url 空串 / 非对象单诊断（断言恰好一条）/ 表达式豁免（`url: '${apiUrl}'`、`data: '${formData}'`，与 `schema-compiler-xui-actions.test.ts:160` 合法模式一致）/ 合法完整 args 零诊断 / 增强项 method 字符串、data、params 对象形状 / source 与 data-source 场景行为不变（`invalid-source-shape` + 'api.url must be a non-empty string.'）。

### Phase 2（回归与文档核对）

- 全仓 grep：`validateApiSchemaShape` 调用点仅定义 + source/data-source 两处（docs/archive 命中为历史记录）。
- 设计文档 v8 §3.7 与 live baseline 一致（ajax definition 样例、SchemaFieldRule 约束载体、表达式豁免语义逐项核对）；§3.7 收口句更新为已完成状态。

### 验证结果

- 局部 `tsc -p tsconfig.build.json`（flux-compiler）通过；flux-compiler 测试 36 文件 / 533 用例全绿（521 既有 + 12 新增）。
- 全量验证与 closure-audit 证据见 Closure Gates（由独立子 agent fresh session 执行）。

## Plan 2026-08-02-2 执行：嵌套 Schema 机制统一与遗留删除（Phase 5-7）

执行 `docs/plans/2026-08-02-2-nested-schema-mechanism-unification.md` 的 Phase 5（P1 决策项）/ Phase 6（typed item 语义）/ Phase 7（全仓审计与定稿）。Phase 1-4 的代码已在本工作树（未提交），本次补齐剩余三个 Phase：

### Phase 5（P1 决策项，代码已在工作树，逐项核对完成）

- **wizard beforeEnter/beforeLeave 接线**：`wizard-renderer.tsx` 新增 `runStepTransitionGuards`（beforeLeave → beforeEnter 顺序 dispatch，`{ok:false}` 阻止导航）+ `resolveStepLifecycleAction`（envelope 解包）；`layout-renderer-definitions.ts` steps fieldRules 声明 `beforeEnter: 'event'`/`beforeLeave: 'event'`；测试：`wizard-renderer.test.tsx` 5 个生命周期用例（envelope 形态、live scope 无旧值污染、{ok:false} 阻止、Next 提交路径触发 beforeLeave）。
- **crud columns[].searchable region 化**：`crud-renderer-definition.ts` columns fieldRules 声明 `searchable: { kind: 'value-or-region', regionKey: 'searchableRegionKey' }`；`table-header-row.tsx:226` 消费路径改为 region 渲染优先（`searchableRegionKey` → `props.regions` 渲染，placeholder 配置对象形态保留内联）；`crud-schema.ts`/data `schemas.ts` 补 `searchableRegionKey` 编译键类型；测试：`schema-compiler-schema-definition-unified.test.ts` searchable 三形态（boolean 内联/配置对象内联/SchemaInput region）+ `data-crud-header-forwarding.test.tsx` region 渲染路径。
- **carousel items[].body 类型删除**：`flux-renderers-content/src/schemas.ts` 删除 `CarouselItemSchema.body` + 注释同步（carousel.tsx 只渲染 image/title/caption）；零引用验证。

### Phase 6（typed item 显式 type 语义，本次实现）

- **编译管道**：`node-compiler-helpers.ts` 的 `classifySchemaDefinitionItem` 增加 typed item 分支——item 带 `type` 且 `registry.get(type)` 命中 → 按子 definition 的 `fields`/`propContracts` 分类（region 提取、event envelope、literal envelope、prop 原样）；父级 fieldRules 与显式 type 并存 → 显式 type 为准 + emit `conflicting-field-definition`（warning）诊断；未注册 type（如 table column `type: 'operation'`）回退内联 fieldRules，不触发诊断。
- **registry 贯通**：`createCompileSingleNode` 增加 `registry` 参数（`schema-compiler.ts` 两处调用点传 `input.registry`），`classifySchemaDefinitionValue`/`Container`/`Item` 贯通 registry + emitDiagnostic。
- **校验管道**：`shape-validation-analyze.ts` `analyzeSchemaDefinitionField` 增加 typed item 分支（`analyzeTypedSchemaDefinitionItem` 按子 definition 递归 region/嵌套 schema-definition 校验），与编译侧对称。
- **诊断码**：`SchemaDiagnosticCode` 新增 `'conflicting-field-definition'`。
- **契约测试**（+5）：typed item 按 registry 分类（region/event/value 三语义）、无 type 走内联、未注册 type 回退内联、并存诊断（compile 侧）、校验侧按子 definition 递归（unknown-property 命中）。

### Phase 7（全仓审计与定稿）

- **grep 审计**：`deepFields`/`nestedRegions`/`booleanKeys`/`DEEP_FIELD_NORMALIZERS`/`extractNestedSchemaRegions`/`visitNestedSchemaRegions`/`RendererDeepFieldDefinition`/`normalizeTableColumns` 等全部零引用（仅历史讨论/设计文档提及）；props 内 dispatch 审计（`dispatch(...props.props.X as ActionSchema` 模式）无遗漏。
- **文档定稿**：`docs/architecture/nested-schema-field-classification.md` §3.3/§3.7/§3.8 与 live baseline 对齐（`SchemaDefinitionFieldKind` 独立词表合并删除的说明、fieldRules 统一 `SchemaFieldRule | SchemaFieldKind` 形态、`conflicting-field-definition` 诊断码、未注册 type 回退语义）；`docs/architecture/renderer-runtime.md` deepFields 章节改写为 schema-definition 管道；`docs/references/quick-reference.md`/`renderer-interfaces.md`/`docs/skills/flux-component-design-review-prompt.md` 残留 deepFields 引用清理。
- **全量验证**：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-compiler 521）。

### 验证结果

- 全量 unit 测试通过（含新增 Phase 6 契约测试 5 个、Phase 5 既有 wizard/searchable/carousel 用例）。

## Mission-driver 验证：plan-2026-08-02 全量门禁修复 + 提交

mission-driver 对 component-audit 工作树执行全量验证（typecheck/build/lint/test），修复全部红项后提交（见 `docs/plans/2026-08-02-1-nested-schema-field-classification.md`）：

### 修复清单

- **nop-debugger `src/index.test.ts`**：`describe` 块被第 195 行重复 `});` 提前闭合（pre-existing parse error，阻塞该包 typecheck/lint/test + 全工作区 typecheck）——删除重复行。
- **flux-runtime `node-resolver.test.ts`**：stale `componentName` target 测试（componentName 移除后类型错误）→ 改为未知空 target 契约测试。
- **flux-compiler open-renderer unknown-property 契约**：恢复 open 模型下 strictMode 仍为 `warning`（回退 0bdb7094 的 collateral damage：error 触发 `skippedPropKeys` 把合法未声明 props（label/name/data）从编译产物中丢弃，vitest/playwright 下 strict 默认 ON，导致 flux-react 3 个渲染测试 + 表单 DOM 全部崩坏）；closed 模型行为不变。
- **flux-renderers-form**：`formFieldRules` 补 `name`（propContracts 使字段 renderer 转 closed 后 `name` 报 unknown-property error）；form-renderer-lifecycle/form-loadaction/form-init-inflight-race 的 mock `ownedForm` 补 `setRefreshHandler`（e9536ea8 refresh handler 接线后 stale mock）；`button-group-select` renderer 注册（fields: options/multiple/direction/dict）+ schemas.ts WIP 注释更新；`combobox-item` 补 `data-value`。
- **flux-react FieldFrame**：字段根 `.nop-field` 补 `data-field` / `data-renderer`（08-01 field-selector 契约冻结属性）；DOM 契约测试 28/28 绿。
- **flux-renderers-basic**：text renderer `name` field 入契约测试预期（4756acae 后 stale）。
- **nop-debugger**：`beforeAction` 事件补 `interactionId`（explainNodeFailure 无法把 action 关联到 api:abort）；`explainNodeFailure` 在最近一次 action 交互成功时回退扫描节点 action 事件中带失败证据的交互（复现：点击两次 → 首请求 abort，锚定失败而非 unknown）。
- **flux-renderers-ai `ai-citations.tsx`**：`scanTextForCodeRegions` 的 run id 补 segment id 前缀（`idCounter` 每次调用归零 → 跨调用重复 key `t@0#0` → React console.error → e2e zero-error 断言失败）。
- **playground**：`button-group-select` 补 route 条目 + lab 页（`button-group-select-lab-page.tsx`）+ renderer-lab-registry；`dialog-lab-page` 场景描述与 e2e `layout-content.spec.ts` 对齐（mission 场景改造后 'Submitted name' 断言移除）；`playground-entry-pages.spec.ts` 补 ai-rich-text/ai-widgets smoke 断言（2ddf70e5 加 route 未加断言，pre-existing）。

### 验证结果

- `pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31（含 i18n schema-prop 覆盖，button-group-select dict/direction 补测试覆盖后绿）。
- `pnpm test` 全包 0 失败（unit：30+ 包全绿；flux-compiler 531、flux-react、flux-renderers-form 643、flux-renderers-ai 474、nop-debugger 125 等）。
- `pnpm test:e2e`：component-lab 144 passed / 1 skipped / 0 failed（mission 影响面全绿）。全量 756-772 passed / 43 skipped，**9 个 pre-existing 失败**（与 HEAD 基线 stash 对比逐项复现一致，均为 mission 未触及包）：ai-chat timestamp、ai-rich-text-sender ×5（Tiptap）、calendar-demo nav、diff-perf 200ms 阈值（机器相关）、input-suggest popover 稳定性。**未达 full-green**，commits 不做 full-green 声明。
- 全绿基线说明：本次提交前的 base（88e4de65）各包失败数见本文上部 plan-1 执行记录（29/23/10/8/3/3/1…）；本验证轮将上述全部归零（除 e2e pre-existing 9）。

### 遗留

- e2e pre-existing 9 失败（ai-chat/ai-rich-text-sender/calendar/diff-perf/input-suggest）建议由 component-audit 后续 work item 或专项处理（diff-perf 需评估阈值与机器基线）。
- `docs/context/project-context.md` 仍标 "production-green"（过期表述），roadmap C0 计划回写。

## Plan 2026-08-02-1 执行：嵌套 Schema 字段分类机制落地（P0 live defect 修复）

执行 `docs/plans/2026-08-02-1-nested-schema-field-classification.md`（8 Phase 全绿，除下述 pre-existing 基线项）：

### 落地内容

- **Phase 1（flux-core）**：`FluxValueShape` 新增 `schema-definition` kind；`SchemaDefinitionFieldKind` 封闭联合（value/event/action/region/schema/schema-array/prop/literal）+ `SchemaDefinitionFieldRule` 对象形态（required/valueType/nonEmpty）；`matchesFluxValueShape` 新增 case（actionValue 本地近似 = isPlainObject + action 字符串键）。`flux-core/src/schema-diagnostics/manifest.ts`、`value-shape-runtime.ts`。
- **Phase 2（flux-compiler）**：`validateFluxValueShape` / `summarizeExpectedFluxValueShape` 新增 case（与 matches 对称，含 required/valueType/nonEmpty 约束 + `${...}` 表达式豁免）；对称性契约测试。
- **Phase 3（编译管道）**：`compileSingleNode` 新增 `renderer.propContracts?.[key]?.shape` 查找（array.item / record.value / object.fields 容器遍历，不依赖 deepFields）；固定顺序处理：actionValue 整值 envelope → 逐字段（event/action → `__nopPreserveLiteral` envelope、region/schema/schema-array → region 提取、value → 表达式）；deepFields normalize 优先级先行。**红线契约测试先写后绿**（全静态 items onClick 不进 props 表达式）。
- **Phase 4（renderer 解包）**：dropdown-button（`unwrap(item.action) ?? item.action ?? unwrap(item.onClick) ?? item.onClick`）与 button-group（仅 action）dispatch 解包链 + 三种产物形态（envelope/原始/混合）focused 单测。
- **Phase 5（P0 定义内联）**：dropdown-button/button-group items（fieldRules：action/onClick → event，label/disabled/destructive/key/icon → value）；select searchSource、tree searchSource/childrenSource、crud/table quickSaveAction/quickSaveItemAction、picker loadAction/labelResolveAction（pickerDialog 显式排除）、input-file/input-image uploadAction/deleteAction、input 系 validate.action → `schema-definition` + `actionValue: true`。**裁定：closed-model 走宽松模式**（未知键放行，host 透传语义，设计 §6/§3.7 依据）。
- **Phase 6（内建 action definition）**：`BUILT_IN_ACTION_DEFINITIONS` 表（16 canonical 每类型一个 fieldRules，含 ajax 约束 + argsRequired）；`refreshNearest` 补入 `BUILT_IN_ACTION_REGISTRY` + `BuiltInActionSchema` TS 联合；兑现边界选项①：`evaluateSurfaceArgs` 对 definition 标注 action 类键（onClose/onSubmitSuccess/onSubmitError）原始值保留；`validateActionShape` 按 definition 校验 args + `args.body` 递归 `analyzeSchemaInput`（ActionValidationContext 新增 analyzeSchemaInput hook，JSON pointer → dot path 转换）。ajax 硬编码分支迁移由 Plan 3 接管（管道已就绪：constants.ts 定义齐全 + validateActionShape 按 definition 分支）。
- **Phase 7（契约收口）**：红线转绿（compile 层 + 真实定义运行时 dispatch + e2e）；searchSource/quickSaveAction/loadAction/uploadAction 不被 props 求值污染断言（toEqual authored 全量保持）；onClose/onSubmitSuccess 不被 dispatch scope 求值（flux-action-core focused 单测）；新增 e2e `dialog-dropdown-row-edit.spec.ts`（CRUD 行内 dropdown → openDialog → 编辑 → 提交编辑值）**真实浏览器通过**——live defect 修复确认。
- **Phase 8（回归）**：`pnpm typecheck`（仅 pre-existing nop-debugger parse error）、`pnpm build`（31/31）、`pnpm lint`（仅 pre-existing nop-debugger parse error）、`pnpm test`（全包：新增 0 失败，见下）。设计文档 `docs/architecture/nested-schema-field-classification.md` 字段约束载体等细节与落地实现对齐（§3.3/§3.7）。

### 测试统计

- 新增测试：flux-core +5、flux-compiler +29（红线/三分支/对称性/definition args/污染断言）、flux-action-core +1（兑现边界）、flux-renderers-layout +6（解包链 ×3 + onClick envelope + 混合 ×2 + 真实定义红线）、e2e +1（dialog-dropdown-row-edit）。
- 全量（相对 base 88e4de65）：**新增 0 失败**。各包失败数均与 base 完全一致（pre-existing）：flux-compiler 3（strict-mode severity）、flux-react 3、flux-renderers-basic 23、flux-renderers-form 29（含 prior-session WIP 测试）、flux-renderers-form-advanced 8、flux-renderers-data 10、nop-debugger 1（index.test.ts parse error，同时阻塞 typecheck/lint）、e2e 4（action-logic ×3 + layout-content ×1）。
- pre-existing 修正：`ButtonGroupSelectSchema` 类型缺失（prior-session WIP renderer 的 typecheck 阻塞）已补 `schemas.ts` 类型声明（renderer 仍未注册，保持 WIP）。

### 遗留事实

- nop-debugger `src/index.test.ts:391` parse error 为 base 既有缺陷（阻塞该包 typecheck/lint/test 全绿），不在本计划 scope。
- button-group-select WIP（renderer 未注册）保持现状，由 component-audit mission 后续接管。

## New Mission: component-audit（逐组件深度审计 + 自动修复）

用户反馈上轮 `audit-remediation`（10 包簇 x 7 审计层）粒度太粗，未逐组件审查。新增 mission + roadmap + checklist，并按要求加入**自动修复机制**、经**三轮独立子 agent 审查**达成共识：

- `missions/component-audit.json` — 新 mission 配置（roadmapPath → `docs/backlog/component-audit-roadmap.md`）
- `docs/backlog/component-audit-roadmap.md` — 32 个 work item（C0 编排基线 + C1~C9 九个 renderer 族逐组件审计 + CR 跨族修复 + CV 全量验证 + CG Guard 沉淀），112 个注册组件全部落到族级 work item，组件级审计卡为单位；含「自动修复机制」8 条契约（P0/P1 同 plan 自动修复、test-first、验证门禁、closure 独立 agent、中断恢复）
- `docs/audits/component-audit-checklist.md` — 18 维逐组件检查清单（含 INV-1 env IO 边界）+ 审计卡模板 + P0/P1/P2/P3 裁决与自动修复纪律 + 18↔23 维映射

### 三轮独立审查达成的修订（共识）

- R1: 组件清单修正（input-suggest 为 hook 非注册 type、button-group-select 未注册 WIP、badge 归 basic、hidden-field 非组件；总数 112）；基线诚实化（实测 flux-renderers-form 29 失败/6 文件，其中含已提交回归 form.tsx:523 setRefreshHandler）；P2 backlog 与复验项归 CR；C1 拆 3 族
- R2: 18↔23 维映射表修正（7 无直接对应）、维度 14/18 补挂脚本、维度 18 IO 清单补全、纪律 4 补 CR 复验归属
- R3: 补 2 个脚本到复用表、维度 4→08 映射；无新增 Blockers/Majors

### 共性问题主动处理条款（用户追加要求）

用户要求：**执行中发现共性问题（同一根因影响 ≥2 组件/跨包/公共层）时，执行 agent 必须主动修改 roadmap 或插入共性重构计划，不得默认囤积到 CR**。已写入 roadmap「自动修复机制」§7（重写为「共性问题主动处理（执行中动态调整 roadmap）」，a-e 五条：插入 CX-n / 当前 plan 多阶段优先修复 / 事后回写以 planned 插入、父 plan closure 后标 done / 结构性重构执行前人工确认、纯行为修复免确认 / 同步更新表与依赖图并记 daily log），并同步：Work Item Status 表注释（CX-n 编号约定）、CR 表行与 CR Phase Details（改为"剩余" shared 缺陷，公共 helper/field-frame/编译期/值所有权公共层由 CX-n 处理）、Cross-Cutting「共性即插即修」、Rule（CX-n 插入或合并进现有同类项（CR 除外）为唯一新增路径）、checklist §3（根因在公共层必须 CX-n，单点落地可归 CR）、mission description。

- R4: 独立审查 revised → 1 Major（CR 表行未同步"剩余/除外"）+ 5 Minor（事后回写 CX-n 生命周期、Rule merge 边界、结构性重构边界（纯行为修复免确认）、依赖图同步可操作性、shared 单点/公共层区分）全部修复
- R5: 终轮共识审查 **pass**（6 条修订逐条核实通过，无新增问题；仅 1 条措辞观察 §1→引言段引用，已修复）
- 验证：`parseRoadmapMarkdown` 仍解析 32 项全部 `todo`；CR 表行含"剩余…CX-n 处理的除外"；mission-driver 识别正常

### 验证

- `./tools/mission-driver.sh list` 已识别 `component-audit` mission
- `parseRoadmapMarkdown` 最终解析 32 个 work item 全部 `todo` 无误
- 本轮仅新增/修订 docs + mission 配置，无代码变更
- 遗留事实：`docs/context/project-context.md` 仍标 "production-green"（过期表述），C0 将回写

## 上轮审计遗漏分析（component-audit 补查面）

1. 组件级契约缺陷（combobox-item 缺 `data-value` 等单组件缺口）
2. 组合宿主场景（CRUD 行内 dropdown 提交旧值这类组合缺陷）
3. 值所有权三态逐组件全路径
4. 四态覆盖（空/加载/错误/禁用）
5. DOM 选择器契约回填（08-01 契约晚于上轮审计）
6. 嵌套 schema 分类（08-02 机制晚于上轮审计）
7. 事件 payload 形状逐组件
8. 真实浏览器行为验证（bug 73：单测绿≠真机正确）
9. 注册完整性逐组件（双注册/playground 页/bundle 导出）
10. 测试质量逐组件（not-throw-only 断言）
11. i18n key 逐组件（含 aria-label）
12. a11y 完整键盘路径
13. 异步生命周期逐组件
14. 样式契约逐组件
15. 安全红线逐组件
16. 性能边界逐组件
17. React 19 逐组件
18. 文档对照逐组件
