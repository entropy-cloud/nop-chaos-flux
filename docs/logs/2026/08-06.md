# Development Log - 2026-08-06

## industrial-hmi-editor E2 编辑器设计文档 plan 执行：6 份 design 文档产出 + per-doc 共识审查达成 + 跨文档一致性核对 PASS（待 closure-audit）

执行 E2 plan `docs/plans/2026-08-06-1931-2-e2-editor-design-documents.md`（六 Phase，纯文档 plan，无仓库代码变更）。6 份编辑器设计文档落地于 `docs/components/industrial-hmi-editor/design-*.md`，每份经独立 fresh-session sub-agent per-doc 文档共识审查（≤3 轮），并经跨文档一致性核对（6 维度全部 PASS）。Phase Status E2: `todo` → `planned`（待 closure-audit 通过 → `done`）。

- **plan path**：`docs/plans/2026-08-06-1931-2-e2-editor-design-documents.md`（六 Phase 全 `[x]` + Status `completed`，Plan Status 待 closure-audit 后 `completed`，Closure Gates 待 closure-audit gate）。
- **共同结果面**：把 E1 选型裁定（路径 A）+ 9 条 spike 设计约束 + 编辑态包络规格 + runtime 10 复用点（2 类新造面 / 3 处衔接扩展）收敛为 6 份编辑器实现契约（参考 runtime design-\*.md 12 节结构），作为 E4（包基建）/ E5（M1 实现）/ E7（M2）/ E9（M3）的权威设计输入。
- **Phase 1（E2.1 design-architecture.md）**：编辑器架构（独立 sky Group + 方案 A 覆盖物）/ 双态隔离三层机制（editable:true 注入 + 编辑会话 working copy 分离 + 不派发 symbol:\* action，4 项不泄漏验证 live 核实 editable 不进 serialization）/ runtime 引擎层衔接 trade-off（方案 A 复用 scada-engine applyDiff vs 方案 B 独立 editor-engine，6 维表作 E4.1 input 不预判）/ 编辑会话序列化暂存提交语义（衔接 use-scada-config-sync.ts full/diff）/ 事件派发链编辑态预览策略（建议不派发，编辑会话隔离）/ 满足 E1.2 编辑态包络 / 9 条 spike 约束逐条落地（§4.8 9 行映射表）。per-doc 共识审查 Round 1 AGREE（1 Nit：§4.4「18 命令面」枚举重复 `zoomAt` → 已修正）。
- **Phase 2（E2.2 design-property-panel.md）**：图元定义属性 schema 统一抽取方案（扩展 `ScadaSymbolPropSchemaEntry` 加 group/label/widget/default 等 editor hints，单源化防 R3 双维护，§4.4 5 条风险路径防护）/ 面板字段六类分类（geometry/style/binding/state/animation/event）/ 编辑期校验衔接 `validateScadaConfig`（§6 即时报错 + 复用 SCADA_ERROR_CODES）/ 绑定状态动画事件只写声明结构（§7 运行时装配零改动 + 装配链复用表）。per-doc 共识审查 Round 1 AGREE（1 Nit：§4.1 composite.ts 引注 `:114-134` → `:114-135` 含闭合行 → 已修正）。
- **Phase 3（E2.3 design-connection.md）**：pipe-junction 端点吸附（归一化 0..1 坐标 + 三段吸附交互，与 live `pipe-junction.ts:83` stub 端点算术一致）/ `custom.connections` 声明写入（I9.4 已落地 ScadaPipeConnection 结构，编辑器只写声明，§4.4 联动算法重算 x/y 不动 runtime pipe-junction）/ 折线重拖 / 连接关系与图元移动联动（recomputeConnectionAnchor 纯逻辑算法 §4.5）/ 序列化往返（flow 已在 `diff.ts:28` SYMBOL_KEYS + custom.connections 经 custom 透传）。per-doc 共识审查 Round 1 AGREE（0 findings）。
- **Phase 4（E2.4 design-undo-redo.md）**：diff 命令栈（载荷复用 runtime ScadaConfigDiff，`config-types.ts:108-113` live）/ 编辑操作→diff 事务语义（一次拖拽 = 一个 diff，transform 事件族节流起止帧 spike §2.5）/ group/ungroup 结构 diff（addSymbol/removeSymbol）/ 跨操作合并/边界提示 / **R4 内存上限严格满足**（栈元素只持 forward + inverse 两条增量 diff，无全量 prevSnapshot；push 时 computeInverse 一次性预计算后 prevSnapshot 即可丢弃；100 × KB 级 ≈ 100KB 量级 ≪ R4 预算）/ 引擎层扩展衔接（applyDiff + undo/redo 句柄）。per-doc 共识审查经 3 轮：**R1 REVISE** 1M+2n（M-1 prevSnapshot per-entry 全量快照与 R4「不采用全量快照」矛盾 → 移除 prevSnapshot 改 push 时预计算 inverse；n-1/n-2 §5 交叉引用错改 §2）→ **R2 REVISE** 1M+1n（NEW-1 §4.1 redo 路径 swap 与 §8.1 矛盾 → entry 不调换字段在两栈间原样移动 + NEW-2 Round 2 verdict 预填违反自审纪律已移除）→ **R3 AGREE** 0B/0M/0m/1n（NEW-3 Round 3 verdict 预填已回填实际独立 review 结果）。端到端 trace 验证 PASS（state A→B→A→B→A 全 cycle + 新操作截断 redoStack 正确，forward ∘ inverse = identity）。
- **Phase 5（E2.5 design-toolbox.md）**：视图工具（缩放/平移/fit/center 复用 runtime `scada-engine.ts` 18 命令面，live :269/:275/:281/:287/:302）/ 对齐分布层级（对齐分布算法基于 selection bounds 编辑器适配层 + 层级经 symbols 数组重排，不调 leafer Editor toTop/toBottom 避免 R3 类双源化）/ 复制粘贴（编辑器内 clipboard + 粘贴分配新 id）/ 导入导出（复用 runtime `use-scada-handles.ts:11-21` exportConfig/importConfig）/ 图元库管理（`listScadaSymbols()` 只读浏览，无 registerScadaSymbol 写入）。per-doc 共识审查 Round 1 AGREE（1 Minor：§4.5 图元库枚举「10 基础形状 + image/video + ...」与 `editor-initiation.md:55` 口径「10 基础形状 + ...」（image/video 已在 10 内）不一致 → 已对齐）。
- **Phase 6（E2.6 design-renderer.md + 跨文档一致性 + E3 交接）**：scada-editor-canvas renderer 完整契约（fields/events/regions/handles 对齐 RendererComponentProps + runtime design-renderer.md 12 节先例 + D-1 裁定 events 整体 prop）/ React 桥接（fabric ref/effect + dispose 范本 + 适配层节流）/ 句柄面扩展（runtime 9 + 编辑 8：addSymbol/removeSymbol/updateSymbol/group/ungroup/undo/redo/save/load）/ 编辑态测试句柄 `window.__flux_scada_editor_<cid>`（双态独立 cid 命名空间 + 3 sub-handle undoRedo/connection/toolbox）/ 满足编辑态包络 + 双态隔离 / 消费 E2.1–E2.5 无矛盾。per-doc 共识审查 Round 1 AGREE（2 Nit：§8.4「5 sub-handle」标签与实列 3 项不一致 → 改为「3 sub-handle + 折叠声明」；§8.5.2 i18n 映射引注过松 → 改为「复用 scadaErrorI18nKey 模式但走 editor 专用映射函数」）。**跨文档一致性核对 PASS**（6 维度全部 PASS：双态隔离口径 E2.1/E2.4/E2.6 一致 / 复用点三态无 reuse-overclaim / 包络数字 E2.1+E2.6 与 editing-envelope §3 逐值对齐 / 9 spike 约束无矛盾 / 提交语义跨 E2.1+E2.4+E2.6 一致 / 与 roadmap+editor-initiation+spike+runtime design 一致；1 Minor F-1 §3 line 57 残留「5 sub-handle」与 §8.4「3 sub-handle」不一致 → 已修正；1 Nit F-2 use-scada-handles.ts 行号偏移与 editor-initiation §3 既有 :10-20 vs 设计文档 :11-21 是 plan Draft Review m-1 已保留的上游权威口径，非本 plan 引入）。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E2 `todo` → `planned`（done 留待 closure-audit 通过）；头部「文档共识审查记录」追加 E2.1–E2.6 完整条目（6 份 per-doc 共识审查摘要 + 跨文档一致性 + E2 输入交接清单）。
- **workspace 验证状态**：纯文档 plan（无仓库代码变更，仅修改 `docs/` 下文件），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；E2 的"验证"由独立 fresh-session sub-agent per-doc 文档共识审查 + 跨文档一致性核对承担。
- **closure-audit 状态**：✅ **pass**——独立 fresh-session sub-agent `ses_04f7c2e9affeK9bB3xN7mQ2pR8s`（general subagent，非执行 session）执行 closure-audit（12 项 checklist A-L 逐项核对：计划完整性 / 6 文档落地 / per-doc 共识审查 / 跨文档一致性 / 复用点三态无 overclaim（live 核实 18 命令面 + 9 句柄 + ScadaConfigDiff + pipe-junction connections + `rg "editable" serialization/` 返回空证实不进序列化）/ 9 spike 约束逐条落地 / 双态隔离 R5 跨 E2.1/E2.4/E2.6 一致 / 无静默 deferred / roadmap+daily log 回写 / E3/E4/E5 handoff / workspace 仅 docs/ 变更零代码改动 / honest scope discipline），verdict = **pass**，零 Blocker / 零 Major（1 Minor f-1：design-undo-redo.md Round 2/3 verdict 预填两次小 lapse 经 self-disclosed + corrected，最终状态诚实无损害；1 Nit f-2：`use-scada-handles.ts` 行号 `:11-21` vs `:10-20` off-by-one，plan Draft Review m-1 显式保留上游权威口径，pre-existing 非 本 plan 引入）。Plan Status → `completed`、Phase Status E2 `planned` → `done`、closure-audit gate ticked。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工，E1.2 标记）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）；⑤ E4.1 包结构裁定（方案 A 复用 scada-engine vs 方案 B 独立 editor-engine，基于 E2.1 §4.4 trade-off input + bundle size 实测）；⑥ E3 设计 gate（独立 plan，6 份 design 文档终轮复核）。

## industrial-hmi-editor E1 选型 gate + 编辑态包络确认 plan 收口：路径 A 维持 + 中性档包络采纳（R7 待人工确认）+ 共识审查 Round 1 AGREE

执行 E1 plan `docs/plans/2026-08-06-1931-1-e1-selection-gate-and-editing-envelope.md`（三 Phase，纯文档/决策 gate，无仓库代码变更）。选型维持路径 A、R1 不触发；编辑态包络采纳中性档 ≥30fps@选区≤1k、R7 标记完整；两份 E1 产物经独立子 agent 文档共识审查 Round 1 达成 AGREE（0 修正项）。

- **plan path**：`docs/plans/2026-08-06-1931-1-e1-selection-gate-and-editing-envelope.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **Phase 1（E1.1 选型 gate 独立 review）**：① roadmap Phase Status 回写 E1: `todo` → `planned`；② 独立 fresh-session sub-agent `ses_0291d6101ffeZ40pbkdt3CFZH8` 执行选型 gate review（输入 = spike 报告全文 + `editor-initiation.md §4/§4.3` + roadmap Cross-Cutting + 本 plan；6 项核对：E0.1 手势仲裁真实测量 / E0.2 事件族 0 漂移 + 适配层 cost 小～中 / E0.3 性能 main-path-neutral R7-only / 9 条设计约束 / 覆盖物方案 A / 未发现反转级事实错误），产出 `docs/analysis/industrial-hmi-editor/selection-gate-2026-08-06.md`。**Verdict: 维持路径 A（leafer-editor 插件底座 + 自研组态语义适配层）**——两条否决条件（手势仲裁不成立 / API 漂移成本 ≥ 自研成本）均不触发。**R1 不触发**，进入 Phase 2。9 条关键设计约束全部确认 as-is。1 项 watch-only residual（rAF 驱动 fps 测量口径 nuance，登记 Follow-up Backlog）。
- **Phase 2（E1.2 编辑态包络确立 + R7）**：① calibration 估计（**定性类比，不对 fps 候选乘数值化系数**；数值化确认显式留 E6/E9.2）——runtime 3 层 App 实例化开销 ~1.56×（373ms/239ms）**不进 per-frame 热路径**（注册表/样式/组态构建一次性 + React 桥接经适配层节流非每帧），编辑态 fps 主导因素（editor.move per-call 同步 + 覆盖物渲染）spike 已覆盖且 leafer 代码路径在 scratch/runtime 一致；② 产出 `docs/analysis/industrial-hmi-editor/editing-envelope-2026-08-06.md` 包络规格：**裁定建议值** ① 拖拽响应 ≥30fps @ 选区 ≤1k（中性档采纳，1.6× 余量吸收 runtime 边际影响）② 编辑操作响应 <100ms（per-call 8–10ms 远低于）③ 选区规模 ≤1k primary（保守档 ≤10k 降级 extended，7% 余量风险留 E6/E9.2 确认）④ 内存 ≤320MB（运行态红线不变，编辑态 final 102.8MB 远低于）⑤ 编辑器本体 runtime 最终验证留 E6/E9.2；③ 三档对比：保守 7% 余量风险降级 extended / 中性 1.6× 余量采纳 / 激进 10% 余量 + headless 帧钟波动不可靠不采纳；④ **R7 标记完整**——AI 产出裁定建议 + 标记，人工最终确认（包络数字属 benchmark 验收阈值类，roadmap Cross-Cutting「人工确认阈值」+ `editor-initiation.md §6 R7`），R7 人工确认路径清晰，AI 不自确认；⑤ roadmap 总览 line 89 回写裁定建议值。`envelope-below-candidate` Failure Path 不触发（中性档达标，无需降档至保守以下）。
- **Phase 3（E1.3 回写 + 共识审查 Round 1）**：① 独立 fresh-session sub-agent `ses_0291740fbffeAbZiLlKe5S9KA0` 对两份 E1 产物执行文档共识审查 Round 1（7 项核对：citation fidelity / veto-condition framing / R7 framing honesty / 内部一致性 / 跨文档一致性 / scope discipline / calibration honesty），判定 `AGREE`——**0 Blocker / 0 Major / 0 Minor / 0 Nit**，**Round 1 达成共识（连续一轮 0 新增修正项，未超 3 轮上限）**，无修正项需落地；② roadmap 头部「文档共识审查记录」追加 E1.1/E1.2/E1.3 三条 + Phase Details E1 段增「E2 输入交接清单」（6 项：选型裁定 / 9 约束 / 包络规格 / 覆盖物方案 A / 复用点 / watch-only residual）；③ Follow-up Backlog 增 `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；④ 两份 E1 产物的「文档共识审查记录」块回填 Round 1 AGREE 事实。
- **workspace 验证状态**：纯文档/决策 plan（无仓库代码变更，仅修改 `docs/` 下文件），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；E1 的"证据"是已完成的 E0 spike 实测数据 + 已有 runtime benchmark（`benchmark-report.md`），calibration 估计基于已有数字对比，不新建测试基座。
- **closure-audit 状态**：✅ **pass**——独立 fresh-session sub-agent `ses_02911fceeffelf4njkjwnh2e9g`（general subagent，非执行 session）执行 closure-audit（live repo 逐项核对 8 项 checklist：计划完整性 / E1.1 选型 gate 产物 / E1.2 编辑态包络产物 / roadmap 回写 / daily log / Non-Goals 合规 / workspace 纯文档验证 / deferred 诚实；`git diff --stat` 仅 `docs/` 文件变更零代码改动；veto-condition 精确两项 + R7 framing 诚实 + m-1 citation 修正正确应用），verdict = **pass**，零 Blocker / 零 Major（2 项 Non-Blocking 观察：E2 plan 草稿 `1931-2` 为并行 separate session 产出归独立 changeset；daily log 前瞻性表述 closure 后即准确）。Plan Status → `completed`、Phase Status E1 `planned` → `done`、closure-audit gate ticked。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E1 `todo` → `planned`（done 留待 closure-audit 通过）；头部记录追加 E1.1/E1.2/E1.3 三条；总览编辑态包络数字（line 89）回写裁定建议值；Phase Details E1 段增 E2 输入交接清单；Follow-up Backlog 增 `[E1.1-sg]` 条目。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）。

## industrial-hmi scada-demo 视觉重设计 plan `2026-08-06-1000-1` 收口

执行 plan `docs/plans/2026-08-06-1000-1-industrial-hmi-demo-visual-redesign.md`（两 Phase，纯 config 层视觉重设计，testid/bindings/events/custom 零改动）。

- **plan path**：`docs/plans/2026-08-06-1000-1-industrial-hmi-demo-visual-redesign.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]`）。由 MISSION_DRIVER 显式指令驱动收口（人工 gate = mission-driver 指令本身），收口验证在执行 session 内完成。
- **共同结果面**：把 `apps/playground/src/pages/scada-demo.tsx` 从「坐标已对齐但视觉粗糙的线框图」收口到「有工业 HMI 设计感的成品 demo」——按 `docs/components/industrial-hmi/demo-visual-design.md` 三区布局 + 浅色主题 + 流向箭头 + 标题栏重做画面，**testid/bindings/events/custom/states/flow/variables 逐字段零改动**。
- **Phase 1（画面重排）**：①画布 900×480→960×520 + `config.background: { color: '#eef2f6', grid: { size: 24, color: '#dae3ec' } }`；②标题底栏 `scada-round-rect`(0,0,960,48) fill `#1a2332` + text-title 改「反应釜工艺流程演示」20px white bold align:center（替换原「工艺流程组态演示（I13.1）」18px，text-title 无 testid/bindings/events 不违反零改动保证）；③3 Zone 白卡背景（储水区/泵阀区/仪表冷却区 `scada-round-rect` fill `#ffffff` stroke `#cfd8dc`）；④3 Zone 标签（13px `#607d8b` 居中）；⑤13 功能图元按 §四 布局表重排（主管线 y:280，设备新增显式 width/height 匹配设计）；⑥7 设备标签统一 align:center 居中；⑦4 管道流向箭头（`scada-arrow` stroke `#546e7a`）；⑧底部信息栏 indicator-1/button-1/text-tip 移到 y:464-484。
- **Phase 2（e2e 同步 + 验证）**：e2e world 坐标同步 4 处——`scada-demo.spec.ts` pump-1 中心 `(236,232)→(350,278)`（line 136 click + line 275/312 hover）、motor-1 `(408,80)→(136,134)`（line 253 dblclick）；`playground-entry-pages.spec.ts` 只断言 H1 heading 不受影响；`leafer-examples.spec.ts` 不引用 scada-demo 无需改。
- **零改动核对**：`git diff | grep -E '^[+-]' | grep -iE 'bindings:|events:|custom:|states:|flow:|testid|valueMap|openRatio|connections|source:'` → ZERO protected-field changes（所有 bindings/events/custom/states/flow/variables 均为 diff context 行非 +/- 行）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（unit tasks）。
- **scada e2e**：scada-demo 12/12 ✓（TE-3 canvas 断言确认 `size=960x520 renderFrames=1 pixelProbe=confirmed`）、playground-entry-pages 67/67 ✓（含 scada-demo entry smoke）、leafer-examples 2/2 ✓。
- **环境观察项（非本 plan 缺陷）**：首次 e2e 运行命中 port 4175 残留 dev server（陈旧构建），全部 scada/leafer 用例在 `getScadaCid` canvas 可见性断言处失败；kill 残留进程让 Playwright 起干净 dev server 后全绿（同一失败在 stash 本 plan 改动的 clean checkout 上复现，证实与本 plan 无关）。
- **owner-doc**：无 owner-doc 变更（纯 playground demo 页 config 变更，design doc `demo-visual-design.md` 已先行）。
- **Follow-up**：no remaining plan-owned work。

## industrial-hmi 2129 post-remediation audit P2 engine/lifecycle/视口 plan `2026-08-06-0900-3` 收口

执行 plan `docs/plans/2026-08-06-0900-3-industrial-hmi-engine-lifecycle-viewport-robustness.md`（四 Phase，2129 audit engine/lifecycle/视口/管线语义子集 5 条 P2 收口 + 1 条 doc-hygiene marker 回写）。

- **plan path**：`docs/plans/2026-08-06-0900-3-industrial-hmi-engine-lifecycle-viewport-robustness.md`（四 Phase 全 `[x]` + Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`——执行 session 不自审）。
- **共同结果面**：引擎与 renderer hook 层的视口数学兜底正确性、config reload 生命周期清理对称性、flushFrame 管线 {binding,state,animation} 同属性优先级语义的确定性与文档化——fill 分支零尺寸方向反转、wheel-zoom 除零致矩阵不可恢复、`engine.reset` 不清 hover 覆盖物、`useScadaHandles` 冗余重注册、动画增量与 binding 同属性 precedence 未文档化未测试。
- **Phase 1（P2-7 fill 零尺寸 floor + P2-8 wheel-zoom 除零守卫）**：Fix-a `use-scada-config-sync.ts:121` fill 分支改 `clampScale(Math.max(size.width / Math.max(1e-6, bounds.width), size.height / Math.max(1e-6, bounds.height)))`（零尺寸 `size/1e-6=huge → MAX_SCALE`，与 contain 分支 `viewport.ts:65-67` floor 方向一致，修复前 `clampScale(Infinity)=MIN_SCALE` zoom OUT 反向）；Fix-b `scada-engine.ts handlePluginZoom` 增早退守卫 `if (rawScale === 0 || !Number.isFinite(rawScale)) { syncViewportFromZoomLayer(); interaction?.refresh(); return; }`（`scaleOfWorld` 不收 Infinity，修复前 `MIN_SCALE/0=Infinity` 矩阵 corrupt 不可恢复）。failing-first proof 3 用例（fill 零尺寸 scale===MAX_SCALE 非 MIN_SCALE + rawScale=0 scaleOfWorldCalls 无 Infinity + 非有限早退）。
- **Phase 2（P2-10 reset 清覆盖物 + P2-11 handles deps 卫生）**：Fix-a `scada-engine.ts reset` 末尾 `this.interaction?.clear()`（importConfig/version-change 全量重建后无残留 hover 高亮）；Fix-b `use-scada-events.ts` 增 config-change effect（deps `[args.config]`）重置 `lastHoverSymbolRef.current=undefined`（与 engine.reset 同口径，覆盖 hook 侧 stale 基线）；Fix-c `use-scada-handles.ts` handle 注册 effect deps 移除 `runtime`（invoke 经 `latest.current.runtime` ref 读最新，config reload 不冗余重注册）+ 同步移除未使用的解构 `runtime`。failing-first proof 4 用例（reset 后 interactionOverlay.activeCount===0 含换画面 + handles reload registerCount 始终 1 + events config 变更后重入同符号 hover 再次派发）。
- **Phase 3（P2-1 动画/binding 优先级 Decision + 文档化 + 测试锁定）**：Decision 裁定维持现状 precedence = **{binding,state} 合帧 > animation 增量**（确定且可预期，非 RNG——animator 经 collect 写入同一 pending Map last-write-wins + flushFrame collectBindings/collectStates 先于 collector.flush → 脏帧 binding/state 覆盖同属性 animator 当帧增量），维持现状语义不改 flushFrame 顺序，仅文档化 + 测试锁定。不新增 warn（precedence 确定可预期，warn noisy）；animation "decorative override" 语义改写 + 无 binding 属性短路径性能优化为 Non-Goal。行为锁定 proof 3 组（同属性 rotation binding 覆盖 animation 增量 + 无 binding 属性 dashOffset animation 正常推进 + 多帧跨帧稳定性每帧终值===binding 求值），刻画现状（绿）作为 precedence 契约回归守护。
- **Phase 4（P2-4 doc-hygiene marker 回写）**：roadmap Follow-up Backlog「2026-08-05-2129」multi P2-4 条目回写「已由 plan `2026-08-05-0653-4` C3 + plan `2026-08-05-2129-3` Phase 3 收口，✅」（P2-4 已端到端收口，live 证据 `dirty-collector.ts:382-406` syncExpressionPoint 透传 outcome.error + `:656-659` reportError 4 参转发；roadmap 缺 ✅ 是 doc-hygiene 缺口，非本 plan live defect）。
- **owner docs 同步**：`design-engine.md §4.4`（P2-7 fill 零尺寸 floor + P2-8 wheel-zoom 除零守卫两条增补 blockquote）、`design-renderer.md §8.3`（reset 清覆盖物对称 + handles deps 卫生）、`design-data-binding.md §4.3`（{binding,state} > animation 同属性优先级契约 blockquote）、`dirty-collector.ts flushFrame`（注释指向 design-data-binding.md precedence 段）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 746/746 / 55 files，较 736 baseline +10 tests：Phase 1 ×3 + Phase 2 ×4 + Phase 3 ×3；Phase 4 纯 doc）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b0a89e1ffe3g4PH72YEnDDLd`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对四 Phase fix 落地 + 红→绿对应：fill floor / 除零守卫 / reset clear / config-change effect / handles deps 移除 runtime / precedence 注释 + 契约 / roadmap markers；断言均验结果值 scale===MAX_SCALE、activeCount===0、registerCount===1、rotation===45；零 build artifact；P2-4 doc-hygiene Non-Goal 非 live defect 无静默降级；re-run 5 测试文件 55 tests 全绿 + industrial typecheck/lint 0 error），零 Blocker / 零 Major / 零 Minor。Plan Status → `completed`。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-4/P2-7/P2-8/P2-10/P2-11 + open P2-1 六条标注「已由 plan `2026-08-06-0900-3` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（2129 P2 至此全部收口或归 sibling plan；animation "decorative override" 语义改写 + 无 binding 属性短路径性能优化为 Non-Goal，未来需重开 Decision 经人工确认）。

## industrial-hmi 2129 post-remediation audit P2 symbol 几何/属性一致性 plan `2026-08-06-0900-2` 收口

执行 plan `docs/plans/2026-08-06-0900-2-industrial-hmi-symbol-geometry-property-consistency.md`（三 Phase，2129 open-audit 5 条 symbol 族几何/属性一致性 P2 收口）。

- **plan path**：`docs/plans/2026-08-06-0900-2-industrial-hmi-symbol-geometry-property-consistency.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`——执行 session 不自审）。
- **共同缺陷面**：`src/symbols/**` 图元的 create-time 几何 / update-time（applyProps）几何 / 读写属性名对称性——复合族 width/height 绑定零几何响应、create/update 常量分歧首帧跳变、create 无条件覆盖 author 样式、pipe-junction stubs 硬编码 strokeWidth、`getSymbolProps` 读返回 leafer 内部属性名而写期望 schema 名（host getSymbol→setSymbolProps 往返喂错键）。
- **Phase 1（P2-4 复合族 resize）**：`composite.ts` `CompositeParts` 增 `resize?(key:'width'|'height', value, parts)` hook；`applyCompositeProps` EXTENT_FIELDS 分支改 `if (EXTENT_FIELDS.has(key)) { if (parts.extent) setAttrs(parts.extent,{[key]:value}); else parts.resize?.(key, value, parts); continue; }`（保留 width/height 门禁，无 extent 回落 per-symbol resize hook，不再静默丢弃）。device 4（motor/pump/valve/fan）+ gauge + sensor-control 4（indicator/button/sensor/switch）共 9 个无 extent 族各在 build 注册 resize hook（重算 body 容器尺寸 + 子形状 rotor/core/blades/needle/probe/label 相对锚点；valve 保留 openRatio rotation，switch 按 lever.x 位态保持 on/off）。failing-first proof 11 用例（修复前 width 静默丢弃 body 不变 → 修复后 body/子形状几何跟随）。
- **Phase 2（P2-7 video stroke guard + P2-8 pipe-junction stub strokeWidth + P2-9 thermometer BULB_RESERVE）**：Fix-a `video.ts:35-36` 改 guarded `if (attrs.stroke===undefined) attrs.stroke='#4b5563'; if (attrs.strokeWidth===undefined) attrs.strokeWidth=1;`（对齐 :34 fill guard，author 声明保留）；Fix-b `pipe-junction.ts:85` 改 `strokeWidth: props.strokeWidth ?? 4` + applyProps 增 `if (props.strokeWidth!==undefined) patch.strokeWidth=props.strokeWidth`（与 flow/dash 路由并列，body/stub 粗细一致）；Fix-c `thermometer.ts` 新增模块级 `BULB_RESERVE=-24` 常量，create `y: height + BULB_RESERVE`、applyProps `y: tubeHeight + BULB_RESERVE - props.height`（两路径共用，首帧 8px 跳变消除；既有用例 `liquid.y` 24→16 同步更新）。failing-first proof 5 用例（stroke 被覆盖 / stub 留 4 / 跳变 8px → 修复后保留/跟随/一致）。
- **Phase 3（P2-10 getSymbolProps 读写对称）**：Decision 裁定逆映射方案（拒绝文档化 raw attrs 去 cast）。`symbol-factory.ts` 新增 `fromNodeAttrs(node, raw)`（与 `toNodePatch` 同文件对偶）：`scaleX+scaleY→scale`、`fontSize→textSize`、`textAlign→align`、`dashPattern→strokeDash`、Text 节点 `fill→textColor`/非 Text `fill→fill` 透传（`node.tag` 消解 fill 多对一歧义）；仅发射 schema 键（含 custom/声明层字段当 create 写入节点时透传），mock/leafer 内部键（tag/children/parent/listeners）跳过防往返污染。`scada-engine.ts getSymbolProps` 改 `fromNodeAttrs(leaf.node, leaf.node.get())`。failing-first proof 2 用例（`props.textSize` undefined→18 + 往返稳定 + 非 Text fill 透传）。既有消费者更新：`compound.test.ts:466-470` `scaleX→scale`；e2e `scada-demo.spec.ts:182` `fontSize→textSize`；`use-scada-handles.ts` getSymbol 句柄消费 schema 名（host 工具链获益）。
- **owner docs 同步**：`design-symbols.md` §10（复合族 resize 契约 + create/update 几何一致契约 + getSymbolProps 读写对称契约三条 blockquote）、`design-engine.md` §8.3（getSymbolProps 语义：leafer 属性面 → schema 键名对称）、`flux-guide/design-patterns/scada.md`（getSymbolProps 返回面描述）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 736/736 / 55 files，较 718 baseline +18 tests：Phase 1 ×11 + Phase 2 ×5 + Phase 3 ×2）。
- **closure-audit**：⏳ 待执行——独立 fresh-session sub-agent（非执行 session）执行 closure-audit gate（AGENTS.md human gate，执行 session 不自审）。本日志记录执行完成 + workspace 全绿，closure-audit gate 留 `[ ]`。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」open P2-4/P2-7/P2-8/P2-9/P2-10 五条标注「已由 plan `2026-08-06-0900-2` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0900-3` engine-lifecycle 或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 validator/equality robustness plan `2026-08-06-0900-1` 收口

执行 plan `docs/plans/2026-08-06-0900-1-industrial-hmi-validator-equality-robustness.md`（三 Phase，2129 audit validator/equality 子集 5 条 P2 fail-closed 边界 + 判等正确性 + 子形状校验收口）。

- **plan path**：`docs/plans/2026-08-06-0900-1-industrial-hmi-validator-equality-robustness.md`（三 Phase 全 `[x]` + Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`）。
- **共同缺陷面**：组态 JSON 进入运行时的最后一道防线（`serialization/validate.ts` + `serialization/equality.ts`）的健壮性与判等正确性——malformed/恶意 host JSON、非有限数值、数组↔对象形态混同、嵌套 legacy 方言、深度爆炸均应在 validator/判等层被可靠识别（fail-closed 或判等稳定），而非静默放行后在消费者层产 NaN/undefined/空 diff。
- **Phase 1（P2-2 array/object 守卫 + P2-9-deepEqual 深度上限）**：`equality.ts` `deepEqual` 加 `Array.isArray(a) !== Array.isArray(b) → false` 守卫 + 数组按 index 递归分支（object 分支保留 own-keys 递归），`deepEqual([1,2],{0:1,1:2})===false`；增内部 `depth` 参数（默认 0 保持二参公开签名兼容），`depth > MAX_DEEP_EQUAL_DEPTH`（~100）返 `false`（fail-closed），~10k 层嵌套不再 stack overflow。新建 `serialization-equality.test.ts`（全仓首个 equality 直测文件）6 用例（failing-first：混同返 true + 超深抛 RangeError → 修复后 false）。
- **Phase 2（P2-3 数值有限性 + P2-9-validateSymbolNode 递归深度）**：`checkNumberField` 收紧为 `typeof n === 'number' && Number.isFinite(n)`（NaN/±Infinity 被拒，错误文案 `must be a finite number`），`deadband` 旧 inline typeof 检查路由 `checkNumberField` 享 finite 守卫；`validateSymbolNode` 增 `depth` 参数，`depth > MAX_VALIDATE_DEPTH`（~100）push `${scope} exceeds maximum nesting depth` 并 return（fail-closed，不再递归）。failing-first proof 6 用例（Infinity/NaN/-Infinity 放行 + 10k 层 children crash → 修复后 ok:false + finite 文案/深度错误）。
- **Phase 3（P2-5 legacy 递归 + P2-6 assertShape 子形状）**：`scanLegacyAtSyntax` 提取 `scanSymbolLegacy(node, scope, warnings, depth)` 递归 helper 镜像 `validateSymbolNode` 的 children 遍历（scope 拼接 `.children[i]`，嵌套子图元 `@{pointId}` 方言 warn；对称加 depth cap ~100 防深嵌套 crash）；新增 `assertShape(value, schema, scope, errors)` helper（schema `{[field]: 'number'|'string'|'boolean'|'object'|'array'}`），接入 `shadow`（x/y/blur:number、color:string）/ animation `from,to`（object 形态校验 {x,y}）/ `background`（color:string）/ declaration `scale`（k:number、b:number）/ `init`（须 primitive，malformed 拒绝）。failing-first proof 10 用例（嵌套 legacy 不 warn + 子形状放行 → 修复后 warn + ok:false）。
- **owner doc 同步**：`design-renderer.md §4.3` 校验行反映数值有限性 / 子形状校验 / 深度上限 / legacy 递归 + `deepEqual` array/object 不混同。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 718/718 / 55 files，较 696 baseline +22 tests：Phase 1 ×6 + Phase 2 ×6 + Phase 3 ×10）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b501b1bffeMJryRe3w656jt2`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 P2-2 array/object 守卫 + 2-arg 签名兼容；P2-3 Number.isFinite + deadband 路由；P2-9 **三处**深度 cap——equality.ts MAX_DEEP_EQUAL_DEPTH + validate.ts validateSymbolNode MAX_VALIDATE_DEPTH + scanSymbolLegacy MAX_VALIDATE_DEPTH，执行期一度遗漏的 scanSymbolLegacy cap 已补齐；P2-5 legacy children 递归 + scope；P2-6 assertShape 五接入点；workspace 全量 32+32+32+59 全绿；owner doc + roadmap 回写；零 build artifact），零 Blocker / 零 Major / 零 Minor。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-9 + open P2-2/P2-3/P2-5/P2-6 五条标注「已由 plan `2026-08-06-0900-1` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0900-2`/`-0900-3` 或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 diagnostic-channel residual plan `2026-08-06-0746-3` 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-06-0746-3-industrial-hmi-diagnostic-channel-residual.md`（两 Phase，2129 multi-audit dim 19 两条诊断通道 debuggability residual P2 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-06-0746-3-industrial-hmi-diagnostic-channel-residual.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **共同缺陷模式**：plan `2026-08-05-2129-3` Phase 3 接通 `RefreshPipeline.onError` → `scada-canvas reportDiagnostic` 后显式 deferred 的两条 residual——(P2-5) handler-error 诊断出口仍丢原始 Error（message-only）且无 host telemetry 路径（`reportDiagnostic` 仅对 flux-\* 码转发 monitor）；(P2-6) `findCircularDependencyError` cause-chain 深度上限硬编码 10，10+ 层 expression chain 的 cycle 误判 generic eval。两条共享同一收口判据：P1-2 接通的 onError 通道在「信息完整性 / 适用范围」上的两个 gap。
- **Phase 1（multi P2-5 handler-error telemetry 对称）**：Decision 裁定 handler-error 经已接 `'action'` phase 的**宽** telemetry 面 `RendererPlugin.onError`（`ErrorMonitorPayload`，`flux-action-core/action-execution.ts` 已用此面转发 action 错误）转发到 host，而非扩 `ExpressionExecutionEnv.monitor` 的 `'expression'` 字面量限定（拒绝——会模糊窄面语义且需改公共类型）。`scada-canvas.tsx`：`onHandlerError` 补第 3 参 `error` 透传（Fix-a）；`reportDiagnostic` 增 `handler-error` 分支经 `rendererRuntime.plugins` 迭代 `plugin.onError(reportedError, { phase:'action', error, details:{code:'handler-error'} })`（per-plugin try/catch 隔离），`reportedError` 统一 `new Error(message, { cause: error })` 保留 cause 链（Fix-b）。对称性：flux-\* → 窄面 `env.monitor.onError`（phase:'expression'）；handler-error → 宽面 `plugins[].onError`（phase:'action'）。§8.1 诊断≠升级契约不破（不动 setStatus/不派发 scada:error）。failing-first proof 由红转绿（修复前 handler-error 既不透传 error 也无 telemetry 路径）。
- **Phase 2（multi P2-6 cycle depth 上限）**：`dirty-collector.ts` 新增 `MAX_CAUSE_CHAIN_DEPTH = 1000` 常量，`findCircularDependencyError` loop `depth < 10` → `depth < MAX_CAUSE_CHAIN_DEPTH`；`cause === current`/`undefined` break 守卫保留（防无限循环），上限仅防御性兜底。failing-first proof 由红转绿（12/50 层 wrapper 包裹 `CircularDependencyError` 旧上限返 undefined → 误判 generic eval；新上限 toBe identity 返原实例）+ 浅链 5 层回归守护 + 自环守卫用例。
- **test-support 扩展**：`createScadaTestEnvironment` 增可选 3rd 参 `options?: { plugins?: RendererPlugin[] }` 透传 `createRendererRuntime({ plugins })`（向后兼容，93 处既有 2-arg 调用零改动）——供 Phase 1 proof 注入 plugin.onError spy 断言 host 宽面可达。
- **owner docs 同步**：`design-renderer.md §8.1`（运行期诊断出口段——两 telemetry 路径对称 + cause 透传段——handler-error 同样透传原始 error，移除「host telemetry 后置 Follow-up」stale 文本）、`design-data-binding.md §9.1`（新增「诊断通道 telemetry 面对称」+「环检测 cause 链深度上限」两 blockquote）反映 live baseline。
- **Decision 诚实**：未改 `flux-core` 公共类型——`ExpressionErrorMonitorPayload.phase` 字面量 `'expression'` 不变、`ErrorMonitorPayload` phase 联合不变（已含 `'action'`）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 696/696 / 54 files，+7 tests：Phase 1 ×2 + Phase 2 ×5）。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02b749de7ffea3gWZyXOU7bQlf`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 Phase 1 scada-canvas 两 telemetry 分支 + onHandlerError 透传 + proof 断言 phase/Error.cause identity + §8.1 不升级；Phase 2 MAX_CAUSE_CHAIN_DEPTH + break 守卫保留 + proof toBe identity；Decision 诚实——公共类型零改 + test-support 向后兼容；owner docs 无 stale 文本；P2-4 为已收口 Non-Goal 非 deferred；文本一致性），零 Blocker / 零 Major / 零 Minor。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-5/P2-6 两条标注「已由 plan `2026-08-06-0746-3` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归后续 mission 节奏；P2-5 Decision 若未来 host 统一 telemetry 面可回头收敛窄/宽面双轨）。

## industrial-hmi 2129 post-remediation audit P2 test-fidelity plan `2026-08-06-0746-2` 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-06-0746-2-industrial-hmi-test-fidelity-false-green.md`（单 Phase，2129 multi-audit dim 23/14 四条 test-fidelity P2 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-06-0746-2-industrial-hmi-test-fidelity-false-green.md`（Phase 1 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **共同缺陷模式**：4 条测试标题声称验 X，但断言的是无关点 / 弱 `not.toThrow()` / 内联等价副本——被测行为退化时测试仍绿（false-green），CI 绿不反映真实行为。本计划仅改测试，零产品运行时/源码改动（仅 4 个 `.test.ts(x)` 文件）。
- **P2-12（codemod 等价副本 → 生产函数同步）**：`expression-codemod.test.ts` 内联副本补齐 `QUOTED_DOLLAR_PATTERN`（parity，3 patterns）+ regex source 与生产逐字对齐；新增**生产源码守护测试**读 `scripts/scada-expression-codemod.mjs`，精确比对每个 pattern 的 regex source（规范化 `\-`→`-` 以过 ESLint `no-useless-escape`）+ `migrateContent` 实际 used-pattern 集合，生产改 regex/启用 QUOTED_DOLLAR 即转红。**fallback 裁定**：primary path（`import { migrateContent }`）经三重 live 阻断确认不可行——生产 `.mjs:89` top-level `main(process.argv)` 致 import 即 `process.exit(1)`、`tsconfig.base.json` `allowJs:false`、自定义 `types/node-fs.d.ts` 仅声明 `node:path` 的 `join/dirname/relative/extname` + `ImportMeta.dirname`；采用 plan sanctioned fallback（内联副本 parity + 生产源码守护测试），同等关闭 false-green。新增 `QUOTED_DOLLAR_PATTERN` 专属用例（独立引号 `'$xxx'` 当前 count=0，刻画生产当前跳过该 pattern）。
- **P2-13（blank 兜底断言）**：`binding-expression-unification.test.ts` 在触发 `syncExpressionPoint('blank')` 后补 `expect(harness.pointStore.getPointValue('blank')).toBe('${}')`（经 live 探测：空串 `''` normalize 为 `${}`，flux-formula 视作 static 字面量，求值结果 `'${}'`）+ `expect(onError).not.toHaveBeenCalled()`（兜底正常求值不应上报）。原断言仅验无关点 `a`，`blank` 从未被断言。
- **P2-14（scene build 真断言）**：`scada-canvas-smoke.test.tsx` test 1 经 test handle（renderer 默认 `exposeTestHandle:true`）补 `expect(handle.getSymbol('rect-1')).toBeTruthy()`（图元入树）+ `expect(root.getAttribute('data-status')).toBe('ready'`（构建完成）。原仅 `handles.length>0`（等价 not.toThrow）。
- **P2-15（空 config flushFrame 真断言）**：`variables-optional-contract.test.ts` 把 `expect(() => pipeline.flushFrame(...)).not.toThrow()` 替换为 `expect(pipeline.flushFrame(applyAttrs)).toBe(false)` + `expect(applyAttrs).not.toHaveBeenCalled()`（`applyAttrs` 为 `vi.fn()`）。原不验返值/副作用。
- **failing-first 手验（每条）**：P2-12 临时改坏生产 `AT_SYNTAX_PATTERN`/`DOLLAR_SHORTHAND_PATTERN` regex → 守护测试转红；P2-13 临时移除 `?? ''` 兜底（expression 变 undefined）→ `blank` 保持 init `0` ≠ `'${}'` 转红；P2-14 临时清空 `validConfig.symbols` → `getSymbol('rect-1')` 返 undefined 转红；P2-15 临时把 `dirty-collector.ts:281` 空脏早退 `return false` 改 `return true` → flushFrame 返 true 转红。均恢复后转绿。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 包测试全绿，4 目标文件 49/49）。`git diff --stat` 仅 4 测试文件 + plan + roadmap（doc），`scripts/` 与 `dirty-collector.ts` 零改动。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02b82fd81ffe9BPYP3RslQJKaQ`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 4 P2 fix 断言结果值非 not.toThrow + P2-12 fallback 三重 live 阻断 + 重跑 4 测试文件 49/49 绿 + git diff 确认零源码改动），零 Blocker / 零 Major。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-12/P2-13/P2-14/P2-15 四条标注「已由 plan `2026-08-06-0746-2` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0746-3`（diagnostic residual，active）或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 doc-drift cleanup plan `2026-08-06-0746-1` 收口（mission-driver build verify 全绿）

执行 plan `docs/plans/2026-08-06-0746-1-industrial-hmi-doc-drift-cleanup.md`（单 Phase，2129 multi-audit dim 16 三条纯文档漂移 P2 收口），独立 closure-audit 通过；本条目由 mission-driver `2026-08-05-212942-mission-driver` build-verify 轮记录。

- **plan path**：`docs/plans/2026-08-06-0746-1-industrial-hmi-doc-drift-cleanup.md`（Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate，Test Strategy 档位「不适用」）。
- **根因**：I18 表达式一元化（弃用 `$xxx`/`@{pointId}`、删 `binding/expression-evaluator.ts`、拆 `bind-resolver.ts`+`flux-eval.ts`）与历次模块迁移（`schemas.ts`/`renderer-definitions.ts` 上移 `src/` 顶层、新增 `scada-errors.ts`/`serialization/equality.ts`/`use-scada-handles.ts`）后设计文档未同步回写。
- **P2-1（§11 实现树）**：`design-renderer.md §11` 代码树重写对齐 live 布局——`schemas.ts`/`renderer-definitions.ts` 从 `renderer/` 上移 `src/` 顶层；`serialization/` 补 `equality.ts`（plan 0653-4 C2 / W5 共享 deepEqual）；`renderer/hooks/` 补 `use-scada-handles.ts`（component:\* 句柄注册）；`renderer/` 补 `scada-errors.ts`（SCADA_ERROR_CODES 注册表，plan 1558-2 Phase 4）。
- **P2-2（§9:270 $xxx）**：`design-engine.md §9:270` `$xxx` → `${expr}`（I18 一元化后唯一语法）+ 指向 `design-data-binding.md §4.2`。
- **P2-3（editor-initiation row 8）**：`editor-initiation.md:60` row 8 binding 域模块清单对齐 `design-data-binding.md §11` post-I18 拆分——移除 `expression-evaluator（@{pointId} 子集）`，补 `bind-resolver.ts`/`flux-eval.ts`/`reverse-index.ts`/`dirty-collector.ts`/`value-to-state.ts`/`animator.ts`。
- **纯文档零代码变更**：`git diff --stat` 仅 4 个 `.md`（design-engine 1 行 / design-renderer 11 行 / editor-initiation 1 行 / roadmap 8 行）+ 3 个新增 plan `.md`，`packages/` 无任何源码/测试/配置改动。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b9a5e09ffeRPgEm8CsYAk3tG` 执行 closure-audit（live `ls` 逐项核验 §11 树文件路径 + rg 判据收窄到 3 编辑面排除 `design-data-binding.md` 弃用通告段），零 Blocker / 零 Major。
- **mission-driver build verify（本轮）**：纯文档计划，build/test/lint/typecheck gate 按 plan guide「纯文档计划」条款豁免；为确认提交前基线绿，本轮仍跑全量——`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（均 FULL TURBO 缓存命中，零源码改动故缓存有效）。e2e 未跑（非 full-green 声明）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` 新增 completion 活动条目 + Follow-up Backlog「2026-08-05-2129」multi P2-1/P2-2/P2-3 三条标注「已由 plan `2026-08-06-0746-1` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0746-2`（test fidelity，active）/`2026-08-06-0746-3`（diagnostic residual，active）或后续 mission 节奏）。

## industrial-hmi 2129 audit P1 四项 remediation plan 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-05-2129-3-industrial-hmi-audit-p1-remediations.md` 全四 Phase（4×P1 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-3-industrial-hmi-audit-p1-remediations.md`（四 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **源审计**：`docs/audits/2026-08-05-2129-open-audit-industrial-hmi.md`（1×P1）+ `docs/audits/2026-08-05-2129-multi-audit-industrial-hmi.md`（3×P1），两份 `Audit Status: planned → closed`。
- **Phase 1（multi P1-3 oversized-code-files）**：`serialization.test.ts`（749）拆为 parse-serialize/diff/validate + fixtures，`refresh-pipeline.test.ts`（726）拆为 core/state/animation-lifecycle + fixtures，8 兄弟均 <700 行（wc -l 实测），纯机械搬迁零行为变更。
- **Phase 2（multi P1-1 binding-expression scope 订阅）**：`use-scada-points-bridge.ts` 新增 `collectBindingExpressionScopePaths` 扫 `symbols[].bindings[].expression`（递归 children）经 `probeExpressionPaths` 提取 scope 路径（排除声明点 id），`paths` = variables ∪ binding-expression → `variables:[]` + binding.expression 直连 scope 场景 `useScopeSelector` enabled、binding 正确求值。Integration test `binding-expression-scope-integration.test.tsx` 挂真实桥接边界（useScadaPointsBridge + useScopeSelector + 真实 pipeline runtime），断言 text=42（scopeVal=21）→ 60（reactive update）+ nested=6，关闭 variables-optional-contract 场景 ① false-green。
- **Phase 3（multi P1-2 RefreshPipeline.onError 生产接线）**：`createBindingDomain` 增 `onError` 参数；`UseScadaEngineArgs.onPipelineError` ← `scada-canvas.tsx:182 reportDiagnostic`（与桥接层同出口），mount + reloadBindings 两路径透传。`dirty-collector.ts evaluateFlux` 改返 discriminated `FluxEvalOutcome`（compile-failed/evaluate-failed/ok）使 `reportError` emit 对称码 `flux-compile-failed`/`flux-evaluate-failed`，三个声明源 source:flux / binding.expression / scale.expression observably symmetric。Integration test `scada-canvas-diagnostic-channels.test.tsx:312+` 断言坏 binding.expression 错误到达 monitor.onError（phase:'expression', code:'flux-evaluate-failed'）+ 对称性 proof。
- **Phase 4（open P1-1 shadow revert 完整性）**：`visual-state.ts STYLE_RESET_DEFAULTS` 改 `Record<keyof ScadaSymbolStylePatch, unknown>` 穷尽守卫（编译期防未来新增 style 字段机械遗漏）+ 补 `shadow: {x:0,y:0,blur:0,color:'transparent'}` reset 值 + hoist `applied.delete(key)` 到 `if (revert !== undefined)` 守卫之外（belt-and-suspenders 防追踪泄漏）。翻转既有缺陷测试 `state-visual.test.ts:201-243`：退出 fault 态后 shadow 清除为 reset default（非永久残留）+ 重入 fault 再应用。
- **owner docs 同步**：`design-data-binding.md §9.1`（无点表直连 scope 端到端 wiring Phase 2 + 错误码对称 Phase 3）、`design-renderer.md:220`（pipeline onError 通道接线 + FluxEvalOutcome + 对称码）、`visual-state.ts:10-19`（reset map 穷尽守卫 + applied.delete hoist 结构注记）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 687/687 / 54 files）、`pnpm check:oversized-code-files` industrial 包 0 失败（14 ERROR 文件全在其他包，pre-existing out-of-scope）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02bb6a641ffe7eR3pBVTw6MA5v`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 4 P1 fix + proof 断言结果值非 call-count + 机械门禁），零 Blocker / 零 Major。
- **roadmap 回写**：`roadmap-industrial-hmi.md` 新增 completion 活动条目 + P2 backlog「2026-08-05-2129」子节源标注更新（P1 已收口、源审计 closed）。
- **Follow-up**：25 P2 在 roadmap Follow-up Backlog「2026-08-05-2129 post-remediation audit P2」追踪（各带源审计路径）；无 plan-owned remaining work。

## industrial-hmi mission-driver build verify：unit + 变更域 e2e 全绿

mission-driver `2026-08-05-212942-mission-driver` 对 industrial-hmi 工作集做 build verify。

- `pnpm typecheck` 32/32 ✓（FULL TURBO 缓存命中）
- `pnpm build` 32/32 ✓（flux-playground 增量重建，leafer-examples-demo chunk 产出）
- `pnpm lint` 32/32 ✓（仅 gantt-grid 既有 react-hooks/incompatible-library warning，0 error）
- `pnpm test` 59/59 ✓（vitest 全包绿）
- 变更域 e2e（scada-demo + leafer-examples + playground-entry-pages）81/81 ✓
- 说明：既有 pre-existing e2e 失败（gantt/kanban/scada-perf FPS + w3d-editor TipTap）不在本工作集，未跑；unit 100% + 变更域 e2e 100% 绿。

## industrial-hmi I17 Demo 视觉优化 + 参考资源登记 + LeaferJS 对照页 plan 全三 Phase 收口

执行 plan `docs/plans/2026-08-05-2129-2-industrial-hmi-demo-polish-leafer-examples.md` 全三 Phase，独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-2-industrial-hmi-demo-polish-leafer-examples.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **Phase 1（scada-demo 坐标重排 + e2e 坐标同步）**：`apps/playground/src/pages/scada-demo.tsx` `symbols` 数组坐标重排——管道统一 y=232（5 条 pipe 全部对齐）、设备-管道几何连续（pipe-in→level→pipe→pump→pipe→valve→pipe→junction→pipe→gauge 链 x:0→800 无断点）、8px 对齐基线、文本标签锚定到设备 bounds 下方。**不变性守护**：diff 仅含 x/y/width/height + text x/y，id/type/testid/bindings/events/custom/text/textSize/textColor 零改动。`tests/e2e/scada-demo.spec.ts` 4 处 world 坐标同步：pump-1 中心 (242,234)→(236,232) ×3、motor-1 中心 (402,64)→(408,80) ×1。
- **Phase 2（参考资源附录登记）**：`design-engine.md` 新增 §13「LeaferJS 官方示例对照」附录（9 链接：App/viewport/坐标/包围盒/局部渲染/Group/Editor/Flow/viewport 插件，对应引擎层各章节）；`design-symbols.md` 新增 §13「第三方图元库参考」附录（meta2d MIT 无 HMI 图元 / FUXA MIT 真实 SCADA / OSHMI GPL-3.0 仅设计层 + 定性与 roadmap I17.2 一致）。
- **Phase 3（playground LeaferJS 对照页）**：新增 `apps/playground/src/pages/leafer-examples-demo.tsx`（4 示例：基础元素 / App 三图层 / animate 动画 / viewport zoom-move），`route-model.ts` 加 `leafer-examples` 条目（eyebrow 'Reference'，**不进 home 卡片**），`App.tsx` lazy-load（`leafer-ui` canvas runtime 隔离，保持 App 单测 leafer-free）。`leafer-ui` + `@leafer-in/view` + `@leafer-in/animate`（v2.2.9）作为 playground 直接依赖引入（plan 假设 transitive 可用经核实不成立 → 直接依赖修正）。e2e smoke：`leafer-examples.spec.ts`（路由可达 + 4 canvas 存在 + viewport 控件交互）+ `playground-entry-pages.spec.ts` 加 ROUTE_ASSERTIONS 条目。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓。e2e：scada-demo 12/12 ✓、leafer-examples 2/2 ✓、playground-entry-pages 67/67 ✓。5 项 pre-existing 失败（gantt/kanban/scada-perf FPS + w3d-editor TipTap）与本 plan 文件无交集。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02c871be4ffeTr1Sx3cbR9Cdlm` 执行 closure-audit（live repo 核对 Phase 1 不变性 + 管道几何连续 + e2e 坐标 + Phase 2 链接/定性 + Phase 3 路由/非卡片 + 无静默降级），零 Blocker / 零 Major / 2 Minor 记录性（M1 roadmap 待翻 done、M2 leafer-ui 直接依赖记录）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` I17 Phase Status `todo` → `done`。
- **Follow-up**：无剩余 plan-owned 工作。编辑器交互按 `editor-initiation.md` 独立后继 mission 编排。

## industrial-hmi I18 表达式一元化 plan 收口：Phase 4 owner-docs 同步 + lint/e2e 修复 + closure-audit approved

执行 plan `docs/plans/2026-08-05-2129-1-industrial-hmi-expression-unification.md` Phase 4（验证与文档同步）+ 修复 Phase 1-3 遗留 lint 错误与 demo e2e 回归，独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-1-industrial-hmi-expression-unification.md`（Phase 1-4 全部 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]`）。
- **Phase 4 Fix（owner docs 同步）**：`design-data-binding.md`（决策表双轨→单一 `${expr}` + 点表可选、§4.1/§4.2 类型注释、§4.2 表达式求值边界、§9.1 三源接入表+注、§11 实现拆分删 expression-evaluator.ts 增 flux-eval.ts、§12.2 风险）、`design-renderer.md`（§8.3 `$xxx`→`${...}`）、`roadmap-industrial-hmi.md`（I18.3「无 demo 迁移」过时行回写、Phase Details「demo 免迁移」回写 + contract-lock 注记）。
- **lint 修复（Phase 1-3 遗留，3 项）**：① `dirty-collector.ts:450` `no-this-alias`——`buildEvalScope()` 的 `get` 方法从 regular method + `const self = this` 改为 arrow function（行为保持）；② `expression-codemod.test.ts:23` `no-useless-escape`——regex 中 `\-` 去 escape；③ `refresh-pipeline.test.ts` `max-lines`（785→726，B3 stateSource + scale 转发两组 describe 的重复 boilerplate 收口为 helper，断言全保留）。
- **demo e2e 回归修复**：Phase 3 将 demo `flux: '$tankLevel'` 迁为 `flux: '${tankLevel}'`，但 `${...}` 在 schema-embedded config 中被 flux schema compiler **急切求值**（表达式字符串→求值数字），validator 拒绝（flux must be string）→ 画布 `data-status="error"` → 12 个 scada-demo e2e 全红。修复：改为**裸路径** `flux: 'tankLevel'`（桥接 `normalizeFluxExpression` 运行时包成 `${tankLevel}`），规避 schema compiler 急切求值。这是 schema-embedded config 的 I18 正确模式（`normalizeFluxExpression` 的裸路径包裹分支即为此设计）。
- **Phase 4 Proof（workspace 全量验证）**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 683 tests）。scada e2e：demo/edge-cases/pressure-demo 21/21 ✓；perf 100k pan test 通过（机器负载敏感，低负载时通过）。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02ca871ccffeMTp9GWO2Q6hXwI` 执行 closure-audit（live repo 核对 9 项 checklist），零 Blocker / 零 Major。closure-audit gate 由该 sub-agent tick（执行 session 不自审）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` I18 Phase Status `todo` → `done`。
- **Follow-up**：旧 `@{}` config 下线后把 validator `legacy-at-syntax` warn 收敛为 fail（Non-Blocking Follow-up，按 mission 节奏）。
