# Development Log - 2026-05-06

### 2026-05-06

- Executed the first closure slice of `docs/plans/219-table-row-scope-publication-and-invalidation-closure-plan.md` by moving table row-scope reconciliation out of render phase, collapsing row payload publication to one minimal `merge()` change-set per affected row, and adding owner-key/unmount cache cleanup in `packages/flux-renderers-data/src/table-renderer/use-table-row-scope-cache.ts`.
- Link to code: `packages/flux-renderers-data/src/table-renderer/use-table-row-scope-cache.ts`, `packages/flux-renderers-data/src/test-support.tsx`, `packages/flux-renderers-data/src/__tests__/{use-table-row-scope-cache.test.tsx,data-table.test.tsx}`.
- Link to docs: `docs/plans/219-table-row-scope-publication-and-invalidation-closure-plan.md`, `docs/architecture/{dependency-tracking.md,table-row-identity-and-scope-performance.md}`.
- Key decision: keep this owner slice table-specific and honest; table now has a supported row-local translation baseline, but generic collection-owner translation for loop/list/tree remains future Phase 4 follow-up rather than an implied closure claim.
- Verification: package-local focused proofs passed via `pnpm exec vitest run --config vitest.config.ts src/__tests__/use-table-row-scope-cache.test.tsx src/__tests__/data-table.test.tsx --reporter=dot` in `packages/flux-renderers-data`; the package script form `pnpm --filter @nop-chaos/flux-renderers-data test -- --run ...` still hung in this environment after printing warnings, so broader verification is continuing via direct `vitest --config` and package/workspace commands.
- Next step: finish package/workspace `typecheck`, `build`, `lint`, and `test`, then run an independent closure audit before marking plan `219` complete.

### 2026-05-06

- Fixed the playground `lab/data-source` example so it follows the live parameterized-region and data-source contracts instead of the old flattened loop-binding and `api/target` schema shapes.
- Link to code: `apps/playground/src/component-lab/renderers/data-source-lab-page.tsx`, `apps/playground/src/component-lab/renderers/data-source-lab-page.test.tsx`.
- Key decision: keep the runtime unchanged because the live docs/tests already define loop body params under `$slot.*` and data-source publication by `name`; the bug was a stale playground example, not a runtime regression.
- Verification: `pnpm --filter @nop-chaos/flux-playground test -- data-source-lab-page.test.tsx`, `pnpm --filter @nop-chaos/flux-playground typecheck`, `pnpm --filter @nop-chaos/flux-playground build`, `pnpm --filter @nop-chaos/flux-playground lint`, plus full-workspace `pnpm test` all passed.
- Next step: audit nearby playground labs that still use legacy `badge.label` or pre-convergence data-source schema shapes so examples stay aligned with the live renderer/runtime contracts.

### 2026-05-06

- Completed `docs/plans/218-form-runtime-structure-and-type-topology-simplification-plan.md` by flattening the internal `form-runtime` shared-state topology in `packages/flux-runtime/src/form-runtime-types.ts` without changing runtime behavior or reopening adjacent owner plans.
- Link to code: `packages/flux-runtime/src/form-runtime-types.ts`, `packages/flux-runtime/src/__tests__/form-runtime-registration.test.ts`.
- Link to docs: `docs/plans/218-form-runtime-structure-and-type-topology-simplification-plan.md`, `docs/logs/2026/05-06.md`.
- Key decision: keep intermediate type aliases only where they still help local consumer readability, but make `ManagedFormRuntimeSharedState` and owner-facing slices readable through direct composition rather than a required relay chain through `FormRuntimeRegistrationState` and `FormRuntimeOwnerState`; no owner-doc update was required because `docs/architecture/{form-validation.md,flux-runtime-module-boundaries.md}` describe behavior/module ownership, not this internal type layering.
- Verification: focused `flux-runtime` proof passed via `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/form-runtime-array.test.ts src/__tests__/form-runtime-owner-lifecycle.test.ts src/__tests__/form-runtime-subtree.test.ts src/__tests__/form-runtime-registration.test.ts --reporter=verbose`; full-workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Independent closure audit: a separate audit initially blocked closure until the final relay naming and plan/log synchronization landed; after the direct-composition follow-up and docs sync, the plan closed honestly with no remaining in-scope work.
- Next step: none for plan `218`; reopen only if future live evidence shows a new owner-scoped `form-runtime` type or module defect.

### 2026-05-06

- Closed `docs/plans/217-deep-audit-2026-05-06-confirmed-defect-remediation-plan.md` after landing the remaining deep-audit-owned remediation set, final manifest cleanup, and closure-audit-driven plan/log synchronization.
- Link to code: `packages/word-editor-renderers/src/{editor-canvas.tsx,__tests__/editor-canvas.test.tsx,package.json}`, `packages/flux-runtime/src/{action-adapter.ts,async-data/source-registry.ts,form-runtime-validation.ts,__tests__/action-adapter.unit.test.ts,__tests__/source-registry.test.ts,__tests__/hidden-field-policy.test.ts}`, `packages/flux-renderers-form/src/{renderers/form.tsx,renderers/input.tsx,field-utils/field-handlers.tsx,__tests__/form-renderer-lifecycle.test.tsx,__tests__/form-validation-ui.test.tsx}`, `packages/flux-renderers-form-advanced/src/{condition-builder.tsx,tag-list.tsx,key-value.tsx,array-editor.tsx,condition-builder/types.ts,condition-builder/condition-group.tsx,wrapped-field-action.tsx,__tests__/widget-classname-contract.test.tsx}`, `packages/flux-code-editor/src/code-editor-renderer/toolbar-button.tsx`, `packages/flux-renderers-basic/src/{use-surface-renderer.ts,__tests__/basic-page-layout.test.tsx}`, `packages/flux-react/src/node-renderer.tsx`, `packages/report-designer-core/src/{core.ts,__tests__/designer-core.test.ts}`, `packages/flux-renderers-data/src/{table-renderer/table-quick-edit-controller.ts,__tests__/table-quick-edit-controller.test.tsx}`.
- Link to docs: `docs/plans/217-deep-audit-2026-05-06-confirmed-defect-remediation-plan.md`, `docs/architecture/{renderer-runtime.md,surface-owner.md,form-validation.md}`, `docs/components/condition-builder/design.md`.
- Key decision: keep 217 bounded to the 05-06 plan-owned confirmed set only; do not reopen plan `211`'s earlier surface/render-phase defects or plan `212`'s WrappedFieldAction baseline, and treat broader `__actionScope` / `node-renderer` cleanup as future reopen candidates rather than dishonest closure blockers.
- Verification: focused Vitest proofs passed for the landed defect families; `pnpm install` succeeded with a non-blocking `pnpm-lock.yaml not compatible with current pnpm` warning; `pnpm check:workspace-manifest-deps`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed on the final closure path.
- Independent closure audit: initial audit caught one remaining manifest issue in `packages/word-editor-renderers/package.json` plus a stale workspace-test blocker note; after the final manifest fix and fresh full-workspace `pnpm test` pass, a second independent audit confirmed 217 is honestly closure-ready and the plan was updated to `completed`.
- Next step: none for plan `217`; only reopen if new live evidence justifies a separate runtime/type-safety owner plan.

### 2026-05-06

- Re-audited `docs/analysis/over-engineering-analysis.md` against the live repo, current owner docs, and plans `163`, `185`, `200`, `201`, `211`, `217`; confirmed that most broad recommendations either conflict with the current baseline or are already adjudicated elsewhere, so the follow-up owner scope was narrowed to `docs/plans/218-form-runtime-structure-and-type-topology-simplification-plan.md` only.
- Link to docs: `docs/analysis/over-engineering-analysis.md`, `docs/plans/218-form-runtime-structure-and-type-topology-simplification-plan.md`, `docs/plans/{163-core-boundary-and-validation-owner-convergence-plan.md,185-large-file-hotspot-split-plan.md,200-duplicate-code-convergence-plan.md,201-surface-family-runtime-convergence-plan.md,211-runtime-state-reactivity-and-safety-closure-plan.md,217-deep-audit-2026-05-06-confirmed-defect-remediation-plan.md}`.
- Link to code: `packages/flux-runtime/src/form-runtime-types.ts`, `packages/flux-core/src/types/runtime.ts`, `packages/flux-react/src/{hooks.ts,contexts.ts,schema-renderer.tsx}`.
- Key decision: do not reopen baseline-conflicting suggestions such as moving host-neutral runtime contracts out of `flux-core`, deleting public renderer hooks, merging multiple contexts, or re-merging owner-shaped `form-runtime-*` modules; the only justified new owner surface is the internal type-topology simplification in `form-runtime-types.ts`.
- Independent review consensus: two separate subagent reviews first rejected the broad-plan approach, then approved the narrowed `218` draft after scope was reduced to type-topology-only and all overlap with active plan `217` was removed.
- Next step: execute `218` only after re-auditing the live `form-runtime-types.ts` consumer set and keeping any touched consumer changes type-only.

### 2026-05-06

- Closed `docs/plans/211-runtime-state-reactivity-and-safety-closure-plan.md` after landing the final retained async/error-propagation fixes and re-auditing the live repo against the original retained defect set.
- Link to code: `packages/flux-renderers-form/src/{renderers/form.tsx,__tests__/form-renderer-lifecycle.test.tsx}`, `packages/flux-renderers-form-advanced/src/detail-view/{detail-view.tsx,detail-field.tsx,detail-revalidation.test.tsx}`, `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,spreadsheet-interactions/use-selection.ts,use-spreadsheet-interactions.ts,__tests__/grid-selection.test.tsx}`, `packages/word-editor-renderers/src/{word-editor-page.tsx,__tests__/word-editor-page-actions.test.tsx}`, `packages/flux-runtime/src/runtime-factory.ts`.
- Link to docs: `docs/plans/211-runtime-state-reactivity-and-safety-closure-plan.md`, `docs/logs/2026/05-06.md`.
- Key decision: keep phases 3 and 4 bounded to the retained user-visible/runtime-contract defects from the audit instead of reopening broader async resource-management or general doc-baseline work; no additional owner-architecture doc updates were required because the supported baseline did not change.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/form-renderer-lifecycle.test.tsx --reporter=verbose`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/detail-view/detail-revalidation.test.tsx --reporter=verbose`, `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/grid-selection.test.tsx --reporter=verbose`, `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-page-actions.test.tsx --reporter=verbose`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` all passed.
- Independent closure audit: final repo re-check found no remaining in-scope retained blocker in dimensions `04`, `05`, `06`, `07`, `08`, `13`, or `19`, so plan `211` now closes honestly instead of carrying a stale “async slice still open” note.
- Next step: none for plan `211`; any future reopen requires new live evidence and fresh owner scope.

### 2026-05-06

- Closed `docs/plans/216-open-ended-adversarial-review-residual-integrity-plan.md` after final workspace verification and plan/status synchronization.
- Link to docs: `docs/plans/216-open-ended-adversarial-review-residual-integrity-plan.md`, `docs/logs/2026/05-06.md`.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed for the full workspace on the final closure pass; the previously stale `flux-renderers-form-advanced` assertions were updated to match the current accessibility contract in `packages/flux-renderers-form-advanced/src/{__tests__/tree-structure.test.tsx,__tests__/tree-values.test.tsx,__tests__/form-tree-value-binding.test.tsx,__tests__/form-tree-control-source-states.test.tsx,condition-builder/condition-builder-renderer.test.tsx,detail-view/value-adaptation-helper.test.ts}`.
- Key decision: treat the remaining form-advanced failures as outdated test baselines rather than reopening product-code ownership, because the live renderer outputs already matched the current package contract and focused re-runs confirmed that alignment.
- Independent closure audit: a separate audit pass rechecked the in-scope plan-216 surfaces and only blocked closure until the plan text plus full-verification evidence were synchronized; no residual code blocker remained after the final verification run.
- Next step: none for plan `216`; any future reopen from the broader 2026-05-06 review requires a fresh owner decision.

### 2026-05-06

- Added `docs/references/reopened-design-decisions-and-audit-adjudications.md` and wired it into both audit prompt families so repeated reopen churn now has one stable adjudication reference instead of being rediscovered from scratch in each new review or plan.
- Link to docs: `docs/references/reopened-design-decisions-and-audit-adjudications.md`, `docs/skills/deep-audit-prompts.md`, `docs/skills/open-ended-adversarial-review-prompt.md`, `docs/plans/217-deep-audit-2026-05-06-confirmed-defect-remediation-plan.md`.
- Key decision: deep-audit prompts must read the new reference doc as a pre-dispatch requirement, while the open-ended prompt keeps it as a pattern-triggered backcheck so open-ended review stays discovery-oriented instead of collapsing into a second checklist audit.
- Next step: when future repeated reopen patterns stabilize beyond one-off plan churn, fold them into this reference doc instead of duplicating the same adjudication in multiple prompt files.

### 2026-05-06

- Executed `docs/plans/216-open-ended-adversarial-review-residual-integrity-plan.md` across runtime guardrails, designer history/document integrity, root render/editor boundaries, and formula pipe semantics.
- Link to code: `packages/flux-runtime/src/async-data/{reaction-runtime.ts,source-registry.ts}`, `packages/flux-action-core/src/operation-control.ts`, `packages/report-designer-core/src/{core.ts,core-dispatch.ts,__tests__/designer-core.test.ts}`, `packages/spreadsheet-core/src/{core.ts,core/internal-state.ts,command-handlers/history-handlers.ts,__tests__/core-advanced.test.ts}`, `packages/flux-react/src/{schema-renderer.tsx,node-error-boundary.tsx,__tests__/error-boundary.test.tsx,__tests__/schema-renderer.test.tsx}`, `packages/flux-code-editor/src/{use-code-mirror.ts,use-code-mirror.test.tsx}`, `packages/word-editor-core/src/{document-io.ts,__tests__/document-io.test.ts}`, `packages/flux-formula/src/{compile/pipe-syntax.ts,index.test.ts}`.
- Link to docs: `docs/architecture/{renderer-runtime.md,flux-formula.md}`, `docs/architecture/report-designer/design.md`, `docs/architecture/word-editor/design.md`, `docs/plans/{192-deep-audit-full-6-and-adversarial-review-remediation-plan.md,203-runtime-validation-and-data-source-contract-closure-plan.md,214-report-designer-performance-hot-path-closure-plan.md,216-open-ended-adversarial-review-residual-integrity-plan.md}`.
- Key decision: keep predecessor plan closures intact except for explicitly reopened claims, and record those successor boundaries directly in plans `192`, `203`, and `214` instead of silently re-owning the same surfaces.
- Verification: focused Vitest runs passed for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-action-core`, `@nop-chaos/report-designer-core`, `@nop-chaos/spreadsheet-core`, `@nop-chaos/flux-react`, `@nop-chaos/flux-code-editor`, `@nop-chaos/word-editor-core`, and `@nop-chaos/flux-formula`; full-workspace verification + closure audit followed.
- Next step: finish plan `216` text/status sync after full verification and independent closure audit evidence are recorded.

### 2026-05-06 (Deep Audit)

- Completed full 20-dimension deep audit following `docs/skills/deep-audit-prompts.md` methodology.
- Link to results: `docs/analysis/2026-05-06-deep-audit-full/` (summary.md + 17 per-dimension files + review-results.md).
- Key findings: 2 P1 bugs (word-editor remount on chart insert, submitForm bare catch), 26 confirmed P2 items across 6 themes (async error swallowing, renderer className contract, type safety, ghost dependencies, accessibility, validation), 18 P2s downgraded after independent review.
- Key decision: applied 9 calibration patterns rigorously; independent review sub-agents re-verified all findings against actual source code; 6 original P1 test-coverage findings downgraded to P2/P3 (indirect coverage exists).
- Next step: fix P1s and highest-priority P2 themes (async error swallowing, renderer className batch).

### 2026-05-06

- Drafted `docs/plans/216-open-ended-adversarial-review-residual-integrity-plan.md` after re-auditing the 2026-05-06 open-ended adversarial-review findings against live code instead of reusing the raw analysis list.
- Link to docs: `docs/plans/216-open-ended-adversarial-review-residual-integrity-plan.md`, `docs/analysis/2026-05-06-open-ended-adversarial-review-01/{round-01.md,round-02.md,round-03.md,round-04.md,round-05.md}`.
- Key decision: keep the new plan narrowly scoped to still-live, currently unowned residual defects (runtime cascade guards, retry error metadata, designer history/document integrity, root render/editor host boundaries, formula pipe rewrite conflict) and explicitly exclude issues already owned by plans `211`-`214`.
- Follow-up review decision: after independent sub-agent review, tighten plan `216` to treat the runtime guardrail and report/spreadsheet integrity slices as explicit successor ownership against completed plans `192`, `203`, and `214`, expand spreadsheet/history and root-boundary targets, and remove the dishonest `Deferred But Adjudicated` catch-all for unreviewed residuals.
- Next step: run independent sub-agent plan reviews, revise the draft until the review reaches explicit approval/consensus, then execute under the new owner path.

- Closed additional retained runtime-state/reactivity items under `docs/plans/211-runtime-state-reactivity-and-safety-closure-plan.md`: declarative surface `defaultOpen` now flows through `SurfaceRuntime`/`SurfaceStore`, `NodeRenderer` moved prepared-import and named-action registration out of render phase, and hidden-parent validation now excludes descendant compiled fields.
- Link to code: `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/{form-store.ts,surface-runtime.ts,form-runtime-field-ops.ts,form-runtime-validation.ts}`, `packages/flux-renderers-basic/src/{use-surface-renderer.ts,__tests__/basic-page-layout.test.tsx}`, `packages/flux-react/src/{node-renderer.tsx,__tests__/compilation-and-boundaries.test.tsx,schema-renderer-imports-basic.test.tsx}`.
- Link to docs: `docs/architecture/{renderer-runtime.md,surface-owner.md,form-validation.md}`, `docs/plans/211-runtime-state-reactivity-and-safety-closure-plan.md`.
- Key decision: keep plan `211` open because the retained async user-visible rejection slice is not fully closed; the attempted WordEditor save-failure message path was reverted after focused proof failed, so it remains honest follow-up work instead of a claimed fix.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-basic exec vitest run src/__tests__/basic-page-layout.test.tsx -t "updates declarative dialog statusPath after local close interactions|does not reopen a defaultOpen declarative dialog after runtime close|marks only the top declarative surface as active|reactivates the next declarative surface when the top one closes" --reporter=verbose`, `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/compilation-and-boundaries.test.tsx -t "does not install prepared imports when render aborts before commit|keeps the no-import path off the import boundary setup fast path" --reporter=verbose`, `pnpm --filter @nop-chaos/flux-react exec vitest run src/schema-renderer-imports-basic.test.tsx -t "loads xui imports and dispatches imported namespace actions|keeps nested imported helpers shadowed by the nearest import frame" --reporter=verbose`.

- Closed the retained renderer/workbench/a11y defects tracked by `docs/plans/212-renderer-workbench-contract-and-accessibility-closure-plan.md` across report-designer roots, table deep regions, tree/detail/condition-builder accessibility, cross-package workbench vocabulary, and playground flow-designer public styling surface removal.
- Link to code: `packages/report-designer-renderers/src/{renderers.tsx,report-designer-inspector.tsx,report-designer-toolbar.tsx,host-action-provider.ts}`, `packages/flux-renderers-data/src/table-renderer/{table-quick-edit-cell.tsx,table-body-row-rendering.tsx,table-header-row.tsx}`, `packages/flux-renderers-form/src/renderers/input.tsx`, `packages/flux-renderers-form-advanced/src/{tree-controls.tsx,key-value.tsx,condition-builder/condition-item.tsx,detail-view/*}`, `packages/flow-designer-renderers/src/{designer-palette.tsx,designer-page.tsx,dingflow/*}`, `packages/word-editor-renderers/src/panels/{outline-panel.tsx,dataset-panel.tsx}`, `packages/flux-code-editor/src/source-resolvers.ts`, `apps/playground/src/{flow-designer-nodes.css,schemas/dingtalk-workflow-tree-schema.json,schemas/action-flow-tree-schema.json}`.
- Key decision: treat plan `212` as owner of the still-live `full-7` retained set and only mark it complete after the repo was re-audited, focused proof was added, and remaining repo-level verification failures were classified as unrelated baseline blockers.
- Verification: focused Vitest runs passed for `@nop-chaos/report-designer-renderers`, `@nop-chaos/flux-renderers-data`, `@nop-chaos/flux-renderers-form`, `@nop-chaos/flux-renderers-form-advanced`, `@nop-chaos/flow-designer-renderers`, and `@nop-chaos/flux-code-editor`; repo-level `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` were attempted.
- Repo blocker context: `pnpm lint` still fails on pre-existing `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-spreadsheet-shell.ts:22` (`react-hooks/set-state-in-effect`), and workspace `typecheck` / `build` / `test` still fail because `packages/flux-renderers-basic/src/use-surface-renderer.ts` references missing `SurfaceStoreApi` methods (`getUncontrolledOpen`, `clearUncontrolledOpen`).
- Next step: if desired, handle the unrelated workspace blockers in a separate owner task so full-repo verification returns green again.
