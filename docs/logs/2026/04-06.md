# Development Log â€” 2026-04-06

### 2026-04-06

- Executed the core implementation path for `docs/plans/38-action-api-source-convergence-migration-plan.md`: renamed the request contract surface toward `ApiSchema` / `ExecutableApiRequest` / `PreparedApiRequest`, added shared `control` carrier support on `ActionSchema`, tightened `ReactionSchema.actions` to a single root `ActionSchema`, introduced runtime `executeSource(...)`, and wired form option consumers to accept inline `type: 'source'` values through the shared runtime execution path.
- Link to code/docs: `packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/actions.ts`, `packages/flux-core/src/types/renderer-api.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-react/src/useSourceValue.ts`, `packages/flux-renderers-form/src/renderers/input.tsx`, `docs/examples/user-management-schema.md`, `apps/playground/src/pages/fluxBasicPageSchema.json`
- Key decision: keep `executeApiObject` as a compatibility export aliased to the new `executeApiSchema` semantics, and introduce the first practical `type: 'source'` consumption path on async form option fields instead of attempting an unsafe repo-wide async prop-resolution rewrite in one step.
- Next step: finish broader documentation wording cleanup around remaining legacy `ApiObject` mentions in narrower docs and extend `type: 'source'` consumption beyond form option fields if more renderer value slots need runtime-owned async values.

### 2026-04-06

- Updated the action/api architecture docs to reflect the settled discussion outcomes from `docs/discussions/2026-04-06-action-api-design-evolution.md`: renamed declarative request wording toward `ApiSchema` versus fetcher-facing `ExecutableApiRequest`, moved request coordination out of the transport contract, documented `type: 'source'` as the anonymous action-based value form, aligned `data-source` as the named/scheduled extension of source, clarified single-root action entry for event authoring, and standardized built-in action naming toward camelCase selectors such as `openDialog` and `showToast` while keeping `component:` and `namespace:` channels explicit. Added a detailed migration plan for the follow-up implementation and doc convergence work.
- Link to docs: `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/frontend-programming-model-improvement-design.md`, `docs/references/flux-json-conventions.md`, `docs/plans/38-action-api-source-convergence-migration-plan.md`
- Key decision: simplify the model by treating anonymous `source` as the action-based value-consumption form and `data-source` as its named/scheduled extension, instead of maintaining a second unrelated dynamic-value DSL.
- Next step: align runtime types and implementation contracts (`packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/actions.ts`, `packages/flux-runtime/src/request-runtime.ts`, and source/action execution paths) to the updated architecture baseline.

### 2026-04-06

- Reviewed `docs/architecture/action-algebra-formal-spec.md` against the top-level programming-model docs and merged the surviving useful parts into the two programming-model documents: action `DAG` is now stated more explicitly as compiler-assembled and structurally acyclic under normal authoring, and compile-time action validation responsibilities are now recorded as part of the convergence direction. Deliberately did not promote the formal spec's more rigid scheduler-priority ladder or fixed numeric cascade defaults into the top-level baseline.
- Link to docs: `docs/architecture/action-algebra-formal-spec.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/frontend-programming-model-improvement-design.md`
- Key decision: keep only the formal spec content that clarifies stable architectural boundaries and validation ownership; leave executor-specific scheduling details in narrower specs until they are truly stable.
- Next step: if needed, tighten `docs/architecture/action-scope-and-imports.md` so its active wording matches the compiler-assembled action-graph and reserved-control-field interpretation rules.

### 2026-04-06

- Added a new companion architecture doc that records only the surviving improvement tracks for the top-level frontend programming model instead of proposing a replacement primitive system. The new doc keeps the seven-primitive closure intact and narrows follow-up work to `Action Algebra`, `Operation Control`, semantic lifecycle entry, `Resource` publication convergence, structure-DSL boundaries, and host-boundary protocol tightening.
- Link to docs: `docs/architecture/frontend-programming-model-improvement-design.md`, `docs/architecture/frontend-programming-model.md`, `docs/discussions/2026-04-06-programming-model-optimality-critique.md`, `docs/index.md`
- Key decision: record the critique outcome as an architecture follow-up design under `docs/architecture/` rather than leaving it only in discussion notes or reviving the broader staged-reactive replacement proposal.
- Next step: align narrower architecture docs such as `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, and `docs/architecture/form-validation.md` to the new follow-up convergence list where the active wording is still weaker than the top-level target.

### 2026-04-06

- Folded the stabilized discussion conclusions back into the normative top-level frontend programming model: added explicit `DSL`-progressive guidance around `Value` versus named `data-source`, clarified `data-source` as named dynamic-value registration, made `name` the normative default publication path, narrowed special publish behavior to `mergeToScope: true`, defined `visible` / `when` / `loop` / `dynamic-renderer` boundaries, and formalized action authoring as progressive while execution semantics are already `DAG`-shaped.
- Link to docs: `docs/architecture/frontend-programming-model.md`, `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: keep the seven-primitive closure and tighten the normative wording instead of introducing a replacement primitive system.
- Next step: align `docs/architecture/api-data-source.md` to the top-level `name` + `mergeToScope: true` publication contract and the narrowed `dataPath` compatibility status.

### 2026-04-06

- Repaired the docs entry-point mismatch by pointing `docs/index.md` back to the actual active top-level frontend programming model file after confirming that `docs/architecture/frontend-programming-model-v7.md` is not present in the workspace.
- Link to docs: `docs/index.md`, `docs/architecture/frontend-programming-model.md`
- Key decision: treat `docs/architecture/frontend-programming-model.md` as the active top-level source of truth until a versioned successor file actually exists in the repository.
- Next step: if a future versioned successor is introduced again, update the file and the entry-point references in the same change.

### 2026-04-06

- Ran a second independent omission audit on `docs/architecture/frontend-programming-model-v7.md`, then closed the last high-value gaps by adding an explicit `best-known feasible` evaluation envelope, `Resource` lexical activation and control-carrier precedence, fuller `Reaction` context/rules/comparator wording, stronger `Host Projection` collision/replacement constraints, fuller `Semantic Lifecycle Entry` timing/ownership rules, and final clarifying sections for `semantic top-level equality`, `Reaction` vs `Action Algebra`, and `Resource` vs `Host Projection`.
- Link to docs: `docs/architecture/frontend-programming-model-v7.md`, `docs/architecture/frontend-programming-model.md`
- Key decision: keep iterating on V7 until independent audit downgraded the remaining issues to near-complete replacement status instead of stopping at the first shortened rewrite.
- Next step: decide whether to promote V7 as the new top-level baseline and align narrower docs (`api-data-source.md`, `action-scope-and-imports.md`) to its terminology and publication rules.

### 2026-04-06

- Remediated `docs/architecture/frontend-programming-model-v7.md` after a gap audit: restored high-priority normative material that had been lost in the first V7 cut, including canonical-term policy/coverage, dependency-tracking baseline, stronger resource publication and ownership rules, `TanStack Query` alignment, detailed action-algebra rules (`ActionResult`, result classes, `continueOnError`, branch-result context, `parallel`, fallback, optimistic update), editable-host patch baseline, execution-boundary matrix, lexical/operational model sections, older-vocabulary reconciliation, and explicit closed decisions.
- Link to docs: `docs/architecture/frontend-programming-model-v7.md`, `docs/architecture/frontend-programming-model.md`
- Key decision: V7 should not merely be a shorter restatement of the old baseline; it must preserve the old document's hard constraints while also incorporating the newer discussion conclusions around Java-loader positioning, DSL progressive evolution, `mergeToScope`, and DAG semantics.
- Next step: run one more omission-focused review on V7 before treating it as a trustworthy top-level replacement candidate.

### 2026-04-06

- Added `docs/architecture/frontend-programming-model-v7.md` as a safer successor document instead of editing the large V6 file in place. V7 keeps the seven-primitive closure but incorporates the discussion conclusions around Java-loader positioning, DSL progressive evolution, `data-source` as named dynamic-value registration, `name` + `mergeToScope: true`, `visible`/`when`/`loop`/`dynamic-renderer` boundaries, and action DAG semantics under progressive authoring.
- Updated the docs entry point to point top-level frontend programming model reads at `docs/architecture/frontend-programming-model-v7.md`.
- Link to docs: `docs/architecture/frontend-programming-model-v7.md`, `docs/index.md`, `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: create a new V7 successor file rather than editing the large V6 document directly, so the old baseline stays intact while the new top-level contract can tighten around the discussion results.
- Next step: decide whether and when narrower docs such as `docs/architecture/api-data-source.md` should be aligned to the V7 `name` / `mergeToScope` direction and the refined action/structure wording.

### 2026-04-06

- Rewrote the staged-reactive alternative analysis into a reassessment memo: withdrew the earlier broad primitive-replacement claims, aligned the analysis with the Java-loader/final-model baseline, and kept only the narrower surviving insights around `data-source` naming, `mergeToScope`, `when`/`loop`/`dynamic-renderer` boundaries, action DAG semantics, and multi-surface/host-projection edge contracts.
- Link to docs: `docs/analysis/2026-04-06-frontend-programming-model-staged-reactive-alternative.md`, `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: the earlier staged-reactive document should no longer read like a candidate replacement baseline; it now serves as a corrected analysis note that records what still survives after the later discussion rounds.
- Next step: decide whether the surviving narrowed conclusions are stable enough to fold into `docs/architecture/frontend-programming-model.md` and `docs/architecture/api-data-source.md`.

### 2026-04-06

- Added an eighth discussion round that locks `mergeToScope: true` as the only narrowed special publish case for `data-source`, confirms `when` as a DSL-level property that may lower to an internal wrapper, and reclassifies the staged-reactive alternative as too broad for Nop's clarified architecture.
- Link to docs: `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: keep new DSL authoring centered on `name`, `mergeToScope: true`, property-level `when`, and progressive action authoring, while treating the earlier staged-reactive proposal as an overreach rather than a replacement baseline.
- Next step: rewrite `docs/analysis/2026-04-06-frontend-programming-model-staged-reactive-alternative.md` so it records the narrowed reassessment instead of still reading like a broad primitive replacement proposal.

### 2026-04-06

- Added a seventh discussion round that narrows `data-source` authoring toward `name` as the only normal path, treats scope-merge as a dedicated special publish mode instead of a generic `dataPath`, keeps `when` as a unified guard concept separate from `visible`, and reframes current action semantics (`when` + `then` + `onError` + `parallel`) as already forming a DAG at execution time.
- Link to docs: `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: distinguish progressive authoring surface from DAG execution semantics instead of treating DAG as a future phase that starts only when explicit graph syntax appears.
- Next step: decide whether to codify the narrowed `data-source` publish semantics and the action DAG semantics in architecture docs or continue refining them inside discussions first.

### 2026-04-06

- Extended the programming-model discussion with a sixth round centered on DSL-level progressive evolution: simple authoring forms must remain valid as requirements grow, `data-source` is better understood as named dynamic-value registration into scope, and `when` / `loop` should stay simpler structural DSLs than `dynamic-renderer`.
- Link to docs: `docs/discussions/2026-04-06-programming-model-optimality-critique.md`
- Key decision: evaluate future programming-model changes primarily against DSL semantic continuity, not only primitive elegance inside the runtime; allow operation-substrate unification without prematurely collapsing authoring into one DAG language.
- Next step: decide whether to reflect the surviving points back into architecture docs as explicit promotion-path guidance for values/resources, `name`/`dataPath` wording, and `when`/`loop` versus `dynamic-renderer` boundaries.

### 2026-04-06

- Supplemented the programming-model discussion record with a fifth round that re-evaluates optimality under the actual Nop constraint that the backend is a Java loader/service layer responsible for schema assembly, XML/JSON conversion, i18n, and policy trimming.
- Link to docs: `docs/discussions/2026-04-06-programming-model-optimality-critique.md`, `docs/discussions/2026-04-05-core-design-clarification.md`, `docs/articles/flux-design-introduction.md`
- Key decision: judge frontend programming-model optimality against Nop's real Java-loader architecture instead of a generic Node/SSR-first frontend-runtime target; keep the staged-reactive alternative as analysis input rather than a replacement baseline.
- Next step: decide whether to fold only the narrow surviving improvements back into `docs/architecture/frontend-programming-model.md`, especially stronger `dynamic-renderer` boundary wording, multi-surface boundary wording, and projection patch/version-feed contracts.

### 2026-04-06

- Added an analysis-only alternative to the frontend programming model that replaces the `Value` / `Resource` / `Host Projection` split with a single `Binding` model, adds first-class `Composition Boundary` and `Runtime Boundary`, and defines an incremental migration path from the current runtime.
- Link to docs: `docs/analysis/2026-04-06-frontend-programming-model-staged-reactive-alternative.md`, `docs/architecture/frontend-programming-model.md`
- Key decision: keep the alternative in `docs/analysis/` rather than promoting it as normative, because it is a counter-proposal for architectural comparison rather than the active source of truth.
- Next step: run an independent comparison against the current closure to determine whether the smaller current kernel or the broader staged-reactive model is the better fit for Flux's real target scope.

### 2026-04-06

- Promoted `docs/architecture/frontend-programming-model-v3.md` as the active top-level frontend programming model baseline and aligned supporting docs/types around the same `Resource` and `ActionSchema` contracts.
- Closed the remaining v3 semantic gaps by standardizing explicit `Resource` publication through `name` or `dataPath`, adding `statusPath` as the readonly runtime status summary path, and defining the reserved chained-action result context for `then` / `onError` as `result`, `error`, and `prevResult`.
- Link to docs/code: `docs/architecture/frontend-programming-model-v3.md`, `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, `docs/index.md`, `packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/actions.ts`
- Key decision: treat `frontend-programming-model-v3.md` as the single normative frontend programming model baseline and pull narrower docs and exported types toward it rather than keeping competing top-level contracts alive.
- Next step: re-run a final bounded-optimality review on v3 and then decide whether additional narrower docs need explicit references to the reserved chained-action result context.

### 2026-04-06

- Extended the table ownership baseline with a narrow scope-backed mode: `paginationOwnership: 'scope'` + `paginationStatePath`, and `selectionOwnership: 'scope'` + `selectionStatePath` now read/write reactive state directly from the current render scope.
- Added regression coverage for scope-backed pagination and row selection, alongside the earlier component-handle refresh/selection tests, and re-verified `@nop-chaos/flux-renderers-data` with test/typecheck/lint/build green.
- Link to code/docs: `packages/flux-renderers-data/src/schemas.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `docs/architecture/renderer-runtime.md`
- Key decision: keep Phase 7's third ownership mode minimal and explicit by using per-concern scope paths instead of introducing a generic table-state DSL or routing renderer-owned writes back through declarative actions.
- Next step: decide whether Phase 7 should stop after `pagination`/`selection` ownership closure or extend the same ownership model to additional table query state such as `sort` and `filter`.

### 2026-04-06

- Completed the first table component-handle baseline in `@nop-chaos/flux-renderers-data`: table instances now expose `refresh`, `getSelection`, and `setSelection` through component-handle dispatch.
- Fixed the handle refresh path so `table.onRefresh` keeps the caller action context instead of switching to a pagination child scope, which keeps runtime-owned `refreshSource` lookup aligned with scope-owned source registration.
- Link to code/docs: `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `docs/architecture/renderer-runtime.md`
- Key decision: for runtime-owned resources such as `data-source`, table instance capabilities should preserve the caller scope/action context unless the action explicitly needs a derived child scope payload.
- Next step: decide whether Phase 7 should stop at this explicit table handle baseline or continue into a third ownership mode such as scope-backed/query-observed state for `pagination` and `selection`.

### 2026-04-06

- Tightened `docs/architecture/frontend-programming-model.md` into the normative top-level closure for Flux core primitives, authority model, resource/reaction semantics, host projection, and authoring-vs-execution boundaries.
- Updated supporting active docs to align terminology and ownership boundaries around `dataPath` / `statusPath`, `xui:imports`, component-handle resolution, host projection visibility, and thin host protocol wording.
- Link to docs: `docs/architecture/frontend-programming-model.md`, `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/flux-dsl-vm-extensibility.md`, `docs/architecture/component-resolution.md`, `docs/architecture/complex-control-host-protocol.md`
- Key decision: treat `frontend-programming-model.md` as the document that closes primitive ownership and schema authority; narrower docs may add subsystem detail but must not reopen new primitive categories or alternate effect channels.
- Next step: finish repo-level supporting-doc cleanup where older wording still implies `xui:import`, bridge-in-scope leakage, or broader-than-intended component-target lookup, then decide whether to align runtime code with the new built-in targeting and component-registry contracts.
