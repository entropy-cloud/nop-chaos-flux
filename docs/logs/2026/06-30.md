# Development Log - 2026-06-30

### 2026-06-30

- **合并 `stash-pre-sync` → master（fast-forward）**：master 旧 tip `a0e1be2c` 是 `stash-pre-sync`（`4729e054` WIP: save pre-sync local changes）的直接祖先，`git merge --ff-only` 零冲突落地，56 文件 / +7244 −332 全部保留。
- **⚠️ 纠错：合并的 flux-guide 是残缺快照，已从 `529d3a69` 恢复完整版**。`stash-pre-sync` 只含 3 个 flux-guide 文件（`02-reference.md`、`flux-types/index.ts`、`flux-types/schema.d.ts`），但 reflog/fsck 显示完整版应是 22 文件，源于 dangling commit `529d3a69`（6/28 "feat: add confirm and alert + flux-guide fixes"）及其父 `d968c7a6`（"docs: add flux-guide with 22 files"）。这些 commit 因某次 `reset` 变成 unreachable。
  - 缺失的 18 个文件已用 `git checkout 529d3a69 -- flux-guide/` 全部恢复：`01-quickstart.md`、`README.md`、`design-patterns/`（cards/cascading-select/conditional/crud/custom/data-source/file-upload/form/tabs）、`mobile/`（countdown/infinite-scroll/notice-bar/pull-refresh/swipe-cell）。
  - `02-reference.md`、`index.ts`、`schema.d.ts` 三文件同步为 529d3a69 的最新版本。
- **撤销一次误判**：曾误以为 `flux-types/index.ts` 的 `import from './common'` 是 broken import 并新建了 `common.ts`。实际上原作者用的是 `flux-guide/flux-types/common.d.ts`（265 行，含 SchemaClassName/ApiSchema 联合类型/Option.children 等，比我手写的更完整地道）。已删除误加的 `common.ts`，恢复原 `common.d.ts`。
- **flux-guide 独立可编译**：新增 `flux-guide/tsconfig.json`（extends base，noEmit，Bundler 解析），`npx tsc -p flux-guide/tsconfig.json` 退出码 0。flux-guide 不在根 tsconfig references 内（根 `files: []` + references 仅指向 packages/apps）。
- **已知既有问题（与本次无关）**：`pnpm typecheck` 在 `packages/spreadsheet-renderers/src/canvas-styles.test.ts:4` 报 `TS2578 Unused '@ts-expect-error' directive`，源于 commit `1aebc41b`（合并前既有）。
- 新增/恢复文件：flux-guide 完整 22 文件 + `flux-guide/tsconfig.json`。
- 未推送：master 合并仅为本地 fast-forward + 本次恢复，未 `git push`。
