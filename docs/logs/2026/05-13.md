# Development Log — 2026-05-13

### 2026-05-13

- Continued Plan 269 execution for retained accessibility items `20-03` and `20-04`: `packages/flux-renderers-form-advanced/src/__tests__/form-tree-control-source-states.test.tsx` now follows the live tree-control interaction model by selecting checkbox-mode options through `role="treeitem"` instead of the internal visual-only checkbox, while still asserting the nested checkbox chrome stays `aria-hidden` and removed from tab order.
- Continued Plan 270 execution for retained performance item `15-05`: `apps/playground/src/pages/ding-talk-flow-demo.test.tsx` now adds focused proof that the DingTalk flow demo still renders both initial branch and merge overlays after the `nodeMap` hot-path optimization in `apps/playground/src/pages/ding-talk-flow-demo.tsx`.
- Continued Plan 270 execution for retained error-fidelity item `19-03`: `packages/flux-action-core/src/action-dispatcher/action-execution.ts` now routes `onActionError` and plugin `onError` through a fail-safe diagnostic helper so observer failures cannot replace the primary action failure and one bad plugin cannot stop later diagnostics. Added focused regression coverage in `packages/flux-action-core/src/__tests__/action-dispatcher-error-guard.test.ts`.
- Continued Plan 270 execution for retained error-fidelity item `19-02`: `packages/flux-action-core/src/operation-control.ts` now records soft `ok:false` results as `lastFailureReason`, and `packages/flux-runtime/src/async-data/request-runtime.ts` now throws structured request failures that retain the final response object, status, response payload, and retry metadata instead of collapsing everything to a message-only `Error`. Updated focused proof in `packages/flux-runtime/src/__tests__/request-runtime.test.ts`.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/form-tree-control-source-states.test.tsx --reporter=verbose` passed (`5` tests); `pnpm --filter @nop-chaos/flux-playground exec vitest run src/pages/ding-talk-flow-demo.test.tsx --reporter=verbose` passed (`1` test); `pnpm --filter @nop-chaos/flux-playground typecheck` passed; `pnpm --filter @nop-chaos/flux-action-core exec vitest run src/__tests__/action-dispatcher-error-guard.test.ts src/__tests__/cancelled-class-and-error-guard.test.ts src/__tests__/action-dispatcher-monitoring.test.ts --reporter=verbose` passed (`8` tests); `pnpm --filter @nop-chaos/flux-action-core typecheck` passed.
- Verification: `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/request-runtime.test.ts src/__tests__/async-data-contracts.test.ts --reporter=verbose` passed (`80` tests); `pnpm --filter @nop-chaos/flux-runtime typecheck` passed; `pnpm --filter @nop-chaos/flux-action-core typecheck` passed.

- Continued Plan 270/271 execution for doc/type/runtime-boundary cleanup: `packages/flux-react/src/node-frame-wrapper.tsx` now treats `tag-list` as an interactive wrapped control root and stops falling back to raw schema field-chrome props when resolved props omit `hint` / `description` / `remark` / `labelRemark` / alignment values; the matching regression proof now lives in `packages/flux-react/src/__tests__/data-source-and-node-identity.test.tsx` and `packages/flux-renderers-form-advanced/src/__tests__/form-double-edit-regression.test.tsx`.
- Continued Plan 270 execution for persisted host-shape narrowing: `packages/word-editor-core/src/document-io.ts` now validates recovered `paperSettings` at runtime and falls back to `DEFAULT_PAPER_SETTINGS` instead of blindly trusting persisted JSON shape. Added regression coverage in `packages/word-editor-core/src/__tests__/document-io.test.ts`.
- Continued Plan 268 execution for retained validation-owner item `08-04`: `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` now keeps the recursive hidden-branch descendant path walk type-safe, and `packages/flux-renderers-form-advanced/src/variant-field/variant-field-selector.test.tsx` now proves hidden-branch external errors are cleared even when the hidden named field sits under wrapper/container nodes.
- Continued Plan 271 execution for retained doc-baseline item `16-01`: `docs/architecture/renderer-runtime.md` now documents the currently exported `flux-react` hooks `useCurrentValidationValues`, `useFormLayout`, and `useStrictMode`, plus their current role in the active hook surface.
- Verification: `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/data-source-and-node-identity.test.tsx --reporter=verbose` passed (`8` tests); `pnpm --filter @nop-chaos/word-editor-core exec vitest run src/__tests__/document-io.test.ts --reporter=verbose` passed (`15` tests); `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/form-double-edit-regression.test.tsx --reporter=verbose` passed (`14` tests); `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/variant-field/variant-field-selector.test.tsx --reporter=verbose` passed (`8` tests); `pnpm --filter @nop-chaos/flux-react typecheck && pnpm --filter @nop-chaos/report-designer-renderers typecheck && pnpm --filter @nop-chaos/word-editor-core typecheck && pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck` passed.

- Continued Plan 266 execution for retained async item `06-01`: `packages/flux-runtime/src/form-runtime-submit-flow.ts` now wraps child-contract validation waits and `submitAction` execution in an abort-aware `awaitWithAbort(...)` helper so parent submit can return a cancelled result and clear `submitting` even when recurse-submit or summary-gate child validation hangs indefinitely. Added focused regression coverage in `packages/flux-runtime/src/__tests__/form-runtime-submit-flow.test.ts` for both hanging recurse-submit and hanging summary-gate child validation.
- Continued Plan 266 execution for retained async item `06-04`: `packages/flux-runtime/src/async-data/api-data-source-controller-state.ts` now settles superseded data-source runs immediately as `stale-dropped` instead of waiting for a newer run to settle first, and `packages/flux-runtime/src/async-data/api-data-source-controller-runtime.ts` tracks whether the current run has already been settled to avoid duplicate governance writes. Updated `packages/flux-runtime/src/__tests__/runtime-sources-refresh.test.ts` so `parallel` mode still allows concurrent requests but no longer permits an older run to publish stale payload/status before the newer authoritative run finishes.
- Verification: `pnpm --filter @nop-chaos/flux-runtime typecheck` passed; `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/form-runtime-submit-flow.test.ts src/__tests__/owner-validation-lifecycle-contracts.test.ts --reporter=verbose` passed (`19` tests); `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/runtime-sources-refresh.test.ts --reporter=verbose` passed (`10` tests).

- Continued Plan 266 execution for retained lifecycle items `07-04` and `07-05`: `packages/flux-react/src/render-nodes.tsx` no longer uses microtask-driven commit gates or cleanup `setState(...)` for fragment scopes. The gate now uses a synchronous versioned layout-effect handoff so fragment renders only proceed after the matching child scope is created and committed, and scope-identity switches no longer transiently fall back to the parent scope.
- Added focused regression coverage in `packages/flux-react/src/__tests__/schema-renderer-runtime-scope.test.tsx` proving fragment-scope identity switches do not expose parent-scope `child` data during the handoff window, while existing commit-order proof in `packages/flux-react/src/__tests__/compilation-and-boundaries.test.tsx` remains green.
- Verification: `pnpm --filter @nop-chaos/flux-react typecheck` passed; `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/schema-renderer-runtime-scope.test.tsx src/__tests__/compilation-and-boundaries.test.tsx --reporter=verbose` passed (`16` tests).

- Continued Plan 266 execution for retained async/lifecycle item `07-07`: `packages/flux-runtime/src/import-stack.ts` now reuses one frame-owned `ActionScope` per auto-owned import frame, records it on the frame, and calls `runtime.releaseActionScope(...)` from `pop()` so import/node-owned scopes stop accumulating in `runtime-factory`'s `ownedActionScopes` set after owner cleanup.
- Added `RendererRuntime.releaseActionScope(...)` to the shared runtime contract in `packages/flux-core/src/types/renderer-core.ts` and implemented the minimal ownership release in `packages/flux-runtime/src/runtime-factory.ts` without changing bare `createActionScope()` semantics.
- Added focused regression coverage in `packages/flux-runtime/src/__tests__/import-stack.test.ts` proving that auto-owned import frames release their runtime-owned action scopes on `pop()` while externally supplied action scopes still just unregister namespaces.
- Synced the import-loader abort path to the actual `ImportedLibraryLoader.load(spec, signal?)` signature in `packages/flux-runtime/src/runtime-factory.ts`, updated the matching assertion in `packages/flux-runtime/src/__tests__/runtime-imports.test.ts`, and fixed `packages/flux-runtime/src/status-owner.ts` to pass a zero-arg projection-version getter into `createProjectedScopeStore(...)` so `@nop-chaos/flux-runtime` typecheck returns to green.
- Verification: `pnpm --filter @nop-chaos/flux-runtime typecheck` passed; `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/import-stack.test.ts --reporter=dot` passed (`32` tests); `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/runtime-imports.test.ts src/__tests__/runtime-status-and-imports.test.ts --reporter=dot` passed (`14` tests).

- Added `docs/architecture/variant-vocabulary.md` as the normative naming split for `variant` / `intent` / `level`: shadcn-compatible component-local visual branches stay under `variant`, action semantics such as `primary/danger/warning/success` move to `intent`, passive status severity uses `level`, and business discriminants should use domain names such as `kind` or `branchKey`.
- Re-reviewed `docs/architecture/variant-vocabulary.md` through three subagent rounds. Final consensus accepted the document after clarifying that existing Flow/Report toolbar `variant` contracts remain live until migration, `level` applies only to passive visual status, notification/diagnostic `error` remains an API-level severity word, and compiler validation currently does not enforce ordinary renderer prop enum ranges from `propContracts.shape`.
- Added `docs/plans/272-compile-prop-value-validation-and-variant-convergence-plan.md` to own implementation of compiler-time finite option-range validation: renderer definitions must register option ranges for finite-valued public fields, validation must enforce statically knowable values, and all resulting `variant` / `intent` / `level` / CSS drift must be fixed before closure.
- Linked the new variant vocabulary baseline from `docs/index.md`, `docs/architecture/README.md`, `docs/architecture/styling-system.md`, and `docs/references/flux-json-conventions.md`.
- Added `scripts/analyze-variant-vocabulary.mjs` to inventory `variant` vocabulary across `apps/`, `packages/`, `docs/`, and `tests/`, separating schema-public style variants, domain-local toolbar vocabularies, UI-private component variants, and `variant-field` business-data discriminants.
- Current scan result: public button schema remains `default/destructive/outline/secondary/ghost/link` (`packages/flux-renderers-basic/src/schemas.ts`), tabs remain `default/line` (`packages/flux-renderers-basic/src/schemas.ts`), while designer/report toolbars still expose separate domain vocabularies (`accent/danger/default` in `packages/flow-designer-core/src/types.ts`, `default/primary/danger` in `packages/report-designer-renderers/src/schemas.ts`).
- The analysis also confirmed the naming collision that makes `variant` feel messy: the same field name is reused for style variants, domain toolbar semantics, UI-private cva variants, and `variant-field` business discriminants in tests/docs.
- Verification: `node scripts/analyze-variant-vocabulary.mjs` passed. Workspace `pnpm typecheck` passed, while `pnpm build` failed on the pre-existing `packages/flux-runtime/src/runtime-factory.ts:285` TypeScript error (`signal` not in `AbortSignal`) and `pnpm lint` failed on the pre-existing `packages/flux-react/src/hook-subscriptions.ts:6` unused `ScopeDependencySet` import.

### 2026-05-13 (Plan 262 Execution)

- Executed `docs/plans/262-adversarial-review-2026-05-13-remediation-plan.md` — 6-phase fix for adversarial review confirmed live defects.
- Phase 1 (Dependency Tracking): Fixed formula-data-source-controller `start()` `.catch()` path to call `onDependenciesChange?.(undefined)`, ensuring first-publish failure doesn't permanently kill reactivity. Changed `scopeChangeHitsDependencies(change, undefined)` to return `true` (conservative, matching `docs/architecture/dependency-tracking.md`). Added focused test `formula-data-source-recovery.test.ts`.
- Phase 2 (Action Dispatch): Added `'cancelled'` to `ActionResultClass`. `classifyActionResult` now returns `'cancelled'` for cancelled/timedOut (not `'failure'`), so cancelled actions skip `onError` but still run `onSettled`. Moved `onActionStart` call inside try/catch. Wrapped `onError` dispatch in try/catch. Updated `runParallelActions` to treat cancelled results as failures for aggregate ok. Added `cancelled-class-and-error-guard.test.ts` with 5 focused tests.
- Phase 3 (Theme Token): Added `--destructive` and `--destructive-foreground` to all 5 CSS blocks in `packages/theme-tokens/src/styles.css` (`:root` + 4 theme variants), fixing invisible error text for non-playground consumers.
- Phase 4 (UI i18n): Added `setI18nGetter()` dependency injection to `packages/ui/src/lib/i18n.ts`, wired to `@nop-chaos/flux-i18n`'s `t()` in `packages/flux-bundle/src/index.tsx`. UI package remains zero-dep on `@nop-chaos/*`.
- Phase 5 (Scope Lifecycle): Added `dispose()` to host projection scope, called on old scope when `useHostScope` replaces it. Added `clear()` to `ModuleCache` interface and `createModuleCache()`. `runtime.dispose()` clears internally-owned moduleCache. Added `scope-lifecycle-leak-fix.test.ts`.
- Phase 6 (Error Boundary): Added `attemptKey` state to `SchemaRootErrorBoundary`; retry increments key and wraps children in `<Fragment key={attemptKey}>` to force `useMemo` cache bust for deterministic compilation errors.
- Verification: `pnpm typecheck` (26 packages), `pnpm build` (26 packages), `pnpm lint` (26 packages), `pnpm test` (all packages, 1048+ tests) all pass.

### 2026-05-13

- Independent closure audit `ses_1e12b2629ffe9LQ1VcHI4R6TKQ` re-reviewed Plans `254` and `255` after the successor split and confirmed they can now be marked `completed` because `09-05` was removed through the recorded 2026-05-13 renderer-contract scope change, `06-02` no longer reproduces, and the still-live retained subsets were moved into explicit successor ownership under `docs/plans/265-deep-audit-2026-05-13-reactive-owner-boundary-successor-plan.md` and `docs/plans/266-deep-audit-2026-05-13-async-lifecycle-owner-successor-plan.md`.
- Independent closure audit `ses_1e137c328ffeVBrqXeuqXvwgdD` re-reviewed Plans 256-261 after the successor split and confirmed `256`, `257`, `258`, `259`, `260`, and `261` are now consistent with `completed`; the earlier audit that only approved Plan 260 reflected the pre-reroute baseline and is now historical context only.
- Closed Plan 261 by removing whole-document spreadsheet `JSON.stringify(...)` comparisons from `packages/report-designer-renderers/src/page-renderer.tsx`, adding report-core sync-source tracking in `packages/report-designer-core/src/{core.ts,types.ts,core-dispatch.ts}`, and covering the behavior with focused tests in `packages/report-designer-core/src/__tests__/designer-core.test.ts` and `packages/report-designer-renderers/src/page-renderer-selector.test.tsx`.
- Closed the remaining low-risk live items in Plans 258 and 259 by tightening table pipeline typing in `packages/flux-renderers-data/src/table-renderer/{use-table-selection.ts,use-table-row-scope-cache.ts}` and replacing spreadsheet grid editing/row-header controls with `@nop-chaos/ui` primitives in `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`; `docs/architecture/styling-system.md` now documents the spreadsheet raw table shell as an explicit exception while keeping interactive controls on UI primitives.
- Re-routed the still-live structural/public-surface/styling residuals into explicit successor ownership under `docs/plans/262-deep-audit-2026-05-13-structural-owner-successor-plan.md`, `docs/plans/263-deep-audit-2026-05-13-public-surface-narrowing-successor-plan.md`, and `docs/plans/264-deep-audit-2026-05-13-layout-contract-and-theme-boundary-successor-plan.md`, allowing Plans 256/257/258/259/261 to reach closure-ready text state pending independent closure audit confirmation.
- Validation on the current snapshot: `pnpm typecheck`, `pnpm build`, and `pnpm lint` passed. Focused package tests for the touched areas passed, and long-run `pnpm test` surfaced an unrelated existing failure in `@nop-chaos/flux-action-core` (`classifyActionResult` expectation drift) outside the files touched in this slice.
- Re-audited successor plans `docs/plans/256-deep-audit-2026-05-12-module-boundary-and-test-hygiene-plan.md` through `docs/plans/261-deep-audit-2026-05-12-performance-redline-follow-up-plan.md` against the live repo, landed the still-live low-risk renderer/test/doc/a11y fixes, and synchronized each plan status with the current baseline.
- Closed the live contract/test drift owned by Plans 256/258/259/260: added Component Lab coverage/write proofs in `tests/e2e/component-lab/{coverage-manifest.ts,complex-form.spec.ts}`, stabilized tabs/CRUD semantic event payloads in `packages/flux-renderers-basic/src/tabs.tsx` and `packages/flux-renderers-data/src/crud-renderer.tsx`, tightened dataset/table typing in `packages/{word-editor-core,flux-renderers-data}/src`, replaced raw UI primitives in `packages/{nop-debugger,word-editor-renderers}/src`, and added explicit icon-button labels in `packages/word-editor-renderers/src/{toolbar/shared.tsx,preview/doc-preview-page.tsx}`.
- Closed the live doc-baseline drift owned by Plan 257 for `15-02`, `17-01`, `17-04`, `17-05`, and `17-07` in `docs/architecture/flux-formula.md`, `docs/architecture/flow-designer/config-schema.md`, `docs/references/flux-json-conventions.md`, `docs/components/condition-builder/design.md`, `docs/components/button/example.json`, and `packages/flux-code-editor/src/types.ts`; the broader retained items `02-15`, `03-01`, and the semantic-vocabulary adjudication for `17-03` remain outside this closure slice.
- Re-audit conclusion by plan after the successor split: `256`, `257`, `258`, `259`, `260`, and `261` are completed with explicit successor routing where needed; Plans 262-264 now own the remaining live structural/public-surface/styling residuals.
- Historical note: earlier audit `ses_1e307f9f2ffeE23XCxAh5o7whD` only approved Plan 260 before the successor-routing closure pass; that conclusion was superseded by `ses_1e137c328ffeVBrqXeuqXvwgdD` after the plans were rewritten to satisfy the closure guide.
