# Development Log - 2026-07-10

### 2026-07-10 (enhancement: RadioGroup + CheckboxGroup 支持 direction: 'horizontal')

**背景**：用户反馈 RadioGroup 只有垂直排列，CheckboxGroup 同样。需要支持水平排列和自动折行。

**根因**：

- RadioGroup: `form-renderers.css` 中 `[data-slot='radio-group-options'] { display: grid }` 强制垂直，specificity (0-3-0) 高于 Tailwind class
- CheckboxGroup: `form-renderers.css` 中 `[data-slot='checkbox-group-wrapper'] { display: grid }` 同样强制垂直，且 wrapper 直接包含 checkbox items

**改动**：

- **`schemas.ts`** — `RadioGroupSchema` 和 `CheckboxGroupSchema` 均新增 `direction?: 'horizontal' | 'vertical'`
- **`input.tsx`** — radio-group 和 checkbox-group 的 field rules 新增 `{ key: 'direction', kind: 'prop' }`
- **`input-choice-renderers.tsx`** — RadioGroupRenderer 根据 `direction === 'horizontal'` 设置 `flex flex-wrap items-center gap-3`
- **`checkbox-group-renderer.tsx`** — 同上，默认改为 `flex flex-col gap-2.5`，horizontal 用 `flex flex-wrap items-center gap-3`
- **`form-renderers.css`** — 拆分 radio/checkbox 共用规则；移除 checkbox-group-wrapper 的 `display: grid`；保留 radio-group-wrapper 的 grid（用于 stacking loading/options/error）
- **lab 页面** — radio-group 和 checkbox-group 第二场景添加 `direction: 'horizontal'`

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright（两组均 vertical→垂直 / horizontal→flex wrap 同行）✅

### 2026-07-10 (enhancement: RadioGroup 支持 direction: 'horizontal' 水平排列 + 自动折行)

**背景**：用户反馈 RadioGroup 只有垂直排列，问是否支持水平排列和折行。对比 AMIS（支持 `mode: inline`），当前无 `direction` prop。

**根因**：`form-renderers.css:50-53` 强制 `[data-slot='radio-group-options'] { display: grid }`，specificity (0-3-0) 高于 Tailwind `.flex` (0-1-0)，覆盖任何 className 尝试。

**改动**：

- **`schemas.ts`** — `RadioGroupSchema` 新增 `direction?: 'horizontal' | 'vertical'`
- **`input.tsx`** — radio-group renderer definition 的 fields 新增 `{ key: 'direction', kind: 'prop' }`
- **`input-choice-renderers.tsx`** — `RadioGroupRenderer` 根据 `direction === 'horizontal'` 设置 className 为 `flex flex-wrap items-center gap-3`（支持自动折行）
- **`form-renderers.css`** — 移除 `[data-slot='radio-group-options']` 的 `display: grid; gap: 0.625rem` 硬编码（让 Tailwind class 控制 display/gap）
- **`radio-group-lab-page.tsx`** — lab 页面第二个场景改用 `direction: 'horizontal'`

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright（vertical→grid 垂直 / horizontal→flex wrap 同行）✅

### 2026-07-10 (enhancement: Markdown Editor 工具栏扩充 5→12 按钮 + 图标化)

**背景**：用户质疑 markdown editor 工具栏只有 5 个按钮是否充分。对比 GitHub/EasyMDE/Vditor 等主流编辑器，缺少 Strikethrough、Quote、List、Image、HR、Table 等常用功能。

**改动**：

- **`markdown-editor-renderer.tsx`** — 工具栏从 5 个按钮扩充到 12 个，分 3 组（inline / block / insert）：
  - Group 1 (inline): Bold, Italic, Strikethrough, Code
  - Group 2 (block): Heading, Quote, Unordered list, Ordered list, Horizontal rule
  - Group 3 (insert): Link, Image, Table
- 所有按钮统一使用 lucide-react 图标（BoldIcon, ItalicIcon, StrikethroughIcon, CodeIcon, Heading2Icon, QuoteIcon, ListIcon, ListOrderedIcon, MinusIcon, LinkIcon, ImageIcon, TableIcon）
- 按钮改为 icon-only（`size="icon"`），32×32px 方形
- 分组间用 1px 竖线分隔

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright（12 按钮 × 2 编辑器 = 24，2 分隔线，Quote/Table 功能正常）✅

### 2026-07-10 (bug fix: Textarea rows 属性无效)

**背景**：用户质疑 textarea `rows` 属性是否生效。经 Playwright `getComputedStyle` 检查，三个 textarea（rows=4/5/3）高度均为 64px。

**根因**：`Textarea` base 组件（`packages/ui/src/components/ui/textarea.tsx`）className 包含 `field-sizing-content`（CSS `field-sizing: content`），使 textarea 按内容自动调整高度，忽略原生 `rows` 属性。

**改动**：

- **`packages/ui/src/components/ui/textarea.tsx`** — 移除 `field-sizing-content` class
- **`docs/testing/2026/07-10.md`** — 记录 bug #10

**验证**：typecheck ✅ · lint ✅ · flux-renderers-form 562/562 ✅ · ui 139/139 ✅ · Playwright（rows=4→98px, rows=5→118px, rows=3→78px, `fieldSizing: "fixed"`）✅

### 2026-07-10 (bug fix: Input-Year 无法输入 + clear 折行 + 可输入超长数字)

**背景**：用户反馈 `/#/lab/input-year` 控件无法输入、clear 按钮折行、且可输入超长数字如 "202043444444"。

**根因**：

1. **无法输入**：year Input value 经 `parsePeriod` 解析部分输入失败 → 清空
2. **折行**：容器 `flex-wrap` + Input `w-full` 占满 → clear 换行
3. **超长数字**：`type="number"` 无 `maxLength`/`max`

**改动**：

- year Input：`type="number"` → `type="text"` + `inputMode="numeric"` + `maxLength={4}` + `replace(/\D/g,'').slice(0,4)` 过滤
- quarter year Input：同上
- value：直接使用 `value ?? ''`，不走 parsePeriod
- 容器：`flex flex-wrap` → `flex`
- month/year Input：添加 `className="w-auto"`/`className="w-24"`
- **`docs/testing/2026/07-10.md`** — 记录 bug #7/#8/#9

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright（逐字输入成功；超长截断为 4 位；非数字过滤；ctrlHeight=36px 单行）✅

### 2026-07-10 (enhancement: 非必选 Select 默认启用 clearable)

**背景**：用户反馈非必选 Select 选中某项后无法回到"未选"状态，需手动配置 `clearable: true`。

**改动**：

- **`input-choice-renderers.tsx:220`** — `clearable` 默认值从 `Boolean(props.props.clearable)` 改为 `props.props.clearable !== undefined ? Boolean(props.props.clearable) : !props.props.required`
- 语义：显式设 `clearable` 时尊重用户值；未设时非必选自动启用，必选不启用
- **`docs/testing/2026/07-10.md`** — 记录 bug #6

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright 验证（required select 无 clear button，non-required select 选中后出现 clear button，点击后清除成功）✅

### 2026-07-10 (bug fix: Select 箭头位置不一致 + 搜索型过滤残留)

**背景**：用户反馈两个问题：(1) 非搜索型 Select 未选中时箭头在最左、选中后跳到最右；(2) TeamMember（searchable）选中后再打开下拉只显示 1 条选项。

**根因**：

1. **箭头位置**：`ComboboxTrigger` base class 用 `justify-between`，children 为 `{ComboboxValue}` + `<ChevronDownIcon>`。未选中时 `ComboboxValue` 不渲染 DOM，flex 只有 1 个子元素，`justify-between` 将 icon 放在左侧。
2. **过滤残留**：搜索型 Select 选中后 `onInputValueChange` 将 `inputValue` 设为所选 label，dropdown 关闭时未重置，重开时 `query = inputValue` 仍非空导致 `visibleOptions` 被过滤为 1 条。

**改动**：

- **`input-choice-renderers.tsx:474`** — `ComboboxValue` 包裹在 `<span className="flex-1 truncate text-left">` 中，空 span 仍占满空间，将 icon 始终推至右侧
- **`input-choice-renderers.tsx:435`** — `Combobox` 的 `onOpenChange` 回调在 `open === false` 时 `setInputValue('')`
- **`docs/testing/2026/07-10.md`** — 记录 bug #4 和 #5

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅ · Playwright 验证（箭头 svgLeft 选中前后一致=1194；TeamMember 选中后 reopen 仍 9 条选项）✅

### 2026-07-10 (bug fix: 非搜索型 Select trigger 不可见 + lint 修复)

**背景**：在 `/#/lab/select` 页面测试发现，非搜索型 Select 在未选中时 trigger 仅 16px 高、无边框无背景，几乎不可见。同时发现 `select-combobox-lists.tsx` 存在 lint 错误（`react/no-array-index-key`）。

**根因**：`ComboboxTrigger`（`packages/ui/src/components/ui/combobox.tsx`）base className 仅有 `flex items-center justify-between gap-1.5`，无任何视觉样式。搜索型场景中 trigger 借助 `InputGroupButton` 获得样式，但非搜索型独立使用时裸露无样式。

**改动**：

- **`input-choice-renderers.tsx:471`** — 非搜索型 `ComboboxTrigger` className 从 `flex-1 justify-between` 补充为 `flex-1 justify-between h-8 rounded-lg border border-input bg-transparent px-2.5 pr-2 text-sm outline-none transition-colors focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:cursor-not-allowed disabled:opacity-50 aria-invalid:border-destructive data-placeholder:text-muted-foreground`
- **`select-combobox-lists.tsx:24-26`** — 修复 lint 错误：`.map((part, index) => ...)` 改为 `.map((part) => ...)`，`key={index}` 改为 `key={part}`
- **`docs/testing/2026/07-10.md`** — 记录 bug #3

**验证**：typecheck ✅ · lint ✅ · 562/562 单测 ✅（flux-renderers-form 全部通过）

### 2026-07-10 (文档: 补充 Layout Renderer 选型决策与 className 路由)

**背景**：用户反馈 container 的 `className: "flex flex-wrap"` 不生效，暴露了架构文档中 layout renderer 选型决策和 `className` 路由说明的缺口。

**分析发现**（通过源码阅读 `container.tsx`）：

- Container 是**双层 DOM**：外层 `<div class="nop-container">` + 内层 `<div data-slot="container-body">`
- `className` 挂在外层，不影响子节点排列
- body 的 flex 布局由 semantic prop（`direction`/`wrap`/`gap`/`align`）或 `bodyClassName` 控制
- `type: "flex"` 是**单层 DOM**，`className` 直接控制根节点，无需了解双层路由

**文档更新**：

1. **`docs/architecture/styling-system.md`** — 新增 `§4.4 Layout Renderer 选型决策`：
   - Container / Flex / Grid 三者的选型对比表
   - Container DOM 结构图 + className 路由说明（附 HTML 示例）
   - 明确指南：简单 flex 用 `type: "flex"`，已有 container 用 semantic prop 或 `bodyClassName`

2. **`docs/components/container/design.md`** — §12 更新：
   - 添加完整 container DOM 结构图（header/body/footer 三层）
   - 明确标注 className → 外层、bodyClassName → data-slot、semantic prop → 内层 body
   - 禁止性指南：不要用 className 给 body 加 flex

3. **`docs/components/flex/design.md`** — §11 补充：
   - flex 与 container 的关键差异：单层 vs 双层 DOM

**dashboard JSON** 已使用正确的 `type: "flex"` + `direction: "row"` + `wrap: true` + `gap: 12` 方案。stat 卡片宽屏 6 列一行、窄屏自动换行。

- **验证**：typecheck ✅ · 110/110 单测 ✅

### 2026-07-10 (dashboard stat cards: 顶部装饰条 + 标准 Tailwind 命名色还原)

**背景**：用户反馈左侧彩色边框（`border-l-4 border-l-chart-N`）不如之前的顶部装饰条好看，chart 色系/primary 配色也不够美观。回退为顶部 4px 彩色横条 + 标准 Tailwind 命名色。

**改动明细**：

- **Dashboard 6 张 stat 卡全部重写**（`page-schemas/dashboard.json`）：
  - 外容器移除 `border-l-4 border-solid border-l-chart-N`，改为纯 `overflow-hidden`
  - body 首个子元素改为 `{ "type": "text", "text": "", "className": "block h-1 w-full bg-{color}" }`（顶部 4px 彩色横条）
  - 颜色从 `chart-1~5`/`primary` 改为标准 Tailwind 命名色：
    - 订单总数 → `bg-blue-500`
    - 今日订单 → `bg-emerald-500`
    - 活跃订单 → `bg-violet-500`
    - 待付款 → `bg-amber-500`（原 chart-2 红色过于扎眼）
    - 累计收入 → `bg-cyan-500`
    - 月增长率 → `bg-rose-500`（原 primary 琥珀棕显脏）
  - 图标背景从 `bg-chart-N/10` 改为统一 `bg-muted`（中性背景，适配各色图标）
  - 图标色从 `text-chart-N` className 改为 `color: "#..."` 内联样式（避免 Tailwind JIT 检测问题）
- **`schema-page.tsx` 添加 JIT safelist 注释**：确保 JSON 中使用的 `bg-blue-500` 等 6 个类名不被 Tailwind JIT tree-shaking 遗漏（JSON 文件不在默认 `@source` 扫描路径中）
- **验证**：typecheck ✅ · 110/110 单测 ✅

### 2026-07-10 (dashboard 改进: 参考 ~/sources 设计实践)

**背景**：用户要求参考 `~/sources` 下其他项目（litemall / jshERP / springreport / Filament 等）的 dashboard 设计进行改进。

**调研结论**（7 个项目对比）：

- **litemall**：4 stat 卡（1×4），hover 时 icon 背景色填充 + icon 变白（`transition: 0.38s`），带 `vue-count-to` 数字动画，点击可筛选图表
- **jshERP**：12 卡（4×3），Ant Design ChartCard 组件（loading/tooltip），3 bar 图
- **springreport**：welcome banner（蓝色渐变 + 头像），日历 + 待办任务面板，纯图标无彩色背景
- **Filament/aureuserp**：Stat card 内嵌 sparkline 趋势小图 + 环比百分比

**改动明细**：

- **Stat cards 布局改为 3×2 grid**（`grid-cols-3 gap-4`）：原来 6 卡 1 行过于拥挤，改为 2 行 × 3 列，每张卡片有更充分的展示空间
- **添加 hover 微交互**（`styles.css` + `dashboard.json`）：
  - 卡片整体：`box-shadow` 升为更强的阴影 + `translateY(-2px)` 微微上浮（`transition: 0.25s`）
  - icon 背景：hover 时从中性 `bg-muted` 变为卡片对应颜色（通过 `data-testid` CSS 属性选择器，每个卡唯一色）
  - icon 图标色：hover 时变为白色（`color: #fff`）
  - 使用纯 CSS 类（`.dash-card` / `.dash-icon-bg` / `.dash-icon-el`），避免 Tailwind JIT JSON 检测问题
- **添加欢迎横幅**：在 stat 卡上方增加标题 "仪表盘" + 副标题 "实时业务概览"
- **验证**：typecheck ✅ · 110/110 单测 ✅

### 2026-07-10 (续 — flux-guide Phase 1 重构: 拆分 02-reference.md)

**背景**：分析报告 (`docs/analysis/2026-07-10-flux-guide-structure-analysis.md`) 指出 `02-reference.md` 578 行过载，覆盖 10 个独立主题，需拆分。

**改动**：

- 将 `02-reference.md` 拆分为 8 个聚焦文件：
  - `02-expression-syntax.md` — 模板/过滤器/条件表达式
  - `03-api-config.md` — API 配置格式
  - `04-action-system.md` — Action Algebra 事件与动作
  - `05-data-flow.md` — 数据源/数据域/组件通信
  - `06-form-validation.md` — 表单校验系统
  - `07-structural-nodes.md` — 结构节点 + 组件方法
  - `08-tabs-state.md` — Tabs 状态管理
  - `09-amis-migration.md` — AMIS 差异对比
- 将 §10 布局选型移至 `design-patterns/layout.md`（新增）
- 删除旧的 `02-reference.md`
- 更新 `README.md` 文件索引 + design-patterns 清单
- 新增 `flux-guide/package.json`（`validate` / `generate-types` 脚本）
- 根 `package.json` 添加 `flux-guide:validate` / `flux-guide:generate-types` 脚本
- `docs/index.md` 路由表添加 `flux-guide/` 入口（"Write Flux JSON schema"）
- `AGENTS.md` 路由表添加 `flux-guide/` 引用（"Write Flux JSON schema"）
- **验证**：`validate.mjs` 223 blocks ✅ 0 errors ✅ · typecheck 55/55 ✅

### 2026-07-10 (续 — Phase 3: 补全 7 个设计模式文档)

**背景**：分析报告第 3.5 节列出 8 个缺失设计模式的优先级。Phase 1+2+4 完成后，继续补全 7 个设计模式文件。

**新增设计模式**：
| 文件 | 场景 | 关键内容 |
|------|------|----------|
| `design-patterns/wizard.md` | 多步骤向导 | 线性/跳跃导航、条件步骤、statusPath 状态发布 |
| `design-patterns/chart.md` | 图表 | bar/line/pie/area + 点击事件 |
| `design-patterns/dynamic-renderer.md` | 动态加载 | loadAction/autoLoad/component:refresh |
| `design-patterns/tree.md` | 树形组件 | Tree 展示 + TreeSelect 表单字段 + 懒加载 |
| `design-patterns/collapse.md` | 折叠面板 | 单/多面板、受控/非受控 |
| `design-patterns/content-display.md` | 内容展示 | Card/Alert/Status/Mapping 四个组件合并 |
| `design-patterns/combo-input-table.md` | 可编辑集合 | Combo 表单组 + InputTable 表格编辑 |

**索引更新**：`flux-guide/README.md` + `design-patterns/README.md` 清单同步更新
**验证**：`validate.mjs` 261 blocks ✅ 0 errors ✅ (warnings: 1 doc-external, 1 compile-time component:refresh type hint)

### 2026-07-10 (续 — 全量清查 + 补全剩余设计模式 + 准确性修复)

**背景**：用户要求"仔细检查flux代码和文档，看flux-guide是否还有需要补充的内容"。

**全量清查**：从 `packages/*/src/*-definition.ts` 提取了全部 100 个注册 renderer type，逐一对照 flux-guide 覆盖情况，更新 `docs/analysis/2026-07-10-flux-guide-structure-analysis.md` 新增 §7 覆盖率评估矩阵。

**发现并修复**：

- `tree.md` 误用 `source`（应为 `data`）— 已修复
- `01-quickstart.md` 标题"15 个"→"17 个"
- `service.md` 最初误用不存在的 `name`/`dataSources`（实际 schema 是 `items/data/body/empty/error/loading/statusPath`）— 已重写
- `steps-timeline.md` Timeline 最初误用 `labelField`/`descriptionField`/`iconField`（实际是静态 `items[]` 数组，每项 `time/title/detail/icon/level`）— 已重写
- 重新生成 `flux-types/schema.d.ts`（确认 TreeSchema.data 在 auto-generate 中缺失 — 工具边界限制）

**新增 4 个设计模式**（覆盖 6 个 renderer type）：

- `button-group.md` — ButtonGroup + DropdownButton
- `steps-timeline.md` — Steps + Timeline
- `service.md` — Service 数据容器
- `picker-transfer.md` — Picker + Transfer

**最终覆盖率**：279 个 JSON 块 ✅ 0 errors ✅ · warnings 仅剩 2 个（1 doc-external, 1 component:refresh 静态分析）

### 2026-07-10 (续 — flux-guide 准确性审计与修复)

**背景**：用户要求"仔细分析flux-guide文档是否准确，是否需要继续完善"。通过对照 `packages/flux-core/src/types/` 源码逐一核对 flux-guide 类型定义与示例。

**修复的不准确之处**（对照源码 `schema-base-types.ts` / `actions.ts` / `schema.ts`）：

- `flux-types/common.d.ts`：
  - `ApiSchema` 从联合类型 `string | ApiSchemaObject` 改为 `interface ApiSchema extends SchemaObject`（与源码一致），删除虚构的 `ApiSchemaObject`，补充缺失的 `selection?: string`
  - `ActionShapeFields` 补充 `messages?: MessagesConfig` 和 `confirmText?: string`（源码 `actions.ts:156-157`）
  - 新增 `MessagesConfig` 接口（`{ success?, failed? }`）
  - `BaseSchema.frameWrap` 从 `string` 改为 `FrameWrapMode`（`boolean | 'label' | 'group' | 'none'`），新增 `FrameWrapMode` 类型定义
  - 删除源码中不存在的 `SchemaClassName` 类型
- `design-patterns/picker-transfer.md`：Transfer 数据源绑定示例误用 `source`（实际 `TransferSchema` 只有 `options`）— 已改为 `options`，字段参考表删除 `source` 行
- `design-patterns/button-group.md`：两处 ButtonGroup 示例误用 `"variant": "primary"`（`ButtonSchema.variant` 有效值为 `default | destructive | outline | secondary | ghost | link`）— 已改为 `"default"`
- `04-action-system.md`：ActionShapeFields 字段表补充 `messages`（`MessagesConfig`）和 `confirmText`（`string`）
- `docs/references/quick-reference.md`：line 45 包命名描述与实际包名冲突（声称 `{feature}-renderers` 但实际是 `flux-renderers-{feature}`）— 已修正

**新增文档**：

- `flux-guide/10-react-integration.md` — 自定义渲染器 React Hooks 速查，覆盖核心/表单/上下文/工具四类标准 Hook + `RendererComponentProps` 数据来源 + 典型模式代码
- `flux-guide/README.md` 文件索引同步更新

**验证**：`tsc --project flux-guide/tsconfig.json` ✅ 0 errors · `validate.mjs` 274 blocks ✅ 0 errors ✅ (warnings: 2，均为既有非本次改动)

### 2026-07-10 (续 — flux-guide 完整性改进: 类型生成器改用内存注册表 + 宿主集成文档)

**背景**：用户要求"仔细分析flux-guide是否只读此文档就完全明确flux使用"。审计发现三类问题：README 集成 API 错误、`code-editor` 渲染器缺失、以及**类型生成器基于源码文本解析导致 39/93 接口为空**。

**根因（关键发现）**：`scripts/generate-types.mjs` 原先用正则解析源码文本提取 `RendererDefinition.fields`，无法处理 `...formFieldRules` 展开、`booleanPropContract(...)` helper、`.concat()` 等 JS 构造，导致 39 个接口只剩 `type` 字段。用户提出「RendererDefinition 已注册在内存，为何不直接从内存结构生成」——采纳后彻底重写。

**改动明细**：

1. **`scripts/shared.mjs`（845 → 144 行）**：删除全部源码文本解析机制（`extractDefinitions`/`jsoncParse`/`matchBracket`/`parseFieldsArray`/`getFieldRuleTable` 等），新增 `loadRegisteredDefinitions()`——加载 `dist` 并执行各包 `register*Renderers`，直接读取**已注册的内存 `RendererDefinition`**。helper 函数/spread/concat 全部由 JS 运行时天然求值。
2. **`scripts/generate-types.mjs`**：`main()` 改为 async，从 `loadRegisteredDefinitions()` 取定义；新增 `flux-code-editor` 分类。空接口 **39 → 8**（剩余 8 个为纯输入控件，专有属性经通用 prop 通道解析，非 `fields` 声明）。`dialog` 从 0 → 23 字段（含精确字面量联合 `size?: "xs" | "sm" | ...`）。
3. **`flux-types/`**：重新生成，**92 → 93 个 type**（新增 `code-editor`/`CodeEditorSchema`），schema.d.ts 22.7KB → 32.3KB。
4. **`README.md`**：
   - 修复集成示例 API 错误：`createSchemaRenderer()` 不接收 registry（可选参 `RendererDefinition[]`），registry 作为 **prop** 传入；改用 `createDefaultRegistry()`；补必填 `schemaUrl`/`formulaCompiler`
   - 完整集成补 `registerCodeEditorRenderers`
   - 类型定义章节改为如实说明「从内存注册表生成」+ 记录 8 个纯输入控件的已知局限（专有属性见各包 `schemas.ts`）
5. **`11-host-integration.md`（新增）**：`env` 契约（fetcher/notify/confirm/alert/navigate）、`loadDict`+`DictBean`、`loadPage`、`xui:roles`+`hasRole`、`xui:imports`+`importLoader`、`plugins`、`functions`/`filters`。已核对 `createXuiRolesPlugin` 未公开导出，文中以 RendererPlugin 伪代码呈现避免误导。
6. **`10-react-integration.md`**：内联 `RendererComponentProps`/`ResolvedNodeMeta`/`RenderRegionHandle`/`RendererHelpers`/`RendererRegistry` 类型签名，去除对 `docs/references/quick-reference.md` 的外部依赖（自定义渲染器开发自包含）。
7. **易混淆区分**：`form-advanced-fields.md` + `composite-fields.md` 补充 `array-field`(对象数组) vs `array-editor`(标量数组)、`markdown-editor`(表单) vs `markdown`(展示) vs `code-editor` 的区分；新增 ArrayField 章节。
8. **`scripts/validate.mjs`**：注册 code-editor；`xui:roles` 加入跳过列表（插件处理的约定键，非编译器已知属性）。
9. **`package.json`**：`generate-types` 脚本改用 css-stub loader（因加载 dist 需 stub `.css`）。

**验证**：`tsc -p flux-guide/tsconfig.json` ✅ 0 errors · `generate-types` ✅ 93 defs · `validate.mjs` 363 blocks ✅ 0 errors（warnings: 2，均既有）
