# Development Log - 2026-05-20

### 2026-05-20 (plan 391 / 406 closure)

- Executed `docs/plans/391-deep-audit-2026-05-19-report-field-panel-contract-plan.md` to completion and closed `R02-03` / `R04-02` plus retained field-panel contract items `06-01`, `10-01`, `11-01`.
- Restored the live report field-panel contract in `packages/report-designer-renderers/src/report-field-panel.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.tsx`, `packages/report-designer-renderers/src/page-renderer.tsx`, and `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`.
- Key fixes:
  - default report-designer left panel now uses the live `ReportFieldPanel` surface instead of the old static-list fallback
  - standalone/public field rows keep drag behavior without faux button semantics and preserve a dedicated keyboard-accessible insert button
  - `report-field-panel` renderer now limits keyboard insert to supported `cell | range` targets and emits canonical `dropFieldToTarget` payloads with `type`
  - spreadsheet canvas now accepts canonical field drag payloads from `dataTransfer` while remaining compatible with existing legacy test state
- Added focused regression coverage in `packages/report-designer-renderers/src/report-field-panel.test.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.test.tsx`, and `packages/report-designer-renderers/src/page-renderer.test.tsx`.
- Re-audited owner docs `docs/components/report-field-panel/design.md` and `docs/architecture/report-designer/design.md`; no text change was needed because they already described the restored live contract.
- Closed `docs/plans/406-open-ended-adversarial-review-2026-05-19-25-round-remediation-routing-plan.md` after verifying all routed owner plans (`391`, `400`, `407`, `408`, `409`, `410`, `411`, `412`) were honestly completed.
- Independent closure audits for `391` and `406` both returned `Verdict: acceptable` / `Findings: none`.

**Full-green verification (2026-05-20):**

- `pnpm --filter @nop-chaos/report-designer-renderers typecheck`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers build`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers lint`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers test`: 14/14 files, 146/146 tests passed
- `pnpm typecheck`: 49/49 successful tasks
- `pnpm build`: 26/26 successful tasks
- `pnpm lint`: 26/26 successful tasks
- `pnpm test`: 49/49 successful tasks

### 2026-05-20 (plan 415 - React 19 redundant memoization cleanup)

- Executed `docs/plans/415-react19-redundant-memoization-cleanup-plan.md`: removed redundant manual `React.memo`, `useCallback`, and `useMemo` calls across the codebase where React Compiler (enabled at error level) auto-memoizes.
- Successfully cleaned up leaf renderers and utility components: `flux-code-editor`, `flux-renderers-data` (chart, crud), `flux-renderers-form` (field-handlers, field-presentation, fieldset), `flux-renderers-basic` (loop, recurse), `nop-debugger`, `spreadsheet-renderers` (sheet-tab-bar, spreadsheet-grid), `ui` (field, json-viewer, slider), `word-editor-renderers` (outline-panel), and `flux-react/workbench` (extracted `resolveWorkbenchGridCols` to module-level pure function).
- **Key finding**: many scanner-flagged sites require manual memoization for correctness, not just performance. Reverted removals in core infrastructure:
  - `schema-renderer.tsx` — stateful singletons (`runtime`, `page`, `ownedSurfaceRuntime`) must not be recreated per render
  - `dialog-host.tsx` — `handleClose` and `surfaceContext` need stable references for dialog/drawer lifecycle
  - `node-renderer.tsx` — `memo()`, `mountedCid`, import scope allocation require stable identities
  - `form.tsx` — form runtime is a stateful singleton
  - `array-field.tsx` / `object-field.tsx` — items, validation owners, projected values used in `useEffect`/`useLayoutEffect` deps
  - `designer-page-body.tsx` and all `flow-designer-renderers` — interdependent provider/registration chains
  - `report-designer-renderers` — stateful core/bridge objects
  - `spreadsheet-renderers/page-renderer.tsx` — stateful `spreadsheetCore`/`spreadsheetBridge`
  - `apps/playground` — cascading `useCallback` dependency chains
  - `table-renderer` files — performance table tests failed
- Kept `useMemo` for Context.Provider values (carousel, dialog, drawer, toggle-group, chart) to satisfy `react/jsx-no-constructed-context-values` lint rule.
- Kept `useCallback` in carousel and sidebar-context for `react-hooks/exhaustive-deps` compliance.
- Phase 3 (derived-state-in-effect, 13 candidates) deferred for individual analysis.
- Scanner still reports 224 candidates — all in "kept for correctness" categories (reverted packages, lint-required, context-provider, or derived-state-in-effect).
- Net reduction: `-209` lines across 22 source files. 224 scanner candidates remain in deferred/reverted categories.

**Full-green verification (2026-05-20):**

- `pnpm typecheck`: 49/49 successful tasks
- `pnpm build`: 26/26 successful tasks
- `pnpm lint`: 26/26 successful tasks
- `pnpm test`: 49/49 successful tasks

Commits:

- `29580bda` chore: add React 19 redundant memoization scanner and plan 415
- `2507654b` refactor: remove redundant memo/useCallback/useMemo where React Compiler handles it
- `852edd5b` docs: update skills, plans, and daily log for React 19 memoization review
- `1a13c031` test: update import-stack test and e2e tests for React 19 changes
- `743e37f4` docs: update plan 415 with execution results and deferred items

### 2026-05-20 (scanner and skills updates)

- Created `scripts/audit/find-react19-optimization-candidates.mjs` with 3 rule buckets: `redundant-react-memo`, `redundant-use-callback`, `redundant-use-memo`, `derived-state-in-effect`.
- Added `react19OptimizationRules` to `scripts/audit/rules.mjs` with `hasUseNoMemoDirective()` exclusion.
- Registered `check:audit-react19-optimization-candidates` in root `package.json`.
- Updated `docs/skills/react19-best-practices-review.md` with "React Compiler 自动记忆化" section.
- Updated `docs/skills/deep-audit-prompts.md` and `docs/skills/open-ended-adversarial-review-prompt.md` with React 19 best practices references.
