# Development Log — 2026-04-14

### 2026-04-14

- Continued the post-Plan-87 doc cleanup by closing the remaining theme-compatibility terminology and anchor drift.
- Links: `docs/architecture/theme-compatibility.md`, `docs/index.md`, `docs/references/maintenance-checklist.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`
- Changes landed:
  - Replaced the stale `.na-theme-root` terminology with the current `.nop-theme-root` wording in the docs entry points that still routed theme-related work.
  - Updated `theme-compatibility.md` to point at current playground/renderer anchors instead of the retired `FlowDesignerExample` path and removed the leftover `AMIS renderers` / `flow-designer2` wording from the active baseline description.
  - Marked the `theme-compatibility.md` row in the architecture audit as consistent now that the file’s terminology and anchor set match the live repo baseline.
- Key decision: this pass only cleans owner-doc terminology and anchor drift; historical logs/plans/analysis records keep their original names when they are describing past baselines rather than the active contract.

### 2026-04-14

- Completed Plan 87 Workstream 3 and closed the successor plan after an independent styling closure audit surfaced the last live debugger-family drift.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/nop-debugger/src/panel.tsx`, `packages/nop-debugger/src/panel/use-inspect-mode.ts`, `packages/nop-debugger/src/panel/network-tab.tsx`, `packages/nop-debugger/src/panel/node-tab.tsx`, `packages/nop-debugger/src/panel/timeline-tab.tsx`, `packages/nop-debugger/src/panel.test.tsx`, `packages/nop-debugger/src/panel-minimized.test.tsx`, `packages/nop-debugger/src/panel-timeline.test.tsx`
- Changes landed:
  - Replaced the remaining debugger `nop-*` modifier/state classes with semantic root markers plus `data-panel-state` / `data-overlay-state`, and normalized the final debugger status/class-merge drift.
  - Replaced the remaining live debugger `button` / `input` controls with `@nop-chaos/ui` `Button` / `Input` while preserving the standalone debugger visual language.
  - Added focused coverage for minimized panel state, inspect overlay state, selected node tree entry, and errors-only toggle state; then updated Plan 87 and the architecture audit so Workstream 3 is now recorded as completed and spreadsheet/word-editor special inputs are explicitly treated as accepted exceptions rather than active drift.
- Key decision: the closure line for Workstream 3 is “semantic root marker + `data-slot` / `data-*` state everywhere except owner-documented domain exceptions”, not “eliminate every raw native control regardless of performance or native semantics”.

### 2026-04-14

- Continued Plan 87 Workstream 3 by removing the last low-risk template-string class merges from the debugger panel surfaces.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/nop-debugger/src/panel/node-tab.tsx`, `packages/nop-debugger/src/panel/timeline-tab.tsx`, `packages/nop-debugger/src/panel.test.tsx`, `packages/nop-debugger/src/panel-timeline.test.tsx`
- Changes landed:
  - Replaced the selected-tree-item class assembly in `node-tab.tsx` and the errors-only filter button class assembly in `timeline-tab.tsx` with explicit array filtering plus `join(' ')`.
  - Added focused debugger coverage proving the node tree still marks the selected entry with `selected` and the timeline errors-only toggle still publishes `ndbg-errors-only-toggle` plus `data-active` when enabled.
  - Re-ran the `@nop-chaos/nop-debugger` package test suite before returning to full workspace verification.
- Key decision: `nop-debugger` does not already carry the shared UI `cn()` dependency, so the smallest correct cleanup here is to remove template-string class assembly without widening the package dependency surface.

### 2026-04-14

- Added and executed a narrow owner-doc cleanup plan to close the remaining architecture wording conflicts around dependency roots, renderer field channels, region render APIs, runtime permission boundaries, and report/spreadsheet namespace ownership.
- Links: `docs/plans/88-owner-doc-conflict-closure-plan.md`, `docs/architecture/dependency-tracking.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/field-metadata-slot-modeling.md`, `docs/architecture/scoped-render-slots.md`, `docs/architecture/flux-dsl-vm-extensibility.md`, `docs/architecture/report-designer/api.md`
- Changes landed:
  - Rewrote `dependency-tracking.md` so it no longer claims `dependsOn` is absent and instead treats explicit dependency roots as an already-landed baseline with follow-up work focused on diagnostics and deeper optimization.
  - Removed the stale `meta.label` wording from `renderer-runtime.md` and aligned the region/slot docs so `render({ bindings, instancePath })` is the normative API while `data` / `instantiate()` remain compatibility carriers and `scopeKey` is documented only as an advanced/internal reuse hint.
  - Aligned `flux-dsl-vm-extensibility.md` with the security requirements by making runtime consume host-projected permission facts rather than act as a permission evaluator.
  - Rewrote `report-designer/api.md` away from global `register*Actions(runtime)` language toward page-owned `ActionScope.registerNamespace(...)` providers for `spreadsheet:*` and `report-designer:*`.
- Key decision: these remaining issues were owner-doc conflicts rather than architecture-model failures; the right fix was to keep the current live code choices and remove stale competing wording from the docs.
- Closure audit: a fresh independent subagent review first found two remaining in-scope drifts (`renderer-runtime.md` still had one stale `meta.label` line and `scoped-render-slots.md` still had old `data` / `scopeKey` examples). After correcting those examples, Plan 88 was closed as completed with audit evidence recorded in the plan file.
- Follow-up sync: updated `docs/analysis/2026-04-14-flux-architecture-principle-consistency-and-performance-review.md` so it now reflects the post-Plan-88 baseline, moving the five owner-doc conflicts from active findings to same-day closed conflicts and narrowing the remaining discussion to compatibility surfaces and non-optimal wording.
- Scope note: Plan 88 is docs-only; its validation checklist now records that no code changed and therefore repo-wide `pnpm` verification was not plan-owned work for this closure.

### 2026-04-14

- Continued the doc-convergence cleanup by aligning `field-frame.md` with the now-frozen normalized renderer contract and tightening the styling ownership wording.
- Links: `docs/architecture/field-frame.md`, `docs/architecture/field-binding-and-renderer-contract.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/styling-system.md`, `docs/architecture/theme-compatibility.md`, `packages/flux-react/src/field-frame.tsx`, `packages/flux-react/src/node-frame-wrapper.tsx`
- Changes landed:
  - Rewrote `field-frame.md` examples so they no longer teach direct `props.schema.*` reads or manual raw-HTML wrapper composition as the primary path.
  - Documented the real wrapper handoff: renderer metadata classifies `label`, `NodeFrameWrapper` resolves normalized wrapper inputs, and `FieldFrame` consumes those normalized values plus current form state.
  - Updated the FieldFrame API and internal-behavior section to match the live implementation more closely, including `cid`, current selector usage, and dynamic required-marker handling.
  - Tightened `styling-system.md` so the ownership table no longer says schema owns literally all visuals; it now distinguishes schema-authored explicit layout choices, component/UI chrome ownership, and global theme/token ownership.
- Key decision: small high-value doc fixes like `field-frame.md` should be applied directly rather than routed through a new plan; the goal here is to remove the remaining stale teaching examples that would otherwise keep reintroducing old mental models.

### 2026-04-14

- Continued the architecture doc cleanup by clarifying `xui:imports` channel semantics and adding explicit debugger performance-budget rules.
- Links: `docs/architecture/action-scope-and-imports.md`, `docs/architecture/debugger-runtime.md`, `docs/architecture/performance-design-requirements.md`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/panel.tsx`, `packages/nop-debugger/src/panel/timeline-tab.tsx`
- Changes landed:
  - Rewrote the `xui:imports` section so it now clearly describes one import declaration feeding two runtime channels: an action namespace view and an expression-helper view.
  - Kept expression-side imported functions explicitly supported, while documenting the recommended usage boundary as “compute in expressions, perform effects through actions”.
  - Added debugger event-budget, retention, snapshot-discipline, sampling, and export-boundary rules so the debugger contract now reflects the performance design requirements instead of only describing capabilities.
  - Anchored those debugger rules to the current bounded-event retention, pinned-error slicing, deferred search, and timeline virtualization behavior already present in `@nop-chaos/nop-debugger`.
- Key decision: for remaining architecture cleanup, the highest-value work is clarifying usage intent and hot-path constraints rather than continuing to add new capability surface area to the docs.

### 2026-04-14

- Continued the doc-hierarchy cleanup by tightening the top-level precedence wording and the `classAliases` execution-model wording.
- Links: `docs/architecture/flux-core.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/styling-system.md`, `docs/index.md`, `docs/analysis/2026-04-14-flux-architecture-principle-consistency-and-performance-review.md`, `packages/flux-react/src/node-renderer.tsx`
- Changes landed:
  - Rewrote the `flux-core.md` purpose section so it now explicitly positions itself beneath `frontend-programming-model.md` instead of sounding like a parallel top-level authority.
  - Tightened the `docs/index.md` routing line so readers are sent to `frontend-programming-model.md` first for top-level precedence and only then to `flux-core.md` for the codebase-wide baseline.
  - Updated `styling-system.md` so the `classAliases` implementation note now matches the live `NodeRenderer` model: alias-map publication is a node-renderer/provider boundary concern, while concrete class-name resolution happens during render.
  - Synced the 2026-04-14 analysis report so these same-day fixes now appear as narrowed or closed tensions rather than still-open contradictions.
- Key decision: after the direct contract conflicts were closed, the most valuable remaining cleanup is preventing document hierarchy and performance mental models from drifting back into parallel or contradictory descriptions.

### 2026-04-14

- Continued the architecture-boundary cleanup by syncing `flux-runtime-module-boundaries.md` to the current runtime module split.
- Links: `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/debugger-runtime.md`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/action-runtime-core.ts`, `packages/flux-runtime/src/action-runtime-handlers.ts`, `packages/flux-runtime/src/imports.ts`, `packages/flux-runtime/src/action-scope.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-runtime/src/operation-control.ts`, `packages/flux-runtime/src/status-owner.ts`
- Changes landed:
  - Expanded the module-boundary doc so it now reflects the already-split runtime modules for action dispatch core/helpers, imported namespace lifecycle, action scopes, component-handle registry, operation-control helpers, and owner-status publication.
  - Added an explicit host-infrastructure section clarifying that `action-scope`, component handles, and imported namespace lifecycle belong to runtime-host infrastructure rather than `ScopeRef`, domain cores, or renderer-local code.
  - Tightened the scope section so it now explicitly says `ScopeRef` stays data-only and must not become a mixed behavior registry for actions, handles, source/reaction entries, imports, or debugger state.
  - Synced the 2026-04-14 analysis report so this now reads as same-day boundary convergence rather than still-open runtime module drift.
- Key decision: once code has already been split into stable focused runtime modules, the owner doc should mirror that split directly instead of preserving an older “monolithic runtime file” mental model.

### 2026-04-14

- Continued the styling/doc-hierarchy cleanup by aligning `theme-compatibility.md` with the updated styling ownership model and tightening the focused-subsystem hierarchy wording.
- Links: `docs/architecture/theme-compatibility.md`, `docs/architecture/styling-system.md`, `docs/architecture/README.md`, `docs/analysis/2026-04-14-flux-architecture-principle-consistency-and-performance-review.md`
- Changes landed:
  - Rewrote the theme-compatibility ownership section so it now explicitly matches the styling-system split: schema owns author-authored explicit visual/layout choices, component/renderers own stable structure and package-owned chrome, and theme/token layers own defaults plus host overrides.
  - Clarified that theme compatibility is not a reason to move schema-authored explicit layout choices into hidden package CSS.
  - Tightened `docs/architecture/README.md` so focused subsystem docs are now described as deferring primitive/core-boundary questions back to `frontend-programming-model.md` and codebase-wide baseline questions back to `flux-core.md`.
  - Synced the 2026-04-14 analysis report so the styling/theme ownership topic now reads as an already-converged same-day fix rather than an active tension.
- Key decision: after the direct runtime and renderer contract conflicts were closed, the next best cleanup is keeping ownership language consistent across all styling/theme entry points so future docs do not recreate hidden “who owns visuals” ambiguity.

### 2026-04-14

- Continued the code/doc convergence work by moving the region-render call sites onto the `render()` / `bindings` primary API path.
- Links: `packages/flux-renderers-basic/src/container.tsx`, `packages/flux-renderers-basic/src/flex.tsx`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-basic/src/dialog.tsx`, `packages/flux-renderers-basic/src/drawer.tsx`, `packages/flux-renderers-basic/src/tabs.tsx`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/node-frame-wrapper.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowNode.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowEdge.tsx`
- Changes landed:
  - Replaced the straightforward no-arg region calls in core/basic/data renderers and test-support renderers from deprecated `instantiate()` to `render()`.
  - Updated the remaining flow-designer scoped region calls from `data: {...}` to `bindings: {...}` while preserving the existing `scopeKey` / `pathSuffix` hints that the current implementation still uses for cache/reuse behavior.
  - Updated shared slot-content helpers and `NodeFrameWrapper` to prefer `render()` when pulling region content into wrapper/title/label paths.
  - Verified that the workspace still passes `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` after the API-path convergence.
- Key decision: this pass deliberately targeted the primary API path first rather than trying to delete every compatibility carrier in one step; `scopeKey`, raw-schema fallback in wrappers, and the deprecated `instantiate()` method can still exist for now, but the common call sites now teach and use the intended contract.

### 2026-04-14

- Continued the wrapper convergence work by removing the remaining raw-schema fallback from `NodeFrameWrapper` and relying on normalized wrapper inputs only.
- Links: `packages/flux-react/src/node-frame-wrapper.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-runtime/src/schema-compiler/fields.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`
- Changes landed:
  - `NodeFrameWrapper` now reads `name`, `label`, and `required` only from normalized `resolvedPropsValue` / `regions`, instead of falling back to `templateNode.schema.*`.
  - This makes wrapper behavior align more closely with the renderer contract docs: classification happens earlier, wrapper code just consumes normalized channels.
  - Re-verified the workspace after the change: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Key decision: repeated/rendered-instance identity is already being handled in important paths such as table rows via `instancePath`; the highest-value remaining wrapper cleanup was to remove the raw-schema escape hatch rather than forcing broader repeated-identity rewrites in the same pass.

### 2026-04-14

- Continued the import lifecycle convergence work by implementing `releaseImportedNamespaces()` and aligning the tests with lexical namespace release semantics.
- Links: `packages/flux-runtime/src/imports.ts`, `packages/flux-runtime/src/__tests__/runtime-imports.test.ts`, `packages/flux-react/src/useNodeImports.ts`, `docs/architecture/action-scope-and-imports.md`
- Changes landed:
  - Implemented real release behavior for imported namespaces: release now decrements the per-scope refcount and unregisters the namespace when the count reaches zero.
  - Preserved the separate module-load dedupe layer, so releasing a scope-local namespace does not imply unloading the underlying module cache.
  - Updated runtime import tests to reflect the intended semantics: final release removes local visibility, child-scope release restores parent shadowing, and re-ensure reuses the already-loaded module while recreating the scope-local registration.
  - Removed an unrelated unused import from `packages/flux-runtime/src/index.ts` to keep package lint clean.
- Verification:
  - `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/runtime-imports.test.ts` passed.
  - `pnpm --filter @nop-chaos/flux-react test` passed.
  - workspace `pnpm typecheck`, `pnpm build`, and `pnpm lint` passed.
  - Full workspace `pnpm test` still has one unrelated pre-existing blocker in `packages/flux-runtime/src/__tests__/scope-merge.test.ts` (`ScopeRef.merge > works on child scope without affecting parent`), which is outside this import-lifecycle change.
- Key decision: this pass intentionally aligned runtime behavior with the architecture rule “unmount or boundary replacement releases the owned registration through the action-scope lifecycle” while keeping module dedupe separate from scope-local visibility.

### 2026-04-14

- Continued Plan 87 Workstream 3 by closing the remaining low-risk raw select drift in the condition-builder value editor.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-renderers-form/src/renderers/condition-builder/ValueInput.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/value-input.test.tsx`
- Changes landed:
  - Replaced the multi-select add affordance in `ValueInput.tsx` from a raw `<select>` / `<option>` pair to `NativeSelect` / `NativeSelectOption` from `@nop-chaos/ui`.
  - Kept the existing multi-select badge toggle behavior intact while aligning the add control with the shared shadcn/native-select DOM marker contract.
  - Extended the focused `value-input` test suite so it now proves the add control exposes `data-slot="native-select-wrapper"` and `data-slot="native-select"` and still supports add/remove multi-select interaction.
- Key decision: for the remaining Workstream 3 raw HTML cleanup, direct replacement with an existing `@nop-chaos/ui` primitive is preferred when the subtree is not performance-sensitive and the new primitive already carries the shared selector contract.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning the remaining explicit class-merge drift in the flow xyflow canvas helpers.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/render-ports.tsx`, `packages/flow-designer-renderers/src/canvas-bridge.test.tsx`
- Changes landed:
  - Replaced the last template-string handle class merge in `render-ports.tsx` with `cn(defaultHandleClass, port.appearance?.className)`.
  - Added focused coverage proving xyflow port handles keep the default handle classes while also honoring custom appearance classes.
  - Re-ran focused flow-designer tests and full workspace verification after the cleanup.
- Key decision: for the remaining Workstream 3 drift, the right scope is small renderer-family cleanup backed by direct selector-contract assertions, not a broad new audit pass before each edit.

### 2026-04-14

- Added a focused architecture review report on doc-vs-doc consistency, Flux design-principle alignment, and whether the current architecture docs represent a clean/performance-optimal end state.
- Links: `docs/analysis/2026-04-14-flux-architecture-principle-consistency-and-performance-review.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/api-data-source.md`, `docs/architecture/dependency-tracking.md`, `docs/architecture/security-design-requirements.md`, `docs/architecture/report-designer/api.md`
- Key findings: the architecture docs now share a strong core baseline, but still contain a small set of real contract conflicts around `dependsOn` current-vs-future status, `label/title` channel ownership, region render API shape, runtime permission semantics, and report-designer action registration ownership; the `xui:imports` expression-function topic is better treated as a guidance-level boundary clarification than as a hard contradiction.
- Key decision: treat the remaining issues as owner-contract cleanup targets rather than evidence that the Flux core model is wrong; keep `xui:imports` expression helpers supported for ergonomics, but document the recommended boundary as “compute in expression, perform effects through action”.
- Next step: tighten the conflicting owner docs so the architecture tree speaks one contract for capability boundaries, dependency roots, renderer normalization, and host-owned namespace registration.

### 2026-04-14

- Completed the remaining Plan 87 Workstream 2 source/reaction closure pass and narrowed the plan to the still-open styling audit only.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/architecture/api-data-source.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-runtime/src/__tests__/runtime-reactions.test.ts`, `packages/flux-renderers-data/src/__tests__/data-source.test.tsx`
- Changes landed:
  - Added focused evidence that runtime reactions dispatch only after the triggering scope write settles and that renderer-mounted `data-source` nodes preserve named `mergeToScope` publication through the live React lifecycle.
  - Updated the source/reaction owner docs and audit notes so they reflect the already-landed runtime baseline instead of older pre-convergence findings.
  - Marked Plan 87 Workstream 2 as completed, leaving Workstream 3 as the main remaining open slice.
- Key decision: the remaining Workstream 2 gap was evidence and doc parity, not another substrate rewrite; the safest closure path was to lock the live behavior with focused tests and remove stale audit language.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning the remaining obvious live root-marker drift in the word-editor family.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/word-editor-renderers/src/WordEditorPage.tsx`, `packages/word-editor-renderers/src/__tests__/word-editor-page.test.tsx`
- Changes landed:
  - Normalized the `WordEditorPage` semantic root onto `cn()` while preserving the `.nop-word-editor` marker.
  - Added a focused page-level test proving the word-editor shell keeps the semantic root marker and still renders the expected high-level shell regions.
  - Re-ran focused word-editor tests plus full workspace verification after the root cleanup.
- Key decision: once the remaining live grep results shrink to isolated root-marker/class-merge stragglers, it is worth closing them immediately even if the broader Workstream 3 audit is still open.

### 2026-04-14

- Continued Plan 87 Workstream 3 by clearing the next concentrated set of live form-control raw HTML and slot drift.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionBuilder.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionItem.tsx`, `packages/flux-renderers-form/src/renderers/tree-controls.tsx`, `packages/flux-renderers-form/src/renderers/input.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-markers.test.tsx`, `packages/flux-renderers-form/src/__tests__/form-tree-checkbox-fields.test.tsx`
- Changes landed:
  - Removed the remaining raw live button usage from `condition-builder` and `tree-controls` in favor of `Button` from `@nop-chaos/ui`.
  - Replaced remaining radio/checkbox option labels with `Label` and aligned both embedded and picker `condition-builder` roots with explicit `field-control` slots.
  - Extended focused form tests so the condition-builder marker contract now also covers the field-control slot while tree-control behavior remains green.
- Key decision: the safest way to keep shrinking Workstream 3 is to take concentrated family slices where the same HTML/slot issue repeats across several neighboring files and can be verified together.

### 2026-04-14

- Continued Plan 87 Workstream 3 by aligning the runtime-owned composite form editors with the field slot contract.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/array-editor.tsx`, `packages/flux-renderers-form/src/renderers/key-value.tsx`, `packages/flux-renderers-form/src/renderers/tag-list.tsx`, `packages/flux-renderers-form/src/__tests__/form-runtime-fields.test.tsx`
- Changes landed:
  - Replaced the direct `<label>` wrapper roots in `array-editor`, `key-value`, and `tag-list` with non-label roots while introducing explicit `data-slot="field-control"` containers around the editor body.
  - Kept the existing validation/touched/dirty state publication intact while making these composite editors structurally closer to the same field slot model used elsewhere in the form family.
  - Added focused runtime-form assertions that the updated composite editors still validate correctly and now expose the `field-control` slot.
- Key decision: for runtime-owned composite editors, the next safest convergence step is structural slot alignment first; broader wrapper unification with shared field chrome can stay for a later pass.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning the next non-flow renderer styling slice in the data and spreadsheet families.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`, `packages/spreadsheet-renderers/src/page-renderer.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Replaced the remaining live raw button affordances in `table-renderer.tsx` and `tree-renderer.tsx` with `Button` from `@nop-chaos/ui`.
  - Normalized the `spreadsheet-page` semantic root onto `cn()` and added focused integration assertions for the page root/header/body slot contract.
  - Re-ran targeted data/spreadsheet tests and then re-ran the full workspace verification with a longer timeout after an earlier timeout cut off recursive `pnpm` execution and produced fake downstream worker errors.
- Key decision: verification should be re-run with enough wall-clock budget when the repo-wide recursive commands are the only thing failing; timeout-induced worker crashes are not treated as code regressions.

### 2026-04-14

- Continued Plan 87 Workstream 3 by removing the last raw palette buttons from flow-designer and keeping the selector contract under focused tests.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-palette.tsx`, `packages/flow-designer-renderers/src/designer-controls.test.tsx`
- Changes landed:
  - Replaced the remaining raw `<button>` elements in `designer-palette.tsx` with `Button` from `@nop-chaos/ui`.
  - Kept the existing palette group/item slot contract intact while adding focused test coverage that palette items now expose shadcn button markers instead of raw button markup.
  - Re-ran focused flow tests plus full workspace verification after the palette component swap.
- Key decision: once a flow renderer is already under Workstream 3 cleanup, the remaining raw HTML affordances should be removed immediately if the shadcn replacement is direct and low risk.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning the next flow-designer selector/UI-component slice around node tooling and shared editor fields.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-field.tsx`, `packages/flow-designer-renderers/src/designer-toolbar.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowNode.tsx`, `packages/flow-designer-renderers/src/index.test.tsx`
- Changes landed:
  - Replaced the remaining raw `<label>` usage in `designer-field.tsx` and the toolbar switch branch with `Label` from `@nop-chaos/ui`.
  - Removed the internal `nop-designer-node-toolbar` marker from `DesignerXyflowNode.tsx` and replaced it with `data-slot="designer-node-toolbar"`.
  - Added focused xyflow coverage proving the node quick toolbar now appears via the `data-slot` contract and that the old internal marker class is absent.
  - Updated the styling audit/plan wording so this flow-designer node-toolbar and editor-field cleanup is recorded as landed convergence evidence.
- Key decision: for Workstream 3, internal flow-designer structure should keep moving toward `data-slot` while root semantic markers remain in place, and every such selector migration should ship with a focused DOM assertion.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning the next flow-designer styling/UI-contract slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-inspector.tsx`, `packages/flow-designer-renderers/src/designer-palette.tsx`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-renderers/src/designer-controls.test.tsx`, `packages/flow-designer-renderers/src/index.test.tsx`, `packages/flow-designer-renderers/src/index.xyflow.test.tsx`
- Changes landed:
  - Normalized `designer-inspector`, `designer-palette`, and `designer-page` root class handling onto `cn()` while preserving the semantic root markers.
  - Replaced the remaining raw `<label>` / `<textarea>` usage in `designer-inspector` with `Label` / `Textarea` from `@nop-chaos/ui`, bringing that file back in line with the project’s UI-component rule.
  - Expanded focused flow tests so inspector interactions still pass while the root marker/shell contract stays covered alongside the existing toolbar, palette, status, and createDialog coverage.
  - Updated the styling audit wording so it now records the newly landed flow inspector/palette/page-shell cleanup.
- Key decision: Workstream 3 should keep treating root marker preservation, `cn()` normalization, and UI-component replacement as one small convergence unit when a renderer file is already being touched.

### 2026-04-14

- Continued Plan 87 Workstream 3 by cleaning another small renderer-marker/class-merge slice in the flow/report families.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/report-designer-renderers/src/report-designer-inspector.tsx`, `packages/report-designer-renderers/src/report-designer-inspector.test.tsx`, `packages/report-designer-renderers/src/report-designer-toolbar.tsx`, `packages/flow-designer-renderers/src/designer-toolbar.tsx`, `packages/flow-designer-renderers/src/designer-controls.test.tsx`
- Changes landed:
  - Normalized `report-designer-inspector`, `report-designer-toolbar`, and `flow designer-toolbar` root class handling onto `cn()` while preserving their semantic root markers.
  - Tightened focused tests so they assert the semantic root markers and existing `data-slot` structure directly instead of depending on looser className string checks.
  - Updated the styling audit wording to record this newly landed flow/report selector-contract coverage and root class-merge cleanup.
- Key decision: Workstream 3 should keep landing small, verifiable slices that remove style-contract drift without trying to batch unrelated renderer families into one risky edit.

### 2026-04-14

- Completed the independent closure audit pass for Plan 87 Workstream 4/5 readiness and closed the last report-inspector provider-context gap in live code.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/report-designer-core/src/adapters.ts`, `packages/report-designer-core/src/runtime/inspector-panels.ts`, `packages/report-designer-core/src/core.ts`, `packages/report-designer-core/src/inspector.ts`, `packages/report-designer-core/src/__tests__/designer-core.test.ts`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Added `spreadsheet: SpreadsheetRuntimeSnapshot` to `InspectorPanelContext` and threaded the live spreadsheet snapshot through report inspector-panel resolution.
  - Kept the earlier report host-scope/shared-bridge work intact and closed the last concrete Workstream 4 mismatch by ensuring inspector providers now receive the same shared spreadsheet runtime context described by the report family docs.
  - Hardened report core error-state handling so inspector provider failures settle into stable `inspector.error` / `loading: false` state instead of escaping only as uncaught refresh failures.
  - Added focused core and renderer coverage proving inspector providers now receive spreadsheet runtime context and that provider failures render through stable report host error state.
  - Re-ran an independent closure audit and updated Plan 87 plus the architecture audit so Workstream 4 and Workstream 5 are now recorded as completed, while the overall plan remains open for Workstream 2/3 debt.
- Key decision: the right closure line for Workstream 4 was to finish the shared host contract all the way through inspector-provider context and failure-state publication, not just page-level namespace and host-scope alias wiring.

### 2026-04-14

- Continued Plan 87 Workstream 4 by moving Report Designer onto a shared page-owned spreadsheet bridge instead of a canvas-private spreadsheet runtime.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Moved spreadsheet core/bridge creation up to `report-designer-page`, so the page host now owns the spreadsheet runtime boundary instead of letting `ReportSpreadsheetCanvas` spin up a private spreadsheet core.
  - Registered a page-level `spreadsheet` namespace alongside `report-designer` in `page-renderer.tsx`, which closes the earlier gap where toolbar/inspector schema inside `report-designer-page` could not dispatch `spreadsheet:*` even though the family docs said that path should exist.
  - Threaded the live spreadsheet snapshot into report host-scope construction so `spreadsheet`, `workbook`, `activeSheet`, `selection`, `activeCell`, `activeRange`, and `runtime` now reflect the same shared page-owned spreadsheet state that the canvas uses.
  - Updated `ReportSpreadsheetCanvas` to consume the shared bridge/snapshot from the page host rather than allocating its own local spreadsheet runtime.
  - Added focused integration coverage proving report-designer toolbar schema can now dispatch `spreadsheet:setCellValue` and immediately observe the changed `A1` value plus dirty state through the shared report host scope.
- Key decision: the correct convergence move is to make `report-designer-page` the owner of the spreadsheet runtime/bridge and namespace wiring, then let canvas and schema regions consume that shared substrate, instead of trying to paper over the drift with more aliases around a canvas-private spreadsheet core.

### 2026-04-14

- Continued Plan 87 Workstream 4 by tightening the host projection shape for spreadsheet/report page-level schema fragments.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/spreadsheet-renderers/src/page-renderer.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Expanded `spreadsheet-page` host scope so it no longer exposes only the nested `spreadsheet` object; it now also aliases `workbook`, `activeSheet`, `selection`, `activeCell`, `activeRange`, and `runtime` at the top level.
  - Expanded `report-designer-page` host scope so the current selection target is now available through `selection` and `target` aliases in addition to the older `selectionTarget` field.
  - Added focused spreadsheet integration coverage proving schema can read `activeSheet` and `runtime.readonly` through the new top-level aliases while preserving the nested `spreadsheet.*` compatibility path.
  - Added focused report integration coverage proving schema can read the new `target` / `selection` aliases and that they reactively switch from the default `sheet` target to `workbook` after `report-designer:openInspector` retargeting.
  - Updated the architecture audit rows for the report/spreadsheet family so they now record these landed host-projection aliases as convergence evidence.
- Key decision: before tackling the bigger missing shared spreadsheet bridge inside `report-designer-page`, Workstream 4 should first make the already-existing page host projections truthful and easier to consume through the documented stable top-level fields.

### 2026-04-14

- Continued Plan 87 Workstream 4 by closing the next shared host-protocol drift in spreadsheet/report host wiring and projection.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/spreadsheet-renderers/src/page-renderer.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Moved `spreadsheet-page` namespace registration from `useEffect` to `useLayoutEffect`, so spreadsheet host wiring now matches the same page-level namespace publication timing already used by flow-designer and required by `complex-control-host-protocol.md`.
  - Replaced the stale `runtime.dirty: false` placeholder in `report-designer-renderers/src/host-data.ts` with the real runtime-owned `snapshot.dirty` value.
  - Added focused report integration coverage proving schema mounted inside the report designer host scope now observes `runtime.dirty` flipping from `false` to `true` after a `report-designer:updateMeta` mutation.
  - Updated the architecture audit so the complex-control host-protocol note now records the landed spreadsheet namespace-timing fix and report host-scope dirty projection fix, instead of still grouping them into generic remaining drift.
- Key decision: Workstream 4 should keep converging all complex-control families onto the same small set of host rules (`useLayoutEffect` namespace publication, runtime-owned dirty/session facts, truthful host projection) before attempting broader closure claims.

### 2026-04-14

- Continued Plan 87 Workstream 4 by finishing the live Flow Designer `createDialog` host path and restoring full workspace verification afterward.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-renderers/src/designer-palette.tsx`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/flow-designer-renderers/src/designer-controls.test.tsx`, `packages/flow-designer-renderers/src/index.xyflow.test.tsx`
- Changes landed:
  - Kept the already-started Flow Designer host implementation that routes `createDialog`-configured palette entries through `openCreateDialog(...)` instead of dispatching `addNode` immediately.
  - Confirmed the page host now owns the pending create-dialog state, renders the configured `createDialog.body` under the designer host `scope` / `actionScope`, optionally merges object data returned by `createDialog.submitAction`, and only then dispatches `addNode`.
  - Fixed the focused xyflow integration mock so it observes the live canvas bridge contract through `props.snapshot.doc.nodes.length` instead of the older `props.nodes` shape, which was the actual reason the create-confirm regression test stayed red after the host path itself was implemented.
  - Updated the architecture audit row for `docs/architecture/flow-designer/config-schema.md` so it now records the landed create-dialog behavior instead of still reporting it as docs-ahead-of-code drift.
- Key decision: for Workstream 4, the right owner path is host-owned dialog orchestration around the existing designer command surface, not a second direct-add code path hidden inside palette or canvas adapters.

### 2026-04-14

- Continued Plan 87 by starting the host-family convergence slice (Workstream 4) and landing the next real Flow/Report host corrections instead of only expanding Workstream 3 selector coverage.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/report-designer-core/src/types.ts`, `packages/report-designer-core/src/core.ts`, `packages/report-designer-core/src/__tests__/designer-core.test.ts`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Moved Flow Designer namespace publication onto an explicit `useLayoutEffect` registration path in `designer-page.tsx`, matching the shared complex-control host protocol more closely than the previous generic helper use at that page boundary.
  - Added an explicit `dirty` field to `ReportDesignerRuntimeSnapshot` and now derive it from the runtime undo stack in `report-designer-core`, instead of leaving report host status publication to guess or hardcode it later in the renderer layer.
  - Updated `report-designer-renderers/src/page-renderer.tsx` so `statusPath` publishes the runtime-owned `snapshot.dirty` value instead of `dirty: false`.
  - Added focused tests proving the new host/session contract: report core now proves `dirty` flips after metadata mutation and clears after undo, and report renderer integration now proves `statusPath` publishes `clean` then `dirty` after a `report-designer:updateMeta` toolbar mutation.
- Key decision: Workstream 4 host alignment should prefer moving session/dirty facts into the domain runtime snapshot itself, then letting page-level status publication read that owner state, instead of continuing to paper over missing host state in renderer pages.

### 2026-04-14

- Continued Plan 87 Workstream 3 into the report-designer renderer package by normalizing the page host root class merge and tightening focused selector-contract coverage for the report host surfaces.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/report-designer-toolbar.test.tsx`, `packages/report-designer-renderers/src/report-designer-inspector.test.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.test.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Changes landed:
  - Replaced the manual root class concatenation in `page-renderer.tsx` with shared `cn('nop-report-designer', props.meta.className)`.
  - Extended the report toolbar, inspector, field-panel, and integration tests so they now explicitly prove the semantic report markers and slots: `.nop-report-toolbar`, `.nop-report-inspector`, `report-field-panel-shell`, `report-designer-stack`, `report-designer-section`, `report-designer-group`, `report-designer-header`, `report-designer-status`, and `report-designer-toolbar`.
  - Updated the architecture audit wording so Workstream 3 evidence now includes report-designer host surfaces alongside the previously covered flow/basic/form/data/react-host slices.
- Key decision: report-designer follows the same clarified styling baseline as flow-designer: preserve domain-owned default presentation, but converge root marker merging and lock stable host-visible slot/marker contracts with focused tests.

### 2026-04-14

- Continued Plan 87 Workstream 3 into the flow-designer renderer package by normalizing remaining local class-merging helpers and locking the shared entry-surface selector contract.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flow-designer-renderers/src/designer-icon.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowEdge.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowNode.tsx`, `packages/flow-designer-renderers/src/designer-palette.tsx`, `packages/flow-designer-renderers/src/designer-controls.test.tsx`, `packages/flow-designer-renderers/src/index.test.tsx`
- Changes landed:
  - Replaced the local `classNames()` helpers in `designer-icon.tsx`, `DesignerXyflowEdge.tsx`, and `DesignerXyflowNode.tsx` with shared `cn()` usage.
  - Replaced the remaining template-string class merges in `designer-palette.tsx` with `cn()` while preserving the existing flow-designer visual language and semantic root markers.
  - Extended `designer-controls.test.tsx` so the palette now explicitly proves `designer-palette-group-header` and `designer-palette-item` slot structure.
  - Extended `index.test.tsx` so `DesignerIcon` now explicitly proves the `nop-icon` root plus `data-icon` identity and `aria-hidden` host contract.
- Key decision: for flow-designer Workstream 3, the right scope is consistency and selector-contract locking, not flattening its domain-owned visual defaults; these renderers are allowed to keep their richer domain styling while still converging on shared merge helpers and stable marker/slot contracts.

### 2026-04-14

- Continued Plan 87 Workstream 3 by locking selector-contract coverage for the remaining shared `scope-debug` and `chart` renderers.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-basic/src/__tests__/basic-reactions.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`, `packages/flux-renderers-basic/src/scope-debug.tsx`, `packages/flux-renderers-data/src/chart-renderer.tsx`
- Changes landed:
  - Extended `basic-reactions.test.tsx` so the `scope-debug` renderer now explicitly proves the semantic `.nop-scope-debug` root coexists with `scope-debug-header`, `scope-debug-kind`, `scope-debug-title`, `scope-debug-body`, and `scope-debug-json` slot structure.
  - Extended `data-tree-and-chart.test.tsx` so the chart renderer now explicitly proves the semantic `.nop-chart` root coexists with `chart-canvas` when data exists and `chart-empty` when it does not.
  - Updated the architecture audit wording so the Workstream 3 evidence now includes `scope-debug` and `chart` alongside the earlier page/container/table/tree/form/react-host slices.
- Key decision: once the broader structural families were covered, the most useful remaining shared renderers to lock were `scope-debug` and `chart` because they are host-observable primitives with simple stable slot contracts and little reason to leave unguarded.

### 2026-04-14

- Continued Plan 87 Workstream 3 by tightening the shared form-root and tree-control selector contract.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/__tests__/form-validation-ui.test.tsx`, `packages/flux-renderers-form/src/__tests__/form-tree-checkbox-fields.test.tsx`, `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-renderers-form/src/renderers/tree-controls.tsx`
- Changes landed:
  - Extended `form-validation-ui.test.tsx` so the form renderer now explicitly proves the `.nop-form` root coexists with `form-body` and conditionally-present `form-actions` slots.
  - Extended `form-tree-checkbox-fields.test.tsx` so the tree-control family now explicitly proves `input-tree-options`, `tree-option-list`, `tree-option-items`, `tree-select-value`, and `tree-select-icons` slot structure.
  - Added focused searchable tree-select coverage to prove the popover option/search structure is exposed through `data-slot` markers when the tree chooser is open.
  - Updated the architecture audit wording so the Workstream 3 evidence now includes the form root/tree-control family in addition to the earlier composite/detail/basic/data/react-host slices.
- Key decision: after the composite/detail editors, the next most leverageful Workstream 3 target was the shared form root and tree-control surfaces because they are reused by many field types and are a common source of host-selector drift.

### 2026-04-14

- Continued Plan 87 Workstream 3 across the composite/detail form renderer family by locking the already-landed slot structure in focused renderer tests.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/array-field.test.tsx`, `packages/flux-renderers-form/src/renderers/variant-field-selector.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-field-basic.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-view-basic.test.tsx`
- Changes landed:
  - Extended `array-field.test.tsx` so the scalar array renderer now explicitly proves `array-field-item` and `array-field-item-body` slot structure under the semantic `.nop-field` root.
  - Extended `variant-field-selector.test.tsx` so the variant renderer now explicitly distinguishes editable `variant-field-selector` structure from read-only `variant-field-readonly-body` structure.
  - Extended the detail-field/detail-view suites so they now explicitly prove the draft-body and draft-error slot markers appear only when the staged surface is active and validation fails.
  - Updated the architecture audit wording so the selector-contract evidence now includes the composite/detail form family, not just the earlier tabs/condition-builder/basic/data/react-host slices.
- Key decision: once the structural/basic/data families were covered, the next highest-value Workstream 3 step was to lock the composite/detail editors because they are the form renderers most likely to regress toward ad hoc internal wrappers during future surface/draft work.

### 2026-04-14

- Continued Plan 87 Workstream 3 across the data renderer family by tightening the table/tree selector contract and eliminating the last obvious conditional class-string merges in that slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`
- Changes landed:
  - Normalized the remaining conditional icon/filter button classes in `table-renderer.tsx` onto `cn()` instead of template-string concatenation.
  - Extended `data-table.test.tsx` so the table renderer now explicitly proves the root `.nop-table` marker coexists with `table-header-region`, `table-container`, `table-header`, `table-empty-row`, `table-empty-cell`, `table-footer`, `table-row`, and `table-pagination` slot structure.
  - Extended `data-tree-and-chart.test.tsx` so the tree renderer now explicitly proves the root `.nop-tree` marker coexists with `tree-node`, `tree-children`, and `tree-empty` slot structure.
  - Updated the architecture audit wording so Workstream 3 evidence reflects the newly landed `FieldFrame`, dialog/drawer host, page/container, and table/tree coverage rather than the older narrower tabs/condition-builder-only baseline.
- Key decision: after the shared form/basic/React host slices, the next highest-value Workstream 3 target is the data renderer family because its root markers and internal slots are reused by host integration and regression tests across pagination, row actions, and empty/loading states.

### 2026-04-14

- Continued Plan 87 Workstream 3 by expanding focused structural coverage for the basic page/container renderer family.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-renderers-basic/src/__tests__/basic-page-layout.test.tsx`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-basic/src/container.tsx`
- Changes landed:
  - Extended the existing basic layout regression suite so `page` now explicitly proves the root `.nop-page` marker coexists with `data-slot="page-header"`, `page-toolbar`, `page-body`, and `page-footer`.
  - Extended the same suite so `container` now explicitly proves the root `.nop-container` marker coexists with `data-slot="container-header"`, `container-body`, and `container-footer`.
  - Kept the assertions focused on structure rather than visual class strings, so the tests lock the selector protocol without overfitting to presentation details.
- Key decision: after the tabs and condition-builder slices, the next most valuable Workstream 3 tests are the shared structural renderers that host many nested schemas and therefore need stable root-marker plus internal-slot coverage.

### 2026-04-14

- Continued Plan 87 Workstream 3 by tightening the shared React host marker protocol around `FieldFrame` and surface-host internals.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-react/src/frame-slot-meta.test.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-dialogs.test.tsx`
- Changes landed:
  - Added focused `FieldFrame` coverage for both sides of the presence-only state-attribute contract: dirty/touched/visited/invalid attributes appear as empty-string presence markers only when active, and stay absent for clean fields while required/hint content remains addressable through `data-slot` markers.
  - Replaced the React host-only `nop-dialog-card` and `nop-drawer-card` internal classes in `dialog-host.tsx` with `data-slot="dialog-surface"` and `data-slot="drawer-surface"`.
  - Extended the focused React dialog tests to assert the new host surface slots and to guard against reintroducing the removed internal `nop-*-card` classes.
- Key decision: once a marker is purely host-internal structure and not renderer identity, `data-slot` is the stable contract; reserve `nop-*` classes for semantic renderer roots.

### 2026-04-14

- Continued Plan 87 Workstream 3 by finishing the focused condition-builder marker regression coverage and normalizing the last obvious conditional class merges in that renderer slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-renderers-form/src/renderers/condition-builder/config-markers.test.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-test-support.tsx`
- Changes landed:
  - Fixed the new `config-markers` test so it now asserts the live selector contract directly: root `.nop-condition-builder` stays as the semantic renderer marker, `condition-group` and `condition-item` are exposed through `data-slot`, and the removed `.nop-cb-group` / `.nop-cb-item` classes stay absent.
  - Normalized the condition-builder conjunction/not segmented-toggle buttons in `ConditionGroup.tsx` onto `cn()` instead of inline template-string class concatenation.
  - Updated the local mocked `@nop-chaos/ui` test support to export `cn()` so the focused condition-builder suites keep matching the real renderer implementation surface.
- Key decision: focused DOM tests should lock the actual documented contract, not incidental visible text that can vary independently of the marker protocol.

### 2026-04-14

- Started Plan 87 Workstream 3 with the current clarified styling-contract baseline: semantic renderers may provide basic format-complete default structure, but internal non-semantic marker classes and inconsistent class-merging helpers still need cleanup.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-basic/src/tabs.tsx`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-basic/src/container.tsx`, `packages/flux-renderers-basic/src/flex.tsx`
- Changes landed:
  - Re-audited Workstream 3 under the updated rule that semantic layout renderers like `flex` are allowed to provide baseline structure when that structure is part of the renderer's meaning.
  - Removed the extra internal `nop-tabs-root` and `nop-tabs-content` classes in `tabs.tsx` and replaced them with `data-slot` markers so the internal structure now matches the selector protocol more closely.
  - Normalized the touched basic renderers (`page`, `container`, `flex`, `text`, `icon`, `dynamic-renderer`, `scope-debug`) onto `cn()` instead of the local `classNames()` helper.
- Key decision: Workstream 3 should target non-semantic internal marker drift and inconsistent renderer merging patterns first, not treat every default semantic layout capability as a styling-contract violation.

### 2026-04-14

- Continued Workstream 3 by removing the last obvious non-semantic internal marker classes from the condition-builder renderer while keeping its default visual structure intact.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionItem.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`
- Changes landed:
  - Replaced `nop-cb-item` with `data-slot="condition-item"`.
  - Replaced `nop-cb-group` with `data-slot="condition-group"`.
  - Kept the condition-builder's explicit default visual classes because they are part of its format-complete editor presentation rather than hidden semantic-marker styling.
- Key decision: Workstream 3 should remove redundant internal marker classes first, while preserving explicit default presentation for complex editor-like controls that are expected to render coherently with no extra schema styling.

### 2026-04-14

- Finished the last Workstream 2 F3 provider-wrap convergence slice by moving React node-boundary publication onto the compiler-emitted wrapper closure.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`
- Changes landed:
  - Extended `TemplateNode` to carry the compiled provider wrapper closure alongside the already-threaded provider metadata.
- Preserved the compiler-emitted `renderPlan.wrapProviders` closure in `schema-compiler` and exposed it through the template artifact.
- Replaced the manual three-branch provider assembly in `NodeRendererProviders` with direct execution of the compiled wrapper closure, so action-scope/component-registry/class-alias publication is now fully compiler-owned in the live React path.
- Closed the corresponding F3 audit note and updated the affected architecture-doc status rows from remaining drift to current consistency.
- Key decision: once the explicit `SurfaceRuntime` extraction was landed, the next most-correct Workstream 2 step was to finish the provider-wrap convergence completely instead of stopping at compiler-owned boolean flags.

### 2026-04-14

- Finished the clean-slate SurfaceRuntime extraction for Plan 87 Workstream 2 and moved live surface ownership fully off `PageRuntime`.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/surface-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-react/src/schema-renderer.tsx`
- Changes landed:
  - Added an explicit shared `SurfaceRuntime` contract plus unified `SurfaceEntry` stack in `flux-core`, replacing the old split `dialogs[]` / `surfaces[]` shape.
  - Added `packages/flux-runtime/src/surface-runtime.ts` as the owner runtime for dialog/drawer/sheet-style surfaces and removed surface ownership from `PageRuntime`.
- Updated `RendererRuntime`, `ActionContext`, `SchemaRenderer`, helper dispatch assembly, and `DialogHost` so built-in dialog/drawer actions and root host rendering flow through `surfaceRuntime` rather than `page.openDialog` / `page.surfaceStore`.
- Updated the focused runtime/react surface tests to validate the new explicit surface owner boundary.
- Key decision: without compatibility constraints, the most correct owner model is one explicit shared `SurfaceRuntime` with one unified stack-based substrate; `PageRuntime` should remain page-shell-only and should not proxy surface ownership.

### 2026-04-14

- Continued Plan 87 Workstream 2 by aligning the import/action-scope provider-publication path with compile-owned metadata and dropping an obsolete release-direction assumption.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/useNodeImports.ts`
- Changes landed:
- Re-audited the current `xui:imports` baseline against the docs and confirmed the updated rule: imported libraries stay resident after load, so no new `releaseImportedNamespaces()` teardown behavior was added.
- Threaded compile-owned provider publication flags into `TemplateNode.providerPlan` during schema compilation.
- Updated `NodeRendererProviders` to use the compiled provider plan for action-scope/component-registry/class-alias publication instead of re-deriving that decision from live schema/component policy.
- Narrowed the architecture audit so F3 and the related docs now reflect the true remaining gap: React still wraps providers imperatively instead of executing the compiler-emitted wrap closure directly, but the provider-publication decision itself is now compiler-owned.
- Key decision: with the current doc baseline, the correct Workstream 2 move is not to reintroduce import release semantics, but to reduce the real remaining drift by moving provider-publication decisions onto compiled metadata.

### 2026-04-14

- Continued Plan 87 Workstream 2 by tightening the surface-owner implementation boundary without forcing a wider `PageRuntime` contract break mid-slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/__tests__/runtime-dialogs-scope.test.ts`, `packages/flux-runtime/src/action-runtime-handlers.ts`, `packages/flux-react/src/dialog-host.tsx`
- Changes landed:
  - Kept the public `PageRuntime` API stable, but extracted the dialog/drawer open-close/store handling inside `page-runtime.ts` behind a dedicated local surface controller so the surface-owner logic is no longer spread across the page-runtime object body.
  - Re-checked the runtime and React call sites so dialog and drawer actions, disposal, and host rendering still route through the stable page surface API.
  - Recorded in Plan 87 that the remaining surface-owner gap is now narrower: an explicit shared `SurfaceRuntime` contract is still missing, but the live implementation has a cleaner single owner-controlled surface substrate than before.
- Key decision: for Workstream 2, prefer an internal ownership extraction that preserves the stable page-facing API over a wider cross-package contract rename that would interrupt convergence progress without finishing the full surface-runtime design.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/__tests__/runtime-dialogs-scope.test.ts"` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-runtime` ✓
  - `pnpm --filter @nop-chaos/flux-runtime lint` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Advanced Plan 87 Workstream 2 by closing the stale `NodeLocator` identity debt and narrowing the remaining owner gap to surface/runtime substrate work.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/nop-debugger/src/controller-inspect.test.ts`, `packages/nop-debugger/src/controller-component-inspector.ts`, `packages/flux-runtime/src/node-resolver.ts`
- Changes landed:
  - Re-audited the live runtime/debugger/action code paths and confirmed active package code no longer uses `NodeLocator`; mounted lookup and inspection already converge on `cid` plus optional `instancePath`.
  - Updated the architecture audit so F4 and the affected per-document notes no longer claim there are still active locator-based runtime/debugger/action paths in package code.
  - Renamed the last leftover debugger test id string that still used `locator` terminology so tests and logs reflect the current identity model.
- Key decision: Workstream 2 should stop treating `NodeLocator` removal as still-open code debt; the real remaining mismatch is repeated-template/runtime-publication cleanup plus the separate surface-owner substrate gap.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/controller-inspect.test.ts" --config vitest.config.ts` in `packages/nop-debugger` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/nop-debugger` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/nop-debugger` ✓
  - `pnpm --filter @nop-chaos/nop-debugger lint` ✓

### 2026-04-14

- Finished Plan 87 Workstream 1 by landing the root execution-boundary convergence on top of the already-landed Host Projection shared-runtime contract.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/workbench/hooks.ts`
- Changes landed:
  - Updated `SchemaRenderer` so the root path now compiles `props.schema` through `runtime.compile(...)` and passes the resulting `CompiledTemplate` into `RenderNodes` instead of relying on `RenderNodes` to normalize raw root schema input.
  - Kept `RenderNodes` flexible for fragment/helper entrypoints by leaving its broader input normalization behavior intact while narrowing the steady-state root contract to compiled input.
  - Reworked the runtime-core regression test to intercept the live `RenderNodes` boundary and assert that the root input is a compiled template carrying the original text node schema, which directly locks the Workstream 1 execution-boundary behavior.
- Key decision: the root renderer boundary should own compile-once normalization, while `RenderNodes` keeps schema fallback support only as a compatibility path for non-root fragment/helper callers.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-runtime.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Restored full workspace verification after the post-Plan-84 cleanup regressions by fixing runtime action evaluation, React render-boundary tests, condition-builder test support, and chart renderer test isolation.
- Links: `packages/flux-runtime/src/action-runtime-handlers.ts`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-actions.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`
- Changes landed:
  - Restored `ajax` and `submitForm` API evaluation to use action-context compiled evaluation in `flux-runtime`, which cleared the broken action, monitor, timeout, and submit test paths.
  - Earlier in the day the `SchemaRenderer` root boundary still routed raw schema into `RenderNodes`; that temporary regression was later corrected by the dedicated Plan 87 Workstream 1 slice above, so the live baseline is again root compile-once into `CompiledTemplate`.
  - Switched the remaining `condition-builder` config action tests to the shared mocked support path so drag/remove assertions no longer bypass the package's UI mocks.
  - Added an ECharts test mock in the chart behavior suite so jsdom no longer crashes on real canvas rendering while still verifying chart handle registration.
- Key decision: keep the product runtime behavior intact and solve the remaining failures with owner-local fixes; use test-side isolation for ECharts instead of weakening the chart renderer.
- Verification:
  - `pnpm --filter @nop-chaos/flux-runtime typecheck` / `build` / `test` ✓
  - `pnpm --filter @nop-chaos/flux-react typecheck` / `build` / `test` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form test` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-data test` ✓
  - `pnpm typecheck` ✓
  - `pnpm build` ✓
  - `pnpm lint` ✓
  - `pnpm test` ✓

### 2026-04-14 (Session 3)

- Implemented DingFlow interactive features for the tree-mode flow designer
- This completes the remaining ~40% of the DingFlow editor task (Phase 1-2 were static rendering + layout fix)
- Key files created:
  - `packages/flow-designer-renderers/src/dingflow/dingflow-constants.ts` — Layout constants (CARD_W=220, BTN_DIAMETER=28, etc.), EdgeLeg type, DingFlowOverlay type
  - `packages/flow-designer-renderers/src/dingflow/DingFlowEdge.tsx` — Custom React Flow edge with Manhattan-style paths (right-angle bends for branch/merge), edge labels
  - `packages/flow-designer-renderers/src/dingflow/dingflow-overlays.ts` — Pure computation: groups edges by source/target to find branch split and merge join positions
  - `packages/flow-designer-renderers/src/dingflow/DingFlowPlusButton.tsx` — Blue #3296fa circle with Plus icon, positioned absolutely below non-terminal nodes
  - `packages/flow-designer-renderers/src/dingflow/DingFlowAddConditionOverlay.tsx` — Green "Add Condition" pill rendered via ViewportPortal at branch split points
  - `packages/flow-designer-renderers/src/dingflow/DingFlowMergeOverlay.tsx` — Blue circle rendered via ViewportPortal at merge join points
  - `packages/flow-designer-renderers/src/dingflow/DingFlowAddNodeMenu.tsx` — Fixed-position popover with Approver/CC/Condition options
  - `packages/flow-designer-renderers/src/dingflow/DingFlowCanvasOverlay.tsx` — Standalone overlay orchestrator (not used in final integration, kept as reference)
  - `packages/flow-designer-renderers/src/dingflow/index.ts` — Barrel exports
- Key files modified:
  - `packages/flow-designer-core/src/tree-projection.ts` — Added `leg: 'near-target'` to branch edges, `leg: 'near-source'` to merge edges (for Manhattan path routing)
  - `packages/flow-designer-renderers/src/designer-command-adapter.ts` — Added 3 new command types (`insertChainNode`, `insertChainNodeAtMerge`, `insertBranchPair`) with full implementations using transactions + auto-relayout
  - `packages/flow-designer-renderers/src/designer-context.ts` — Added optional `onPlusButtonClick` callback to `DesignerContextValue`
  - `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowCanvas.tsx` — Registered DingFlowEdge, added `TreeModeOverlays` component with ViewportPortal, added `documentMode`/`onPlusButtonClick` props
  - `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowNode.tsx` — Added DingFlowPlusButton inside non-terminal tree-mode nodes, reads `onPlusButtonClick` from context
  - `packages/flow-designer-renderers/src/designer-xyflow-canvas/xyflow-utils.ts` — `createXyflowEdges` now routes tree-mode edges through `dingflowEdge` type
  - `packages/flow-designer-renderers/src/designer-canvas.tsx` — Added popover state for AddNodeMenu, handles plus-button clicks, menu selection dispatches insert commands
  - `packages/flow-designer-renderers/src/designer-page.tsx` — Bridges `plusButtonHandlerHolder` between canvas content and DesignerContext
  - `apps/playground/src/schemas/dingtalk-workflow-tree-schema.json` — Enabled undo/redo/history/shortcuts features
- Architecture decisions:
  - Edge type routing: `createXyflowEdges` checks `documentMode` to use `dingflowEdge` vs `designerEdge`
  - Handler bridge pattern: `plusButtonHandlerHolder` module-level object connects `DesignerCanvasContent` (where menu lives) to `DesignerContext` (where nodes read from)
  - Command pattern: 3 mutation commands wrap graph operations in transactions with automatic tree relayout via `simpleTreeLayout`
  - Overlay rendering: ViewportPortal inside ReactFlow for pan/zoom-synced overlays; fixed-position popover for AddNodeMenu
- All verification passes: typecheck ✅ (20 packages) build ✅ test ✅ lint ✅ (pre-existing DingTalkFlowDemo error only)

### 2026-04-14 (Session 2)

- Implemented DingFlow + ActionFlow tree-mode editors in playground with full DingTalk-style visual styling
- Fixed tree-mode node positioning bug (all nodes rendered at 0,0 origin)
- Key files created/modified:
  - `packages/tailwind-preset/src/styles/flow-designer-nodes.css` — All DingFlow (`nop-dt-node*`) and ActionFlow (`nop-af-node*`) CSS styles with colored header bars per node variant
  - `packages/tailwind-preset/src/styles/base.css` — Added import for flow-designer-nodes.css
  - `apps/playground/src/schemas/dingtalk-workflow-tree-schema.json` — Enhanced 7 node type body schemas with rich card DOM
  - `apps/playground/src/schemas/action-flow-tree-schema.json` — Enhanced 3 node type body schemas with metadata fields
  - `packages/flow-designer-renderers/src/designer-inspector.tsx` — Beautified with NODE_TYPE_INFO mapping (16 types → icon/color), editable fields per type
  - `packages/flow-designer-core/src/tree-layout.ts` — Added `simpleTreeLayout()` synchronous BFS-based layout algorithm
  - `packages/flow-designer-renderers/src/designer-page.tsx` — Added `TreeModeLayoutWrapper` component for initial layout, enabled auto-layout for tree mode
- Key decision: Expression evaluation only supports simple `${data.field}` in node body schemas; use static text for defaults
- Key decision: Synchronous `simpleTreeLayout()` (BFS layering) used for initial positions instead of async ELK, because the component architecture uses plain function calls (not React components) preventing useEffect from firing
- All verification passes: typecheck ✅ build ✅ 1389+ tests ✅ lint ✅ browser QA ✅

### 2026-04-14

- Added interactive +button functionality to DingTalkFlowDemo page.
- Link: `apps/playground/src/pages/DingTalkFlowDemo.tsx`
- Key changes:
  - `AddNodeMenu` component: fixed-position popover with 3 options (Approver/CC/Condition), each as a colored circle with icon and label
  - `insertNode()` function: handles chain insertion (between source and downstream) and merge-overlay insertion (above merge target)
  - `insertBranch()` function: creates 2 condition cards + branch/merge edges when inserting a condition branch
  - State management: `useState` for nodes/edges/popover, `useRef` for ID counter
  - Module-level `_onPlusClick` callback pattern to bridge React Flow node components to canvas state
  - Dynamic overlay computation: `useMemo` recalculates "Add Condition" and merge + button positions from current graph topology
  - `BranchOverlay` interface extended with `sourceId` for click routing
- Updated: `docs/architecture/flow-designer/dingflow-visual-spec.md` — added Section 8 documenting +button interaction spec (popover, insertion behavior, state management)
- Typecheck and build pass clean
- Next step: browser testing of the full interaction flow, then apply patterns to formal tree mode rendering
