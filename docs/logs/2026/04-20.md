# 2026-04-20 Dev Log

### 2026-04-20 (session 17)

- Added clean-slate next-generation runtime design `docs/experiments/next-gen-low-code-runtime-kernel-design-v7.md`, derived only from `docs/low-code-dsl-runtime-requirements.md` and `docs/architecture/flux-design-principles.md` rather than current implementation docs/code.
- Iterated the v7 design through multiple independent sub-agent review rounds until the remaining feedback dropped to non-blocking residual risks.
- Key v7 decisions:
  - keep the semantic primitive set smaller than current Flux while preserving Flux principles
  - make `Execution Package` the explicit precompile boundary
  - keep `Resource` lifecycle-owned but route actual visible effects through `Capability`
  - use deterministic transaction publication instead of a looser global scheduler story
- Added comparison doc `docs/experiments/next-gen-runtime-vs-current-flux-comparison-v7.md` against current Flux normative docs and selected runtime/react implementation anchors.
- Reworked the comparison doc after independent review to keep four axes explicit: clean-slate design purity, normative architecture maturity, implementation maturity, and migration cost.
- Key decision: position v7 as a stronger clean-slate experimental kernel that should pull current Flux forward, not as evidence that current Flux's current architecture baseline is invalid.
- Link to docs: `docs/experiments/next-gen-low-code-runtime-kernel-design-v7.md`, `docs/experiments/next-gen-runtime-vs-current-flux-comparison-v7.md`
- Next step: if v7 is used as an active convergence target, split the absorbable pieces into focused owner docs or plans instead of treating the experiment doc itself as normative architecture.

### 2026-04-20 (session 16)

- Completed `docs/plans/121-unified-value-adapter-for-all-field-types-plan.md` end to end.
- Added the shared adapter contract in `packages/flux-core/src/value-adapter.ts` and exported it from `packages/flux-core/src/index.ts`.
- Migrated simple form fields to declarative adapters in `packages/flux-renderers-form/src/field-utils.tsx` and `packages/flux-renderers-form/src/renderers/input.tsx`:
  - `input-text` / `textarea` / `select` / `radio-group` now use `stringAdapter()`
  - `checkbox` / `switch` now use `booleanStringAdapter()` with direct boolean UI writes
  - `checkbox-group` now uses a dedicated array adapter
- Wired composite owners onto the same contract without adding staged submit semantics:
  - `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx` now applies `transformInAction` on child-scope initialization and `transformOutAction` on projected writeback
  - `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` now routes target-variant migration through `actionAdapter()` and maps the variant key to `AdapterContext.name`
- Refactored `packages/flux-renderers-form-advanced/src/detail-view/value-adaptation-helper.ts` to delegate internally to `actionAdapter()` while preserving the public helper API and existing staged owner lifecycle in `detail-field.tsx` / `detail-view.tsx`.
- Added focused regression coverage in `packages/flux-core/src/value-adapter.test.ts`, `packages/flux-renderers-form/src/__tests__/form-field-handlers.test.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/value-adaptation-helper.test.ts`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.test.tsx`, and `packages/flux-renderers-form-advanced/src/variant-field/variant-field-transform.test.tsx`.
- Updated `docs/architecture/value-adaptation-and-detail-field.md` so `ValueAdapter` is now the normative shared protocol and the staged helper is documented as an owner-level layer on top.
- Key decision: keep `nullableAdapter()` as a same-shape null passthrough wrapper instead of a cross-shape adapter because null passthrough is only type-safe when external and internal value shapes match.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`. One workspace `pnpm test` run observed a transient `flux-runtime` async-validation diagnostics failure; immediate focused rerun `pnpm --filter @nop-chaos/flux-runtime test -- runtime-validation.test.ts` passed, and a full `pnpm --filter @nop-chaos/flux-renderers-form-advanced test` pass also completed green.
- Next step: future value-oriented fields should declare adapters directly instead of adding JSX-local coercion or one-off transform helpers.

### 2026-04-20 (session 15)

- Closed Phase 6 of `docs/plans/120-runtime-async-governance-convergence-plan.md` by wiring the existing bounded runtime async-owner inspection surface through the debugger host boundary instead of adding a new event stream.
- Added additive host/runtime plumbing for debugger inspection:
  - `SchemaRendererProps` now supports `onRuntimeChange` in `packages/flux-core/src/types/renderer-hooks.ts`
  - `packages/flux-react/src/schema-renderer.tsx` now reports the live runtime instance to hosts the same way it already reports component registry and action scope
  - `packages/nop-debugger/src/controller.ts`, `automation.ts`, and `types.ts` now expose `getAsyncOwnerDebugSnapshot()` and `setRuntime(...)`
- Key decision: keep plain action/request execution on the existing execution-control and monitor surfaces; do not promote them into owner-level async epoch state just to match source/reaction/validation diagnostics.
- Added debugger-focused regression coverage in `packages/nop-debugger/src/index.test.ts` and `automation.test.ts` to prove the automation API can read unified async owner diagnostics without generating extra `state:snapshot` events.
- Closure audit then found one plan-owned lifecycle gap: validation async-owner entries were not fully cleared on form refresh/dispose. Fixed that in `packages/flux-runtime/src/form-runtime-owner.ts` and added regression coverage in `packages/flux-runtime/src/__tests__/runtime-validation.test.ts`.
- Verification results:
  - plan-owned focused checks passed: `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime test`, `pnpm --filter @nop-chaos/nop-debugger typecheck`, `pnpm --filter @nop-chaos/nop-debugger build`, `pnpm --filter @nop-chaos/nop-debugger test`, `pnpm --filter @nop-chaos/flux-react typecheck`, `pnpm --filter @nop-chaos/flux-react test`, `pnpm --filter @nop-chaos/flux-core build`
  - workspace `pnpm typecheck` and `pnpm build` are still blocked by unrelated `packages/flux-core/src/value-adapter.ts` type errors surfacing through downstream packages and the playground build
  - workspace `pnpm lint` is still blocked by unrelated `packages/flux-renderers-form/src/field-utils.tsx` lint failures
  - workspace `pnpm test` is still blocked by unrelated failing tests in `packages/flux-renderers-form`
- Updated `docs/architecture/debugger-runtime.md` and the owner plan so the plan scope is now recorded as closure-audited and complete, with the remaining unchecked workspace verification items explicitly marked as external blockers rather than hidden async-governance debt.
- Next step: address the unrelated workspace failures under their owning areas if the goal is a fully green repository baseline.

### 2026-04-20 (session 14)

- Executed the first landed slices of `docs/plans/120-runtime-async-governance-convergence-plan.md` against the live repo after re-auditing concurrent third-party edits instead of assuming the earlier baseline still held.
- Added a shared runtime-local async governance substrate in `packages/flux-runtime/src/async-governance.ts` plus additive `flux-core` types in `packages/flux-core/src/types/async-governance.ts`, `renderer-core.ts`, and `runtime.ts`.
- Converged runtime-owned repeating async owners onto that substrate without changing their main author-visible contracts:
  - `data-source` refresh runs now track owner-level run identity and stale publish gating in `packages/flux-runtime/src/data-source-runtime.ts` and `source-registry.ts`
  - `reaction` async dispatch now reports queued/running plus shared run diagnostics in `packages/flux-runtime/src/reaction-runtime.ts`
  - async validation now records owner-level run diagnostics while preserving existing path-level supersession behavior in `packages/flux-runtime/src/form-runtime-validation.ts` and `form-runtime.ts`
- Exposed additive runtime inspection via `runtime.getAsyncOwnerDebugSnapshot()` in `packages/flux-runtime/src/runtime-factory.ts` while keeping plain action/request execution primarily on the execution-control layer.
- Had to repair the current mixed action baseline before continuing because concurrent work had already moved the runtime toward compiled actions; hardened `packages/flux-runtime/src/action-runtime.ts` and `action-runtime-core.ts` to tolerate missing compiled control metadata instead of conflicting with the in-flight migration.
- Added focused regression coverage for stale-dropped parallel source results, reaction queued/running async diagnostics, and validation stale async completion diagnostics in `packages/flux-runtime/src/__tests__/runtime-sources-refresh.test.ts`, `reaction-runtime.test.ts`, and `runtime-validation.test.ts`.
- Synced owner docs in `docs/architecture/api-data-source.md`, `docs/architecture/form-validation.md`, `docs/architecture/debugger-runtime.md`, and `docs/architecture/action-interaction-state.md` so the current baseline now distinguishes execution control from owner-level async governance explicitly.
- Verification so far: `pnpm --filter @nop-chaos/flux-runtime typecheck`, targeted Vitest passes for `runtime-scope-actions`, `runtime-reactions`, `runtime-sources-refresh`, and `runtime-validation`.
- Next step: finish Phase 6/7 by auditing debugger/request alignment, then run full workspace verification and closure audit evidence.

### 2026-04-20 (session 13)

- Completed Phase 3 and Phase 4 of `docs/plans/119-action-precompile-and-args-unification-plan.md` by finishing schema-compiled action program wiring and migrating the runtime executor to compiled-only execution internally.
- Landed compiled-executor runtime changes across `packages/flux-runtime/src/action-runtime.ts`, `action-runtime-core.ts`, `action-runtime-handlers.ts`, and `runtime-action-helpers.ts`:
  - `dispatch()` now normalizes raw `ActionSchema` input at the boundary into `CompiledActionProgram`
  - the executor now runs `CompiledActionNode` directly for `when`, `args`, `api`, `value`, `values`, `dialog`, `drawer`, branch chains, and parallel branches
  - `beforeAction` plugin hooks still receive raw `ActionSchema`, but plugin output is recompiled before execution so the runtime core no longer mixes raw-schema execution paths
- Updated schema-compiler assertions in `packages/flux-runtime/src/__tests__/schema-compiler-registry.test.ts` to validate `CompiledActionProgram` / `CompiledActionNode.targeting` structure instead of legacy raw action shapes.
- Restored full `flux-runtime` verification while finishing the migration:
  - fixed `ajax.dataPath` handling to read compiled targeting metadata
  - added compiled-program boundary caching so repeated `runtime.dispatch(rawAction)` calls do not recompile payload/api objects on every dispatch
  - repaired reaction/data-source async diagnostics regressions so queued reaction runs and late parallel source results still produce correct debugger-facing state
- Updated plan/reference docs to reflect that action payload compilation now happens at schema compile / dispatch-entry time rather than lazily inside the main executor path.
- Verification: `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`, `pnpm --filter @nop-chaos/flux-runtime lint`, `pnpm --filter @nop-chaos/flux-runtime test`.
- Next step: execute Phase 5 (`args` authoring convergence for `ajax` / `submitForm` / dialog/drawer payload carriers) and Phase 7 compile-time diagnostics coverage.

### 2026-04-20 (session 12)

- Added new owner plan `docs/plans/120-runtime-async-governance-convergence-plan.md` to converge async result-governance semantics across runtime-owned async owners.
- The new plan distinguishes two layers explicitly:
  - `Operation Control` stays responsible for timeout / retry / abort / debounce / dedup execution control.
  - shared async governance is responsible for run identity, supersession, publish gate, settle metadata, and debugger-facing async diagnostics.
- Current baseline recorded from live repo and owner docs:
  - request/action already share substantial execution control infrastructure
  - `data-source` has dedup + abort + in-flight status but not a shared owner-level publish-gate substrate
  - `reaction` has debounce / queue / fire-count guard but not a shared async run contract
  - async validation already suppresses stale runs locally, but with validation-specific machinery
- Key decision: the plan does not try to merge all async behavior into one giant abstraction; first-class convergence scope is limited to repeating runtime-owned async owners (`data-source`, `reaction`, async validation) plus additive debugger alignment.
- Next step: execute Phase 1 baseline audit and terminology freeze before coding any shared async-governance helper.

### 2026-04-20 (session 11)

- Executed Phase 1 and Phase 2 of `docs/plans/119-action-precompile-and-args-unification-plan.md`
- **Phase 1 (Baseline Audit And Contract Freeze)**:
  - Inventoried all built-in action payload/targeting/control fields against live runtime code
  - Froze `action + args` authoring contract in `docs/references/action-payload-matrix.md`
  - Made convergence decisions: `ajax`/`submitForm` → `args: ApiSchema` (YES), `openDialog`/`openDrawer` → `args` (YES), `setValue`/`setValues` → keep specialized carriers
- **Phase 2 (Compiled Action IR Design)**:
  - Added `CompiledActionProgram`, `CompiledActionNode`, `CompiledActionPayload`, `CompiledActionTargeting`, `CompiledActionControl` types to `packages/flux-core/src/types/actions.ts`
  - Created `packages/flux-runtime/src/action-compiler.ts` with `compileAction()` and `compileActions()` functions
  - Lowering rules: `when`/`args`/`api`/`value`/`values`/`dialog`/`drawer` → `CompiledRuntimeValue<T>`, `then`/`onError`/`onSettled`/`parallel` → recursive `CompiledActionNode[]`, targeting/control → unchanged
  - Legacy top-level payload fallback preserved for compatibility
  - Updated `docs/architecture/action-algebra-formal-spec.md` with Compiled Action IR section
- Key decisions:
  - `args` only carries payload, not targeting or control-flow fields
  - Write actions (`setValue`/`setValues`) keep specialized carriers due to path+value semantics
  - Targeting fields remain independent (not compiled to dynamic values since typically static)
- Verification: `pnpm --filter @nop-chaos/flux-core build`, `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`
- Next step: Phase 3 (Schema Compiler Integration) — integrate action compiler into node compilation

### 2026-04-20 (session 10)

- Executed and closed `docs/plans/117-renderer-definition-unified-static-contract-plan.md` end to end.
- Landed unified static renderer contract fields in `packages/flux-core/src/types/renderer-core.ts`: `rendererClass`, `rendererTraits`, `propContracts`, `eventContracts`, `componentCapabilityContracts`, and `scopeExportContracts`.
- Added shared `CapabilityMethodContract` to `packages/flux-core/src/schema-diagnostics/manifest.ts` so host manifests and ordinary renderer capability metadata reuse one method-signature language without sharing one envelope.
- Added tooling-facing adapter baseline in `packages/flux-core/src/types/renderer-authoring-contract.ts` with `resolveRendererAuthoringContract()` and `ResolvedAuthoringContract`.
- Wired diagnostics baseline in `packages/flux-runtime/src/schema-compiler/shape-validation.ts` so `propContracts` now participates in the closed authoring prop model used by unknown-property checks.
- Added live pilot metadata for `button`, `form`, `crud`, and `designer-page` across `packages/flux-renderers-basic/src/index.tsx`, `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-renderers-data/src/index.tsx`, and `packages/flow-designer-renderers/src/index.tsx`.
- Added focused coverage in `packages/flux-core/src/renderer-authoring-contract.test.ts`, `packages/flux-runtime/src/schema-compiler-renderer-contracts.test.ts`, and renderer-package contract tests to keep the static metadata aligned with live runtime capabilities/scope exports.
- Updated docs in `docs/references/renderer-interfaces.md`, `docs/architecture/capability-contract-model.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/capability-projection-manifest.md`, and `docs/architecture/action-scope-and-imports.md` to fix the `editableProps` vs runtime `props` distinction and keep `hostContract` explicitly host-only.
- Key decision: diagnostics currently consume `propContracts` as the ordinary renderer closed-prop baseline, while `eventContracts`, `componentCapabilityContracts`, and `scopeExportContracts` remain tooling-facing metadata; runtime dispatch order and host manifest envelope remain unchanged.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Next step: if Inspector/productized online editing is pursued later, build directly on `ResolvedAuthoringContract` instead of reopening the contract-surface decision.

### 2026-04-20 (session 9)

- Created experimental next-generation low-code runtime kernel design v5: `docs/experiments/next-gen-low-code-runtime-kernel-design-v5.md`
- Design is based solely on `docs/low-code-dsl-runtime-requirements.md` requirements and `docs/architecture/flux-design-principles.md` principles, without referencing current codebase
- Core design features:
  - Algebraic type system with tagged unions (Value, Effect, CompiledAction)
  - Signal-first reactivity with explicit TrackFn (no global ReactiveContext.current)
  - ActionScope parallel to ScopeRef for lexical capability resolution
  - ScopePatch with sourceId for self-write protection
  - CompiledLoop and NodeRef for iteration and recursion
  - FormRuntime.draftMode with commit/discard for nested form isolation
  - ErrorBoundary for render error handling
- Underwent two rounds of sub-agent review, addressing 18 issues including:
  - Missing ScopeFactory in RenderEnv
  - RenderEnv access policy clarification (kernel direct, renderers via props)
  - $slot parameter access documentation
  - ValidationContext definition
- Created comparison analysis: `docs/experiments/next-gen-runtime-vs-current-flux-comparison-v5.md`
- Key findings from comparison:
  - Current Flux scores 8.5/10 vs v5's 6.9/10
  - Flux's seven-primitive model with promotion test is more mature
  - Flux's lexical root dependency tracking prevents over-invalidation
  - Flux's prototype-chain scope optimization is critical for performance
  - Flux's `useSyncExternalStore` integration solves real concurrent mode issues
  - v5's Effect algebra and draft form model could enhance Flux
- Verdict: Current Flux is the genuinely next-generation design; v5 offers theoretical elegance without practical proof
- Next step: Consider adopting v5's Effect algebra and draft form concepts to enhance current Flux

### 2026-04-20 (session 8)

- Updated architecture docs to formalize the renderer contract split around one unified static entry (`RendererDefinition`) without flattening host and ordinary renderer envelopes.
- Added `docs/architecture/capability-contract-model.md` as the owner doc for shared contract language, renderer classification (`instance-renderer` / `flux-owner-renderer` / `domain-host-renderer`), and the `FluxValueShape` vs Zod boundary.
- Updated `docs/architecture/capability-projection-manifest.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/renderer-runtime.md`, and `docs/architecture/schema-file-validator.md` to keep `hostContract` host-only, preserve publication-attribution rules, and distinguish ordinary renderer metadata from host manifest semantics.
- Added execution plan `docs/plans/117-renderer-definition-unified-static-contract-plan.md` for the follow-on code/type/tooling work.
- Ran multiple independent sub-agent reviews over the architecture docs and the new plan, then revised the docs/plan until the remaining review output dropped to non-blocking wording notes only.
- Key decision: future unification happens at the static contract entry and shared shape language level, not by merging `ActionScope` with `ComponentHandleRegistry` or by promoting ordinary controls into host manifests.
- Next step: execute plan 117 by landing the `RendererDefinition` static metadata fields, a dependency-safe authoring adapter contract, and a small pilot set (`button`, `form`, `crud`, `designer-page`).

### 2026-04-20 (session 7)

- Updated capability-contract architecture baseline across `docs/architecture/capability-projection-manifest.md`, `docs/architecture/action-scope-and-imports.md`, and `docs/architecture/renderer-runtime.md`.
- Added new owner doc `docs/architecture/capability-contract-model.md` to make the contract-layer decision explicit: share `FluxValueShape` and method-contract language across host manifests and ordinary renderer metadata, but keep host manifest envelope and runtime lookup mechanisms (`ActionScope` vs `ComponentHandleRegistry`) separate.
- Updated `docs/index.md` routing so future capability-contract questions go to the new owner doc first.
- Key decision: do not generalize host manifests into the generic renderer metadata envelope; do not replace `FluxValueShape` with Zod as the platform-owned contract IR; Zod remains optional adapter/runtime-guard territory only.
- Next step: if ordinary renderer static metadata (`propContracts` / `capabilityContracts`-style fields) lands in code, update `packages/flux-core/src/types/renderer-core.ts` and align `docs/references/renderer-interfaces.md` with the new contract split.

### 2026-04-20 (session 6)

- Re-audited the Budibase-derived actionable ideas against Flux's own architecture constraints before accepting any recommendation into `docs/analysis/2026-04-20-budibase-vs-nop-chaos-flux-architecture-comparison.md`.
- Used multiple independent sub-agents to evaluate proposal fit against `docs/architecture/renderer-runtime.md`, `docs/architecture/field-binding-and-renderer-contract.md`, `docs/architecture/styling-system.md`, and `docs/architecture/complex-control-host-protocol.md`.
- Key decision: only record recommendations that survive Flux-native ownership boundaries; reject any idea that creates a second contract beside existing `statusPath`, `ComponentHandle`, host scope, namespaced actions, or styling ownership.
- Accepted only in narrowed form: richer static `RendererDefinition` authoring metadata, shared handle helper/factory on top of existing `ComponentHandleRegistry`, inspector-only static capability hints, and owner-level form remount/recreation rules.
- Explicitly rejected as non-Flux-native: generic `data-provider renderer` abstraction, Budibase-style `styleable`/preview DOM injection, and builder/runtime coupling patterns.

### 2026-04-20 (session 5)

- Added Budibase vs Flux comparison analysis: `docs/analysis/2026-04-20-budibase-vs-nop-chaos-flux-architecture-comparison.md`
- Compared `c:/can/ai/budibase` and `c:/can/nop/nop-chaos-flux` across architecture layering, runtime/rendering model, state ownership, plugin system, docs governance, engineering quality, and implementation maturity.
- Research method: used multiple parallel sub-agents plus direct repo/doc inspection for cross-validation.
- Key decision: separate `architecture design quality` from `implementation maturity` so the comparison does not conflate clean architecture with long-lived product breadth.
- Conclusion summary:
- `nop-chaos-flux` scores higher on architecture clarity, boundary discipline, renderer/runtime abstraction, and docs-to-code alignment.
- Budibase scores higher on product/platform maturity, deployment and upgrade readiness, and breadth of shipped low-code platform capabilities.
- Next step: use this Budibase comparison together with the existing NocoBase/Flux comparison reports as an external baseline set for future Flux architecture positioning work.

### 2026-04-20 (session 4)

- Created comprehensive architecture comparison analysis: `docs/analysis/2026-04-20-nocobase-vs-nop-chaos-flux-architecture-comparison.md`
- Compared NocoBase (`c:/can/ai/nocobase`) and nop-chaos-flux across 14 dimensions using 8 parallel sub-agents
- **NEW: Added detailed architecture design quality and code implementation quality evaluation sections**
- Key architecture design findings:
  - NocoBase: B rating - good plugin system but God Object anti-pattern in core classes (Application 1424 lines, FlowModel 1600+ lines)
  - nop-chaos-flux: A- rating - strict unidirectional dependencies, framework-agnostic runtime, clear module boundaries
- Key code quality findings:
  - NocoBase: C+/B- rating - 5678 type safety issues (3463 `: any`, 1808 `as any`, 407 `@ts-ignore`), ESLint rules disabled, no strict mode
  - nop-chaos-flux: A-/A rating - strict TypeScript mode, max-lines: 700 enforced, 82% test code ratio, React 19 + Compiler ready
- Specific code problems identified:
  - NocoBase: `flowContext.ts` 4664 lines, `hooks/index.ts` 2259 lines with 50+ unrelated hooks, 278 useEffect calls many missing deps
  - nop-chaos-flux: Some high cognitive complexity in `dispatch` (~100 lines) and `node-renderer.tsx` (374 lines), but well-structured
- Research methodology: 8 parallel sub-agents investigating overall architecture, schema design, state management, plugin system, code quality, and architecture maturity for both projects
- Expanded `DataSourceState` and `statusPath` summary additively for finer-grained source loading semantics without deleting prior fields: added `hasData`, `hasError`, `isInitialLoading`, `isRefreshing`, and `inFlightCount` in `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/data-source-state.ts`, and `packages/flux-runtime/src/data-source-runtime.ts`.
- Added runtime coverage for the new additive source-state fields in `packages/flux-runtime/src/__tests__/runtime-sources.test.ts`, `runtime-sources-merge.test.ts`, and `runtime-sources-refresh.test.ts`.
- Aligned `packages/flux-runtime/src/source-registry.ts` debug snapshots with the new additive source-state fields so `getSourceDebugSnapshot()` now reports `hasData`, `hasError`, `isInitialLoading`, `isRefreshing`, and `inFlightCount` alongside the legacy debug fields.
- Added `useDataSourceStatus(path)` to `packages/flux-react/src/hooks.ts` and exported it from `packages/flux-react/src/index.tsx` so React renderers can read published `statusPath` summaries directly from scope without reaching into runtime registries.
- Added a focused React runtime test in `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx` covering loading and ready transitions through `useDataSourceStatus`.
- Updated `docs/architecture/api-data-source.md` to state the compatibility rule explicitly: source-state refinement should add new fields rather than redefining or removing legacy `status`, `fetchStatus`, `loading`, or `ready` signals.
- Verification: `pnpm --filter @nop-chaos/flux-runtime test -- runtime-sources.test.ts runtime-sources-merge.test.ts runtime-sources-refresh.test.ts request-runtime-polling.test.ts request-runtime.test.ts`, `pnpm --filter @nop-chaos/flux-runtime test -- runtime-sources-merge.test.ts runtime-sources.test.ts runtime-sources-refresh.test.ts`, `pnpm --filter @nop-chaos/flux-core build`, `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`, `pnpm --filter @nop-chaos/flux-runtime lint`.

### 2026-04-20 (session 3)

- Tightened API-backed data-source refresh behavior in `packages/flux-runtime/src/data-source-runtime.ts` so in-flight refreshes now respect `control.dedup = 'ignore-new'` instead of always forcing internal cancel-previous semantics.
- Extended the same data-source refresh path to honor `control.dedup = 'parallel'`: concurrent refreshes can now run side-by-side without aborting the first request, while `stop()` / `reset()` still abort all in-flight source requests.
- Added regression coverage in `packages/flux-runtime/src/__tests__/runtime-sources-refresh.test.ts` for data-source refresh dedup behavior while a request is already in flight.
- Updated `docs/architecture/api-data-source.md` to explicitly define source-level refresh dedup semantics for `cancel-previous`, `ignore-new`, and `parallel`, and to require `stop()` / `reset()` to abort all in-flight requests owned by a data-source controller.
- Updated comparison analysis in `docs/analysis/2026-04-20-nocobase-reference-analysis.md` to clarify Flux's current three-layer subscription model and to record the landed data-source control improvement.
- Key decision: prefer small behavior-aligning changes that honor existing `OperationControlConfig` contracts before introducing broader new source-control APIs.
- Verification: `pnpm --filter @nop-chaos/flux-runtime test -- runtime-sources-refresh.test.ts request-runtime-polling.test.ts request-runtime.test.ts`, `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`, `pnpm --filter @nop-chaos/flux-runtime lint`.

### 2026-04-20 (session 2)

- Deep audit cleanup batch: resolved 6 low-priority items + 1 medium item
- **NEW-2**: Removed dead `next-themes` dependency from `packages/ui/package.json` — zero imports remain after prior `sonner.tsx` cleanup
- **U18**: Deleted `writeMetadata` dead code (85 lines) from `packages/report-designer-core/src/runtime/metadata.ts` — superseded by immutable `updateMetadata`, zero callers
- **U15**: Un-exported report-designer toolbar helpers from barrel `packages/report-designer-renderers/src/index.ts` — 7 symbols (readState, evalBooleanExpr, evalTextTemplate, toCommand, mergeToolbarItems, DEFAULT_TOOLBAR_ITEMS, ToolbarItem) had zero external consumers
- **NEW-3**: Fixed AGENTS.md dependency flow — added missing `flux-i18n -> ui` edge
- **U13-partial**: Reduced `flux-runtime` barrel exports from 22 symbols to 12 — removed 10 internal-only helpers (`createScopeRef`, `createApiCacheStore`, `resolveCacheKey`, `createAbortScope`, `withRetry`, `withTimeout`, `createReadonlyScopeBinding`, `isOwnerCompatible`, `prepareApiData`, `buildUrlWithParams`)
- **U4/U5 partial**: ChartSchema `any` → `SchemaValue` (narrower than `any`, compatible with `BaseSchema` index signature). ConditionBuilder blocked: `ConditionField[]` not assignable to `SchemaValue` because `ConditionTextField` etc. lack `SchemaObject` index signatures — same `BaseSchema` constraint as U3
- Key learning: `BaseSchema`'s `[key: string]: SchemaValue` index signature prevents typed interfaces from being used directly in schema definitions — `any` is the practical workaround, replaced with `SchemaValue` where possible
- All packages pass typecheck, build, lint. Tests clean (2 pre-existing failures in `schema-renderer-imports-basic.test.tsx` unrelated to changes)
- **PF5 reclassified + docs corrected**: Rich UI widget renderers (condition-builder, tag-list, key-value, etc.) are complete styled controls, not styling violations. Updated `docs/architecture/styling-system.md` to formally distinguish layout renderers (marker-only) from widget renderers (self-styled). Updated `AGENTS.md` styling rules to match.

### 2026-04-20 (session 1)

- Fix spreadsheet selection dual-source-of-truth in `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-selection.ts`
  - `selectedCell` was maintained as local `useState`, duplicating the canonical selection in `SpreadsheetHostSnapshot.activeCell`
  - Changed: `selectedCell` now derived from `snapshot.activeCell` via `useMemo`; `setSelectedCell` only dispatches to core, no longer writes local state
  - Drag transient state (`setDragEnd`, `dragStateRef`) kept as local state/ref since it's pure UI acceleration, not semantic selection
  - Resolves the last remaining "dual source of truth" issue from `docs/analysis/2026-04-17-deep-audit/04-state-ownership.md`
  - External `spreadsheet:setSelection` commands will now correctly drive grid highlighting and toolbar state
- Key decision: `dragStateRef` remains a ref because it provides synchronous access within event handlers (snapshot update is deferred to next render via `useSyncExternalStore`); `getSelectedRange` checks `dragStateRef` first, so the transition is safe
- All spreadsheet-renderers (46 tests) and report-designer-renderers (123 tests) pass; typecheck, build, lint clean
