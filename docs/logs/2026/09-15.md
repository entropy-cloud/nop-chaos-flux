# Development Log - 2026-09-15

## plan 469 — Three.js I2 后继收口（three-canvas 演示场景 + 浏览器侧 fps e2e 基准）

分支 `feat-threejs-integration`。roadmap I0–I4 全部 done 后，兑现 plan 465 Deferred 的最后一条已裁定后继义务（「浏览器侧 fps e2e 基准」Successor Required: yes，路径「I2.2 关闭后评估：图元库就位时随演示场景一并挂 e2e」）。plan：`docs/plans/469-threejs-i2-successor-demo-fps-e2e-plan.md`（draft review：independent sub-agent，pass-with-minors，3 Minor 已落盘）。

### 交付

- **playground 演示页 `#/three-canvas-demo`**：`three-canvas-demo.tsx` + `three-canvas-demo-schema.ts`（页面内自建 registry；三 懒加载隔离——`WebGLRenderer` 仅出现在独立 chunk，主 bundle 无 three；4 点路由接线）。场景含 6 图元（box/sphere/cylinder/cone/torus/plane）、4 类灯光、绑定（tween/range/condition）、time 触发 loop 关键帧、`onObjectClick`，10Hz 模拟数据驱动绑定热路径。
- **`tests/e2e/three-canvas-perf.spec.ts`**：硬门禁（ready 埋点/canvas 尺寸/rAF 推进）+ WebGL readPixels 像素探测（40 帧窗口，非空场景全零判败，scada-canvas-assert T5 同法）+ fps 基准（2s 窗口 rAF 计数、3 采样取最大、下限 5 fps）。先红后绿双向复现：硬门禁红（双空模型 → error 态）、像素路径红（去背景 + mesh 移出视锥 → `fallback-all-zero` sampled:40/nonZero:0），还原后绿。
- **fps 实测（SwiftShader 软渲染口径）**：3 采样 88.4 / 85.7 / 90.1（首绿）、94.2 / 85.8 / 90.1（复跑），max 90.1 / 94.2，远超下限 5——简单图元场景 + 绑定热路径负载下渲染循环健康。

### 执行期发现并修复的两处 confirmed live defect（plan 469 Phase 2，Proof 先行）

1. **bindings 表达式被 props 编译层吞掉**：demo 页是首个 schema 级消费方（此前 177 单测全部 hook 级直调，未经 `createSchemaCompiler` 管线）。`compileValue` 对 prop 树内 `${...}` 求值 → `binding.source.expression` 到达渲染器时已是数字（`expression.trim is not a function`，error boundary 兜住）。修复：`renderer-definitions.ts` bindings propContract 声明 `schema-definition` + literal fieldRules（`source.expression`/`condition.expression`/`transform.convert`），编译器打 preserve-literal 信封；新增 `binding-literals.ts`（`normalizePreservedBinding(s)` 解包，非信封透传），`useBindingBridge` 入口统一解包。红线测试先红（propsProgram `kind=dynamic`）后绿。design-data-binding.md §3 已回写。
2. **纯图元场景 ready 闩缺失（mock/real 漂移）**：`loadModels` 对全图元场景在挂载 effect 内同步 `emitReady`，React 壳 onReady 订阅 effect 晚一个 render——ready 被确定性错过，状态永久 loading（场景实际在渲染）。`test-support/scene-manager-mock.ts` 早已建模闩语义，live `SceneManager` 缺失。修复：`emitReady` 置 `readyEmitted` 位、晚订阅立即重放；新增 `scene-manager.ready-latch.test.ts` 回归。design-renderer.md §7 已回写。

### 验证

- 3d 包：25 文件 / **181 测试全绿**（177 存量 + binding-literals 2 + ready-latch 2），覆盖率四维 ≥90 维持。
- playground：33 文件 / 347 测试全绿；typecheck/build 通过；dist 产物抽查主 bundle 无 three。
- e2e：`three-canvas-perf` 2/2 全绿（含 pixel-confirmed 正向证据）。
- e2e 全量（两轮，各约 24 分钟）：首轮 1465 passed / 5 failed（其中 inventory 缺口当轮修复）；次轮 1465 passed / 4 failed / 1 flaky，残留在案构成如下，均为与本计划无关的负载性 flake：
  - 首轮 5 failed = `playground-entry-pages` inventory（print-designer 既有清单缺口 + 本计划新条目，已修复转绿）+ c3-1 picker-host、dialog-real-schema、w4c picker ×2。
  - 次轮 4 failed = c3-1 picker-host、dialog-real-schema、w4c picker ×2（同首轮）+ linear-replica L1（首轮通过、次轮失败——轮盘证据）；inventory 已绿，flux-basic smoke flaky 一次复跑过。
  - c3-1/dialog-real-schema/w4c 三 spec 孤立复跑 11/11 全绿；跨轮次失败集合不重合；与 three-canvas 改动无代码路径交集。建议 owner 择期按 watch-only 流程核认该 component-lab 交互 spec 簇（本计划不代为注册）。
  - 旧 watch-only（gantt-perf ×2 + kanban-perf ×1）两轮均通过。
- roadmap「Current Baseline → 主要缺口」过期描述修正（原「无任何实现代码」→ 缺口闭合实况）；Phase Status 未动（全 done），I4.1 行「Gemini API 集成」人工评审标记维持（Rule 5，待人工裁定）。

---

以下为 `feat-echarts-integration` 分支同期日志（ECharts roadmap 终态收口核查，随分支合并带入）。

### 2026-09-15

- **ECharts roadmap 终态收口核查**（执行 session）：确认 `docs/backlog/echarts-integration-roadmap.md` 全部 work item（E0.1–E5.2）`done`，6 个 execution plan（E1.1–E5.2）均 `completed` 且 closure audit 为独立 fresh-session `approved`。本 session 零代码行为变更，仅验证 + 补登记。
- **发现并修复一处 check 红漏登记**：`pnpm check` 链在既有 i18n 4 键红处短路，导致 i18n 之后的步骤在 E1.1–E5.2 各 session 均未被跑到；本次逐步骤补扫发现 `check:audit-ui-consistency-gaps` 有 **6 个新增未登记命中**——`packages/flux-renderers-data/src/echarts-theme.ts:3-7,24` 的 `FALLBACK_*` 静态回退调色板与 `hsl()` 包装函数（E2.1 引入）。此前 log（09-13）"仅存既有 i18n 4 键红（零新增）"的表述系未扫到该步骤所致，特此更正口径。
- **处置**：按既有数据可视化域豁免惯例（`chart-renderer.tsx` 默认调色板同类）在 `scripts/audit/find-ui-consistency-gaps.mjs` EXEMPTIONS 登记 1 条（`hardcoded-literal-color` @ `echarts-theme.ts`，source 指向本日 log）。复跑 `pnpm check:audit-ui-consistency-gaps` → exit 0，零未登记命中。
- **终态验证记录（执行 session）**：`pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37（turbo 缓存命中）；`turbo run lint`（eslint）✅ 37/37；`pnpm test` ✅ 68/68 tasks（缓存命中，对应上次实跑 11,554 tests / 0 failed）；echarts e2e spec ✅ 2/2（真实浏览器 canvas + dataset 切换）。`pnpm check` 全链 15 步逐一步骤核查：唯余既有 i18n 4 键红（master 级，`docs/logs/2026/09-13.md` 登记，零新增），ui-consistency 6 命中已登记转绿。
- **口径说明**：全量 e2e 套件未跑（沿用本分支 E1–E5 各 session 惯例，roadmap Rule 6 不要求），故不作 full-green 声明。
- Next step：roadmap 已全部收口，无 plan-owned 剩余工作；i18n 4 键与 e2e 已知 flake 属 master 级独立事项。

---

### 2026-09-15（续）— feat-echarts-integration 合入 master + master e2e 基线红修复

- **合并**：`feat-echarts-integration` → `master`（merge commit `d84cf7e88`，`--no-ff` 对齐仓库惯例）。冲突 3 处均为 daily log add/add（09-06/09-13/09-15 双分支各自建文件），按保留双方内容解决；`docs/index.md`、`flux-bundle package.json`、`pnpm-lock.yaml`、`find-ui-consistency-gaps.mjs` 自动合并并抽查两侧条目均在。
- **合并后全量验证**：`pnpm typecheck` 40/40、`pnpm build` 40/40、`pnpm lint` 全链过（master 已补齐 4 个 i18n 键定义，check-i18n-keys 恢复绿，check 链不再短路）、`pnpm test` 74/74 tasks 0 failed、`pnpm check` **全链绿**（含 audit-ui-consistency-gaps，echarts/three 豁免共存）。
- **全量 e2e 首轮**：1468 passed / 4 failed / 43 skipped——4 个失败为 master 既有基线红（自 09-07 起的 c3-1 picker-host、dialog-real-schema、w4c picker ×2）。
- **根因（本轮修复的核心）**：2026-09-02 picker v3 重构（plan-2026-09-02-2028）将 `pickerDialog` → `pickerPopup`、`valueKey/labelKey` → `valueField/labelField`、弹层内容收敛进 `pickerSchema` region 并移除默认内容构建（G1 裁决），但 **playground 消费侧 schema 从未迁移**：picker trigger 的 `openDialog` 守卫（无 popup 无 content）静默 no-op（`configMissing` 警告无 Toaster 不可见）→ 对话框永远打不开；dialog-real-schema 场景中 verbatim nop-entropy schema 的 pickerSchema 缺 `type` 字段，region 编译 `isSchemaInput` 硬抛（"Region ... must contain schema input."）→ openDialog 的 surface-validation 编译失败被静默吞掉（`env.notify` 不可见）。诊断方法：浏览器侧带 MutationObserver/事件时间线探针 + schema 二分 + flux-compiler scratch 复现拿到确切报错。
- **修复**：① 全部消费 schema 迁移 v3（picker-lab-page 三场景、w4c demo、form-wizard.json、standard-crud.json：`pickerPopup`/`valueField/labelField`/补 `pickerSchema.type:'crud'`/删死 `options`）；② picker lab CRUD 场景按 v3 单测裁决形态补 `pickerSchema` crud 内容（bug-73 逐行隔离 e2e 恢复）；③ w4c e2e 改钉 G1 popup-only 契约（空弹层、Confirm disabled、空值时 clear disabled）；④ `docs/components/picker/design.md` §1/§4/§5/§6 同步 v3 契约（清除 pickerDialog/valueKey 漂移）；⑤ 吸收在途的 dialog-real-schema 20s 可见性容忍。
- **修复后全量验证（full-green）**：`pnpm typecheck` 40/40、`pnpm build` 40/40、`pnpm lint` 全链过、`pnpm test` 74/74 tasks 0 failed、`pnpm check` 全链绿；全量 e2e **1472 passed / 0 failed / 43 skipped（22.5 分钟）**——master 自 09-07 以来的 4 个基线红清零，首次达成单测 + e2e 双全绿基线（43 skipped 为既有 describe.skip）。
- **教训**：破坏性 schema 重命名必须同 PR 迁移仓内全部消费 schema（含 JSON demo 页）；region 编译对非法 schema input 硬抛 + `env.notify` 在无 Toaster 宿主不可见 = 静默失效，"基线红"若长期存在应做根因归档而非连续 watch-only。
- 分工说明：工作区中 `docs/analysis/2026-09-12-*`、`docs/logs/2026/09-12.md` 等未提交改动属另一 session（ma43/hucre 分析），不属本链路，保持未提交。
