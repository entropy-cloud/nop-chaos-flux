# Development Log - 2026-05-25

### 2026-05-25

- Closed the remaining `维度03-01` API-surface drift in Workstream 1 by stabilizing the production renderer-facing subset that had already become a live contract. `packages/flux-react/src/index.tsx` now exports `RenderNodes`, `FormContext`, `ScopeContext`, `ValidationContext`, `FormLayoutContext`, `createFormComponentHandle`, and `createReadonlyScopeBinding` from the root entry, and the production renderer paths in `packages/flux-renderers-form`, `packages/flux-renderers-form-advanced`, `packages/flux-renderers-data`, and `packages/flow-designer-renderers` now import those symbols from `@nop-chaos/flux-react` instead of `@nop-chaos/flux-react/unstable`.
- Focused proof for that stabilization slice is green for `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/public-surface.test.ts`, `pnpm --filter @nop-chaos/flux-react typecheck`, `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, plus focused renderer package tests in `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/edge-label-expression.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/form-renderer-lifecycle.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/variant-field/variant-field-owner-contract.test.tsx`, and `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-package-units.test.tsx`.

- Recovered the remaining workspace verification fallout after the earlier deep-audit remediation slices: `packages/flux-renderers-basic/src/__tests__/basic-page-layout-surfaces.test.tsx` now uses stable dialog status/close queries, `packages/spreadsheet-renderers/src/canvas-styles.css` no longer mixes package-local `--nop-*` fallback token declarations into the shared token contract, `packages/flux-renderers-data/src/table-renderer/capability-action-context.ts` now tolerates direct capability invokes without a passed context, `packages/nop-debugger/src/adapters.test.ts` now matches the current exported error-event contract, `apps/playground/src/pages/report-designer-demo.tsx` now writes spreadsheet cell binding metadata through `field`, and `packages/flux-action-core/src/__tests__/action-dispatcher-error-guard.test.ts` now narrows its schema test casts so workspace typecheck stays honest.
- Focused recovery proof is green for `pnpm --filter @nop-chaos/flux-renderers-basic test`, `pnpm --filter @nop-chaos/spreadsheet-renderers test`, `pnpm --filter @nop-chaos/flux-renderers-data test`, `pnpm --filter @nop-chaos/nop-debugger test`, and `pnpm --filter @nop-chaos/flux-playground test -- src/pages/report-designer-demo.test.tsx`.
- Workspace full-green verification is restored on the live repo for `pnpm test`, `pnpm build`, `pnpm lint`, and `pnpm typecheck`. This records the current known-good workspace baseline for the 436 remediation plan, but the parent plan remains only partially completed because Workstreams 1, 3, and 4 still have open retained findings and Workstream 10 closure audit is not yet complete.
- Re-audited the completed-or-near-complete documentation workstreams against the live docs tree while updating the parent plan: Workstream 6 owner docs already describe the landed accessibility contracts in `docs/architecture/flow-designer/design.md`, `docs/components/code-editor/design.md`, `docs/architecture/word-editor/design.md`, and related docs; Workstream 7 owner docs already describe supported-vs-diagnostic E2E boundaries in `docs/testing/e2e-standards.md` and `docs/references/e2e-test-diagnostic-guide.md`; Workstream 8 remains closed at the docs/content layer and is now also reflected in this date log so the parent plan's log criterion stays text-consistent.

- Finalized the remaining Workstream 1 dependency/cycle fallout after the `flux-core` seam extraction: `packages/flux-renderers-data/src/{crud-renderer-state.ts,table-renderer/use-table-handle.ts,table-renderer/capability-action-context.ts}` now explicitly bridge component capability context into the renderer-event `Partial<ActionContext>` contract instead of depending on widened core types, and `packages/flux-renderers-basic/src/use-surface-renderer.ts` dropped the stale callback dependency that the hook lint gate still rejected.
- Closed the follow-on workspace verification drift caused by the new boundary seams: `packages/flux-code-editor/src/index.ts`, `packages/flux-renderers-form-advanced/src/index.tsx`, `packages/flux-react/src/{hooks.ts,node-renderer-resolved.tsx}`, `packages/nop-debugger/src/{adapters.ts,controller-component-inspector.ts,explanations.ts}`, `packages/flux-formula/src/evaluate.ts`, and `packages/report-designer-renderers/src/page-renderer.tsx` were tightened so exported renderer-definition arrays, debugger diagnostics adapters, expression env usage, and React Compiler memoization all match the current live contracts.
- Owner-doc sync for that boundary slice: updated `docs/architecture/{flux-runtime-module-boundaries.md,frontend-baseline.md,renderer-runtime.md}` so the docs now describe the injected `actionProgramCompiler` / `expressionCompiler` boundary, the `@nop-chaos/flux` facade verification expectations, and the explicit bridge between component capability context and renderer event action context.
- Workspace verification is green for `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm audit:deps`, `pnpm check:workspace-manifest-deps`, and `pnpm check:flux-bundle-pack` after the above boundary fixes. `pnpm audit:deps` now reports `✔ no dependency violations found (1501 modules, 3055 dependencies cruised)`.

- Closed the remaining Flow keyboard port-connect accessibility slice in Workstream 6: `packages/flow-designer-renderers/src/designer-xyflow-canvas/{render-ports.tsx,designer-xyflow-canvas.tsx,designer-xyflow-node.tsx,port-connection-a11y-context.tsx}` together with `packages/flow-designer-renderers/src/designer-canvas.tsx` and `designer-page-body.tsx` now expose real focusable graph-mode port controls for connect/reconnect, keep port identity in pending intent state via `pendingConnectionSourcePortId`, and let `Enter` / `Space` / `Escape` drive start, complete, and cancel flows through the same host command boundary as pointer interactions (`维度20-13`).
- Focused proof for that slice is green in `packages/flow-designer-renderers/src/{canvas-bridge.test.tsx,index.xyflow.test.tsx,designer-controls.test.tsx}`; package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers lint`, `typecheck`, and `build`, and `pnpm --filter @nop-chaos/flux-i18n typecheck` also passed for the new localized port labels.
- Closed the retained Flow edge-creation proof gap in Workstream 7: `tests/e2e/flow-designer-edge-creation.spec.ts` now treats the synthetic `nop-designer:test-connect` path as diagnostic-only and proves supported edge creation through a real keyboard handle-to-handle interaction on the live Flow Designer page (`维度14-01`).
- Focused E2E proof is green for `pnpm exec playwright test "tests/e2e/flow-designer-edge-creation.spec.ts" --reporter=list` with the supported keyboard connection path passing.
- Owner doc sync: `docs/architecture/flow-designer/{design.md,runtime-snapshot.md}` now record the live graph-mode keyboard port contract and the actual top-level designer host-scope summary fields instead of the removed `$designer` wrapper.
- Closed the remaining docs-gate proof gap in Workstream 0: `scripts/check-active-doc-code-anchors.mjs` now supports an explicit `NOP_ACTIVE_DOC_ROOT` override for fixture-backed regression coverage, `scripts/__tests__/check-active-doc-code-anchors.test.ts` plus `scripts/__tests__/fixtures/active-doc-anchor-check/` prove that missing anchors still fail inside active docs while `docs/analysis/` and `docs/logs/` remain intentionally excluded, and `vitest.scripts.config.ts` / `pnpm test:scripts` provide the focused verification entry point for this hard gate.
- Focused docs-gate proof is green for `pnpm test:scripts` and `pnpm check:active-doc-code-anchors`.
- Closed the next test-hygiene slice in Workstream 7: `packages/flux-code-editor/src/code-editor.integration.test.tsx`, `packages/flux-renderers-basic/src/__tests__/basic-structural.test.tsx`, and `packages/flux-react/src/__tests__/schema-renderer.test.tsx` now use file-level `afterEach` cleanup with `vi.restoreAllMocks()` so `console.error` / `console.warn` / runtime-factory spies and DOM state no longer depend on success-path restores (`维度14-07`, `维度14-09`, residual `维度14-10`).
- Focused proof for that slice is green in `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor.integration.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-basic exec vitest run src/__tests__/basic-structural.test.tsx`, and `pnpm --filter @nop-chaos/flux-react exec vitest run src/__tests__/schema-renderer.test.tsx`.
- Closed the retained default-suite diagnostic isolation gap in Workstream 7: `tests/e2e/flow-designer-ui.spec.ts`, `tests/e2e/component-lab/crud-table-body-diag.spec.ts`, and `tests/e2e/debugger-meta-diagnostic.spec.ts` are now explicitly skipped so screenshot capture, HTML/console dump collection, and `__NOP_DEBUGGER_API__` probing no longer masquerade as supported E2E coverage (`维度14-08`).
- Owner-doc sync for that slice: `docs/testing/e2e-standards.md` and `docs/references/e2e-test-diagnostic-guide.md` now define the default Playwright run as the supported user-path suite and require capture/probe-only specs to stay isolated until a separate diagnostic project exists.
- Closed the retained Word Editor and CRUD proof-fidelity gaps in Workstream 7: `packages/word-editor-renderers/src/word-editor-page.tsx` now exposes a visible saved-preview/save-status strip for the persisted document baseline, `tests/e2e/{word-editor.spec.ts,word-editor-persistence.spec.ts}` assert typed markers and save/reload persistence through that user-visible surface, and `tests/e2e/component-lab/crud-query-and-ownership.spec.ts` now proves CRUD query/filter/refresh/client-mode behavior through visible rows/footer state only, with `tests/e2e/component-lab/crud-test-utils.ts` dropping the supported `scope-debug-json` helper (`维度14-04`, `维度14-05`).
- Focused proof for that slice is green for `PLAYWRIGHT_PORT=4190 pnpm exec playwright test "tests/e2e/word-editor.spec.ts" --workers=1 --reporter=list`, `PLAYWRIGHT_PORT=4191 pnpm exec playwright test "tests/e2e/word-editor-persistence.spec.ts" --workers=1 --reporter=list`, `PLAYWRIGHT_PORT=4192 pnpm exec playwright test "tests/e2e/component-lab/crud-query-and-ownership.spec.ts" --workers=1 --reporter=list`, and package verification passed for `pnpm --filter @nop-chaos/word-editor-renderers typecheck` plus `build`.
- Closed the retained mixed-test topology gaps in Workstream 7 by replacing `packages/flux-renderers-basic/src/__tests__/basic-page-layout-structure.test.tsx` with focused `basic-page-and-layout-structure.test.tsx`, `basic-page-and-tabs-status.test.tsx`, `basic-tabs-behavior.test.tsx`, and `basic-class-alias-and-icon-markers.test.tsx`, and by replacing `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx` with focused tree/chart/instance-path/interaction/large-render suites while updating `packages/flux-renderers-data/src/index.test.tsx` to import the new split files (`维度14-02`, `维度14-06`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/flux-renderers-basic exec vitest run src/__tests__/basic-page-and-layout-structure.test.tsx src/__tests__/basic-page-and-tabs-status.test.tsx src/__tests__/basic-tabs-behavior.test.tsx src/__tests__/basic-class-alias-and-icon-markers.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-tree-rendering-and-status.test.tsx src/__tests__/data-chart-handles.test.tsx src/__tests__/data-repeated-instance-path.test.tsx src/__tests__/data-tree-interaction.test.tsx src/__tests__/data-tree-large-render.test.tsx`, and package `typecheck` passed for both `@nop-chaos/flux-renderers-basic` and `@nop-chaos/flux-renderers-data`.
- Closed the retained shared-primitive coverage gap in Workstream 7: `packages/ui/src/components/ui/{accordion.test.tsx,dropdown-menu.test.tsx,table.test.tsx,radio-group.test.tsx,textarea.test.tsx,sheet.test.tsx}` now provide direct minimal behavior/ARIA/data-slot proof for the previously uncovered high-use primitives (`维度14-03`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/ui exec vitest run src/components/ui/accordion.test.tsx src/components/ui/dropdown-menu.test.tsx src/components/ui/table.test.tsx src/components/ui/radio-group.test.tsx src/components/ui/textarea.test.tsx src/components/ui/sheet.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/ui typecheck` plus `build`.
- Closed the retained error-fidelity slice in Workstream 9: `packages/flux-runtime/src/async-data/formula-data-source-controller.ts`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flow-designer-renderers/src/{use-designer-auto-layout.ts,designer-page-body.tsx}`, `packages/word-editor-renderers/src/word-editor-action-provider.ts`, and `packages/nop-debugger/src/{controller-helpers.ts,adapters.ts}` now preserve non-`Error` failures through `Error.cause`, keep auto-layout host reports on the normalized failure object instead of a fresh message-only error, and export redacted structured debugger error payloads including nested cause and monitor details (`维度19-01` through `维度19-05`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/formula-data-source-recovery.test.ts src/__tests__/runtime-factory-utils.test.ts`, `pnpm --filter @nop-chaos/flux-react exec vitest run src/schema-renderer-imports-errors.test.tsx`, `pnpm --filter @nop-chaos/nop-debugger exec vitest run src/controller-helpers.test.ts src/adapters.test.ts`, `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-action-provider.test.ts`, and `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/auto-layout-guards.test.tsx`.
- Workspace verification after the Workstream 9 slice is green for `pnpm typecheck` and `pnpm build`; `pnpm lint` also passes after fixing the pre-existing `flux.common.unsaved` locale drift in `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}` and removing an unrelated unused test import in `packages/flux-renderers-data/src/__tests__/data-chart-handles.test.tsx` so the workspace can satisfy the required gate again.
- Closed the retained Word host provider normalization gap in Workstream 2: `packages/word-editor-renderers/src/word-editor-action-provider.ts` now applies one provider-boundary `try/catch` to all published `word-editor:*` methods, so bridge/store throws from `insertField`, `insertChart`, `insertCode`, `undo`, and `redo` return stable `ActionResult` failures instead of rejecting past the namespace boundary (`维度18-02`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-action-provider.test.ts`, with package `typecheck` and `build` also green for `@nop-chaos/word-editor-renderers`.
- Closed the remaining Flow `moveNodes.deltas` host-contract gap in Workstream 2: `packages/flux-core/src/schema-diagnostics/{manifest.ts,value-shape-runtime.ts}` and `packages/flux-compiler/src/schema-compiler/flux-value-shape-validation.ts` now support a shared `FluxValueShape` `record` dictionary shape, and `packages/flow-designer-renderers/src/designer-manifest.ts` now publishes `designer:moveNodes` as a real `record<{ dx: number; dy: number }>` contract instead of an untyped empty object bag (`维度03-02`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/flux-core exec vitest run src/schema-diagnostics/manifest.test.ts`, `pnpm --filter @nop-chaos/flux-compiler exec vitest run src/schema-compiler-shape-validation-value-shape.test.ts src/schema-compiler-host-action-validation.test.ts`, and `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-provider-and-manifest.test.tsx`; package verification also passed for `pnpm --filter @nop-chaos/flux-core typecheck`, `pnpm --filter @nop-chaos/flux-core build`, `pnpm --filter @nop-chaos/flux-compiler typecheck`, `pnpm --filter @nop-chaos/flux-compiler build`, `pnpm --filter @nop-chaos/flow-designer-core typecheck`, `pnpm --filter @nop-chaos/flow-designer-core build`, `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`, and `pnpm --filter @nop-chaos/flow-designer-renderers build`.
- Re-audited the retained Report host-contract findings and confirmed `preview.mode` plus no-args rejection were already fixed live; the remaining truthfulness gap was nested projection normalization. `packages/report-designer-renderers/src/host-data.ts` now normalizes `designer.selectionKind` and `designer.inspectorPanels` to `null` so the live scope matches the manifest's published `null` unions (`维度03-14`).
- Focused proof for that Report slice is green for `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/host-data.test.ts src/host-action-provider.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/report-designer-renderers typecheck` plus `build`.
- Closed the remaining Flow provider-tail contract drift in Workstream 2: `packages/flow-designer-renderers/src/designer-action-provider.ts` now maps direct-core failures from `commitTransaction`, `rollbackTransaction`, `toggleNodeSelection`, `toggleEdgeSelection`, `setSelection`, `moveNodes`, and `updateMultipleNodes` onto the same ActionResult-shaped `{ ok: false, error, cause }` boundary used by adapter-backed commands, instead of returning ad hoc bare `{ ok: false, reason }` objects (`维度18-04`).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-provider-and-manifest.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` plus `build`.
- Closed the spreadsheet-local CSS scope/token slice in Workstream 5 by moving the sheet-tab-bar chrome onto explicit `data-slot="spreadsheet-sheet-*"` markers in `packages/spreadsheet-renderers/src/sheet-tab-bar.tsx` and scoping the corresponding styles through those markers in `packages/spreadsheet-renderers/src/canvas-styles.css`. The sheet-tab bar no longer relies on package-global bare class selectors for its public chrome, and its rename/close/add controls now use the same marker-based contract as the rest of the spreadsheet canvas (`维度10-01` through `维度10-04`).
- Focused proof for that spreadsheet CSS slice is green for `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/sheet-tab-bar.test.tsx src/bridge.test.ts src/__tests__/schema-integration.test.tsx`, package verification passed for `pnpm --filter @nop-chaos/spreadsheet-renderers typecheck` plus `build`, and `pnpm check:package-css-exports` is green.
- Re-ran `pnpm check:active-doc-code-anchors` after the current plan/doc updates and it remains green, confirming the active-doc gate still matches the live owner-doc baseline.
- Closed the runtime half of the retained `componentCapabilityContracts` enforcement gap in Workstream 2: `packages/flux-runtime/src/action-adapter.ts` now resolves the target handle's renderer definition through `runtime.registry.get(handle.type)` and validates published `componentCapabilityContracts` args/result shapes before and after `component:<method>` invocation, so payload/result drift now fails at the runtime adapter boundary instead of silently passing through (`维度03-12` runtime slice).
- Focused proof for that slice is green for `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/action-adapter.unit.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/flux-runtime typecheck` plus `build`.
- Closed the compiler half of `维度03-12`: `packages/flux-compiler/src/schema-compiler/{shape-validation-traversal.ts,shape-validation-analyze.ts,shape-validation-rules.ts,action-selector-validation.ts}` now build a validation-only index of unique schema `componentId -> renderer definition` bindings and consume published `componentCapabilityContracts` for `component:<method>` method/args validation when the target id is statically unique. Duplicate ids and `componentName` targets remain intentionally warning-only via `unvalidated-component-target`, preserving honest uncertainty instead of pretending compile-time certainty.
- Focused proof for that compiler slice is green for `pnpm --filter @nop-chaos/flux-compiler exec vitest run src/schema-compiler-host-action-validation.test.ts src/schema-compiler-registry-compilation.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/flux-compiler typecheck` plus `build`.
- Closed the next Workstream 5 theme-compatibility slice from `维度10-09`: `packages/word-editor-renderers/src/styles.css` now scopes Word Editor package token fallbacks to `.nop-word-editor-page` instead of re-declaring shared `--nop-*` tokens on `.nop-theme-root`, and all fallback reads now wrap shared HSL fragment tokens with valid `hsl(var(...))` colors before feeding page chrome/background derivations. This keeps host/root theme tokens authoritative while preserving standalone defaults for the Word Editor surface itself.
- Focused proof for that Word Editor styling slice is green for `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/styles.test.ts src/__tests__/doc-preview-page.test.tsx src/__tests__/word-editor-page-actions.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/word-editor-renderers typecheck` plus `build`.
- Closed the next public-token slice from `维度10-15`: `packages/ui/src/components/ui/sonner.tsx` now maps Sonner `--normal-*` variables through valid theme color functions (`hsl(var(--popover...))`, `hsl(var(--border))`) instead of handing Sonner bare HSL fragment tokens like `var(--popover)` / `var(--border)`. This keeps toast chrome aligned with the public popover/border token contract even when tokens are published as HSL components.
- Focused proof for that UI slice is green for `pnpm --filter @nop-chaos/ui exec vitest run src/components/ui/sonner.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/ui typecheck` plus `build`.
- Closed the next public-token slice from `维度10-17`: `packages/tailwind-preset/src/index.ts` now maps Tailwind `popover` utilities to public `--popover*` tokens with card-based fallback (`hsl(var(--popover, var(--card)))`, `hsl(var(--popover-foreground, var(--card-foreground)))`) instead of hard-binding them to `--card*`. This brings `bg-popover` / `text-popover-foreground` back in line with the public floating-surface token contract used by Popover consumers.
- Focused proof for that preset slice is green for `pnpm --filter @nop-chaos/tailwind-preset exec vitest run src/index.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/tailwind-preset typecheck` plus `build`.
- Closed the next public-token slice from `维度10-13`: `packages/tailwind-preset/src/index.ts` now maps `destructive` utilities to public `--destructive*` tokens with backward-compatible `--danger` / `--primary-foreground` fallback instead of bypassing the public destructive token pair. This restores the documented public token surface while keeping older theme definitions from breaking immediately.
- Focused proof for that preset slice is green for `pnpm --filter @nop-chaos/tailwind-preset exec vitest run src/index.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/tailwind-preset typecheck` plus `build`.
- Closed the next shared-overlay slice from `维度10-11`: `packages/ui/src/components/ui/{dialog.tsx,alert-dialog.tsx,drawer.tsx}` now expose token-backed overlay chrome (`--nop-dialog-backdrop`, `--nop-drawer-backdrop`) instead of hardcoded `bg-black/10`, so public UI consumers no longer depend on playground-only overlay overrides to theme dialogs and drawers. Focused proof is green in `packages/ui/src/components/ui/{dialog.test.tsx,drawer.test.tsx,alert-dialog.test.ts,sonner.test.ts}`, and package verification passed for `pnpm --filter @nop-chaos/ui typecheck` plus `build`.
- Closed the next public-token root-default slice from `维度10-16`: `packages/theme-tokens/src/styles.css` now publishes baseline root HSL tokens (`--background`, `--foreground`, `--card*`, `--popover*`, `--border`, `--input`, `--ring`, `--primary*`, `--muted*`, `--accent*`) directly in `:root`, so `@nop-chaos/ui/base.css` and other public consumers no longer depend on theme-specific `data-theme/data-mode` attributes just to get valid base colors. Focused proof is green in `packages/theme-tokens/src/styles.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/theme-tokens typecheck` plus `build`.
- Closed the retained Flow branch-focus token drift in Workstream 5: `packages/flow-designer-renderers/src/{designer-xyflow-canvas/designer-xyflow-edge.tsx,dingflow/ding-flow-edge.tsx}` now use `hsl(var(--primary))` for branch-focused edge stroke chrome instead of consuming the bare `var(--primary)` HSL fragment token directly, keeping focused Flow edges on the public primary color contract (`维度10-14`).
- Focused proof for that Flow styling slice is green for `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/edge-label-xyflow.test.tsx src/dingflow/ding-flow-edge.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` plus `build`.
- Closed the retained Flow Designer theme-root and palette drift in Workstream 5: `packages/flow-designer-renderers/src/designer-theme.css` no longer publishes default `--fd-*` values on `.fd-theme-root` / `.nop-designer`, and `packages/flow-designer-renderers/src/designer-palette.tsx` now derives palette chrome from `resolveNodeTypeAccent()` through `--fd-palette-accent` instead of hardcoded `nodeType.id -> fd-palette-appearance-*` classes (`维度10-05`, `维度10-06`).
- Focused proof for that Flow theme slice is green for `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-controls.test.tsx src/designer-theme.test.ts src/edge-label-xyflow.test.tsx src/dingflow/ding-flow-edge.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` plus `build`.
- Closed the retained Code Editor theme-inheritance gap in Workstream 5: `packages/flux-code-editor/src/code-editor-styles.css` now derives default toolbar/header/variable-panel/result chrome from shared semantic theme tokens (`--background`, `--foreground`, `--muted*`, `--accent`, `--border`, `--ring`) instead of fixed light-only rgba/hex defaults on `.nop-code-editor` (`维度10-07`).
- Focused proof for that Code Editor styling slice is green for `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor-styles.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/flux-code-editor typecheck` plus `build`.
- Closed the retained debugger theme-root and selector-scope drift in Workstream 5: `packages/nop-debugger/src/panel/styles-css.ts` no longer writes default debugger tokens onto `.nop-theme-root`, and its injected `.ndbg-*` selectors are now anchored under `.nop-debugger` / `.nop-debugger-launcher` while reading `var(--nop-debugger-*, fallback)` directly at debugger-owned surfaces (`维度10-08`, `维度10-18`).
- Focused proof for that debugger styling slice is green for `pnpm --filter @nop-chaos/nop-debugger exec vitest run src/panel/styles.test.ts src/panel.test.tsx src/panel-minimized.test.tsx src/panel-timeline.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/nop-debugger typecheck` plus `build`.
- Closed the retained public sidebar/facade styling drift in Workstream 5: `packages/theme-tokens/src/styles.css` now publishes `--sidebar*` defaults as raw HSL fragments, `packages/tailwind-preset/src/index.ts` maps sidebar utilities through valid `hsl(var(--sidebar...))` color functions with public token fallback, and `packages/flux-bundle/src/index.test.tsx` now treats the facade stylesheet as a composition layer instead of pretending `.nop-flux-root` rewrites imported canonical CSS (`维度10-10`, `维度10-12`).
- Focused proof for that public styling slice is green for `pnpm --filter @nop-chaos/theme-tokens exec vitest run src/styles.test.ts`, `pnpm --filter @nop-chaos/tailwind-preset exec vitest run src/index.test.ts`, `pnpm --filter @nop-chaos/flux exec vitest run src/index.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/theme-tokens typecheck`, `pnpm --filter @nop-chaos/theme-tokens build`, `pnpm --filter @nop-chaos/tailwind-preset typecheck`, `pnpm --filter @nop-chaos/tailwind-preset build`, `pnpm --filter @nop-chaos/flux typecheck`, and `pnpm --filter @nop-chaos/flux build`.
- Owner-doc sync for the completed Workstream 5 styling slice: updated `docs/architecture/{theme-compatibility.md,styling-system.md,renderer-markers-and-selectors.md}`, `docs/components/designer-page/design.md`, `docs/components/code-editor/design.md`, and `docs/architecture/debugger-runtime.md` so the docs now describe fallback-read Flow tokens, config-driven palette accents, semantic-token code-editor chrome, debugger-root-scoped injected CSS, and the raw-HSL sidebar-token/public-utility contract.
- Re-ran `pnpm check:package-css-exports` and `pnpm check:active-doc-code-anchors` after the Workstream 5 doc/code changes; both remain green.
- Closed the retained local table visible-columns owner-drift slice in Workstream 4: `packages/flux-renderers-data/src/table-renderer/use-table-visible-columns.ts` now keeps local visibility state as an explicit `columnKey -> visible` override map instead of an undirected delta array, so a user-hidden default-visible column is not accidentally reinterpreted as an explicit show override after schema `columns` / `hidden` defaults change (`维度04-02`).
- Focused proof for that table state slice is green for `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-table-columns.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flux-renderers-data typecheck` plus `build`.
- Closed the retained report/spreadsheet dirty-owner drift in Workstream 4 by adding a shared save acknowledgement boundary: `packages/spreadsheet-core/src/core.ts` now exposes `acceptCurrentDocumentAsSaved()` so the spreadsheet owner can accept the current workbook as saved without resetting selection/history/view state, and `packages/report-designer-renderers/src/page-renderer.tsx` now calls that acknowledgement after successful `report-designer:save` so the documented aggregated `runtime.dirty = designer.dirty || spreadsheet.runtime.dirty` contract stays true after save (`维度04-03`).
- Focused proof for that dirty-owner slice is green for `pnpm --filter @nop-chaos/spreadsheet-core exec vitest run src/__tests__/core-advanced.test.ts`, `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/page-renderer-host-projection.test.tsx src/host-data.test.ts src/bridge.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/spreadsheet-core typecheck`, `pnpm --filter @nop-chaos/spreadsheet-core build`, `pnpm --filter @nop-chaos/report-designer-renderers typecheck`, and `pnpm --filter @nop-chaos/report-designer-renderers build`.
- Closed the retained Report Designer wide spreadsheet-subscription slice in Workstream 4: `packages/report-designer-renderers/src/page-renderer.tsx` now keeps the page shell on a narrower spreadsheet runtime selector (`document`, `history`, `dirty`) for sync/status work, while a lightweight host-scope sync child owns the high-frequency spreadsheet projection updates (`selection`, `viewport`, `layout`, `readonly`) through `scope.replace()` instead of waking the whole shell. `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx` now reads the bridge host snapshot directly rather than receiving a parent-prop runtime snapshot (`维度15-04`).
- Focused proof for that subscription slice is green for `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/page-renderer-selector.test.tsx src/report-spreadsheet-canvas.test.tsx src/page-renderer-host-projection.test.tsx src/host-data.test.ts src/bridge.test.ts`, and package verification passed for `pnpm --filter @nop-chaos/report-designer-renderers typecheck` plus `build`.
- Closed the retained Flow tree command clone hot path in Workstream 4: `packages/flow-designer-renderers/src/tree-commands.ts` now uses an explicit structural clone for `TreeDocument` / `TreeNode` / `TreeNodeBranch` instead of `JSON.parse(JSON.stringify(tree))`, so insert/update/delete/move branch commands no longer pay a JSON round-trip on every tree edit (`维度15-02`).
- Focused proof for that tree command slice is green for `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-page.tree-history.test.tsx src/designer-page.tree.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` plus `build`.
- Closed the retained Flow tree-mode stringify hot path in Workstream 4: `packages/flow-designer-renderers/src/designer-tree-mode.tsx` now uses an explicit structural graph compare for the accepted-host vs projected/local graph check instead of `JSON.stringify(toComparableTreeGraphDocument(...))`, preserving the existing host-replacement semantics proved by the tree-history suite while removing the audited full-graph stringify equality path (`维度15-01`).
- Focused proof for that tree host-sync slice remains green for `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-page.tree-history.test.tsx src/designer-page.tree.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` plus `build`.
- Closed the retained `TreeRenderer` O(n^2) id-collection path in Workstream 4: `packages/flux-renderers-data/src/tree-renderer.tsx` now uses a single shared-Set DFS helper (`collectTreeNodeIdsInto`) instead of recursively creating subtree Sets and merging them back up the chain, so active-node existence checks no longer pay repeated Set copy costs on deep trees (`维度15-03`).
- Focused proof for that tree renderer slice is green for `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-tree-interaction.test.tsx src/__tests__/data-tree-large-render.test.tsx src/__tests__/data-tree-rendering-and-status.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flux-renderers-data typecheck` plus `build`.
- Closed the retained Word Editor chart/code owner-drift slice in Workstream 4: `packages/word-editor-renderers/src/hooks/use-word-editor-state.ts` now derives host `document`, `runtime.chartCount`, and `runtime.codeCount` strictly from the persisted/autosaved `savedDocument.data` baseline instead of mixing in transient renderer-local chart/code state, so host projections and runtime counts stay aligned with the saved truth surface (`维度04-05`).
- Focused proof for that Word Editor state slice is green for `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-action-provider.test.ts src/__tests__/word-editor-page-host-scope-projections.test.tsx src/__tests__/word-editor-page-actions.test.tsx src/__tests__/use-word-editor-save.test.tsx src/__tests__/editor-canvas.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/word-editor-renderers lint`, `typecheck`, and `build`.
- Re-audited the retained Word Editor `runtime.currentPage` finding and confirmed it is already stale/live-closed: `packages/word-editor-renderers/src/word-editor-manifest.ts` no longer publishes that field, and the remaining `currentPage` references are limited to `packages/word-editor-core/src/editor-store.ts` plus renderer test-support snapshots rather than any live host projection path (`维度04-04`).
