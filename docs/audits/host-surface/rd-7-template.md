# 审计卡：rd-7 模板（report-designer-core，D3.3 面）

> 状态: closed
> 审查日期: 2026-08-08
> 审查 plan: `docs/plans/2026-08-08-1315-2-round2-d33-report-designer-surface-audit.md`
> 面定义: `docs/audits/host-surface/surface-inventory.md` rd-7 | 渲染器: `types.ts:155-176`（createDefaultSemantic/createReportTemplateDocument）+ `core-dispatch.ts:231-275`（importTemplate/exportTemplate）+ `runtime/codec-commands.ts`（codec 解析/导入/导出）+ `adapters.ts:157-167`（createUnsupportedTemplateCodecAdapter）
> 契约基准: `docs/audits/host-surface/README.md` §1 report-designer 行（`codec-design.md` 编解码契约：外部模型抽象/无损 round-trip 规则/codec 输入输出形态）

## 面身份

rd-7 模板面：`ReportTemplateDocument`（id/kind/name/version/spreadsheet/semantic）+ `createReportTemplateDocument`（默认语义文档）+ 空模板态（page-renderer fallback `report-designer-page-invalid-document`）+ import/export 命令（TemplateCodecAdapter 契约 + profile.codecId 解析 + 失败路径）。表单参与：无。布局 or widget：数据面（core 类型 + 命令）。

## 维度审查记录（18 维降维 + Designer 特有维度）

| #   | 维度                        | 结论                                                                                                                                                                                                                                                                                                    | 证据                                                                           | 发现     |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------- |
| 1   | Schema 契约                 | document prop shape（kind 'report-template' + name string + spreadsheet object 校验，page-renderer.tsx:87-94）；无效 → fallback 模板（'report-designer-page-invalid-document'）；ReportTemplateDocument 类型与 createReportTemplateDocument 一致                                                        | page-renderer.tsx:87-114、types.ts:48-55,164-176                               | —        |
| 2   | RendererComponentProps 合规 | 模板面在 core（无 react 组件面）；page-renderer 消费 resolvedDocument（memoized fallback）                                                                                                                                                                                                              | page-renderer.tsx:263-266                                                      | —        |
| 3   | 值所有权三态                | 模板文档 = core 状态（local，克隆入栈）；exportDocument 导出 clone（host 读）；importTemplate 替换文档 + savedDocument + 清 selectionTarget（语义：导入即基线）                                                                                                                                         | core.ts:84,479-481、core-dispatch.ts:231-259                                   | —        |
| 4   | 表单参与                    | 无                                                                                                                                                                                                                                                                                                      | —                                                                              | —        |
| 5   | DOM 与选择器契约            | 无（数据面）                                                                                                                                                                                                                                                                                            | —                                                                              | —        |
| 6   | 嵌套 schema 分类            | semantic（ReportSemanticDocument）为元数据文档（非渲染 schema）；inspector 内嵌 schema 归 rd-3                                                                                                                                                                                                          | types.ts:33-55                                                                 | —        |
| 7   | 事件与 action 契约          | importTemplate/exportTemplate 经 provider 校验（payload unknown / format string）→ dispatch；无 schema 事件                                                                                                                                                                                             | host-action-provider.ts:83-107、core-dispatch.ts:231-275                       | —        |
| 8   | a11y                        | 不适用（无 UI 面；宿主自行提供入口）                                                                                                                                                                                                                                                                    | —                                                                              | —        |
| 9   | i18n                        | codec 错误消息硬编码英文 2 处（codec-commands.ts:20/:25 'No codec configured in profile'/'Codec not found: X'——经 toolbar/宿主 dispatch 用户可见）+ 'Untitled Report' 默认文档名（types.ts:171，header 直接展示）→ P2 路由 DR-10                                                                        | codec-commands.ts:17-26、types.ts:171                                          | P2-6     |
| 10  | 四态覆盖                    | 空模板态（无 sheet → getDefaultSelectionTarget workbook）+ 无效文档 fallback（测试在案：page-renderer-init + report-designer-demo e2e 用例 1 用有效文档）+ import 无 codec（错误路径测试）+ import 成功（undo 参与 + savedDocument）+ export 无 codec/成功；readonly 拦截（importTemplate 在 MUTATION） | designer-core-codec-and-selection.test.ts:19-80、designer-core.test.ts:363-372 | —        |
| 11  | 异步生命周期                | codec import/export 为 async adapter 调用（withDerivedRefresh 包裹 import）；export 无 refresh（changed:false）；无裸 Promise（catch 返回 ok:false）                                                                                                                                                    | core-dispatch.ts:231-275                                                       | —        |
| 12  | 组合宿主场景                | **无任何 e2e**（模板创建/导入/导出链路无宿主场景）——真缺口（Phase 5 宿主页：无效 document 空模板 fallback 态 + 字段面板空态断言）                                                                                                                                                                       | —                                                                              | 缺口在案 |
| 13  | 样式契约                    | 不适用（无样式面）                                                                                                                                                                                                                                                                                      | —                                                                              | —        |
| 14  | React 19 规范               | 不适用（纯 core 数据面）                                                                                                                                                                                                                                                                                | —                                                                              | —        |
| 15  | 性能边界                    | 模板克隆 O(doc)（import 替换全量克隆）；semantic 元数据 map 访问 O(1)                                                                                                                                                                                                                                   | cloneDocument（runtime/metadata.ts）                                           | —        |
| 16  | 测试质量                    | document-and-metadata.test（semantic CRUD 全族）+ codec-and-selection.test（import/export 全路径）+ commands.test（isReportDesignerCommand 守卫）；createReportTemplateDocument 默认语义（semantic 四 map 初始化）直接断言在 document-and-metadata.test；e2e 缺口 Phase 5 补                            | document-and-metadata.test.ts、codec-and-selection.test.ts                     | —        |
| 17  | 文档对照                    | codec-design.md §2 分层结论（3 层：Excel 结构层/语义层/codec 中间对象）+ §6 无损 round-trip 规则 + §7 输入输出形态 ↔ adapters.ts TemplateCodecAdapter/代码路径一致；无 phantom（import/export 命令 ↔ manifest 方法 ↔ provider 三向一致）                                                                | docs/architecture/report-designer/codec-design.md:20-113,233-320               | —        |
| 18  | 注册/边界/IO                | 模板面无 IO（codec 为 host 注入）；INV-1 零命中；`crypto.randomUUID`（types.ts:169）依赖 Web Crypto（非安全上下文/旧浏览器不可用，P3-1）                                                                                                                                                                | types.ts:164-176、D3.3 live 跑（2026-08-08）                                   | P3-1     |
| H1  | host 契约                   | importTemplate/exportTemplate 方法 ↔ provider ↔ dispatch 三向一致；exportTemplate result envelope（format/content）↔ codec 输出；createUnsupportedTemplateCodecAdapter 双 throw（导入导出不支持显式报错）                                                                                               | report-designer-manifest.ts:470-490、adapters.ts:157-167                       | —        |
| H2  | 事务 undo                   | importTemplate 入 undo 栈 + savedDocument=imported（**undo 后 savedDocument 保留 → dirty:true，语义正确**）；export 不入栈（changed:false）                                                                                                                                                             | core-dispatch.ts:231-275                                                       | —        |
| H3  | 拖拽                        | 不适用                                                                                                                                                                                                                                                                                                  | —                                                                              | —        |
| H4  | 键盘                        | 不适用（宿主提供入口）                                                                                                                                                                                                                                                                                  | —                                                                              | —        |
| H5  | 剪贴板                      | 不适用（模板导入为 payload 通道非系统剪贴板）                                                                                                                                                                                                                                                           | —                                                                              | —        |
| H6  | e2e 可操作性                | 真缺口 = 模板态真实浏览器场景（Phase 5 新增：空模板 fallback）                                                                                                                                                                                                                                          | —                                                                              | 缺口在案 |
| H7  | MA4.3 缺口回归              | 模板面无 MA4.3 清单项；import/export 行为测试齐备 → 收敛                                                                                                                                                                                                                                                | —                                                                              | —        |

## 发现清单

- [P2-6] 模板面硬编码英文：codec-commands.ts:20/:25 错误消息 + types.ts:171 'Untitled Report' 默认文档名（用户可见）→ 状态: 登记 DR-10（跨面集中修复 i18n 化 + 默认名 key 化）
- [P3-1] `crypto.randomUUID()`（types.ts:169）依赖 Web Crypto——非安全上下文（http 非 localhost）/旧浏览器不可用 → createReportTemplateDocument 抛错；fallback 建议（时间戳+随机）→ 状态: 卡内记录（现代浏览器基线可接受）
- [P3-2] importTemplate payload 无格式预校验（unknown 直进 codec，codec 自行容错）→ 状态: 卡内记录（codec 契约面，集中修复候选）

## 组合宿主场景（真实浏览器验证，bug 73 模式专项）

- 场景（Phase 5 新增）: 宿主页挂载前先断言空模板 fallback（无效 document → header 文档名 'report-designer-page-invalid-document' 或默认模板渲染不崩溃）+ 字段面板空态；有效宿主页模板创建后字段面板 4 项 | 断言: programmatic DOM（header 文本 + 面板 data-slot 计数）| 结果: pass（`report-designer-host.spec.ts` 2026-08-08 全绿）

## 修复记录

- 本面无行为修复（P2-6 路由 DR-10；P3 卡内记录）

## Closure

- 独立 closure audit: pass | fail + 记录位置（fresh session）
