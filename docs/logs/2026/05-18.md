# Development Log - 2026-05-18

### 2026-05-18

- Fixed the final `#/flux-basic` table row `Inspect` root cause in `packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx`: compiled table `buttons`, `cell`, responsive hidden-cell, and expanded-row regions now render with the current `rowScope`, so parameterized `$slot.record` frames stay isolated per materialized row instead of reusing one column-level fragment scope.
- Aligned the playground dialog payload with the supported surface contract: `apps/playground/src/pages/fluxBasicPageSchema.json` now snapshots row data through `openDialog.args.data`, and the dialog body reads `${username}` / `${email}` from dialog scope instead of depending on the table row slot inside the surface body.
- Recorded the manual repro and resolution in `docs/testing/2026/05-18.md`, indexed it from `docs/testing/index.md`, and added the historical bug note `docs/bugs/60-table-operation-slot-scope-isolation-fix.md`.
- Focused verification passed: `pnpm vitest run --environment happy-dom "packages/flux-renderers-data/src/__tests__/data-table.test.tsx" --exclude ".stryker-tmp/**"` -> `1 passed`, `17 passed`; `pnpm vitest run --environment happy-dom "apps/playground/src/pages/flux-basic-page.debugger.test.tsx" --exclude ".stryker-tmp/**"` -> `1 passed`, `5 passed`; `pnpm vitest run --environment happy-dom "packages/flux-renderers-data/src/__tests__/data-table.test.tsx" "apps/playground/src/pages/flux-basic-page.debugger.test.tsx" --exclude ".stryker-tmp/**"` -> `2 passed`, `22 passed`; `pnpm exec playwright test tests/e2e/flux-basic-row-inspect.spec.ts --project=chromium --reporter=list` -> `1 passed`; `pnpm typecheck`, `pnpm build`, and `pnpm lint` all passed.
- Corrected the form `select` renderer baseline in `packages/flux-renderers-form/src/renderers/input.tsx`: schema `type: 'select'` now uses the custom `@nop-chaos/ui` `Select` primitives instead of `NativeSelect`, preserving native-only usage for explicit `native-select` surfaces.
- Added a regression assertion in `packages/flux-renderers-form/src/__tests__/form-submit-actions.semantic.test.tsx` so form submit coverage now proves the renderer does not expose a native `<select>` for schema `select` while still submitting the updated selected value.
- Updated adjacent custom-select test assumptions in `packages/flux-renderers-data/src/__tests__/data-table.test.tsx` and `apps/playground/src/pages/flux-basic-page.debugger.test.tsx` so they assert stable custom-select contracts instead of native-select label text rendering.
- Full green verification after the select fix: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed. Final green package counts observed in the touched surfaces: `@nop-chaos/flux-renderers-form` `28 files / 193 tests`, `@nop-chaos/flux-renderers-data` `31 files / 292 tests`, `@nop-chaos/flux-renderers-form-advanced` `68 files / 619 tests`, and `@nop-chaos/flux-playground` `15 files / 63 tests`.

### 2026-05-18

- Closed Plans `361` and `362`. For Plan `361`, `packages/flux-react/src/render-nodes.tsx` now preserves fragment `$slot` publication when bindings and explicit scope are combined, `packages/flow-designer-renderers/src/{designer-host-projection.ts,designer-context.ts,designer-manifest.ts}` now share one host-projection source, `packages/spreadsheet-renderers/src/{spreadsheet-manifest.ts,host-action-provider.ts}` now share one published spreadsheet method contract, and the owner-doc set (`docs/architecture/array-field.md`, `docs/architecture/field-binding-and-renderer-contract.md`, `docs/architecture/action-scope-and-imports.md`, `docs/references/renderer-interfaces.md`, plus related report/spreadsheet/flow docs) now matches the final live baseline.
- Closed the remaining Plan `362` blocker in `apps/playground/src/pages/report-designer-demo.tsx`: field-panel click insert now runs the same canonical spreadsheet/designer transaction path as drag-drop instead of only setting drag state. Added live consumer proof in `apps/playground/src/pages/report-designer-demo.test.tsx`, alongside the focused package proofs in `packages/report-designer-renderers/src/report-field-panel.test.tsx`, `packages/spreadsheet-renderers/src/__tests__/context-menu-operations.test.tsx`, `packages/word-editor-renderers/src/__tests__/insert-controls.test.tsx`, and `packages/word-editor-renderers/src/toolbar/page-controls.test.tsx`.
- Focused verification for the final retained closure surfaces passed: `pnpm vitest run --environment happy-dom "packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx" "packages/flux-renderers-form-advanced/src/composite-field/array-field.test.tsx" "packages/spreadsheet-renderers/src/__tests__/schema-integration.test.tsx" "packages/report-designer-renderers/src/report-field-panel.test.tsx" "packages/spreadsheet-renderers/src/__tests__/context-menu-operations.test.tsx" "packages/word-editor-renderers/src/__tests__/insert-controls.test.tsx" "packages/word-editor-renderers/src/toolbar/page-controls.test.tsx" --exclude ".stryker-tmp/**"` -> `7 passed`, `57 passed`; `pnpm vitest run "src/pages/report-designer-demo.test.tsx" --environment happy-dom --exclude ".stryker-tmp/**"` in `apps/playground` -> `1 passed`.
- Full workspace verification for Plans `361` and `362`: `pnpm typecheck`, `pnpm build`, and `pnpm test` completed green. `pnpm lint` initially failed on pre-existing stale plan links in `docs/index.md`; after retargeting those anchors to `docs/archive/plans/{122-compiler-package-extraction-and-boundary-plan.md,131-static-analysis-optimization-plan.md}`, `pnpm lint` also completed green.
- Bug-note adjudication for Plans `361` and `362`: `No bug-note update required`. The retained items closed supported contract/doc drift and accessibility baselines directly in live code, owner docs, and focused regression tests without introducing a separate bug-note-worthy operational incident record.
- Independent closure audits for the final `361`/`362` state: initial audit `ses_1c55a694cffez9SDKin6SfqwYw` identified the last Plan `362` live-consumer blocker in `apps/playground/src/pages/report-designer-demo.tsx`; after the consumer fix plus the final lint/doc cleanup, re-audit `ses_1c535e086ffevAPe86bOliyo0S` reported both plans closure-ready with `No findings.`.

- Closed remediation routing Plan `350` after syncing the final one-to-one owner matrix for findings `R1-1` through `R3-5`; the docs-only routing surface now records successor ownership through Plans `351`-`360` without ambiguous buckets or placeholder deferments.
- Closed runtime scope lifecycle Plan `351`: `packages/flux-runtime/src/runtime-factory.ts` now creates surface scopes by lexical parent inheritance instead of copying `readVisible()` into `initialData`, `packages/flux-runtime/src/scope.ts` tears down composite subscriptions and makes disposed scopes inert/unreadable, and `packages/flux-runtime/src/runtime-host-projection-scope.ts` no longer exposes readable post-dispose state.
- Closed action/compiler boundary Plan `352`: `packages/flux-action-core/src/action-dispatcher/{action-runners.ts,built-in-actions.ts}` now normalize returned action results consistently and avoid latent `invocation!` crashes, while `packages/flux-compiler/src/schema-compiler/shape-validation-rules.ts` now validates action `when` and recursive `onSettled` branches.
- Closed React/surface visibility Plan `353`: `packages/flux-react/src/container-hooks.ts` re-registers live DOM refs instead of stale detached nodes, `packages/flux-react/src/dialog-host-surface.tsx` returns the selected snapshot value, and the existing `packages/flux-renderers-form/src/renderers/form.tsx` user-visible `Form initAction failed` behavior was reconfirmed as the supported baseline.
- Closed form validation Plan `354`: `packages/flux-runtime/src/validation/rules.ts`, `packages/flux-compiler/src/validation-lowering.ts`, and `packages/flux-compiler/src/schema-compiler/validation-collection.ts` now preserve explicit `dependsOn` dependency paths, `packages/flux-runtime/src/form-runtime-field-ops.ts` clears stale `errors` and `validating` state on unregister, and `docs/architecture/form-validation.md` now matches the landed dependency/cleanup baseline.
- Closed flow-designer Plan `355`: `packages/flow-designer-core/src/core/transactions.ts` now honors explicit transaction index `0`, `packages/flow-designer-renderers/src/designer-tree-mode.tsx` only blocks host tree replacement while local edits diverge and accepts replacements again after realignment, and `packages/flow-designer-renderers/src/use-designer-auto-layout.ts` recreates ELK ownership after cleanup so strict-mode remounts do not reuse invalidated state.
- Closed renderer/button Plan `356`: safe button semantics are centralized in `packages/ui/src/components/ui/button.tsx` with default `type="button"`, explicit submit remains preserved in `packages/ui/src/components/ui/button.test.tsx`, and the layout-renderer styling contract stayed aligned with the existing `docs/architecture/styling-system.md` baseline without owner-doc changes.
- Closed compiler hot-path Plan `357`: `packages/flux-compiler/src/schema-compiler/validation-collection.ts` now uses indexed queue traversal instead of `shift`/`unshift`, and `packages/flux-compiler/src/validation-collection.test.ts` now contains a large-root regression guard that fails if those queue operations return on the traversal hot path.
- Closed helper convergence Plan `358`: `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` now uses canonical `isAbortError` semantics from `@nop-chaos/flux-core` instead of a divergent local helper.
- Closed package hygiene Plan `359`: `packages/flux-renderers-form/package.json` no longer declares the unused production dependency `@nop-chaos/flux-runtime`.
- Closed compiler determinism Plan `360`: `packages/flux-compiler/src/schema-compiler/target-enrichment.ts` no longer mutates caller-provided `cidState` in place, and `docs/architecture/template-instantiation-and-node-identity.md` now records per-compile caller-owned `CompiledCidState` as the deterministic baseline.
- Focused verification passed for the final hot-path and flow-designer closure surfaces: `pnpm vitest run --environment happy-dom "packages/flux-compiler/src/validation-collection.test.ts" --exclude ".stryker-tmp/**"` -> `21 passed`; `pnpm vitest run --environment happy-dom "packages/flow-designer-core/src/__tests__/core-graph.test.ts" "packages/flow-designer-renderers/src/designer-page.tree-history.test.tsx" "packages/flow-designer-renderers/src/auto-layout-guards.test.tsx" "packages/flux-compiler/src/validation-collection.test.ts" --exclude ".stryker-tmp/**"` -> `4 passed`, `45 passed`.
- Full required workspace verification passed after the final `357` proof landed and the typed test-helper fix was applied: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all completed green.
- Independent closure audits: `ses_1c66e86ebffeUQPLe8MOl7YoC6` reconfirmed Plans `351`, `352`, `353`, `354`, `356`, `358`, `359`, and `360` as closure-ready; `ses_1c63fd605ffeJCKRcr7zDfq3aD` re-checked the fully synced plans, confirmed the substantive closure evidence for Plans `355` and `357`, and after log/plan sync left no remaining findings.

### 2026-05-18

- Stabilized the supported E2E page-error gate after a live false-negative in component-lab runs: `tests/e2e/fixtures.ts` now tracks fixture-managed pages by Playwright internal page guid when available instead of relying only on `WeakMap<Page, ...>` object identity.
- Added a positive regression proof in `tests/e2e/fixtures-hard-gate.spec.ts` so the supported helper path now proves both sides of the contract: untracked pages still fail loudly, and fixture-managed pages still pass through `openRendererDirect(...)` across helper boundaries.
- Component-lab navigation helpers in `tests/e2e/component-lab/helpers.ts` now use `page.goto(..., { waitUntil: 'commit' })` to match the current SPA/hash-route baseline and avoid load-sensitive `domcontentloaded` stalls during focused Playwright reruns.
- Focused Playwright regression verification passed after the fixture + navigation fix: `pnpm exec playwright test "tests/e2e/fixtures-hard-gate.spec.ts" "tests/e2e/component-lab/simple-form.spec.ts:286" "tests/e2e/component-lab/smoke.spec.ts" --grep "input-tree|fixture-managed page|toggle switch" --reporter=list` -> `3 passed`.

### 2026-05-18

- Started executing `docs/plans/349-open-ended-adversarial-review-2026-05-18-diagnostics-trustworthiness-plan.md` from `docs/analysis/2026-05-18-open-ended-adversarial-review-01/{round-01.md,summary.md}`.
- Baseline decision: debugger `inspectByElement()` now owns a strict runtime-scoped contract. Foreign-runtime clicks must return no inspect result and must not mutate the current panel selection. Code paths: `packages/nop-debugger/src/controller-component-inspector.ts`, `packages/nop-debugger/src/panel/use-inspect-mode.ts`.
- Baseline decision: supported Playwright specs must use the fixture-managed `page` when calling `assertTrackedPageErrors(page)`. Untracked pages are now a hard failure rather than a silent no-op. Code/doc paths: `tests/e2e/fixtures.ts`, `docs/testing/e2e-standards.md`.
- Supported proof decision: `tests/e2e/debugger-meta-diagnostic.spec.ts` stays in the supported `tests/e2e/` surface and is being converted from a log-only probe into explicit `explainNodeMeta()` contract assertions.
- Landed focused panel proof for selection preservation on foreign-runtime inspect clicks in `packages/nop-debugger/src/panel.test.tsx`; together with `packages/nop-debugger/src/controller-inspect-basic.test.ts`, the debugger surface now proves both controller rejection and panel-level no-selection-mutation behavior.
- Focused debugger verification passed: `pnpm vitest "packages/nop-debugger/src/controller-inspect-basic.test.ts" "packages/nop-debugger/src/panel.test.tsx"` -> `12 passed`, `153 passed`.
- Focused supported E2E verification passed: `pnpm exec playwright test "tests/e2e/fixtures-hard-gate.spec.ts" "tests/e2e/code-editor.spec.ts" "tests/e2e/debugger-meta-diagnostic.spec.ts" --reporter=list` -> `17 passed`, `1 skipped`.
- Workspace verification stayed green for the plan-owned change set: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all passed.
- Independent closure audit passed with `No findings.` and confirmed plan `349` can close once plan/log text are synced. Audit session: `ses_1c73ab044ffeDO7ob9qNse7YXy`.
- Extra full Playwright sweep `pnpm exec playwright test --reporter=list` did not go green: `302 passed`, `41 skipped`, `5 failed`. Failures were out-of-scope discoveries in `tests/e2e/component-lab/crud-query-and-ownership.spec.ts` (4) and `tests/e2e/word-editor.spec.ts` (1), so they are recorded as follow-up ownership candidates rather than blockers for this diagnostics-trustworthiness plan.
