# Development Log - 2026-06-23

### 2026-06-23 (M3 容器与布局响应式 — 执行 + closure audit，plan 收口)

- **M3 容器与布局响应式（page / flex / container；grid 延后）执行完成，独立 fresh-session closure audit `approved`（2 轮：首轮 1 Blocker + 2 Major，二轮 1 Major doc inconsistency，三轮 narrow re-audit 全绿），Plan Status → completed。** Plan: `docs/plans/2026-06-23-0410-1-m3-container-and-layout-responsive-plan.md`（3 Phase，`建议有测` tier）。Mission: mobile，Work Item: M3。
- **Phase 1（M3a page 响应式）**：`packages/flux-renderers-basic/src/page.tsx` 消费 `useIsMobile()` —— (1) aside 小屏折叠为触发式 Sheet 滑出（`data-slot="page-aside-toggle"` + `@nop-chaos/ui` Sheet，side=`asidePosition`，**非纯 hidden**）；(2) toolbar 小屏 `flex flex-col` 纵列堆叠；(3) footer fixed VisualViewport hook（新文件 `packages/flux-renderers-basic/src/use-fixed-footer-visual-viewport.ts`），mobile + `footerClassName.includes('fixed')` + 软键盘弹起时注入 `style.bottom: ${offset}px` + `data-keyboard-offset` marker；无 `window.visualViewport` 早返回（Failure Path）。收口 M2 plan + M0.1 plan 路由到 M3a 的 VisualViewport deferred 项。
- **Phase 2（M3a §14 骨架模式 playground + e2e + deferred 收口）**：`apps/playground/src/pages/m3-layout-demo.tsx`（路由 `/m3-layout`，5 类骨架模板各独立 SchemaRenderer，env.navigate → toast 可观测），`tests/e2e/m3-layout.spec.ts`（7 用例：移动视口下 Tabbar navigate toast、NavBar back、ActionBar/SubmitBar/Sticky 结构、VisualViewport `data-keyboard-offset` 注入、桌面无 mobile-only artifact）。**§14 模板修正**（Decision）：`flex.direction: "col"` → `"column"`、`button.variant: "solid"` → `"default"`、`fixed bottom-0 ...` 提升至 `footerClassName`（VisualViewport hook 检测点，非 inner flex）、NavBar `sticky top-0 ...` 提升至 `toolbarClassName`（非 `headerClassName`——`page-header` wrapper 仅在 title/subTitle/remark/mobile-aside-toggle 时渲染，无 title 的 NavBar schema 走 `page-toolbar`）。M1/M2/M0.1 源 plan Deferred 段加 `Closure Note (2026-06-23)`。
- **Phase 3（M3b flex/container 断点响应式）**：`packages/flux-renderers-basic/src/schemas.ts` 新增 `ResponsiveBreakpoint`/`ResponsiveFlexDirection`/`ResponsiveContainerDirection`/`ResponsiveWrap` 类型，`FlexSchema`/`ContainerSchema` 加 `responsiveDirection?` / `responsiveWrap?` 字段；`packages/flux-renderers-basic/src/utils.ts` 加 `resolveResponsiveDirection` / `resolveResponsiveWrap`（输出 `sm:` / `md:` / `lg:` / `xl:` / `2xl:` Tailwind 类前缀）；`flex.tsx` + `container.tsx` 消费，container `useFlexChild` 扩 `hasResponsive` 触发 flex body。缺省（无字段）输出完全不变（无回归）。
- **owner docs 同步**：`page/design.md` §13 重写（断点表对齐 live `useIsMobile()` 阈值 768px、aside Decision「折叠为触发式 Sheet 滑出」非纯 hidden、新增「软键盘 VisualViewport 处理（footer fixed 栏）」小节）；§14.1-§14.4 模板按上述修正；`flex/design.md` 新增 §14 响应式行为小节；`container/design.md` 新增 §16 响应式行为小节（与 flex 共享 per-breakpoint 字段约定）。
- **roadmap 同步**：`docs/components/mobile-roadmap.md` —— M3 Phase Status 标 `done`，Current Baseline 表更新（M3 done 含 grid 延后 W3a successor），M3 work item 表 M3a/M3b → ✅，Last Updated → 2026-06-23。
- **Focused 单测（23 case）**：`page-responsive.test.tsx`（11：aside 内联 vs mobile toggle、toolbar 堆叠、VisualViewport 早返回 + offset 注入）、`flex-responsive.test.tsx`（12：flex + container 的 direction/wrap 断点切换 + 缺省回退 + 共享断点约定）。均采用 `vi.mock('@nop-chaos/ui', ... useIsMobile)` 控制 mobile/desktop 分支。
- **Closure audit 证据**：独立 fresh-session subagent `ses_10eefec68ffet5HHhTo5SuuNtR`（首轮，发现 BLOCKER：`/m3-layout` 未传 `registry` prop 导致渲染空；MAJOR：VisualViewport e2e 替换 vv 而非原地 mutate；MAJOR：fixed 提升至 footerClassName）→ 修复 → `ses_10ee1dc6dffe44r7Oeqaea2oDH`（二轮，runtime/e2e 全绿，但发现 §14.2 NavBar `headerClassName` vs `toolbarClassName` doc 不一致 MAJOR）→ 修复 → `ses_10edec846ffeBhm4bOdrZb2IGX`（narrow re-audit，`approved`，关闭 plan）。**3 轮 audit 全部由独立 fresh-session subagent 完成**，执行 session 不自审。
- **仓库级验证（执行 session + audit session 双重确认）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（1 pre-existing TanStack Virtual warning in flux-renderers-form-advanced 非 M3）、`pnpm test` 51/51、`npx playwright test tests/e2e/m3-layout.spec.ts` 7/7，全绿（**full-green verification**：unit + e2e 均通过）。
- **Closure 裁定**：grid 响应式延后主 roadmap W3a successor（grid renderer 未落地，非 M3 结果面内 defect）；container query 体系化（baseline §7 标「未来」）+ page aside 折叠态增强交互（可拖拽宽度）归 Non-Blocking Follow-ups。无 in-scope live defect 被静默降级。Plan Status `active`→`completed`，11 项 Closure Gates 全 `[x]`。

### 2026-06-23 (M2 表单控件触摸适配 — 执行 + closure audit，plan 收口)

- **M2 表单控件触摸适配（input / textarea / input-number / checkbox / radio / switch / button）执行完成，独立 fresh-session closure audit `approved`，Plan Status → completed。** Plan: `docs/plans/2026-06-22-2335-2-m2-form-controls-touch-adaptation-plan.md`（4 Phase，`建议有测` tier）。Mission: mobile，Work Item: M2。
- **新增共享工具** `packages/flux-renderers-form/src/renderers/mobile-touch-utils.ts`：`resolveInputMode()`（inputType→inputmode 映射 email/tel/search/url + decimal）、`scrollRefIntoViewOnMobile()`（mobile-only scrollIntoView）、`shouldStackChoicesVertically()` + `MOBILE_CHOICE_STACK_THRESHOLD=3`、`InputModeValue` 类型（对齐 React inputmode union）。
- **M2a（input/textarea/input-number）代码落地**：
  - `packages/flux-renderers-form/src/schemas.ts` — `InputSchema` + `InputNumberSchema` 加 `inputMode?: string`。
  - `input.tsx` — `useIsMobile` + `resolveInputMode(inputType, props.props.inputMode)` 透传 `inputMode` 到 sharedInputProps + `scrollRefIntoViewOnMobile` 于 onFocus；`inputEnhancementFieldRules` 注册 `{ key: 'inputMode', kind: 'prop' }`。
  - `textarea-renderer.tsx` / `input-number-renderer.tsx` — `useIsMobile` + mobile-only scrollIntoView；input-number 默认 `inputMode='decimal'`（schema 可覆盖）。
  - font-size ≥16px 防 iOS 缩放：**复用** `@nop-chaos/ui` `Input`/`Textarea` 已有 `text-base md:text-sm`（mobile=16px），非新增 class。
- **M2b（checkbox/radio/switch）代码落地**：`input-choice-renderers.tsx` CheckboxRenderer/SwitchRenderer/RadioGroupRenderer + `checkbox-group-renderer.tsx` — `useIsMobile`；item Label 条件加 `min-h-11 py-2`（mobile，min-height 44px 优先于 size height）+ 无条件 `nop-haptic`；`shouldStackChoicesVertically(isMobile, options.length)` 为真时 wrapper/RadioGroup 加 `flex flex-col gap-1` + `data-mobile-stack="true"` marker。
- **M2c（button）代码落地**：`packages/flux-renderers-basic/src/button.tsx` — `useIsMobile`；`MOBILE_TOUCH_TARGET_SIZES=['default','lg']`，mobile 且 size 属该集合时 className 加 `min-h-11`；schema `block`（`block && 'w-full'`）不变（Decision A：不 auto-block，避免 button-group 多按钮堆叠）。`nop-haptic` 由 `@nop-chaos/ui` button cva 基础串无条件提供（M0.1c）。
- **owner docs 同步**：6 份 design.md 各补响应式小节 —— `input-text/design.md` §13、`textarea/design.md` §13、`input-number/design.md` §15、`checkbox/design.md` §13（含 radio-group，radio/design.md 未单列）、`switch/design.md` §13、`button/design.md` §13。
- **playground / e2e**：`apps/playground/src/pages/m2-touch-demo.tsx`（路由 `/m2-touch`，经 `route-model.ts` + `App.tsx` 注册）；`tests/e2e/m2-touch.spec.ts`（程序化断言：inputmode、font-size、min-height、flex-col，非截图）。
- **roadmap 同步**：`docs/components/mobile-roadmap.md` Phase Status L75 + Current Baseline L23 + M2 work item 表 L155-157 均标 `done`（M2a~M2c → ✅）。
- **Focused 单测（30 case，全绿）**：`input-touch-adaptation.test.tsx`（14）、`choice-touch-adaptation.test.tsx`（9）、`button-touch-adaptation.test.tsx`（7）。均采用 `vi.mock('@nop-chaos/ui', ... useIsMobile)` 控制 mobile/desktop 分支。
- **仓库级验证（执行 session）**：`pnpm typecheck` 51/51、`pnpm build` 27/27、`pnpm lint` 27/27（0 errors，1 pre-existing warning in select-combobox-lists.ts useVirtualizer 非 M2）、`pnpm test` 51/51，全绿。
- **Closure audit**：独立 fresh-session subagent `ses_10f14948bffeAuvE0516c8r3vP` verdict `approved`（Phase exit criteria 全 PASS，3 个不可自审 Closure Gates 全 PASS，focused 单测独立 re-run 30/30 PASS，interface-vs-semantics 抽查 text-base=16px/min-h-11=44px/flex-col 真实生效）。e2e 受 pre-existing dev server SchemaRenderer 渲染问题阻塞（与 M1 plan `m1-responsive.spec.ts` 先例一致：静态 heading 渲染、schema body 不渲染；clean repo mobile-components smoke 同样失败），focused 单测为主验证路径。
- **Closure 裁定**：无 in-scope live defect 被静默降级；两项 deferred（page footer VisualViewport→M3a、input suggest mobile→M1a 延伸）均为合法 out-of-scope improvement。Plan Status `active`→`completed`，14 项 Closure Gates 全 `[x]`。

### 2026-06-23 (M1 高频交互控件响应式 closure audit — plan 收口)

- **M1 高频交互控件响应式（select / tree-select / table / dialog / drawer / tabs）closure audit 通过，Plan Status → completed。** Plan: `docs/plans/2026-06-22-2335-1-m1-high-frequency-controls-responsive-plan.md`（5 Phase，`建议有测` tier）。本日由独立 fresh-session closure audit subagent（CLOSURE_AUDIT step，不复用执行 session 上下文）对照 live repo 逐项核对全部 5 Phase + Closure Gates。
- **代码落地核对（对照 live repo，非信任 checklist）**：
  - M1a：`packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx:236` + `packages/flux-renderers-form-advanced/src/tree-controls.tsx:182` 消费 `useIsMobile()`，mobile 走 `@nop-chaos/ui` Sheet（side=bottom）。
  - M1c：`packages/flux-react/src/dialog-host.tsx:189,367`（DialogView/DrawerView）消费 `useIsMobile()`；L416 drawer mobile `effectiveSide = isMobile && schemaSide !== 'bottom' ? 'bottom' : schemaSide` + `data-mobile-side-overridden` marker。
  - M1d：`packages/flux-renderers-basic/src/tabs.tsx:141,235-253,279,380-396`（swipeStateRef + 内联 `onTouchStart`/`onTouchMove`/`onTouchEnd` + `overflow-x-auto` + `nop-scrollbar-hide`，仅 mobile 注入）。
  - M1b：`packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx:653,672`（`nop-safe-bottom` + `nop-hairline--bottom`）+ `table-renderer.tsx:383`（`data-responsive-expand` marker）；裁定 A（现有 expand 满足，不做 `responsive.mode: 'card'`）。
- **Focused 单测核对**：5 个 responsive 测试文件存在（select-responsive / tree-select-responsive / dialog-host-responsive / table-responsive / tabs-responsive），断言覆盖 mobile/desktop 分支、Sheet/Popover 切换、swipe 方向/阈值/边界、drawer mobile side override、table expand marker。
- **design.md 同步核对**：6 份均含响应式小节引用 M0 baseline —— `select/design.md` §13、`tree-select/design.md` §12、`table/design.md` §13（M1b 裁定 + 视觉增强）、`dialog/design.md` §14、`drawer/design.md` §14、`tabs/design.md` §21（M1d Decision 裁定 A）。
- **roadmap 同步核对**：`docs/components/mobile-roadmap.md:23,74,138,144-147`（M1 → `done`，M1a~M1d → ✅）。
- **playground / e2e 核对**：`apps/playground/src/pages/m1-responsive-demo.tsx` + `tests/e2e/m1-responsive.spec.ts`（程序化断言，非截图）。
- **deferred 诚实性核对**：三项 deferred 均为合法 non-blocking —— bottom-sheet 拖拽/下滑关闭（optimization candidate）；surface runtime sheet-kind branch（out-of-scope improvement）；tabs sidebar 移动端（out-of-scope improvement + Successor: M3a）。e2e spec 受 pre-existing dev server SchemaRenderer 渲染问题阻塞（clean repo `mobile-components` smoke 同样失败，与本 plan 无关），focused 单测（28 case across 5 packages）作为主验证路径，属 `建议有测` 档可接受。
- **仓库级验证（本审计会话独立 re-run，full-green）**：`pnpm typecheck` = 51/51、`pnpm build` = 27/27、`pnpm lint` = 27/27、`pnpm test` = 51/51，全绿（cached green baseline）。
- **Closure 裁定**：无 in-scope live defect 被静默降级；Plan Status 由 `active` → `completed`，Last Reviewed → 2026-06-23，15 项 Closure Gates 全 `[x]`，Closure Audit Evidence 写入真实证据（live code path/行号 + 测试文件 + design.md 章节 + roadmap 行号 + 验证输出）。
