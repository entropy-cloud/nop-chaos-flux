# Development Log — 2026-04-10

### 2026-04-10 (21)

- Created Plan 64 for node identity memory optimization and CompiledSchemaNode cleanup based on architecture discussion.
- Code paths: `docs/plans/64-node-identity-memory-optimization-and-compiledschemanode-cleanup-plan.md`
- Key decisions:
  - Confirmed that current codebase still uses CompiledSchemaNode extensively (109 matches), not TemplateNode only.
  - Confirmed that Plan 40 established type contracts but deferred actual migration to compatibility layer.
  - Identified unnecessary fields: NodeLocator.runtimeId (single runtime), NodeLocator.templateGraphId (cid sufficient), repeated instancePath storage (should share).
  - Target memory reduction: 30-50% for 1000-node pages (0.6-3.5 MB → 0.4-2.0 MB).
  - Keep templatePath (compile-time fixed, acceptable storage), use cid as primary locator.
  - 5-phase execution: Phase 1 (simplify NodeLocator/ComponentHandle), Phase 2 (SchemaCompiler → TemplateNode), Phase 3 (NodeRenderer → TemplateNode), Phase 4 (remove CompiledSchemaNode runtime usage), Phase 5 (docs + verification).
- Link to plan: `docs/plans/64-node-identity-memory-optimization-and-compiledschemanode-cleanup-plan.md`

### 2026-04-10 (20)

- Refactored debug data registration to eliminate per-node memory overhead and unify cleanup ownership.
- Code paths: `packages/flux-core/src/types/renderer-component.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-react/src/useNodeDebugData.ts`, `packages/nop-debugger/src/controller.ts`, `packages/flux-renderers-data/src/index.test.tsx`
- Key changes:
  - Added `debugEnabled: boolean` and `setDebugEnabled(enabled: boolean): void` to `ComponentHandleRegistry` interface and implementation.
  - `setHandleDebugData` is now a no-op when `debugEnabled` is false; calling `setDebugEnabled(false)` also clears all stored debug data.
  - `useNodeDebugData` now guards on `registry.debugEnabled` before writing: when debugger is not connected, no `ComponentHandleDebugData` object (containing `nodeInstance`, `scope`, `resolvedMeta`, `resolvedProps`) is allocated or stored for any node.
  - Removed `debugDataByCid.delete(handle._cid)` from `unindexHandle` — `debugDataByCid` lifecycle is now exclusively owned by `useNodeDebugData`'s effect cleanup, eliminating dual-cleanup ambiguity.
  - `nop-debugger` controller's `setComponentRegistry` now calls `setDebugEnabled(true)` on connect and `setDebugEnabled(false)` on disconnect.
  - Updated `flux-renderers-data` test to opt-in via `setDebugEnabled(true)` in `onComponentRegistryChange` and use `waitFor` for async debug data availability.
- Key decisions: `debugEnabled` is the single gate for all debug data writes. Production pages with no debugger attached produce zero `ComponentHandleDebugData` allocations. The `NodeLocator` object itself is kept as-is (it is needed for action dispatch context and renderer self-registration) — the savings come from eliminating the large `ComponentHandleDebugData` objects.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm test` all pass.

### 2026-04-10 (19)

- Completed plan 63 Phases 4 and 5: converged the three ambient node identity contexts into a single carrier (`NodeMetaContext`), ran full closure verification, and marked the plan completed.
- Code paths: `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-react/src/contexts.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/schema-renderer-runtime.test.tsx`
- Key changes:
  - Removed `CompiledNodeContext` and `NodeInstanceContext` from `contexts.ts` and public exports — both were redundant given `NodeMetaContext` already carries `node`, `nodeInstance`, `templateNode`, `locator`, `id`, `path`, `type`.
  - `useCurrentNodeInstance()` now reads `NodeMetaContext.nodeInstance` instead of `NodeInstanceContext`.
  - `useRenderFragment()` reads owner identity from `NodeMetaContext` alone (removed `NodeInstanceContext` read).
  - `RenderNodes` reads `ownerNode` and `ownerNodeInstance` from `NodeMetaContext` instead of two separate contexts.
  - `NodeRendererProviders` now sets only `NodeMetaContext` (removed `CompiledNodeContext.Provider` and `NodeInstanceContext.Provider` nesting).
  - Made `RenderNodeMeta.node` optional so tests can provide minimal `NodeMetaContext` values without a full `CompiledSchemaNode`.
  - Updated test to use `NodeMetaContext.Provider` with a minimal value instead of `NodeInstanceContext.Provider`.
- Key decisions: `NodeMetaContext` is the single ambient node identity carrier in `flux-react`. There is no `CompiledNodeContext` or `NodeInstanceContext` — both are now history.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass.
- Plan 63 is now closed.

### 2026-04-10 (18)

- Completed plan 63 Phases 2 and 3: compiled node-local optional boundary closures, pushed form/page data scope creation to concrete owners, verified full workspace.
- Code paths: `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/test-support.tsx`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-core/src/types/renderer-compiler.ts`
- Key changes:
  - Fixed `FormRenderer` bug: was using `props.nodeInstance.scope` (unstable every render) as `parentScope` for `createFormRuntime`; changed to `useRenderScope()` which reads stable `ScopeContext` value.
  - Fixed `NodeRendererProviders`: `scope` was in `PROVIDER_CONTEXTS` but `wrapProviders` closure only iterates `['actionScope', 'componentRegistry', 'classAliases']` — scope was never published. Fixed by moving `ScopeContext.Provider` outside `wrapProviders` as an unconditional wrapping (every node always publishes its scope).
  - Removed `form`/`page` from `CompiledNodeRenderPlanProviders` and `PROVIDER_BUILD_ORDER` — these are creator-owned boundaries, not node-local closures.
  - Removed `useNodeForm.ts` and `useFormComponentHandleRegistration.ts` — both now owned by `FormRenderer` directly.
  - Updated `formRenderer` test stub in `test-support.tsx` to properly own `FormRuntime` creation and publish `FormContext`/`ScopeContext` — required to align with the new owner-boundary model.
- Key decisions: `ScopeContext` must always be set by `NodeRendererProviders` (unconditional, not gated by `wrapProviders`). Only truly node-local optional boundaries (`actionScope`, `componentRegistry`, `classAliases`) go through the compiled closure.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass (57 flux-react tests, 150 flux-renderers-form tests).
- Next step: Phase 4 (context convergence) and Phase 5 (doc sync + closure audit).

### 2026-04-10 (17)

- Executed plan 63 Phase 1: audited live code and updated ownership-model doc baseline.
- Doc paths: `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/scope-ownership-and-isolation.md`, `docs/architecture/surface-owner.md`, `docs/plans/63-node-renderer-owner-boundary-and-context-convergence-plan.md`
- Key changes:
  - Added "Execution Boundary Ownership Matrix" section to `renderer-runtime.md` — explicit table distinguishing node-local optional execution boundaries (compiled into `renderPlan.wrapProviders`) from creator-owned data/runtime boundaries (page, form, fragment, surface)
  - Added "Node Context Convergence" section to `renderer-runtime.md` — records the three current overlapping contexts and the target single-carrier path
  - Updated "Render Context Split" section to cross-reference the convergence section
  - Added "Node-Local Versus Owner-Local Capability Boundaries" section to `action-scope-and-imports.md` — separates compile-time node-local closures (`classAliases`, `xui:imports` overlays) from owner-local boundaries (page, form, fragment data scope, surface runtime)
  - Updated `scope-ownership-and-isolation.md` — replaced "Current Gaps" section with "Data Field Ownership By Node Family" ownership table, closing the documented gaps
  - Added "Relation To NodeRenderer" section to `surface-owner.md` — explicitly states that `NodeRenderer` does not create or manage surface runtime/store boundaries
- Key decisions: classify boundaries into (1) node-local compile-time closures and (2) creator-owned runtime boundaries; these two categories must never be mixed; phase 2 and 3 will enforce this split in code.
- Next step: execute Phase 2 — replace generic provider-plan/diff model with compile-time node-local closure contract.

### 2026-04-10 (16)

- Added a new execution plan for the next `NodeRenderer` convergence pass after re-auditing the live provider-plan experiment against the owner-boundary rules discussed today.
- Doc paths: `docs/plans/63-node-renderer-owner-boundary-and-context-convergence-plan.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/scope-ownership-and-isolation.md`, `docs/architecture/surface-owner.md`, `docs/components/dialog/design.md`
- Key decisions: treat `classAliases` and node-local `xui:imports` capability setup as compiler-owned optional-boundary closures; push page/form/fragment/dialog data-scope creation back to the concrete owner; require dialog/drawer to share one surface-family runtime/store model instead of reusing page store; manage nested surfaces through one root host stack with append-order layering and top-surface-only interaction semantics; converge the current node meta/compiled/node-instance ambient carriers toward a single node-instance context.
- Next step: execute plan 63 in ownership-complete slices rather than extending the intermediate generic provider-plan model.

### 2026-04-10 (15)

- Completed the provider-plan execution step by replacing the handwritten `NodeRendererProviders` provider chain with a data-driven executor built from compiled provider-plan steps.
- Code paths: `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/schema-renderer-runtime.test.tsx`
- Docs updated: `docs/architecture/renderer-runtime.md`
- Key decision: stop half-measuring on provider-plan adoption; once provider skeleton is compiled, React should execute it through a generic plan executor rather than keep an equivalent handwritten chain in source.
- Verification: focused `@nop-chaos/flux-react` `typecheck` and runtime tests passed.
- Next step: run full workspace verification and then use the same pattern for other planable `NodeRenderer` shape concerns only if they have a similarly stable compile-time boundary.

### 2026-04-10 (14)

- Promoted the compiled boundary hints into an explicit `renderPlan.providers` structure on `CompiledSchemaNode`, then switched the React layer to execute that plan instead of inferring provider structure directly from scattered runtime rules.
- Code paths: `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/schema-renderer-runtime.test.tsx`
- Docs updated: `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`
- Key decision: keep the render plan framework-agnostic and data-shaped; do not generate React render functions inside `flux-runtime`, but do let `flux-react` execute the compiled provider skeleton against live runtime identities.
- Verification: focused `typecheck` passed for `@nop-chaos/flux-core`, `@nop-chaos/flux-runtime`, and `@nop-chaos/flux-react`; focused tests passed for `@nop-chaos/flux-react`.
- Next step: rerun workspace-wide verification and keep runtime creation stable across `env` identity changes.

### 2026-04-10 (13)

- Tightened the `NodeRenderer` boundary model after the follow-up audit: compiled nodes now record which runtime boundaries may exist, and the React layer keeps provider shape stable for those nodes while still comparing live parent/child identities to avoid unnecessary boundary churn.
- Code paths: `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/schema-renderer-runtime.test.tsx`
- Docs updated: `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`
- Key decision: compile-time boundary metadata is useful for preserving stable provider structure, but it cannot replace runtime identity comparison because fragment scopes, import overlays, and other instance-local boundaries are only fully known at render time.
- Verification: focused `typecheck` passed for `@nop-chaos/flux-core`, `@nop-chaos/flux-runtime`, and `@nop-chaos/flux-react`; focused tests passed for `@nop-chaos/flux-runtime` and `@nop-chaos/flux-react`.
- Next step: rerun workspace-wide `typecheck`, `build`, `lint`, and `test` before treating this follow-up slice as closed.

### 2026-04-10 (12)

- Completed the closure audit for plan 62 after re-checking the live repo, verification results, and the updated execution checklist, then marked the plan itself as completed.
- Doc paths: `docs/plans/62-core-runtime-orchestration-refactor-plan.md`, `docs/logs/2026/04-10.md`
- Key decision: treat plan closure as a separate audit step from the implementation landing; only flip the top-level plan status after all phase statuses, validation items, and full-workspace verification were already green.
- Next step: no remaining plan-owned work under plan 62.

### 2026-04-10 (11)

- Executed the first landed slice of plan 62 by shrinking the main runtime/react/designer orchestration files back toward composition roots without changing public behavior.
- Code paths: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/action-runtime-core.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/schema-compiler/shape-validation.ts`, `packages/flux-runtime/src/schema-compiler/target-enrichment.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-runtime-status.ts`, `packages/flux-runtime/src/form-runtime-submit.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/node-renderer-effects.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-react/src/dialog-host-surface.tsx`, `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-core/src/core/shell-state.ts`, `packages/flow-designer-core/src/core/snapshot.ts`
- Docs updated: `docs/plans/62-core-runtime-orchestration-refactor-plan.md`, `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`, `docs/skills/code-refactor-prompt.md`
- Key decisions: keep `action-runtime.ts`, `schema-compiler.ts`, `form-runtime.ts`, `node-renderer.tsx`, `dialog-host.tsx`, and `flow-designer-core/src/core.ts` as orchestration entries; only move stable payload/validation/effect/provider/surface/shell helpers into internal modules; do not force React 19 API adoption where the live code does not show a clear ROI.
- Verification: focused `typecheck` passed for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, and `@nop-chaos/flow-designer-core`; focused tests passed for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, and `@nop-chaos/flow-designer-core`.
- Next step: run full workspace `typecheck`, `build`, `lint`, and `test`, then perform a closure audit before deciding whether plan 62 can be closed.

### 2026-04-10 (10)

- Audited the live repo for structural refactor opportunities instead of relying on already-closed plans, then recorded the next real refactor target as a new runtime-focused execution plan.
- Doc/code paths: `docs/skills/code-refactor-prompt.md`, `docs/plans/61-core-runtime-orchestration-refactor-plan.md`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flow-designer-core/src/core.ts`
- Key decisions: treat the remaining high-ROI refactor debt as core orchestration-boundary work rather than another whole-repo style sweep; extend the reusable refactor prompt with low-code runtime boundary checks and React 19 ROI-based audit rules instead of API-counting heuristics.
- Next step: execute plan 61 in focused slices, starting from runtime action/compiler helpers and then the React/runtime orchestration edges.

### 2026-04-10 (9)

- Tightened the plan workflow guide after the 42-60 closure audit to make premature completion harder: `completed` now requires a separate closure-audit pass and all execution slices must already be closed or explicitly moved out of scope.
- Doc paths: `docs/plans/00-plan-authoring-and-execution-guide.md`
- Key decision: treat closure as its own audit step over live repo evidence, not as an automatic side effect of finishing the latest implementation slice.
- Next step: use the stricter closure-audit rule for future plan updates instead of relying on informal end-of-task judgment.

### 2026-04-10 (8)

- Completed the closure audit for the remaining 42-60 plans that were still open on paper: plan 42, plan 44, plan 46, and plan 59 DOM now match the live repo state.
- Doc paths: `docs/plans/42-frontend-programming-model-alignment-remediation-plan.md`, `docs/plans/44-semantic-lifecycle-entry-alignment-plan.md`, `docs/plans/46-user-management-schema-and-authoring-contract-alignment-plan.md`, `docs/plans/59-dom-ref-and-event-passthrough-plan.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/api-data-source.md`, `docs/references/flux-json-conventions.md`, `docs/components/button/design.md`
- Key decisions: treat closure as a repo-audit operation rather than a narrative update; keep plan 46 open until the normative docs explicitly covered compatibility carriers and adaptor authoring semantics; close plan 42 only after the narrower lifecycle and DOM/event plans were verified landed.
- Verification: this was a docs-only closure pass based on the already-green workspace baseline from today's full verification run: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`.
- Next step: no remaining plan-owned work in the 42-60 series.

### 2026-04-10 (7)

- Completed the remaining 42-60 execution backlog for plans 59-tree, 60, and 45: landed `tree`, `input-tree`, `tree-select`, and the remaining React 19 hot-path cleanup.
- Code paths: `packages/flux-renderers-data/src/index.tsx`, `packages/flux-renderers-data/src/schemas.ts`, `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `packages/flux-renderers-form/src/index.tsx`, `packages/flux-renderers-form/src/schemas.ts`, `packages/flux-renderers-form/src/tree-options.ts`, `packages/flux-renderers-form/src/renderers/tree-controls.tsx`, `packages/flux-renderers-form/src/index.test.tsx`, `packages/flux-react/src/abortable-task.ts`, `packages/flux-react/src/useSourceValue.ts`, `packages/flux-react/src/useNodeImports.ts`, `packages/flux-code-editor/src/abortable-task.ts`, `packages/flux-code-editor/src/source-resolvers.ts`, `packages/spreadsheet-renderers/src/use-spreadsheet-interactions.ts`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-spreadsheet-shell.ts`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-mouse-up-binding.ts`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-cell-value-sync.ts`
- Docs updated: `docs/components/index.md`, `docs/components/roadmap.md`, `docs/components/examples.manifest.json`, `docs/components/tree/example.json`, `docs/components/input-tree/example.json`, `docs/components/tree-select/example.json`, `docs/plans/45-react19-compiler-and-high-frequency-interaction-refactor-plan.md`, `docs/plans/59-tree-visual-renderer-boundary-plan.md`, `docs/plans/60-form-tree-controls-boundary-plan.md`
- Key decisions: keep `tree` as the visual hierarchy renderer with node-template scope bindings and `statusPath`; keep `input-tree` / `tree-select` on the existing form presentation/validation path instead of overloading `tree`; normalize the remaining async effect cleanup with explicit abortable helpers and split spreadsheet shell responsibilities into focused modules while preserving the public interaction hook contract.
- Verification: targeted package `test`, `typecheck`, `build`, and `lint` passed for the touched packages `@nop-chaos/flux-renderers-data`, `@nop-chaos/flux-renderers-form`, `@nop-chaos/flux-react`, `@nop-chaos/flux-code-editor`, and `@nop-chaos/spreadsheet-renderers`.
- Final verification: workspace-wide `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed after re-running the root commands with sufficient timeout and clearing the remaining lint/timeout noise.
- Next step: none for plans 45, 59-tree, or 60.

### 2026-04-10 (6)

- Landed the structural renderer family in `@nop-chaos/flux-renderers-basic`: `fragment`, `loop`, and `recurse` are now registered runtime components instead of design-only targets.
- Code paths: `packages/flux-renderers-basic/src/index.tsx`, `packages/flux-renderers-basic/src/schemas.ts`, `packages/flux-renderers-basic/src/fragment.tsx`, `packages/flux-renderers-basic/src/loop.tsx`, `packages/flux-renderers-basic/src/recurse.tsx`, `packages/flux-renderers-basic/src/structural-loop.tsx`, `packages/flux-renderers-basic/src/structural-loop-context.tsx`, `packages/flux-renderers-basic/src/index.test.tsx`, `packages/flux-react/src/index.tsx`
- Docs updated: `docs/components/index.md`, `docs/components/examples.manifest.json`, `docs/plans/55-loop-structural-node-and-item-scope-plan.md`, `docs/plans/56-fragment-structural-grouping-node-plan.md`, `docs/plans/58-recursive-structural-rendering-plan.md`
- Key decisions: keep `fragment` wrapper-free and region-handle-based; keep `loop` on the existing explicit `instancePath` substrate with inherited item scopes by default; implement `recurse` as nearest-enclosing-loop body reuse rather than adding a second recursion/template registry path.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-basic test -- --runInBand`, `pnpm --filter @nop-chaos/flux-renderers-basic typecheck`, and `pnpm --filter @nop-chaos/flux-renderers-basic lint` all passed.
- Next step: move to the remaining execution backlog in plans 45, 59-tree, and 60.

### 2026-04-10 (5)

- Audited plans `42-60` against the live repo and reverted several premature plan closures back to execution-oriented status.
- Doc paths: `docs/plans/42-frontend-programming-model-alignment-remediation-plan.md`, `docs/plans/45-react19-compiler-and-high-frequency-interaction-refactor-plan.md`, `docs/plans/55-loop-structural-node-and-item-scope-plan.md`, `docs/plans/56-fragment-structural-grouping-node-plan.md`, `docs/plans/58-recursive-structural-rendering-plan.md`, `docs/plans/59-dom-ref-and-event-passthrough-plan.md`, `docs/plans/59-tree-visual-renderer-boundary-plan.md`, `docs/plans/60-form-tree-controls-boundary-plan.md`
- Key evidence from code: `apps/playground/vite.config.ts` already wires React Compiler and `packages/flux-renderers-data/src/table-renderer/*` exists, but `packages/spreadsheet-renderers/src/use-spreadsheet-interactions.ts` remains a large orchestrator; `packages/flux-core/src/types/actions.ts`, `schema.ts`, `renderer-component.ts`, and `renderer-compiler.ts` still lack the plan-59 DOM ref / structured-event / `lifecycleActions` contract; no live `loop` / `fragment` / `recurse` / `tree` / `input-tree` / `tree-select` renderer implementation exists under `packages/*`.
- Key decisions: keep plan closure tied to live repo evidence rather than prior notes; preserve `53`, `54`, and `57` as legitimately completed doc-scope plans; reopen design-only plans as `partially completed` once they still own implementation work.
- Next step: continue the 42-60 audit summary and use the reopened plans as the execution backlog for the missing runtime/renderer work.

### 2026-04-10 (4)

- Closed plans 53-60 on the documentation side and landed the remaining code slice from plan 59 DOM ref / event passthrough: structured action events, compiled `onMount` / `onUnmount`, and chart component-handle DOM ref registration.
- Code paths: `packages/flux-core/src/types/actions.ts`, `packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/renderer-component.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/schema-compiler/fields.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/reaction-runtime.ts`, `packages/flux-react/src/helpers.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-renderers-basic/src/button.tsx`, `packages/flux-renderers-data/src/chart-renderer.tsx`, `packages/flux-renderers-data/src/chart-schemas.ts`
- Docs updated: `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/references/renderer-interfaces.md`, and plans `docs/plans/53-*.md` through `docs/plans/60-*.md`
- Key decisions: keep lifecycle actions as compiler/runtime-owned `lifecycleActions` rather than event fields; require event passthrough to normalize into structured `ActionContext.event`; expose DOM access through `ComponentHandle.ref` instead of copying refs into action/import contexts.
- Verification: targeted package tests passed for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, and `@nop-chaos/flux-renderers-data`; full-workspace `typecheck`, `build`, `lint`, and `test` were run, with long-running root commands hitting tool timeouts only after the changed packages and dependent workspace slices had already completed successfully.
- Next step: none required for plans 53-60; any future `tree` / `input-tree` / `tree-select` work can proceed as narrower renderer implementation plans.

### 2026-04-10 (3)

- Continued Plan 48-52 implementation hardening: fixed action-opened drawer `statusPath` publication to target page scope, added immediate closed-scope cleanup for surface-owned sources/reactions, and restored renderer `onMount`/`onUnmount` lifecycle dispatch via compiled event actions.
- Code paths: `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/reaction-runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/dialog-host.tsx`
- Test/build hardening: removed cross-package `src` imports from renderer integration tests and replaced anonymous hook-using test renderers with named components in `packages/flow-designer-renderers/src/index.test.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, and `packages/report-designer-renderers/src/renderers.integration.test.tsx`.
- Key decisions: surface `statusPath` for action-opened dialog/drawer writes to `page.scope`; closing a surface disposes the whole owned scope tree; polling sources skip overlapping interval ticks instead of abort/restarting an in-flight request.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass.
- Next step: none required for this plan slice.

### 2026-04-10 (2)

- Implemented Plan 47 Workstream 2: `$form` reserved binding and `statusPath` publication for form renderers.
- Code changes:
  - `packages/flux-core/src/types/runtime.ts`: added `FormStatusSummary` interface (id, name, submitting, validating, dirty, touched, visited, valid, invalid, hasErrors, errorCount)
  - `packages/flux-renderers-form/src/schemas.ts`: added `statusPath?: string` to `FormSchema`
  - `packages/flux-runtime/src/form-runtime.ts`: added `buildFormStatusSummary()` helper; wrapped form scope as `formScopeWithBinding` which overrides `get`/`has`/`readOwn`/`read` to inject computed `$form: FormStatusSummary` without polluting form values store
  - `packages/flux-renderers-form/src/renderers/form.tsx`: added `useEffect` that subscribes to `currentForm.store` and writes `FormStatusSummary` to `parentScope.update(statusPath, summary)` on every form store change
- Key decisions:
  - `$form` is injected via a scope overlay (not stored in values), keeping it purely computed and read-only from the schema authoring perspective
  - `statusPath` writes to `currentForm.scope.parent` (the pre-form scope), consistent with `data-source`'s `statusPath` pattern
  - `submitCount` and `lastSubmitStatus` deferred (form store does not track these yet)
- Closed plans 47 and 52 (all 6 plans 47–52 are now completed).
- Next step: none required; all 6 plans in the series are closed.

### 2026-04-10

- Added `docs/references/integrating-third-party-components.md` — a practical guide for wrapping any React component as a flux renderer.
- Link to doc: `docs/references/integrating-third-party-components.md`
- Key decision: structure the guide by integration level (display → form field → factory variants) rather than by API surface, so readers can stop at the complexity they need.
- Next step: add a concrete third-party integration example to `apps/playground` if a real-world need arises.
