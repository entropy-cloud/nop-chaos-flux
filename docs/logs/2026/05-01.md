### 2026-05-01

- Executed `docs/plans/165-reactive-subscription-precision-plan.md` (all 3 phases completed).
- Phase 1 (P0): `useSurfaceScopeSnapshot` in `packages/flux-react/src/dialog-host-surface.tsx` now accepts optional `paths: string[]` for selective scope path subscription, with `shallowEqual` comparison. Backward compatible when `paths` not provided. Tests: 4 new selective subscription tests added.
- Phase 2 (P1): Added `shallowEqual` equalityFn to `word-editor-page.tsx:96-110` `editorRuntime` selector and `use-table-controls.ts:26-30` `useTablePagination` scope selector. Split `useCrudRuntimeState` single selector into 5 fine-grained selectors (queryState, paginationState, sortState, filterState, selectedRowKeys) each with its own equalityFn. Stabilized `defaultQuery` via ref to avoid unnecessary useEffect re-runs. Wrapped `word-editor-page.tsx` hostScope data in `useMemo`.
- Phase 3 (P1): Wrapped `spreadsheet-renderers/src/page-renderer.tsx` hostScope data in `useMemo` with `[spreadsheet]` dep. Fixed `flow-designer-renderers/src/designer-context.ts` `useDesignerHostScope` useMemo deps from `[input]` to `[input.snapshot, input.config, input.core]`.
- Key decision: `crud-renderer-state.ts` `defaultQuery` stabilization uses a ref updated in a dedicated `useEffect` to satisfy `react-hooks/refs` lint rule while keeping the initialization effect deps stable.
- Verification: `pnpm typecheck`, `pnpm build`, targeted package lint (0 errors), and targeted package tests all pass.

- Fixed workspace compile regressions caused by stale test/runtime references and a playground import casing mismatch.
- `packages/flux-action-core/src/__tests__/action-dispatcher.test.ts`: updated mock `ScopeStore` and `CompiledRuntimeValue` fixtures to match the current `flux-core` contracts (`kind`/`isStatic`/`node` shape, no `getState`).
- `packages/ui/src/components/ui/separator.test.tsx`: replaced a nullable DOM query usage with an explicit non-null guard so `tsc` accepts the test.
- `apps/playground/src/main.tsx`: changed the root import from `./app` to `./App` to match the actual file name and remove the TS1149 casing conflict on Windows.
- Verification: `pnpm --filter @nop-chaos/flux-action-core typecheck`, `pnpm --filter @nop-chaos/flux-action-core build`, `pnpm --filter @nop-chaos/flux-action-core test`, `pnpm typecheck`, and `pnpm build` pass.
- Environment note: `pnpm lint` is still blocked before package linting because `scripts/check-react19-legacy-apis.mjs` cannot resolve the `typescript` package in the current environment.

### 2026-05-01

- Re-read Plans 165 and 166 under the stricter rule that they are already in execution and must not be rewritten just to absorb newly re-audited findings.
- Added three successor owner plans for the gaps that still remain outside the immutable 165/166 scopes:
  - `docs/plans/170-field-interaction-reactivity-and-async-safety-successor-plan.md`
  - `docs/plans/171-workbench-surface-and-package-boundary-successor-plan.md`
  - `docs/plans/172-spreadsheet-filter-semantics-convergence-plan.md`
- Key decision: do not widen in-flight plans after the fact. `Plan 165` keeps dialog/surface/hostScope reactive work, `Plan 166` keeps module-hygiene/designer-async/UI slices, and the newly confirmed residual surfaces get their own successor ownership.
- Next step: execute Plans 168-172 in dependency order, while leaving 165/166 unchanged except for normal execution updates.

### 2026-05-01

- Executed and closed `docs/plans/163-core-boundary-and-validation-owner-convergence-plan.md`.
- `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-core/src/types/runtime.ts`, and `packages/flux-core/package.json`: removed direct React type imports from the scoped public contracts and dropped `@types/react` from `flux-core`.
- `packages/flux-react/src/react-contracts.ts`, `packages/flux-react/src/index.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/dialog-host.tsx`, and `packages/flux-react/src/dialog-host-surface.tsx`: moved React-specialized renderer aliases into `flux-react`, wired caller-supplied `surfaceRuntime`, and published managed surface-root validation owners around managed dialog/drawer bodies.
- `packages/flux-runtime/src/runtime-owned-factories.ts`, `packages/flux-runtime/src/runtime-factory.ts`, and `packages/flux-runtime/src/surface-runtime.ts`: separated the generic `ValidationScopeRuntime` factory path from direct `FormRuntime` return, created per-surface validation owners, and fixed runtime disposal so page validation owners, page-store sync subscriptions, surface-owned validation owners, and tracked runtime-owned forms are released.
- Focused verification added/updated in `packages/flux-react/src/__tests__/schema-renderer.test.tsx`, `packages/flux-react/src/__tests__/dialog-host.test.tsx`, `packages/flux-react/src/__tests__/schema-renderer-validation-owner-boundary.test.tsx`, `packages/flux-runtime/src/__tests__/runtime-dialogs-scope.test.ts`, and `packages/flux-runtime/src/__tests__/runtime-factory-utils.test.ts`.
- Docs synced to final state only: `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/form-validation.md`, and `docs/architecture/surface-owner.md`.
- Verification: targeted `flux-react` and `flux-runtime` tests pass; `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` pass repo-wide for the live worktree baseline.
- Independent closure audit: fresh audit pass re-read the live code paths and Plan 163 exit criteria, confirmed `surfaceRuntime` is a real seam, confirmed managed surfaces now resolve a nearer validation owner than page root, and confirmed no remaining plan-owned work in scope.

### 2026-05-01

- Executed `docs/plans/164-adversarial-review-uncovered-findings-remediation-plan.md` Phases 1-4:
  - **Phase 1 (Scope DANGEROUS_KEYS filtering)**: Added `sanitizeSnapshot()` to `scope.ts`, applied to `merge()`, `replace()`, `update()` (dangerous path head check), `readVisible()`, and `createScopeStore()`. Extended formula scope proxy and adaptor scope view to block `constructor` and `prototype` in addition to `__proto__`. Tests: `scope-dangerous-keys.test.ts`.
  - **Phase 2 (Formula parser/evaluator depth limits + error visibility)**: Added `MAX_PARSER_DEPTH=256` and `MAX_EVAL_DEPTH=256` counters to `Parser` and `evaluateNode`. `compile-node.ts` now calls `reportDiagnostic` on parse failures. `formula-compiler.ts` expression `exec` throws on eval failure instead of returning `undefined as T`; template `exec` returns `[error]` for failed segments. Tests: parser depth, evaluator depth, reportDiagnostic, error propagation.
  - **Phase 3 (Form validation resilience)**: Replaced `Promise.all` with `Promise.allSettled` in `form-runtime-owner.ts` `validateForm()`. Tests: `form-validation-resilience.test.ts`.
  - **Phase 4 (Accessibility + i18n)**: Tree renderer: added `onKeyDown` handlers for expand/collapse trigger and expand-on-click node area. Table header: added `aria-label` to select-all checkbox, `role="button"` + `tabIndex` + `onKeyDown` to sortable headers. Table body: replaced hardcoded aria-labels with `t()` calls, added `aria-label` to row selection checkbox. Added i18n keys (`selectAll`, `selectRow`, `collapse`, `search`) to both zh-CN and en-US locales.
- Key files: `packages/flux-runtime/src/scope.ts`, `packages/flux-formula/src/parser.ts`, `packages/flux-formula/src/evaluator.ts`, `packages/flux-formula/src/compile/compile-node.ts`, `packages/flux-formula/src/compile/formula-compiler.ts`, `packages/flux-runtime/src/form-runtime-owner.ts`, `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/table-header-row.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx`, `packages/flux-i18n/src/locales/zh-CN.ts`, `packages/flux-i18n/src/locales/en-US.ts`
- Verification: typecheck passes (only pre-existing `flux-action-core` build failure), build passes (same), lint passes (warnings only), tests pass (3 pre-existing failures from prior uncommitted work, 0 new failures).

### 2026-05-01

- Re-audited whether retained findings from `docs/analysis/2026-05-01-deep-audit-full/` and findings from `docs/analysis/2026-05-01-adversarial-review*.md` are actually owned by Plans 162-166 using the stricter plan-authoring rule: coverage only counts when goals, scope, phase tasks, and exit criteria explicitly own the behavior.
- Added two new owner plans instead of widening `Plan 163` / `Plan 164` into omnibus catch-all plans:
  - `docs/plans/168-validation-and-built-in-form-targeting-semantics-convergence-plan.md`
  - `docs/plans/169-complex-renderer-contract-and-field-slot-convergence-plan.md`
- Key decision: keep `Plan 163` focused on `core -> react -> runtime` root-boundary and owner-lifecycle work, and keep `Plan 164` focused on safety/a11y/i18n slices; remaining validation/action semantics and complex renderer-contract drift each get their own owner plan.
- Key decision: do not treat `Plan 165` / `Plan 166` as reliable coverage baselines until they are re-audited against the live `deep-audit-full` snapshot; both still reference non-existent `deep-audit-full-2` sources and their current scope has drifted from the retained findings.
- Next step: either execute Plans 168-169 directly, or first rewrite Plans 165-166 so the remaining styling/reactivity/package-hygiene findings also have clean owners.

### 2026-05-01

- Completed 18-dimension deep audit (`docs/analysis/2026-05-01-deep-audit-full-2/`): 1 P0, 16 P1, 28 P2, 36 P3 findings across all 24 packages. Key results:
  - P0: `useSurfaceScopeSnapshot` in `dialog-host-surface.tsx` subscribes to full scope, causing all Dialog/Drawer to re-render on any scope change
  - Cross-package pattern: designer host pages (word-editor, spreadsheet, flow-designer) use inline object literals for hostScope, triggering `scope.replace` every render
  - 30+ sub-agents used across 5 audit batches with independent review passes
- Cross-compared deep audit findings with Plans 161-164 to identify coverage gaps
- Created `docs/plans/165-reactive-subscription-precision-plan.md` for P0 useSurfaceScopeSnapshot fix + P1 equalityFn/hostScope stability (dim 05)
- Created `docs/plans/166-module-hygiene-and-designer-async-cleanup-plan.md` for P1 entry file extraction (dim 02), P2 designer async cleanup (dim 06), P2 styling/UI compliance (dim 10/11)
- Remaining uncovered: dim 14 P1 test quality (shared state leakage, cross-cutting test files, 907 `as any`) — recommended as independent follow-up plan

### 2026-05-01

- Updated `docs/analysis/2026-05-01-live-design-and-architecture-audit.md` after a second live-code pass to sharpen two key boundaries: `flux-core` still exposes a React-specialized renderer surface, and validation owner support is currently page-root-first but still missing managed dialog/drawer surface-root owners.
- Added successor owner plan `docs/plans/163-core-boundary-and-validation-owner-convergence-plan.md` for the single root-boundary result surface that remains after the 2026-05-01 audit: core/React contract separation plus page/surface/form validation-owner convergence.
- Key decision: treat page-owned root validation as already-landed live baseline, but keep per-surface dialog/drawer validation owners explicitly as target work rather than misdocumenting them as current fact.
- Key decision: keep renderer-contract bypass and Flow/Report/Word host-contract drift out of Plan 163 as separate successor-plan surfaces, alongside lower-priority items such as `tag-list`/`key-value`/`array-editor` required semantics, CRUD query-form imperative bridge redesign, field chrome convergence, and report canvas hard-coded policy.
- Independent review outcome for Plan 163: after multiple fresh review passes, the plan is now `ready-with-small-edits` and effectively stabilized as a single `core -> react -> runtime` owner plan. The accepted narrowing was to keep only core/validation-owner boundary work in scope and promote the runtime-disposal leaks (`runtime.dispose()` page-sync unsubscribe cleanup, page validation-owner disposal, and tracked runtime-owned form disposal) into explicit Phase 2 scope instead of leaving them only in the adversarial review appendix.
- Evidence paths rechecked before drafting Plan 163: `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-runtime/src/runtime-owned-factories.ts`, `packages/flux-runtime/src/surface-runtime.ts`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/flow-designer-renderers/src/designer-manifest.ts`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/report-designer-manifest.ts`, `packages/word-editor-core/src/dataset-model.ts`, `packages/word-editor-renderers/src/editor-canvas.tsx`, `packages/word-editor-renderers/src/word-editor-page.tsx`.
- Final review note: the remaining nits were doc-consistency fixes only (`runtime.ts` explicitly added to Phase 1 targets, and this log entry updated to reflect the completed review loop). Plan 163 stays in `proposed` status until execution begins.

### 2026-05-01

- Added remediation owner plan `docs/plans/162-designer-page-and-report-selection-audit-remediation-plan.md` to close the small deep-audit slice around flow designer resolved props and report selection projection.
- `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-renderers/src/schemas.ts`, and `packages/flow-designer-renderers/src/index.tsx`: moved `config` / `document` / `treeDocument` / `statusPath` to the resolved `props.props` channel, removed tree-mode render-phase state sync, and aligned schema/prop contracts with `docs/components/designer-page/design.md` and `docs/architecture/renderer-runtime.md`.
- `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`: fixed spreadsheet-to-report target mirroring so `selection.kind === 'none'` clears only spreadsheet-driven targets; mirror logic now keys off `ssSnapshot.selection` instead of a stale derived interaction cell.
- Regression coverage: `packages/flow-designer-renderers/src/designer-page.tree.test.tsx` now covers resolved tree props, and `packages/report-designer-renderers/src/renderers.integration.test.tsx` now covers selection clear plus current report-designer baseline behavior.
- Docs synced: `docs/architecture/frontend-baseline.md` restored `flux-compiler` and `flux-action-core`; `docs/plans/143-unit-test-coverage-80-percent-target-plan.md` now matches its completed status wording.
- Verification so far: `pnpm --filter @nop-chaos/flow-designer-renderers test -- designer-page.tree.test.tsx designer-page-shell.test.tsx` and `pnpm --filter @nop-chaos/report-designer-renderers test -- renderers.integration.test.tsx` pass.
- Next step: run `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`, then do a closure audit before marking Plan 162 complete.

### 2026-05-01

- Removed legacy action payload path (`extractLegacyPayload`), legacy `dataPath` on actions, and legacy `cacheTTL`/`cacheKey`/`dedupStrategy` on ApiSchema — all unified to canonical paths

**Legacy action payload removal** (`action.api`/`dialog`/`drawer`/`value`/`values` → `action.args`)

- `flux-compiler/src/action-compiler.ts`: deleted `extractLegacyPayload()` (60 lines), simplified `compilePayload()` to only read `action.args`
- `flux-core/src/types/actions.ts`: removed `dataPath` from `ActionShapeFields` and `CompiledActionTargeting`
- `flux-runtime/src/runtime-action-helpers.ts`: removed `dataPath` response-path write logic
- `flux-runtime/src/async-data/request-runtime.ts`: removed `applyResponseDataPath()` utility
- `flux-compiler/src/schema-compiler/shape-validation-rules.ts`: removed legacy `action.api` shape validation
- `apps/playground/src/pages/fluxBasicPageSchema.json`: migrated 4 actions from `api:` to `args:`
- Tests updated: removed 7 legacy payload tests, 2 legacy shape validation tests, 1 dataPath test

**Legacy cache/dedup field removal** (`ApiSchema.cacheTTL`/`cacheKey`/`dedupStrategy` → `control.{dedup,cacheTTL,cacheKey}`)

- `flux-core/src/types/schema.ts`: removed legacy fields from `ApiSchema` and `ExecutableApiRequest`
- `flux-core/src/types/compilation.ts`: removed legacy fields from `CompiledApiConfig`
- `flux-compiler/src/source-compiler.ts`: removed legacy field copy-through
- `flux-runtime/src/async-data/data-source-runtime-utils.ts`: removed legacy field copy-through
- `flux-runtime/src/async-data/api-data-source-controller.ts`: removed `compiledApi.dedupStrategy`/`cacheTTL` fallbacks, reads `control.*` only
- `flux-runtime/src/async-data/request-runtime.ts`: removed `api.dedupStrategy` fallback from `resolveRequestDedup`
- `flux-runtime/src/async-data/api-cache.ts`: `resolveCacheKey` param type changed from `Pick<ApiSchema, ...>` to inline `{ cacheTTL?, cacheKey? }`
- Tests updated: 19 test instances migrated from top-level to `control: { ... }`

- Verification: `pnpm typecheck` all 25 packages pass, flux-compiler 280/280, flux-runtime 685/685, flux-react 267/267, flux-core 119/119

- Also earlier today: executed deprecated feature cleanup audit using `docs/skills/deprecated-feature-cleanup-prompt.md`
- Cleaned up 3 deprecated features with docs+code dual confirmation:

**1. `RenderRegionHandle.instantiate()` — removed**

- `flux-core/src/types/renderer-hooks.ts:82-86`: removed `instantiate()` method from `RenderRegionHandle` interface
- `flux-react/src/node-renderer.tsx:234`: removed `instantiate` property from region handle construction
- `flux-react/src/frame-slot-meta.test.tsx:181`: removed `instantiate` from test mock
- Docs updated: `docs/architecture/renderer-runtime.md`, `docs/architecture/scoped-render-slots.md`, `docs/architecture/field-metadata-slot-modeling.md`

**2. `RenderFragmentOptions.data` — removed**

- `flux-core/src/types/renderer-hooks.ts:14-19`: removed `data` field from `RenderFragmentOptions` interface
- `flux-react/src/node-renderer.tsx:197`: removed `data` fallback, now reads `options?.bindings` directly
- `flux-react/src/render-nodes.tsx:193`: removed `data` fallback
- `flux-renderers-basic/src/fragment.tsx:10`: changed `data:` to `bindings:`
- `flux-react/src/test-support-runtime.tsx:177,198`: changed `data:` to `bindings:` in test helpers

**3. `ReportDesignerHostData.selection`/`target` compat aliases — removed**

- `packages/report-designer-renderers/src/host-data.ts:70-73`: removed `selection` and `target` fields from type
- `packages/report-designer-renderers/src/host-data.ts:111-112`: removed alias assignments in `createHostData()`
- Verified zero consumers via grep before removal

- Also drafted `docs/skills/code-quality-audit-prompt.md` and `docs/skills/deprecated-feature-cleanup-prompt.md` as reusable skill prompts
- Also updated `docs/skills/code-refactor-discovery-prompt.md` to add Dimension 5.5 (code-block duplication) and Dimension 5.6 (over-splitting) — gaps identified when comparing audit results with Plan 158 findings
- Also drafted `docs/plans/159-code-refactor-discovery-remediation-plan.md` covering issues not in Plan 158

- Verification: `pnpm typecheck` passes, `pnpm --filter @nop-chaos/flux-react test` 267/267 pass, `pnpm --filter @nop-chaos/flux-core test` 119/119 pass

### Items evaluated but NOT removed (with reasoning)

- `extractLegacyPayload` in `action-compiler.ts`: Plan 159 P6.1 scheduled for evaluation — still in use by compiled action paths
- `schema.ts` legacy compatibility carriers (`cacheTTL`/`cacheKey`/`dedupStrategy`): Plan 159 explicitly deferred to next major version
- `closeDialog`/`closeDrawer` compat aliases: docs say "remain compatibility aliases" — still in active use
- `dataPath` on `DataSourceSchema`: docs mark as legacy but runtime still reads it — migration incomplete
