# Development Log - 2026-06-25

### 2026-06-25 (ui 包 silent-correctness + a11y + i18n 桥契约 — plan 0630-2 P0-2/P1-2/P1-3/P1-4/P1-5/P1-8/S-5/S-6)

- Executed plan `docs/plans/2026-06-25-0630-2-ui-correctness-accessibility-i18n.md`：收口 `packages/ui`（零 `@nop-chaos/*` 依赖的 shadcn 基座）由 2026-06-24 components 审计确认的 silent-correctness defect、a11y 缺陷与 i18n 注入桥契约裁定。四 Phase 全部落地。
- **Phase 1（P0-2/P1-2 silent correctness）**：(P0-2) `sonner.tsx` 把末尾 `{...props}` 移到 `<Sonner>` 最前，再用 `cn('toaster group', props.className)` 合并 className、`props.icons ?? defaultIcons`、`{...zIndex, ...props.style}`、`props.toastOptions ?? defaultToastOptions` 兜底——修掉 `<Toaster />` 无 props 时 `{...props}`（含 undefined）覆盖 zIndex/icons/className/toastOptions 的自毁；(P1-2) `use-dialog-drag.ts` unmount cleanup 补 `removeProperty('user-select')/-webkit-user-select`（与 `stopDrag` 同逻辑），消除拖拽中途关闭 dialog 致整文档不可选的泄漏。新增 `sonner.test.tsx`（props 捕获 mock 断言 zIndex/className 合并/icons 兜底/调用方覆盖保留 zIndex）+ `use-dialog-drag.test.tsx`（mid-drag unmount 后 `body.style.userSelect` 复原为 `''`）。
- **Phase 2（P1-3/P1-4/P1-5 a11y）**：新增共享 `lib/focus-target.ts` `isEditableTarget`（input/textarea/select/contenteditable/role textbox|slider|spinbutton）。(P1-3) `card.tsx` 传 `onClick` 时补 `role="button"`/`tabIndex={0}`/`onKeyDown`（Enter/Space → preventDefault + `currentTarget.click()`，转发调用方 onKeyDown）；(P1-4) `carousel.tsx` ArrowLeft/Right 仅当 target 非 editable 才拦截/滚动；(P1-5) `sidebar-context.tsx` Cmd/Ctrl+B window 监听加 target 过滤。新增/扩展 `card.test.tsx`（Enter+Space 各激活一次）、`carousel.test.tsx`（input 内方向键不 scrollPrev/Next，slide div 仍滚动）、`sidebar-context.test.tsx`（input 内 Cmd+B 不 preventDefault/onOpenChange，普通 div 仍 toggle）。
- **Phase 3（P1-8 i18n 桥契约裁定 = 方案 A 接线）**：**Re-audit live repo 发现桥已于 2026-05-19（commit `3924be5d`）接线**——`flux-i18n` 的 `initFluxI18n()` 经 `bindUiI18n(instance)` 写入与 `ui` 共享的 `Symbol.for('nop.ui.i18nBridge')` 全局槽，`ui` 的 `t()` 据此走 flux-i18n；`apps/playground/src/main.tsx` 已调用 `initFluxI18n()`。审计 P1-8 的 grep 仅查 `setI18nGetter`（确实零生产调用方）而漏掉了 `bindUiI18n`，故误判 18 条 chrome 串“permanently English”。证据：`flux-i18n/src/i18n-contract.test.ts` 新增 zh-CN 用例断言 `ui` 的 `t('flux.carousel.label')==='轮播图'`、`t('flux.dialog.close')==='关闭'`（值不在 ui 内置英文 map，只有桥真正路由才成立），直接证伪。owner-doc：`docs/architecture/frontend-baseline.md` 新增“UI i18n Bridge Contract”章节记录真实接线机制 + host 义务（启动调 `initFluxI18n`）+ `flux-bundle` 刻意不持 i18n init 的边界，消除 doc↔code 矛盾。架构上不把 `flux-i18n` 耦合进 `flux-bundle`（i18n init 属 app 入口职责）。
- **Phase 4（S-5/S-6 selector/key hygiene）**：(S-5) `field.tsx` `FieldTitle` data-slot `field-label`→`field-title`（`FieldLabel` 保留 `field-label`），`fieldVariants` horizontal/responsive 选择器同步追加 `*:data-[slot=field-title]:flex-auto` 保布局；(S-6) `chart.tsx` 新增 `payloadItemKey(item)`（复合 `dataKey|name|color|type`，过滤 function-typed dataKey），`ChartTooltipContent`/`ChartLegendContent` React key 改用它替掉裸 `item.value`/`key`（消除同值/同名碰撞致行消失）。**不用数组 index**——仓库 `react/no-array-index-key` 为不可降级硬约束（首轮 index 方案被 lint 拦下后改为复合 key）。新增 `field.test.tsx` distinct-slot 断言 + `chart.test.tsx` 两条同 value payload 仍渲染 2 行断言（recharts ResponsiveContainer 在 happy-dom 0x0 不渲染，测试 stub 为直渲 children）。
- 验证（full-green，未跑 e2e——不在本 plan scope/gates）：受影响包 `pnpm --filter @nop-chaos/ui test`(37 files/132) + `flux-i18n test`(2 files/25 含 zh-CN proof)；closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，0 errors) + `pnpm test`(55 packages 全过)。注：closure re-verify 暴露并修复了 ui build（`tsconfig.build.json` 比 typecheck 更严：recharts `dataKey` 可为 function，`payloadItemKey` 参数放宽为 `unknown` 并过滤 function）。
- Roadmap ❌→✅：N/A——本 plan findings（P0-2 等）来自 components 审计，audit-level `Audit Status` 已 `closed`、无 per-finding ❌ roadmap 条目可翻转；状态由本 daily log + plan Closure 记录。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），verdict `approved`（逐条 live file:line 证据 + 复跑 ui/flux-i18n test 绿 + 确认 P1-8 为真实 fix 非 hand-wave），见 plan `Closure Audit Evidence`。

### 2026-06-25 (renderer correctness + selection/key invariants + contract honesty guard — plan 0630-1 Ph3-6)

- Executed plan `docs/plans/2026-06-25-0630-1-renderer-correctness-and-contract-guard.md`（Phases 1–2 前序 run 已完成；本轮完成 Ph3–6）。收口 `flux-renderers-data`/`form-advanced`/`basic`/`content`/`layout`/`form` 六个 renderer 包由 2026-06-24 两次 components 审计确认的 P0/P1/P2 silent-correctness defect 与 P3 hygiene 漂移；落地 selection-state-vs-visible-data 不变量与 row-drag/chart/projected-form 行为修复。
- **Phase 3（P0-4/P1-6/P1-7 selection & key invariants）**：(P0-4) `list-renderer.tsx` fallback key 用 global index `(currentPage-1)*pageSize+index`，消除跨页 `item:N` 碰撞（已有 Proof）；(P1-6) `use-table-selection.ts` 改收 `filteredData`（filter+sorted，未分页）使 select-all/allSelected 基于可见行；(P1-7) render-time 交集 prune `localSelectedRowKeys`（尊重 `keepOnPageChange`，无 setState-in-effect）。新增 `table-selection-invariants.test.tsx`（filter select-all / 行删除 prune / keepOnPageChange 保留 / filtered-out prune）。
- **Phase 4（P0-1/P0-3/C-07/C-10/S-1/S-2/C-30/pagination drift）**：(P0-1) 新增 `orderOwnership`/`orderStatePath` schema+definition；`use-row-drag-sort.ts` 加 `localOrderKeys` 内部持久化 + `orderedRows`/`orderedKeys` + `controlled` 分支 + dev warn；`table-renderer.tsx` 改用专用 order 路径 + `displayData` 线程；(P0-3) chart 用 callback-ref + `chartNode` state 使 ResizeObserver 在异步 canvas 挂载后附加，`handleResize` 实装 measure；(C-07) query 失败改 `flux.common.queryFailed` + `cause`；(C-10) `props.id`；(S-1) per-sentinel observer registry；(S-2) memo `toTreeNodes` 输出（下游走 React Compiler）；(C-30) `bg-warning/30`；(pagination) 移除未实现 `pageOwnership`/`pageStatePath`。新增 `table-row-drag-sort-persist.test.tsx` + chart async Proof。C-03（CRUD carrier recompilation）adjudicated-deferred（option-2 reactive-carrier 是非平凡重构；remount-key 是 documented workaround，非 silent defect）。
- **Phase 5（P0-5/P0-6/P1-1/S-3/S-4/C-01/C-06/C-08/C-09/C-18/C-19/C-23/C-29）**：(P0-5) projected-form `validateAt/Field/Subtree/All` 转发 `options`(signal)；`clearErrors(undefined)` 改 render-time 枚举 projected fieldStates 只清 prefix 子树；(P0-6) array-field scope-only Add/Remove 写 `parentScope.update`；(P1-1) condition-builder `useCallback()()` → `useMemo`；(S-3) key-value `validateSubtree(name,'change')`；(S-4) combo 删未读 `multiple`、transfer 增 `searchOnly`；(C-01) `projected-scope.ts` 改从 `flux-react/unstable` re-export + `flux-runtime` 移 devDeps；(C-08) `registerRendererDefinitions`（`as RendererDefinition[]` cast 保留——承担泛型擦除）；(C-09) 删零消费者 helper/barrel；(C-18) transfer→`<Label>`+`<Checkbox>`；(C-06/C-29) 删 dead BEM；(C-19) upload `<Button>`；(C-23) test `beforeEach`。新增 `projected-form-runtime-p0-5.test.ts`（signal 转发 + clearErrors 局部）。C-18 picker / P0-6 隔离测试 adjudicated-deferred（Base UI Checkbox 双元素 + aria-labelledby 与 listbox getByLabelText 冲突 / array-field FieldFrame 无 form 不渲染）。
- **Phase 6（C-02/C-11/C-16/C-19/C-22/C-31/C-32/C-33/C-35）**：dynamic 合并重复 evaluate（C-02）；page 用 compiled region handle 内容感知检测 aside（C-11）；删 `classNames` 重复（C-16）；qrcode dev `console.warn`（C-22）；删 `nop-form-body--inline`（C-31）；textarea/markdown/wizard/upload raw `<button>`→`<Button>`（C-32/C-33/C-19）；test `beforeEach`（C-35）。C-13/14/15/17/28/34/S-7 adjudicated-deferred（token 体系缺 success/info/error 属 Plan 2；CSS auto-load 涉构建管线；field-control slot 约定 + React Compiler memo broad sweep 各归专项）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：受影响包 `pnpm test` 全过（data 546 / form-advanced 839 / form 499 / basic 388 / content 158 / layout 62）；closure gates `pnpm typecheck`(55/55) + 各包 `build`/`lint`(0 errors) + `pnpm check:oversized-code-files`(tree/table-body 不再 ERROR) + `pnpm check:workspace-manifest-deps`（C-01 通过）。`pnpm install --no-frozen-lockfile` 更新 lockfile（flux-runtime dep 迁移）。
- 源审计关闭：`docs/audits/2026-06-24-2213-open-audit-components.md` + `multi-audit-components.md` `Audit Status: planned → closed`（deferred 项已记入 plan `Deferred But Adjudicated` + Follow-up；next OPEN_AUDIT 会 re-discover 不足的 fix）。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），见 plan `Closure Audit Evidence`。
- Closure re-verification 修复：复跑 `pnpm lint` 暴露执行期被 stale eslint cache 掩盖的 1 个 `react-hooks/refs` error——`array-field.tsx:353` `itemsRef.current = items`（render 期写 ref，P0-6 else 分支读取的 latest-items ref）。改为 `useEffect` 同步（event handler 在 commit 后触发，`itemsRef.current` 始终为最新已提交数组，行为不变）。修复后 `pnpm lint`(29/29，0 errors，仅余 tree-option-list pre-existing `useVirtualizer` warning) + `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm test`(55/55，form-advanced 839 含 array-field-runtime 24) 全绿。

### 2026-06-25 (NEW 包 advertised-but-dead 契约 + 生命周期诚实 — open-audit F2/F3/F4/S2/S3)

- Executed plan `docs/plans/2026-06-25-0510-2-new-package-advertised-contract-and-lifecycle-honesty-plan.md`：收口 open-audit（`docs/audits/2026-06-24-2213-open-audit-components.md`）的 [F2]/[F3]/[F4]/[S2]/[S3]——NEW 包（`flux-renderers-content` + `flux-renderers-layout`）发布了渲染器本体从不接线的契约 / 生命周期与 payload 不诚实。三 WS 全部落地。
- **WS-A（F2+F4 ownership 词表统一，per-component Decision 全选 (B) 移除/文档化）**：canonical duo = steps/collapse（`valueOwnership`/`valueStatePath` 真正实现，**未动**）。
  - **cards（F2）**：删 `CardsSchema` + definition 的死契约 `selectionOwnership`/`selectionStatePath`/`onPageChange`（renderer 0 引用、selection 本地 `useState`、无分页逻辑）；保留实现的 `selectionMode`/`onSelectionChange`/`onItemClick`。
  - **wizard（F4）**：删 `WizardSchema` + definition 的 `valueOwnership`/`valueStatePath`（renderer 0 引用，`value`/`defaultValue` 仅作 local-controlled 种子）；保留实现的 `statusPath`（`useStatusPathPublication`）。与 `{1}`（disabled 解包）同文件不同关注点，无冲突。
  - **button-group（F4）**：`value` 非响应式种子——文档化为 seed-only（schema JSDoc + propContract description 由 "Currently selected" 改为 "Initial ... seed only ... non-reactive"），不声明 ownership 字段。
  - Proof：cards/wizard 各加 grep-style 契约↔renderer 一致性断言（死字段从 schema+definition+renderer 三处均消失、实现项保留）；button-group 断言无 ownership 字段 + value 契约含 "seed" 不含 "currently selected" + 无 `useScopeSelector`。
- **WS-B（F3 qrcode onLoadError 事件契约）**：`QrCodeSchema` 加 `onLoadError?:ActionSchema`；definition 加 `{key:'onLoadError',kind:'event'}`（对齐 image/audio/video 的 `:147/:439/:457`）；`qrcode.tsx` catch 内 `void onLoadError?.()` 触发事件（**只加事件触发**，保留 `void error`，诊断 `console.warn` 归 450/C-22 不在此加）。Proof：`vi.mock('qrcode')` controllable（默认透传真实 `toCanvas`，flip `failGeneration` 使其 reject）→ 断言 `onLoadError` 调用 1 次 + `data-state="error"` fallback。
- **WS-C（S2/S3 生命周期与 payload）**：(S3 Decision A) carousel `onChange` payload 单一规范键 `activeIndex`，去冗余 `index`（renderer/已知消费者 0 引用 `index`）；(S2) json-view 复制定时器迁 ref + unmount `clearTimeout` + 重复制前清旧 timer（消除 pending `setCopied` / 不叠加）。Proof：carousel 源码 payload 形状断言（含 `activeIndex` 不含 `index:`）；json-view `setTimeout`/`clearTimeout` spy 断言 unmount 清掉 copy-reset timer id。
- **owner-doc 同步**：cards（§4/§5/§7/§8 + 新 §8.1 死契约裁定）、wizard（§4 schema block 去 ownership + §6.1 local-controlled + 新 §10.1 死字段裁定）、button-group（§7 seed-only 裁定）、qrcode（§8 onLoadError 对齐家族）、carousel（§8 规范键 Decision A）、json-view（§11.1 定时器生命周期）6 份 design.md 反映裁定。
- 验证（full-green）：`pnpm --filter @nop-chaos/flux-renderers-content test`(20 files/156) + `flux-renderers-layout test`(8 files/60) focused 先绿；closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(55/55 全过)。注：本轮 unit-green，未跑 e2e（不在本 plan scope/gates）。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），见 plan `Closure Audit Evidence`。

### 2026-06-25 (wizard disabled 门禁修复 + `__nopPreserveLiteral` 共享解包 helper — open-audit F1)

- Executed plan `docs/plans/2026-06-25-0510-1-wizard-boolean-literal-normalization-correctness-plan.md`：收口 open-audit HEADLINE finding [F1]。wizard `step.disabled` 经编译器 boolean normalization（`__nopPreserveLiteral` 包裹）后被渲染器静默忽略——`isStepDisabled` 只比裸 `true/'true'/1`，匹配不到编译产出的 `{__nopPreserveLiteral:true, value:true}` 对象，导致声明的 `disabled` 步骤仍可进入/可点/计入 `canGoNext`（多步提交流程门禁 correctness 缺陷）。根因：4 个 renderer 侧消费者里 wizard 唯一忘记解包，且无共享 helper、无 compile-through 契约测试。
- **Phase 1 Decision + Proof（TDD 红）**：(Decision 1) 共享 helper 落点选 **`@nop-chaos/flux-react`**——4 个消费者均依赖它、且有 `resolve-gap.ts` 非-hook 纯工具先例。(Decision 2) 死字段 `optional` 选**移除**——wizard 0 处读取、`computeCanGoTo` 未实现跳过语义、仓库内无作者使用，v1 无 compat 负担。新增两条 Proof 先红：`__tests__/wizard-boolean-literal-compile-through.test.ts`（直接调生产 `layoutRendererDefinitions` 的 `deepFields.normalize`，断言 envelope 形态 + `isStepDisabled` 解包）；`wizard-renderer.test.tsx` 增 disabled-step 组件用例（经 `createLayoutSchemaRenderer()` 走生产 `wizardStepsNormalize`）。运行确认两条目标断言先红（`isStepDisabled`→false、`data-disabled`→null；envelope 形态断言已绿证明编译契约成立）。
- **Phase 2 修复 + helper**：新增 `packages/flux-react/src/preserve-literal.ts` 导出 `unwrapPreservedLiteral(value):unknown`（通用核心）+ `unwrapBooleanLiteral(value):boolean`（boolean 便利，兼容裸 `true/'true'/1` 与 `value:false`），从 flux-react 入口导出。`wizard-renderer.tsx` `isStepDisabled` 改用 `unwrapBooleanLiteral(step.disabled)`（并 export 供契约测试）；核对 `isStepVisible`——`visible` 不在 booleanKeys、走表达式路径，无需改。死字段 `optional` 移除：`schemas.ts`(`WizardStepSchema.optional`)、`layout-renderer-definitions.ts`(`booleanKeys`+normalize loop+描述)、`test-support.tsx`(镜像)。两条 Proof 转绿。
- **Phase 3 兄弟消费者收敛**：`collapse-renderer.tsx` `isItemDisabled`、`tabs.tsx` `isTabDisabled` 改用 `unwrapBooleanLiteral`（行为零变化）；`variant-field-matching.ts` `unwrapPreservedMatchWhen` 改用通用核心 `unwrapPreservedLiteral`（**该消费者是 string 解包 `match.when` 而非 boolean**，故走通用核心——校正了 plan 原把它列为 boolean 消费者的不精确）。grep 确认 renderer 侧不再有任何内联 `__nopPreserveLiteral` 解包实现（剩余引用=helper 自身 / 编译层消费者 `compile-node.ts:50`(语义不同，按设计排除) / envelope 生产者 / 测试断言）。
- 验证（unit-green）：`pnpm --filter @nop-chaos/flux-renderers-layout test`(58/58，含新增 2 Proof)、`flux-renderers-basic`(386/386 含 tabs)、`flux-renderers-form-advanced`(834/834 含 variant-field)；closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，0 errors；1 pre-existing unrelated `useVirtualizer` warning) + `pnpm test`(55 packages 全过)。注：本轮仅 unit-green，未跑 e2e（不在本 plan scope/gates）。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），见 plan `Closure Audit Evidence`。

### 2026-06-25 (D1a designer-node-card / designer-edge-row 注册落地)

- Executed plan `docs/plans/2026-06-25-0307-1-d1a-designer-node-card-edge-row-registration-plan.md`：把 schema 已声明但**刻意未注册**的两个 designer 内部 renderer——`designer-node-card`、`designer-edge-row`——收口为已注册、有 runtime 消费、有 focused 验证的落地契约。design.md §12 两处 host-bridge-stability deferral 已翻转。主组件 roadmap `todo`/`deferred` 队列清空。
- **Phase 1 注册 + 共享 id 解析层**：新建 `packages/flow-designer-renderers/src/designer-summary-helpers.ts`（纯函数 `resolveNodeSummary`/`resolveEdgeSummary`——从 `DesignerSnapshot` 按 id 解析 node/edge 摘要，返回归一化的 `{id,type,position,data,selected,active}` / `{id,type,source,target,sourcePort,targetPort,data,selected,active}`，缺失/空 id 返回 `undefined`，无 React 依赖便于单测）。新建 `designer-node-card.tsx` / `designer-edge-row.tsx`（`useDesignerContext()` 越界保护 + `useDesignerSnapshotSelector` 按 id 订阅 + `getRootMetaProps` 一致化 + 缺失 id 受控降级）。`renderer-definitions.ts` 注册两个 type（lazy component + `fields:[{key:'nodeId'|'edgeId', kind:'prop'}]`，`'prop'` 与 `designer-field` 的 `name` 同模式）。capability contract：node-card 点击派发 `selectNode`、edge-row 点击派发 `selectEdge`（复用 `dispatch`）。
- **Phase 2 视觉 + command 交互**：node-card 渲染 type label（`useNodeTypeConfig`）+ position（i18n `flux.flowDesigner.nodeCard.position`）+ 选中态（snapshot `selection` → `data-active`/`data-selected`）+ `nop-designer-node-card` marker。edge-row 渲染 source→target（经 node id 查 type label 或直显 id）+ `nop-designer-edge-row` marker + `selectEdge` command。缺失/空 id 三个降级路径（node-card-missing / edge-row-missing / node-card-no-id）均降级为空态 marker（`data-empty="true"` + `aria-hidden="true"`，不抛错）。
- **Phase 3 playground + e2e + owner-doc**：`apps/playground/src/schemas/designer-summary-demo-schema.json`（新建 demo schema）+ `flow-designer-page.tsx` 增「节点/边摘要」tab（复用现有 flow-designer 路由，不新建独立路由）；demo inspector region 组合 4 个 node-card（start/task/end/missing）+ 3 个 edge-row（2 real + 1 missing），点击卡片派发 command 并经 `useDesignerSnapshotSelector` 反映 active/selected 派生态。e2e `tests/e2e/designer-summary-renderers.spec.ts`（3 用例：marker 渲染 + selectNode click + selectEdge click，非截图，用 `page.locator`/`getByTestId`/`toHaveAttribute` 程序化断言）。owner-doc：两份 design.md §3 翻转「已注册」、§12 deferral 翻转并指向本 plan；`amis-baseline-matrix.md` 两个 renderer `declaredButUnregistered` → `runtime`；`examples.manifest.json` `declaredButUnregistered: []`、runtime 列表补两 type；`roadmap.md` Phase Status D1a `planned` → `done`、核心缺口 deferred 叙述收口。
- **关键裁定**：(1) host bridge deferral 解除证据——`HostProjectionContract`（`designer-host-projection.ts:185-187` + `designer-manifest.ts:441` + `designer-context.ts:132`）已稳定，投影字段覆盖 node/edge 摘要。(2) capability 命名——design.md §8 提「select/focus」，实际 designer command 集只有 `selectNode`/`selectEdge`（无独立 `focusNode`/`focusEdge`），故 focus 语义映射到 select command（选中即聚焦）。(3) playground demo 选择——不新建独立路由（plan Non-Goals），而是在现有 flow-designer 页加 tab；不修改既有 workflow/dingtalk/action-flow demo（零回归）。
- 验证（focused）：`pnpm --filter @nop-chaos/flow-designer-renderers typecheck` + `lint` + `test`(28 files / 180 tests 全绿，含新增 `designer-summary-helpers.test.ts` 7 + `designer-node-card-edge-row.test.tsx` 9)；`pnpm --filter @nop-chaos/flux-playground typecheck` + `test`(88 全绿)；`playwright test designer-summary-renderers`(3 passed) + `playwright test flow-designer`(33 passed / 3 pre-existing skipped，无回归)。

### 2026-06-25 (CRUD cards/list 渲染模式 — E1d deferred 收口)

- Executed plan `docs/plans/2026-06-24-2358-2-crud-cards-list-rendering-mode-plan.md`：收口 E1d 显式 deferred 的「CRUD cards/list 模式」。CRUD 不再硬编码 `<TableRenderer>` 为唯一行渲染载体；按 schema `listMode` 选择 `table`（缺省，零回归）/ `cards` / `list`。依赖（W1c `list` / W2a `cards` 已 done）均已满足。
- **Phase 1 契约**：`packages/flux-renderers-data/src/crud-schema.ts` CrudSchema 新增 `listMode?: 'table'|'cards'|'list'` + `card?`/`item?`（SchemaInput 行模板）；`normalizeCrudSchema` 缺省 `'table'`。`crud-renderer-definition.ts` 补 `listMode` propContract + `card`/`item` region 字段（`params:['item','index']`）+ `toggleSelection` capability contract。`crud/design.md` §2 L41 翻转为「实现」、新增 §4.1.1 carrier 边界表（selection/pagination 所有权裁定）。
- **Phase 2 实现**：`crud-renderer.tsx` 增 `listMode` 分支——非 table 模式构建嵌套 carrier schema（`{type:'list'|'cards', items, selectionMode:'none', keyField, ...}`）经 `props.helpers.render` 按 type 解析渲染（**不与 `flux-renderers-content` 产生包级硬依赖**——`cards` renderer 在 sibling 包，无法直接 import；type 解析是 idiomatic 方案）。cards 模式 CRUD 预切片当前页喂 carrier `items` + footer 渲染分页（`PaginationPrevious`/`PaginationNext` 绑定 `paginationStatePath`）；list 模式传全量 `filteredRows` + `paginationOwnership:'scope'` 复用 list runtime 分页。selection 在所有非 table 模式由 CRUD 自持：carrier `selectionMode:'none'`，模板 `button` 调用新增的 CRUD `toggleSelection` capability 写同一 `selectionStatePath`。`useCrudHandle` 扩展第 5 参 `toggleSelection`。
- **关键发现/裁定（执行期）**：(1) carrier 经 `helpers.render` 嵌套，其 item 作用域 `setValue` 只落本地 store（不回传 CRUD scope）→ selection 写必须用 CRUD 句柄 capability（非 setValue）。(2) carrier item param 经 `$slot.item`/`$slot.index` 访问（非裸 `item`）。(3) **React Compiler 交互**：carrier 子树（helpers.render RenderNodes）被 compiler memoize，pagination/selection 变更时不重渲染 → 用 `key={listMode:currentPage:selectedRowKeys.length}` 在 carrier wrapper div 强制 remount（代码注释标注 + 显式 follow-up：investigate reactive 非重渲染更新）。模板 selection 显示态读真实 `selectionStatePath`（store-backed reactive），非 `$crud.selectedRowKeys` overlay（后者只对 CRUD 直接子节点 reactive）。
- **Phase 3 交付**：`crud-lab-page.tsx` 增「CRUD cards mode」/「CRUD list mode」scenario（25 条记录、selection 按钮、footer `$crud.selectionCount`）；`multi-scenario-lab-page.tsx` 补 `registerContentRenderers`（cards carrier 在 lab runtime 可解析）。e2e `tests/e2e/component-lab/crud-list-mode.spec.ts`（2 用例：cards + list）。E1d plan Deferred 补 Closure Note。
- **owner docs 同步**：`crud/design.md` §2/§4.1/§6.1；`existing-components-improvement-analysis.md` E1d 行 + `-detail.md` cards/list gap 翻转为已实现。
- 验证（full-green）：`pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(全 packages，flux-renderers-data 532 含新增 6) + `playwright test`(528 passed / 46 pre-existing skipped / 0 failed，chromium；含新增 2 crud-list-mode + 既有 13 crud 全绿、无回归)。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），verdict `approved`，见 plan `Closure Audit Evidence`。Non-blocking follow-up：carrier keyed-remount → reactive 更新优化。

### 2026-06-25 (grid/list/cards 响应式 successor — newly-landed renderer responsive follow-ups)

- Executed plan `docs/plans/2026-06-24-2358-1-newly-landed-renderer-responsive-followups-plan.md`：收口主组件 wave（W1c `list` / W2a `cards` / W3a `grid`）落地后显式 deferred 的三组移动端响应式 successor（grid 断点列数切换、list 触摸滚动 + hairline 分隔线迁移、cards 列数 schema 化 + hairline 裁定）。依赖（W1c/W2a/W3a done、M0.1 hairline 基础设施 done）均已满足。
- **Phase 1 grid**：`packages/flux-renderers-layout/src/schemas.ts` GridSchema 新增 `responsiveColumns?: { sm?: number; md?: number; lg?: number }`（`GridResponsiveColumns extends SchemaObject`）。`grid-renderer.tsx` 消费 `useIsMobile()`（Decision A 运行时分支——内联 `gridTemplateColumns` 无法表达 `@media`、Tailwind v4 content scanning 无法识别动态 `grid-cols-*`，故纯 CSS 范式不可行，按 plan 裁定回落 A）：mobile(<768)→`sm`、desktop(≥768)→`lg ?? md`，各桶回退 base `columns`；`data-columns` 反映有效列数；mobile + responsiveColumns 配置时根节点增 `data-responsive="narrow"`；colSpan 按有效列数 clamp。缺省（无 responsiveColumns）逐字节零回归。`layout-renderer-definitions.ts` + `test-support.tsx` 补 `responsiveColumns` 字段规则。
- **Phase 2 list**：`packages/flux-renderers-data/src/list-renderer.tsx` 消费 `useIsMobile()`——(1) 根节点 `divide-y divide-border` 迁移到逐项 `nop-hairline nop-hairline--bottom`（M0.1b 0.5px 高 DPI 细线，末项无底边，桌面/移动一致）；(2) mobile 根加 `touch-pan-y` + 条目 `py-3`（desktop `py-2`）增强触摸目标；(3) 根（含空态）mobile 增 `data-responsive="narrow"`。`data-slot`/选择/分页/infinite 核心逻辑不动。
- **Phase 3 cards**：`packages/flux-renderers-content/src/schemas.ts` CardsSchema 新增 `columns?: number | CardsResponsiveColumns`。`cards-renderer.tsx` `resolveCardsLayout()`——缺省=原 `sm:grid-cols-2 lg:grid-cols-3`（零回归）；number=统一列数 inline `gridTemplateColumns`；object=useIsMobile 派生（mobile→`sm ?? 1`、desktop→`lg ?? md ?? 3`）+ mobile `data-responsive="narrow"`。hairline 迁移裁定（Decision）：cards 用 `gap-3` 间距作视觉分隔，无 `divide-*`/border 分隔线，故「无需迁移」（记录于 `cards/design.md` §13）。`content-renderer-definitions.ts` 补 `columns` 字段规则。
- **Phase 4 playground + e2e**：扩展 `apps/playground/src/pages/m4-data-display-demo.tsx`（注册 layout+content renderers，新增 grid/list/cards 三 Section + testid，标题保留「M4 数据展示响应式」兼容既有 e2e openM4Data regex）。`tests/e2e/m4-data.spec.ts` 新增 6 个双视口程序化断言（mobile: grid 1 列+narrow、list hairline+touch-pan-y+narrow、cards 1 列+narrow；desktop: grid 3 列无 marker、list hairline+py-2 无 marker、cards 3 列无 marker；均非截图）。
- **owner docs 同步**：`grid/design.md` §13、`list/design.md` §13、`cards/design.md` §13 各补「响应式行为」小节（引用 M0 基线断点 + hairline + Decision 裁定）。`mobile-roadmap.md` M3b/M4b 标 successor 已落地（M4b → ✅）；M3b/M4b/M0.1 plan `Deferred But Adjudicated` 补 Closure Note 指向本 plan。
- 验证（full-green）：`pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(55 packages 全过) + `playwright test`(527 passed / 46 pre-existing skipped / 0 failed，chromium)。focused 单测：`grid-renderer.test.tsx`(+5 responsive)、`list-responsive.test.tsx`(4 new)、`cards-renderer.test.tsx`(+5 responsive)。
- Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），见 plan `Closure Audit Evidence`。

### 2026-06-25 (Renderer Correctness, Selection/Key Invariants, Contract Honesty Guard — plan 0630-1 Phases 1-3)

- Executed Phases 1-3 of `docs/plans/2026-06-25-0630-1-renderer-correctness-and-contract-guard.md`：Contract Honesty Guard（H1）+ 文件拆分 + list 跨页 key 修复（P0-4）。Phases 4-6（P0-1/P0-3/P0-5/P0-6/P1-6/P1-7 等）待执行。
- **Phase 1 — Contract Honesty Guard (H1)**：新建 `packages/flux-core/src/contract-honesty.ts` 导出 `findUnreferencedContracts`/`isRendererEventKeyReferenced`/`isCapabilityHandleReferenced`/`ContractHonestyViolation`。全部 6 个 renderer 包（basic/form/form-advanced/data/content/layout）各新增 contract-honesty.test.ts 遍历 `RendererDefinition[]`，断言每条声明被 renderer body 或其 register handle 引用。有效性自证：每个 guard 含"注入假 event → guard 失败" anti-regression 断言。Fix：data-source 移除 lying contracts `onSuccess`/`onError`（数据源生命周期 dispatch，不走 `props.events`）；basic dynamic-renderer `loadAction` 纠正为 `kind:'prop'`（经 `helpers.evaluate` 自求值，不走 events 通道）。`flux-core/src/index.ts` 新增导出。
- **Phase 2 — Oversized File Splits (C-04/C-05)**：`tree-renderer.tsx`(772→~214 行)抽出 `tree-node-helpers.ts`/`tree-focus-nav.ts`/`tree-search.ts`；`table-body-row-rendering.tsx`(723→~396 行)抽出 `table-cell-chrome.tsx`/`table-flattened-items.ts`/`table-expanded-row.tsx`。零 API 变更。
- **Phase 3 — List Cross-Page Key Fix (P0-4)**：`list-renderer.tsx` `keyGlobalIndex`=全局索引 `(currentPage-1)*pageSize + index`，消除跨页 key 碰撞（每页复用 `item:0..pageSize-1` 导致 React reconcile 串接 ListItemView，泄漏选择/作用域状态）。
- **其他**：`types/node-fs.d.ts` 补 node 模块声明；`canvas-styles.test.ts` 移除无用 `@ts-expect-error`。
- 验证（unit-green）：`pnpm typecheck`(55/55) + `pnpm lint`(29/29 0 errors) + 涉及包测试全绿（flux-core 442/basic 388/data 535/form 499/form-advanced 836/content 158/layout 62）。

### 2026-06-25 (amis bug-driven improvements 信号库 — 14 组件文档 + README)

- **任务**：挖掘 `~/app/nop-chaos-flux-tests/data/useful-llm/`（amis 历史 bug-fix issue 集，1785 issue，2026-05-07 audit）对 nop-chaos-flux 有意义的部分——**不是抄 amis**，而是把 amis-specific bug 翻译成 Flux 应保证的**通用正确性属性**（设计缺口 DESIGN-GAP / 测试缺口 TEST-GAP / LOCK 回归锚 / NOT-ADOPTED 明确拒绝 amis 坏设计）。
- **目录**：新建 `docs/components/amis-bug-driven-improvements/`（README + 14 个 per-component 文件，每组件一个文件，多 issue 归并到同文件）。14 文件：`01-form-validation`(24)、`02-table-and-crud`(34)、`03-select`(13)、`04-combo-and-array-field`(15)、`05-input-fields`(13)、`06-date-fields`(12)、`07-input-tree`(6)、`08-input-file-and-image`(8)、`09-layout-surfaces`(16)、`10-data-display`(14)、`11-api-data-and-scope`(19)、`12-i18n`(5)、`13-mobile-responsive`(2)、`14-action-button-toast-mapping-cards-status-styling`(33)。**共 ~214 high-signal 条目**。
- **方法**：6 个独立 explore 子 agent 并行 triage（validation / table / select+combo / input+date+upload / layout+renderers / api-data+i18n+mobile），每 agent 读 INDEX 后深读 ~30-60 高信号 issue，过滤 amis-specific 垃圾（SDK build / React-version / iconfont sprite / amis-editor-only / echarts-config-deep / amis `dataProvider`/`themeCss`/`mobileUI` / host-routing），每条翻译成通用属性 + 引用 amis issue 号便于反查。amis 仅作参考之一**非标尺**，对齐 `existing-components-improvement-analysis.md` §0.2 Flux 设计原则。
- **关键 framing**：每个 NOT-ADOPTED 表显式拒绝 amis 坏设计并给一行理由（散落条件属性 `visibleOn`→Flux `when`；组件级 `api`/`initFetch`→data-source；amis 值编码 `valueField`/`joinValues`→`{label,value}` 真数组；`level`/`actionType` 判别树→`variant`+action graph；`themeCss`/`borderMode`/`mobileUI`→Tailwind+响应式；`dataProvider` JS 串/前端导出/echarts→拒绝）。新增功能必给详细理由；缺测试则记录要加哪些测试。
- **7 个跨组件共性主题**：lifecycle teardown（dialog GC/iframe listener/chart timer）、stale-snapshot-vs-live-scope、renderer 不硬编码几何压过 author intent、echo-when-value-source-missing、async request correctness、clear 必须对称传播 set、**umbrella-switch atomicity**（condition-builder `disabled` 扇出到所有 mutation affordance / card `selectable` 同时关 visual+behavior）。
- **两轮独立 fresh-context 子 agent review**：Pass 1 verdict `ACCEPT-WITH-MINOR-FIXES`——发现 `other/` cluster（178 issue）完全未覆盖（真实缺口，含 button/action/toast/mapping/condition-builder/cards/status，非 amis-specific）→ 补写 doc 14（33 条目）；entry count 漂移（README ~150 实际 ~214）；`DESIGN-ACK-NOT-IMPL` 词表缺；T14→T17 cross-ref typo。全部修复。Pass 2 verdict `若 P0+P1 计数修复落地即 ACCEPT`——spot-check 8 个 amis ref 全 PASS（faithful + 非 amis-parity）；无 broken doc-path；无 parity smell；补 per-doc header count 对齐 + 第 7 个 cross-cutting theme + 01↔04 / 09↔14 cross-ref。全部修复。
- **最终核验**：per-doc header claim == actual rows（214 total），所有 `Add design note to <path>` 目标 doc 均解析存在（`table-row-identity-and-scope-performance.md`/`array-field.md`/`api-data-source.md`/`scope-ownership-and-isolation.md`/`styling-system.md` 等）。
- **范围/性质**：纯文档（research + improvement backlog 信号源），无 CODE 变更，无 typecheck/build/lint/test gate 适用（Test Strategy Tier = Not applicable，纯 docs）。不替换 `existing-components-improvement-analysis.md` 主 roadmap，而是作为其证据源（真实生产报告）。

### 2026-06-25 (amis bug-driven improvement roadmap — 规划阶段，docs/backlog/)

- **任务**：基于已完成的 `amis-bug-driven-improvements/` signal 库（14 doc/214 条），按 `roadmap-authoring-guide` 拟制独立 roadmap，并定义 mission 任务（工作项）。**仅规划阶段**——不拟制具体开发计划（`docs/plans/`），不写代码。
- **新增 `docs/backlog/`**（项目原本无此目录，遵循 `attractor-guided-engineering-template` 约定）：
  - `00-roadmap-authoring-guide.md` — 从 template 复制的权威 guide（定义 roadmap 是什么、10 段结构、阶段粒度规则、反模式）。
  - `README.md` — 平面 per-item backlog 表（B1.1–B6.2 + B7，Priority/Signal source/Owner Doc/Plan/Status/Autonomy/Blocker），与 `implementation-roadmap.md` 共享同一工作项，权威状态在后者。
  - `implementation-roadmap.md` — amis bug-driven 改进 roadmap，严格遵循 guide 10 段结构。
- **mission 任务定义**：14 个工作项（B1.1–B6.2 + B7），按 6 个 themed wave（B1–B6）组织 + B7 backlog。wave 是纯优先级标签（不携带状态），只有工作项出现在 Phase Status。每个工作项 sized 到一个 execution plan 可收口的范围。**全部 `todo`**（无 plan，符合"仅规划阶段"）。P0+P1（109 条）分布在 B1–B6；P2/P3（103 条）归 B7。依赖图：B1/B2 可并行 → B3 依赖 B1.1/B2.1；B4/B6 可并行 → B5 依赖 B4.1/B6.1；禁止跳过 B1.1/B2.1 直接做 B3.x。
- **amis 不是标尺**：每个工作项的 signal 已是 Flux-first 通用正确性属性（非 amis-parity）；amis 坏设计显式 NOT-ADOPTED。AMIS-REF 指向源 issue 仅便于反查。
- **独立 sub-agent review**：fresh-context 审 roadmap vs guide。Verdict **pass-with-minors**（零 Blocker/Major）：10 段结构 PASS、唯一动态状态块 PASS、wave 无状态 PASS、Phase Details 无 exec 步骤/复选框/closure criteria PASS、reuse 全标注 PASS、依赖一致 PASS（应用 3 个 Minor 修复后 graph↔table 完全一致）、AMIS-REF 抽查 4 条全 PASS、amis framing PASS。3 个 Minor 已修：(1) B3.3 表依赖补 B3.2 匹配 graph；(2) B1.2 加 plan-authoring note 标注潜在拆分点；(3) B1.1 AMIS-REF 格式澄清。**达成共识**。
- **范围/性质**：纯规划文档（roadmap + backlog + guide），无 CODE 变更，无 typecheck/build/lint/test gate（Test Strategy Tier = Not applicable，纯 docs）。删除了早先误放在 `docs/components/` 的 roadmap 草稿，改用 `docs/backlog/` 正确路径。`docs/index.md` 加两行路由（signal 库 + roadmap）。
- **后续**：mission-driver 按 `implementation-roadmap.md` 的 Phase Status 取第一个 `todo` 工作项，自动起草/执行 `docs/plans/` 计划（本轮不做）。AI 可自主推进 `todo`→`planned`→`done`，但工作项增删/重排需人确认。

### 2026-06-25 (corrections: roadmap 命名 + mission 定义)

- **用户指正三处**：(1) `implementation-roadmap.md` 在多 roadmap 项目里命名太泛；(2) "mission" 未定义——我误当成工作项概念；(3) mission JSON 格式未核对。调查后发现 **flux 已装 mission-driver 且有 3 个现成 mission**（`components.json`/`components-improvement.json`/`mobile.json`），我此前完全错过。
- **修正 1（roadmap 命名+位置）**：`docs/backlog/implementation-roadmap.md` → `docs/components/amis-bug-driven-improvement-roadmap.md`（具名 + 与 flux 其他 3 个 roadmap 同位 `docs/components/`，符合既有约定；mall 项目 `enhanced-features-roadmap.md` 先例）。删除孤儿的 `docs/backlog/README.md`（平面表指向已移走的 roadmap）。修掉 roadmap 内对 `docs/backlog/README.md` 的 stale 引用。`docs/backlog/` 现仅留 `00-roadmap-authoring-guide.md`（guide 本身补一行 multi-roadmap 具名约定说明）。
- **修正 2+3（mission 定义 + JSON）**：调查 `attractor-guided-engineering-template/tools/mission-driver/design/mission-design.md` §4 与 `mission-check.mjs` 验证器（REQUIRED: name/roadmapPath/plansDir/commands.test；路径存在性查 roadmapPath/plansDir/contextDir/moduleDir）。新建 `missions/amis-bug-driven-improvements.json`，严格镜像 flux 既有 3 mission 约定（`moduleDir:packages`、pnpm 四命令、`deep-audit-prompts.md`+`open-ended-adversarial-review-prompt.md` audit prompts、`feat(<scope>): <description>`），唯三差异：`name`/`description`/`roadmapPath`（指向新 roadmap 路径）。description 含 amis-not-a-ruler + NOT-ADOPTED 清单 + 与 components-improvement 互补不重复声明。
- **验证**：`mission-check.mjs` 跑新 mission + 3 既有 mission，全部 `{valid:true}`、路径全存在。`docs/index.md` 路由行更新指向新 roadmap 路径 + 列出 mission。全仓 stale-ref sweep：无 live 指针指向已删 `docs/backlog/implementation-roadmap.md`/`README.md`（剩余命中均为概念性/历史性 prose）。
- **独立 sub-agent review**：fresh-context 审 renamed roadmap + new mission。Verdict **pass**（零 blocker/fix）：validator 通过、字段完备、路径全存在、与 3 既有 mission 逐字段一致、roadmap guide 10 段结构 intact、stale-ref 清零、roadmap↔mission↔index 三方一致、amis framing + 与 components-improvement 互补声明均成立。1 个 optional polish（guide 加 multi-roadmap 具名说明）已采纳。
- **范围/性质**：纯规划文档修正（rename + mission JSON），无 CODE 变更，无 typecheck/build/lint/test gate（纯 docs）。mission-driver 执行入口：`./tools/mission-driver.sh amis-bug-driven-improvements`，按 roadmap Phase Status 取第一个 `todo` 工作项自动起草/执行 plan。

### 2026-06-25 (Flux 原则审计：signal 库 215 条逐条核验)

- **用户挑战**：roadmap 这些改进是否真严格符合 Flux 设计原则？amis 很多陈旧设计在 flux 已本质性改进，flux 表达式机制（任意属性支持 `${expr}`）是否已通用解决大量"缺口"？独立 sub-agent 逐条核验。
- **核验依据**（yardstick）：`field-binding-and-renderer-contract.md` **Rule 1「Expressions Use Ordinary Props」**（任意字段即载表达式，禁 `xxxExpr`/`xxxOn` 平行命名）+ Frozen `META_FIELDS` 集（`when`/`visible`/`hidden`/`disabled` 统一 meta，非散落条件属性）；`naming-conventions.md` §3 NOT-ADOPTED；`existing-components-improvement-analysis.md` §0.2/§5 六原则。
- **结论：0 hard violation**。无条目提议 amis 坏设计（无 `visibleOn`/`disabledOn`、无 `valueField`/`joinValues`/`delimiter` 值编码、无组件级 `api`/`initFetch`、无 amis `trueValue`/`falseValue`、无 `xxxExpr` 变体）。NOT-ADOPTED 表是合规机制本身。但审计发现并修 **5 处 framing 缺陷**：
  1. **I11**（input-number min/max）——把已定 BY-DESIGN 决策（`input-number/design.md` §6："min/max 约束不放在 adapter 的 validate 中，而是放在渲染器内部的 clamp 逻辑中"）当成开放 DESIGN-GAP。改写为 **BY-DESIGN / TEST-GAP P3**（从 P1 降级，移出首个 mission P0+P1 范围）；clamp 是刻意的，反转为 validate 需 design-owner 签字，非 P1 缺口。
  2. **TR6**（input-tree）——一半测 `enableNodePath`，而它在 `input-tree/design.md` §4 标 **暂不实现**。split：TR6 = `valueField` remap（live 字段，保留，注意它与 select 被拒的 value-**encoding** 不同，是 node-key remapping）；**TR7** = `enableNodePath` 路径构建 → DESIGN-ACK-NOT-IMPL P3（测暂不实现功能是 roadmap smell）。
  3. **ST1**（status/property）——原写法重述 Rule 1（"所有字段接受 `${expr}`"）当 status 专属缺口，是同义反复。改写为 **per-renderer 字段一致性**（amis #4900 = `title` 表达式不解析而 sibling 字段解析）——真实回归锚，非 Rule 1 重复。
  4. **L12**（grid className）——重述 frozen `META_FIELDS.className` 表达式支持当 grid 缺口。收窄到 **grid-column 表达式响应性**（通用 className-expr 归 styling-system）；P2→P3。
  5. **D3**（cleared date）——原以推测性 `valueMode`/`nullValue` 字段先行。改为 Flux-idiomatic **`transformOutAction` submit-transformer** 先行（`null` for backend 走 transformer），新字段仅条件性 X5 follow-up。
- **次要**：I10（`precisionMode`）标注为 conditional X5 follow-up（不默认引入）。
- **计数同步**：TR6 split → TR6+TR7，总数 214→**215**；I11/L12/D3 降级使首个 mission 的 P0+P1 范围 109→**114**（注：之前 109 是 linter 重排后的旧估，重算 P0=22/P1=92/P2=84/P3=13/BY-DESIGN-combo=4）。README（input-tree 6→7 entries、215 总数）、roadmap（215、114、B4.1 去 I11、B4.2 TR1-TR6 + TR7 注）、per-doc header（07: 6→7）全部同步。每 doc header == 实际行数全过。
- **审计结果持久化**：roadmap Cross-Cutting 增「Flux 原则审计」行；README 新增 `## Flux-Principles Audit` 节（5-entry fix 表）。
- **验证**：14 doc header==rows 全 OK；README↔roadmap 总数一致（215）；mission `mission-check.mjs` valid:true；5 处重构行 grep 确认落地。
- **范围/性质**：纯文档修正（framing + 计数），无 CODE 变更。确认 signal 库经独立审计是 Flux-principles-compliant——用户挑战未被坐实（证据指向相反方向），但确实暴露了 5 处需修的 framing 缺陷，均已修。

### 2026-06-25 (请求下沉审计：api/data-source/action 层 vs amis initApi)

- **用户挑战**：api/data-source/action 等设计是否严格遵循 Flux 最佳实践？是否引入 amis `initApi` 之类过时设计？
- **核验依据**：`docs/bugs/15-component-level-initfetch-analysis-and-fix.md`——Flux 硬规则：**组件 schema 字段不得成为数据加载入口**（即使经 `executeSource`/data-source 路由）。三条合规模式：①表达式值绑定（组件读 scope 不 fetch）②gated action graph（显式 boolean gate，走 action 通道）③用户交互驱动（用户行为触发，非 mount 自动）。先例：condition-builder `source` 字段已因此被删除。
- **第二轮独立 sub-agent 专项审计** B2/B3/B6 + U5/T11/T23/T25/T27/AG3/C13。**结论：0 violation，0 amis initApi/组件级 api 引入**。每个 `interval`/`silent`/`sendOn`/`initFetch`/`cache`/`dedup`/`resultMapping`/`mergeStrategy` 引用都绑定到 data-source/DataSourceController/ApiSchema（请求 OWNER，X4 layering），非消费组件字段。逐条映射三条合规模式：A9/A10/A11/A19→②（data-source 层 gate）；T11 tree lazy-load→③（user-expand，镜像 `childrenSource`）；T23/T25/T27 table `source`→①（`source?: SchemaValue` 读 scope 不 fetch）；U5 `deleteAction`→③（action-ref via `kind:'prop'`，user click-remove 时 `props.helpers.dispatch`，镜像 `uploadAction`）；AG3 reload→action-graph capability 定向具名 source；C13 per-item writeback→③（action 写标准 value channel）。
- **修 1 处措辞歧义**：**T25**「source init fires once per mount」可误读为 mount-fetch → 改为「`source` 表达式每 dialog open 求值一次（无 fan-out）」+ pattern #1 标注（表达式值绑定，组件不 fetch）。
- **NOT-ADOPTED 覆盖确认完整**：api/table/file/action doc 均含「组件级 api/initApi/interval → 拒绝」行。
- **审计结果持久化**：roadmap Cross-Cutting 新增「请求下沉审计」行；README 新增 `## Request-Sink Audit` 节（6-entry 合规模式映射表）。
- **范围/性质**：纯文档审计 + 1 处措辞修正，无 CODE 变更。确认 api/data-source/action 层严格遵循 Flux 请求下沉原则，无 amis initApi 引入——用户挑战未被坐实。
