### 2026-04-01

- Executed full Code Audit Bug Fix Plan (#25) — 14 fixes across 8 packages
- Plan doc: `docs/plans/25-code-audit-bug-fix-plan.md`

**Phase 1 — Build Hygiene (P0)**
- FIX-1: Removed stale `"../flux-react/src/**/*.d.ts"` from 3 tsconfig include paths (`flux-renderers-data`, `flux-renderers-form`, `flux-code-editor`)
- FIX-2: Created `scripts/verify-no-src-artifacts.mjs` CI guard, wired into `lint` via `check:src-artifacts`

**Phase 2 — Logic Correctness (P0–P1)**
- FIX-3: Deleted erroneous `allowMultiEdge=true` duplicate-edge check in `flow-designer-core/src/core.ts` (lines 197-199), added tests for both true/false cases
- FIX-4: Implemented `report-designer:importTemplate` and `report-designer:exportTemplate` in `report-designer-core/src/core.ts` with codec resolution via `profile?.codecId`
- FIX-5: DataSourceRenderer now reads `props.props.api` (precompiled) instead of `schema.api` (raw), removed `runtime.evaluate()` recompilation
- FIX-6: DynamicRenderer removed `runtime.evaluate()` on already-precompiled `schemaApi`
- FIX-7: Extracted `executeApiObject()` in `flux-runtime/src/request-runtime.ts` — unified pipeline applying requestAdaptor → fetcher → responseAdaptor. Both DataSourceRenderer and DynamicRenderer now use it instead of raw `env.fetcher()`

**Phase 3 — Defensive Fixes (P1–P2)**
- FIX-8: Added boundary guards in `evaluateArray`/`evaluateObject` for schema hot-reload length mismatches
- FIX-9: Wrapped `NodeRenderer` in `React.memo` for shallow prop comparison
- FIX-10: Fixed `createNodeId` regex in `flux-core/src/utils/schema.ts` — replaced `[^a-zA-Z0-9-_:.]` with `[^a-zA-Z0-9_.\-:]` to avoid character range allowing illegal HTML ID chars
- FIX-11: Removed `has()` + `resolve()` double traversal in Proxy get trap in `scope.ts`
- FIX-12: Added `anyChildChanged` early-exit before allocation in `evaluateArray`/`evaluateObject`

**Phase 4 — Performance (P2)**
- FIX-13: Added LRU eviction with `MAX_ENTRIES=200` in `api-cache.ts` using doubly-linked list
- FIX-14: Added `WeakMap<EvalContext>` Proxy cache in `createFormulaScope`

**Verification**: `pnpm typecheck` ✅, `pnpm build` ✅, `pnpm test` ✅ (2 pre-existing failures in `flux-react` dialog tests, 1 pre-existing in `spreadsheet-renderers` stale `.js` artifact)

**Key decision**: FIX-5/6/7 were implemented together since FIX-7 (unified pipeline) depends on FIX-5/6 (precompiled API). Both renderers now keep `runtime` for `runtime.expressionCompiler` passed to `executeApiObject`.

**Next step**: Consider FIX-15 through FIX-22 (P3 deferred items) in future sprints
