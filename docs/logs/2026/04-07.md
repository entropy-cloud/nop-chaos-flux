# Development Log — 2026-04-07

### 2026-04-07

- Recorded a hard-to-diagnose cross-layer regression in `docs/bugs/27-reaction-registration-churn-and-initial-page-sync-test-hang-fix.md`: a workspace test run looked like it hung in `flux-renderers-basic`, but the real causes were `SchemaRenderer` replaying initial page data after mount and `ReactionRenderer` re-registering reactions when `helpers.dispatch` identity changed, which cancelled debounce timers and masked the failure as a non-returning test run.
- Link to docs/code: `docs/bugs/27-reaction-registration-churn-and-initial-page-sync-test-hang-fix.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-renderers-basic/src/reaction.tsx`, `packages/flux-renderers-basic/src/index.test.tsx`, `packages/flux-runtime/src/index.test.ts`
- Key decision: keep this incident in `docs/bugs/` rather than only the daily log because the symptom looked like workspace/Vitest infrastructure trouble while the actual root cause lived in React subscription lifecycle and root scope synchronization.
- Next step: when more repeated or dynamic reaction surfaces are introduced, preserve stable registration lifetimes and treat root data replay as an update-only path, not a mount-time reconciliation step.

- Continued Plan 40 debugger/runtime identity migration: the Node panel component tree now enumerates mounted components from the runtime/component-registry debug snapshot instead of scanning the DOM for all `[data-cid]` markers, while tree clicks inspect by `cid` and only use DOM presence as supplemental tag/class metadata.
- Link to code/tests/docs: `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/panel/use-inspect-mode.ts`, `packages/nop-debugger/src/panel/node-tab.tsx`, `packages/nop-debugger/src/controller-inspect.test.ts`, `packages/nop-debugger/src/panel.test.tsx`, `docs/architecture/debugger-runtime.md`
- Key decision: mounted-node enumeration is now a controller/runtime concern exposed as a stable snapshot API, so the debugger panel no longer treats the DOM as the source of truth for the component tree.
- Next step: continue moving richer tree structure and not-materialized/debugger tree states onto runtime-owned snapshots so virtualized and repeated instances can surface without any DOM-wide discovery step.

- Landed the first end-to-end root-level dependency tracking slice from Plan 39: formula scope collection now records lexical roots instead of deep member paths, nested object enumeration stays anchored to its owning root instead of escalating to whole-scope wildcard, `ScopeChange` invalidation now matches on normalized roots, fragment child-scope snapshot updates publish minimal top-level root changes, and `data-source` / `reaction` now support explicit `dependsOn` roots that override runtime fallback collection.
- Link to code/docs/tests: `packages/flux-core/src/utils/path.ts`, `packages/flux-core/src/types/schema.ts`, `packages/flux-formula/src/scope.ts`, `packages/flux-runtime/src/scope-change.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/reaction-runtime.ts`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/fragment-scope.ts`, `packages/flux-runtime/src/source-reaction-dependencies.test.ts`, `packages/flux-runtime/src/__tests__/scope-change.test.ts`, `packages/flux-formula/src/scope.test.ts`, `docs/architecture/dependency-tracking.md`
- Key decision: keep raw change paths for diagnostics, but normalize both runtime-collected dependencies and invalidation matching to lexical roots; explicit `dependsOn` wins over runtime fallback, and self-write filtering now removes the source's own published root before dependency hit testing.
- Next step: thread the same root-only dependency substrate into more repeated/child-scope reconcile sites and continue replacing remaining wildcard fallbacks that are only present because update publishers do not currently emit minimal root changes.

- Landed the first compiler-integrated diagnostics slice from Plan 41: `flux-core` now exposes formal schema diagnostics contracts, `CompileSchemaOptions` carries diagnostics/validation policies, `RendererDefinition` can contribute `schemaValidator`, `createSchemaCompiler(...).validate(...)` and `validateSchema(...)` now reuse compiler-owned analysis helpers, built-in `xui:imports` validation runs through a namespace validator, closed prop models can report unknown bare keys, and namespaced passthrough now stays in `CompiledSchemaNode.extensions` instead of normal compiled props.
- Link to code/docs/tests: `packages/flux-core/src/schema-diagnostics/index.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/schema-compiler/diagnostics.ts`, `packages/flux-runtime/src/schema-compiler-diagnostics.test.ts`, `docs/architecture/schema-file-validator.md`
- Key decision: keep compile return compatibility and put diagnostics behind explicit per-call options, but make diagnostics-only validation consume the same compiler-owned field/namespace/carrier analysis helpers rather than introducing a second validator walker.
- Next step: expand renderer-owned `schemaValidator` usage beyond the initial substrate so richer renderer-specific shapes can opt into strict diagnostics without pretending that every renderer already exposes a closed prop model.

- Advanced the debugger/runtime identity migration with a low-risk Plan 40 improvement: `inspectByCid()` now trusts registry/runtime inspect payloads for mounted-state resolution even when there is no matching DOM element, and only uses DOM presence as supplemental tag/class metadata.
- Link to code/docs/tests: `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/controller-inspect.test.ts`, `docs/architecture/debugger-runtime.md`
- Key decision: the runtime registry is now the primary truth source for live inspect state; DOM lookup remains useful for human-facing element metadata, but it should no longer downgrade a live resolved node to “not mounted” just because an element is temporarily absent.
- Next step: continue migrating debugger tree/inspect flows away from DOM-wide scans and onto runtime-owned registry snapshots so repeated/virtualized nodes can be distinguished without relying on page structure.

- Tightened repeated-target resolution without inventing a new migration protocol: runtime `resolveTarget(...)` now uses the current action/render locator `instancePath` for `repeatedPlan` and `repeatedSelector` when it already matches the requested repeated boundary, and component-action dispatch forwards that contextual `instancePath` from `ActionContext.locator` into runtime resolution.
- Link to code/tests: `packages/flux-runtime/src/node-resolver.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/node-resolver.test.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: prefer the canonical `instancePath` already present on the active repeated node before falling back to callback-based instance-path reconstruction; this improves repeated-safe targeting now without extending legacy `_targetTemplateId` string semantics.
- Next step: start compiling real internal repeated target plans/selectors from repeated render sites such as table row children, then remove the remaining string-based dynamic compatibility path after those canonical call sites exist.

- Reworked the table renderer onto the architecture described in `table-row-identity-and-scope-performance.md`: rows now normalize through explicit row entries, support `schema.rowKey`, use a compiled table-row repeated boundary id instead of the hardcoded `table.row` literal, and reuse one isolated row scope per materialized `rowKey` through a renderer-owned keyed cache.
- Link to code/docs: `packages/flux-renderers-data/src/schemas.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `docs/architecture/table-row-identity-and-scope-performance.md`
- Key decision: keyed row-scope reuse stays renderer-owned rather than becoming a new generic runtime scope registry; repeated-instance identity is now aligned with compiled row boundaries while row-local data transport remains `record` + `index`.
- Next step: use the now-real table row repeated boundary to migrate legacy dynamic `_targetTemplateId + componentInstanceKey` behavior toward internal repeated target plans/selectors, then remove the string-based compatibility path.

### 2026-04-07

- Upgraded compile-time component target rewriting to attach an internal static target plan alongside legacy `_targetCid`, and taught runtime dispatch to execute correctly against those static plans using the current action/runtime identity instead of a hardcoded runtime key.
- Link to code: `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: keep legacy `_targetCid` for compatibility during migration, but make the compiled output also emit canonical internal target structure now so the eventual removal of cid-centric targeting is incremental rather than disruptive.
- Next step: continue replacing legacy `_targetTemplateId`/`componentInstanceKey` dynamic targeting with repeated-aware internal target shapes, then remove the old fields after call sites stop depending on them.

### 2026-04-07

- Pushed locator-first identity into action monitoring and debugger events: action monitor payloads now carry `locator`, debugger adapter/plugin events preserve it, and action/debugger correlation can start depending on canonical runtime identity instead of only `nodeId/path` summaries.
- Link to code: `packages/flux-core/src/types/actions.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/adapters.ts`, `packages/nop-debugger/src/adapters.test.ts`
- Key decision: keep `nodeId/path` as convenience summaries for humans, but move event correlation toward `locator` as the canonical identity carrier.
- Next step: finish retiring remaining `ctx.node`-only assumptions in runtime/debugger internals, then remove compiled-node-specific action identity fallbacks when `locator` is always available on the active render path.

### 2026-04-07

- Threaded `locator` into the action/helper path as a minimal additive identity field on `ActionContext`, so renderer events and `helpers.dispatch()` now carry row-aware locator context into action execution without introducing `nodeInstance` into every action consumer up front.
- Link to code: `packages/flux-core/src/types/actions.ts`, `packages/flux-react/src/helpers.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`
- Key decision: action execution only needs canonical node identity, not the full render-time `nodeInstance`; pass `locator` first and keep the action context surface minimal.
- Next step: continue replacing internal `ctx.node` assumptions with `ctx.locator`/`nodeInstance` where it reduces coupling, then remove remaining legacy compiled-node-only resolution paths.

### 2026-04-07

- Reduced duplicated locator/state assembly by making debug-data capture reuse the new compatibility `nodeInstance`: render-time debug registration now stores `nodeInstance`, the registry inspect payload exposes `nodeInstance.state` when already available, and debugger inspect can surface a compact node-state summary without inventing another state source.
- Link to code: `packages/flux-core/src/types/renderer-component.ts`, `packages/flux-react/src/useNodeDebugData.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/controller-inspect.test.ts`
- Key decision: debugger/runtime integration should consume the new `nodeInstance` data opportunistically where it already exists, instead of creating a parallel inspect-state channel.
- Next step: continue collapsing internal consumers onto `nodeInstance`/`locator` so the eventual removal of compiled-node-specific runtime state paths becomes mechanical rather than architectural.

### 2026-04-07

- Added the first compatibility `nodeInstance` slice without introducing a second runtime state system: `NodeRenderer` now adapts the existing compiled-node runtime state into a minimal `nodeInstance`, exposes it through `RendererComponentProps`, and verified that repeated table row children can read their locator `instancePath` through `props.nodeInstance`.
- Link to code: `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-react/src/node-instance.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`
- Key decision: `NodeInstance` integration should stay additive and reuse the current compiled-node/runtime-state substrate until the project is ready to replace `createRuntimeState()` wholesale; avoid introducing parallel caches, extra contexts, or duplicate state ownership.
- Next step: if migration continues, convert the remaining internal execution paths to consume `nodeInstance`/`locator` first and only then remove direct compiled-node state assumptions.

### 2026-04-07

- Refined the table row performance architecture again after a second review pass: downgraded raw `record` reference equality from the only fast path to one optional semantic-stability signal, added explicit rematerialization/retention semantics, documented commit/pre-paint versus render-phase reconciliation boundaries, added a required `rowKey` to `sourceIndex` bridge for reconciliation, and recorded deterministic single-turn bulk reconciliation as a performance requirement.
- Link to docs: `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/architecture/performance-design-requirements.md`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/hooks.ts`, `packages/flux-runtime/src/scope-change.ts`
- Key decision: the current baseline remains row-root invalidation with narrow cell reads, but the contract must stay open to future row-field publication and to source-owned row-dirty signals such as revisions or changed-row payloads instead of treating referential equality as the only viable optimization path.
- Next step: if implementation starts, keep the first slice minimal by fixing the table shell subscription, introducing owner-qualified stable row identity plus row-index reconciliation, and deferring virtualization-specific retention policy until a real windowing surface exists.

### 2026-04-07

- Tightened the new table row performance note after review: clarified that row-scope reuse is a renderer-owned lifecycle rather than a generic runtime feature, made owner-qualified row scope ids mandatory, documented the parent-fan-out cost of non-isolated row scopes plus the `isolate: true` optimization path for pure row-local tables, and explicitly recorded immutable row-record replacement plus provenance-aware over-invalidation as current constraints.
- Link to docs: `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/architecture/dependency-tracking.md`, `packages/flux-runtime/src/scope.ts`, `packages/flux-formula/src/scope.ts`, `packages/flux-react/src/render-nodes.tsx`
- Key decision: keep row scopes as the current React baseline carrier, but document that expression/evaluation transport is more flexible than raw scope count and should use isolation or lighter carriers whenever parent lexical fallback is unnecessary.
- Next step: when implementing the table rewrite, prefer a memoized row component that owns row-scope synchronization, remove the dead whole-scope table subscription, and only use non-isolated row scopes where row descendants or row-scoped actions truly need parent bindings.

### 2026-04-07

- Added a dedicated architecture note for high-performance table row identity and scope reuse, covering the split between index-addressed form values and `rowKey`-addressed runtime identity, row-scope caching, row-local invalidation, same-row `record.*` access, and explicit guidance to avoid extra row-specific React context layers.
- Link to docs: `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/index.md`, `docs/architecture/dependency-tracking.md`, `docs/architecture/template-instantiation-and-node-identity.md`
- Key decision: table performance should be built around stable `rowKey` plus reused row scopes and row-root invalidation, while `instancePath` remains a repeated-instance identity carrier instead of becoming the primary state-update mechanism.
- Next step: implement the design with an explicit renderer-owned row-scope reconciliation algorithm, require narrow row-local reads in row descendants, and keep `viewIndex` opt-in so visible-order metadata does not become default hot-path churn.

### 2026-04-07

- Landed the first real repeated-instance identity path by threading `instancePath` through `RenderFragmentOptions` and React render context, then wiring table row child renders (`cell`, `buttons`, `expanded`) to pass `[{ repeatedTemplateId: 'table.row', instanceKey: rowKey }]`, which now shows up in row-child debug locator data.
- Link to code: `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-react/src/contexts.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/index.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/node-renderer-utils.ts`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`
- Key decision: repeated-instance identity should not be invented globally in advance; it should first be threaded only through real repeated render sites that already know their concrete instance key, starting with table rows.
- Next step: extend the same `instancePath` propagation pattern to other repeated render surfaces and then start converting compatibility-era compiled-node runtime state into explicit `NodeInstance` inputs.

### 2026-04-07

- Corrected the render-identity direction after review: removed the overreaching React `RenderIdentityContext` experiment, promoted `runtimeId` onto `RendererRuntime`, and switched compiled-node locator helpers to derive locators from `runtime.runtimeId + templateGraphId + templateNodeId` instead of hardcoded fallback values.
- Link to code: `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-react/src/contexts.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/node-renderer-utils.ts`, `packages/flux-runtime/src/index.test.ts`, `packages/flux-runtime/src/node-resolver.test.ts`
- Key decision: runtime owns the global part of identity (`runtimeId`), compiled/template nodes own structural ids, and render-scoped carriers should be reserved for future repeated-instance `instancePath` only when that data is actually available.
- Next step: when continuing Plan 40, the next substantial slice is to thread real repeated-instance identity (`instancePath`) through repeated rendering paths and then start introducing `NodeInstance` as a first-class render input instead of extending compiled-node compatibility further.

### 2026-04-07

- Continued Plan 40 by moving component action dispatch onto the runtime-owned resolver path, adding registry-backed `inspectCid()` substrate for debugger use, and introducing transitional `templateGraphId` / `templateNodeId` fields on compiled nodes so live registrations and debug data can start carrying real locator structure instead of placeholder-only compatibility values.
- Link to code/docs: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/node-resolver.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/index.test.ts`, `packages/flux-runtime/src/node-resolver.test.ts`, `packages/flux-core/src/compiled-cid.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/types/renderer-component.ts`, `packages/flux-react/src/node-renderer-utils.ts`, `packages/flux-react/src/useFormComponentHandleRegistration.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/controller-inspect.test.ts`
- Key decision: author-facing `ActionSchema` remains clean and does not gain `locator`/`staticPlan`/`repeatedPlan` fields; internal targeting is now carried through runtime/plugin-added component-target objects and resolved by runtime contracts rather than by schema shape.
- Next step: replace more legacy `cid`-centric/debugger-only paths by threading a proper render/runtime identity carrier through the React render path, so future `NodeInstance` adoption can reuse the same locator substrate instead of inventing a second one.

### 2026-04-07

- Completed the schema diagnostics design to cover strictness profiles, blocking-versus-nonblocking compiler errors, compile-return compatibility, and extension passthrough boundaries, then added a full execution plan for implementing compiler-integrated diagnostics without introducing a second validator engine.
- Link to docs: `docs/architecture/schema-file-validator.md`, `docs/plans/41-compiler-integrated-schema-diagnostics-implementation-plan.md`, `docs/index.md`, `docs/references/flux-json-conventions.md`
- Key decision: the first implementation slice must keep `compile(...)` return compatibility, add diagnostics via explicit compile options plus collector/reporter contracts, and run strict unknown-key checks before permissive prop lowering while preserving namespaced-only passthrough and `xui:*` built-in validation.
- Next step: implement Phase 0/1 by freezing regression coverage around current permissive compiler behavior, then introduce core diagnostics types and extended `CompileSchemaOptions` in `flux-core`.

### 2026-04-07

- Re-checked `docs/components/components-audit.md` against actual component docs, renderer code, and manifest counts; corrected the audit conclusions, then aligned the affected component contracts/examples by renaming `condition-builder.showANDOR` to `showAndOr`, renaming `spreadsheet-page.readonly` to `readOnly`, clarifying `report-toolbar`'s `ToolbarItem.action` contract, and filling missing event examples for button/dialog/drawer/table/image/link.
- Link to docs/code: `docs/components/components-audit.md`, `docs/components/condition-builder/example.json`, `docs/components/spreadsheet-page/design.md`, `docs/components/spreadsheet-page/example.json`, `docs/components/chart/example.json`, `docs/components/report-toolbar/design.md`, `packages/flux-renderers-form/src/renderers/condition-builder/types.ts`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-integration.test.tsx`, `packages/spreadsheet-renderers/src/types.ts`, `packages/spreadsheet-renderers/src/page-renderer.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: audit-driven fixes must not stop at prose; when the audit identifies a real schema naming issue, update the runtime contract, examples, and regression tests in the same change, while design-specific `ToolbarItem` shortcuts stay documented as domain contracts instead of being force-fit into generic renderer event rules.
- Next step: finish verification, then run an independent review pass against the updated audit and follow up on any remaining mismatches until the review comes back clean.

### 2026-04-07

- Refined the new Flux schema validator design toward compiler-integrated diagnostics: the compiler now owns the authoritative structural analysis pass in the documented target model, while diagnostics-only validation becomes a thin adapter over the same pass instead of a separate walker.
- Link to docs: `docs/architecture/schema-file-validator.md`, `docs/references/flux-json-conventions.md`
- Key decision: do not drive validation from hidden global switches and do not auto-merge unknown bare keys into normal compiled props; diagnostics must be controlled by explicit compile options, and only namespaced extensions may be preserved through a separate passthrough channel.
- Next step: if implemented, add compiler diagnostics options plus collector/reporter interfaces, run strict unknown-key checks before permissive prop lowering, and keep `xui:*` as a built-in validated namespace rather than a skipped extension bucket.

### 2026-04-07

- Started Plan 40 with the first code slice: introduced formal node-identity/result-contract types in `flux-core`, added a runtime node-resolver skeleton plus locator-aware registry helpers in `flux-runtime`, and threaded initial locator support into debugger inspect payloads without rewriting the React render path yet.
- Link to code/docs: `packages/flux-core/src/types/node-identity.ts`, `packages/flux-core/src/types/renderer-component.ts`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/types/compilation.ts`, `packages/flux-formula/src/compile.ts`, `packages/flux-runtime/src/node-resolver.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-runtime/src/node-resolver.test.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/controller.ts`, `docs/plans/40-template-instantiation-and-node-identity-implementation-plan.md`
- Key decision: keep the first implementation slice additive and compatibility-oriented: new `NodeLocator`/`ResolutionResult`/`InspectResult` contracts exist now, locator-based registration and lookup are live, but legacy `CompiledSchemaNode` and old component-action resolution paths remain in place until later phases.
- Next step: finish the first clean verification pass, then move into the next migration slice that starts wiring the runtime-owned resolver into component-action dispatch and reducing direct dependence on `_targetCid`/`_targetTemplateId` fast paths.

### 2026-04-07

- Added a dedicated file-level Flux schema validator design that separates JSON document structure checks from runtime form validation and defines a namespaced-property extension policy for forward-compatible schema ingestion.
- Link to docs: `docs/architecture/schema-file-validator.md`, `docs/architecture/form-validation.md`, `docs/index.md`, `docs/references/flux-json-conventions.md`
- Key decision: schema-file validation should stay pure and compiler-adjacent, reuse renderer registry plus field metadata, and treat namespaced properties through an explicit `error | ignore | delegate-or-ignore` policy instead of hard-failing every unknown extension key.
- Next step: if the mechanism is implemented, keep the walker and issue model outside `flux-runtime` hot paths and let renderer-owning packages contribute their own validator descriptors next to schema contracts.

### 2026-04-07

- Tightened the template-instantiation/node-identity architecture after multiple independent reviews, then added a new executable migration plan for the implementation phase.
- Link to docs: `docs/architecture/template-instantiation-and-node-identity.md`, `docs/architecture/component-resolution.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/debugger-runtime.md`, `docs/architecture/flux-core.md`, `docs/plans/40-template-instantiation-and-node-identity-implementation-plan.md`
- Key decision: `cid` is now strictly a mount-time live-node bridge id, structural resolution belongs to a runtime-owned resolver that returns explicit result categories, and the registry is reduced to live-handle/selector support rather than canonical structural truth.
- Next step: start Phase 0 regression harness and Phase 1 core type introduction from `docs/plans/40-template-instantiation-and-node-identity-implementation-plan.md`.

### 2026-04-07

- Added `example.json` alongside every current component doc under `docs/components/`, aligned the examples with implemented contracts where renderer metadata already exists, and ran repeated independent audits plus JSON parsing passes to close design/example mismatches around `data-source`, `dynamic-renderer`, `reaction`, report-designer examples, and migration wording.
- Link to docs: `docs/components/index.md`, `docs/components/roadmap.md`, `docs/components/examples.manifest.json`, `docs/components/data-source/design.md`, `docs/components/dynamic-renderer/design.md`, `docs/components/report-designer-page/example.json`, `docs/components/report-inspector/example.json`
- Key decision: `docs/components/<type>/example.json` is now part of the minimum component-doc contract, and examples must prefer real current runtime fields over aspirational rename proposals unless the document explicitly marks the schema as target-only.
- Next step: when new renderers or schema changes land, update both `design.md` and `example.json` in the same change and keep `docs/components/roadmap.md` as the only planning backlog.

### 2026-04-07

- Revised the clean-slate identity design again so `cid` now means live runtime node id rather than template-node id. Template-level identity is explicitly named `templateNodeId`, while `data-cid` remains the required compact DOM/debugger bridge for mounted nodes and resolves onward to `NodeLocator` plus nested scope-chain state.
- Link to docs: `docs/architecture/template-instantiation-and-node-identity.md`, `docs/architecture/component-resolution.md`, `docs/architecture/debugger-runtime.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flux-core.md`
- Key decision: keep `data-cid` as the final mounted-node lookup marker, but move all compile-time/lowering semantics off `cid` and onto template identities (`templateGraphId`, `templateNodeId`) plus `instancePath`.
- Next step: when code refactoring starts, mount-time runtime ownership must allocate `cid`, while compiler output stops treating any `cid`-like field as a template identity.

### 2026-04-07

- Retired the stale top-level component inventory and moved component planning fully under `docs/components/`: `docs/component-list.md` is replaced by `docs/components/roadmap.md`, while `docs/components/index.md` now requires each component directory to carry both `design.md` and `example.json` and distinguishes registered renderers from planned ones.
- Link to docs: `docs/components/index.md`, `docs/components/roadmap.md`, `docs/components/tabs/design.md`, `docs/analysis/2026-04-01-docs-design-review-2026-03-29.md`
- Key decision: component-related source-of-truth material must stay under `docs/components/`; top-level mixed AMIS/Flux inventories should not survive as a parallel planning surface.
- Next step: add `example.json` for every documented component and run multiple independent audits to catch omissions or stale contracts.

### 2026-04-07

- Expanded the component-design area from the initial `tabs` seed into a full renderer catalog under `docs/components/`, covering current basic/form/data renderers, domain-specific designer/report/spreadsheet shells, and the near-term high-priority generic controls that are planned but not yet implemented.
- Link to docs: `docs/components/index.md`, `docs/components/page/design.md`, `docs/components/form/design.md`, `docs/components/table/design.md`, `docs/components/designer-page/design.md`, `docs/components/report-designer-page/design.md`, `docs/components/spreadsheet-page/design.md`, `docs/components/dialog/design.md`, `docs/components/empty/design.md`
- Key decision: component docs should track both the currently registered renderer surface and the explicitly planned high-priority generic controls, but each document must distinguish between landed runtime behavior and target contract so the docs remain useful without pretending every renderer already exists in code.
- Next step: as new renderers land, keep the corresponding `docs/components/<type>/design.md` files aligned with real renderer metadata and move repeated guidance back into `docs/components/index.md` instead of letting per-component docs drift.

### 2026-04-07

- Replaced the earlier `cid`-centric identity note with a clean-slate architecture doc centered on `compile template once, instantiate runtime instances many times`, including core interface sketches for `TemplateNode`, `NodeInstance`, `NodeLocator`, `NodeState`, repeated-instance `instancePath`, and registry ownership.
- Link to docs: `docs/architecture/template-instantiation-and-node-identity.md`, `docs/architecture/component-resolution.md`, `docs/architecture/debugger-runtime.md`, `docs/index.md`
- Key decision: `cid` is no longer treated as the primary architecture concept; the canonical model is template identity plus runtime instance identity, with `ScopeRef` kept data-only and repeated rows/loops instantiated from one shared compiled template.
- Next step: when code refactoring starts, remove the current compiled-node/live-instance conflation (`createRuntimeState()` on compiled nodes) and introduce an explicit template-instantiation API.

### 2026-04-07

- Removed the short-lived intermediate identity note and folded the stable design into a stronger canonical architecture document that also closes template-graph namespace, repeated cross-instance targeting, DOM inspect, debugger locator identity, and repeated-instance dependency ownership.
- Link to docs: `docs/architecture/template-instantiation-and-node-identity.md`, `docs/architecture/component-resolution.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flux-core.md`, `docs/architecture/debugger-runtime.md`
- Key decision: the canonical runtime address is now `NodeLocator = runtimeId + templateGraphId + templateNodeId + instancePath`; repeated-instance identity and later-compiled fragment ownership are first-class, not debugger/runtime afterthoughts.
- Next step: use these target contracts to drive a full refactor from `CompiledSchemaNode + createRuntimeState()` toward `TemplateNode + instantiate() + NodeInstance`.

### 2026-04-07

- Performed another pass on the new component-doc baseline to remove remaining naming drift and added a practical renderer inventory to `docs/components/index.md`. The list now reflects Flux’s current packages and simplified DSL direction instead of mirroring the full AMIS component matrix, and it explicitly calls out cases where AMIS type names should be collapsed or retired, such as preferring `button` over `action|submit|reset` and avoiding `tpl` as a first-class renderer type.
- Link to docs: `docs/components/index.md`, `docs/components/tabs/design.md`, `packages/flux-renderers-basic/src/schemas.ts`, `packages/flux-renderers-form/src/schemas.ts`, `packages/flux-renderers-data/src/schemas.ts`, `packages/flux-renderers-data/src/chart-schemas.ts`, `docs/amis-types/button.d.ts`, `docs/amis-types/display.d.ts`
- Key decision: component planning should track Flux’s real renderer surface plus near-term gaps, not AMIS’s raw component count; when AMIS splits one concept into several renderer types for historical reasons, Flux should prefer the simpler conceptual type unless there is a concrete runtime need for separate types.
- Next step: use this renderer inventory as the seed list for additional `docs/components/<renderer>/design.md` documents, starting from high-priority generic controls rather than long-tail AMIS parity items.

### 2026-04-07

- Reworked internal `cid` allocation so compiled nodes carry `cid` directly instead of mutating raw schema, and runtime-assembled fragments such as dialog bodies/titles and `dynamic-renderer` payloads now inherit the owner node's compile context to continue the same page-local numbering/path space.
- Link to code paths: `packages/flux-core/src/compiled-cid.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/node-runtime.ts`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-runtime/src/page-runtime.ts`
- Link to docs: `docs/discussions/2026-04-07-cid-allocation-and-runtime-fragment-compilation.md`, `docs/architecture/component-resolution.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flux-core.md`
- Key decision: static `_targetCid` pre-resolution now applies only to unique `componentId`; duplicate ids warn with conflicting compiled `path`s, while `componentName` stays a runtime registry concern because conditional branches may legally reuse names.
- Next step: if later-compile cost becomes material, design a separate `template compile + instantiate` cache rather than reusing full compiled node trees with baked-in `cid`/`path`.

### 2026-04-07

- Rechecked the naming baseline against the actual `@nop-chaos/ui` tabs primitive and corrected the component-doc guidance: direct UI-prop mappings should keep the UI names (`value`, `defaultValue`, `orientation`, `variant`), while renderer-only extensions should not be renamed just for stylistic uniformity. The `tabs` design now uses `value/defaultValue` as the primary active-tab contract and keeps extension flags such as `addable`, `closable`, and `draggable` instead of inventing `allowAdd/allowRemove/allowReorder` synonyms.
- Link to docs: `docs/components/index.md`, `docs/components/tabs/design.md`, `packages/ui/src/components/ui/tabs.tsx`
- Key decision: naming consistency should follow the nearest stable contract boundary; when Flux schema is a thin adapter over a shadcn/base-ui prop, use the same name, and only introduce new names for genuinely renderer-owned capabilities.
- Next step: apply the same “nearest stable contract” test to later component docs so schema names do not drift away from owned UI primitives without a concrete semantic reason.

### 2026-04-07

- Corrected the `tabs` component design after re-checking Flux principles: removed `itemsSource` as a first-class tabs contract field, restored `items` as the single formal collection field, and documented that dynamic collection input should prefer final `items` values via loader output, `${expr}`, or explicitly field-enabled `type: 'source'` rather than a second parallel source field.
- Link to docs: `docs/components/index.md`, `docs/components/tabs/design.md`, `docs/architecture/field-metadata-slot-modeling.md`, `docs/architecture/flux-dsl-vm-extensibility.md`, `docs/architecture/api-data-source.md`
- Key decision: for component DSLs, “one concept, one field” wins by default; separate `xxxSource` fields should not be introduced just to model dynamic values when the existing Flux value model already supports static values, expressions, and opt-in `type: 'source'` carriers.
- Next step: keep checking future component docs against the same rule so collection-like controls do not drift into `items + itemsSource` dual contracts without a stronger architectural reason.

### 2026-04-07

- Expanded `docs/components/index.md` with a concrete naming baseline for component docs, covering identifiers, common region names, event names, action selector forms, ownership naming, styling names, and collection-field conventions; then tightened `docs/components/tabs/design.md` to use Flux-native names such as `items`, `itemsSource`, `defaultActiveKey`, `variant`, `orientation`, `allowAdd`, `allowRemove`, `allowReorder`, `activeOwnership`, and `activeStatePath`, with a dedicated AMIS mapping section kept separate from the main contract.
- Link to docs: `docs/components/index.md`, `docs/components/tabs/design.md`, `docs/references/flux-json-conventions.md`, `docs/architecture/field-metadata-slot-modeling.md`, `packages/ui/src/components/ui/tabs.tsx`
- Key decision: component docs should expose a stable Flux vocabulary first and relegate AMIS terms to migration tables, so future component contracts converge on shared collection/action/style naming instead of accreting per-component aliases.
- Next step: apply the same vocabulary to subsequent component docs and, where necessary, reconcile older architecture text that still uses less standardized collection or state-axis names.

### 2026-04-07

- Tightened `docs/components/index.md` so component-doc authoring now explicitly requires Flux-native naming and schema conventions instead of treating AMIS field names as the default baseline.
- Link to docs: `docs/components/index.md`, `docs/references/flux-json-conventions.md`, `docs/architecture/field-metadata-slot-modeling.md`
- Key decision: component design docs may use AMIS only as a capability reference; final schema/action/slot naming must follow current Flux architecture terminology unless a concrete compatibility constraint is documented.
- Next step: as more component docs are added, keep AMIS compatibility notes in dedicated comparison sections instead of letting upstream names leak into the main proposed contract.

### 2026-04-07

- Added a new per-component design-doc area under `docs/components/`, established `docs/components/index.md` as the entry and directory convention doc, and wrote the first component design input for `tabs` based on the AMIS `tabs` feature set but normalized to Flux renderer/runtime/action/style contracts.
- Link to docs: `docs/components/index.md`, `docs/components/tabs/design.md`, `docs/index.md`
- Key decision: component docs should be development-input documents rather than end-user usage notes, with one directory per component and explicit sections for schema, field semantics, runtime ownership, events/actions, and implementation phasing.
- Next step: continue adding component directories for other high-priority controls and, where patterns repeat, extract shared component-authoring guidance into `docs/components/index.md` instead of duplicating it across every component doc.

### 2026-04-07

- Marked stale dependency-tracking wording inside `docs/plans/35-form-runtime-performance-and-linkage-implementation-plan.md` and `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` as outdated after the new Plan 39 baseline settled. The retained historical text now explicitly points readers to the root-binding dependency model in `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md` instead of silently coexisting as an alternate execution baseline.
- Link to docs: `docs/plans/35-form-runtime-performance-and-linkage-implementation-plan.md`, `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md`, `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md`, `docs/architecture/dependency-tracking.md`
- Key decision: only mark content as outdated where Plan 39 truly supersedes the old dependency model; debugger Plan 34 was left unchanged because it does not carry a conflicting dependency-tracking baseline.
- Next step: if Plan 39 starts landing code, keep pruning any remaining deep-path dependency wording from parent plans instead of letting multiple baselines coexist.

### 2026-04-07

- Audited `docs/plans/34-nop-debugger-ai-diagnostics-improvement-plan.md`, `docs/plans/35-form-runtime-performance-and-linkage-implementation-plan.md`, and `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` against the current code/docs baseline and corrected stale plan metadata: Plan 34 and Plan 35 were moved from `planned` to `in progress`, while Plan 37 stays `in progress` with an explicit note that the remaining dependency-tracking closure work now continues in Plan 39.
- Link to docs/code: `docs/plans/34-nop-debugger-ai-diagnostics-improvement-plan.md`, `docs/plans/35-form-runtime-performance-and-linkage-implementation-plan.md`, `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md`, `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md`, `docs/architecture/debugger-runtime.md`, `tests/e2e/debugger.spec.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-core/src/utils/path.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/reaction-runtime.ts`
- Key decision: treat recent logs, active architecture docs, and landed tests/code as evidence that these plans are no longer in the untouched `planned` state, but keep completion gated on explicit acceptance closure rather than inferring `completed` from partial implementation slices.
- Next step: close Plan 34 after final debugger doc/terminology cleanup, keep Plan 35 open until linkage/presentation follow-up slices land, and decide whether Plan 37 should close directly after Plan 39 or remain as the parent roadmap.

### 2026-04-07

- Added `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md` to turn the new dependency-tracking architecture into an executable code migration plan, and linked it from `docs/index.md`.
- Link to docs: `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md`, `docs/index.md`
- Key decision: execute the code migration in six stages: baseline tests, root-only collector, root-normalized change matching, explicit source/reaction roots, fragment/child-scope reconcile, and table row-scope invalidation.
- Next step: start with Phase 0 regression harness and Phase 1 collector changes.

### 2026-04-07

- Revised `docs/architecture/dependency-tracking.md` to replace the earlier static-extraction-plus-runtime-union direction with an explicit-root-first, lexical-root runtime fallback model, and aligned `docs/architecture/api-data-source.md` to the same baseline.
- Link to docs: `docs/architecture/dependency-tracking.md`, `docs/architecture/api-data-source.md`
- Key decision: dependency tracking should converge on lexical root bindings such as `user`, `row`, and `record`; explicit declarations are authoritative when present, and table/loop owners must translate collection updates into row-local invalidation instead of widening everything to the whole collection binding.
- Next step: implement the self-write guard refinement, root-only collector behavior, and row-scope reconciliation in code.

### 2026-04-07

- Created `docs/architecture/dependency-tracking.md` as a standalone design document for dependency tracking architecture. Documents the current fully-implicit Proxy-based tracking model, six identified gaps (cold-start, wildcard over-invalidation, ephemeral eval discarding, no explicit declaration, imprecise self-write guard, dual dependency systems), and a convergence path toward "declarative hints + implicit fallback" with five phases.
- Key design: three-layer dependency resolution (Schema `dependsOn` → compile-time AST extraction → runtime Proxy tracking), where higher layers provide initial deps before first evaluation and runtime tracking updates deps after each evaluation.
- Next step: Phase 1 (self-write guard refinement) and Phase 2 (compile-time static extraction in `packages/flux-formula/src/static-deps.ts`).

### 2026-04-07

- Reviewed scope-owned cleanup against dialog lifecycle, recorded the conclusion in `docs/discussions/2026-04-07-scope-level-dispose-and-dialog-lifecycle.md`, and added a regression test covering dialog-scoped polling source cleanup on close.
- Link to code paths: `packages/flux-react/src/index.test.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/source-registry.ts`
- Key decision: current React-host correctness relies on subtree unmount cleanup rather than a unified runtime `disposeScope(...)` call; closed dialogs are removed from `page.store.dialogs`, so dialog-local polling timers stop through renderer cleanup and `controller.stop()`.
- Next step: if runtime later needs non-React hosts, retained/suspended subtree semantics, or non-renderer-owned scope registrations, expose a scope-level disposal path as a runtime backstop.

### 2026-04-07

- Recorded the scheduling-model review in `docs/discussions/2026-04-07-scheduling-model-settled-update-turn-clarification.md` and tightened `docs/architecture/frontend-programming-model.md` so `Settled Update Turn` is documented as a runtime-store settlement boundary rather than a macro-task or host-scheduler contract.
- Key decision: keep `Settled Update Turn` as a semantic store/publication boundary, but reject both host-scheduler overreach and the proposed schema-level `$flush` escape hatch; current baseline remains per-reaction local coalescing rather than a globally visible scheduler.
- Next step: if tests or debugger tooling later need explicit drain hooks, design them as runtime/debug APIs instead of adding a new author-visible action surface.

### 2026-04-07

- Completed `xui:imports` expression projection so imported aliases now work in expressions as `$alias.method(...)` alongside existing `namespace:method` action dispatch.
- Link to code paths: `packages/flux-formula/src/compile.ts`, `packages/flux-react/src/useNodeImports.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/index.test.tsx`
- Key decision: keep import-expression support narrow and explicit by rewriting only the documented `$alias.method(...)` form into generated formula functions backed by the same imported namespace provider map used for action dispatch.
- Next step: if schemas need broader imported-expression syntax later, extend from this validated `$alias.method(...)` baseline instead of relying on parser-specific ambient member access behavior.

### 2026-04-07

- Aligned normative architecture docs to the `name` + `mergeToScope: true` Resource publication convergence direction. Updated `docs/architecture/api-data-source.md` (interface, binding target, examples, status notes), `docs/references/terminology.md` (DataSourceSchema entry), `docs/articles/flux-design-introduction.md` (data-source example and description), `docs/examples/user-management-schema.md` (clarified ActionSchema.dataPath vs DataSourceSchema.dataPath).
- Key decision: `dataPath` on `DataSourceSchema` is now documented as legacy compatibility-only across all normative docs; new schema should use `name` as the sole publication path with optional `mergeToScope: true` for object-result scope merge.
- Next step: continue converging runtime code toward `name`-first identity and registration.

### 2026-04-07

- Centralized field-level `type: 'source'` prop resolution through renderer metadata instead of renderer-local hooks.
- Link to code paths: `packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/use-node-source-props.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-renderers-form/src/renderers/input.tsx`
- Link to doc update: `docs/architecture/field-metadata-slot-modeling.md`
- Key decision: keep source support explicit with `SchemaFieldRule.allowSource` so only renderer-owned value fields opt into anonymous source execution.
- Next step: expand `allowSource` to additional proven value fields after validating behavior for form options and basic text content.

### 2026-04-07

- Added `SchemaFieldRule.sourceStateKey` so source-enabled props can inject one companion transient-state object into renderer props.
- Link to code paths: `packages/flux-core/src/types/schema.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/use-node-source-props.ts`, `packages/flux-renderers-form/src/schemas.ts`, `packages/flux-renderers-form/src/renderers/input.tsx`
- Link to tests: `packages/flux-react/src/index.test.tsx`, `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: model loading/error/status as one narrow UI-facing state object instead of scattering multiple sibling props.
- Next step: keep the transient-state contract narrow and only adopt it where a renderer actually needs loading/error chrome.

### 2026-04-07

- Extended `optionsSourceState` consumption from `select` to `radio-group` and `checkbox-group` so all source-backed option-group controls show loading chrome consistently.
- Link to code paths: `packages/flux-renderers-form/src/renderers/input.tsx`, `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: keep option-group loading UX minimal and consistent: inline spinner plus temporarily disabled option controls.
- Next step: only add error-specific inline chrome if a concrete renderer needs it; for now the transient state contract remains available without forcing every renderer to render errors.

### 2026-04-07

- Verified concrete source error messages surface to option controls when the fetcher fails by throwing, and covered `select` / `radio-group` / `checkbox-group` with regression tests.
- Link to tests: `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: preserve the typed fetcher contract and treat thrown fetcher errors as the canonical path for renderer-visible source failure messages.
- Next step: if needed, formalize non-throwing API failure payload semantics separately rather than overloading `ApiResponse<T>` with ad-hoc `error` fields.

### 2026-04-07

- Aligned request execution with the fetcher contract: non-OK `ApiResponse` values are now converted into thrown errors inside `executeApiSchema`, so runtime callers observe successful `data` or an exception.
- Link to code paths: `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: `ApiResponse` remains a fetcher boundary object; renderer/runtime callers should not branch on `ok === false` and instead rely on thrown failures.
- Next step: document the fetcher contract in a stable reference doc if this behavior will be exposed to host integrators directly.

### 2026-04-07

- Updated architecture/reference docs to make the fetcher boundary explicit: `ApiResponse` belongs to `env.fetcher(...)`, while runtime consumers receive adapted data or a thrown error.
- Link to docs: `docs/architecture/api-data-source.md`, `docs/references/renderer-interfaces.md`, `docs/references/terminology.md`
- Key decision: keep the non-OK `ApiResponse` fallback as an internal compatibility path, but document thrown failures as the intended host integration contract.
- Next step: if external integrator docs are added later, reuse this same wording so host fetcher implementations do not depend on deprecated non-OK response handling.
