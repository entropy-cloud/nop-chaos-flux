# Development Log - 2026-08-25

### 2026-08-25（mission `ai-widgets-product`：plan `2026-08-25-0440-2` 执行——fixture 中文关键词分发 + e2e 货币/`\(` 数学增强）

- **背景**：执行 `docs/plans/2026-08-25-0440-2-fixture-zh-dispatch-e2e-math.md`（2 Phase 全 planned，Test Strategy「必须自动化」proof-first）。收口两条延后增强：0410 plan Non-Blocking Follow-ups 的 e2e 层货币 / `\(` fixture 增强 + `docs/backlog/audit-followups-2026-08-24-0803.md` 备注的审计盲区切入点（`pickAiWidgetsFixture` 仅英文 keyword，中文提问全部落入 default preset）。
- **Phase 1（中文关键词分发，unit 层）**：Proof 先红——新 colocated `apps/playground/src/ai/ai-widgets-fixture.test.ts`（中文 5 输入命中 + 中英混合 `'weather 天气'` + 多关键词 KEYWORD_ORDER 优先级（`'这个推理和公式哪个更基础'` → formula，与文本位置无关）+ 无关键词中文回落 default + 英文 5 关键词回归），中文断言组实测红（2 failed——全部回落 default）。Fix——`ai-widgets-fixture.ts` `KEYWORD_ORDER` 条目扩为 `{ keywords: ['weather', '天气'], id }` 别名组（weather/天气、code/代码、formula/公式、reasoning/推理、citation/引用），匹配循环 `keywords.some(includes)`，顺序语义与 default 回落不变；头注约束清单补中文别名 echo-collision 行（别名不得出现在任何 preset / default 正文首句，fixture 正文英文天然满足）。转绿：playground 27 files / 181 tests 全绿，英文分发零回归。
- **Phase 2（formula fixture 增强 + e2e 断言）**：Proof 先红——`tests/e2e/ai-widgets-fixture.spec.ts` formula 用例追加 (a) 货币段（`p` hasText `'A premium plan costs'`）无 `.katex`（count 0）且 `$5` 字面可见、(b) `\(E = mc^2\)` 行内 paren 渲染 `span.katex` 且 `.katex-display` 计数保持 1（paren 行内不产生块级）+ 新增中文分发用例（`'解释一下质能公式'` → blockquote 可见）；fixture 追加前 (a)/(b) 实测红（货币段落定位超时）。Fix——formula preset **末尾 append-only** 追加两段：`A premium plan costs $5 today and $10 tomorrow.`（0410 货币消歧行为 e2e 层可观测：普通段落、无 `.katex`、金额字面）+ `In inline form, \(E = mc^2\) holds for every inertial frame.`（0410 paren 映射行为 e2e 层可观测）；既有 blockquote / `$$` 块 / `$...$` 行内字节不动（git diff 审计确认）。owner-doc 同步——`product-spec.md` §4.2 补中文别名表（5 中文名 + KEYWORD_ORDER 顺序语义 + default 回落 + echo-collision）+ preset 表关键词列中英并注 + formula 行描述/覆盖/锚点补货币句与 `\(…\)`；§4.1 附加项口径补 paren 定界符与货币句语义（顺带修正 D6 后已 stale 的「文本含 `$$`」锚点为 `span.katex`）；fixture 头注 formula 定界符说明同步。
- **验证（full-green，口径 = 全量单测 + AI 面 e2e，沿用 DV/0410/0440-1 先例）**：全仓 `pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks（playground 27 files / 181 tests，含新增 fixture dispatch 套件）、`pnpm check` exit 0（零新增红）；**AI 面 e2e 19 文件 147 passed / 0 failed**（`ai-*.spec.ts` 全家族两批跑取：54 + 93，另 2 例 attachments 首轮环境级 flaky 经 retries=1 吸收后通过——dev-server 冷启动延迟，登记于 playwright.config.ts 的既有模式；本 plan 改动面 `ai-widgets-fixture.spec.ts` 7 测 + `ai-widgets-demo.spec.ts` 17 测（G5 在内）全绿，`ai-widgets-demo.spec.ts` 零改动）。
- **收口**：2 Phase 全 completed；独立 closure-audit（fresh sub-agent task `ses_fca5c6f0bffeWwhBs93xTXyt4w`，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major / 1 Minor closure 时序 bookkeeping / 1 Nit 断言超时惯例，10 项 live-repo 核对全过：别名表与 live KEYWORD_ORDER 一致、append-only git diff 证实、中文别名零碰撞、unit/e2e 覆盖面、product-spec 一致性、`\[` deferral 正当（unit proof 存在）、demo 路径可达性）；plan Closure Gates 全勾 + `Plan Status → completed`。backlog `audit-followups-2026-08-24-0803.md` 备注行补处置标注（盲区切入点由本 plan 收口）；plan 无 `> Source Audits:` 行（followup-sourced，roadmap D6 已 done、无对应 work item 行——追踪链 = 0410 plan follow-ups + 0803 backlog 备注 → 本 plan）。

### 2026-08-25（mission `ai-widgets-product`：plan `2026-08-25-0440-1` 执行——0803 audit P2×5 批量收敛（dark guard / lowlight 声明 / feedback 播种 / setSenderDraft 清空 / useMemo 裁定））

- **背景**：执行 `docs/plans/2026-08-25-0440-1-audit-p2-remediation-batch.md`（3 Phase 全 planned，Test Strategy「必须自动化」proof-first）。把 `docs/backlog/audit-followups-2026-08-24-0803.md` 登记的 5 条 P2 收敛到各自明确 disposition；与 0410 plan（同源审计 P1×3，已收口）closure surface 不同源，零折叠。
- **Phase 1（P2-1 dark media 轨 light guard）**：`styles.css` 3 处 `@media (prefers-color-scheme: dark)` 块（typography `:282` / avatar `:336` / welcome `:361`）内层 selector 加 `:root:not([data-mode='light'])` 前缀——显式 light（`data-mode` 挂 root，theme-compatibility.md:165 先例）下 OS dark 偏好让位基线 light 值，standalone host 不再在浅色页面解析出 literal dark fallback；trigger-2 `[data-mode='dark']` 块不动（显式 dark 恒胜，两 dark 轨 token 相同无视觉影响）。Proof 先红后绿：`markdown-content.test.tsx` (c1) 扩展——新 helper `darkPrefersColorSchemeBlocks`（括号深度解析提取 media 块内文）+ `occurrences`，断言 media 块恰 3、每块 guard 数 == 规则数（内层 selector 全 guard）、全文件 guard 恰 3（红：guard 0/块 → 绿）。design.md §10.7 dark 双触发 bullet 补 light guard 语义一句。
- **Phase 2（P2-3 播种 + P2-4 清空）**：P2-3——`ai-feedback.tsx` `voted` 改惰性播种 `useState(() => metadata?.feedback ?? null)`（mount 读一次；限制语如实记入 renderers.md §8：同 mount 引用切换不重播种），虚拟列表回收 / 分支切换重挂载后视觉态与 D4 持久化 metadata 不再脱钩；Proof 3 用例先红后绿（like/dislike 播种 + 无 metadata 回归）。P2-4——`ai-component-handle.ts` `setSenderDraft` 校验次序重排：type check（不变）→ channel 存在性（不变，清空也需 channel）→ mode 解析前移 → 空串策略（`replace`+`''` 放行为合法清空 `{ok:true}`；`append`+`''` 保持拒绝、文案与缺参拒绝可区分）；Proof 4 用例先红后绿（replace 清空红→绿、append 空串拒绝、replace/append 非空回归 ×2；既有缺参/空串默认 append 拒绝用例不变仍绿）。renderers.md §13b 补清空契约、§8 补播种语义。
- **Phase 3（P2-2 lowlight 声明 + P2-5 useMemo 裁定 + backlog 清零）**：P2-2——playground `dependencies` 补 `lowlight@^3.1.0`（与 content 包区间对齐），`pnpm install` 后 lockfile importer 记录直接依赖，消除 auto-install-peers / 经 content 包传递解析的隐式依赖（bug 167 若走 removal 不再连坐 showcase 高亮构建失败）。P2-5 三处逐一裁定（先核实 Compiler 覆盖面：`reactCompilerPreset` 仅 playground vite dev/build（`apps/playground/vite.config.ts:15`，workspace alias 指 src）；包 dist 构建纯 tsc、vitest 不走 babel/react-compiler）：① `ai-chat.tsx:261` senderDraftStore——**keep-with-reason**（identity 流入 componentHandle register-effect deps 与 Provider-bound context value，未编译环境移除即每 render 重建 store 丢 draft；注释更新指向本裁定）；② demo `:203` env 工厂——**removed**；③ demo `:205` importLoader pair——**removed**（demo 页仅运行于 Compiler 全覆盖环境，纯工厂纯冗余；非 scope 的 connector/decoratedEnv/pageData memo 未动）。backlog 5 条全部标注处置（清零）。
- **验证（full-green，口径 = 全量单测 + AI 面 e2e，沿用 DV/0410 先例）**：全仓 `pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks（flux-renderers-ai 84 files / **789 passed** = 781 基线 + 8 新增：c1 扩展 1 + feedback 播种 3 + handle 清空 4；各 Phase 后全量 `pnpm test` 均零回归）、`pnpm check` exit 0（零新增红）；**e2e `ai-widgets-*` 家族 4 spec 35 passed / 0 failed**（含 G6 lowlight 高亮、DV light/dark computed-style typography、streaming cadence——覆盖 useMemo 移除后 demo 页 env/engine 稳定性）。
- **收口**：3 Phase 全 completed；独立 closure-audit（fresh sub-agent task `ses_fca6c4376ffe0tK8ICri7tII6x`，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major，10 项 live-repo 核对全过：guard 计数、播种行、校验次序、manifest/lockfile、三 disposition 依据、owner-doc 一致性、backlog 处置、诚实性、Failure Paths 5 行覆盖、文本一致性）；plan Closure Gates 全勾 + `Plan Status → completed`。0803 audit 文件无 `> Source Audits:` 行（audit-sourced plan，且该 audit 已于 0410 收口时 `closed`）——追踪链 = audit → P1 plan（0410，closed）+ P2 backlog（本 plan 清零）。

### 2026-08-25（mission `ai-widgets-product`：plan `2026-08-25-0410-1` 执行——ai-bubble 数学定界符管线收敛（0803 audit P1×3 remediation））

- **背景**：执行 `docs/plans/2026-08-25-0410-1-ai-bubble-math-delimiter-pipeline-remediation.md`（3 Phase 全 planned，Test Strategy「必须自动化」proof-first）。修复 `docs/audits/2026-08-24-0803-open-audit-ai-widgets-product.md` 三条 P1：code 内 `$$` 截断完成态消息（P1-1）、完成态货币美元被 remark-math 排版为乱码数学（P1-2）、`\(...\)`/`\[...\]` 主流定界符永不渲染 + owner-doc 漂移（P1-3）。
- **Phase 1（buffer 层 code 掩码统一，P1-1）**：`markdown-buffer.ts` `findUnclosedMathCutoff` 的 `$$`/`\[`/`\(` 计数改为经 `computeCodeRegionMask`（新导出，code 区域识别单一定义点）过滤掩码匹配，与单 `$` 扫描器共用同一掩码（消除同文件两套 dollar 语义）；fence 识别口径收敛——`findUnclosedFenceCutoffForKind` 的 `(^|\n)({ch}{3,})` 对齐 `maskFencedCode` 的 `^ {0,3}`（≤3 空格缩进 fence 两扫描器同身份，截断下标含缩进即 fence 行起点）。Proof 先红后绿：bash `$$` PID 完成态全文（audit live-probe 原 case）、PHP `$$var`、inline code `` `$$` ``、掩码后非码区未闭合 `$$` 仍截断、`\(`/`\[` 掩码、缩进 fence 3 用例（7 红→全绿；「balanced indented fences」1 例本绿为过切守卫）。
- **Phase 2（渲染层预处理，P1-2+P1-3 实装半）**：Decision (b) 渲染前货币消歧（拒绝 `singleDollarTextMath:false` 破坏已交付 `$...$` 契约、拒绝 wont-fix 登记）。新 `math-delimiter-preprocess.ts`（纯字符串函数，INV-1）固定两遍：① 原文单 `$`+数字（非 `$$`-run、非 `\$`、非 code）→ `\$`；② 配对 `\(`/`\)`→`$`、`\[`/`\]`→`$$`（自左向右配对、孤立闭保持字面、`\\(` 偶反斜杠 run 不映射；映射生成 `$` 不回头参与 ①）。`markdown.tsx` 接线 `preprocessMathDelimiters(safeMarkdownSlice(raw))` → sanitize → ReactMarkdown。Proof 先红后绿：两金额句普通 `<p>` 无 `.katex`（audit live-probe 原 case）、`\(` 行内 / `\[` 块级渲染数学、数字开头逃生口 `\(3 \times 10^8\)`（映射）+ `$$5x + 1$$`（`$$`-run 免疫）、code 内 `\(`/`$5` 字面（inline + fenced）、`\$5` 不二次转义、孤立 `a \] b` 无数学且 `]` 可见（4 红→全绿）。
- **Phase 3（owner-doc 语义表同步，P1-3 文档半 + G4）**：`design.md` §10.4 新增**数学定界符语义表**（buffer 截断 / 渲染预处理 / remark-math 三层 × 8 输入形态：code 字面、`$$` 块级、`$` 行内含货币契约与 close 侧差异注记、`\(`/`\[` 配对映射、孤立闭、`\$`、`\\(` 退化类、非法 LaTeX in-band error）+ 两遍固定顺序说明 + fence 口径注记；:378 LaTeX 叙事漂移修正（"仅 buffer 边界"→"映射后渲染"）；文件树补 `math-delimiter-preprocess.ts` 与掩码导出。`renderers.md` §3.3 表后登记 markdown 数学定界符契约（`$`/`$$`/`\(`/`\[` 集合 + 字面例外，与语义表同源）。
- **验证（full-green，口径 = 全量单测 + AI 面 e2e，沿用 DV 先例）**：`pnpm --filter @nop-chaos/flux-renderers-ai test` 84 files / **781 passed**（764 基线 + 17 新增：buffer 8 + 渲染层 9；Phase 1 后 772 全绿零回归、Phase 2 后 781 全绿零回归）；全仓 `pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks 全绿、`pnpm check` exit 0（仅 2 exempt i18n locale 既有登记红，零新增命中）；**AI 面 e2e 19 文件 130 passed / 0 failed**（ai-widgets-demo / fixture / showcase / button-actions 35 + 其余 ai-\* 家族 95，本 plan 改动面 e2e 零回归，与 DV 基线 130 全绿口径一致）。
- **收口**：3 Phase 全 completed；独立 closure-audit（fresh sub-agent task `ses_fca885420ffes1M0fk69RPyr4s`，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major / 2 Minor 非阻塞：`$x$5` 差异注记无独立 proof、语义表 `$$` 行措辞——均与 plan scoping 一致）；plan Closure Gates 全勾 + `Plan Status → completed`；0803 audit 3 条 P1 全修复 → `Audit Status: planned → closed`；5 条 P2 维持 `docs/backlog/audit-followups-2026-08-24-0803.md`（不同源，non-blocking）。roadmap 无对应 work item（audit-sourced plan，D6 已 done，追踪链 = audit → plan → followups backlog）。

### 2026-08-25（mission `ai-widgets-product` DG：plan `2026-08-25-0215-2` 收口——owner-doc 同步 + bug 167 登记 + closure log + mission closeout 材料备齐）

- **背景**：执行 `docs/plans/2026-08-25-0215-2-dg-owner-doc-sync-closure.md`（3 Phase 全 planned；纯文档 + 收口登记，零代码变更——Closure Gates 按 plan guide 模板豁免 typecheck/build/lint/test）。本 plan 是 roadmap `docs/backlog/ai-widgets-product-roadmap.md` 最后一项；mission 是否 closeout 由 engine 依审计轮次决定，本 plan 只备齐材料。
- **Phase 1（owner-doc 同步）**：`design.md` 新增 **§10.7 `ai-bubble-typography`**（G11 收口：scoped `[data-slot='ai-bubble-markdown']` 自定义 CSS + 拒绝 `@tailwindcss/typography` 体积依据（tarball ~25-30KB / unpacked ~78KB / gzip ~17KB）+ dark 双触发（`prefers-color-scheme` + `[data-mode='dark']`）+ D6 增量（`.katex` 容器排版 + `.tok-*` 配色）+ rich-text 独立 scope 边界，只写最终设计状态）；`renderers.md` 三块补齐——**§3.1c 气泡 avatar**（lucide `<Bot/>`/`<User/>` 按 role + `data-role` + 32×32 圆形 token 规格 + `avatar` ReactNode 扩展位 + `showAvatar` 从 ai-chat/ai-message-list 的转发链）、**§13b `component:setSenderDraft`**（args `{ text, mode?: 'append'|'replace' }` 默认 append、`\n` join 保留键入、同值 dedupe、cid 隔离、缺 text 拒绝）、**§13c `ai` namespace 8-action 清单 + `ai:regenerate` truncate-rerun 语义**（丢弃尾部 assistant 轮次重新请求、计数不变、branchId 更新、busy 拒绝）。配套：§1.2/§2.1 schema 块补 `showAvatar`、§3.1 补 `avatar`；`avatarRegion` 死字段按 contract-honesty 残留注记模式移除（§14.2 示例同步修正）。层化说明：design.md §14.2/§14.3 的 7-action/6-method 表为 P1/P2 基线，D4 增量权威清单在 renderers.md §13b/§13c（与 `AI_NAMESPACE_ACTIONS`/`AI_COMPONENT_METHODS` drift-guard 测试同步）。Exit grep 全过：`ai-bubble-typography` design.md 1 段命中、`setSenderDraft`/`ai:regenerate` renderers.md 各 ≥1、avatar 段 32×32/lucide/showAvatar 三关键词齐、`prose` 在 markdown.tsx 0 命中。
- **Phase 2（bug 167 登记）**：新 `docs/bugs/167-diff-view-lowlight-zero-grammar-registration.md`（open，登记不修复）——根因 `packages/flux-renderers-content/src/diff-view/adapters/syntax-highlight.ts:7` 裸 `createLowlight()` 零 grammar 注册（lowlight@3 无参构造 = 空注册表，`highlight()` 对任何非 plaintext 语言抛 `Unknown language`），三消费视图（diff-split `:6` / diff-unified `:5` / diff-three-column `:7`）语法高亮**从未生效**，`:73-74` catch 静默回退 `escapeHtml` 掩盖；修复方向 `createLowlight(common)`（参照 D6 `markdown.tsx:112` 接法）；successor 路由 content 包 owner（ai-invariant-loop 轮次或独立修复 plan），不属本 mission 授权目录；范围澄清注明与本 mission 已修代码零交叠。
- **Phase 3（closure log + roadmap 状态 + closeout 材料）**：见下述各节。

#### G1–G12 闭合 checklist（gap → 闭合 phase → 证据锚点）

| Gap                           | 闭合 | 证据锚点（file / spec / test 名）                                                                                                                                                                                                                                                                    |
| ----------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1 mock 回复 8 词贫瘠         | D1   | `apps/playground/src/ai/ai-widgets-fixture.ts`（6 preset + 11 元素覆盖矩阵）；`mock-ai-env.ts` `createMockAiEnv({ delayMs, fixtures })`；e2e `ai-widgets-fixture.spec.ts` 6 测试                                                                                                                     |
| G2 `prose` 死类裸默认排版     | D2   | `packages/flux-renderers-ai/src/styles.css` `[data-slot='ai-bubble-markdown']` 150 行段；`markdown.tsx:47` 容器仅 `max-w-none break-words`（prose 0 命中）；unit `markdown-content.test.tsx` 4 断言组；e2e typography light/dark（`ai-widgets-demo.spec.ts`，DV 落地）                               |
| G3 avatar 空 div 不可见       | D3   | `ai-bubble/index.tsx:143-148`（`props.avatar ?? (role==='user' ? <User/> : <Bot/>)`）；`styles.css [data-slot='ai-bubble-avatar']` 32×32/9999px；unit `renderers/__tests__/ai-bubble-avatar.test.tsx` 5 case；e2e avatar 32×32 圆                                                                    |
| G4 全部按钮唯一副作用 toast   | D4   | `ai-component-handle.ts` `setSenderDraft`（`AI_COMPONENT_METHODS` 7 项）；`ai-action-provider.ts` `regenerate`（`AI_NAMESPACE_ACTIONS` 8 项）；`ai-feedback.tsx` 写 `message.metadata.feedback`；e2e `ai-widgets-button-actions.spec.ts` 7 测试                                                      |
| G5 LaTeX 不渲染               | D6   | `markdown.tsx:4-6,49-50`（`remark-math` + `rehype-katex` 硬 peer）；e2e 'formula keyword renders KaTeX spans (G5)'。**human gate supersession 链**：2026-08-23 重新决策内置，`design.md` §10.4 + `improvement-analysis.md` §4.2 + plan A3 Deferred 段三处 supersede（保留旧裁定 + 指向 roadmap §D6） |
| G6 fenced code 无高亮         | D6   | `markdown.tsx:112` `createLowlight(common)` + `styles.css` `.tok-*` 调色板；e2e 'code keyword renders lowlight token spans (G6)'                                                                                                                                                                     |
| G7 showcase 仅 6 widget       | D5   | `ai-widgets-demo.tsx` 2 缩略卡（`# /ai-tools` / `# /ai-citations`）+ fixture 三类专属触发；e2e `ai-widgets-showcase.spec.ts` 5 测试（首屏计数 9 ≥ 8）                                                                                                                                                |
| G8 welcome icon 字面字符串    | D3   | `ai-welcome.tsx` lucide 预设分发 + `AiWelcomeSchema.iconLucide`；e2e 'welcome icon renders the Bot lucide preset'                                                                                                                                                                                    |
| G9 suggestion pill emoji      | D4   | `ai-suggestions.tsx` `SUGGESTION_ICON_PRESETS` 5 项；e2e ⑥ pill svg 非 emoji                                                                                                                                                                                                                         |
| G10 流式节奏不可感知          | D1   | `ai-widgets-demo.tsx` `{ delayMs: 200, fixtures: true }`（其余 12 调用点默认 15 不变）；e2e 'streaming cadence'（DV 落地）；注记 bug 166 约束（chunk 不逐帧上屏，default preset ≤~42 chunk 上限写入 fixture 头注释）                                                                                 |
| G11 owner-doc typography 缺段 | DG   | `design.md` §10.7 `ai-bubble-typography`（本 plan 落地）+ `renderers.md` §3.1c/§13b/§13c                                                                                                                                                                                                             |
| G12 e2e 无视觉产物断言        | DV   | `ai-widgets-demo.spec.ts` 10→17（typography light/dark computed-style + cadence）；product-spec §7 14 面证据矩阵闭合（本日 DV 条目）                                                                                                                                                                 |

**12/12 闭合**（G5 含 human gate supersession 链注记）。

#### 各 phase 收口摘要（D0–D6 + DV）

- **D0**（2026-08-24，plan `2026-08-24-1045-1`）：`product-spec.md` 落地（11 元素矩阵 + 6 preset + typography/avatar 规格 + §7 断言清单）；closure-audit approved（`ses_fcc2a2d57ffe9LVbbSmS1ww73I`）。
- **D1**（2026-08-24，plan `2026-08-24-1045-2`）：6 preset fixture + env 工厂 option 化（`delayMs`/`fixtures`）+ e2e 6/6；发现并登记 bug 166（流式 chunk 不逐帧上屏，路由 successor）。
- **D2**（2026-08-24，plan `2026-08-24-1045-3`）：`prose` 类移除 + 150 行 scoped CSS（≤150 红线贴线达标）+ dark 双轨；closure-audit pass 8/8（`ses_fcbe229a9ffehH6ArhZF8ran77`）。
- **D3**（2026-08-25，plan `2026-08-24-2237-1`）：气泡 avatar（lucide + 32×32 圆）+ welcome icon 分发 + `showAvatar` 转发链；closure-audit issues→approved（`ses_fcb6fcb34ffeRjMSZYm7xAB9It`）。
- **D4**（2026-08-25，plan `2026-08-24-2317-1`）：`setSenderDraft` + `ai:regenerate` + feedback 写 metadata + pill lucide；ai 包 764 tests + e2e 7/7；closure-audit approved（`ses_fcb0a4058ffe8OrqqjDZ8iJ9A6`）。
- **D5**（2026-08-25，plan `2026-08-24-2237-2`）：showcase ≥8（6 live + 2 卡 + citations 实例 = 9）+ fixture 三类触发；closure-audit issues→approved（`ses_fcb529d07ffeMGH4kDjbl0IwXQ`）。
- **D6**（2026-08-25，plan `2026-08-24-2237-3`）：LaTeX（remark-math/rehype-katex/katex 硬 peer）+ lowlight 高亮 + 流式边界扩展；closure-audit approved（`ses_fcb30af23ffeyZjbRptL7F0foE`）。
- **DV**（2026-08-25，plan `2026-08-25-0215-1`）：§7 证据矩阵 14/14 + route-inventory 红消解 + 产品快照落档。**full-green 验证状态（引用 DV 收口记录，本日 DV 条目）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks、`pnpm check` exit 0；全量 e2e **1310 passed / 14 failed / 43 skipped**——14 条失败全部 stash 实证为 master 干净树同红的登记 baseline red（barcode ×5 / crud-demo ×6 / sundial ×2 / scada ×2，归各自 owner），ai 家族 19 文件 130 测试全绿。

#### 6 条偏差注记（D4/D5/D6 收口记录指定 DG 处置；不回写 roadmap / 历史计划原文）

1. **D4 / truncate-rerun**：roadmap §D4 / product-spec §7「refresh → assistant 消息数 +1」措辞 vs live 引擎 truncate-rerun（计数不变 + branchId 更新 + 重新流式）——product-spec §7 行已由 DV 修正（本日 DV 条目）；roadmap §D4 正文措辞按 roadmap Rule（动态区仅 Work Item Status 表）不回写，以本注记为准。
2. **D4 / D-race**：roadmap §D4「失焦 / 显式触发时执行」draft-race 措辞被 D-race 裁定舍弃——实装为 append-保留 + 同值 dedupe（`AiSenderDraftStore.apply`，键入 `setLocal` 写透防回环），交付其意图（不打断键入 + 不重复追加）而无时序限定。
3. **D5 / 缩略卡**：roadmap §D5「3 个缩略卡」vs 实装 2 卡（`# /ai-tools` + `# /ai-citations`）+ reasoning 例外（ai-bubble content renderer，无路由无卡）；「fixture A–F 每个 ≥1 种新 widget 触发」vs D-c 三类专属触发分布（weather→toolRound / reasoning→reasoning_content / citation→`[N]` marker；default/code/formula 保持纯 markdown 保 D1 基线与 10s 预算）。
4. **D5 / §6.1**：product-spec §6.1 首屏计数字面规则（distinct slot 排除 4 常驻件 →「现状 = 6」）与嵌套 slot 实测基线（>8）自相矛盾——showcase spec (a) 按 §6.2 目标清单白名单计数为可执行口径（已绿）；§6.1 修文按 DG plan Deferred But Adjudicated 裁定 out-of-scope improvement（no successor，注记即闭环）。
5. **D6 / A3 占位**：supersession 链中 plan A3 指向从未起草的占位路径（`2026-08-23-0002-1-a7-d6-latex-impl.md`）——D6 plan `2026-08-24-2237-3` 即真身，`design.md` §10.4 已回填 canonical 指针；按 plan guide Rule 21 不回写历史计划原文。
6. **D6 / D-b**：roadmap §Cross-Cutting「try/catch dynamic require 缺失降级」措辞被 Decision D-b 按硬 peer 语义 supersede——实装静态 import（缺 peer = 安装期失败，无运行时降级路径），与 `react-markdown`/`rehype-raw` 治理一致。

#### roadmap 状态 + human gate 待签 + mission closeout 材料核对单

- **roadmap Work Item Status**（状态机校验，无提前翻转）：D0–D6 + DV 全 `done`（各自 closure-audit 通过后流转）；**DG**——独立 closure-audit（fresh sub-agent `ses_fcac4369fffemymHQo7dvU0v9n`，非执行 session，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major / 1 Minor：design.md「≤150 行红线」措辞可误读为全文件上界，已按审计建议修正为「D2 增量 150 行贴红线」）后：plan Closure Gates 全勾、`Plan Status → completed`、roadmap DG `planned → done`。**全表 D0–D6/DV/DG done，roadmap 八 phase 全闭环。**
- **human gate 待签项（本 plan 不代签）**：roadmap Rule「双向范围独立声明（人工 gate 签字）」——本图与 `ai-invariant-loop-roadmap.md` / `component-audit-roadmap.md` / `component-audit-round2-roadmap.md` 范围不重叠为**单向声明**，closure 时由 human 显式签字承认该单向性；反向 ack 留待 ai-invariant-loop 下一轮修订时加入。**状态：待签**。
- **mission closeout 材料核对单（供 engine 决策引用）**：(a) roadmap 全 phase done——D0–D6/DV/DG 全表闭环；(b) closure log 完整——本条目（G1–G12 逐条 + phase 摘要 + full-green 引用）；(c) 偏差注记齐备——6/6 落档；(d) bug 167 已登记并路由 successor（content 包 owner）；(e) bug 166 已登记（D1，路由 successor）；(f) human gate 待签 1 项（上）；(g) mission closeout 决策权归 engine（依审计轮次），本 plan 不自行宣布。

### 2026-08-25（mission `ai-widgets-product` DV：plan `2026-08-25-0215-1` 全量验证与 e2e 增量化执行——§7 证据矩阵闭合 + route-inventory 红消解 + 全量链/全量 e2e）

- **背景**：执行 `docs/plans/2026-08-25-0215-1-dv-full-verification-e2e.md`（3 Phase 全 planned；roadmap §DV + product-spec §7 为契约）。把 D0–D6 产品化改动收口到仓库级可验证基线：补 §7 断言清单 e2e 层真缺口 3 面（typography light/dark + 流式节奏）、修登记在册的 route-inventory 红（5 条路由缺 smoke 断言）、跑通全量验证链 + 全量 e2e、落档产品级快照。改动面：`tests/e2e/ai-widgets-demo.spec.ts`（+3 测试，14→17）、`tests/e2e/playground-entry-pages.spec.ts`（+5 条 `ROUTE_ASSERTIONS`）、`docs/components/flux-renderers-ai/product-spec.md`（§7 refresh 行语义修正）。
- **Phase 1（§7 证据矩阵补齐）**：新 describe「DV computed-style typography + streaming cadence」3 测试——(1) typography（light）：`citation` 关键词（唯一同时覆盖 h2+links+blockquote 的 preset）断言 `h2` 计算 `fontSize`='20px'（1.25rem）、`blockquote` 计算 `backgroundColor` 非 `rgba(0, 0, 0, 0)`、`a` 计算 `color` ≠ 'rgb(0, 0, 238)'；(2) typography（dark）：**双轨注入**（`page.emulateMedia({colorScheme:'dark'})` + `document.documentElement data-mode='dark'`；live 核实 host-attribute 轨为 playground 下 operative 轨——theme-tokens 在 `:root` 定义了 light `--muted`，media 轨的字面 fallback 不触发，符合 D2 双轨设计「主题变量优先」），断言 fenced code（`[data-slot="ai-bubble-pre"]`）计算 `backgroundColor` 非透明且 max(r,g,b) < 100（live 实测暗值 rgb(31,42,61) vs light rgb(240,245,250)）；(3) 节奏：submit → `data-state='completed'` elapsed ≥ 2000ms（default preset ≈38 词 × 200ms ≈ 7.7s；legacy 15ms/词 ≈ 120ms 必 fail 该下界；不断言精确时长）。product-spec §7 refresh 行修正：`assistant 消息数 +1` → `重新流式 + 计数不变 + branchId 更新（truncate-rerun，D4 裁定）`，与 `ai-widgets-button-actions.spec.ts` ④ 断言语义归一（D4 plan Non-Blocking Follow-ups ① 消解）。
- **Phase 1 变异抽查证据（Exit 硬项）**：临时注释 `packages/flux-renderers-ai/src/styles.css` 的 `[data-slot='ai-bubble-markdown'] h2 { font-size: 1.25rem }` → typography 测试转红（Expected '20px' / Received '14px'——Tailwind preflight 下 h2 回退 inherit 基准 14px，非浏览器裸默认，断言仍非恒真）→ 还原后复跑转绿（18.2s pass）；`git diff` 确认 styles.css 零残留。
- **Phase 2（route-inventory 修复）**：`ROUTE_ASSERTIONS` 补 5 条 smoke 断言（执行时 live probe 核实 marker，`_tmp/dv-route-probe.mjs` 已清理）：`gantt-states` → h1 'Gantt States — empty / loading / baselines / regions'、`ai-coverage` → h1 'AI Coverage — states, widgets & edge cases'、`scada-editor-demo` → h1 'scada-editor-demo 编辑器演示页'、`dashboard-demo` → h1 'dashboard-editor 演示页'、`dingtalk-flow-demo` → h1 'Playground'（**live 发现**：独立 demo 页已随 plan 2026-08-07-1053-2 删除，路由 id 无 App.tsx case → 实际渲染 HomePage fallback；该路由条目系 feat-industrial-hmi merge 复活的历史残留，smoke 按 live fallback 行为钉住，条目清理归 route-inventory owner——successor 注记见下）。`playground-entry-pages.spec.ts` 全文件 **77 passed**（inventory 1 + smoke 76，71 既有有效 + 5 本 phase 升级，条数不变）；**route-inventory 红消解**（76 = 76）——自 2026-08-24 D1 记录起登记在册的 pre-existing 红就此移出登记清单。
- **Phase 3（全量验证链）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 tasks（turbo 缓存——本 plan 零 runtime 源码改动）、`pnpm check` exit 0（零新增红）。
- **Phase 3（全量 e2e，roadmap §DV 核心交付）**：`pnpm test:e2e` 全套件（headless Chromium，retries:1）**1310 passed / 14 failed / 43 skipped（22.0m）**。14 条失败按 Failure Paths `e2e-red-unregistered` 协议 **stash 实证归因**（master 干净树 ca1c9c37c 同红 → 登记 baseline red，非本 plan 引入）：① barcode-input 家族 ×5（`component-lab/smoke.spec.ts:18`、`component-lab/c9-host-surfaces.spec.ts:125`、`exploratory/domain-page-zero-error.spec.ts:37`、`exploratory/lab-batch-zero-error.spec.ts:14`）——根因：本地 Chromium `BarcodeDetector` 不支持 `pdf_417` 枚举值 → `createBarcodeDetector`（`barcode-detector-utils.ts:53`）抛 TypeError → console.error 风暴触发 zero-error 门禁（浏览器能力面缺陷，归 scheduling 包 owner）；② `crud-demo.spec.ts` ×6——`getByText('用户管理（CRUD）')` 15s 不可见（页面未渲染预期标题，归 crud-demo owner 深查）；③ `sundial-replica-visual.spec.ts` ×2（:62/:176）——task 详情 dialog 内 `sundial-detail-title-text` 不存在（归 sundial owner）；④ `scada-edge-cases.spec.ts` ×2（:131/:148）——line/polygon hover overlay 时序敏感（全套件负载下红、聚焦复跑 1 flaky 1 过，环境敏感型，归 scada owner）。既有 watch-only 终态项（gantt-perf ×2 / kanban-perf ×1）本次全过未触发；`ai-attachments` 冷启动 flaky 未触发（dev server 全程热）。**ai 家族 19 文件 130 测试**（D4 收口 18 文件 120 + D5 showcase 文件已计 + 本 plan +3），`ai-widgets-demo.spec.ts` 17/17、4 个 ai-widgets 专属 spec 35 测试全绿。
- **Phase 3（产品级快照）**：`_tmp/ai-widgets-product-snapshot.png`（294KB，fullPage 1280×900，citation 富回复 completed 后截取）——捕获命令：`node _tmp/dv-product-snapshot.mjs`（playwright chromium → `#/ai-widgets` → 发送 citation 关键词 → `waitForFunction(data-state==='completed')` → `page.screenshot({path:'_tmp/ai-widgets-product-snapshot.png', fullPage:true})`；脚本用后已删，仅留档 PNG，不入 tracked 目录；roadmap 明示「仅留档对照」，非肉眼判定断言）。
- **执行期观察（successor 注记，均 non-blocking）**：(a) `dingtalk-flow-demo` 路由条目为 merge 复活的历史残留（demo 页已删），smoke 钉住 live fallback，条目清理归 route-inventory owner；(b) `tests/e2e/exploratory/domain-page-interactions.spec.ts` 会在运行时**自写** `docs/analysis/2026-07-27-ma43-.../04-e2e-domain-pages.md`（`writeFileSync`，本次仅表格 reformat 噪音，已 `git checkout` 还原）——测试自突变 docs 属治理缺口，归 e2e 治理 owner；(c) 全量 e2e 会生成未忽略的 `tests/e2e/artifacts/`（sundial snap 输出）——`.gitignore` 候选。
- **§7 14 面证据矩阵闭合（每面 → 证据位置）**：typography light → `ai-widgets-demo.spec.ts` 'citation fixture typography computes styled h2/blockquote/link'；typography dark → 同文件 'dark-mode typography computes a dark fenced-code background'；avatar → 同文件 'assistant bubble avatar renders 32×32 circle with a lucide svg (D3)'；welcome icon → 同文件 'welcome icon renders the Bot lucide preset'；副作用 prompts/refresh/feedback/pill ×4 → `ai-widgets-button-actions.spec.ts` ①④⑤⑥（refresh 行语义已与 ④ 对齐）；fixture ×6 → `ai-widgets-fixture.spec.ts` 6 测试；showcase → `ai-widgets-showcase.spec.ts` (a)–(e)；公式渲染 → `ai-widgets-demo.spec.ts` 'formula keyword renders KaTeX spans (G5)'；代码高亮 → 同文件 'code keyword renders lowlight token spans (G6)'；流式边界 → 单元层 `markdown-buffer.test.ts`（D6 裁定归属，见 plan Deferred But Adjudicated）；节奏 → `ai-widgets-demo.spec.ts` 'streaming cadence'。**14/14 闭合**。
- **闭环**：独立 closure-audit（fresh sub-agent，非执行 session，三件套 fresh-context 输入）verdict approved 后：plan Closure Gates 全勾、`Plan Status → completed`、roadmap DV `planned → done`。剩余队列：DG（`docs/plans/2026-08-25-0215-2-dg-owner-doc-sync-closure.md`，收口：owner-doc 同步 + closure log + mission closeout）。

### 2026-08-25（mission `ai-widgets-product` D4：plan `2026-08-24-2317-1` 真实按钮副作用 + Suggestion Pill Icon 执行——G4+G9 收口）

- **背景**：执行 `docs/plans/2026-08-24-2317-1-d4-real-button-side-effects.md`（4 Phase 全 planned；roadmap §D4 + product-spec §3.3/§7 为契约；mission 预授权包络 = `setSenderDraft` 新 ComponentHandle 方法 + `ai:regenerate` 新 namespace action 两项结构性变更）。收口 G4（demo 全部按钮唯一副作用 showToast）+ G9（suggestion pill emoji 字面）。
- **Phase 1（断言先行 red 锁定）**：unit 六组断言落盘——(a) `action-provider.test.tsx` 增 `AI_NAMESPACE_ACTIONS` 字面 8 项 drift-guard + `ai:regenerate` idle 委托（truncate-rerun：计数不变 + `branch-1` 戳）/`args.branchId` 转发/busy `ok:false`/null-engine 拒绝 ×5；(b) `ai-component-handle.test.ts` 增 `setSenderDraft` ×6（7 方法 drift-guard / 空基直写 / `\n` join 保留键入 / dedupe / replace / 缺 text `ok:false`）；(b2) 新 `adapters/__tests__/ai-sender-draft.test.ts` ×7（D-handle 存储语义：setLocal 写透不通知 / apply 基于当前值 / dedupe / 不同文本仍追加 / replace / unsubscribe）；(c) `ai-feedback.test.tsx` 增 ×4（like 写 `message.metadata.feedback` + `aria-pressed`/`data-active` 双镜像 + un-vote 清除 / dislike 互斥 / sources Popover 列表 + href / 空态提示复用 `flux.ai.citationNoSource`）；(d) `ai-suggestions.test.tsx` 增 ×3（`pencil` → svg / 未知字面回退 / popover overflow 同映射）；(e) 新 `renderers/__tests__/ai-sender-draft-channel.test.tsx` ×3（apply 合并进本地 draft / append 保留键入文本 / submit 后 store 基值同步清空）；e2e 新 `tests/e2e/ai-widgets-button-actions.spec.ts` 7 测试（①prompt 点击写 draft 单值 ②重复点击 dedupe ③suggestion 写空 draft ④refresh 重新流式 + 计数不变（truncate-rerun live 语义）⑤like aria/data 双镜像 ⑥pill svg 非 emoji ⑦sources Popover）。red 证据（记 plan Phase 1 备注）：unit 6 文件 **26 red**（738 既有 PASS）/ e2e **7/7 red**。
- **Phase 2（setSenderDraft 通道 + ai:regenerate）**：`ai-chat-context.tsx` 落 `AiSenderDraftStore`（`{ get, setLocal, apply, subscribe }` 工厂 `createAiSenderDraftStore`——append 空基直写 / 非空 `\n` join / `lastApply {text,result}` 双条件 dedupe / replace 覆盖）+ `AiChatContextValue.senderDraft?` optional 字段；`ai-component-handle.ts` `AI_COMPONENT_METHODS` 7 项 + `senderDraft` 通道输入 + payload 校验（缺 text → `component:setSenderDraft requires { text: string }`，无通道显式拒绝，null-engine 窗口沿既有顶部 guard 统一拒绝）；`ai-action-provider.ts` `AI_NAMESPACE_ACTIONS` 8 项 + `regenerate` case（busy-guard 与 send/clear 同口径 + `args.branchId` + null-engine `needsEngine` 扩项）；`ai-chat.tsx` per-chat 存储 `useMemo` 创建（引用稳定，context value 不因 draft 写入重建——AI-31 纪律；bug 166 messages 失效机制零改动，仅新增字段）+ handle 闭包接线；`ai-sender.tsx` 订阅 apply 通知合并进本地 draft + Textarea/extension 两路键入 `setLocal` 写透（不通知防回环）+ commit 清空时同步 `setLocal('')`。
- **Phase 3（feedback/suggestions 渲染器 + demo 重写）**：`ai-feedback.tsx`——like/dislike 写 `message.metadata.feedback`（un-vote delete）+ `aria-pressed` 镜像（toggle 按钮双态）+ refresh 走 D-refresh 渲染器内默认（ctx 存在 → busy-guard + `ctx.engine.regenerate()`，无 ctx 不变；demo onAction 仅保留 toast 通知，避免单一 ActionSchema 双触发）+ sources 动作 `Popover`（`normalizeFeedbackSources` 容错 string/{label|title,url}，`data-slot="ai-feedback-sources-list"/"-item"/"-empty"`，空态 i18n）；`ai-suggestions.tsx`——`SUGGESTION_ICON_PRESETS` 5 项 lucide 分发（pencil/languages/lightbulb/sparkles/plus，D3 welcome 同模式）+ `SuggestionIcon` 组件统一主路径与 popover overflow 路径 + 未命中字面回退（`ai-p4-widgets-demo` emoji 页向后兼容）；`ai-widgets-demo.tsx`——ai-chat `componentId: 'ai-widgets-chat'`（D-id：避开与另一 demo 同名）、prompts/suggestions onSelect 与 voice onResult → `component:setSenderDraft`（args `${item.label}`/`${item.text}`/`${transcript}`+`mode:'append'`）、`SUGGESTION_ITEMS` 换 lucide 字符串键、`feedbackMsg` 增 `metadata.sources` 两条（design.md/product-spec.md 链接）；showToast 仅剩 feedback onAction + voice onError 两处通知用途，`component:setSenderDraft` ×3（grep 记 plan）。dev 实跑抽查按 AGENTS.md 程序化纪律由 e2e ①-⑦ 覆盖（voice 转写追加由 unit (b2)/(e) append-on-typed-text 覆盖——Web Speech API 无法 headless 触发）。
- **Phase 4（e2e 转绿 + 回归）**：新 spec **7/7 首跑全过**（无 flaky；`openWidgetsPage` 首屏预算 30s——冷 dev server 编译 demo 模块图超 15s 的环境因素，非断言变化）；ai e2e 全家族 **18 文件 120 测试全绿**（118 首跑 + `ai-attachments` 2 条 cold-server 首屏 flaky：retry 过 + 单文件复跑 2/2 首跑绿，与 D5 执行同款既有环境模式、attachments 面与 D4 改动无涉）；重点保护面 `ai-widgets-demo.spec.ts` 12/12（事件重写零结构破坏）、`ai-coverage-widgets.spec.ts` like/dislike 面、`ai-component-handle.spec.ts`（新增方法零破坏）、`ai-widgets-fixture.spec.ts` 6/6；零既有断言修改。
- **全量验证（Closure Gates）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks（ai 包 **84 files / 764 tests** 全绿 = 738 既有 + 26 新增）、`pnpm check` exit 0（零新增红；oversized 2 exempt locale 为登记在册项）。全量 e2e（ai 家族之外）未跑（沿用 D3/D5/D6 先例：范围外存在登记的 pre-existing route-inventory 红），不主张仓库级 full-green。
- **闭环**：独立 closure-audit（fresh sub-agent `ses_fcb0a4058ffe8OrqqjDZ8iJ9A6`，非执行 session，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major / 1 Minor 记录性观察——setSenderDraft 空 text 拒绝为与 sendMessage 一致的刻意校验；审计独立复跑 71 聚焦 + 764 包级 + e2e 7/7 + 双包 typecheck）；plan Closure Gates 全勾、`Plan Status → completed`；roadmap D4 `planned → done`。**DG 注记（Non-Blocking Follow-ups 对齐）**：① roadmap §D4 / product-spec §7「refresh → assistant 消息数 +1」措辞与 live 引擎 truncate-rerun 语义不符（live = 计数不变 + branchId 更新 + 重新流式），本 plan 按可观测重新生成落地；② roadmap §D4「失焦/显式触发时执行」draft-race 措辞被 D-race 裁定舍弃（append-保留 + dedupe 交付其意图）；③ refresh 的 schema 显式覆盖（onRefresh 类专用事件）裁定 out-of-scope improvement（D-refresh 渲染器默认已交付语义，DG 后有真实需求再评估）。
- **剩余队列**：D0–D6 全 done → DV（全量验证与 e2e 增量化）→ DG（收口）。

### 2026-08-25（mission `ai-widgets-product` D6：plan `2026-08-24-2237-3` LaTeX 公式渲染 + 代码高亮 + 流式边界扩展执行——G5+G6 收口）

- **背景**：执行 `docs/plans/2026-08-24-2237-3-d6-latex-code-highlight.md`（4 Phase 全 planned；`design.md:378` supersession 裁定内置）。收口 G5（无 LaTeX 渲染）+ G6（fenced code 无高亮）+ 流式安全缓冲单美元 / `\[` 边界扩展。
- **Phase 1（依赖 + red 锁定）**：ai 包 peer + devDeps 增 `remark-math@^6`/`rehype-katex@^7`/`katex@^0.16`/`lowlight@^3`（实测 remark-math@6.0.0 / rehype-katex@7.0.1 / katex@0.16.47 / lowlight@3.3.0）+ playground dependencies 增 `katex`；**Decision D-b**：静态 import（硬 peer 语义，无 try/catch dynamic require 降级路径）。unit 断言先行：新 `markdown-d6-math-code.test.tsx`（`.katex`/`.katex-display`/`.tok-*`/未知语言回退/math-parse-error 容错）+ `markdown-buffer.test.ts` D6 两 describe（单美元 + `\[` 边界、货币防误切）；e2e 新 2 测试（formula → `span.katex` ≥2 + `.katex-display`；code → `.tok-*` ≥1）。red 证据（记 plan）：unit 7 failed（729 pass）/ e2e 2 failed；guard 型断言（货币不截断 / fence 回归）green-by-construction。
- **Phase 2（接线 + 缓冲扩展）**：`markdown.tsx` 挂 `remarkPlugins=[remarkGfm, remarkMath]` + `rehypePlugins=[rehypeRaw, rehypeKatex]`；`code` override 接 lowlight——**Decision D-c**：`createLowlight(common)`（~37 语言；live 核实 highlight.js common 为 `typescript` 注册 `ts`/`tsx` 别名，fixture `tsx` fence 免 `register()`）+ hast → 受控 React 元素映射 `.tok-*` 4 语义类调色板（`HLJS_SCOPE_TO_TOK`，表外 scope 无 class；无 `dangerouslySetInnerHTML`）；未知语言/异常回退纯文本。`markdown-buffer.ts`：`findUnclosedSingleDollarCutoff`（**Decision D-a**：open 守卫 = `$` 后随非空白非数字；micromark-extension-math@3.1 实测校准 close 规约为「任意后续单 `$` 闭合（空格 padding 合法）」；免疫面 = `$$` run / 行内 code span / fenced code 区域 / `\$` 转义）+ `\[`/`\]` 块级边界（与 `\(` 对齐）。包内 82 files **736 tests 全绿**（Phase 1 时 729+7 red）；全仓 typecheck 37/37。
- **Phase 3（样式 + host CSS）**：`styles.css` 新 D6 段 ~40 行（`.katex` line-height 1.2 防 markdown 1.7 行距撑高公式、`.katex-display` 居中 + overflow-x auto、markdown scope `.tok-key/.tok-str/.tok-num/.tok-bool`——key/num 复用 `--ai-md-primary` 自动随 D2 双轨暗色，str/bool `hsl(var(--success/destructive, 字面回退))`）；playground `styles.css` 增 `@import 'katex/dist/katex.min.css'`（紧随 ai 包 styles）。**fixture `\[` Decision：不采纳**（remark-math@6 只解析 `$`/`$$`，`\[...］` 无 math 节点，写入 fixture 会字面渲染方括号——`\[` 与 `\(` 同为 buffer-only 边界，unit 覆盖）。dev 实跑抽查（programmatic probe `_tmp/d6-dev-spot-check.mjs` 对 live dev server，遵守 AGENTS.md 禁截图判读，已清理）：formula katex=4 span + display 居中/overflow auto/line-height 20.3px、code tok 18 个（light rgb(28,110,242)）、暗态抽查（`data-mode='dark'`）tok 色走 theme-tokens 暗轨 rgb(77,141,245)、公式暗态渲染/布局不破——4/4 PASS。
- **Phase 4（e2e 转绿 + 断言迁移 + 回归）**：D6 2 测试转绿；`ai-widgets-fixture.spec.ts` formula 断言迁移（`toContainText('$$')` → `.katex` 存在，D1 预埋的计划内迁移；blockquote 保持；文件头注释同步）；`design.md:378` 回填本 plan 链接。ai e2e 全家族 **18 文件 120 passed / 0 failed**（plan 起草计数 17，live 18——在册文件全量跑过）；lint 修复 2 处（`react/no-array-index-key` → 内容派生 key；unused `rest`）。修复后 demo+fixture spec 复跑 20/20。
- **全量验证（Closure Gates）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks（ai 包 736/736）、`pnpm check` exit 0（零新增红，oversized 清单为登记在册项且 markdown.tsx/markdown-buffer.ts 远低于 500 行阈值）。全量 e2e（ai 家族之外）未跑（沿用 D3/D5 先例：范围外存在登记的 pre-existing route-inventory 红），不主张 full-green。
- **闭环**：独立 closure-audit（fresh sub-agent `ses_fcb30af23ffeyZjbRptL7F0foE`，非执行 session，三件套 fresh-context 输入）verdict **approved**（0 Blocker / 0 Major / 3 Minor：`_tmp` probe 清理漂移→已删、收口动作→本条执行、ai-welcome/ai-prompts 冷载 flake→watch-only 与 D6 无关）；审计独立复跑 736 unit + 20/20 e2e + typecheck/lint 全绿、断言语义与 deferred 诚实性逐项核对。plan Closure Gates 全勾、`Plan Status → completed`；roadmap D6 `planned → done`。至此 D0–D3/D5/D6 done，剩 D4（`docs/plans/2026-08-24-2317-1-d4-real-button-side-effects.md`，依赖已满足）→ DV → DG。

### 2026-08-25（mission `ai-widgets-product` D5：plan `2026-08-24-2237-2` Showcase 完整性执行——G7 收口 + 缩略卡 + 结构化 fixture 触发 + 独立 closure-audit）

- **背景**：执行 `docs/plans/2026-08-24-2237-2-d5-showcase-completeness.md`（4 Phase 全 planned；product-spec §6 为 showcase 口径契约）。收口 G7：`# /ai-widgets` 从 6 个 live widget 扩到首屏 ≥8（6 live + 2 缩略卡 + 1 live citations 实例 = 9），fixture 触发后对话区展示 tool-call 卡 / citations marker / reasoning 折叠三类缺席能力。全部改动落在 playground host 层，`packages/flux-renderers-ai` 零改动。
- **Phase 1（e2e red 锁定）**：新 `tests/e2e/ai-widgets-showcase.spec.ts` 5 测试：(a) 白名单首屏计数 ≥8（review 修正口径——`[data-slot^="ai-"]` 裸匹配会计入嵌套 slot 使基线虚高，只计 7 个 showcase 根 slot + 2 卡 href）；(b) 两卡链接 + §6.2 摆放（welcome 与 prompts 之间，DOM order）；(c) reasoning 折叠面板；(d) tool 卡 + 后续表格；(e) citations `sup[data-citation-index]` marker。red 证据（记 plan）：5/5 red，(a) 计数证据 = 6 < 8，三类元素 not found。**Decision D-b**：缩略卡裁 content 包 `link` renderer + flex 组合（真 `<a href>` + `isSafeNavigationUrl` 协议门，`link.tsx:29-34`；`#/ai-*` 相对 URL 通过）——弃 html 锚点卡（等价 `<a>` 语义但避免 schema 内维护原始 HTML 串），锚点选择器不变。
- **Phase 2（fixture 结构化扩展）**：`AiWidgetsFixture` 增 `reasoning?: string` / `toolRound?: boolean`；**Decision D-c** 落地三类专属触发——weather→`toolRound`（get_weather 参数与 `tool-mock.ts` executor 匹配）、reasoning→前导 `reasoning_content`、citation→content 内 `[1]`/`[2]`/`[1,2]` marker；`default`/`code`/`formula` 保持纯 markdown（chunk 序列与 D1 基线逐 chunk 等价，保护 default 单 bubble + `Hello` 10s 预算与 D6 断言面）。`mock-ai-env.ts` fixture 模式：tool 轮判定复用 `createMockToolStream` 的 trailing `role:'tool'` 轮次模式；6 关键词分发零改动（grep 核对记 plan）。
- **Phase 3（demo 接线）**：`ai-widgets-demo.tsx` ai-chat 增 `tools`/`toolExecutor`（`${$ai.tools}` 表达式，`createAiImportLoader` extra 注入）+ **`maxToolRounds: 2`**（fixture tool 轮恰需 2 轮，比默认 8 紧的确定性上界）；beforeMessages 两卡 + afterMessages `ai-citations` inline 实例。**Decision D-a（首选落地）**：静态 `pageData` message + `sources` explicit prop（`ai-citations-demo.tsx:29-51` canonical 用法）——组合模型合规（`ai-citations.tsx:37-43` Decision-C 禁 widget 与 ai-bubble 双渲染同一 slice）；`${messages}` live 绑定弃用（inline 模式全文重渲染 message.content 为 plain text `ai-citations.tsx:92-123`，会在消息列表与输入框间产生每条回复纯文本副本，违背产品化目标——如未来需 live 性须先解双渲染，successor 候选记 plan Non-Blocking Follow-ups）。dev 实跑抽查（programmatic probe `_tmp/d5-showcase-inspect.mjs` 对 live dev server，遵守 AGENTS.md 禁截图判读，已清理）：`get_weather` 卡 + 表格、折叠面板默认折叠/点击展开、2 个 `[N]` marker 点击打开 source 卡（portal）、pageErrors=[]。
- **Phase 4（转绿 + 回归）**：新 spec 5/5 绿（(a) 计数 9）；ai e2e 全家族 18 文件 **116 passed + 2 flaky**（`ai-attachments.spec.ts` 上传用例，并行 worker 环境flake，单文件复跑 2/2 稳定，attachments demo 走 legacy `createMockAiEnv()` 路径与本改动无涉）；重点保护面 `ai-widgets-demo.spec.ts` 12/12（plan 文本"D1 期 10 测试"+D3 增 2 = live 12）与 `ai-widgets-fixture.spec.ts` 6/6（weather 多 bubble 链式 locator 仍单命中）全绿；**零既有断言修改**（strict mode 修正预案未触发）。
- **全量验证（Closure Gates）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks（playground 26 files/175 tests）、`pnpm check` exit 0（零新增红）。全量 e2e（ai 家族之外）未跑（存在登记的 pre-existing route-inventory 红），不主张 full-green、未触发 full-green 提交条件。
- **闭环**：独立 closure-audit（fresh session `ses_fcb529d07ffeMGH4kDjbl0IwXQ`，非执行 session）verdict **issues → approved**——唯一 Blocker 为本 daily log 缺失（docs-only，审计明示补写后 flip approved；代码与测试面全验证通过：4 Phase checklist 全勾、fixture/mock/demo 逐行核对、spec 断言与真实 renderer data-slot 面核对、`git status` 零 `packages/flux-renderers-ai` diff、独立复跑 23/23 绿）；本条目即为该 Blocker 的修复。plan Closure Gates 全勾、`Plan Status → completed`；roadmap D5 `planned → done`（roadmap 状态流转约定）。下一项：D4（`docs/plans/2026-08-24-2317-1-d4-real-button-side-effects.md`，依赖 D1+D2+D3 已满足）或 D6。

### 2026-08-25（mission `ai-widgets-product` D3：plan `2026-08-24-2237-1` 气泡 Avatar + Welcome Icon 执行——G3+G8 收口 + showAvatar 接线 + 独立 closure-audit + roadmap D3 → done）

- **背景**：执行 `docs/plans/2026-08-24-2237-1-d3-avatar-welcome-icon.md`（3 Phase 全 planned；product-spec §3.1–§3.2 为契约）。收口 G3（`ai-bubble/index.tsx` avatar 空 div）+ G8（`ai-welcome.tsx` icon 字面字符串渲染）+ live 发现的 showAvatar 链路缺口（chat/message-list schema 无 `showAvatar` → ai-chat 子树内 bubble 永不显示 avatar）。
- **Phase 1（断言先行 red 锁定）**：新 `packages/flux-renderers-ai/src/renderers/__tests__/ai-bubble-avatar.test.tsx` 5 case（assistant/user svg+data-role、自定义 avatar ReactNode 覆盖、showAvatar 缺省无节点、`AiBubbleRenderer` 转发 `resolved.avatar`）+ `ai-welcome.test.tsx` 新分发 describe 3 case（`bot` 预设 svg / `✨` 字面回退 / `iconLucide` 优先）+ e2e `ai-widgets-demo.spec.ts` 新 2 测试（avatar `getBoundingClientRect()` 32×32 + `borderRadius` 9999px + svg；welcome icon svg 且非字面 `bot`）。red 证据（记 plan Phase 1 备注）：unit **6 red**（其余 712 全绿）+ e2e **2 red**。
- **Phase 2（实现 + 接线）**：avatar 节点 `props.avatar ?? (role==='user' ? <User/> : <Bot/>)` + `data-role` + `aria-hidden`；`AiBubbleViewProps.avatar?: ReactNode`；`AiBubbleRenderer` 转发 `resolved.avatar`（`isValidElement` 守卫，senderExtensions 消费先例——非 declaration-only）；`styles.css` 新增 `[data-slot='ai-bubble-avatar']` 32×32/9999px 圆/token bg+border+色（D2 双轨 dark fallback：media query + `[data-mode='dark']`）+ `:has()` 行布局（avatar-less bubble 布局零影响）+ welcome icon muted-foreground 色 + 24px svg；`ai-welcome.tsx` 6 项预设映射（bot/user/sparkles/chat/lightbulb/search）+ 字面回退 + `iconLucide` 优先；`schemas.ts` 增 `AiChatSchema.showAvatar` / `AiMessageListSchema.showAvatar` / `AiBubbleSchema.avatar` / `AiWelcomeSchema.iconLucide`；definitions 补 `showAvatar` booleanContract×2 + `avatar`/`iconLucide` `fields[]` 纯条目（shape 校验 accepted keys 就绪）；`ai-chat.tsx`/`ai-message-list.tsx`（virtual + 非 virtual 两调用点 + 注册 renderer）转发链；`ai-widgets-demo.tsx` ai-chat 设 `showAvatar: true`。计划内断言更新：`ai-welcome.test.tsx:40` `icon:'bot'` 字面 → svg 结构断言（G8 既定行为变更）。
- **playground dev 实跑抽查（Phase 2 Exit 承诺项，programmatic 探针 `_tmp/d3-avatar-inspect.mjs` 实跑后已清理）**：`#/ai-widgets` 发消息后——**user 与 assistant 双方 avatar 均 32×32 / borderRadius 9999px / svg count 1 / data-role 正确**；welcome icon **svg count 1、textContent 空（非字面 `bot`）**。
- **Phase 3（转绿 + 回归）**：`flux-renderers-ai` 包 **81 files / 718 tests 全绿**；ai e2e 全家族 **17 个 `ai-*.spec.ts` 113 passed**（含 `ai-coverage-widgets.spec.ts:187` `🤖` 字面回退证明 + `ai-widgets-fixture.spec.ts` 6 测试 + widgets demo 12 测试）；零非计划断言修改。
- **全量验证（Closure Gates）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks、`pnpm check` exit 0（零新增红）。全量 e2e 套件未跑（本 plan 验证面为 ai spec 家族；范围外存在登记的 pre-existing route-inventory 红），故不主张 full-green、未触发 full-green 提交条件。
- **闭环**：独立 closure-audit（fresh session `ses_fcb6fcb34ffeRjMSZYm7xAB9It`，非执行 session）verdict **issues → approved**——唯一 Blocker 为本 daily log 缺失（docs-only，审计明示补写后 flip approved，代码零问题：9 项 checklist 全过 + 独立复跑 15/15 聚焦 + 718 包级 + e2e 43+6 复现 + 无构建产物泄漏）；本条目即为该 Blocker 的修复。plan Closure Gates 全勾、`Plan Status → completed`；roadmap D3 `planned → done`。下一项为 D4（`docs/plans/2026-08-24-2317-1-d4-real-button-side-effects.md`，依赖 D1+D2+D3 已满足）。
