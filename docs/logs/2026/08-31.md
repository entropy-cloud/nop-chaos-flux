# Development Log - 2026-08-31

### 2026-08-31

- **D1 G-B2 键盘导航框架产品化执行**（plan `docs/plans/2026-08-30-2312-1-d1-gb2-keyboard-navigation-framework.md` Phase 2–4 全部完成；Phase 1 契约裁定为前序 session 已完成。执行顺序约束满足——C2 `done` + P4b `done` + P6b `done`，本 plan（N=1）先于同批 G-B3 plan（N=2）执行）。
- **Phase 2（共享 helper + 绑定通道）**：`@nop-chaos/flux-react` 新增 `src/keyboard.ts`（`parseKeyCombo`/`parseModifierHotkey`/`parseKeySequence`/`comboMatchesKey`/`isEditableKeyboardTarget`/`createChordMatcher`）+ `src/use-keyboard-bindings.ts`（window 监听 + chord 缓冲/超时/最长匹配兜底 + 输入门控 + defaultPrevented 内建优先 + surface 栈路由），经 `index.tsx` 导出；`command-palette.tsx` 删除私有 `parseHotkey`/`matchesHotkey` 改消费共享 helper（行为零变化，既有 31 条单测零回归）。新 invisible logic renderer `keyboard` 落 `flux-renderers-basic`（`keyboard.tsx` 恒渲染 null + `KeyboardSchema`/`KeyboardBindingConfig` + 定义登记 + playground route-matrix 121→122 + keyboard lab page 三场景）。先红后绿：`keyboard.test.ts` 20 条 + `keyboard-bindings.test.tsx` 20 条（红态实测 16 failed）。
- **Phase 3（table 选区修饰键 + J/K 组合 + 裁定执行）**：`rowSelection.modifierSelect` 落地（`use-table-selection.ts` 锚点 ref + shift 加法并集范围/不可选跳过/上限截断/永不取消；`table-row-leading-cells.tsx` 新件承接 mousedown 修饰键捕获；`table-renderer.tsx` 容器级 ⌘/ctrl+A）；schema/validation/定义描述同步。先红后绿：hook 级 11 条 + UI 手势 7 条（红态实测 14 failed）。**执行期发现并修复：`normalizedRows` 直接进 `handleSelectRow` deps 使回调身份随数据翻转、击穿行 memo 局部性（perf locality 诊断 `expected 2 to be 0`）——改 ref 镜像 rows/anchor 保持回调恒定身份后回归全绿**。J/K 指针组合测试锁定（`keyboard-pointer-option-row.test.tsx`，三元投影足用，零公式函数候选）。四维度裁定执行落字 plan Deferred 节（roving 不抽取 / kanban 手势解耦 deferred / hover-peek deferred / fill-handle 编辑器选区模型 deferred；**kanban 零改动确认**）。
- **Phase 4（文档对齐）**：`docs/references/renderer-interfaces.md` §Keyboard Binding Contract + §Table Modifier Selection Contract 终稿（draft → implemented and verified，CX-10 evaluation bindings 精确化）；flux-guide `07-structural-nodes.md` §Keyboard Bindings（绑定字段表 + 匹配语义 + J/K 指针组合样例）+ `design-patterns/table.md` §4 `modifierSelect` 条目；C2 回写 ⑫ 追加式落字（G-B2 已产品化终态 + 回写 ⑤「无 schema 事件面」表述修正 + 四维度 successor 登记，初版裁决表零改动）；styling-system 核查：`keyboard` 恒 null + 修饰键复用既有 marker，零改动（无凑条目）。
- **验证状态（full-green verification，本日 G-B2 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37（执行期修复三处 React Compiler/a11y lint 新红：`use-keyboard-bindings.ts` 渲染期 ref 写改 useMemo 派生、`keyboard.tsx` dev warn 移入 effect、table 容器 keydown 改 props spread）、`pnpm test` 68/68 任务全绿（新增 59 条单测：flux-react 20 + basic 20 + data 19；playground route-matrix 122 全绿；flux-renderers-data 950 含既有 2 条 `onSelectRow` 调用签名断言随契约扩参同步）；`check:renderer-definition-fields-only` 零红；`check:audit-event-dispatch-ctx` 零红；`check:schema-prop-coverage` 全覆盖；`pnpm check` 零新增红（oversized 194W/2E/2 exempt 与 08-30 注册基线一致——执行期 `table-body-row-rendering.tsx` 一度 704 行触发第 3 条 ERROR，经抽取 `table-row-leading-cells.tsx` 收敛消除，同 08-30 option-row 先例）。测试档位：必须自动化（schema 公共契约面 + Protected Areas owner-doc 对齐）。
- **Closure audit**：fresh session 独立子 agent `ses_fab859cbcffeayVGaaF6ZRqz8n` 1 轮 **APPROVED** 零 Blocker/零 Major/零 Minor（3 Informational——Closure 段占位符系审计时点正确协议状态/build-lint-fulltest 以 typecheck+check+分阶段记录佐证/红态计量历史性说明）；审计独立重跑全部目标单测与门禁（keyboard 20 + data 19 + helper 20 全绿、双门禁 exit 0、`pnpm check` exit 0 基线一致、typecheck 37/37、command-palette 回归 31/31）+ git status 确认 scheduling/flux-core/ui 零改动 + C2 回写 ⑫ append-only 0 删行。plan Status → `completed`。
- Next step: G-B3 批量操作栏语义件 plan（`2026-08-30-2312-2`）执行；跨页 palette 单例/app 级命令注册中心（G-B1 successor）维持候选池登记。
- **D1 G-B3 批量操作栏语义件产品化执行**（plan `docs/plans/2026-08-30-2312-2-d1-gb3-batch-bar-semantic-component.md` Phase 1–4 全部完成；执行顺序约束履行——G-B2 plan（N=1）本日收口后本 plan（N=2）执行，共写面冲突消除）。
- **Phase 1（契约设计与采纳面裁定）**：inventory live 复核（`$crud.*` 投影/句柄、table selection hooks、`bulkActions` 死状态、ui Alert 底座、AntD Pro/Linear 手工拼装对照）落字 plan Decision Record；五项 Decision——①形态 = 新 renderer type `batch-bar` 落 flux-renderers-data（否决双字段族分轨与 `toolbarLayout` block 复活：dead 命名零复活）；②字段族 = `selectionPath` 直接 path 反应式绑定（否决 `target` componentId 反应式读：句柄仅命令式通道）+ `countTemplate` + `actions` region + `clearTarget` 统一句柄解析 facade（crud `clearSelection` 优先 → table `setSelection` 空集兜底；两套选择集 API 本体不合并不平移）+ 可见性 AND 叠加语义 + Failure Paths 终态化；③`selectAllMode: 'all'|'page'` 缺省 'all' 零回归、crud/table 双侧采纳；④marker 输出（root `nop-batch-bar` + `data-slot` + `data-count`）；⑤ui 包零改动确认（ask-first 门禁未触发）。`renderer-interfaces.md` 契约草案条目成形（Protected Areas owner evidence 就位）。
- **Phase 2（批量栏语义件实现）**：`batch-bar.tsx` + `batch-bar-definition.ts`（`validateBatchBarSchema`）+ `BatchBarSchema` 落 `schemas.ts` + 定义登记/导出 + i18n `flux.batchBar.*` 双语言。**执行期契约级发现：`countTemplate` 普通 prop 声明会被 props 解析管线提前求值（节点 scope 无 `count` → 抛错/静默降级静态串）——改 `lazyEval: true`（loop `itemData` 先例）进 `structuralFields`，渲染期 `{count, selectedRowKeys}` 子作用域求值**。连带面：playground route-matrix 门禁要求 route + lab page——`data-route-entries.ts` + `batch-bar-lab-page.tsx`（三场景）+ lab registry。先红后绿 15 条（`batch-bar.test.tsx` 13 + `batch-bar-hosts.test.tsx` 2 双宿主对接）。
- **Phase 3（selectAllMode + 共存回归）**：`rowSelection.selectAllMode` + `CrudSelectionConfig.selectAllMode`（`buildCrudTableSchema` 透传双侧采纳）；`useTableSelection` 新增 `options.selectAllRows`（页切片作用域）+ `selectAllScopeSelectedCount`；table-renderer 页计算块上移 + 表头 `selectAllChecked`/`selectAllIndeterminate` 页感知（'all' 模式 legacy 公式零变化）。'page' 语义终态 = check/uncheck-all-visible（与手动逐行勾选同构，不发明第三种累积通道）。先红后绿 11 条（缺省零回归 2 + page 3 + 服务端 1 + 共存 2 + crud 透传 1 + 校验 2）；连带登记 `crud-selection-drift-deletion.test.ts` 字段清单 6→7（契约有意增长）。
- **Phase 4（文档对齐）**：`renderer-interfaces.md` §Batch Bar Semantic Component 终稿（landed + live-verified）+ §Table Select-All Mode Contract 新节；flux-guide `crud.md` §4a batch-bar 双宿主样例 + `table.md` §4 selectAllMode 条目；C2 回写 ⑬ 追加式落字（G-B3 已产品化终态 + 观察面余量 + G-C/G-D 拆分登记，初版裁决表零改动）；styling-system 核查零改动（无凑条目）。
- **验证状态（full-green verification，本日 G-B3 后）**：`pnpm test` 68/68 任务全绿（新增 26 条单测落 flux-renderers-data 976 条总量；playground route-matrix 123 全绿）；`check:renderer-definition-fields-only` 零红；`check:audit-event-dispatch-ctx` 零红；`pnpm typecheck`/`pnpm build`/`pnpm lint` 全量通过（见 Closure Gates）；`pnpm check` 零新增红。测试档位：必须自动化（schema 公共契约 + 批量操作核心回归路径）。
- Next step: closure audit（fresh session 独立子 agent）后 plan 收口；G-C/G-D 语义件族独立 plan 起草（G-D 双前置 G-B2+G-B3 均已就位）。
- **D1 G-C 多视图数据库语义件产品化执行**（plan `docs/plans/2026-08-31-0721-1-d1-gc-multiview-database-semantic-components.md` Phase 1–4 全部完成；执行顺序约束履行——本 plan（N=1）先于 G-D plan（N=2 `2026-08-31-0721-2`）执行，共写面 `renderer-interfaces.md`/flux-guide/C2 回写冲突消除）。
- **Phase 1（契约设计与维度终态裁定）**：inventory live 复核落字 plan Decision Record——三姊妹死声明族确认（`TabsSchema.closable/draggable/addable`（schemas.ts:169-174）+ `TabsItemSchema.closable`（:137-138）声明 + 定义登记（definitions:639-641）vs `tabs.tsx` 实现零命中 grep EXIT 1；playground 零存量消费，`m5-showcase-app.json:69` 的 `closable` 属 notice-bar）+ items 渲染链/句柄现状（仅 setValue/getValue）/valueStatePath 写入链/溢出现状/回写 ⑥ 五条不可达面维持。六项 Decision：①载体 = 候选① 组件句柄族 + 受管集合（四句柄 `addTab/removeTab/renameTab/moveTab` + 四事件 `onTabAdd/onTabClose/onTabRename/onTabMove` + `itemsOwnership/itemsStatePath` 集合所有权轴；否决纯 scope 驱动读侧与 runtime 字段族）；②三姊妹全部接线（G-A 分轨「接线」轨；无内建删除确认；item 级 closable 逐 tab 覆盖；末位禁删守卫）；③溢出收纳显式 deferred；④kanban 聚合 = 候选① `columnAggregate` 板级声明 × 列级求值（client 列内过滤可见卡片集口径）；⑤peek 联动 deferred（作者侧创作约定非 runtime 机制缺口）；⑥偏好存储层维持 optimization candidate（INV-1 host 注入 adapter 先例）。
- **Phase 2（tabs 视图集合管理动态化）**：`TabsSchema` 新增 `itemsOwnership`/`itemsStatePath`/`onTabAdd`/`onTabClose`/`onTabRename`/`onTabMove` + `TabsItemSchema.closable` 注释契约化；`tabs.tsx` 接线（受管集合 local/scope 写回/controlled 拒绝；active 迁移复用 `resolveCandidateValue` 走 valueStatePath 写链；UI affordance ✕/＋/拖拽 + `data-slot` 标记；`removeTab` 末位守卫 + 一次性 dev warn `tabs-remove-last`）；`basic-renderer-definitions.ts` fields/句柄契约/propContracts 登记（`itemsOwnership` union + `itemsStatePath` string）。先红后绿 25 条（`tabs-view-management.test.tsx`：句柄矩阵 14 + active 迁移 3 + 死声明接线/兼容 6 + 所有权轴 2；落地前 red 实测 24 failed）。
- **Phase 3（kanban per-column 列头聚合 + 两维度裁定执行）**：`KanbanSchema.columnAggregate: { fn: sum|avg|min|max|count, field?, label? }` + 纯函数 `kanban-aggregate.ts`（count 忽略 field；sum/avg/min/max 跳过缺失/非数值、全无效显 `-`；avg 两位小数）+ `use-kanban-column-aggregate.ts` hook（无效 fn 弃用 + `kanban-aggregate-invalid` warn；`kanban-aggregate-missing-field` 板级一次性 warn）+ board→column→header 传递（默认列头内嵌 `data-slot="kanban-column-aggregate"`，与计数徽章同源过滤联动；`columnHeader` region 覆盖抑制）+ 定义字段/propContract（object shape fn union）登记。先红后绿 14 条（`kanban-aggregate.test.ts` 9 + `kanban-column-aggregate.test.tsx` 5）。两维度裁定执行落字 plan Deferred 区（溢出收纳/peek 联动/偏好存储层，无静默跳过）。
- **Phase 4（文档对齐）**：`renderer-interfaces.md` §Tabs View Collection Management Contract + §Kanban Column Aggregate Contract 双节 landed 终稿；flux-guide `08-tabs-state.md` items 轴节 + `design-patterns/tabs.md` §视图集合管理 + `design-patterns/kanban.md` §列头聚合与字段表条目；C2 回写 ⑭ 追加式落字（G-C 已产品化终态证据 + 两维度裁定 + G-D 后继 ⑮ 占位登记，初版裁决表零改动）；styling-system 核查零改动（tabs affordance 为 widget 式内建交互件，非容器类 layout renderer 契约约束面；零 BEM 零根 marker 变化）。
- **验证状态（full-green verification，本日 G-C 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37（执行期修复四处 React Compiler lint 新红：`cloneCollection`/`resolveItemIndex` 未用删除、runX 函数声明序、`use-kanban-column-aggregate` 渲染期 ref 访问改 effect、tabs 句柄 useMemo 改 effect 内构建 + 模块级 Set dev-warn 闩）、`pnpm test` 68/68 任务全绿（新增 39 条单测：basic 591 总量 +25、scheduling 943 总量 +14）；`pnpm check` exit 0 零新增红——**oversized 门禁初触双 ERROR（tabs.tsx 784 / definitions 728）后责任拆分收敛：`tabs.tsx` 462 行（退出 WARN 清单）+ 新 `tabs-utils.tsx`/`tabs-view-management.ts`/`tabs-renderer-definition.ts`/`use-kanban-column-aggregate.ts` 四模块，WARN 列表与 HEAD diff 零新增条目**；`check:finite-prop-contracts` 门禁 `tabs.variant` contractFile 因定义文件拆分同步登记至 `tabs-renderer-definition.ts`（form/crud per-renderer definition 先例口径）；`check:renderer-definition-fields-only`/`check:audit-event-dispatch-ctx`/`check:i18n-keys`（`flux.tabs.newTab` 双语言）/`check:schema-prop-coverage` 零红。测试档位：必须自动化（renderer 定义字段保护区 plan-first 契约变更）。
- Next step: closure audit（fresh session 独立子 agent）后 plan 收口；G-D 网格编辑语义件族 plan（N=2）执行。
- **Closure audit（G-C plan）**：fresh session 独立子 agent `ses_faa3ff7d5ffeC7F8BNMM9sdLtI` R1 `ISSUES`（1 Blocker：plan 内 Closure Gates 段重复——模板位未勾选副本与操作位已勾选副本并存，随共识修复合并为单段全勾选；1 Minor：Closure `Follow-up:` 占位符残留——随共识修复落实内容；实体面全部验证通过——39 条单测独立重跑全绿/basic 591 + scheduling 943、四门禁 exit 0、oversized 195W/2E-exempt 与登记基线一致零新增、C2 回写 ⑭ append-only +14/−0、Protected Areas 零命中）→ 同 auditor `ses_faa37a7e4ffe55OSKfkl0CBkJM` R2 scoped re-audit **APPROVED** 零 Blocker/零 Major/零 finding。plan Status → `completed`。
