# Development Log - 2026-08-06

### 2026-08-06

- **Plan 453 DingFlow 单一 Tree 布局统一执行完毕**（`docs/plans/453-dingflow-single-tree-layout-unification-plan.md`，5 Phase 全 completed，55 执行项全勾选）：
  - **Phase 1 core tree session/contract migration**：`createTreeDesignerCore(initialTreeDocument, config, options): TreeCoreCreationResult` 落地（config migration + `projectAndLayoutTree` + 原子 pair 安装）；删除 root exports `projectTree` / `layoutStructuredTree` / `simpleTreeLayout` / `layoutTreeWithElk`，`layoutWithElk` 仅保留 graph mode；tree core 禁止 `setTreeOwner` / graph-pair `replaceDocument` / `replaceDocumentFromHost`，新增 validated `replaceTreeFromHost(tree, epoch)` + `getTreeDocument()` / `relayoutTree()` / tree-only export；config migration `1.0`/`1.0.0` → `1.1.0`（autoLayout 删除、appearance→tree.layoutSize 拷贝、version 拒绝、失败原子）；`NodeTypeConfig.tree` 承接 `TreeNodeTypeConfig`（deprecated alias）；JSON-safe payload 校验、canonical 序列化、structured errors（duplicate-id/reserved-id/unknown-node-type/invalid-layout-size/invalid-spacing/invalid-tree-payload/cyclic-tree/invalid-tree-config/unsupported-version 等）；中央 tree-mode mutation gate（13+ graph 方法 no-op/null/unavailable + `mutationRejected` 诊断）；pair history replay（undo/redo/restore/rollback 恢复存储 pair 不重跑布局）。新模块：`tree-validation.ts`、`tree-structure.ts`、`tree-session-impl.ts`、`core/graph-command-gate.ts`、`core/config-migration.ts`。
  - **Phase 2 structured projection/geometry/empty branches**：唯一 `projectAndLayoutTree`（main/cross 轴、固定 footprint、整数锚点、rounding 后子树边界）；min gaps chain=60 / TB split=134 / LR split=204 / merge=120（layerSpacing 低于下限按 floor 生效，高于下限原样使用）；split/merge 共享线 midpoint 公式；`__fdTree` runtime geometry（kind/direction/ownerId/branchId/continuationId/lineMain/fanoutCross）；tree edge decoration 白名单校验（markerEnd/animated/label/body/strokeDasharray 拒绝 → `unsupported-tree-edge-decoration`，`TreeNodeBranch.data.label` 合法）；虚拟 empty branch slot（`__fd_internal__/slot/<owner>/<branch>`、`__fd-tree-empty-slot`、TB ≥120×52 / LR ≥140×32、affordance 120×32）；`insertBranchChild` + `deleteTreeNode` rewrite matrix；geometry oracle tests（TB/LR × nodeSpacing 0/60 × layerSpacing 0/80/200 零正面积相交）。
  - **Phase 3 renderer tree session/writeback/command boundary**：`TreeModeLayoutWrapper` 改用 `createTreeDesignerCore` + error surface；`TreeDocumentSession`（session UUID、serial FIFO dispatch、strict ack matching、256-entry digest LRU、coalesced tail-only、epoch replacement、`classifyActionResult` canonical classification、generation invalidation、dispose）；schema fields `treeDocumentEpoch` / `treeDocumentAckSessionId` / `treeDocumentAckDispatchId` / `treeDocumentChangeAction` 全量进入 prop contract/fields/formal validation；adapter/provider/canvas/shortcut 统一 graph-only command gate + all-or-nothing tree `deleteSelection`；host projection sanitization（virtual ids/incident edges/`__fd*`/stale selection 剥离，counts 重算）；mounted config 冻结到 key remount。
  - **Phase 4 DingFlow TB/LR rendering**：外层 geometry wrapper + 内层 clipped body（box-sizing border-box / overflow hidden）；center-based + placement（36+14px）；TB/LR handles（render-ports 显式 rounded anchors）；DingFlowEdge 只消费 `__fdTree`（butt/round、strokeWidth 1..4 clamp、无 marker/label）；overlays 从 `__fdTree` lineMain/fanoutCross 计算（移除 `outs[0]`/`ins[0]` 猜测与 BRANCH_SHORT_LEG/MERGE_SHORT_LEG）；empty slot affordance + `DingFlowAddNodeMenu` → `insertBranchChild`；`dingflow-command-dispatch` 支持 slot/merge/branch-group kinds。
  - **Phase 5 migration/docs/e2e**：playground tree schemas ×4 + docs examples ×2 迁移至 config 1.1.0（无 authored autoLayout、显式 layoutSize、edge appearance 白名单清理、action-flow 补 mergeEdgeType）；owner docs 更新（tree-mode.md 重写、design.md §17、config-schema.md §20/21、dingflow-visual-spec.md §2/3/5/7/8、api.md、collaboration.md、canvas-adapters.md、runtime-snapshot.md、designer-page/design.md、designer-view-vs-edit.md）；repo 搜索确认已删除 API 无活跃消费者（仅独立 style prototype 页保留历史常量）；新增 `tests/e2e/flow-designer-tree-mode.spec.ts` 4 用例（mount 零错误、固定 footprint/clip、node 碰撞 DOM bounds、action-flow mount）全绿。
  - **关键 bug 修复**：`migrateTreeConfig` 早期 JSON round-trip 会破坏 compiled schema body（tree tab 渲染 "Element type is invalid"）→ 改为结构化迁移保留对象引用；`use-designer-auto-layout` tree 分支改用同步 `relayoutTree()`（ELK 零调用，graph mode 保留）；edge path Playwright visibility 断言改用 path `d` 属性 + edge group count。
  - **验证**：workspace `pnpm typecheck` / `pnpm build` / `pnpm lint` 全绿；`pnpm test` 62/62 task 全绿（flow-designer-core 175、flow-designer-renderers 209、其余包无回归）；`pnpm test:e2e -- --grep "flow-designer|dingflow|taskflow|tree-mode"` 42/42 通过。playground-entry-pages `graph-demo` assertion-coverage 缺口为 pre-existing（clean HEAD 同值复现，与本次变更无关）。
