# Development Log - 2026-06-12

### 2026-06-12

- Full-green verification after the `scope-debug` serialization alignment is complete: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed with the new JSON-like debug contract. Full-suite evidence includes `@nop-chaos/flux-renderers-basic` focused scope-debug regressions green, `@nop-chaos/flux-renderers-form-advanced` 72 files / 681 tests passed, `@nop-chaos/flux-renderers-data` 36 files / 325 tests passed, `@nop-chaos/nop-debugger` 18 files / 122 tests passed, and `apps/playground` 19 files / 88 tests passed.
- Link to doc or code path: `packages/flux-renderers-basic/src/{scope-debug.tsx,__tests__/scope-debug.test.tsx,__tests__/basic-coverage-gaps.test.tsx}`, `docs/architecture/playground-experience.md`, `docs/testing/e2e-standards.md`

- Refined `scope-debug` serialization so its output now tracks `JSON.stringify` semantics much more closely: object properties with `undefined` are omitted, array `undefined` entries become `null`, and non-JSON types are displayed with explicit encodings such as `@function`, `@bigint:...`, and `@circular` instead of ambiguous placeholders like `"[undefined]"`.
- Added focused proof in `packages/flux-renderers-basic/src/__tests__/{scope-debug.test.tsx,basic-coverage-gaps.test.tsx}` and re-ran package typecheck to confirm the new debug contract.
- Link to doc or code path: `packages/flux-renderers-basic/src/{scope-debug.tsx,__tests__/scope-debug.test.tsx,__tests__/basic-coverage-gaps.test.tsx}`

- Follow-up fix after executing plan 447: confirmed that the reported `"[undefined]"` values were primarily coming from `scope-debug` display serialization rather than persisted condition-builder state, then fixed a real adjacent bug in `condition-group.tsx` where newly added conditions bypassed `resolveDefaultOp()` and could mix legacy operator vocabularies such as `eq` with newer defaults such as `equal`.
- Added regression proof that repeatedly adding new conditions keeps `right` absent/`undefined` in real form state instead of serializing `"[undefined]"`, and that field-defined operator vocabularies are honored when creating new conditions.
- Link to doc or code path: `packages/flux-renderers-form-advanced/src/condition-builder/{condition-group.tsx,config-actions.test.tsx,condition-builder-renderer.test.tsx}`, `packages/flux-renderers-basic/src/scope-debug.tsx`

- Full-green verification for plan 447 is complete: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed after the condition-builder custom value-editor remediation landed. Verification evidence included focused custom-editor regressions plus full-suite green for `@nop-chaos/flux-renderers-form-advanced` (72 files / 678 tests) and `apps/playground` (19 files / 88 tests).
- Link to doc or code path: `docs/plans/447-condition-builder-schema-value-editor-remediation-plan.md`, `packages/flux-renderers-form-advanced/src/condition-builder/{condition-builder.tsx,condition-group.tsx,condition-item.tsx,value-input.tsx,value-input.test.tsx,condition-builder-renderer.test.tsx}`

- Executed the live code portion of `docs/plans/447-condition-builder-schema-value-editor-remediation-plan.md`: removed the exported React widget-registry APIs from `@nop-chaos/flux-renderers-form-advanced`, rewired `condition-builder` custom fields to render `field.value` as Flux schema fragments, and projected each condition item's `right` value through standard `name: 'value'` bindings for both form-owned and page-owned usage.
- Added focused regression proof for the corrected contract in `packages/flux-renderers-form-advanced/src/condition-builder/{value-input.test.tsx,condition-builder-renderer.test.tsx}`, including positive writeback coverage and negative proof that invalid custom editor schemas do not silently fall back to unrelated built-in widgets.
- Refined `docs/components/condition-builder/design.md` so the owner doc now states the live projected-owner/value-binding contract explicitly instead of only the general design direction.
- Link to doc or code path: `packages/flux-renderers-form-advanced/src/condition-builder/{condition-builder.tsx,condition-group.tsx,condition-item.tsx,value-input.tsx,types.ts}`, `packages/flux-renderers-form-advanced/src/index.tsx`, `docs/components/condition-builder/design.md`, `docs/plans/447-condition-builder-schema-value-editor-remediation-plan.md`

- Corrected the `condition-builder` owner design after re-auditing the extensibility seam against the higher-precedence renderer/field architecture docs. Removed the incorrect React widget-registry direction from `docs/components/condition-builder/design.md` and replaced it with the supported Flux-native rule: custom value editors must be authored as Flux JSON schema / compiled fragments rendered through the normal compiler/runtime path, not runtime-registered React components.
- Added `docs/plans/447-condition-builder-schema-value-editor-remediation-plan.md` as the explicit execution plan to remove the live React-registry code drift and re-align code/tests/package exports with the corrected schema-driven contract.
- Link to doc or code path: `docs/components/condition-builder/design.md`, `docs/plans/447-condition-builder-schema-value-editor-remediation-plan.md`, `docs/architecture/{field-metadata-slot-modeling.md,renderer-runtime.md}`

- Full-green verification for the plan 445 execution branch is now complete: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed after landing runtime-owned form-store diagnostics plus follow-up fixes in `strict-mode`, compiler fail-fast honoring of `continueOnError`, and condition-builder multi-select/operator regression cleanup.
- Verification summary: workspace `typecheck` green, workspace `build` green, workspace `lint` green, workspace `test` green; focused diagnostics and wrapper-path tests also green.
- Link to doc or code path: `packages/flux-core/src/{types/runtime.ts,strict-mode.ts}`, `packages/flux-runtime/src/{form-store.ts,form-store-owned.ts}`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-renderers-form-advanced/src/condition-builder/{value-input.tsx,operators.ts}`, `docs/plans/445-form-store-diagnostics-and-debugger-bridge-plan.md`

- Executed plan 445 by landing runtime-owned form-store diagnostics on `FormStoreApi`: explicit session controls (`startDiagnosticsSession`, `stopDiagnosticsSession`, `clearDiagnosticsSession`), bounded retained commit snapshots, dropped-commit accounting, and coarse truthful `changedPaths` / `changedKinds` capture in `packages/flux-runtime/src/form-store.ts`.
- Extended projected/public store wrappers to preserve consumer-visible path semantics for diagnostics snapshots instead of leaking parent absolute coordinates: `packages/flux-runtime/src/form-store-owned.ts` and `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.ts`.
- Added focused automated proof for disabled-mode gating, stop/clear semantics, bounded retention, mixed batch commits, and projected path translation in `packages/flux-runtime/src/__tests__/form-store-subscriptions.test.ts` and `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.test.ts`.
- Updated owner/reference docs to reflect the landed runtime contract and created explicit successor bridge ownership in `docs/plans/446-form-store-debugger-bridge-plan.md`.
- Link to doc or code path: `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/form-store.ts`, `packages/flux-runtime/src/form-store-owned.ts`, `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.ts`, `docs/architecture/form-store-diagnostics.md`, `docs/plans/446-form-store-debugger-bridge-plan.md`

- Added `docs/architecture/form-store-diagnostics.md` as the normative owner doc for bounded, explicitly gated form-store commit diagnostics, and added `docs/plans/445-form-store-diagnostics-and-debugger-bridge-plan.md` (runtime-focused scope with explicit successor routing for debugger bridge work).
- Ran multiple independent subagent reviews over the new architecture/plan docs and tightened the result surface before closure: clarified that runtime owns capture, v1 requires explicit session controls, debugger bridge delivery is not silently implied by the runtime plan, and `docs/index.md` now routes store-diagnostics work to the new owner doc.
- Link to doc or code path: `docs/architecture/form-store-diagnostics.md`, `docs/plans/445-form-store-diagnostics-and-debugger-bridge-plan.md`, `docs/index.md`

- Added a manual test record for `condition-builder` AND/OR toggle lag in `docs/testing/2026/06-12.md`, documented the bug in `docs/bugs/63-condition-builder-conjunction-hover-selected-visual-lag-fix.md`, and diagnosed it with a focused regression test at `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder-latency.test.tsx`.
- Verified the logic path is healthy: the pressed state updates normally and a single conjunction change stays within a bounded number of form store commits, so the reported slowness was not a store/update-latency regression.
- Fixed the perceived lag by removing the conjunction pill color transition in `packages/flux-renderers-form-advanced/src/condition-builder/condition-group.tsx`, switching the AND/OR toggle from `transition-colors` to `transition-none` so the selected state appears immediately.
- Link to doc or code path: `docs/testing/2026/06-12.md`, `docs/bugs/63-condition-builder-conjunction-hover-selected-visual-lag-fix.md`, `packages/flux-renderers-form-advanced/src/condition-builder/{condition-group.tsx,condition-builder-latency.test.tsx}`

- Tightened the automated-test baseline so Vitest and Playwright now bootstrap with strict schema validation enabled and schema compile diagnostics configured as fail-fast gates. This prevents playground/test schemas from silently compiling through invalid `component:<method>` payload contracts during automated runs.
- Added regression coverage for compile-time rejection of `component:submit` payloads when the target form capability publishes no args contract in `packages/flux-compiler/src/schema-compiler-host-action-validation.xui-actions.test.ts` and aligned `packages/flux-react/src/__tests__/schema-renderer.test.tsx` with the stricter automated-validation baseline.
- Fixed the `#/flux-basic` key-value demo submit wiring so `component:submit` no longer passes unsupported `{ method, url }` payload into the form component handle. The form now owns `/api/composite-demo` through `submitAction`, which restores normal validation-blocked behavior and prevents debugger-only action failures.
- Added a page-level regression in `apps/playground/src/pages/flux-basic-page.debugger.test.tsx` covering `Add metadata pair -> Submit key-value demo`, asserting the page surfaces `Entry 1 key is required` and does not emit `/api/composite-demo` request traffic when validation blocks submission.
- Added the manual test record at `docs/testing/2026/06-12.md` for the original user-reported repro and resolution.
- Link to doc or code path: `vitest.shared.ts`, `test-setup/strict-validation.ts`, `playwright.config.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/schema-compiler-host-action-validation.xui-actions.test.ts`, `packages/flux-react/src/__tests__/schema-renderer.test.tsx`, `apps/playground/src/pages/{fluxBasicPageSchema.json,flux-basic-page.debugger.test.tsx}`, `docs/testing/2026/06-12.md`
