# Development Log - 2026-07-28

### 2026-07-28

- **MISSION_DRIVER — Compile-Once Contract Remediation** (plan `docs/plans/2026-07-28-0800-1-compile-once-contract-remediation.md`):
  - All 3 Phases completed: CRUD item/card → compiled region handles (P0), NodeFrameWrapper frameWrap → resolvedMeta (D09-02 P1), DetailField parentScope.get() → useScopeSelector (D09-06 P1).
  - Cross-module impact: added `frameWrap` to `ResolvedNodeMeta`/`metaProgram`, updated variant-field-view to read from `meta.frameWrap` instead of `schemaProps.frameWrap`.
  - New tests: `crud-list-mode.test.tsx` (compile-once P0 + AUDIT-02), `frame-slot-meta.test.tsx` (D09-02 compile-once), `detail-field-basic.test.tsx` (D09-06 reactive subscription), `crud-item-card-compile-contract.test.ts` (C-03).
  - Verification: `pnpm typecheck 58/58 ✓`, `pnpm build 31/31 ✓`, `pnpm lint 31/31 ✓`, `pnpm test 58/58 ✓` (full green).
  - Source audits closed: `docs/audits/2026-07-28-0650-multi-audit-audit-remediation.md`, `docs/audits/2026-07-28-0650-open-audit-audit-remediation.md`.

- **MISSION_DRIVER — build verification for MG**:
  - `pnpm typecheck` 58/58 ✓, `pnpm build` 31/31 ✓, `pnpm lint` 31/31 ✓, `pnpm test` 58/58 ✓ (verified green, docs-only changes).
  - Plan: `docs/plans/2026-07-27-2156-3-mg-guard-activation.md` — closure confirmed, build passes. Committed.

- **MV — 全量验证与回归** plan executed to completion:
  - `pnpm typecheck` 58/58 ✓, `pnpm build` 31/31 ✓, `pnpm lint` 31/31 ✓, `pnpm test` 58/58 ✓ (full green).
  - `pnpm test:e2e` sample: 60/61 passed (1 pre-existing ai-citations duplicate-key flake).
  - All 13 `check:audit-*` scripts re-run; values within M0 baseline (most improved, minor +1 in test-global-leaks due to new test).
  - `pnpm audit:deps`: 14 violations (unchanged). `pnpm audit:knip`: exit 1 (unchanged).
  - arm-index integrity: all 48 P0/P1 + 21 P2 findings `fixed`. No open findings.
  - MV arm-index status → `done`. Roadmap MV.1–MV.3 → `done`.
  - Plan file: `docs/plans/2026-07-27-2156-2-mv-full-verification.md` — all phases completed, all items ticked, closure gates satisfied.
  - arm-index Audit Tool Baseline updated with MV re-run dates and values.

- **Surface Lifecycle Callbacks — feature implementation** landed:
  - Plan: `docs/plans/2026-07-28-1430-surface-lifecycle-callbacks.md` (all 5 Phases `completed`)
  - Design: `docs/architecture/surface-lifecycle-callbacks.md` (architecture-doc README compliant; design-only, no migration diary)
  - Analysis (调研来源): `docs/analysis/2026-07-27-flux-dialog-submit-refresh-design.md`
  - Goal: 让业务方在 schema 里写 `openDialog args.onSubmitSuccess: { action: 'refreshNearest' }` 即可在 dialog form 提交成功后刷新外部 CRUD / data-source / tree，**不需要知道外层组件 id/name**，且多 form dialog 场景行为可控（通过 `form.submitScope` 区分主提交 form）。
  - Phase 1 — Owner Context Infrastructure:
    - `OwnedSurfaceStateBase` 加 `onCloseNodes` / `onSubmitSuccessNodes` / `onSubmitErrorNodes` (`ActionSchema | ActionSchema[]`) + `ownerActionCtx` (完整 ActionContext 快照)（`packages/flux-core/src/types/runtime.ts`）
    - `SurfaceRuntimeOptions` + `SurfaceRuntime.open` 实现透传
    - `action-adapter.ts` openDialog/openDrawer 把 args.onClose/onSubmitSuccess/onSubmitError + caller ctx 存到 entry
  - Phase 2 — refreshNearest action:
    - 新增 `packages/flux-runtime/src/refresh-nearest.ts`：`findNearestRefreshable` + `refreshNearest`，沿 scope.parent 链向上查找最近的 CRUD / tree / data-source
    - `ComponentHandleRegistry.findFirstInScope`（新增 scope-id 索引）+ `SourceRegistry.findFirstInScope`
    - `ComponentHandle` 加可选 `scope` 字段；register/unregister 维护 scope 索引
    - action-adapter + built-in-actions 注册 `case 'refreshNearest'`
  - Phase 3 — Hooks Triggering:
    - 新增 `packages/flux-runtime/src/surface-hooks.ts`：`dispatchInOwner` 在 owner ctx 触发 hook schema，注入 `$formData` / `$result` / `$hook`
    - `surface-runtime.close` / `closeTop` 改为"先 dispose + fire-and-forget hook"（保持 sync close 契约，不破坏 5 处调用方）
    - `surface-runtime.triggerHook` 新增（form.tsx 调用入口）
    - `form.tsx` setLifecycleHandlers 包装 onSubmitSuccess/onSubmitError：先调 form 自己的 handler，再调 `triggerSurfaceSubmitHook`（仅当 `submitScope === 'surface'`）
    - declarative surface function-based `onClose` 路径完全不动
  - Phase 4 — Schema field `submitScope: 'local' | 'surface'` 加到 `FormSchema`（flux-renderers-form + flux-guide d.ts 同步）；严格 schema validator 推迟（Deferred）
  - Phase 5 — flux-guide examples + 现有 design-patterns / flux-types / 04-action-system 文档已对齐
  - Verification: `pnpm typecheck/build/lint/test` 4 项全绿；focused tests `surface-lifecycle-hooks.phase1.test.ts` (3 tests) / `refresh-nearest.phase2.test.ts` (8 tests) / `surface-lifecycle-hooks.phase3.test.ts` (3 tests) 通过；flux-renderers-form 全套 593 tests / flux-react 全套 468 tests / flux-runtime 全套 1375 tests 保持绿
  - Closure audit: round 1 由 fresh sub-agent session 执行，发现 3 个 owner-doc drifts + 2 个文本一致性 issue + daily-log 缺失，已全部修复。Round 2 重审 pending。
