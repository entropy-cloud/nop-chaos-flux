# 2026-04-21 Dev Log

### 2026-04-21 (session 9)

- Renamed the first primitive from `Base Tree` to `Template` across architecture docs, with enriched semantics emphasizing compile-time artifact, immutability, and template/instance separation.
- Link to doc or code path: `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-dsl-vm-extensibility.md`, `docs/architecture/flux-design-principles.md`, `docs/articles/flux-programming-model-explained.md`
- Key decision: `Template` better conveys the primitive's nature as a compile-time, immutable program definition that can be instantiated multiple times at runtime. The name `Base Tree` implied a dynamic structure and did not express the compile-once/instantiate-many relationship or the connection to `TemplateNode`/`CompiledTemplate` in the implementation. Updated content to describe: `SchemaCompiler` produces immutable `TemplateNode` graph; runtime only consumes and instantiates; physical form can be tree, graph, or flat table; `templateNodeId` identifies compiled structure, `cid` identifies live instance.
- Next step: continue reviewing remaining docs and discussions for any stray `Base Tree` references.

### 2026-04-21 (session 8)

- Added `docs/plans/123-flux-runtime-split-and-boundary-hardening-plan.md` as the new owner plan for post-compiler `flux-runtime` seam hardening, focused on pre-package boundaries for action/request/source/reaction rather than reviving the cancelled `kernel/session` direction.
- Link to doc or code path: `docs/plans/123-flux-runtime-split-and-boundary-hardening-plan.md`, `docs/plans/118-flux-internal-kernel-session-refactor-plan.md`, `docs/plans/120-runtime-async-governance-convergence-plan.md`, `docs/plans/122-compiler-package-extraction-and-boundary-plan.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/reaction-runtime.ts`
- Key decision: revise plan 123 so it must actually land `@nop-chaos/flux-action-core` in the same owner plan; keep public `ActionContext` stable, keep `ActionScope` / component registry / import lifecycle runtime-owned, make `action-core` own dispatch ordering + execution framework only, and route all runtime-specific effects through one runtime adapter object.
- Independent review outcome: repeated fresh subagent audits first forced scope tightening, then converged on “coherent enough to execute” after the plan froze the built-in ownership matrix, kept `ActionContext` unchanged, documented `operation-control` as generic async execution control rather than ajax-specific logic, and finally hardened the baseline to “compiled-action execution only” with `evaluateCompiled(...)` as the only action-core runtime-eval dependency.
- Next step: execute plan 123 with `flux-action-core` extraction as the owner result, assuming compiled actions only and no delayed compile fallback, then open only the `flux-request-runtime` successor plan if the post-extraction runtime/request boundary still needs a dedicated package owner.

### 2026-04-21 (session 7)

- Follow-up cleanup: migrated the remaining five `schema-compiler-*.test.ts` files from `packages/flux-runtime/src/` into `packages/flux-compiler/src/`, and adjusted one stale helper-level diagnostics test to match the current `flux-compiler` host-action validation helper signatures.
- Landed the compiler package extraction from `flux-runtime` into `@nop-chaos/flux-compiler`, moved registry ownership to `@nop-chaos/flux-core`, and rewired runtime/direct consumers to the new package boundary.
- Link to doc or code path: `packages/flux-compiler/src/index.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/action-compiler.ts`, `packages/flux-compiler/src/compile-symbol-table.ts`, `packages/flux-core/src/registry.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/form-runtime-subtree.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `docs/architecture/schema-file-validator.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/plans/122-compiler-package-extraction-and-boundary-plan.md`
- Key decision: keep `@nop-chaos/flux-runtime` as a compatibility re-export layer for compiler APIs during migration, but make `@nop-chaos/flux-compiler` the physical owner of schema compile/validate, action precompile, compile symbol tables, diagnostics, and validation lowering.
- Verification follow-up: while closing plan 122, fixed two runtime-adjacent regressions blocking clean verification: removed the `react-hooks/set-state-in-effect` lint violation in `packages/flux-react/src/use-node-imports.ts`, and normalized top-level `SourceSchema.api` in `packages/flux-runtime/src/data-source-runtime.ts` so source-backed option controls again surface loading and inline error state.
- Verification passed: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Focused verification also passed for the cleanup slice: `pnpm --filter @nop-chaos/flux-compiler typecheck`, `pnpm --filter @nop-chaos/flux-compiler test -- src/schema-compiler-diagnostics.test.ts src/schema-compiler-host-contract.test.ts src/schema-compiler-renderer-contracts.test.ts src/schema-compiler-table.test.ts src/schema-compiler-validate-schema-adapter.test.ts`, `pnpm --filter @nop-chaos/flux-runtime test`.
- Next step: reduce remaining compatibility re-exports/importers over time so new code imports compiler APIs directly from `@nop-chaos/flux-compiler`.

### 2026-04-21 (session 6)

- Added `docs/plans/122-compiler-package-extraction-and-boundary-plan.md` to turn the current compiler refactor discussion into an executable owner plan instead of leaving it as an implicit runtime follow-up.
- Link to doc or code path: `docs/plans/122-compiler-package-extraction-and-boundary-plan.md`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/action-compiler.ts`, `packages/flux-runtime/src/compile-symbol-table.ts`, `docs/architecture/schema-file-validator.md`, `docs/architecture/flux-runtime-module-boundaries.md`
- Key decision: treat schema compile/validate, action precompile, compile-time symbol tables, and compiled validation-model lowering as compiler-owned responsibilities under a new `@nop-chaos/flux-compiler` package, while keeping runtime evaluation, dispatch, source/reaction/form/page/surface lifecycle, and live value validation in `@nop-chaos/flux-runtime`.
- Next step: execute the plan by creating the new package skeleton, moving compiler-owned files and tests out of `flux-runtime`, and cutting the remaining compiler-to-runtime pure-helper dependencies via the smallest necessary `flux-core` extraction.

### 2026-04-21 (session 5)

- Completed the live implementation slices for `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md`: runtime `ImportStack` landed in `packages/flux-runtime/src/import-stack.ts`, React node import setup now uses frame-based lexical ownership, schema compilation threads `CompileSymbolTable`, and formula compilation now emits conservative `$` diagnostics plus static folding for builtin-only expressions.
- Link to doc or code path: `packages/flux-runtime/src/import-stack.ts`, `packages/flux-runtime/src/imports.ts`, `packages/flux-react/src/use-node-imports.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/compile-symbol-table.ts`, `packages/flux-formula/src/compile.ts`, `packages/flux-formula/src/index.test.ts`, `packages/flux-react/src/schema-renderer-imports-basic.test.tsx`, `packages/flux-react/src/schema-renderer-imports-errors.test.tsx`, `docs/architecture/module-cache-and-import-stack.md`, `docs/architecture/action-scope-and-imports.md`
- Key decision: keep compile-time symbol validation conservative and additive. Builtin/import/slot/injected-local categories are now known at compile time, but import-helper signature manifests remain a separate follow-up instead of blocking this plan.
- Next step: run the required full-workspace `typecheck`, `build`, `lint`, and `test`, then record an independent closure audit before freezing plan 116 as fully closed.

### 2026-04-21 (session 4)

- Removed the remaining first-version action payload compatibility from the active authoring baseline so request and surface actions now use only `args`, while `setValue` and `setValues` remain the only specialized built-in payload carriers.
- Updated runtime-facing examples, regression tests, and CRUD/table docs to the final contract: `ajax` / `submitForm` use `args: ApiSchema`, `openDialog` / `openDrawer` use `args`, and the old `action: 'dialog'` / `action: 'drawer'` aliases no longer appear in active docs or targeted tests.
- Link to doc or code path: `packages/flux-renderers-form-advanced/src/`, `packages/flux-renderers-data/src/crud-schema.ts`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `docs/references/action-payload-matrix.md`, `docs/references/flux-json-conventions.md`, `docs/components/crud/design.md`, `docs/components/crud/example.json`, `docs/components/form/example.json`, `docs/components/reaction/example.json`
- Key decision: keep the public action contract minimal and final in v1 instead of documenting deprecated carriers that the runtime no longer accepts for these built-ins.
- Next step: run targeted renderer/data tests, then full workspace `typecheck`, `build`, `lint`, and `test` to confirm no remaining active-doc or test drift.

### 2026-04-21 (session 3)

- Expanded the closure record in `docs/plans/118-flux-internal-kernel-session-refactor-plan.md` so the plan now states which runtime-ownership concerns were absorbed by narrower plans/docs and which gaps remain explicitly unowned by the abandoned `kernel/session` direction.
- Link to doc paths: `docs/plans/118-flux-internal-kernel-session-refactor-plan.md`, `docs/plans/120-runtime-async-governance-convergence-plan.md`, `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/debugger-runtime.md`
- Key decision: keep plan 118 cancelled, but make its closure section explain the replacement handling path so future audits do not misread it as untouched or merely deferred work.
- Next step: if runtime ownership refactoring is revived later, start from the live module/registry/import baseline instead of restoring the old `kernel/session` topology vocabulary.

### 2026-04-21 (session 2)

- Re-audited `docs/plans/118-flux-internal-kernel-session-refactor-plan.md` through `docs/plans/121-unified-value-adapter-for-all-field-types-plan.md` against the live repo and updated each plan to match actual landing state instead of stale status text.
- Closure decisions after live audit:
  - `118` marked cancelled/abandoned because the proposed explicit internal `kernel/session` topology never landed in `packages/flux-runtime/src/runtime-factory.ts` or related runtime docs.
  - `119` completed after landing the remaining `args`-authoring convergence for `ajax`, `submitForm`, `openDialog`, and `openDrawer`, and after explicitly removing the later ActionTrace observability expansion from the plan's owner scope.
  - `120` remains completed; previous external workspace blocker notes were cleared after a fresh full-workspace verification rerun passed.
  - `121` remains completed after a fresh post-cleanup audit confirmed no reopened `ValueAdapter` gap.
- Runtime changes for plan 119 closure:
  - `packages/flux-runtime/src/action-runtime-core.ts` now treats evaluated `args` as the preferred payload carrier for `ajax`, `submitForm`, `openDialog`, and `openDrawer`, while preserving legacy `api` / `dialog` / `drawer` compatibility.
  - Added focused regression coverage in `packages/flux-runtime/src/__tests__/runtime-actions-monitor.test.ts`, `runtime-actions-submit.test.ts`, and `runtime-dialogs-scope.test.ts`.
- Synced docs to the landed baseline:
  - `docs/references/action-payload-matrix.md`
  - `docs/references/flux-json-conventions.md`
  - `docs/examples/user-management-schema.md`
- Independent closure audit evidence came from fresh subagent audit `ses_252ce7a6fffe2ZkG2GLcS6JRo4`.
- Verification passed: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Key decision: when a plan has a landed core but later speculative expansion, shrink the owner scope and mark the extra expansion cancelled/out-of-scope instead of leaving the plan permanently half-open.
- Clarification recorded 2026-04-30 during plan 156 execution: the closure wording here overstated the `submitForm -> args` convergence. The runtime does prefer evaluated `args` as the common payload carrier in the compiled action path, but the live built-in `submitForm` invocation still reaches the adapter with `args: undefined` and submits through `ctx.form`, so the docs/log baseline has been downgraded to match live behavior.
- Next step: if runtime ownership refactoring or action trace observability is revisited later, create new focused owner plans from the then-current live baseline instead of reopening abandoned or closed plans.

### 2026-04-21

- Simplified `packages/flux-renderers-form-advanced/src/detail-view/value-adaptation-helper.ts` after Plan 121 closure by removing the exported `valueAdaptationOwnerHelper` object and its unused interface-only wrapper types.
- The detail-value adaptation boundary now stays minimal and local to staged owners via direct function exports: `runTransformIn()`, `runTransformOut()`, and `runValidate()`.
- Updated `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `detail-view.tsx`, and `value-adaptation-helper.test.ts` to consume the direct functions instead of the object wrapper.
- Continued the cleanup by inlining the old `ActionRunner` export into direct function signatures and collapsing adapter construction into one private helper.
- Updated `docs/architecture/value-adaptation-and-detail-field.md` so the staged-owner layer is documented as helper functions on top of `ValueAdapter`, not as a required helper object shape.
- Key decision: keep the detail-specific helper file as a local semantic boundary, but remove the extra object/interface shell because it no longer adds reuse or stability value.
- Next step: if future staged owners appear, keep reusing these helper functions unless a real shared stateful owner abstraction is needed.
