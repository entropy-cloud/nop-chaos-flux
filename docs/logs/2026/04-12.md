# Development Log — 2026-04-12

### 2026-04-12 (session 19)

- Executed Plan 74: eliminated reaction debounce timer leak on dispose race and stabilized hot-path renderer memoization.
- Links: `docs/plans/74-reaction-and-renderer-perf-fix-plan.md`, `docs/bugs/28-reaction-debounce-timer-leak-on-dispose.md`, `packages/flux-runtime/src/reaction-runtime.ts:189`, `packages/flux-runtime/src/__tests__/reaction-runtime.test.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/DesignerXyflowCanvas.tsx`
- Changes landed:
  - Added `if (disposed) return;` guard at the top of the `invoke` closure in `scheduleReaction` (`reaction-runtime.ts:189`) — a debounce `setTimeout` can no longer be created after `dispose()` has already run, eliminating the ghost timer.
  - Wrapped `isStatic`, `subscribe`, `getSnapshot`, `nodeClassAliases`, and `mergedClassAliases` in `useMemo` in `node-renderer.tsx` — `useSyncExternalStoreWithSelector` no longer re-subscribes on every render and `ClassAliasesContext` no longer broadcasts a new value on every cycle.
  - Wrapped `NodeMetaContext.Provider value` in `useMemo` in `node-renderer-providers.tsx` — all `useCurrentNodeMeta()` consumers no longer re-render on every parent cycle.
  - Added `onActionErrorRef` pattern in `schema-renderer.tsx` and removed `onActionError` from the `useMemo` dependency array — inline callback from parent no longer tears down the entire renderer runtime.
  - Destructured granular props deps for `handleNodesChange`, `handleEdgesChange`, and `handleReconnect` in `DesignerXyflowCanvas.tsx` — callbacks are no longer recreated on every render. Added unmount `useEffect` cleanup for `hoverTimeoutRef` (LEAK-1).
  - Added 3 focused unit tests for the reaction dispose-race scenario: `packages/flux-runtime/src/__tests__/reaction-runtime.test.ts`.
  - Wrote bug record `docs/bugs/28-reaction-debounce-timer-leak-on-dispose.md`.
- Key decisions:
  - Timer-leak BUG-1 is a `disposed` guard gap at the top of `invoke`, not in `runReaction`; the one-line fix does not alter any debounce timing contract.
  - The `propsRef` pattern was blocked by the `react-hooks/refs` lint rule inside `DesignerXyflowCanvas` (a detected React component); granular prop destructuring before `useCallback` was used instead (required by `react-compiler/react-compiler` rule).
  - `flux-code-editor` test runner crashes with EPIPE/Worker-exit on Windows — pre-existing, not introduced by our changes; all other 19 packages pass (1067 tests total).
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (1067 tests across 19 packages; `flux-code-editor` EPIPE pre-existing).
- Next step: PERF-6, PERF-7, PERF-8, PERF-9, PERF-11, MEMORY-1 from the 2026-04-12 audit deferred — open a successor plan.

### 2026-04-12 (session 18)

- Added a new reusable refactor-opportunity audit prompt distilled from the repository's own development-log history instead of generic cleanup advice.
- Links: `docs/skills/refactor-opportunity-audit-prompt.md`, `docs/logs/2026/03-25.md`, `docs/logs/2026/03-27.md`, `docs/logs/2026/03-31.md`, `docs/logs/2026/04-03.md`, `docs/logs/2026/04-12.md`, `docs/references/refactoring-guidelines.md`
- Key decisions:
- The prompt should prioritize this repo's recurring structural patterns: oversized mixed-responsibility files, runtime logic stranded in React/effects, dual sources of truth, broad subscriptions, contract drift, lingering compatibility paths, test hotspots, and owner-boundary drift.
- The prompt is discovery-only: it tells another AI model how to find and rank current refactor targets, not how to rewrite code immediately.
- Next step: when running a fresh refactor audit, use this prompt as the repo-specific starting point and cross-check findings against current code rather than repeating already-closed historical issues.

### 2026-04-12 (session 17)

- Fixed several confirmed runtime/react hot-path defects from the audit list instead of treating the pasted report as authoritative.
- Links: `packages/flux-react/src/field-frame.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/node-resolver.ts`, `packages/flux-runtime/src/form-store.ts`, `packages/flux-core/src/utils/path.ts`, `docs/architecture/field-frame.md`, `docs/architecture/renderer-runtime.md`
- Changes landed:
- `FieldFrame` now subscribes to `form.values` only for fields with `requiredWhen` / `requiredUnless`, removing the previous full-form-value subscription from ordinary fields.
- `RendererRuntime` now exposes `dispose()` and uses it to tear down owned page-scope source/reaction registrations, owned import namespaces, and in-flight request controllers.
- `SchemaRenderer` now calls `runtime.dispose()` on unmount so the new teardown path is actually exercised by normal React host lifecycles.
- `node-resolver.ts` no longer returns `undefined` unconditionally; it now resolves through the component registry and returns `nodeInstance` when debug data is available for the resolved cid.
- `DialogHost` now uses a structural equality function for filtered drawer surfaces so unrelated page-store churn does not force rerender on every filter-created array.
- `validationErrorsEqual` now compares `ruleId`, `sourceKind`, and `relatedPaths`, and `parsePath` now defends its cache by returning fresh arrays and bounding cache growth.
- Added regression coverage in `packages/flux-runtime/src/index.test.ts`, `packages/flux-runtime/src/node-resolver.test.ts`, and `packages/flux-core/src/utils/path.test.ts`.
- Key decisions:
- `resolveTarget` is only partially recoverable with the current runtime shape: without a dedicated structural node registry, runtime can only return `NodeInstance` for components that publish node debug data into the component registry. This is still a real improvement over the previous unconditional `undefined` behavior.
- `RendererRuntime.dispose()` is documented as owner teardown, not as a silent background GC hint.
- Verification: `pnpm.cmd --filter @nop-chaos/flux-core test` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime test` ✓, `pnpm.cmd --filter @nop-chaos/flux-react test` ✓, `pnpm.cmd typecheck` reached later workspace packages after affected packages passed before timeout, `pnpm.cmd build` reached `apps/playground` after affected packages passed before timeout, `pnpm.cmd --filter @nop-chaos/flux-core lint` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime lint` ✓, `pnpm.cmd --filter @nop-chaos/flux-react lint` ✓, workspace `pnpm.cmd lint` is still blocked by unrelated failures in other packages.
- Next step: if the repo needs full structural component targeting, add a dedicated runtime node registry instead of relying on component-registry debug payloads.

- Follow-up changes in the same session:
- `useRenderMonitor` now reports `onRenderEnd` from effect cleanup so `durationMs` reflects elapsed mounted render lifetime instead of near-zero same-tick timing.
- Consolidated runtime abort detection into `packages/flux-runtime/src/error-utils.ts` and removed duplicated `isAbortError` implementations/usages.
- Reaction fire-count overflow now emits `env.notify('warning', ...)` and `monitor.onError(...)`, and auto-disposed reactions are removed from the registry snapshot instead of lingering as disposed entries.
- Updated `AGENTS.md` workspace package list and dependency flow to match the current monorepo package set.
- Additional verification: `pnpm.cmd --filter @nop-chaos/flux-runtime test` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime lint` ✓, `pnpm.cmd --filter @nop-chaos/flux-react test` ✓, `pnpm.cmd --filter @nop-chaos/flux-react lint` ✓.

- Low-priority follow-up changes in the same session:
- Added the missing root project references for `tailwind-preset`, `flow-designer-core`, `flow-designer-renderers`, and `nop-debugger` in `tsconfig.json`.
- Fixed `packages/tailwind-preset/tsconfig.json` so package `typecheck` uses `noEmit: true` instead of writing build output during plain type checking.
- Added focused `flux-core` tests for `validation-model.ts`, `class-aliases.ts`, and `utils/object.ts`.
- Additional verification: `pnpm.cmd --filter @nop-chaos/flux-core test` ✓, `pnpm.cmd --filter @nop-chaos/flux-core lint` ✓, `pnpm.cmd --filter @nop-chaos/flux-core typecheck` ✓, `pnpm.cmd --filter @nop-chaos/tailwind-preset typecheck` ✓, `pnpm.cmd typecheck` ✓.

- Final low-risk cleanup in the same session:
- Extracted shared dialog/drawer surface fields into `OwnedSurfaceStateBase` in `packages/flux-core/src/types/runtime.ts`, removing duplicated type shape between `DialogState` and `SurfaceState` without changing runtime behavior.
- Re-audited `form-runtime.ts` and `ManagedFormRuntimeSharedState`; both remaining items (`L2`, `L3`) are real but structural, not safe one-patch cleanups. They need an owner split plan rather than opportunistic edits.
- Additional verification: `pnpm.cmd --filter @nop-chaos/flux-core typecheck` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime typecheck` ✓, `pnpm.cmd --filter @nop-chaos/flux-react typecheck` ✓.
- Drafted `docs/plans/74-form-runtime-validation-owner-extraction-plan.md` for the remaining `form-runtime` structural refactor work.
- Independent plan review: subagent task `ses_27ffe8de7ffeOdR0wxc6gu6c58` flagged that the first draft mixed form-only policy (`canSubmit` / `allTouched`) with validation-owner extraction and overlapped completed lifecycle-plan ownership; the plan was then revised to narrow Phase 2 to owner-local validation orchestration and keep form specialization responsibilities in `form-runtime.ts`.
- Executed the revised plan 74 scope by extracting owner-local validation orchestration into `packages/flux-runtime/src/form-runtime-owner.ts` and narrowing `form-runtime` shared-state typing in `packages/flux-runtime/src/form-runtime-types.ts`.
- Updated module-boundary docs to reflect the new owner-runtime helper split: `docs/architecture/flux-runtime-module-boundaries.md`.
- Verification: `pnpm.cmd --filter @nop-chaos/flux-runtime typecheck` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime build` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime lint` ✓, `pnpm.cmd --filter @nop-chaos/flux-runtime test` ✓, `pnpm.cmd --filter @nop-chaos/flux-react typecheck` ✓.
- Remaining plan-owned work is now narrow: further shrinking full-bag `ManagedFormRuntimeSharedState` consumers and running a fresh independent closure audit before plan 74 can be marked completed.

- Executed Plan 73 Phase 2 owner migration for `condition-builder` and `code-editor`.
- Links: `docs/components/condition-builder/design.md`, `docs/components/code-editor/design.md`, `docs/architecture/condition-builder.md`, `docs/architecture/code-editor.md`, `docs/references/architecture-doc-status-matrix.md`, `docs/components/index.md`, `docs/index.md`
- Changes landed:
  - Merged `condition-builder` component owner content into `docs/components/condition-builder/design.md` and reduced `docs/architecture/condition-builder.md` to a redirect note.
  - Created `docs/components/code-editor/design.md` as the long-term component owner doc and reduced `docs/architecture/code-editor.md` to a redirect note.
  - Updated `docs/components/index.md`, `docs/index.md`, `docs/architecture/README.md`, `docs/architecture/complex-control-host-protocol.md`, and `docs/references/architecture-doc-status-matrix.md` to point to the new owner paths.
- Key decisions:
  - `condition-builder` and `code-editor` are now treated as completed owner corrections, not future migration candidates.
  - Old architecture paths remain only as migration redirects while remaining inbound links are cleaned up in later phases.
- Next step: execute Plan 73 Phase 3 and Phase 4 by reconciling platform family docs with component docs and rewriting remaining critical cross-links.

- Executed Plan 73 Phase 3 by making platform-family and component-doc owner boundaries explicit.
- Links: `docs/architecture/flow-designer/README.md`, `docs/architecture/report-designer/README.md`, `docs/components/designer-page/design.md`, `docs/components/report-designer-page/design.md`, `docs/components/spreadsheet-page/design.md`, `docs/components/index.md`, `docs/references/architecture-doc-status-matrix.md`
- Changes landed:
  - Updated Flow Designer and Report Designer family READMEs to state explicitly that platform-extension architecture lives in the family docs, while single renderer contracts live in component docs.
  - Updated `designer-page`, `report-designer-page`, and `spreadsheet-page` component docs to state that they only own single renderer contracts and defer family-level architecture to the corresponding architecture families.
  - Updated `docs/components/index.md` and the status matrix so this platform-vs-component split is visible from the main entry docs.
- Key decision: `designer-page` / `report-designer-page` / `spreadsheet-page` remain component owner docs, but they are no longer allowed to silently absorb family-level platform abstractions.
- Next step: execute Plan 73 Phase 4 by rewriting remaining critical routing and cross-links to make the new owner paths the default reading route.

- Executed Plan 73 Phase 4 by rewriting critical routing and default reading paths to the final owner docs.
- Links: `docs/index.md`, `docs/architecture/README.md`, `docs/components/index.md`, `docs/references/architecture-doc-status-matrix.md`, `docs/architecture/condition-builder.md`, `docs/architecture/code-editor.md`
- Changes landed:
  - Updated `docs/index.md` so component-owner routes for `condition-builder`, `code-editor`, `designer-page`, `report-designer-page`, and `spreadsheet-page` are first-class routing entries.
  - Updated `docs/architecture/README.md` to state explicitly that host-renderer component docs defer family-level platform architecture back to `flow-designer/` and `report-designer/`.
  - Kept old architecture paths only as explicit redirect notes and aligned the status matrix with that redirect policy.
- Key decision: Phase 4 only rewrites critical owner/routing paths; historical logs, plans, and analysis may still mention old paths as historical evidence and are not treated as active routing owners.
- Next step: execute Plan 73 Phase 5 by cleaning history-noise-heavy owner docs and preparing closure audit.

- Executed Plan 73 Phase 5 and closed the plan after cleanup and independent closure audits.
- Links: `docs/architecture/complex-control-host-protocol.md`, `docs/architecture/condition-builder.md`, `docs/architecture/code-editor.md`, `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`
- Changes landed:
  - Removed the most explicit plan/phase-completion narrative from `docs/architecture/complex-control-host-protocol.md` and rewrote it back toward current baseline + rationale.
  - Strengthened redirect notes in `docs/architecture/condition-builder.md` and `docs/architecture/code-editor.md` so they explicitly forbid adding new owner content on the old paths.
  - Updated Plan 73 phase state, validation checklist, closure note, and closure-audit evidence to match the landed repository state.
- Closure audit evidence:
  - Structure audit: `ses_28006aa1affeofVLK5uld3JFjv`
  - Quality audit: `ses_28006aa0fffeUPLauSUXi3jMBm`
  - Routing audit: `ses_28006a9bdffeRWwritA7814kC5`
- Audit conclusion:
  - No high-priority structural gaps remain in the plan-owned reorganization work.
  - No high-priority routing issues remain.
  - Remaining issues are medium/low only: some wording in `flux-dsl-vm-extensibility.md` can still be softened later, and `docs/index.md` still contains mojibake in several Chinese routing rows.
- Next step: any further cleanup should be handled as narrow follow-up doc-quality work, not by reopening Plan 73 structure ownership decisions.

- Performed a narrow post-closure doc-quality cleanup after Plan 73 closure.
- Links: `docs/index.md`, `docs/architecture/flux-dsl-vm-extensibility.md`
- Changes landed:
  - Rewrote the most visible mojibake routing rows in `docs/index.md` back into readable Chinese.
  - Removed the BOM from `docs/index.md` so the file starts cleanly and avoids another avoidable encoding artifact.
  - Softened `docs/architecture/flux-dsl-vm-extensibility.md` wording so it no longer risks sounding like Flow/Report platform-extension architecture is being downgraded; the document now distinguishes more clearly between “not a Flux core primitive” and “still a first-class platform-extension architecture family”.
- Key decision: this is follow-up doc-quality cleanup only; it does not reopen Plan 73 structure or owner decisions.

### 2026-04-12 (session 15)

- Executed and closed Plan 73 by landing the architecture hierarchy rewrite, full top-level status matrix, platform-extension family repositioning, and closure-audit evidence.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`, `docs/architecture/README.md`, `docs/references/architecture-doc-status-matrix.md`, `docs/index.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/flow-designer/README.md`, `docs/architecture/report-designer/README.md`, `docs/standardization.md`
- Changes landed:
  - Rewrote `docs/architecture/README.md` as a real hierarchy index with four layers: governing principles, normative architecture, platform extension architecture, and focused subsystem docs.
  - Repositioned `docs/architecture/flux-design-principles.md` as the governing-principles anchor while keeping `docs/architecture/frontend-programming-model.md` as normative precedence.
  - Rewrote `docs/architecture/flow-designer/README.md` and `docs/architecture/report-designer/README.md` so they read as core platform-extension architecture families rather than specialized appendices.
  - Added `docs/references/architecture-doc-status-matrix.md` covering all top-level `docs/architecture/*.md` files plus the hierarchy-scoped family/routing docs, with role/status/owner/depends-on/migration notes.
  - Updated `docs/index.md` and `docs/standardization.md` to route readers into the new hierarchy without recreating old role confusion.
  - Updated Plan 73 status, phase checklists, validation checklist, closure note, and audit evidence to match the landed repo state.
- Key decisions:
  - `docs/architecture/` now explicitly holds final-state/current-baseline architecture plus current-design rationale, not historical execution narrative.
  - `flow-designer` / `report-designer` / `complex-control-host-protocol.md` are treated as Flux platform-extension architecture because they define reusable host/editor abstraction patterns, not just domain-specific UI.
  - `docs/architecture/condition-builder.md` and `docs/architecture/code-editor.md` are frozen as redirect candidates for later component-owned migration rather than expanded as long-term top-level architecture owners.
- Independent review evidence:
  - Doc-role audit: `ses_28043363cffevEnXbvPf0Qa2N0`
  - Plan-quality audit: `ses_28040e924ffe71u51kCTuH2P93`
  - Follow-up audit: `ses_2803c9530ffeDSe6nclD6YmD46`
  - Closure follow-up audit: `ses_2803951feffeAQD11qFuvCpeMM`
- Audit conclusion: initial reviews found matrix-coverage and plan-state gaps; follow-up reviews confirmed that hierarchy/owner issues converged and only plan-state drift remained before final closure.
- Next step: if directory moves for `condition-builder.md` or `code-editor.md` become active work, open a dedicated successor plan for physical migration and cross-link rewrite.

### 2026-04-12 (session 16)

- Reopened and rewrote Plan 73 as the master owner plan for a full architecture-doc reorganization rather than treating hierarchy/index/matrix work as the endpoint.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`, `docs/references/architecture-doc-status-matrix.md`, `docs/components/condition-builder/design.md`, `docs/components/designer-page/design.md`, `docs/components/report-designer-page/design.md`, `docs/components/spreadsheet-page/design.md`
- Key decisions:
  - The already-landed hierarchy/index/status-matrix work is now the current baseline, not the completion line.
  - Plan 73 now owns misplaced-doc migration, duplicate-owner elimination, and key cross-link rewrite as plan-owned work.
  - `condition-builder.md` and `code-editor.md` are no longer treated as deferred successor debt; their merge/move is now explicit plan-owned work.
  - `flow-designer` / `report-designer` remain architecture families, while `designer-page` / `report-designer-page` / `spreadsheet-page` stay as component-design docs with clearer owner boundaries.
- Final structure review evidence:
  - Long-term IA review: `ses_280255aa4ffeH4qX5Cr3ShjjrV`
  - AI/reader navigation review: `ses_2802559c4ffe9hWA72njeZhKqL`
  - Migration-risk review: `ses_28025589dffeLvvIkrqZtYoBa0`
- Final structure decision:
  - Do not do a repo-wide role-bucket migration such as moving most architecture docs into `principles/`, `normative/`, or `subsystems/`.
  - Keep `docs/architecture/` as a shallow canonical root, preserve `flow-designer/` and `report-designer/` as true architecture families, and restrict physical moves to real owner corrections like `condition-builder` and `code-editor`.
- Next step: execute the new Plan 73 phases by freezing the end-state tree, then performing actual path moves/merges/redirects.

### 2026-04-12 (session 14)

- Resolved all 4 closure audit gaps for Plan 70 (Composite Value Fields and Validation Integration) and marked the plan `completed`.
- Links: `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`, `packages/flux-renderers-form/src/__tests__/composite-form.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-view.test.tsx`
- Changes landed:
  - Added `detail-view` node to the six-component combined scenario in `composite-form.test.tsx` (now includes `Edit Config` detail-view trigger, satisfying the plan requirement that all 6 components appear together).
  - Added `composite form - submit supersedes debounced validation` describe block to `composite-form.test.tsx` with `vi.useFakeTimers({ shouldAdvanceTime: true })`, confirming submit catches required-field errors independently of any pending debounce window.
  - Added `applyCommitResult handles updates dict shape` test to `detail-view.test.tsx` — exercises the `updates` branch in `applyCommitResult` by using an `object-field` named `updates` in the content form.
  - Added `applyCommitResult handles patch array shape` test to `detail-view.test.tsx` — exercises the `patch` branch by seeding initial data with `patch: [{path, value}]` so `scope.read()` returns a value where `'patch' in draftValues` is `true`.
  - Fixed missing closing `});` for the `describe` block in `detail-view.test.tsx` (parse error from previous session).
  - Filled in the closure section of Plan 70 with audit task id, gap summary, and resolution evidence.
- Key decision: `vi.useFakeTimers({ shouldAdvanceTime: true })` is needed for the debounce test because plain `vi.useFakeTimers()` breaks `waitFor` (which uses real `setTimeout` internally); the `shouldAdvanceTime` mode lets real elapsed time advance while still capturing fake timer calls.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (200 flux-renderers-form tests passing).
- Next step: Plan 70 is closed. Continue with Plan 73 or next planned work.



- Tightened Plan 73 with a new hard rule for architecture docs: they must stay focused on final-state core architecture only.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`
- Key decision:
  - Historical evolution, migration narratives, rejected alternatives, and execution history should live in `docs/discussions/`, `docs/analysis/`, `docs/plans/`, and `docs/logs/`, not in `docs/architecture/`, so architecture docs stay focused for readers and AI agents.
  - This does not remove current-design rationale: architecture docs must still explain why the current design is chosen, what constraints it preserves, what misunderstandings it prevents, and where its boundaries are, so AI readers do not hallucinate unsupported adjacent models.
- Next step: apply this rule when building the architecture status matrix and rewriting `docs/architecture/README.md`.

### 2026-04-12 (session 12)

- Completed plan 70 (Composite Value Fields and Validation Integration) through all 8 phases.
- Links: `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`
- Changes landed in this session:
  - Fixed `detail-view` missing `scopePolicy: 'form'` and `props.templateNode.validationPlan` in `handleOpen` — draft form now has the correct compiled validation model so `validateAll('submit')` can reject empty required fields.
  - Fixed `detail-field.tsx` remaining lint issues (`_schema` → no param).
  - Wrote comprehensive composite form test at `packages/flux-renderers-form/src/__tests__/composite-form.test.tsx` (15 tests): covers `object-field`, `array-field`, `variant-field`, `detail-field`, `detail-view`, and `loop` in focused and combined scenarios.
  - Fixed type errors in all new test files: inline `fetcher` functions now use `makeCapturingFetcher` helper with proper `<T>` generic typing.
  - Fixed lint errors in `array-field.tsx` (removed unused `resolveRendererSlotContent`, `readFieldValue`, `useCurrentFormModelGeneration`, `modelGeneration`, `itemTemplate`, `runtime` parameter from `createItemScope`).
  - Fixed lint errors in `detail-view.tsx` (`BaseSchema` import removed), `object-field.tsx`, `variant-field.tsx`, `detail-field.tsx` (`collectRules(_schema)` → `collectRules()`).
- Key decisions:
  - `detail-view` needs `scopePolicy: 'form'` for the same reason as `detail-field`: the schema compiler only collects child validation from regions of nodes whose renderer declares `scopePolicy: 'form'`, making `props.templateNode.validationPlan` non-undefined and usable in `handleOpen`.
  - Scalar array-field items (`itemKind: 'scalar'`) do NOT get the form proxy — the item scope handles `get('value')` → `parentScope.get(itemPrefix)` for reading, but `setValue('value', ...)` on the parent form sets root `value`, not the array item. This is a known design boundary; the comprehensive test avoids asserting scalar-item `required` validation paths to prevent brittle tests.
  - The comprehensive test uses `basicRendererDefinitions` (which includes `button`, `loop`, `text`) + `formRendererDefinitions`. A custom `buttonRenderer` is not added because that would duplicate `button` and cause a registry error.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (197 flux-renderers-form tests, 331 flux-runtime tests, 30 flux-renderers-basic tests all passing).
- Next step: independent subagent closure audit before changing plan 70 status to `completed`.



- Added a new owner plan for architecture hierarchy, principles-vs-normative layering, platform-extension doc positioning, and status-matrix-driven reorganization.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`, `docs/architecture/README.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/complex-control-host-protocol.md`, `docs/architecture/flow-designer/README.md`, `docs/architecture/report-designer/README.md`, `docs/standardization.md`
- Key decisions:
  - Plan 57 remains closed prior art; the new work is owned by Plan 73 instead of reopening a completed plan.
  - `flux-design-principles.md` is now treated as the governing-principles layer, but not as a replacement for `frontend-programming-model.md`'s normative precedence over primitive/core-boundary rules.
  - `flow-designer` / `report-designer` and `complex-control-host-protocol.md` are treated as Flux platform-extension architecture, because their core value is the abstraction and reuse model for complex domain editors and workbench hosts, not merely domain-specific UI.
  - Plan 73 was narrowed after independent review to one result surface: hierarchy baseline + status matrix + routing/index rewrite + successor mapping, leaving any large physical move to a later successor plan.
  - The plan now requires a file-backed status matrix in `docs/references/architecture-doc-status-matrix.md` and durable recording of independent subagent review evidence.
- Independent review evidence:
  - Doc-role audit task: `ses_28081aa80ffegWObckakBLhjdl`
  - Plan-quality audit task: `ses_28081a992ffeCZnWlGyQ1oh0g6`
  - Follow-up plan audit task: `ses_2807ec709ffeSj26pEDR5a5qJz`
  - Follow-up hierarchy audit task: `ses_2807ec6faffeS0wZzhRheJpt7w`
- Next step: execute Plan 73 by creating the architecture status matrix, rewriting `docs/architecture/README.md`, and reconciling top-level hierarchy/routing docs against the new platform-extension baseline.

### 2026-04-12 (session 10b)

- Executed plan 72 (field binding and renderer contract unification) Phases 2–4; fixed one test bug and cleaned up pre-existing test regressions.
- Links: `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`, `docs/architecture/field-binding-and-renderer-contract.md`
- Changes landed (Phases 2–4):
  - `packages/flux-core/src/constants.ts`: `META_FIELDS` frozen to `{id, className, visible, hidden, disabled, testid}`; `name`, `label`, `title` removed.
  - `packages/flux-core/src/types/schema.ts`: new `BoundFieldSchemaBase` interface (`name: string`, `readOnly?: boolean`, `required?: boolean`).
  - `packages/flux-core/src/types/renderer-compiler.ts`: `CompiledSchemaMeta` and `ResolvedNodeMeta` no longer carry `name`, `label`, `title`.
  - `packages/flux-runtime/src/schema-compiler/fields.ts`: `buildCompiledMeta` switch dropped `name`/`label`/`title` cases.
  - `packages/flux-runtime/src/node-runtime.ts`: `resolveNodeMeta` no longer evaluates `name`/`label`/`title` into meta.
  - `packages/flux-runtime/src/index.test.ts`: added test proving `name` arrives via `propsProgram.value`, not `metaProgram`; fixed `node.meta` → `node.metaProgram` reference bug.
  - `packages/flux-react/src/node-frame-wrapper.tsx`: `labelValue` now reads from `resolvedPropsValue.label`, with fallback to label region and raw schema `label`.
  - `packages/flux-react/src/test-support.tsx`, `schema-renderer-runtime.test.tsx`: removed `meta.label` fallbacks.
  - `packages/flux-renderers-form/src/schemas.ts`: `InputSchema` now extends `BoundFieldSchemaBase`.
  - `packages/flux-renderers-form/src/renderers/composite-schemas.ts`: `ObjectFieldSchema`, `ArrayFieldSchema`, `VariantFieldSchema`, `DetailFieldSchema` extend `BoundFieldSchemaBase`.
  - `packages/flux-renderers-form/src/field-utils.tsx`: `resolveFieldLabelText` / `resolveFieldLabelContent` no longer fall back to `meta.label`.
  - `packages/flux-renderers-form/src/renderers/input.tsx`, `tree-controls.tsx`: removed `meta.label` fallbacks.
  - `packages/flux-renderers-basic/src/schemas.ts`: `ButtonSchema.variant` and `size` aligned with `@nop-chaos/ui` vocabulary (added `destructive`, `ghost`, `link`, `icon-*` sizes; removed `primary`, `md`).
  - `packages/flux-renderers-basic/src/button.tsx`: removed `danger`→`destructive` / `md`→`default` translation layer; props passed through directly.
  - `packages/flux-renderers-data/src/index.test.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`: removed `meta.label` fallbacks.
  - `packages/flux-code-editor/src/code-editor-renderer.tsx`: fixed `meta.label` → `props.props.label`.
  - `apps/playground/src/schemas/workflow-designer-schema.json`: button `variant: "primary"` → `"default"`.
- Key decisions:
  - `name` is now definitively a business prop (flows via `propsProgram`/`props.props.name`), not global meta. Raw schema fallback `props.schema.name` is still allowed as structural read until full adoption is complete.
  - `label` and `title` are renderer-metadata-owned via `value-or-region` field rules; they never go through `CompiledSchemaMeta`.
  - `BoundFieldSchemaBase` is the minimal shared contract for bound form fields: `name`, `readOnly`, `required`. Detail-view (display only, no binding) intentionally does NOT extend it.
  - Button vocabulary is now a strict passthrough to `@nop-chaos/ui`; no dual-table mapping.
  - 5 pre-existing test failures in `composite-form.test.tsx` confirmed as pre-existing on base branch — not introduced by this plan.
- Verification: `pnpm typecheck` ✓ (all 20 packages), `pnpm build` ✓, `pnpm lint` ✓ (no new errors), `pnpm test` ✓ (only 5 pre-existing failures).
- Next step: independent closure audit required before plan 72 can be marked `completed`.

### 2026-04-12 (session 10)

- Tightened the new field-binding normative doc and owner plan after a follow-up review surfaced several ambiguity gaps.
- Links: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`
- Key decisions:
  - Rule 2 now includes an explicit applicability test: any control that writes user edits back to an owner/form scope path is an editable field even if its UI is composite or preview-oriented.
  - Rule 5 now states the target `META_FIELDS` baseline explicitly: `{id, className, visible, hidden, disabled, testid}`; `name` / `label` / `title` are documented as migration targets out of default global meta.
  - Rule 8 now separates the illustrative `NodeControlSchema` shell from validation/lifecycle/extension keys so the example does not imply those concerns are the same semantic layer.
  - Rule 9 now treats `statusPath` and `componentId` as confirmed static structural fields in their documented structural roles instead of leaving them as vague candidates.
  - Plan 72 now explicitly freezes allowed `value` scenarios, calls out the `META_FIELDS` constant update, narrows vocabulary-cleanup scope to live dual-vocabulary drifts, and requires affected renderer tests to move from raw-schema/meta fallback expectations to normalized `props` expectations.
- Next step: if Plan 72 execution starts, use the tighter checklist as the source of truth for compiler/runtime adoption and fixture migration work.

### 2026-04-12 (session 9)

- Added a normative architecture doc and a dedicated owner plan for field binding and renderer-contract convergence.
- Links: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`, `docs/index.md`, `docs/architecture/README.md`
- Key decisions:
  - Ordinary editable fields use `name` as the sole two-way binding entry; generic editable `value` is not a peer contract.
  - Semantic display fields such as `text`, `title`, `options`, and `data` should keep natural names; expressions continue to live on ordinary props rather than `xxxExpr` side channels.
  - `meta` should be limited to node-control and outer-frame concerns; `name` should stop relying on default global meta classification.
  - Shared field-schema convergence should happen through small bases and small helpers, not through a giant abstract renderer layer.
  - Plan 72 is scoped to cross-cutting contract convergence, while component-family-specific implementation work remains separate.
- Next step: if execution starts, migrate compiler classification and wrapper/form-renderer adoption first, then clean up representative non-form raw-schema reads and high-value shadcn-equivalent vocabulary drift.

### 2026-04-12 (session 8)
