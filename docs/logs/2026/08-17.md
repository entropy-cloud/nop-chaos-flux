# Development Log - 2026-08-17

### 2026-08-17

- **AI 控件示例显示样式与操作正确性分析（`docs/analysis/2026-08-17-ai-control-display-analysis.md`）**：
  - 范围：通过 Playwright + headless Chromium 自动化截图 13 个 AI 控件 demo（`ai-widgets` / `ai-chat` / `ai-conversations` / `ai-tools` / `ai-attachments` / `ai-component-handle` / `ai-virtual-scroll` / `ai-persistence` / `ai-citations` / `ai-hitl` / `ai-p4` / `ai-linkage` / `ai-rich-text`），覆盖初始态 + 8 个交互后态
  - 结论：12/13 完全正确；1 个 P1 显示 bug（HITL Approve 按钮初始态文字不可见）、1 个 P3 布局小瑕疵（ComponentHandle 输入框被裁切）、2 个 console 警告（ActionScope namespace 与 `MessageEngine.getState()` 缺 React adapter）
  - **P1 根因**：`apps/playground/src/styles.css` `:root` 未定义 `--success` / `--warning` / `--info`，而 `@theme inline` 把 `bg-success` 映射到 `hsl(var(--success))`；`bg-success text-white` 的按钮因 `hsl(undefined)` 解析失败背景透明 → 白字消失；theme-tokens 的 `data-theme` 选择器在 playground index.html/main.tsx 未挂载也命中不了
  - **P3 根因**：`ai-component-handle-example.json` `ai-chat` 缺 `min-w-0` 导致 flex 子项约束失败
  - **P2 警告**：各 demo 的 `createMessageEngine` 没传 `adapter: createReactMessageAdapter()`（`ai-linkage-demo.tsx:63` 是现成正确范式），与已知 R1-F5 多实例 namespace 警告叠加
  - 修复清单按 P1→P2→P3 排期：1) playground `:root` 补 success/warning/info + 设 `data-theme="classic" data-mode="light"`；2) `createMessageEngine` 调用统一补 adapter；3) `ai-component-handle-demo.json` 补 `min-w-0`
  - 截图原始文件保留在 `/tmp/screenshots/ai/`（13 张 fullPage）+ `/tmp/screenshots/ai-interactive/`（8 张交互后），可作后续视觉回归 baseline
  - Next step：P1 hot-fix 立单 + 把 13 个 demo 的截图接入 playwright snapshot 自动化

- **AI 控件「点击无效果」专项审计（`docs/analysis/2026-08-17-ai-dead-buttons-analysis.md`）**：
  - 范围：用户反馈「很多按钮点击后没有效果」——sub-agent 全量代码 audit + headless Chromium 程序化 click + DOM 前后快照对比 13 个 demo + component-lab C8.2
  - 实证（4 个程序化 click 验证）：
    - `ai-widgets` click prompt "What is the weather?" → input='', msgs=0, toasts=0（**DEAD**）
    - `ai-widgets` click suggestion "Summarize" → 同上（**DEAD**）
    - `ai-citations` click `[1]` → popover 弹出但内含 `https://example.com/design` 假 URL，点了直接 404；`onSourceClick` 未挂（**半通**）
    - `ai-p4` click suggestion → 0 副作用（**DEAD**）
  - **死按钮清单（22+ 个实例，分布在 4 个控件）**：
    - **P1 严重** 13 个：`ai-prompts` 4 卡（`ai-widgets-demo.tsx:60-65` + `ai-p4-example.json`）、`ai-suggestions` 5+2 药丸（同上 2 处）、`ai-citations` 2 上标 + 2 列表项（`ai-citations-example.json`）
    - **P2 中度** 5 个：`ai-voice-input` 1 麦克风按钮（demo 未挂 `onResult`/`onError`）+ `ai-feedback` 5 按钮（在 demo 页面**完全没 mount**）
    - **P3 设计限制**：`ai-chat` 不传播 `ai-sender` 的 `onSubmit`/`onCancel`/`onChange`（`ai-chat.tsx:546-555`）；`ai-welcome` 无任何 click handler（设计上不是按钮）
  - **横向根因（4 类）**：
    1. **模式 A — schema event 字段未挂**（最高频，4 个 P1）：渲染器已写 `events?.onSelect?.()`，但 demo JSON 漏写 `onSelect` action 块；可选链 no-op 让按钮 click 静默；单元测试用 `mockProps` 直构造 props 不走 schema→registry 链路未发现
    2. **模式 B — 浏览器 API 依赖 + demo 未挂 fallback**：`ai-voice-input.tsx:92` 启动瞬间硬判定 `getSpeechRecognitionCtor()`，headless 直接 disabled 仅 hover tooltip；真实浏览器下 transcript 没 host wire 也被丢
    3. **模式 C — demo 页面缺 widget**：`ai-widgets-demo.tsx` 标题为 "AI Widgets Showcase" 意图覆盖全家桶，但 `ai-feedback` 5 按钮完全没 mount
    4. **模式 D — 设计限制**：`ai-chat` 子树 `ai-sender` 的 schema events 不可触达
  - 修复清单：4 个 P1 dead-click 修复合并入下一轮 demo 收口（与 `ai-hitl` 按钮 hotfix 同行）；`ai-voice-input` tooltip 改为默认可见；`ai-widgets-demo` 补 `ai-feedback`；`ai-chat.tsx` 透传 sender events
  - 截图原始文件保留在 `/tmp/screenshots/ai-dead-buttons/`（4 张程序化 click 验证）
  - Next step：P1 fix 立单 + 给 4 个 widget 加 component-lab click → DOM 断言回归测试

- **Plan 461 执行完成（AI demo 视觉 + 死按钮修复，7 Phase 全绿 + closure-audit 通过）**：
  - **Phase 1 playground 主题修复**：`apps/playground/src/styles.css` `:root` 补 `--success: 160 84% 39%` / `--warning: 38 92% 50%` / `--info: 199 89% 48%` + `-bg`；`apps/playground/src/main.tsx` 在 `initFluxI18n` 后调 `document.documentElement.setAttribute('data-theme', 'classic'/'data-mode', 'light')`。验证：HITL Approve 按钮 `bg-success` 解析为 `rgb(16, 183, 127)`，文字 `批准` 显示绿色背景 + 白字
  - **Phase 2 demo JSON wiring**：`apps/playground/src/pages/ai-widgets-demo.tsx` 给 `ai-prompts.onSelect` / `ai-suggestions.onSelect` / `ai-voice-input.onResult` / `ai-voice-input.onError` 补 `showToast` action；`apps/playground/src/ai/ai-p4-example.json` 同步 3 处；`apps/playground/src/ai/ai-citations-example.json` 给两个 `ai-citations` 补 `onSourceClick: showToast`；`apps/playground/src/pages/ai-citations-demo.tsx` 把 `https://example.com/*` 假 URL 改为 `https://github.com/nop-chaos/nop-chaos/blob/main/docs/architecture/ai/*` 真实地址。验证（headless click）：4 类 demo 点击均触发 toast（prompts 1 toast, suggestions 2 toasts, citations 1 toast）
  - **Phase 3 ai-voice-input 徽章**：`packages/flux-renderers-ai/src/renderers/ai-voice-input.tsx:295-308` 在 `unsupported` 时新增 `[data-slot="ai-voice-input-unavailable-badge"]` 默认可见徽章。注：headless Chromium 检测到 `SpeechRecognition` API 存在故 `unsupported=false`，徽章不出现（设计正确）
  - **Phase 4 ai-feedback mount**：`ai-widgets-demo.tsx` `afterMessages` 加 `ai-feedback` 实例（5 个 actions + `onAction: showToast`），`pageData.feedbackMsg` 提供 mock 消息。验证：5 个按钮（复制/重试/赞/踩/来源）正确显示
  - **Phase 5 ai-component-handle 布局**：发现 `direction: "col"` 短形式不被 flex 渲染器接受（schema 要求 `"column"`），demo JSON 误用短形式导致 `flex-direction` 默认 row、ai-chat 被压到右侧窄列。修复：`apps/playground/src/ai/ai-component-handle-example.json` `direction: "col"` → `"column"`、ai-chat `className` 加 `min-w-0 w-full`。验证：输入框宽度 485px（修复前 183px），placeholder 完整可见
  - **Phase 6 useEngineView 假警告修复**：原 `useEngineView` 的 stability guard 在 React adapter 引擎上误报（mutation between getSnapshot calls → cache invalidation → 2 mismatches → warning）。`packages/flux-renderers-ai/src/engine/create-engine.ts` 加 `isAdapterCaching()` 检测 + `engine.hasStableSnapshotAdapter` 字段；`packages/flux-renderers-ai/src/engine/types.ts` 暴露字段；`packages/flux-renderers-ai/src/adapters/use-engine-view.ts` 跳过有 caching adapter 的引擎告警。`packages/flux-renderers-ai/src/adapters/__tests__/use-engine-view.test.ts` 补 2 focused 单测。`packages/flux-core/src/types/compiled-renderer-contract.ts` 补 `frameWrap` 字段（顺手修 sundial plan 460 typecheck 残缺）。验证：dev server 跨 5+ chat demo 路由不再触发 `MessageEngine.getState() returns a new snapshot reference` 警告
  - **mock-ai-env 顺手修复**：`apps/playground/src/ai/mock-ai-env.ts` 改 `notify: () => undefined` 为把 `showToast` 路由到 sonner Toaster（仿 `showcase-env.ts:621-627` 模式）—— Phase 2 的 toast 才能真正显示出来
  - **size-budget 抽取**（commit `50c6bd084`，plan 461 closure-audit 第 1 轮 `revised` 后修复）：`create-engine.ts` 从 721 行降到 697 行（700-line 硬门禁）。抽取 `regenerateTurn` (A-16 message branches) 到 `engine/regenerate.ts` (64 行 + `RegenerateDeps` interface)；抽取 `isAdapterCaching()` + `resolveAdapterCachesSnapshot()` 到 `engine/snapshot-cache-detection.ts` (32 行)。`create-engine.ts` 经 closure bridge（thin pass-through）保留原 `pendingBranchId` + `runTurn` 闭包引用。`pnpm check:oversized-code-files` ERROR 命中从 4 回到 3（pre-existing baseline），plan 461 +0 新增
  - **验证**：flux-renderers-ai 单测 706/706 全绿；playground lint/flux-renderers-ai lint 全绿；typecheck/build 通过（playground 仅 plan 460 残留 23 个错误，与本 plan 无关）；Playwright 程序化 click 验证 4 类 demo + 5 按钮 + 输入框全部生效
  - **closure-audit 两轮**：
    - 第 1 轮（独立子 agent session `ses_feb583221ffexdXAXmUZJfnY99`，fresh session）：verdict `revised`，唯一 Blocker 为 `create-engine.ts` 越过 700-line 硬门禁（committed `52801fe93` 后 +26 行）
    - 修复 commit `50c6bd084` 抽取 regenerate + cache-detection 后
    - 第 2 轮（独立子 agent session `ses_feb43b9a1ffeyH5S6G0644TuPp`，fresh session）：verdict `approved`，所有 7 Phase 已落地，`pnpm check:oversized-code-files` ERROR 命中回到 pre-existing baseline（3 个文件：2 个 exempt locales + wizard-renderer.tsx 716），所有 in-scope live defects 已修复，无静默降级到 deferred/follow-up 的项
  - **Plan 461 已升 `completed`**（详见 `docs/plans/461-ai-demo-display-and-dead-button-fixes-plan.md` `## Closure` 节）
  - **pre-existing 残缺**（不属于 plan 461 debt）：`flux-react` 与 `flux-playground` 有 24 个 typecheck 错误来自 plan 460 sundial 改动（`sundial-mock-backend.test.ts` 等），本 plan 顺手修了 1 个（`CompiledRendererContract.frameWrap` 字段缺失），其余与本 plan 无关，留待 plan 460 收口；`pnpm test` 1 个 `flux-runtime` 单测失败来自 plan 460 残缺；`flux-renderers-basic` lint 1 个 Redundant Boolean 来自 plan 459

- **Plan 457 执行完成（Sundial 复刻页交互补齐，4 Phase 全绿）**：
  - **Phase 1 flex/container onClick 渲染器增强**：`FlexSchema`/`ContainerSchema` 增加 `onClick?: ActionSchema | ActionSchema[]`（`packages/flux-renderers-basic/src/schemas.ts`）；flex/container renderer 渲染 div 挂 `onClick` handler（走 `props.events.onClick`，与 button 同模式，eventCtx 带 scope）；`basic-renderer-definitions.ts` 增加 eventContracts.onClick + `{ key: 'onClick', kind: 'event' }`；可点击时补 `role="button"`/`tabIndex={0}`/`onKeyDown`（Enter/Space 激活，jsx-a11y click-events-have-key-events + no-static-element-interactions 双规则要求；动态 role 三元表达式无法通过静态分析，拆为"可点击/不可点击"双分支渲染）。测试 `__tests__/flex-container-click.test.tsx` 5 例（点击触发 action、未声明时行为不变、键盘激活 + button 语义，先红后绿）。文档：quick-reference.md "Layout Interaction — flex/container onClick" 节、styling-system.md "Interaction on Layout Renderers" 节
  - **Phase 2 按钮/图标类补接线**（C4/C5/C6/C9/C13/C15/C16/C2）：detail 清除日期/清除时间/移到列表/移到垃圾箱/状态行 X（icon-only button `sundial-detail-close`）/子任务 chevron+trash（`sundial-subtask-open-N`/`sundial-subtask-delete-N`）、settings 返回行（`sundial-settings-back`）/保存按钮、workbench sidebar 设置图标（icon-only button `sundial-settings-icon`）→ setValue/showToast
  - **Phase 3 行级交互接线 + 动态选中态**：初始值注入 `page-data.ts`（workbench: `activeSection:'workbench'`/`activeView:'all'`；新增 settings 条目: `mode:'local'`/`activeSection:'sync'`；detail: `recur:'weekly'`/`list:'work'`/`flagged:true`）；C1 主导航 3 行 + 视图 5 行（`setValue` + toast + `visible` 表达式双渲染选中态，双变体带 `data-selected` 属性）；C7 模式卡 3 张双渲染 + 连接信息区 `visible:"${mode !== 'local'}"` 联动（补 testid `sundial-connection-info`）；C8 rail 5 行；C3 详情字段行 4 行（日期行拆"选择区 `sundial-detail-row-date-pick` 开 dialog + 清除按钮独立 flex"防冒泡双触发，`sundial-detail-row-date` 保留外层 wrapper；重复/列表行开对应 dialog；旗标行 `setValue(${!flagged})` 切换 + toast，尾部"已标记/未标记"双渲染）；C11 重复选项 4 行 / C12 列表选项 3 行双渲染 + toast（移除 baked circle-check）；C14 任务行 7 行 toast；C10 todo-dialog 字段行 3 行 toast 占位反馈。JSON 改动用结构化 Node 脚本（解析-修改-写回），每文件 `JSON.parse` 校验
  - **Phase 4 收口验证**：`pnpm typecheck`/`build`/`lint` 37/37 全绿；`pnpm test` 66 task 全绿（basic 494/494、playground 164/164 其中 Sundial 22/22 含 11 个新交互断言）；`pnpm check` 仅既有 2 项登记红（wizard-renderer.tsx 716 行 oversized、industrial event-dispatch-ctx 6 处），无新增命中
  - 测试断言要点：清除按钮无冒泡双触发（点击清除 → toast 且日期 dialog 不打开）；模式卡 Supabase → 连接信息区可见、本地 → 隐藏；主导航/视图/rail/模式卡/选项行 `data-selected` 切换；旗标切换 → /未标记/
  - 文档：plan 457 各 Phase 标 completed（含 Phase 3 执行偏差：C10 无二级 dialog 可开按 Non-Goals 仅 toast、PAGE_DATA 补 `flagged:true` 保持既有 /已标记/ 断言）
  - Next step：独立子 agent closure audit（fresh session）→ plan 457 标 completed

- **Plan 459 修复 controlled dialog X 关闭无效 + 隐藏视觉拖拽 handle（`docs/plans/459-controlled-dialog-x-close-and-drag-handle-fix-plan.md`）**：
  - **问题**：用户报告 dialog 右上角 X 按钮点击后 dialog 不关闭；标题栏 6 圆点（"::"）拖拽 handle 与标题视觉重叠。根因：(1) `use-surface-renderer.ts` `handleSurfaceOpenChange(false)` 在 controlled 模式（`open:"${expr}"`）只 dispatch schema `onClose` hook，不调 `surfaceRuntime.close(id)` —— base-ui X click 语义失效，所有 controlled dialog 受影响；(2) base-ui `<Dialog>` 默认 `draggable=true` 渲染 `GripHorizontalIcon` 装饰 handle，sundial 复刻页标题与其视觉接近
  - **Phase 1 runtime 修复**：`use-surface-renderer.ts` `!nextOpen` 分支在 dispatch onClose 后无条件 `surfaceRuntime.close(id)`；controlled 模式加 `setUserClosed(true)` latch（防止 React effect 因 controlledOpen 仍 true 而 reopen），`effectiveOpen` 并入 `&& !userClosed`；schema `onClose` hook 仍先 fire（CX-10 event ctx 保留）。新增 `__tests__/surface-controlled-x-close.test.tsx` 4 例（controlled X-close 移除 surface / uncontrolled parity / onClose hook dispatch / X-close 后 controlled 表达式 false→true 翻转可重开）；调整 `surface-event-ctx.test.tsx` 1 例（X-close 现在真正卸载 dialog，改 confirm-cancel 路径验证 ${surfaceId}）。回归全绿：basic 498/498、react 481/481、runtime 1422/1422、form 809/809、playground 164/164
  - **closure-audit 三轮**：Round 1（`ses_ff0308634ffe6GSQWWkd8unhJC`）verdict `revised`，发现 Blocker——`setUserClosed(false)` 原位于 `handleSurfaceOpenChange(true)` 分支但该函数无 caller 以 true 调用（base-ui 只上报 close），X-close 后 controlled dialog 无法重开（"关一次就再也打不开"比原 bug 更严重）。修复：移除死代码，改为 effect 监测 controlledOpen false→true 翻转时清除 latch（canonical reopen 路径）；补 reopen 回归测试。Round 2（`ses_ff02088efffeqBhxNvMgim2nCN`）verdict `revised`，Blocker 功能修复确认落地，唯一剩余 `!Boolean(prev)` lint 红灯。Round 3（`ses_ff01c81f3ffeI8zrGcwvn7qoLR`）verdict `approved`：`!prev` 修复后 lint/typecheck 37/37、basic 498/498、全仓 66/66 tasks 全绿、pnpm check 零新增红；e2e spec 12 it 全部基于 HEAD base 元素
  - **Phase 2 CSS**：`apps/playground/src/sundial-replica/sundial-replica.css` `.sd-dialog [data-slot='dialog-drag-handle'] { display: none; }` —— 拖拽功能保留（drag surface 是 dialog header 整体，见 `use-dialog-drag.ts:167`），仅隐藏装饰 handle
  - **Phase 3 e2e + 文档**：`tests/e2e/sundial-replica-visual.spec.ts` 重写为 HEAD base 元素（plan 458 改动已整体撤销，spec 不再引用其特有 testid）+ 2 个 plan 459 新测试（X-close 关闭 date picker dialog / drag-handle hidden）；12/12 e2e 全绿（26s）。`docs/references/quick-reference.md` 新增 "Declarative dialog X-close semantics (plan 459)" 节
  - **状态注记**：plan 458 的所有 schema 改动（5 个 sundial JSON + mock backend + page-data + test 扩展）因存在 nested-dialog scope/portal 缺陷（picker 选项 closeSurface 触发外层 dialog 一并关闭）已整体 `git checkout HEAD -- .` 撤销，回到 plan 457 末尾基线（22 unit tests + 12 e2e visual tests 全绿）。plan 458 文件保留为 executing/需重做状态，nested-picker 缺陷成为后续重做时的已知约束
  - **Next step**：plan 459 closure audit（独立子 agent fresh session）→ 标 completed

- **Plan 460 执行（Sundial 占位交互全量实现，Phase 1-4 完成 + runtime 缺陷修复）**：
  - **runtime 缺陷（编译侧 action-class args 急切求值）**：openDialog 的 `onSubmitSuccess` 内 `${$formData.x}` 模板在 openDialog 执行时对 dispatch scope 求值抛错（$formData 是 hook dispatch 时才注入的绑定）→ 整个 openDialog 失败。根因：C8.3 P1-2 的 `preserveSchemaArgs` 只静态化了 schema 输入（body），漏了 fieldRules `kind:'action'` 的字段。修复：按 `BUILT_IN_ACTION_DEFINITIONS.fieldRules` 把 action/event 类字段一并 `__nopPreserveLiteral` 静态化（`flux-compiler/action-compiler.ts`），嵌套模板延迟到 hook dispatch 时在 owner ctx 求值。回归测试 `flux-runtime/src/__tests__/surface-hook-formdata-binding.test.ts`
  - **todo-dialog picker 真实化（架构重做，弃声明式嵌套 dialog）**：日期/列表字段行 → `openDialog` action（form + radio-group + submitForm，`submitScope:'surface'`，`onSubmitSuccess` 在 owner scope 回写 `setValue(todoDate, ${$formData.todoDate})` + `closeOnSubmit`）；旗标行直接 toggle。**外层 dialog 补 `closeOnOutsideClick:false`**——picker 的点击对外层是 outside press，此前连带关闭外层（plan 458 撤销的嵌套 dialog 缺陷真正根因）
  - **workbench 视图切换**：5 collapse 按 activeView visible 切换（all/today/scheduled 显示对应分组），completed/trash 独立 panel；任务行 7 行 → `setValue(taskDetailOpen,true)` 打开任务详情 dialog（4 字段行 + 移动按钮）
  - **mock backend**：SundialTask 10 条（done/trashed/recur/list）+ `Sundial__todos?view=` 过滤 + `Sundial__updateTodoItem` + `Sundial__subtasks`；`__tests__/sundial-mock-backend.test.ts` 4 例
  - **验证**：compiler 551 / action-core 210 / runtime 1423 / basic 499 / react 481 / playground 169（sundial-replica 23/23 含 P1-P3 owner-scope 回写断言）全绿

- **Plan 460 B6 收口（detail 日期/时间/重复/列表 picker 真实化 + 写回修正，sundial 测试拆文件）**：
  - **date picker 真实日历**：detail 日期 dialog 改为真实 `input-date`（year/month 导航 dropdown caption），时间用 `input-time` steppers 模式（schema 修正：误写 `mode:"steppers"` → 正确字段 `steppers:true`，`InputTimeSchema` 无 mode 字段）；旧手搓静态 8 月月历网格（`sundial-cal-day-*`）删除
  - **confirm 写回链修复**：`onSubmitSuccess` 里 `setValue(pickedDateLabel)` 的 value 原为 `${$formData.pickedDateLabel}`，但表单体没有 pickedDateLabel 字段 → `$formData.pickedDateLabel` 永远是初始值 `8/18`，写回无效。修复：value 改为 `${ $Date.month($formData.pickedDate) }/${ $Date.day($formData.pickedDate) }`（模板分段拼接）。**踩坑**：`String($Date.month(...))` 包装失败——表达式求值器不注册 `String` 内置（`Call target is not a function`，`$Math.round(3.7)` bare 调用正常）；IIFE 也不支持（字面量透传）；仅支持命名空间成员调用 + 模板分段拼接。用 `/tmp/expr-test.test.ts` 隔离复现验证（已删除）
  - **测试环境 vs 浏览器差异**：react-day-picker day 按钮在 Playwright `click()` 下静默失效（`clicks: 0`，elementFromPoint 命中按钮本身无遮挡），但原生 `dispatchEvent(MouseEvent click)` 有效（input 值 08-18→08-20）——jsdom 的 `fireEvent.click` 等价原生派发故测试可用；steppers 模式 `baseTestId` 走 schema testid（`sundial-time-stepper-*`），display 是 `${baseTestId}display`，测试改为 `[data-testid="sundial-time-stepper-display"], [data-testid="time-display"]` 双选择器兜底
  - **C13 time clear toast**：删掉 toast 文本断言（playground 无 Toaster 挂载，`findByText('时间已清除')` 必然超时——plan 457 闭包时此断言未真正生效过）；改为断言点击 clear-time 后关闭 dialog 正常
  - **超限拆分**：sundial-replica.test.tsx 807 行超 700 上限（pnpm check 新增红）→ detail inspector describe（335 行）拆出到 `sundial-detail-replica.test.tsx`（347 行），主文件回 220 行，零新增红（仅既有 wizard-renderer.tsx:716）
  - **验证**：playground 164/164（sundial-replica 13 + sundial-detail-replica 9 = 22/22）；e2e sundial-replica-visual 20/20；typecheck/lint/build 全绿；pnpm check 零新增红；`pnpm test` 全量仅 1 个预存失败（flux-renderers-basic surface-event-ctx，HEAD 即失败）
