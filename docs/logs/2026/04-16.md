# Development Log — 2026-04-16

### 2026-04-16 (PM23)

- **Plan 97 Workstream 3 Completed**: Routing Authority And Dialog Naming Doc Closure
  - Updated `docs/index.md`:
    - Added "Routing Authority" section explicitly declaring it as the authoritative docs navigation baseline
    - Clarified that `AGENTS.md` contains a deliberately reduced operational subset
    - Defined conflict resolution: update `docs/index.md` first, then sync necessary subset to `AGENTS.md`
  - Updated `AGENTS.md`:
    - Reduced "By Task" routing table from 22 to 11 entries (removed specialized/duplicate entries covered by `docs/index.md`)
    - Reduced "By Code Location" table from 12 to 8 entries
    - Added explicit header stating `docs/index.md` is the authoritative baseline
  - Updated `docs/architecture/action-scope-and-imports.md`:
    - Added "Schema authoring preference" section after built-in action list
    - Stated new schema should prefer `openDialog` over `dialog`
    - Documented that both names resolve to same runtime behavior (naming convention, not functional difference)
  - Audit items D-08 and D-09 marked as resolved in `docs/analysis/2026-04-16-comprehensive-project-audit.md`
- Reference: `docs/plans/97-comprehensive-audit-remediation-owner-plan.md` Workstream 3
- Next step: Continue Plan 97 Workstream 5 (Code-Editor Type Narrowing And Renderer Cast Cleanup)

### 2026-04-16 (PM22)

- **Plan 97 Workstream 2 Completed**: Active Owner Doc Size Closure For Form Validation
  - Created `docs/references/form-validation-runtime-types.md`:
    - Complete TypeScript type definitions for `ValidationScopeRuntime`, `FormRuntime`, `CompiledValidationModel`
    - Detailed state structures: `FieldRegistrationState`, `FieldValidationStateSnapshot`, `FormStoreState`
    - Per-path subscription API (`FormStoreApi`) with semantics and guarantees
    - React hook integration example
  - Simplified `docs/architecture/form-validation.md`:
    - Runtime Model section now uses summary table + concept descriptions instead of full interfaces
    - Layered State Model section condensed to key concepts with link to reference doc
    - File size reduced from 52 KB to 43 KB (below 50 KB guidance)
  - Updated `docs/index.md`:
    - Added routing entry for `form-validation-runtime-types.md`
    - Updated form-validation entry to include reference doc link
  - Audit item D-07 marked as resolved in `docs/analysis/2026-04-16-comprehensive-project-audit.md`
- Reference: `docs/plans/97-comprehensive-audit-remediation-owner-plan.md` Workstream 2
- Next step: Continue Plan 97 Workstream 3 (Routing Authority And Dialog Naming)

### 2026-04-16 (PM21)

- **Plan 97 Workstream 1 Completed**: Word Editor Owner Docs
  - Created `docs/architecture/word-editor/design.md`:
    - Architecture overview covering two-package pattern (core + renderers)
    - Package responsibilities table for both `word-editor-core` and `word-editor-renderers`
    - Data flow diagram showing store/bridge relationships
    - Document model structure (header/main/footer zones)
    - Template expression system (XLang syntax: `${expr}`, `<c:for>`, `<c:if>`)
    - Integration points with Flux Runtime and nop-entropy backend
  - Created `docs/components/word-editor-page/design.md`:
    - Component contract following report-designer-page pattern
    - Schema design with `onBack`, `onSave`, `initialDocument`, `datasets` fields
    - Field classification table (value-or-region, event, region)
    - Runtime state ownership (editor-store, dataset-store, CanvasEditorBridge)
    - Namespaced actions (`word-editor:save`, `word-editor:insertField`, etc.)
  - Updated `docs/index.md`:
    - Added routing entry for Word Editor architecture
    - Added routing entry for word-editor-page component contract
    - Added `docs/architecture/word-editor/design.md` to Active Source Of Truth
    - Added `docs/components/word-editor-page/design.md` to Component contract intent
  - Audit item D-06 marked as resolved in `docs/analysis/2026-04-16-comprehensive-project-audit.md`
- Reference: `docs/plans/97-comprehensive-audit-remediation-owner-plan.md` Workstream 1
- Next step: Continue Plan 97 Workstream 2 (Form Validation Doc Size Closure)

### 2026-04-16 (PM20)

- **Plan 97 Workstream 4 Completed**: Word Editor Logs and Code-Editor Resolver Error Handling
  - Deleted 3 production `console.log` calls from `WordEditorPage.tsx` (lines 55, 131, 135)
  - Extracted shared `useAsyncApiResolver<T>` helper in `source-resolvers.ts`:
    - Handles abort, error reporting via `console.warn`, and returns `{ items, error, loading }`
    - Uses `queueMicrotask` for loading state updates to comply with React 19's strict effect rules
    - Replaces 4 near-identical async resolver implementations
  - All type escapes removed from `source-resolvers.ts`: action properly typed as `ActionSchema`, no more `as any` or untyped reducer patterns
  - Audit items closed: C-01, C-02, C-03, C-04, C-06, C-11
- Audit document updated: `docs/analysis/2026-04-16-comprehensive-project-audit.md` — marked C-01 through C-04, C-06, and C-11 as resolved with strikethrough
- Verification: `pnpm typecheck && pnpm build && pnpm lint` all pass
- Reference: `docs/plans/97-comprehensive-audit-remediation-owner-plan.md` Workstream 4
- Next step: Continue Plan 97 Workstream 5 (Code-Editor Type Narrowing) and remaining workstreams

### 2026-04-16 (PM19)

- **Plan 96 Completed**: Final Architecture Doc-Code Closure Plan (Master Plan)
  - All three successor plans completed and marked `completed`:
    - **Plan 100**: CompiledSchemaNode Public Boundary Closure — confirmed `@internal` annotation is correct, no public/runtime leakage
    - **Plan 98**: Data Source Publication and Dependency Declaration Closure — confirmed code and docs are consistent, `name`-first publication is normative
    - **Plan 99**: Flow Designer Capability Facade Closure — re-evaluated 31 "migration" targets as correct owner-internal behavior, updated docs to remove "implementation debt" wording
  - Key insight: All three plans concluded **zero code changes required** — existing implementation was already correct, plans clarified boundaries and documentation
  - Two independent closure audits completed for master plan:
    - **Accuracy Audit** (Session `ses_26aecbfc3ffezGY7JciUbYldUr`): PASS — verified successor completion, documentation consistency, code spot-checks
    - **Closure Quality Audit** (Session `ses_26aeca2dfffegU4ldMrIMHPrv1`): PASS — no overengineering, closure discipline maintained, no plan drift
  - Architecture boundary clarification: Schema-facing external consumers use `designer:*` action strings via ActionScope; owner-internal components (Inspector, Palette, Canvas) use direct `dispatch(command)` — both are correct by design
- Documentation updates: `docs/architecture/flux-core.md` (CompiledSchemaNode boundary table), `docs/architecture/action-scope-and-imports.md` (Flow Designer owner-internal clarification)
- Reference: `docs/plans/96-final-architecture-doc-code-closure-plan.md`, `docs/plans/100-compiledschemanode-public-boundary-closure-plan.md`, `docs/plans/98-data-source-publication-and-dependency-declaration-closure-plan.md`, `docs/plans/99-flow-designer-capability-facade-closure-plan.md`
- Next step: Execute Plan 97 (Comprehensive Audit Remediation) — 12 workstreams covering test coverage, type escape remediation, oversized file analysis, and doc maintenance

### 2026-04-16 (PM18)

- Added `docs/plans/97-comprehensive-audit-remediation-owner-plan.md` to own the currently valid findings in `docs/analysis/2026-04-16-comprehensive-project-audit.md`.
- Key decision: freeze the plan to the already re-verified 2026-04-16 audit baseline instead of letting the owner plan expand with newly discovered or concurrently introduced repo issues.
- Independent review pass 1 rejected the first draft as too broad and too subjective around closure, especially for oversized-file scope, doc-routing ownership, and coverage-policy wording.
- Independent review pass 2 rejected the revised draft on two concrete blockers: the frozen oversized-file baseline was not explicitly part of `Audit Mapping`, and T-03 could still be closed by policy wording rather than a committed additional threshold gate.
- Independent review pass 3 rejected the near-final draft on one remaining blocker: `C-06` (`packages/flux-code-editor/src/source-resolvers.ts` type escapes) was mapped but not explicitly owned by any execution slice.
- Final reconciliation updates: added the frozen oversized-file baseline to `Audit Mapping`, made T-03 require a committed additional package-level threshold gate, and explicitly assigned `source-resolvers.ts` type-escape cleanup to Workstream 4.
- Final independent subagent verdicts: `ses_26b24a816ffeKGkvUeOlgsHd1r` -> `No blocker; approve.`; `ses_26b24a761ffedBw3bF1qXjXNR4` -> `No blocker; approve`.
- Next step: execute Plan 97 in owner-aligned slices and keep `docs/analysis/2026-04-16-comprehensive-project-audit.md` synchronized as each cluster lands.

### 2026-04-16 (PM17)

- **Article Enhancement Round 2**: Read `c:/can/nop/nop-entropy/docs/theory/essence-of-react.md` and evaluated its insights for improving `docs/articles/flux-programming-model-explained.md`.
- Multi-perspective review process:
  1. **Pragmatist reviewer**: Argued existing article is sufficient, no need for more React theory
  2. **Theory reviewer**: Found "three trees' different growth patterns" and "Hooks top-level call constraint" as valuable insights
  3. **Implementation reviewer**: Verified insights match Flux implementation, identified gaps in explaining "how three trees connect"
  4. **Final decision**: Small enhancement — add one focused Hooks comparison paragraph
- New content added in Section 5 "Lexical Ownership": explains why Flux doesn't need React Hooks' top-level-only constraint — because DSL schema encodes dependencies explicitly in structure, not implicitly via call order.
- Technical accuracy fix: Initial draft incorrectly claimed "compile-time dependency analysis"; corrected after subagent review revealed Flux uses **runtime** dependency tracking (like Vue/MobX), not static analysis.
- Key insight preserved: "Declarative schema structure is explicit, JavaScript function body structure is implicit" — this is the core DSL vs general-language advantage.
- Reference: `docs/articles/flux-programming-model-explained.md` (now ~355 lines)
- Source material: `c:/can/nop/nop-entropy/docs/theory/essence-of-react.md`

### 2026-04-16 (PM16)

- Updated owner architecture docs to remove stale transition wording where the main code path is already landed and to keep only the real remaining compatibility gaps: `docs/architecture/flux-core.md`, `docs/architecture/dependency-tracking.md`, `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`.
- Key decision: owner docs now describe the current baseline directly for the template-instance render path, `cid` live identity, root-normalized dependency tracking, and `designer:*` action-scope availability, while still documenting unresolved closure areas precisely: `CompiledSchemaNode` tooling/public residue, `data-source` compatibility publication lanes, runtime dependency fallback/diagnostics, and Flow Designer mixed schema-facing versus owner-internal command paths.
- Added a new closure-routing plan plus focused successor owner plans: `docs/plans/96-final-architecture-doc-code-closure-plan.md`, `docs/plans/100-compiledschemanode-public-boundary-closure-plan.md`, `docs/plans/98-data-source-publication-and-dependency-declaration-closure-plan.md`, `docs/plans/99-flow-designer-capability-facade-closure-plan.md`.
- Independent audits drove two follow-up fixes immediately:
- corrected `docs/architecture/api-data-source.md` so it no longer overstates unnamed API-backed source publication behavior
- narrowed Plan 96 from a broad implementation umbrella into a master closure-routing plan, with 100/98/99 now owning the three distinct implementation surfaces
- Plan 96 now exists mainly to enforce reverse doc updates and independent closure-audit evidence across the successor plans, which keeps the overall closure work balanced without hiding unrelated leftovers under one file.
- Link to docs: `docs/plans/96-final-architecture-doc-code-closure-plan.md`, `docs/plans/100-compiledschemanode-public-boundary-closure-plan.md`, `docs/plans/98-data-source-publication-and-dependency-declaration-closure-plan.md`, `docs/plans/99-flow-designer-capability-facade-closure-plan.md`, `docs/analysis/2026-04-16-architecture-transition-closure-review.md`
- Next step: rerun the plan-quality audit against the narrowed plan structure, then finish with a concise closure summary for the user.

### 2026-04-16 (PM15)

- Re-audited `docs/analysis/2026-04-16-comprehensive-project-audit.md` against the live repo and rewrote the report so it now distinguishes real defects from stale or overstated findings.
- Updated doc owners and maintenance rules: `docs/architecture/styling-system.md`, `docs/architecture/action-scope-and-imports.md`, `docs/index.md`, `docs/logs/index.md`, `docs/references/maintenance-checklist.md`.
- Key decision: oversized append-only history files under `docs/logs/`, `docs/analysis/`, and similar historical areas are not audit defects by themselves; size-based cleanup should target active owner docs and repo-enforced oversized code-file checks instead.
- Verification/context: used live file reads plus the existing repo guardrail `node scripts/check-oversized-code-files.mjs` to separate current code hotspots from already-cleared or misclassified items.
- Next step: if a follow-up cleanup starts, prioritize `WordEditorPage` debug logs, `flux-code-editor` silent catch blocks, `word-editor` owner docs, and the remaining oversized production code files identified by the repo check.

### 2026-04-16 (PM14)

- **Article Enhancement**: Enriched `docs/articles/flux-programming-model-explained.md` with insights from nop-entropy AMIS theory documents (`why-amis-is-good.md`, `amis-and-declarative-programming.md`).
- New content added:
  1. **Declarative programming evolution** (Section 1) — Explains Flux as the "endpoint" of declarative paradigm shift, introduces `output = FluxPage(schema, input)` formula
  2. **Resource as async computed extension** (Section 2) — Clarifies the evolutionary path: reactive binding → computed → Resource
  3. **Value/function duality** (Section 3) — Explains why Value/Resource/Reaction must be distinguished using the "value as zero-arg function" perspective
  4. **Three-tree origin** (Section 6) — Traces the design back to OO GUI framework reflection
  5. **ActionTree function resolution analogy** (Section 6) — Explains `xui:imports` as dynamic function namespace injection
- Also added Resource's complete semantics (lifecycle ownership, status semantics, refresh/invalidation) to prevent oversimplification of the "async computed" analogy.
- Third review by subagent confirmed no P0 issues, all code references accurate.
- Reference: `docs/articles/flux-programming-model-explained.md` (now 345 lines)
- Source material: `c:/can/nop/nop-entropy/docs/theory/amis/why-amis-is-good.md`, `c:/can/nop/nop-entropy/docs/theory/amis/amis-and-declarative-programming.md`

### 2026-04-16 (PM13)

- **New Article**: Created `docs/articles/flux-programming-model-explained.md` — an explanatory article helping readers (including AI) build the correct mental model for understanding Flux.
- Key message: Flux is a "Final Execution Schema frontend runtime", not a traditional low-code framework. The seven primitives (Base Tree, ScopeRef, Value, Resource, Reaction, Capability, Host Projection) form a semantically complete minimal set, not a functional classification.
- Article positioning: Complements `flux-design-introduction.md` (design philosophy) by focusing on mental model correction and conceptual clarity before reading normative architecture docs.
- Two-round review process: Initial draft reviewed by subagent, revised based on feedback (P0: fixed outdated `read()` API reference, P1: added terminology mapping for "three trees", P2: added document navigation and clarified Algebraic Effects analogy), second review confirmed all issues resolved.
- Code paths verified: All referenced paths (`flux-formula/src/compile.ts`, `flux-runtime/src/scope.ts`, `flux-runtime/src/action-scope.ts`, `flux-runtime/src/action-runtime.ts`) confirmed accurate.
- Reference: `docs/articles/flux-programming-model-explained.md`
- Next step: Consider adding entry to `docs/index.md` and cross-linking from `docs/architecture/frontend-programming-model.md`.

### 2026-04-16 (PM12)

- Hardened `array-editor` and `key-value` hidden row identity generation so add/remove flows no longer synthesize duplicate client ids when existing ids are sparse or externally supplied.
- Code paths: `packages/flux-renderers-form/src/renderers/array-editor.tsx`, `packages/flux-renderers-form/src/renderers/key-value.tsx`, `packages/flux-renderers-form/src/renderers/composite-item-id.ts`, `packages/flux-renderers-form/src/__tests__/form-array-validation.test.tsx`
- Key decision: keep the existing `[{ id, ... }]` value model, but generate new ids with collision-free monotonic allocation and use index-based local filtering during remove so duplicate ids cannot accidentally wipe multiple rows in renderer-local state.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/form-array-validation.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/bug-dual-state.test.tsx src/__tests__/form-double-edit-regression.test.tsx`
- Next step: rerun repo-level `pnpm lint`, `pnpm typecheck`, and `pnpm build`; `pnpm check` is still expected to be blocked only by the pre-existing oversized tracked files unless a new oversized regression appears.

### 2026-04-16 (PM11)

- Added a transition-closure review to distinguish architecture transitions that are already landed from those still incomplete: `docs/analysis/2026-04-16-architecture-transition-closure-review.md`.
- Key decision: stop describing already-landed runtime changes as vague future convergence. The new analysis treats Scope API closure, TemplateNode/NodeInstance render path, `cid` live identity, root-normalized dependency tracking, and Flow Designer `designer:*` namespace registration as current baseline, while keeping `CompiledSchemaNode` public residue, `dataPath` / `mergeToScope`, runtime dependency fallback, and Flow Designer direct `core.*` shell calls as remaining closure work.
- Updated `docs/analysis/2026-04-16-architecture-doc-consistency-audit.md` to mark it superseded as a transition-status source, because it was too optimistic about closure completeness.
- Link to docs: `docs/analysis/2026-04-16-architecture-transition-closure-review.md`, `docs/analysis/2026-04-16-architecture-doc-consistency-audit.md`
- Next step: update owner docs such as `docs/architecture/flux-core.md`, `docs/architecture/dependency-tracking.md`, `docs/architecture/api-data-source.md`, and `docs/architecture/action-scope-and-imports.md` so already-landed transitions use present-tense contracts and only real remaining gaps stay documented as closure work.

### 2026-04-16 (PM10)

- Continued the detail renderer duplication cleanup by extracting shared draft body/footer helpers into `packages/flux-renderers-form/src/renderers/detail-surface.tsx`.
- `detail-field` and `detail-view` now share the draft `FormContext`/`ScopeContext` wrapper and confirm/cancel footer chrome, while keeping their separate open/commit behavior and field/view-specific slots.
- Updated code paths: `packages/flux-renderers-form/src/renderers/detail-surface.tsx`, `packages/flux-renderers-form/src/renderers/detail-field.tsx`, `packages/flux-renderers-form/src/renderers/detail-view.tsx`.
- Key decision: stop extraction at the shared draft UI shell; keep commit/writeback orchestration local because `detail-field` and `detail-view` still own materially different value adaptation and parent write semantics.
- Verification: detail renderer tests passed (`44 passed`), and workspace `pnpm typecheck`, `pnpm build`, `pnpm lint` all passed.

### 2026-04-16 (PM9)

- Refactored composite field renderer duplication into shared helpers under `packages/flux-renderers-form/src/renderers/`.
- Added `projected-form-runtime.ts` to centralize projected `FormStoreApi` / `FormRuntime` proxy creation for `object-field`, `array-field`, and `variant-field`, replacing repeated path-prefix proxy logic.
- Added `detail-surface.tsx` to centralize shared dialog/drawer shell rendering used by `detail-field` and `detail-view`, while preserving their distinct body/error slots.
- Updated code paths: `packages/flux-renderers-form/src/renderers/object-field.tsx`, `packages/flux-renderers-form/src/renderers/array-field-runtime.ts`, `packages/flux-renderers-form/src/renderers/variant-field-runtime.ts`, `packages/flux-renderers-form/src/renderers/detail-field.tsx`, `packages/flux-renderers-form/src/renderers/detail-view.tsx`.
- Key decision: keep the projected form proxy architecture, but collapse the repeated implementation into shared helpers rather than pushing path-prefix mapping back into each renderer/hook call site.
- Verification: changed renderer tests passed (`90 passed`), and workspace `pnpm typecheck`, `pnpm build`, `pnpm lint` all passed.

### 2026-04-16 (PM8)

- Hardened `array-field` object-item identity so object arrays no longer rely on positional continuity when a stable key is available.
- Code paths: `packages/flux-renderers-form/src/renderers/array-field.tsx`, `packages/flux-renderers-form/src/renderers/array-field-runtime.ts`, `packages/flux-renderers-form/src/renderers/composite-schemas.ts`, `packages/flux-renderers-form/src/renderers/array-field.test.tsx`
- Docs updated: `docs/skills/react19-best-practices-review.md`, `docs/architecture/unified-runtime-indexing-and-path-binding.md`
- Key decision: object arrays now resolve runtime identity from `schema.itemKey -> __rowKey -> id -> legacy-index`, pass item-level `instancePath` into child regions, and fall back to compatibility index identity only for missing or conflicting keys.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form test -- array-field.test.tsx`
- Next step: confirm repo-level `pnpm lint`, `pnpm build`, and `pnpm check` status after the identity fix.

### 2026-04-16 (PM7)

- **Architecture Documentation Consistency Audit Executed**:
  - Completed L1-L4 documentation review and code verification
  - Generated audit report: `docs/analysis/2026-04-16-architecture-doc-consistency-audit.md`
  - Overall consistency score: 98%

- **Dependency Tracking Convergence Confirmed**:
  - Updated `docs/architecture/dependency-tracking.md` to mark Phase 1-3 as completed
  - Phase 1: Self-write guard (`filterScopeChangeByIgnoredRoots`) correctly filters self roots
  - Phase 2: Root-only runtime tracking via `normalizeRootPath()` in `flux-formula/src/scope.ts`
  - Phase 3: `dependsOn` on `DataSourceSchema`/`ReactionSchema` is authoritative; `reaction-runtime.ts` uses explicit roots first

- **Documentation Improvements**:
  - Added Related Documents section to `docs/architecture/form-validation.md`
  - Cross-references: `field-binding-and-renderer-contract.md`, `field-metadata-slot-modeling.md`, `flux-runtime-module-boundaries.md`, `scope-ownership-and-isolation.md`, `renderer-runtime.md`, `dependency-tracking.md`

- **Type Error Fix**:
  - Fixed `tests/e2e/report-designer-demo.spec.ts` null-assertion errors (lines 68-82, 105)
  - TypeScript couldn't infer non-null after early return; added `!` assertions

- Verification: `pnpm typecheck` passes
- Reference: `docs/analysis/2026-04-16-architecture-doc-consistency-audit.md`

### 2026-04-16 (PM6)

- **Plan 95 Executed**: Form Field Controller Options Extension and Duplicate Hook Elimination
  - Extended `useFormFieldController` options to support `disabled`, `required`, `readOnly` parameters
  - Passed options through to internal `useFieldPresentation` call, eliminating need for duplicate calls
  
- **Changes Made**:
  - `packages/flux-renderers-form/src/field-utils.tsx`: Extended `useFormFieldController` signature
  - `packages/flux-renderers-form/src/renderers/input.tsx`: Updated 9 components (InputRenderer, SelectRenderer, TextareaRenderer, CheckboxRenderer, SwitchRenderer, RadioGroupRenderer, CheckboxGroupRenderer) - removed duplicate `useFieldPresentation` calls
  - `packages/flux-renderers-form/src/renderers/tree-controls.tsx`: Updated 2 components (InputTreeRenderer, TreeSelectRenderer)
  - Complex Field Renderers updated to use `useFormFieldController` with full destructuring:
    - `tag-list.tsx`: Now uses `{ currentForm, scope, presentation }` from single hook
    - `key-value.tsx`: Same pattern, updated `KeyValueRow` prop type to `FormRuntime | undefined`
    - `array-editor.tsx`: Same pattern, updated `ArrayEditorRow` prop type
    - `condition-builder/ConditionBuilder.tsx`: Same pattern

- **Design Decision** (Phase 4):
  - Complex Field Renderers **can** use `useFormFieldController` because the hook returns both `currentForm` and `scope`
  - This eliminates the need for separate `useRenderScope()` + `useCurrentForm()` + `useFieldPresentation()` calls
  - Unified pattern: all field renderers now use `useFormFieldController` as single entry point

- **Before/After Pattern**:
  ```typescript
  // Before (duplicate hooks):
  const { value, handlers, currentForm } = useFormFieldController(name);
  const presentation = useFieldPresentation(name, currentForm, {
    disabled: props.meta.disabled,
    required: Boolean(props.props.required)
  });
  
  // After (single hook):
  const { value, handlers, presentation } = useFormFieldController(name, {
    disabled: props.meta.disabled,
    required: Boolean(props.props.required)
  });
  ```

- **Verification**: `pnpm typecheck`, `pnpm build` pass. `flux-renderers-form` tests: 409 passed.
- Reference: `docs/plans/95-form-field-controller-options-extension-and-duplicate-hook-elimination-plan.md`

### 2026-04-16 (PM5)

- **Plan 93 Executed**: Form Renderer Schema Fallback Elimination (Plan 72 Follow-up)
  - Reactivated Plan 93 as successor to Plan 72, per Plan 72's explicit Follow-up guidance:
    > "If the transitional `props.props.name ?? props.schema.name` fallback is to be eliminated, open a dedicated successor plan for full renderer adoption of the normalized channel."
  
- **Changes Made**:
  - Eliminated **13 `props.schema.name` fallbacks** from 6 files:
    - `input.tsx`: 7 occurrences → `String(props.props.name ?? '')`
    - `tree-controls.tsx`: 2 occurrences
    - `ConditionBuilder.tsx`: 1 occurrence
    - `tag-list.tsx`: 1 occurrence
    - `key-value.tsx`: 1 occurrence
    - `array-editor.tsx`: 1 occurrence
  - Eliminated **13 `props.schema.required` fallbacks** from same 6 files → `Boolean(props.props.required)`

- **Architecture Alignment**:
  - Per `docs/architecture/field-binding-and-renderer-contract.md` Frozen Contract Matrix:
    - `name` → `props.name` (removed from META_FIELDS)
    - `required` → `props.required` (field-level validation semantics)
  - All form renderers now use normalized channels only, no raw schema fallbacks

- **Verification**: `pnpm typecheck`, `pnpm build`, `pnpm lint` pass. `flux-renderers-form` tests: 409 passed.
- Reference: `docs/plans/93-form-renderer-duplication-elimination-and-field-controller-unification-plan.md`, `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`

### 2026-04-16 (PM4)

- **Architecture Document Deep Audit** - Re-evaluated Plan 93 and Plan 94 against authoritative architecture docs:

- **Plan 93 Cancelled** - `useFormFieldController` hook already exists at `packages/flux-renderers-form/src/field-utils.tsx:144-163`:
  - Discovered through codebase search that hook is already implemented and used by `input.tsx` (7 components) and `tree-controls.tsx` (2 components)
  - Analyzed `docs/architecture/field-binding-and-renderer-contract.md` to understand `name` position specification
  - Key finding: `name` belongs in `props.props.name` (Frozen Contract Matrix Line 349), removed from `META_FIELDS`, classified as `prop` by renderer metadata
  - The `props.schema.name` fallback pattern is **transitional**, not normative — some renderers lacking this fallback are actually closer to the final spec
  - Remaining migration work (making other renderers use existing hook) belongs to Plan 72 follow-up, not a new plan

- **Plan 94 Updated** - Added architecture alignment check:
  - Cross-referenced with `docs/architecture/report-designer/contracts.md` and `docs/architecture/report-designer/design.md`
  - Confirmed current switch implementation **already complies** with Bridge/Projection architecture (all writes go through `SpreadsheetBridge.dispatch(command)`)
  - Added "Architecture Preservation Checklist" to ensure refactoring maintains these boundaries
  - Plan is valid as code quality improvement, not architecture violation fix

- **Key Architecture Insights Documented**:
  - `META_FIELDS` is frozen to `{id, className, visible, hidden, disabled, testid}` per `packages/flux-core/src/constants.ts`
  - Designer families (Flow, Spreadsheet, Report) use Projection pattern: schema reads only-read snapshots via host scope, writes via namespaced actions
  - `field-binding-and-renderer-contract.md` Rule 7: "renderer metadata owns field classification" — `name` enters `props` channel, not `meta`

- Reference: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/architecture/report-designer/design.md`, `docs/architecture/report-designer/contracts.md`

### 2026-04-16 (PM3)

- **Deep Code Quality Audit** - Conducted comprehensive codebase audit following `docs/plans/00-plan-authoring-and-execution-guide.md`:
  - Analyzed 6 core packages: `flux-react`, `flux-runtime`, `flux-renderers-form`, `flux-renderers-basic`, `flow-designer-core`, `spreadsheet-core`
  - Identified key refactoring opportunities across performance, code duplication, and architecture dimensions

- **Key Findings**:
  1. **Form Renderer Duplication (High)**: 17 occurrences of `String(props.props.name ?? ...)` pattern with inconsistent fallback behavior; 17 occurrences of `useFieldPresentation(name, currentForm, {...})` pattern
  2. **Spreadsheet Command Dispatch (High)**: 527-line switch statement in `core-dispatch.ts` with O(n) lookup; should be Map-based O(1)
  3. **Utility Function Duplication (Medium)**: `isObjectRecord` and `applyResultMapping` duplicated in `data-source-runtime.ts` and `source-registry.ts`
  4. **flow-designer-core/src/core.ts (484 lines)**: Manages 7+ state concerns in single closure; candidate for subsystem extraction

- **Plans Created**:
  - `docs/plans/93-form-renderer-duplication-elimination-and-field-controller-unification-plan.md` - Unify field controller hook
  - `docs/plans/94-spreadsheet-command-dispatch-pattern-refactor-plan.md` - Refactor switch to Map pattern

- **Removed**: `docs/plans/react19-code-review-plan.md` - Did not follow plan guide template
- Key decision: Prioritize measurable duplication (17 occurrences) and architectural issues (giant switch) over minor optimizations
- Reference: `docs/plans/00-plan-authoring-and-execution-guide.md`

### 2026-04-16 (PM2)

- **React 19 Best Practices Code Review** - Applied P0 fixes from React 19 best practices audit:
  - **P0-1**: Removed invalid `useMemo(() => nodeState, [nodeState])` in `packages/flux-react/src/node-renderer.tsx:127-130`. This useMemo was completely useless as it directly returned its dependency with no transformation.
  - **P0-2**: Fixed derived state anti-pattern in `packages/flux-renderers-form/src/renderers/variant-field.tsx:123-139`. Replaced `useEffect + setState` pattern for computing `activeKey` with `useMemo`. Introduced `userSelectedKey` state for user-initiated switches, and derived `activeKey` via useMemo from `matchedKey`, `userSelectedKey`, `detectedKey`, and `initialKey` priorities.
  - **P0-3/P0-4 deferred**: External state sync patterns in `key-value.tsx:249-254` and `array-editor.tsx:178-183` were analyzed but not modified. The existing pattern uses `useSyncExternalStore`-based hooks (`useCurrentFormState`, `useScopeSelector`) with custom equality functions, then syncs to local state via useEffect. While this causes a double render on external value change, this is an acceptable trade-off for the controlled input pattern used.
- Key decision: Prioritize removing truly useless code (P0-1) and fixing anti-patterns (P0-2) over theoretical performance optimizations that require significant architectural changes.
- Reference: `docs/skills/react19-best-practices-review.md`, `docs/plans/react19-code-review-plan.md`
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass.

### 2026-04-16 (PM)

- **Plan 92 completed**: Architecture Implementation Polish and Documentation Consistency
  - **Phase 1**: Removed 10 inline layout style violations from `packages/flux-renderers-form/src/renderers/input.tsx`. Replaced `gap-*`, `grid`, `inline-flex items-center` with semantic marker classes (`nop-select-wrapper`, `nop-checkbox-wrapper`, `nop-switch-wrapper`, `nop-radio-group-wrapper`, `nop-checkbox-group-wrapper`). Added default styling rules to `apps/playground/src/styles.css` (lines 866-959).
  - **Phase 2**: Moved `normalizeInstancePath()` from `packages/flux-core/src/types/node-identity.ts` to `packages/flux-core/src/utils/instance-path.ts`. Now `types/` contains only type definitions per flux-core conventions.
  - **Phase 3**: Fixed `as any` type casts in `packages/flux-renderers-basic/src/button.tsx` by introducing `ButtonVariant` and `ButtonSize` type aliases from schema.
  - **Phase 4**: Documentation verified aligned after fixes, no updates needed.
- Key decision: Marker classes follow `nop-{component}-wrapper` pattern for form field wrappers, with sub-elements like `nop-radio-group-item`, `nop-checkbox-group-loading`.
- Independent closure audit by subagent confirmed all fixes correctly implemented.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass.
- Reference: `docs/plans/92-architecture-implementation-polish-and-doc-consistency-plan.md`

### 2026-04-16 (AM)

- **Phase 7 (Docs) completed**: Updated `docs/architecture/form-validation.md` with new `FormStoreState` and `FieldState` type definitions, including design rationale for the unified `fieldStates` map. Added `getFieldState()` and `setFieldState()` to `FormStoreApi` documentation. Updated `docs/architecture/performance-design-requirements.md` with cross-reference to Plan 91. Plan 91 is now fully completed.

- Completed Plan 91 core phases (1-5): refactored form state storage from multiple separate Boolean Maps (`errors`, `validating`, `touched`, `dirty`, `visited`) to a single flat `fieldStates: Record<string, FieldState>` Map.
- **Phase 1 (Types)**: Added `FieldState` interface with optional `touched?`, `dirty?`, `visited?`, `validating?`, `errors?` properties using `true | undefined` pattern for memory efficiency. Updated `FormStoreState` to `{ values, fieldStates, submitting }`. Added `getFieldState()` and `setFieldState()` methods to `FormStoreApi`. Exported `validationErrorsEqual` utility from `flux-core`.
- **Phase 2 (Store)**: Rewrote `packages/flux-runtime/src/form-store.ts` to use unified `fieldStates` structure with `mergeFieldState()` helper for immutable updates. Simplified `batchUpdate` to diff single Map instead of 5. Per-path subscription mechanism preserved.
- **Phase 3 (Array remapping)**: Added `remapFieldStates()` to `packages/flux-runtime/src/form-path-state.ts`, replacing multiple `remapBooleanState`/`remapErrorState` calls. Array operations now traverse Map once.
- **Phase 4 (React hooks)**: Updated `packages/flux-react/src/form-state.ts` selectors (`selectCurrentFormFieldState`, `selectCurrentFormErrors`) and hooks (`useFieldError`) to read from `fieldStates[path]`. Updated `EMPTY_FORM_STORE_STATE`.
- **Phase 5 (Subform projection)**: Added `projectFieldStates()` to `packages/flux-core/src/utils/path-binding.ts`. Updated `object-field.tsx`, `array-field.tsx`, and `variant-field-runtime.ts` to use it.
- Additional fixes: Updated `form-runtime.ts`, `form-runtime-values.ts`, `form-runtime-field-ops.ts`, `form-runtime-owner.ts` to use new `fieldStates` access patterns. Fixed test mocks in `frame-slot-meta.test.tsx`.
- Added ESLint rule `@typescript-eslint/no-unused-vars: ['error', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }]` to `eslint.config.js` to allow underscore-prefixed unused vars (for destructuring rest patterns).
- Key decision: Phase 6 (renderer data-attribute cleanup) deferred as optional — existing pattern via `useCompositeFieldFrame` returning `'data-field-*'` attributes already works.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass (1800+ tests).
- Reference: `docs/plans/91-form-field-state-normalization-refactor-plan.md`
