# Development Log - 2026-05-24

### 2026-05-24

- Closed the fieldset disclosure primitive residual in Workstream 6: `packages/flux-renderers-form/src/renderers/fieldset.tsx` now routes collapsible fieldset behavior through `@nop-chaos/ui` `Collapsible` primitives while preserving the `fieldset/legend` host shape through the trigger `render` path (`维度11-06`).
- Focused proof for that slice is green in `packages/flux-renderers-form/src/__tests__/fieldset-interaction.test.tsx` and `src/__tests__/form-package-exports.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flux-renderers-form test`, `lint`, `typecheck`, and `build`.
- Owner doc sync: `docs/components/fieldset/design.md` now records the shared `Collapsible` primitive as the supported disclosure baseline for collapsible fieldsets.
- Closed the DingFlow add-node focus-return residual in Workstream 6: `packages/flow-designer-renderers/src/dingflow/ding-flow-canvas-overlay.tsx` now threads the opening trigger ref into `ding-flow-add-node-menu.tsx`, and the menu restores focus to that trigger on `Escape`, outside click, or item selection when the trigger still exists (`维度20-08`).
- Focused proof for that slice is green in `packages/flow-designer-renderers/src/dingflow/ding-flow-add-node-menu.test.tsx`, together with `src/dingflow/ding-flow-add-branch-overlay.test.tsx` and `src/designer-controls.test.tsx`; package `lint`, `typecheck`, and `build` also passed for `@nop-chaos/flow-designer-renderers`.
- Owner doc sync: `docs/architecture/flow-designer/design.md` now records focus return as part of the supported DingFlow add-node overlay accessibility contract.
- Closed the remaining base field error-linkage residual in Workstream 6: `packages/flux-renderers-form/src/renderers/input.tsx` and `packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx` now bind validation errors to real `input-text`/`input-email`/`input-password` and `textarea` controls through both `aria-describedby` and `aria-errormessage`, so these controls no longer rely on wrapper-only error linkage (`维度20-01`).
- Focused proof for that slice is green in `packages/flux-renderers-form/src/__tests__/form-validation-ui.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flux-renderers-form test`, `lint`, `typecheck`, and `build`.
- Closed the current Flow palette primitive residual in Workstream 6: `packages/flow-designer-renderers/src/designer-palette.tsx` now uses `@nop-chaos/ui` `Collapsible` primitives for palette group disclosure instead of a renderer-local `Button + aria-expanded` implementation, so the palette stays on the shared disclosure contract (`维度11-07`).
- Focused proof for that slice is green in `packages/flow-designer-renderers/src/designer-controls.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flow-designer-renderers test`, `lint`, `typecheck`, and `build`.
- Owner doc sync: `docs/architecture/flow-designer/design.md` now records palette group disclosure as part of the shared `Collapsible` primitive baseline.
- Closed the current Spreadsheet toolbar accessibility residual in Workstream 6: `packages/spreadsheet-renderers/src/spreadsheet-toolbar/toolbar-button.tsx` now mirrors active style/alignment state through `aria-pressed`, so toolbar semantics no longer depend on `data-toolbar-active` plus visual variant alone (`维度20-15`).
- Focused proof for that slice is green in `packages/spreadsheet-renderers/src/spreadsheet-toolbar.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/spreadsheet-renderers test`, `lint`, `typecheck`, and `build`.
- Owner doc sync: `docs/components/spreadsheet-page/design.md` now records `aria-pressed` as part of the supported spreadsheet toolbar state baseline.
- Closed the current Code Editor accessibility residual in Workstream 6: `packages/flux-code-editor/src/use-code-mirror.ts` now uses CodeMirror `EditorView.contentAttributes` to bridge field error semantics onto the real `.cm-content` focus target, and `packages/flux-code-editor/src/code-editor-renderer.tsx` forwards the wrapped field ARIA state into that hook so screen readers reach the live editor surface instead of only the outer wrapper (`维度20-12`).
- Focused proof for that slice is green in `packages/flux-code-editor/src/code-editor.integration.test.tsx` and `packages/flux-code-editor/src/use-code-mirror.test.tsx`, and package verification passed for `pnpm --filter @nop-chaos/flux-code-editor test`, `lint`, `typecheck`, and `build`.
- Owner doc sync: `docs/components/code-editor/design.md` now records that CodeMirror accessibility state must be bridged onto `.cm-content` through `EditorView.contentAttributes` rather than left only on the outer field shell.
- Executed the code slices behind Plans `437` to `440` and recorded the corresponding retained deep-audit/open-ended findings under the same live repo pass.
- Report Designer: centralized the aggregated top-level `runtime` summary so host scope, bridge snapshot, and `statusPath` now all derive dirty/undo/redo from the same helper. Code paths: `packages/report-designer-renderers/src/{host-data.ts,bridge.ts,page-renderer.tsx}`.
- Flow Designer: transaction rollback now restores the paired owner `TreeDocument`, and public `designer:commitTransaction` / `designer:rollbackTransaction` no longer report false success for missing or unavailable transactions. Code paths: `packages/flow-designer-core/src/{core.ts,core/transactions.ts,designer-core-types.ts}`, `packages/flow-designer-renderers/src/{designer-action-provider.ts,designer-manifest.ts}`.
- Action engine: `cancelled` / `timedOut` results are back on the supported failure-class path, so `onError`, `onSettled`, and chain-abort semantics match the active action algebra baseline again. Code paths: `packages/flux-action-core/src/{action-core.ts,action-dispatcher/action-execution.ts}`.
- Word Editor: live selection projection now includes `superscript` / `subscript`; `runtime.currentPage` was removed from the published contract because the current canvas bridge still has no trustworthy production page-change callback. Code paths: `packages/word-editor-renderers/src/{editor-canvas.tsx,hooks/use-word-editor-state.ts,word-editor-manifest.ts}`.
- Owner docs synced to the new live baseline: `docs/components/word-editor-page/design.md`, `docs/architecture/word-editor/design.md`, `docs/architecture/flow-designer/api.md`.
- Focused package regression tests passed for the touched owner packages: `@nop-chaos/report-designer-renderers`, `@nop-chaos/flow-designer-core`, `@nop-chaos/flow-designer-renderers`, `@nop-chaos/flux-action-core`, and `@nop-chaos/word-editor-renderers`.
- Workspace verification status: `pnpm typecheck`, `pnpm build`, and `pnpm test` passed during the remediation pass; `pnpm lint` initially failed on stale active-doc links that still pointed at archived analysis notes, so the routing docs were updated to the live `docs/archive/analysis/` paths.
- Code/doc paths for the lint routing fix: `docs/index.md`, `docs/architecture/{playground-experience.md,debugger-runtime.md}`, `docs/references/maintenance-checklist.md`.
- Re-ran workspace `pnpm lint` after the routing-doc fix; `check-active-doc-code-anchors` passed and the full workspace lint chain is green again.
- Final current-session workspace verification passed on the post-fix tree: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`.
- Independent closure audit subagent `ses_1a8678678ffeji9P1UDeRQCJQj` re-checked Plans `437` to `440` against live code/docs/tests and confirmed they can be marked `completed`; the same audit confirmed `docs/plans/436-deep-audit-2026-05-24-full-remediation-plan.md` must remain `partially completed` because only the docs-gate blocker was closed in this session.
- Plan closure sync completed for `docs/plans/437-open-ended-adversarial-review-2026-05-24-report-designer-aggregated-runtime-plan.md`, `docs/plans/438-open-ended-adversarial-review-2026-05-24-flow-designer-transaction-truthfulness-plan.md`, `docs/plans/439-open-ended-adversarial-review-2026-05-24-action-failure-class-restoration-plan.md`, and `docs/plans/440-open-ended-adversarial-review-2026-05-24-word-editor-host-projection-completion-plan.md`.
- Continued the deep-audit parent plan by expanding `scripts/check-active-doc-code-anchors.mjs` from a 6-file allowlist to active-doc discovery over `docs/architecture/`, `docs/components/`, `docs/references/`, plus `docs/index.md`, with explicit exclusions for `docs/archive/`, `docs/analysis/`, `docs/logs/`, and `docs/plans/`.
- Closed the newly covered doc drift exposed by that hard gate in active owner docs: `docs/architecture/performance-design-requirements.md`, `docs/architecture/array-field.md`, `docs/architecture/flow-designer/{runtime-snapshot.md,config-schema.md,canvas-adapters.md}`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/components/package-splitting-strategy.md`, `docs/references/audit-tooling.md`, and `docs/references/maintenance-checklist.md`.
- Continued Plan `436` into live retained-finding remediation workstreams beyond the docs gate. `packages/flux-renderers-form-advanced/package.json` now declares `@nop-chaos/flux-runtime` as a runtime dependency so the projected-scope helper import matches the release manifest again (`维度01-01`).
- Tightened published host contracts to match live core unions: `packages/report-designer-renderers/src/report-designer-manifest.ts` now constrains `preview.mode` to `inline | dialog | replace-page | download`, and `packages/flow-designer-renderers/src/designer-manifest.ts` now constrains `moveBranch.direction` to `left | right`. Focused provider tests in `packages/report-designer-renderers/src/host-action-provider.test.ts` and `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx` now prove invalid literals are rejected (`维度03-03`, `维度03-06`).
- Closed the current form accessibility slice in `packages/flux-renderers-form`: `input-number` now binds field errors through both `aria-describedby` and `aria-errormessage`, and async `select` / `radio-group` / `checkbox-group` loading UI now publishes `role="status" aria-live="polite"`. Focused proof landed in `packages/flux-renderers-form/src/__tests__/{input-number.test.tsx,input-source-state.test.tsx}` (`维度20-02`, `维度20-03`).
- Focused verification passed for the new remediation slice: `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/host-action-provider.test.ts`, `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-provider-and-manifest.test.tsx`, and `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/input-source-state.test.tsx src/__tests__/input-number.test.tsx`.
- Continued Workstream 2 host-contract remediation by centralizing runtime payload validation in `packages/flux-core/src/schema-diagnostics/value-shape-runtime.ts` and extending `FluxObjectShape` with explicit `unknownKeys: 'allow' | 'reject'` semantics so compiler and provider enforcement now share the same closed-object rule (`维度03-11`, `维度03-13`, `维度18-01`).
- Tightened the remaining live host contracts to the actual runtime baseline: spreadsheet methods now publish explicit args shapes and literal unions, report no-args methods now reject payloads consistently, report/spreadsheet absent projections now publish `null` where the manifest declares `null | object`, and word editor chart/code type contracts are narrowed to the core enums (`维度03-04`, `维度03-05`, `维度03-07`, `维度03-09`, `维度03-10`, `维度03-14`, `维度18-02`).
- Focused Workstream 2 proof is green on the current tree: `pnpm --filter @nop-chaos/flux-core exec vitest run src/schema-diagnostics/manifest.test.ts`, `pnpm --filter @nop-chaos/flux-compiler exec vitest run src/schema-compiler-host-action-validation.test.ts`, `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/schema-integration.test.tsx src/bridge.test.ts`, `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/host-action-provider.test.ts src/host-data.test.ts`, `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-action-provider.test.ts`, and `pnpm check:active-doc-code-anchors`.
- Continued `436` with two smaller retained residual closures: `packages/flow-designer-renderers/src/renderer-definitions.ts` dropped the non-live `$designer` `scopeExportContracts` alias so domain-host publication now stays on `hostContract` only, and `packages/flux-renderers-data/src/table-renderer/{use-table-selection.ts,use-table-sort.ts,use-table-filter.ts,table-event-context.ts}` now publish semantic `event + scope + evaluationBindings` payloads for selection/sort/filter just like pagination (`维度18-07`, `维度18-08`).
- Focused proof for that slice passed: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-provider-and-manifest.test.tsx` and `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/use-table-controls.selection.test.tsx src/__tests__/use-table-controls.sort-filter-expand.test.tsx src/__tests__/use-table-controls.pagination.test.tsx`.
- Continued Workstream 8 with the current i18n copy closure slice: `packages/word-editor-renderers/src/dialogs/dataset-dialog.tsx` now sources dataset column placeholder/remove labels from `@nop-chaos/flux-i18n`, `packages/flow-designer-renderers/src/designer-inspector.tsx` now localizes branch move/delete `aria-label`s, and `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}` publishes the corresponding message keys (`维度18-05`, `维度18-06`).
- Focused proof for that i18n/a11y slice passed: `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/dialog-accessibility.test.tsx` and `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-controls.test.tsx`.
- Closed the remaining Report i18n residual in the same workstream: `packages/report-designer-renderers/src/field-panel-renderer.tsx` now renders the header field-count summary with `t('flux.reportDesigner.fields')` instead of a hardcoded English suffix, and `packages/report-designer-renderers/src/field-panel-renderer.test.tsx` proves the zh-CN header summary stays localized (`维度18-03`).
- Closed the current discovery-metadata residual in Workstream 8: `packages/flux-renderers-form/src/renderers/input.tsx` now publishes `sourcePackage: '@nop-chaos/flux-renderers-form'` for all 10 base form input definitions, and `packages/flux-renderers-form/src/__tests__/form-renderer-definition-contracts.test.ts` proves the form package metadata now matches the component/docs ownership baseline (`维度18-09`).
- Closed the current Flow naming residual in Workstream 8: `packages/flow-designer-renderers/src/index.tsx` no longer exports `createFlowDesignerRegistry()` from the stable root surface, `packages/flow-designer-renderers/src/unstable.ts` keeps the deprecated alias for legacy/internal consumers, and `packages/flow-designer-renderers/src/public-surface.test.ts` now proves the alias only exists behind `@nop-chaos/flow-designer-renderers/unstable` (`维度17-01`).
- Closed the current docs inventory residual in Workstream 8: `docs/components/index.md` and `docs/components/roadmap.md` no longer list `input-number` as “documented but not yet implemented”, so the active component inventory now matches the live `input-number` renderer already registered from `@nop-chaos/flux-renderers-form` (`维度16-07`).
- Closed two small architecture-anchor residuals in Workstream 8: `docs/architecture/{flow-designer/design.md,report-designer/design.md}` no longer point at the stale `packages/flux-react/src/index.tsx:479` anchor and now cite the current `schema-renderer/hooks/dialog-host/workbench` owner files, while `docs/architecture/action-scope-and-imports.md` now points `RendererRuntime.dispatch()` at `packages/flux-core/src/types/renderer-core.ts` instead of the short root barrel (`维度16-08`, `维度16-10`).
- Closed the current data-source doc drift in Workstream 8: `docs/components/data-source/design.md` now documents source refresh through the live runtime-owned `refreshSource` + `targetId` path, and no longer presents `component:refresh` as a currently supported component handle (`维度16-11`).
- Closed the current components inventory residual in Workstream 8: `docs/components/{index.md,roadmap.md}` and `docs/components/examples.manifest.json` now include the live `form-advanced` composite/detail renderer registrations (`object-field`, `array-field`, `variant-field`, `detail-field`, `detail-view`) instead of omitting them from the active runtime inventory (`维度16-12`).
- Closed the current renderer-metadata docs residual in Workstream 8: `docs/architecture/renderer-runtime.md` no longer describes renderer static metadata as if it were still limited to the original `button/form/crud/designer-page` pilot set, and now points readers to the live expanded coverage that already includes `tabs`, `table`, `chart`, and `code-editor` (`维度16-15`).
- Closed the remaining package-strategy doc drift slice in Workstream 8: `docs/components/package-splitting-strategy.md` now treats `flux-renderers-content` / `flux-renderers-layout` as target-state packages rather than live topology, records the `form-advanced` split as already completed, removes the stale `input-number` future-work item from Phase 5, and rewrites the dependency matrix to distinguish live runtime vs dev/test dependencies from `package.json` (`维度16-05`, `维度16-09`, `维度16-16`).
- Closed the current surface-handle doc drift in Workstream 8: `docs/architecture/surface-owner.md` and `docs/components/dialog/design.md` now describe `component:open/close/toggle` as future/target capability only, while the current live baseline stays on `openDialog` / `openDrawer` / `closeSurface` because `dialog` and `drawer` renderer definitions still do not publish component handle contracts (`维度16-06`).
- Closed the current runtime-boundary doc drift in Workstream 8: `docs/architecture/flux-runtime-module-boundaries.md` now points compiler ownership at the live `schema-compiler` entry/node/tables split and references current runtime validation suites instead of stale/deleted test locations (`维度16-03`).
- Closed the debugger tabs accessibility/primitive slice in Workstream 6: `packages/nop-debugger/src/panel.tsx` now uses `@nop-chaos/ui` `Tabs` primitives instead of a handwritten `role="tablist" + Button` pattern, and `packages/nop-debugger/src/panel.test.tsx` now proves the live tablist/tab/tabpanel semantics and selected-state publication (`维度11-03`, `维度20-17`).
- Closed the current Word Editor tabs primitive slice in Workstream 6: `packages/word-editor-renderers/src/word-editor-page.tsx` and `src/dialogs/expr-insert-dialog.tsx` now use controlled `Tabs value/onValueChange` state with stable per-panel `TabsContent` values instead of handwritten trigger `onClick/data-state` drift, and focused proof remains green in `src/__tests__/expr-insert-dialog.test.tsx` plus `src/__tests__/word-editor-page-host-scope.test.tsx` (`维度11-04`, `维度11-05`).
