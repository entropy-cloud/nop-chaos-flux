# Development Log - 2026-08-30

### 2026-08-30

### 2026-08-30（P5a 执行）

- **P5a Notion database 多视图复刻（静态）执行**（plan `docs/plans/2026-08-30-0040-2-p5a-notion-database-static-replica.md` Phase 1–5 全部完成，closure audit 通过，roadmap P5a `planned`→`done`；执行顺序约束已于启动时 live 复核满足——P4b 已 done、roadmap P5a `planned`）。产出：单页 `page-schemas/notion-database.json`（6066 行，category `app-replica`）——视图 tab 条（tabs 内建切换 `valueStatePath: activeView` + 五视图分支 `notion-views-*` testid + tab 三态样本声明）+ table 视图（9 列型别分派单元格、列头 label region 型别 glyph+菜单入口 dialog、行 hover OPEN/⋮⋮ 纯 CSS、行条件配色左缘色条近似、客户端分页 4 页）+ board 视图（kanban `draggable:false` 显式声明、列头原生计数、mock 预计算 Count/Percent 聚合条 + §7 候选注记）+ gallery 封面墙（coverTone pastel 封面 + hover 控制条 + 空卡 New）+ calendar 自绘六周竖向月网格（固定锚点 2026-08 / today 2026-08-30 确定性、今日强调、hover `+`、落格卡 center peek）+ list 极简单列（配置集过滤/排序 mock 服务端预应用）+ View settings 抽屉（布局/属性可见性/condition-builder 静态内嵌"不生效"注记/sort 多键/group/条件配色/当前视图配置集 visible 分支命中）+ peek 双形态（side 抽屉 table/board/list 语境、center 弹窗 gallery/calendar 语境，13 型别属性行、`Notion__record?id=` form loadAction、`card-` 前缀容错）+ 新建视图/新建记录/列头菜单/搜索四类静态浮层）+ `notion-replica/notion-replica.css`（`--nt-*` 令牌 `.nt-root/.nt-dialog` 双作用域：浅色画布/侧栏/hover 灰/正文 `#37352F` 逆向近似声明/主蓝 `#2383E2`/10 色盘三套全集 30 变量/33px 行高/圆角 3·4·6·8/chip pill 模式/light-only/营销紫隔离；差异声明 D1/D2/D3 落字 plan + CSS 头注）+ `shared/mock-backend-notion.ts`（499 行，32 records 裁剪型别全覆盖，`Notion__records` 五视图参数化 + 未知 view 兜底 + keyword 空路径、`Notion__record` 占位兜底、`Notion__viewConfigs` 每视图私有 filter/sort/group/layout，get-only，post 拒绝）+ showcase-env 余量整理（antdpro/cal/linear/notion 四分支收敛 `[prefix, handler]` 派发循环，697 行 ≤700）+ `styles.css` 头部 @import 一行（`@source` 前）+ `COMPLEX_PAGE_ENTRIES` 注册 + `notion-mock-backend.test.ts` 15 条单测 + `notion-replica-visual.spec.ts` 6 条初屏结构 e2e。
- **执行期实测与登记**：① container 外层 Tailwind flex 类不作用于 `container-body` 包裹层内子节点——横向布局必须用 container `direction/align/gap` props（grid 用 `bodyClassName`），className 只落品牌类与间距工具类；② opacity:0 元素对 Playwright 仍判 visible——hover 显隐断言改 reveal-group 计算透明度轮询（`revealOpacity` helper）；③ kanban `columnHeader`/`cardTemplate` region 全列/全卡共享无逐项绑定（P4a 同族证据，列头计数用 renderer 原生计数徽标、聚合走 mock 预计算条）；④ 表格 `[data-slot="table-row"]` 同携 header 行，行级断言限定 `tbody`；⑤ loop item 字段须 `$slot.item.*` 显式引用（table cell 的 record scope 直引不适用于 loop）；⑥ tabs keepMounted 默认五分支并存挂载——切换为 Panel hidden 切换零重取数（G-C 重渲染观察实测点）；⑦ peek form 载入期嵌套属性访问需可选链（`${person?.name}`/`${files?.length === 0}`），否则求值抛错进 console-error 门；⑧ kanban 内建工具条/标签过滤云非 Notion 形态——复刻层按稳定 `nop-kanban-tag-filter` 类与 `#kanban-search` id `:has()` 隐藏（ln-replica 先例）。
- **G-C 静态实测结论落字**（plan「G-C 静态实测结论」节，供 P5b 起点与 C2 回写携带）：tab+visible+viewConfig 数据预载承载面成立项、切换零重取数观察、§5 L4 风险三处静态对照（双层权限未模拟/peek 联动以语境静态表达/嵌套 filter 求值未验证）、缺口注记逐项核对（无 popover/冻结列与类型切换 G-D/board 聚合 §7 候选/偏好存储层 §7 候选/hover CSS 口径 G-F）、P5b 接线落点清单。
- **验收自查**：AI 模板感两维通过（零 demo 占位、hover 成对、数据七路经 mock 端点、令牌抽查 33px 行高/主蓝/圆角阶/pill 模式一致）；样式契约通过（新 CSS 全在 `.nt-*` scope、零 renderer 包改动、复刻页零 "Notion" 字样 e2e 断言）；分析篇 §5 逐行复核——12 行无矛盾、1 行事实勘误（calendar 行：月视图为资源时间轴形态原语直载不符，已行内括注记勘误）。
- **验证状态（full-green verification，本日 P5a 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test --force` 68/68 任务全绿（playground 31 files/293 tests 含 15 条 notion mock 单测）、`pnpm check` exit 0 零新增命中（mock-backend-notion.ts 499 行 wc / 500 切分计不触 >500 WARN）；目标 e2e `npx playwright test tests/e2e/notion-replica-visual.spec.ts` **6/6 全绿**（初屏结构+`--nt-*` 令牌 getComputedStyle 断言、面板/浮层、board+gallery 分支、calendar+list 分支、peek 双形态、五视图走查；hover 用计算透明度轮询）。**unit + e2e 双全绿成立**。治理线：`showcase-env.ts` 697 行 ≤700、`mock-backend.ts` 零触碰、`packages/` 零改动、变更面仅 In Scope 清单 + plan + 分析篇勘误行 + roadmap + 本日志。
- **P5a closure audit 通过**：fresh session 独立子 agent 两轮——`ses_fb098471effeL2VnKlL8rwhh1f` R1 全量审计 verdict **issues**（Major-1：plan 行数记录不实 504/505 且提交后将成为新增未登记 oversized WARN；Minor-4：JSON 行数笔误 ~5.3k vs 6066、CSS 令牌头注 `#F7F5F5` 笔误、勘误行合并破坏 GFM 表格、两处 API 错误文案含品牌词）→ 执行 session 全部修复（mock 模块裁至 499 行 wc / 500 切分计、plan 图数更正、CSS 笔误、表格行复位、错误文案去品牌词）→ `ses_fb08d4f35ffel36MFdkW2uOmyi` R2 scoped re-audit verdict **APPROVED** 零 finding。plan `Plan Status: completed`，roadmap P5a `planned`→`done`。Next: P5b（Notion 交互接线与测试）按 roadmap 顺序为下一 work item。

### 2026-08-30（P4b 执行）

- **P4b Linear 风格 issue tracker 交互接线与测试执行**（plan `docs/plans/2026-08-30-0040-1-p4b-linear-interaction-wiring-and-tests.md` Phase 1–5 全部完成，closure audit 通过，roadmap P4b `planned`→`done`；接续被中断执行 session 的断点——Phase 1 已完成（写端点契约/承载机制/模块拆分三项 Decision 落字 + `mock-backend-linear-issues.ts` 467 / `mock-backend-linear-detail.ts` 205 / 主模块重写 + 16 条写操作单测），本 session 从 Phase 2 断点恢复）。产出：`Linear__` 写端点会话态终态 6 个（`bulkUpdate` 扁平/嵌套 patch 双收 + 部分匹配、`createIssue` 会话库新序号 ENG-135、`archiveIssue` 软归档 + 默认读过滤、`moveCard` 状态翻转 + `toIndex` 会话排序 + `card-`/`col-` 前缀容错 + `forceMiss` 钩子、`inboxUpdate` 空 ids=全部语义、`copyLink` get 语义模拟）+ 4 张 schema 交互接线：linear-issues（⌘K 裸 input scope 写入 + `ARRAYFILTER` 内联过滤 + 命令项 `href`/`act` 双路执行与 `closeSurface` 门控收尾、`rowSelection` scope 契约批量栏动态化（计数/禁用/清空 `component:setSelection` 空集）、批量四钮 dialog+`Linear__bulkUpdate`、筛选 dialog→scope setValue→url 模板 `dependsOn` 自动刷新、新建必填校验、peek 链路）、linear-board（`draggable` 内建开启 + `onCardMove`→`Linear__moveCard` + `then`/`onError` 双路 `component:refresh` + dnd 注记改「拖拽已接线」+ 零动作 filter 残钮移除）、linear-inbox（逐条归档/全部已读/全部归档，未读 pill 联动）、linear-detail（copyLink/状态 form（select+应用钮→bulkUpdate 单 id）/归档 `control:{debounce:1200}` 跳转）+ `linear-replica.css` 选中行 scope 选择态类（`:has([data-checked])`，Base UI checkbox 语义）。
- **执行期实测与登记**：① 链式动作 `closeSurface` 时序——面板关闭后同链后续分支 `when` 于已销毁 scope 求值（console error 噪声）、trailing `closeSurface` 走 closeTop 会关掉链中先开的新浮层、`args.surfaceId="${dialogId}"` 被新浮层 scope 同名变量遮蔽——终态：`when` 门控收尾（导航/复制/归档/搜索类关面板），create/filter/help 类保持堆叠浮层 Esc 逐层退出；② Base UI checkbox 选中态为 `data-checked`（非 `data-state`），P4a CSS 三处规则一并修正；③ 表头 `<tr>` 同携 `data-slot="table-row"`，e2e 行定位须域内 `[data-slot="table-body"]`；表头全选内建语义=源数据全量 34 行进选择集（分页仅裁显示）；④ form select 单选渲染 combobox（`combobox-trigger`/`combobox-item`），多选渲染 chips（`combobox-chip-input` 无 trigger）；⑤ 字段级 schema `onChange` 无 event fieldRule 不编译进 events（detail 状态变更改走 bulk-dialog form 模式，`submitOnChange` 存在异步 defaultValue 落库触发空提交风险故不采用）；⑥ kanban 拖拽源注册在 React Compiler dev 双挂载下滞后 reconciliation 一拍（dragstart 载荷 cardId 与元素 `data-card-id` 不一致，浮动命中他卡）——`__linearTestHooks.lastMove` 钩子做数据一致性断言，本体登记 C2 回写 ⑤ G-A 归 D1/renderer；⑦ container-body wrapper 布局语义（schema `flex flex-row` 不透传 wrapper，复刻页在 shell 预画画布纵排收缩，P4a 收口形态即如此）登记 C2 ⑤ G-A；inbox 顶栏钮下半与分组区重叠以按钮非遮挡区点击规避。
- **C2 回写与对照**：C2 回写 ⑤ 追加（G-B1 ⌘K 深度/G-B2 五类手势不模拟终态/G-B3 scope 选择集契约与全选=全量语义/G-A 四条含 cardTemplate P4a 证据引用/clipboard 第二例/帮助面板静态承载收口/富文本维持缺口 + toast debounce 第三例与 refreshSource 复现注记）；分析篇 `linear.md` §4.8「预测 vs 实测」12 行对照 + §5 两行原位勘误标注（⇧click/⌘A 改判「仅按钮面支持」、chord/J·K/Space 改判「显式不模拟」）。
- **验证状态（full-green verification，本日 P4b 后）**：`pnpm typecheck`/`pnpm build`/`pnpm lint` 全绿、`pnpm test` 68/68 任务全绿（playground 30 files/278 tests 含 34 条 linear mock 单测：写端点成功/miss/会话持久性）、`pnpm check` exit 0（仅 2 条已登记 i18n 豁免 + WARN 观察名单）；目标 e2e `npx playwright test tests/e2e/linear-replica-visual.spec.ts tests/e2e/linear-replica-interactions.spec.ts` **24/24 全绿**（18 条交互：L1 过滤执行×3/L3/L4/L7 选择集×2/L8 peek/L10 批量四动作/L11 新建×2 与 detail 三链路/L12 筛选×2/L13 拖拽×3；6 条初屏零回归）。**unit + e2e 双全绿成立**。治理线：`showcase-env.ts` 699 行零改动（≤700）、`mock-backend.ts` 463 行零触碰、`packages/` 零改动（git diff 为空）、变更面仅 In Scope 清单。
- **P4b closure audit 通过**：fresh session 独立子 agent 两轮——`ses_fb0f28534ffeSbxjF4OS4giq0z` R1 全量 9 门审计 verdict **REJECTED**（Blocker-1：C2 回写⑤缺 G-A cardTemplate 证据引用；Major：linear.md §5 表未原位标注；Minor：行数记录漂移 467→470 等）→ 执行 session 修复（C2 追加 cardTemplate 引用、§5 两行勘误、行数记录修正）→ `ses_fb0e88198ffeUomdn1HZjztDiZ` R2 scoped re-audit verdict **APPROVED**（7/7 回写输入、append-only 核验、标注成立、零新 finding）。plan `Plan Status: completed`，roadmap P4b `planned`→`done`。Next: P5a（Notion database 多视图复刻——静态）按 roadmap 顺序取队首。

- **P4a Linear 风格 issue tracker 复刻（静态）执行**（plan `docs/plans/2026-08-29-1819-2-p4a-linear-tracker-static-replica.md` Phase 1–6 全部完成，closure audit 通过，roadmap P4a `planned`→`done`；P3b 虚线时序约束已于执行启动时 live 复核满足——P3b 已 done、HEAD `9a802cc5b`、working tree clean）。产出：6 张 `linear-*` 页面 schema（issues 三栏列表 + 高密度 issue 表 + 底部批量栏 + Display 抽屉 + ⌘K 命令壳 + peek 浮层 / board kanban 状态分列 + 列头计数 / inbox 通知分组流 + 未读样本 + 逐条与批量已读形态 / detail 六分区（标题/描述/子问题/关系/活动流/属性侧栏）/ projects 项目卡 + 周期概览行 / settings 子导航 + tabs 分节 + 偏好开关静态表单，category `app-replica`）+ `apps/playground/src/linear-replica/linear-replica.css`（令牌 `.ln-root/.ln-dialog` 双作用域，dark-only：背景四层/文本四级/品牌紫三档/语义 rgba pill 模式/边框 rgba 三档/圆角 2·4·6·8·12·9999；差异声明三项 Decision 落字：①页面粒度 6 张（⌘K/peek 为页内浮层）②浮层与替代承载（dialog ⌘K 壳、dialog peek、drawer Display、container+缩写自绘头像、⌘K 清单条目承载帮助面板、纯 text 多段描述）③令牌（逆向源多源交叉采纳、priority 色 rgba(主色,0.15) 等效拟定、Inter 510→静态 500、Berkeley Mono→JetBrains Mono 声明栈））+ `shared/mock-backend-linear.ts`（34 条确定性 issue 数据集 ≥3 页、状态/优先级/标签/指派/估算全覆盖、board 视图 BoardData 结构、inbox 三组已读未读、详情兜底记录路径、三组命令清单）+ showcase-env `Linear__` 5 端点 get-only 分支（issues 含 `view=board` 参数化，分支体下沉，showcase-env.ts 699 行 ≤700 红线）+ `COMPLEX_PAGE_ENTRIES` 6 条注册 + `tests/e2e/linear-replica-visual.spec.ts` 6 条初屏结构用例 + 18 条 mock 单测（`linear-mock-backend.test.ts`）。
- **执行期实测与登记**：① kanban `cardTemplate` region 的 definition 字段规则未声明 `params`，region 实例化拿不到 `{card,column,index}` 绑定，`$slot.card` 求值抛错——卡片自定义模板不可达，实际以默认卡面承载（title/description 元信息行/meta.tags 标签 pill/meta.members 首字圆/meta.color 优先级色点），G-A 证据落字 plan 供 P4b/C2 回写；② kanban `draggable` renderer 默认开启（`resolved.draggable !== false`），不声明即开拖拽——board schema 显式 `draggable:false` + 页面静态注记；③ table cell region 为 isolate scope，cell 内动作打开的 dialog 看不到页面级 data-source——peek 改走 approval-tasks 先例（`args.data` + form `loadAction` 按行 id 取数）；④ tabs 分节内容必须按 `items:[{key,title,body}]` 携带（误用节点级 `body` 分桶时编译静默失败、页面空白，vitest probe 二分定位后修正）；⑤ className 模板（`ln-pill ln-pill-${status}` 等）全链路可用（meta 编译模板节点 + 依赖订阅响应式），复刻类不进 Tailwind 扫描无 JIT 风险；⑥ 进度条宽度无 schema style 通道，以 mock 字面量枚举 Tailwind 任意值宽度类（`w-[62%]` 等）承载。
- **Phase 6 自查**：键盘缺口静态证据清单（分析篇 §4.1–4.7 十三组键位 ↔ 承载物 ↔ 缺口（G-B1/G-B2/G-B3）↔ 可模拟性初判）落字 plan；AI 模板感两维自查通过（零 demo 占位、hover/空态/选中态成对、数据全经端点、令牌六项抽查）；样式契约自查通过（新 CSS 全在 `linear-replica.css` scope，零 `packages/` 改动）；分析篇 §5 逐行复核无事实矛盾（两处口径澄清不改文档：avatar 为 ui 组件非 renderer type 沿 P1 review B1 口径；kanban"高（拖拽）"指拖拽底座成立，卡面 params 缺口为新实测证据归 G-A）——No owner-doc update required。
- **验证状态（full-green verification，本日 P4a 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37（仅 flux-renderers-scheduling `gantt-grid.tsx` 1 条 0-error 既有 warning，未触碰包）、`pnpm test` 68/68 任务全绿（playground 30 files/258 tests 含 18 条 linear 单测）、`pnpm check` exit 0 零新增命中；目标 e2e `npx playwright test tests/e2e/linear-replica-visual.spec.ts` **6/6 全绿**（含 `--ln-brand/--ln-bg/--ln-radius/--ln-bg-panel` getComputedStyle 令牌断言、`:has` 选中行品牌紫底断言、批量栏/⌘K 壳/peek/Display 抽屉可见性、六页零 "Linear" 商标字样断言）。**unit + e2e 双全绿成立**（本 spec 为新增文件，不触碰既有 e2e 基线红清单）。治理线：`mock-backend.ts` 463 行零触碰、`showcase-env.ts` 699 行 ≤700、`styles.css` 仅追加 1 行 @import（置于 `@source` 前）。改动面：playground In Scope 文件 + 本 spec + 截图 artifacts（antdpro/cal 先例同轨）+ plan + roadmap + 本日志。
- **P4a closure audit 通过**：fresh session 独立子 agent `ses_fb1adfa83ffeCRq6zO65oUo012` verdict **APPROVED**（零 Blocker/零 Major；3 Minor 随收口处置：① `Linear__` 端点名在 showcase 目录 chrome 的 features/description 可见——沿 AntdPro**/Cal** 先例判可接受，登记惯例注记于 plan Non-Blocking Follow-ups；② `mock-backend-linear.ts` 772 行超 ~500 抽取启发线（本计划 700 行门仅约束 showcase-env.ts），登记 Follow-up 观察；③ 日志与提交随本收口落盘——即本条目与 full-green commit）。审计 10 gate 逐项以 live 命令复核（schemas/注册/e2e 6+6、差异声明零占位、get-only guard + 34 issues + 699 行 + mock-backend.ts 未触碰、CSS 双作用域 + @import 序、键盘清单结构、品牌边界 grep、变更面=In-Scope、Phase 全 [x]、testid/令牌断言抽查）。plan `Plan Status: completed`，roadmap P4a `planned`→`done`。Next: P4b（Linear 交互接线与测试）按 roadmap 顺序取队首，起点即 plan 键盘缺口静态证据清单与 G-A/G-B1/G-B2/G-B3 实测证据。
