### 2026-05-07

- 执行 `221` / `224` / `226` 的首批 remediation，先收口低冲突且已确认 live 的 validation subtree、tree a11y、field-control `meta.className`、`hiddenFields` remap、`WrappedFieldAction` 键盘路径、以及 README `closeSurface` 术语漂移。
- 代码/文档路径: `packages/flux-runtime/src/{form-runtime-owner.ts,form-runtime-subtree.ts,form-runtime-array.ts,__tests__/form-runtime-{subtree,deferred,array}.test.ts}`, `packages/flux-renderers-data/src/{tree-renderer.tsx,__tests__/data-tree-and-chart.test.tsx}`, `packages/flux-renderers-form/src/{renderers/input.tsx,__tests__/input-classname-contract.test.tsx}`, `packages/flux-renderers-form-advanced/src/{tree-controls.tsx,wrapped-field-action.tsx,__tests__/widget-classname-contract.test.tsx,condition-builder/condition-item.test.tsx,tag-list.test.tsx,composite-field/array-field.test.tsx}`, `docs/architecture/{form-validation.md,renderer-runtime.md,field-binding-and-renderer-contract.md}`, `docs/components/tree/design.md`, `README.md`, `README.zh-CN.md`, `docs/plans/{221,224,226}*.md`.
- Key decision: `validateSubtree()` 在无 executable compiled model 时不能再返回“普通 clean success”；`applyChangesAndRevalidate('change')` 必须包含 changed path 自身；`expandOnClickNode` 模式下 `aria-expanded` 必须发布在真实焦点目标上；field-like widget 的 `meta.className` 需要落在可命中的 control root。
- Verification: `pnpm --filter @nop-chaos/flux-runtime test -- --run src/__tests__/form-runtime-subtree.test.ts src/__tests__/form-runtime-deferred.test.ts src/__tests__/form-runtime-array.test.ts`, `pnpm exec vitest run "src/__tests__/input-classname-contract.test.tsx"` in `packages/flux-renderers-form`, `pnpm exec vitest run "src/__tests__/data-tree-and-chart.test.tsx"` in `packages/flux-renderers-data`, and `pnpm exec vitest run "src/__tests__/widget-classname-contract.test.tsx" "src/condition-builder/condition-item.test.tsx" "src/tag-list.test.tsx" "src/composite-field/array-field.test.tsx"` in `packages/flux-renderers-form-advanced`.

### 2026-05-07

- 修复 `playground/performance-table` 的 `tag-list` 误报错：问题并非 table 行级错误广播，而是 `tag-list` 在未声明 `required` 时仍无条件注册“至少选择一个 tag”的 runtime validation。
- 代码/文档路径: `packages/flux-renderers-form-advanced/src/{tag-list.tsx,tag-list.test.tsx,__tests__/form-runtime-fields.test.tsx}`, `tests/e2e/performance-table.spec.ts`, `docs/testing/2026/05-07.md`, `docs/bugs/45-tag-list-non-required-runtime-validation-fix.md`.
- Key decision: 不继续扩大到 table row owner/path rebasing 修复；最小正确修复是把 `tag-list` 的非空校验重新收敛到 schema `required` 语义。
- Verification: `pnpm exec vitest run src/tag-list.test.tsx src/__tests__/form-runtime-fields.test.tsx` in `packages/flux-renderers-form-advanced`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced lint`, `pnpm exec vitest run src/__tests__/data-table.test.tsx` in `packages/flux-renderers-data`, and `pnpm exec playwright test tests/e2e/performance-table.spec.ts --grep "tag-list validation local to the edited row" --reporter=list`.

### 2026-05-07

- 基于 `docs/analysis/2026-05-07-deep-audit-full-8/` 新建并重写 `docs/plans/221`-`230` 的 successor owner plan set，覆盖 build/public-doc、module boundary、runtime ownership/reactive、validation、test hardening、accessibility、security/performance、styling、async/error、renderer/slot/type cleanup 十个执行面。
- 代码/文档路径: `docs/plans/{221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md,222-large-file-and-owner-boundary-successor-plan.md,223-reactive-and-async-follow-up-closure-plan.md,224-validation-subtree-follow-up-plan.md,225-test-hardening-follow-up-plan.md,226-accessibility-follow-up-plan.md,227-safety-and-performance-redlines-plan.md,228-styling-and-css-surface-cleanup-plan.md,229-async-lifecycle-and-error-integrity-plan.md,230-renderer-slot-and-type-contract-cleanup-plan.md}`.
- Key decision: 旧 `221`-`226` 基于过时 `deep-audit-full` baseline 且 scope 过宽，不能诚实承接 `full-8` retained set；因此改为 `221`-`230` execution-ready owner map，并把 async/error 与 renderer/slot/type cleanup 从原 `223`/`224` 中拆出。
- Key decision: 对 `220`/`211`/`212`/`168`/`185`/`193`/`195`/`213`/`214` 已关闭或在执行的 plan 明确写 carve-out，避免 reopen 已关闭 correctness baseline 或把 confirmed defect 静默降级为 follow-up。
- Audit result: 经过多轮独立子 agent 复审后，最终确认 `221`-`230` 已为 `full-8` retained finding family 提供 execution-ready owner coverage；最后一轮确认 task `ses_1fdbfe7e2ffeHiTcIZV2z9h1nJ` 返回 `YES`。

### 2026-05-07

- 完成 `docs/analysis/2026-05-07-deep-audit-full-8/` 全量 20 维深度审核归档，按 `docs/skills/deep-audit-prompts.md` 执行多轮深挖、维度复核和高风险子项复核。
- 关键文档输出: `docs/analysis/2026-05-07-deep-audit-full-8/summary.md`, `docs/analysis/2026-05-07-deep-audit-full-8/{01-dependency-graph.md..20-accessibility.md}`.
- Key decision: 本次 `full-8` 汇总只纳入已独立复核保留的结论；未复核初审线索、已降级项和已驳回高频误报不进入 P0/P1 清单。
- Key retained risks: dist ESM import failure, build/test/CSS asset publishing gaps, validation external-error lifecycle, CRUD/Table owner split, action result error propagation, regex/CSS injection/performance redlines, and complex widget accessibility gaps.

### 2026-05-07

- 将 `docs/articles/ai-assisted-framework-development-methodology.md` 引言中的项目背景句恢复为作者确认的原始事实表述：单人、一个多月、完全依赖文档驱动、由 AI 独立完成实现。
- 代码/文档路径: `docs/articles/ai-assisted-framework-development-methodology.md`, `docs/logs/2026/05-07.md`.
- Key decision: 该句属于作者直接确认的项目事实，而不是仅依赖仓库内可交叉核验材料得出的保守改写，因此恢复原文。

### 2026-05-07

- 将 `docs/plans/221`-`226` 全部收敛为 execution-ready plan set，并通过最终独立子 agent 复核确认它们已满足 `docs/plans/00-plan-authoring-and-execution-guide.md` 的执行前要求。
- 代码/文档路径: `docs/plans/{221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md,222-large-file-and-owner-boundary-successor-plan.md,223-reactive-and-async-follow-up-closure-plan.md,224-validation-subtree-follow-up-plan.md,225-test-hardening-follow-up-plan.md,226-accessibility-follow-up-plan.md}`, `docs/logs/2026/05-07.md`.
- Key decision: 不再把 `222`-`226` 维持为 successor placeholder；它们现在都必须具备 Goals / Scope / Non-Goals / fixed workstreams / focused proof / owner-doc obligations / workspace verification / closure audit，才能作为 `221` 的 honest successor owner set。
- Key decision: 最后一轮边界收口采用 same-file carve-out + disjoint proof surface 原则，而不是简单避免同名文件；其中 `223` 保留对 shared report-designer reactive/async test surface 的 ownership，`225` 改为固定 owning 新的 dedicated hardening tests，避免再与 `223` 共享 toolbar proof 文件。
- Key decision: `222` 预先冻结 4 个 retained hotspot 的目标 split boundary、live size baseline 与 closure re-audit command；`225` 则把 report-designer hardening proof 明确钉死到 `report-designer-page-lifecycle.test.tsx` / `report-toolbar-hardening.test.tsx` / `report-inspector-shell.test.tsx` 这些 dedicated targets。
- Audit result: 最终独立复核确认 `221`-`226` 之间已无 execution-blocking overlap，且整组计划已达到 execution-ready。
- Next step: 从 `221` 或任一 `222`-`226` successor plan 中选择一个进入真实 implementation / verification 执行。

### 2026-05-07

- Flow designer tree mode: fix port positioning and node dimension consistency.
- Link to code: `packages/flow-designer-renderers/src/designer-xyflow-canvas/render-ports.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/designer-xyflow-node.tsx`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/xyflow-utils.ts`.
- Root cause analysis compared with reference implementation at `C:\can\ai\wflow-web-next` (pure CSS flexbox approach, no canvas/SVG).
- Key fixes: (1) Handle CSS changed from `!top-0`/`!bottom-0` to `!-top-1.5`/`!-bottom-1.5` to center 12px port circles on node edges. (2) Tree mode node wrapper uses exact `width`/`height` instead of `minWidth`/`minHeight` to match layout algorithm assumptions. (3) Removed `measured` override in tree mode so React Flow auto-measures actual DOM dimensions for edge computation. (4) Terminal nodes render as compact circle+label instead of full Card. (5) Delete button hidden for terminal nodes in tree mode.
- Key decision: reference wflow-web-next uses pure CSS pseudo-elements for lines (no SVG edges, no coordinate math). Our React Flow approach requires dimension consistency between layout algorithm and DOM rendering.
- Next step: verify visual rendering in playground; consider whether the layout `layerSpacing` values need adjustment for actual rendered card heights.

- 重写 `docs/articles/ai-assisted-framework-development-methodology.md`，把引言改成基于 live repo 轨迹的经验归纳表述，替换不稳的 async/await 自洽叙事例子，并收紧第十三、第十七、第二十一节与结语的结构。
- 代码/文档路径: `docs/articles/ai-assisted-framework-development-methodology.md`, `docs/bugs/06-array-editor-key-value-dual-state-fix.md`, `docs/logs/{2026/04-16.md,2026/04-23.md,2026/04-24.md,2026/05-02.md,2026/05-04.md}`, `docs/plans/173-adversarial-review-2-critical-findings-remediation-plan.md`.
- Key decision: 文章保留“单人 + AI + 文档驱动”的项目事实，但明确降格为仓库经验归纳而非统计结论；所有强判断尽量改用仓内 logs / bugs / plans 锚定。
- Key decision: 第十七节明确标记为基于现有多 Agent 审计实践外推出来的 future operating model，不再伪装成当前单仓实践已经完全验证的经验结论。
- Next step: 继续用独立子 agent 多轮审核该文，直到没有剩余的结构性反对意见或证据锚点异议。

### 2026-05-07

- Closed `docs/plans/220-cross-boundary-state-and-host-contract-closure-plan.md` after removing the last debugger page-global DOM fallback and re-running full workspace verification plus an independent closure audit.
- Link to code: `packages/nop-debugger/src/{controller-component-inspector.ts,controller-inspect-advanced.test.ts}`, `packages/report-designer-renderers/src/{page-renderer.tsx,report-designer-toolbar-helpers.ts}`, `packages/word-editor-renderers/src/__tests__/word-editor-page-host-scope.test.tsx`, `packages/flow-designer-core/src/core.ts`, `packages/flux-runtime/src/{runtime-factory.ts,surface-runtime.ts}`.
- Link to docs: `docs/plans/220-cross-boundary-state-and-host-contract-closure-plan.md`, `docs/architecture/{report-designer/design.md,word-editor/design.md,debugger-runtime.md,surface-owner.md,flow-designer/tree-mode.md}`, `docs/components/{report-designer-page/design.md,report-toolbar/design.md,word-editor-page/design.md}`.
- Key decision: debugger DOM correlation is now runtime-root-scoped only; when a runtime is known but its root is missing, inspect metadata must stay unresolved instead of falling back to page-global `[data-cid]` lookup.
- Verification: `pnpm --filter @nop-chaos/nop-debugger test -- --run src/controller-inspect-advanced.test.ts`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Independent closure audit: the final re-audit confirmed no remaining in-scope blocker or silent scope drift for plan `220`.
- Next step: none for plan `220`; later broader workbench host-projection convergence or generic multi-runtime debugger federation remains out of scope unless a new owner plan adopts it.

### 2026-05-07

- Closed `docs/plans/219-table-row-scope-publication-and-invalidation-closure-plan.md` after stabilizing the remaining `flux-renderers-data` verification path and synchronizing the final closure audit evidence.
- Link to code: `packages/flux-renderers-data/src/table-renderer/use-table-row-scope-cache.ts`, `packages/flux-renderers-data/src/__tests__/{use-table-row-scope-cache.test.tsx,data-table.test.tsx}`, `packages/flux-renderers-data/package.json`, `packages/flow-designer-renderers/src/edge-label-xyflow.test.tsx`, `apps/playground/src/{route-model.ts,component-lab/renderer-lab-registry.ts,component-lab/renderers/index.ts,component-lab/renderers/input-number-lab-page.tsx}`.
- Link to docs: `docs/plans/219-table-row-scope-publication-and-invalidation-closure-plan.md`, `docs/architecture/{dependency-tracking.md,table-row-identity-and-scope-performance.md}`, `docs/logs/2026/05-07.md`.
- Key decision: fix the `use-table-row-scope-cache` suite at the test-isolation level by adding explicit `cleanup()` plus module-cache reset coverage, instead of keeping a script-level per-test split workaround for a stateful module-cache leak.
- Verification: `pnpm exec vitest run src/__tests__/use-table-row-scope-cache.test.tsx --reporter=verbose` in `packages/flux-renderers-data`, `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `pnpm --filter @nop-chaos/flux-renderers-data build`, `pnpm --filter @nop-chaos/flux-renderers-data lint`, `pnpm --filter @nop-chaos/flux-renderers-data test`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Independent closure audit: a separate audit pass on 2026-05-07 confirmed the table-owned slice now closes honestly; the only remaining blocker was stale plan/log synchronization, which is resolved by this entry and the final plan status update.
- Next step: none for plan `219`; any future collection-owner translation work should land as the generic loop/list/tree successor rather than reopening the closed table-only slice.

### 2026-05-07

- 对 `221` + `222`-`226` 的 plan structure 进行多轮独立评审后，最终决定是不合并成一个更大的总计划，保留按 defect family 拆分的 successor owner map。
- 代码/文档路径: `docs/plans/{221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md,222-large-file-and-owner-boundary-successor-plan.md,223-reactive-and-async-follow-up-closure-plan.md,224-validation-subtree-follow-up-plan.md,225-test-hardening-follow-up-plan.md,226-accessibility-follow-up-plan.md}`.
- Key decision: `223` 已经是唯一合理的合并桶（reactive + async）；其余 retained P2 family 继续保持拆分 placeholder，等真正开工时再各自扩展成完整 executable plan。
- Audit consensus: 2 个独立审阅给出 `KEEP_SPLIT`，1 个给出 `PARTIAL_MERGE`，其唯一保留的“合并”建议也是维持当前 `223` 的现状，因此最终共识为保持拆分。

### 2026-05-07

- 将 `docs/plans/222`-`226` 的状态从 `planned` 收紧为 `proposed`，并明确它们当前只是 successor owner placeholders，不是已经进入执行的 implementation plans。
- 代码/文档路径: `docs/plans/{222-large-file-and-owner-boundary-successor-plan.md,223-reactive-and-async-follow-up-closure-plan.md,224-validation-subtree-follow-up-plan.md,225-test-hardening-follow-up-plan.md,226-accessibility-follow-up-plan.md,221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md}`.
- Key decision: retained P2 successor owner 需要先存在，但 placeholder plan 不能假装自己已经具备完整执行粒度；真正开工前必须先扩展为完整 executable plan。

### 2026-05-07

- 为 `docs/analysis/2026-05-07-deep-audit-full/02-module-responsibility.md` 补充显式注记，说明 `维度复核结论` 中的个别过渡候选没有进入 `最终保留项`，后续 remediation / successor routing 一律以 `最终保留项` 与 `summary.md` 为准。
- 代码/文档路径: `docs/analysis/2026-05-07-deep-audit-full/02-module-responsibility.md`.
- Key decision: analysis 文档中的复核阶段暂存条目不能被误读为最终 retained closure set；owner plan 只对最终 retained 集合负责。

### 2026-05-07

- 新增 `docs/plans/221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md`，把 05-07 深度审核中未被既有 plan owning 的高优先级 confirmed defects 收敛成一份执行计划。
- 关键收敛: `221` 明确只 owning 9 个 family（3 个 `>700` 测试硬门禁、flow-designer public contract drift、create-dialog 并发保护、`hiddenFields` remap、renderer `props.meta` residual、semantic form submit E2E、tree-layout hot path、README `closeSurface` 术语、`WrappedFieldAction` keyboard parity），不重开 `220` 的 report/word cross-boundary defects，也不重开 `212` 对 `WrappedFieldAction` element shape 的既有裁定。
- 代码/文档路径: `docs/plans/221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md`, `docs/plans/{222-large-file-and-owner-boundary-successor-plan.md,223-reactive-and-async-follow-up-closure-plan.md,224-validation-subtree-follow-up-plan.md,225-test-hardening-follow-up-plan.md,226-accessibility-follow-up-plan.md}`.
- Key decision: retained confirmed P2 family 不允许继续停留在抽象 successor bucket；必须在 `221` 文本中绑定到具体 successor plan path，并写清 owner files。
- Key decision: `221` 的 Workstream targets / proof / exit criteria 收紧到文件级，包括 `designer-provider-and-manifest.test.tsx`, `designer-controls.test.tsx`, `index.xyflow.test.tsx`, `form-runtime-array.test.ts`, `tree-values.test.tsx`, `widget-classname-contract.test.tsx`, `designer-page-shell.test.tsx` 等可审计 proof 面。
- Key decision: `dim02` 中停留在 `维度复核结论` 但未进入 `最终保留项` 的过渡条目不再作为 retained confirmed closure set；05-07 successor routing 只以各维度 `最终保留项` 与 `summary.md` P1 列表为准。
- Key decision: successor plans `222` / `223` / `225` 对与 `220` 共享的文件，必须按 defect family 明写 carve-out，不能只写“整体不重叠”的宽泛表述。
- Key decision: `props.meta.className` / `meta.disabled` 已是 supported contract，因此 `221` 明确要求同步 `docs/architecture/{renderer-runtime.md,field-binding-and-renderer-contract.md}`，不再保留宽泛的 `No owner-doc update required` 出口。
- Next step: 对更新后的 `221` 进行独立审阅，确认 successor routing、owner 边界、以及 file-level exit criteria 已足够可执行。

### 2026-05-07

- 收口 `docs/plans/220-cross-boundary-state-and-host-contract-closure-plan.md` 的第一批 in-scope cross-boundary fixes，覆盖 report-designer、word-editor、debugger/runtime surface、以及 flow-designer tree history。
- 代码路径: `packages/report-designer-renderers/src/{page-renderer.tsx,report-designer-toolbar.tsx}`, `packages/word-editor-core/src/document-io.ts`, `packages/word-editor-renderers/src/{editor-canvas.tsx,word-editor-page.tsx}`, `packages/flux-runtime/src/{surface-runtime.ts,runtime-factory.ts}`, `packages/nop-debugger/src/controller-component-inspector.ts`, `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-renderers/src/designer-command-adapter.ts`.
- Key decision: report-designer 对外的 canonical workbook baseline 仍是 `reportDocument.document.spreadsheet`；spreadsheet-core 编辑态必须同步回这条 report-owned snapshot，toolbar 默认项也统一只发 canonical namespaced actions。
- Key decision: word-editor recovery/datasets 采用 persisted-first precedence；`runtime.dispose()` 现在驱动 surface close lifecycle；flow tree history 记录 owner `treeDocument` 与 projected graph 的同一条 undo/redo baseline。
- Proof: focused tests 已覆盖 report host dirty/status publication、save/export parity、word recovery/dataset precedence、debugger multi-runtime inspect isolation、runtime surface cleanup、以及 flow tree owner/history continuity。
- Doc updates: `docs/architecture/{report-designer/design.md,word-editor/design.md,debugger-runtime.md,surface-owner.md,flow-designer/tree-mode.md}`, `docs/components/{report-designer-page/design.md,report-toolbar/design.md,word-editor-page/design.md}`.
- Next step: 完成 plan 220 剩余的 workspace `typecheck/build/lint/test` 与独立 closure audit，再决定是否可以把计划状态切到 completed。

### 2026-05-07

- 完成 `docs/analysis/2026-05-07-deep-audit-full/` 全量 20 维深度审核汇总，包含多轮深挖、维度复核、关键 P1 子项复核。
- 重点确认的 live 问题集中在: Flow Designer 的 action/manifest 漂移与 create-dialog 并发保护、Report Designer 的 spreadsheet 双 owner、Word Editor 的 datasets precedence、validation subtree/hiddenFields 不一致、字段型 renderer 的 `props.meta` 透传缺口、以及 `WrappedFieldAction` 的键盘可操作性缺陷。
- 关键文档输出: `docs/analysis/2026-05-07-deep-audit-full/summary.md`。
- 关键决定: 最终汇总只保留经独立复核确认的条目；像 `TextRenderer nop-text`、`container/flex` 语义布局类、`WrappedFieldAction` 本体不是 `<Button>` 等高频误报已明确排除。
- 下一步: 将 P1 条目转为 remediation plan 或直接修复批次，并优先补 `flow-designer/api.md` vs manifest/provider 对齐、`WrappedFieldAction` 键盘回归测试、以及 `word-editor` / `report-designer` owner 收敛测试。
