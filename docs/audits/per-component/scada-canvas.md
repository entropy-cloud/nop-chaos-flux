# 审计卡：scada-canvas（flux-renderers-industrial）

> 状态: closed
> 审查日期: 2026-08-08
> 审查 plan: inline（component-audit mission，industrial-hmi mission 产出组件）
> 注册定义: `packages/flux-renderers-industrial/src/renderer-definitions.ts:47` | 渲染器: `packages/flux-renderers-industrial/src/renderer/scada-canvas.tsx:69` | design.md: `docs/components/industrial-hmi/design-renderer.md` | playground: `apps/playground/src/pages/scada-demo.tsx` | e2e: `tests/e2e/scada-demo.spec.ts`, `tests/e2e/scada-edge-cases.spec.ts`

## 组件身份

scada-canvas / @nop-chaos/flux-renderers-industrial / ScadaCanvasSchema（`schemas.ts:12-27`）/ `{type:'scada-canvas', config:{version:1,variables:[],symbols:[]}}` / 表单参与: 否 / instance-renderer（canvas 场景图引擎 + LeaferJS 渲染层 + 数据绑定/点表/动画/符号库）

## 18 维审查记录

| #   | 维度                        | 结论 | 证据                                                                                                                                                                                                                                                                                                                                                                                                                      | 发现                                                                                     |
| --- | --------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 1   | Schema 契约                 | pass | `renderer-definitions.ts:55` defaultSchema 存在且合法；`:196-204` fields（config/width/height/viewport/events=prop, loading/empty=region）与 `schemas.ts:12-27` ScadaCanvasSchema 一致；propContracts（`:58-99`）、eventContracts（`:100-133`）、componentCapabilityContracts（`:135-195`）齐全；`__nopPreserveLiteral` 未涉及（config 为 raw JSON 通道）                                                                 | —                                                                                        |
| 2   | RendererComponentProps 合规 | pass | `scada-canvas.tsx:69-313` 仅读 `props.props`（config/events/width/height/viewport）、`props.meta`（cid/testid/className）、`props.regions`（loading/empty）、`props.helpers`、`props.node?.scope`、`props.id`；用标准 hooks `useRendererRuntime`/`useCurrentComponentRegistry`/`useFluxTranslation`；无直接 store 访问；无 ad-hoc context                                                                                 | —                                                                                        |
| 3   | 值所有权三态                | n-a  | canvas display renderer，无表单值语义；config 为 schema-driven prop（经 useMemo parseAndValidateConfig 派生，非 owner state）                                                                                                                                                                                                                                                                                             | —                                                                                        |
| 4   | 表单参与                    | n-a  | 非表单字段（instance-renderer，无 name/required/validation）                                                                                                                                                                                                                                                                                                                                                              | —                                                                                        |
| 5   | DOM 与选择器契约            | pass | 根 `data-slot="scada-canvas"` + marker class `nop-scada-canvas`（`:296-298`）；`data-status`/`data-testid`/`data-cid` 齐全；canvas DOM 经 effect（`:233-239`）标 `data-slot="scada-canvas-canvas"` + `nop-scada-canvas-canvas`；`check:audit-missing-renderer-markers` 无命中                                                                                                                                             | —                                                                                        |
| 6   | 嵌套 schema 分类            | pass | events 整体 prop（D-1 裁定：flux-compiler classifyField 仅匹配顶层 key，ActionSchema 字面量经 props 通道保留，`:201`）；loading/empty 为 region（`:202-203`）；无 deepFields 残留                                                                                                                                                                                                                                         | —                                                                                        |
| 7   | 事件与 action 契约          | pass | 5 events（onSymbolClick/onSymbolDblClick/onSymbolHover/onReady/onError）经 `useScadaEvents` → `createNormalizedActionEvent` + `helpers.dispatch`（eventContracts `:100-133`）；9 handles（fit/center/getSymbols/getSymbol/setPointValue/getPointTable/exportConfig/importConfig/destroy，`:135-195`）；payload shape 与 symbolEventPayloadShape 一致                                                                      | —                                                                                        |
| 8   | a11y                        | fail | 根 div 无 `role`/`aria-label`（canvas 交互面无 a11y 语义声明）；`<canvas>` 元素本身无键盘操作路径（LeaferJS 交互层，非标准 web content）                                                                                                                                                                                                                                                                                  | **P2-1：canvas 交互面缺 role/aria-label**（已修复：`role="application"` + `aria-label`） |
| 9   | i18n                        | pass | `t('industrial.scada.canvasError')` 在 zh-CN/en-US 均存在（locale :934/:935）；错误码经 `useScadaErrorText` + `scadaErrorI18nKey` 映射（`scada-errors.ts:60-97`），全部 13 码在 locale 注册；`console.warn('[scada-canvas]', ...)` 为 dev 诊断前缀非用户文案                                                                                                                                                              | —                                                                                        |
| 10  | 四态覆盖                    | pass | loading（`:300-301` loading region/fallback）；error（`:302-307` empty region + error binding + i18n 文案）；ready（`:308-309` canvas div）；destroyed（data-status 反映）；空场景（ready + 0 symbols）直接渲染空 canvas——runtime SCADA 空画面即空画面，合理                                                                                                                                                              | —                                                                                        |
| 11  | 异步生命周期                | pass | 引擎生命周期经 `useScadaEngine`/`useScadaConfigSync` 承接（config sync onBuildError→handleError）；诊断通道 try/catch 自保护（`:166-168`）；`void eventsApi.notifyReady()`/`notifyError()` 为 fire-and-forget dispatch（语义正确）；无裸奔 Promise                                                                                                                                                                        | —                                                                                        |
| 12  | 组合宿主场景                | pass | e2e `scada-demo.spec.ts`（场景树/点表刷新/事件联动）、`scada-edge-cases.spec.ts`（空画面/超大画面/非法 JSON）、`scada-perf.spec.ts`（性能基线）；playground 5 页（demo/edge/perf-scale/pressure）                                                                                                                                                                                                                         | —                                                                                        |
| 13  | 样式契约                    | pass | instance-renderer（非 layout renderer）自样式；marker class（nop-scada-canvas/-canvas/-loading/-error）；`cn()` 合并（`:298`）；`h-full w-full` 为 canvas 容器尺寸填充（非 layout 布局类）；无 BEM；`check:audit-styling-suspects` 无命中                                                                                                                                                                                 | —                                                                                        |
| 14  | React 19 规范               | fail | 6 处 `useCallback`（handleReady `:94`/handleError `:100`/reportDiagnostic `:135`/getPointValuesForLatest `:175`/handleDestroyed `:218`/reloadConfig `:278`）——React Compiler 基线下冗余（editor 同型组件已清理，`:91` 注释）；部分有 load-bearing 身分依赖注释（reloadConfig `:276-285`），但 Compiler 可覆盖                                                                                                             | **P3-1：6 处 useCallback 为 React Compiler 冗余候选**                                    |
| 15  | 性能边界                    | pass | React 层仅渲染 wrapper div（canvas 渲染在 LeaferJS）；selector 精度不涉及（无 useScopeSelector）；engine/binding 订阅清理经 `destroy`；getPointValuesForLatest 为 per-event lookup（非热路径）；`check:audit-performance-suspects` 仅 equality.ts/compound.ts 注释命中（非组件代码）                                                                                                                                      | —                                                                                        |
| 16  | 测试质量                    | pass | 97 test files / 1302 tests 全绿；scada-errors.test.ts 覆盖全部错误码；scada-handles.test.tsx 覆盖 importConfig/exportConfig 契约 + invalid-config 失败路径；e2e 覆盖 demo/edge/perf                                                                                                                                                                                                                                       | —                                                                                        |
| 17  | 文档对照                    | pass | design-renderer.md §1/§4.1/§8/§10 与实现一致；quick-reference 无独立词条（industrial 为独立包，n-a）；flux-guide 待补（I15.2 backlog）                                                                                                                                                                                                                                                                                    | —                                                                                        |
| 18  | 注册、包边界与 IO/安全红线  | pass | 双注册齐全（`index.ts:64` registerScadaRenderers + definition 导出）；bundle 导出（`index.ts:16-54`）；playground 页存在；依赖方向合规（flux-core→flux-react→flux-i18n→ui）；`cn()` 复用 ui；INV-1 合规：无直接 localStorage/fetch/WebSocket（env 经 `rendererRuntime.env` 传递）；`querySelector('canvas')` 为组件自有 DOM 子树访问（非 IO 边界）；`console.warn` 为诊断（非 IO）；无 dangerouslySetInnerHTML/URL 注入面 | —                                                                                        |

## 发现清单

- [P2-1] canvas 交互面缺 `role`/`aria-label`（`scada-canvas.tsx:292-299`）——读屏无语义，键盘用户无法感知画布角色 → 状态: **fixed**（加 `role="application"` + `aria-label={t('industrial.scada.canvasLabel')}`）
- [P3-1] 6 处 `useCallback` 为 React Compiler 冗余候选（`scada-canvas.tsx:94,100,135,175,218,278`）——editor 同型组件已清理（`:91` 注释），runtime 侧待 Compiler 迁移 → 状态: **recorded**（P3，不阻塞）

## 组合宿主场景（真实浏览器验证）

- 场景: scada-demo 页面加载 → 场景树渲染 → 点击图元 → 事件联动 | 断言: `data-slot="scada-canvas"` + `data-status="ready"` 存在；点击后事件 payload 含 symbolId | 结果: **pass**（`scada-demo.spec.ts` 程序化断言）
- 场景: 非法 JSON config → 错误态渲染 | 断言: `data-status="error"` + `data-code` 属性存在；错误文案经 i18n | 结果: **pass**（`scada-edge-cases.spec.ts`）
- 场景: 100k 图元压力测试 | 断言: fps≥45 + 内存≤320MB | 结果: **pass**（`scada-perf.spec.ts` / `scada-pressure-demo.spec.ts`）

## 修复记录

- 实现: `scada-canvas.tsx:292-300` 加 `role="application"` + `aria-label={t('industrial.scada.canvasLabel')}`；`zh-CN.ts:935`/`en-US.ts:936` 加 `canvasLabel` key
- 验证: `pnpm --filter @nop-chaos/flux-renderers-industrial typecheck/build/lint/test` 全绿（1302 tests）；`pnpm --filter @nop-chaos/flux-i18n typecheck/test` 全绿（28 tests）

## Closure

- 独立 closure audit: **PASS**（fresh session `ses_0202b17cbffeXMF0hmlW22VM6N`，2026-08-08）
- Evidence（live `文件:行` 复核，行为完成非仅接口存在）：
  - **P2-1** a11y role/aria-label LANDED：`scada-canvas.tsx:298` `role="application"` + `:299` `aria-label={t('industrial.scada.canvasLabel')}`；i18n `zh-CN.ts:935` / `en-US.ts:936` 双 locale key 存在。JSX 每渲染发射到根交互面，i18n 驱动（非硬编码）。
  - **P3-1** 6 处 `useCallback`（`:94/:100/:135/:175/:218/:278`）确认为 P3 recorded（非阻塞），分类诚实，归 HCA-LL/HCA-CR（本卡 `Deferred But Adjudicated`）。
  - 18 维 tally：14 pass / 2 n-a（dim 3 值所有权 + dim 4 表单参与）/ 2 fail（dim 8 a11y 已修 + dim 14 React19 P3 recorded）；无未分类维度，无未修阻塞性 fail。
- Closure remediation（fresh session 抽查发现，已落地）：
  - owner doc drift：`docs/components/industrial-hmi/design-renderer.md` §10 补画布交互面 a11y（role/aria-label + i18n 双 locale）说明。
  - a11y 回归守护：`scada-canvas-smoke.test.tsx` 增 `role/aria-label` DOM 断言（HCA1 P2-1 / HCAX-2）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-industrial typecheck/build/lint/test` 全绿（98 test files / 1319 tests）。
