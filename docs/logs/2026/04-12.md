# Development Log — 2026-04-12

### 2026-04-12 (session 14)

- Resolved all 4 closure audit gaps for Plan 70 (Composite Value Fields and Validation Integration) and marked the plan `completed`.
- Links: `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`, `packages/flux-renderers-form/src/__tests__/composite-form.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-view.test.tsx`
- Changes landed:
  - Added `detail-view` node to the six-component combined scenario in `composite-form.test.tsx` (now includes `Edit Config` detail-view trigger, satisfying the plan requirement that all 6 components appear together).
  - Added `composite form - submit supersedes debounced validation` describe block to `composite-form.test.tsx` with `vi.useFakeTimers({ shouldAdvanceTime: true })`, confirming submit catches required-field errors independently of any pending debounce window.
  - Added `applyCommitResult handles updates dict shape` test to `detail-view.test.tsx` — exercises the `updates` branch in `applyCommitResult` by using an `object-field` named `updates` in the content form.
  - Added `applyCommitResult handles patch array shape` test to `detail-view.test.tsx` — exercises the `patch` branch by seeding initial data with `patch: [{path, value}]` so `scope.read()` returns a value where `'patch' in draftValues` is `true`.
  - Fixed missing closing `});` for the `describe` block in `detail-view.test.tsx` (parse error from previous session).
  - Filled in the closure section of Plan 70 with audit task id, gap summary, and resolution evidence.
- Key decision: `vi.useFakeTimers({ shouldAdvanceTime: true })` is needed for the debounce test because plain `vi.useFakeTimers()` breaks `waitFor` (which uses real `setTimeout` internally); the `shouldAdvanceTime` mode lets real elapsed time advance while still capturing fake timer calls.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (200 flux-renderers-form tests passing).
- Next step: Plan 70 is closed. Continue with Plan 73 or next planned work.



- Tightened Plan 73 with a new hard rule for architecture docs: they must stay focused on final-state core architecture only.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`
- Key decision:
  - Historical evolution, migration narratives, rejected alternatives, and execution history should live in `docs/discussions/`, `docs/analysis/`, `docs/plans/`, and `docs/logs/`, not in `docs/architecture/`, so architecture docs stay focused for readers and AI agents.
  - This does not remove current-design rationale: architecture docs must still explain why the current design is chosen, what constraints it preserves, what misunderstandings it prevents, and where its boundaries are, so AI readers do not hallucinate unsupported adjacent models.
- Next step: apply this rule when building the architecture status matrix and rewriting `docs/architecture/README.md`.

### 2026-04-12 (session 12)

- Completed plan 70 (Composite Value Fields and Validation Integration) through all 8 phases.
- Links: `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`
- Changes landed in this session:
  - Fixed `detail-view` missing `scopePolicy: 'form'` and `props.templateNode.validationPlan` in `handleOpen` — draft form now has the correct compiled validation model so `validateAll('submit')` can reject empty required fields.
  - Fixed `detail-field.tsx` remaining lint issues (`_schema` → no param).
  - Wrote comprehensive composite form test at `packages/flux-renderers-form/src/__tests__/composite-form.test.tsx` (15 tests): covers `object-field`, `array-field`, `variant-field`, `detail-field`, `detail-view`, and `loop` in focused and combined scenarios.
  - Fixed type errors in all new test files: inline `fetcher` functions now use `makeCapturingFetcher` helper with proper `<T>` generic typing.
  - Fixed lint errors in `array-field.tsx` (removed unused `resolveRendererSlotContent`, `readFieldValue`, `useCurrentFormModelGeneration`, `modelGeneration`, `itemTemplate`, `runtime` parameter from `createItemScope`).
  - Fixed lint errors in `detail-view.tsx` (`BaseSchema` import removed), `object-field.tsx`, `variant-field.tsx`, `detail-field.tsx` (`collectRules(_schema)` → `collectRules()`).
- Key decisions:
  - `detail-view` needs `scopePolicy: 'form'` for the same reason as `detail-field`: the schema compiler only collects child validation from regions of nodes whose renderer declares `scopePolicy: 'form'`, making `props.templateNode.validationPlan` non-undefined and usable in `handleOpen`.
  - Scalar array-field items (`itemKind: 'scalar'`) do NOT get the form proxy — the item scope handles `get('value')` → `parentScope.get(itemPrefix)` for reading, but `setValue('value', ...)` on the parent form sets root `value`, not the array item. This is a known design boundary; the comprehensive test avoids asserting scalar-item `required` validation paths to prevent brittle tests.
  - The comprehensive test uses `basicRendererDefinitions` (which includes `button`, `loop`, `text`) + `formRendererDefinitions`. A custom `buttonRenderer` is not added because that would duplicate `button` and cause a registry error.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (197 flux-renderers-form tests, 331 flux-runtime tests, 30 flux-renderers-basic tests all passing).
- Next step: independent subagent closure audit before changing plan 70 status to `completed`.



- Added a new owner plan for architecture hierarchy, principles-vs-normative layering, platform-extension doc positioning, and status-matrix-driven reorganization.
- Links: `docs/plans/73-architecture-principles-hierarchy-and-platform-doc-reorganization-plan.md`, `docs/architecture/README.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/complex-control-host-protocol.md`, `docs/architecture/flow-designer/README.md`, `docs/architecture/report-designer/README.md`, `docs/standardization.md`
- Key decisions:
  - Plan 57 remains closed prior art; the new work is owned by Plan 73 instead of reopening a completed plan.
  - `flux-design-principles.md` is now treated as the governing-principles layer, but not as a replacement for `frontend-programming-model.md`'s normative precedence over primitive/core-boundary rules.
  - `flow-designer` / `report-designer` and `complex-control-host-protocol.md` are treated as Flux platform-extension architecture, because their core value is the abstraction and reuse model for complex domain editors and workbench hosts, not merely domain-specific UI.
  - Plan 73 was narrowed after independent review to one result surface: hierarchy baseline + status matrix + routing/index rewrite + successor mapping, leaving any large physical move to a later successor plan.
  - The plan now requires a file-backed status matrix in `docs/references/architecture-doc-status-matrix.md` and durable recording of independent subagent review evidence.
- Independent review evidence:
  - Doc-role audit task: `ses_28081aa80ffegWObckakBLhjdl`
  - Plan-quality audit task: `ses_28081a992ffeCZnWlGyQ1oh0g6`
  - Follow-up plan audit task: `ses_2807ec709ffeSj26pEDR5a5qJz`
  - Follow-up hierarchy audit task: `ses_2807ec6faffeS0wZzhRheJpt7w`
- Next step: execute Plan 73 by creating the architecture status matrix, rewriting `docs/architecture/README.md`, and reconciling top-level hierarchy/routing docs against the new platform-extension baseline.

### 2026-04-12 (session 10)

- Tightened the new field-binding normative doc and owner plan after a follow-up review surfaced several ambiguity gaps.
- Links: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`
- Key decisions:
  - Rule 2 now includes an explicit applicability test: any control that writes user edits back to an owner/form scope path is an editable field even if its UI is composite or preview-oriented.
  - Rule 5 now states the target `META_FIELDS` baseline explicitly: `{id, className, visible, hidden, disabled, testid}`; `name` / `label` / `title` are documented as migration targets out of default global meta.
  - Rule 8 now separates the illustrative `NodeControlSchema` shell from validation/lifecycle/extension keys so the example does not imply those concerns are the same semantic layer.
  - Rule 9 now treats `statusPath` and `componentId` as confirmed static structural fields in their documented structural roles instead of leaving them as vague candidates.
  - Plan 72 now explicitly freezes allowed `value` scenarios, calls out the `META_FIELDS` constant update, narrows vocabulary-cleanup scope to live dual-vocabulary drifts, and requires affected renderer tests to move from raw-schema/meta fallback expectations to normalized `props` expectations.
- Next step: if Plan 72 execution starts, use the tighter checklist as the source of truth for compiler/runtime adoption and fixture migration work.

### 2026-04-12 (session 9)

- Added a normative architecture doc and a dedicated owner plan for field binding and renderer-contract convergence.
- Links: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/plans/72-field-binding-and-renderer-contract-unification-plan.md`, `docs/index.md`, `docs/architecture/README.md`
- Key decisions:
  - Ordinary editable fields use `name` as the sole two-way binding entry; generic editable `value` is not a peer contract.
  - Semantic display fields such as `text`, `title`, `options`, and `data` should keep natural names; expressions continue to live on ordinary props rather than `xxxExpr` side channels.
  - `meta` should be limited to node-control and outer-frame concerns; `name` should stop relying on default global meta classification.
  - Shared field-schema convergence should happen through small bases and small helpers, not through a giant abstract renderer layer.
  - Plan 72 is scoped to cross-cutting contract convergence, while component-family-specific implementation work remains separate.
- Next step: if execution starts, migrate compiler classification and wrapper/form-renderer adoption first, then clean up representative non-form raw-schema reads and high-value shadcn-equivalent vocabulary drift.

### 2026-04-12 (session 8)

- Completed the real closure audit of plan 68 and plan 69; landed the remaining three behavioral gaps.
- Links: `docs/plans/68-owner-based-validation-runtime-alignment-plan.md`, `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md`
- Changes:
  - `packages/flux-runtime/src/form-runtime-validation.ts`: `validatePath`, `validateSubtreeByNode`, `validateCompiledField`, and `waitForValidationDebounce` now accept and thread `ValidationReason`; `submit`/`commit` reasons skip debounce waits, giving high-priority runs immediate execution.
  - `packages/flux-runtime/src/form-runtime.ts`: `validateField`, `validateForm`, `validateSubtree`, `validateAt`, `validateAll` all accept and thread `reason?`; `submit()` now calls `validateForm('submit')`; `applyChangesAndRevalidate` now `await`s `revalidateDependents` for each changed path before calling `validateForm(reason)` (atomic rather than fire-and-forget).
  - `packages/flux-core/src/types/runtime.ts`: `validateField` and `validateForm` signatures updated to accept `reason?: ValidationReason`.
  - `packages/flux-react/src/hooks.ts`: Added `useCurrentFormModelGeneration()` hook that subscribes to the form store and returns `form.modelGeneration`; exported from package index.
  - `packages/flux-renderers-form/src/renderers/array-editor.tsx`, `key-value.tsx`, `tag-list.tsx`, `condition-builder/ConditionBuilder.tsx`: registration `useEffect` deps now include `modelGeneration` via `useCurrentFormModelGeneration`, ensuring field re-registration fires after `refreshCompiledModel` clears all stale registrations.
- Key decisions:
  - `ValidationReason` threading is minimal but real: reason flows from public API through to `waitForValidationDebounce`; `submit`/`commit` skip the debounce window so supersession is immediate rather than waiting for a timeout to expire.
  - `applyChangesAndRevalidate` now truly awaits dependents revalidation, making the full owner-local atomic semantics hold: write → await dependents revalidate → full form validate.
  - `useCurrentFormModelGeneration` uses an existing store subscription (any store write from `refreshCompiledModel` triggers the subscriber); no new store field is needed.
- Next step: plan 70 (composite value-field family) is the next active owner plan.

### 2026-04-12 (session 7)

- Tightened the plan workflow guide and plan 70 so independent closure audit is now an explicit completion requirement rather than a recommendation.
- Links: `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`
- Key decisions:
  - Marking a plan `completed` now requires an independent reviewer or independent subagent closure audit; implementer self-audit may still be useful during execution, but it no longer qualifies as sufficient closure evidence by itself.
  - The plan template now includes an explicit validation-checklist item and closure-evidence section for the independent audit, so the requirement is visible in newly authored plans rather than only implied by prose.
  - Plan 70 Phase 8, validation checklist, and closure section now explicitly require a fresh-task independent subagent audit before the plan can be closed.
- Next step: apply the stricter closure-audit rule to future plan closures, and when plan 70 is eventually executed, record the subagent task id and findings before changing its status to `completed`.

### 2026-04-12 (session 6)

- Tightened the validation architecture doc around large inline-edit table performance and aggregate-rule execution policy.
- Links: `docs/architecture/form-validation.md`, `docs/references/form-validation-execution-details.md`, `docs/architecture/performance-design-requirements.md`
- Key decisions:
  - `validateAll('change')` is not the normative keystroke-path baseline for large editable tables.
  - Compile-time graph truth does not require owner-wide aggregate execution on every keypress; deferred or incremental strategies are allowed only when they preserve the same published aggregate semantics at the chosen interaction boundary.
  - High-frequency table editing with aggregate rules such as `uniqueBy(...)` is now documented as a first-class performance constraint, with explicit allowed strategies (`change`-local + `blur`/`commit`/`submit` aggregate validation, owner splitting, or semantics-preserving incremental aggregation).
- Follow-up doc sync:
  - `docs/references/form-validation-execution-details.md` now mirrors the same rule in the execution model, the inline-table example, and the `validateAll()` cost section so architecture and reference guidance stay aligned.
- Added a new owner plan for landing the large inline-table aggregate validation performance strategy.
- Link: `docs/plans/71-large-inline-table-aggregate-validation-performance-plan.md`
- Plan decision:
  - The plan is intentionally scoped to one result surface: make aggregate-heavy editable table validation use an explicit, testable interaction policy instead of accidental owner-wide keystroke validation.
  - The first closure target is allowed to be either deferred aggregate policy or a semantics-preserving incremental aggregate path, but the repo must choose one explicit baseline rather than mix undocumented heuristics.
- Next step: if runtime work starts on large-table validation optimization, anchor it to the updated `form-validation.md` constraints instead of adding undocumented hot-path shortcuts.

### 2026-04-12 (session 5)

- Added a new owner plan for the composite value-field family and validation integration scope.
- Links: `docs/plans/70-composite-value-fields-and-validation-integration-plan.md`, `docs/architecture/object-field.md`, `docs/architecture/array-field.md`, `docs/architecture/variant-field.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `docs/components/loop/design.md`
- Key decisions:
  - `loop` is treated as an already-landed structural baseline from plan 55; the new plan only owns `loop` integration coverage and any bug fixes required by the composite form scenario.
  - The plan closure target is a single feature family: `object-field`, `array-field`, `variant-field`, `detail-field`, `detail-view`, plus one comprehensive form test that exercises their interaction with the owner-based validation runtime.
  - `object-field` / `array-field` / `variant-field` inherit the parent validation owner; `detail-field` / `detail-view` use child draft owners for editable surface flows.
  - Every component must ship with focused tests, and the overall plan is not considered complete without one dedicated comprehensive form test that reaches complex validation paths such as branch cleanup, draft confirm gating, parent revalidation, array remap, repeated rendering, and at least one async or debounced validation path.
  - The implementation is expected to introduce a shared value-adaptation helper rather than duplicating `transformInAction` / `transformOutAction` / `validateValueAction` orchestration across five new renderers.
- Next step: execute plan 70 starting from the shared substrate and owner-boundary freeze, then land each component with focused tests before building the comprehensive validation scenario.

### 2026-04-12 (session 4)

- Completed remaining gaps in plan 68 and plan 69 identified by the session 3 closure audit.
- Changes landed:
  - `packages/flux-runtime/src/form-runtime-lifecycle.ts` (new): `isOwnerCompatible`, `buildRuleIdentitySet`, `computeRefreshErrorRetention`. Exported `isOwnerCompatible` and `OwnerBoundaryKind` from package index.
  - `packages/flux-runtime/src/form-runtime.ts`:
    - `applyExternalErrors` now rebuilds `store.errors` atomically from `externalErrors` side-map via `rebuildStoreErrorsFromExternal` (no message-equality guessing — strips all `sourceKind === 'external'` from base then re-injects from map).
    - `setValue` / `setValues` call `clearExternalErrorsForPath` (sourceId-scoped, ancestor-chain-aware) then republish via `rebuildStoreErrorsFromExternal` when anything changed.
    - `submit()` calls `supersedeLowerPriorityWork()` before `validateForm()` to bump all `validationRuns` and cancel all debounces.
    - `computeCanSubmit()` blocks on active `summary-gate` child contracts.
    - `submit()` notifies active `recurse-submit` child contracts after own-form validation passes.
    - `refreshCompiledModel` now uses `computeRefreshErrorRetention` to selectively retain errors for unchanged rule-identity paths and clear errors for removed/changed paths.
  - New focused test file: `packages/flux-runtime/src/__tests__/plan-68-69-remaining-behaviors.test.ts`.
- Key decisions:
  - `isOwnerCompatible` requires explicit `ownerSlotId` and `boundaryKind` parameters; `ownerId` and `rootPath` come from the compiled model. React key-based remount for incompatible changes remains a React layer concern.
  - `ruleIdentitySet` uses `${ruleId}:${ruleKind}` encoding per analysis doc §7.1.
  - `recurse-submit` calls `unregister()` on the contract as a notification signal; full child-form submit orchestration is a higher-level integration concern.
- Plans closed: `docs/plans/68-owner-based-validation-runtime-alignment-plan.md`, `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md`.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (331 flux-runtime tests).

### 2026-04-12 (session 3)

- Re-ran an explicit live-repo closure audit for `docs/plans/68-owner-based-validation-runtime-alignment-plan.md` and `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md` after noticing plan-completion claims were ahead of shipped behavior.
- Links: `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/plans/68-owner-based-validation-runtime-alignment-plan.md`, `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md`
- Key findings:
  - Plan 68 had real contract-surface landing, but still lacked full reason-aware execution semantics, full submit supersession behavior, source-scoped external-error clear semantics, stronger `applyChangesAndRevalidate` semantics, and child-contract behavior beyond registration plumbing.
  - Plan 69 had real lifecycle scaffolding (`modelGeneration`, `lifecycleState`, `refreshCompiledModel`, `dispose`, stale-generation async isolation), but did not yet implement `isOwnerCompatible`, refresh-vs-recreate branching, React integration contract, or full refresh/rebind semantics.
- Corrections applied:
  - Both plans were downgraded from `completed` to `partially completed`.
  - `docs/plans/00-plan-authoring-and-execution-guide.md` was tightened so closure audit now explicitly requires checking live behavior, not just interface/type presence.
  - Added explicit guidance that independent reviewer / subagent closure audits are preferred, and that interface-vs-semantics mismatch must force plan status rollback.
- Key decision: future plan closure must distinguish “contract surface landed” from “runtime semantics landed”; the former is not sufficient for `completed`.
- Next step: continue execution against the remaining open items in plans 68 and 69 instead of treating them as closed.

### 2026-04-12 (session 2)

- Completed plan 68 (Owner-Based Validation Runtime Alignment) and plan 69 (Dynamic Schema Validation Owner Lifecycle).
- Key changes landed:
  - `packages/flux-core/src/types/validation.ts`: added `ValidationOwnerLifecycleState`, `ValidationReason`, `FieldRegistrationHandle`, `ApplyExternalErrorsInput`, `ScopeValidationStateSnapshot`, `ChildValidationMode`, `ChildValidationContract`, `ChildValidationContractRegistration`; extended `ValidationError.sourceKind` with `row`, `scope-root`, `external`, `runtime-overlay`, `runtime-opaque`.
  - `packages/flux-core/src/types/runtime.ts`: added `ValidationScopeRuntime` base interface; `FormRuntime` extends it; added `canSubmit`, `allTouched`, `ApplyScopeChangesInput`.
  - `packages/flux-runtime/src/form-runtime-types.ts`: `RegisteredFieldEntry`, `ExternalErrorEntry`; `ManagedFormRuntimeSharedState` extended with `runtimeFieldRegistrations` (registrationId-keyed), `pathToRegistrationId`, `lifecycleState`, `modelGeneration`, `externalErrors`, `childContracts`.
  - `packages/flux-runtime/src/form-runtime-registration.ts`: rewritten to registrationId-based identity.
  - `packages/flux-runtime/src/form-runtime.ts`: full implementation of `lifecycleState`, `modelGeneration`, `getScopeState`, `getScopeRootErrors`, `isPathOwned`, `getFieldState`, `applyExternalErrors`, `applyChangesAndRevalidate`, `refreshCompiledModel`, `dispose`, `registerChildContract`, `unregisterChildContract`, `registerField` → `FieldRegistrationHandle`, `updateFieldRegistration`, `canSubmit`, `allTouched`, `validateAt`, `validateAll`.
  - `packages/flux-react/src/test-support.tsx` and all `flux-renderers-form` call sites updated for `FieldRegistrationHandle.unregister`.
  - New focused test file: `packages/flux-runtime/src/__tests__/owner-based-validation-contracts.test.ts` (313 tests total, all passing).
- Key decisions:
  - `registerField` duplicate-path rejection returns `accepted: false` with empty `registrationId`.
  - External errors use `sourceId`-scoped replacement; clear-on-write fires on `setValue` for the affected path.
  - `refreshCompiledModel` increments `modelGeneration`, clears stale registrations and debounces, transitions `refreshing` → `active`.
  - `dispose` transitions to `disposed` and permanently gates all subsequent validation/registration calls.
  - Real `validateRule` from `validation-runtime.ts` used in tests that exercise required-error output paths.
- Plans closed: `docs/plans/68-owner-based-validation-runtime-alignment-plan.md`, `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md`.
- Verification: `pnpm typecheck` ✓, `pnpm build` ✓, `pnpm lint` ✓, `pnpm test` ✓ (313 flux-runtime tests pass; all packages pass individually).

### 2026-04-12

- Stabilized the owner-based validation documentation baseline after multiple independent review passes.
- Links: `docs/architecture/form-validation.md`, `docs/references/form-validation-execution-details.md`, `docs/analysis/2026-04-11-dynamic-schema-hot-reload-and-validation-owner-lifecycle.md`
- Key decisions:
  - Added normative owner-local entry arbitration: `submit` / `commit` supersede lower-priority validation work and bypass change-oriented debounce.
  - Confirmed rule execution is collect-all rather than implicit short-circuit.
  - Clarified owner-local dependency cycles are legal and closure expansion must be cycle-safe.
  - Added external error injection as a first-class owner-local contract, including atomic owner-summary publication and source-scoped clear-on-write behavior.
  - Tightened registration semantics around instance-addressed `registrationId`, accepted/rejected registration handles, and generation-safe stale update rejection.
  - Refined dynamic schema lifecycle design with `ownerSlotId`, compatibility checks, refresh/recreate split, and React remount guidance.
- Added execution-ready plans:
  - `docs/plans/68-owner-based-validation-runtime-alignment-plan.md`
  - `docs/plans/69-dynamic-schema-validation-owner-lifecycle-implementation-plan.md`
- Plan split decision:
  - Plan 68 now owns steady-state validation runtime alignment only.
  - Plan 69 separately owns dynamic schema refresh / owner recreation lifecycle.
- Validation process note:
  - Architecture, reference, analysis, and plan docs were iteratively reviewed by independent subagents until no major findings remained.
- Next step: execute plan 68 first for steady-state runtime alignment, then execute plan 69 for dynamic schema lifecycle landing.
