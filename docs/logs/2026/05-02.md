# Development Log — 2026-05-02

### 2026-05-03 (Morning) — Test module-top leak queue reduced from 14 to 9

- Replaced module-top mutable holders with a single stable mock-state object in `packages/word-editor-renderers/src/__tests__/word-editor-page.test.tsx`, covering shortcut wiring, captured canvas props, and dataset-store state.
- Localized the remaining form-owned probe state in `packages/flux-renderers-form-advanced/src/__tests__/bug-dual-state.test.tsx` and replaced the `array-field` mount counter in `packages/flux-renderers-form-advanced/src/composite-field/array-field.test.tsx` with stable test-owned state.
- Verification: `pnpm --filter @nop-chaos/word-editor-renderers exec vitest run src/__tests__/word-editor-page.test.tsx --maxWorkers=1` and `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/bug-dual-state.test.tsx src/composite-field/array-field.test.tsx --maxWorkers=1` pass after the local helper fix.
- Audit baseline: `pnpm check:audit-test-global-leaks` and `pnpm check:audit-suspects` now report `9` remaining matches, all in larger flow-designer / flux-react files.

### 2026-05-03 (After Midnight) — Test module-top leak queue reduced from 16 to 14

- Continued the same holder-object cleanup pattern in `packages/flux-renderers-data/src/__tests__/crud-renderer-state.unit.test.tsx` so mocked registry and scope state no longer depend on module-top mutable `let` bindings.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/crud-renderer-state.unit.test.tsx --maxWorkers=1` passes.
- Audit baseline: `pnpm check:audit-test-global-leaks` and `pnpm check:audit-suspects` now report `14` remaining matches, still all in the single `test-module-top-let` bucket.

### 2026-05-02 (Late Night) — Test module-top leak queue reduced from 19 to 16

- Continued the remaining `test-module-top-let` cleanup with small isolated test files instead of changing the scanner.
- Replaced module-top mutable holders with stable object wrappers in `packages/word-editor-core/src/__tests__/document-io.test.ts`, `packages/flux-renderers-data/src/__tests__/chart-renderer.unit.test.tsx`, and `packages/flux-renderers-data/src/__tests__/use-table-controls.test.tsx`.
- Verification: focused runs now pass for the touched files: `pnpm --filter @nop-chaos/word-editor-core exec vitest run src/__tests__/document-io.test.ts --maxWorkers=1` and `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/chart-renderer.unit.test.tsx src/__tests__/use-table-controls.test.tsx --maxWorkers=1`.
- Audit baseline: `pnpm check:audit-test-global-leaks` and `pnpm check:audit-suspects` now report `16` remaining matches, all in the single `test-module-top-let` bucket.

### 2026-05-02 (Late Night) — Plans 183/184/185 doc baseline sync and closure prep

- Re-audited the live code for `docs/plans/183-renderer-props-and-host-neutral-typing-convergence-plan.md`, `184-reactive-hot-path-precision-and-notification-scaling-plan.md`, and `185-large-file-hotspot-split-plan.md` against the owner docs before marking any additional closure state.
- Synced `docs/architecture/renderer-runtime.md`, `docs/architecture/field-binding-and-renderer-contract.md`, and `docs/references/renderer-interfaces.md` to the live renderer typing baseline: `RendererComponentProps<S, P>` now carries a typed resolved prop bag, core stays host-neutral, and React-only `reactComponent` ownership remains in `@nop-chaos/flux-react`.
- Synced `docs/architecture/form-validation.md` and `docs/architecture/dependency-tracking.md` to the live reactive-hot-path baseline: dynamic required indicators now subscribe via explicit dependency paths, `useCurrentFormState(..., { paths })` is the multi-path hook surface, and deep-path dependency matching no longer uses the old pairwise overlap scan.
- Synced `docs/architecture/flux-runtime-module-boundaries.md`, `docs/components/spreadsheet-page/design.md`, and `docs/references/refactoring-guidelines.md` to the live large-file split baseline: `api-data-source-controller.ts` is now a thin coordinator over `*-types/state/runtime` modules, `field-utils.tsx` is a pure barrel, and `spreadsheet-toolbar.tsx` is a thin composition shell over `spreadsheet-toolbar/` submodules.
- Key decision: close these plans only against the landed contract/module boundaries that are now explicitly documented, not against earlier draft expectations such as pushing React render-result types back into `flux-core`.

### 2026-05-02 (Late Night) — Audit suspect queue reduced to test module-top state only

- Removed the last non-test `reactive-render-read` suspect by deleting unused helper exports from `packages/flux-renderers-form/src/field-utils/field-reading.tsx`; the helper had no production callers and only test-only coverage.
- Consolidated repeated form-package DOM polyfills into `packages/flux-renderers-form/src/test-dom-polyfills.ts`, then reused existing shared support imports in `flux-renderers-form-advanced` so scattered `PointerEvent` global patches no longer dominate the audit queue.
- Tightened `scripts/audit/rules.mjs` so `test-global-patch` ignores same-file cleaned-up `window.__NOP_DEBUGGER__` setup, and synced `docs/references/audit-rule-source-discovery-candidates.md` to describe that heuristic.
- Verification: `pnpm check:audit-reactive-render-reads` now reports no suspects, `pnpm check:audit-test-global-leaks` and `pnpm check:audit-suspects` now report only the remaining `test-module-top-let` bucket (`20` matches).
- Verification note: while rechecking nearby form-advanced tests, the package-local run was still noisy from an unrelated pre-existing failure in `src/__tests__/form-double-edit-regression.test.tsx` (`useCompositeChildFieldState is not a function`), outside this scanner-tightening slice.

### 2026-05-02 (Late Night) — Plan 186 closure recorded

- Re-read `docs/plans/186-detail-and-variant-async-sequencing-safety-plan.md` against `docs/plans/00-plan-authoring-and-execution-guide.md` before final closure wording.
- Adjusted the Phase 4 / validation wording so the completed plan no longer implies repo-wide `pnpm typecheck` / `build` / `lint` / `test` all passed; the plan now records the honest baseline instead: focused in-scope verification passed, broader package/workspace failures were attempted and explicitly documented as unrelated debt outside Plan 186 ownership.
- Recorded the second independent closure audit (`ses_216c4f367ffeJkqrMZz8ejGm9u`) as the final closure evidence: post-fix review found no remaining plan-owned sequencing gaps in `detail-field`, `detail-view`, or `variant-field`.
- Key decision: keep Plan 186 closed because its in-scope contract, code, tests, docs, and independent closure audit are complete; track the remaining non-green workspace verification separately instead of silently treating it as plan-owned work.

### 2026-05-02 (Late Night) — Plan 186 closure-audit follow-up

- Ran an independent closure audit for `docs/plans/186-detail-and-variant-async-sequencing-safety-plan.md` in a separate agent session (`ses_216df323bffe6FJCQIfATCcbtb`).
- The audit confirmed `variant-field` Phase 3 was honestly landed, but found one remaining in-scope detail gap: opening a newer `detail-field` / `detail-view` draft did not invalidate an older in-flight confirm token, so a stale `validateValueAction` / `transformOutAction` completion could still close or commit over the newer session.
- Fixed that gap in `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.ts` by invalidating `confirmSequencer` and clearing `confirming` inside `openDraft(...)` before activating the newer draft session.
- Added focused regressions proving stale confirm completions are now dropped after reopen supersession in both owner flavors: `packages/flux-renderers-form-advanced/src/detail-view/detail-field-commit.test.tsx` and `detail-view-transform.test.tsx`; also extended `detail-draft-controller.test.tsx` to prove reopening a draft clears inherited confirming state.
- Re-ran package-local detail verification after the audit fix: `detail-view.test.tsx`, `detail-field.test.tsx`, and `detail-draft-controller.test.tsx` now pass together (`41` tests with `--maxWorkers=1`).
- Key decision: keep Plan 186 open only for a fresh post-fix independent closure audit and the still-unrelated full-workspace verification debt. The audit-found live in-scope detail supersession gap is now closed in code and focused tests.

### 2026-05-02 (Late Night) — Detail async sequencing follow-up

- Continued `docs/plans/186-detail-and-variant-async-sequencing-safety-plan.md` into the remaining `detail-field` / `detail-view` scope instead of stopping at the earlier variant-only slice.
- Re-audited the live detail owner code and confirmed request-token sequencing was already present for async open/confirm flows in `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx` and `detail-field.tsx`; the real remaining defect was shared controller state in `detail-draft-controller.ts`.
- Fixed the live bug by making `closeDraft()` clear `confirming` immediately in `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.ts`, so a successful or superseded confirm no longer leaves the next detail session stuck without a usable `Confirm` action.
- Added focused regression coverage in `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.test.tsx` for controller reset, plus new stale-open sequencing coverage in `detail-field-unmount.test.tsx` and `detail-view-transform.test.tsx` proving older async `transformInAction` completions are dropped when a newer open request wins.
- Updated `docs/architecture/value-adaptation-and-detail-field.md` to record the current detail baseline: open/confirm flows are latest-request-wins, stale completions are dropped, stale draft forms are disposed, and closing a detail session clears owner-local confirming state for the next session.
- Verification: package-local detail suites now pass (`38` tests with `--maxWorkers=1`), package-local variant suites still pass (`48` tests), `@nop-chaos/flux-renderers-form-advanced` lint passes, and `pnpm check:audit-async-failure-paths` remains clean. Full package/workspace `typecheck` / `build` / `test` are still blocked by pre-existing unrelated baseline issues outside this plan slice.

### 2026-05-02 (Late Night) — Variant-field verification follow-up and runtime async failure path closure

- Fixed the new `variant-field` detection regression test import gap in `packages/flux-renderers-form-advanced/src/variant-field/variant-field-detection.test.tsx` so the stale-detect coverage now imports `fireEvent` and `screen` explicitly.
- Stabilized the landed `variant-field` sequencing implementation in `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` by memoizing `handleVariantSwitch`, which preserves the latest-request-wins behavior without tripping `react-hooks/exhaustive-deps` in the wrapped fire-and-forget event handler.
- Closed the last live `void-promise-no-catch` audit hit in `packages/flux-runtime/src/form-runtime-values.ts` by adding an explicit local rejection handler around fire-and-forget `revalidateDependents(...)` calls; dependent revalidation failures now emit a scoped runtime warning instead of relying on implicit unhandled rejection behavior.
- Added regression coverage in `packages/flux-runtime/src/__tests__/form-runtime-values.test.ts` proving rejected dependent revalidation is reported locally and does not rethrow through `executeSetValues(...)`.
- Verification: `pnpm check:audit-async-failure-paths` is now clean, package-local `variant-field` Vitest suites pass (`48` tests), and `pnpm check:audit-suspects` now reports only the remaining test-leak buckets (`test-module-top-let`, `test-global-patch`).
- Package verification for touched owners is mixed but honest: `@nop-chaos/flux-runtime` `typecheck`, `build`, targeted `test`, and the new async audit check pass; `@nop-chaos/flux-runtime` `lint` is still blocked by pre-existing unused-import test lint errors, and `@nop-chaos/flux-renderers-form-advanced` `typecheck` / `build` remain blocked by pre-existing cross-package renderer typing failures outside this sequencing slice.

### 2026-05-02 (Late Night) — Variant-field async sequencing baseline

- Executed the `variant-field` slice of `docs/plans/186-detail-and-variant-async-sequencing-safety-plan.md` without reopening the broader detail-view scope.
- `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` now uses latest-request-wins sequencing for both `detectVariantAction` and async target-variant `transformInAction` switching, so superseded completions can no longer overwrite the current active variant or parent-owned field value.
- Added explicit local failure handling for the same fire-and-forget paths so detect/switch failures leave a visible scoped warning instead of relying on implicit unhandled rejection behavior.
- Added focused regression coverage in `packages/flux-renderers-form-advanced/src/variant-field/variant-field-detection.test.tsx` and `variant-field-transform.test.tsx` proving stale detect results and stale migration completions are dropped when a newer request wins.
- Updated `docs/architecture/variant-field.md` to record latest-request-wins sequencing as part of the current live baseline for detect and switch migration, while leaving the broader future validation/transform ordering unchanged.

### 2026-05-02 (Late Night) — Async fire-and-forget scanner tightening

- Re-audited the remaining `void-promise-no-catch` bucket and found the dominant residual noise came from three families: JSX event-handler wrappers, runtime self-scheduling loops (`runRequest` / `runReaction`), and owner methods whose local implementation already contains explicit `try/catch/finally` failure strategy.
- Tightened `scripts/audit/rules.mjs` so the async scanner now ignores those documented-safe families by path and local context instead of continuing to report them as generic swallowed-failure suspects.
- The new heuristic deliberately keeps the scanner read-only and conservative: it still reports genuine unknown fire-and-forget callsites, but it no longer treats event-wrapper `void` usage or runtime-owned retry/cascade loops as equivalent to silent failure paths.
- Synced `docs/references/audit-rule-source-discovery-candidates.md` to record the same first-pass async exclusions.

### 2026-05-02 (Late Night) — Successor-plan portfolio for Plans 164, 173, and 182

- Re-audited the closed-plan follow-up notes in `docs/plans/164-adversarial-review-uncovered-findings-remediation-plan.md`, `docs/plans/173-adversarial-review-2-critical-findings-remediation-plan.md`, and `docs/plans/182-deep-audit-full-3-mechanical-fixes-plan.md` against the live repo and the plan-guide closure rules.
- Drafted four concrete successor plans for the still-live owner surfaces instead of reopening another omnibus residual plan: `docs/plans/183-renderer-props-and-host-neutral-typing-convergence-plan.md`, `docs/plans/184-reactive-hot-path-precision-and-notification-scaling-plan.md`, `docs/plans/185-large-file-hotspot-split-plan.md`, and `docs/plans/186-detail-and-variant-async-sequencing-safety-plan.md`.
- Rewired the old plan `Follow-up` sections to those concrete files so the retained work is now assigned by owner surface rather than left as generic "needs a separate plan" notes.
- Key decision: keep the residuals split into four owner surfaces only. Do not create a placeholder omnibus follow-up, and do not invent a dedicated `stableStringify` plan until api-cache work is active enough to justify its own owner.

### 2026-05-02 (Late Night) — Reactive-read and wrapper-exception scanner tightening

- Re-audited the remaining `reactive-render-read` and `fieldframe-bypass` scanner hits against live code and docs before changing production code.
- Outcome: the current `reactive-render-read` matches were dominated by one-shot helper/effect paths (`SchemaRenderer` effect synchronization, form lifecycle helper closures, field registration callbacks, playground fetcher code, and test-support helpers), not actual render-time subscription bugs.
- Tightened `scripts/audit/rules.mjs` so the reactive-read scanner now ignores known one-shot helper paths, registration callbacks, runtime fetcher code, and similar non-render contexts instead of continuing to present them as high-signal render defects.
- Confirmed `variant-field` is a documented non-default wrapper path in `docs/architecture/variant-field.md`, then excluded `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` from the `fieldframe-bypass` first-pass scanner so the queue tracks undocumented bypasses rather than already-documented exceptions.
- Updated `scripts/audit/shared.mjs` to pass `relativePath` into rule filters, enabling path-aware heuristics without duplicating scan infrastructure.
- Synced `docs/references/audit-rule-source-discovery-candidates.md` to record that first-pass scanners now exclude known one-shot helper contexts and documented wrapper exceptions.

### 2026-05-02 (Late Night) — Missing renderer marker scanner

- Added a first focused styling-contract scanner at `scripts/audit/find-missing-renderer-markers.mjs` and wired it through `scripts/audit/rules.mjs` plus `package.json` as `pnpm check:audit-missing-renderer-markers`.
- The first heuristic is intentionally conservative: it scans `packages/flux-renderers-*/src/*.tsx` renderer component files and reports files that look like real renderer implementations but contain no `nop-*` marker contract anywhere in the source.
- Included the new rule in the aggregate `scripts/audit/discover-audit-suspects.mjs` path so repository-wide suspect discovery now covers markerless renderer candidates in addition to render-time imperative reads, async failure-path gaps, FieldFrame bypasses, and test leakage patterns.
- Synced `docs/references/audit-rule-source-discovery-candidates.md` to document the live marker scanner path under the `single-owner-styling-defaults-and-marker-contracts` discovery section.
- Key decision: keep this scanner file-level and low-precision for now. A future iteration can parse root JSX/class tokens, but the immediate value is identifying renderer files that appear to have omitted the marker contract entirely.

### 2026-05-02 (Late Night) — Audit scanner split into focused entrypoints

- Split the original all-in-one audit suspect scanner into reusable focused scripts under `scripts/audit/` while keeping `scripts/audit/discover-audit-suspects.mjs` as the aggregate entrypoint used by `pnpm check:audit-suspects`.
- Added shared scan infrastructure in `scripts/audit/shared.mjs` and centralized rule definitions in `scripts/audit/rules.mjs` so new discovery rules can be added without re-copying traversal, printing, and filtering logic.
- Added focused entrypoints for the current highest-value families: `find-reactive-render-reads.mjs`, `find-async-without-failure-path.mjs`, `find-fieldframe-bypasses.mjs`, and `find-test-global-leaks.mjs`.
- Exposed those focused scanners through `package.json` as `check:audit-reactive-render-reads`, `check:audit-async-failure-paths`, `check:audit-fieldframe-bypasses`, and `check:audit-test-global-leaks`.
- Synced `docs/references/audit-rule-source-discovery-candidates.md` so the documented repository layout now matches the live `scripts/audit/` structure.

### 2026-05-02 (Late Night) — Audit suspect scanner false-positive reduction

- Tightened `scripts/audit/discover-audit-suspects.mjs` after the first live run surfaced predictable noise buckets that were useful for exploration but too broad for repeatable review.
- `reactive-render-read` now skips test files and ignores obvious inline callback/event-handler reads, keeping the report closer to production render-path misuse.
- `test-module-top-let` now scans only top-level mutable declarations instead of every local `let` inside test bodies, aligning the output with the actual `test-reliability-and-contract-freshness` rule intent.
- `fieldframe-bypass` now excludes the canonical owner path under `packages/flux-react/src/` and focuses on bypass candidates outside the shared wrapper owner package.
- `void-promise-no-catch` now suppresses several known validation/revalidation fire-and-forget patterns that are high-volume but low-value for the first-pass suspect queue.
- Synced `docs/references/audit-rule-source-discovery-candidates.md` to record the same first-pass false-positive guardrails so future scanner splits preserve the tighter behavior.

### 2026-05-02 (Late Night) — Audit-rule source discovery layer and first scanner

- Extended the recurring-audit docs beyond rule extraction into source-discovery guidance by adding `docs/references/audit-rule-source-discovery-candidates.md`.
- The new reference separates full automation from suspect-location discovery and maps each stable audit rule to pragmatic search strategies: `rg`, grep-style CI checks, or small AST/scripts-based scanners.
- Added the first repository-level discovery script at `scripts/audit/discover-audit-suspects.mjs` covering five high-value suspect families: render-time imperative reads, fire-and-forget async without visible local failure handling, direct `FieldFrame` usage, mutable module-top test state, and test global patching.
- Wired the scanner into workspace checks through `package.json` as `pnpm check:audit-suspects`, and included it in the aggregate `pnpm check` path so future rule-extraction work can start from a repeatable candidate report instead of a fresh manual grep pass.
- Key decision: keep the first scanner read-only and false-positive-tolerant. Its job is to produce a review queue, not to pretend every recurring audit rule is already precise enough for CI-failing enforcement.

### 2026-05-02 (Late Night) — Plan 179 container default-gap contract cleanup

- Executed `docs/plans/179-container-default-gap-contract-successor-plan.md` against the retained deep-audit residual in `packages/flux-renderers-basic/src/container.tsx`.
- Removed the last renderer-owned default gap fallback from `packages/flux-renderers-basic/src/container.tsx`: the flex-child path no longer injects `gap: var(--space-form-item-gap)` inline when the schema did not ask for an explicit `gap`.
- Moved container default spacing to a single explicit owner path in `packages/flux-react/src/default-spacing.css` by applying the base `container-body` flex/gap baseline to both bare and semantic flex-child paths; renderer code now only contributes explicit semantic overrides.
- Added focused regression coverage in `packages/flux-renderers-basic/src/__tests__/slot-classname.test.tsx` proving both the bare path and the flex-child path without explicit `gap` avoid inline default-gap injection.
- Synced the active styling docs to the final baseline: `docs/architecture/styling-system.md` and `docs/architecture/container-spacing-design.md` now describe container default spacing as CSS-owned even when `data-flex` marks the semantic path.
- Key decision: allow the CSS base layer to supply the shared container `display: flex` / column / gap baseline on both paths, and treat semantic classes or inline styles as the only renderer-owned overrides. This removes the hidden renderer-code default without reopening the broader spacing-token plan.

### 2026-05-02 (Late Night) — Plan 178 validation-owner residual closure

- Executed and closed `docs/plans/178-validation-owner-bootstrap-and-hidden-participation-plan.md` against the two retained deep-audit residuals: dishonest page-root bootstrap publication and form-only hidden-field participation in non-form owners.
- Page-root owner bootstrap: `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-runtime-owner.ts`, `packages/flux-runtime/src/runtime-owned-factories.ts`, and `packages/flux-react/src/schema-renderer.tsx` now keep page-root validation owners in `bootstrapping` with `ready: false` until `refreshCompiledModel(...)` attaches the compiled root validation plan; only then does the owner publish `active` semantics.
- Key implementation decision: keep the lifecycle fix targeted to the page-root creation path instead of globally forcing every no-model form or validation owner into `bootstrapping`.
- Generic hidden participation: `packages/flux-core/src/types/runtime.ts` now exposes `notifyFieldHidden()` on `ValidationScopeRuntime`, and `packages/flux-react/src/node-renderer.tsx` plus `packages/flux-renderers-form/src/field-utils.tsx` now publish hidden-field participation through the generic owner contract instead of `FormRuntime` casts.
- Regression coverage now proves both behaviors in live code: `packages/flux-react/src/__tests__/schema-renderer-validation-owner-boundary.test.tsx`, `packages/flux-runtime/src/__tests__/hidden-field-policy.test.ts`, and `packages/flux-runtime/src/__tests__/owner-based-validation-contracts.test.ts` cover bootstrap publication, post-attach activation, and non-form hidden participation.
- Docs updated to the final baseline: `docs/architecture/form-validation.md` now records page-root bootstrap semantics and generic hidden participation, and `docs/references/form-validation-runtime-types.md` now documents `notifyFieldHidden()` on `ValidationScopeRuntime` plus the `ready`/lifecycle relationship.
- Independent closure audit passed in a fresh subagent session (`ses_217a287f7ffeAUbNGkuvYtjMFg`): no remaining Plan-178 in-scope residuals were found, and the audit explicitly concluded the plan could close from an in-scope behavior perspective.
- Repo-wide verification is green: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass.
- Incidental workspace verification fixes outside Plan 178 scope were landed to restore the green baseline: `packages/word-editor-core/tsconfig.build.json` now points `@hufe921/canvas-editor` back at the shared root declaration file, and `packages/nop-debugger/src/automation.test.ts` now imports `afterEach` so the package build/test pipeline compiles again.

### 2026-05-02 (Late Night) — Plan 175 pattern extraction and reusable audit rule

- Re-audited the Plan 175 findings after closure to check whether they were isolated fixes or repeated repository-level patterns.
- Confirmed four recurring classes of failure behind the plan work: instance-vs-module async ownership drift, host bridge semantic drift, primary-state replacement without derived-state refresh, and persisted/autosave truth diverging from live runtime truth.
- Follow-up global scan across `docs/plans/`, `docs/bugs/`, and `docs/analysis/` confirmed the same pattern family already appears outside Plan 175 as well, including Plan 146 host-projection timing drift, Plan 166/180 stale-result and cancellation ownership work, Plan 170 async stale-write protection, bug 32 bridge snapshot stability, and bug 38 report preview ownership/cancellation.
- Added `docs/references/audit-rules/owner-bridge-async-state-coherence.md` as a reusable review rule for designer/editor/host-bridge work.
- Added `docs/references/cross-plan-recurring-anomaly-patterns.md` to record broader recurring anomaly families beyond Plan 175, including reactive subscription/snapshot instability, dishonest owner lifecycle publication, split styling-default ownership, `wrap: true` FieldFrame interaction traps, and vocabulary/i18n drift.
- Expanded the cross-plan anomaly map again after a wider scan of `docs/plans/`, `docs/bugs/`, and `docs/analysis/`: newly confirmed recurring families now also include reactive-read vs imperative-read misuse, snapshot/change-token publication gaps, async/timer cleanup after dispose, wrapper bypass of shared renderer contracts, and “false-positive-friendly” UI diagnostics where default values hide the real bug.
- Promoted five high-frequency recurring families into reusable audit rules under `docs/references/audit-rules/`: reactive subscription / derived snapshot stability, owner lifecycle / generic owner contracts, FieldFrame `wrap: true` interaction semantics, reactive-read vs imperative-read misuse, and snapshot/change-token publication.
- Continued beyond representative plans into a full active-plan sweep under `docs/plans/`, which surfaced additional repeated families worth tracking: swallowed exception / async diagnosability gaps, test reliability and stale contract assertions, docs/logs/code landed-claim drift, and local shadow-contract duplication.
- Promoted another five recurring families into reusable audit rules: async error diagnosability / swallowed failures, docs-log-code landed-claim adjudication, test reliability / contract freshness, single-owner styling defaults / marker contracts, and cleanup / disposal boundaries.
- Promoted three more recurring families into reusable audit rules: wrapper bypass of shared renderer contracts, vocabulary / cross-shell contract drift, and shadow types / duplicate contract surfaces.
- Promoted the last remaining high-frequency candidate from this scan into a reusable audit rule: false-positive-friendly UI diagnostics, covering contrasting-case checks and producer-boundary tracing when fallback/default behavior can mask the real defect.
- Added `docs/references/audit-rule-automation-candidates.md` to map the extracted audit rules onto realistic enforcement layers (ESLint, grep/CI, focused tests, or mostly-manual review), and added a lightweight `docs/references/README.md` entrypoint so future work can start from the consolidated references instead of rediscovering the rule set.
- Indexed the new rule in `docs/references/audit-rules/README.md` and added a condensed cross-link entry to `docs/references/architecture-guardrails-from-bugs.md` so future reviews can treat this as a stable guardrail instead of a one-off Plan 175 postmortem.
- Key documentation decision: this pattern belongs primarily in `docs/references/audit-rules/` when used as a fixed review rule, while architecture docs remain the source of truth for subsystem-specific semantics.

### 2026-05-02 (Late Night) — Plan 175 closure and workspace verification baseline restored

- Closed `docs/plans/175-review-4-findings-remediation-plan.md` after restoring a fully green workspace baseline instead of stopping at focused plan-owned regressions.
- Fixed a follow-up `flux-renderers-form` build break by adding the missing `FormRuntime` type import in `packages/flux-renderers-form/src/field-utils.tsx`; targeted package `build` and `test` are green again.
- Stabilized repo-wide `pnpm test` under Windows memory pressure by changing the root `test` script in `package.json` to `turbo run test --concurrency=2`, matching the passing end-to-end verification path and eliminating the earlier Turbo/Vitest OOM failures.
- Fixed an unrelated workspace build misconfiguration in `packages/word-editor-core/tsconfig.build.json`: removed the package-local `@hufe921/canvas-editor` path override that pointed at a nonexistent declaration file so the package now resolves the shared root `types/hufe921__canvas-editor.d.ts` baseline again.
- Final verification baseline is green: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass.
- Key decision: keep the two extra verification fixes with this closure pass because they were the remaining blockers preventing honest repo-wide validation for Plan 175; the plan itself still owns no further code work.

### 2026-05-02 (Late Night) — Plans 180 and 181 execution

- Executed `docs/plans/180-report-preview-cancellation-and-stale-result-plan.md` and `docs/plans/181-word-editor-dataset-vocabulary-convergence-plan.md` against the live residuals retained after Plan 176.
- Plan 180: `packages/report-designer-core/src/core.ts`, `core-dispatch.ts`, `runtime/preview-commands.ts`, and `adapters.ts` now give preview requests single-owner sequencing with `AbortController`. New preview requests abort the previous in-flight run, `report-designer:stopPreview` aborts the active adapter call, and stale completions are ignored unless they still own the active request.
- Plan 180 tests/docs: added focused preview regression coverage in `packages/report-designer-core/src/__tests__/designer-core.test.ts` for superseded preview requests and explicit stop/cancel behavior; updated `docs/architecture/report-designer/design.md` to record the live preview cancellation baseline and adapter `signal` contract.
- Plan 181: converged the canonical word-editor public vocabulary to `Dataset*` in `packages/word-editor-core/src/dataset-model.ts` and package exports, while keeping `DataSet*` as explicit deprecated compatibility aliases instead of silent co-equal naming.
- Plan 181 consumer alignment: updated in-scope consumers in `packages/word-editor-core/src/document-io.ts`, `packages/word-editor-core/src/dataset-store.ts`, `packages/word-editor-renderers/src/types.ts`, and `packages/word-editor-renderers/src/word-editor-page.tsx` to the canonical `Dataset*` vocabulary.
- Plan 181 tests/docs: refreshed dataset model/document-io tests to cover both the canonical exports and the compatibility aliases; aligned `docs/architecture/word-editor/design.md` and `docs/components/word-editor-page/design.md` to the same `Dataset` baseline.

### 2026-05-02 — Plan 182 Deep Audit Full-3 Mechanical Fixes completed

- Executed `docs/plans/182-deep-audit-full-3-mechanical-fixes-plan.md` — mechanical fixes from the full-3 deep audit.
- **Phase 1 — CRUD i18n**: Added `total`, `previous`, `next` pagination keys to zh-CN/en-US locales. Replaced hardcoded English text in `crud-renderer-toolbar.tsx` with `t()` calls. Fixed type error: `pagination.totalPages` doesn't exist — computed from `Math.ceil(summary.total / pagination.pageSize)`.
- **Phase 2 — Marker classes + cn()**: Added `nop-*` marker class and `cn()` to 7 form-advanced renderers: array-editor, key-value, tag-list, array-field, object-field, detail-field, condition-builder.
- **Phase 3 — Void promise .catch()**: Skipped after review. All 3 flagged `void` calls target async functions with comprehensive internal try/catch/finally blocks — false positive.
- **Phase 4 — Flex/startTransition/deps**: Moved `display: flex` from inline Tailwind class to CSS base layer `.nop-flex { display: flex }` in `default-spacing.css`. Removed dead `startTransition(() => {})` in spreadsheet use-sheet-commands.ts. Removed unused `@nop-chaos/flux-compiler` dependency from flux-renderers-form and flux-renderers-data (not in devDependencies — completely unused).
- Verification: typecheck, build, lint (pre-existing only), test all pass.
- Key decision: flux-compiler was not imported anywhere in either package — removed entirely rather than moved to devDependencies.
- Next step: follow-up plans for giant file splits, renderer type system, subscription precision, and async cancellation.

### 2026-05-02 (Late Evening) — Plan 177 doc baseline sync

- Executed the documentation-only sync in `docs/plans/177-deep-audit-doc-baseline-sync-plan.md` against the live package graph and renderer hook types.
- Updated `docs/skills/deep-audit-prompts.md` dimension-01 rule `h` so audit agents no longer misclassify the live `@nop-chaos/ui -> @nop-chaos/flux-i18n` dependency and instead focus on undocumented private coupling, internal subpath imports, and inappropriate runtime-boundary leakage.
- Removed stale removed renderer API wording from active owner docs:
  - `docs/architecture/renderer-runtime.md` no longer documents `RenderRegionHandle.instantiate()` or `RenderFragmentOptions.data`, and now consistently describes fragment child-scope creation through `render({ bindings })`
  - `docs/architecture/flux-core.md` no longer describes the pipeline as `RendererRuntime.instantiate(...)`
  - `docs/architecture/action-scope-and-imports.md` and `docs/architecture/scope-ownership-and-isolation.md` now use the same `bindings` baseline for fragment child-scope creation
- Key decision: widen Plan 177 slightly to include the two extra active architecture docs that still repeated the removed fragment `data` wording, so the doc-sync closure is honest instead of leaving hidden owner-doc drift behind.
- Verification: re-grepped the in-scope Plan 177 files after the edits; no remaining `instantiate()` / fragment `data` contract wording remains in those active docs.

### 2026-05-02 (Late Evening, closure follow-up) — Plan 176 owner-assignment closure

- Re-read `docs/plans/176-deep-audit-residual-owner-assignment-plan.md` against the live successor set and the plan guide, then closed its remaining bookkeeping placeholders.
- Updated Plan 176 from `proposed` to `completed`, marked all three phases complete, checked the validation checklist, and recorded the three review rounds that shaped the final owner map.
- Closure baseline confirmed again from live docs: Plan 167 owns the three retained `>700` test-file splits, and successor Plans `177`, `178`, `179`, `180`, and `181` each own exactly one remaining retained non-test residual surface.
- Key decision: keep Plan 176 narrow as a completed owner-assignment portfolio only; implementation remains with the successor plans instead of reopening another omnibus remediation plan.
- Next step: begin execution with the successor plans in priority order; no further structural rewrite is needed for the residual owner map.

### 2026-05-02 — Plan 174 Schema Strict Validation Mode executed

- Implemented all 5 phases of `docs/plans/174-schema-strict-validation-mode-plan.md`.
- **Phase 1 — Core infrastructure**: Created `packages/flux-core/src/strict-mode.ts` with `isStrictValidationEnabled()` / `setStrictValidationGlobal()` and `STRICT_VALIDATION_KEY`. Added `strictMode?: boolean` to `SchemaCompileValidationOptions`. Modified compiler `inspectSchemaNodeFields()` in `packages/flux-compiler/src/schema-compiler/shape-validation.ts` to check unknown properties for all renderers when strict mode is on — closed prop model renderers get `error` severity, open ones get `warning`.
- **Phase 2 — SchemaRenderer integration**: Added `strictValidation?: boolean` to `SchemaRendererProps`. `SchemaRenderer` reads strict mode via `isStrictValidationEnabled(props.strictValidation)` and passes `validation: { strictMode }` and `diagnostics: { enabled: true, continueOnError: true }` to the compiler when strict mode is on.
- **Phase 3 — Runtime state**: Added `strictMode: boolean` to `RendererRuntime` interface. `createRendererRuntime()` accepts `strictMode` input. Added `useStrictMode()` hook in `flux-react`.
- **Phase 4 — Debugger integration**: Added `strictMode: boolean` to `NopDebuggerSnapshot` and `setStrictMode()` to `NopDebuggerController`. Debugger controller reads `runtime.strictMode` on `setRuntime()`. Overview tab shows strict validation status indicator.
- **Phase 5 — Tests**: Added strict-mode tests in `packages/flux-core/src/strict-mode.test.ts` and `packages/flux-compiler/src/schema-compiler-diagnostics.test.ts`. Updated debugger test mocks. Full `pnpm test` passes (all packages).
- Key files: `packages/flux-core/src/strict-mode.ts`, `packages/flux-compiler/src/schema-compiler/shape-validation.ts`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/panel/overview-tab.tsx`.
- Plan status updated to `completed`.

### 2026-05-02 (Late Night) — Plan 175 review-4 findings remediation

- Executed `docs/plans/175-review-4-findings-remediation-plan.md` across Flow Designer, Report Designer, and Word Editor owner bridges.
- Flow Designer: replaced module-global ELK invalidation with instance-owned layout owners so one `designer-page` cleanup no longer cancels a sibling instance's in-flight layout; regression coverage added in `packages/flow-designer-renderers/src/auto-layout-guards.test.tsx` and code landed in `packages/flow-designer-core/src/elk-layout.ts`, `packages/flow-designer-renderers/src/use-designer-auto-layout.ts`.
- Report Designer: aligned dirty semantics so `designer.dirty` now reflects designer-only semantic changes while host/runtime dirty stays aggregated; `report-designer:importTemplate` now refreshes derived state immediately after document replacement. Code/tests: `packages/report-designer-renderers/src/bridge.ts`, `packages/report-designer-core/src/core-dispatch.ts`, `packages/report-designer-core/src/__tests__/adapters-and-helpers.test.ts`.
- Word Editor: moved dirty clearing to after successful host save and switched autosave/persisted projection extras to live runtime `charts` / `codes`. Code/tests: `packages/word-editor-renderers/src/word-editor-action-provider.ts`, `packages/word-editor-renderers/src/editor-canvas.tsx`, `packages/word-editor-renderers/src/__tests__/word-editor-action-provider.test.ts`, `packages/word-editor-renderers/src/__tests__/editor-canvas.test.tsx`.
- Docs updated to final behavior: `docs/architecture/flow-designer/canvas-adapters.md`, `docs/architecture/report-designer/design.md`, `docs/architecture/word-editor/design.md`.
- Verification: focused regression suites passed for all three phases; workspace `lint` passes after local cleanup. Workspace `typecheck` / `build` / `test` remain blocked by pre-existing `@nop-chaos/flux-react` build failure at `packages/flux-react/src/schema-renderer.tsx:208` (`SchemaRendererProps.strictValidation` missing), outside Plan 175 scope.
- Next step: record independent closure-audit evidence in Plan 175 and route the unrelated `flux-react` workspace baseline failure to its owning work item if repo-wide green verification is required for plan closure.

### 2026-05-02 (Late Night) — Plan 175 closure-audit follow-up

- Ran an independent closure audit for `docs/plans/175-review-4-findings-remediation-plan.md` in a fresh agent session (`ses_21a0053f1ffe6YsO3FJRRSSx4U`).
- Audit found one live-code gap: `packages/report-designer-renderers/src/host-data.ts` was still not publishing `designer.dirty` into host scope even though the bridge/docs contract had been aligned. Fixed by adding the host-scope projection and a focused regression test in `packages/report-designer-renderers/src/host-data.test.ts`.
- Re-ran focused report-designer renderer verification after the audit fix: `bridge.test.ts`, `host-data.test.ts`, and `renderers.integration.test.tsx` all pass.
- Updated baseline note from the first log entry: workspace `pnpm typecheck` and `pnpm build` now pass; the remaining plan-closure blocker is repo-wide `pnpm test`, which still has unrelated red suites outside Plan 175 ownership.
- Key decision: keep Plan 175 `in progress` instead of `completed` because independent closure audit is now satisfied, but the plan validation checklist still intentionally leaves `pnpm test` unchecked until the workspace baseline is green or explicitly re-scoped.

### 2026-05-02 (Late Night) — Post-Plan-175 workspace verification follow-up

- Continued after Plan 175 execution to reduce repo-wide verification debt instead of stopping at the plan boundary.
- Fixed `packages/flux-renderers-form/src/renderers/form-definition.ts` so schema-driven form rendering now forwards `bodyClassName` and `actionsClassName` through the renderer field contract; this clears the `slot-classname` regression that had been showing up in workspace test runs.
- Re-checked the previously reported focused failures: `flow-designer-renderers` targeted tests were green on rerun, and `flux-renderers-form` `src/index.test.tsx` is green after the field-contract fix.
- Workspace `pnpm test` still does not complete, but the blocker has moved: the run now reaches a `@nop-chaos/flux-renderers-basic#build` crash (`exit code -1073741523`) inside the turbo test pipeline, rather than failing on the earlier Plan 175-related assertions.
- Next step: diagnose the `flux-renderers-basic` build crash separately from Plan 175, since the remaining failure is now a build-process instability rather than a regression in the review-4 remediation slices.

### 2026-05-02 (Late Evening, follow-up) — Deep-audit residual owner split after review

- Re-audited the 2026-05-02 deep-audit residuals against the live repo and earlier overlap plans. Key retained live residuals remain:
  - `docs/skills/deep-audit-prompts.md:335-343` stale dimension-01 dependency rule vs live `packages/ui/package.json`
  - `packages/flux-runtime/src/form-runtime.ts:154` + `packages/flux-runtime/src/runtime-owned-factories.ts:110-115` + `packages/flux-react/src/schema-renderer.tsx:105-107,228-240` page-root validation owner publishes before compiled model attach
  - `packages/flux-react/src/node-renderer.tsx:316-334` and `packages/flux-renderers-form/src/field-utils.tsx:485-500` still publish hidden-field participation through `FormRuntime` casts
  - `packages/flux-renderers-basic/src/container.tsx:24-29` still injects fallback gap from renderer code
  - `packages/report-designer-core/src/core-dispatch.ts:169-211` preview path still lacks true cancellation / stale-result guard
  - `packages/word-editor-core/src/index.ts:7-34` still mixes `Dataset*` and `DataSet*` vocabulary
- Independent review rejected the first omnibus residual draft as too wide for the plan guide's "one result surface" rule.
- Replaced the omnibus approach with a portfolio-style owner map plus narrow successors:
  - `docs/plans/176-deep-audit-residual-owner-assignment-plan.md`
  - `docs/plans/177-deep-audit-doc-baseline-sync-plan.md`
  - `docs/plans/178-validation-owner-bootstrap-and-hidden-participation-plan.md`
  - `docs/plans/179-container-default-gap-contract-successor-plan.md`
  - `docs/plans/180-report-preview-cancellation-and-stale-result-plan.md`
  - `docs/plans/181-word-editor-dataset-vocabulary-convergence-plan.md`
- Also widened `docs/plans/167-test-quality-and-reliability-improvement-plan.md` so the retained `>700` test-file residuals have an explicit existing owner:
  - `packages/flux-compiler/src/schema-compiler-registry.test.ts`
  - `packages/flux-compiler/src/schema-compiler-shape-validation.test.ts`
  - `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`
- Key plan-structure decision: treat the 2026-05-02 residuals as a small owner portfolio rather than one abstract "current-baseline drift" bucket or a mixed omnibus successor.
- Independent follow-up review passed after the latest split and wording fixes. No further structural rewrite is needed; the next step is to execute the successor plans in owner priority order.

### 2026-05-02 (Evening) — Plan 173 Adversarial Review Remediation

- Completed adversarial review of entire nop-chaos-flux codebase (24 packages). Report: `docs/analysis/2026-05-02-adversarial-review.md`
- Created remediation plan: `docs/plans/173-adversarial-review-2-critical-findings-remediation-plan.md`
- **Phase 1 — Compiler-Runtime Contract Consistency** (completed):
  - Fixed `$Date` symbol table: updated `compile-symbol-table.ts:60` from 7 members (with 2 phantom methods) to all 14 actual runtime methods. Removed `startOfDay`/`endOfDay` (no runtime), added 9 missing methods (`today`, `parse`, `year`, `month`, `day`, `hours`, `minutes`, `seconds`, `diff`)
  - Fixed `booleanStringAdapter` in `value-adapter.ts:162`: `in()` now parses `"true"`/`"false"` strings correctly (`"false"` → `false`) instead of using `Boolean()` which returned `true` for the string `"false"`
  - Added `MAX_ACTION_COMPILE_DEPTH = 128` depth limit to `compileActionNode` and `isNodeFullyStatic` in `action-compiler.ts`, preventing stack overflow from deeply nested action chains
  - Added try/catch around `new RegExp(rule.value)` in `validation-lowering.ts:206`, preventing invalid regex patterns from crashing compilation
  - Added `console.warn` to `actionAdapter` catch blocks (`value-adapter.ts:234,262`) for error visibility
- **Phase 2 — Reaction Cascade Depth** (completed):
  - Replaced `MAX_REACTION_FIRE_COUNT = 10` (lifetime counter) with `MAX_CASCADE_DEPTH = 100` (cascade depth semantics) in `reaction-runtime-helpers.ts`
  - Refactored `runReaction` in `reaction-runtime.ts` to accept `cascadeDepth` parameter: incremented in the `finally` block recursive path (cascade), reset to 0 on external triggers via `scheduleReaction`
  - External triggers (user actions, API responses) no longer accumulate toward the limit; only self-referential cascades do
- **Phase 3 — Hot-Path Performance** (completed):
  - Added `childPathToRegistrationId: Map<string, string>` reverse index to `FormRuntimeRegistrationState` for O(1) child path lookups
  - Updated `registerField`, `unregister`, and `updateFieldRegistration` to maintain reverse index consistency
  - Improved `scopeChangeHitsDependencies` single-segment fast path to use `Set.has()` instead of `Array.includes()`
- **Phase 4 — Action Adapter Safety** (completed):
  - Removed dead-code store check (lines 43-56) from `resolveFormTarget` in `action-adapter.ts`, eliminating `as any` duck typing that checked form store shape but always returned `not-found`
  - Changed `Promise.all` to `Promise.allSettled` in `node-source-prop-controller.ts`, allowing partial source prop success when some sources fail
- **Phase 5 — Documentation And Contract Sync** (completed):
  - Updated `docs/architecture/flux-runtime-module-boundaries.md` with 19 missing source files across form-runtime, async-data, and schema-compiler subsystems
  - Corrected Plan 103 validation checklist: marked `useNodeLifecycleActions` ref pattern as unimplemented
  - Added `data-testid`/`data-cid` to 7 renderer components in `flux-renderers-form-advanced`
- Verification: `pnpm typecheck` passes, `pnpm lint` passes, all affected package tests pass (flux-core 119, flux-compiler 281, flux-runtime 708, flux-react 272)
- **Closure Audit Fixes**:
  - Fixed regex diagnostic: `validation-lowering.ts` now logs `console.warn` for invalid regex and returns `{ error }` in precompiled; runtime `validators.ts` skips pattern validation when `precompiled.error` is set (prevents runtime crash from re-attempting invalid regex); added `error?: string` to `CompiledValidationRule.precompiled` type
  - Updated Plan 173 Phase 3 checklist: clarified `scopeChangeHitsDependencies` only optimized single-segment paths with `Set.has()`; multi-segment O(n\*m) deferred
  - Updated Plan 103 Phase 2 text and checklists to accurately reflect `useNodeLifecycleActions` ref pattern was NOT implemented

### 2026-05-02

- Re-audited and closed `docs/plans/163-core-boundary-and-validation-owner-convergence-plan.md` against the live repo after confirming the remaining `RendererDefinition.component` / `RenderRegionHandle.render` / `RendererHelpers.render` surfaces are host-neutral callable contracts, not React-specific leakage.
- `packages/flux-core/src/types/renderer-core.ts` and `packages/flux-core/src/registry.ts`: removed the last React-only registration convenience from core by deleting `reactComponent` from the core `RendererDefinition` surface and requiring `component` at core registration time.
- `packages/flux-react/src/react-contracts.ts`: made `reactComponent` explicitly React-owned on top of the host-neutral core contract so `flux-react` continues to auto-wrap plain React components before they reach the core registry.
- Docs updated to the final boundary: `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`, `docs/references/integrating-third-party-components.md`, `docs/references/renderer-interfaces.md`, and `docs/plans/163-core-boundary-and-validation-owner-convergence-plan.md`.
- Verification: workspace `pnpm typecheck`, `pnpm build`, and `pnpm lint` pass; focused Plan 163 tests continue to pass (`packages/flux-react/src/__tests__/defaults-and-auto-renderer.test.tsx`, `schema-renderer.test.tsx`, `schema-renderer-validation-owner-boundary.test.tsx`, `dialog-host.test.tsx`, `schema-renderer-runtime-dialogs.test.tsx`, `packages/flux-runtime/src/__tests__/runtime-dialogs-scope.test.ts`, `runtime-factory-utils.test.ts`).
- Verification note: workspace `pnpm test` still reports unrelated failures outside Plan 163 scope (including `flow-designer-renderers` timeouts/unhandled errors and `spreadsheet-renderers` context-menu test timeout). Plan 163 closure is based on focused green tests plus a fresh independent audit, not on claiming repo-wide test green.
- Closure audit evidence: fresh independent audit confirmed the narrowed Phase 1 boundary is honest, `SchemaRenderer.surfaceRuntime` is live, managed surface-root validation owners are live, and no remaining plan-owned code work remains in scope.
- Next step: route remaining renderer-contract bypass work and unrelated workspace test failures to their owning plans instead of re-opening Plan 163.
