# C4.1 table 逐组件审计

> Plan Status: active
> Mission: component-audit
> Work Item: C4.1
> Last Reviewed: 2026-08-03
> Source: `docs/backlog/component-audit-roadmap.md`（C4.x Phase Details）、`docs/audits/component-audit-checklist.md`（18 维 + 审计卡模板 + 自动修复纪律；§3「与 deep-audit-prompts 23 维的关系」复杂交互渲染器必选 21-23）、`docs/skills/deep-audit-prompts.md`、`CONTEXT.md`（CRUD 域设计语言——table 是 CRUD 数据展示与行 scope/分页/排序/选择的数据层基础）、`docs/logs/2026/08-03.md`
> Related: 依赖 C0（`2026-08-02-2043-1`，completed）完成后开工；与 C3.4（`2026-08-03-1616-1`）/ C3.5（`2026-08-03-1616-2`）并行独立（均只依赖 C0）。历史机制：08-02 nested-schema-field-classification（completed）已把 table `quickSaveAction`/`quickSaveItemAction` 分类为 actionValue（`data-renderer-definitions.ts`——维度 6 复验基础）并修复 CRUD 行内 dropdown-button 行 scope 污染（本组件维度 12 复验）；`2026-07-21-0800-2-table-data-component-enhancement-plan.md` 等历史表族计划均 completed

## Purpose

对 `flux-renderers-data` table 单组件完成 18 维逐组件审计（一张审计卡，P0/P1/P2/P3 裁决留痕）并追加 deep-audit 21-23 维（复杂交互渲染器必选——table 是数据展示复杂交互渲染器：列设置/固定列/合并单元格/树表/快速编辑/行拖拽/虚拟滚动/行 scope 投影），P0/P1 缺陷在**同一 plan 内自动修复**（test-first + 回归测试 + 受影响包验证门禁），完成 ≥1 个真实浏览器组合宿主场景验证（含 bug 73 模式专项检查），最终审计卡 `closed`（P0/P1 清零）。重点维度：3 值所有权三态（行 scope/投影隔离、选中态）、4 表单参与（快速编辑写回）、6 嵌套 schema 分类（quickSaveAction/quickSaveItemAction 08-02 机制复验）、7 事件与 action 契约（行点击/按钮/action payload 形状、normalizeActionEvent）、8 a11y（键盘导航/焦点管理）、11 异步生命周期（懒加载 children、数据源异步）、12 组合宿主场景（CRUD 行内编辑、行 scope 污染 bug 73 模式复验）、15 性能边界（虚拟滚动、列重排、key 稳定性）、16 测试质量（40+ 既有测试断言有效性）。

## Current Baseline

- **组件与文件**：`table-renderer.tsx`（主文件）+ `table-renderer/` 子模块目录（33+ 文件：`table-body-rows.tsx`、`table-body-row-rendering.tsx`、`table-header-row.tsx`、`table-header-tree.ts`、`table-cell-chrome.tsx`、`table-cell-popover.tsx`、`table-quick-edit-cell.tsx`、`table-quick-edit-controller.ts`、`use-table-controls.ts`、`use-table-selection.ts`、`use-table-sort.ts`、`use-table-filter.ts`、`use-table-expand.ts`、`use-table-tree.ts`、`use-table-pagination.ts`、`use-table-lazy-children.ts`、`use-table-visible-columns.ts`、`use-column-resize.ts`、`use-row-drag-sort.ts`、`fixed-columns.ts`、`combine-cells.ts`、`table-data.ts`、`use-table-row-scope-cache.ts`、`table-pagination-bar.tsx`、`table-summary-row.tsx`、`table-loading-overlay.tsx`、`table-event-context.ts`、`table-flattened-items.ts`、`capability-action-context.ts`、`column-settings-state.ts`、`types.ts` 等）。
- **注册定义**：`data-renderer-definitions.ts:20`（type 'table'，`quickSaveAction`/`quickSaveItemAction` 已标 `actionValue: true`；`schemaValidator: validateTableSchema`）。
- **设计文档**：`docs/components/table/design.md` + `example.json` 存在（维度 17 可核对）。
- **playground**：`apps/playground/src/component-lab/renderers/table-lab-page.tsx` 存在。
- **既有单测（极厚）**：`__tests__/` 下 40+ 文件（`data-table.test.tsx`、`table-data-and-layout.test.tsx`、`table-e1b-enhancements.test.tsx`、`table-e1c-*.test.tsx` ×8、`table-selection-invariants.test.tsx`、`table-selection-phantom-prune.test.tsx`、`table-row-key-resolution.test.ts`、`table-body-rows-virtual.test.tsx`、`table-quick-edit-*.test.tsx` ×3、`table-t11-lazy-children.test.tsx`、`table-t28-dynamic-columns.test.tsx`、`table-expandable-when.test.tsx`、`table-pagination-*.test.tsx`、`table-tree-*.test.tsx`、`table-cell-popover.test.tsx`、`table-column-order.test.tsx`、`table-responsive.test.tsx`、`table-row-drag-sort-persist.test.tsx`、`table-b33-advanced-boundary.test.tsx`、`data-table-b33-source-refresh-anchor.test.tsx`、`data-table-row-scope-identity.test.tsx`、`table-dialog-scope-inheritance.test.tsx`、`table-click-dispatch-priority.test.tsx`、`data-table-columns.test.tsx`、`data-table-pagination-clamp.test.tsx`、`data-table-pagination-selection.test.tsx` 等）。
- **e2e**：`tests/e2e/table-popover.spec.ts`、`tests/e2e/performance-table.spec.ts`（200ms 阈值——与 diff-perf 同机器相关类别）、`tests/e2e/crud-demo.spec.ts`（CRUD 宿主）、`tests/e2e/component-lab/crud-table-body-diag.spec.ts`、`tests/e2e/component-lab/c1-1-host-surfaces.spec.ts`（table 在 C1.1 宿主中出现）存在；本组件无 `tests/e2e/component-lab/c4-1-host-surfaces.spec.ts`（需新增）。
- **历史基础**：`2026-07-21-0800-2-table-data-component-enhancement-plan.md` 等表族历史计划 completed（列宽持久化/复制/多排序/嵌套表头/行拖拽/树表/懒加载等能力已落地）；08-02 nested-schema 机制已修复 CRUD 行内 dropdown-button 行 scope 污染（`2026-08-02-1`，completed——本组件维度 12 复验基础，`table-dialog-scope-inheritance.test.tsx` 与 `data-table-row-scope-identity.test.tsx` 已存在）。
- **基线**：以 C0 回写基线为准（unit 全绿 58/58；e2e pre-existing 8 项中本组件无归属项——diff-perf 200ms 阈值属 content 包机器相关类别（CV 专项），calendar-demo/ai 项属 C9/C8.1）。

## Goals

- 1 张审计卡（`docs/audits/per-component/table.md`）18 维逐项核对 + deep-audit 21-23 维，P0/P1/P2/P3 裁决留痕，`文件:行` 证据，`closed`（P0/P1 清零）。
- 本组件 P0/P1 缺陷 test-first 自动修复（契约/公共层修复 Must automate），P2 低成本当场修复、其余登记卡内 backlog 归 CR。
- ≥1 个真实浏览器组合宿主场景（含 bug 73 模式专项）：候选——CRUD 行内 table 行编辑 + 提交（行 scope 污染复验）、table 快速编辑写回 store、列设置/行选择/分页交互。
- roadmap C4.1 行标 `done`（独立子 agent closure-audit pass 后）。

## Non-Goals

- C4.2 CRUD 本体（table 宿主场景仅以组合验证方式触及，不审 CRUD 自身）、C4.3 data 其余组件（tree/chart/list/pagination/statistics/data-source）。
- checklist v2 修订（CG）、审计工具脚本升级（CG）。
- 结构性重构（公共 API/包边界/编译期机制，需人工确认后另立 CX-n；纯行为修复不视为结构性）。
- 各审计卡 P2 backlog（>15 分钟项）归 CR 处理，不在本 plan 内实现。

## Scope

### In Scope

- 1 组件 × 18 维审计卡 + deep-audit 21-23 维（维度重点：1 Schema 契约（TableSchema 与 validateTableSchema/注册 fields 一致）、2 RendererComponentProps 合规、3 值所有权三态（行 scope 投影隔离、选中态/展开态 ownership、source 数据层）、4 表单参与（快速编辑 name 挂接/写回/校验、data-field-_）、5 DOM 与选择器契约（列 name 进 DOM、data-row/data-cell/data-value/data-testid、行/列 marker 契约）、6 嵌套 schema 分类（**quickSaveAction/quickSaveItemAction 08-02 机制落地后复验**、column 内嵌 action 分类、无 deepFields 残留）、7 事件与 action 契约（行点击/行内按钮/quickSave payload 形状、normalizeActionEvent 语义、dispatch 优先级）、8 a11y（表头/表体键盘导航、排序/选择/展开焦点管理、aria 语义）、9 i18n（空态/加载/分页/操作文案 key）、10 四态覆盖（空/加载/错误/禁用——loading overlay、空表、错误态）、11 异步生命周期（懒加载 children abort/竞态/失败态、数据源异步、source 刷新）、12 组合宿主场景（**CRUD 行内编辑提交——08-02 行 scope 污染修复复验（bug 73 模式）**、dialog 内 table、投影稳定性）、13 样式契约（widget 自样式）、14 React 19（startTransition 使用核查）、15 性能边界（虚拟滚动正确性、列重排、key 稳定性、行 scope 缓存）、16 测试质量（40+ 既有测试断言正确行为而非 not-throw、DOM 契约断言、错误路径——尤其 table-_.test 是否覆盖真实行 scope 语义）、17 文档对照（design.md ↔ 实现 props/行为）、18 注册/包边界/IO 安全红线（surface 双注册、data-source 远程加载走 env IO 边界 INV-1））；追加 deep-audit 21（显示与定位正确性——固定列/合并单元格/树展开/虚拟滚动定位）、22（集成接线与可操作性——CRUD/form/dialog 集成、快速编辑接线）、23（测试有效性与假绿——既有 table 测试是否真断言行为）。
- P0/P1 自动修复（test-first）+ P2 低成本即时修复。
- 真实浏览器宿主场景（CRUD 行内 table 行编辑 + 提交（bug 73 模式：行 scope 污染复验）、快速编辑写回、列设置/选择/分页交互）。
- 审计卡状态流转（open → fixing → fixed-pending-closure → closed）与 daily log 记录。

### Out Of Scope

- C4.2 CRUD 本体、C4.3 data 其余组件。
- checklist v2 修订（CG）。
- 结构性重构（需人工确认后另立 CX-n）。
- e2e pre-existing 剩余项（ai/scheduling/content 包，successor C8.1/C9/CV）。

## Failure Paths

| 可测场景编号    | 触发                                 | 行为（含错误码）                                       | 可重试 | 用户可见表现          |
| --------------- | ------------------------------------ | ------------------------------------------------------ | ------ | --------------------- |
| host-table-row  | CRUD 行内 table 行编辑并提交（真机） | 编辑值进入 store、提交新值（bug 73 行 scope 污染复验） | 是     | 提交结果正确回显      |
| host-table-qe   | table 快速编辑单元格（inline 编辑）  | 值写回 store、校验错误展示与清除                       | 是     | 单元格值更新正确      |
| host-table-lazy | 懒加载 children（t11 模式）          | 子行加载正确、失败态可重试、不崩溃                     | 是     | 行展开内容正确        |
| host-table-sel  | 行选择/全选/跨页选择                 | 选中集正确、无 phantom 选中（selection invariants）    | 是     | 选中行高亮 + 计数正确 |

## Test Strategy

本档选择：**必须自动化** —— table 是数据展示核心回归路径（行 scope 投影契约 + 选择/排序/分页状态机 + 快速编辑写回），承接 08-02 行 scope 污染修复复验；契约/公共层修复必须 test-first（先写失败测试再实现）。验证门禁：受影响包 `pnpm --filter @nop-chaos/flux-renderers-data typecheck/build/lint/test` + 相关 e2e 回归（`table-popover.spec.ts`、`crud-demo.spec.ts`、`crud-table-body-diag.spec.ts`）+ 本组件新增宿主场景 Playwright（programmatic DOM 断言）。

## Execution Plan

### Phase 1 - 逐组件 18 维审计（含 deep-audit 21-23）与审计卡产出

Status: planned
Targets: `packages/flux-renderers-data/src/table-renderer.tsx`、`packages/flux-renderers-data/src/table-renderer/`、`docs/audits/per-component/`

- Item Types: `Proof`

- [ ] 审计前核对注册定义：table 注册项（type/fields/quickSaveAction/quickSaveItemAction actionValue 声明）与 validateTableSchema 一致（维度 1/18）。
- [ ] 产出审计卡：18 维逐项核对（结论 pass/fail/n-a + `文件:行` 证据 + 发现），P0/P1/P2/P3 裁决留痕（checklist §3）；**追加 deep-audit 21-23 维**（显示与定位/集成接线与可操作性/测试有效性与假绿）。
- [ ] 维度重点核查：行 scope 投影与值所有权（维度 3：`use-table-row-scope-cache.ts`、行 scope 隔离——`data-table-row-scope-identity.test.tsx`/`table-dialog-scope-inheritance.test.tsx` 复验）；嵌套 schema 分类（维度 6：quickSaveAction/quickSaveItemAction 08-02 机制复验、column 内嵌 action）；事件 payload（维度 7：行点击/行内按钮 dispatch 形状与优先级）。
- [ ] a11y（维度 8：表头/表体键盘导航、排序/选择/展开焦点管理）与四态覆盖（维度 10：loading overlay/空表/错误态）。
- [ ] 异步生命周期（维度 11：懒加载 children abort/竞态/失败态、数据源异步）与性能边界（维度 15：虚拟滚动正确性、列重排、key 稳定性）。
- [ ] 测试质量（维度 16/23）：40+ 既有 table 测试断言正确行为而非 not-throw、DOM 契约断言、错误路径——判定是否覆盖真实行 scope 语义（假绿核查）。
- [ ] 文档对照（维度 17）：design.md + example.json ↔ 实现 props/行为逐项核对；quick-reference.md 词条准确性。

Exit Criteria:

> 本 Phase 交付 1 张审计卡（含裁决 + 21-23 维），是后续修复的唯一事实来源。

- [ ] `docs/audits/per-component/table.md` 卡存在，18 维表完整 + deep-audit 21-23 维记录、`文件:行` 证据可验证。
- [ ] 卡内发现清单带 P0/P1/P2/P3 裁决；无 P0/P1 则标记 `closed`，否则 `open`；08-02 机制复验结论已记录。

### Phase 2 - P0/P1 自动修复（test-first）

Status: planned
Targets: 发现涉及的 renderer/子模块文件、schema 文件、契约测试文件、e2e spec

- Item Types: `Fix | Proof | Decision`

- [ ] 按审计卡处理 P0/P1：先写复现/回归测试（断言正确行为，非仅 not-throw），再实现修复；DOM/选择器契约变更追加 focused 契约测试与 e2e（test-first 证据：复现测试先于实现）。
- [ ] P2 低成本（约 15 分钟内）当场修复；其余登记卡内 backlog 归 CR。
- [ ] 共性缺陷裁决（Decision）：公共层/跨包发现（行 scope 机制、数据源请求层）影响 ≥2 组件/跨包 → 按 roadmap 自动修复机制 §7 处理（当前 plan 内优先修复并事后回写 CX-n，或插入 CX-n work item）；根因单点 `shared:` 标记归 CR。
- [ ] 修复后卡内发现标 `fixed` + commit/plan 引用；卡状态流转 `open → fixing → fixed-pending-closure`。
- [ ] 复杂/跨包 bug 修复按 AGENTS.md Bug Fix Test Coverage Rule 记录到 `docs/bugs/`（参考 `docs/bugs/73-*.md` 格式）。

Exit Criteria:

> 本 Phase 交付"发现清零或已裁定"，卡状态与代码行为一致。

- [ ] 全部 P0/P1 已修复（卡内标 `fixed` + 证据）或已显式裁定延期（仅允许依赖未落地跨 plan 机制者，标「机制落地后复验」并登记）；无静默跳过。
- [ ] 受影响包 `pnpm --filter @nop-chaos/flux-renderers-data typecheck && build && lint && test` 绿（含新增回归测试）。

### Phase 3 - 组合宿主真实浏览器场景

Status: planned
Targets: `tests/e2e/component-lab/c4-1-host-surfaces.spec.ts`（新增）、playground lab 页

- Item Types: `Proof | Fix`

- [ ] 设计并实现 ≥1 个本组件真实浏览器组合宿主场景（programmatic DOM 断言，禁截图诊断）：候选——CRUD 行内 table 行编辑 + 提交（bug 73 模式：行 scope 污染复验——编辑值 → store → 提交新值而非旧值）、快速编辑写回 store、行选择/全选/跨页选择、懒加载展开；若使用新 lab 页路由，同步在 `playground-entry-pages.spec.ts` 的 ROUTE_ASSERTIONS 补该路由断言（先例：08-03 路由覆盖门禁）。
- [ ] bug 73 模式专项检查：在宿主场景中显式验证单测绿但真机失败类风险（行内编辑真机提交新值、投影 scope 稳定——08-02 修复复验）。
- [ ] 宿主场景发现的新缺陷按 Phase 2 流程修复（test-first）。
- [ ] 既有相关 e2e（`tests/e2e/table-popover.spec.ts`、`tests/e2e/crud-demo.spec.ts`、`tests/e2e/component-lab/crud-table-body-diag.spec.ts`）在本组件改动后回归。

Exit Criteria:

> 本 Phase 交付"真实浏览器行为成立"。

- [ ] 宿主场景 spec 通过（Playwright，programmatic DOM 断言）；table-popover/crud-demo/crud-table-body-diag 回归绿。
- [ ] bug 73 模式专项检查结论记录于 daily log（pass 或带证据 fail→已修复）。

### Phase 4 - 组件回归与审计卡 closure

Status: planned
Targets: 审计卡、`docs/logs/2026/08-03.md`、`docs/backlog/component-audit-roadmap.md`（C4.1 行）

- Item Types: `Proof`

- [ ] 全卡复查：18 维 + 21-23 维表结论与最终代码一致；P0/P1 清零；卡状态 `closed`（含 fixed-pending-closure → closed 流转）。
- [ ] 本组件范围回归：`pnpm --filter @nop-chaos/flux-renderers-data test` + 相关 e2e spec 全绿；如本 plan 触及公共层（flux-react/field-frame/编译器/数据源请求层），追加受影响包验证并记录。
- [ ] daily log 记录：卡 closure 汇总、修复清单（commit/plan 引用）、宿主场景结果、08-02 机制复验结论、CX-n 插入（如有）与决策。
- [ ] 若插入了 CX-n：同步更新 roadmap Work Item Status 表（新行 + 依赖边/注释）并按 §7b（事后回写：父 plan closure 后标 done）/§7c（正常生命周期）走；结构性 CX-n 执行前标注待人工确认。
- [ ] roadmap C4.1 行标 `done` 的前置：独立子 agent closure-audit pass（Closure Gates 项，不在本 plan 执行 session 内自审；已交付全部执行证据，由 mission-driver CLOSURE_VERIFY fresh session 执行 audit 后收口）。

Exit Criteria:

> 本 Phase 交付"可进入 closure-audit 的完成态"。

- [ ] 审计卡 `closed`；`docs/audits/per-component/` 汇总可读。
- [ ] daily log 已记录本 plan 收口证据（含 closure-audit 证据位置）。

## Draft Review Record

> 起草后、执行前的独立审查证据。详见 `docs/plans/00-plan-authoring-and-execution-guide.md` 的 `Plan Review Rule`。由独立审阅者或独立子 agent 填写。

- Reviewer / Agent: 独立子 agent fresh session（task `ses_0394a4be9ffe4BsNQsVKFjGqTX`）
- Verdict: `pass`（零 Blocker/Major；Minor ×2）
- Rounds: 1
- Findings addressed: 全部文件/行引用经 live repo 核对通过（data-renderer-definitions.ts `:20` type 'table' + quickSaveAction/quickSaveItemAction actionValue `:33-44`、table-renderer.tsx + 35 子模块抽查 9 处、design.md + example.json、table-lab-page.tsx、44 个 table 测试文件抽查 8 处、4 e2e spec、08-02-1 行 scope 污染修复与 table actionValue 分类、roadmap C4.1 `todo` 仅依赖 C0、无既有审计卡）；Minor 已处理：table-e1c-\*.test.tsx 计数 ×9→×8、Phase 3 补 playground-entry-pages.spec.ts ROUTE_ASSERTIONS 路由断言项（新 lab 页路由时）。

## Closure Gates

> 关闭条件：本 section 与每个 Phase 的 Exit Criteria 全部勾选后才能将 `Plan Status` 改为 `completed`。

- [ ] 审计卡存在、18 维表完整 + 21-23 维记录、P0/P1 清零、`closed`
- [ ] 全部 in-scope P0/P1 已 test-first 修复并有回归测试；无被静默降级到 deferred 的 live defect/contract drift
- [ ] ≥1 个真实浏览器组合宿主场景通过（含 bug 73 模式专项检查）
- [ ] 共性缺陷已按 §7 处理（CX-n 插入/合并或当前 plan 内修复，决策记录在卡与 daily log）
- [ ] 受影响的 owner docs 已同步到 live baseline（design.md/quick-reference/roadmap 表按发现实际影响为准）
- [ ] 由独立子 agent（fresh session）执行的 closure-audit 已完成并记录证据；执行 session 不得自审勾选本项
- [ ] `pnpm typecheck`
- [ ] `pnpm build`
- [ ] `pnpm lint`
- [ ] `pnpm test`

## Deferred But Adjudicated

### 审计卡 P2 backlog（成本 >15 分钟的非阻断体验/文档/测试加固项）

- Classification: `optimization candidate`
- Why Not Blocking Closure: checklist §3 明确 P2 可入审计卡 backlog 由 CR 自动处理；不阻塞本组件 supported baseline 成立。
- Successor Required: `yes`
- Successor Path: CR work item（跨族集中修复）

### 依赖未落地跨 plan 机制的发现（若有，「机制落地后复验」）

- Classification: `watch-only residual`
- Why Not Blocking Closure: 依赖的机制未落地时无法在卡内闭合；按 checklist §3 自动修复纪律（「机制落地后复验」显式登记）由 CR 集中复验，卡内不悬挂。
- Successor Required: `yes`
- Successor Path: CR work item

### e2e pre-existing 其余项（ai/scheduling/content 包）

- Classification: `watch-only residual`
- Why Not Blocking Closure: C0 已逐项裁定为 mission 未触及包失败，successor 归属 C8.1/C9/CV；不在本组件 scope。
- Successor Required: `yes`
- Successor Path: C8.1/C9/CV work item

## Non-Blocking Follow-ups

- 审计中发现但裁定为 P3（风格 nit/注释）的项仅卡内记录，不处理。
- 工具脚本新增模式（若有）记入卡内，CG 统一升级。

## Closure

Status Note: 待执行（draft → active → completed 流程）。

Closure Audit Evidence:

- Auditor / Agent: 待独立子 agent fresh session（mission-driver CLOSURE_VERIFY 阶段）
- Evidence: 待填写

Follow-up:

- 待填写（或明确 no remaining plan-owned work）。
