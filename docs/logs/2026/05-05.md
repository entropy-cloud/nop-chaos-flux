# Development Log — 2026-05-05

### 2026-05-05

- Continued plan `213` with focused doc/test/public-vocabulary cleanup: replaced retained fixed-sleep Playwright waits in `word-editor`, `debugger`, and `performance-table` e2e coverage with readiness-based assertions; updated active docs to match the live renderer/runtime hook and terminology baseline; and converged `flux-code-editor` source-ref vocabulary to canonical `path` while keeping legacy `dataPath` compatibility reads.
- Link to code: `tests/e2e/{word-editor.spec.ts,word-editor-template-expr.spec.ts,word-editor-dataset.spec.ts,word-editor-persistence.spec.ts,debugger.spec.ts,performance-table.spec.ts}`, `packages/flux-code-editor/src/{types.ts,source-resolvers.ts,types.test.ts}`, `docs/architecture/{renderer-runtime.md,flux-runtime-module-boundaries.md}`, `docs/references/terminology.md`, `docs/plans/213-doc-test-boundary-and-hardening-closure-plan.md`
- Key decision: treat `path` as the only canonical author-facing source-ref field in `flux-code-editor`, but keep resolver-level compatibility for legacy `dataPath` inputs so this cleanup removes public ambiguity without forcing an immediate breaking migration.
- Key decision: keep this pass bounded to the in-scope retained Playwright files instead of widening into a full repo-wide e2e modernization effort.
- Verification: targeted `pnpm exec playwright test tests/e2e/word-editor.spec.ts tests/e2e/word-editor-template-expr.spec.ts tests/e2e/word-editor-dataset.spec.ts tests/e2e/word-editor-persistence.spec.ts tests/e2e/debugger.spec.ts tests/e2e/performance-table.spec.ts --reporter=list` passed, and focused `flux-code-editor` verification remains pending in this pass.
- Next step: finish the remaining plan `213` retained items around `flux-i18n` semantic coverage, then rerun full workspace verification if this slice is extended further.

### 2026-05-05

- Executed plan `215` implementation: rewrote `performance-table` batch measurement semantics around synchronized `Profiler` metrics plus batch-local scheduling/quiescence summaries, relabeled the page as a same-environment comparative surface, and added focused helper proof plus retained page e2e.
- Link to code: `apps/playground/src/pages/performance-table-page.tsx`, `apps/playground/src/pages/performance-table/{types.ts,measurement.ts,measurement.test.ts,schema.ts,index.ts}`, `tests/e2e/performance-table.spec.ts`, `apps/playground/src/{App.tsx,route-model.ts,pages/home-page.tsx}`
- Key decision: the trustworthy page-level baseline is `commit count + commit-duration aggregates`, while scheduling and quiescence remain visible only as explicitly labeled page-local signals instead of being presented as render cost.
- Key decision: debugger disabled mode now owns the component-registry debug-data boundary, and render/update instrumentation is separately gated through explicit `capturePerformance` host intent so ordinary playground runtime can keep the launcher without paying render-event collection cost.
- Link to owner docs: `docs/architecture/{performance-design-requirements.md,debugger-runtime.md,playground-experience.md}`, `docs/plans/215-performance-measurement-and-debugger-gating-plan.md`
- Verification: targeted `pnpm --filter @nop-chaos/nop-debugger test -- --runInBand`, `pnpm --filter @nop-chaos/flux-runtime test -- --runInBand src/__tests__/component-handle-registry.test.ts`, `pnpm --filter @nop-chaos/flux-playground test -- --runInBand src/pages/performance-table/measurement.test.ts`, targeted package `typecheck`, and `npx playwright test tests/e2e/performance-table.spec.ts --reporter=list` passed; full workspace verification still pending.
- Next step: run full `pnpm build`, `pnpm lint`, and `pnpm test`, then perform an independent closure audit before marking plan `215` completed.

### 2026-05-05

- Executed the first remediation pass for plans `211`-`214`: closed plan `214` completely, closed plan `213` phase 1, and landed partial `211`/`212` fixes across report-designer core, flow-designer renderer contracts, code-editor root metadata, and repository UI/test guardrails.
- Link to code: `packages/report-designer-core/src/{core.ts,core-dispatch.ts,runtime/adapter-context.ts,runtime/field-sources.ts,__tests__/designer-core.test.ts}`, `packages/flux-renderers-basic/src/use-surface-renderer.ts`, `packages/flow-designer-renderers/src/{designer-field.tsx,index.tsx,schemas.ts,designer-page-shell.test.tsx}`, `packages/flux-code-editor/src/code-editor-renderer.tsx`, `packages/flux-renderers-form/src/renderers/shared/label.tsx`, `apps/playground/src/pages/report-designer-demo.tsx`, `packages/word-editor-{core,renderers}/src/__tests__/*`, `packages/flux-compiler/src/schema-compiler-prop-coverage*.test.ts`, `packages/*/vitest.config.ts`.
- Key decision: close plan `214` by removing deep-copy hot paths with structural sharing and shared adapter-context snapshots instead of broader report-designer architecture churn; no owner-doc update required because the normative docs did not encode the old clone-heavy implementation detail.
- Key decision: mark plans `211` and `212` as in progress and plan `213` as partially completed rather than over-claim closure; only the slices with live code, focused proof, and verification evidence were checked off.
- Verification: `pnpm check:oversized-code-files`, targeted Vitest runs for `report-designer-core`, `flux-compiler`, `flow-designer-renderers`, `flux-renderers-basic`, `flux-code-editor`, `word-editor-core`, and `word-editor-renderers`, plus full `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Closure audit: independent review task `ses_207fba247ffeNMMLw8JwYGQM0j` approved closing `docs/plans/214-report-designer-performance-hot-path-closure-plan.md`.
- Next step: continue plan `211` on runtime-side async/type/error retained defects, continue plan `212` on remaining styling-interface/deep-region/a11y issues, and continue plan `213` on remaining doc drift, Playwright wait cleanup, `flux-i18n` semantic coverage, and `flux-code-editor` `dataPath` vocabulary cleanup.

### 2026-05-05

- Stabilized the remaining `code-editor` Playwright failure by changing the route readiness check to wait for the final editor field instead of assuming all CodeMirror instances appear within the default per-assert timeout during the first full-suite render.
- Link to code: `tests/e2e/code-editor.spec.ts`, `apps/playground/src/App.tsx`
- Key decision: treat this as an e2e readiness issue rather than a runtime regression because the failure snapshot already showed the missing editor fields arriving later on the same page; keep the direct page import in `App.tsx` and make the test wait for the full editor stack before continuing.
- Verification: `npx playwright test tests/e2e/code-editor.spec.ts --reporter=list`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and full `npx playwright test --reporter=list` passed.
- Next step: if `code-editor` startup cost grows again, add an explicit page-level ready marker emitted after schema render completion instead of relying on the last visible field.

### 2026-05-05

- Completed the successor split under program plan `210`: drafted plans `211`-`214` for runtime/state/reactivity/safety, renderer/workbench/a11y, docs-test-boundary/public-vocabulary, and report-designer performance hot paths, then iterated each plan with independent reviewers until all reached `APPROVE`.
- Link to doc or code path: `docs/plans/{210-deep-audit-full-7-confirmed-defect-remediation-program-plan.md,211-runtime-state-reactivity-and-safety-closure-plan.md,212-renderer-workbench-contract-and-accessibility-closure-plan.md,213-doc-test-boundary-and-hardening-closure-plan.md,214-report-designer-performance-hot-path-closure-plan.md}`.
- Key decision: successor plans must enumerate retained must-fix sets explicitly and may not rely on vague dimension-level ownership or closure gates that allow silent scope shrinkage; retained dim14, dim17, and dim15 items stayed non-degradable, while the truncated reactive item remained explicitly unadjudicated.
- Verification: docs-only change; independent review tasks `ses_2084c885cffeBFP7KOGwk4KNSA`, `ses_2083740d3ffeGTZPLdlm1HwDtS`, `ses_208373e3dffeCN6oRBJypkLZ0W`, `ses_208373e1affeEVjyUpo2vaFRCu`, and `ses_208373e08ffeysO5BD0TzkObi4` all ended in `APPROVE` after targeted revisions.
- Next step: start actual remediation under plans `211`-`214`; do not reopen plan `210` unless the retained correction matrix itself changes.

### 2026-05-05

- Fixed a real `variant-field` runtime bug where tab switching could abort before writing the next variant value during React development StrictMode remount cycles.
- Link to code: `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx`
- Link to regression coverage: `tests/e2e/component-lab/complex-form.spec.ts:236`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-selector.test.tsx`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-transform.test.tsx`
- Key decision: keep the `mountedRef` guard for async safety, but re-arm it on effect mount so StrictMode's development-only unmount/remount sequence does not leave the renderer permanently marked as unmounted.
- Key decision: keep the e2e on real tab switching behavior and use forced tab clicks in this lab case because the stage can stay in transient error/re-render states during interaction, which makes Playwright actionability checks noisier than the underlying contract.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, targeted `variant-field` Vitest suites, targeted `component-lab` variant-field Playwright spec, and full `pnpm test` passed.
- Next step: investigate the remaining unrelated `tests/e2e/code-editor.spec.ts` navigation timeout seen in the latest full Playwright sweep.
