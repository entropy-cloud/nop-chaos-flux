# Development Log — 2026-05-11

### 2026-05-11

- Followed up on the independent audit finding for plan `248` by extending `packages/flux-react/src/__tests__/use-source-value.test.tsx` with a real lifecycle proof for `React.StrictMode` remounts and post-unmount stale settlements; the test now shows stale observer settlements do not republish into the mounted hook and do not trigger state writes after unmount.
- Updated `docs/plans/248-unit-test-contract-closure-plan.md` to correct the Phase 3 status/evidence so the source-lifecycle item now matches the live repo instead of overstating rerender-only coverage.
- Link to doc or code path: `packages/flux-react/src/__tests__/use-source-value.test.tsx`, `docs/plans/248-unit-test-contract-closure-plan.md`
- Key decision: the supported source-hook contract is lifecycle-level stale-drop at the hook boundary, so the proof must exercise `StrictMode` remount/unmount behavior directly rather than infer it from rerender latest-wins alone.

### 2026-05-11

- Unified expanded-state side-panel collapse affordances across Flow Designer, Report Designer, and Word Editor by moving the visible collapse button into the shared `WorkbenchShell` instead of leaving it as Flow-only panel chrome.
- Removed the now-duplicated Flow-local collapse buttons, added shared shell coverage for expanded-panel collapse buttons, and added family-level focused proofs in Report Designer and Word Editor that expanded sides can collapse into the shared rails.
- Focused verification passed for `packages/flux-react/src/workbench/workbench-shell.test.tsx`, `packages/flow-designer-renderers/src/designer-page-shell.test.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`, and `packages/word-editor-renderers/src/__tests__/word-editor-page-host-scope.test.tsx`; note that the two downstream package tests required rebuilding `@nop-chaos/flux-react` first because workspace package exports resolve from `dist/`.
- Link to doc or code path: `packages/flux-react/src/workbench/{workbench-shell.tsx,workbench-shell.test.tsx}`, `packages/flow-designer-renderers/src/{designer-page-body.tsx,designer-palette.tsx,designer-inspector.tsx,designer-page-shell.test.tsx}`, `packages/report-designer-renderers/src/{page-renderer.tsx,renderers.integration.test.tsx}`, `packages/word-editor-renderers/src/{word-editor-page.tsx,__tests__/word-editor-page-host-scope.test.tsx}`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`, `docs/architecture/designer-workbench-shell.md`
- Key decision: collapse affordance placement is now a shared shell responsibility for all designer families; family panels can keep their own local header actions, but shell-level side collapse must not depend on family-specific panel implementations.

### 2026-05-11

- Re-ran the full workspace verification after landing the final Phase 2 page-root owner proof and recovered the chain to green: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed with the new `variant-field` owner-boundary test included in the formal workspace run.
- Verification evidence from the rerun: Turbo again reported `48 successful, 48 total` tasks for the final test pipeline; the touched contract-owner packages finished green, including `@nop-chaos/flux-renderers-form` (`25` files / `181` tests), `@nop-chaos/flux-renderers-form-advanced` (`65` files / `573` tests), `@nop-chaos/flux-playground` (`12` files / `58` tests), and `@nop-chaos/nop-debugger` (`16` files / `132` tests).
- Updated `docs/plans/248-unit-test-contract-closure-plan.md` so Phase 5's verification sub-items now reflect the completed workspace chain while keeping the independent closure-audit item open.
- Link to doc or code path: `docs/plans/248-unit-test-contract-closure-plan.md`, `docs/logs/2026/05-11.md`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-selector.test.tsx`
- Key decision: treat this second full-green workspace rerun as the authoritative verification baseline for plan `248`, because it is the first one that includes the final page-root owner-boundary proof.

### 2026-05-11

- Closed successor plans `241` through `247` and the owner-assignment umbrella `242` after re-auditing the live repo, syncing each plan's phase/closure gates, and recording independent closure-audit evidence.
- Confirmed the final known-good workspace baseline after the remaining `flux-react` test-only drifts were fixed (`packages/flux-react/src/__tests__/schema-renderer-runtime-dialogs.test.tsx` and `packages/flux-react/src/__tests__/use-source-value.test.tsx`) and after re-running the full verification chain: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Full green workspace test summary from the final rerun: Turbo reported `48 successful, 48 total` tasks, including the packages that had been the last moving blockers or closure dependents such as `@nop-chaos/flux-react` (35 files / 334 tests), `@nop-chaos/flow-designer-renderers` (18 files / 121 tests), `@nop-chaos/flux-renderers-data` (31 files / 262 tests), `@nop-chaos/flux-renderers-form` (25 files / 181 tests), `@nop-chaos/flux-renderers-form-advanced` (65 files / 571 tests), `@nop-chaos/report-designer-renderers` (10 files / 136 tests), `@nop-chaos/nop-debugger` (16 files / 132 tests), and `@nop-chaos/flux-playground` (12 files / 58 tests).
- Independent closure audit evidence came from fresh subagent review `ses_1e9d55336ffeCpAIoAhaJaR1oL`, which confirmed there are no remaining in-scope residual blockers for `241`, `243`, `244`, `245`, `246`, or `247`, and that `242` only retains its already-adjudicated automation-check follow-up.
- Link to doc or code path: `docs/plans/{241-workbench-host-projection-boundary-and-truth-surface-closure-plan.md,242-deep-audit-2026-05-11-residual-owner-assignment-plan.md,243-package-boundary-manifest-hygiene-successor-plan.md,244-runtime-degradation-observability-and-isolation-plan.md,245-flow-designer-graph-fallback-bounded-cost-plan.md,246-surface-validation-plan-failure-semantics-plan.md,247-flow-designer-host-visibility-and-instance-isolation-plan.md}`, `packages/flux-react/src/__tests__/{schema-renderer-runtime-dialogs.test.tsx,use-source-value.test.tsx}`, `docs/architecture/{api-data-source.md,surface-owner.md,flow-designer/runtime-snapshot.md,flow-designer/canvas-adapters.md,report-designer/design.md,word-editor/design.md}`
- Key decision: do not leave retained 2026-05-11 defect owners in a "fixed in code but plan still pending" state; once repo-level verification and independent closure audit were green, every successor plan was explicitly closed in docs instead of relying on implicit completion.

### 2026-05-11

- Closed the remaining Phase 2 owner-boundary gap in `docs/plans/248-unit-test-contract-closure-plan.md` by replacing the unstable page-root required-rule assumption with a stronger real-entry proof: `packages/flux-renderers-form-advanced/src/variant-field/variant-field-selector.test.tsx` now injects a page-root owner external error for `payload.value` and proves variant switching clears that hidden-branch error through `notifyFieldHidden`.
- Re-audited the runtime/validation dependency-graph gap and confirmed the repo already had the required real-entry proof in `packages/flux-renderers-form/src/__tests__/form-validation-rules.test.tsx`, where changing `password` revalidates `confirmPassword` and changing `role` revalidates `adminCode` through the live renderer/runtime path rather than compile-only metadata assertions.
- Updated `docs/plans/248-unit-test-contract-closure-plan.md` to mark Phase 2 complete and record that child-owner submit gating is already covered by `packages/flux-renderers-form-advanced/src/__tests__/composite-form-detail-and-loop.test.tsx`; owner-doc sync remains `No owner-doc update required`.
- Link to doc or code path: `docs/plans/248-unit-test-contract-closure-plan.md`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-selector.test.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/composite-form-detail-and-loop.test.tsx`, `packages/flux-renderers-form/src/__tests__/form-validation-rules.test.tsx`
- Key decision: for page-root/non-form owner cooperation, the durable contract is hidden-path error cleanup at the owner/store boundary, not an assumption that every page-root render must surface required-field UI through the same mechanism as a managed form.

### 2026-05-11

- Closed `docs/plans/249-config-driven-designer-workbench-convergence-plan.md` after full-workspace verification stayed green (`pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`) and an independent closure audit re-checked the live shell/docs baseline.
- Added one final focused shell proof in `packages/flux-react/src/workbench/workbench-shell.test.tsx` to freeze the responsive rule that keeps the canvas primary by suppressing side chrome on narrower breakpoints when both sides exist.
- Corrected the Word Editor owner/component docs so the default right-side generator is described as the shipped outline panel rather than an overstated outline/property surface.
- Full test/package summary for the green baseline: Turbo reported `48 successful, 48 total` tasks for `pnpm test`; package file counts captured from the run included `@nop-chaos/flux-react` 35 files, `@nop-chaos/flow-designer-renderers` 18, `@nop-chaos/report-designer-renderers` 10, `@nop-chaos/word-editor-renderers` 14, `@nop-chaos/flux-renderers-form` 25, `@nop-chaos/flux-renderers-form-advanced` 65, `@nop-chaos/flux-renderers-data` 31, and `@nop-chaos/flux-playground` 12.
- Link to doc or code path: `packages/flux-react/src/workbench/{workbench-shell.tsx,workbench-shell.test.tsx}`, `docs/architecture/{designer-workbench-shell.md,word-editor/design.md}`, `docs/components/word-editor-page/design.md`, `docs/plans/249-config-driven-designer-workbench-convergence-plan.md`
- Key decision: responsive breakpoint suppression is now treated as part of the shared shell contract rather than an undocumented implementation detail; config still controls side existence, while viewport width controls whether available side chrome stays visible.

### 2026-05-11

- Re-ran the full workspace verification chain for the plan-owned changes and got a full green baseline: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed after fixing one test-only TypeScript narrowing issue in `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts` and removing stale unused imports from `packages/flux-react/src/__tests__/schema-renderer-runtime-monitoring.test.tsx`.
- Full test run summary captured from the workspace verification: 25 packages executed under `turbo run test`, including the newly touched contract-owner packages (`@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, `@nop-chaos/ui`, `@nop-chaos/theme-tokens`, `@nop-chaos/tailwind-preset`) plus downstream dependents such as `@nop-chaos/flux-renderers-form` (25 files / 181 tests), `@nop-chaos/flux-renderers-form-advanced` (65 files / 571 tests), `@nop-chaos/flux-renderers-data` (31 files / 262 tests), `@nop-chaos/flux-playground` (12 files / 58 tests), and `@nop-chaos/nop-debugger` (16 files / 132 tests); the workspace run completed successfully with Turbo reporting `48 successful, 48 total` tasks.
- Link to doc or code path: `docs/plans/248-unit-test-contract-closure-plan.md`, `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts`, `packages/flux-react/src/__tests__/schema-renderer-runtime-monitoring.test.tsx`
- Key decision: keep Phase 5 verification marked complete only for the workspace-green portion; Phase 2 owner-boundary closure and the independent closure audit still remain open before the plan itself can close.

### 2026-05-11

- Executed Phases 1-3 of `docs/plans/249-config-driven-designer-workbench-convergence-plan.md`: `WorkbenchShell` now expands from whole collapsed rails with center-adjacent affordances, Flow/Report side existence is resolved from family config instead of permanent fallback chrome, and Word Editor now uses a minimal `WordEditorConfig` to control default left/right panel generators.
- Added focused regression coverage in `packages/flux-react/src/workbench/workbench-shell.test.tsx`, `packages/flow-designer-renderers/src/designer-page-shell.test.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`, and `packages/word-editor-renderers/src/__tests__/word-editor-page-host-scope.test.tsx` to prove hidden-side behavior and whole-rail expand behavior.
- Updated owner/component docs to freeze the live baseline around config-driven side existence and Word Editor's new minimal panel contract.
- Link to doc or code path: `packages/flux-react/src/workbench/{workbench-shell.tsx,workbench-shell.test.tsx}`, `packages/flow-designer-renderers/src/{designer-page-body.tsx,designer-page-shell.test.tsx}`, `packages/report-designer-renderers/src/{page-renderer.tsx,renderers.integration.test.tsx}`, `packages/report-designer-core/src/types.ts`, `packages/word-editor-renderers/src/{types.ts,renderers.tsx,word-editor-page.tsx,__tests__/word-editor-page-host-scope.test.tsx}`, `apps/playground/src/pages/word-editor-page.tsx`, `docs/architecture/{designer-workbench-shell.md,word-editor/design.md}`, `docs/components/{designer-page/design.md,report-designer-page/design.md,word-editor-page/design.md}`, `docs/plans/249-config-driven-designer-workbench-convergence-plan.md`
- Key decision: page override regions stay as mount surfaces only; they no longer create a side panel unless the owning family config resolves that side as available.

### 2026-05-11

- Added `docs/architecture/designer-workbench-shell.md` to freeze a shared baseline for designer-style hosts: left/center/right workbench structure, config-driven optional side panels, and whole-rail collapsed expand behavior.
- Updated the Flow Designer, Report Designer, and Word Editor owner docs to stop treating side panels as permanently renderer-private chrome and to reframe page regions as override surfaces on top of family-config-driven panel resolution.
- Drafted `docs/plans/249-config-driven-designer-workbench-convergence-plan.md` as the execution plan for shared shell behavior, config-driven left/right panel visibility, and Word Editor side-panel convergence.
- Link to doc or code path: `docs/architecture/designer-workbench-shell.md`, `docs/architecture/{flow-designer/design.md,flow-designer/config-schema.md,report-designer/README.md,report-designer/design.md,report-designer/config-schema.md,word-editor/design.md,README.md}`, `docs/components/{designer-page/design.md,report-designer-page/design.md,word-editor-page/design.md}`, `docs/plans/249-config-driven-designer-workbench-convergence-plan.md`
- Key decision: treat Flow Designer, Report Designer, and Word Editor as general designers hosted by one shared workbench-shell baseline; side-panel existence is family-config-driven, while final business JSON remains owned by family document models and codecs.

### 2026-05-11

- Extended the Phase 3 closure work with real `component:*` capability-lookup proofs in `packages/flux-react/src/__tests__/schema-renderer-runtime-dialogs.test.tsx`, covering mounted child-registry preference by `componentName`, direct `componentId` targeting, and same-registry ambiguous-name failure from the `SchemaRenderer` entry path.
- Updated `docs/plans/248-unit-test-contract-closure-plan.md` again to mark Phase 3 completed while keeping Phase 2 and workspace-wide verification open.
- Link to doc or code path: `packages/flux-react/src/__tests__/schema-renderer-runtime-dialogs.test.tsx`, `docs/plans/248-unit-test-contract-closure-plan.md`
- Key decision: the repo's supported ambiguous-target baseline is registry-local for `componentName` lookup; page-root dispatch does not treat duplicate names that live only in separate sibling child registries as ambiguous.

### 2026-05-11

- Executed the first closure pass for `docs/plans/248-unit-test-contract-closure-plan.md`: adjudicated the disputed baselines, landed the minimal runtime/UI fixes, and converted the resolved items into focused package-level contract proofs instead of helper-only coverage.
- Fixed `packages/flux-runtime/src/node-runtime.ts` so live runtime evaluation consumes `templateNode.structuralWhen`, then added the compile -> runtime -> react handoff proof in `packages/flux-react/src/__tests__/schema-renderer-runtime-monitoring.test.tsx`.
- Locked the shared sidebar token baseline at the package boundary by adding public defaults in `packages/theme-tokens/src/styles.css` plus regression coverage in `packages/theme-tokens/src/styles.test.ts` and `packages/tailwind-preset/src/index.test.ts`.
- Corrected the shared UI/public-entry contract surface: `packages/ui/src/components/ui/native-select.test.tsx` now asserts the supported disabled/value/onChange semantics through `@nop-chaos/ui`, and `packages/ui/src/public-entry-contract.test.ts` now guards root-entry/export alignment.
- Added focused React/runtime boundary proofs for schema-import latest-wins, source stale-drop, submit supersession, mounted child-registry preference, and nested import lexical shadowing in `packages/flux-react/src/{schema-renderer.tsx,__tests__/schema-renderer.test.tsx,__tests__/schema-renderer-runtime-dialogs.test.tsx,__tests__/use-source-value.test.tsx,schema-renderer-imports-basic.test.tsx,schema-renderer-imports-boundaries.test.tsx}` and `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts`.
- Updated `docs/plans/248-unit-test-contract-closure-plan.md` to mark Phase 1 and Phase 4 completed, Phase 2 and Phase 3 in progress, and to record `No owner-doc update required` for the resolved items because the work aligned live behavior to the documented baseline rather than changing the documented contract.
- Link to doc or code path: `docs/plans/248-unit-test-contract-closure-plan.md`, `packages/flux-runtime/src/{node-runtime.ts,__tests__/owner-validation-lifecycle-contracts.test.ts}`, `packages/flux-react/src/{schema-renderer.tsx,__tests__/schema-renderer.test.tsx,__tests__/schema-renderer-runtime-monitoring.test.tsx,__tests__/schema-renderer-runtime-dialogs.test.tsx,__tests__/use-source-value.test.tsx,schema-renderer-imports-basic.test.tsx,schema-renderer-imports-boundaries.test.tsx}`, `packages/theme-tokens/src/{styles.css,styles.test.ts}`, `packages/tailwind-preset/src/index.test.ts`, `packages/ui/src/{public-entry-contract.test.ts,components/ui/native-select.test.tsx}`
- Key decision: treat `structuralWhen`, sidebar tokens, and `NativeSelect` disabled semantics as supported repo-level baselines and fix/prove the live behavior accordingly, but keep Phase 2 owner-boundary closure and full workspace verification open until the remaining focused proofs and `pnpm {typecheck,build,lint,test}` pass.

### 2026-05-11

- Recorded two new manual playground issues in `docs/testing/2026/05-11.md`: the Flux Basic table row `Inspect` button did not open its dialog, and the `Submit only / Array child items` delete button was a no-op.
- Fixed the Flux Basic row dialog example by switching the dialog body bindings in `apps/playground/src/pages/fluxBasicPageSchema.json` from stale `record.*` reads to the live table row slot contract `$slot.record.*`.
- Fixed `packages/flux-renderers-form-advanced/src/array-editor.tsx` so delete operations update `itemsRef` before calling `form.removeValue(...)`; this keeps renderer-local canonical state aligned during remove-time validation/registration reads and prevents deleted rows from being written back.
- Added and corrected regression coverage in `apps/playground/src/pages/flux-basic-page.debugger.test.tsx` and `packages/flux-renderers-form-advanced/src/__tests__/{form-array-validation.test.tsx,composite-item-id.test.tsx}`.
- Added historical root-cause note: `docs/bugs/49-flux-basic-inspect-dialog-and-array-editor-remove-fix.md`.
- Link to doc or code path: `docs/testing/2026/05-11.md`, `apps/playground/src/pages/{fluxBasicPageSchema.json,flux-basic-page.debugger.test.tsx}`, `packages/flux-renderers-form-advanced/src/{array-editor.tsx,__tests__/form-array-validation.test.tsx,__tests__/composite-item-id.test.tsx}`, `docs/bugs/49-flux-basic-inspect-dialog-and-array-editor-remove-fix.md`
- Key decision: keep the fix minimal and layer-local: the dialog issue belonged to playground schema authoring, while the delete bug belonged to `array-editor`'s local ref synchronization rather than form runtime array ops.

### 2026-05-11

- Added `docs/skills/unit-test-logic-and-contract-coverage-audit-prompt.md` after reviewing the existing coverage-oriented prompts and confirming they do not directly audit whether existing unit tests really constrain stable contracts and cross-layer logic.
- Recorded the main gap explicitly: `docs/plans/143-unit-test-coverage-80-percent-target-plan.md` closed package coverage gates, but that plan targets statements/branches/functions/lines rather than contract coverage, logic coverage, or “why old tests failed to stop this bug family” analysis.
- Link to doc or code path: `docs/skills/unit-test-logic-and-contract-coverage-audit-prompt.md`, `docs/plans/143-unit-test-coverage-80-percent-target-plan.md`, `docs/skills/deep-audit-prompts.md`, `docs/skills/exploratory-contract-testing-prompt.md`, `docs/skills/code-quality-audit-prompt.md`, `docs/bugs/43-flow-designer-node-edge-text-empty-expression-pre-evaluation-fix.md`
- Key decision: coverage audit, exploratory contract testing, and logic/contract-coverage audit are now treated as three different jobs; high package coverage is useful evidence, but not proof that the unit suite protects real cross-layer contracts.

### 2026-05-11

- Added explicit CRUD radio-selection coverage across the playground lab, component-lab Playwright suite, and CRUD unit tests so single-row selection is no longer only protected indirectly by lower-level table tests.
- Raised the CRUD component-lab coverage manifest entry from `read` to `edit` because the renderer now has a first-class interaction scenario beyond shell rendering.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `tests/e2e/component-lab/crud-editing-and-selection.spec.ts`, `tests/e2e/component-lab/coverage-manifest.ts`, `packages/flux-renderers-data/src/__tests__/crud-selection-and-features.test.tsx`
- Key decision: keep the new proof at the CRUD layer instead of relying only on table radio tests, because CRUD normalizes `selection.type` and publishes `$crud` selection summaries/list-action state on top of the internal table bridge.

### 2026-05-11

- Drafted `docs/plans/241-workbench-host-projection-boundary-and-truth-surface-closure-plan.md` as the successor owner plan for the 2026-05-11 Flow/Spreadsheet/Report/Word residuals.
- The plan explicitly adopts the current frontend-programming-model baseline: zero-copy readonly internal surfaces stay allowed, but schema-visible and external projection boundaries must be trimmed to stable readonly contracts, and canonical save/history truth surfaces must re-converge where today they split.
- Ran an independent subagent review on the draft, revised the scope to avoid reopening `208` / `220`, rewrote the projection workstream around per-host retained/removed surface matrices, and received final approval on the revised plan.
- Link to doc or code path: `docs/plans/241-workbench-host-projection-boundary-and-truth-surface-closure-plan.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/capability-projection-manifest.md`
- Key decision: this defect family is not a blanket “clone every snapshot” program; the closure target is narrower projection boundaries plus owner-sealed canonical truth surfaces.

### 2026-05-11

- Followed through on the action-contract cleanup by replacing the remaining live playground `submitForm + formId` examples with canonical `component:submit` usage and removing the last active-doc references to deleted built-in compatibility targeting.
- Link to doc or code path: `apps/playground/src/pages/fluxBasicPageSchema.json`, `docs/references/action-payload-matrix.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/api-data-source.md`
- Key decision: once the runtime contract is narrowed, active examples and current architecture/reference docs must stop documenting removed compatibility carriers immediately instead of keeping them as “maybe still supported” wording.

### 2026-05-11

- Removed the remaining built-in compatibility targeting paths for `setValue`, `setValues`, and `submitForm` so the runtime now matches the narrowed authoring contract exactly.
- Updated action docs and the open-ended adversarial review notes to freeze the new baseline: `setValue`/`setValues` are current-scope writes, while cross-instance writes/submits must go through `component:setValue` / `component:setValues` / `component:submit`.
- Updated focused tests in `flux-runtime`, `flux-action-core`, and `flux-compiler` to lock the canonical behavior and remove the old `formId`/`componentId` compatibility assumptions from built-in actions.
- Link to doc or code path: `packages/flux-runtime/src/action-adapter.ts`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-core/src/types/actions.ts`, `packages/flux-runtime/src/__tests__/action-adapter.unit.test.ts`, `packages/flux-action-core/src/__tests__/action-dispatcher-routing.test.ts`, `packages/flux-compiler/src/action-compiler.ts`, `docs/references/action-payload-matrix.md`, `docs/architecture/action-scope-and-imports.md`
- Key decision: built-in write actions and component-targeted capability calls are separate families; the runtime should reject or ignore cross-family targeting sugar instead of keeping silent compatibility fallbacks.

### 2026-05-11

- Fixed the `Tabs with forms` lab regression where switching tabs unmounted the inactive panel and reset nested form draft values on return.
- Added a focused cross-renderer regression test that locks down nested form value retention across tab switches, and recorded the manual-test issue plus bug note for the root cause.
- Link to doc or code path: `packages/ui/src/components/ui/tabs.tsx`, `packages/flux-renderers-basic/src/__tests__/basic-page-layout-structure.test.tsx`, `docs/components/tabs/design.md`, `docs/testing/2026/05-11.md`, `docs/bugs/48-tabs-form-panel-unmount-reset-fix.md`
- Key decision: panel lifetime belongs to the tabs visibility container, so the shared `TabsContent` primitive now defaults `keepMounted` to preserve nested owner runtimes instead of asking form runtime to recover from destructive unmounts.

### 2026-05-11

- Aligned the `container` playground lab with the clarified container-family docs so the primary shell example now adds padding explicitly through slot className props instead of visually implying a hidden default card padding.
- Kept the existing scenario titles stable to avoid unnecessary E2E churn while updating the intro and scenario copy to teach the intended baseline directly in the lab UI.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/container-lab-page.tsx`, `tests/e2e/component-lab/layout-content.spec.ts`
- Key decision: the misleading part lived in the demo presentation, not in `ContainerRenderer` or its contract tests, so the fix belongs in the playground schema rather than runtime code.

### 2026-05-11

- Reworked the container-family documentation baseline so `container` is described as the generic content-shell renderer rather than a catch-all wrapper.
- Updated owner docs to make the family split explicit across `container`, `flex`, `fragment`, `page`, `form`, `fieldset`, `tabs`, `dialog`, and `drawer`.
- Updated architecture spacing guidance to state why `container` keeps default body gap but intentionally has no default padding.
- Link to doc or code path: `docs/components/container/design.md`, `docs/components/flex/design.md`, `docs/components/fragment/design.md`, `docs/components/page/design.md`, `docs/components/form/design.md`, `docs/components/fieldset/design.md`, `docs/components/tabs/design.md`, `docs/components/dialog/design.md`, `docs/components/drawer/design.md`, `docs/components/index.md`, `docs/architecture/container-spacing-design.md`
- Key decision: container-family docs must define the selection boundary between owner containers, semantic group containers, pure layout containers, and no-UI structural nodes instead of describing each file in isolation.
- Next step: align playground examples so `container` demos stop visually implying default card padding when the architecture baseline says padding is explicit.
