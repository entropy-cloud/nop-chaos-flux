# Development Log - 2026-08-05

### 2026-08-05

- **C8.1 ai 会话主链审计执行完毕**（plan `docs/plans/2026-08-05-1314-2-c8-1-ai-conversation-main-chain-audit.md`，4 Phase 全 completed，5 卡 closed；closure-audit 待 mission-driver CLOSURE_VERIFY fresh session 执行）：
  - **审计**：5 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{ai-chat,ai-message-list,ai-bubble,ai-sender,ai-conversations}.md`），P0×0/P1×3（onConversationChange 静默 no-op + 事件派发缺 evaluationBindings ctx 跨 4 渲染器，ai-tool-call onApproval 同型）/P2×6/P3×1 裁决留痕。维度重点核对：值所有权三态（ai-chat messages/status turn-boundary structuredClone 投影、ai-sender 输入受控、ai-conversations 会话选择 scope-owned）pass；5 DOM 契约 marker + data-slot/data-cid 全对齐（`check:audit-missing-renderer-markers` 0 命中）；i18n key 双语核对 pass（唯一缺口 BranchPicker 硬编码英文 aria-label → P2 修复）；事件 payload 形状全核对发现 2 处契约问题（见 P1 修复）。
  - **P1 修复 ×3（test-first）**：① `onConversationChange` 声明（schema+definitions+renderers.md）但从不派发 → 静默 no-op（`ai-chat-conversation-change.test.tsx` 先红后绿，activeConversationId prop 变化时派发 `{ conversationId }` 含清空到 null）；② **事件派发缺 `event`/`evaluationBindings`/`scope` ctx**（ai-chat×5 + ai-sender×3 + ai-bubble×1 + ai-conversations×4 + ai-tool-call onApproval×1 共 14 处——action args 模板 `${id}`/`${text}`/`${action}` 等先前解析为空字符串，bug 83 / diff-view P1-10 同族先例；实测文档示例 `${event.x}` 亦不可解析——`getEvaluationScope` 仅合并 evaluationBindings+scope（flux-action-core/src/action-core.ts:206-208），`event` 绑定只用于 preventDefault 求值；修复补 ctx 后 c8-1 host 场景实证 `${id}|${conversation.title}`=c2|Second chat 等解析）。
  - **P2 ×6**：BranchPicker aria-label i18n（`flux.ai.branchGroups/branchPrevious/branchNext` 双语 key + p2-a11y-i18n 断言）；renderers.md §1.2 ai-chat schema 漂移（phantom `conversationId`/`onSend` 移除 + 10 个 live 字段补齐 + Events 总览 onSend 行删除 + `onConversationChange` 触发语义补注）；`ai-conversations-example.json`/renderers.md 14.2 `${event.*}` → 裸键同步；lab 页 ×5 补维度 18 缺口（`ai-{chat,message-list,bubble,sender,conversations}-lab-page.tsx` + `RENDERER_LAB_REGISTRY` + `AI_RENDERER_ROUTES`（route-model 接入）+ `COMPONENT_LAB_COVERAGE_MANIFEST` ×5 + route-matrix 更新 + multi-scenario-lab-page 注册 ai renderers）。
  - **共性缺陷裁决（CX-10 事后回写）**：事件 ctx 缺失根因跨 5 渲染器（含 C8.2 的 ai-tool-call onApproval）——按 roadmap §7b 当前 plan 内修复后回写 CX-10（`component-audit-roadmap.md` 新行，`planned`，父 plan closure audit pass 后标 done）；跨包文档口径校正：renderer-runtime.md Event Passthrough Contract 补注（evaluationBindings 裸键约定）+ layout-selection-guide/graph design.md 示例改裸键（`${event.value.id}` → `${value.id}`、`${event.node}` → `${node}`）。
  - **e2e pre-existing 复验**：C0 基线归属本 work item 的 `ai-chat.spec.ts:112` timestamp + `ai-rich-text-sender.spec.ts` ×5（Tiptap）——live repo 已由 commit `abcecfa9`（2026-08-04，本 plan HEAD 祖先）修复，本 plan 复验全绿（10/10 + 5/5，programmatic 断言），记录为 successor 修复证据（plan Phase 2 项「修复」以复验代实现——缺陷在基线快照后已被修复，非降级）。
  - **宿主场景 5/5**（`tests/e2e/component-lab/c8-1-host-surfaces.spec.ts`，programmatic DOM 断言，禁截图）：host-ai-dialog（**bug 73 专项**：ai-chat 在 openDialog 内流式对话 + 发送，mock connector 流式回复渲染于 dialog surface）、host-ai-stream（流式 DOM 契约：data-state processing→completed、气泡 data-streaming presence-only、aria-busy 翻转、cancel→aborted→sender 恢复可用）、host-ai-hitl（**HITL 死点击专项**：wired handler 快速双击仅 1 次派发 `approve|call_c8_1` + host setValue 翻转 decided → 徽章替换按钮（第二次点击不可达）；no-handler 卡按钮禁用）、host-ai-cc（会话侧栏 item 点击 → `${id}|${conversation.title}`=c2|Second chat + ai-chat onConversationChange=`c2`）、host-ai-sender（Enter 提交 trim 后 `${text}`=hello from lab + 字数计数）。
  - **验证**：ai 包 479/479 tests 绿（新增 conversation-change×3 + branch i18n×1；p1-renderers 3 断言更新为 payload+ctx 双参契约）；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；playground 141/141（route-matrix 新行校验）；e2e c8-1 5/5 + smoke 91/91 + ai 族回归 29/29 全绿（ai-chat 10 + rich-text 5 + conversations 4 + component-handle 2 + hitl 2 + virtual-scroll 1 + persistence 2 + branches-linkage 3）。
  - **roadmap C8.1 行**：执行证据已回写，`planned → done` 翻转待 CLOSURE_VERIFY closure-audit pass 后执行（执行 session 不自审）。**CLOSURE_VERIFY fresh session closure-audit 已 pass**（2026-08-05，独立审计子 agent：5 卡 closed 复验、P1 修复（onConversationChange 派发 + 事件 ctx）×14 处代码实证（ai-chat `:321`/ai-sender `:210/:230/:237/:243`/ai-bubble `:265-268`/ai-conversations `:31-134`/ai-tool-call `:285-298`）、宿主 spec 5/5 fresh 重跑 + ai-chat 10/10 + ai-rich-text-sender 5/5 全绿、ai 包 479/479 + workspace typecheck/build/lint 32/32 + test 59/59 全绿、plan-check --strict 全勾选）；**roadmap C8.1 行已由 mission-driver 翻转 `planned → done`，CX-10 依赖项同步标 done**（证据见 plan Closure Audit Evidence 与 plan 全文一致性）。
  - 文档：5 审计卡 + renderers.md §1.2/§13/§14.2 + renderer-runtime.md + layout-selection-guide + graph design.md + ai-conversations-example.json + plan/roadmap（C8.1 + CX-10 行）/daily log。

- **mission-driver 最终全量验证（full-green verification）**（plan `docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md` C7）：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；mobile 包 170/170（uncached 重跑）；e2e `c7-host-surfaces.spec.ts` 6/6 + mobile 族（mobile-components/m5-mobile-showcase/m2-touch）37 passed/1 skip 全绿；unit + e2e 双绿，提交含 full-green 标记。
- **C7 mobile 交互族审计执行完毕**（plan `docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md`，4 Phase 全 completed，5 卡 closed；closure-audit 待 mission-driver CLOSURE_VERIFY fresh session 执行）：
  - **审计**：5 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{pull-refresh,infinite-scroll,swipe-cell,countdown,notice-bar}.md`），P0×0/P1×9/P2×8/P3×1 裁决留痕。维度重点：值所有权三态（countdown time/paused、infinite-scroll hasMore/loading/error 运行时受控）pass；DOM 契约 5 marker + data-slot/data-state 全对齐（`check:audit-missing-renderer-markers` 0 命中）；**i18n key 核对为 pass**（`flux.mobile.*` 键与 locale 一致，en-US 解析经新增测试实证——早前 draft 中疑似 `n.` 前缀问题经 live repo 复核不存在）；notice-bar onClick/onClose 原生事件转发与 link/card 同族惯例一致（不强制 eventContracts）。
  - **P1 修复 ×9（test-first）**：① 事件派发缺 `event`/`evaluationBindings`/`scope` ctx（pull-refresh/infinite-scroll/swipe-cell×3/countdown 共 6 处——action args `${direction}`/`${source}`/`${side}` 先前不可解析，bug 83 同族先例，`docs/bugs/86`）；② 注册定义缺 eventContracts ×7（onRefresh/onLoadMore/onAction/onOpen/onClose/onFinish，carousel P1-1/diff-view P1-7 先例）；③ **NEW-C7-02 swipe-cell 操作区真实浏览器不可见**（bug 73 模式：region 恒 `translateX(-100%)/+100%` 位于行 overflow:hidden 裁剪盒外，open 时仅 content 平移露出空档——单测断言 content offset 从不断言 region 屏位；宿主场景 elementFromPoint 探针定位，修复为 region transform 跟随 openState + 同曲线 transition，`docs/bugs/87`）。
  - **P2 ×8**：lab 页 ×5 补维度 18 缺口（`pull-refresh/infinite-scroll/swipe-cell/countdown/notice-bar-lab-page.tsx` + `RENDERER_LAB_REGISTRY` + `MOBILE_RENDERER_ROUTES`（route-model 接入）+ `COMPONENT_LAB_COVERAGE_MANIFEST` ×5 + route-matrix 更新 + multi-scenario-lab-page 注册 mobile renderers）；design.md payload 文档 ×4 + countdown prefix/suffix「value-or-region」漂移更正。
  - **宿主场景 6/6**（`tests/e2e/component-lab/c7-host-surfaces.spec.ts`，programmatic DOM 断言）：host-pr-dialog（bug 73 专项：pull-refresh dialog 内 CDP 触摸下拉 → `${direction}|${threshold}`=down|50）、host-is-dialog（infinite-scroll dialog 内 immediateCheck → `${source}`=immediate）、host-is-retry（error 态文本 + 重试 → `${source}`=retry）、host-sw-action（行内滑动 → 点击操作按钮 → `${side}|${$slot.index}`=open-left|0 + 自动回弹）、host-cd-finish（1.5s 归零 → data-finished=true + `${type}`=finish）、host-nb-close/click/static。**触摸注入技术沉淀**：CDP `Input.dispatchTouchEvent` 是唯一可靠路径（JS 合成 TouchEvent 与 page.mouse-as-touch 均不可达 React 委托监听，详见 `docs/bugs/87` Notes）。
  - **验证**：mobile 包 168→**170 tests** 绿（新增 evalCtx×4 + i18n 锁定×3 + eventContracts×4 + NEW-C7-02×2）；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；e2e c7 6/6 + smoke 86 + mobile 7/1skip + m5 21 + m2 9 全绿（本 plan 零新增 e2e 失败）。
  - **共性缺陷裁决**：evalCtx/eventContracts 影响 4 组件但均为组件单点派发代码（无公共层根因）——按 roadmap §7 当前 plan 内修复，不插入 CX-n（bug 83/diff-view P1-10 先例一致）。
  - **pre-existing e2e 观察（非本 plan 引入，供 CLOSURE_VERIFY 核对）**：① `c5-2-host-surfaces.spec.ts:189` host-timeline 失败——timeline v2 commit `af7e89e7` 起 timeline 恒发 `data-ownership`，与 c5-2 display-only 断言冲突（clean HEAD 复现确认；该 commit 验证仅跑 w4b 9/9 未跑 c5-2；归属 timeline-v2 successor/CR）；② `c3-5-host-surfaces.spec.ts` editor/link 批次 flake（Tiptap 同族 pre-existing，单跑绿批次红，clean HEAD 复现确认）。
  - **roadmap C7 行**：执行证据已回写（`docs/backlog/component-audit-roadmap.md`），`planned → done` 翻转待 CLOSURE_VERIFY closure-audit pass 后执行（执行 session 不自审）。**CLOSURE_VERIFY fresh session closure-audit 已 pass**（2026-08-05，独立审计子 agent：5 卡 closed 复验、P1 修复 + eventContracts 代码实证、宿主 spec 6/6 fresh 重跑、mobile 170/170 + workspace typecheck/build/lint 32/32 + test 59/59 全绿、plan-check --strict 39/39 checked）；**roadmap C7 行已由 mission-driver 翻转 `planned → done`**（证据见 plan Closure Audit Evidence 与 plan 全文一致性）。
  - 文档：5 审计卡 + `docs/bugs/86` + `docs/bugs/87` + 4 份 design.md + plan/roadmap/daily log。

- **P2/P3 修正计划补充：i18n lint 三盲区实证 + Phase 1 范围修订**（plan `docs/plans/2026-08-05-1359-1-p2p3-rigor-remediation-plan.md`，保持 active）：
  - **核验结论**：`eslint.config.js:185-306` 实际已启用 `i18next/no-literal-string`（error 级，`mode:'jsx-text-only'`，覆盖 packages 源码，`pnpm lint` 生效）；`scripts/check-i18n-keys.mjs` 随 `check:i18n-keys` 校验 t() 用 key 定义完整性。但 3 盲区使 I1–I10 全部漏检：① `words.exclude '^[\s\d\W]*$'` 的 `\W` 匹配 CJK（实测 `选择图标`/`上一页` 被放行）；② `jsx-attributes.exclude 'aria-.*'` 放行 aria-label；③ `jsx-text-only` 不查 JS 字符串字面量（I1/I4–I6/I9–I10 均属 JS 非 JSX 文本）。
  - **修复验证**：修复版 `^(?!.*[\\u4e00-\\u9fa5])[\\s\\d\\W]*$`（文件层双反斜杠）下纯符号/数字仍放行、中文报错；临时 patch 后全仓 `eslint packages` 跑测 **0 新增 error**（零风险收益）。注意 JS 转义陷阱：sed 单反斜杠会把 `\s` 解析为 `s` 破坏规则（已通过 require 真实配置 + node 复现确认根因）。
  - **Phase 1 修订**：新增 eslint 第 1 盲区修复 item（`words.exclude` 正则 + Preflight 实证 + Exit Criteria 增加 `pnpm lint` 0 新增 error 与 `node -e` 复现记录）；Non-Goals 明确 ②③ 盲区不修（牵连 C7/C8 族 ~16 处 aria-label / 32 个含中文文件，超本 plan 审计范围，随 C7/C8 审计后统一评估）；`analysis` §8.5 对应更新"①落地、②③等待审计"。
  - 文档变更（plan/analysis 各一处），无代码变更，无 typecheck/build/lint/test 门禁。

- **mission-driver 起草下一批组件审计计划**（C7/C8.1/C8.2，3 个 work item，均 `draft → active`）：
  - **C7 mobile 交互族**（`docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md`）：pull-refresh/infinite-scroll/swipe-cell/countdown/notice-bar 5 组件增量 18 维审计（roadmap C7 定位"已有密集审计，仅增量——组件卡回填、DOM 契约维度、真实浏览器触摸场景"）；基线实测：definitions 127 行（注册 `:17/:41/:62/:82/:101`）、renderer 256/255/338/233/265 行、单测 144 `it(`、e2e mobile-components 8 + m5-mobile-showcase 21 + m2-touch 9；**5 组件均无 component-lab lab 页**（维度 18 缺口待补）。
  - **C8.1 ai 会话主链**（`docs/plans/2026-08-05-1314-2-c8-1-ai-conversation-main-chain-audit.md`）：ai-chat/ai-message-list/ai-bubble/ai-sender/ai-conversations 5 组件增量审计；**承接 C0 基线 e2e pre-existing 裁定项（successor C8.1）**：`ai-chat.spec.ts:112` timestamp + `ai-rich-text-sender.spec.ts` ×5（Tiptap）——计划内 test-first 修复并复验（`Fix` 非降级）。
  - **C8.2 ai 工具内容族**（`docs/plans/2026-08-05-1314-3-c8-2-ai-tool-content-family-audit.md`）：ai-tool-call/ai-attachments/ai-citations/ai-feedback/ai-token-usage 5 组件增量审计；重点安全红线（附件 URL/文件名校验）与 HITL 死点击场景专项。
  - 三 plan 均经**独立子 agent fresh session review**（task `ses_02fa4cbbcffeHDGkVhB9R8eBSz`）`pass-with-minors`（0 Blocker/0 Major）：Minor 仅 2 类——C8.1/C8.2 `src/index.ts` import 行号引用错误（已修正为 renderer exports `:39-43`/`:46-51`）与 `it(` 计数措辞（已用"约"）——达成共识后 `draft → active`；roadmap C7/C8.1/C8.2 行 `todo → planned`。
  - 不涉及代码变更，无 typecheck/build/lint/test 门禁；e2e pre-existing 修复在 plan 执行期进行。

- **timeline v2 受控当前事件落地**（plan `docs/plans/2026-08-04-2030-2-timeline-v2-controlled-current-event-plan.md`，Phase 1-3 全部完成，独立 closure-audit `approved`）：
  - timeline 由展示型扩展为支持受控当前事件：`value`/`defaultValue`/`valueOwnership`(local|controlled|scope)/`valueStatePath`/`onChange` 五字段落地于 `flux-renderers-layout`（`schemas.ts` TimelineItemSchema/TimelineSchema + `process-display-definitions.ts` propContracts/fields/eventContracts + `timeline-renderer.tsx`）。
  - value 解析链与 steps 同构（key 匹配 → 数字 clamp → 未匹配 -1），本地实现（`resolveEventIndex`）；**渲染层裁定**：未匹配 → 回退 defaultValue → 仍 -1 → 无 active（不回退首项，与 steps →0 兜底显式不同，design §2.1-1 已记录裁定：首版不抽共享 helper，第三个同族消费者出现时再提升 flux-core）。
  - 点击 seek 仅 `onChange` 声明时事件项可点（`data-clickable` + tabindex + Enter/Space），payload `{ value, index, item }`（含 `type:'timeline:change'`，steps 同构）；controlled 只派发不 mutate、scope 写 valueStatePath（缺路径降级 local + dev 告警）；active marker `data-state="active"` + 根节点 `data-active-index` + `data-ownership`；reverse 下 active 按逻辑顺序解析。
  - 关键排障：scope/degraded 单测首版忘记声明 onChange 导致点击不可达（timeline v2 可点态以 onChange 声明为门）——补 onChange 后三态写回用例通过。
  - focused 单测 +10（timeline-renderer.test.tsx 共 17 条）+ playground 演示（w4b 页新增 4 个 v2 demo：scope 写回/controlled 只派发/reverse+active/未匹配无 active）+ e2e +5（w4b-process-display-family.spec.ts 共 9 条全绿）。
  - 状态同步：flux-guide v2 标注翻「已实现」；design.md §2.1-1 裁定 + §3 实现状态；roadmap 新增 timeline-v2 work item 标 done；`renderer-markers-and-selectors.md` 登记 timeline v2 marker（data-state=active/data-active-index/data-clickable/data-ownership）。
- 全量验证（full-green verification）：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（layout 包 103/103、playground 141/141 uncached 重跑）；e2e `w4b-process-display-family.spec.ts` 9/9（4 既有 + 5 新增）。
- closure-audit（独立子 agent，fresh session `ses_02fe894b8ffeZ1OugDo16g4WGL`）`approved`：0 Blocker/Major，2 Minor（roadmap 前置声明「closure audit 通过」需与 Plan Status 翻转同步、daily log 需记录本条目——均在本条目中收口）。Plan Status → `completed`；roadmap timeline-v2 标 done；`examples.manifest.json` timeline 保持 `runtime`（v2 为能力增强，非新组件）。

- **G1 graph 图查看器落地**（plan `docs/plans/2026-08-04-2030-1-g1-graph-viewer-plan.md`，Phase 1-4 全部完成，待独立 closure-audit 翻转 `completed`）：
  - 新包 `@nop-chaos/flux-renderers-graph` bootstrap：`@xyflow/react ^12.10.2` + `dagre ^0.8.5` + zustand/use-sync-external-store；`vite.workspace-alias.ts`/`tsconfig.base.json`/根 `tsconfig.json` 三处 monorepo 集成；`flux-i18n` 增 `flux.graph.searchPlaceholder/clearSearch` 键。
  - `graph` renderer 全链路：`schemas.ts`（GraphNode/GraphEdge/GraphSchema + DEFAULT_LEVEL_MAP）、`graph-layout.ts`（dagre LR/TB 分层投影 + 畸形数据过滤硬契约）、`graph-search.ts`（子串匹配 + 循环索引）、`graph-store.ts`（Zustand vanilla per-instance：视口/布局/搜索/选中）、`xyflow-canvas.tsx`（只读适配层：nodesDraggable/nodesConnectable/selectionOnDrag 全禁用 + elementsSelectable:false + 受控 viewport）、`graph-node.tsx`（node region 编译 + label 回退 + data-level/data-selected/data-matching）、`graph-renderer.tsx`（7 句柄 + 单选模型 + empty slot）、`graph-definitions.ts`（propContracts/eventContracts/componentCapabilityContracts）。
  - 关键实现裁定（排障所得）：
    - **受控 nodes 下不得在 node 上写 `selected` 字段**——会触发 xyflow elevateNodesOnSelect z-index 更新风暴（`Maximum update depth exceeded`）；单选态改经 node `data` 下发（renderer 自持），xyflow 层 `elementsSelectable:false` 禁用内部选中状态机。
    - **回调 identity 稳定性**：xyflow 的 StoreUpdater/SelectionListener 依赖回调引用，canvas 全部回调经 `canvasHandlersRef` 镜像 + `useCallback([])` 稳定化，否则每次渲染触发额外事件派发。
    - **受控视口写回守卫**：`onMove` 值相等时跳过 `setViewport` 写回，避免 prop↔store 更新风暴。
    - region 模板表达式须用 `$slot.<绑定>` 语法（同 list `item` region）：`${$slot.node.label}`（design §4.3 已补注，`example.json` 已同步）。
  - playground `#/graph-demo` 演示页（hierarchy+flow 双布局、搜索、单选联动、畸形/空数据降级、5 个 component handle 触发按钮）+ e2e `tests/e2e/graph-demo.spec.ts`（8 条程序化断言全绿）。
  - 单测 42 条（layout/search/畸形数据/只读画布配置/node 视图/单选 payload/句柄失败路径/searchable:false 句柄共存）。
  - 状态同步：`examples.manifest.json` graph `targetContract→runtime`；roadmap G1 `proposed→planned`（`done` 待 closure audit）；design.md §3/§12/Checklist G 实现状态翻转。
- 全量验证：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；`tests/e2e/graph-demo.spec.ts` 8/8。
- closure-audit（独立子 agent）Major 修复：**悬垂边 dev 告警补发**（`computeGraphLayout` 现返回 `skippedEdges`，renderer 数据变化时 `console.warn` 一次，focused 单测断言告警 + 跳过行为）；design.md §10 边容器 marker 按首版范围裁掉（默认 xyflow 边，无自定义 slot）；`searchable:false` + `component:search` 句柄共存场景补 focused 单测（句柄注册无条件、UI 框按 searchable 门控；e2e 仅覆盖 searchable:true 图的内置框）。
- **closure-audit 通过（3 轮独立 fresh-session 审计链）**：首轮 ses_0300735f6ffekxS3YUIyEUXBK9 发现 Major-1（悬垂边 dev 告警缺失）+ 2 Minor；修复后复审 ses_030029e93ffekTC9mjIpng7Bj0 批准 Major 修复、指出 Minor-2 日志声称不实；补单测 + 修正日志后终审 ses_02ff7df1effeXnWmcg46OSg7gf `approved`。Plan Status → `completed`；roadmap G1 `planned→done`；`examples.manifest.json` graph 已 `runtime`。
- **mission-driver 最终全量验证（full-green verification）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；graph 包 fresh（uncached）重跑 typecheck + 42 单测通过；e2e `tests/e2e/graph-demo.spec.ts` 8/8 通过（19.6s）。unit + e2e 双绿，提交含 full-green 标记。

- **WorkbenchShell 面板调宽决策分析与计划起草**（文档产出，无代码变更）：
  - **分析报告**：`docs/analysis/2026-08-05-page-vs-workbench-layout-analysis.md` —— 裁定：(1) **不扩展 page 吸收 workbench 布局**（语义错位 + WorkbenchShell 已共享三 family + 折叠态须留 designer core + 契约膨胀）；(2) 左右收缩已有（WorkbenchShell rail + core store），保留在 workbench 侧；(3) 上下不收缩/不调高（top 是 chrome，bottom 当前无面板）；(4) 左右拖拽调宽**需要且 opt-in**，落 WorkbenchShell，状态归 family。
  - **文档补充**：`docs/architecture/designer-workbench-shell.md` 新增 Panel Resize Contract 节（opt-in、handle 贴中心侧、pointer + 键盘 Arrow 等价、role=separator + aria-valuenow/min/max、宽度 family-owned、collapsed 无 handle、默认宽度不变）+ "Relationship To Page Regions" 补 page 边界说明（page.aside 保持业务页单侧栏，page 不吸收 workbench）。
  - **计划起草**：`docs/plans/2026-08-05-1540-1-workbench-shell-panel-resize-plan.md`（draft，待独立子 agent plan review 后 `draft → active`）——4 Phase：① WorkbenchShell opt-in 调宽（Decision：保持 grid + pointer handle，拒绝 RRP 迁移；test-first 7 组单测）；② core shellState 宽度 + designer:setPanelWidths 命令/action 全链路（types/shell-state/shell-controls/snapshot/core.ts/command-types/adapter/action-provider/manifest）；③ designer-page 接线 + playground schema + e2e（resizable spec 新 + collapsible 8/8 零回归）；④ 文档同步。Test Strategy：必须自动化（共享组件 + public action 契约）。
