### 2026-04-28

- Executed Plan 150: `xui:actions` — Schema-Local Named Action Chains. All 3 phases implemented and tested.
- **Phase 1 (Core Types & Compiler)**: Added `XuiActionDefinitions` type, `namedActionPlans` field on `TemplateNode`, `pushNamedActionSymbols()` for symbol table, `xui:actions` namespace validator in diagnostics, two-stage compilation in `compileSingleNode()`.
- **Phase 2 (Runtime & Action Resolution)**: Added `createNamedActionProvider()` in flux-core (parent scope fallback for inheritance), `runNamedAction()` step in action dispatcher (step 3), NodeRenderer registration/unregistration of `__xui_actions__` namespace provider.
- **Phase 3 (Tests)**: 10 compiler tests, 8 diagnostic tests, 6 provider tests. All pass.
- Key files: `packages/flux-core/src/named-action-provider.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-action-core/src/action-dispatcher/action-runners.ts`, `packages/flux-react/src/node-renderer.tsx`
- Key decisions: `__xui_actions__` synthetic namespace reuses ActionScope mechanism; provider falls back to parent scope for lexical inheritance; resolution order: built-in → component → named → namespaced → error; `xui:actions` read directly from schema (not via `fieldInspection.extensions`).
- Next step: update `docs/architecture/action-scope-and-imports.md` with xui:actions documentation

- Executed plan 149: Deep Audit Remediation — 6 workstreams completed.
- **WS1 (P1)**: variant-field FieldFrame property forwarding — added 7 missing BoundFieldSchemaBase attributes (required/hint/description/remark/labelRemark/labelAlign/labelWidth). Extracted `toFieldRemarkProps` from node-frame-wrapper.tsx to field-frame.tsx as shared utility.
- **WS2**: BEM `--` modifier cleanup — replaced `nop-field--label-top`/`nop-field--label-left` with `data-label-align` attribute in field-frame.tsx + default-spacing.css; replaced `nop-tabs--${mode}`/`nop-tabs--sidebar-right` with `data-tabs-mode`/`data-tabs-sidebar-right` in tabs.tsx. Updated `container-spacing-design.md`.
- **WS3**: schema-compiler exception observability — added `unhandled-compilation-error` to SchemaDiagnosticCode, emit structured diagnostics in `validateSchemaInput` catch block instead of silently swallowing.
- **WS4**: Fixed `flux-runtime-module-boundaries.md` — corrected `packages/flux-action-core/src/utils/debounce.ts` to `packages/flux-core/src/utils/debounce.ts` with re-export note.
- **WS5**: Async unmount protection — added mountedRef stale-check to variant-field `runDetectVariantAction` (useEffect) and detail-field `handleOpen`/`handleConfirm` (multiple await points).
- **WS6**: Test file splits — `object-field.test.tsx` (755→3 files <320 each), `controller-inspect.test.ts` (750→2 files <450 each). All test counts preserved.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`, `pnpm check:oversized-code-files` all pass.

- Completed full 18-dimension deep audit of nop-chaos-flux, following the audit manual at `docs/skills/deep-audit-prompts.md`. Each dimension went through initial review (初审) → dimension review (维度复核) → sub-item review (子项复核) for high-risk findings. Total 38 sub-agents dispatched.
- Link to doc or code path: `docs/analysis/2026-04-27-deep-audit-full/` (18 dimension files + summary.md)
- Key findings:
  - **1 P1**: variant-field manually instantiates FieldFrame but missing 7 BoundFieldSchemaBase properties (required/hint/description/remark/labelRemark/labelAlign/labelWidth) — `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx:243-256`
  - **20 P2**: 8 renderers missing testid/cid, 2 BEM `--` remnants, 2 oversized test files, compiler exception swallowing, full-store subscribe in reaction-runtime, doc path drift, async missing cancellation, type naming issues
  - **10 P3**: observation items
  - **0 P0**: no security breaches or core data corruption risks
  - Dimensions 04 (State Ownership) and 06 (Async Safety) scored "excellent" with zero findings
- Key decision: Dim 16 P1 downgraded to P2 after sub-item review — only 1 of 75+ doc paths was wrong (debounce.ts), concept description was correct
- Next step: fix P1 variant-field FieldFrame property forwarding, then address P2 items by ROI order (testid补全 → test file splits → compiler logging → BEM cleanup)

### 2026-04-28

- Drafted Plan 149 (`docs/plans/149-deep-audit-remediation-plan.md`) to execute deep audit fixes. Plan went through 3 rounds of independent sub-agent review:
  - Round 1 (scope/baseline): 8 issues found → all fixed
  - Round 2 (technical feasibility): 7 issues found → critical discovery that dim 09's 8 testid/cid findings were **false positives** (all renderers have `wrap: true`, FieldFrame already provides testid/cid). Also identified `toFieldRemarkProps` is private, `dispatch()` doesn't accept AbortSignal, BEM cleanup is larger than initially scoped
  - Round 3 (consensus): **Approved**. All 15 revision points verified against live code. 3 non-blocking suggestions applied.
- Link to doc or code path: `docs/plans/149-deep-audit-remediation-plan.md`
- Key decisions:
  - Removed WS2 (testid/cid) entirely — confirmed false positive after 3 rounds of independent verification
  - WS5 (async protection) changed from AbortController to stale-check pattern — `dispatch()` API limitation
  - WS1 and WS5 must be serial (both modify variant-field.tsx)
  - WS2 BEM expanded to include `nop-field--label-left` and `nop-tabs--sidebar-right`
- Plan status: `planned`, ready for execution
- Next step: execute WS1 (variant-field FieldFrame property补全) first, then WS5, with WS2/WS3/WS4/WS6 in parallel
