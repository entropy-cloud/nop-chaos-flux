# Development Log - 2026-08-24

### 2026-08-24（mission driver：plan `2026-07-25-2` gantt/ai e2e 全覆盖执行轮——Phase 1/2/3 全部收口 + 3 个 live defect 修复 + 1 个 runtime 缺陷登记）

- **背景**：执行 `docs/plans/2026-07-25-2-gantt-ai-e2e-test-coverage-and-fix-plan.md` 剩余全部 Phase（~200 项 checklist）。接手时工作树含上一轮未提交半成品（`/#/ai-coverage` fixture、`ai-chat-states`/`ai-bubble-content`/多个 gantt spec 均 untracked 且从未全绿）。
- **Phase 1（Gantt 矩阵 → 11 specs / 105 tests 全绿）**：新增 `tests/e2e/gantt-coverage-gaps.spec.ts`（root role/tabindex 直断言、Fit 回中间级、内联 blur 提交、bar aria-label、Backspace、键盘逐父折叠↔展开、动态删空任务转空态、默认 zoom 格式）；**Fix**：`gantt.tsx` 内置默认 zoomLevels 字面 `'MM/DD'/'YYYY'/'DD'`（formatDate 只认 `%`-token，未配置 zoomLevels 的 gantt 渲染字面占位符）→ `'%m/%d'/%Y/%d` + `gantt-states-demo.tsx` 增 `gantt-default-zoom` fixture；修正 `gantt-mount-timing.test.tsx` scroll-sync 旧 wrapper 断言（上一轮已把 gridRef 移到真实滚动容器）。
- **Phase 2（AI 矩阵 → 16 specs / 105 tests 全绿）**：新增 `tests/e2e/ai-coverage-widgets.spec.ts` 31 tests（sender 模式/限额/取消、conversations 全生命周期、welcome/prompts/feedback/tool-call 状态+HITL focus trap、attachments 拖拽/粘贴/限额/上传门控、citations 变体、voice 双态注入、token-usage 环形比例、suggestions 三 overflow 模式）；**Fix×3**：`docs/bugs/164`（setMessageEditing 原地写数组不换身份 → ai-chat context memo 不重算 → 编辑 UI 永不出现）、`docs/bugs/165`（image renderer slice 层注册却渲染整条消息 → 混合内容 grid 重复 N 份 → messageLevel 化 + occurrence key 替 index key）、`user-edit.tsx` 补 Escape 取消；**发现+缓解**：`docs/bugs/163` runtime `xui:imports` overlay 捕获 schema-event setValue 写入（写入落 `*.body_N_:imports:*` 临时 scope，页面公式永不可见；订阅链仅最后一个 bridge 收到 form-store-backed page 写入，未时间盒内根因收口）——`/#/ai-coverage` 改 page data 供 connectors 绕开，runtime 修复留 successor；修复上一轮 spec 半成品（同元素定位器 / A-5 banner 对齐 / portal / marker-slot）；删遗留探针 `zz-live.test.tsx`。
- **Phase 3**：核对 28 个 gantt/ai spec `assertTrackedPageErrors` 全覆盖、`allowConsoleErrors`/`waitForTimeout` 零残留；`retries:1` 已在 config；新增 `docs/references/e2e-data-slot-selectors.md`（全量 data-slot 清单 + 定位器陷阱）。
- **验证（full green）**：gantt+ai e2e **210 passed / 27 files**（独立 closure-audit 复跑确认）；`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 全 workspace unit 全绿、`pnpm check` exit 0（`create-engine.ts` 注释压缩回 700 行内，零新增红）。
- 本轮新文件：`tests/e2e/gantt-coverage-gaps.spec.ts`、`tests/e2e/ai-coverage-widgets.spec.ts`、`packages/flux-runtime/src/__tests__/scope-composite-chain.test.ts`、`docs/bugs/163-165`、`docs/references/e2e-data-slot-selectors.md`。
- **BUILD_VERIFY 复验（verify 轮独立复跑后提交）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 68/68 tasks 全绿、`pnpm check` exit 0（零新增红）、gantt+ai e2e 27 files **210 passed**（2.6m）本轮独立复现——full-green 确认后按 code(docs) 分批提交。

### 2026-08-24（component-audit-round2 mission driver：plan `2026-08-11-1929-3` claim-vs-reality 四 Phase 执行——五份历史 plan 状态事实性修正 + componentName 幽灵契约消除 + surface-lifecycle 文档自洽 + refreshSource/refreshNearest 失败传播契约）- **背景**：执行 `docs/plans/2026-08-11-1929-3-claim-vs-reality-plan-doc-contract-integrity-remediation.md`（4 Phase 全 planned；P1-03..P1-06 + 折叠 P2-11/12/13/14/15/22/23/34/35）。

- **Phase 1（plan 状态事实性修正）**：五份历史 plan 按 live 代码逐项核对后修正——①gantt-ai-e2e（P1-03）：`completed → in progress` + Outdated Note；Phase 0 七项核对（6 补勾 + bars-width 项 superseded 注记，`gantt.tsx:514` minHeight 路径）；撤回「~80/~100 功能点全覆盖」「无 waitForTimeout 残留」失真 gates（live：gantt 50 tests/4 specs + ai 47 tests/13 specs、waitForTimeout 20+4 处、gantt-perf allowConsoleErrors(10)）。②452（P1-04）：`completed → in progress`；Phase 1/2 共 14 项核对全落地补勾（surfaceForm 链路 `renderer-helpers.ts:264-266` + `resolveInScope` 单路径 `component-handle-registry.ts:279-309`）；`schema.ts:83` 幽灵字段移交 Phase 2。③444（P2-22）：保持 completed + Outdated Note + 11 workstream 逐项抽核补勾（~30 项带 live 锚点）；3 项偏差（17-03 rename 未落地 / 15-01 stopWhen null-member 特例仍在 / 17-04 命名映射未见实证）保持未勾选并移入 Deferred But Adjudicated。④2026-07-28-1430（P2-23）：Phase 1/2/3 共 19 项按实际落地形态补勾（close 两项按 sync fire-and-forget `surface-runtime.ts:199-229` 改写）+ 执行偏差摘要（hook 存储 ActionSchema 化 / sync close / 触发位置 form.tsx）。⑤CR 0329-1（P2-34）：4 项 R2 追加项（19-1/19-2/23-1/23-2）live 核对已落地补勾 + Closure Evidence 修正注记。修正摘要同步登记 `docs/logs/2026/08-11.md`（2026-08-24 追记节）。
- **Phase 2（componentName 幽灵契约消除，Decision (a)+(b) 组合）**：删除 `flux-core/types/schema.ts` `ActionShapeLikeFields.componentName` + `flux-guide/flux-types/common.d.ts` 同步；`flux-compiler` `validateActionShape` 加 `invalid-action-shape` 拒绝（message 提示 componentId 迁移，兑现 Failure Paths「编译期诊断而非静默丢弃」；index signature 使「类型不可写」断言不可行故选组合方案）。**先红后绿**：`schema-compiler-componentname-targeting.test.ts` 修复前 1 failed → 后 flux-compiler 40 files/553 tests 全绿。文档族清扫：`action-scope-and-imports.md` 12 处 + `dialog`/`drawer`「remains supported」失真断言删除（live registry 无此 selector）+ 折叠 P2-12 六份架构文档（capability-contract-model / flux-core / flux-runtime-module-boundaries / static-capability-validation / surface-owner / template-instantiation）targeting 语境 componentName 零残留。
- **Phase 3（surface-lifecycle-callbacks.md 自洽）**：§Relationship With Declarative Surface 重写（declarative 仅 function-based onClose、submit hooks 只对 action-style 有效——与 :289 及 live `use-surface-renderer` 不写 onSubmitSuccessNodes → triggerHook skipped 三方一致；「行为对齐」改标注为未承诺项）；§Finding Algorithm 对齐 live（auto 匹配集含 form、`handle.type`、registry.parent 链）；findFirstInScope 改写为已实现状态；console.warn 片段同步 `reportRuntimeHostIssue`；行号锚点全部改函数名/调用点锚点；`ActionNode[]` 存储描述更正为 `ActionSchema | ActionSchema[]`。
- **Phase 4（异步失败传播契约）**：`DataSourceRefreshResult` 扩展 `ok?/error?` 三态契约 + 新增 `DataSourceRefreshOutcome {found, result?}`（not-found 与 request-failed 可区分）；`runRequest` 返回 `DataSourceRequestRunOutcome`（succeeded/failed/cancelled/deferred 全 return 路径分类）→ `refresh()` 映射 → registry `runControllerRefresh` 归一化 rejection → refreshSource/refreshNearest/`component:refresh` 三出口失败传播（`{ok:false, error}`，then 不执行）；formula controller 补 `ok:true`；`resolveInitFetch` 裸 catch 补 `reportRuntimeHostIssue`（保守 fetch 保留）；`blob-download` 损坏 JSON / 无文件名改 `{ok:false}` 不再合成成功；`initFetch` 类型放宽 `boolean | string`（动态表达式类型面如实化）；`api-data-source.md` 契约节重写。**先红后绿**：`runtime-sources-refresh-failure.test.ts` 6 用例 + `blob-download.test.ts` 2 用例修复前 **8 failed** → 全绿；7 个既有断言按新 outcome 形态更新；component:refresh 失败传播新增渲染器级测试（onError 触发 + then 不执行）。
- **验证**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 tasks 全绿（flux-runtime 1431 / flux-renderers-data 871 / flux-compiler 553 / flux-core 513）；`pnpm check` exit 0 仅既有登记红（oversized 2 exempt locale）零新增。
- 偏差：444 抽核发现 15-01（stopWhen null-member 特例未按原案移除）与 17-04（命名映射文档未见实证）两项历史执行偏差，已按 Deferred But Adjudicated 裁定处理（非本 plan scope，watch-only + successor 路径登记）。

### 2026-08-24（component-audit-round2 mission driver：plan `2026-08-11-1929-2` flux-bundle facade 宿主契约收口——P1-01/P1-02 修复 + 门禁升级 + 独立 closure-audit approved + Plan Status → completed）

- **背景**：执行 `docs/plans/2026-08-11-1929-2-flux-bundle-facade-host-contract-remediation.md`（Phase 1/2 均 planned，全项未勾）。两 P1：P1-01 打包 d.ts 引用未声明 `@nop-chaos/flux-core`（干净宿主 TS2307）+ 门禁只查 manifest 不查 d.ts；P1-02 `FluxSchemaRendererProps` 类型承诺 10 props 实现 6 个（静默 no-op）。
- **P1-01 Fix（Decision (a) 类型自包含）**：flux-core 不发布 registry，(b) peer 声明必坏干净安装；手写内联漂移风险高 → **构建期 dts 打包**：`scripts/prepare-flux-bundle-dist.mjs` 以 `dts-bundle-generator`（root 新 devDeps：`dts-bundle-generator ^9.5.1` + `typescript ^6.0.3`）从 `types/public-types.d.ts` 生成自包含 `dist/index.d.ts`（引用 flux-core 类型全量内联，react 保持外部 peer import；authored face 文本零改动）。门禁升级：`check-flux-bundle-pack.mjs` 拆出可导入 `verifyTarball`/`findUndeclaredTypeReferences`（run-as-main guard），解包扫描 d.ts 的 `from/import('@nop-chaos/*')` 逐一断言 manifest 声明，缺失精确报包名；prepare 侧同步自检（残留内建 import 即构建失败）。
- **P1-02 Fix（Decision (a) 完整转发）**：`packages/flux-bundle/src/index.tsx` 补齐 10 个透传 props（plugins/pageStore/surfaceRuntime/moduleCache/parentScope/actionScope/componentRegistry/onRuntimeChange/onComponentRegistryChange/onActionScopeChange），类型面不变；新测试 `src/__tests__/index-props-forwarding.test.tsx` 以 vi.mock `createSchemaRenderer`（importOriginal 部分 mock 保真传递依赖）逐字段 identity 断言 16 个类型可传 props + registry 闭包 + facade formulaCompiler。
- **先红后绿实证**：门禁回归测试 `scripts/__tests__/check-flux-bundle-pack.test.ts`（4 用例：纯函数正/负 + 模拟 tarball 门禁失败 + 自包含通过）实现前 4/4 红；真实工件先红（升级后门禁对 live 缺陷工件报 `references undeclared @nop-chaos packages: @nop-chaos/flux-core`）→ d.ts 打包后绿。props 测试修复前红（`expected undefined to be [{name:'fixture-plugin'}]`）→ 补齐后绿。
- **clean-install tsc 实证**（可重复，命令记录于 plan Phase 1 证据）：temp dir `npm install <repo>/dist-packages/nop-chaos-flux-0.1.0.tgz react@^19 --legacy-peer-deps` + `tsc --skipLibCheck false` → exit 0 零 TS2307；tarball d.ts @nop-chaos 引用计数 0；tarball index.js 含 5 个转发实码抽查。
- **全量验证**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 task 绿、`pnpm check` exit 0（oversized 2 条为既有 exempt 登记，零新增）、`pnpm test:scripts` 9 files / 51 tests、flux-bundle 3 files / 8 tests。e2e 未跑（mission scope 不含），不声明 full-green。
- **owner doc**：`docs/architecture/frontend-baseline.md` Host-Facing Release Baseline 补两条——自包含类型面规则（构建期打包 + 门禁扫描）与 props 完整转发契约（注明钉住测试路径）。
- **独立 closure-audit（fresh session `ses_fce9f61d4ffeDNNZu6sM6fCscf`）**：verdict **approved**，0 Blocker / 0 Major / 1 Minor cosmetic（`prepare-flux-bundle-dist.mjs:49-50` index.js no-op 重写，预存行为）+ 1 Informational（未提交属 human gate）。审计独立复跑回归测试 4/4、门禁 exit 0、flux-bundle 测试绿、clean-install tsc 复现、类型面 git diff 空、转发集逐字段核对一致。
- **收口**：Plan Status → completed，Closure Gates 全勾，Closure 证据回填。源 multi-audit 保持 planned（P1-03..06 归 1929-3 等其余路由计划）。未提交（mission 未含 commit 指令，human gate 处置）。

### 2026-08-24（mission `ai-widgets-product` BUILD_VERIFY：全量验证 + 基线红 surface-event-ctx 收口）

- **背景**：CHECK 修复（wizard 拆分 / manifest-deps 迁移 / industrial hub 重命名 / doc anchor）已落地未提交，本轮执行 BUILD_VERIFY 全量验证。开工先 `git stash -u` 验证：唯一失败在 master HEAD 上即存在（与 CHECK 改动无因果）。
- **唯一失败与修复**：`pnpm test` 首轮 67/68——`flux-renderers-basic/surface-event-ctx.test.tsx` 第 2 例（`getByTestId('surface-confirm-submit')` 找不到），即 08-20 登记、08-24 plan 1929-1 closure gate 注记的 baseline 红（bisect 已归因 `f616f0163` plan 459 语义变更令旧测试"X 关闭后仍挂载"前提过期）。修复采用 10-F-01 裁定的契约修订分支，且直接对齐并行 worktree commit `66476513d`（plan 460 B1 "escaped regression repaired at equal strength"，2026-08-19 已在另一 worktree 修复但未落 master）的测试版本：confirm-first 单生命周期捕获 onConfirm `/confirm-<id>` 与 onClose `/close-<id>` 并断言同一非空 surfaceId——保留 plan 459/460 受控 X-close 拆除语义，仅修过期测试契约。修复后 basic 500/500。
- **全量验证（最终态）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37（0 errors，scheduling 1 条预存 warning）、`pnpm test` 68/68 tasks 全绿、`pnpm check` exit 0（`runtime-raw-schema-read` 2 命中为 `d5214cc48` 预存，非本次引入）。e2e 本轮未跑（mission 步骤未含），不声明 full-green。
- **登记同步**：`docs/audits/check/00-baseline-tooling.md` 偏离 2 + `docs/audits/check/SUMMARY.md` 基线偏离 2 补"已解决"注记；DV 单测基线红清零。

### 2026-08-24（component-audit-round2 mission driver：plan `2026-08-11-1929-1` 收口——Closure Gates 执行 + 独立 closure-audit approved + Plan Status → completed + 源 open-audit 翻 closed）- **背景**：plan 五 Phase 已于 2026-08-12 全部落地（commit `4152e4840`，四包测试/typecheck 绿），本轮补齐剩余 Closure Gates：全量验证、独立 closure-audit、收口落档。

- **收口审计前置核对（执行 session 自查，非 closure-audit）**：逐 phase live 抽查修复在位——barcode poll 自挂载运行 + 关闭态→打开转换测试、pivot `loading` 入生命周期 deps（:183）与 release 路径（:130）、wizard committing 门（:278/:315）+ 冻结快照（:442）+ stepError 真实消息（:621）、dropdown hover 150ms 延时关闭 + 菜单取消、parseDate ISO fallback（date-utils.ts:184）+ 三渲染器 value 路径接线；bug note 161 / design.md（continuousScan 默认 false、wasmUrl fail-closed）/ pivot `indicators[].format` 零残留均确认。
- **全量验证**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm check` exit 0（仅 2 个 [exempt] locale 登记豁免，零新增命中）。
- **`pnpm test` 51/52 包绿 + 唯一失败的 out-of-scope 裁定**：`flux-renderers-basic/src/__tests__/surface-event-ctx.test.tsx`（500 用例中 1 失败）——**bisect 实证**（stash + 逐 commit 复跑）：`f616f0163^` 绿 → `f616f0163`（plan 459 controlled dialog X-close，2026-08-18）红 → 此后持续红；即引入时间晚于本 plan 落地（2026-08-12）6 天，属 plan 459 语义变更（X-close 改为拆除 surface）令旧测试前提过期，非本 plan 改动。与既有登记一致：`docs/logs/2026/08-22.md`（bug 162 轮 stash 验证为 pre-existing）+ `docs/audits/check/00-baseline-tooling.md:37`（"待独立 plan 修"）。本 plan 四包（form/layout/pivot/scheduling）全绿。**本轮不声明 full-green**。
- **独立 closure-audit（fresh session `ses_fced38d61ffekYQxxfhDchwcxX`）**：verdict **approved**，零 Blocker/Major/Minor。G1-G9 逐门 PASS（含 live 逐处核验修复语义、deferred 诚实性、owner docs 同步）；审计 session 独立复跑四包全绿（scheduling 920 / pivot 56 / layout 124 / form 813）+ distrust 复跑 flux-renderers-basic 仅既有登记红；`pnpm test` 门禁裁定 = 可勾（因果无关 + 已登记 baseline 红 + 四包独立复绿，符合 repo registered-baseline-red 惯例）。
- **收口落档**：plan Closure Gates 9 项全 [x]（gate 9 附 baseline 红注记）、Closure 节证据回填、`Plan Status → completed`；源审计 `docs/audits/2026-08-11-1929-open-audit-component-audit-round2.md` `planned → closed`（其 P0/P1+折叠 P2 全部由本 plan 收口，其余 P2 已路由 backlog）；`docs/backlog/audit-followups-2026-08-11-1929.md` 补收口注记。multi-audit 保持原状（1929-2/1929-3 仍 active，归其收口轮处置）。
- 偏差：无（工作区其余未提交改动属同日 ai-widgets-product CHECK 修复 mission，未触碰）。

### 2026-08-24

- **mission `ai-widgets-product` CHECK 门禁修复（gate restore to DV 08-09 baseline）**：CHECK 入口跑 `pnpm check` exit 1。诊断出 **4 个红门禁**（`&&` 链在第一失败处截断，后 3 个被掩盖），全部为 2026-08-12（4152e4840）相对 DV 08-09 全绿基线的回归：
  1. `check:oversized-code-files`：`flux-renderers-layout/wizard-renderer.tsx` 716 行（08-20/08-23 已登记未注册 ERROR 级超限）。
  2. `check:active-doc-code-anchors`：`docs/components/flux-renderers-ai/design.md:378` → `docs/plans/2026-08-23-0002-1-a7-d6-latex-impl.md`（未落档的待起草 plan 路径，来自工作区未提交的 LaTeX 决策编辑）。
  3. `check:workspace-manifest-deps`：`flux-compiler` 两个测试文件（plan 462 引入）import 11 个未声明的下游 workspace 包。
  4. `check:audit-event-dispatch-ctx`：industrial 6 处命中（08-15/16/17 登记"既有预存红"）。
- **Fix 1（wizard 拆分）**：`wizard-renderer.tsx` 716→631 行。新增 `packages/flux-renderers-layout/src/wizard-step-nav.tsx`（137 行，`WizardStepNavItem` 每步导航项组件，`owner` prop 仿 `wizard-step-body.tsx` 模式），主文件保留 map+filter 与全部交互/lifecycle 逻辑。layout 124/124 绿。
- **Fix 2（doc anchor）**：design.md LaTeX 决策段移除未落档 plan 路径，改为"（实现 plan 待起草，起草后在此回填 plan 链接）"。
- **Fix 3（manifest-deps，两轮）**：
  - 第一轮（**已回退**）：flux-compiler 补 11 条 devDeps → `check:workspace-manifest-deps` 绿，但引入 flux-compiler ⇄ renderers 构建依赖环，`pnpm build` 失败（turbo `^build` 拓扑拒绝环）——上游包不可 dev-depend 下游，判定测试文件架构错位。
  - 第二轮（**落地**）：`git mv` 两个测试到 `apps/playground/src/`（`renderer-prop-coverage-audit.test.ts` + `flex-direction-col-rejection.test.ts`），`./schema-compiler.js` 内部导入改公开 API `@nop-chaos/flux-compiler`（index.ts:1 已导出 `createSchemaCompiler`）；playground 补 2 条缺失 devDeps（`flux-renderers-scheduling`、`flux-renderers-dashboard`）；`docs/architecture/renderer-runtime.md:501` 锚点同步新路径+迁移原因。**附带收益**：08-23 登记的 5 个 pre-existing typecheck 错误（vite ImportMeta env + CSS side-effect import）随之消失——根因正是这两个测试把 renderer 源码拉进 flux-compiler 编译程序。迁移后 `pnpm typecheck` 37/37 全绿（HEAD 原为 5 errors）。playground 175/175、flux-compiler 551/551 绿。
- **Fix 4（event-dispatch-ctx）**：核实 6 处命中全部是 `Animator`/`PointStore`/`RefreshPipeline` 内部 `EventHub` 类型化 pub/sub（`.on()` 订阅、engine 内部消费），非 `props.events` schema 派发面——门禁正则 `ident.events.<name>(` 对 `this.events.emit(` 误捕获。根因修复：私有字段 `events`→`hub` 重命名（animator.ts/point-store.ts/refresh-pipeline.ts，声明+引用共 14 处，private 零 API 影响，不改门禁脚本、不扩 allowlist）。industrial 1449/1449 绿，复扫全仓 0 命中。
- **验证（最终态）**：`pnpm check` exit 0（14 项全链）、`pnpm typecheck` 37/37 exit 0、`pnpm lint` 37/37 exit 0、`pnpm build` 37/37 exit 0；受影响包测试 layout 124/124、industrial 1449/1449、playground 175/175、flux-compiler 551/551。未跑全量 `pnpm test`（BUILD_VERIFY 职责；08-20 登记的 flux-renderers-basic surface-event-ctx 1 用例失败未在本 scope 内触碰）。
- **git 状态**：修复涉及 `packages/flux-renderers-layout/src/wizard-renderer.tsx`（改）+ `wizard-step-nav.tsx`（新）、`packages/flux-renderers-industrial/src/binding/{animator,point-store,refresh-pipeline}.ts`（改）、`apps/playground/src/{renderer-prop-coverage-audit,flex-direction-col-rejection}.test.ts`（迁入）、`apps/playground/package.json`（+2 devDeps）、`docs/architecture/renderer-runtime.md`（锚点）、`docs/components/flux-renderers-ai/design.md`（未提交 LaTeX 段的路径修正）、`pnpm-lock.yaml`。未提交（mission 驱动，等 BUILD_VERIFY 后统一处置）。
