### 2026-04-26

- Re-ran Plan 143 closure validation from the workspace root and then commissioned a fresh independent closure audit. Workspace `pnpm typecheck`, `pnpm build`, and `pnpm lint` now complete successfully; workspace `pnpm test` ran through most packages successfully but still timed out before the full recursive run returned. The independent audit (`ses_235a1832fffeBbNYQhAWMXCLFx`) confirms the plan must remain open: `flux-runtime`, `flux-react`, and `flux-renderers-data` now clear their enforced package gates, but `flux-renderers-form` still fails direct coverage execution on a timed-out validation test and `flux-renderers-form-advanced` still misses the enforced 80% branch threshold.
- Link to doc or code path: `docs/plans/143-unit-test-coverage-80-percent-target-plan.md`, `packages/flux-renderers-form/src/__tests__/form-validation-rules.test.tsx`, `packages/flux-renderers-form-advanced/vitest.config.ts`
- Key decision: keep Plan 143 as `partially completed` and do not force-close it based only on the now-green `flux-runtime`/`flux-react`/`flux-renderers-data` package state, because the closure audit found two remaining in-scope blockers that still need implementation work.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, long-running `pnpm test` (timed out late), independent closure audit `ses_235a1832fffeBbNYQhAWMXCLFx`
- Next step: fix the `flux-renderers-form` timed-out validation test under coverage and raise `flux-renderers-form-advanced` branch coverage to 80%, then run another closure audit.

### 2026-04-26

- Continued Plan 143 by closing the remaining `flux-renderers-data` package coverage gap. Added focused unit tests for table control ownership hooks, chart renderer branches, virtual table body behavior, table data/fixed-layout/row-rendering helpers, CRUD state helpers, and quick-edit branches. `flux-renderers-data` now passes its enforced package gate at `92.32%` statements, `80.04%` branches, `94.23%` functions, and `93.33%` lines.
- Link to doc or code path: `docs/plans/143-unit-test-coverage-80-percent-target-plan.md`, `packages/flux-renderers-data/src/__tests__/use-table-controls.test.tsx`, `packages/flux-renderers-data/src/__tests__/chart-renderer.unit.test.tsx`, `packages/flux-renderers-data/src/__tests__/table-body-rows-virtual.test.tsx`, `packages/flux-renderers-data/src/__tests__/table-data-and-layout.test.tsx`, `packages/flux-renderers-data/src/__tests__/table-quick-edit-cell.unit.test.tsx`, `packages/flux-renderers-data/src/__tests__/crud-renderer-state.unit.test.tsx`
- Key decision: concentrate on branch-dense pure and near-pure modules (`crud-renderer-state.ts`, `table-data.ts`, `use-table-controls.ts`, `table-quick-edit-cell.tsx`, `table-body-rows.tsx`) instead of relying only on broader renderer integration tests, because those helpers had the highest remaining branch yield for the least harness churn.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `pnpm --filter @nop-chaos/flux-renderers-data build`, `pnpm --filter @nop-chaos/flux-renderers-data lint`, `pnpm --filter @nop-chaos/flux-renderers-data test`, `pnpm exec vitest run --config vitest.config.ts --coverage`
- Next step: Plan 143 no longer has a known package-local 80% gap; the next required step is a fresh independent closure audit plus whatever final plan-level verification is still needed against the current workspace baseline.

### 2026-04-26

- Closed Plan 144 after wiring a real tooling consumer for the shared host-contract authoring helper path into `nop-debugger` inspection. `buildInspectResult(...)` now resolves `authoringContract` from the live runtime registry via `resolveRendererAuthoringContract(...)`, so debugger inspection can expose resolved `hostManifest`, `hostProjection`, and `hostActions` for domain-host renderers instead of leaving the shared helper path as tests-and-docs-only infrastructure. The remaining owner-doc drift was also synchronized: `schema-file-validator.md` now matches the current `schemaUrl`-backed `sourceLocation` baseline, and `capability-contract-model.md` now documents `ResolvedAuthoringContract.hostManifest`.
- Link to doc or code path: `docs/plans/144-flux-execution-boundary-diagnostics-and-host-contract-tooling-plan.md`, `packages/nop-debugger/src/controller-component-inspector.ts`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/controller-inspect.test.ts`, `docs/architecture/schema-file-validator.md`, `docs/architecture/capability-contract-model.md`
- Key decision: treat Phase 3 as incomplete until a real non-test consumer used the shared helper path, then close the plan only after an independent audit plus full workspace verification confirmed that the remaining gaps were documentation drift rather than implementation debt.
- Verification: independent audits `ses_235cdc75bffenhmG4NvWad61Ip` and `ses_235c76aa6ffen7C3G5vSr0Pn6H`; `pnpm --filter @nop-chaos/nop-debugger typecheck`, `pnpm --filter @nop-chaos/nop-debugger build`, `pnpm --filter @nop-chaos/nop-debugger lint`, `pnpm --filter @nop-chaos/nop-debugger test`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`

### 2026-04-26

- Closed Plan 147 by aligning `object-field`'s default inline semantics with the documented parent-owned projected-editor baseline. `object-field.tsx` now reads directly from the parent owner when no transform actions are declared, so full-object replacements at the parent path immediately flow through the projected child scope/form, while transform-enabled cases still keep the minimal local working value needed for adapted draft vs committed-value divergence. Added a focused regression proving parent-owned object replacement is reflected through the projected view without reopening the transform contract.
- Link to doc or code path: `docs/plans/147-object-field-inline-semantics-alignment-successor-plan.md`, `docs/architecture/object-field.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.test.tsx`
- Key decision: keep the local working buffer only for `transformInAction` / `transformOutAction` cases that genuinely need adapted draft-vs-committed divergence, and remove it from the default inline path instead of forcing a larger composite-field rewrite.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced lint`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- object-field.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced test`

### 2026-04-26

- Continued Plan 143 by closing the remaining `flux-runtime` package coverage gap. Added focused unit tests for async governance stale/cancelled/retention behavior and form owner refresh/dispose lifecycle cleanup, then fixed pre-existing strict-null assertions in `component-handle-registry.test.ts` so package verification could complete cleanly. `flux-runtime` now passes its enforced package gate at `87.78%` statements, `80.16%` branches, `90.48%` functions, and `88.28%` lines.
- Link to doc or code path: `docs/plans/143-unit-test-coverage-80-percent-target-plan.md`, `packages/flux-runtime/src/__tests__/async-governance.test.ts`, `packages/flux-runtime/src/__tests__/form-runtime-owner-lifecycle.test.ts`, `packages/flux-runtime/src/__tests__/component-handle-registry.test.ts`
- Key decision: target small branch-heavy helper modules (`async-governance.ts`, `form-runtime-owner-lifecycle.ts`) instead of broad integration tests, because `flux-runtime` was already close to the threshold and these modules offered the highest coverage gain for the least harness churn.
- Verification: `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`, `pnpm --filter @nop-chaos/flux-runtime lint`, `pnpm --filter @nop-chaos/flux-runtime test`, `pnpm exec vitest run --config vitest.config.ts --coverage`
- Next step: return to the remaining Plan 143 package gap in `flux-renderers-data`, then run a fresh independent closure audit once all in-scope package thresholds are green.

### 2026-04-26

- Closed Plan 145 by finishing Phase 5 and Phase 6. `flux-react` now routes repeated form-store subscription patterns through `hook-subscriptions.ts`, `flux-runtime` now shares owner-local external-error field-state merge logic through `form-runtime-owner-field-states.ts`, stale runtime/designer/playground tests were updated to current live contracts, and the full workspace verification baseline is green again. Closure audit evidence is recorded in the plan with independent subagent task `ses_235f82372ffeNrcYm8Fd1AFzcc`, while the remaining `object-field` inline semantic gap is now explicitly owned by new successor plan `docs/plans/147-object-field-inline-semantics-alignment-successor-plan.md` instead of remaining unnamed debt.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `docs/plans/147-object-field-inline-semantics-alignment-successor-plan.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/form-validation.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/object-field.md`, `packages/flux-react/src/hook-subscriptions.ts`, `packages/flux-runtime/src/form-runtime-owner-field-states.ts`, `packages/flux-runtime/src/__tests__/runtime-owned-helpers.test.ts`, `packages/flux-runtime/src/__tests__/action-adapter.unit.test.ts`, `packages/flow-designer-core/src/__tests__/core-ui-state.test.ts`, `apps/playground/src/flow-designer/parity.test.tsx`
- Key decision: treat the remaining `object-field` semantic mismatch as a separate owner surface and give it an explicit successor plan, rather than keeping Plan 145 open after the hotspot-boundary convergence work itself is already complete.
- Verification: `pnpm --filter @nop-chaos/flux-react typecheck/build/lint/test`, `pnpm --filter @nop-chaos/flux-runtime typecheck/build/lint/test`, `pnpm --filter @nop-chaos/flow-designer-core test`, `pnpm --filter @nop-chaos/flux-playground typecheck`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`

### 2026-04-26

- Updated PPT outline (`docs/ppt/nop-chaos-flux-ai-large-scale-development-outline.md`) with 4 new pages: author introduction, nop-chaos-flux project introduction and positioning, development statistics with real repo data (62,400 lines TS, 24 packages, 1,336 commits, 42 days), and AI vs traditional team efficiency comparison (1.5 person-months AI-assisted vs 12-24 person-months traditional). Added appendix C with data quick-reference table. Total pages expanded from 24 to 29 (including 3 appendix pages). All statistics verified against live repository.
- Link to doc or code path: `docs/ppt/nop-chaos-flux-ai-large-scale-development-outline.md`
- Key decision: use real `git log` / `wc -l` / `find` data for all statistics rather than estimates; author page left as TODO placeholder for user to fill in.
- Next step: user fills in author details; optionally creates actual PPT from outline.

### 2026-04-26

- Executed Plan 146 Phase 2-4: converged word-editor host projection semantics and report-designer host vocabulary. Word editor: fixed `editorRuntime` selector to read only editor-store state (no cross-store contamination), documented `document` autosave-vs-realtime timing in component and architecture docs, added focused tests for runtime projection and document fallback. Report designer: classified all host projection fields into canonical/alias/convenience categories, promoted `selectionTarget` as canonical selection field with `selection`/`target` as explicit compat aliases, removed unused convenience mirrors from `ReportDesignerHostData` type while preserving `inspector`/`inspectorPanels`/`meta` for existing consumers, updated manifest descriptions and component/architecture docs with canonical vocabulary table.
- Link to doc or code path: `docs/plans/146-domain-host-projection-and-vocabulary-convergence-plan.md`, `packages/word-editor-renderers/src/word-editor-page.tsx:78-113`, `packages/word-editor-renderers/src/__tests__/word-editor-page.test.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/report-designer-renderers/src/report-designer-manifest.ts`, `docs/components/word-editor-page/design.md`, `docs/components/report-designer-page/design.md`, `docs/architecture/word-editor/design.md`, `docs/architecture/report-designer/design.md`
- Key decision: preserve existing consumer contracts (inspector-shell reads `inspector`/`meta`/`inspectorPanels` from scope) rather than breaking them to achieve a cleaner surface; classify them as documented convenience mirrors instead.
- Verification: `pnpm --filter @nop-chaos/word-editor-renderers typecheck/build/lint/test` (64/64), `pnpm --filter @nop-chaos/report-designer-renderers typecheck/build/lint/test` (124/124) all pass.

### 2026-04-26

- Completed Plan 145 Phase 3 for `flux-renderers-data`. Extracted CRUD owner-path and query bridge logic into `crud-renderer-ownership.ts`, kept query submit/reset on the existing form-handle + owner-state path chain, and wired `$crud.visibleColumnNames` / `statusPath` to the same scope-owned column-settings state used by the internal table so CRUD summary publication no longer lags behind visible-column ownership.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `docs/components/crud/design.md`, `docs/components/table/design.md`, `docs/architecture/action-interaction-state.md`, `docs/architecture/data-domain-owner.md`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/crud-renderer-ownership.ts`, `packages/flux-renderers-data/src/__tests__/data-crud-state-interactions.test.tsx`
- Key decision: keep CRUD as a workflow shell that aggregates query/table owner results, instead of introducing a second renderer-local owner for table column visibility or widening CRUD into a request owner.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `pnpm --filter @nop-chaos/flux-renderers-data build`, `pnpm --filter @nop-chaos/flux-renderers-data lint`, `pnpm --filter @nop-chaos/flux-renderers-data test`
- Next step: Plan 145 now narrows to Phase 5 (`flux-react` hooks + `flux-runtime` form runtime substrate) plus final closure audit.

### 2026-04-26

- Completed Plan 145 Phase 4 for `flow-designer-renderers`. `designer-page.tsx` now keeps the page-shell / provider / dialog composition-root role while auto-layout and shortcut side effects live in dedicated hooks, tree-owned commands no longer get intercepted by graph-only routing in tree mode, the old omnibus `index.test.tsx` was split into focused page-shell and provider/manifest suites, and `test-support.tsx` now exports explicit portable helper types so declaration build no longer leaks `vitest` spy internals.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `docs/architecture/flow-designer/collaboration.md`, `docs/architecture/flow-designer/canvas-adapters.md`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-renderers/src/use-designer-auto-layout.ts`, `packages/flow-designer-renderers/src/use-designer-shortcuts.ts`, `packages/flow-designer-renderers/src/designer-command-adapter.ts`, `packages/flow-designer-renderers/src/test-support.tsx`, `packages/flow-designer-renderers/src/designer-page-shell.test.tsx`, `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx`
- Key decision: keep `DesignerPageBody` as the page-shell composition root and extract only the repeatable side-effect surfaces and test harnesses, rather than pushing more runtime semantics into canvas host code or reopening the renderer contract.
- Verification: `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`, `NODE_OPTIONS="--max-old-space-size=4096" pnpm --filter @nop-chaos/flow-designer-renderers build`, `pnpm --filter @nop-chaos/flow-designer-renderers lint`, `pnpm --filter @nop-chaos/flow-designer-renderers test`
- Next step: proceed to the remaining Plan 145 owner surfaces, with Phase 3 (`flux-renderers-data` CRUD/table ownership convergence) now the next most natural slice.

### 2026-04-26

- Completed Plan 146 Phase 1: froze host projection baseline and compatibility policy for both `word-editor-page` and `report-designer-page`. Documented the word-editor's mixed-timing issue (`document` is debounced autosave while `runtime`/`selection` are real-time), identified the cross-store contamination in `runtimeSnapshot`, and mapped all report-designer top-level convenience mirrors (`selection`/`target`/`selectionTarget` triple alias plus 10+ duplicated fields). Defined canonical vs alias vs convenience mirror classification for all host projection fields, mapped test dependencies on aliases, and identified docs owner gaps.
- Link to doc or code path: `docs/plans/146-domain-host-projection-and-vocabulary-convergence-plan.md`, `packages/word-editor-renderers/src/word-editor-page.tsx:118-129`, `packages/report-designer-renderers/src/host-data.ts:137-197`, `packages/report-designer-renderers/src/renderers.integration.test.tsx:80-92`
- Key decision: treat `selectionTarget` as the canonical report-designer selection field and classify `selection`/`target` as compatibility aliases to be converged in Phase 3, rather than renaming fields in Phase 1.
- Next step: proceed to Phase 2 (converge word-editor host projection semantics — fix `runtimeSnapshot` cross-store aggregation, clarify `document` timing semantics in code and docs).

### 2026-04-26

- Completed the current Plan 145 Phase 2 slice for `flux-renderers-form-advanced`. `array-field-runtime` now also uses the shared projected-owner scope helper, the package's proxy/runtime tests were updated to the current `flux-core` validation/runtime contracts, and the remaining condition-builder test typing issue was isolated to test-side `data-*` prop assertions rather than widening the public component prop surface.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `packages/flux-renderers-form-advanced/src/projected-owner-scope.ts`, `packages/flux-renderers-form-advanced/src/composite-field/array-field-runtime.ts`, `packages/flux-renderers-form-advanced/src/composite-field/array-field-runtime.test.ts`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-runtime.test.ts`, `packages/flux-renderers-form-advanced/src/condition-builder/condition-item.tsx`, `packages/flux-renderers-form-advanced/src/condition-builder/condition-item.test.tsx`
- Key decision: keep `ConditionItem.dragHandleProps` aligned with the existing component call sites and solve the `data-*` typing friction in tests, instead of overfitting the public prop type to a test-only literal-object pattern.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced lint`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- array-field-runtime.test.ts variant-field-runtime.test.ts object-field.test.tsx` (Vitest again ran the package's current full 42-file/455-test suite and passed).
- Next step: move from the completed form-advanced substrate slice to the next Plan 145 owner surface, prioritizing either CRUD/table ownership consolidation or flow-designer page-shell / adapter / test-boundary convergence.

### 2026-04-26

- Continued Plan 145 by landing the first shared projected-owner scope slice for advanced form renderers. Added `projected-owner-scope.ts` as a common `ScopeRef` substrate, migrated `object-field` and `variant-field` to use it, updated the plan baseline to reflect that CRUD query ownership no longer relies on DOM scraping, and recorded the remaining `object-field` semantic gap as a successor-plan concern rather than silently absorbing it into this refactor.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `docs/architecture/object-field.md`, `packages/flux-renderers-form-advanced/src/projected-owner-scope.ts`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-runtime.ts`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.test.tsx`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-runtime.test.ts`
- Key decision: unify only the projected `ScopeRef` helper layer first and leave `object-field`'s renderer-local working copy semantics intact, so the refactor reduces duplicated proxy code without changing parent-owned inline editing behavior.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced lint`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- variant-field-runtime.test.ts object-field.test.tsx` (Vitest ran the package's current full 42-file/455-test suite and passed).
- Known blocker: `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck` still fails on pre-existing test typing debt in `array-field-runtime.test.ts`, `variant-field-runtime.test.ts`, and `condition-builder/condition-item.test.tsx`; the runtime code changes in this slice now build cleanly.
- Next step: continue Plan 145 Phase 2 by extending the shared substrate into the remaining advanced field paths, then move to the flow-designer adapter/test-boundary slice once the current form-advanced convergence is sufficiently closed.

### 2026-04-26

- Continued Plan 145 execution by finishing the CRUD/query boundary convergence slice and stabilizing the projected inline form helper refactor. `crud-renderer.tsx` now updates owner query state from the form handle instead of DOM reads, table handle selection payloads accept the array shape already used by tests, and `component-handle-registry.ts` now resolves `componentId` targets through child registries so parent owners can reach nested form handles across the existing `componentRegistryPolicy: 'new'` boundary.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/table-data.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-renderers-form-advanced/src/composite-field/projected-inline-form.ts`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/flux-renderers-form-advanced/src/composite-field/array-field-runtime.ts`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-runtime.ts`
- Key decision: preserve the form/query/component-handle contract and fix the owner boundary by making registry resolution child-aware rather than reintroducing DOM scraping or changing form renderer registry policy.
- Next step: continue with the remaining Plan 145 phases (`flow-designer` adapter/test boundary convergence and `flux-react`/form-runtime hotspot convergence) after the unrelated workspace verification blockers in `flow-designer-renderers` and existing `flux-renderers-form-advanced` test typing debt are handled or isolated.

### 2026-04-26

- Executed Plan 144: Flux Execution Boundary Diagnostics And Host-Contract Tooling. Phase 1 added explicit "Input Invariants" to `frontend-programming-model.md` enumerating 5 invariants for `Final Execution Schema`. Phase 2 added `SchemaDiagnosticSourceLocation` to `SchemaDiagnostic`, wired `schemaUrl` propagation through the compiler diagnostics context, and documented the source-location backtracking design in `schema-file-validator.md`. Phase 3 added `resolveHostContractManifest()` shared helper, enhanced `resolveRendererAuthoringContract()` with version selector support and `hostManifest` exposure, and documented the standard tooling consumption path in `capability-projection-manifest.md`.
- Link to doc or code path: `docs/plans/144-flux-execution-boundary-diagnostics-and-host-contract-tooling-plan.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/schema-file-validator.md`, `docs/architecture/capability-projection-manifest.md`, `packages/flux-core/src/schema-diagnostics/index.ts`, `packages/flux-compiler/src/schema-compiler/diagnostics.ts`, `packages/flux-core/src/types/renderer-authoring-contract.ts`
- Key decision: Phase 1 is documentation-only (no `FluxAssembledResource` type in code); Phase 2 uses minimal `sourceLocation` sidecar on diagnostics rather than a separate global source map; Phase 3 exposes the standard `hostContract -> resolveManifest(...)` path as a shared helper instead of introducing a global manifest registry.
- Next step: run independent closure audit before marking plan `completed`.

### 2026-04-26

- Split the newly confirmed domain-host follow-up surface out of Plan 145 and drafted owner plan `146-domain-host-projection-and-vocabulary-convergence-plan.md`. The new plan isolates `word-editor-page` and `report-designer-page` host projection contract work from the runtime/react/form-advanced hotspot plan, with separate phases for (1) baseline + compatibility policy freeze, (2) word-editor realtime-vs-persisted host projection semantics, (3) report-designer canonical vocabulary + alias boundary convergence, and (4) verification + closure audit.
- Link to doc or code path: `docs/plans/146-domain-host-projection-and-vocabulary-convergence-plan.md`, `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `packages/word-editor-renderers/src/word-editor-page.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `docs/components/word-editor-page/design.md`, `docs/components/report-designer-page/design.md`
- Key decision: treat domain-host projection / vocabulary convergence as its own result surface instead of letting it remain an implicit appendix under Plan 145, because the live repo audit showed it has different contract risk and different owning docs/tests than the runtime/react/form-advanced boundary work.
- Next step: if execution starts on this surface, begin with the Phase 1 compatibility-policy freeze so `word-editor` realtime-vs-autosave fields and `report-designer` selection aliases are both made explicit before any implementation change lands.

### 2026-04-26

- Re-audited the current runtime/react/renderer hotspot backlog against the live repo and folded the confirmed findings into owner plan `145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`. The plan now explicitly captures four newly confirmed convergence targets: `object-field`'s renderer-local working copy drift from the parent-owned inline baseline, `word-editor-page` host projection mixing realtime runtime data with debounced autosave document snapshots, `report-designer` host projection vocabulary duplication, and large cross-domain designer/report integration tests that now need owner-boundary-oriented splits instead of file-size-only cleanup.
- Link to doc or code path: `docs/plans/145-runtime-react-renderer-hotspot-boundary-convergence-plan.md`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/word-editor-renderers/src/word-editor-page.tsx`, `packages/word-editor-renderers/src/editor-canvas.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/flow-designer-renderers/src/index.test.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`
- Key decision: keep Plan 145 as the owner plan for these boundary-convergence hotspots instead of spawning a parallel successor plan, because the newly confirmed items still belong to the same result surface: runtime/react/renderer host-owner and hotspot-boundary convergence.
- Next step: run independent subagent review passes against the revised plan, update the plan until the audit wording and phase ownership converge, then use that reviewed plan as the execution baseline rather than treating the audit output as a detached one-off report.

### 2026-04-26

- Corrected `docs/skills/react19-best-practices-review.md` to match the live workspace automation baseline instead of overstating what `pnpm check` covers. The guide now distinguishes `lint` from `check`, adds the existing src-artifact and i18n-key scripts, restores the full React Hooks `recommended-latest` baseline summary, and fixes oversized-file wording to the real 500 warn / 700 fail thresholds.
- Link to doc or code path: `docs/skills/react19-best-practices-review.md`, `package.json`, `eslint.config.js`, `scripts/check-react19-legacy-apis.mjs`, `scripts/check-oversized-code-files.mjs`, `scripts/verify-no-src-artifacts.mjs`, `scripts/check-i18n-keys.mjs`
- Key decision: treat this as a documentation-baseline correction only; do not widen or tighten repo automation in the same patch.
- Next step: if future reviews keep surfacing the same uncaught issue class, add or refine automation first instead of letting the skills doc drift into a shadow policy.

### 2026-04-26

- Fixed three review findings that were not covered by the current React 19 lint/check baseline. `word-editor-page.tsx` now subscribes to dataset-store updates before projecting host/runtime data, `use-word-editor-shortcuts.ts` and `designer-page.tsx` now scope global key handlers to the active host shell instead of intercepting shortcuts across the whole document/window, and `detail-field.tsx` now resolves nested fallback values from scope data by path instead of flat-key lookup.
- Link to doc or code path: `packages/word-editor-renderers/src/word-editor-page.tsx`, `packages/word-editor-renderers/src/hooks/use-word-editor-shortcuts.ts`, `packages/word-editor-renderers/src/__tests__/word-editor-page.test.tsx`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field-basic.test.tsx`
- Key decision: keep the fixes local to host-shell event scoping and projection subscription boundaries rather than introducing new shared shortcut infrastructure or broader host-scope refactors in the same patch.
- Next step: if we want stronger automation coverage for this review class, add focused interaction tests for designer/word-editor shortcut scoping; current lint/check still cannot detect cross-store stale snapshots or globally over-scoped host shortcuts.

### 2026-04-26

- Executed a substantial portion of Plan 143 by fixing the existing failing tests in `flux-renderers-form`, `flux-renderers-data`, and `flux-renderers-form-advanced`, adding broad new unit coverage across `flux-core`, `flux-compiler`, `flux-runtime`, `flux-react`, and `flux-renderers-form-advanced`, and wiring 80% Vitest coverage thresholds into all target package configs.
- Link to doc or code path: `docs/plans/143-unit-test-coverage-80-percent-target-plan.md`, `packages/flux-core/src/validation-model.test.ts`, `packages/flux-compiler/src/action-compiler.test.ts`, `packages/flux-compiler/src/schema-compiler-shape-validation.test.ts`, `packages/flux-runtime/src/__tests__/validation-rules.test.ts`, `packages/flux-runtime/src/__tests__/page-runtime.test.ts`, `packages/flux-react/src/__tests__/form-state.test.ts`, `packages/flux-renderers-data/src/__tests__/data-source.test.tsx`, `packages/flux-renderers-form-advanced/src/condition-builder/condition-item.test.tsx`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field-runtime.test.ts`, `packages/flux-react/vitest.config.ts`, `packages/flux-runtime/vitest.config.ts`
- Key decision: keep Plan 143 open as `partially completed` instead of force-closing it, because the independent closure audit confirmed that while the package-local regressions are fixed and coverage improved sharply, several packages still miss the newly enforced 80% thresholds and repo-wide verification is still noisy due to unrelated workspace issues.
- Next step: finish the remaining coverage gaps called out by the closure audit in `flux-runtime`, `flux-react`, `flux-renderers-form`, `flux-renderers-data`, and `flux-renderers-form-advanced`, then re-run final verification against a clean enough workspace baseline.

### 2026-04-26

- Reduced the oversized-file backlog to test-only warnings by splitting `table-body-rows.tsx`, `nop-debugger` explanation/type surfaces, and `flux-formula/src/parser.ts` into focused internal helpers. Table body row rendering now lives in a dedicated helper module, debugger explanation/failure/type definitions are separated by concern, and the formula parser now delegates AST node factory helpers without changing `parseFormula` behavior or exports.
- Link to doc or code path: `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-row-rendering.tsx`, `packages/nop-debugger/src/explanations.ts`, `packages/nop-debugger/src/explanations-failure-async.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/types-explanations.ts`, `packages/flux-formula/src/parser.ts`, `packages/flux-formula/src/parser-node-factories.ts`
- Key decision: stop after internal helper extraction once the oversized report contained only test files, instead of forcing broader parser/debugger rewrites that would increase change risk for little payoff.
- Next step: leave the remaining oversized warnings for test-file cleanup when there is a dedicated pass to reorganize large fixtures and end-to-end scenario coverage.

### 2026-04-26

- Reduced the oversized-file backlog again by splitting `shape-validation.ts` and `form-runtime.ts` into focused helper modules. Shape validation now delegates generic schema-key utilities plus action/source validation rules, while form runtime now delegates derived submit/touched state and array mutation context helpers. This removed both files from the oversized warning list without changing exported APIs.
- Link to doc or code path: `packages/flux-compiler/src/schema-compiler/shape-validation.ts`, `packages/flux-compiler/src/schema-compiler/shape-validation-utils.ts`, `packages/flux-compiler/src/schema-compiler/shape-validation-rules.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-runtime-derived-state.ts`, `docs/architecture/flux-runtime-module-boundaries.md`
- Key decision: extract only pure/shared helper logic and keep traversal/runtime orchestration in the owner files, so the split lowers file size without obscuring the main control flow.
- Next step: the highest-value remaining production-code oversized files are now `reaction-runtime.ts`, `designer-page.tsx`, and a few near-threshold core/runtime files; the rest of the warning list is mostly tests.

### 2026-04-26

- Reduced the oversized-file backlog further by splitting `designer-command-adapter.ts` and `schema-compiler.ts` into focused helper modules. The designer adapter now delegates graph-only command handling and shared graph/tree result helpers, while the compiler now delegates plugin application plus schema-root/import preparation helpers. This removed both files from the oversized warning list without changing their exported factory names.
- Link to doc or code path: `packages/flow-designer-renderers/src/designer-command-adapter.ts`, `packages/flow-designer-renderers/src/designer-command-adapter-helpers.ts`, `packages/flow-designer-renderers/src/designer-command-adapter-graph.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/schema-compiler-helpers.ts`, `docs/architecture/flux-runtime-module-boundaries.md`
- Key decision: keep tree-command branching inside the main adapter for now and only extract graph-only paths/common helpers, which was enough to cross the 500-line threshold without turning the adapter into a many-file rewrite.
- Next step: the highest-value remaining production-code oversized files are now `shape-validation.ts`, `form-runtime.ts`, `reaction-runtime.ts`, and `designer-page.tsx`.

### 2026-04-26

- Applied P0 and P1 refactoring fixes from the code-refactor-discovery audit. Changes: (1) Re-exported `publishOwnerStatus`, `createFormComponentHandle`, `executeApiObject`, `createProjectedScopeStore` from `@nop-chaos/flux-react` to close the package boundary violation where 11 renderer files imported directly from `@nop-chaos/flux-runtime`. (2) Consolidated byte-for-byte duplicate debounce (`scheduleDebounce`/`cancelPendingDebounce`) from `flux-action-core` and `flux-runtime` into a single copy in `flux-core`. (3) Consolidated `toPositiveNumber`, `toStringArray`, `isRecord`/`toRecord`/`shallowEqualRecords` from 4 separate implementations into `flux-core/src/utils/object.ts`. (4) Replaced `disposed` boolean flag in `schema-renderer.tsx` with `AbortController`. (5) Fixed `word-editor-page.tsx` setTimeout with no cleanup. (6) Fixed `CrudRenderer` dead `loading` state, removed duplicate path derivation. (7) Fixed `Dialog`/`Drawer` one-frame delay by computing `effectiveOpen` synchronously instead of via `useEffect` sync. (8) Split `flux-renderers-data/src/index.tsx` into `data-schema-validation.ts` for validation/transform functions.
- Link to doc or code path: `packages/flux-react/src/index.tsx`, `packages/flux-core/src/utils/object.ts`, `packages/flux-core/src/utils/debounce.ts`, `packages/flux-renderers-data/src/data-schema-validation.ts`, `packages/flux-react/src/schema-renderer.tsx`
- Key decision: deferred CrudSortState vs SortState unification and designer-page.tsx splitting as they require more invasive schema-level changes.
- Next step: unify Crud/Table column schema types, split designer-page.tsx shortcuts and dialogs.

### 2026-04-26

- Reduced the oversized-file backlog again by splitting three more owner-heavy production files into focused internal modules: `form.tsx` now delegates renderer definition metadata and status/value publication helpers, `form-runtime-owner.ts` now delegates external-error and owner lifecycle cleanup helpers, and `use-table-controls.ts` now delegates visible-column state handling. This removed `form.tsx`, `use-table-controls.ts`, and `form-runtime-owner.ts` from the oversized warning list without changing their public hooks or renderer contracts.
- Link to doc or code path: `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-renderers-form/src/renderers/form-definition.ts`, `packages/flux-renderers-form/src/renderers/form-status-publication.ts`, `packages/flux-runtime/src/form-runtime-owner.ts`, `packages/flux-runtime/src/form-runtime-owner-external-errors.ts`, `packages/flux-runtime/src/form-runtime-owner-lifecycle.ts`, `packages/flux-renderers-data/src/table-renderer/use-table-controls.ts`, `packages/flux-renderers-data/src/table-renderer/use-table-visible-columns.ts`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/renderer-runtime.md`
- Key decision: stop at internal helper extraction instead of also renaming exports or moving public import paths, so consumers keep the same entry points while oversized-file pressure drops.
- Next step: the most worthwhile remaining production-code targets are now `designer-command-adapter.ts`, `schema-compiler.ts`, `shape-validation.ts`, and `form-runtime.ts`; the rest of the warning list is increasingly test-heavy.

### 2026-04-26

- Reduced the oversized-file backlog by splitting three high-priority production files into focused internal modules: `flux-runtime/runtime-factory.ts` now delegates owned runtime factory wiring and host projection scope helpers, `flux-react/hooks.ts` now delegates shared subscription helpers, and `api-data-source-controller.ts` now delegates repeated async-data state transition helpers. This removed `hooks.ts` and `api-data-source-controller.ts` from the oversized warning list and pulled `runtime-factory.ts` down closer to the assembly-only boundary described in the runtime module-boundaries doc.
- Link to doc or code path: `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-runtime/src/runtime-owned-factories.ts`, `packages/flux-runtime/src/runtime-host-projection-scope.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/hook-subscriptions.ts`, `packages/flux-runtime/src/async-data/api-data-source-controller.ts`, `packages/flux-runtime/src/async-data/api-data-source-controller-helpers.ts`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/renderer-runtime.md`
- Key decision: keep the refactor strictly internal and behavior-preserving by extracting only helper modules with clear ownership boundaries instead of changing exported runtime/react APIs in the same patch.
- Next step: continue the oversized cleanup with the next owner-heavy files (`form.tsx`, `form-runtime-owner.ts`, `use-table-controls.ts`, `designer-command-adapter.ts`) after the repo-wide pre-existing test/lint debt is cleaned up enough to make full-package verification signal clearer.

### 2026-04-26

- Fixed the existing `spreadsheet-core` filter typing regression that was blocking package verification: `filter-operations.ts` now builds `filters` with an explicit `WorksheetFilterState`, preserving the `'cellValue'` literal type instead of widening `kind` to `string`. This unblocked `spreadsheet-core` and dependent `report-designer-core` typecheck/build again.
- Link to doc or code path: `packages/spreadsheet-core/src/core/filter-operations.ts`, `packages/spreadsheet-core/src/__tests__/core-basics.test.ts`, `packages/report-designer-core/`
- Key decision: keep the fix type-only and local to the filter object construction rather than broadening `WorksheetColumnFilter.kind` or weakening workbook document types.
- Next step: `flux-runtime` still has a separate backlog of test-only type errors (validation/import/runtime test fixtures) that is unrelated to today's performance work and can be addressed as its own cleanup patch if needed.

### 2026-04-26

- Optimized report-designer range field drops by replacing the per-cell `updateMetadata()` loop with a single batched `cellMeta[sheetId]` rebuild. The public `report-designer:dropFieldToTarget` range path now computes changed entries against the original semantic snapshot and applies all changed cell-meta writes in one semantic update instead of repeatedly cloning `document.semantic` and `cellMeta[sheetId]` for each cell.
- Link to doc or code path: `packages/report-designer-core/src/runtime/metadata.ts`, `packages/report-designer-core/src/__tests__/adapters-and-helpers.test.ts`, `packages/report-designer-core/src/__tests__/designer-core.test.ts`, `packages/report-designer-core/src/__tests__/document-and-metadata.test.ts`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`
- Key decision: optimize only the proven hot path (`applyFieldDrop` for range targets) and leave generic metadata mutation helpers untouched, so the patch improves the audited path without widening behavior changes.
- Next step: remaining verification blockers are now outside the changed code, mainly the existing `spreadsheet-core/src/core/filter-operations.ts` type error that also affects dependent packages.

### 2026-04-26

- Extended the new path-aware form value subscription mode into advanced form renderers that were still doing single-path value reads through whole-store `useCurrentFormState` selectors: `object-field`, `array-field`, `detail-field`, `variant-field`, `array-editor`, and `key-value`. These were all true single-path reads, so switching them to `{ path: name }` reduces unrelated wake-ups without changing derived selector semantics.
- Link to doc or code path: `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/flux-renderers-form-advanced/src/composite-field/array-field.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx`, `packages/flux-renderers-form-advanced/src/array-editor.tsx`, `packages/flux-renderers-form-advanced/src/key-value.tsx`
- Key decision: only convert callers that are provably single-path value readers; leave broader derived selectors alone until there is a dedicated owner patch, to avoid cargo-cult path options on selectors that still depend on wider form state.
- Next step: remaining performance debt should be re-evaluated case by case; the obvious easy wins in audited hot paths are now mostly exhausted.

### 2026-04-26

- Narrowed form field value subscriptions by adding optional path-aware subscription support to `useCurrentFormState`, teaching `form-store` to notify subscribed value paths when `values` changes, and switching `useBoundFieldValue` plus test probes onto that path-aware mode. Added a focused render-performance regression test showing that an unrelated field update no longer rerenders another field's value probe.
- Link to doc or code path: `packages/flux-runtime/src/form-store.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-renderers-form/src/field-utils.tsx`, `packages/flux-renderers-form/src/__tests__/form-render-performance.test.tsx`, `packages/flux-renderers-form/src/__tests__/form-test-support.tsx`, `docs/analysis/2026-04-26-deep-audit-performance/05-reactive-precision.md`
- Key decision: stop at value-path subscriptions for field reads instead of trying to convert every `useCurrentFormState` caller in one patch; this captures the highest-frequency field-value path with a smaller correctness surface.
- Next step: the remaining subscription debt is mostly field presentation/dynamic-required selectors and other advanced form renderers still using whole-store selectors for derived data.

### 2026-04-26

- Replaced condition-builder's `JSON.stringify`-based field-value comparator with a recursive structural equality helper, keeping semantic equality for nested condition trees while removing string allocation from the field subscription equality path. Added focused util tests for equal and unequal nested trees.
- Link to doc or code path: `packages/flux-renderers-form-advanced/src/condition-builder/utils.ts`, `packages/flux-renderers-form-advanced/src/condition-builder/utils.test.ts`, `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder.tsx`, `docs/analysis/2026-04-26-deep-audit-performance/05-reactive-precision.md`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`
- Key decision: keep the comparator specialized to plain objects/arrays used by condition-builder values instead of introducing a generic deep-equality utility in shared packages.
- Next step: remaining audited performance debt is mostly broader subscription precision (`useCurrentFormState`) and lower-priority report-designer range metadata updates; both are larger follow-up changes rather than quick hot-path fixes.

### 2026-04-26

- Optimized spreadsheet paste/cut mutation paths so pasted cells are written with a single `setCells(...)` batch instead of repeated `setCell(...)` full-map clones, and source-cell clearing now batches deletes instead of cloning `cells` once per cleared key. Added a same-sheet overlap regression test for cut+paste to preserve the pasted overlap while clearing only the non-overwritten source cells.
- Link to doc or code path: `packages/spreadsheet-core/src/core/clipboard-operations.ts`, `packages/spreadsheet-core/src/core/document-access.ts`, `packages/spreadsheet-core/src/__tests__/new-commands-mutation.test.ts`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`
- Key decision: reuse the existing `setCells(...)` helper and add a local batched delete helper instead of redesigning the document mutation API, keeping the paste-path fix small and behaviorally compatible.
- Next step: reduce the remaining hot-path deep equality cost in condition-builder by removing `JSON.stringify` from its field subscription comparator.

### 2026-04-26

- Removed a hot-path whole-value `JSON.stringify` check from `FormRenderer`'s `valuesPath` publication effect. The renderer now republishes only when the form store exposes a new `values` object reference, preserving existing `valuesPath` behavior while avoiding full serialization on non-value store updates such as field-state or submit-status changes.
- Link to doc or code path: `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-renderers-form/src/__tests__/form-submit-actions.test.tsx`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`
- Key decision: use reference-based dedupe as the smallest safe fix because `form-store` already replaces `values` immutably on `setValue` / `setValues`, so adding a broader path-subscription redesign was unnecessary for this patch.
- Next step: tackle the spreadsheet paste path that still clones the full `sheet.cells` map once per pasted cell.

### 2026-04-26

- Fixed a verified async-data controller bug where a request could be marked succeeded before result mapping or scope publication finished, causing later publish failures to be dropped without failure state or telemetry. Added a regression test that forces `scope.update('payload', ...)` to throw after a successful API response and now asserts `notify`, `monitor.onError`, and controller error state all surface correctly.
- Link to doc or code path: `packages/flux-runtime/src/async-data/api-data-source-controller.ts`, `packages/flux-runtime/src/__tests__/runtime-sources.test.ts`, `docs/analysis/2026-04-26-deep-audit-performance/06-async-safety.md`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`
- Key decision: keep the fix minimal by tightening the stale-run early-return condition instead of redesigning async-governance settlement semantics in the same patch.
- Next step: follow up on the remaining audited performance items, especially form `valuesPath` whole-value stringify checks and spreadsheet bulk paste immutable-copy cost.

### 2026-04-26

- Completed a deep audit focused on performance-sensitive runtime, form, spreadsheet, and report-designer paths; retained independently reviewed findings around async-data swallowed failures, form `valuesPath` whole-value stringify checks, spreadsheet paste immutable-copy scaling, condition-builder deep equality cost, and remaining whole-store field subscription debt.
- Link to doc or code path: `docs/analysis/2026-04-26-deep-audit-performance/summary.md`, `docs/analysis/2026-04-26-deep-audit-performance/15-security-performance.md`, `docs/analysis/2026-04-26-deep-audit-performance/06-async-safety.md`, `docs/analysis/2026-04-26-deep-audit-performance/05-reactive-precision.md`, `packages/flux-runtime/src/async-data/api-data-source-controller.ts`, `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/spreadsheet-core/src/core/clipboard-operations.ts`, `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder.tsx`
- Key decision: only include findings that survived an independent re-read of the live code; downgrade transitional subscription/cancellation gaps instead of overstating them as hard correctness failures.
- Next step: prioritize fixes in the order `api-data-source-controller` telemetry/state consistency, form `valuesPath` publish cost, spreadsheet bulk paste writes, then condition-builder equality and broader per-path value subscription convergence.

### 2026-04-26

- Added an architecture analysis note on which Flux improvements are still worthwhile without changing the current DSL-VM baseline or duplicating `nop-entropy` loader responsibilities.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`, `docs/index.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/flux-dsl-vm-extensibility.md`
- Key decision: place the write-up under `docs/analysis/` rather than `docs/architecture/` because it records forward-looking improvement options and tradeoffs, not a new active normative contract.
- Next step: if any item from the analysis is accepted as the new baseline, promote only that settled subset into the owning `docs/architecture/` document instead of letting the analysis file become a shadow source of truth.

- Refined the first improvement direction in the new analysis note: the point is not to invent a larger Loader-to-Flux envelope, but to fix the semantic invariants of the `Final Schema` that Flux executes.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`
- Key decision: keep `schema` as the only execution subject; optional metadata such as `xui:version`, diagnostics, and per-node `xui:location` may exist, but they do not change the fact that the DSL executed by Flux is the final schema itself.
- Next step: if needed, mirror this clarification into the relevant normative docs when the wording is promoted from analysis to baseline.

- Further tightened the analysis note to avoid over-designing source tracing: replaced the earlier source-map-oriented wording with a minimal per-node `xui:location` approach for diagnostics and debugger backtracking.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`
- Key decision: prefer schema-native location metadata on nodes over a separate global source map structure unless a concrete future requirement proves that the simpler approach is insufficient.
- Next step: keep diagnostics design aligned with `schema` as the only execution subject and avoid introducing a parallel metadata system without demonstrated need.

- Tightened two remaining sections in the same analysis note: replaced abstract `toolchain-aware` wording with concrete editor/debugger/docs consumption paths for `hostContract`, and rewrote the React 19 section as specific usage constraints instead of high-level slogans.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`
- Key decision: keep improvement language close to the current Nop/Flux codebase and workflows, avoiding generic platform jargon when a concrete contract or rule is sufficient.
- Next step: only promote these clarifications into `docs/architecture/` if the team wants them to become active baseline wording rather than analysis guidance.

- Clarified improvement direction three in the same analysis note: prefer compile-time dependency inference when it is precise, use runtime dependency collection only as fallback/refinement, and avoid making compile-time plus runtime double-calculation the default path.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`
- Key decision: the goal is more precise invalidation, not preserving runtime dependency collection everywhere for convenience after compile-time dependency plans already exist.
- Next step: if this direction is adopted later, encode it as a concrete dependency-tracking rule in the owning architecture doc rather than leaving it only in analysis wording.

- Refined the host-contract section in the same analysis note: tools can already consume manifests directly through `RendererDefinition.hostContract` plus version resolution; the improvement is to standardize that path rather than invent a new capability layer.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/schema-diagnostics/manifest.ts`
- Key decision: treat `hostContract -> resolveManifest(...) -> tool consumption` as the correct direct path for editor/debugger/docs use, while keeping explicit host context as a fallback for standalone fragment scenarios.
- Next step: if promoted later, capture this as a small shared helper or documented rule so each tool does not re-implement manifest resolution differently.

- Ran an independent cross-audit of the analysis note with two separate subagents and tightened the wording to align with active baseline docs: switched to the canonical `Final Execution Schema` term, downgraded `xui:location` to a minimal candidate rather than a fixed contract, restored runtime dependency collection as the semantic baseline with compile-time inference as an optimization, and separated two governance items from the four main workstreams.
- Link to doc or code path: `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/capability-projection-manifest.md`
- Key decision: keep the analysis directionally ambitious but terminologically subordinate to the active architecture baseline, so it does not accidentally become a competing source of truth.
- Next step: only promote the parts that reach team agreement into the owning architecture docs; leave the rest explicitly as analysis-level guidance.

- Drafted owner plan `docs/plans/144-flux-execution-boundary-diagnostics-and-host-contract-tooling-plan.md` to capture the initially agreed, directly landable workstreams from the analysis note in one execution document, while keeping the governance-only items out of scope.
- Link to doc or code path: `docs/plans/144-flux-execution-boundary-diagnostics-and-host-contract-tooling-plan.md`, `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`, `docs/plans/00-plan-authoring-and-execution-guide.md`
- Key decision: keep a single owner plan because the executable surface is still one coherent result area, but explicitly exclude governance-only topics so the plan can still close cleanly.
- Next step: re-audit live code for each phase before execution begins, then either keep the single owner plan or split only if one phase turns out to have a materially separate result surface.

- Audited the new owner plan against the plan-writing guide and related completed plans. Tightened it to one clearer result surface by removing the dependency-precision phase, which should be treated as successor-plan material rather than part of the same owner plan.
- Link to doc or code path: `docs/plans/144-flux-execution-boundary-diagnostics-and-host-contract-tooling-plan.md`, `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md`, `docs/plans/41-compiler-integrated-schema-diagnostics-implementation-plan.md`, `docs/plans/112-capability-projection-manifest-implementation-plan.md`
- Key decision: keep Plan 144 as a single owner plan only for execution-boundary wording, diagnostics backtracking, and host-contract tooling consumption; dependency-tracking precision is explicitly out of scope and should get its own successor plan if revived.
- Next step: rename or supersede any stale references to the old broader title if needed, then start a live baseline audit for Phase 1 rather than reopening dependency-tracking under this plan.

- Updated `docs/skills/react19-best-practices-review.md` with project-specific React 19 usage constraints for Flux: `useSyncExternalStore` as the runtime store baseline, narrower rules for `startTransition`, `useDeferredValue`, `useEffectEvent`, and `Suspense/use`, and a stricter “conditionally applicable” framing for `useActionState`, `useFormStatus`, and `useOptimistic`.
- Link to doc or code path: `docs/skills/react19-best-practices-review.md`, `docs/analysis/2026-04-26-flux-architecture-improvement-opportunities.md`
- Key decision: keep this skills doc focused on React 19 review guidance, but add the minimum Flux-specific constraints needed to avoid treating generic React migration advice as a replacement for runtime semantics.
- Next step: if future reviews repeatedly hit the same missing rule, consider moving the stable subset into lint/check automation rather than growing the review doc indefinitely.

### 2026-04-26

- Added a unified `closeSurface` built-in action and kept `closeDialog` / `closeDrawer` as compatibility aliases. The runtime now treats close as a surface-family concern: opening still distinguishes `openDialog` vs `openDrawer`, while closing defaults to the current surface unless `surfaceId` is provided explicitly.
- Link to doc or code path: `packages/flux-core/src/types/actions.ts`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-runtime/src/action-adapter.ts`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/surface-owner.md`, `docs/architecture/flux-core.md`
- Key decision: make close semantics align with the shared `SurfaceRuntime` family instead of forcing schema authors to mirror dialog-vs-drawer presentation choices in internal close buttons.
- Next step: migrate representative playground/examples to `closeSurface` over time while leaving compatibility aliases in place.

- Executed Plan 142: Per-Slot ClassName Props for Layout Containers
  - Added `bodyClassName`, `headerClassName`, `footerClassName`, `toolbarClassName` to `PageSchema` (`packages/flux-renderers-basic/src/schemas.ts`)
  - Added `bodyClassName`, `headerClassName`, `footerClassName` to `ContainerSchema`; also added `header?: BaseSchema[]` and `footer?: BaseSchema[]` interface fields
  - Added `contentClassName`, `toolbarClassName` to `TabsSchema`
  - Added `bodyClassName`, `actionsClassName` to `FormSchema` (`packages/flux-renderers-form/src/schemas.ts`)
  - Added `bodyClassName`, `titleClassName` to `FieldsetSchema` (`packages/flux-renderers-form/src/renderers/fieldset.tsx`)
  - Wired all props through renderers: `page.tsx`, `container.tsx`, `tabs.tsx`, `form.tsx`, `fieldset.tsx`
  - Migrated grid container in `fluxBasicPageSchema.json` from `className` to `bodyClassName`
  - Added focused tests in `packages/flux-renderers-basic/src/__tests__/slot-classname.test.tsx` (13 tests) and `packages/flux-renderers-form/src/__tests__/slot-classname.test.tsx` (5 tests)
  - Updated `docs/architecture/container-spacing-design.md` with "Per-Slot ClassName Override" section
  - Updated `docs/architecture/renderer-runtime.md` with per-slot className contract documentation
  - Updated `docs/architecture/styling-system.md` with per-slot className reference
- Key decision: Used typed `slotProps = props.props as SchemaType` pattern to avoid `unknown` type issues with `cn()`
- Follow-up: Dialog/Drawer per-slot className deferred; `slots` configuration object pattern deferred
