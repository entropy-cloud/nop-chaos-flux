# Development Log — 2026-05-09

### 2026-05-09

- completed the final tree-mode continuity follow-up in `packages/flow-designer-renderers/src/designer-tree-mode.tsx`: the wrapper now initializes its `DesignerCore` once with `useState(...)` instead of re-memoizing on `config` identity, so host `treeDocument` rerenders preserve the same core instance and keep selection/undo continuity intact.
- this resolved the previously exposed `packages/flow-designer-renderers/src/designer-page.tree.test.tsx` regressions (`does not recreate core when treeDocument changes in tree mode` and `preserves selection and undo history continuity across treeDocument updates`) without changing the intended tree-mode host contract.
- final verification for the `plan 239` slice is now fully green: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass on the current workspace baseline; `pnpm --filter @nop-chaos/flow-designer-renderers test` now also passes (`18` files, `113` tests).
- final focused proof remains green on the live playground baseline: `PLAYWRIGHT_PORT=4187 pnpm exec playwright test tests/e2e/flow-designer-label-text.spec.ts --workers=1` passes (`6` tests), confirming node titles, subtitles, and edge labels still resolve correctly after the tree-mode continuity fix.
- `pnpm test` is now fully green across the workspace again; the remaining console output observed during package tests is warning-only noise such as `--localstorage-file` and known React test-environment warnings, not failing assertions.

### 2026-05-09

- started `docs/plans/239-schema-within-prop-custom-field-compilation-plan.md` and landed the core compiler change: `SchemaFieldRule` now supports a field-level `compile` hook so renderer definitions can override default prop compilation for schema-within-a-prop cases.
- `packages/flux-compiler/src/schema-compiler/node-compiler.ts` now compiles props at field granularity: fields with `compile` bypass the default prop path, while ordinary fields still use the existing default compilation semantics. This restores the intended decision boundary described in plan `239` without expanding `TemplateNode`.
- `packages/flow-designer-renderers/src/index.tsx` now gives `designer-page.config` a renderer-owned compile strategy that preserves nested `SchemaInput` subtrees such as `nodeType.body`, `nodeType.inspector.body`, `createDialog.body`, `quickActions`, and `edgeType.body` / `edgeType.inspector.body` instead of pre-evaluating them at page scope.
- added focused regression proof in `packages/flux-compiler/src/schema-compiler-registry-compilation.test.ts` for field-level `compile`, and in `packages/flow-designer-renderers/src/edge-label-expression.test.tsx` for `designer-page.config` nested schema preservation.
- `tests/e2e/flow-designer-label-text.spec.ts` now passes fully again on the live playground baseline (`PLAYWRIGHT_PORT=4187 pnpm exec playwright test tests/e2e/flow-designer-label-text.spec.ts --workers=1`), confirming node titles, subtitles, and edge labels survive compilation and resolve in the correct node/edge render scopes.
- verification status for this slice is now final: `pnpm --filter @nop-chaos/flux-compiler typecheck`, `pnpm --filter @nop-chaos/flux-compiler build`, `pnpm --filter @nop-chaos/flow-designer-renderers build`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass on the current workspace baseline.

### 2026-05-09

- fixed the remaining Playwright regressions in `tests/e2e/performance-table.spec.ts` and `tests/e2e/component-lab/crud-query-and-ownership.spec.ts` by restoring the table expanded-row scoped-slot contract and preserving submitted CRUD query values across query-form remounts.
- `packages/flux-compiler/src/schema-compiler/tables.ts` now compiles `table.expandable.expandedRow` with `params: ['record', 'index']` and `isolate: true`, and `apps/playground/src/pages/performance-table/schema.ts` now uses the canonical `expandable.expandedRow` field instead of a custom top-level region key.
- `packages/flux-renderers-data/src/{crud-renderer.tsx,crud-renderer-ownership.ts}` now keep query-form draft values separate from committed CRUD query state and submit the draft when the form handle bridge is unavailable during remount-sensitive flows such as `clientMode.fetchOnFilter`.
- verification: `PLAYWRIGHT_PORT=4186 pnpm exec playwright test tests/e2e/performance-table.spec.ts` and `PLAYWRIGHT_PORT=4186 pnpm exec playwright test tests/e2e/component-lab/crud-query-and-ownership.spec.ts --grep "re-enters the upstream source owner"` both pass on the updated baseline.

### 2026-05-09

- closed `docs/plans/234-deep-audit-2026-05-08-critical-closure-program-plan.md` and successor plans `235`-`238` after re-auditing the live repo against `docs/plans/00-plan-authoring-and-execution-guide.md`, syncing stale plan state, and recording the final closure evidence.
- finished `docs/plans/235-workspace-test-hard-gate-closure-plan.md` by splitting `packages/flow-designer-renderers/src/designer-command-adapter.test.ts` into graph-focused coverage plus `packages/flow-designer-renderers/src/designer-command-adapter.tree.test.ts`, which brought `pnpm check:oversized-code-files` back to `53 warnings, 0 errors`.
- finalized `docs/plans/236-validation-owner-critical-closure-plan.md`: `08-02` now has focused proof in `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts`, `08-06` in `packages/flux-renderers-form-advanced/src/variant-field/variant-field-owner-contract.test.tsx`, and `08-12` in `packages/flux-renderers-form-advanced/src/detail-view/detail-revalidation.test.tsx`, so the previously open validation-owner closure items are now closed.
- `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx` now revalidates the parent validation owner after non-form draft commits, and `packages/flux-react/src/hooks.ts` now drives `useCurrentFormModelGeneration()` from `currentForm ?? currentValidationScope`; together with `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, these changes bring generic/non-form owner behavior back to the documented validation baseline without changing the supported author-facing contract.
- finalized `docs/plans/237-lifecycle-and-error-integrity-critical-closure-plan.md` with refreshed evidence for retry cancellation, import rollback, Flow Designer transaction rollback, and quick-edit `ActionResult.ok === false` handling in `packages/flux-action-core/src/__tests__/action-dispatcher-control-flow.test.ts`, `packages/flux-runtime/src/__tests__/import-stack.test.ts`, `packages/flow-designer-renderers/src/{designer-command-adapter.test.ts,designer-command-adapter.tree.test.ts}`, and `packages/flux-renderers-data/src/__tests__/table-quick-edit-controller.test.tsx`.
- finalized `docs/plans/238-spreadsheet-canonical-state-owner-closure-plan.md`: `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-resize.ts` now reads committed row/column sizing from `snapshot.activeSheet` and commits back through core resize commands, with row/column proof remaining in `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx`.
- owner-doc decision for plans `235`-`238`: `No owner-doc update required`. These fixes bring live behavior back to the documented owner/runtime baseline without changing the supported author-facing contracts.
- independent closure audit `ses_1f3d7d6fcffeN1812fS1mT7gvS` re-checked the live repo after the final focused proofs and confirmed plans `234`-`238` are closure-ready with no remaining real blockers beyond doc/status synchronization.
- final verification for the post-sync baseline is now recorded again: `pnpm check:oversized-code-files`, `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed on the current workspace baseline. `pnpm test` still emits the existing `--localstorage-file` warning-only output, but it is non-failing.

### 2026-05-09

- executed `docs/plans/233-lazy-eval-field-rule-plan.md`: replaced the `schema.type === 'loop' || schema.type === 'recurse'` hardcoding in `node-compiler.ts` with a declaration-driven `lazyEval` property on `SchemaFieldRule`.
- renamed `TemplateNode.structuralItemData` to `TemplateNode.structuralFields` as a generic `Record<string, CompiledRuntimeValue>` so any renderer can declare lazy-eval fields without compiler knowing specific renderer types.
- `SchemaFieldRule.lazyEval` works with `params` (same as region `params`) to declare what symbols are available at evaluation time; the compiler pushes those as ambient symbols during compilation for precise diagnostics and dependency tracking.
- updated all consumers: `loop.tsx`, `recurse.tsx`, `node-runtime.ts`, compiler prop-coverage tests, and renderer field definitions in both `basic-renderer-definitions.ts` and the test support file.
- all in-scope packages pass typecheck, build, lint, and test.

### 2026-05-09

- closed `docs/plans/232-open-ended-adversarial-review-2026-05-08-remediation-plan.md` after re-auditing the live repo against `docs/plans/00-plan-authoring-and-execution-guide.md` and synchronizing the remaining closure evidence.
- final verification is now fully recorded for plan `232`: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed on the current workspace baseline after the last `spreadsheet-toolbar` test-scope fix.
- owner-doc sync for workstream 4 is now explicit: `docs/components/dynamic-renderer/design.md` records the final `loadAction` field name plus the renderer-owned invalid-action / invalid-schema diagnostic contract; `docs/architecture/component-resolution.md` and `docs/references/action-payload-matrix.md` already matched the landed component-targeting and named-action payload baseline.
- owner-doc decision for workstream 5: `No owner-doc update required`. The host-contract fix preserves validation-context equivalence across object-valued and array-valued capable regions without changing the documented author-visible contract; focused proof remains in `packages/flux-compiler/src/schema-compiler-host-contract.test.ts`.
- independent closure audit `AUDIT-232-CLOSURE-2026-05-09-A1` found no remaining plan-owned live defect or silent in-scope deferral after the owner-doc and evidence sync, so plan `232` now moves to `completed`.
