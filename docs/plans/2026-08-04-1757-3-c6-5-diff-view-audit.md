# C6.5 content diff-view 逐组件审计

> Plan Status: completed
> Mission: component-audit
> Work Item: C6.5
> Last Reviewed: 2026-08-05
> Source: `docs/backlog/component-audit-roadmap.md`（C6.x Phase Details）、`docs/audits/component-audit-checklist.md`（18 维 + 审计卡模板 + 自动修复纪律 + 复杂交互渲染器 deep-audit 21-23 必选）、`docs/skills/deep-audit-prompts.md`、`docs/architecture/styling-system.md`、`docs/logs/2026/08-04.md`（C6.1-C6.4 节）、`docs/plans/2026-08-04-0043-1-c4-2-crud-audit.md`（CX-9 反应通道）
> Related: 依赖 C0（`2026-08-02-2043-1`，completed）完成后开工；与 C6.1-C6.4（同包先例，均 completed/draft 链）并行独立（均只依赖 C0）。前置基础：08-02 nested-schema 机制（completed）已分类本族字段（diff-view fields `:502-520` 全 prop + onLineClick/onHunkExpand event + toggleViewType/setViewType/expandAll/collapseAll kind:'reaction'）；**CX-9 反应式结果捕获与 scope 投影通道（completed，C4.2，`renderer-reaction-handle.ts` `__setLoadCallbacks`/`__setScopeOverride`/`__setIgnoreWritesTo` + flux-core `ReactionHandle.dispatch` ctx.scope）——diff-view 的 4 个 kind:'reaction' 字段（toggleViewType/setViewType/expandAll/collapseAll）同受益，本 plan 核对反应接线是否生效（不重做 CX-9 机制，仅验证本组件消费）**

## Purpose

对 `flux-renderers-content` 复杂交互渲染器 diff-view 完成 18 维逐组件审计（单组件一张审计卡，P0/P1/P2/P3 裁决留痕），**追加 deep-audit 维度 21（显示与定位正确性）/22（集成接线与可操作性）/23（测试有效性与假绿）**（checklist §2 复杂交互渲染器必选），P0/P1 缺陷在**同一 plan 内自动修复**（test-first + 回归测试 + 受影响包验证门禁），完成 ≥1 个真实浏览器组合宿主场景验证（含 bug 73 模式专项检查），最终审计卡 `closed`（P0/P1 清零）。roadmap C6.x Phase Details 记录「diff-view P2 已闭包仅补卡」——上轮审计（07-28 audit-remediation）已处理 P2 类发现（memo/useCallback 模式），本 plan 以组件级卡回填 + 18 维（含 21-23）+ 组合宿主场景 + 反应接线验证为增量交付。重点维度：3 值所有权三态（viewType/activeFileIndex 三态路径）、5 DOM 契约（`nop-diff-view` marker + data-view/data-diff-type/data-slot diff-hunk-header/diff-gutter/data-expanded）、7 事件与 action 契约（onLineClick/onHunkExpand payload）、11 异步生命周期（无外部 IO——内部纯计算，核对无泄漏）、12 组合宿主场景（dialog/CRUD 行内 diff-view、bug 73 模式）、14 React 19（07-28 记录的 memo/useCallback 模式复核）、18 注册/IO 安全（纯客户端渲染、无 IO、syntax-highlight 依赖）。

## Current Baseline

- **组件与文件**：`diff-view/diff-view-renderer.tsx`（443 行）+ 子模块：components/（diff-file-list、diff-gutter、diff-header、diff-hunk、diff-line、diff-split-view、diff-three-column-view、diff-unified-view ×8）、model/（diff-3way、diff-file、diff-inline、diff-parse ×4）、utils/（diff-stats、diff-template ×2）、adapters/（syntax-highlight ×1）。
- **注册定义**：`content-renderer-definitions.ts` diff-view `:496`（defaultSchema `{ type: 'diff-view', viewType: 'split', showLineNumbers: true }`）；fields `:502-520`：oldContent/newContent/middleContent/files/activeFileIndex/language/viewType/showLineNumbers/showInlineDiff/defaultCollapsedLines/wrapLines 全 prop + `onLineClick`/`onHunkExpand` event + `toggleViewType`/`setViewType`/`expandAll`/`collapseAll` **kind:'reaction'**（CX-9 反应通道消费点）。
- **设计文档**：`docs/components/diff-view/design.md` + `example.json` 存在；§10 marker 约定（根 `nop-diff-view` + `data-view="split|unified"` + `data-diff-type` add/delete/context/hunk + `data-diff-inline` + `data-slot="diff-hunk-header"`/`diff-gutter` + `data-expanded`）。
- **playground**：无 component-lab lab 页（维度 18 缺口待核对）；demo 宿主在 `apps/playground/src/pages/diff-demo.tsx`（split/unified/cross-file 多场景 `:138-222`）与 `diff-perf-scale-demo.tsx`（1500+ 行性能页，route-model `:511`）。
- **既有单测**：`diff-view/__tests__/`（diff-3way.test.ts、diff-core.test.ts、diff-cross-file.test.tsx、diff-view-renderer.test.tsx ×4 文件）。
- **e2e**：`tests/e2e/diff-demo.spec.ts`（5 测试：split 行类型、unified 切换、hunk 结构、cross-file 文件列表+导航）、`tests/e2e/diff-perf.spec.ts`（1 测试，首屏渲染 <**5000ms** 阈值——原 `<200ms` 预算在 full-line 渲染改造前从未真实执行，C5.1 verify 已按 calendar-perf 先例校准为 `<5000ms`，`diff-perf.spec.ts:56-59`、`docs/logs/2026/08-04.md` C5.1 verify 节）；本族无 `tests/e2e/component-lab/c6-5-host-surfaces.spec.ts`（需新增）。
- **上轮审计记录**：07-28 audit-remediation 已记录 diff-view memo/useCallback 模式（`docs/audits/2026-07-28-0814-open-audit-audit-remediation.md:69-79`：diff-header/diff-hunk/diff-line/diff-gutter memo + 自定义 comparator、renderer 8×useCallback、split-view 2×useMemo）——本轮按 React 19 维度（14）复核是否仍为反模式；roadmap 记录「diff-view P2 已闭包仅补卡」。
- **基线**：C0 基线 unit 全绿（typecheck/build/lint 31/31、test 58/58）；**e2e 已于 C5.1 verify 轮达成 full-green（882 passed / 43 skipped / 0 failed），C6.2 VERIFY 轮维持（929 passed / 43 skipped / 0 failed，`docs/logs/2026/08-04.md`）**——本 plan 不继承 e2e pre-existing 债务；`gantt-bars-and-links.spec.ts:132` 拖拽时序 flake 为机器负载 watch-only residual（successor C9，本 plan 不涉及）。

## Goals

- 1 张审计卡（`docs/audits/per-component/diff-view.md`）18 维 + deep-audit 21-23 逐项核对，P0/P1/P2/P3 裁决留痕，`文件:行` 证据，状态 `closed`（P0/P1 清零）。
- 本组件 P0/P1 缺陷 test-first 自动修复（契约/公共层修复 Must automate），P2 低成本当场修复、其余登记卡内 backlog 归 CR。
- ≥1 个真实浏览器组合宿主场景（含 bug 73 模式专项）：候选——diff-view 在 dialog 内渲染 + 行点击事件、cross-file 模式切换 + 文件导航、reaction 字段（toggleViewType/setViewType/expandAll/collapseAll）接线验证。
- roadmap C6.5 行标 `done`（独立子 agent closure-audit pass 后）。

## Non-Goals

- C6.3/C6.4 content 其余族（`2026-08-04-1757-1`/`2026-08-04-1757-2` 覆盖）、C6.1/C6.2 已收口组件不重审。
- CX-9 反应通道机制本身（已在 C4.2 收口，本 plan 仅验证 diff-view 组件消费接线）。
- checklist v2 修订（CG）、审计工具脚本升级（CG）。
- 结构性重构（公共 API/包边界/编译期机制，需人工确认后另立 CX-n；纯行为修复不视为结构性）。
- 审计卡 P2 backlog（>15 分钟项）归 CR 处理，不在本 plan 内实现。

## Scope

### In Scope

- diff-view × 18 维审计卡（维度重点：1 Schema 契约（DiffViewSchema 与注册 fields `:502-520` 一致——**含 4 个 kind:'reaction' 字段的声明与消费核对**）、2 RendererComponentProps 合规、3 值所有权三态（**viewType/activeFileIndex 三态完整路径：local/controlled/scope、defaultSchema 默认 `viewType:'split'`、越界钳制（activeFileIndex 超 files 范围）**）、4 表单参与（非表单字段——核对无泄漏）、5 DOM 与选择器契约（**根 `nop-diff-view` marker + data-view + data-diff-type add/delete/context/hunk + data-slot diff-hunk-header/diff-gutter + data-expanded，与 design.md §10 一致**）、6 嵌套 schema 分类（08-02：全 prop 无嵌套 schema/action——核对 files/oldContent 等无深嵌套分类残留）、7 事件与 action 契约（**onLineClick/onHunkExpand payload 形状与 eventContracts**）、8 a11y（diff 行语义、键盘操作 hunk 展开/折叠、aria 契约）、9 i18n（t('flux.diff.noChanges') 等 key 存在性）、10 四态覆盖（空态 noChanges、错误态、禁用态、加载态——纯同步计算无加载）、11 异步生命周期（**无外部 IO（无 fetch/流式）——纯客户端计算，核对无泄漏/无竞态；syntax-highlight 同步处理大文件性能**）、12 组合宿主场景（**dialog/CRUD 行内 diff-view（bug 73 模式）、cross-file 导航、reaction 字段接线**）、13 样式契约（widget 自样式 + diff-view.css）、14 React 19（**07-28 记录 memo/useCallback 模式复核——Compiler 基线是否仍冗余**）、15 性能边界（**1500+ 行 diff 渲染路径、diff-perf 5000ms 阈值（C5.1 校准后，不得回退 200ms）、行级渲染拆分/虚拟化核对**）、16 测试质量（**diff-perf 假绿核查、既有 diff-view-renderer.test.tsx 断言正确行为而非 not-throw、deep-audit 23 测试有效性**）、17 文档对照（design.md ↔ 实现 props/行为）、18 注册/包边界/IO 安全红线（surface 双注册、**无 component-lab lab 页——覆盖缺口核查**、无 IO 边界触碰、syntax-highlight 依赖核查））。
- **追加 deep-audit 维度**（checklist §2 复杂交互渲染器必选）：21 显示与定位正确性（split/unified/three-column 三视图行对齐、gutter 定位）、22 集成接线与可操作性（ComponentHandle/reaction 接线、事件接线、与宿主交互）、23 测试有效性与假绿（单测真实断言、perf 测试可靠性）。
- P0/P1 自动修复（test-first）+ P2 低成本即时修复。
- 真实浏览器宿主场景（dialog 内 diff-view + 行点击、cross-file 切换、reaction 字段）。
- 审计卡状态流转（open → fixing → fixed-pending-closure → closed）与 daily log 记录。

### Out Of Scope

- C6.1-C6.4 content 其余族（并行/后续 plan 覆盖）。
- checklist v2 修订（CG）。
- 结构性重构（需人工确认后另立 CX-n）。
- CX-9 机制实现本身（C4.2 已收口）。

## Failure Paths

| 可测场景编号       | 触发                          | 行为（含错误码）                                                         | 可重试 | 用户可见表现  |
| ------------------ | ----------------------------- | ------------------------------------------------------------------------ | ------ | ------------- |
| host-diff-dialog   | diff-view 在 dialog 内渲染    | 三视图正常渲染、行点击派发 onLineClick payload 正确、dialog 内交互不逃逸 | 是     | diff 内容正确 |
| host-diff-crosfile | cross-file 模式文件切换       | 文件列表导航驱动内容切换、activeFileIndex 越界钳制                       | 是     | 文件切换正确  |
| host-diff-reaction | toggleViewType/setViewType 等 | reaction 字段经 CX-9 通道派发后 viewType 更新生效（scope 投影正确）      | 是     | 视图切换正确  |
| host-diff-expand   | hunk 展开/折叠                | expandAll/collapseAll + 单 hunk 展开折叠、data-expanded 状态正确         | 是     | 展开折叠正确  |
| host-diff-empty    | 无差异内容                    | 空态 noChanges 渲染、不崩溃                                              | 是     | 空态文案可见  |

## Test Strategy

本档选择：**必须自动化** —— diff-view 是复杂交互渲染器（checklist §2 必选 deep-audit 21-23），reaction 字段接线（CX-9 通道消费）与事件 payload（onLineClick/onHunkExpand）为核心契约回归路径；diff-perf **5000ms** 阈值为性能回归红线（C5.1 已校准，**不得改回原 200ms 预算**，不得回归）；契约/公共层修复必须 test-first（先写失败测试再实现）。验证门禁：受影响包 `pnpm --filter @nop-chaos/flux-renderers-content typecheck/build/lint/test` + 相关 e2e 回归（`diff-demo.spec.ts`、`diff-perf.spec.ts`）+ 本族新增宿主场景 Playwright（programmatic DOM 断言）。

## Execution Plan

### Phase 1 - 18 维审计 + deep-audit 21-23 与审计卡产出

Status: completed
Targets: `packages/flux-renderers-content/src/diff-view/`（renderer + components/model/utils/adapters）、`content-renderer-definitions.ts`、`schemas.ts`、`docs/audits/per-component/`

- Item Types: `Proof`

- [x] 审计前核对注册定义：diff-view 注册项（fields `:502-520` 全 prop + event + reaction 分类）与 schema 一致（维度 1/18）。
- [x] 产出 1 张审计卡：18 维逐项核对（结论 pass/fail/n-a + `文件:行` 证据 + 发现）+ deep-audit 维度 21/22/23（显示定位/集成接线/测试有效性），P0/P1/P2/P3 裁决留痕（checklist §3）。
- [x] 维度重点核查：值所有权三态（维度 3：**viewType/activeFileIndex 三态完整路径 + defaultSchema 默认值 + 越界钳制**）；**CX-9 反应接线（维度 1/7/22：toggleViewType/setViewType/expandAll/collapseAll 4 个 kind:'reaction' 字段经 ReactionHandle 派发后 scope 投影生效——不重做机制，仅验证组件消费**）。
- [x] DOM 契约（维度 5：`nop-diff-view` + data-view + data-diff-type + data-slot diff-hunk-header/diff-gutter + data-expanded 与 design.md §10 一致）与事件契约（维度 7：onLineClick/onHunkExpand payload）。
- [x] React 19 复核（维度 14）：07-28 记录的 memo/useCallback 模式（diff-header/diff-hunk/diff-line/diff-gutter memo + renderer 8×useCallback + split-view 2×useMemo）在 Compiler 基线下是否仍冗余——裁定 keep/修复。
- [x] 性能边界（维度 15/21/23）：1500+ 行 diff 渲染路径、diff-perf **5000ms** 阈值复验（保持校准后阈值，不得回归到未被执行过的 200ms 预算）、行级渲染拆分；测试有效性（维度 16/23）——diff-view-renderer.test.tsx 断言正确行为而非 not-throw、perf 测试假绿核查。
- [x] 文档对照（维度 17）：design.md ↔ 实现 props/行为逐项核对。
- [x] playground 覆盖核查（维度 18）：无 component-lab lab 页——记录缺口并裁决（P2 低成本当场补页，Phase 3 落地）。

Exit Criteria:

> 本 Phase 交付 1 张审计卡（含裁决 + 21-23），是后续修复的唯一事实来源。

- [x] `docs/audits/per-component/diff-view.md` 卡存在，18 维 + 21-23 表完整、`文件:行` 证据可验证。
- [x] 卡内发现清单带 P0/P1/P2/P3 裁决；无 P0/P1 的卡标记 `closed`，否则 `open`；CX-9 反应接线与 08-02 机制复验结论已记录。

### Phase 2 - P0/P1 自动修复（test-first）

Status: completed
Targets: 发现涉及的 renderer/子模块文件、schema 文件、契约测试文件、e2e spec

- Item Types: `Fix | Proof | Decision`

- [x] 按审计卡处理 P0/P1：先写复现/回归测试（断言正确行为，非仅 not-throw），再实现修复；DOM/选择器契约变更追加 focused 契约测试与 e2e（test-first 证据：复现测试先于实现）。
- [x] P2 低成本（约 15 分钟内）当场修复；其余登记卡内 backlog 归 CR。
- [x] 共性缺陷裁决（Decision）：若发现 ≥2 组件/跨包/公共层根因，按 roadmap §7 主动插入 CX-n 或并入现有项并回写 daily log；组件单点根因则记录裁决、不插入 CX-n。
- [x] 修复后卡内发现标 `fixed` + commit/plan 引用；卡状态流转 `open → fixing → fixed-pending-closure`。
- [x] 复杂/跨包 bug 修复按 AGENTS.md Bug Fix Test Coverage Rule 记录到 `docs/bugs/`（参考 `docs/bugs/73-*.md` 格式）。

Exit Criteria:

> 本 Phase 交付"发现清零或已裁定"，卡状态与代码行为一致。

- [x] 全部 P0/P1 已修复（卡内标 `fixed` + 证据）或已显式裁定延期（仅允许依赖未落地跨 plan 机制者，标「机制落地后复验」并登记）；无静默跳过。
- [x] 受影响包 `pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 绿（含新增回归测试）。

### Phase 3 - 组合宿主真实浏览器场景

Status: completed
Targets: `tests/e2e/component-lab/c6-5-host-surfaces.spec.ts`（新增）、playground demo/lab 页

- Item Types: `Proof | Fix`

- [x] 设计并实现 ≥1 个本组件真实浏览器组合宿主场景（programmatic DOM 断言，禁截图诊断）：新增 **1 个 lab 页**（`diff-view-lab-page.tsx`，补上维度 18 缺口——P2 低成本当场补页裁决）+ **`RENDERER_LAB_REGISTRY` 注册 1 条**（`apps/playground/src/component-lab/renderer-lab-registry.ts`，缺注册则 component-lab-page 不渲染 lab 页且 smoke 覆盖失败——C6.1/C6.2 先例均含 registry 条目）+ `data-c6c5-host.ts` 宿主 schema 模块 + CONTENT_RENDERER_ROUTES 追加 1 条（与 C6.1-C6.4 共用常量模块——协调先例：C6.1 建模块 5 条、C6.2 至 11 条、C6.3 至 14 条、C6.4 至 18 条，本 plan 追加至 19 条；**最终落地条数以实际落地顺序为准——各 plan 追加互斥固定集合，先落地先追加，route-matrix liveTotal 动态求和自动一致**）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 1 条（smoke.spec.ts 自动覆盖）+ route-matrix 计数测试同步（lab 路由不经 `DOMAIN_RENDERER_ROUTES`，由 smoke.spec 门禁；route-matrix liveTotal 动态求和自动一致）。
- [x] bug 73 模式专项检查：**diff-view 在 dialog 内渲染 + 行点击事件**（真实浏览器宿主，非仅单测）；cross-file 导航切换组合。
- [x] 宿主场景发现的新缺陷按 Phase 2 流程修复（test-first）。
- [x] 既有相关 e2e（`diff-demo.spec.ts`、`diff-perf.spec.ts`）在本族改动后回归（**diff-perf 5000ms 阈值不得回归**）。

Exit Criteria:

> 本 Phase 交付"真实浏览器行为成立"。

- [x] 宿主场景 spec 通过（Playwright，programmatic DOM 断言）；相关既有 e2e 回归绿。
- [x] bug 73 模式专项检查结论记录于 daily log（pass 或带证据 fail→已修复）。

### Phase 4 - 组件回归与审计卡 closure

Status: completed
Targets: 审计卡、`docs/logs/2026/08-04.md`、`docs/backlog/component-audit-roadmap.md`（C6.5 行）

- Item Types: `Proof`

- [x] 全卡复查：卡 18 维 + 21-23 表结论与最终代码一致；P0/P1 清零；卡状态 `closed`（含 fixed-pending-closure → closed 流转）。
- [x] 本组件范围回归：`pnpm --filter @nop-chaos/flux-renderers-content test` + 相关 e2e spec 全绿（e2e 基线 full-green 929 passed/43 skipped/0 failed，本 plan 不得引入新增失败）；workspace 全量 `pnpm typecheck`/`build`/`lint`/`test` + `pnpm test:e2e` 最终以 Closure Gates 为准（指南 Minimum Rule 18：全量验证归 closure，非 Phase 默认项——此处仅作收口前置预跑）。
- [x] daily log 记录：卡 closure 汇总、修复清单（commit/plan 引用）、宿主场景结果、CX-9 反应接线验证结论、08-02 机制复验结论、CX-n 插入（若有）与决策、与 C6.1-C6.4 的 CONTENT_RENDERER_ROUTES 协调结论。（`docs/logs/2026/08-04.md` C6.5 节）
- [x] roadmap C6.5 行标 `done` 的前置：独立子 agent closure-audit pass（Closure Gates 项，不在本 plan 执行 session 内自审；已交付全部执行证据，由 mission-driver CLOSURE_VERIFY fresh session 执行 audit 后收口）。

Exit Criteria:

> 本 Phase 交付"可进入 closure-audit 的完成态"。

- [x] 审计卡全部 `closed`；`docs/audits/per-component/` 汇总可读。
- [x] daily log 已记录本 plan 收口证据（含 closure-audit 证据位置）。

## Draft Review Record

> 起草后、执行前的独立审查证据。详见 `docs/plans/00-plan-authoring-and-execution-guide.md` 的 `Plan Review Rule`。由独立审阅者或独立子 agent 填写。

- Reviewer / Agent: 独立子 agent fresh session（task `ses_033c68f6effesLC7IF48prFVUE`）
- Verdict: `pass-with-minors`（Round 2 零 Blocker/Major；Round 1 `revised` 的 M1 + m1/m2/m3 已修正复核通过）
- Rounds: 2
- Findings addressed: **Major（M1 diff-perf 阈值错写 200ms）已修正**——live 实证 `diff-perf.spec.ts:30/:59` 为 `<5000ms`（`<200ms` 预算在 full-line 渲染改造前从未被执行，C5.1 verify 按 calendar-perf 先例校准，`docs/logs/2026/08-04.md:227`）；4 处（baseline/Test Strategy/Phase 1/Phase 3）+ Scope 维度 15 第 5 处全部改为 **5000ms** 并加"不得改回 200ms"守卫；**m1 已修正**（route-model `:511`）；**m2 已修正**（Phase 3 补 `RENDERER_LAB_REGISTRY` 注册 1 条）；**m3 已修正**（route 追加条数以落地顺序为准、liveTotal 动态求和的 landing-order 注记）。其余引用核对通过（注册 :496、defaultSchema :500、fields :502-520（onLineClick/onHunkExpand event :514-515 + 4×kind:'reaction' :516-519）、markers :106/:136/:168/:409、design.md §10 :111-121、子模块清单、4 测试文件、diff-demo.spec.ts 5 测试、route-model :249、roadmap C6.5 `todo` :41 + 「diff-view P2 已闭包仅补卡」:164、07-28 audit 记录 :69-79、CX-9 完成前提（`renderer-reaction-handle.ts:352/:361/:370` + 父 plan completed）、e2e 基线 929/43/0）。

## Closure Gates

> 关闭条件：本 section 与每个 Phase 的 Exit Criteria 全部勾选后才能将 `Plan Status` 改为 `completed`。

- [x] 1 张审计卡存在、18 维 + 21-23 表完整、P0/P1 清零、状态 `closed`
- [x] 全部 in-scope P0/P1 已 test-first 修复并有回归测试；无被静默降级到 deferred 的 live defect/contract drift
- [x] ≥1 个真实浏览器组合宿主场景通过（含 bug 73 模式专项检查——diff-view dialog 内渲染 + 行点击）
- [x] 共性缺陷已按 §7 处理（CX-n 插入/合并或当前 plan 内修复，决策记录在卡与 daily log）
- [x] 受影响的 owner docs 已同步到 live baseline（design.md/quick-reference/roadmap 表按发现实际影响为准；无变更则不写）
- [x] 由独立子 agent（fresh session）执行的 closure-audit 已完成并记录证据；执行 session 不得自审勾选本项
- [x] `pnpm typecheck`
- [x] `pnpm build`
- [x] `pnpm lint`
- [x] `pnpm test`

## Deferred But Adjudicated

### 审计卡 P2 backlog（成本 >15 分钟的非阻断体验/文档/测试加固项）

- Classification: `optimization candidate`
- Why Not Blocking Closure: checklist §3 明确 P2 可入审计卡 backlog 由 CR 自动处理；不阻塞本组件 supported baseline 成立。
- Successor Required: `yes`
- Successor Path: CR work item（跨族集中修复）

### 依赖未落地跨 plan 机制的发现（若有，「机制落地后复验」）

- Classification: `watch-only residual`
- Why Not Blocking Closure: 依赖的机制未落地时无法在卡内闭合；按 checklist §3 自动修复纪律（「机制落地后复验」显式登记）由 CR 集中复验，卡内不悬挂。
- Successor Required: `yes`
- Successor Path: CR work item

## Non-Blocking Follow-ups

- 审计中发现但裁定为 P3（风格 nit/注释）的项仅卡内记录，不处理。
- 工具脚本新增模式（若有）记入卡内，CG 统一升级。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 pre-existing 红（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。

## Closure

Status Note: 已执行（四 Phase 全部 completed——1 卡 closed + P1×10 test-first 修复 + P2×4 当场修复 + 宿主 5/5 含 bug 73 专项 + 全量 typecheck/build/lint/test 绿）。closure-audit 由 mission-driver CLOSURE_VERIFY fresh session 于 2026-08-05 pass，roadmap C6.5 行已标 `done`，本 plan 正式收口。执行证据见 `docs/logs/2026/08-04.md` C6.5 节。

Closure Audit Evidence:

- Auditor / Agent: mission-driver CLOSURE_VERIFY 独立 fresh session（`MISSION_DRIVER:2026-08-05-065620-mission-driver`，2026-08-05）
- Evidence: live repo 逐项核对通过——① 审计卡存在且 `closed`（`docs/audits/per-component/diff-view.md`，P0 ×0 / P1 ×10 fixed / P2 全裁定 / P3 keep ×5）；② P1-1..P1-10 各修复实证（`diff-hunk.tsx` data-diff-type="hunk"、`diff-template.ts` 'insert'→'add' 映射、`diff-three-column-view.tsx` 三 pane onLineClick 接线、`diff-view-renderer.tsx:293` reactions ready() + componentRegistry.register + clamp `Math.max(0,…)` + 事件 ctx 注入、`content-renderer-definitions.ts` eventContracts + 契约测试、`diff-view-renderer.test.tsx` 假绿重写）；③ bug 记录 `docs/bugs/85-diff-view-handle-and-reaction-dead-wiring-fix.md` 存在；④ lab 页 `diff-view-lab-page.tsx` + `RENDERER_LAB_REGISTRY` 1 条（registry `:187`）+ `data-c6c5-host.ts` + `CONTENT_RENDERER_ROUTES` 19 条 + `COMPONENT_LAB_COVERAGE_MANIFEST` 1 条（manifest `:638-645`）+ 宿主 spec `tests/e2e/component-lab/c6-5-host-surfaces.spec.ts`；⑤ 实测：`pnpm --filter @nop-chaos/flux-renderers-content typecheck/build/lint/test` 279/279 绿、workspace `pnpm typecheck`/`build`/`lint`/`test` 31/31/31/58 全绿、`c6-5-host-surfaces.spec.ts` 5/5 绿（含 bug 73 模式 host-diff-dialog 行点击 probe）、`diff-demo.spec.ts` 5/5、`diff-perf.spec.ts` 1/1（实测 1067ms < 5000ms 校准阈值）；⑥ design.md §4/§5/§7/§8/§9/§10/§11/§12 文档同步实证；⑦ roadmap C6.5 行 `done`；⑧ deferred 项分类诚实（P2-2 i18n backlog 归 CR、P2-5/P2-6 keep 裁定留痕、pre-existing oversized-file watch-only，均 non-blocking）；⑨ CX-9 反应接线验证通过（P1-4 修复后 host-diff-reaction 实证 data-view 翻转）、08-02 嵌套 schema 分类复验通过（dim 6 pass）、CX-n 不插入决策记录于卡与 daily log。

Follow-up:

- P2 backlog（diff-file-list 硬编码英文 i18n 机制项）归 CR work item 集中修复（卡内记录）。
- `pnpm check:oversized-code-files` pre-existing 红（C5.1 基线 14 文件）为 watch-only residual，归 CG/CR。
- no remaining plan-owned work。
