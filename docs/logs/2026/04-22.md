# Development Log — 2026-04-22

### 2026-04-22

- Locked the new owner-semantics terminology to `Data Domain Owner` instead of `Data Scope Owner`, to keep `scope` reserved for lexical read/projection semantics and avoid implying that every new scope or projection automatically creates a new owner boundary.
- Link to doc or code path: `docs/architecture/data-domain-owner.md`
- Key decision: use `domain` for semantic ownership and keep `scope` for read visibility and projection mechanics.
- Next step: preserve this terminology split when future architecture docs are updated to align with the new owner baseline.

### 2026-04-22

- Promoted the Data Domain model into a formal architecture baseline under `docs/architecture/`, defining `Data Domain Owner` as the long-term owner-semantics layer above validation and staged/live publish policies, and documented a phased migration path that does not require immediate runtime rewrites.
- Link to doc or code path: `docs/architecture/data-domain-owner.md`, `docs/index.md`, `docs/architecture/README.md`, `docs/references/architecture-doc-status-matrix.md`
- Key decision: treat this as a necessary long-term architecture doc because it unifies future `form` / `detail-*` / row editor / filter / wizard evolution under one stable owner vocabulary; keep it as an owner-semantics document rather than a new primitive or mandatory runtime type.
- Next step: converge `form-validation.md`, `value-adaptation-and-detail-field.md`, and `table-row-identity-and-scope-performance.md` onto this owner-semantics baseline before introducing any new shared runtime substrate.

### 2026-04-22

- Unified the final runtime action invocation boundary by routing built-in, `component:<method>`, and namespaced actions through `ActionRuntimeAdapter`, while keeping payloads args-centric and leaving dispatch ordering plus compiled payload evaluation in `flux-action-core`.
- Link to doc or code path: `packages/flux-core/src/types/actions.ts`, `packages/flux-action-core/src/action-dispatcher.ts`, `packages/flux-runtime/src/action-adapter.ts`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/flux-core.md`, `docs/analysis/experiment-retrospective-value-assessment.md`, `docs/plans/126-runtime-effect-channel-convergence-plan.md`
- Key decision: do not re-split built-in payloads below the public adapter contract; plan 119 remains the payload baseline, so built-in invocations stay `args`-centric while component and namespace calls share the same runtime adapter outlet.
- Next step: finish focused verification, then treat future effect-channel work as host/runtime side-effect convergence on top of the unified adapter boundary rather than as a prerequisite for unifying action invocation.

### 2026-04-22

- Added a compatibility audit for the proposed Data Domain / Validation facet / Staged Owner architecture, concluding that it fits the current docs well as a high-level semantic model but only if owner boundaries stay narrow and projected editors or repeated scopes are not automatically promoted into child owners.
- Link to doc or code path: `docs/discussions/2026-04-22-data-domain-compatibility-audit.md`, `docs/architecture/form-validation.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/architecture/object-field.md`, `docs/architecture/array-field.md`, `docs/architecture/variant-field.md`, `docs/architecture/scope-ownership-and-isolation.md`
- Key decision: treat `Data Domain Owner` as a unifying semantic lens over existing owner-based boundaries, not as a mandate that every local scope, loop item, or value-oriented editor becomes a new runtime owner.
- Next step: if this direction becomes normative, update `form-validation.md`, `value-adaptation-and-detail-field.md`, and `table-row-identity-and-scope-performance.md` first; keep lower-level `object-field` / `array-field` / `variant-field` docs aligned as parent-owned projected-editor baselines.

### 2026-04-22

- Expanded the staged-owner discussion into a zero-baseline architecture proposal: treat `Data Domain Owner` as the primary runtime unit, with `ValidationScope` as its validation facet and staged/live editing as publish policies rather than separate top-level subsystems.
- Link to doc or code path: `docs/discussions/2026-04-22-staged-owner-draft-semantics.md`, `docs/architecture/form-validation.md`, `docs/architecture/surface-owner.md`, `docs/architecture/table-row-identity-and-scope-performance.md`
- Key decision: if the project were redesigned from scratch, validation should attach directly to each data domain, while `dialog`/`drawer` remain pure surface owners and row-local staged editing is modeled as a child data domain with `rowKey`-based retarget semantics.
- Next step: if this architecture direction graduates from discussion to design work, define the minimal `DataDomainOwner` contract and decide whether current `FormRuntime` should eventually be reframed as one concrete domain runtime rather than the implicit default for all validation-capable scopes.

### 2026-04-22

- Added a discussion note that reframes future `DraftScope` work as a broader staged-owner design problem across `form` / `dialog` / `row` / `detail-*`, with policy axes for ingress, egress, validation ownership, and row identity retargeting.
- Link to doc or code path: `docs/discussions/2026-04-22-staged-owner-draft-semantics.md`, `docs/architecture/scope-ownership-and-isolation.md`, `docs/architecture/surface-owner.md`, `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/architecture/value-adaptation-and-detail-field.md`
- Key decision: recommend `Staged Owner` as the unifying architectural concept, keep `dialog` / `drawer` as surface owners, and treat `rowKey` retargeting as the key complexity for row-local staged editing rather than forcing a universal `DraftScope` abstraction first.
- Next step: if this direction becomes active implementation work, decide whether the first reusable substrate should be a shared staged-owner status contract or a lighter `ValidationScopeRuntime` factory for local draft editors.

### 2026-04-22

- Tightened the retrospective's effect-discipline section and added a successor plan that distinguishes the already-landed `ActionRuntimeAdapter` boundary from the still-missing runtime-wide interception plane.
- Link to doc or code path: `docs/analysis/experiment-retrospective-value-assessment.md`, `docs/plans/126-runtime-effect-channel-convergence-plan.md`, `packages/flux-runtime/src/action-adapter.ts`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`
- Key decision: treat `EffectChannel` as an internal middleware convergence target, not an immediate pure-data/effect-algebra rewrite; keep `data-source` as a runtime owner with its own refresh/publication semantics even though its action-backed execution body already reuses action-core.
- Next step: if this plan is executed, start with an effect taxonomy and adapter middleware shell before touching data-source/reaction host-boundary calls.

### 2026-04-22

- Reframed the retrospective draft-isolation theme so it now recognizes the live renderer-local staged form baseline in `detail-field` / `detail-view` and treats a shared `DraftScope` substrate as conditional future convergence instead of an already-proven next step.
- Link to doc or code path: `docs/analysis/experiment-retrospective-value-assessment.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx`
- Key decision: keep the current staged-owner pattern as the baseline, and only promote it into shared draft infrastructure if multiple owner families really need common draft lifecycle, conflict detection, or isolated validation semantics.
- Next step: if wizard, row editor, or other staged editors start duplicating the same lifecycle semantics, evaluate a shared draft substrate from those live usages rather than from the experiment design alone.

### 2026-04-22

- Tightened the ScopeChange retrospective claim to match live usage, then landed a minimal runtime baseline by adding monotonic `revision` metadata to runtime-owned `ScopeChange` emissions without forcing a full write-source taxonomy.
- Link to doc or code path: `docs/analysis/experiment-retrospective-value-assessment.md`, `packages/flux-core/src/types/scope.ts`, `packages/flux-runtime/src/scope.ts`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-runtime-status.ts`, `packages/flux-runtime/src/__tests__/scope-merge.test.ts`
- Key decision: ship only the low-risk shared ordering signal (`revision`) now; defer `source` classification until a concrete self-write protection, debugger provenance, or loop-diagnosis consumer exists.
- Next step: if a future debugger or reaction guard needs cause-level write attribution, thread `source` through runtime-owned write entry points instead of inferring it after the fact.

### 2026-04-22

- Recalibrated the experiment retrospective so the async-governance section now matches the live runtime baseline instead of overstating `epoch` / lane / `ConcurrencyPolicy` as immediate missing architecture.
- Link to doc or code path: `docs/analysis/experiment-retrospective-value-assessment.md`, `packages/flux-runtime/src/async-data/async-governance.ts`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`, `packages/flux-runtime/src/form-runtime-validation.ts`
- Key decision: treat the current shared `runId` / `supersededBy` / `stale-dropped` substrate as an already-landed baseline, and frame stronger `epoch` / lane abstractions as conditional future convergence rather than a clearly required next step.
- Next step: if future runtime work exposes real owner multi-lane concurrency gaps, evaluate the smallest additive governance abstraction instead of precommitting to the full Final design.

### 2026-04-22

- Updated the action authoring baseline so `setValue` and `setValues` now treat `args` as the canonical payload carrier, using narrow write DTOs (`{ path, value }` and `{ path?, values }`) while keeping targeting and control-flow as separate field families.
- Link to doc or code path: `docs/architecture/action-algebra-formal-spec.md`, `docs/references/action-payload-matrix.md`, `docs/references/flux-json-conventions.md`, `packages/flux-action-core/src/action-core.ts`, `packages/flux-action-core/src/action-dispatcher.ts`, `packages/flux-runtime/src/action-adapter.ts`
- Key decision: standardize payload on `args` without collapsing the whole action object into only `action + args`; `path` becomes the preferred write-path field name, and legacy top-level `value` / `values` remain compatibility input only.
- Next step: finish migrating tests and representative schemas away from legacy write payload fields, then verify the workspace against the updated contract.

### 2026-04-22

- Clarified the architecture taxonomy split between platform layers and execution-model categories so readers can distinguish `Authoring Model` / loader / execution / host layering from primitive-owned surfaces and derived runtime systems.
- Link to doc or code path: `docs/architecture/frontend-programming-model.md`, `docs/architecture/action-algebra-formal-spec.md`
- Key decision: keep the four platform layers unchanged, but add an explicit execution-model taxonomy that places `Action Algebra` inside `Flux` execution as a derived system and keeps JSON/XML authoring schema outside core.
- Next step: apply the same classification language consistently when future docs discuss expressions, `ApiSchema`, or other primitive-owned surfaces.

### 2026-04-22

- Executed `docs/plans/125-flux-runtime-async-data-internal-reorganization-plan.md` by moving the runtime-owned request/cache/async-governance/source/reaction implementation cluster into `packages/flux-runtime/src/async-data/` without changing the public `packages/flux-runtime/src/index.ts` surface.
- Link to doc or code path: `docs/plans/125-flux-runtime-async-data-internal-reorganization-plan.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/architecture/dependency-tracking.md`, `packages/flux-runtime/src/async-data/request-runtime.ts`, `packages/flux-runtime/src/async-data/data-source-runtime.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`, `packages/flux-runtime/src/async-data/api-cache.test.ts`
- Key decision: keep `scope-change.ts` and `status-owner.ts` at the top level as focused helpers, avoid adding an internal `async-data/index.ts` barrel, and limit the change to physical regrouping plus import/doc/test realignment so plans 110 and 120 semantics remain unchanged.
- Next step: finish workspace-wide verification and record independent closure-audit evidence before marking plan 125 completed.

### 2026-04-22

- Drafted successor plan `docs/plans/125-flux-runtime-async-data-internal-reorganization-plan.md` for package-internal async-data regrouping inside `flux-runtime`, including an explicit same-package unit-test migration matrix, preserved behavior invariants from plans 110 and 120, and a shallow flat `async-data/` target layout to keep maintenance and AI navigation costs low.
- Link to doc or code path: `docs/plans/125-flux-runtime-async-data-internal-reorganization-plan.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `packages/flux-runtime/src/__tests__/request-runtime.test.ts`, `packages/flux-runtime/src/api-cache.test.ts`
- Key decision: keep this as an internal layout plan only, not a request/data package extraction plan; `scope-change.ts` and `status-owner.ts` stay out of the first regrouping pass, and the first reorganization target uses a single flat `async-data/` directory instead of nested request/data-source subdirectories.
- Next step: execute plan 125 only after a fresh live import-graph audit confirms the frozen target layout and test matrix.

### 2026-04-22

- Closed plan 124 after the final active-doc audit removed stale schema-compiler ownership anchors that still pointed at `flux-runtime` instead of `flux-compiler`.
- Link to doc or code path: `docs/architecture/field-binding-and-renderer-contract.md`, `docs/architecture/field-metadata-slot-modeling.md`, `docs/plans/124-runtime-compat-removal-and-boundary-cleanup-plan.md`
- Key decision: treat active architecture docs as part of closure evidence, so plan completion requires fixing stale owner code anchors instead of leaving them as harmless historical references.
- Next step: None for plan 124; future request-runtime extraction, if needed, should land in a separate successor plan.

### 2026-04-22

- Executed plan 124 cleanup slices that removed runtime-side compiler/action compatibility residue and corrected plan closure drift from plans 122 and 123.
- Link to doc or code path: `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/action-adapter.ts`, `packages/flux-runtime/src/runtime-action-helpers.ts`, `packages/flux-compiler/src/schema-compiler-registry.test.ts`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/plans/122-compiler-package-extraction-and-boundary-plan.md`, `docs/plans/123-flux-runtime-split-and-boundary-hardening-plan.md`, `docs/plans/124-runtime-compat-removal-and-boundary-cleanup-plan.md`
- Key decision: adopt the no-compat first-version baseline fully by removing runtime compiler re-exports, deleting runtime-side duplicate action/operation-control files, and treating direct owner imports as the only supported surface.
- Next step: finish active-doc wording cleanup and rerun workspace verification plus independent closure audit evidence for plan 124.

### 2026-04-22

**Completed Plan 123: Flux Runtime Split And Boundary Hardening**

- Created `@nop-chaos/flux-action-core` package
  - `packages/flux-action-core/src/action-dispatcher.ts` - Main dispatcher with dispatch ordering
  - `packages/flux-action-core/src/action-core.ts` - Action result classification, monitor payload building
  - `packages/flux-action-core/src/operation-control.ts` - Generic async execution control (timeout, retry, abort)
  - `packages/flux-action-core/src/utils/debounce.ts` - Debounce utilities

- Implemented `ActionRuntimeAdapter` pattern
  - Interface defined in `packages/flux-core/src/types/actions.ts`
  - Implementation in `packages/flux-runtime/src/action-adapter.ts`
  - 12 methods for runtime-specific effects (setValue, ajax, dialog, navigate, etc.)

- Key technical discovery: microtask timing in Promise chains
  - Using `async/await` in adapter methods creates extra microtask boundaries
  - Adapter's `invokeNamespacedAction` must return provider's Promise directly
  - Dispatcher enriches result with `sourceScopeId`/`providerKind` after awaiting

- Configuration updates
  - Added `@nop-chaos/flux-action-core` to `vite.workspace-alias.ts`
  - Added path mapping to `tsconfig.base.json`

- Documentation updates
  - Updated `docs/architecture/flux-runtime-module-boundaries.md` with new ownership model
  - Updated `docs/plans/123-flux-runtime-split-and-boundary-hardening-plan.md` to completed status

- Verified boundaries preserved
  - `executeApiSchema(...)` convergence across ajax, submit, validation, data-source confirmed
  - `reaction-runtime.ts` uses `helpers.dispatch` port
  - `data-source-runtime.ts` uses `executeAction` port
  - No parallel action executors in reaction/data-source

- All tests pass: 377 flux-runtime, 320 flux-renderers-form-advanced
- typecheck, build, lint all pass

Next steps: None for this plan. Plan 123 is closed. Future request extraction can start fresh without reopening action-core baseline.
