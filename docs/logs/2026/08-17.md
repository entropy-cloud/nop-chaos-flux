# Development Log - 2026-08-17

### 2026-08-17

- **Plan 457 执行完成（Sundial 复刻页交互补齐，4 Phase 全绿）**：
  - **Phase 1 flex/container onClick 渲染器增强**：`FlexSchema`/`ContainerSchema` 增加 `onClick?: ActionSchema | ActionSchema[]`（`packages/flux-renderers-basic/src/schemas.ts`）；flex/container renderer 渲染 div 挂 `onClick` handler（走 `props.events.onClick`，与 button 同模式，eventCtx 带 scope）；`basic-renderer-definitions.ts` 增加 eventContracts.onClick + `{ key: 'onClick', kind: 'event' }`；可点击时补 `role="button"`/`tabIndex={0}`/`onKeyDown`（Enter/Space 激活，jsx-a11y click-events-have-key-events + no-static-element-interactions 双规则要求；动态 role 三元表达式无法通过静态分析，拆为"可点击/不可点击"双分支渲染）。测试 `__tests__/flex-container-click.test.tsx` 5 例（点击触发 action、未声明时行为不变、键盘激活 + button 语义，先红后绿）。文档：quick-reference.md "Layout Interaction — flex/container onClick" 节、styling-system.md "Interaction on Layout Renderers" 节
  - **Phase 2 按钮/图标类补接线**（C4/C5/C6/C9/C13/C15/C16/C2）：detail 清除日期/清除时间/移到列表/移到垃圾箱/状态行 X（icon-only button `sundial-detail-close`）/子任务 chevron+trash（`sundial-subtask-open-N`/`sundial-subtask-delete-N`）、settings 返回行（`sundial-settings-back`）/保存按钮、workbench sidebar 设置图标（icon-only button `sundial-settings-icon`）→ setValue/showToast
  - **Phase 3 行级交互接线 + 动态选中态**：初始值注入 `page-data.ts`（workbench: `activeSection:'workbench'`/`activeView:'all'`；新增 settings 条目: `mode:'local'`/`activeSection:'sync'`；detail: `recur:'weekly'`/`list:'work'`/`flagged:true`）；C1 主导航 3 行 + 视图 5 行（`setValue` + toast + `visible` 表达式双渲染选中态，双变体带 `data-selected` 属性）；C7 模式卡 3 张双渲染 + 连接信息区 `visible:"${mode !== 'local'}"` 联动（补 testid `sundial-connection-info`）；C8 rail 5 行；C3 详情字段行 4 行（日期行拆"选择区 `sundial-detail-row-date-pick` 开 dialog + 清除按钮独立 flex"防冒泡双触发，`sundial-detail-row-date` 保留外层 wrapper；重复/列表行开对应 dialog；旗标行 `setValue(${!flagged})` 切换 + toast，尾部"已标记/未标记"双渲染）；C11 重复选项 4 行 / C12 列表选项 3 行双渲染 + toast（移除 baked circle-check）；C14 任务行 7 行 toast；C10 todo-dialog 字段行 3 行 toast 占位反馈。JSON 改动用结构化 Node 脚本（解析-修改-写回），每文件 `JSON.parse` 校验
  - **Phase 4 收口验证**：`pnpm typecheck`/`build`/`lint` 37/37 全绿；`pnpm test` 66 task 全绿（basic 494/494、playground 164/164 其中 Sundial 22/22 含 11 个新交互断言）；`pnpm check` 仅既有 2 项登记红（wizard-renderer.tsx 716 行 oversized、industrial event-dispatch-ctx 6 处），无新增命中
  - 测试断言要点：清除按钮无冒泡双触发（点击清除 → toast 且日期 dialog 不打开）；模式卡 Supabase → 连接信息区可见、本地 → 隐藏；主导航/视图/rail/模式卡/选项行 `data-selected` 切换；旗标切换 → /未标记/
  - 文档：plan 457 各 Phase 标 completed（含 Phase 3 执行偏差：C10 无二级 dialog 可开按 Non-Goals 仅 toast、PAGE_DATA 补 `flagged:true` 保持既有 /已标记/ 断言）
  - Next step：独立子 agent closure audit（fresh session）→ plan 457 标 completed
