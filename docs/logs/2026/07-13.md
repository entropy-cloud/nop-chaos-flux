# Development Log - 2026-07-13

### 2026-07-13

- **Icon 系统 Ant Design 兼容层 + IconPicker 控件**：完成 icon 组件的 Ant Design 图标名称映射（~205 条）和 icon-picker 表单高级字段 renderer 实现。
  - **Phase 1 — Ant Design 映射表**：`packages/ui/src/lib/icon-utils.ts` 新增 `ANT_DESIGN_LUCIDE_MAP`（205 条 Ant Design → Lucide kebab-case 映射）、`ANT_DESIGN_VARIANT_SUFFIX` 正则、`resolveAntdIconName()` 内部函数；`normalizeIconName()` 更新支持 `ant-design:` 前缀检测和 `-outlined`/`-filled`/`-twotone` 后缀剥离；新增 9 个 antd 映射测试用例。
  - **Phase 2-3 — IconPicker Renderer**：`packages/flux-renderers-form-advanced/src/icon-picker.tsx` 实现 `IconPickerSchema` + `IconPickerRenderer` + `iconPickerRendererDefinition`；Popover 弹层 + 搜索框 + 6 列 CSS Grid 图标网格 + 分批加载（200 递增）；值写入通过 `useCurrentFormState` + `useScopeSelector`；注册到 `formAdvancedRendererDefinitions`。
  - **Phase 4 — 测试**：`icon-picker.test.tsx` 7 个用例（触发器渲染、Popover 打开、hidden input、visible=false、marker class、clear button 逻辑）。
  - **Phase 5 — 全量验证**：typecheck ✓ (55/55)、build ✓ (29/29)、lint ✓ (29/29)、test ✓ (all packages green)。
  - **Playground**：新增 `icon-picker-lab-page.tsx`（3 个场景）+ 路由注册。
  - **设计文档**：`docs/components/icon/design.md` 更新（§11 图标名称解析流程 + Ant Design 映射表）；`docs/components/icon-picker/design.md` 新建；`docs/components/index.md` 更新。
  - **执行计划**：`docs/plans/2026-07-13-icon-system-antd-mapping-and-icon-picker-plan.md` 完成并关闭（independent review pass + closure audit pass）。
  - **全量验证**：typecheck ✓、build ✓、lint ✓、test ✓，full-green verification。
