# Development Log - 2026-08-08

### 2026-08-08（DR 跨面集中修复与裁决执行：16 条 P2 路由逐条终态 + Tiptap editor 面竞态修复（bug 117）+ P3 残留复核收口）

- **Plan `2026-08-08-2034-1-round2-dr-cross-surface-centralized-remediation.md` 执行完毕（4 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 i18n 族集中修复（DR-1..DR-14 全 fixed）**：flow-designer（DR-1 命令适配器错误消息 16 处 `flux.flowDesigner.command.*` + DR-2 默认节点标签 16 条 `flux.flowDesigner.nodeType.*`，host label 优先不受影响）；spreadsheet（DR-3 编辑保存消息 + 同族 addLog 前缀、DR-5 页头状态行、DR-6 查找结果 6 处，`flux.spreadsheet.*`）；report-designer（DR-7 预览 2 处 + 按钮标签 + 兜底、DR-8 readonly/save/fields badge、DR-9 undo/redo 消息 + 标签、DR-10 codec 2 处 + Untitled 默认名、DR-11 字段拖放 4 处，`flux.reportDesigner.*`；report-designer-core 新增 flux-i18n 依赖）；word-editor（DR-12 工具栏/对话框 ~40 处、DR-13 数据集族 + Copy field reference 裁决=改 label、DR-14 模板标签 15 条 labelKey/descriptionKey 化，`flux.wordEditor.*` ~60 新 key 双 locale）；`check:i18n-keys` exit 0；e2e 选择器同步为 data-testid 稳定定位（word-editor 4 spec + report-designer-host + exploratory 2 spec）；locale 双断言测试在案（flow-designer/spreadsheet/report-designer-core）。
  - **Phase 2 契约裁决（DR-4/DR-15/DR-16）**：DR-4 = **ADJUDICATED-REMOVED**（use-spreadsheet-shell no-op setCellValue/setCommentText 死管线移除，消费点 5 文件同步清理，`SpreadsheetInteractionsReturn` 移除 setCommentText；comment UI 不可达 = out-of-scope improvement）；DR-15 = **ADJUDICATED-KEEP**（initialCharts/initialCodes `@reserved` 标注 + design.md 注记，移除留人工确认门）；DR-16 = **ADJUDICATED-DOC-ALIGNED**（design.md 措辞对齐 live throw 语义，read/write helper 区分，返回语义变更留人工确认门）。
  - **Phase 3 Tiptap editor 面竞态修复（w3d-editor:28 判真 + c3-5 ×2 同族）**：**根因 = 字段 frame `<label>` 隐式 control = 首个 labelable 后代（bold 工具栏按钮）**——点击内容区触发 label activation（focusin + 合成 click detail:0）→ toggleBold stray mark + 焦点跳转丢 keystroke（"hel"/"ditor" 变体）；修复 = editor renderer 定义 `frameRootTag: 'div'`（combo/picker/array-field 同款既有模式）；回归测试 `editor-renderer.test.tsx` + w3d-editor/c3-5 **30/30 复跑绿（3 轮 repeat）**；**bug note 117** 行内补写 + README 索引同步；watch-only 清单 w3d-editor/c3-5 ×2 移除（gantt-perf/kanban-perf 50Hz 维持）。
  - **Phase 4 P3 残留复核 + 收口**：裁决表 keep 99 / dismissed 24 抽查 ≥10 条 live 核对理由成立；**DR 表 §2 卡内 P3 以正文表 30 行为准**（fd ×9 + ss ×21；§2 摘要「18 条」过时表头已更正）；**跨面共性候选逐条裁决 = keep（P3 语义维持）**——ss-3 P3-2/ss-6 P3-2/ss-10 P3-4（no-op 命令无条件 pushUndo 污染 undo 栈 + dirty 误标，internal-state.ts:81-90 机制级 10 面共享）与 ss-3 P3-4（onLog 宿主接线）live 核对确认 + 无静默升级为 P2，**CX-13+ 插入建议登记 DR 表 §2 + daily log（人工确认门，本 plan 不执行）**；结论回写 ss-3/ss-6/ss-10 卡。
  - **验证（全绿）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59、聚合 `pnpm check` exit 0（oversized 仅 2 条既有 locale 豁免）；关联 e2e 全绿（word-editor 32 + report-designer 14 + spreadsheet-demo + flow-designer + w3d 16 + c3-5 等）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` DR 行 `todo`→`done`（附执行证据）；DR 表 17 条全部标注终态（fixed/adjudicated + 理由）零悬挂；bug note 117 + README 索引一致；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（CRUD 批量操作选中键 `${ids}` 绑定（AMIS 兼容）+ status-owner 额外顶层绑定能力）

用户反馈 nop-entropy grid_crud 场景：CRUD checkbox 选中后，批量操作按钮的 `@mutation:X__batchDelete?ids=${ids}` 中 `${ids}` 在按钮 action scope 不存在 → 求值为空串 → 后端收到 `ids:['']`。

- **`flux-runtime` `createReadonlyScopeBinding` 新增第 4 参 `getExtraBindings`**（`status-owner.ts`）：额外顶层绑定叠加到投影 scope——`get`/`has` 先查 extra 再回落父 scope；`readOwn`/`readVisible`/`materializeVisible` 均叠加；投影 store 版本化改为 summary+extra 合并版本（`getCombinedVersion`），`readVisible`/`materializeVisible` 缓存判定加 extra 版本比对（浅等复用缓存、变更出新 overlay）。
- **`flux-renderers-data` CRUD `selectionField`（默认 `'ids'`）**：`crud-schema.ts` 新增 `selectionField?: string`（normalize 默认 `'ids'`，与 `pageField`/`pageSizeField` 同一参数名映射模式）；`crud-renderer.tsx` 把选中行键（`selectedRowKeys`）以 `[selectionField ?? 'ids']` 发布到 `$crud` 绑定 scope。
- **回归测试**：`status-owner.test.ts` +4 用例（get/has、readOwn/readVisible/materializeVisible 叠加、浅等复用、变更出新）；新增 `crud-selection-ids-binding.test.tsx` 2 用例（默认 `ids=1,2` 与自定义 `selectedPositionIds=1`，断言 fetcher 收到完整 URL）。
- **flux-guide `design-patterns/crud.md`** 批量操作参数映射说明（AMIS 兼容，`selectionField` 自定义名示例）。
- **验证（全绿）**：`pnpm typecheck`/`build`/`lint` 32/32；`pnpm test` 全量绿（flux-runtime 1406 / flux-renderers-data 770 含新增 6 用例）；`crud-demo` 关联 e2e 6/6；聚合 `pnpm check` exit 0。

### 2026-08-08（D3.4 word-editor 大面审计执行：7 面审计卡全 closed + 5 条 P1 修复（test-first）+ 4 条低成本 P2 修复 + 5 条 P2 路由（DR-12..16）+ 3 条 bug note（114–116）+ 新增恢复/选区 e2e spec）

- **Plan `2026-08-08-1315-3-round2-d34-word-editor-surface-audit.md` 执行完毕（5 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 审计准备**：7 面（we-1..we-7）面清单 live 核对（全部核心文件在案：word-editor-page/editor-canvas/toolbar 族/editor-store/dataset-store/document-io/template-expr/template-tags/preview）；遗留输入终态复核——**MA43-P1-08（5 normalize 函数）收敛**（`document-io-normalize.test.ts` 直接测试）、**MA43-P1-09/10（resolveWordEditorManifest/wordEditorHostContract）收敛**（`word-editor-manifest.test.ts` 两 describe 块，纯复核非 re-fix）、**MA5 P3-01（整包零测试）过期作废**（live 19 测试文件）、MA5 P2-02 probe stale ref 收敛（savedDocumentRef 惰性读取在案）、MA4 包簇 8 P2 行（5 个 field-reference 函数）确认仍零测试（H7 补测项）、MA5 P3-07/10/11 live 确认在案（升级为面发现）；e2e 覆盖矩阵（4 spec → we-1/2/4/5/6 覆盖，**真缺口 we-3 选区回显 + we-7 导入显式场景**）；7 张卡骨架建立。
  - **Phase 2/3 逐面审计（7 卡）**：§6.1 18 维 + §6.2 H1-H7 逐维核对；声明即契约双向核对（manifest 6 方法 ↔ action-provider 6 case ↔ `WORD_EDITOR_HOST_METHOD_CONTRACTS` 三向一致；**ghost 声明 P2：initialCharts/initialCodes schema 字段零消费者**（测试锁定不流入 document）；EditorCanvas charts/codes 死参数）；发现分级——**P0 零、P1 5 条**（we-2 重复 Redo 按钮 / we-4 数据集选中路径缺失 + 死菜单按钮 + 空 label 列恢复丢弃 / we-7 loadDocument null 根崩溃）、P2 9 条（4 条当场修复 + 5 条路由 DR）、P3 各卡记录；遗留输入全部终态 Decision。
  - **Phase 4 修复 + bug note**：**5 条 P1 全 test-first**——① **loadDocument 合法 JSON null/标量/数组根崩溃**（`JSON.parse("null")` → `parsed.data` TypeError 逸出 try → render 期崩溃；修复 = normalize 前根类型守卫 + json-parse-failed 上报；`document-io-persist.test.ts` 3 用例先红后绿；**bug note 114**）；② **数据集选中路径缺失 → Fields tab 恒空**（`datasetStore.select` renderers 零调用；修复 = 行点击 select + 编辑对话框保留；`dataset-panel-menu.test.tsx` 先红后绿；**bug note 116**）；③ **MoreVertical 死按钮**（MA5 P3-07 确认；修复 = DropdownMenu Edit/Delete + 确认 Dialog + `onDeleteDataset`（store.remove + setDirty）+ i18n key 双 locale；**bug note 116**）；④ **空 label 列数据集持久化后整集恢复丢弃**（dialog 保存门 ≠ validateDataset label required → 静默数据丢失；修复 = canSave 派生门；`dataset-dialog.test.tsx` 先红后绿；**bug note 115**）；⑤ **Redo 按钮重复渲染**（font-controls.tsx 双份；`font-controls.test.tsx` 唯一性断言先红后绿）；**低成本 P2 当场修复 4 条**：Ctrl+F preventDefault 空动作移除（`use-word-editor-shortcuts.test.tsx` 5 用例：守卫/映射/Ctrl+F 不吞默认）、EditorCanvas charts/codes 死参数移除、5 个 field-reference 函数补测（template-expr.test.ts +13 用例，MA4 包簇 8 残留闭合）；**bug note 114–116** 行内补写（README 索引同步）；**P2 路由 DR-12..DR-16**（we-2 工具栏 i18n 族 / we-4 数据集 i18n 族 / we-6 模板标签 i18n / we-1 ghost schema 声明 / we-5 persist 契约措辞）登记 `round2-dr-adjudication.md`（16 条总计零悬挂）。
  - **Phase 5 宿主场景 + 收口**：**新增 `tests/e2e/word-editor-recovery.spec.ts` 6 用例全绿**——① seed 合法文档恢复渲染（we-7 导入 happy path）② **seed `"null"` 打开不崩溃 + 空文档 fallback（P1-114 e2e 确认）** ③ seed 语法错误 JSON fail-closed ④ **selection 回显：toolbar-bold aria-pressed 真机断言（we-3 缺口闭合）** ⑤ **选中数据集 → Fields tab 展示字段（P1-116 e2e 确认）** ⑥ **行菜单删除确认（P1-116 e2e 确认）**；word-editor 关联 e2e **32 用例全绿**（既有 26 + 新增 6）；收口登记：roadmap D3.4 行 `todo`→`done`（附执行证据）；surface-inventory D3.4 增量登记（覆盖矩阵 + 遗留输入终态 + 收口增量）；README §2/§3 word-editor 行补新 spec；7 卡全 closed（P2/P3 零悬挂声明）。
  - **验证（全绿）**：`pnpm typecheck`/`build`/`lint` 全量绿；`pnpm test` 全量绿（word-editor-core 265 / word-editor-renderers 147，含 24 条新增回归/补测）；e2e word-editor 关联 32 用例全绿；聚合 `pnpm check` exit 0（详见 Closure Gates）。
  - **门禁盲区发现（供 DG 承接）**：`check:audit-event-dispatch-ctx` 不覆盖 word-editor-renderers（正则仅 10 个 flux-renderers-\* 包）——本 plan 人工核零 schema 事件派发点（D3.1/D3.2/D3.3 同款登记维持）；manifest metadata docsPath 双文档并存（`docs/components/word-editor-page/design.md` vs owner doc `docs/architecture/word-editor/design.md`，we-1 P3-4）文档治理项登记待 DG。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D3.4 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session `ses_01eb53775ffeR6VtvDLmmczaB2`，verdict `approved`，零 Blocker/Major，4 Minor 全部收口：we-1 P2-3/we-3 P2-1 显式收敛注记、we-6 补测计数更正 13→14、DR 表零悬挂枚举补全）。
  - **full-green 验证记录（2026-08-08）**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 任务全绿（word-editor-core 265 / word-editor-renderers 147）、word-editor 关联 e2e 32/32（既有 26 + 新增 recovery 6）、`pnpm check` exit 0。
  - **watch-only 归因（Minor-4，非本 plan 引入）**：首次批量跑既有 4 spec 时 `word-editor-persistence.spec.ts:22` 一次超时 flake（并行负载下 ~50s vs 60s 上限）；单跑 + 全量重跑 26/26 绿——环境瞬时 flake，维持 watch 归因记录（如复发按 D2 纪律复查）。

### 2026-08-08（D3.3 report-designer 大面审计执行：7 面审计卡全 closed + 3 条 P1 修复（test-first）+ 1 条低成本 P2 修复 + 5 条 P2 路由（DR-7..11）+ 3 条 bug note（111–113）+ 独立宿主页与 spec）

- **Plan `2026-08-08-1315-2-round2-d33-report-designer-surface-audit.md` 执行完毕（5 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 审计准备**：7 面（rd-1..rd-7）面清单 live 核对（全部核心文件在案）；遗留输入终态复核——**MA4.3 九条缺口（MA43-P0-01..05 + P1-02..05）全部收敛**（`__tests__/commands.test.ts`、`adapters-and-helpers.test.ts`、`report-designer-manifest-and-helpers.test.ts`、`host-action-provider.test.ts` 直接测试逐条在案，纯复核非 re-fix）；**MA5 P3-09 `bridge.ts:75-86` `as never` 复核 = 仍存在** → H1 面登记裁决（P2 低成本当场修复）；MA5 P2-03 inspector auto-open race 收敛（actionScope 已在 deps）；e2e 覆盖矩阵（demo 9 用例 → rd-1/2/3 间接，真缺口 rd-4/5/6/7）；7 张卡骨架建立。
  - **Phase 2/3 逐面审计（7 卡）**：§6.1 18 维 + §6.2 H1-H7 逐维核对；声明即契约双向核对（manifest 12 方法 ↔ action provider 12 ↔ core-dispatch 12 case ↔ REPORT_DESIGNER_HOST_METHODS 12 条四向一致）；发现分级——**P0 零、P1 2 条（审计阶段）**、P2 6 条（1 条当场修复 + 5 条路由 DR）、P3 各卡记录；遗留输入全部终态 Decision。
  - **Phase 4 修复 + bug note**：**P1-1 undo/redo/importTemplate 不回传 spreadsheet canvas**（page-renderer.tsx stale `spreadsheetSyncSource === lastSynced` guard 阻断 report 侧文档替换传播——canvas 不随 undo 回退；根因两层：syncSource 单向标记不清除 + replaceDocument 内部 clone 引用无法被 lastSynced 识别）——test-first：`page-renderer-shell.test.tsx` undo/redo 传播用例先红后绿 + `designer-core-codec-and-selection.test.ts` syncSource 清除语义（core-dispatch 清 syncSource + page-renderer 以 applied clone 更新 lastSynced）；**低成本 P2 修复**：MA5 P3-09 `as never` → `buildAggregatedRuntimeSummary` 窄接口 `SpreadsheetRuntimeSummaryInput`（类型安全，行为由既有 bridge.test 锁定）；**bug note 111** 行内补写；**P2 路由 DR-7..DR-11**（rd-4 预览 / rd-5 保存 / rd-6 undo / rd-7 模板 / rd-2 拖放 i18n 族）登记 `round2-dr-adjudication.md`（11 条总计零悬挂）。
  - **Phase 5 宿主场景 + 收口（当场暴露 2 条新 P1）**：新建 `apps/playground/src/pages/report-designer-host-demo.tsx` + `report-designer-host-page.tsx`（route `#/report-designer-host`，真实 `report-designer-page` renderer 宿主：toolbar 派发 + 画布编辑 + 字段面板 + inspector shell + 空模板 toggle + window hook；domain-route-entries + App.tsx + playground-entry-pages ROUTE_ASSERTIONS 同步）；**新增 `tests/e2e/report-designer-host.spec.ts` 5 用例全绿**——① 宿主面渲染（含初始 Undo disabled）② **undo/redo 往返画布回退（P1-111 e2e 确认）** ③ save 清除 dirty（dirty probe）④ preview adapter 运行 + Stop 可见性 ⑤ 空模板 fallback 不崩溃；**当场暴露 P1-2 React StrictMode core dispose**（playground main.tsx StrictMode 双挂载 → effect cleanup dispose → 重挂载 initialize no-op → 字段源 0；test-first `page-renderer-strict-mode.test.tsx` 红→绿；ref-diff 托管修复；bug note 112）+ **P1-3 toolbar 取反模板死代码**（`evalBooleanLike` `!` 分支 NUL 标记递归经 trim 后不满足 `${}` 契约恒 undefined → Undo/Redo disabled 永不生效；test-first `report-designer-toolbar-helpers.test.ts` negation 用例红→绿；直读 readStatePath 取反修复；bug note 113）；关联 e2e **78 用例全绿**（host 5 + demo 9 + entry-pages 64）；收口登记：roadmap D3.3 行 `todo`→`done`（附执行证据）；surface-inventory D3.3 增量登记（覆盖矩阵 + 遗留输入终态）；7 卡全 closed（P2/P3 零悬挂声明）。
  - **验证（全绿）**：`pnpm typecheck` 全量绿（含 playground 新宿主页）；`pnpm build` 全量绿；`pnpm lint` 全量绿；`pnpm test` 全量绿（report-designer-core 185 / report-designer-renderers 202，含 4 条新增回归）；e2e 关联 78 用例全绿；聚合 `pnpm check` exit 0（详见 Closure Gates）。
  - **门禁盲区发现（供 DG 承接）**：`check:audit-event-dispatch-ctx` 不覆盖 report-designer-renderers（正则仅 10 个 flux-renderers-\* 包）——本 plan 人工核零 schema 事件派发点（D3.1/D3.2 同款登记维持）；宿主页场景断言依赖 data-slot 稳定性（无独立门禁）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D3.3 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（D3.2 spreadsheet 大面审计执行：10 面审计卡 + 4 条 P1 修复（test-first）+ 4 条 P2 路由 + 4 条 bug note + 独立宿主页与 spec（coverage gap 闭合）+ spreadsheet owner doc 新建）

- **Plan `2026-08-08-1315-1-round2-d32-spreadsheet-surface-audit.md` 执行完毕（5 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 审计准备**：10 面（ss-1..ss-10）面清单 live 核对（全部核心文件在案）；遗留输入终态复核——MA43-P1-06/07 收敛（`spreadsheet-manifest.test.ts` 两 describe 块在案，纯复核非 re-fix）、默认工厂 4 函数零测试确认（H7 补测项）、MA5 P3-03 同义反复确认（重写项）、P3-06 no-op 确认（use-spreadsheet-shell.ts:25-35）、P3-12 span 计数确认（constants.ts:105-123）；**宿主页方案 Decision = 新建独立 playground 宿主页 + route**（README §2 登记）；**owner doc gap 裁决 = 新建 `docs/architecture/spreadsheet/design.md`**（审计发现契约漂移 + 契约面无文档归属，README §1 登记，Phase 5 落地）；e2e 覆盖矩阵（既有 9+1 用例 → ss-1/ss-3/ss-4/ss-7 间接，真缺口 = 独立宿主页与 spec）；10 张卡骨架建立。
  - **Phase 2/3 逐面审计（10 卡）**：§6.1 18 维 + §6.2 H1-H7 逐维核对；声明即契约双向核对（manifest 66 方法 ↔ action-provider ↔ handler）；发现分级——**P0 零、P1 4 条**（ss-2 setCellNumberFormat no-op / ss-6 冻结窗格渲染不固定 / ss-7 getSelectedAxisInfo span 计数（P3-12 升级）/ ss-9 findResultShape 契约漂移）、P2 6 条（2 条低成本当场修复 + 4 条路由 DR）、P3 各卡记录；遗留输入全部终态 Decision。
  - **Phase 4 修复 + bug note**：**4 条 P1 全 test-first**——① `setCellNumberFormat` 空 patch no-op（cell-handlers.ts:144-147）→ 新增 `applySetCellNumberFormat`（写 CellDocument.numberFormat）+ 接线（`__tests__/number-format.test.ts` 先红后绿）；② **冻结窗格渲染不固定**（viewport 计算正确但 CSS 无 sticky 支撑，冻结行/列滚动时随内容滚走——单滚动容器 + 单 table 结构仅表头 sticky）→ table-shell 冻结 tr sticky top（22 + rowOffsets）+ 冻结列 cell sticky left（40 + colOffsets）+ GRID_HEADER_HEIGHT 常量（`__tests__/freeze-pinning.test.tsx` 先红后绿，e2e ss-6 真实浏览器确认）；③ `getSelectedAxisInfo` count 返回 span → 实际选中数（多选删除未选中行数据丢失风险）→ count = values.length + **非连续多选删除 per-index + begin/commit 事务包裹**（单 undo 条目；`axis-info.test.ts` + context-menu-structure 回归先红后绿，e2e ss-7 确认）；④ findResultShape 声明 `{cell,value}` vs 实现 `FindResult` 平面形状 → shape 对齐 + manifest 契约测试；**低成本 P2 当场修复**：setViewport 补 READ_ONLY_COMMANDS 白名单（readonly 滚动持久化）、Escape 关面板提升至 isEditableTarget 守卫前 + 焦点归还 grid 根；**H7 补测**：默认工厂 4 函数（types.test.ts）+ MA5 P3-03 同义反复测试重写为行为断言（use-spreadsheet-interactions.test.tsx）；**bug note 107–110** 行内补写（README 索引同步）；**P2 路由 DR-3..DR-6**（编辑保存消息 i18n / no-op 死管线 + comment UI 不可达 / 页头状态 i18n / 查找结果 i18n）登记 `round2-dr-adjudication.md`（卡内 P2 零悬挂）。
  - **Phase 5 宿主场景 + 收口**：新建 `apps/playground/src/pages/spreadsheet-demo.tsx` + `spreadsheet-page.tsx`（route `#/spreadsheet`，domain-route-entries + App.tsx + home-page + route-matrix/app.test 同步）；**新增 `tests/e2e/spreadsheet-demo.spec.ts` 10 用例**（10 面每面 ≥1，programmatic DOM 断言，zh-CN 文案）**全绿 10/10**——ss-1 虚拟窗口 / ss-2 编辑提交取消 / ss-3 加粗类 / ss-4 地址+冻结徽章 / ss-5 公式写入（window hook 导出断言）/ **ss-6 冻结滚动固定（P1-107 e2e 确认）** / **ss-7 非连续多选删除（P1-108 e2e 确认）** / ss-8 键盘全链路 / ss-9 查找替换 + Escape / ss-10 undo 往返；`playground-entry-pages.spec.ts` 补 spreadsheet 路由断言（63 用例全绿）；新建 `docs/architecture/spreadsheet/design.md`（最终设计状态：宿主契约/命令面/事务 undo/选择视图冻结/渲染样式/设计限制/登记汇总）；roadmap D3.2 行 `todo`→`done`（附执行证据）；surface-inventory/README 增量登记（新 spec 行 + owner doc 契约基准更新）；卡状态 10 张卡全部 closed（P2/P3 零悬挂声明）。
  - **验证（全绿）**：`pnpm typecheck`/`build`/`lint` 全量绿；`pnpm test` 全量绿（59 包，新增 spreadsheet-core 272 tests + spreadsheet-renderers 152 tests 含 14 条新增回归）；e2e 全量 **1023 passed / 43 skipped / 17 failed**——17 failed 全部归因**既有环境/watch-only**（c6-5 diff-host ×5 + smoke/lab-batch diff-view ×2 + c3-5 media-rich ×2（Tiptap 面）+ gantt ×3 + gantt-perf/kanban-perf ×2（50Hz 阈值）+ 其它 ×3；**clean-tree stash 复跑同款失败确认非本 plan 引入**，非静默吞掉）；聚合 `pnpm check` exit 0。
  - **门禁盲区发现（供 DG 承接）**：`check:audit-event-dispatch-ctx` 不覆盖 spreadsheet-renderers（正则仅 10 个 flux-renderers-\* 包）——本 plan 人工核零命中（D3.1 同款登记维持）；spreadsheet 面级场景断言依赖宿主页 data-slot 稳定性（无独立门禁）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D3.2 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。
  - **closure 复核更正（2026-08-09，DV 全量验证轮）**：live 核对发现原收口记录的 closure-audit 证据不存在——plan 仍 `Plan Status: active` + Closure Audit Evidence 空 + Closure Gates 审计门禁项已无证据勾选 `[x]`，roadmap/daily log 中「closure-audit pass（fresh session，2026-08-08）」为过早断言、「卡状态 6 closed + 4 fixed-pending-closure」为过时计数（live 10 卡全部 `closed`，与 plan Status Note 一致）；D3.2 closure 由独立 fresh session 于 2026-08-09 正式审计收口——轮 1 verdict `fail`（1 Major：closure 流程完整性——gate 无证据勾选 + status 未翻转 + 过早断言；3 Minor：卡状态计数过时 / ss-3/4/5/6/10 卡场景行「待 Phase 5」过时 / 10 卡 Closure 占位未填），全部修复后轮 2 verdict `pass`（零 Blocker/Major）——证据见 plan Closure 节。

### 2026-08-08（DL Lessons 沉淀 06–08 执行完毕：3 Phase 全 completed + README 索引收口 + roadmap DL 行 done）

- **Plan `2026-08-08-0900-3-round2-dl-lessons-distillation.md` 执行完毕（3 Phase 全 completed；纯文档计划，零产品代码变更；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 Lesson 06 审计必漏定律**：新建 `docs/lessons/06-audit-closure-only-guarantees-no-known-issues.md`——closure 只保证「本轮无已知问题」；证据链（08-06 收口 CV full-green 10,397 单测 + 1054 e2e → 08-06/08-07 两轮 post-closure audit 仍 85 条 P2 且多为真缺陷（pointercancel 缺失、polling 竞态、重试跳页、NaN fail-closed、gantt 键盘编辑派发错通道、事件 ctx 别名盲区），逐条 plan 修复在案）；制度结论（大规模审计收口后必须安排 1–2 轮 post-closure audit，审计轮间隔不宜长）。
  - **Phase 2 Lesson 07 工具门禁沉淀法**：新建 `docs/lessons/07-tool-gate-sedimentation-with-committed-regression-tests.md`——每修一类模式落一个 `check:` + committed 回归测试（`scripts/__tests__/`），基线零命中才是真收口；门禁自身也会回归（browser-io 6d2497ea 漏扫 10 包）；证据 live 核对（0150-1 修复记录 + `find-renderer-browser-io.test.ts:60-64` 正例夹具 + `pnpm test:scripts` 6 files/15 tests 在案）。
  - **Phase 3 Lesson 08 声明即契约 + README 收口**：新建 `docs/lessons/08-ghost-contract-declaration-is-contract-detection.md`——注册项 defaultSchema/fields、事件派发点、`component:*` 句柄、schema 字段与 design.md 双向核对；「声明即死」是第一轮最高产缺陷类别（stopWhen/finishAction/importsReady/22-12 等，证据含 D2 @reserved 7 处核对先例 `docs/audits/round2-p3-adjudication.md` §5）；README 索引 06–08 三行登记（链接 live 核对有效）；roadmap DL 行 `todo` → `done`（附执行证据引用）。
  - **验证（全绿）**：纯文档变更，`pnpm test`/`lint`/`typecheck`/`build` 按纯文档计划条款从门禁表移除；聚合 `pnpm check` **exit 0**（`check:active-doc-code-anchors` / `check:docs-garbled` 覆盖 docs 变更，零新增命中）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` DL 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（TableRenderer 序号列（index column）能力）

用户反馈 nop-app-erp flux 模式 CRUD 需要 checkbox 选择列 + 序号列 + 固定列（checkbox/序号/首数据列锁定，序号列宽度固定）。

- **新增 `type:'index'` 序号列**（`flux-renderers-data`）：
  - `table-body-row-rendering.tsx` DataRowView 特判 `column.type==='index'` 渲染序号单元格（`data-slot="table-index-cell"`），显示跨页累计行号 `viewIndex + indexColumnOffset + 1`，对齐 AMIS `__index` 的 offset 语义。
  - `table-renderer.tsx` 计算 `indexColumnOffset=(currentPage-1)*pageSize`；经 `TableBodyRows`（NonVirtual/Virtual）→ `renderDataRow` → `MemoizedDataRow`（comparator 含 indexColumnOffset）透传。
  - 固定列布局复用现有 `createFixedColumnLayout`（`fixed:'left'` 列参与 sticky 偏移累计），序号列配 `width`+`fixed:'left'` 即固定。
  - i18n：`flux.table.index`（zh-CN 序号 / en-US Index）。
  - 新增回归测试 `__tests__/table-index-column.test.tsx`（无分页 1..N；分页从 (page-1)\*size+1 起）；包级 110 files/768 tests 全绿 + typecheck ✅。
  - flux-guide `design-patterns/table.md` 列类型表补 `index` 行。
- **下游**：nop-chaos-next `repack-flux-and-refresh.sh` 重打 tarball + host 重建，产物含 `table-index-cell`/`indexColumnOffset`/序号 i18n（字符串验证）。

### 2026-08-08（DB Bug 沉淀回补执行：15 条历史缺口补写 92–106 + README 索引漂移修复 + 盘点表零悬挂）

- **Plan `2026-08-08-0900-2-round2-db-bug-note-backfill.md` 执行完毕（3 Phase 全 completed；纯文档回补，零产品代码变更；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 盘点表建立（live 核对）**：新建 `docs/audits/round2-bug-note-gaps.md`——15 候选全部 live 核对为「已修复 + 回归测试在案」→ **零悬挂（补写 15 / 不写 0 / 未修复路由 0）**。每条落：缺陷描述 + 修复 plan 出处 + 门槛命中理由 + note 去向。**编号裁决**：roadmap 约定「90 起」，但 live 核对 **90/91 已被 D3.1（`2026-08-08-0900-1`，commit 68fcc326）按 Cross-Cutting bug note 纪律行内补写占用** → 本 plan 从 **92 起**顺序递增（编号唯一，不沿用重号先例），裁决记录于盘点表。README 漂移盘点：`comm -23 <(ls) <(README 条目)` 实测缺登 **34 文件 / 31 编号**（62–89 全段 30 文件 + 15-component-level/48/54/55 共 4 条），与 plan Baseline 口径逐位一致。
  - **Phase 2 逐条补写（92–106 共 15 note）**：按 guide 模板 7 节（Problem / Diagnostic Method / Root Cause / Fix / Tests / Affected Files / Notes For Future Refactors）逐条补写——92 CX-6 原生值选中态 adapter（choice-native-value-echo.test.tsx）、93 CX-7 日历 locale（date-calendar-locale.test.tsx）、94 CX-8 复合字段 readOnly 传播（c3-2-readonly-propagation.test.tsx）、95 2-8 CRUD 自定义 path 双 fetch（crud-loadaction-reaction-regression.test.tsx:234）、96 2-9 无限滚动重试跳页（crud-loadaction-infinite.test.tsx:256 + crud-infinite-scroll.test.tsx:194）、97 2-10 polling 挂载序竞态（crud-lifecycle.test.tsx:337）、98 2-11 pagination 渲染期 clamp（data-pagination-rendering.test.tsx:294）、99 2-12 surface scope dispose + GC 测试假保障（surface-teardown-gc.test.ts）、100 2-13 barcode 连续同值丢弃（barcode-scanner-overlay.test.tsx:230 + barcode-queue.test.ts:24,66）、101 2-14 四 drag hook pointercancel（use-gantt-drag/use-gantt-link-draw/use-calendar-drag.test）、102 2-16 onErrorError 抑制标记（nested-onerror-suppression.test.ts）、103 2-19 gantt 键盘编辑通道（gantt-keyboard-edit.test.tsx）、104 15-2 NaN fail-closed（core-ui-state.test.ts:455 + designer-action-provider.test.ts:57）、105 22-07 onTaskEdit 契约（gantt-keyboard-edit/gantt-editor/gantt.test）、106 22-12 kanban 死句柄（kanban-handle.test.tsx）；每条 Tests 节引述既有回归测试（live 核对存在性），零新增测试。
  - **Phase 3 README 索引修复 + 收口**：`docs/bugs/README.md`「Current Entries」全量补登——缺登 34 文件 / 31 编号（62–89 全段 30 文件 + 15-component-level/48/54/55 共 4 条，既有行不改）+ 本次 92–106 共 15 条；**零缺口终验：`diff <(ls docs/bugs/*.md) <(README Current Entries 条目)` 实测 ZERO GAP（113 = 113，含指南 00）**。盘点表去向全覆盖复核（15 = 92–106 一一对应）。
  - **验证（全绿）**：纯文档变更，`pnpm test`/`lint`/`typecheck`/`build` 按纯文档计划条款从门禁表移除；聚合 `pnpm check` **exit 0**（`check:active-doc-code-anchors` / `check:docs-garbled` 覆盖 docs 变更，零新增命中）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` DB 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（CRUD 双分页器缺陷修复：footerToolbar 区域的 pagination renderer 未隐藏表格内置分页栏）

用户反馈 nop-app-erp flux 模式标准页面（如 `ErpMdMaterialSku-main`）显示两个分页器（表格内置"每页行数/1-5 of 5" + footerToolbar 的独立 pagination renderer）。

- **根因**：`crud-renderer.tsx` 的 `hasExternalPaginationControl` 只检测 `toolbarLayout` header/footer 块（`pagination`/`switch-per-page`），但 nop-entropy `grid_crud.xpl` 生成的是 `footerToolbar` 区域（`<statistics/>`+`<pagination/>` renderer 节点，flux-guide 文档化的标准模式）。区域节点未被识别为外部分页控件 → `hideBar=false` → TableRenderer 渲染自带 `TablePaginationBar`，与区域内的 `PaginationRenderer` 并存 → 双分页器。
- **修复**（`flux-renderers-data/src/crud-renderer.tsx`）：新增 `regionHasRendererType` 辅助函数，`hasExternalPaginationControl` 增加对 `props.regions.footerToolbar`/`props.regions.toolbar` 区域内 `pagination`/`switch-per-page` 节点的检测，命中即 `hideBar=true` 隐藏表格内置分页栏。
- **验证**：新增回归测试（`__tests__/crud-loadaction-ajax.test.tsx`：footerToolbar 含 pagination → `[data-slot="table-pagination"]` 为 null、`crud-footer-toolbar`/`pagination-root` 存在）；包级全量 109 files/766 tests 全绿，typecheck ✅。
- **oversized 拆分（收口时补做）**：`crud-renderer.tsx` 706 行超 700 行门禁 → `regionHasRendererType`/`RenderRegionLike` 移入 `crud-query-region.tsx`（同包 pagination 区域辅助模块），crud-renderer.tsx 回落 683 行；`pnpm check` exit 0（oversized 恢复仅 2 条既有 locale 豁免），包级 109 files/766 tests 全绿复跑。
- **下游**：nop-chaos-next `repack-flux-and-refresh.sh` 重打 tarball + host 重构建，产物含新逻辑（已字符串验证）。

### 2026-08-08（D3.1 flow-designer 大面审计执行：13 面审计卡全 closed + 2 条 P1 修复（test-first）+ 2 条 P2 路由 + 2 条复杂 bug note + 新增 2 个 e2e spec）

- **Plan `2026-08-08-0900-1-round2-d31-flow-designer-surface-audit.md` 执行完毕（5 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 审计准备**：13 面（fd-1..fd-13）面清单 live 核对（全部核心文件在案）；遗留输入终态复核——19-3 JSON.parse 收敛（`designer-page-body.tsx:193-255` try/catch + reportHostIssue + 回归测试）、15-2 NaN fail-closed 收敛（`designer-xyflow-node.keyboard.test.tsx:121-139` 零尺寸矩形 (0,0) 回退）、MA4.3 缺口（`createDesignerStoreAdapter` 已补测；`resolveDesignerManifest`/`designerHostContract`/`DESIGNER_CAPABILITY_PUBLICATION` 零直接测试 → H7 回归项）；e2e 覆盖矩阵 12 spec ↔ 13 面落 surface-inventory（缺口：fd-4 槽位键盘 / fd-8 undo / fd-9 节点拖拽 / fd-10 快捷键 / fd-11 剪贴板 / fd-13 失败路径单元覆盖决策）；13 张卡骨架建立。
  - **Phase 2/3 逐面审计（13 卡全 closed）**：§6.1 18 维 + §6.2 H1-H7 逐维核对；声明即契约双向核对（manifest 41 方法 ↔ action-provider ↔ adapter 三向一致）；发现分级——**P0 零、P1 零（审计阶段）**、P2 2 条（fd-2 默认节点标签硬编码中文 15 条 + fd-7 命令适配器错误消息硬编码英文 9 处）、P3 9 条卡内记录；19-3 终态 Decision 收敛。
  - **Phase 4 H7 补测 + P2 路由**：`designer-manifest-contract.test.ts` +3（resolveDesignerManifest 三版本别名 / designerHostContract 绑定 / DESIGNER_CAPABILITY_PUBLICATION 常量，包级 239→241 tests）；新建 `docs/audits/round2-dr-adjudication.md`（零登记基线 + DR-1 fd-7 错误消息 i18n + DR-2 fd-2 默认标签 i18n，卡内 P2 零悬挂）。
  - **Phase 5 宿主场景补全 + 收口验证（现场发现 2 条 P1）**：
    - 新增 e2e `flow-designer-undo-clipboard.spec.ts`（3 用例：Delete→Ctrl+Z 恢复→Ctrl+Y 再删 / Ctrl+C+V 复制粘贴 / 无复制粘贴不产生节点）+ `flow-designer-slot-drag.spec.ts`（2 用例：tree plus button → Add node 菜单链路 / 鼠标拖拽节点 transform 提交）；14 flow-designer e2e spec **57 用例全绿**（3 skip 为既有 test.skip）。
    - **P1-1 toolbar 模板态冻结**（e2e 现场发现）：`designer-toolbar.tsx` items useMemo 依赖缺失 → snapshot 变化后 disabled/active/body/level 永不重解析（撤销/重做/保存按钮 + dirty 徽章冻结）；**根因两层**——memo 掩盖 + `useHostScope`（flux-react）StrictMode 模拟卸载 dispose 后守卫不重建（disposed scope 常驻 store，materializeVisible 返回 {}）。test-first 修复：`designer-controls.test.tsx` 重解析回归用例（先红后绿）+ `useHostScope` 守卫补丁（`__fdDisposed__` 标记，`flux-react/workbench/hooks.ts` + `flux-runtime/runtime-host-projection-scope.ts`，4 host 家族共用机制）+ `hooks.test.tsx` 回归用例；bug note **90**。
    - **P1-2 pasteClipboard 命令缺口**（e2e 现场发现）：`designer-command-adapter.ts` switch 缺 `case 'pasteClipboard'`（声明 3 处执行 0 处，Ctrl+V 静默 unavailable）——test-first 修复：`designer-command-adapter.test.ts` 复制粘贴回归用例（先红后绿）+ 补 case；bug note **91**。
    - 收口登记：roadmap D3.1 行 `todo`→`done`（附执行证据）；surface-inventory 覆盖矩阵增量登记（2 新 spec 行 + 缺口清单全部闭合）；13 卡全 closed；**P2 路由零悬挂（DR-1/DR-2）+ P3 卡内记录零悬挂声明**；taskflow-designer-ui.spec.ts:34 既有 spec 漂移（08-07 dingtalk-flow-demo 移除后 nav 卡断言未同步）当场修正（Style Prototype→M1–M5 All Mobile、DingTalk Flow Demo→Mobile Showcase）。
  - **验证（全绿）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59（包级：flow-designer-renderers 35 files/241 tests、flow-designer-core 9 files/180 tests、flux-react 53 files/476 tests、flux-runtime 123 files/1402 tests 全绿）、flow-designer e2e 14 spec 57 用例全绿；聚合 `pnpm check` exit 0（详见 Closure Gates）。
  - **门禁盲区发现（供 DG 承接）**：`check:audit-event-dispatch-ctx` 仅覆盖 10 个 flux-renderers-\* 包，4 个 host renderer 包靠逐面人工核对（本 plan 已人工核零命中）；`check:audit-test-global-leaks` 类机制对 host 包作用域有限——host 面事件 ctx 门禁覆盖升级归 DG。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D3.1 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（D2 P3 裁决与残余收口执行：4 Phase 全 completed + 6 条低成本修复 + @reserved 核对 + watch-only e2e 复核 + Follow-ups 归集）

- **Plan `2026-08-08-0715-3-round2-d2-p3-adjudication-residual.md` 执行完毕（4 Phase 全 completed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 P3 逐条裁决**：新建 `docs/audits/round2-p3-adjudication.md`——**149 P3 逐条裁决零悬挂（fixed 26 / keep 99 / dismissed 24 = 149，live `rg -c -- "[P3-"` 复核一致）** + 08-06/08-07 审计 P3 5 条全部 fixed（05-02/05-03→0421-2、14-4/14-5+23-3→0150-3、23-4→2228-2）+ **D1 登记 P3 = 0 条**（D1 plan completed 零登记项，顺序纪律已覆盖）。四档处理：(b) 20 条卡内既有 fixed 镜像（原 plan 引用在案）；(a) **6 条低成本当场修复（test-first + focused 测试）**——wizard P3-1 恒等三元去化（`className="mt-4"`，wizard-c5-1-contract.test.tsx +1 双 orientation 断言）、kanban P3-4 注释同步 command-based（kanban-board.tsx:6-8）、gantt P3-5 卸载调用 `store.destroy()`（gantt.tsx:162-166，gantt-store.test.ts +1 destroy 重置用例）、diff-view P3-3 flash setTimeout cleanup（diff-three-column-view.tsx:83-90，diff-view-renderer.test.tsx +1 假时钟 unmount 清零用例）、form P3-1 loadAction 失败消息独立 "Form loadAction failed"（form-lifecycle-helpers.ts message 参数 + form-load-action.ts:76，form-loadaction.test.tsx +1 断言）、input-time P3-1 design.md §4 C2.4 说明复核闭环（已在案）；(c)/(d) keep 99 / dismissed 24 逐条理由。**卡内状态回写 6 卡**（wizard/kanban/gantt/diff-view/form/input-time .md `[P3-x]` 行标注 fixed + plan 引用）。验证：layout 110 / content 292 / scheduling 918 / form 772 包级全绿 + 4 包 typecheck/build/lint 绿。
  - **Phase 2 @reserved 契约核对（7 处 / 3 文件）**：全量登记 + 逐处 live 消费核对 + design.md 对照——**1 激活（crud-schema.ts:37 注释更正：@reserved 语义归 stopWhen 移除留痕，接口本体 live——enabled/sourceId 被 useCrudPolling 消费；design.md §Polling 启停状态发布已对齐）+ 6 维持**（calendar onBatchSchedule/onImport/onTimezoneChange/onGroupToggle/importICal/exportToICal 全仓零派发零消费，仅 ready 激活；ComponentHandle 无对应 method）。ghost contract 专项结论落裁决表 §5。
  - **Phase 3 6 条 watch-only e2e 复核（dev server 带 CI 同参 env 标志隔离复跑）**：c3-5-host-surfaces :27/:81 **6/6 全绿**（维持 watch-only 环境归因，根因纳入 DR editor 面）；**w3d-editor:28 判真缺陷（间歇性）**——隔离复跑 **3/15 绿**，click+type keystroke 丢失竞态（"hel"/"hello edi" 等变体），CV 后 editor 代码零变更，"隔离全绿"归因不再成立 → **显式路由 DR**（editor/Tiptap 面跨面集中修复，非静默延期）；gantt-perf（idle 50.0 ✓ / scroll 49.5 ✗ / drag 50.0 ✗）与 kanban-perf（idle 50.1 ✓ / 20×300 idle 49.7 ✓ / drag 50.0 ✗）——**主屏实测 50.00Hz**（system_profiler "UI Looks like: 1920 x 1080 @ 50.00Hz"），rAF 50fps 硬上限致 >50/>60 阈值物理不可达 → 维持 watch-only + 标注「需 60Hz 环境最终确认」；**ai-attachments 8/8 全绿（4 轮全文件复跑）** → flake 终态 closed。复核结论落裁决表 §6。
  - **Phase 4 Non-Blocking Follow-ups 归集收口**：2228-1 ERP watch-only 维持（`schema-gap-from-erp-integration-design.md` :355/:359/:453/:481 live 在案，`$surface.hasOpenSurface` 全仓零消费者）；2228-3 工具治理 8 条逐条复核引用（0150-1/2/3 全部落定，roadmap Follow-up Backlog `[x]`）；0150-1 stagedDirs（find-event-dispatch-without-ctx.test.ts:14-32）**路由裁定 = 工具治理轮次**（D1 未承接）；归集收口声明无悬挂。结论落裁决表 §7。
  - **验证（全绿）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（1 条既有 gantt-grid React Compiler informational warning）、`pnpm test` 59/59（10,397+ passed / 0 failed）、聚合 `pnpm check` **exit 0**（oversized 仅 2 条既有 locale 豁免）。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D2 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

### 2026-08-08（D1 门禁漂移回扫执行：Phase 2 四模式族回扫 + Phase 3 收口验证）

- **Plan `2026-08-08-0715-2-round2-d1-gate-drift-pattern-family-rescan.md` 执行完毕（3 Phase 全 completed；纯回扫轮，零产品代码变更；closure-audit 待独立 fresh session，证据将见 plan Closure 节）**：
  - **Phase 2 四模式族回扫（扫描目标 = CV 2026-08-06 后新增/修改 renderer 代码 + 第一轮 allowlist/豁免点复验；范围 = 10 个 flux-renderers-\* 包）——四族全部零命中，P0/P1/P2/P3 均无**：
    - **事件派发 ctx**：`check:audit-event-dispatch-ctx` 全量输出零命中 + 7 条 allowlist（notice-bar ×3 / button ×1 / chart-renderer ×3，全部原生 DOM 事件转发形态）逐行 live 复验存在（Phase 1 已记）；人工抽查——carousel.tsx:71（payload + `{event, evaluationBindings, scope}` 第二参 ctx）、video.tsx:42（零参通知，C3.x 空参契约裁决类别，门禁 `checkCallArgs` 零参跳过语义一致）、graph-renderer.tsx:161-179（动态 key 派发 + full ctx，CX-10/bug-83 家族规范）、form.tsx 生命周期派发（`undefined` 首参，门禁 Form 2 跳过逻辑覆盖）；template `${key}` 动态形态核查——graph `props.events[type]` + field-handlers `events?.[eventName]` 均被门禁 Form 2（dynamic index 解析）覆盖且零命中。
    - **`kind:'reaction'` 三件套**：`rg "kind: 'reaction'" packages/flux-renderers-*/src` 全量 **13 处声明**（crud loadAction ×1、gantt zoomIn/zoomOut/scrollToToday/scrollToTask ×4、calendar print/exportPNG/importICal/exportToICal ×4、diff-view toggleViewType/setViewType/expandAll/collapseAll ×4）vs 消费矩阵——**13/13 接线闭环**：crud ready() `crud-renderer-load.ts:320`（内部数据加载机制消费，非 `component:*` 能力句柄面）；gantt ready() `gantt.tsx:351-355` + ComponentHandle `:359-409`（invoke 即派发，22-13 家族）+ UI 派发 `:481-483`；calendar reactionsRef `:59-60` + ready() `:179-183` + ComponentHandle `:196-259`（exportToPNG→exportPNG / exportToPrint→print 派发；importICal/exportToICal @reserved 仅 ready——既定裁决维持）；diff-view reactionsRef `:236-239` + ready() `:302-306` + ComponentHandle `:241-300` + UI 派发 `:311`。gantt 未用 reactionsRef 但 `props.reactions` 为稳定 identity（`reaction-handle-proxy.ts` useMemo 单例）+ 句柄 effect deps 含 `props.reactions` 重注册，与 ref 模式功能等价（reaction-handle-proxy 源码核实）。非 renderer 包命中 11 处（flux-core 6 / runtime 2 / compiler 2 / react 1）为编译期/runtime 机制声明面（`renderer-reaction-handle.ts`、`node-renderer-resolved.tsx` 等），按计划不在回扫范围，归 D2/DR 裁决面。
    - **scope 生命周期**：`rg "createScope"` 全量配对核对——list-renderer.tsx:75/83、cards-renderer.tsx:125/133（useState 初始化 + unmount effect dispose）、loop.tsx:87/96+114/123、recurse.tsx:88/97、picker-renderer.tsx:194/202、use-select-remote-search.ts:63/112、crud-renderer-load.ts:194/396、condition-builder.tsx:106/114、tree-control-sources.ts:43/50+57/64（同步 formula 路径即时 dispose）、use-table-selection.ts:108/119、use-table-lazy-children.ts:60/89、use-table-row-scope-cache.ts（disposeRowScope 175/193/238 全路径）、variant-field-matching.ts:56/60（try/finally 保证）——**全部成对，零泄漏路径**；`check:audit-test-global-leaks` **47/2 与 D0 基线一致**。
    - **i18n 硬编码**：`rg "[\u4e00-\u9fa5]" packages/flux-renderers-*/src`（排除 `__tests__` 与 `*.test.*`）命中全部为注释（graph 家族/kanban-column）与测试夹具资源，**零生产代码中文字面量**；硬编码英文抽样——content 新组件（audio/video/qrcode/carousel/alert/diff-view）全部走 `t()` key（`t('flux.common.loadFailed')`、`t('flux.qrcode.ariaLabel')` 等），graph-node label 为数据驱动回退（非 UI 文案），graph-search 'label'/'type'/'level' 为字段名选项（数据契约）；`check:i18n-keys` exit 0。
    - **共性缺陷模式判定（Decision）**：四族零发现 → **显式声明无共性缺陷，不插入 CX-13+**（roadmap 表/依赖图/CX-n 编号均不动）。
    - **裁决与登记**：零发现，审计卡追加节回写——gantt.md / calendar.md / crud.md / diff-view.md / reaction.md 各加「第二轮回扫记录（D1）」节（声明点 `文件:行` + 接线矩阵 + 零命中裁决）；P2 路由清单（`docs/audits/round2-dr-adjudication.md`）与 P3 裁决表（`docs/audits/round2-p3-adjudication.md`）本 plan **零登记项**（无发现可路由）。
  - **Phase 3 收口验证**：受影响 audit 门禁复跑零命中（`check:audit-event-dispatch-ctx` / `check:audit-renderer-browser-io` / `check:audit-runtime-raw-schema-reads` 全 exit 0）；全量验证 **typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿**（turbo 全缓存命中，本 plan 零代码变更）；聚合 `pnpm check` exit 0；`pnpm test:scripts` 6/15 绿。roadmap D1 行状态流转申请：待 closure-audit pass（独立 fresh session）后 `todo → done`，本 plan 不自行翻转。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D1 行执行证据引用本节（门禁 27/28 exit 0 与 D0 一致 + `check:duplicates:detail` 非门禁归因维持 + test:scripts 6/15 全绿 + 四族零命中 + 5 卡回扫节回写）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转。

### 2026-08-08（D1 门禁漂移回扫执行：Phase 1 门禁全量重跑零新增复验）

- **Plan `2026-08-08-0715-2-round2-d1-gate-drift-pattern-family-rescan.md` 执行中（Phase 1 completed，2026-08-08 实测）**：
  - **D0 基线就绪确认**：plan `2026-08-08-0715-1` Plan Status `completed`（4 Phase 全 completed），28 项逐项输出 + `pnpm test:scripts` 通过数在案（`docs/logs/2026/08-08.md` D0 节）。
  - **28 项 `check:*` 逐项重跑（live `package.json` 28 项含 14 项 `check:audit-*`）**：**27/28 exit 0**，与 D0 基线完全一致——零新增命中判定成立。`check:duplicates:detail` exit 1（Decision，只登记不修复）：`.jscpd.json` `threshold: 0` 的 raw `jscpd packages/` dump（exit 1 = 存在克隆即报的固有行为，非门禁），带阈值的门禁 `check:duplicates`（`scripts/check-duplicates.mjs`，8% 阈值）exit 0 **454 clones / 3.04%**——与 D0 实测数字逐位一致，代码树无新增漂移；判定维持既有设计语义，**不做任何门禁规则变更**（门禁纪律：规则变更必须带 committed 回归测试，本项无变更需求）。
  - **既有红集复核**：`check:oversized-code-files` exit 0（156 warnings / 2 errors / 2 exempt = en-US.ts + zh-CN.ts locale 豁免，与 D0/project-context 声明一致）；`check:workspace-manifest-deps` 零命中（含反向规则）；audit 三门禁零命中（`check:audit-event-dispatch-ctx` / `check:audit-renderer-browser-io` / `check:audit-runtime-raw-schema-reads`）。
  - **`pnpm test:scripts` 复跑**：**6 files / 15 tests 全绿**（vitest.scripts.config.ts，门禁回归套件），扫描器自身无回归。
  - **扫描器回归专项核查（browser-io 6d2497ea 教训）**：`scripts/audit/find-renderer-browser-io.mjs:152` 扫描范围正则 `^packages\/(?:flux-renderers-[^/]+|flow-designer-renderers|spreadsheet-renderers|report-designer-renderers|word-editor-renderers)\//` 前缀匹配正确（10 个 flux-renderers-\* 包 + 4 host renderer 包）；committed 夹具正例 `find-renderer-browser-io.test.ts:60-64`（`flux-renderers-data` 包 fetch 夹具，锁定前缀覆盖）+ `FLUX_AUDIT_SCAN_ROOT` env 判别用例在案；`check:audit-renderer-browser-io` live 零命中。event-dispatch-ctx 门禁（`find-event-dispatch-without-ctx.mjs`）ALLOWLIST 7 条静态登记逐行 live 复核存在（notice-bar.tsx:198/:204/:210 + button.tsx:220 + chart-renderer.tsx:605/:609/:612，均为原生 DOM 事件转发形态，符合 C3.x 裁决类别）；当前 0 命中 0 新增 allowlist 触发（类级 `event` 形参跳过先于 allowlist 判断，allowlist 为防御性静态登记）。
  - **新增命中裁决（Decision）**：28 项逐项输出与 D0 基线一致（含计数类 informational 桶同基线），零新增命中；无"临时改门禁规则"路径触发。

### 2026-08-08（D0 编排基线执行：门禁基线重跑 + 保护区域核对 + 收口登记）

- **D0 全量门禁基线重跑（plan `2026-08-08-0715-1` Phase 3，2026-08-08 实测）——D1 零新增判定基准 = 本次实测输出**：
  - **28 项 `check:*` 逐项重跑**（live `package.json`：28 项独立脚本含 14 项 `check:audit-*`；聚合 `pnpm check` 另含 12 项子集）——**27/28 exit 0**；`check:duplicates:detail` exit 1（归因见下）。零门禁 ERROR，与既有红集（oversized 2 条 locale 豁免）一致。
  - **既有红集核对（与 project-context 声明一致）**：`check:oversized-code-files` exit 0（156 warnings / 2 errors / **2 exempt = en-US.ts + zh-CN.ts locale 豁免**）；`check:workspace-manifest-deps` exit 0 零命中（含反向规则与跨包相对导入规则）；audit 三门禁零命中——`check:audit-event-dispatch-ctx`（7 条原生 DOM 转发 allowlist 静态登记，7 个站点 live 逐行核对存在）、`check:audit-renderer-browser-io`（No direct browser IO）、`check:audit-runtime-raw-schema-reads`（No suspect matches found）。
  - **审计工具计数（exit 0，informational 桶）**：audit-suspects 431/8 桶、audit-async-failure-paths 202/3、audit-test-global-leaks **47/2**（0150-3 收口后 49→47 一致）、audit-styling-suspects 142/1、audit-performance-suspects 22/1、audit-reactive-render-reads 18/1、audit-non-retained-renderer-references 59/5 类型、audit-react19-optimization-candidates 507/5——相对 CV（2026-08-06）计数波动为 CV 后代码/测试增长（scheduling 修复族、新增测试、browser-io 作用域 14 包），全部门禁 exit 0 零 ERROR，非新增红项。
  - **`pnpm test:scripts`（vitest.scripts.config.ts，门禁回归套件）**：**6 files / 15 tests 全绿**。
  - **`check:duplicates:detail` exit 1 归因（Decision，只登记不修复）**：该脚本为无阈值的 raw `jscpd packages/` dump（exit 1 = jscpd 发现克隆的固有行为，非门禁）；带阈值的门禁脚本 `check:duplicates`（`scripts/check-duplicates.mjs`）**exit 0**（454 clones / 3.04% < 8% 阈值，OK）；代码树自 CV 以来仅 docs 变更（HEAD 4f7b8a25 后无 packages 变更），判定为**既有设计语义非 CV 后漂移**，归 **D1 Phase 1 门禁漂移回扫复核**（D1 按门禁语义裁决是否需要调阈值/注释）。
  - **其他独立检查**：`check:i18n-keys` ✅、`check:active-doc-code-anchors`（308 active docs 锚点）、`check:package-css-exports`（17 子路径）、`check:flux-bundle-pack`、`check:renderer-definition-fields-only`、`check:finite-prop-contracts`、`check:src-artifacts`、`check:schema-prop-coverage`、`check:react19`、`check:docs-garbled`、`check:audit-fieldframe-bypasses`/`audit-missing-renderer-markers`/`audit-hardcoded-type-dispatch` 零命中——全部 exit 0。

- **D0 编排基线 plan `2026-08-08-0715-1` 执行完毕（4 Phase 全 completed；纯编排/盘点轮，零产品代码变更；门禁基线 `pnpm check` + `pnpm test:scripts` 为 in-scope Proof 交付物；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 输入盘点**：新建 `docs/audits/round2-p3-inventory.md`——149 条 P3 全量提取（live `rg -c -- "\[P3-" docs/audits/per-component/*.md` = 149，与 pc-index 汇总双重核对一致；74 卡含 P3；状态按卡内标注：fixed 19 / keep 55 / 卡内记录 31 / 记录 12 / 其余 32）+ 08-06/08-07 审计 P3 登记（05-02/05-03 原 P3→P2 由 0421-2 修复、14-4/14-5+23-3 降级 P3 由 0150-3 修复、23-4 P3 由 2228-2 修复）+ Follow-ups 归集终态核验（2228-3 八条全部「由 plan X 落定」复核引用；2228-1 ERP `polling.stopWhen` watch-only 维持（live 复核 :355/:359/:453/:481 仍在、无新消费者证据）；0150-1 stagedDirs 登记路由=工具治理轮次或 D1/DR）+ 零悬挂声明（双重核对方式写明）。
  - **Phase 2 4 host 面范围核对**：新建 `docs/audits/host-surface/` 目录——`README.md`（8 包核对清单：每包 src 结构 + 导出面 + owner docs 契约基准；flow-designer 9 文件 / report-designer 9 文件 / word-editor `design.md` / **spreadsheet 无独立 owner doc（gap 登记，基准取 report-designer 侧 spreadsheet-canvas-css.md + contracts.md）**；宿主 e2e 清单：flow-designer 9 spec + node-title-subtitle-gap + designer-summary-renderers + **taskflow-designer-ui.spec.ts 宿主归属 Decision = flow-designer-renderers（`.react-flow__node` ×7 断言，归 D3.1）**、report-designer 1 spec、word-editor 4 spec、**spreadsheet 0 命中 → coverage gap 确认，由 D3.2 承接补场景**）+ `surface-inventory.md`（host 面清单初稿：fd-1..fd-13 / ss-1..ss-10 / rd-1..rd-7 / we-1..we-7，覆盖 roadmap D3.x 全部列举面）。
  - **Phase 3 门禁基线重跑**：见本日首条（28 项 `check:*` 27/28 exit 0 + `check:duplicates:detail` exit 1 归因登记 + `pnpm test:scripts` 6 files/15 tests 全绿 + 既有红集核对与 project-context 一致）；**D1 零新增判定基准 = 本次实测输出**。
  - **Phase 4 host 模板 + 保护区域**：`component-audit-checklist.md` v2 新增 **§6 Host 大面审计卡模板**（6.1 18 维降维表 host 面语义化 + 6.2 Designer 特有维度 H1 host 契约（RendererEnv/hostContract/manifest）/H2 事务与 undo/H3 拖拽（pointercancel 守卫）/H4 键盘（isEditable 守卫）/H5 剪贴板/H6 e2e 可操作性/H7 MA4.3 覆盖缺口回归 + 6.3 host 面审计卡模板（含「每面 ≥1 真实浏览器宿主场景，bug 73 模式专项，programmatic DOM 断言禁截图」条款）+ 6.4 保护区域地图（4 host 面包默认 `implement`、`packages/ui/src/index.ts` `ask-first`、结构重构人工确认、fields/样式契约 `plan-first`、Auth/security `ask-first`）——**历史 18 维组件级 checklist 与 §4 模板未改写**。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` D0 行 `todo` → `done`（附执行证据引用）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；D0 无代码缺陷 scope，新命中（check:duplicates:detail）显式归 D1 Phase 1 裁决；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

- **新建 `docs/backlog/component-audit-round2-roadmap.md`（逐组件审计第二轮路线图，draft 待人工 review；docs/index.md 登记导航行）**：
  - 立项依据（第一轮 Gap Analysis）：mission 08-06 收口后 08-06/08-07 两轮 post-closure audit 仍发现 ~90 条 P2 真缺陷 →「一轮审计必然不彻底」；149 P3 backlog 未经逐条裁决；4 个 host 大面（flow-designer/spreadsheet/report-designer/word-editor 共 8 包）从无逐面审计卡（M 轮为包簇级）。
  - Work Items（全部 `todo`）：D0 编排基线（P3 盘点 + host 面核对 + 门禁基线 + host 审计卡模板 18 维降维 + designer 特有维度）→ D1 门禁漂移回扫与四模式族回扫（事件 ctx / reaction 三件套 / scope 生命周期 / i18n）→ D2 P3 裁决与残余收口（@reserved 核对 + 6 watch-only e2e 复核）→ D3.1–D3.4 host 大面逐面审计（每面一 plan，独立闭环 + closure）→ DR 跨面集中修复（CX-n 从 CX-13 续编）→ DV 全量验证 → DG Guard 沉淀（round2-index、lessons 06+、门禁升级）。
  - 关键纪律：host 面卡入 `docs/audits/host-surface/`；门禁规则变更必须带 committed 回归测试；提交按 `fix(component-audit-round2)` 前缀；新 work item 由人工确认后插入，AI 不自行增删优先级。
  - **追加（同日）**：按用户要求插入 DB / DL 两个 work item——**DB. Bug 沉淀回补**（盘点第一轮 + post-closure 已修复但缺 `docs/bugs/` note 的复杂缺陷 ~15 条：CX-6/7/8、2-8/2-9/2-10/2-11/2-12/2-13/2-14/2-16/2-19、15-2、22-07、22-12，编号 90 起，盘点表 `docs/audits/round2-bug-note-gaps.md`）、**DL. Lessons 沉淀 06–08**（审计必漏定律 + post-closure audit 制度、工具门禁沉淀法、ghost contract 检测法）；DG 改从 09 起续写并承接 bug-note 缺口终验；Cross-Cutting 新增「bug note / lessons 编号纪律」（D3.x 修复复杂 bug 必须当 plan 行内补写，不积压）；依赖图与 Gap Analysis 同步更新。

### 2026-08-08（续：round-2 mission 拟制与独立审查）

- **按用户指示搁置 industrial-hmi 分支合并**（`feat-industrial-hmi` 158 commits ahead / 183 behind，用户决定暂不合并）；industrial-hmi 审计明确排除在 round-2 范围之外（另立 successor mission），未在 roadmap 增加其 work item。
- **新建 `missions/component-audit-round2.json`**（字段对齐 `component-audit.json` 先例：name/model/variant/roadmapPath/plansDir/planGuide/auditsDir/contextDir/moduleDir/commands/prompts/commitFormat=`fix(component-audit-round2): <description>`）：description 覆盖五项范围（① 门禁漂移回扫 + 四模式族回扫（10 个 flux-renderers-\* 包，host renderer 包归 D3.x）；② 149 P3 逐条裁决 + @reserved 核对 + 6 watch-only 复核；③ 4 host 大面 8 包首次逐面审计（18 维降维 + designer 特有维度）；④ bug note 回补（90 起，盘点表零悬挂）；⑤ lessons 06–08）+ 自动修复授权（沿用第一轮 roadmap「自动修复机制」§1–§7，CX-13 起编号）+ 保护区域授权（同 component-audit）+ industrial-hmi 范围排除。
- **roadmap 头部更新**：`> Mission：missions/component-audit-round2.json`；目的 §4 具名「自动修复机制」指针（指向第一轮 roadmap，消除互指悬空）。
- **独立子 agent 两轮审查达成共识**：Round 1（fresh session `ses_0218c3747ffeWp2gKalnC8MNJZ`）verdict `approved`（零 Blocker，1 Major + 6 Minor）→ 修正（mission 指针、check 计数、D1 范围表述、DG 依赖补 DB、ai-attachments flake 措辞、D2 裁决口径、`pnpm test:scripts` 点名）；Round 2（fresh session `ses_02186e7c9ffe57KCNAW1ffJrG4`）verdict `approved`（零 Blocker/Major，2 Minor：check 计数 24/13 → live 实测 **28/14**、P2 计数 ~90 → 精确 85（08-06 批 46 + 08-07 批 39））→ 均已修正并经本地复核（`grep` 零残留 + JSON 校验通过）。两轮共识达成，roadmap 与 mission 可进入执行（D0 plan 起草需先按 plan-authoring guide 走 draft review）。

### 2026-08-08（续：AI 不变式闭环诊断与 roadmap）

- **AI 模块审计疲劳诊断**（用户问"是否需 round-2 审计 + nop-stream 同病？"）：核查 AI 4 轮 audit（07-23/07-24×2/07-25）发现**打地鼠模式铁证**——每轮"已修复"被下一轮在同族兄弟路径击穿：①并发守卫族（Bug 07→AI-01 send 加守卫→1757 发现 finally/catch 未做 controller 身份守卫）；②stale-closure 族（P1-3 修 switchConversation 加 activeIdRef→0707 发现 deleteConversation 漏改，审计原话"same family, missed method"）；③storage 静默丢族（P1-2 修 create/rename→0707 open 发现 clearAll 完全无 delete 路径，"precise inverse"）。根因 = **修实例不修类别** + 反应式测试（per-bug）非穷举 + **零 engine 不变式门禁**（scripts/ 无 engine check，renderer 包有 check:audit-event-dispatch-ctx 但 engine 无等价物）。结论：nop-ai 与 nop-stream 同病；直接跑通用 round-2 审计会继续打地鼠，需先沉淀不变式门禁。
- **新建 `docs/backlog/ai-invariant-loop-roadmap.md`**（AI engine 不变式驱动的持续审计闭环，自驱动飞轮）：把修-漏-再修改造为 **I0 盘点→I1 沉淀不变式(→门禁)→I2 按不变式审计→I3 裁决成工作项→I4 修(强制类别清扫+test)→I5 验证→I6 收口(新族?→下轮I1; 否则稳态)** 循环。可持续机制：①不变式=可执行门禁入 CI（回归自动被抓）；②门禁集合单调棘轮（只增不减，弱化需人工确认）；③Loop Rule 预授权下轮 I1/I2 自动派生（类 CX-n，附触发证据），不需逐次人工重启；④稳态暂停 + 三条件复触发（CI 变红/结构变更/周期复探）。Cycle 1 首批 5 类不变式（isProcessing 入口守卫 / await 后用 ref / controller 身份守卫 / storage 经 reportStorageError / abort 清 controller）落参数化穷举测试 + `check:ai-engine-invariants` 门禁。Cross-Cutting 标注方法论可推广到其他有状态子系统（flow-designer 事务等，本轮不纳入）。docs/index.md 登记导航行。
- **AI 闭环 roadmap + mission 共识审查（2 轮）**：Round 1（fresh session `ses_01f311f70ffeaxkSCNZN7Q9U00`）verdict `revised`（3 Blocker + 3 Major + 5 Minor）→ 修正：① **B1 基线未核对 live**（三大族 live code 已修复， Purpose/基线改过去时、论点转"需多轮才捕获=效率问题"）；② **B2 无 mission 驱动**（新建 `missions/ai-invariant-loop.json`，字段对齐 round2 先例，moduleDir scoped 到 AI 包）；③ **B3 D-AI 悬空引用**（删除，本图独立）；M1 表完备性门禁、M2 send/sendMessage 并列、M3 并发族计数校正 + 5 Minor。Round 2（fresh session `ses_01f267d3bffej8MYRUURVeESVi`）verdict `approved`（零 Blocker/Major，8/8 修正到位 + live code 实证），残余 3 Minor（I0 摘要标签、bug note 编号 107+→114+ 实测最高 113、mission description 范围独立声明）已修正。两 roadmap 头部回写产出方法 + 共识审查轮次。
- **新建元提示词 `docs/skills/roadmap-and-mission-authoring-with-consensus-review.md`**（过程元 skill，区别于既有对象级审计 skill）：固化本日工作流——步骤 0 诊断优先（反复出问题先查根因，禁直接再开一轮）、步骤 1 选型（线性 vs 持续闭环飞轮）、步骤 2/3 拟 roadmap+mission（基线核对 live、无悬空引用、计数 live 实测）、**步骤 4 共识审查常设纪律**（每次 roadmap/mission 修订必须独立 fresh session 反复审查至 approved，executor 不自审，Blocker/Major/Minor）、步骤 5 执行闭环。含反模式清单。docs/skills/README.md By Task 表登记；两已共识 roadmap 头部标注产出方法。**立为常设纪律：今后任何 roadmap/mission 修订均须走本 skill 的共识审查。**
- **元提示词重构为可移植形态**（用户问能否用于 nop-app-erp / nop-entropy）：核查三仓基建——nop-app-erp 有同套（00-roadmap-authoring-guide / 00-plan guide / docs/skills / missions 同 schema / 9 个 roadmap），nop-entropy 仅 missions/ 无 plans/skills/backlog。既有同族 skill `audit-remediation-roadmap-authoring-prompt.md` 已在 chaof-flux + erp 两仓以"同主体 + 各自项目定制层"形态共存。据此重构本 skill：**主体（步骤 0–5 + 规则 + 反模式）项目无关；项目特定内容（验证命令/命名/保护区域/先例/CX-n/编号基线）集中到顶部「项目定制化层」overlay 块**（对齐同族 skill 既定约定）；新增「如何移植到其他项目」节（核对基建→copy→替换 overlay→登记 README）；反模式补"跨仓带入命令/命名"。本仓版本填 nop-chaos-flux overlay 值；移植 erp 时 copy + 换 overlay（pnpm→mvn、@nop-chaos→erp module、先例换 erp roadmap）即可；nop-entropy 基建不全建议先补。

- **Plan `2026-08-08-0150-3-test-infra-patterns-remediation.md` 执行完毕（4 Phase 全 completed；测试基建模式 3 条 P2/P3（14-2/14-4/14-5+23-3）全部收口，纯测试代码改动无产品代码变更；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（14-2 document-io-test-utils 显式 install）**：`packages/word-editor-core/src/__tests__/document-io-test-utils.ts` 模块级 `beforeEach`/`afterEach` 副作用收敛为导出 `installDocumentIoTestHooks()`（内部注册 localStorage stub + `setRecoveryLoadErrorHandler(undefined)` 清理 + `vi.unstubAllGlobals()`），`localStorageState` 保留为显式可访问 mock 实例、常量导出（STORAGE_KEY/DATASET_STORAGE_KEY）与 hook 副作用解耦；两个 importer（`document-io-datasets.test.ts`/`document-io-persist.test.ts`）文件级显式调用一次；新合成用例 `document-io-test-utils.install.test.ts` 证明常量-only 导入不注册全局 stub（vitest 4 node env 自带 `{}` localStorage mock，故以本模块 `_store` 标记不存在为判别；`vi.isStubGlobal` 在 vitest 4.1.2 不存在）。`rg '^beforeEach|^afterEach'` 模块顶层零命中；word-editor-core 14 files / **248 passed** 全绿。
  - **Phase 2（14-4 scopeCounter 局部化）**：`table-selection-checkable-scope-dispose.test.tsx:12` 与同包 `use-table-controls.test-support.tsx:28` 两处模块顶层 `let scopeCounter` 分别移入 `createSpyHelpers`/`createHelpers` 局部作用域（断言全为配对相对断言，不依赖绝对 id）；`pnpm check:audit-test-global-leaks` **49 → 47**（两文件各去 1 条、按桶核对零新增）；`rg '^let .*Counter = 0'` 两文件零命中；data 包 109 files / **765 passed** 全绿。
  - **Phase 3（14-5+23-3 Space 断言对齐 + fail-closed 用例）**：`designer-xyflow-node.keyboard.test.tsx` Space 用例 `Number.isFinite` 永真断言改精确中心值 (200,90)（与 Enter 用例对齐，同一 `openSlotMenuFromElement` handler 路径）；新增 fail-closed 用例——零尺寸矩形（left:0/top:0/right:0/bottom:0 + width/height:0）→ **行为裁定**：live 核对 `openSlotMenuFromElement`（designer-xyflow-node.tsx:205-216）**无 NaN 保护**，`rect.left + rect.width/2`/`rect.top + rect.height/2` 对零矩形得 (0,0) 有限值不抛错，故断言精确 (0,0) + `onPlusButtonClick` 调用 1 次（锁定"坐标按矩形中心推导、零矩形落到原点"的真实语义，非永真）；flow-designer-renderers 35 files / **236 passed** 全绿（235 + 1 新用例）。
  - **收口登记**：roadmap Follow-up Backlog「2026-08-07-1747 两轮审计 P2」14-2/14-4 两行 `[ ]`→`[x]` 附 plan 引用；14-5+23-3 无独立 roadmap 行，按 plan 以**行内注记**在「2026-08-07-1747 两轮审计 P2」节末登记收口（含 2228-3 Non-Blocking Follow-ups 引用收口状态，2228-3 为 completed 历史计划按 Rule 21 不回写正文）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

- **Plan `2026-08-08-0150-2-public-api-deps-hygiene.md` 执行完毕（4 Phase 全 completed；公共 API 面与依赖卫生 2 条 P2（03-01/03-02）收口，含反向检查裁决实施；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（03-01 fork 直接单测 + JSDoc）**：新建 `packages/flux-react/src/__tests__/use-sync-external-store-with-selector.test.tsx` 7 条直接单测（仿 dialog-host-surface.test.tsx renderHook + act 先例）锁定四组客户端可观测语义——(a) 相等性缓存：同值新 snapshot 通知 selection 稳定 + selector 逐 snapshot 恰一次（spy 计数）+ 同引用通知零调用；节点引用/渲染计数双断言；(b) getServerSnapshot 可选签名契约：第三参显式 `undefined` 渲染正常且订阅走 getSnapshot，提供真实函数时客户端渲染 spy 零调用（SSR hydrate 分支超出 happy-dom 面，写入注释而非断言，与 Failure Paths 表一致）；(c) isEqual 自定义：默认 objectIs（selection 变化重渲染）vs 自定义 `(a,b)=>a.count===b.count`（selection 引用稳定 + 零重渲染）；(d) 订阅生命周期：unmount 时 unsubscribe 恰一次 + listeners 清零、selector 抛错在 act 中传播且订阅仍清理。fork 文件头补 JSDoc：hand-copied from npm `use-sync-external-store/with-selector`、`getServerSnapshot` 由必传放宽为可选（React 19 原生第三参语义）、维护警示（6 处真实消费点：flux-react 同包 5 处相对导入 + flux-bundle shim bare 1 处；npm shim 12 处消费点（5 包）不属于 fork 同步面）。验证：flux-react 包测试 53 files / **474 passed** 零回归，typecheck 绿。
  - **Phase 2（03-02 graph 死依赖移除）**：`flux-renderers-graph/package.json` 删除 `use-sync-external-store`，`pnpm install --lockfile-only` 同步 lockfile（graph importer 声明消失；包条目因 zustand peer 依赖与其他 5 个声明方保留属预期）；`rg "use-sync-external-store" packages/flux-renderers-graph/` 零命中，graph typecheck/build/test（5 files / 48 tests）绿，`check:workspace-manifest-deps` exit 0。
  - **Phase 3（「声明但未引用」反向检查裁决）**：**裁决 = 实施**——`scripts/check-workspace-manifest-deps.mjs` 新增反向规则：`dependencies` 声明的非 workspace 依赖做全包 src 引用计数（静态 `from`/动态 `import()`/副作用 `import`/`require()`/`vi.mock()` 均计引用，subpath 规约到顶层包名，`import type` 计引用），零引用报红；`peerDependencies`（host 提供）与 `devDependencies`（测试/构建配置面）排除。作用域**全包**而非仅 renderer 包（全仓普查显示零引用依赖全部落在 renderer 家族，carve-out 无收益）。规则上线时全仓清零——试跑暴露并清除 2 条同类残留死依赖：`flux-renderers-content` 的 `@tanstack/react-virtual`（`diff-virtual-list.tsx` 于 8d723dd2 死模块清理删除时 package.json 声明残留）与 `word-editor-renderers` 的 `@hufe921/canvas-editor`（真实消费者为 word-editor-core 自身声明；renderers src 零导入，`types/` shim 目录已不存在，tsconfig.build.json path 映射保留为惰性配置未动）；两条删除 + lockfile 同步后 content（35 files / 291 tests）、word-editor-renderers（19 files / 136 tests）typecheck+build+test 全绿。新夹具测试（`scripts/__tests__/check-workspace-manifest-deps.test.ts` +1）：暂存 `packages/__manifest-reverse-fixture__`（`dead-dep-fixture` 声明 + 零引用 src）→ 门禁红；补引用后 → 绿（先红后绿）。
  - **验证（全绿）**：`pnpm test:scripts` **6 files / 15 tests** 全绿（manifest-deps 3/3 含新反向规则用例）；`pnpm check` 聚合 **exit 0**（workspace-manifest-deps 真实仓库零命中；oversized 仅 2 条既有 locale 豁免；audit 三门禁零命中）。
  - **收口登记**：roadmap Follow-up Backlog 03-01/03-02 两行 `[ ]`→`[x]` 附 plan 引用（含反向检查裁决与两条连带死依赖清除）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

- **Plan `2026-08-08-0150-1-audit-tooling-gates-remediation.md` 执行完毕（4 Phase 全 completed；3 条 audit 工具/门禁 P2（01-02/03-03/14-1）全部收口，两条工具修复 test-first 先红后绿；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（check-workspace-manifest-deps 未跟踪扫描，01-02）**：`getTrackedFiles()` 双源合并——`git ls-files` + `git ls-files --others --exclude-standard`，去重后统一走既有过滤链（packages/ 前缀、/src/ 包含、ts/tsx、dist/.d.ts 排除）；拆分/迁移中途态的跨包相对导入本地即报红。新文件 `scripts/__tests__/check-workspace-manifest-deps.test.ts` 先红后绿（红 = 修复前门禁 exit 0 漏检未跟踪夹具）。**执行细节**：夹具暂存于 `packages/<pkg>/src/temp/`（`temp` 在 `scripts/audit/shared.mjs` ignoreDirectoryNames 中 → 并行 fork 的其他门禁测试 collectSourceFiles 不可见，消除 ENOENT 竞态；且非 gitignore 模式 → `--others --exclude-standard` 可见）；夹具经 `../../../ui/src/index.js` 到达 `packages/ui/src/index.ts`（.js→.ts probe 真实解析）。连带：`vitest.scripts.config.ts` 设 `fileParallelism: false`（脚本门禁测试 exec 真实门禁 + 真实仓库树内夹具，并行文件会竞态夹具生命周期）。
  - **Phase 2（raw-schema-reads 块注释排除 + as T).schema 形态，03-03）**：`shared.mjs` 新增 `getCodeTextForLine`（跨行块注释状态机 + 字符串保留，剥除 `//`/`/*...*/` 后判定）；`rules.mjs` patterns 补 `\bas\s+TemplateNode\)\.schema\b`/`\bas\s+BaseSchema\)\.schema\b`；`:115` 确定性命中（全仓唯一 as-cast 形态）经**窄分支排除**（crud-renderer-schema-builders.ts + `nodes.map((n) => (n as TemplateNode).schema` 行形精确匹配，仿 variant-field 先例）——裁决：region handle schema 组合/搬运，非 compile-once 违规（与 audit 已裁定一致），**不改业务代码语义**。新文件 `scripts/__tests__/find-runtime-raw-schema-reads.test.ts` 合成夹具（JSDoc 块注释 `props.schema` 负例 + `(n as TemplateNode).schema`/`(v as BaseSchema).schema` 正例）先红后绿；`:128` JSDoc 误报消失，门禁零输出。
  - **Phase 3（browser-io 夹具临时目录治理，14-1）**：`find-renderer-browser-io.mjs` 支持 `FLUX_AUDIT_SCAN_ROOT` env 覆盖（扫描根 + 相对路径基准双切换，`packages/<pkg>/...` 相对形态保持）；`find-renderer-browser-io.test.ts` 夹具改 `os.tmpdir()/flux-inv1-scan-fixtures-*` 临时树镜像（目录名避开 ignore 集合），`stagedDirs` 局部化；**裁决 = 不实施 `__inv1_scan_fixture__` ignore 兜底**（collectSourceFiles 任意层级跳过 ignore 目录名，会连临时树内同名目录一起跳掉导致正例失效）；`rg "__inv1_scan_fixture__" packages/` 零命中；env 判别验证（临时根放真实 fetch 夹具 → 带 env 门禁 rejects exit 1）通过。**连带发现并修复（执行中裁定）**：env 验证暴露 `:127` 扫描范围正则回归——commit `6d2497ea`（2026-08-07「widen 14 renderer 包」）把 `^packages\/flux-renderers-`（前缀，正确）改坏为 `^packages\/(flux-renderers-|...)\/`（要求 `flux-renderers-` 后紧跟 `/`，**10 个 flux-renderers-\* 包被静默漏扫**）；修订为 `^packages\/(?:flux-renderers-[^/]+|flow-designer-renderers|spreadsheet-renderers|report-designer-renderers|word-editor-renderers)\//`，带 isCodePosition 全量复扫真实仓库 0 code-position 命中（5 个 raw 命中全在字符串/注释），门禁维持 exit 0；新增 committed 正例用例（flux-renderers-data 包 fetch 夹具）同时锁定前缀覆盖 + env 判别力。注：`find-event-dispatch-without-ctx.test.ts` 同根 `stagedDirs` + 真实包目录夹具模式按 plan Non-Blocking Follow-ups 归未来工具治理轮次（本次未扩大范围）。
  - **验证（全绿）**：`pnpm test:scripts` **6 files / 14 tests** 全绿（新增 manifest-deps 2 + raw-schema 2 + browser-io flux 正例 1）；`pnpm check` **exit 0**（三条受影响门禁零命中零新增；oversized 仅 2 条既有 locale 豁免——rules.mjs 增量经 `getCodeTextForLine` 移入 shared.mjs 后回落 644 行）；`check:workspace-manifest-deps`/`check:audit-renderer-browser-io` 真实仓库 exit 0。
  - **收口登记**：roadmap Follow-up Backlog 01-02/03-03/14-1 三行 `[ ]`→`[x]` 附收口注记（plan 路径 + 交付面 + 14-1 连带正则修复裁定）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。

---

<!-- feat-industrial-hmi branch entries (merged) -->

### 2026-08-08（plan 2026-08-08-1910-2 industrial SCADA connection lifecycle & editor rollback integrity 执行）

- **plan `2026-08-08-1910-2`（编辑器 connection 声明生命周期 + 引擎场景回滚）执行完成**——把 `flux-renderers-industrial` 编辑器 4 个 P1（A2 生产连线 id 碰撞覆盖 / A6 applyUndoRedoDiff 引擎回滚缺失 / A7 clipboard connection target/id 不重写 / A8 删除不 prune dangling connection）收口，4 Phase 全交付。源 audit `docs/audits/2026-08-08-1712-open-audit-industrial-hmi-component-audit.md`（A2/A6/A7/A8）。与 first-wave `1809-2`（node id + 事务态 + selection）互补——后者管「node id 空间 + 事务态 + selection」，本 plan 管「connection 声明生命周期 + 引擎回滚」。
  - **Phase 1（A2：生产连线拖拽 id 不碰撞）**：`connection-drag-controller.ts` `beginDrag` 用 `readConnections(junctionNode.custom)` 读现有 connections 传入 `beginConnectionDrag({junctionId, existingConnections})`；`connection-adapter.ts` `beginConnectionDrag` 接 `existingConnections?: ScadaPipeConnection[]` 喂 `generateConnectionId(junctionId, existing ?? [])`。根因：生产 UI pointerdown → `beginDrag` 不传 connectionId → 恒 `generateConnectionId(junctionId, [])` = conn-0 → 第二次拖拽 commit 用 conn-0 findIndex 命中第一条 → 静默覆盖（junction 最多持 1 条连线）。e2e 走 programmaticConnect（显式 connectionId → idx 恒 -1 → push）故绕过缺陷。failing-first 1 测（生产路径两次拖拽 → 两条 connections 并存，onCommit 写回 junction 模拟 writeConnection）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 2（A6：applyUndoRedoDiff 引擎回滚）**：Decision（strategy a）：catch 中 `engine.build(beforeWorking)` 全量重建（leafer 无事务，全量重建是唯一可靠回滚）+ `synced.config = cloneConfigSnapshot(beforeWorking)`（闭合 steady-state 空 diff → 引擎永不愈合的永久背离）。根因：旧 catch 仅回滚 working copy 不回滚引擎；engine.applyDiff（remove→build→update→reorder）半途抛错时场景树已半变。failing-first 1 测（mock engine.applyDiff 先 registry.add 再抛模拟半应用 → undo → 断言 engine.getSymbol('foo') undefined / synced.config 对齐 / onError 派发）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 3（A7：clipboard connection target/id 重写）**：`toolbox/clipboard.ts` `buildClipboardPaste` 改两遍：Pass 1 `buildIdMapRecursive` 建 oldId→newId 全图映射；Pass 2 clone + `reassignIdsFromMap` + `rewriteConnectionsRecursive`（target 命中映射用副本 id / 未命中 dangling-tolerant 保留原件；id 经 `generateConnectionId(副本 junction id, rewritten)` 重新生成）。移除旧 `reassignIdsRecursive`（lint unused）。Dangling 策略抉择：保留原件（与 `listAllConnections` dangling 诊断一致，丢弃破坏性更大）。failing-first 3 测（target→副本 / target 未复制 dangling-tolerant / 多 connection id 不碰撞）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 4（A8：删除 prune dangling connection）**：新增共享 helper `editor-working-helpers.ts` `pruneDanglingConnections(symbols, removedIds)`（递归含 group 子树，不可变纪律仅重建被改节点）。`removeWorkingSymbol` 在 `removeNodeRecursive` 后调 prune（snapshot-based pushOperation 自动含 pruned custom 的 updated，inverse 从 prevSnapshot 恢复）。`cutSelection` 同型 prune + 由 `pushForward`（手工 forward 仅记 removed，undo 无法恢复 prune）改 `pushOperation`（snapshot-based，使 undo 完整恢复 pruned connection + 被剪节点）。根因：删节点仅 filter 节点不扫其它 junction 的 connection.target → 保存后永久 dangling 污染。failing-first 3 测（removeWorkingSymbol prune + undo 恢复 / cutSelection prune + undo 恢复 / sibling connection 保留）先于 Fix 验证失败，Fix 后转 pass。
- **验证**：`pnpm typecheck` workspace 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32 · `pnpm test` workspace 59/59（industrial **1387 tests** = 1379 基线 + 8 新 failing-first，零回归）。公共导出面（runtime 句柄签名 / clipboard buildClipboardPaste 返回结构 / runtime mutators 签名）不变；新增 `pruneDanglingConnections` + clipboard 内部 `buildIdMapRecursive`/`reassignIdsFromMap`/`rewriteConnectionsRecursive` 均 internal-only。
- **closure audit**：**PASS（fresh session `ses_01e340ec6ffeLvZZFWfd1UPV33`，mission-driver closure-audit step，非执行 session 自审）** verdict=`approved`（0 Blocker / 1 Major-non-blocking-expected / 2 Minor，全部已落地）。独立 live 复核：4 phase fix 全部 live & wired（非 stub）—— A2 `connection-drag-controller.ts:81-84` 读现有 connections；A6 `runtime-mutators.ts:154-156` catch 调 engine.build + synced 对齐；A7 `clipboard.ts:87-108/143-158` idMap + target/id 重写、旧 reassignIdsRecursive 已移除；A8 `runtime-mutators.ts:99-108` + `toolbox-runtime.ts:180-195` + `editor-working-helpers.ts:183-206` prune + cutSelection 改 pushOperation。failing-first 测试均断言可观测结果（A2 走生产路径 onCommit 写回；A6 mock 模拟半应用再抛；A8 undo 断言 pruned connection 恢复）。fresh session 重跑 industrial typecheck/lint/test 全 green。Major（已落地）：`design-connection.md` §4.4 editor-delete 现 prune vs 文档原「保留 dangling」drift；Minor（已落地）：`design-toolbox.md` §4.3 / `design-undo-redo.md` omission。
- **plan 收口**：`2026-08-08-1910-2` Plan Status `active` → `completed`（closure audit PASS 后回写），4 Phase status + Exit Criteria + Closure Gates 全勾（closure-audit gate 经 fresh session PASS 勾选）；Closure section 填 fresh session audit evidence。无 `> Source Audits:` 行（`> Source:` 指 audit 但非该 label；source audit 驱动 6 个 sibling plans 1809-1/2/3 + 1910-1/2/3，不可仅凭 1 plan 关闭整文件 Audit Status），source audit closure 步骤省略。plan 无 `> Work Item:` label，roadmap 无 ❌/✅ work item tracking（A2/A6/A7/A8 经 plan 完成本身 tracking），但 roadmap `## Follow-up Backlog` 补「第二波执行进度」`1910-2` ✅ completed。
- **owner doc**：已同步（closure audit Major/Minor 全部落地）——`design-connection.md` §4.4（目标设备删除：编辑器主动 prune vs 运行态 dangling-tolerant 分工）+ §5 connection.id（生产拖拽读现有 connections 碰撞避让）+ §8.2 removeSymbol（prune 说明）+ §12.1 C2（editor prune）/ C4（id 唯一性防护落地）；`design-toolbox.md` §4.3（粘贴 connection target/id 重写子节）；`design-undo-redo.md` §12.1 U8（applyDiff 半途抛错引擎回滚风险行）。
- **successor**：第二波剩余 `2026-08-08-1910-3`（A3 零缩放飞隙 / A4 runtime meta disabled/visible）待执行；P2 簇（本轮-11 paste id 碰撞检查 / 本轮-12 align-distribute world 坐标）归 backlog follow-up。

### 2026-08-08（plan 2026-08-08-1910-1 industrial SCADA symbol geometry & prop routing 执行）

- **plan `2026-08-08-1910-1`（复合图元几何定位 / 尺寸路由 / 样式路由三类系统性缺陷）执行完成**——把 `flux-renderers-industrial` 符号族 A1（P0）+ A11（P1）+ A12（P1）收口，3 Phase 全交付。源 audit `docs/audits/2026-08-08-1712-open-audit-industrial-hmi-component-audit.md`（A1/A11/A12）。
  - **Phase 1（A1 P0：复合图元中心点几何修复 + 真实 leafer boxBounds 回归网）**：7 个按中心点放置的内层 `Ellipse`/`Group` 加 `around:'center'`（逐形状落地，非 `toShapeAttrs` 统一——base rect/ellipse/line 共用后者，统一会破坏 top-left 自洽）。修复范围：`device/{pump,motor,fan}` 的 impeller/rotor/blades、`sensor-control/{indicator,sensor}` 的 lamp/probe、`instrument/{gauge,thermometer}` 的 body/bulb。排除：`valve` core（`x: width/2 - height*0.22` 已 top-left 自洽）、gauge needle `Line`（points-relative pivot 本就正确）、所有 body `Rect({x:0,y:0})`（top-left 自洽）。新增 3 条**真实 leafer** e2e 几何断言（`scada-demo.spec.ts` 经 `page.evaluate` 读 `getBounds('box','local')`）：gauge body `localBox.x/y≈0`（footprint 内）+ pump impeller center≈(30,30)（body 圆心）+ indicator lamp `around==='center'`。**关键发现**：经 empirical probe 确认 leafer `boxBounds` getter 返回 inner box（恒 `{x:0,y:0,w,h}`），不反映 around 锚定——改用 `getBounds('box','local')`（parent-local 空间 box）。mock 层 `*-symbols.test.ts` 零回归（`around` 不改 mock `x` 值，仅改真实 leafer 渲染矩阵）。
  - **Phase 2（A11 P1：level/thermometer/progress 补 parts.resize）**：`composite.ts` `applyCompositeProps` EXTENT_FIELDS 分支改为 extent routing 后**也**调 resize（原 `else` 改非 else——device/gauge 无 extent 不受影响）。三仪表补 `parts.resize` hook：level/thermometer 处理 width（重算 body/tube 宽度 + liquid 宽度 + bulb 中心/尺寸 + label 宽度），progress 处理 height（重算 track 高度 + bar 高度 + label y）。**语义隔离**：level/thermometer 的 height = binding 液位（extent 长度，不 resize 容器），progress 的 width = binding bar 长度（不 resize 容器）——resize hook 只处理非 binding 维度，避免几何/液位打架。新增 4 条 canvas 层回归测（level width / thermometer width / progress height / level height binding 不被覆盖）。
  - **Phase 3（A12 P1：pipe-junction stubs stroke/fill 路由）**：`pipe-junction.ts` `applyProps` 的 stub patch 循环增加 `stroke`/`fill` 写入（与既有 flow/dashOffset/strokeWidth 同路径），body 与 stubs 同色。新增 2 条 canvas 层回归测（stroke + fill 路由到 stubs）。
- **验证**：`pnpm typecheck` workspace 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32 · `pnpm test` industrial 103 文件 **1379 tests**（= 1373 基线 + 6 新，零回归）。e2e A1 三条 PASS。Pre-existing e2e failure（4 scada-demo hover/click + 2 scada-edge-cases hover）经 stash 验证确认非本 plan 引入。
- **closure audit**：**PASS（fresh session，mission-driver closure-audit step，非执行 session 自审）** verdict=`approved`（0 Blocker / 0 Major / 0 Minor）。独立 live 复核：Phase 1 grep `around:'center'` 命中 7 文件（pump/motor/fan/indicator/sensor/gauge/thermometer）+ e2e `scada-demo.spec.ts:476-544` 3 条真 leafer `getBounds('box','local')` 断言；Phase 2 `composite.ts:72-79` EXTENT_FIELDS 改非 else + level/thermometer/progress 真实 resize hook（非空体，语义隔离正确）；Phase 3 `pipe-junction.ts:135-136` stub stroke/fill 路由 + `pipe-symbols.test.ts:306-347` 回归。anti-hollow / 五点一致性 / deferred honesty（4 P2 全 backlog 非 in-scope defect）/ docs sync 全通过。
- **plan 收口**：`2026-08-08-1910-1` Plan Status `active` → `completed`（closure audit PASS 后 fresh session 回写），3 Phase status + Exit Criteria + Closure Gates 全勾（含 closure-audit gate 经 fresh session PASS 勾选）；Closure section 填 fresh session audit evidence。无 `> Source Audits:` 行（`> Source:` 指 audit 但非该 label；且 source audit 驱动 6 个 sibling plans，不可仅凭 1 plan 关闭整文件 Audit Status），source audit closure 步骤省略。plan 无 `> Work Item:` label，roadmap 无 ❌/✅ work item tracking（A1/A11/A12 经 plan 完成本身 tracking）。
- **owner doc**：No update required —— `around:'center'` 是 leafer 原生锚定 API（非项目自创约定），`parts.resize` 纪律已在 `composite.ts:20-27` 文档化（plan 2026-08-06-0900-2 P2-4），pipe-junction stubs 路由已在代码注释中文档化。`design-symbols.md` 复合图元章节描述角色路由架构（body/rotor/extent/label），不涉及 leafer around 锚定细节。

### 2026-08-08（plan 2026-08-08-1809-3 industrial SCADA canvas correctness 执行）

- **plan `2026-08-08-1809-3`（画布/视口在嵌套与响应式场景下不反映真实状态）执行完成**——把 `flux-renderers-industrial` 3 个 P1（P1-2 嵌套 group 子图元 canvas 同步 / P1-4 结构 mutator 嵌套解析 / P1-5 响应式视口 fit）收口，3 Phase 全交付。源 audit `docs/audits/2026-08-08-1712-multi-audit-industrial-hmi-component-audit.md`（P1-2/P1-4/P1-5）。
  - **Phase 1（P1-2 applyUpdate 递归应用 children canvas 层同步）**：`editor/renderer/editor-engine.ts` `applyUpdate` 收到含 `children` patch 时，按 `registry.subtreeIds` 解链旧子树 + 经 `buildNode` 按 patch.children 重建（镜像 runtime `ConfigAdapter.applyUpdate` config-adapter.ts:141-154 重建模式），不再无条件 `delete (attrPatch).children`。根因：`diffScadaConfig` 顶层迭代 → 嵌套子图元 fill 变更 / 嵌套 junction connection 写入产 `{id:group, patch:{children:[…]}}`；旧 applyUpdate 删 children → canvas 停旧态而 workingConfig 已前进（WYSIWYG 断裂，仅全量 engine.build 能恢复）。failing-first 4 测（canvas 层 `engine.getSymbol('inner-1').node.get('fill')` / 嵌套 junction stub 数 / 孙级增删重建 / 顶层无 children 回归）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 2（P1-4 结构 mutator 嵌套解析）**：`editor/runtime-mutators.ts` 三个结构 mutator 改递归解析：`removeWorkingSymbol`（新 `removeNodeRecursive` walker 从任意深度解链）/ `groupSymbols`（`collectAllSymbols` 递归收集选中节点 + `detachNodesRecursive` 从原父递归解链，混合顶层+嵌套不丢项）/ `ungroupSymbols`（`findNodeInWorking` 递归定位 group + `ungroupRecursive` 在任意深度拆解，子图元提升进父 children 槽位）。根因：旧实现仅 `.filter`/`.find` 顶层 symbols，嵌套 id 不匹配 → 静默 no-op（与 selectionNodes 经 collectAllSymbols 解析嵌套 id 的纪律不对称）。failing-first 5 测（嵌套 delete 解链+engine tree 移除+非空 undo / undo 恢复嵌套子图元 / 混合 group 不丢嵌套 / 同父两嵌套子 group / 嵌套 ungroup 提升）+ 2 顶层回归守卫先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 3（P1-5 解耦 world/DOM size + resize 重应用 fit）**：组合策略 (a)+(b)。(a) `engine/scada-engine.ts` + `editor/renderer/editor-engine.ts` 构造器容器真实 `clientWidth/clientHeight` 优先，schema width/height 仅 fallback（jsdom 容器 0 尺寸时测试仍可用）；`appConfig.width/height` 用解耦后的值（不再把 schema width 当 leafer DOM 尺寸）。(b) `renderer/hooks/use-scada-engine.ts` + `editor/renderer/hooks/use-editor-engine.ts` 的 ResizeObserver handler 在 `setSize` 后调 `runtime.refitViewportOnResize?.()` 重应用声明 fit policy；refit 数学抽到 `use-scada-config-sync.ts` 的 `applyScadaViewportPolicy`（从 `applyInitialViewport` 拆 bounds→viewport 核心），runtime + editor 共用避免双实现漂移；refit 注册经稳定 setter `setResizeRefit`（react-compiler/immutability 友好，不 mutate 传入 ref）。`scada-editor-canvas.tsx` 同型修复（viewport policy 不再 mount-once，resize 时经 setResizeRefit 重挂 refit 闭包）。无声明 policy 时 refit 短路 no-op（用户 viewport 保留）。failing-first 2 测（schema width:960 + fit:contain → resize 302×200 → getViewportPoint 落画布内 / 无 policy → 用户 viewport 保留）+ 编辑器 2 测先于 Fix 验证失败，Fix 后转 pass。
  - 新增 `runtime-mutators-nested.test.ts`（7 测，复用 editor-state-integrity harness）+ `renderer/scada-canvas-viewport-resize.test.tsx`（2 测，controllable ResizeObserver）+ `editor-engine.test.ts` 增 P1-2 canvas 层 4 测 + `scada-editor-canvas-viewport-region.test.tsx` 增 P1-5 编辑器 2 测。
- **验证**：`pnpm typecheck` workspace 32/32 · `pnpm build` 32/32 · `pnpm --filter @nop-chaos/flux-renderers-industrial lint` PASS · `pnpm test` workspace 59/59（industrial **1373 tests** = 1358 基线 + 15 新 failing-first，零回归；coverage branches 90% 阈值通过）。公共导出面（`ScadaCanvasRuntime`/`engine` 命令签名 / `viewport` schema 契约）不变；新增 `applyScadaViewportPolicy`/`removeNodeRecursive`/`detachNodesRecursive`/`ungroupRecursive`/`setResizeRefit` 均 internal-only。
- **closure audit**：_待 fresh session 独立 closure-audit 轮（AGENTS.md「执行 session 不得自审」，closure-audit gate 留 [ ]）_。
- **plan 收口**：`2026-08-08-1809-3` Plan Status `active` → `completed`，3 Phase status + Exit Criteria + Closure Gates（机械 typecheck/build/lint/test + 3 P1 landed + canvas 层测 + resize 集成测 + perf 未退化 + no silent deferral）勾选；Closure Gates 仅留「独立 fresh session closure-audit」项 [ ]。无 `> Source Audits:` 行（`> Source:` 指 audit 但非该 label；且 source audit 驱动 6 个 sibling plans 1809-1/2/3 + 1910-1/2/3，不可仅凭 1 plan 关闭整文件 Audit Status），source audit closure 步骤省略。roadmap Follow-up Backlog「第一波执行进度」补 `1809-3` ✅ completed。
- **owner doc**：No update required —— `design-renderer.md` §4.1 viewport contract（fit/center policy）+ `design-architecture.md` engine sizing + `design-undo-redo.md` §4.3 group/ungroup 描述的是 _intended_ 行为（嵌套子图元编辑应反映、响应式视口应 fit）；三 P1 Fix 均为 closing code↔doc gaps（使 live 行为对齐文档既有契约），未改文档化行为契约。递归 diff 优化（保留 `diffScadaConfig` 顶层-only，applyUpdate 端递归重建）归 Non-Blocking Follow-ups。
- **successor**：第二波 `2026-08-08-1910-{1,2,3}`（A1 P0/A11/A12 + A2/A6/A7/A8 + A3/A4）待执行；递归 diff / scada-perf:155 视口归因（源 audit 标「plausibly」）归 follow-up backlog。

### 2026-08-08（plan 2026-08-08-1809-2 industrial SCADA editor state & lifecycle integrity 执行）

- **plan `2026-08-08-1809-2`（编辑器协作状态持有者在边界上不对账）执行完成**——把 `flux-renderers-industrial` 编辑器 4 个「外部 config / 操作纳入 working copy 时，session / adapter / selection / id 计数器没有单一 owner 在每个边界做全量对账」P1 收口，3 Phase 全交付。源 audit `docs/audits/2026-08-08-1712-open-audit-*.md`（F1/F3）+ `2026-08-08-1712-multi-audit-*.md`（P1-1/P1-3）。
  - **Phase 1（F1 回归锁定 Proof + F3 拖拽 id 去重 Fix）**：F1——`editor-session.test.ts` 增缺失-variables 回归用例（`createScadaEditorSession({version:1,symbols:[{id:'x',...}]})` 不抛 + symbols 深度相等 + variables undefined），锁定 commit `c2dd1627b` 守卫防回归（既有 5 fixture 恒带 variables，缺失路径从未测）。F3——`runtime-mutators.ts` 把 id 去重收敛进 `addWorkingSymbol` 单一 owner（新增 `resolveUniqueNodeId` helper：解析尾随数字碰撞自增 `${base}-${n}`，无数字从 1 起；与 `groupSymbols`/clipboard paste 的 do/while 同形），覆盖 drop / palette click / test handle / component handle 全部 add 路径（不再依赖组件内不重置的 idCounter）。failing-first 用例（load(rect-1) → addWorkingSymbol(rect-1) → 新 id ≠ rect-1 + 无重复）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 2（P1-1 事务边界对账 Fix）**：`runtime-mutators.ts` `load()` + `toolbox-runtime.ts` `importConfigFn` 在 `resetSession` **之前**调 `undoRedo.abortTransaction()`。根因：`resetSession` 清栈但不清 `UndoRedoAdapter` 的 `inTransaction`/`prevAtOpStart`；拖拽途中 programmatic load 打断时，pointerup 的 `commitTransaction` 用 pre-load 快照 diff post-load working copy 产巨型 bogus entry → undo 还原到错误 pre-load config。failing-first 2 测（load/importConfig：beginTransaction → load/import → 断言 `isInTransaction===false` / `canUndo===false` / 随后 commitTransaction 返回 undefined 不推 entry）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 3（P1-3 undo/redo selection 修剪 Fix）**：`runtime-mutators.ts` `applyUndoRedoDiff` 成功 `commit()` 后，用 `collectAllSymbols`（递归，前向兼容 plan {3} P1-4 嵌套 id）收集现存 id 集，修剪 `session.selection`；调 `setSessionSelection(pruned)` 统一同步 canonical + React mirror + 触发 `onSelectionChange`，空集 `engine.clearEditorSelection()` / 非空 `engine.setEditorTargets(resolvedNodes)`。根因：undo 一个 add（或 redo 一个 remove）后 selection 持有已不存在的 id → inspector/toolbox 在死 id 上静默 no-op。failing-first 3 测（undo 修剪 + 死 id 后续 removeWorkingSymbol 干净 no-op 不推 entry / redo 修剪 / live selection 不误触发）先于 Fix 验证失败，Fix 后转 pass。
  - 新增 `editor-state-integrity.test.ts`（8 测，复用 `runtime-error-propagation.test.ts` harness：真 engine+session+ctx+mutators+toolbox，跨 data↔engine 两层断言可观测结果）。
- **验证**：`pnpm typecheck` workspace 32/32（industrial 1 cache miss 实跑通过）/ `pnpm --filter @nop-chaos/flux-renderers-industrial build` PASS / `pnpm --filter @nop-chaos/flux-renderers-industrial lint` PASS / `pnpm test` workspace 59/59（industrial **1358 tests** = 1349 基线 + 9 新，零回归）。公共 runtime 命令面（addWorkingSymbol/load/importConfig/undo/redo）签名不变；`resolveUniqueNodeId` internal-only。
- **closure audit**：_待 fresh session 独立 closure-audit 轮（AGENTS.md「执行 session 不得自审」，closure-audit gate 留 [ ]）_。
- **plan 收口**：`2026-08-08-1809-2` Plan Status `active` → `completed`，3 Phase status + Exit Criteria + Closure Gates（机械 typecheck/build/lint/test + 4 Fix/Proof landed + no silent deferral + owner-doc No-update-required）全勾；Closure Gates 仅留「独立 fresh session closure-audit」项 [ ]。无 `> Source Audits:` 行（`> Source:` 指 audit 但非该 label；且两份 1712 audit findings 映射 6 个 sibling plans 1809-1/2/3 + 1910-1/2/3，不可仅凭 1 plan 关闭整文件 Audit Status），source audit closure 步骤省略。roadmap Follow-up Backlog「第一波执行进度」补 `1809-2` ✅ completed。
- **owner doc**：No update required —— `design-undo-redo.md:372`（load 重置 undoStack/redoStack）+ §4.2 事务语义 / `design-renderer.md` §4.5 load/reset / `design-architecture.md` selection 描述的是 _intended_ 行为；三 Fix 均为 closing code↔doc gaps（使 live 行为对齐文档既有契约），未改文档化行为契约。
- **successor**：同波 `1809-3`（canvas correctness，P1-2/P1-4/P1-5）待执行；F6/F11/P2-4 归 P2 follow-up。

### 2026-08-08（plan 2026-08-08-1809-1 industrial SCADA validation & input-defense 执行）

- **plan `2026-08-08-1809-1`（industrial SCADA 校验层 F2+F4 防御补全）执行完成**——把 `flux-renderers-industrial` 序列化校验层两类「防御只做了一半」缺陷收口，2 Phase 全交付。源 audit `docs/audits/2026-08-08-1712-open-audit-industrial-hmi-component-audit.md`（F2=A10 / F4=A9）。
  - **Phase 1（F2：assertShape finite 数值校验对齐）**：`packages/flux-renderers-industrial/src/serialization/validators/helpers.ts` 新增 `isFiniteNumber(v)` 共享 helper（`typeof==='number' && Number.isFinite`）；`assertShape` 的 `'number'` 分支由 `typeof v==='number'` 改为 `isFiniteNumber(v)`，错误文案对齐 `must be a finite number`；`checkNumberField` 复用同一 helper（消除「同类 finite 修复只做一半」——P2-3 之前只落 checkNumberField）。3 个 call site（`validate.ts` background.grid.size / `animation.ts` from-to 对象形态 / `point-declaration.ts` scale.k-b）malformed 数值（NaN/±Infinity，`JSON.parse('1e400')→Infinity`）现被拒。failing-first 4 条（grid.size=1e400/NaN、animation.from={x:NaN}、scale.k=1e400）先于 Fix 验证失败，Fix 后转 pass。
  - **Phase 2（F4：validate 广度/总量上限 DoS 早退）**：`helpers.ts` 增常量 `MAX_SYMBOLS=200_000` / `MAX_VARIABLES=200_000` / `MAX_TOTAL_NODES=200_000`（benchmark 10 万图元上限 `docs/analysis/industrial-hmi/benchmark-report.md` §1 的 2× 留余量；Decision 取 2× 统一而非草拟 500k，控回归用例成本，距 DoS 百万量级两个数量级）。`validate.ts` 顶层 `symbols.length > MAX_SYMBOLS` / `variables.length > MAX_VARIABLES` → push 结构化 error 并 **O(1) 早退跳过 forEach**（不遍历）；`symbol-node.ts` `validateSymbolNode` 增 `counter: NodeCounter` 透参，每节点计数 +1，超 `MAX_TOTAL_NODES` push error（`exceeded` flag 仅一次）并 return（不递归 children，与 `MAX_VALIDATE_DEPTH` 早退同纪律）。failing-first 5 条（sparse symbols/variables 超阈 + dense children 总量超阈 + 阈内边界/零回归）先于 Fix 验证，Fix 后转 pass。
- **验证**：`pnpm --filter @nop-chaos/flux-renderers-industrial typecheck/build/lint` 全绿；`pnpm typecheck` workspace 32/32；`pnpm test` workspace 59/59（industrial **1349 tests** = 1340 基线 + 9 新 failing-first，零回归）。公共导出面（`validateScadaConfig` / `ScadaValidationResult`）签名不变；新增 `isFiniteNumber`/`MAX_*`/`NodeCounter` 均 internal-only（`src/index.ts` / `serialization/index.ts` 不导出）。
- **closure audit**：**PASS（fresh session `ses_01ece5f09ffelaMWMFqJ5u73pE`，非执行 session 自审）** verdict=`approved`（0 Blocker / 0 Major / 1 out-of-scope Minor）。独立 live 复核：F2（assertShape number 分支 `isFiniteNumber` + 文案 + 3 call site 全路由）/ F4（validate.ts else-if 早退跳 forEach + symbol-node 计数早退不递归）/ 公共面 intact / 9 测真 failing-first 断言结果值 / deferred 诚实 / 200k 阈值 defensible。Minor（animation `from`/`to` 裸数值形态 `continue` bypass finite，F2 邻接缺口）显式 out-of-scope（本计划 scope=assertShape call site，裸数值不经 assertShape），已登入 plan Non-Blocking Follow-ups + roadmap Follow-up Backlog，可复用 `isFiniteNumber` 跟进。
- **plan 收口**：`2026-08-08-1809-1` Plan Status `active` → `completed`（closure audit PASS 后回写），2 Phase status + Exit Criteria + Closure Gates 全勾；Closure section 填 fresh session audit evidence。无 `> Source Audits:` 行（`> Source:` 指 audit 但非该 label；且 source audit 驱动 6 个 plan，不可仅凭 1 个 plan 关闭），source audit closure 步骤跳过。roadmap Follow-up Backlog 补「第一波执行进度」标注 1809-1 ✅。
- **successor**：同波 `1809-2`（editor state integrity）/ `1809-3`（canvas correctness）待执行；F10 / animation 裸数值 finite 收紧归 P2 follow-up（可复用 `isFiniteNumber`）。

### 2026-08-08（plan 2026-08-08-0748-1 HCA-CG Guard 沉淀 执行）

- **plan `2026-08-08-0748-1`（HCA-CG Guard 沉淀）执行完成**——industrial HMI component audit mission 最后一个 work item 收口。把审计产出沉淀为仓库永久 guard（CI 机械拦截 + 文件行数治理 + flux-guide 裁定），3 Phase 全交付。
  - **Phase 1（validate.ts 拆分落地，纯重构行为等价）**：`packages/flux-renderers-industrial/src/serialization/validate.ts` 537→72 行拆分为瘦主入口（`validateScadaConfig` + `export type ScadaValidationResult` + import）+ 新增 `validators/`（`helpers.ts` 共享 helper/常量含 `MAX_VALIDATE_DEPTH` 单源 + 6 per-shape 校验器 binding/animation/state-declaration/symbol-event/symbol-node/point-declaration + `index.ts` re-export）+ `legacy-scan.ts`（scanLegacyAtSyntax/scanSymbolLegacy/LEGACY_AT_PATTERN）。全 ≤152 行（WARN 桶清零），公共导出面签名与导出位置不变（validate.ts 不在 index.ts 公共导出，消费方经 internal path `../serialization/validate.js`），`serialization-validate.test.ts` 655 行 import 不变（`./validate.js`），1340 tests 零偏差。
  - **Phase 2（canvas wrapper a11y CI guard，HCAX-2 沉淀）**：新增 `scripts/audit/canvas-a11y-rules.mjs`（`canvasWrapperA11yRules` + allowlist pin 两 industrial canvas renderer scada-canvas/scada-editor-canvas，flow-designer 显式排除 role="button" 另立契约；scanCanvasWrapperA11y 用 data-slot 锚定 host 元素 opening tag，string/brace/comment 状态机穿越 expr handler，逐 contract attr 校验）+ `find-canvas-wrapper-a11y-gaps.mjs`（薄壳）。`rules.mjs` import + re-export + spread 进 `allAuditSuspectRules`（CI 可见，关键）；root `package.json` 注册 `check:audit-canvas-wrapper-a11y`。failing-first proof 通过（删 role="application" → CI path discover-audit-suspects + 专用脚本双命中），合规态 0 suspect。实现注记：rule 定义抽到独立模块（mirror react19-rules.mjs 模式），避免 rules.mjs 626→779 越过 700 ERROR 桶（接入后 rules.mjs=628 仍 WARN 桶）。
  - **Phase 3（flux-guide I15.2/E9.2 裁定 + guard 核对收口）**：I15.2（文档维度）/E9.2（性能维度）裁定 = **option (a) 已覆盖**（`scada.md` 字段参考 :138-149 + 性能注意 :123-127；`scada-editor.md` 字段参考 :88-100 + perf :75/:100），证据记录于 plan，不改 flux-guide 文件。HCA-LL pre-landed guard 项 spot-check 无回退（checklist §2.1 IND-1~6 / deep-audit industrial calibration / markers canvas a11y / 9 bug 卡 77–85 全 live）。审计卡汇总索引 2 张 industrial 卡 closed。
- **验证**：`pnpm typecheck` 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32 · `pnpm test` 59/59（industrial 100 files / 1340 tests，与 HCA-CV closure 基线零偏差）。canvas a11y rule failing-first proof + 合规态 0 suspect 双确认。（注：`pnpm check` 整体因 14 个 pre-existing >700 行文件于 baseline 即红，非本 plan 引入；本 plan rule 在合规态产 0 suspect、不引入新 fail，rules.mjs 保持 WARN 桶。）
- **closure audit**：**PASS（fresh session `ses_01f4c2374ffekZutASrHvb2T3i`）**（AGENTS.md「closure audit 须 fresh session，执行 session 不得自审」）。独立 auditor 逐条 live 核对：Phase 1 behavior-equivalence（MAX_VALIDATE_DEPTH 单源 helpers.ts 被 symbol-node.ts + legacy-scan.ts 共用无 drift + 1340 tests 零回归）/ Phase 2 CI guard（allAuditSuspectRules spread + 独立 failing-first proof 执行 + 文件精确还原）/ Phase 3 adjudication honesty（flux-guide 真覆盖 + HCA-LL guard 全 live）/ no silent deferral（14 ERROR 全 pre-existing，rules.mjs 不在其中）/ full green re-run。roadmap §HCA-CG `planned` → `done` 已回写（含 header `最后更新` + dependency graph CG✅ + Phase Details 完成摘要）。
- **plan 收口**：`2026-08-08-0748-1` Plan Status `active` → `completed`（closure audit PASS 后回写），3 Phase status + Exit Criteria + Closure Gates 全勾；Closure section 填写 fresh session audit evidence。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。Mission（industrial HMI component audit）最后一个 work item 收口。

### 2026-08-08（plan 2026-08-08-1527-2 HCA-LL Lesson 总结 执行）

- **plan `2026-08-08-1527-2`（HCA-LL Lesson 总结）执行完成**——纯文档/skills 沉淀计划（无代码变更），3 Phase 全交付。把 HCA1–HCA11 + HCAX-1/2 全层审计的架构/工程经验 + HCA-BL 9 bug 卡（77–85）+ HCA-CR 24 residual 裁定表提炼为 durable 防复发工件，让未来 industrial 包（及同类 canvas 场景图/编辑器子系统）审计直接复用。
  - **Phase 1（Lesson 目录编制 + 裁定）**：从 9 bug 卡 + 各层审计 + HCA-CR residual 表提炼 **16 候选**，逐条裁定终态落「裁定结果」表——**15 沉淀**（落 checklist v2 §2.1 / deep-audit / 架构文档）+ **1 不沉淀**（文件行数，理由：已被 dim 02 + `check:oversized-code-files` 硬门禁 + HCA-CG 工具治理覆盖）。roadmap §HCA-LL 6 主题全覆盖（a11y/schema/error code/四态/React19 沉淀；文件行数显式裁定不沉淀+理由）。补 8 缺口主题（canvas 全量重建 parity / 符号库 applyProps 路由 / 序列化三向类型同步 / undo-redo 合并窗口不变量 / import-load 跨点 parity / custom 深克隆隔离 / 裁定方法论×3 源自 HCA-CR residual）。
  - **Phase 2（沉淀 durable 工件 + 三向回链）**：
    - `docs/audits/component-audit-checklist.md` **v2 增 §2.1 industrial 专项维度**（IND-1 canvas 引擎 / IND-2 绑定管线 / IND-3 序列化 / IND-4 符号库 / IND-5 编辑器子系统 / IND-6 跨切）每维度含检查点 + bug 卡回链；§3.1 增裁定方法论（防御纵深 vs 真实缺陷 / 公共 API footgun / cosmetic-latent）；「18 维↔23 维关系」子节同步 §2.1。
    - `docs/skills/deep-audit-prompts.md` 项目校准说明增 industrial 指针块（指向 §2.1，不重复维护第二份清单）+ 维度 07/19/20 增 industrial 包级提示（useCallback canvas 生命周期 / error code 升级码 vs 命令句柄码 / canvas role="application"）。
    - `docs/architecture/renderer-markers-and-selectors.md` 补 **canvas / scene-graph interaction surfaces** a11y gap（唯一 live doc gap：原文档 role 仅覆盖 button/dialog/tab，无 canvas `role="application"` 指引）。
    - 三向回链：9 bug 卡（77–85）「Notes For Future Refactors」各回链对应 lesson 落点；沉淀条目（checklist §2.1 + deep-audit）回链源 bug 卡。抽查双向可追溯（bug 78↔L-A11Y-1 / bug 79↔L-SCHEMA-1 / bug 85↔L-ED-3）。审计记录「喂入 HCA-LL」节不存在 → 该回链子步骤 n/a。
  - **Phase 3（roadmap 状态 + 收口）**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` §HCA-LL `todo` → `planned`（draft review pass-with-minors）+ Phase Details 补完成摘要 + 框架复用表 checklist 行 + 审计维度对照节同步 §2.1；header `最后更新` 同步。
- **验证**：纯文档计划（Closure Gates 已删 `pnpm test/lint/typecheck/build`）；执行后跑 `pnpm typecheck` 32/32 + `pnpm test` 59/59（FULL TURBO，zero 源码偏离）确认无回归。（未跑 e2e；本 plan 无代码变更，e2e full-green 不适用。）
- **closure audit**：**PASS（fresh session）**（AGENTS.md「closure audit 须 fresh session，执行 session 不得自审」）。Closure Gates 执行可验证项 1–5 勾选（catalog 终态 / 落点 live / 6 主题覆盖 / 三向回链 / 无 gap 静默跳过）；第 6/7 项（roadmap `done` + closure-audit-by-fresh-session）由 fresh session 独立复核 live repo 后勾选。fresh session 逐条 grep/read 核对：§2.1 IND-1~IND-6 + §3.1 裁定方法论 live、deep-audit 维度 07/19/20 industrial 提示 live、`renderer-markers-and-selectors.md` canvas a11y gap（lines 79-84）live、9 bug 卡（77–85）三向回链双向可追溯（bug 78↔L-A11Y-1 / bug 79↔L-SCHEMA-1 / bug 85↔L-ED-3 + 其余 6 张）；anti-hollow + deferred-honesty + 五点一致性全通过。roadmap §HCA-LL `planned` → `done` 已回写（roadmap line 64 + header `最后更新`）。
- **plan 收口**：`2026-08-08-1527-2` Plan Status `completed (execution)` → 归一为 `completed`（closure audit PASS 后 fresh session 回写），3 Phase status + Exit Criteria + Closure Gates 全勾；Closure section 填写 fresh session audit evidence。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。
- **successor**：HCA-CG（Guard 沉淀，`todo`）将 consume 本 plan 的 checklist v2 §2.1 增量 + 做 validate.ts 拆分 + 工具脚本升级；HCA-CV（全量验证）已 done。

---

### 2026-08-08（plan 2026-08-08-1527-1 HCA-CV 全量验证 收口）

- **plan `2026-08-08-1527-1`（HCA-CV 全量验证）收口完成**——3 Phase 全交付。industrial 包审计 mission 级**纯验证收口 plan**：在 HCA1–HCA11 + HCAX-1/2 + HCA-BL + HCA-CR 全部代码修复落地后，做 workspace 全量 `pnpm typecheck/build/lint/test` + 6 scada e2e spec + I14/E9.2 perf 包络复测，对失败项诚实裁定（audit 回归 → Fix；非 audit → watch-only residual），不改 industrial 包 supported 行为。完成后解锁 HCA-CG（Guard 沉淀）。
- **Phase 1（workspace 全量静态验证）**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（59/59 tasks）；`pnpm --filter @nop-chaos/flux-renderers-industrial test` **100 test files / 1340 tests**——与 HCA-CR closure 基线（100/1340）**精确匹配，零 unit 回归**。FULL TURBO cache replay（cache key 未变 = 无源码偏离）。
- **Phase 2（6 scada e2e + triage）**：首轮 23 fail 根因为**陈旧 dev server**（PID 32950 cwd=`nop-chaos-flux-master` sibling worktree，经 `reuseExistingServer:!CI` 复用，服务无 scada 路由的主分支首页）——kill 后复测 24 pass / 7 fail。程序化诊断（`page.evaluate`，禁截图）：overlay 机制正常（手动 `highlight('pump-1')` → activeCount=1）但 hover 事件未发射——mouse 坐标落在 canvas DOM box 外（box width 302px，pump-1 world x=350 → screen x=816 > box 右边界 769）。归因 08-06 **非 audit** 提交 `9a8a6f38` "SCADA demo 视觉重设计（三区布局）" 收窄 canvas（声明 960 / 渲染 302），在 hover 测试最后全绿（08-04 `cb2e753a` "scada-\* e2e 23/23"）之后。`scada-perf:155` fail 非 perf 突破（FPS pointer-rAF 50.3 / throughput 49.5 均 ≥45 达 I14），仅 TE-1 pointer-drag-viewport guard（同类 pointer/canvas-interaction）。**HCA audit diff 全量复核**：HCA\* feat 提交仅触及 connection-wiring/undo-redo-adapter/scada-engine(importConfig→reset)/validate/editor-helpers/pipe-junction/dirty-collector+refresh-pipeline(import 重构)/editor palette+renderer-definitions/scada-canvas a11y(仅 role+aria-label)/config-types/diff，**均不触及** canvas-sizing/viewport-fit/hover-event-bridge/hit-testing/pointer-drag-pan。裁定：**0 audit 回归**，7 failures 全部 non-audit watch-only residual。
- **Phase 3（perf 包络复测 + CV 报告）**：I14 runtime 六项全达标（首屏创建 348ms<2s / pointer fps 50.3≥45 / throughput 49.5≥45 / 内存 stroke 131.7MB≤320 / no-stroke 129MB≤320 / 1万点刷新 43.6ms<200ms，与 I14.3 基线同量级）；E9.2 编辑态三项全达标（编辑操作 <100ms / 拖拽 fps ≥30@1k / 内存 ≤320MB，3/3 test green）。无 envelope 退化，无阈值放宽（R7 维持待人工最终确认）。产出 CV 报告 `docs/analysis/industrial-hmi-component-audit/hca-cv-verification-2026-08-08.md`（四节齐全 + 与 HCA-CR closure diff）。
- **closure audit**：独立子 agent fresh session `ses_01f976451ffeyxrjLAqE5BeqDH`（three-piece set 独立审计，非执行 session 自审）verdict=`pass`——零 Blocker/Major/Minor。live 复核 static 32/32·32/32·32/32 + industrial 100/1340；triage 诚实性确认（`9a8a6f38` 非 audit，audit canvas diff 仅 a11y 不触及 sizing/hover/hit；0 audit 回归静默降级）；perf 阈值原样未放宽；zero 源码改动（仅 docs）；Closure Gates 10/10。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA-CV `todo` → `done`；§HCA-CV Phase Details 补完成摘要（101 source / 100 test files / 1340 tests）；header `最后更新` 同步。解锁 HCA-CG。
- **successor**：HCA-CG（Guard 沉淀，`todo`，前置已满足）；industrial successor（非 HCA-CG scope）修复 08-06 三区布局 canvas sizing 回归。

### 2026-08-08（plan 2026-08-08-1430-2 HCA-CR 跨层集中修复与裁决 收口）

- **plan `2026-08-08-1430-2`（HCA-CR 跨层集中修复与裁决）收口完成**——3 Phase 全交付。industrial 包审计的**收敛 owner plan**：把 HCA2/HCA4/HCA5/HCA6/HCA8/HCA9/HCA10/HCA11 各层审计「归 HCA-CR」的全部 deferred P2/P3 项 + 2 watch-only residual 汇总成单一 triage 表，逐项做出最终裁定（Fix / residual / out-of-scope），对 Fix 项 test-first 修复并带回归。完成后解锁 HCA-CV 全量验证。
  - **Phase 1（Backlog 全量汇总 + 逐项裁定）**：从各层审计记录 Finding Triage 拉取精确措辞（HCA5 P3×3 / HCA8 P3×11 等），落「裁定结果」表。全量 backlog 逐项终态：**5 Fix**（1 P2 HCA11-P2-2 + 4 P3：HCA2-P3-ENG-1 / HCA4-P3-1 / HCA4-P3-2 / HCA11-P3-1）+ **24 P3 residual** + **2 watch-only residual 复核维持**（#1 / #5）+ **2 out-of-scope**（HCA5-P3-2 HCA6 已解 / HCA5-P3-3 已记录非新缺陷）。零「optional/consider」模糊措辞；唯一 P2 必须修复。
  - **Phase 2（Fix 项 test-first 修复）**：
    - **HCA11-P2-2**（`connection-wiring.ts:58-60` pointerup 挂 container，释放于容器外时 connectionDragActiveRef 卡 true + overlay 残留）：failing-first 4 测（新 `connection-wiring.test.ts`，断言 window pointerup 后 `connectionDragActiveRef===false` + cleanup 复位，修复前 `expected true to be false`）→ pointerdown 命中 junction 后挂 `window.addEventListener('pointerup', endConnectionDrag, { once: true })`（对齐 `editor-adapter.ts:80`）+ 移除 container pointerup + cleanup 兜底 removeEventListener + ref=false → 转绿。15 条 connection 集成测试不回归（canvas pointerup bubbles→window）。
    - **HCA2-P3-ENG-1**（`scada-engine.ts:173-189` 公共命令 API 缺 destroyed 门控）：failing-first 3 测（`scada-engine.test.ts` destroyed guard，断言 destroy 后 reset→registry 保持 0 / viewport 命令不变 / applyDiff 不抛，修复前 registry=2 / viewport 变 / throw `config adapter is not built`）→ reset/applyAttrs/fit/center/setViewport/zoomAt/setSize/applyDiff 增 `if (this.destroyed) return`（viewport 命令返 `{...this.viewport}`）→ 转绿。
    - **HCA4-P3-1**（`validate.ts:287` 字符串校验循环遗漏 `align`）：failing-first 2 测（reject `'diagonal'` / accept `left|center|right`，修复前 `expected true to be false`）→ validateSymbolNode 增 align 枚举校验 → 转绿。
    - **HCA4-P3-2**（`validate.ts:448-451` background.grid 子形状未校验）：failing-first 3 测（bad size / bad color / well-formed，修复前 `expected true to be false`）→ background 校验增 grid assertShape（size:number/color:string）→ 转绿。
    - **HCA11-P3-1**（`undo-redo-adapter.ts:253-256` cloneNodeDeep 未深克隆 custom，与 P2-1/editor-session 同型残留）：failing-first 2 测（事务期间 in-place 改 custom 被 diff 捕获 + inverse 持原值，修复前 `patch.custom` undefined）→ cloneNodeDeep 增 `structuredClone(node.custom)` → 转绿。
  - **Phase 3（Residual 裁定回写 + 回归抽查）**：24 residual 项逐项写本 plan「Deferred But Adjudicated」（Classification + Why-Not-Blocking + Successor=no，除 #5 标 successor=yes 待需求升级）+ 各层审计记录回写 `residual-adjudicated at HCA-CR`；Fix 项回写 `fixed at HCA-CR` + 落点。构成依赖回归抽查 ✅（全 workspace test green 覆盖 HCA9 connection 算法 / HCA11 adapter 事件序 / 防御 guard 不误拒合法输入）。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（59/59 tasks；industrial 100 files / 1340 tests，+1 file connection-wiring.test.ts / +14 regression：4 P2-2 + 3 ENG-1 + 5 P3-1/P3-2 validate + 2 P3-1 undo-redo-adapter）。（未跑 e2e；e2e full-green 验证归 HCA-CV work item。）
- **closure audit**：独立子 agent fresh session `ses_01fc7ae3affepjO1mMYegbl7vk` verdict=`PASS`——A-E 五维独立 live 核验（5 fix 正确性 + failing-first 断言结果值 / 裁定完整性 33 项全终态零模糊 / 审计记录 fixed/residual 状态回写 / 回归风险 connection bubbles 推理 + destroyed guard 不拒合法 / scope 纪律仅 4 源文件 + 5 测试文件无新公共面 + validate.ts 拆分未做归 CG）。1 Minor（residual 计数标签 17→24）已修正后通过。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA-CR `planned` → `done`；§HCA-CR Phase Details 补完成摘要；header `最后更新` 同步。解锁 HCA-CV。
- **plan 收口**：`2026-08-08-1430-2` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾（closure-audit gate 经独立子 agent fresh session PASS 后勾选，执行 session 不自审）+ Closure section 填写。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。

---

### 2026-08-08（plan 2026-08-08-1430-1 HCA-BL Bug 总结 收口）

- **plan `2026-08-08-1430-1`（HCA-BL Bug 总结）收口完成**——纯文档计划（无代码变更），2 Phase 全交付。把 HCA1–HCA11 + HCAX-1/2 各层审计中符合 `docs/bugs/00-bug-fix-note-writing-guide.md` 归档判据的 bug 归档为 `docs/bugs/NN-industrial-hmi-component-audit-*.md` 系列并回链各审计记录「喂入 HCA-BL」节。
  - **Phase 1（候选裁定）**：逐候选独立套用 writing-guide 四判据，落「裁定结果」表。13 候选 → **9 张卡归档（77–85）+ 3 类留痕（HCA1/HCA9/HCA7）**。4 个候选从源审计「留痕」**升级**为归档（HCA2 P2-ENG-1 / HCA10 P1-1+P2-1 合并 / HCA11 P1-1 / HCA11 P2-1），均命中「非显然根因 + 回归测试 + 易被重构再引入」（writing-guide 不要求必须跨包），anti-slacking 安全机制显式（plan M-1）。HCA10 P1-1+P2-1 合并为单卡（同模块/同验证路径/同重构风险面）；HCA11 P1-1/P2-1 独立（根因/机制/parity 主题不同）。
  - **Phase 2（写卡 + 回链）**：写 9 张卡（`docs/bugs/77–85-industrial-hmi-component-audit-*.md`），每卡八节齐全（Problem / Diagnostic Method / Root Cause / Fix / Tests / Affected Files / Notes For Future Refactors）+ `文件:行` 证据经 live 核对 + failing-first 回归测试引用经核对存在（14 测试文件 + `scripts/check-scada-symbol-keys.mjs`）。回链闭环：9 份日期审计记录（hca2/3/4/5/6/8/9/10/11）+ 2 张 per-component 卡（scada-canvas/scada-editor-canvas）+ closure plan（1316-1）+ roadmap HCAX-1/HCAX-2 行均从「候选」更新为「已归档 NN / 已裁定留痕」，无残留候选状态。
  - **编号**：`docs/bugs/` 当前最大编号 76，自 77 起递增；slug 前缀统一 `industrial-hmi-component-audit-`。
- **closure audit**：独立子 agent fresh session `ses_01fe44fb6ffeyodZdG0PPmrYDM` verdict=`approved`（三件集输入）。6 项核对全 PASS：裁定表覆盖全部 13 候选无 optional / 9 卡八节齐全抽查 live file:line 真实 / 14 回归测试文件存在 plausibly 覆盖 / 9 审计记录 + 2 per-component 卡回链闭环无残留候选 / 4 升级均有 writing-guide 判据理由 / 无候选被静默丢弃。1 Minor（card 85 `runtime-mutators.ts:17` → 实际 `:196` save 内）已修正后通过。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA-BL `planned` → `done`；HCAX-1/HCAX-2 行补 bug 卡回链；dependency graph BL 节点 ✅；header `最后更新` 同步。
- **plan 收口**：`2026-08-08-1430-1` Plan Status `active` → `completed`，2 Phase status + closure gates 全勾（closure-audit gate 经独立子 agent fresh session approved 后勾选，执行 session 不自审）+ Closure section 填写。纯文档计划，Closure Gates 已删 `pnpm test/lint/typecheck/build`。HCA-LL successor 依赖本 plan 产出。

---

### 2026-08-08（plan 2026-08-08-1316-2 HCA11 Editor infra 23 维包级深审 closure）

- **plan `2026-08-08-1316-2`（HCA11 Editor infra 审计）收口完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/editor/` 顶层 9 源文件（editor-session 130 / editor-adapter 205 / editor-working-helpers 162 / runtime-factories 261 / runtime-mutators 248 / toolbox-runtime 264 / connection-wiring 69 / test-handle-factory 130 / editor-test-handle 138，Σ1607 行）做 23 维包级深审（infra 胶水层非复杂交互层，dim 21-23 非必选；本次因 importConfig 接线 + clone 一致性疑点触发 dim 22 集成接线 / dim 23 测试有效性）。**HCA1–HCA11 全 done，解锁 HCA-BL/HCA-CR 收敛**（industrial 包最后一个待审计模块族收口）。
  - **Phase 1（逐文件深审 + finding triage + 边界值抽查）**：产出审计记录 `docs/audits/2026-08-08-1316-hca11-editor-infra.md`（9 文件逐文件 finding 表 + 边界值抽查表 11 行 + 维度覆盖统计 + 全 finding triage + `文件:行` 证据）。session 构造期不可变隔离（cloneConfig 含 P2 #4 custom 深克隆）/ adapter 事务节流（window 级 pointerup `{ once: true }` 捕获容器外释放）/ working 纯派生 + 联动 O(n+k) / runtime 闭包捕获一致性（ctx.notifySession 重赋 + ctx.save 回填时序）/ mutator 幂等（同载连续 remove/writeConnection diff 空不入栈）/ toolbox 命令接线（align/z-order/clipboard）/ connection 接线互斥 / test handle window 挂载-卸载清理 + 双态命名空间隔离——**零 P0；1 项 P1 + 2 项 P2 + 2 项 P3**。
  - **Phase 2（P1/P2 test-first fix）**：
    - **P1-1**（`toolbox-runtime.ts:221-241` importConfigFn 缺 engine.mode 同步——P1-08 parity mode desync）：`resetSession` 强制 session.mode='edit'，但 `engine.build` 不改 engine.mode（setMode 是唯一改 engine.mode 的路径）；importConfig 在 `engine.build` 后未调 `engine.setMode(session.mode)`，对照 `runtime-mutators.ts:219-221` load() 有该同步（P1-08 落地）。preview 态 importConfig → session.mode='edit' 但 engine.mode='preview'（desync）→ 画布不可编辑但 session 真相源/inspector/palette 认为 edit 态。failing-first proof（`scada-editor-canvas-ops.test.tsx` 断言 switchMode('preview')→importConfig→`engine.currentMode === 'edit'`，修复前 `expected 'preview' to be 'edit'`）→ importConfigFn 在 engine.build 后增 `if (engine.currentMode !== session.mode) engine.setMode(session.mode)`（与 load() byte-parity）→ 转绿。
    - **P2-1**（`editor-working-helpers.ts:79-83` cloneNodeDeep + `runtime-mutators.ts:191-202` save 浅克隆 custom——扩展 plan 2026-08-08-0900-1 P2 #4）：P2 #4 闭合了 `editor-session.cloneNode` 的 custom 深克隆，但同型浅克隆残留 cloneConfigSnapshot（mutator prevSnapshot + synced.config）与 save committedBaseline。save 浅 `symbols.map(n => ({...n}))` 共享 custom/children ref，违反 createScadaEditorSession/resetSession 已建立的 R5 Layer 2 隔离契约（editor-session.ts:30-31 doc）。failing-first proof 3 test（`editor-working-helpers.test.ts` 断言 cloneConfigSnapshot 顶层 + 嵌套 custom 隔离，修复前 `expected 0.99 to be 0.5` / `0.77 to be 0.1`；`scada-editor-canvas-ops.test.tsx` 断言 save 后 baseline.custom !== working.custom + in-place 改不泄漏，修复前 `expected {...} not to be {...}`）→ cloneNodeDeep 增 `if (node.custom) clone.custom = structuredClone(node.custom)`（对齐其 doc「deep-cloned」声明 + editor-session.cloneNode P2 #4 同纪律）+ save 改用 `cloneConfigSnapshot(session.workingConfig)` → 转绿。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-architecture.md`（session/adapter/runtime + INV-4 + R5）/ `design-toolbox.md`（§4.4 importConfig 契约 + §4.5 导入派发 onSessionChange）/ `editor-initiation.md`（R5 双态隔离）经逐条核对**无 drift**（P1-1/P2-1 修复后 live 匹配 doc，doc 本身正确无需改）。构成依赖回归抽查 ✅（HCA7 renderer 宿主消费 mountEditorEngine/createRuntimeCore/buildRuntimeMutators/buildToolboxRuntime/wireConnectionDrag/buildEditorTestHandle 装配链 + controlled-mode 推回 / HCA9 connection-wiring 接线互斥 + #1 watch-only residual 接线侧无新缺陷 / HCA10 ScadaConfigDiff 形状与 mutator/undoRedo 消费一致 + 两段式失败可回滚）。HCA-BL 喂入：本层无复杂/跨层 bug 候选（P1-1/P2-1 单概念局部 defect + 回归测试覆盖，按 bug 指南简单 bug 审计卡留痕即可）。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（industrial 99 files / 1326 tests，+1 file editor-working-helpers.test.ts / +7 regression：2 P2-1 cloneConfigSnapshot unit + 1 P2-1 save baseline + 1 P1-1 importConfig mode + 3 working-helpers regression）。（未跑 e2e；e2e full-green 验证归 HCA-CV work item。）
- **closure audit**：独立子 agent fresh session `ses_020036ae8ffe2TUR9zV3sOslGP` verdict=`approved`——9 点逐项 live code 复核（plan 内部一致 / 审计记录 9 文件 file:line + 全 triage / P1-1 fix `toolbox-runtime.ts:235-239` 与 load() byte-parity + 集成测试断言结果值 / P2-1 fix `editor-working-helpers.ts:82-85` + `runtime-mutators.ts:193` 与 `editor-session.ts:127` P2 #4 同 structuredClone 纪律 / 4 failing-first 测试断言结果值非 not.toThrow / 3 deferred（P2-2/P3-1/P3-2）诚实裁定无 P0/P1 伪装 / owner-doc 无 drift / src/ 无构建产物 / 独立重跑 vitest 99/1326 green）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA11 `planned` → `done`；dependency graph HCA11 标 ✅；§HCA11 节补完成摘要；header `最后更新` 同步。
- **plan 收口**：`2026-08-08-1316-2` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾（closure-audit gate 经独立子 agent fresh session pass 后勾选，执行 session 不自审）+ Closure section 填写。P2-2（connection-wiring pointerup 容器外卡死）+ P3×2 归 HCA-CR。

---

### 2026-08-08（plan 2026-08-08-1316-1 HCA1+HCA7 Renderer 层 closure audit 收口）

- **plan `2026-08-08-1316-1`（HCA1 scada-canvas + HCA7 scada-editor-canvas renderer 层 closure audit）收口完成**——3 Phase 全交付。对 industrial 包两个注册 renderer type（scada-canvas / scada-editor-canvas）的 18 维组件审计卡做独立 closure audit，把两张卡从 `fixed-pending-closure` 推进到 `closed`，roadmap §HCA1/§HCA7 从 `planned` 推进到 `done`。本 plan 不重做 18 维审计（已完成），只做收口验证 + 状态同步；closure 由两个独立子 agent（fresh session）分别完成。
  - **Phase 1（HCA1 scada-canvas closure audit）**：独立子 agent fresh session `ses_0202b17cbffeXMF0hmlW22VM6N` verdict=`pass`。逐条 live `文件:行` 复核：**P2-1** a11y LANDED（`scada-canvas.tsx:298-299` role="application" + aria-label；i18n `zh-CN.ts:935`/`en-US.ts:936` 双 locale）；**P3-1** 6 处 useCallback（`:94/:100/:135/:175/:218/:278`）确认 P3 recorded 非阻塞，归 HCA-LL/HCA-CR。18 维 tally 14 pass / 2 n-a / 2 fail（dim 8 fixed + dim 14 P3 recorded），无未分类/未修阻塞性维度。closure remediation：owner doc `docs/components/industrial-hmi/design-renderer.md` §10 补画布交互面 a11y 说明（drift 修正）；`scada-canvas-smoke.test.tsx` 增 role/aria-label DOM 断言（a11y 回归守护）。
  - **Phase 2（HCA7 scada-editor-canvas closure audit）**：独立子 agent fresh session `ses_0202ae215ffeqrZBtX2DOumfch` verdict=`pass`（先 `issues` 后 remediation 转 pass）。6 finding 全部 LANDED：**P1-1** schema region（`editor/schemas.ts:41,43,45` loading/empty/error + `:1` import SchemaInput + `renderer-definitions.ts:165-167` region 注册）；**P2-1** propContracts(7)+eventContracts(7)（`renderer-definitions.ts:30-149`）；**P2-2** a11y role/aria-label（`scada-editor-canvas.tsx:270-271` + i18n `zh-CN.ts:955`/`en-US.ts:957`）；**P2-3** disabled meta（`:217` disabled + `:272` aria-disabled + `:273` inert + `:275` drop guard + `:305` keyboard guard）；**P2-4（shared）** error code `config-invalid`（`:42` 与 runtime `scada-canvas.tsx:46` 一致，HCAX-1 done，`invalid-config` 为独立命令句柄失败码语义切分一致）；**P3-1** palette title=def.name（`editor-palette.tsx:51`）。HCAX-1/HCAX-2 共性 fix 在 editor 与 runtime 落地一致。closure remediation：owner doc `docs/components/industrial-hmi-editor/design-renderer.md` §4.1 schema 块 + §4.3 fields 表 + §10 marker 表（补 empty 行 + a11y/disabled 四态说明）drift 修正；新增 `scada-editor-canvas-disabled-meta.test.tsx`（3 测：role/aria-label + disabled→aria-disabled/inert + 非 disabled 可交互，断言结果值）。
  - **Phase 3（roadmap / 依赖图 / 索引表 / 日志状态同步）**：roadmap §Work Item Status HCA1/HCA7 `planned` → `done`（依赖图 HCA1/HCA7 节点本已 ✅，现与状态表一致）；「已完成审计卡索引」表两卡 `fixed-pending-closure` → `closed`；§HCA1/§HCA7 Phase Details 补 closure PASS 摘要。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm --filter @nop-chaos/flux-renderers-industrial build`/`lint` green + `pnpm --filter @nop-chaos/flux-renderers-industrial test` 全绿（98 test files / 1319 tests，+4 regression：1 HCA1 a11y + 3 HCA7 disabled）。（未跑 e2e；e2e full-green 验证归 HCA-CV work item。）
- **plan 收口**：`2026-08-08-1316-1` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写。P3-1（HCA1 useCallback）Deferred 记录归 HCA-LL/HCA-CR。closure-audit gate 经两个独立子 agent fresh session pass 后勾选（执行 session 不自审）。

---

### 2026-08-08（plan 2026-08-08-1230-3 HCA10 Editor undo-redo 23 维包级深审 closure）

- **plan `2026-08-08-1230-3`（HCA10 Editor undo-redo 审计）收口完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/editor/undo-redo/` 4 源文件（undo-redo-adapter 257 / undo-stack 161 / compute-inverse 218 / operation-coalesce 114，Σ750 行）做 23 维包级深审（复杂交互层，维度 21 显示与定位 / 22 集成接线 / 23 测试有效性 必选触发）。
  - **Phase 1（逐文件深审 + finding triage + 边界值抽查）**：产出审计记录 `docs/audits/2026-08-08-1230-hca10-editor-undo-redo.md`（4 文件逐文件 finding 表 + 边界值抽查表 10 行 + 维度覆盖统计 + 全 finding triage + `文件:行` 证据）。逆计算对称性（added→removed / removed→added(原值) / updated→reverse(前值) / variables 同规则 / reordered→prevSnapshot 顶层 id 序）、round-trip 恒等（U4，含 group 子结构 + variables + reordered）、事务边界（嵌套忽略 / 空 diff 不入栈 / 事务期间不入栈 / commit 原子性）、合并窗口（500ms inclusive 边界 / M2 同 nodeId+同字段 / M3 同 group / 不合场景穷尽）、U7 满栈 FIFO 淘汰、canUndo/canRedo 边界——**零 P0；1 项 P1 + 1 项 P2 + 1 项 P3**。
  - **Phase 2（P1/P2 test-first fix）**：
    - **P1-1**（`undo-stack.ts:147-154` replaceUndoTop 不清空 redoStack——U6 redo-after-new-commit 违约）：coalesce-merge 是「新提交」却走 replaceUndoTop（不清 redo），而 push（清 redo）是新提交的另一路径。undo 后做一次可合并的新编辑（同 nodeId + 同字段 + ≤500ms），redoStack 残留旧 entry → redo 按钮错误可用 → redo 重放本应被 U6 丢弃的 forward diff。failing-first proof 2 test（`undo-stack.test.ts` 断言 redoStackDepth===0 + canRedo===false + peekUndoTop()===merged；`undo-redo-adapter.test.ts` 端到端 fake-timer 断言 undoStackDepth===1 + redoStackDepth===0 + canRedo===false + forward.updated[0].patch.x===20，修复前 `expected 1 to be 0`）→ replaceUndoTop 末尾加 `this.redoStack = []`（与 push 同语义，coalesce 唯一调用方是新提交）→ 转绿。
    - **P2-1**（`operation-coalesce.ts:103-107` singleNodeUpdate 未拒绝 variables/reordered——M2 合并载荷丢失）：singleNodeUpdate 仅校验 added/removed 空 + updated.length===1，未校验 variables/reordered。M2 合并构造 mergedForward/mergedInverse 只搬运 updated 字段，若 forward 携带 variables/reordered 则静默丢弃。failing-first proof 2 test（`operation-coalesce.test.ts` 断言 tryCoalesce===undefined for variables 与 reordered 两路径，修复前 `expected {...} to be undefined`）→ singleNodeUpdate 增 `if (diff.variables !== undefined || diff.reordered !== undefined) return undefined` 守卫 → 转绿。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-undo-redo.md` §4.1.1 computeInverse / §4.1.2 UndoStackEntry / §4.2 事务语义 / §4.4 跨操作合并 / §4.5+U6 边界提示 / `editor-initiation.md` §2.1 M2 + R4 / `design-renderer.md` §12.3 经逐条核对**无 drift**（P1-1 修复后 live 匹配 doc U6/§4.5，doc 本身无需改）。载荷基线回归抽查 ✅（ScadaConfigDiff 形状与 undo-redo 三处消费一致 / diffScadaConfig 产出对称 / applyDiffToConfig 顺序与 runtime 一致）。HCA-BL 喂入：本层无复杂/跨层 bug 候选（P1-1/P2-1 单层局部 defect + 回归测试覆盖，按 bug 指南简单 bug 审计卡留痕即可）。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（industrial 97 files / 1315 tests，+4 regression）。（未跑 e2e；e2e full-green 验证归 HCA-CV work item。）
- **closure audit**：独立子 agent fresh session `ses_0203aed46ffenCYxzgSchG4gT1` verdict=`pass`——8 点逐项 live code 复核（plan 内部一致 + 审计记录 4 文件 file:line + 全 triage / P1-1 replaceUndoTop fix `:148-156` 两 coalesce 路径均经此 / P2-1 singleNodeUpdate 守卫 `:103-110` 真实违约 / 4 failing-first 测试断言结果值非 not.toThrow / 独立重跑 vitest 4 文件 66 测试 green + 全包 97/1315 / P3 时间戳诚实归 HCA-CR 非 live defect 降级 / owner-doc U6 §4.5 本就正确无 drift / scope 纪律仅 undo-redo/ 4 文件）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA10 `planned` → `done`；dependency graph HCA10 标 ✅；§HCA10 节补完成摘要。
- **plan 收口**：`2026-08-08-1230-3` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾（closure-audit gate 经独立子 agent fresh session pass 后勾选）+ Closure section 填写。P3×1（合并窗口时间戳源 Date.now 非单调）归 HCA-CR。

---

### 2026-08-08（plan 2026-08-08-1230-2 HCA9 Editor connection 23 维包级深审 closure）

- **plan `2026-08-08-1230-2`（HCA9 Editor connection 审计）收口完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/editor/connection/` 6 源文件（connection-adapter 291 / connection-drag-controller 151 / anchor-snap 162 / connection-link 97 / connection-overlay 70 / connection-overlay-renderer 58，Σ829 行）做 23 维包级深审（复杂交互层，维度 21 显示与定位 / 22 集成接线 / 23 测试有效性 必选触发）。
  - **Phase 1（逐文件深审 + finding triage + #1 复核）**：产出审计记录 `docs/audits/2026-08-08-1230-hca9-editor-connection.md`（6 文件逐文件 finding 表 + 维度覆盖统计 + 全 finding triage + `文件:行` 证据）。状态机正确性（pick/drag/release 穷尽 + 非法转移守卫 + 重入保护）、吸附阈值边界（`distSq <= thresholdSq` inclusive + 空候选集 free-endpoint 降级 + 退化 bounds 跳过）、联动几何正确性（normalized↔world 逆运算 + safeDiv 零除守卫）、dangling 检测（递归 collectIds 含 group 子树）、覆盖物生命周期（update 先 clear 后 add + clear destroy-all + wiring cleanup 对称）——**零 P0/P1 live defect**（先验 #8/#3/#2 + #1 watch-only residual 已收口/证伪主路径）。
  - **#1 复核裁定（plan 0900-1 转交项）**：`connection-wiring.ts:45,47` preventDefault + connectionDragActiveRef 手势仲裁仍接线、e2e `scada-editor-interaction-correctness.spec.ts:65-106` #1 test 仍覆盖（`vpAfter === vpBefore`）、本轮无接线变更/新冲突 → **维持 watch-only residual**（不升级修复）。
  - **Phase 2（P2 低成本 fix）**：零 P0/P1（vacuously satisfied）。P2-1（dim 23：`connection-overlay-renderer.test.ts:34-42` sky-missing 测试以 `not.toThrow()` 为唯一断言——假绿风险）：保留 not.toThrow（crash-safety 契约）+ 追加 `expect((renderer as unknown as {nodes:unknown[]}).nodes).toHaveLength(0)` 断言 noop 未创建节点（test-strengthen，prior coverage 保留）。P3×4（moveDrag O(n) bounds 重建 / 等距候选 tie-break 首入数组 / 源码注释「12 锚点」措辞 / sky overlay 颜色硬编码）归 HCA-CR。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-connection.md` §4.1/§4.2/§4.3/§4.5/§6 + `design-engine.md` §6 overlay 节经逐条核对**无 drift**（§4.4 row1 runtime 渲染 vs §4.5 适配层联动为正交关注点，非 drift）。先验修复回归抽查 ✅（#8 `connection-adapter.ts:271-276` collectIds 递归 / #3 rg tooltip=0 / #2 `connection-link.ts:55-57` safeDiv + proof test:71-101）。HCA-BL 喂入：本层无复杂/跨层 bug 候选；§4.4 row1 vs recomputeLinkagesForMovedNode 跨层观察归 HCA11 复核（非 HCA9 defect）。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（industrial 97 files / 1311 tests，+1 strengthened assertion）。（未跑 e2e；e2e full-green 验证归 HCA-CV work item。）
- **closure audit**：独立子 agent fresh session `ses_02053c2e7ffelppC3F1v7m3OzL` verdict=`pass`——7 点逐项 live code 复核（审计完整性 6 文件 file:line + 全 triage / 零 P0/P1 抽查 状态机+吸附阈值+safeDiv+overlay 生命周期 / #1 仲裁 `connection-wiring.ts:45,47,50,55-56` + e2e `:105` / #8 `:271-276` + #3 rg=0 / P2-1 fix 断言有意义且保留 prior / owner-doc §4.5 公式一致 / scope 纪律仅 connection/ 6 文件 + #1 接线点）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA9 `planned` → `done`。
- **plan 收口**：`2026-08-08-1230-2` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写 + #1 Deferred 记录。P3×4 归 HCA-CR。

---

### 2026-08-08（plan 2026-08-08-1230-1 HCA8 Editor panels 23 维包级深审 closure）

- **plan `2026-08-08-1230-1`（HCA8 Editor panels 审计）收口完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/editor/{palette,inspector,toolbox}/` 3 子目录 9 源文件（palette 1 / inspector 4 / toolbox 4，~1,112 行）做 23 维包级深审（align-distribute/z-order 追加维度 21 显示与定位正确性）。
  - **Phase 1（逐文件深审 + finding triage + #5 复核）**：产出审计记录 `docs/audits/2026-08-08-1230-hca8-editor-panels.md`（9 文件逐文件 finding 表 + 全 finding triage + `文件:行` 证据）。@nop-chaos/ui 复用（无裸 HTML）/ schema 抽取穷尽性（deriveWidget + GROUP_ORDER + defaults 权威源）/ 字段路由（widget switch + #6 parse-fail 不调 onChange）/ align-distribute 六方向数学 + 分布均布（维度 21）/ z-order block-aware 数组重排 + 幂等（维度 21）/ clipboard 深 clone + counter 新 ID + structuredClone(custom)——**零 P0/P1 live defect**（先验 #6/#21/#4 + HCA7 P3-1 已收口主路径）。
  - **#5 复核裁定（plan 0900-1 转交项）**：`align-distribute.ts:5-7` 文档化注释在位、`align-distribute.test.ts:159-185` group-relative convergence 三测覆盖且通过、无 P1-C2/C3 回归迹象（P1-C2/C3 修 selection 可达性 + fit/center 世界 bounds，未改 align 扁平语义）→ **维持 watch-only residual**（M3 T1 文档化接受限制，归 HCA-CR 复验项）。
  - **Phase 2（P2 test-first fix）**：零 P0/P1（vacuously satisfied）。P2-FE-1（`field-errors.ts` findSymbolIndex 对 group 子节点返回父索引 → validate.ts 真实嵌套错误 `symbols[N].children[M].field` 的 fieldKey 错提为 `children[M]`，不匹配任何 panel 字段 → 嵌套子节点选中时校验错误静默丢失）：failing-first proof 2 test（断言 `result.fill`/`result.x`/`result.width` defined + `result['children[0]']` undefined，含 depth-2 multi-level group）→ 替换 findSymbolIndex 为 findSymbolScopePath（递归累积 basePath 返回完整 scope path `symbols[N]`/`symbols[N].children[M]`，prefix 对齐 validate.ts:342 格式）→ 转绿；旧假绿测试（用 `symbols[0].fill` 描述子节点，P3-FE-2）一并改用真实嵌套格式。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-toolbox.md` §4.2/§4.3/§5/§10 + `design-property-panel.md` §4.3/§5/§6 经逐条核对**无 drift**（§4.2.2 moveUp/moveDown doc 单元素描述 vs 代码 block-aware 为设计级简化，方向一致）。先验修复回归抽查 ✅（#6 parse-fail 不调 onChange / #21 canPaste canonical 派生 / HCA7 P3-1 palette title=def.name）。P2-FE-1 bug 候选（非平凡根因 + 跨 inspector/validate 边界）喂入 HCA-BL。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（industrial 97 files / 1311 tests，+2 regression）。
- **closure audit**：独立子 agent fresh session `ses_02067b890ffe6MWtR9rRp5Br7X` verdict=`pass`——逐条 file:line 复核（plan checklist 全 [x]、P2-FE-1 fix findSymbolScopePath 递归 + prefix 正确 + 旧 findSymbolIndex 已移除、failing-first 测试断言结果值、审计记录 9 文件全覆盖 + finding 全 triage + #5 verdict、重跑 97 files/1311 tests green、align-distribute.ts:6-7 文档化 + test:159-185 覆盖、`git diff --stat` 仅 field-errors.ts/.test.ts 无 scope creep）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA8 `planned` → `done`；dependency graph HCA8 标 ✅。
- **plan 收口**：`2026-08-08-1230-1` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写 + #5 Deferred 记录。P3×13（onError 未 wiring ×3 / a11y ×3 / 注释代码不符 / number 清空写 0 / Label htmlFor / 虚拟字段重复 latent / id 查重 / 浮点位移判定）归 HCA-CR。

---

### 2026-08-08（plan 2026-08-08-1121-1 HCA6 Symbol shapes 23 维包级深审 closure）

- **plan `2026-08-08-1121-1`（HCA6 Symbol shapes 审计）收口完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/symbols/` 5 族 27 源文件（base-shapes 11 / device 5 / instrument 5 / sensor-control 5 / pipe 1，~1660 行）做 23 维包级深审（symbol shapes 非复杂交互层，维度 21-23 可选触发）。
  - **Phase 1（逐文件深审 + finding triage + HCA5 P3-2 复核）**：产出审计记录 `docs/audits/2026-08-08-1121-hca6-symbol-shapes.md`（27 文件逐文件 finding 表 + HCA5 P3-2 per-shape 裁定表 23 行 + 全 finding triage + `文件:行` 证据）。几何正确性（pipe path / arrow / gauge needle / level-thermometer value→高度 / polygon points）、applyProps 路由（toShapeAttrs 13 direct keys + composite applyCompositeProps + 族级增量钩子）、diff-resize、状态驱动视觉、动画绑定、create/applyProps 幂等——**零 P0/P1 live defect**（先验 0900-2 几何/属性 P2 + HCA5 框架 P1 已收口主路径）。
  - **HCA5 P3-2 转交项复核（逐图元 23 行裁定）**：12 composite 族图元全部有 extent part(3: level/thermometer/progress) 或 resize hook(9: motor/pump/valve/fan/gauge/sensor/indicator/switch/button)，**无静默丢弃**；10 base-shapes 全响应 width/height（3 points-based 自定义 applyProps + 7 default toNodePatch 路径）。唯一缺口：**pipe-junction**（自定义 applyProps 借用 applyCompositeProps 但 parts 无 extent/resize → width/height 静默丢弃）升级为 P2-1。
  - **Phase 2（P2 test-first fix）**：零 P0/P1（vacuously satisfied）。P2-1 pipe-junction resize：failing-first proof 2 test（`pipe-symbols.test.ts` 断言 body.width/height + stub points 结果值，修复前 body.width 仍 80）→ 补 `pipe-junction.ts` width/height resize 逻辑（重算 body 尺寸 + 各 stub points 按 connection 归一化坐标 × 新尺寸派生，与 create 同公式；JunctionState 增 connections 字段 + import setAttrs）→ 转绿。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-symbols.md` §4.4（23 图元分类）/ §5（字段分类）/ §10（resize 契约）经 6 契约逐条核对**无 drift**（§10 P2-4 Decision 范围为 composite 族，pipe-junction 非 composite 不在范围，其 resize 由 P2-1 code-level fix 落地）。先验修复回归抽查 ✅（0900-2 P2-4 resize hook / B2 points-based / P2-9 BULB_RESERVE / 0653-2 P1-2 indicator swap / HCA5 P1-1 守卫）。P2-1 pipe-junction bug 候选（单包，根因中非显然：HCA5 P3-2 裁定聚焦 composite.ts:72-77 未覆盖 pipe-junction 自定义 applyProps 路径）喂入 HCA-BL。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace green（industrial 97 files / 1309 tests，+2 regression）。
- **closure audit**：独立子 agent fresh session `ses_02083e810ffeCdi5DCHN4TvasA` verdict=`pass`——逐条 file:line 复核（plan checklist 全 [x]、P2-1 fix create↔applyProps 公式一致 + 2 test 断言结果值、审计记录 27 文件 + 23 行裁定表、P3-2 抽查 motor/level/pipe-junction 匹配 live、0 P0/P1 无静默降级、重跑 97 files/1309 tests green）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA6 `todo` → `done`；dependency graph HCA6 标 ✅。
- **plan 收口**：`2026-08-08-1121-1` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写 + P3×4 Deferred 记录。P3×4（switch on/off 推断边缘 / extent 族内边距 cosmetic / polygon 空点 / NaN 防御纵深）归 HCA-CR。

---

### 2026-08-08（plan 2026-08-08-1051-1 HCA4 Serialization 层 23 维包级深审 closure）

- **plan `2026-08-08-1051-1`（HCA4 Serialization 层审计）收口完成**——3 Phase 全交付，零代码变更（audit-only plan）。对 `packages/flux-renderers-industrial/src/serialization/` 6 文件（config-types 131 / validate 524 / diff 138 / equality 56 / parse 25 / serialize 27）做 23 维包级深审（serialization 非复杂交互层，维度 21-23 可选触发）。
  - **Phase 1（逐文件深审 + finding triage + validate.ts 拆分 Decision）**：产出审计记录 `docs/audits/2026-08-08-1051-hca4-serialization-layer.md`（6 文件逐文件 finding 表 + 全 finding triage + `文件:行` 证据）。校验完整性（fail-closed + 子形状 + malformed + duplicate id + MAX_VALIDATE_DEPTH=100）、deepEqual 数组/对象守卫 + 深度上限、diffScadaConfig 结构正确性（added/removed/updated + type-change remove-then-rebuild + children 空数组 patch）、序列化往返保真、legacy `@{}` 扫描递归覆盖、reordered 由 editor-action 产出（非 diffScadaConfig，by-design）——**零 P0/P1 live defect**（先验 0900-1/0653-4/2129-1 三轮硬化已收口主路径）。
  - **validate.ts 524 行拆分 Decision：移交 HCA-CG**（DA-1）。理由：WARN 桶低位 524/700（距 ERROR 36% 余量，非 CI 阻塞）+ 单一职责内聚（per-shape 校验器共享 helper → 拆分须同时抽 helpers/ 致 524 行拆 3+ 文件过度碎片化）+ validate.ts 不在包公共 index 导出 + HCA-CG 是集中治理指定批次。拆分缝 + re-export 方案已记录审计卡供 HCA-CG 采用。
  - **Phase 2（P0/P1 fix + 拆分落地）**：零 P0/P1（vacuously satisfied）+ Decision=移交 → 无代码变更。P3-1（`validate.ts:287` align 字段校验遗漏，graceful fallback 无数据丢失）/ P3-2（`validate.ts:448-451` background.grid 子形状校验遗漏，视觉 glitch）归 HCA-CR backlog（Follow-up-1）。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：design-renderer.md / design-data-binding.md / design-engine.md 散见 serialization 章节经 rg/读核对**无 drift**（拆分 Decision=移交不改模块清单，无需同步）。先验修复回归抽查 ✅（serialization-validate.test.ts / serialization-equality.test.ts / expression-codemod.test.ts）。HCA-BL 喂入：本层无复杂/跨层 bug 候选。
- **workspace 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 59/59 全 FULL TURBO cached（零代码变更）；industrial baseline 97 files / 1307 tests green。
- **closure audit**：独立子 agent fresh session `ses_020a453a9ffek94PcJx7hmGYA7` verdict=`pass`，零 blocking——逐条 file:line 复核（6 文件行数、P3-1/P3-2 真实且为 P3、validate.ts 不在包 index、reordered editor-action-driven、deepEqual 单一事实源、tests 独立复核 1307 green）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA4 `todo` → `done`；HCA-LL 文件行数 lesson 更新（validate 524 标注已审 + Decision=移交 HCA-CG）。
- **plan 收口**：`2026-08-08-1051-1` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写 + DA-1/Follow-up-1 记录。

---

### 2026-08-08（plan 2026-08-08-0748-1 HCA5 Symbols core 23 维包级深审 closure）

- **plan `2026-08-08-0748-1`（HCA5 Symbols core 审计）收口完成**——3 Phase 全交付（commit `dd6f111f`）。对 `packages/flux-renderers-industrial/src/symbols/` 顶层 8 文件（symbol-types 157 / symbol-registry 44 / symbol-factory 106 / style-resolver 14 / visual-state 118 / composite 135 / compound 98 / register-builtin 75）做 23 维包级深审。
  - **Phase 1（逐文件深审 + finding triage + guard Decision）**：产出审计记录 `docs/audits/2026-08-08-0748-hca5-symbols-core.md`（8 文件逐文件 finding 表 + 全 finding triage + `文件:行` 证据）。注册并发安全 / 工厂 deep-merge / 视觉状态 revert 完整性 / 复合装配 applyProps 路由 / compound 装配子符号 props 传播 / register-builtin 幂等——当场发现 **P1-1**（`ScadaSymbolProps` 声明 `fontFamily`/`fontWeight`/`align` 但序列化层 `ScadaSymbolNode` + `SYMBOL_KEYS` 缺三字段 → `diffScadaConfig` 漏键，与 0653-2 `flow` 同根因复发）。`SYMBOL_KEYS` 派生 lint 守卫 governance 项 **Decision：现在落地**（Phase 1 当场证伪 watch-only 可行性）。
  - **Phase 2（test-first fix）**：P1-1 failing-first proof 3 test（`serialization-diff.test.ts` 断言 patch 含三字段值，修复前空 patch）→ 补 `ScadaSymbolNode`（config-types.ts）+ `SYMBOL_KEYS`（diff.ts）→ 转绿。实现 `scripts/check-scada-symbol-keys.mjs`（三向断言：ScadaSymbolNode 字段 ∈ SYMBOL_KEYS / ScadaSymbolProps 字段 ∈ ScadaSymbolNode / SYMBOL_KEYS 无多余键）当场报出 P1-1 drift，修复后转绿；接入 `pnpm check` + `check:scada-symbol-keys` 脚本条目。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-symbols.md` 经核对与 live 无 drift（P1-1 drift 根因在序列化层 wire 类型，doc 不逐字段文档化，无需同步）。先验修复（visual-state STYLE_RESET_DEFAULTS / factory fromNodeAttrs 读写对称 / composite resize 路由）回归抽查通过。P3×3 归 HCA-CR/HCA6；P1-1 bug 候选喂入 HCA-BL。
- **workspace 验证**：`pnpm --filter @nop-chaos/flux-renderers-industrial test` 全绿（97 files / 1307 tests）；`node scripts/check-scada-symbol-keys.mjs` pass（30 node fields / 27 props fields / 30 keys）；`pnpm typecheck` 32/32 FULL TURBO。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA5 `todo` → `done`。
- **plan 收口**：`2026-08-08-0748-1` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure section 填写。

---

### 2026-08-08（plan 2026-08-08-0748-3 HCA3 Binding 层 23 维包级深审 + dirty-collector 拆分 execution）

- **plan `2026-08-08-0748-3`（HCA3 Binding 层审计）执行完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/binding/` 7 文件（point-store 330 / reverse-index 126 / dirty-collector 665 / value-to-state 59 / animator 269 / bind-resolver 139 / flux-eval 111）做 23 维包级深审（复杂交互层，维度 21-23 必选）。
  - **Phase 1（逐文件深审 + finding triage + 拆分 Decision）**：产出审计记录 `docs/audits/2026-08-08-0748-hca3-binding-layer.md`（7 文件逐文件 finding 表 + 维度 21/22/23 专项节 + 全 finding triage + `文件:行` 证据）。脏收集合帧顺序确定性（collect → collectBindings → flush）、animation vs binding 同属性优先级（P2-1 契约复核 + 三组 focused 测试）、flux 求值订阅收集（并集 + 解绑对称 + 无泄漏）、point-store 监听器清理（subscribe/unsubscribe 对称 + dispose）、reverse-index 增量维护、value-to-state 阈值边界、animator rAF 生命周期（start/stop 对称 + 无孤儿帧 + tick 迭代安全）、bind-resolver 求值优先级 + format-target 限定——**零 P0/P1 live defect**（先验修复 P2-1/P2-4/表达式一元化/订阅并集/B1-B4 系列已扎实覆盖）。P2-BND-1（dirty-collector 665 行 > 500 WARN 阈值）拆分 Decision：**本 plan 落地**。
  - **Phase 2（test-first fix）**：零 P0/P1 finding，无需 failing-first test。P2-BND-1 文件行数治理为 Phase 3 拆分处置。
  - **Phase 3（dirty-collector 拆分落地 + owner doc 同步 + 回归抽查）**：dirty-collector.ts（665 行）按职责拆分为 3 文件：`dirty-collector.ts`（104 行，DirtyCollector + 帧类型）、`expression-errors.ts`（65 行，CircularDependencyError + cause-chain 解包 + FluxEvalOutcome）、`refresh-pipeline.ts`（469 行，RefreshPipeline + 接口）。各模块 ≤ 500 行 WARN 桶清零。公共面不变（RefreshPipeline/DirtyCollector API 不变），20 个 import 路径更新。owner doc `design-data-binding.md` §11 模块清单同步（7→9 文件）。先验修复回归抽查通过（1307 tests 全绿含 P2-1 优先级 / P2-4 cycle onError / 表达式一元化 / B3 stateSource / B4 scale focused 测试）。
- **workspace full-green 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace EXIT=0 / 0 FAIL（industrial 97 files / 1307 tests，拆分前后测试数不变证无行为漂移）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA3 `todo` → `done`；HCA-CG dirty-collector 行数治理项标注已落地（~~strikethrough~~）；HCA-LL 文件行数 lesson 更新。
- **plan 收口**：`2026-08-08-0748-3` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾（closure-audit gate 待独立子 agent）。HCA-CG 可跳过 dirty-collector 行数治理。
- **待办**：plan closure-audit gate（独立 fresh session，执行 session 不自审）。

---

### 2026-08-08（industrial-hmi-component-audit mission 立项 + C10 审计执行）

- **新 mission 立项**：`missions/industrial-hmi-component-audit.json` + `docs/backlog/industrial-hmi-component-audit-roadmap.md`——对标 `component-audit-roadmap.md` 结构，审计对象覆盖 industrial 包全部 11 层子系统（~84 源文件 ~13k 行），按模块族编排 HCA0–HCA11 + HCAX-n + HCA-BL（bug 总结）+ HCA-LL（lesson 总结）+ HCA-CR/CV/CG。
  - HCA1（Renderer 层）/ HCA7（Editor renderer 层）已完成（审计卡 fixed-pending-closure）。
  - HCAX-1（error code 共性重构）/ HCAX-2（canvas a11y 共性重构）已完成。
  - HCA2–HCA6, HCA8–HCA11 待执行（engine/binding/serialization/symbols/editor 子系统逐层审计）。
  - HCA-BL / HCA-LL 为独立 work item（bug 归档到 docs/bugs/，lesson 沉淀到 checklist/skills）。
- **C10 审计执行**（component-audit mission 交叉审计）——仿照 component-audit 18 维 checklist 对 `scada-canvas` + `scada-editor-canvas` 进行逐组件审计，P0/P1 当场自动修复。
  - **审计卡创建**：`docs/audits/per-component/scada-canvas.md` + `scada-editor-canvas.md`（状态 fixed-pending-closure）。
  - **P1-1 修复**（scada-editor-canvas schema 类型漂移）：`editor/schemas.ts` `ScadaEditorCanvasSchema` 缺 `loading`/`empty`/`error` region 声明（消费 `props.regions.*` 但 interface 未声明）→ 补齐 `SchemaInput` 声明。
  - **P2-1 修复**（editor definition 静态元数据缺失）：补齐 7 `propContracts` + 7 `eventContracts`（含 payload shape），对齐 runtime 定义的工具链可发现性。
  - **P2-2 修复**（a11y——canvas 交互面缺 role/aria-label）：两个组件 canvas wrapper 加 `role="application"` + `aria-label`（+ i18n key `canvasLabel`）。
  - **P2-3 修复**（disabled meta 未响应）：editor canvas 加 `aria-disabled` + `inert` + drop/keyboard guard。
  - **P2-4 修复**（shared/CX-9：error code 语义混淆）：editor `parseAndValidateConfig` 误用 `invalid-config`（命令句柄失败码）→ 统一为 `config-invalid`（校验失败升级码，与 runtime 一致）。
  - **P3-1 修复**（palette title 用 def.type → def.name）。
  - **roadmap 更新**：component-audit-roadmap C10（fixed-pending-closure）+ CX-9（done）；组件计数 113→115（+industrial 2）。industrial-hmi roadmap 新增 I19（bug/lesson 总结 work item）；industrial-hmi-editor roadmap 新增 E11（bug/lesson 总结 work item）。
  - **workspace 验证**：typecheck/build/lint/test 全绿（industrial 97 files / 1302 tests + i18n 28 tests）。

---

### 2026-08-08（plan 2026-08-08-0900-2 P2 test-fidelity/docs-drift/cleanup execution）

- **plan `2026-08-08-0900-2`（P2 test-fidelity/docs-drift/cleanup 子集）执行完成**——4 Phase 全交付。40 条 post-remediation P2 findings 全部落定（行为耦合子集 plan 1 + 本计划 test/docs/cleanup 子集）。
  - **Phase 1（Test Fidelity 重写）**：
    - **#22/#23 Proof**：复核仍强（已由 plan `2026-08-07-1835-2` P1-12 重写为 `toBe(321)`/`toBe(198)` + 真实 mutation）。
    - **#24**：toolbox 6 处 status-message 测试从 `not.toBeNull()` 推进到断言实际 i18n 文本（'无可见图元'/'无变化'/'配置非法，导入失败'）。
    - **#25**：toolbox runtime-call 从「被调用」推进到「调用参数/副作用可观测」——view 工具断言 zoom 参数(1.2/1÷1.2)+计数+viewport status；align/distribute/zorder 断言方向/action 参数；copy/cut/paste/export 断言 status 文本反映计数。
    - **#26**：inspector onChange 从「patch defined」推进到「patch 字段值正确(`x===200`)+workingConfig 反映」。
    - **#27**：e2e coalesce 注入可控时钟（`vi.spyOn(Date,'now')` + `mockReturnValue`）替代真实 wall-clock；+boundary 测试（>500ms 不合）证明时钟控制真实生效。
    - **#28**：connection pointer-drag 复核 linkage 坐标（snap 锚点归一化值 `x===1, y===0.5`，证明 snap 算法命中 right-middle 锚点）。
  - **Phase 2（设计文档文件树与句柄契约漂移修复）**：
    - **#33/#34/#35**：design-renderer.md §11 + design-architecture.md §11 文件树重写为权威 live 树——删除 phantom `use-editor-session.ts`/`use-editor-events.ts`；补齐 `editor-working-helpers.ts`/`runtime-factories.ts`/`runtime-mutators.ts`/`connection-wiring.ts`/`test-handle-factory.ts`/`toolbox-runtime.ts`/`palette/editor-palette.tsx`/`connection-drag-controller.ts`/`connection-overlay-renderer.ts`/`inspector-field.tsx`/`inspector-panel.tsx`；校正错位 `scada-editor-canvas.tsx`（顶层非 renderer/）+ `renderer/editor-engine.ts`/`editor-errors.ts`。
    - **#32**：design §8.4 契约块补齐顶层 `addSymbol/removeSymbol/updateSymbol/group/ungroup/undo/redo` 7 方法（测试投影，对齐 `editor-test-handle.ts` 实际导出）。
  - **Phase 3（公共面错误码与文档同步）**：
    - **#36**：quick-reference editor 失败码双向对齐——补齐 registry 9 码（duplicate-id/invalid-patch/empty-selection/no-undo/no-redo）+ runtime 共享 3 + not-visible 标注（handle-path 未入 registry）；destroyed 标注为 data-status 值（非失败码）。
    - **#37**：flux-guide toolbox 子句柄列表从 12 补齐到 18 方法（+resetView/getViewport/toBottom/moveUp/moveDown/getClipboard）。
    - **#31**：design §8.5.2 补 `editor-mount-failed` 文档化（码已注册发射但未文档化）。
    - **[E0-spike]**：research-render-engines.md §5:122 补 InnerEditorEvent（inner editor 打开/关闭生命周期）。
  - **Phase 4（Dead/Cosmetic 表面与冗余 Memo 清理）**：
    - **#3**：移除 `SnapHighlightMark.tooltip` 死字段（renderer 从未消费）+ 非 i18n 硬编码中文；3 处测试引用同步清理。
    - **#14**：design-toolbox.md §10 补 4 子标记文档化（识别钩子，无 dead CSS；视觉经 @nop-chaos/ui 组件承载）。
    - **#20**：移除 canvas 8 冗余 `useCallback` + palette 1 冗余 `useMemo`；保留 canvas `useMemo(parseAndValidateConfig)`（load-bearing：useEditorEngine useEffect identity check）+ use-editor-engine.ts 2 load-bearing `useCallback`。
    - **#38 residual**：`errorMessage` 同包导入（非跨包）+ 非 trivial（~30 行 cause-chain+stack 截断），不内联。
    - **#39 residual/moot**：`use-editor-engine.ts` 已从前序 824 行降至 265 行（远低于 710 阈值），原漂移自动消解。
- **workspace full-green**：typecheck 32/32 + build（industrial green）+ lint（industrial green）+ test industrial 97 files / 1302 tests green（+1 boundary coalesce test）。
- **roadmap 收口**：Follow-up Backlog P2 test-fidelity/docs-drift/cleanup 子集标 ✅；40 条 P2 findings 全部落定。
- **待办**：plan closure-audit gate（独立 fresh session，执行 session 不自审）。

---

### 2026-08-08（plan 2026-08-08-0900-1 P2 behavior-coupled execution）

- **plan `2026-08-08-0900-1`（P2 行为耦合子集）执行完成**——Phase 4 + Phase 5 全交付（Phase 1–3 由前序完成）。
  - **Phase 4（交互正确性浏览器复核）**：
    - **#40 confirmed+fixed**：leafer canvas 覆盖兄弟 panel——`.nop-scada-editor-layout` 原 `display:flex`（row）把 palette/canvas/inspector/toolbox/statusBar 同处一行，toolbox 1226px 挤尽 canvas 到 0px。Fix：layout 改 `flex-direction:column` + 新增 `.nop-scada-editor-body` 行（palette|canvas|inspector）；toolbox 顶栏 / statusBar 底栏。e2e 断言 canvas-area 不与 palette/inspector 水平重叠。
    - **#1 adjudicated residual**：e2e 证伪——junction pointerdown 已 `preventDefault()` + 设 `connectionDragActiveRef`，viewport 不被平移（`vpAfter===vpBefore`）。
    - **#5 adjudicated residual**：P1-C2 修复后 group child 经 `collectAllSymbols` 进入 selection（收敛）；扁平算法 local 坐标是文档化 M3 T1 接受限制。
  - **Phase 5（行为耦合公共面接线）**：
    - **#21**：移除 `clipboardCount` React state mirror → `canPaste` 从 canonical `runtime.getClipboard()` 派生（外部清空 clipboard 不再 stale enabled）。
    - **#30**：`ScadaEditorViewportPolicy` 接线到 `schemas.ts` viewport 字段（不再 inline 重复类型）；canvas 消费侧去 inline cast + ref guard 替代 eslint-disable（react-compiler 规则）。
    - **#29**：`use-editor-handles.ts` not-mounted/destroyed 路径返 registry code `'not-mounted'`（非自由格式英文 Error）。
  - **附带修复**（前序 Phase 1–3 遗留 lint 阻塞）：`inspector-field.tsx` ref-during-render（`lastEmittedRef.current` render 期访问）→ 改 `selfUpdate` state flag；i18n 缺失键 `emptyScene`/`invalidJson` 补到 en-US/zh-CN locale。
- **workspace full-green**：typecheck 32/32 + build 32/32 + lint 32/32 + test 59/59（industrial 97 files / 1301 tests）+ e2e 2/2（scada-editor-interaction-correctness）。
- **roadmap 收口**：Follow-up Backlog P2 行为耦合子集标 ✅；test-fidelity/docs-drift/cleanup 子集归 successor plan `2026-08-08-0900-2`。
- **待办**：plan closure-audit gate（独立 fresh session，执行 session 不自审）。

---

### 2026-08-08

- **plan `2026-08-07-1835-2`（工业 HMI 编辑器契约接线/结构/UI/测试/性能）执行完成**——Phase 2–5 全交付（Phase 1 oversized 拆分由前序完成）。11 条 P1 全部收口：
  - **P1-04** save/load dispatch：`runtime-mutators.ts` save/load 经 `onSave`/`onLoad` 派发 `scada-editor:save`/`load`；canvas `handleSave`/`handleLoad` 透传 events。
  - **P1-05** commitPolicy auto-save（方案 A）：`runtime-factories.ts` `notifySession` 在 `commitPolicy='auto'` 且非事务期触发 `ctx.save()`（`ctx.save` 由 `use-editor-engine.ts` 回填）；transform 逐帧由 `commitTransaction` 兜底。
  - **P1-06** runtime 9 句柄合并注册：`use-editor-handles.ts` 注册全部 18 方法（runtime 9 + 编辑 9），destroy 置 `data-status="destroyed"`。
  - **P1-08** mode 同步：`mountEditorEngine` build 后 `engine.setMode(session.mode)` + load 后重同步。
  - **P1-09** controlled pushback（方案 A）：`use-editor-engine.ts` effect watcher 监听 `initialConfig`/`initialMode` 变化→load/switchMode。
  - **P1-B** delete/group/ungroup 默认 UI 按钮 + canvas 键盘层（`scada-editor-canvas.tsx` onKeyDown：Delete/Ctrl+Z/Y/Ctrl+G/Ctrl+Shift+G/方向键）。
  - **P1-10** raw `<textarea>`→`@nop-chaos/ui` `<Textarea>`（`toolbox-panel.tsx`）。
  - **P1-11** palette drop 落指针处（`getBoundingClientRect`+`engine.getWorldPoint`）。
  - **P1-12** false-green 修复：`editor-adapter.test.ts` programmaticSelect 补 expect；`scada-editor-canvas-interaction.test.tsx` onReady/onError 注入 dispatch spy 断言 mock.calls；`scada-editor-canvas-ops.test.tsx` move 几何断言 + group/ungroup 真实 mutation；反向验证（移除 onSave dispatch → contract test 红）。
  - **P1-13** 性能增量化：`editor-working-helpers.ts` `recomputeLinkagesForMovedNode` 建 `Map<id,node>` O(1) lookup + `collectSymbolBounds` 复用跨 junction（`precomputedBounds`）+ applyPatch 直接写预解析 node ref；`runtime-factories.ts` `handleGeometryChange` trailing syncWorkingCopy（microtask 批处理去掉 s 乘子）。
- **新增测试文件**：`scada-editor-canvas-contract.test.tsx`（10+ contract proof）、`scada-editor-canvas-affordance.test.tsx`（12 affordance proof）、`editor-perf.test.ts``（5 perf/bounds-reuse proof）。
- **i18n**：新增 6 key（toolbox delete/deleted/group/grouped/ungroup/ungrouped）注册到 en-US/zh-CN locale。
- **workspace full-green**：typecheck/build/lint 32/32 + test 59/59（industrial 95 files / 1254 tests，coverage ≥90% branches）+ `check:oversized-code-files` industrial editor 0 ERROR。
- **owner docs 同步**：`design-renderer.md` §4.5（commit/controlled）+ §8.5.1（runtime 9 合并注册）、`flux-guide/design-patterns/scada-editor.md`（提交语义 + UI/键盘 affordance）、`docs/references/quick-reference.md`（scada-editor-canvas 句柄/契约/affordance）。
- **审计/roadmap 收口**：两份源审计 `> Audit Status: planned` → `closed`；roadmap 新增 plan {2} 完成条目 + Follow-up Backlog 注记两份 P1 全收口。
- **待办**：plan closure-audit gate（独立 fresh session，执行 session 不自审，按 AGENTS.md collaboration discipline 待人工/独立 session 触发）。

---

### 2026-08-08（plan 2026-08-08-0748-2 HCA2 Engine 层 23 维包级深审 execution）

- **plan `2026-08-08-0748-2`（HCA2 Engine 层审计）执行完成**——3 Phase 全交付。对 `packages/flux-renderers-industrial/src/engine/` 9 文件（scada-engine 499 / config-adapter 180 / event-bridge 190 / hit 29 / interaction-overlay 176 / tree-registry 108 / viewport 96 / batch-add-probe 44 / test-handle 29）做 23 维包级深审（复杂交互层，维度 21-23 必选）。
  - **Phase 1（逐文件深审 + finding triage）**：产出审计记录 `docs/audits/2026-08-08-0748-hca2-engine-layer.md`（9 文件逐文件 finding 表 + 维度 21/22/23 专项节 + 全 finding triage + `文件:行` 证据）。视口数学（clampScale fit/contain/fill 三分支 + scaleOfWorld 矩阵可恢复性 P1-9/D3/P2-8）、命中测试边界、覆盖物生命周期（create/update/dispose 对称 + reset 清理 P2-10）、event-bridge 事件映射完备 + 监听器清理对称、tree-registry DFS subtreeIds、config-adapter diff 幂等、引擎 reset/destroy 清理对称——先验修复基线确认成立。
  - **Phase 2（test-first fix）**：零 P0/P1（先验 P1-9/P2-7/P2-8/P2-10/P1-7 已收口基线）。**P2-ENG-1 修复**（importConfig 全量重建路径与 reset 不一致）：`scada-engine.ts:343` `importConfig` 原直调 `adapter.build`，旁路 `config.background.color` 应用 + `InteractionOverlay.clear()`（reset 两步都做）→ 改调 `this.reset(config)` 使两条全量重建路径一致。failing-first proof（`scada-engine-plugin-sync.test.ts` P2-ENG-1：断言 activeCount=0 / ground.fill='#112233' / 符号注册结果值）红 `expected 1 to be +0` → 转绿。
  - **Phase 3（owner doc 一致性 + 回归抽查 + bug 喂入）**：`design-engine.md` §4.4/§4.2/§8.1/§8.3 与 live 一致；§8.2 importConfig「序列化契约转发」drift 经 Phase 2 impl 修复对齐 doc（doc 本身正确无需同步）。先验修复回归抽查通过。bug 候选喂入 HCA-BL 节（P2-ENG-1 单层低复杂，同 P2-10 同类）。
  - **P3 记录**（归 HCA-CR backlog）：P3-ENG-1（engine 公共命令缺 destroyed 门控，binding 层有 / engine 缺，主路径无 reproducer）、P3-ENG-2（applyDiff nextConfig 省略时 config 陈旧，renderer 恒传）、P3-ENG-3（TreeRegistry.add 不查重，validator 主路径拒绝）。
- **closure audit**：独立子 agent fresh session `ses_020e3ccfdffeCOapJ5YGnsNr7m` 三件套输入（plan 摘要 + diff + 验证输出），VERDICT: PASS（审计完整性 / P2-ENG-1 fix 正确性 / 无静默 P0/P1 降级 / owner doc 一致 / 先验修复回归 / scope 纪律均独立核验通过，并独立重跑四命令绿）。
- **workspace full-green 验证**：`pnpm typecheck` 32/32 + `pnpm build` 32/32 + `pnpm lint` 32/32 + `pnpm test` 全 workspace EXIT=0 / 0 FAIL（industrial 97 files / 1307 tests，+1 P2-ENG-1 回归测试）。
- **roadmap 收口**：`docs/backlog/industrial-hmi-component-audit-roadmap.md` HCA2 `todo` → `done`（closure audit PASS）+ Phase Details 补完成注记。
- **plan 收口**：`2026-08-08-0748-2` Plan Status `active` → `completed`，3 Phase status + closure gates 全勾 + Closure Audit Evidence 记录。
