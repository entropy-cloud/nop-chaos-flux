# Development Log - 2026-08-22

### 2026-08-22

- **Sundial 工作台嵌套 dialog bug 诊断 + 框架层修复**：
  - 用户要求：检查 sundial 示例应用的工作台任务详情 dialog,以及"嵌套 dialog 关闭"的实际框架层问题。先在 schema 层做了 inline setValue cycle 绕过(`taskDetailDate` 等行),后被指出"框架问题必须在框架层解决",回滚绕过,定位 `use-surface-renderer.ts:224` 的 `node.scope.update(openPath, false)` 路径,在 `packages/flux-react/src/dialog-host.tsx` 的 `handleOpenChange` 中加 `if (!isTopmost && reason ∈ {outside-press, escape-key}) return;` 守卫。
  - 根因:Base UI `<Dialog>` 每个实例各自挂 outside-press / Esc 监听器,`DialogHost` 把所有 dialog 作为兄弟节点渲染到 body,所以 picker(内层)打开时点击 picker 内的 radio,会触发外层 dialog 的 outside-press → Plan 459 B1 受控关闭路径 → `node.scope.update(openPath, false)` → 写入 `taskDetailOpen = false` → 外层 dialog 关闭。用户根本没想关外层。
  - 诊断方法:`docs/bugs/162-stacked-dialog-non-topmost-outside-press-closes-parent-fix.md` 第二节完整记录,关键教训:**先停下来确认自己在查什么**,而不是 5 轮 `console.log` 撒满 `scope.ts` / `form-store.ts` / `surface-runtime.ts` / `action-adapter.ts` / `schema-renderer.tsx` / `use-surface-renderer.ts` 逐层抓调用栈。用户当面批评过这种调试方式。
  - 修复:`packages/flux-react/src/dialog-host.tsx` `DialogView.handleOpenChange` 加 13 行守卫,语义对齐既有的 `modal={isTopmost}` —— 非 topmost 的 dialog 在交互上是惰性的。
  - 回归测试:`packages/flux-react/src/__tests__/dialog-host-close-behavior.test.tsx` 新增 `describe('DialogHost stacked dialogs')` 3 用例(non-topmost outside-press / escape-key 不关、topmost 仍正常关)。原 481 → 现 484 passed。
  - 全验证:`pnpm typecheck` 37 packages ✓;`pnpm lint` 37 packages ✓;`pnpm test` flux-runtime 1423 / flux-react 484 / flux-playground 164 全绿。**`flux-renderers-basic/src/__tests__/surface-event-ctx.test.tsx` 仍 fail**(master HEAD 上即存在,与本次修复无关 —— `git stash` 验证过),属于 2026-08-20 全仓审计列出的 baseline 红(`docs/audits/check/00-baseline-tooling.md`),待独立 plan 修。
  - Schema 层回退:`apps/playground/src/complex-pages/page-schemas/sundial-workbench.json` 日期行改回正经 `openDialog` picker(testid `sundial-workbench-detail-date-picker`);recur/list/flag/move-list 行保留 inline cycle,够 demo 不展开 4 个 picker 的 schema 体积。`apps/playground/src/complex-pages/page-data.ts` 新增 `taskDetailTitle/Note/Date/Recur/Flag/List` 初值。
  - 视觉:`apps/playground/src/sundial-replica/sundial-replica.css` 把 `--sd-*` token 同时声明到 `.sd-root` 和 `.sd-dialog`(dialog 通过 portal 渲染脱离 `.sd-root` 子树,之前 `--sd-brand` 等 token 在 picker 内取不到,导致确认按钮白底白字隐形)。
  - 新增文档:`docs/bugs/162-stacked-dialog-non-topmost-outside-press-closes-parent-fix.md`(Bug 编号 162);`docs/bugs/README.md` 同步加条目。
  - Next step:可顺道把该 bug 列入 `docs/audits/check/SUMMARY.md` P0 链下的"受控生命周期"主题(`use-surface-renderer.ts:224` 已列入),与 baseline `surface-event-ctx.test.tsx` 失败同根。本周内若开 plan 修 `surface-event-ctx`,顺手把 `162` 的回归测试一起扩 cover(目前覆盖 `outside-press`/`escape-key`,还需覆盖 `close-press` / `cancel-press` 栈场景)。

### 2026-08-22 (续) — 界面显示问题补遗

- **Settings rail 图标与文字纵向错位**:`.sd-nav-row` 在 `sundial-replica.css:238-247` 用 `align-items: center`,但 row 内 `flex-1` 容器包了 title + sub-title(总高 54px,大于 18px 图标),两端都垂直居中后,图标落在 title 与 sub-title 的中间空白处。视觉上 title 与图标不共线。CSS 改为 `align-items: flex-start` 后,图标与 title 都在 row 顶部对齐,sub-title 自然落在底部,符合 Sundial 原版设计语义。工作台 sidebar(同步类名)一并修复。
- **工作台任务行"打开详情"toast 重复**:7 个 task row 的 onClick 数组都带 `showToast "打开详情"`,语义上是 setValue 开启 dialog 的死反馈。多次点击时 toast 在右下角堆叠(截到 3 条)。批量删除 7 处 toast action,只保留 setValue。

### 已知未改

- **Checkbox 点击冒泡到 row onClick**:Sundial 原版 checkbox 点击应切换 done 状态,但当前 schema 把 checkbox 放在 sd-task-row 内层,row 的 onClick 会捕获冒泡,导致点击 checkbox 也开 detail dialog。框架框架层面缺 child control 的 stopPropagation 机制,schema 层面无 `targeting` 表达式可在 onClick 内区分事件源。短期影响:用户点 checkbox 误开 dialog。已记入 `162` 同根(嵌套 dialog 受控关)plan 待办。
