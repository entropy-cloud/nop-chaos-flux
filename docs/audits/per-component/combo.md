# 审计卡：combo（flux-renderers-form-advanced）

> 状态: closed
> 审查日期: 2026-08-03
> 审查 plan: `docs/plans/2026-08-03-0921-1-c3-1-form-advanced-composite-input-family-audit.md`
> 注册定义: `packages/flux-renderers-form-advanced/src/combo-renderer.tsx:553-613` | 渲染器: `packages/flux-renderers-form-advanced/src/combo-renderer.tsx:226-551`（ComboItem :74-224） | design.md: `docs/components/combo/design.md` | playground: `apps/playground/src/component-lab/renderers/combo-lab-page.tsx` | e2e: `tests/e2e/w4c-composite-form-family.spec.ts` + `tests/e2e/component-lab/c3-1-host-surfaces.spec.ts`（本族宿主场景新增）

## 组件身份

combo / flux-renderers-form-advanced / ComboSchema（`composite-schemas.ts:99-113`）/ `{type:'combo', name, items}` / 表单参与: 是（name/required/validation/提交路径，wrap: true FieldFrame 提供 label/校验 chrome + data-field-\*）/ widget 控件 renderer——重复对象项字段编辑器（items region + array-field staged owner 内核 + canonical composite handle）

## 18 维审查记录

| #   | 维度                        | 结论 | 证据                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 发现                                                                                                             |
| --- | --------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| 1   | Schema 契约                 | pass | 注册 fields（:560-576，含 removeWhen lazyEval params ['record','index','value'] :570、items region :575）与 ComboSchema（:99-113）双侧一致；minItems/maxItems validation collectRules（:578-611）；无 deepFields 声明；schema/design 声明差异（design `multiple`）见 P2-1                                                                                                                                                                              | P2-1 已修复（design.md §4/§5 移除不存在的 `multiple`，与 ComboSchema 对齐）                                      |
| 2   | RendererComponentProps 合规 | pass | 仅读 props.props/meta/regions/events/helpers/templateNode（:226-551）；标准 hooks：useRenderScope/useCurrentForm/useCurrentFormState/useCurrentValidationScope/useScopeSelector/useRenderInstancePath/useFieldPresentation/useCompositeFieldHandle；无 ad-hoc context/prop-drilling（item 经 FormContext/ScopeContext/ValidationContext 注入 projected item scope，复合字段族既有模式）                                                                | —                                                                                                                |
| 3   | 值所有权三态                | pass | 表单内 formValue（:256-264）+ 表单外 scopeValue（:265-274）双路径；staged owner 写回经 parentForm.setValue/appendValue/removeValue/moveValue + touchField（:349-364, 424-494）；无 name 时 scope.update 写回；初始值 defaultValue/initValue 由 form runtime 提供（C2.1 已核）；minItems/maxItems 地板/上限（:241-248, 311, 377, 432-434）；行项编辑经 projected item scope 写回（b32/combo-renderer.test.tsx 断言正确行为）                            | —                                                                                                                |
| 4   | 表单参与                    | pass | name/required/validation 挂接（useFieldPresentation :251-254 + definition validation :578-611，minItems/maxItems 规则入提交校验）；disabled/visible 响应（presentation.effectiveDisabled/readOnly :500）；提交数据形状 = 数组项对象（combo-renderer.test.tsx:72-81）；data-field-\* 由 FieldFrame 输出（wrap: true，field-frame.tsx:226-235）                                                                                                          | —                                                                                                                |
| 5   | DOM 与选择器契约            | pass | 根 `nop-combo` + data-slot=field-control（:504）；item `data-slot="combo-item"`（:147）/`combo-item-body`（:149）/`combo-item-actions`（:157）/`combo-add`（:540）/`combo-remove`（:189）/`combo-move-up`/`combo-move-down`（:164,175）/`combo-empty`（:506）；契约冻结于 combo-renderer.test.tsx:203-214 + b32-combo-remove-when.test.tsx；data-field/data-renderer/data-cid 由 FieldFrame 输出                                                       | P2-2 已修复（design.md §10 `nop-combo__item` → 实际 `data-slot="combo-item"`，C-06 BEM 移除后文档同步）          |
| 6   | 嵌套 schema 分类            | pass | items region（region 语义，:575 params ['index','value']）；removeWhen lazyEval 表达式 prop（:570，08-02 机制 value→表达式，编译期 lazyEval handle）；无 deepFields 残留；行 scope 隔离：createItemScope/createItemFormProxy/createProjectedValidationRuntime（:96-115）+ instancePath 逐项（:288-300）；嵌套 combo-in-combo 写隔离（b32-array-combo-nested-isolation.test.tsx C1:45-94）与 per-item action 写回隔离（C13:235-277）——bug 样板专项 pass | —（08-02 机制一致：region→items、value→removeWhen lazyEval、event→onAdd/onRemove/onReorder）                     |
| 7   | 事件与 action 契约          | pass | onAdd/onRemove/onReorder 事件注册（:572-574）并在 handleAdd/handleRemove/handleMove 内 `props.events.onX?.()` 派发（:372,392,416）；事件为空参派发（无 payload 契约声明，normalizeActionEvent 语义无定制需求）；addItem/removeItem/moveItem 句柄 op 结果 {index}/{outOfBounds}/{skipped}（:431-493，冻结于 composite-field-handles.test.tsx）                                                                                                          | —                                                                                                                |
| 8   | a11y                        | pass | add/remove/move-up/move-down 按钮 aria-label 走 t()（:166,177,192，含 defaultValue 回退）；remove 禁用态 hover:text-destructive 区分；按钮 size icon-sm 键盘可达；item 内容区域为字段组合（各子字段自带 a11y）                                                                                                                                                                                                                                         | —                                                                                                                |
| 9   | i18n                        | pass | `flux.form.noItems`（:507）/`flux.form.addItem`（:546）/`flux.form.remove`/`moveUp`/`moveDown`（:166,177,192）keys 均存在 en-US.ts:172-192 + zh-CN.ts 同步；aria-label 也走 t()                                                                                                                                                                                                                                                                        | —                                                                                                                |
| 10  | 四态覆盖                    | pass | 空态 `combo-empty`（:505-509，t('flux.form.noItems')）；禁用/readOnly → interactionDisabled 隐藏增删移控件（:500, 156, 535）+ **item 字段经 FormLayoutContext.staticReadOnly 传播只读（P1-2 修复后，:146-160）**；minItems 地板禁用 remove（:129-130, 377）、maxItems 上限禁用 add（:311, 541）；错误态经 FieldFrame data-field-invalid + field-error 槽（wrap: true）；无崩溃路径（EMPTY_ITEMS 兜底 :46-50）                                          | P1-2 已修复（readOnly/disabled 传播到 item 字段；test-first：composite-readonly-propagation.test.tsx）           |
| 11  | 异步生命周期                | pass | 无内嵌 IO（组合宿主动作由 helpers.evaluateCompiled/dispatch 经 env）；行 scope 求值为同步投影；无裸 Promise（composite 编辑操作均为同步 form runtime 调用）                                                                                                                                                                                                                                                                                            | n-a（无异步数据路径；远程候选集为 design §9 明确 successor）                                                     |
| 12  | 组合宿主场景                | pass | 单测：combo-renderer.test.tsx 8 用例（写回/增删移/句柄/min/max/markers）+ b32 嵌套隔离 3 组 + b32-remove-when（per-row 门控）；真实浏览器：`c3-1-host-surfaces.spec.ts` host-combo-submit（form 内 combo 多行嵌套编辑 + 提交，bug 73 模式 + 行 scope 不污染）全绿 + w4c-composite-form-family.spec.ts combo 3 场景回归绿                                                                                                                               | Phase 3 完成（见「组合宿主场景」节）                                                                             |
| 13  | 样式契约                    | pass | widget 自样式（卡片边框 + stack-\* 语义的 gap 类 :147, 504）；根仅 marker + meta.className（:504）；item 卡片内部布局属 widget 设计；无 BEM（C-06 已移除 `nop-combo__item`）；cn() 合并；`check:audit-styling-suspects` form-advanced 0 命中                                                                                                                                                                                                           | —                                                                                                                |
| 14  | React 19 规范               | pass | ComboItem React.memo + 显式逐字段比较器 + instancePathEqual（:205-224，per-item 重渲染隔离，b32 C12 断言）；useCallback 仅用于事件处理/写值（Compiler 友好）；console.warn effect 为外部同步；无 effect+setState 镜像                                                                                                                                                                                                                                  | `check:audit-react19-optimization-candidates` 命中 memo/useCallback 为复合字段族既有设计（b32 C12 验证其正确性） |
| 15  | 性能边界                    | pass | itemEntries useMemo + 稳定 itemKey（buildStableObjectItemKeys :284-300，重复 key 回退兼容身份 :302-309）；useCurrentFormState/useScopeSelector 路径限定订阅（:256-274）+ 数组相等比较器；无 O(n²) 热点（removeBlockedByIndex 仅 removeWhen 配置时启用 :313-342）                                                                                                                                                                                       | —                                                                                                                |
| 16  | 测试质量                    | pass | 既有 4 文件覆盖正确行为断言（form state 写回、handle op、markers、嵌套隔离、render-count 隔离、removeWhen 门控、提交校验 b32-array-submit-and-validate）；本族新增：input-table 同源 removeWhen 契约 + combo/input-table/transfer/picker 宿主场景 e2e；无 not-throw 空断言                                                                                                                                                                             | 本族新增测试见修复记录；P3-1 卡内记录                                                                            |
| 17  | 文档对照                    | pass | design.md §4/§5 与 ComboSchema 对齐（P2-1 修复后）；§10 marker 契约同步（P2-2 修复后）；§8 canonical composite handle 与实现（:424-494）一致；§7 值 owner 归属与实现一致；quick-reference 无组件级词条（n-a，与 form 卡同裁定）                                                                                                                                                                                                                        | —                                                                                                                |
| 18  | 注册、包边界与 IO/安全红线  | pass | 定义经 `formAdvancedRendererDefinitions`（index.tsx:82-99）单注册 + src/index.tsx 导出（:22）；无 surface 双注册；playground combo-lab-page.tsx 存在（2 场景）；无浏览器 IO（INV-1 合规，唯一 `import()` 为类型注解）；复用 @nop-chaos/ui Button/cn；`check:audit-runtime-raw-schema-reads`/`check:audit-fieldframe-bypasses`/`check:audit-hardcoded-type-dispatch` 0 命中；label 渲染走 React 文本节点（无 XSS 面）                                   | —                                                                                                                |

## 发现清单

- [P0] 无
- [P1-1] 无（初始审计）
- [P1-2] `readOnly`/`disabled` 仅锁定复合组件自身 chrome（增删移按钮），item 字段 presentation 只看自身 schema props（`useFieldPresentation` readOnly 源：field-presentation.tsx:51 + form-state.ts:192）——item 内嵌字段保持可编辑、写回照常落 store（**真实浏览器宿主场景 host-itable-disabled 实证**；combo/input-table 同根因 → 共性缺陷，CX-8 事后回写）→ 状态: fixed（ComboItem 以 `FormLayoutContext.Provider staticReadOnly` 传播 item 布局只读（combo-renderer.tsx:117-129, 168-170）；test-first：composite-readonly-propagation.test.tsx 4 用例先红后绿）
- [P2-1] design.md §4/§5 声明 `multiple` 而 ComboSchema 无此字段（`docs/components/combo/design.md:21,27`）→ 状态: fixed（design.md §4/§5 移除 multiple 声明，与 ComboSchema/定义一致）
- [P2-2] design.md §10 承诺 `nop-combo__item` 每项 BEM marker（`docs/components/combo/design.md:50`），实现已按 C-06 移除 BEM、稳定契约为 `data-slot="combo-item"`（combo-renderer.tsx:147）→ 状态: fixed（design.md §10 同步为 data-slot 契约）
- [P3-1] 根 div 输出嵌套 `data-slot="field-control"`（:504，与 FieldFrame :258 重复；form-advanced 族 array-field/object-field/condition-builder 同模式，C2.2 input-number P3 同先例）→ 状态: 卡内记录，归 CR 集中裁定

## 组合宿主场景（真实浏览器验证）

- 场景: form 内 combo 多行嵌套编辑并提交（bug 73 模式 + 行 scope 不污染专项）| 断言: programmatic DOM（c3-1-host-surfaces.spec.ts host-combo-submit）| 结果: pass（2 行编辑 + 新增行 + 行序移动 → Submit → echo 提交值形状与行隔离正确）
- 场景: w4c 复合表单族 combo add/remove/reorder 回归 | 断言: programmatic DOM（w4c-composite-form-family.spec.ts）| 结果: pass（3/3 全绿）

## 修复记录

- plan: `docs/plans/2026-08-03-0921-1-c3-1-form-advanced-composite-input-family-audit.md` Phase 2/3
- test-first 证据: `composite-readonly-propagation.test.tsx`（4 用例：combo/input-table readOnly+disabled 单元格只读，先红后绿——宿主 host-itable-disabled 实证缺陷，in-session 工作区未提交属设计内）
- 实现: `combo-renderer.tsx`（ComboItem FormLayoutContext.Provider staticReadOnly 传播，P1-2 共性 → CX-8）、`docs/components/combo/design.md` §4/§5/§10
- 验证: `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（939）；宿主场景 c3-1 6/6 + w4c 回归 8/8（详见 plan Phase 4 与 daily log）

- 独立 closure audit: 见 plan `## Closure` 节（独立子 agent fresh session）

## Closure

- 独立 closure audit: pass（见 plan `2026-08-03-0921-1` Closure 节，CLOSURE_VERIFY fresh session）
