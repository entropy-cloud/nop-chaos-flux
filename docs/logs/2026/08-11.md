# Development Log - 2026-08-11

### 2026-08-24 追记（plan `2026-08-11-1929-3` Phase 1 执行：五份历史 plan 状态事实性修正，P1-03/P1-04/P2-22/P2-23/P2-34）- **背景**：执行 `docs/plans/2026-08-11-1929-3-claim-vs-reality-plan-doc-contract-integrity-remediation.md` Phase 1（本 log 按该 plan Exit Criteria 指定路径登记修正记录；执行日为 2026-08-24）。五份 plan 均按 live 代码逐项核对后修正，详录见 `docs/logs/2026/08-24.md`。

- **gantt-ai-e2e plan（P1-03）**：`completed → in progress` + 新增 Outdated Note；Phase 0 七项按 live 核对（6 项落地补勾、bars-width 项标注 superseded-by-minHeight-route）；撤回 Closure Gates 中「全部 ~80/~100 功能点 e2e 覆盖」「无 waitForTimeout 残留」等失真 [x]（live：gantt 50 tests/4 specs + ai 47 tests/13 specs，waitForTimeout 残留 20+4 处，gantt-perf 用 allowConsoleErrors(10)）；closure Status Note/Evidence 改写为诚实状态。
- **452 plan（P1-04 plan 部分）**：`completed → in progress` + Outdated Note；Phase 1（3 项待办）与 Phase 2（11 项）经 live 核对全部落地补勾（surfaceForm 链路 + componentId/name 统一，锚点注记在各项）；Status Note 改写；`schema.ts:83` ActionShapeLikeFields.componentName 幽灵字段移交本 plan Phase 2。
- **444 plan（P2-22）**：保持 `completed`，新增 Outdated Note + 全量 checklist 回写——11 个 workstream 逐项 live 抽核补勾（带核对锚点）；识别 3 项偏差（17-03 rename 未落地 / 15-01 stopWhen null-member 特例仍在 `api-data-source-controller-state.ts:147-158` / 17-04 命名映射文档未见实证）保持未勾选并移入 Deferred But Adjudicated。
- **2026-07-28-1430 plan（P2-23）**：Phase 1（6 项）与 Phase 2（6 项）、Phase 3（7 项）按实际落地形态补勾；close 两项按 live 形态（sync fire-and-forget，`surface-runtime.ts:199-229`）改写；新增「执行偏差摘要」（hook 存储形态 ActionSchema 化 / sync close / 触发位置移至 form.tsx 三处偏差）。
- **CR plan 2026-08-06-0329-1（P2-34）**：Phase 3 的 4 项 0529-1 R2 追加项（19-1 tree-session ack 看门狗 / 19-2 calendar exportToPNG / 23-1 xui-roles-plugin barrel / 23-2 gantt components 死代码删除）经 live 核对确认已落地，补勾并附锚点；Closure Audit Evidence 补 P2-34 修正注记（原「全部 [x]」声称在补勾前与文件状态不符）。

### 2026-08-11（component-audit-round2 mission driver：19:29 多/单双审计 + plan `2026-08-11-1929-1` renderer 核心路径缺陷五 phase 修复 + plans 1929-2/1929-3 起草 + bug note 161）- **背景**：19:29 双审计（multi + open，mission component-audit-round2）产出 3 条 P1 + 折叠 P2；P0/P1 路由到三份 plan（1929-1 renderer 核心路径 / 1929-2 flux-bundle facade 宿主契约 / 1929-3 claim-vs-reality plan-文档契约完整性），折叠 P2/backlog 合并登记 `docs/backlog/audit-followups-2026-08-11-1929.md`；本 log 记录 1929-1 执行 + 1929-2/1929-3 草案起草（执行待后续轮次）。

- **审计产出（19:29）**：multi-audit 发现 P1-01（flux-bundle dist/index.d.ts 导入未声明 `@nop-chaos/flux-core` → 干净宿主安装 TS2307，check:flux-bundle-pack 只查 manifest 不查 d.ts 覆盖盲区，tsc 实证复现）+ P1-02（facade 类型承诺 10 props 仅转发 6，宿主按公开类型传入静默 no-op）+ 文档/契约族 P2（action-scope 幽灵契约、live shape 代码块残留、plan 状态事实性矛盾 ×3、surface-lifecycle 锚点漂移等）；open-audit 发现 P1-03（wizard 异步提交期导航未锁定，提交续体过期闭包）/ P1-04（dropdown hover 菜单 portal 逃逸即关，对鼠标用户不可用）/ P1-05（`minDate:'now'`/`value:'today'` 相对日期表达式在渲染器接线死区）+ P0-01（barcode 关闭态挂载 → 扫码零检测，P0 高价值缺陷）+ 渲染器族 P2（upload clearAll 在途写回、null 跳过复活种子、timeline/button/input-number/dashboard 等）；6 并行子 agent 初审 → 主 agent 逐条 live 复核（工具基线：`pnpm check` 仅既有登记红，oversized 2 exempt locale + audit-event-dispatch-ctx 6 industrial；audit-runtime-raw-schema-reads / audit-fieldframe-bypasses 零命中）。审计文件 `docs/audits/2026-08-11-1929-multi-audit-*.md` + `open-audit-*.md`（Audit Status: planned，贡献 P0/P1 到已起草 plan）+ 分析档 `docs/analysis/2026-08-11-1929-open-audit-component-audit-round2/`（round-01/02/03）+ P2 路由登记 backlog。
- **plan 1929-1 Phase 1（P0-01 barcode 扫码主流程 + 折叠 P2-11/P2-17）**：**RED 实证 ×2**——`use-barcode-detect.test.ts`「关闭态挂载（video null）→ 打开 → 输出」序列断言（修复前 advanceTimers 后零调度 RED）；overlay「open=false 挂载 → open=true」转换用例。**根因**：hook 主 effect deps `[]` + 挂载瞬间必为关闭态 → `const video...; if (!video) return;` 早退，`poll()` 永不启动；`:77-88` 内建 video 缺失重试本就完备，早退纯冗余致命。**修复**：删除 `use-barcode-detect.ts:45-46` 早退（主 effect deps 保持 `[]`，轮询自挂载运行，video 缺失由 poll 内建重试）；离线横幅 i18n 文案如实化（en/zh 双副本，不再承诺不存在的自动提交）；`design.md` 同步 continuousScan 默认 false / wasmUrl fail-closed 无内置 CDN；随收口删除 `clearCameraAvailabilityCache` 死导出（P2-17 barcode 子项，camera-utils.test 改 `vi.resetModules()` + 动态 import 缓存隔离）。**bug note 161 落档**（跨 package 边界，全 8 节）。
- **plan 1929-1 Phase 2（P1-02 pivot loading 周期空白 + 折叠 P2-17 pivot 子项）**：**RED 实证 ×1**——loading=true → false 往返：修复前 loading=true 时旧实例存活于无 canvas div 的 DOM 外 + loading 翻回 false 走 :175-182 no-op 分支（canvas div 挂载但实例 effect 不重跑 → 空白）。**修复**：实例生命周期 effect 纳入 `loading` 且与 `empty` 同构（loading=false 时走 release 路径：释放 instanceRef + clearExposedInstance；翻回 false 因 instanceRef null 走新建路径绑定新 canvas div；卸载 effect 无需变化）。**裁决**：`indicators[].format` 零读取（schemas.ts:19 + definitions:195，无文档承诺生效）→ 删除定义（schemas + definitions + design.md 表同步）。
- **plan 1929-1 Phase 3（P1-03 wizard 提交期导航锁定 + 折叠 P2-09/P2-10）**：**RED 实证 ×3**——(1) commitStep await 挂起期间 goToStep/goPrev → 修复前导航成功 + 迟到 `wizard:change`；末步挂起期间 goToStep → 修复前 `wizard:complete` 提前派发；(3) 数值 `value` 不按 key 匹配。**修复**：`goToStep`/`goPrev` 在 `lifecycle.committing` 时 early-return（Next 保持既有 disabled）；commitStep 续体读提交时冻结快照（committedStepIndex/Key/isLastStep，杜绝过期闭包——payload 的 currentStepKey/Index 与 complete/advance 全走冻结值）；`:92-94` 数值 value 先 key 匹配后 clamp（对齐 steps/timeline 同族先例，schemas.ts:33 + layout-renderer-definitions.ts:56 措辞同步）；`:682-693` 错误盒渲染 `stepError` 真实消息（仅空时回退通用 i18n）。新增 `wizard-commit-navigation-lock.test.tsx`。
- **plan 1929-1 Phase 4（P1-04 dropdown hover 可用性）**：**RED 实证 ×1**（jsdom + 真实 DropdownMenu 不 stub portal）——触发按钮 mouseLeave 同步关闭 → portal 菜单逃逸即关。**裁定**：最小改动方案 (a)——wrapper onMouseLeave **150ms 延时关闭** + portal 菜单 onMouseEnter 取消 / onMouseLeave 重臂；click 模式零影响（延时仅作用于 hover 路径），disabled/触摸路径无新竞态；`data-trigger="hover"` 语义保留。
- **plan 1929-1 Phase 5（P1-05 相对日期表达式接线）**：**RED 实证 ×4**（渲染器级集成，对齐「接线面假绿」教训）——input-date `minDate:'now'` 日历不禁过去日期 / `value:'today'` 输入值不解析为今天 / input-datetime + date-range 同型各一。**根因**：`resolveRelativeDate` 产出 ISO-8601 串，被 valueFormat 锚定正则 `^…$` 拒绝 → `parseDate` 返回 undefined → 约束静默丢弃。**修复**：`parseDate` 增 ISO 串 fallback 分支（合法 ISO instant 直解）；`input-date`/`input-datetime`/`date-range` value 路径接入 `resolveRelativeDate`（非相对串原样透传）。新增 `input-date-relative-wiring.test.tsx`（渲染器级），`input-date-relative.test.tsx` 12+ 单函数用例零回归。
- **验证**：四包测试全绿——form **93 files / 803 tests**、layout **16 files / 117 tests**、pivot **3 files / 56 tests**、scheduling **82 files / 920 tests**（含本 plan 新增 7 用例：barcode ×2 + overlay ×1 转换 + pivot ×1 往返 + wizard ×3 → 各 phase 先红后绿记录在案）；四包 typecheck 绿；i18n en/zh 双副本对称修改。**plan 1929-2/1929-3 草案**：1929-2（flux-bundle facade P1-01 TS2307 d.ts 依赖闭合 + P1-02 props 转发收敛，2 Phase planned）+ 1929-3（claim-vs-reality：gantt/452/444/2026-07-28-1430/CR 五份 plan 状态事实性修正 + action-scope 幽灵契约文档收口 + surface-lifecycle 锚点校准，Phase 1 planned）起草完毕待执行。
- **plan 状态**：1929-1 五 Phase 全 completed + 全 checklist [x]；**Plan Status 保持 `active`**——Closure Gates 待独立 closure-audit（fresh sub-agent）后勾选（执行 session 不自审）；1929-2/1929-3 保持 `active` + Phase `planned`（草案已评审落档，执行待后续轮次）。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0335-3` 契约/文档族 P2 收口——FIND-07 contentResolverName 死字段 drop + FIND-08 未导出类型补导出 + FIND-09 @tiptap/core optional peer + FIND-14 jsonrepair 去重 + FIND-19 双命名接口裁定 + FIND-10/11/15/16/17/18 六处文档锚点/契约漂移 + bug note 154）- **背景**：执行 plan `docs/plans/2026-08-11-0335-3-ai-contract-doc-truthfulness-remediation-2245.md`（`2026-08-10-2245` 双审计契约/文档族 11 条 P2；共享审计 `docs/audits/2026-08-10-2245-multi-audit-ai-invariant-loop.md`）。5 Phase 全 completed，grep 门禁（contentResolver / registerImport / 失效行锚）+ 类别清扫（未导出类型面 / manifest 三区 / 消费面核对）+ 登记处同步（engine.md / renderers.md / design.md / implementation.md / invariant-catalog.md / gates.md / cycle2 记录 / bug note 154 / daily log）。

- **Phase 1（FIND-07 [P2] 死字段 drop + FIND-08 [P2] 未导出类型补导出）**：**基线 grep 实证**：`rg contentResolver` 于 `packages/flux-renderers-ai/src/` 仅 2 命中（schemas.ts:123 声明 + ai-renderer-definitions.ts:106 注册），零消费者——真实机制 = 注入 `contentRenderers` matcher 数组（ai-bubble/index.tsx:72）。**FIND-07 落地**：schemas.ts 删字段 + definitions 删注册 + renderers.md 删声明 + `flux-guide/flux-types/schema.d.ts:1645` 删类型 + `flux-guide/design-patterns/ai.md:163` 删字段表引用（1606-3 autofocus drop 全仓 grep 先例）；**目标面零残留**（5 文件 exit 1），**豁免面显式登记**（源审计 / roadmap backlog / per-component card / 本 plan / ai-survey 历史分析，Minimum Rule 21 不重写）。**FIND-08**：RED 断言（scratch `import type { ConversationStorageErrorEvent } from '@nop-chaos/flux-renderers-ai'` → TS2305）→ index.ts Group 3c 补 `type ConversationStorageErrorEvent` → 正向断言通过（scratch 删除）。**类别清扫（未导出类型面，结论入档）**：入口 106 个导出类型全量核对——公共签名引用链全覆盖；仅 3 个「未导出」为内部模块类型/测试 helper（AutoSaveDeps / ConversationStorageBootstrapDeps / MockStorageCalls），不入入口面。engine.md §8.6 补 FIND-08 注记。
- **Phase 2（FIND-09 [P2] @tiptap/core manifest + FIND-14 [P2] jsonrepair 去重）**：`@tiptap/core: ^3.27.1` 升 peerDependencies + peerDependenciesMeta（optional: true），devDependencies 保留——与 `@tiptap/react`/`@tiptap/starter-kit` 同构；devDependencies 移除冗余 `jsonrepair`（dependencies 保留，生产 import ai-tool-call.tsx:11）。`pnpm install --lockfile-only` 零变更（版本已由同族带入）。**类别清扫（manifest 三区「声明-引用」一致性，结论入档）**：生产 import 10 个外部包全部落在依赖区覆盖内；无「生产 import 未声明」、无「dependencies 声明零引用」；jsdom + workspace 测试面仅 devDependencies。`check:workspace-manifest-deps` PASS + AI 包 typecheck PASS。
- **Phase 3（FIND-19 [P2] 双命名接口文档化裁定）**：**裁定**：不做结构性合并/删除（公共导出面变更需人工确认）；`AiConversationController`（namespace/action-provider 消费面，MaybePromise 语义）与 `AiConversationControllerBridge`（hook 产出面）结构性可赋值、双命名保留（hook 产物类型稳定性）；「任一未来成员只加其一即分叉契约」登记 watch-only residual。**落地**：Bridge 定义补 doc-comment（锁步演进要求 + FIND-19 裁定来源）；engine.md §8.6 + renderers.md §1.4 各补「双命名接口关系」注记（与 R1-F5 多实例注记同文档面）。**类型兼容验证**：AI 包 + playground typecheck PASS（`ai-persistence-demo.tsx:73` Bridge→Controller prop 绑定实证）。**消费面核对**：`rg AiConversationController` 8 文件全命中（生产 5 + 测试 2 + 文档 3），零未记录面。
- **Phase 4（FIND-10 [P2] §7.1 代码块漂移 + FIND-11 [P2] §9.4 虚构 API + FIND-15 [P2] 运行清单漏文件 + FIND-17 [P2] §14.3 line 锚失效）**：**FIND-10**：engine.md §7.1 `ChatMessageUIState` 同步 live types.ts（补 `ChatMessageEditingState` + thinking 形状 + `editing` 字段）。**FIND-11**：engine.md §9.4 重写为真实机制（env.importLoader = `createAiImportLoader` 解析 `ai://` spec → 模块 `createExpressionHelpers: ({ connectors: { mock } })`；schema `xui:imports: [{ from: 'ai', as: 'ai' }]` + `${$ai.connectors.mock}`，对齐 playground mock-ai-env.ts:96-125）；design.md §11.3 注入块改 `useConversation({ storage })`（顺带修正相邻漂移：schema 无 storage 字段）；implementation.md §6 示例改 `createAiImportLoader` 形态；**注记措辞避免复述虚构 API 名**（grep 门禁口径）。**FIND-15**：engine.md 运行命令清单补 `engine-invariants-p2.test.ts`（现 8 文件）+ 注释行标注 ⑧ 泄漏臂归属；gates.md ⑧ 行运行列「同上」改显式文件。**FIND-17**：engine.md/types.ts 的「design.md §14.3 line 556」改节引用去行号（§14.3 标题为稳定锚点——选优理由入档）。**Proof**：`rg registerImport` 目标面 exit 1 零残留 + `rg "line 556"` exit 1 零残留 + focused 实跑 `engine-invariants-p2.test.ts` 1 file / 3 tests PASS。
- **Phase 5（FIND-16 [P2] catalog §4.1 controller 锚点 + FIND-18 [P2] cycle2 记录行号失效 + 收口）**：**FIND-16**：invariant-catalog.md §4.1 controller 锚点 `:425-430` → live `use-conversation.ts:666-671`（对象字面量，P2-16 校准表漏掉的「adapter 非函数字段」注记行）。**FIND-18**：cycle2-findings.md/adjudication.md 行号校准（K-⑩-3 autoSave 引用改 `use-conversation-autosave.ts:56/:101` + create-engine catch `:579-580`/`commitOrDropResidue :495-507`；W-E onAfterRequest `:552-554`；W-⑨-a connector-missing 早退 `:215-238`；W-⑨-c clear `:646-660`/setMessages `:150-162`/`:247` 清除）——两文件头部补 **anchor-epoch 注记**（行号 = 2026-08-09/10 时点，live 以复核为准，multi-audit 交叉模式 4）；「修正痕迹」行补豁免注记（历史验证记录不重写）。**Proof**：`rg "use-conversation.ts:190|:203-209|:425-430"` 目标文件零残留（豁免注记除外）。**bug note 154 落档**（FIND-07 drop + FIND-08 导出 + FIND-19 裁定契约族，按 guide 全 8 节）。**收口执行中处理 oversized 新命中**：FIND-19 Bridge doc-comment 使 `use-conversation.ts` 698→709 触发 `check:oversized-code-files` 硬门禁 → 注释压缩（保留语义：Bridge 产出面 + 结构性可赋值 + 锁步要求 + FIND-19 来源）回落 699 行，check 恢复仅 2 既有豁免红。
- **验证**：AI 包 **80 files / 704 tests 全绿**；AI 包 + playground typecheck 绿；`check:ai-engine-invariants` exit 0 零命中；`check:active-doc-code-anchors` exit 0；`check:workspace-manifest-deps` exit 0；全仓 `pnpm check` 仅既有登记红零新增（audit-event-dispatch-ctx 6 条 industrial + oversized 2 exempt locale——git stash 基线复跑确认非本 plan 新增）。
- **plan 状态**：Phase 1-5 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 于独立 closure-audit 完成后全 [x]（closure-audit = 独立 fresh sub-agent 两轮：Round 1 `ses_01265ec1cffeQKmCkPe8kKCRSz` verdict `issues`（唯一 blocking B1 = plan 文本 Phase 5 遗留 planned 未勾选，工作已落地；2 条 non-blocking 措辞观察）→ 修正（Phase 5 勾选 + evidence 补写 + Phase 3 编译期断言措辞修正 + roadmap FIND-18 措辞）→ Round 2 `ses_012603b77ffe6LrsM5PnUHdn5s` verdict **approved**，G1-G8 全 PASS，证据见 plan Closure 节）；**Plan Status → `completed`**；roadmap「2026-08-10-2245 双审计 P2 填充」节补 **P2 路由收口注记（0335-3 契约/文档族）** + 11 条 work item 标记 done（2026-08-10-2245 双审计 P2 全数闭环 23/23）。
- **mission-driver verify 复跑确认（full-green）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 66/66 tasks 全绿（AI 包 80 files/704 tests）。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0335-2` renderer/UI 族 P2 收口——R2-F1 citations 显式空 sources + R2-F2 clipboard 假成功 + R3-F1 timestamp 非法值崩溃 + FIND-13 空态 className + FIND-20 region 命名 + R1-F5 namespace 冲突守卫 + bug notes 159-160）- **背景**：执行 plan `docs/plans/2026-08-11-0335-2-renderer-ui-p2-remediation-2245.md`（`2026-08-10-2245` 双审计 renderer/UI 族 6 条 P2；roadmap-sourced plan，无 `Source Audits:` 面）。5 Phase 全 completed，test-first 先红后绿（14 条新用例）+ 类别清扫强制（4 族）+ 登记处同步（design.md / engine.md / renderers.md / i18n / bug notes 159-160 / daily log）。

- **Phase 1（R2-F1 [P2]：citations 显式空 sources）**：**RED 实证 ×2**（`ai-citations.test.tsx`）——显式 `sources: []` + metadata.sources 非空 → 修复前解析落到 metadata（断言零引用卡 RED）；`data-sources` part 同面 RED。**修复**：`resolveSources` 改 `Array.isArray(explicitSources)`（含空数组）即权威返回 `normalizeSources`——metadata / data-sources fallback 仅在 explicit 为 undefined/非数组时触发（P2-7「显式空数组 = 显式意图」sibling 契约）；+1 契约边界测试（非数组 explicit 回退 metadata）。**类别清扫（显式输入覆盖 metadata 面，结论入档）**：ai-feedback `normalizeActions`（P2-7 已修 ✓）；ai-citations `resolveSources`（本面 ✓）；ai-token-usage `resolveUsage`——显式 `usage` prop 优先，但 `{}` 非合法 `AiTokenUsage`（`normalizeUsage` 归 null 属「无效值」非「显式空」，与 `[]` 是合法空列表语义不同）→ 不构成同族缺陷，记录结论不改；其余 `metadata.*` 消费面（isError / toolStatus / branchId / toolLoopMaxReached）为 engine 内部标记非 host 显式输入覆盖面。既有 metadata 路径用例零回归。
- **Phase 2（R2-F2 [P2]：clipboard 假成功）**：**RED 实证 ×2**（`markdown-and-data-part.test.tsx`，直接操纵 `navigator.clipboard` 走生产 adapter 路径）——API 缺失 / writeText 缺失 → 修复前 `undefined` 经 `Promise.resolve` 翻「Copied」（断言不翻 RED）。**修复**：`clipboardAdapter.writeText` 缺失面改 `Promise.reject(new Error('navigator.clipboard is not available'))`——`handleCopy` 既有 `.catch` 保持按钮原态，与拒绝写路径对称。**类别清扫（全包复制面，结论入档）**：markdown CodeBlock（本面 ✓）；**ai-feedback `copyMessageText` 同根因 P3 登记成员顺带收敛**（缺失面 `Promise.resolve()` → reject，+2 回归测试钉住）；ai-attachments paste 为读取面（`clipboardData.files`）非写入面不适用。既有「正常写成功翻 Copied」「reject 不翻」用例零回归。
- **Phase 3（R3-F1 [P2]：timestamp 非法值崩溃）**：**RED 实证 ×3**（`p1-renderers.test.tsx`）——`metadata.createdAt` = NaN / `Number.MAX_VALUE` / ±Infinity → 修复前 `toISOString()` 抛 RangeError 崩树（断言不抛 RED）。**修复**：`new Date(createdAt)` 后 `Number.isNaN(date.getTime())` 早退返回 null——单点覆盖全部 Date 不可表示形状（isFinite 单独不够：`Number.isFinite(Number.MAX_VALUE)` 为 true 但 Date 越界），`toISOString()` 不再接触非法 Date。**类别清扫（host 可写 metadata 数值面，结论入档）**：ai-token-usage（`hasNum`/`cost` 已 isFinite ✓）/ timestamp（本面 ✓）/ ai-citations `normalizeSourceEntry`（index isFinite ✓）；无其他 `new Date`/`toISOString` 消费 host 可写数值。既有合法 timestamp 用例零回归。
- **Phase 4（FIND-13 [P2] 空态 className + FIND-20 [P2] region 命名）**：**RED 实证 ×2**——FIND-13（`renderers.test.tsx`）：空会话根 class 缺 schema className（断言含值 RED）；FIND-20（`ai-attachments-a11y.test.tsx`）：region 根无 aria-label（断言非空 RED）。**修复**：空态分支改 `cn('nop-ai-message-list', props.className)`（对齐非空分支 + ai-prompts/ai-suggestions 先例）；attachments 根补 `aria-label={t('flux.ai.attachments')}`（i18n en/zh 对称新增 key，`check:i18n-keys` 绿）。既有「非 button 表面」测试断言随契约更新（role/tabindex 面保留、aria-label 面改为新契约）。**类别清扫（空态 className 面 + region 命名面，结论入档）**：ai-prompts/ai-suggestions/ai-token-usage 空态均保留 className ✓；包内 `role="region"` 仅 attachments 一处 ✓（ai-suggestions `role="list"` / ai-tool-call `role="group"` 已有 aria-label）。
- **Phase 5（R1-F5 [P2]：ActionScope namespace 冲突守卫 + 文档化 + 收口）**：**RED 实证 ×1**（`namespace-integration.test.tsx`）——同页双 ai-chat（同一 actionScope）：修复前无任何 warn（断言 `[ai-chat]` warn ≥1 RED）。**修复**：ai-chat 在 `useNamespaceRegistration` **之前**声明 `useLayoutEffect`（layout effect 声明序 = 执行序，被动 effect 会看到本实例已注册的自身 provider 无法区分冲突）——`actionScope.listNamespaces()` 已含 `ai` 时 `console.warn` 一次（指向 design.md §14.2 多实例注记）；注册/注销语义零改动（`registerNamespace` 顶替语义保持）。+1 回归测试（单实例零 warn）。**Decision（完整实例隔离方案，裁定入档 plan Phase 5 Decision Record + bug note 160 + design.md §14.2）**：按实例 namespace / 前缀隔离 / action-scope 多 provider 三方案均涉及 flux-runtime 公共 API 变更或既有 `ai:*` 契约迁移 → 裁定超出 P2 范围入非阻塞 follow-up 需人工确认；P2 守卫 + 文档化消除「静默路由错乱」，多实例走 ComponentHandle 路径（cid-isolated）。**登记处同步**：`design.md` §14.2 多实例注记（后挂载者接管 / 先卸载者注销 / cid-isolated 替代面 / 完整方案 follow-up）；`engine.md`「renderer 面行为注记（2026-08-11，R1-F5）」节；`renderers.md` §1.2 schema 后 R3-F1 host 可写字段防御注记 + §1.4 R1-F5 多实例注记；**bug notes 159-160 落档**（159 R2-F1+R3-F1+FIND-13+FIND-20 renderer 行为族 / 160 R2-F2+R1-F5 host 环境防御族含 P3 成员收敛，按 guide 全 8 节；README 索引补齐 121-158 既有缺口 + 159-160 登记）；daily log 收口记录（本节）。
- **验证**：AI 包 **80 files / 704 tests 全绿**（基线 690 + 本 plan 新增 14 用例：R2-F1 ×3 + R2-F2 ×4 + R3-F1 ×3 + FIND-13 ×1 + FIND-20 ×1 + R1-F5 ×2）；`check:ai-engine-invariants` exit 0 零命中；`check:i18n-keys` 绿；全仓验证见 Closure Gates（typecheck/build/lint/test 全量）。
- **plan 状态**：Phase 1-5 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 于独立 closure-audit 完成后全 [x]（closure-audit = 独立 fresh sub-agent，证据见 plan Closure 节）；**Plan Status → `completed`**；roadmap「2026-08-10-2245 双审计 P2 填充」节补 **P2 路由收口注记（0335-2 renderer/UI 族）** + 6 条 work item 标记 done。
- **mission-driver verify 复跑确认（full-green）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 66/66 tasks 全绿（AI 包 80 files/704 tests）+ `check:ai-engine-invariants` exit 0 零命中；`pnpm check` 仅既有登记红零新增（audit-event-dispatch-ctx 6 条 industrial + oversized 2 exempt locale）。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0335-1` engine/adapter 族 P2 收口——FIND-12 autoSave connector-missing + R1-F2 ⑪ 嵌套写隔离 + R1-F3 no-storage 首会话 + R1-F4 delete-during-load 幽灵 + FIND-22/R2-F3 branching 契约 + bug notes 154-158）- **背景**：执行 plan `docs/plans/2026-08-11-0335-1-engine-adapter-p2-remediation-2245.md`（`2026-08-10-2245` 双审计 engine/adapter 族 6 条 P2；roadmap-sourced plan，无 `Source Audits:` 面）。5 Phase 全 completed，test-first 先红后绿（12 条新用例）+ 类别清扫强制（4 族）+ ⑪ 门禁评估补成员 + 登记处同步（engine.md / invariant-catalog.md / gates.md / bug notes 154-158）。

- **Phase 1（FIND-12 [P2]：autoSave 不覆盖 connector-missing 轮）**：**RED 实证 ×2**（`conversation-invariants-p2.test.ts` 新文件，engine-invariants-p2 先例命名）——connector null 发送用户消息：修复前 `saveMessages` 从未收到含该消息的快照（`saved[convId]` undefined，用户消息只进内存）；remount 后消息不可恢复。**修复**：`use-conversation-autosave.ts` 触发谓词从「`wasProcessing && isDone`」扩展为「`wasProcessing && isDone` ‖ `isDone && messagesGrew`」——per-conversation 消息数水位线（`lastSeenCount` vs `engine.getMessages().length`）判定「error 前收到过用户消息」；**`full` 通道同步订阅**（`clear`/`setMessages` 只发 `full` 不发 `requestState`，clear-then-connector-missing 序列仍可判定）；K-⑩-3 空残留剥离谓词组合保持（无消息 vacuous 轮如 connector-missing regenerate 不持久化）。**类别清扫（autoSave 轮次终态，结论入档）**：completed / aborted / stream-error / tool-no-executor / plugin-throw（onTurnStart 抛错）五面均经 `wasProcessing` 臂持久化（含 K-⑩-3 剥离）；connector-missing 带消息 = 新水位线臂；connector-missing 无消息（regenerate）= 显式排除。既有 autoSave / K-⑩-3 臂测试零回归（adapters 全套 15 files 绿）。
- **Phase 2（R1-F2 [P2]：⑪ plugin ctx 嵌套写隔离）**：**RED 实证 ×3**（`engine-invariants-p1.test.ts` ⑪ nested 块）——plugin 对 `ctx.request.messages[i]` 的 `tool_calls` push / `content` 部件 push / `metadata.nested` in-place mutate 全部写穿 engine 历史（修复前深比较 RED）。**修复**：`projectWireMessage` 白名单字段 + `metadata` 全部改 `deepClone`（`combineDeltaData` 先例），wire payload 与 engine 历史全深度断引用；`types.ts` MessageEngineContext 注释 + `build-context.ts` 同步如实描述隔离深度。**Decision（⑪ 门禁评估，结论入档 gates.md/catalog）**：既有 ⑪ 8 成员仅覆盖数组 + element 层，**需补嵌套深度成员**——已补 3 成员（R1-F2 面无防回归守卫），`check:ai-engine-invariants` 不静态化面维持（行为面裁定不变）、live 零命中。**类别清扫（engine→payload 投射面，结论入档）**：`projectWireMessage`（本面深克隆 ✓）；`sanitizeDanglingToolCalls` 仍浅拷贝——唯二消费者为 buildContext（随后 `projectWireMessage` 深隔离）与 autoSave（O-2 快照进 storage 序列化，无 plugin 变更入口）；buildContext 其余组装面 `tools`/`extraRequestParams` 为 host 自有配置（非 engine state），plugin 变更仅影响出站请求。
- **Phase 3（R1-F3 [P2]：no-storage 首会话 build-on-demand）**：**RED 实证 ×2**（`conversation-invariants-p2.test.ts` R1-F3 块）——无 storage + `initialConversations` 挂载：修复前 `activeEngine` 恒 null（非 null 断言 RED）+ 首会话不可发消息（`engine!` 抛错）。**修复**：`use-conversation.ts` mount effect——无 storage 且 `activeIdRef.current` 已种子（`initialConversations[0]`）且无缓存 engine 时 `buildEngineFor` + cache + `setActiveEngine`（K-⑥-3 同语义跳过 hydrate；strict-mode 双 mount 幂等）。**类别清扫（activeId→engine 缺失面，结论入档）**：storage bootstrap（K-⑥-3 已修）/ no-storage 首会话（本面）/ switchConversation 已建面 / delete 后 next 面（`ensureEngineAndHydrate`）——四面对齐 build-on-demand。既有 K-⑥-3 storage 面测试零回归。
- **Phase 4（R1-F4 [P2]：delete-during-load 幽灵）**：**RED 实证 ×2**（`conversation-invariants-p2.test.ts` R1-F4 块）——gated `loadConversations` 窗口内 `deleteConversation(A)`：修复前 resolve 后 A 复活为幽灵列表项；唯一会话 A 被删后复活为 active。**修复**：`use-conversation.ts` 新增 `deletedDuringLoadRef`（delete 同步记录）+ bootstrap merge 基在非空判断**前**过滤（`loaded = convs.filter(c => !deletedDuringLoadRef.current.has(c.id))`，merge 后清空）——唯一被删会话不 re-seed active。**类别清扫（bootstrap merge 并发变更面，结论入档）**：create-during-load（K-⑦ mirror-append 保留 ✓）/ clearAll-during-load（`listClearedRef` ✓）/ delete-during-load（本面 ✓）/ rename-during-load（**非同类漏网**：rename 读 load 窗口内空 mirror 于入口 no-op——未知 id 文档化语义，且 create-during-load 后 rename 由 mirror-append 保留）。既有 K-⑦ clearAll-during-load 测试零回归。
- **Phase 5（FIND-22/R2-F3 [P2]：branching 契约 + 收口）**：**新用例 ×3**（`engine-branches.test.ts`，行为断言与 live 一致）——FIND-22：`branch-abc` 先前 id → `regenerate()` fallback mint `branch-1`（此前零覆盖，删掉该分支测试仍绿）；R2-F3：`branch-01` → 前进 `branch-2`（`parseInt` 前导零归一化锁死）；`findPriorAssistantBranchId` 对 `branch-abc` 原样透传（不校验格式锁死）。**契约文档化**：`branching.ts` JSDoc + `engine.md` §8.1「A-16 branch id 契约」（parseInt 归一化 / 无后缀 fallback mint / 不校验格式三规则）。**bug notes 154-158 落档**（154 FIND-12 / 155 R1-F2 / 156 R1-F3 / 157 R1-F4 / 158 FIND-22+R2-F3，按 guide 全 8 节）。**收口执行中处理 oversized 新命中**：`use-conversation.ts` 因本 plan 增行 699→739 触发 `check:oversized-code-files` 硬门禁（新增红不可接受）→ 抽取 `use-conversation-bootstrap.ts`（`hydrateConversationsFromStorage` 纯函数 helper，attachAutoSave 先例；初版 custom-hook 抽取被 lint 拒——react-compiler 禁 hook 参数 ref 变更 + useEffectEvent 不可传递——改纯函数 + in-effect `onFirstActiveSelected` 回调保持 useEffectEvent 组件内作用域），文件回落 699 行，check exit 0。
- **验证**：AI 包 **80 files / 690 tests 全绿**（基线 79/678 + 本 plan 新增 12 用例：FIND-12 ×2 + R1-F2 ×3 + R1-F3 ×2 + R1-F4 ×2 + FIND-22/R2-F3 ×3）；AI 包 typecheck/lint 绿；`check:ai-engine-invariants` exit 0 零命中；全仓 `pnpm typecheck`/`build`/`lint` 37/37 + `pnpm test` 66/66 tasks 全绿（Closure Gates 记录）；`pnpm check` 零新增命中（仅既有登记红：oversized 2 exempt locale + audit-event-dispatch-ctx 6 条 industrial）。
- **plan 状态**：Phase 1-5 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 于独立 closure-audit 完成后全 [x]（closure-audit = 独立 fresh sub-agent `ses_012a906afffeowqwtEmlz5mwiv`，verdict issues→remedy 后通过——1 条文本 truthfulness 问题 I-1：本 log 初稿与 roadmap 注记在审计前过早断言「Closure Gates 全 [x] / Plan Status → completed」，已按 D3.2 披露先例修订为待审计口径，审计通过后本段修订为最终态；证据见 plan Closure 节）；Closure 节证据回填；**Plan Status → `completed`**；roadmap「2026-08-10-2245 双审计 P2 填充」节补 **P2 路由收口注记（0335-1 engine/adapter 族）** + 6 条 work item 标记 done。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0008-3` conversation adapter/宿主契约收口——FIND-02 [P1] clearAll×create 反向竞态 + FIND-04 [P1] 命令边界失败保真 + FIND-05 [P1] native adapter 渲染循环守卫 + bug notes 151-153）- **背景**：执行 plan `docs/plans/2026-08-11-0008-3-conversation-adapter-host-contract-remediation.md`（`2026-08-10-2245` 双审计 adapter/宿主契约面 3 条 P1；共享审计 `docs/audits/2026-08-10-2245-multi-audit-ai-invariant-loop.md` 已于 0008-1 收口翻 closed，本 plan 幂等跳过）。4 Phase 全 completed，test-first 先红后绿 ×3 族 + 类别清扫强制（时序窗口 / 命令边界 / getSnapshot 面）+ 登记处同步。

- **Phase 1（FIND-02 [P1]：clearAll×create 反向竞态）**：**RED 实证 ×1**（`use-conversation-clear-all.test.ts` 新增 FIND-02 用例：原子 `clearAll` storage + gated `saveMessages`（drain 悬在 A 的 in-flight 消息 save 上）→ 同 tick `clearAll(); createConversation(B)`（B 的快 save 先落盘）→ 释放 gate → drain 结算 → 延迟原子 `storage.clearAll()` 最后执行抹掉 B 记录——修复前 `store=[]` RED（真实数据丢失窗口复现：列表存活、记录被擦）。**Decision 方案 (a) 采用**：存储清空 = clearAll 时点快照 per-id `deleteConversation` fan-out，**永不调用 `storage.clearAll()`**；方案 (b)（list-mirror-union drain）拒绝（需引入代际 tracking，复杂度不成比例）；原子 clear 的「全删」语义在快照上逐 id 删除同样收敛（快照 = engineCache ∪ pendingSavesRef ∪ conversationsRef，覆盖全部可持存储状态的会话）。**修复**：`use-conversation.ts` `clearAll` drain 后统一 per-id fan-out（`storage?.clearAll` 分支删除）；`ConversationStorageStrategy.clearAll` 保留为 host 直调面（`storage/types.ts` 文档同步，公共 API 零变更）。**测试扩展**：既有 create+clearAll（create 在前）、无 `clearAll` 实现 storage 的 fallback 用例零回归 + 新增原子 clear + clearAll-first 组合（RED→GREEN）。**类别清扫（时序窗口逐对核对，结论入档）**：正向方向（旧写必须先 drain 再删）——deleteConversation await 逐 id 排空 + clearAll 全量 drain 链（K3 守卫保持，零回归）；反向方向（clear/delete 后新写不被误清）——create/rename 链入排空链 + settlement 镜像再校验（同 tick delete/clearAll 跳过写）；deleteConversation 只删目标 id（新 id 天然不触碰）；clearAll 快照 per-id（修复后新 id 永不被扫）；switchConversation 无 storage 写面无窗口。已知残余（裁定记录）：bootstrap loadConversations 失败 → storage 残留记录且快照为空 → clearAll 只删快照（与既有 per-id fallback 语义一致；host 需真全删可直调 `storage.clearAll()`）。

- **Phase 2（FIND-04 [P1]：命令边界失败保真）**：**RED 实证 ×4**（`action-provider.test.tsx` ×2 + `component-handle.test.tsx` ×2）——失败轮（connector `stream` 抛错 → engine 四失败分支 settle `requestState:'error'` + `lastError`）：`ai:send` / `component:sendMessage` 修复前 `{ok:true}` 谎报成功；busy drop（slow connector 在途轮 + 二次发送，runTurn 入口 isProcessing 守卫静默丢弃）：修复前二次发送 `{ok:true}` 且消息未发出。**修复**：`ai-action-provider.ts` `send` 分支——await 后快照 `engine.getState()`，`requestState==='error'` → `{ok:false, error: lastError}`（非 Error 包装 `{cause}`）；发送前 `isProcessing` 预检 → `fail('engine busy: ...')`；`clear` 分支同语义预检（引擎 processing 时静默 no-op 的忙时谎报面补齐）。`ai-component-handle.ts` `sendMessage`/`clear`/`regenerate` 同步（`:67-72` dead-catch 修正为与 provider 同语义的失败保真；engine void-settle 契约零改动）。**类别清扫（AI_NAMESPACE_ACTIONS 7 命令逐条核对，结论入档）**：send = 预检 busy + await 后状态读（本面修复）；abort = 无失败面（idle abort 文档化 no-op 返回 ok）；clear = 预检 busy（本面补齐）；create/switch/delete/rename = controller-bound void 契约（参数同步校验已有；controller 内部语义非 provider 可判定，裁定记录）；component-handle 另 3 方法（getMessages 读 / setMessages 参数校验已有 / regenerate 预检 busy 本面补齐）。**测试扩展**：成功路径既有断言零回归（provider 5 + handle 2 用例不动）+ 失败轮 + busy-drop 双面各 1 新断言（provider + handle 双面，消息未发出断言）。

- **Phase 3（FIND-05 [P1]：native adapter 渲染循环守卫）**：**RED 实证 ×2**（`use-engine-view.test.ts` + `ai-chat-external-engine.test.tsx`）——native adapter 默认构造 engine 绑定 `useEngineView`/ai-chat：修复前无任何可诊断输出（React 自身 uncached-getSnapshot 守卫 + 嵌套更新上限终止渲染，无 adapter 指引）。**Decision 方案 (a)+(b) 组合采用**：`useEngineView` 运行时不稳定 getSnapshot 检测（比较前两次 `getState()` 返回值引用，**连续 ≥2 次不等**才 warn 一次——React adapter 的合法变更产生孤立 mismatch 不连续，零误报；module-level WeakMap 每 engine 一次）+ 指向 `createReactMessageAdapter` 的修复指引；engine.md §8.2/§8.5 文档化 adapter 前置条件。方案 (b) 单独（纯文档）拒绝（host 接入仍裸崩，无运行时可诊断输出）。**修复**：`use-engine-view.ts` 守卫（`getSnapshot` 包装，仅 warn + 提示，渲染行为本身不改）；注释同步。**测试扩展**：native engine 绑定 → warn 断言（render 抛错被 try/catch 包住——React 深度上限仍终止循环，守卫先于崩溃发出）；React adapter 绑定 → 零警告零循环断言。**类别清扫（全部 useSyncExternalStore 绑定面，结论入档）**：`use-engine-view`（useMessage/useConversation→ai-chat 全部 React 绑定面的汇聚点）——native adapter 可击中，本面守卫覆盖；`useMessage` 自建 engine 恒 `createReactMessageAdapter`（缓存 adapter，不可击中）；`useConversation` buildEngine 恒 React adapter（不可击中）；宿主直绑 `ai-chat` `engine` prop = useEngineView 路径（守卫覆盖）；仓内无其他裸 `useSyncExternalStore` 绑定点（grep 核对）。

- **Phase 4（登记处同步 + 收口）**：`engine.md` 更新——§8.2 native adapter 快照稳定性限制（非 React 宿主直绑 + 守卫指引）、§8.5 外部 engine 接入 adapter 前置条件（`createReactMessageAdapter` 必选）、§8.6 存储/驱逐契约 FIND-02 裁定（方案 a + 快照语义）、§Invariants 历史行 + 失败路径记录节补 2026-08-11 plan `2026-08-11-0008-3` 三条；`storage/types.ts` `clearAll` 文档同步；**bug notes 151-153 落档**（151 FIND-02 反向竞态 / 152 FIND-04 命令边界保真 / 153 FIND-05 native adapter 渲染循环，按 guide 全 8 节）；daily log 收口记录（本节）；roadmap「2026-08-10-2245 双审计 P2 填充」节补 **P0/P1 路由收口注记（0008-3 adapter/宿主契约族）**。
- **验证**：AI 包 **79 files / 678 tests 全绿**（基线 79/669 + 本 plan 新增 9 用例：FIND-02 ×1 + FIND-04 ×4（provider/handle 双面） + FIND-05 ×4（use-engine-view + ai-chat 双面））；AI 包 typecheck/lint 绿；`check:ai-engine-invariants` exit 0 零命中；`pnpm check` 零新增命中（仅既有登记红：oversized 2 exempt locale + audit-event-dispatch-ctx 6 条 industrial）；全仓 `pnpm typecheck`/`build`/`lint` 37/37 + `pnpm test` 66/66 tasks 全绿（Closure Gates 记录）。
- **plan 状态**：Phase 1-4 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 全 [x]（closure-audit 由独立 fresh sub-agent `ses_012db03a2ffezNOnwNF4m1Qf5K` 执行——G2-G7 全 PASS（代码落点 + focused 测试复跑绿 + 文档同步 + 无静默降级），G1/G8 为 plan 收尾动作，审计通过后本执行轮完成最终化）；Closure 节证据回填；**Plan Status → `completed`**；源审计 `docs/audits/2026-08-10-2245-multi-audit-ai-invariant-loop.md` 已 `closed`（0008-1 收口翻 closed，幂等跳过）；roadmap 补 P0/P1 路由收口注记。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver verify 复跑：plan `2026-08-11-0008-2` 全量 fresh 强制复跑 + e2e 基线核对）

- **verify 复跑（本记录）**：`pnpm typecheck --force` 37/37、`pnpm build --force` 37/37、`pnpm lint --force` 37/37、`pnpm test --force` 66/66 tasks（0 cached）全绿（AI 包 79 files/669 tests）；`check:ai-engine-invariants` exit 0 零命中；`pnpm check` 仅既有登记红零新增（oversized 2 exempt locale + audit-event-dispatch-ctx 6 条 industrial）。
- **e2e 基线核对（Playwright，Chromium）**：1150 passed / 43 skipped / 12 failed——**12 项全部为 HEAD 既有失败，与本 plan 改动无关**（stash 掉 plan 改动后同 12 项原样复现）：crud-demo ×4（zh 默认语言下 footer 为「第 1-10 条，共 33 条」，`1-10 of 33` 断言在既有 i18n pagination commit `8047c7b75` 后失效）+ scada-edge-cases ×2 + gantt-perf ×2 / kanban-perf ×1（FPS 基线负载敏感）+ playground-entry-pages ×1（route inventory）+ ai-attachments ×2（并行负载 flaky，单跑/HEAD 单跑均绿）。**mission 零新增 e2e 失败**；非 full-green（e2e 未全绿），本记录不声明 full-green。
- 偏差：e2e 复跑顺带重写了 `docs/analysis/2026-07-27-ma43-designer-office-e2e-test-audit/04-e2e-domain-pages.md`（exploratory spec 副作用，格式化为纯表格），已还原至 HEAD 不入本次提交。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0008-2` engine 循环终止/错误载体收口——R1-F1 [P1] tool-loop-max marker 载体归位 + renderer 终止消费 + FIND-03 [P1] A-5 错误载体回归 + bug notes 149-150）- **背景**：执行 plan `docs/plans/2026-08-11-0008-2-engine-loop-termination-and-error-carrier-remediation.md`（`2026-08-10-2245` 双审计 engine 循环终止/错误可见性族 2 条 P1，含 R1-F1 事实修正——审计原文「dangling 第四面」经独立 review + live 复核重述为 marker 错位 + 零消费）。3 Phase 全 completed，test-first 先红后绿 + 无害性守卫 + 类别清扫强制 + 登记处同步。

- **Phase 1（R1-F1 [P1]：tool-loop-max marker 载体归位 + renderer 终止消费）**：**RED 实证 ×2**——`engine-tool-loop.test.ts` loop-max 用例改断**末条 assistant** 带 `toolLoopMaxReached`（修复前 marker 在 tool 消息 tail，`expected undefined to be true` RED）+ 新 render 集成测试 `ai-message-list-loop-termination.test.tsx`（真实 engine 多轮 tool_calls 达上限 → 终止 note 断言，修复前 RED）。**修复**：`create-engine.ts` loop-top break recipe 写面从 `draft.messages[len-1]` 改为**末条 assistant**（从 tail 向前跳过 `role:'tool'` 定位，recipe 内 read-old → build-new → replace，snapshot 恒等纪律保持）；`ai-message-list.tsx` 消息级消费（末条 assistant 带 marker → `data-slot="ai-message-list-loop-limit"` 终止 note，i18n `flux.ai.toolLoopMaxReached` en/zh 新 key，`check:i18n-keys` 绿；auto-scroll trigger 含 marker 标志；非错误态、无操作栏、工具卡保持已提交终态零改动）。**两个钉住旧错位的既有测试随修复更新**（`engine-snapshot-write-path.test.ts` 载体断言 `at(-1)` → 末条 assistant + 恒等纪律保持；`engine-tool-loop.test.ts:210-212` 注释修正），tool 消息计数断言不动。**无害性守卫**：`tool-loop-max: no dangling shape`（`isDanglingToolCallsMessage` 对全部 assistant 恒 false——钉住修正后 ⑩ 契约：正常 loop-max 路径无 dangling，工具响应逐 call 配对，未来重构改变循环顺序即红）。**类别清扫**：⑩ 清理面修正后枚举核对入档（三 dangling 面 tool-no-executor/abort-mid-executor/runOnce-abort 均调 `cleanDanglingAssistantAt` + vacuous drop 面 `commitOrDropResidue`/`isVacuousAssistantResidue`，两套谓词区分：dangling 要求 `finishReason:'tool_calls'` 存在、vacuous 要求不存在）；`toolLoopMaxReached` 写入面 1（`create-engine.ts`）消费面 ≥1（`ai-message-list.tsx` + 2 locale + 3 测试文件）grep 核对清零。
- **Phase 2（FIND-03 [P1]：A-5 错误载体回归）**：**RED 实证 ×2**（`ai-message-list-error-banner.test.tsx`，真实 engine + mock connector 抛 401 / `onBeforeRequest` 拒绝 → 零 chunk 失败轮 → 空产物 drop 后末条为 user → 无任何错误 UI）。**Decision 方案 (a) 采用**：list 级错误横幅（`ListErrorBanner`，`error.tsx` 同文件实现 + `lastUserTextBefore` 扫描 helper 复用 + `requestFailed`/`retry` key + retry `void` 纪律），渲染条件 = `requestState==='error'` 且末条非 assistant；方案 (b)（投影面 `metadata.isError` 载体）拒绝（污染 live 历史，违背 ⑩ 协议洁净目标）。**修复**：`ai-message-list.tsx` 渲染 `data-slot="ai-message-list-error"` + retry `data-slot="ai-message-list-error-retry"`（重发末条 user 文本）；末条为 assistant 时 bubble 级错误继续生效（不重复渲染）；aborted 非 error 态无横幅；engine 侧 drop 语义不变（`engine-invariants-i4.test.ts:139-157` 零回归）。**测试扩展**：401 失败轮 → 横幅 + 重试入口；点击 retry → 新轮发起成功（横幅消失）；onBeforeRequest 拒绝同面；部分内容失败（末条 assistant）→ bubble 错误优先无横幅；abort → 无横幅。**类别清扫**：A-5 失败轮 render 面五类终态核对入档（零 chunk 失败/onBeforeRequest 拒绝/tool-no-executor 空产物 drop → list 横幅；流中部分内容失败 → bubble 错误；connector-missing → ai-chat 根 `ai-chat-error`；abort → 无 UI）；`error-retry.test.tsx` 既有合成用例保持。
- **Phase 3（登记处同步 + 收口）**：`engine.md` 更新——⑩ 面枚举修正注记（正常 loop-max 路径无 dangling + marker 载体契约 = 末条 assistant + 消费面 = list 终止 note）、空产物清理设计裁定节补 A-5 错误载体契约（list 级横幅双载体条件）、失败路径记录节新增 2026-08-11 双审计 P1（R1-F1 + FIND-03）两条、invariants 节历史行补 2026-08-11 plan 条目；**bug notes 149-150 落档**（149 R1-F1 marker 载体 + 零消费，含事实修正说明；150 FIND-03 A-5 载体不可达，按 guide 全 8 节）；daily log 收口记录（本节）。
- **验证**：AI 包 **79 files / 669 tests 全绿**（基线 77/662 + 新 render 集成 2 文件 7 用例；`engine-invariants-i4.test.ts` 11 用例零回归）；AI 包 typecheck/lint 绿；`check:ai-engine-invariants` exit 0 零命中；`check:i18n-keys` 绿（`flux.ai.toolLoopMaxReached` en/zh 对称注册）；`pnpm typecheck`/`build`/`lint` 全仓 37/37、`pnpm test` 全仓 66/66 tasks、`pnpm check` 零新增命中（仅既有登记红）。
- **plan 状态**：Phase 1-3 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 全 [x]（closure-audit 由独立 fresh sub-agent `ses_01316b533ffeMHxco0FkWyIo1i` 执行，verdict **pass-with-1-minor**——G1-G8 全 PASS，1 Minor 为 daily log 收口措辞超前（本节即修订版）已就地修复）；Closure 节证据回填；**Plan Status → `completed`**；源审计 `docs/audits/2026-08-10-2245-open-audit-ai-invariant-loop.md` `planned → closed`（multi-audit 已于 0008-1 收口翻 closed，跳过）；roadmap Follow-up Backlog「2026-08-10-2245 双审计 P2 填充」节补 **P0/P1 路由收口注记（0008-2 engine 循环终止/错误载体族）**。
- **mission-driver verify 复跑确认（full-green）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 66/66 tasks 全绿（AI 包 79 files/669 tests）+ `check:ai-engine-invariants` exit 0；`pnpm check` 仅既有登记红零新增。
- 偏差：无。

### 2026-08-11（ai-invariant-loop mission driver：plan `2026-08-11-0008-1` renderer 契约接线收口——FIND-01 [P0] onApproval 编译注册 + FIND-06 [P1] 投影重建信号盲区 + bug notes 147-148）- **背景**：执行 plan `docs/plans/2026-08-11-0008-1-renderer-contract-wiring-onapproval-and-projection-remediation.md`（`2026-08-10-2245` 双审计 renderer 契约接线族 1 P0 + 1 P1）。3 Phase 全 completed，test-first 先红后绿（真实编译管线断言，禁 spy 注入）+ 类别清扫强制 + 登记处同步。

- **Phase 1（FIND-01 [P0]：onApproval 编译注册 + 假绿测试重写）**：**RED 实证 ×2**——重写 `ai-bubble-hitl.test.tsx` 为**真实编译管线**断言（schema 声明 `onApproval: { action: 'capture:approval' }` + 注册 ActionScope provider 捕获 dispatch，移除 `{ ...props.events, onApproval: spy }` 注入）：ai-chat 气泡路径点击审批按钮 `capturedApprovals` 恒空（静默 no-op）；独立 ai-bubble 路径 `approve.disabled === true`（`hitl-no-handler` 守卫）——修复前全红。**修复**：`ai-renderer-definitions.ts` ai-chat fields 与 ai-bubble fields 各追加 `{ key: 'onApproval', kind: 'event' }`（对齐 ai-tool-call `:201`；此前 `classifyField` 把未声明 `on*` 键归 `kind:'prop'`，`props.events.onApproval` 恒 undefined）→ 全绿。**类别清扫**：全包 `on*` 已消费/已声明逐渲染器核对（ai-chat 6 / ai-bubble 2 / ai-sender 3 / ai-conversations 4 / ai-attachments 3 / ai-voice-input 2 / ai-citations / ai-tool-call / ai-feedback / ai-prompts / ai-suggestions / ai-token-usage 各 1）——FIND-01 双注册面是唯一"已消费未声明"实例，零其他实例。**反向门禁评估**：`contract-honesty`（flux-core，查「已声明未消费」）扩展反向检查需新消费点提取启发式，按 plan 裁定不落门禁，候选 backlog（Non-Blocking Follow-ups 登记）。FIND-21 同面（模块级 `let captured` 无 afterEach 重置）在重写时按 `ai-chat-projection.test.tsx:83-88` 正确模式顺带修复。
- **Phase 2（FIND-06 [P1]：投影重建信号盲区 A/B）**：**RED 实证 ×2**（`ai-chat-projection.test.tsx` FIND-06 用例集）——盲区 A：gated toolExecutor（abort 信号 reject）+ `finishReason:'tool_calls'` 连接器 → abort-mid-executor → `cleanDanglingAssistantAt` strip-keeps-text 原地等长替换（同引用同 length）→ 投影保留 pre-strip ghost tool_calls（waitFor 超时）；盲区 B：会话 A(idle) → swap 到 processing 中会话 B（switch-while-stream）→ `!isProcessing` gate 短路 → 投影显示 A 直到 B 完成（waitFor 超时）。**修复**：`ai-chat.tsx` 投影谓词——idle 守卫加**元素恒等指纹**（`messageSetFingerprint`：last-message id + finishReason + tool_calls length，O(1)，第 5 信号）；engine 身份交换（`snapSourceRef !== messages`）改**无条件**（移除 `!isProcessing` gate，streaming 期间数组引用稳定不误触发）；`snapSourceRef`/`snapLength`/`snapFingerprint` 每 render 收敛语义保持、流式不克隆纪律不变 → 全绿。**类别清扫**：引擎状态镜像面四面对照新谓词复核入档——投影快照（新谓词 ✓）/ `hostScopeData` bundle（`[isProcessing, projectedMessages, activeConversationId]` deps，重建经 `projectedMessages` 恒等流动，无需改）/ `chatContextValue`（live 数组引用 render 时按 index 取元素，原地替换天然可见；加指纹会破坏流式不失效纪律，不改）/ `onResponseComplete` handoff（仅 processing→completed 触发，与 abort-strip 路径无交集，不改）。既有 turn-boundary 稳定性（P1#2 gated-streaming 等）零回归。
- **Phase 3（登记处同步 + 收口）**：`design.md` 投影重建触发条件节重写（五信号：翻转 / 终态 / 引用【无条件】/ 空闲 length / 空闲元素指纹 + FIND-06 盲区说明）；`renderers.md` §10.4 气泡路径 + §13 events 总表 onApproval 两行补 FIND-01 编译注册收口注记；**bug notes 147-148 落档**（147 FIND-01 假绿测试 / 148 FIND-06 盲区 A/B，按 guide 全 8 节 + Notes For Future Refactors）；daily log 收口记录。
- **验证**：AI 包 **77 files / 662 tests 全绿**（基线 660 + FIND-06 盲区 A/B 2 条；FIND-01 重写 2 条走真实编译管线；`ai-chat-projection.test.tsx` 按 oversized 门禁拆分——共享 harness 提取 `ai-chat-projection-test-support.tsx` + FIND-06 套件移入新文件 `ai-chat-projection-find06.test.tsx`，既有 turn-boundary 用例零回归）；AI 包 typecheck/lint 绿；`pnpm typecheck`、`pnpm build`、`pnpm lint` 全仓 37/37、`pnpm test` 全仓 66/66 tasks、`pnpm check` 零新增命中（仅既有登记红：audit-event-dispatch-ctx 6 条 industrial + oversized 2 exempt locale）、`check:ai-engine-invariants` exit 0。
- **plan 状态**：Phase 1-3 全 completed + 全 checklist [x] + Exit Criteria 全勾；Closure Gates 全 [x]（closure-audit 由独立 fresh sub-agent `ses_0134f556cffer9khmovGaoTHw8` 执行，verdict **pass-with-minors**——G1-G8 全 PASS，3 Minor 均文档 truthfulness 已就地修复：文件计数 76→77 / daily log 终态措辞（本节即修订版）/ bug 148 Tests 路径指向 split 文件）；Closure 节证据回填；**Plan Status → `completed`**；源审计 `docs/audits/2026-08-10-2245-multi-audit-ai-invariant-loop.md` `planned → closed`（按 1301-1 先例：剩余 engine/adapter 族 FIND-02/03/04/05 由 plan `2026-08-11-0008-2`/`0008-3` 独立 closure surface 跟踪）；roadmap Follow-up Backlog「2026-08-10-2245 双审计 P2 填充」节补 **P0/P1 路由收口注记（0008-1 renderer 契约接线族）**。
- **mission-driver verify 复跑确认（full-green）**：`pnpm typecheck` 37/37（36 cached）、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 66/66 tasks 全绿（AI 包 77 files/662 tests fresh 复跑）+ `check:ai-engine-invariants` exit 0；`pnpm check` 仅既有登记红零新增（audit-event-dispatch-ctx 6 条 industrial + oversized 2 exempt locale）。
- 偏差：无。
