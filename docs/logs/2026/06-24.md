# Development Log - 2026-06-24

### 2026-06-24 (W2b 日期族 — input-date/input-datetime/input-time/date-range)

- Executed/closed plan `docs/plans/2026-06-24-0335-2-w2b-date-family-form-plan.md` (status → `completed`): W2b 日期族 4 个 renderer 落地于 `flux-renderers-form`（目标包已存在，无新包），首次建立共享日期底层，解锁 W3d（`W2b → W3d`）进而解锁 W4c。
- **Phase 1 共享日期底层**: `src/renderers/date/date-utils.ts`（纯 helper，基于原生 `Date`+`Intl`+既有 `react-day-picker`，**零重型日期库**——grep 确认无 date-fns/dayjs/luxon）。token format/parse 系统（`YYYY`/`YY`/`MM`/`DD`/`HH`/`mm`/`ss`，longest-first 分词 + 正则捕获 + 日历有效性回检）；`formatDate`/`parseDate`/`isWithinRange`/`toUtc`/`fromUtc`/`convertValueFormat`/`normalizeRange`/`parseDateRange`/`joinDateRange`/`compareDates`；解析失败一律回退 `undefined`（不抛错）。focused 单测 30 个（format/parse 往返、多格式组合、解析失败回退、min/max 约束含 inclusive/open-ended/invalid-bound、utc 往返、range 归一化含 swap、calendar↔storage 时区桥接）。
- **关键修复（calendar↔UTC 时区桥接）**: `utc:true` 在非 UTC 时区下 `formatDate` 读 UTC 分量会把 react-day-picker 给出的 local-midnight 日期错移一天（jsdom 跑 UTC 故单测漏网，e2e 浏览器暴露）。新增 `toStorageDate(day, utc)`/`toCalendarDate(parsed, utc)`：日历恒以 local wall-clock 日期运作，仅在存储边界做 UTC 桥接，消除 day-shift 漂移；renderer 的 min/max 边界同样经 `toCalendarDate` 对齐 local 日历日。5 个桥接单测锁定修复。
- **Phase 2 三单值字段**: `date/date-field-control.tsx`（共享 Popover+`Calendar` 单值选择器，**自带 `useInputComponentHandle`**，避免 ref-cast mutation lint）+ `input-date-renderer.tsx`/`input-datetime-renderer.tsx`/`input-time-renderer.tsx`。markers `nop-input-date`/`nop-input-datetime`/`nop-input-time`；标准 field bridge（`useFormFieldController`，无 value adapter——renderer 自行 parse/format）；`valueFormat`/`displayFormat`/`minDate`/`maxDate`/`utc`/`clearable`/`minTime`/`maxTime`。input-time 走原生 `type=time` 控件经 date-utils 转 valueFormat。focused 单测 13 个（date: marker/display/选择写回/clearable/utc/min-max 禁用；datetime: marker/日期换保留时间/时改；time: marker/写回/minTime-maxTime 夹逼/valueFormat↔HH:mm 转换）。
- **Phase 3 date-range + 注册**: `date-range-renderer.tsx`（`nop-date-range` marker，canonical range owner——**一个 type 经 `rangeKind` 统一 date/datetime/time，不分裂三个**，grep + guard 单测验证无并行 `input-*-range` type）；`normalizeRange` 起止归一（读+写双侧）；`delimiter`/`shortcuts`。`date-renderer-definitions.ts` 导出 4 个 `RendererDefinition`（formFieldRules + 日期字段 value props + rangeKind value），`definitions.ts` `formRendererDefinitions` spread 接入 → `registerFormRenderers` 注册。focused 单测 10 个（contract 收敛/rangeKind 三态/无并行 type/delimiter 写回/自定义 delimiter/反向归一化读+写/shortcuts 归一/min-max 禁用/time 写回）。
- **Phase 4 playground+e2e+doc**: playground `w2b-date-family-demo.tsx`（4 组件 + scope 派生 report text 节点）+ route `w2b-date-family` + App.tsx；component-lab 4 个 lab page + FORM_RENDERER_ROUTES 增 4 条（route-matrix inventory 一致性测试驱动）；e2e `tests/e2e/w2b-date-family.spec.ts` 6 程序化断言（input-date 选择写回 scope 经 scope 派生 text、min/max 超界禁用、date-range rangeKind 收敛、delimiter 拼接、shortcut 归一、time/datetime 初值；**非截图**）。roadmap W2b `todo`→`done`（+L0 清单加 4 type）；amis-baseline-matrix L153-156 4 组件 `targetContract`→`runtime`；4 份 design.md §3 无 drift。
- **调试裁定（react-day-picker + Playwright 交互）**: react-day-picker v9 DayButton 在 focus/hover 触发 re-render，Playwright 完整 pointer 序列 `.click()` 会与该 re-render 竞态导致选择不触发（`fireEvent.click` 单测 + 真实浏览器 `dispatchEvent('click')` 均验证选择路径正常）；e2e 改用 `.dispatchEvent('click')` 触发真实 onSelect handler（程序化、非截图、仍走真实写回路径）。min/max e2e 用 demo 内独立 bounded 字段（初值锚定 6 月）避免脆弱的多次翻月导航。
- 全量 full-green 验证：`pnpm typecheck` 55/55、`pnpm build` 29/29、`pnpm lint` 29/29（0 error，1 个无关 select-virtualizer 预存 warning）、`pnpm test` 29 包全绿（flux-renderers-form 468 / playground 88）；e2e W2b 6/6。
- 独立 fresh-session closure audit `pass`（task `ses_109822590ffe9VFWdPrRe15kFz`，非执行 session）：8 项 Closure Gates 全 PASS——共享底层+30 单测、4 renderer 注册+标准 bridge、date-range contract 未分裂、无重型日期库、focused 单测 23 + e2e 6 复验、marker 齐、roadmap/matrix 同步、closure gates 一致；执行 session 未自审勾选 closure-audit 项（由独立审计满足）。
- Non-blocking follow-ups（未阻断 closure）：时区选择器 UI（`utc` 仅存储格式，非完整时区切换器）；W3d 周期字段（month/quarter/year）复用本底层的评估（归 W3d）；date-range `shortcuts` 预设库扩展。

---

### 2026-06-24 (W2a 数据组合组 — service/pagination/cards/wizard/alert)

- Executed/closed plan `docs/plans/2026-06-24-0335-1-w2a-data-composition-family-plan.md` (status → `completed`): W2a 数据组合组 5 个 renderer 落地，跨 3 个 package（`service`/`pagination` → `flux-renderers-data`；`cards`/`alert` → `flux-renderers-content`；`wizard` → 首次 bootstrap 的 `flux-renderers-layout`），解锁 W3a/W3b/W4b（首次落地 layout 包）。
- **Phase 1 layout 包 bootstrap**: `packages/flux-renderers-layout/{package.json,tsconfig.json,tsconfig.build.json,vitest.config.ts,src/index.ts,src/styles.css}`（deps flux-core/flux-i18n/flux-react/@nop-chaos/ui；`./styles.css`→`./dist/styles.css` 导出）；`vite.workspace-alias.ts` 双别名 + 根 `tsconfig.json` project ref + `tsconfig.base.json` paths；`apps/playground` 加 workspace dep + `App.tsx` 接入 `registerLayoutRenderers`。
- **Phase 2 service+pagination (data)**: `src/{service-renderer,pagination-renderer,w2a-data-composition-definitions}.tsx` + `schemas.ts` 增 `ServiceSchema`/`PaginationSchema`/`PaginationMode`。service 遵循请求下沉约束——零组件级请求字段（`api`/`initFetch`/`interval`/`sendOn`/`source` 全归 `<data-source>`）；只读 scope 经 `items` 表达式 + `statusPath` 发布 `idle`/`ready`/`error` 摘要（派生自 items 解析，非请求镜像）；`empty`/`error`/`loading` value-or-region 反馈区。pagination 复用 `@nop-chaos/ui` `Pagination`；边界归一（`currentPage` 超界归末页/1 页）；**裁定**页大小变更重置 `currentPage` 到第 1 页（避免空页，记 design.md §7）；`mode:'simple'|'with-page-size'`；`statusPath` 只读 summary。focused 单测 14 个（service 7：items 解析/空/null/loading/error/请求下沉 grep 验证/data-source 组合；pagination 7：边界归一/onChange/页大小重置/edges/ellipsis/simple 模式）。
- **Phase 3 cards+alert (content)**: `src/{cards-renderer,alert-renderer}.tsx` + `schemas.ts` 增 `CardsSchema`/`AlertSchema`/`AlertLevel`/`CardsSelectionMode`。cards 单一 `items` + `card` region + `selectionMode` local controlled（独立 collection contract，不跨包 import list，design §12 watch-only residual）；alert 复用 ui Alert primitive + level→视觉变体（info/success/warning/error）+ `closable` local state + `onClose`。**Decision**: cards 归属 content（roadmap §95 + package-reorganization-analysis L210 双源裁定），收敛 `cards/design.md` §3 `data`→`content`；同步收敛 `alert/design.md` §3 `basic`→`content`。focused 单测 11 个（cards 6：item 实例化/空/null/single 互斥/multiple 累积/单一 items 字段 grep；alert 5：title+body+actions/level 映射/closable+onClose/默认 level）。
- **Phase 4 wizard (layout)**: `src/{wizard-renderer,layout-renderer-definitions,schemas}.tsx`。**分层状态**：interaction owner（`WizardInteractionState`/`currentStepIndex`）与 semantic lifecycle owner（`WizardLifecycleState`/`committing`/`validating`/`lastCommitStatus`/`stepError`）刻意分离为两个 `useState`——closure gate 单测 grep 验证 `stepIndex` 与 `committing` 不混入同一 state 对象。`steps` renderer-owned structured（`deepFields` + `nestedRegions` per-step title/body/actions region）；`linear:true` 默认 + `allowStepJump` 放行 + `mountOnEnter`/`unmountOnExit`；不引入 `$wizard`（用 `statusPath` 收口）；`onChange`/`onStepCommit`/`onComplete`/`onStepError` 区分 navigation 与 commit。focused 单测 10 个（默认激活首步/Next 推进+onChange/Prev 边界/onStepCommit lifecycle+interaction 分层/grep 验证分层/linear 阻塞跳步/allowStepJump 放行/onComplete/statusPath layered summary 摘要）。
- **Phase 5 注册+playground+e2e+doc 同步**: 三个包 `src/index.ts` 导出齐全；playground `w2a-data-composition-demo.tsx`（service+data 组合请求下沉演示/pagination/cards/alert/wizard）+ route `w2a-data-composition` + App.tsx；component-lab `service-lab-page.tsx`/`pagination-lab-page.tsx` + DATA_RENDERER_ROUTES 增 service/pagination；e2e `tests/e2e/w2a-data-composition.spec.ts` 7 程序化断言（service data-status=ready/data-item-count=3/body 渲染；pagination 页码切换+边界；cards N 卡+single 互斥；alert closable+onClose；wizard Next+分层；linear 阻塞；onComplete；非截图）。3 份 design.md §3 drift 收敛（cards/alert→content、wizard→layout）；pagination §7 补边界归一+页大小重置裁定；roadmap W2a `todo`→`done`；amis-baseline-matrix L72/L77/L99/L114/L118 5 组件 `targetContract`→`runtime`。
- 全量 full-green 验证：`pnpm typecheck` 55/55、`pnpm build` 29/29、`pnpm lint` 29/29、`pnpm test` 55/55（flux-renderers-data 512 单测 + flux-renderers-content 94 单测 + flux-renderers-layout 10 单测 + playground 88 单测）；e2e W2a 7/7 + W1b 5/5 + W1c 4/4 相关回归 16/16。
- **i18n**: 新增 `flux.wizard.*` 命名空间（previous/next/complete/committing/noSteps/stepNav/close）en-US + zh-CN。
- **React 19 裁定**: layout 包 wizard-renderer 初版用 `furthestReachedRef.current` 在 render-time `useMemo` 中读取触发 `react-hooks/refs`；改为 `useState` + 在 `goToStep` handler 内同步更新（非 effect）以避开 `react-hooks/set-state-in-effect`。alert-renderer 动态 lucide 图标选择初版 `<IconComp />` 触发 `react-compiler/react-compiler` "Cannot create components during render"——提取 `CustomIconRenderer` 子组件 + `React.createElement(IconComp, ...)` 规避。
- Non-blocking follow-ups（未阻断 closure）：service `statusPath` 在跨多层 flex scope 的兄弟 text 节点读取需额外等待（demo 已用同 flex 直接 sibling 演示，单测验证 statusPath 发布）；cards 与 list 的 grid/列布局变体；wizard `$wizard` 局部绑定（design §8 待语义稳定）；wizard 跨步 form 持久化框架。

---

### 2026-06-24 (W1c 集合展示组 — list renderer)

- Executed/closed plan `docs/plans/2026-06-24-0040-3-w1c-list-collection-display-plan.md` (status → `completed`): W1c `list` renderer 落地于 `@nop-chaos/flux-renderers-data`——有 UI 的顺序型集合展示（视觉壳 + `nop-list` marker + 单一 `items` 字段 + `item` region + `empty` value-or-region + `selectionMode` local controlled state + `onItemClick`/`onSelectionChange`），解锁 W2a（依赖图 `L0 & W1c → W2a`）。区别于无 UI 结构展开节点 `loop`。
- **Phase 1 list 核心**: `src/list-renderer.tsx`（`ListRenderer` + `ListItemView`）。消费 `items`（从 `props.props`）迭代 → 每项经 `props.regions.item.render({ scope, bindings:{item,index}, instancePath })` 实例化 `item` region（`params:['item','index']`，`$slot.item`/`$slot.index`）；`empty`（value-or-region，`resolveRendererSlotContent`+fallback）；复用 loop/table repeated-instance substrate（`InstanceFrame`+`repeatedTemplateId`，不新建 substrate）。每项 per-item scope（`helpers.createScope`，`merge` 更新、unmount `disposeScope`）供 `onItemClick` 派发。遵循 `RendererComponentProps`（不直接访问 store）。`ListSchema` 加 `schemas.ts`。
- **Phase 2 选择态+注册+演示+e2e+doc**: `selectionMode`（single 互斥 / multiple 累加 / none 不可选）local controlled（`useState<Set>`）+ `onSelectionChange` 上报 `{selectedKeys,selectionMode}`；`onItemClick` 派发 per-item scope 携带 `{type,item,index,key}` 上下文。`data-renderer-definitions.ts` 增 `list` `RendererDefinition`（category `data`；propContracts items/selectionMode/keyField；eventContracts onItemClick/onSelectionChange；fields item region/empty value-or-region/items/selectionMode/keyField/onItemClick/onSelectionChange event）随 `registerDataRenderers` 注册；`index.tsx` 导出 `ListRenderer`。playground component-lab `list-lab-page.tsx`（4 场景：item 模板/空态/single+onItemClick/multiple）+ DATA_RENDERER_ROUTES + renderer-lab-registry + e2e coverage-manifest。e2e `tests/e2e/w1c-list.spec.ts`（4 程序化断言：N 项渲染、空态、onItemClick openDialog、single/multiple 选择高亮；非截图）。roadmap W1c `planned`→`done`（+L0 landed 清单加 `list`）；amis-baseline-matrix `list` `targetContract`→`runtime`/`wave 1`→`landed`。
- **验证**: `pnpm typecheck` 53/53、`pnpm build` 28/28、`pnpm lint` 28/28、`pnpm test` 53/53（flux-renderers-data 498 单测 + playground 88 单测）；e2e W1c 4/4 + component-lab smoke/nav/batch（含新增 `list` zero-error）97 passed。
- **调试裁定（Non-Blocking Follow-up）**: `onItemClick` 派发 payload 不得携带原生 DOM Event 引用（`nativeEvent`）——经 `normalizeActionEvent` 原样保留后会阻断 surface 动作（openDialog 不开）；改为传递 clean payload `{type,item,index,key}` + `{scope:itemScope}`。playground strict validation 下 dialog body 不继承 per-item scope（`${item}` 在 dialog body 不可达，会 throw）；item/index 上下文由 focused 单测（openDialog body `${item.name}` 验证）覆盖，e2e 改用静态 dialog body 断言事件触发。surface body scope 继承记为 watch-only follow-up。
- 独立 fresh-session closure audit `approved`（opencode mission-driver closure-audit pass；非执行 session，不复用执行者上下文）：Closure Gates 复核全 PASS——`list-renderer.tsx` 非空壳且遵循 `RendererComponentProps`（无直接 store 访问）、`data-renderer-definitions.ts:511-584` definition 完整并随 `registerDataRenderers` 注册、focused 单测 `data-list-rendering.test.tsx`（item 计数/空态/onItemClick `${item.name}`/selection 三态/key-field）验证结果非仅"不报错"、e2e `w1c-list.spec.ts` 程序化断言（非截图）、roadmap W1c→done / amis-baseline-matrix list→runtime、deferred 诚实（infinite-scroll→W1d、分页→W2a、虚拟滚动→optimization 均带 non-blocking 理由）；plan `Closure Gates` 独立审计项已勾选，plan 五点一致。

---

### 2026-06-24 (W1a 内容展示组 + 受控渲染安全门禁)

- Executed/closed plan `docs/plans/2026-06-24-0040-2-w1a-content-family-sanitization-plan.md` (status → `completed`): W1a 内容展示组 5 个 renderer（markdown/html/link/image/json-view）落地于 `@nop-chaos/flux-renderers-content`，并在 `flux-renderers-content` 内首次建立受控渲染安全门禁（DOMPurify sanitize）。
- **Phase 1 sanitize 门禁 + 依赖**: `src/sanitize.ts`（`sanitizeHtml(html,{sanitize})` 纯 helper；DOMPurify 默认 allowlist strip `<script>`/事件处理器/`javascript:`；`sanitize:false` 显式 trusted 直通；无 DOM 环境 fail-closed strip 全标签）+ `sanitize.test.ts`（6 focused 单测）。net-new 依赖 4 个（react-markdown@10 / remark-gfm@4 / rehype-raw@7 / dompurify@3）按 recharts 的 peer+dev 模式声明。**裁定**：sanitize 决策以既有 `docs/architecture/security-design-requirements.md` 为父文档，填补其未覆盖的 content-rendering XSS 维度。
- **环境裁定**：DOMPurify 在 happy-dom 下解析/序列化异常（`<script>...<b>` 返回空、`<p>` 被剥），故安全敏感的 sanitize/html/markdown 单测改用 jsdom（DOMPurify 参考实现 DOM）经 per-file `// @vitest-environment jsdom`；其余组件仍 happy-dom。
- **Phase 2 link/image/json-view**: `src/{link,image,json-view}.tsx` + schemas + focused 单测。link 语义化 `<a>`（ui 无 Anchor，design §3/§11 允许）+ `nop-link`，onClick 与导航并存（action preventDefault 才阻断）；image 原生 `loading=lazy` + IntersectionObserver fallback（旧浏览器）+ ui Dialog 预览 + 错误回退 + `nop-image`（click 加 a11y role/tabIndex/onKeyDown）；json-view 复用 ui `JsonViewer`（增强 `expandLevel` 支持层级折叠，顺带修复 `defaultExpand:false` 致 react-json-view-lite 崩溃的潜在缺陷）+ `nop-json-view` + 本地复制态。
- **Phase 3 markdown/html**: `src/{markdown,html}.tsx` 消费 sanitize 门禁。html `dangerouslySetInnerHTML` 仅承载 sanitized 输出（`nop-html` 稳定 DOM 边界）。markdown allowHtml 默认 off（react-markdown 转义），on 时先 DOMPurify 预 sanitize（已实证保留 `#`/`*`/`|`/`>` markdown 语法仅 strip 危险 HTML）再 rehype-raw 渲染存活安全标签（`nop-markdown`）。
- **Phase 4 注册+playground+e2e+doc**: `content-renderer-definitions.ts` 增 5 个 `RendererDefinition`（category `content`；markdown/html/json-view `empty` value-or-region）；`src/index.ts` 导出 5 组件 + `sanitizeHtml`/`SanitizeOptions` + 5 schema 类型；playground `w1a-content-display-demo.tsx`（含 XSS 安全演示）+ route `w1a-content`；e2e `tests/e2e/w1a-content-family.spec.ts` 7 程序化断言（`page.evaluate` 验 `window.__W1A_XSS_HTML__` undefined + DOM 无 script 节点；非截图）；5 份 design.md §3 归属 `basic`→`content` + html/markdown §12 补 sanitize 决策引用；roadmap W1a `planned`→`done`；amis-baseline-matrix 5 组件 `targetContract`→`runtime`。
- 全量 full-green 验证：`pnpm typecheck` 53/53、`pnpm build` 28/28、`pnpm lint` 28/28、`pnpm test` 53/53（content 83 单测）；e2e 437 passed / 46 skipped / 0 failed（W1a 7/7）。
- 独立 fresh-session closure audit `approved`（task `ses_10a3d6652ffeUAd34gPVbZP6xW`）：零 Blocker/Major/Minor；9 项 Closure Gates 全 PASS（sanitize 门禁、契约无直连 store、XSS 单测+e2e 程序化断言、依赖声明、owner-doc 收敛、roadmap/baseline、deferred 诚实、无 src 构建产物、独立复验 content 83/83 + W1a e2e 7/7）；advisory：rehype-raw 为第 4 个 net-new 依赖（Phase 3 补记已透明记录）。
- Schema 类型修正：`BaseSchema` 有 `[key:string]:SchemaValue` 索引签名 + `label?:string`——LinkSchema 不可重声明 `label?:SchemaInput`（已改用继承 `label`），`disabled` 对齐 `boolean|string`；JsonViewSchema `value` 用 `SchemaValue`（非 `unknown`）以满足索引签名。
- Deferred honesty：`security-design-requirements.md` 升级为完整内容 sanitize 通则规范 = out-of-scope（successor 待安全治理专项）；image 完整 lightbox、json-view 大对象虚拟滚动 = non-blocking follow-up。

---

- Closed plan `docs/plans/2026-06-24-0040-1-w1b-feedback-family-content-package-bootstrap-plan.md` (status → `completed`): 首次落地 NEW 包 `@nop-chaos/flux-renderers-content` + W1b 容器与反馈组 5 个 renderer（separator/spinner/progress/empty/card），解锁 W1a/W2a-content/W3c/W4a。
- **Phase 1 包骨架**: `packages/flux-renderers-content/{package.json,tsconfig.json,tsconfig.build.json,vitest.config.ts,src/index.ts,src/styles.css}`（deps flux-core/flux-i18n/flux-react/@nop-chaos/ui；`./styles.css`→`./dist/styles.css` 导出）；`vite.workspace-alias.ts` 双别名；根 `tsconfig.json` project ref；`apps/playground` 加 workspace dep。
- **Phase 2 5 renderer**: `src/{separator,spinner,progress,empty,card}.tsx` + `schemas.ts` + `test-support.ts`，均消费 `RendererComponentProps`（读 props.props/meta/regions/events；用 `resolveRendererSlotContent`/`hasRendererSlotContent`；无直接 store 访问）；marker `nop-separator`/`nop-spinner`/`nop-progress`/`nop-empty`/`nop-card`；复用 `@nop-chaos/ui` Separator/Spinner/Progress/Empty*/Card*。`progress.tsx` 导出纯函数 `normalizeProgressValue`（value>max 归一化不溢出，5 个专属单测）。`spinner.visible` 走 frozen META_FIELDS `meta` 通道（`field-binding-and-renderer-contract.md` 一致）。
- **Phase 3 注册+演示+e2e+doc 同步**: `content-renderer-definitions.ts` 声明 5 个 `RendererDefinition`（separator/spinner/progress/empty category `content`、card category `layout`；card header/body/footer/actions region + onClick event；empty title/description value-or-region + actions region）；`src/index.ts` 导出组件 + `contentRendererDefinitions` + `registerContentRenderers`；playground App.tsx 注册 + 新增 `w1b-content-feedback-demo.tsx` + route `w1b-content`；`styles.css` 加 separator 取向尺寸 + progress 变体主题；e2e `tests/e2e/w1b-feedback-family.spec.ts`（5 程序化断言，不靠截图）；5 份 design.md §3 归属 `flux-renderers-basic`→`flux-renderers-content`；`amis-baseline-matrix.md` 5 组件 `targetContract`→`runtime`；roadmap W1b `planned`→`done`。
- 全量 full-green 验证：`pnpm typecheck` 53/53、`pnpm build` 28/28、`pnpm lint` 28/28、`pnpm test` 53/53（content 42 单测 + playground 88 单测）；e2e 5/5。
- 独立 fresh-session closure audit `approved`（task `ses_10a757996ffeM8YJPAVsKbxI8O`）：零 Blocker/Major；5 项审计全 PASS（包集成、契约、注册、deferred 诚实、interface-vs-semantics）；判定 category `display/container`→`content/layout` 映射为唯一正确选择（代码库无 display/container 类目）。
- Non-blocking follow-ups（未阻断 closure）：progress 圆形/仪表盘变体、card 展开/选中状态机、spinner/skeleton 语义边界文档化。

---

- **MM-17 (Decision: correct doc)**: `docs/components/notice-bar/design.md` §2 决策表/§3/§4/§5/字段分类 — removed phantom `body`/`icon` region claims; `icon?: IconSchema` → `icon?: string` (lucide name), matching `schemas.ts:103` (`kind:'prop'`), `notice-bar.tsx:41-42` (`resolveLucideIconStrict`). No `body` region exists; `body` row moved to 不采纳.
- **MM-18 (Decision: fix doc)**: `notice-bar/design.md §6` reconciled with the NEW-MM-06 `styles.css:24-31` comment — now states package-local fixed hsl literals + `--nop-notice-bar-*` override vars, NOT derived from shared theme tokens.
- **MM-19 (Decision: correct doc)**: `infinite-scroll/design.md §4` deleted the non-existent scroll-math fallback; replaced with an honest IntersectionObserver precondition (v1 ships no fallback; all evergreen browsers have IO).
- **MM-20 (Decision: implement)**: `packages/flux-renderers-mobile/src/infinite-scroll.tsx` — added `findScrollableAncestor()` (walks `parentElement` chain, matches `getComputedStyle(ancestor).overflowY === 'auto'|'scroll'`); observer effect now sets `observerOptions.root = scrollableAncestor` when found, omits `root` (viewport, prior behavior) otherwise. `design.md §5` "容器滚动 vs 视口滚动" row updated to describe the implemented auto-detect. +2 focused tests (one asserts `options.root === scrollableAncestor`, which fails against pre-fix code; one asserts viewport fallback when no scrollable ancestor).
- **MM-21 (Decision: fix code)**: `packages/flux-renderers-mobile/src/pull-refresh.tsx` — resting rebound `transition` changed from `ease` to `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (matching `pull-refresh/design.md §5:94` and `swipe-cell.tsx:11`). +1 focused test asserting the cubic-bezier in the resting `transition` style.
- Mobile package tests 154→157 (+3 regression proofs). Full workspace `pnpm typecheck` (51/51) / `pnpm build` (27/27) / `pnpm lint` (27/27) / `pnpm test` (51/51) green.
- Independent fresh-session closure audit `approved` (task `ses_10ab5115dffes9d5EuijsdmHtz`): all 9 Closure Gates re-verified against live repo; scope confirmed via git diff (7 files; countdown/swipe-cell/pull-refresh design docs correctly untouched); no "Proposed vs Current" residuals.
- Updated `docs/components/mobile-roadmap.md` M5 with the plan-7 design-doc-reconciliation note.

---

- Closed plan `docs/plans/2026-06-23-2235-1-mobile-runtime-correctness-and-test-rigor-plan.md` (status → `completed`): 18:24 mobile re-audit runtime-correctness + missing-test findings NOT owned by sibling 2031-1.
- **Countdown wall-clock unification (MM-14, OA-21)**: `packages/flux-renderers-mobile/src/countdown.tsx` — replaced the branched tick (`targetTime - Date.now()` / subtractive `prev - interval`) with a unified wall-clock derivation `remaining = max(0, remainingAtStart - (Date.now() - startTimestamp))`, anchored via `remainingRef`/`startTimestampRef`/`remainingAtStartRef` on every start/resume/config-change. Effect: `targetTime` pause/resume no longer swallows the pause window (was 50→39 jump); `time` mode no longer drifts under throttled `setInterval`. MA-16 clamp / OA-13 reset / MA-02 finish-dispatch / MM-08 autoStart contracts preserved.
- **MM-22 doc reconciliation**: `docs/components/countdown/design.md` §6 now honestly states `setInterval` + wall-clock (rAF deferred to §11 as smoothness-only); §6↔§11 no longer contradict.
- **MM-15**: `notice-bar.tsx` carousel effect adds `if (!visible) return;` + `visible` dep → no 3s `setCurrentIndex` churn after close.
- **MM-16**: `infinite-scroll.tsx` in-flight guard now releases only on actual `loading`/`error` change (`prevLoadingErrorRef`) → React 19 StrictMode double-setup no longer double-dispatches `onLoadMore`; OA-16/MA-13 preserved.
- **MM-25**: `infinite-scroll.tsx` retry `<Button>` forwards `disabled` → honest WCAG 4.1.2 operability (was a dead button under `disabled + error`).
- **MM-23 / MM-24**: added countdown + infinite-scroll StrictMode dispatch tests; locked `direction:'right'→'normal'` and `'left'`/default→`'reverse'` marquee branches.
- **OA-22**: Decision A (lock mapping, public schema stable) — `notice-bar/design.md` + code comment make the "value names the inverse of motion direction" semantics explicit.
- Mobile package tests 147→154 (+7 behavior tests: throttled-tick, targetTime pause, StrictMode×2, close-no-churn, direction×2, disabled+error). Full workspace `pnpm typecheck` (51/51) / `pnpm build` / `pnpm lint` (27/27) / `pnpm test` (51/51) green.
- Independent fresh-session closure audit `approved` (task `ses_10acae81fffebNY4S17eaUulNr`): zero Blocker/Major; all 5 regression proofs verified to fail pre-fix; contracts preserved.
- Updated `docs/components/mobile-roadmap.md` M5 with the plan-6 remediation note.
- Next: real-device CDP touch-emulation + `getComputedStyle` verification successor (deferred, `out-of-scope improvement`) pairs with sibling 2031-1's OA-18/OA-20 device follow-up.
