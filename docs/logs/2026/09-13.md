# 2026-09-13 — E1.1 ECharts 渲染器骨架落地

## What landed today

### E1.1 — `echarts` 渲染器骨架（plan `docs/plans/2026-09-13-2343-echarts-e1-1-renderer-skeleton-plan.md`）

**Plan review**：独立子 agent 2 轮（round 1 fail：3 Major——flux-bundle 单文件构建内联 echarts、events 声明层 lying contract、缺 new-renderer-introduction-audit；round 2 收敛至零 Major）。events 体系整体移交 E2.1；根 marker class `nop-echarts`；audit §A–F 纳入 Phase 4。

**决策**：按需引入粒度 = **按功能模块**（`echarts/core` + `echarts/charts` + `echarts/components` + `echarts/renderers` 官方 tree-shaking 入口，集中注册于 `flux-renderers-data/src/echarts-setup.ts` 单一模块；渲染器组件动态 import → echarts 全量位于懒 chunk，未挂载 echarts 渲染器的宿主零加载）。`analysis/echarts-migration-analysis.md` Open Questions 已勾选。

**代码落地**（`packages/flux-renderers-data`）：

- `echarts-setup.ts` — 22 类 chart + 通用组件 + Canvas/SVG renderer 集中注册（`use` 经 `registerEChartsModules` 别名调用，规避 eslint react-hooks 对 `use*` 顶层调用的误报）；`getECharts()` 导出
- `echarts-schemas.ts` — `EChartsSchema`（type/option/dataset/renderer/initOptions/theme/notMerge/lazyUpdate/height/componentId；`events` 移交 E2.1；`componentId`/`height:number|string` 为对齐 chart 先例的记录在案偏差；`EChartsDatasetSchema`/`EChartsInitOptions` extends `SchemaObject` 以满足 BaseSchema 索引签名）
- `echarts-schema-validation.ts` — 结构校验（`RendererSchemaValidationContext`，诊断码 `invalid-property-shape`；option 缺失 = warning，非法形态 = error）
- `echarts-renderer.tsx` — 生命周期：懒加载（模块级 promise 去重）→ init（canvas/svg、theme、initOptions）→ setOption（引用比对，notMerge/lazyUpdate 透传）→ ResizeObserver resize → dispose；空态/错误态/`data-empty` 标记；`nop-echarts` marker；ComponentHandle `resize` 能力
- `echarts-renderer-definition.ts` — RendererDefinition（schemaValidator + propContracts + resize capability + fields，无 events 声明）；并入 `dataRendererDefinitions`；barrel 导出
- 测试：`echarts-schema-validation.test.ts`（16）、`echarts-renderer.unit.test.tsx`（12）、`echarts-renderer-load-failure.unit.test.tsx`（2，独立文件规避 `vi.doMock`/`resetModules` 与 hoisted mock 的交叉污染）、`echarts-renderer-definition-contracts.test.ts`（5）；`data-package-units.test.tsx` 注册清单断言更新（manifest 契约）

**依赖接入**：`echarts@^6.0.0`（npm latest 6.1.0）→ `flux-renderers-data` optional peer + devDep；`flux-bundle` optional peer + devDep + `vite.config.ts` `hostOwnedExternal` 增加 `/^echarts(\/.*)?$/`（防单文件 bundle 内联）。

**Playground 契约强制项**：`route-matrix.test.ts` 要求每个注册 data renderer 有 route + lab page → `data-route-entries.ts` 增 `echarts` 路由、`echarts-lab-page.tsx`（3 场景：canvas bar / svg line / 显式空态）挂入 lab registry + renderers barrel。

**New Renderer Audit §A–F**：全部通过并记录于 plan「New Renderer Audit Record」（A 无外部 IO；B 数据零请求/UI/i18n 复用既有键零新增；C 域内部 state 不进 scope、resize 经 ComponentHandle projection；D 标准 RendererComponentProps 契约；E events 预留归 E2.1；F `nop-echarts` + data-slot + presence-only data-empty）。

### 实现期发现（工程记录）

- **vitest 模块 runner 竞态**：组件在首个动态 import 未决时卸载后，同文件后续 `import('./echarts-setup.js')` 会绕过 `vi.mock` 拉取真实模块（复现于 double-render-with-unmount 用例，`init returned ECharts` 实证）。渲染器层模块级 promise 去重（`loadEChartsSetup`）既是正确的生产代码（避免重复 chunk 请求）也消除了测试竞态。
- **`RendererDefinition` 泛型变体**：显式 `RendererDefinition<EChartsSchema>` 因 `authoringTransform` 逆变形不可赋给 `RendererDefinition<BaseSchema>` 数组——对齐 sparkline 先例使用非泛型 `RendererDefinition` + 上下文类型化。

## Verification（执行 session）

- `pnpm typecheck` ✅ 37/37（turbo，repo root）
- `pnpm build` ✅ 37/37（turbo，repo root；echarts 经 hostOwnedExternal 保持外部化）
- `pnpm lint`：turbo eslint ✅ 37/37；链上前置 `check-i18n-keys` ❌ 4 个未定义键（见下方既有红登记）
- `pnpm test` ✅ 68/68 tasks，约 11,501 tests passed / 0 failed（含 flux-renderers-data 147 files / 1081 tests、playground 347 tests）
- `pnpm check`：除同一 i18n 既有红外全过（`check-oversized-code-files` 2 个 error 为已豁免 locale 数据文件，plan 2026-08-07-1053-1 登记）
- e2e：本计划未跑（E5.1 统一测试覆盖），故不构成 full-green 口径

## Pre-existing red 登记（非本分支引入，本变更零新增命中）

`check-i18n-keys`：4 个 undefined keys——`flux.picker.remove`（引入提交 `44ef5188f`，picker feat，on origin/master）、`flux.validation.url` / `flux.validation.integer` / `flux.validation.format`（引入提交 `646d16ba4` 2026-09-02，format-registry feat，on origin/master）。取证：`git merge-base --is-ancestor 646d16ba4 origin/master` 成立；`git diff origin/master...HEAD -- packages/flux-i18n packages/flux-runtime packages/flux-renderers-form-advanced` 为空（本分支对涉事包零改动）。属 master 级既有红，E1.1 不处理（不在 scope），建议独立修复项补齐两 locale 的 4 键。

## Status

- E1.1 执行完成，closure audit 待独立子 agent 复核（plan 保持未关闭直至 audit 通过）
- roadmap E1.1 状态待 audit 通过后 todo → done

## What's next

- E1.1 closure audit → commit → E2.1（dataset/encode 数据映射 + 事件桥接 + 主题 token 裁决）

---

## E2.1 — ECharts dataset 绑定 + 事件桥接 + 主题 token（续）

**Plan review**：独立子 agent 2 轮（round 1 fail：2 Major——eventContracts 通道移交缺口、tooltip/legend 可观测收口缺失；round 2 `pass-with-minors`，3 残余 Minor 已合并）。

**主题 token 裁决**：CSS 变量 → `flux` 注册主题（`echarts-theme.ts` 的 `resolveFluxEChartsTheme()`：`--chart-1..5` hsl 包装 + `--foreground`/`--muted-foreground`/`--border`/`--popover`/`--popover-foreground`；`registerTheme('flux')`；`theme` 未指定时默认应用，指定时透传；SSR/jsdom 回退静态调色板；运行时切换需重挂载）。analysis Open Questions 已全部 resolved。

**代码落地**：

- `echarts-theme.ts`（新建）：flux 主题解析
- `echarts-setup.ts`：模块加载时 `registerTheme('flux', ...)`
- `echarts-renderer.tsx`：dataset 组合（`helpers.evaluate` 解析表达式/恒等透传已解析数组；schema 级 dataset 覆盖 `option.dataset`；三形态直传）+ 防御校验（source 形态/dimensions-vs-source/encode 引用 → warn 降级）+ 空 dataset 显式空态 + `empty` 插槽 + 事件桥接（`NATIVE_EVENT_BINDINGS` 9 键 on\*→native 字面清单，`events?.<key>` 显式引用满足 contract-honesty 守卫；`createNormalizedActionEvent` + `helpers.dispatch(action, { event, scope })`；`off` 清理；未知键 warn；dispatch reject 捕获）
- `echarts-schemas.ts`：补 `events`/`empty` 字段
- `echarts-renderer-definition.ts`：fields 补 `events`（prop）/`empty`（value-or-region）；`propContracts.events`；9 键 `eventContracts`
- validator：`events` 值形态/`transform` 数组诊断
- 测试：`echarts-theme.test.ts`（4）、`echarts-dataset-binding.test.tsx`（13，真实编译链 `createDataSchemaRenderer`：对象数组/列式/二维数组、覆盖优先级、空态/插槽、encode 警告、bar/line/pie/scatter dataset 形态、tooltip+legend 透传断言）、`echarts-events-bridge.test.tsx`（6：on/off/normalized event/未知键/reject 捕获/dataZoom）；`echarts-renderer.unit.test.tsx` 默认 theme 断言更新为 `'flux'`（记录在案的行为变更）
- lab page：dataset 切换按钮场景、svg line、click→showToast、dark 主题 pie、空态

**实现期发现**：flux-core `contract-honesty` 守卫（`isRendererEventKeyReferenced`）要求 `events?.<key>` 字面引用模式——动态键 map（`NATIVE_EVENT_MAP[fluxKey]`）不被识别；重构为显式 `nativeBindings` 字面清单（`['click', events?.onClick, ...]`），既满足守卫又比动态查表更诚实可读。

## Verification（E2.1，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37（修复：未用 helper 删除、eslint-disable 摘除改显式 deps——React Compiler 禁optimize 告警）
- `pnpm test` ✅ 68/68 tasks，约 11,527 tests / 0 failed（flux-renderers-data 150 files / 1107 tests）
- `pnpm lint`/`pnpm check`：同 E1.1 登记的 4 个既有 i18n 未定义键红，本计划零新增命中
- e2e 未跑（E5.1 统一覆盖），不构成 full-green 口径

**Status**：E2.1 执行完成，closure audit 待独立子 agent 复核。
