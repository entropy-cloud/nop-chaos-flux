# Daily Log 2026-07-22

- **Calendar P1 Defect Remediation**: Executed plan [`docs/plans/2026-07-22-2300-2-calendar-p1-defect-remediation.md`] — all 3 phases complete.
  - **Closure Audit**: Independent MISSION_DRIVER closure audit completed (fresh sub-agent session). All 7 Calendar P1 defects verified landed against live codebase via grep/glob/read. Owner docs `docs/components/calendar/design.md` §4 (data→events) and §7 (ownership model) synced to implementation. Plan status confirmed `completed`.
  - **Phase 1 — Timezone + event source + resource fallback**: C-DISP-05: `toISODateString`/`isToday`/`isSameDay` unified to `getUTC*` methods; non-UTC timezone tests added. C-DRIFT-01: events read from `resolved.events` with backward-compat `resolved.data` fallback + `console.warn`. C-OPS-07: `_default` resource fallback normalized across month/week/day views.
  - **Phase 2 — Overflow events + drag-create + export**: C-DISP-04: `positionEventsInMonth` iterates same `days` as render loop. C-OPS-02: `confirmCreate` uses `currentDate` min/max for multi-cell drag. C-OPS-04: `useImperativeHandle` exposes `exportToPNG` with AbortSignal + `exportingRef` concurrency guard.
  - **Phase 3 — Ownership model + death component adjudication**: C-OPS-08: Three-mode ownership (local/controlled/scope) with `scope.merge()`. Death components: `CalendarBatchScheduler`, `CalendarTimezoneSelector`, `CalendarResourceGroup`, `CalendarResourceHeader`, `useCalendarICal` all marked `@deprecated` (unwired, retained per design). `useCalendarExport` revived in Phase 2 C-OPS-04.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors ✓, `pnpm test` — scheduling 689/689 ✓ (70 files).

- **Kanban P1 Defect Remediation**: Executed plan [`docs/plans/2026-07-22-2300-1-kanban-p1-defect-remediation.md`] — all 3 phases complete, 689/689 tests ✓ (70 files).
  - **Closure Audit**: Independent MISSION_DRIVER closure audit completed (fresh sub-agent session). All 9 P1 defects verified landed against live codebase via grep/glob/read. Owner docs `docs/components/kanban/design.md` synced to BoardData model (KanbanItem→BoardItem, content type, filterTags added). Docs sync pushed test count to 689/689 (unchanged — doc-only change). Plan status confirmed `completed`.
  - **Phase 1 — BoardData model + filterCard + filterTags**: K-DISP-01: `BoardItem` gains `title?`/`content?` with dual-path reads (`card.title || card.data?.title`). K-DISP-02: `config.render` compiled via `helpers.render()`. K-OP-06: `filterCard` expression compiled through formula compiler (`expressionCompiler.compileValue` → `evaluateCompiled`); `useKanbanFilter.matches` fixed to read `card.data?.title`. `filterTags` registered in renderer definitions.
  - **Phase 2 — DnD visuals + drop indicator**: K-DISP-03: `data-dragging` moved from board root to dragged card element; CSS selector changed to `.nop-kanban-card[data-dragging='true']`. K-DISP-04: Closest edge tracking via `onDrag` (before/after based on `clientY` vs element midpoint); `.nop-kanban-drop-indicator` 2px blue line rendered between cards.
  - **Phase 3 — Regions + operability fixes**: K-DISP-05: All 4 regions (`columnHeader`/`cardTemplate`/`columnFooter`/`columnHeaderToolbar`) passed to `<KanbanColumn>`. K-OP-02: `wipOverLimitColumns` wrapped in `useMemo`. K-OP-04: `selectedTagIds` passed to column with `meta.tags` intersection filtering. K-OP-05: `registerBoardDropZone` called per column element in DnD effect. 5 new focused tests added verifying dual-path title, drop indicator, tag filtering, wip warning, and `data-dragging` attribute.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors (1 pre-existing TanStack Virtual warning) ✓, `pnpm test` — scheduling 689/689 ✓ (70 files).

- **Scheduling P0 Display & Operability Fixes**: Executed plan [`docs/plans/2026-07-22-1600-1-scheduling-p0-display-operability-fixes.md`] — all 4 phases complete.
  - **Phase 1 — Gantt P0 (6 defects)**: G-DISP-01: zoomLevels passed to `createInitialStore` with day/week/month defaults. G-DISP-02: `<tr>` height forced to `store.rowHeight`. G-OPS-01: scrollToToday/scrollToTask scroll timeline directly (not grid). G-OPS-02: `_dirty` guard removed from `parse()`. G-OPS-11+G-OPS-12: GanttGrid/Bars/CellGrid/TimeScale subscribe `useGanttTreeSnapshot()`; `toggleOpen` calls `computeCoordinates`+`computeLinkPolylines` and bumps `layoutRevision`.
  - **Phase 2 — Kanban P0 (1 defect)**: K-OP-01: Controlled mode gate removed. `boardData` always uses local `useState` seeded from `data` prop; `setBoardData` always updates local state (mutations no longer silently dropped).
  - **Phase 3 — Calendar P0 (4 defects)**: C-OPS-01: `activeView` read from `useCalendarState` hook instead of schema `view`. C-DISP-01: Event width changed to `100 / Math.min(dayBlocks.length, effectiveMax)` — single event fills 100%. C-DISP-02: Month grid uses `getDateRange(getMonthStartEnd(...))` — resource×days-in-month (28-31 cols) instead of 42-day generic grid. C-DISP-03: Virtualizer rows use `position:absolute; left:0; right:0`.
  - **Phase 4 — BarcodeInput P0 (1 defect)**: B-DISP-02: `<video>` rendered unconditionally (CSS hidden/visible by phase) so stream attaches to existing element.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors (1 pre-existing TanStack Virtual warning) ✓, `pnpm test` — scheduling 667/667 ✓ (70 files), full workspace all green.
- **Scheduling Contract Drift & Dead Code Remediation**: Executed plan [`docs/plans/2026-07-22-2-scheduling-contract-drift.md`] — all 2 workstreams complete.
  - **Workstream 1 — Kanban contract alignment**: F-55 filterCard wired end-to-end (types→hook→board→definition→test). F-56: all 16 unconsumed Kanban props either consumed or removed. F-57: `data-dragging`/`data-drop-target` CSS selectors applied to DOM, `moveCardKeyboard` wired. F-68: 7 distinct undo command types (`moveCard`, `moveColumn`, `addCard`, `removeCard`, `changeCard`, `addColumn`, `removeColumn`). F-69: `deepCloneBoard` uses `structuredClone`. Prior residual: Kanban undo-stack FIXME resolved with typed command union.
  - **Workstream 2 — Calendar contract alignment + Barcode/Gantt/infrastructure**: F-60: all Calendar unconsumed props/events consumed or removed; dead Calendar components deferred as watch-only residual. F-61: BarcodeInput validation props (`required`/`trimContents`/`minLength`/`maxLength`/`pattern`/`validate`/`continuousScan`) all wired. F-64: critical-path.ts uses `diffWorkingMs()` with `calendar.countWorkDays()`. F-65: resource-load.ts denominator uses `getWorkMinutesForDate()`. F-66: `GanttResource`/`GanttAssignment` single-sourced in `gantt.types.ts` with `id: GanttId = string | number`, re-exported from `schemas.ts`. F-67: `BaselineBars` imported and rendered in `gantt.tsx`. F-70: `react-dom` added to peerDependencies.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors (1 pre-existing TanStack Virtual warning only) ✓, `pnpm test` — scheduling 667/667 ✓ (70 files).
  - **Closure Audit**: Independent MISSION_DRIVER closure audit completed (fresh session). All 12 findings (F-55 through F-70 + prior residual) verified landed against live codebase via grep/glob/read. Zero remaining unchecked items in plan. Plan closed.
- **Scheduling Quality Polish**: Executed plan [`docs/plans/2026-07-22-3-scheduling-quality-polish.md`] — all 3 phases complete.
  - **Phase 1 — Reactive Precision**: Removed redundant subscriptions (treeRevision from GanttBars, taskRevision from GanttLinks, layoutRevision from GanttHeader, dataRevision from store); wrapped unstable callbacks with useCallback in Gantt (handleBarKeyAction, onBarDoubleClick), Kanban DnD (registerCard, registerColumn, registerColumnHeader), and Calendar (goNext/goPrev/goToday/goToDate). All subscription-trivial components still re-render correctly.
  - **Phase 2 — Error Propagation**: WebSocket error/close handlers now log diagnostic context (event, code, reason). Non-Error throw handling in use-barcode-detect uses String(err) fallback. Applied err instanceof Error pattern across use-calendar-ical, use-calendar-export, kanban-export, gantt-compact. Added camera diagnostic logging before user-friendly message in use-barcode-camera. Audit confirmed zero catch→new Error wrapping sites in scheduling package.
  - **Phase 3 — Accessibility**: Added role="columnheader" to Calendar weekday headers. Changed Gantt role from "treegrid" to "grid". Added roving tabIndex + Arrow key navigation to Calendar date cells. Made Kanban "Add Column" button aria-hidden (non-focusable). i18n'd kanban-tag-filter hardcoded Chinese strings, Gantt/Kanban column aria-labels, Calendar conflict indicator. Removed non-standard Enter/Space from CalendarOverlay.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` ✓, `pnpm lint` 0 errors (1 pre-existing warning) ✓, `pnpm test` — scheduling 667/667 ✓ (70 files).
- **Diff-view Defect Fixes & Test Coverage**: Executed plan [`docs/plans/2026-07-22-1600-3-diff-view-defect-fixes-and-test-coverage.md`] — both phases complete.
  - **Phase 1 — P1 fixes**: S19.1: `useImperativeHandle` exposes `toggleViewType`/`setViewType`/`expandAll`/`collapseAll` (React 19 ref-as-prop); `kind: 'reaction'` entries registered in definition. S19.2: `middleContent`/`files`/`activeFileIndex` added to definition `fields`. S19.3: Three-column mode checks `viewType` — user can toggle to split/unified. S19.4: Integration smoke test for `DiffViewRenderer` renders split/unified/three-column modes and verifies DOM structure.
  - **Phase 2 — P2 code quality fixes**: S20.1: Removed unused `useRendererRuntime()`/`useRenderScope()`. S20.2: `language` prop passed to and used by `DiffThreeColumnView` (syntax highlighting via `highlight()`). S20.3: `onHunkExpand` added to `areHunkPropsEqual`. S20.4: Ctrl+↑/↓ scoped to diff-view container via ref focus check. S20.5: 3-way conflict logic refactored to single-pass over original rows. S20.6: `DiffGutterCell`/`renderFileListSidebar` marked `@deprecated`. S20.7: Zero-assertion tests rewritten with concrete assertions. S20.8: Unused `content` parameter removed from `buildInlineHtml`.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors ✓, `pnpm test` 232/232 ✓ (31 files, content package).
- **Full-green verification (MISSION_DRIVER)**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors ✓, `pnpm test` — all packages green (8203 tests across 905 test files).
- **Scheduling Gantt P1 Display & Operability Fixes**: Executed plan [`docs/plans/2026-07-22-1600-2-gantt-p1-display-operability-fixes.md`] — both phases complete.
  - **Phase 1 — Display (6 defects)**: G-DISP-03: bar width min changed from 4px to cellWidth. G-DISP-04: `linkToPolyline` branches by link.type (FS/SS/FF/SF) with leftward wrapping when target is left of source. G-DISP-05: `%V`(ISO week)/`%W`(week-of-year)/`%q`(quarter) formatters added to `FORMAT_TOKENS`. G-DISP-06: milestone diamond wrapper now interactive (tabIndex, role, pointer-events auto, link handles). G-DISP-07: `regions.taskBar` threaded through `gantt.tsx`→`GanttBars`. G-DISP-08: `getCellValue` default reads `(task as any)[col.name]`; predecessor reads `$target`; `col.cell` region rendering wired.
  - **Phase 2 — Operability (7 defects)**: G-OPS-03: ghost uses `translateX(dx)` only; original bar opacity 0.3; drop indicator rendered. G-OPS-04: `onCommit` callbacks passed to drag/link hooks; call `events.onTaskDragEnd`/`events.onLinkDragEnd` on commit. G-OPS-06: ArrowLeft=collapse, ArrowRight=expand; per-bar ArrowLeft/Right→resize removed. G-OPS-07: `updateRowAria` wired into keyboard handler for ArrowUp/Down focus. G-OPS-08: chevron condition uses `store.getVisibleDescendantCount(task.id) > 0`. G-OPS-13: `updateTask` bumps `layoutRevision`. G-OPS-14: per-bar ArrowUp/Down handlers add `stopPropagation()`.
  - **Verification**: `pnpm typecheck` 56/56 ✓, `pnpm build` 30/30 ✓, `pnpm lint` 0 errors ✓, `pnpm test` — scheduling 684/684 ✓ (70 files). Roadmap S15 items updated to `done`.
