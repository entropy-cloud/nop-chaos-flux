# Development Log — 2026-04-25

### 2026-04-25

- Landed the first AI-first explanation contracts for `nop-debugger` instead of adding another snapshot wrapper. `NopDebuggerAutomationApi` and `NopDebuggerController` now expose `explainNodeValue()`, `explainNodeMeta()`, `explainNodeFailure()`, and `explainNodeAsync()`, all returning bounded machine-oriented results with `answer`, `confidence`, `limitations`, `evidenceRefs`, `related`, and `truncated` metadata. The controller implementation reuses existing inspect snapshots, recent related events, async owner snapshots, and existing redaction rules rather than exposing runtime-private objects. Focused controller/automation tests and Playwright contract coverage were extended toward explanation semantics, and the debugger architecture doc now marks explanation APIs as the preferred AI/E2E entry point for “why” questions.
- Link to doc or code path: `packages/nop-debugger/src/{types.ts,controller.ts,automation.ts,controller-component-inspector.ts,explanations.ts,automation.test.ts,controller-inspect.test.ts,panel.test.tsx}`, `tests/e2e/debugger.spec.ts`, `docs/architecture/debugger-runtime.md`
- Key decision: keep the first explanation slice bounded and conservative. Value/meta explanations use current inspect snapshots plus dependency summaries, failure explanation uses bounded related events, and async explanation only returns node-attributable owners inferred from scope/owner ids. Whenever evidence is incomplete, the contract says so through `limitations` instead of pretending to prove a full causal graph.
- Next step: if future debugger work needs deeper dependency-propagation proofs or stronger async owner-to-node identity, split that into a successor plan instead of widening this contract into a runtime architecture rewrite.

### 2026-04-25

- Promoted the first-phase spreadsheet filter state from a purely derived renderer convention into explicit worksheet data owned by the shared spreadsheet model. `WorksheetDocument` now carries a minimal `filters.columns` collection describing active column filter conditions, filter commands now write both that explicit model and the derived `filteredOut` row results, and the shared spreadsheet grid/header filter markers now read from `sheet.filters` instead of reverse-engineering state from hidden rows. Focused core tests now prove filter conditions are persisted and cleared explicitly, while renderer and report-designer tests remain green on top of the new model.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-core/src/{types.ts,core/filter-operations.ts,__tests__/core-basics.test.ts}`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`
- Key decision: introduce the smallest honest explicit filter model now instead of waiting for a full AutoFilter implementation. This keeps the shared spreadsheet/report-designer path moving toward a real spreadsheet state model without overcommitting to a much larger filter UI surface in one step.
- Next step: if filter fidelity keeps expanding, use `sheet.filters` as the owner model for richer header affordances and multi-condition filter workflows rather than layering more implicit derivations on top of row flags.

### 2026-04-25

- Added the first minimal visible filter-state feedback for the shared spreadsheet/report-designer grid so filtering is no longer only inferred from disappearing rows. After updating the docs to require filter-state feedback on the grid/header itself, the shared `SpreadsheetGrid` now marks the whole workbench with `data-sheet-filtered` when any row is filtered out and marks affected column headers with `data-col-filtered`. Canvas styles now give filtered sheets a subtle active outline and filtered columns a header-level marker, and focused renderer tests prove both the row visibility change and the new visual state markers clear correctly when the filter is removed.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,canvas-styles.css,renderers.integration.test.tsx}`
- Key decision: keep the first filter feedback slice attached to existing grid and header affordances instead of introducing a separate filter bar or teaching panel. This preserves the spreadsheet workbench feel while making the filtered state discoverable.
- Next step: if filter fidelity keeps expanding, the next natural step is an explicit shared filter model and richer header affordances rather than relying only on derived row-hidden state.

### 2026-04-25

- Added unit tests for `flux-action-core` (previously zero test coverage). Three test files covering `operation-control.ts` (withRetry, withTimeout, createAbortScope), `action-core.ts` (result classification, evaluation bindings, action control parsing), and `utils/debounce.ts` (scheduleDebounce, cancelPendingDebounce). Total: 71 new tests, all passing with typecheck/build/lint clean.
- Link to code path: `packages/flux-action-core/src/__tests__/{operation-control.test.ts,action-core.test.ts,debounce.test.ts}`
- Key decision: attach `expect(promise).rejects` assertions before advancing fake timers to avoid unhandled rejection warnings in vitest. Pattern: `const assertion = expect(promise).rejects.toThrow(...)` then `await vi.advanceTimersByTimeAsync(ms)` then `await assertion`.
- Next step: consider integration-style tests for `action-dispatcher.ts` (738 lines, complex runtime dependencies).

### 2026-04-25

- Landed the first real shared spreadsheet `filter` slice instead of inventing a fake Excel-style placeholder. After narrowing the docs to a minimal honest scope, `spreadsheet-core` now supports filtering rows by the currently selected cell value in a column and clearing worksheet filter results, implemented via a dedicated `filteredOut` row flag so filter results stay distinct from manually hidden rows. The shared spreadsheet renderer now omits `filteredOut` rows from the visible grid and exposes `Filter by Selected Cell Value` plus `Clear Filter` in the shared context menu. Focused core and renderer tests now prove both the low-level filter behavior and the end-to-end context-menu path, including clearing the filter without undoing manual row hides.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-core/src/{types.ts,commands-base.ts,commands.ts,index.ts,core/filter-operations.ts,command-handlers/sheet-handlers.ts,__tests__/core-basics.test.ts}`, `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,renderers.integration.test.tsx}`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`
- Key decision: implement `filter` as a real phased capability instead of waiting for a full AutoFilter clone. The first shared slice supports a genuine Excel-native action (`filter by selected value`) with actual row visibility feedback, while still deferring richer dropdown/filter-state UX until the shared spreadsheet model grows further.
- Next step: if we keep pushing filter fidelity, the next shared slice should add a more explicit filter model and richer UI affordances rather than overloading row hidden flags indefinitely.

### 2026-04-25

- Landed the first real shared spreadsheet `sort` baseline instead of faking a broader `sort/filter` menu. After tightening the docs to allow phased delivery where real `sort` can ship before `filter`, `spreadsheet-core` now exposes a new `spreadsheet:sortRange` command and a focused sort operation that reorders whole rows inside a range by a selected key column. The shared spreadsheet context menu now surfaces `Sort A to Z` / `Sort Z to A`, using the current selection anchor as the key column and expanding the sorted range to the rightmost used column in the selected row block so row data stays together. Focused core and renderer tests now prove both the low-level sort command and the shared context-menu path.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-core/src/{types.ts,commands-base.ts,commands.ts,index.ts,core/sort-operations.ts,command-handlers/cell-handlers.ts,__tests__/core-basics.test.ts}`, `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,renderers.integration.test.tsx}`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`
- Key decision: ship only the smallest honest Excel-native slice by implementing real sort end-to-end and continuing to defer filter. This keeps the shared report-designer/spreadsheet workbench on the “no fake UI” path while still making forward progress on a genuine spreadsheet capability.
- Next step: if `filter` is still required, the next owner slice must start in `spreadsheet-core` with an explicit filter model and command contract before any shared menu exposure.

### 2026-04-25

- Audited whether Excel-style `sort/filter` should be the next shared report-designer/spreadsheet context-menu slice and deliberately stopped short of adding any UI because the shared spreadsheet layer does not yet have real sort/filter commands, state, or renderer feedback. Instead of shipping placeholder menu items or a report-designer-only shim, the architecture and component docs were updated to make the boundary explicit: Excel-native actions may only enter the shared menu once the shared spreadsheet runtime already supports them end-to-end.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`
- Key decision: preserve the “Excel-native only, no fake UI” rule by refusing to expose sort/filter affordances before the shared spreadsheet model and command surface exist. This keeps the report-designer workbench honest and avoids another host-specific fork in behavior.
- Next step: if sort/filter is still desired, the next real slice must start in `spreadsheet-core` with a data model and command contract, then surface upward into the shared renderer/menu only after that foundation exists.

### 2026-04-25

- Tightened the shared spreadsheet/report-designer context menu so action availability now follows the current Excel-style selection context instead of staying broadly clickable. The shared `SpreadsheetGrid` now disables merge for single-cell selections, disables unmerge unless the active selection exactly matches an existing merge range, disables freeze for whole-sheet selection, and gates row-vs-column structural actions by the current selection kind rather than simply checking for any anchor cell. Focused renderer tests now lock down two key user-facing cases: `Merge Cells` is disabled for a single selected cell, and `Freeze Panes` is disabled when the whole sheet is selected.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,renderers.integration.test.tsx}`
- Key decision: tighten enablement at the shared menu layer instead of leaving commands callable and then no-oping later. The user-facing Excel contract is about what appears actionable in a given context, not just which commands happen to early-return internally.
- Next step: decide whether to continue expanding Excel-native context actions like sort/filter, or shift to polishing the exact freeze-anchor and merge/unmerge matching semantics.

### 2026-04-25

- Closed the remaining Excel-style batch-structure validation gap for column selections by adding focused shared-renderer coverage for multi-column insert and delete actions. The spreadsheet renderer tests now prove that selecting columns `A:B` and choosing `Insert Column Right` inserts two columns after the selected block, and that selecting `B:C` and choosing `Delete Column` removes both selected columns and shifts later content left. This locks down the same batch structural semantics for columns that had already been established for multi-row selections, ensuring the shared report-designer/spreadsheet canvas treats row and column header selections consistently.
- Link to doc or code path: `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,renderers.integration.test.tsx}`
- Key decision: keep batch column semantics validated at the shared renderer layer because the user-visible risk was in the selection-to-command translation performed by the context menu, not in the low-level structure operations themselves.
- Next step: decide whether to keep extending the shared context menu with additional Excel-native actions like sort/filter, or shift focus to finer behavior details such as selection-sensitive enablement and exact freeze-anchor conventions.

### 2026-04-25

- Added the first visible canvas feedback for shared branch selection. The renderer now maps `snapshot.activeBranch` onto a lightweight branch-focus visual state: the active branch’s first node is marked as branch-focused in xyflow node data, edges touching that branch child are marked as branch-focused in edge data, and both the generic edge renderer and the DingFlow tree edge renderer now visually highlight those branch-focused connectors. This gives tree-mode users a visible confirmation on the canvas when the shared `activeBranch` selection changes, without inventing separate branch overlay rendering yet.
- Link to doc or code path: `packages/flow-designer-renderers/src/designer-xyflow-canvas/{types.ts,xyflow-utils.ts,xyflow-utils.test.ts,designer-xyflow-node.tsx,designer-xyflow-edge.tsx,designer-xyflow-canvas.tsx}`, `packages/flow-designer-renderers/src/dingflow/ding-flow-edge.tsx`
- Key decision: start branch-focus visuals with the branch’s first real node and adjacent connectors instead of trying to render a brand-new branch-only selection layer. This keeps the feedback tied to existing graph projection artifacts and is enough to make shared branch selection visible immediately.

### 2026-04-25

- Promoted branch focus from local inspector UI state into the shared tree-mode selection contract. The flow-designer architecture docs now define branch-level selection as a tree-only structural selection attached to a branch owner, `SelectionSummary` now carries `activeBranchId`, `DesignerSnapshot` now carries `activeBranch`, and the core exposes `selectBranch(ownerNodeId, branchId)` so renderer consumers can share the same branch focus instead of each inventing local state. The host-scope bridge now exposes `selection.activeBranchId` and `activeBranch`, and the default inspector now reads that shared selection state instead of its own private branch focus. Focused core and renderer tests now cover the new selection behavior.
- Link to doc or code path: `docs/architecture/flow-designer/{design.md,tree-mode.md}`, `packages/flow-designer-core/src/{types.ts,designer-core-types.ts,core.ts,core/selection.ts,core/selection-controller.ts,core/snapshot.ts,__tests__/core-graph.test.ts}`, `packages/flow-designer-renderers/src/{designer-command-types.ts,designer-command-adapter.ts,designer-action-provider.ts,designer-manifest.ts,designer-context.ts,designer-inspector.tsx,designer-controls.test.tsx,index.test.tsx,canvas-bridge.test.tsx}`
- Key decision: keep branch selection as a single tree-mode focus (`activeBranchId`) rather than introducing branch multi-select. This is enough to unify inspector/toolbar/schema consumption now while keeping the selection contract small and compatible with the existing node/edge-first graph editor baseline.

### 2026-04-25

- Extended the new branch-group inspector into a branch-focused inspector workflow without changing the global core selection model yet. Tree projection branch summaries now include the first branch child’s id/type/label, and the default inspector keeps a local focused branch within the active branch owner. Clicking a branch card now reveals a dedicated “当前分支” area and a `定位节点` action that jumps selection to that branch’s first node, which gives authors a lightweight branch-targeting flow before introducing full branch-level selection into the shared core snapshot model. Focused tests now cover both the richer projection summary and the inspector’s branch-focus dispatch behavior.
- Link to doc or code path: `packages/flow-designer-core/src/tree-projection.{ts,test.ts}`, `packages/flow-designer-renderers/src/{designer-inspector.tsx,designer-controls.test.tsx}`
- Key decision: add branch-focused inspection as local inspector UI state first, rather than expanding the core selection contract immediately. This keeps the new branch authoring workflow useful now while avoiding a premature cross-cutting change to every selection consumer.

### 2026-04-25

- Landed the first branch-group inspector editing slice for Flow Designer tree mode. Tree projection now exposes branch header summaries on branch-owner node data (`data.branches`), the renderer command surface gained `updateBranchData`, and the default inspector now renders a dedicated branch-group section whenever the active node owns branches. From that panel users can rename branch headers, move branches left/right, delete branches, and append a new branch, all through the tree-owned command path instead of editing projected graph artifacts. Focused tests now cover both the projection summary and the inspector dispatch behavior.
- Link to doc or code path: `packages/flow-designer-core/src/tree-projection.{ts,test.ts}`, `packages/flow-designer-renderers/src/{designer-command-types.ts,designer-command-adapter.ts,designer-action-provider.ts,designer-manifest.ts,tree-commands.ts,designer-inspector.tsx,designer-controls.test.tsx}`
- Key decision: expose only branch header metadata in the first inspector slice, not full branch-subtree editing. This keeps the initial branch-group authoring UI small and stable while still making add/move/delete/rename available from a visible panel instead of only through menus.

### 2026-04-25

- Finished the first visible DingFlow wording cleanup so the UI matches the new structured branch semantics. The branch-group overlay component now uses a dedicated `flowDesigner.addBranch` i18n key and renders `Add Branch` / `添加分支` instead of incorrectly reusing `Add Condition` / `添加条件`. The component export was renamed to `DingFlowAddBranchOverlay`, the tree canvas paths were updated to import it under that meaning, and a focused jsdom test now locks the new wording down.
- Link to doc or code path: `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`, `packages/flow-designer-renderers/src/dingflow/{ding-flow-add-condition-overlay.tsx,index.ts,ding-flow-add-branch-overlay.test.tsx}`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/designer-xyflow-canvas.tsx`
- Key decision: fix the overlay wording at the component/i18n level instead of relying on users to infer the difference from placement alone. Once `dt-condition` is split between “create branch group” and “add branch to existing group,” the branch-group overlay must say `Add Branch` explicitly.

### 2026-04-25

- Closed the validation gap for the new shared freeze-pane context actions by adding focused renderer coverage for both `Freeze Panes` and `Unfreeze Panes`. The shared spreadsheet renderer tests now prove that freezing from a context-menu action at `B2` writes `{ row: 1, col: 1 }` into the active sheet’s `frozen` state, and that `Unfreeze Panes` clears that same state again. This gives the shared report-designer/spreadsheet canvas path end-to-end verification that the new Excel-native context-menu actions are not just visible, but actually mutate the underlying sheet model as intended.
- Link to doc or code path: `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: verify freeze/unfreeze at the shared renderer layer instead of relying only on lower-level spreadsheet-core tests, because the real user-facing risk was whether the context-menu surface was correctly wired into the existing commands.
- Next step: decide whether to keep extending the shared context menu with additional Excel-native actions such as sort/filter, or pivot to polishing existing behavior details like freeze anchor conventions and selection-sensitive enablement.

### 2026-04-25

- Split the DingFlow `dt-condition` authoring semantics by entry source so the UI no longer conflates “create a new branch group” with “add a branch to an existing group.” The tree-mode plus-button plumbing now carries a `sourceKind` (`node`, `branch-group`, or `merge`) from the canvas entrypoint into the shared DingFlow menu-command helper. As a result, selecting `dt-condition` from a node’s bottom plus button still creates a new branch pair (`insertBranchPair`), while selecting it from the branch-group overlay correctly adds one more branch (`addBranch`). Focused tests now lock down both command paths.
- Link to doc or code path: `packages/flow-designer-renderers/src/{designer-context.ts,designer-page.tsx,designer-canvas.tsx,designer-xyflow-canvas/designer-xyflow-canvas.tsx,designer-xyflow-canvas/designer-xyflow-node.tsx}`, `packages/flow-designer-renderers/src/dingflow/{dingflow-command-dispatch.ts,dingflow-command-dispatch.test.ts}`
- Key decision: separate authoring semantics at the command-dispatch layer before changing visuals. The same menu item (`dt-condition`) has two legitimate meanings in DingFlow, and the correct meaning depends on whether the user clicked a plain node continuation, an existing branch-group overlay, or a merge continuation.

### 2026-04-25

- Started moving DingFlow authoring UI onto the new branch-native command surface instead of routing every condition-menu action through the old `insertBranchPair` shortcut. The menu dispatch logic behind the DingFlow overlay was extracted into a dedicated helper and updated so choosing a condition node from an existing branch-group overlay now emits `addBranch`, while merge overlays still emit `insertChainNodeAtMerge` and ordinary chain insertions still emit `insertChainNode`. Focused tests now lock that command selection down directly.
- Link to doc or code path: `packages/flow-designer-renderers/src/dingflow/{ding-flow-canvas-overlay.tsx,dingflow-command-dispatch.ts,dingflow-command-dispatch.test.ts}`
- Key decision: fix the authoring command semantics first, before redesigning the overlay visuals. Existing DingFlow overlays were already sitting on top of real branch groups, so their condition-menu action should add a branch to that group rather than pretending a brand-new branch pair is being created each time.

### 2026-04-25

- **Phase 3 of Plan 139 completed**: Removed `api: ApiSchema` from `ActionDataSourceSchema` and all data-source bypass paths. Schema now uses `action: 'ajax', args: { url, method, ... }` instead of `api: { url, method, ... }`. Changes: (1) `flux-core/types/schema.ts`: `ActionDataSourceSchema.api` removed, `action: string` now required. (2) `flux-core/types/compilation.ts`: `CompiledDataSource.kind` changed from `'api'|'formula'` to `'action'|'formula'`. (3) `flux-compiler/source-compiler.ts`: Compiles `action`+`args` into internal `CompiledApiConfig`. (4) `flux-compiler/shape-validation.ts`: Validates `action` instead of `api`. (5) `flux-runtime/async-data/source-registry.ts`: Uses `kind === 'action'`. (6) `flux-runtime/async-data/data-source-runtime.ts`: Removed `api` fallback from `createSourceExecutor`. (7) All test files migrated: `runtime-sources.test.ts`, `runtime-sources-refresh.test.ts`, `runtime-sources-merge.test.ts`, `runtime-imports.test.ts`, `source-reaction-dependencies.test.ts`, `schema-renderer-runtime-core.test.tsx`, `schema-renderer-runtime-dialogs.test.tsx`, `source-compiler.test.ts`.
- Link to doc or code path: `docs/plans/139-unified-action-dispatch-for-submit-validation-and-data-access.md`, `packages/flux-core/src/types/schema.ts`, `packages/flux-compiler/src/source-compiler.ts`
- Key decision: Kept `CompiledApiConfig` as internal compiled representation (renamed `kind` from `'api'` to `'action'`), derived from `action.args` when `action === 'ajax'`. This preserves the full caching/polling/dedup runtime without rewriting `createDataSourceController`.
- Next step: Phase 4 (docs cleanup) is also done. Plan 139 is now complete.

### 2026-04-25

- Updated documentation to reflect Plan 139 completion (unified action dispatch). Changes: (1) Removed `api?: ApiSchema` from the documented `ActionSchema` interface in `docs/architecture/action-scope-and-imports.md`. (2) Removed legacy `api.dedupStrategy` reference from `docs/architecture/api-data-source.md`. (3) Updated stale async validation example in `docs/references/integrating-third-party-components.md` from `{ kind: 'async', api: ... }` to `{ kind: 'async', action: ... }`. (4) Marked Plan 139 as completed with all phases checked and closure section filled.
- Link to doc or code path: `docs/plans/139-unified-action-dispatch-for-submit-validation-and-data-access.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/api-data-source.md`, `docs/references/integrating-third-party-components.md`
- Key decision: Plan 139 doc updates confirmed that `api-data-source.md` already uses `action: "ajax"` pattern in all examples; `object-field.md` line 11 `api: { method, url, headers }` is a form field usage example, not a schema-level `api:` reference — no change needed.

### 2026-04-25

- Added the first real branch-group-native command surface for Flow Designer tree mode. The renderer command/action/manifest layer now exposes `addBranch`, `deleteBranch`, and `moveBranch`, and the tree-owned command path rewrites `TreeDocument.branches[]` directly instead of faking branch-group edits by only manipulating projected graph edges. The initial semantics are intentionally narrow and structured: adding a branch appends a new branch with renumbered priority, moving a branch swaps order left/right and rewrites priorities, and deleting a branch is only allowed when the owner still keeps at least two branches after deletion. Focused adapter tests now cover all three operations against a real tree-owned branch group.
- Link to doc or code path: `packages/flow-designer-renderers/src/{designer-command-types.ts,designer-action-provider.ts,designer-manifest.ts,tree-commands.ts,designer-command-adapter.ts,designer-command-adapter.test.ts}`
- Key decision: introduce explicit branch-group commands before attempting richer UI parity. This keeps the structured-process-tree contract honest at the runtime layer first, so later overlay/inspector work can call real tree operations instead of graph-mutation shims.

### 2026-04-25

- Extended the new tree-owned Flow Designer path beyond insertion commands. In tree mode, `updateNodeData` and `deleteNode` now also operate on the live `TreeDocument` first, then re-project the graph into the shared core, instead of mutating the projected graph document directly. The first delete behavior is intentionally conservative: root deletion is still rejected, ordinary chain-node deletion reconnects the deleted node’s `child`, and deleting the last node inside a branch clears that branch so the existing implicit merge semantics fall back to the branch owner continuation. Focused adapter tests now cover tree-owned update and delete flows in addition to the earlier insert cases.
- Link to doc or code path: `packages/flow-designer-renderers/src/{tree-commands.ts,designer-command-adapter.ts,designer-command-adapter.test.ts}`
- Key decision: keep the first tree-owned delete semantics narrow and structurally safe instead of trying to solve every branch-owner/gateway deletion case in one pass. The priority is to stop ordinary tree editing from silently mutating the projected graph as the source of truth.

### 2026-04-25

- Promoted more Excel-native spreadsheet actions into the shared context menu so report-designer no longer depends on toolbar-only access for basic merge and pane-freeze workflows. After updating the report-designer docs to treat merge/unmerge and freeze/unfreeze as valid shared workbench actions, the shared `SpreadsheetGrid` context menu now exposes `Merge Cells`, `Unmerge Cells`, `Freeze Panes`, and `Unfreeze Panes`, all implemented by reusing the existing `spreadsheet:*` command chain rather than adding host-specific behavior. Focused renderer coverage now proves that selecting `A1:B1` and invoking `Merge Cells` from the shared menu creates the expected merge range, which means report-designer inherits the same context-menu capability through the shared spreadsheet canvas.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: add merge/freeze actions at the shared spreadsheet menu layer because they are Excel-native grid behaviors and already have stable low-level commands. This keeps the workbench interaction model aligned across standalone spreadsheet pages and report-designer canvases without inventing report-designer-only shortcuts.
- Next step: keep tightening Excel parity by verifying freeze behavior through focused tests and deciding whether other native actions like sort/filter belong in the same shared context surface.

### 2026-04-25

- Continued the Excel-native structural editing convergence by making shared context-menu insert/delete behavior respect the full size of multi-row and multi-column header selections instead of acting only on the selection anchor. After updating the report-designer docs to say multi-selection structural edits should execute against the whole selected extent, the shared `SpreadsheetGrid` now derives row/column start, end, and count from header selections and forwards that count into the existing insert/delete commands. Focused renderer coverage now proves that selecting two rows and choosing `Insert Row Below` inserts two rows below the selected block and shifts following content down accordingly, so report-designer inherits Excel-style batch structure edits through the shared canvas.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: keep batch row/column structure semantics in the shared spreadsheet menu layer and map multi-selection extent onto the existing `count` support already present in the low-level insert/delete commands, instead of introducing separate batch commands.
- Next step: continue Excel parity by refining more native structure actions for multi-column selection and evaluating whether merge/freeze menu actions should be exposed through the same shared context surface.

### 2026-04-25

- Started the real ownership correction for Flow Designer tree mode: structural commands no longer have to mutate the projected graph document first. Tree-mode `designer-page` now keeps a live `TreeDocument` state, `createDesignerCommandAdapter()` can receive a tree owner, and the first structured commands (`insertChainNode`, `insertChainNodeAtMerge`, `insertBranchPair`) now rewrite the `TreeDocument`, re-project it, and replace the whole projected graph document in the shared core. To support that bridge, `DesignerCore` gained a host-facing `replaceDocument()` method. Focused tests now prove both the new core replacement path and that tree-owned command execution updates the underlying `TreeDocument` instead of only rewriting graph edges/nodes.
- Link to doc or code path: `packages/flow-designer-core/src/{designer-core-types.ts,core.ts,__tests__/core-graph.test.ts}`, `packages/flow-designer-renderers/src/{designer-page.tsx,designer-command-adapter.ts,designer-command-adapter.test.ts,tree-commands.ts}`
- Key decision: move tree mode toward tree-owned authoring incrementally instead of rewriting the whole core in one step. The projected graph remains the rendering/runtime bridge, but tree-mode structure commands now treat `TreeDocument` as the source of truth whenever the renderer provides a tree owner.

### 2026-04-25

- Continued the Excel-native convergence for the shared spreadsheet/report-designer workbench by replacing the context menu’s generic insert actions with directional variants that match spreadsheet user expectations. After updating the report-designer docs to say structural actions should prefer Excel-style directionality, the shared `SpreadsheetGrid` context menu now exposes `Insert Row Above`, `Insert Row Below`, `Insert Column Left`, and `Insert Column Right` instead of only ambiguous generic insert entries. The implementation still reuses the existing `spreadsheet:insertRow` / `spreadsheet:insertColumn` commands by mapping current selection anchors to the correct before/after index, and focused renderer coverage now proves `Insert Row Below` really inserts after the current row and shifts following content down.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`
- Key decision: keep directional insert behavior in the shared spreadsheet context menu and implement it by translating Excel-style wording to the existing structural command contract, rather than introducing new low-level command types just for menu semantics.
- Next step: continue tightening Excel parity in the shared menu by adding more native spreadsheet actions or refining delete/insert behavior for multi-row and multi-column selections.

### 2026-04-25

- Tightened the Excel-compatibility boundary for the report-designer workbench and landed the next shared spreadsheet interaction slice: double-clicking the fill handle now auto-fills downward using an adjacent data-region extent instead of behaving like a custom whole-grid shortcut. The docs now explicitly say that, outside report-designer-specific property editing, default canvas behavior should stay within Excel-native capabilities. In the shared spreadsheet interactions layer, the fill handle now supports a double-click path that reuses the existing `spreadsheet:fillSeries` command and computes the fill target from the nearest adjacent populated column, so report-designer inherits the same behavior through the shared canvas. Focused renderer coverage now proves both the positive case (adjacent data exists) and the no-op case (no adjacent extent).
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-fill-handle.ts`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/use-spreadsheet-interactions.ts`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`, `apps/playground/src/pages/report-designer-demo.tsx`
- Key decision: keep fill-handle double-click inside the shared spreadsheet interaction layer and implement only the Excel-native downward autofill path. The feature reuses the existing fill-series command contract and avoids introducing any report-designer-only or non-Excel gesture semantics into the shared canvas.
- Next step: extend the Excel-native convergence with other real spreadsheet behaviors such as richer context-menu actions or better insert-row/insert-column variants, while continuing to exclude non-Excel custom canvas gestures.

### 2026-04-25

- Re-baselined DingFlow/tree mode from a loose “tree projected onto a graph canvas” description to the more accurate structured-process-tree contract: a node may own a branch group, the branch group implicitly merges, and the continuation then flows into the owner node’s downstream `child`. After updating the architecture and `designer-page` docs to encode that model, the renderer layer was tightened so tree mode no longer exposes graph-first interaction paths by default: free connect, free reconnect, and manual node dragging are now disabled on the React Flow bridge when `documentMode === 'tree'`. Focused renderer tests now cover both the disabled bridge callbacks and the absence of pending free-graph connect intent in tree mode.
- Link to doc or code path: `docs/architecture/flow-designer/{design.md,tree-mode.md}`, `docs/components/designer-page/design.md`, `packages/flow-designer-renderers/src/{designer-canvas.tsx,designer-xyflow-canvas/designer-xyflow-canvas.tsx,canvas-bridge.test.tsx,index.xyflow.test.tsx}`
- Key decision: treat DingFlow-like authoring as structured process editing, not as graph editing with a tree-looking layout. The graph canvas remains the rendering/bridge surface, but tree mode operations must stay within structured sequence / branch-group / continuation semantics.

### 2026-04-25

- Landed the first real shared context-menu baseline for the spreadsheet/report-designer canvas path after updating the report-designer docs to treat right-click actions as part of the primary workbench contract. `SpreadsheetGrid` now exposes a shared `ContextMenu` surface that follows the already-normalized selection state and provides copy, cut, paste, clear, insert row, delete row, insert column, and delete column actions without routing users back through toolbar-only flows. Added focused renderer coverage proving the shared menu can open on a selected cell and clear its contents, which means `report-designer-page` inherits the same right-click action surface through the shared canvas.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`
- Key decision: keep the context-menu slice inside the shared spreadsheet renderer instead of inventing a report-designer-only host menu. The existing spreadsheet command contract was already sufficient for the first menu, so this slice adds no new core command surface and keeps spreadsheet/report-designer behavior aligned.
- Next step: extend the shared context menu with richer spreadsheet actions such as merge/fill/freeze or move on to the next Excel-like mouse behavior gap, especially double-click fill-handle behavior.

### 2026-04-25

- Aligned the DingFlow example data with the visual spec instead of leaving the right-side condition branch empty. The playground `dingtalk-workflow-tree-schema.json` now gives the “短期请假” branch a real approval node (`直接主管审批`), the core DingTalk projection fixture/tests were updated to the same two-branch / two-approval shape, and the example doc now shows both condition branches converging into the downstream CC node. This removes one of the last “looks wrong” mismatches where the tree renderer was behaving consistently with the data, but the sample data itself did not match the intended DingTalk visual contract.
- Link to doc or code path: `apps/playground/src/schemas/dingtalk-workflow-tree-schema.json`, `packages/flow-designer-core/src/tree-projection.dingtalk.test.ts`, `docs/examples/dingtalk-workflow-tree.md`
- Key decision: treat the playground DingFlow schema and the core DingTalk projection fixture as the same contract example, so visual-spec cleanup happens in both places together instead of letting demo data and test data drift apart.

### 2026-04-25

- Tightened the shared spreadsheet/report-designer mouse baseline to better match Excel by making right-click selection behave like a real spreadsheet workbench instead of a passive browser context action. In the shared `SpreadsheetGrid`, right-clicking a cell now first selects that cell when it is outside the current selection, and right-clicking a row header, column header, or the top-left corner similarly normalizes selection to that row, column, or whole sheet before any context-menu flow continues. Added focused renderer integration coverage for right-click cell selection so report-designer inherits the same behavior through the shared canvas path.
- Link to doc or code path: `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: land the next Excel-like interaction slice in the shared spreadsheet renderer layer instead of patching report-designer only. Selection normalization on right-click is a core grid behavior and should be consistent across standalone spreadsheet pages and report-designer canvases.
- Next step: continue on the next shared mouse-path gap, likely richer fill-handle behavior or a real context-menu action surface.

### 2026-04-25

- Improved the Flow Designer tree-mode first-paint baseline so DingFlow-like examples no longer stay on the coarse synchronous fallback layout until the user manually clicks auto-layout. Tree mode still gets a cheap synchronous `simpleTreeLayout()` seed for the first render, but `designer-page` now immediately runs one ELK layout pass after mount when `treeConfig.autoLayout` is enabled and applies the resulting positions into the live core. Focused tree-mode renderer tests now prove that initial mount triggers exactly one ELK layout pass.
- Link to doc or code path: `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-renderers/src/designer-page.tree.test.tsx`
- Key decision: keep initial tree rendering responsive by retaining the synchronous fallback layout, then converge to the better ELK layout through a one-shot post-mount refinement instead of making `designer-page` wait on async layout before creating the core.

### 2026-04-25

- Fixed two Flow Designer UX/baseline issues that were showing up together as “canvas drag feels insensitive” and “tree-mode single-edge chains look slightly off-center.” The first fix relaxed viewport normalization from integer / 0.1-step quantization to finer decimal precision and started actually passing the normalized pan/zoom canvas config through to React Flow, so controlled viewport updates no longer snap as aggressively during pan/zoom gestures. The second fix corrected `simpleTreeLayout()` to convert its center-based layer coordinates into React Flow top-left node positions, which keeps single-column tree chains center-aligned and allows DingFlow straight edges to stay vertical when the nodes are genuinely in the same column.
- Link to doc or code path: `packages/flow-designer-renderers/src/designer-xyflow-canvas/{designer-xyflow-canvas.tsx,xyflow-utils.ts}`, `packages/flow-designer-core/src/{tree-layout.ts,core/viewport.ts}`, `packages/flow-designer-core/src/tree-layout.test.ts`, `packages/flow-designer-renderers/src/{index.test.tsx,designer-command-adapter.test.ts}`
- Key decision: keep the current controlled-viewport architecture and simple tree prelayout path, but remove the avoidable precision loss and coordinate-space mismatch before considering a larger DingFlow-specific layout rewrite.

### 2026-04-25

- Updated the `report designer` documentation baseline before changing code so the workbench contract now explicitly treats the canvas as an Excel-like direct-manipulation editor rather than a teaching demo. The family design doc and `report-designer-page` component contract now say the default workbench should prioritize mouse-first cell interaction and should not reserve bottom-of-canvas space for `event log` or keyboard-shortcut cheat sheets. The playground demo was then aligned to that contract by removing the bottom `Event Log` and `Keyboard Shortcuts` blocks and dropping the demo-only logging state.
- Link to doc or code path: `docs/architecture/report-designer/design.md`, `docs/components/report-designer-page/design.md`, `apps/playground/src/pages/report-designer-demo.tsx`
- Key decision: treat shortcut support as an optional productivity layer, not the primary visible interaction model. Report designer must feel operable from the grid itself, with toolbar/inspector/shortcuts augmenting direct cell manipulation instead of explaining it through persistent training UI.
- Next step: continue tightening Excel-like mouse behavior in the shared spreadsheet/report-designer canvas path, especially for any remaining gaps such as richer fill-handle and context-menu interactions.

### 2026-04-25

- Promoted the report-designer selection summary into a shared default inspector provider used by the real `report-designer-page` path, and stabilized it so it does not steal the primary inspector tab from host-provided panels. The page renderer now injects the default provider through config wrapping and registers the shared provider at runtime, while the provider itself renders its summary as a low-priority inspector section instead of a competing tab.
- Link to doc or code path: `packages/report-designer-renderers/src/default-selection-summary-inspector.ts`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/index.ts`
- Key decision: keep the default selection summary in the shared renderer layer so both the formal report-designer path and playground can reuse one inspector implementation, but surface it as a `section` to preserve existing host tab semantics such as custom save/provider panels.
- Next step: decide whether the playground demo should fully delegate to this shared provider and delete any remaining local selection-summary duplication.

### 2026-04-25

- Replaced the playground report-designer's hardcoded inspector summary JSX with the same schema-driven inspector pipeline used by the real report-designer renderer path. The demo now registers inspector providers that return `InspectorPanelDescriptor.body` schemas for cell/row/column/sheet/range targets, rebuilds report-designer host scope data from the real spreadsheet runtime snapshot, and renders the right-side inspector through `report-inspector-shell` instead of a one-off React summary component.
- Link to doc or code path: `apps/playground/src/pages/report-designer-demo.tsx`, `packages/report-designer-core/src/runtime/inspector-panels.ts`, `packages/report-designer-renderers/src/inspector-shell-renderer.tsx`
- Key decision: align the playground with the production report-designer panel pipeline instead of teaching the demo a separate inspector architecture. Playground-only selection summaries can still exist, but they should exist as inspector providers that return schema bodies, not bespoke JSX.
- Next step: run full workspace verification and then decide whether the current playground-only summary providers should be promoted into a reusable default report-designer inspector package layer.

### 2026-04-25

- Tightened the newer non-cell selection slice by making the playground report-designer inspector summarize multi-row and multi-column selections instead of only showing a single row/column index. The inspector now exposes count/start/end/list summaries for row and column selections, and spreadsheet renderer integration coverage now includes `Shift`-extended row-header selection in addition to the earlier column-header path.
- Link to doc or code path: `apps/playground/src/pages/report-designer-demo.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: present multi-row/multi-column selections as “count + start/end + explicit list” because the current spreadsheet core stores them as sorted discrete arrays, not guaranteed contiguous ranges.
- Next step: run full workspace verification and then decide whether row/column/sheet/range summaries should move from the playground demo into a reusable inspector panel contract.

### 2026-04-25

- Extended the newer spreadsheet/report-designer selection baseline in two directions: header selection now supports `Shift` extension for multi-row and multi-column selections, and the playground report-designer inspector no longer assumes every selection is a cell. The inspector now reacts to live report-designer selection targets for row, column, sheet, and range states so non-cell selections stay visible instead of collapsing back to the old cell-only summary.
- Link to doc or code path: `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`, `apps/playground/src/pages/report-designer-demo.tsx`
- Key decision: keep the richer inspector behavior in the playground host page for now rather than broadening the generic report-inspector renderer contract. The shared runtime already publishes the right selection target kinds; the immediate owner gap was the host-page presentation layer.
- Next step: run full workspace verification, then decide whether the generic report-inspector renderer should gain built-in fallback panels for row/column/sheet/range targets or whether playground coverage is enough for the current slice.

### 2026-04-25

- Closed Plan 138 after a second independent closure audit came back clean on the live repo state. Before closure, Phase 4 synced the remaining user-facing surfaces: the CRUD playground now includes request-owned refresh, quick-edit, and client-mode scenarios; the migration example now reflects source-result/request-owner cooperation and `clientMode` migration notes; the quick-edit test suite now proves `quickSaveAction` fallback as well as `quickSaveItemAction`. The re-audit found no remaining plan-owned gaps after those updates.
- Link to doc or code path: `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `docs/components/crud/design.md`, `docs/components/crud/migration-example.json`, `packages/flux-renderers-data/src/__tests__/data-crud-quick-edit.test.tsx`
- Key decision: close the plan on plan-owned evidence, not on total workspace cleanliness. Ambient noise remains outside the plan scope: an unrelated `flux-playground` lint failure in `dynamic-renderer-lab-page.tsx` and non-blocking large-chunk warnings during playground build.
- Closure audit evidence: `task_id: ses_23c4c10a0ffelzd3faTDRoVl8z`

### 2026-04-25

- Continued the report-designer/spreadsheet workbench convergence by making non-cell selections behave like real spreadsheet ranges instead of being UI-only states. Shared spreadsheet interactions now derive copy/clear/paste targets from row, column, and whole-sheet selections, and the shared canvas styling moved closer to the newer workbook-style visual baseline with stronger header/selection affordances.
- Link to doc or code path: `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-selection.ts`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-clipboard.ts`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/canvas-styles.css`, `packages/spreadsheet-renderers/src/renderers.integration.test.tsx`
- Key decision: keep row/column/sheet clipboard behavior inside the existing range-based command model by deriving synthetic ranges in the renderer interaction layer, instead of widening the clipboard command contract for every selection kind.
- Next step: validate the richer selection baseline in the full workspace and then decide whether row/column/sheet-specific inspector panels should become the next owner slice for report-designer.

### 2026-04-25

- Improved the live report-designer/spreadsheet workbench experience instead of only adjusting a standalone demo. The playground `report-designer` page now uses a full-screen editor layout, its left field palette can collapse to preserve canvas space, and the shared spreadsheet grid now wires header-driven sheet/row/column selection through the existing selection model so report-designer selection targets can follow non-cell selections too.
- Link to doc or code path: `apps/playground/src/pages/report-designer-page.tsx`, `apps/playground/src/pages/report-designer-demo.tsx`, `apps/playground/src/styles.css`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-selection.ts`, `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`
- Key decision: keep the visual/layout improvements inside the shared spreadsheet/report-designer workbench path and playground shell, following the existing Flux widget-renderer styling contract, instead of copying the standalone `web-excel` styling into a playground-only layer.
- Next step: finish verification, then iterate on richer inspector handling for row/column/sheet targets and broader spreadsheet canvas visual convergence if the updated selection/layout baseline proves stable.

### 2026-04-25

- Finished scoping the remaining `clientMode` question in Plan 138 by explicitly deferring `matchFunc`. The repo now has a narrow live baseline for `loadDataOnce` / `fetchOnFilter`, but not a general custom record-matching contract. Decision: do not add `matchFunc` until there is a concrete need for business-defined client-side matching beyond the current default local filtering behavior, because that would widen the filtering contract more than this plan should own.
- Link to doc or code path: `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`, `docs/components/crud/design.md`
- Key decision: keep the current client-mode closure boundary narrow. `loadDataOnce` / `fetchOnFilter` are in; `matchFunc` joins `syncLocation` and primitive query parsing as explicitly deferred work.
- Next step: move Plan 138 toward Phase 4 docs/playground sync and closure-audit preparation instead of reopening Phase 3 with a broader filtering contract.

### 2026-04-25

- Re-scoped the remaining Phase 3 query-sync work after reviewing whether `syncLocation` is actually needed right now. Decision: keep `syncLocation` and primitive query parsing explicitly deferred from Plan 138's live runtime scope. The feature is only worth landing once CRUD query state must participate in page-level navigation semantics such as refresh persistence, browser back/forward restoration, or shareable deep links. That routing/state-ownership slice is broader than the current CRUD-owned query baseline, so the plan and CRUD design doc now say so directly instead of leaving `syncLocation` as an ambiguous near-term TODO.
- Link to doc or code path: `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`, `docs/components/crud/design.md`
- Key decision: do not expand CRUD into a route/location owner without a concrete product need. Keep the current Plan 138 closure path focused on the landed request-owned, quick-edit, and `clientMode.loadDataOnce` baselines.
- Next step: decide whether Plan 138 should explicitly defer `matchFunc` as well and move toward Phase 4 closure prep, or whether there is still one more narrow client-side filtering slice worth landing before closure audit.

### 2026-04-25

- Landed the first `clientMode` slice in Plan 138 Phase 3. CRUD query submit/reset now stay local when `clientMode.loadDataOnce` is enabled, which matches the current request-owned/source-owned runtime shape where CRUD consumes upstream result objects but still owns local query filtering. When `clientMode.fetchOnFilter` is explicitly true, the same query controls opt back into upstream `onQuerySubmit` refresh actions. To make that work reliably for source-owned refresh, CRUD query events now preserve the owner scope and pass `${query}` through `evaluationBindings` instead of a synthetic child scope.
- Link to doc or code path: `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud-request-owned.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`
- Key decision: interpret `loadDataOnce` in terms of CRUD-owned query behavior instead of inventing a new request owner. In this runtime, the meaningful baseline is "query stays local unless fetchOnFilter opts back into upstream refresh", not "CRUD fetches once by itself".
- Next step: move to the remaining Phase 3 query-sync gap, `syncLocation` plus primitive query parsing, or explicitly defer `matchFunc` if it would require a broader filtering contract than this plan should own.

### 2026-04-25

- Extended Plan 138 Phase 3 quick-edit again with a minimal `quickEdit.mode: 'dialog'` baseline. The implementation deliberately stays local to the table-backed quick-edit cell: it opens a small dialog shell using the shared `@nop-chaos/ui` dialog primitives, renders the configured quick-edit body on the existing row scope, saves through the same `quickSaveItemAction` / `quickSaveAction` bridge, and restores the last saved row value when the dialog closes without saving. This keeps the slice narrow and avoids prematurely coupling quick-edit authoring to the managed `openDialog` surface-runtime path.
- Link to doc or code path: `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud-quick-edit.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`
- Key decision: land dialog quick-edit as a local shell first. Reusing the shared dialog UI component is enough for this slice; managed surface/runtime convergence is a separate concern and remains deferred.
- Next step: either continue Phase 3 on the query-sync side (`clientMode` / `syncLocation`) or revisit whether quick-edit needs deeper convergence with managed surfaces and richer batch editing.

### 2026-04-25

- Broadened the new Plan 138 Phase 3 quick-edit baseline without leaving the inline row-scope model. `quickEdit.body` can now render custom inline field content directly inside the table-backed CRUD cell, still using the existing row scope (`record`, `index`) and the same `quickSaveItemAction` / `quickSaveAction` save bridge. Added focused coverage proving a custom inline `input-text` body can edit `record.name` and save the updated row payload.
- Link to doc or code path: `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud-quick-edit.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`
- Key decision: broaden inline quick-edit before introducing a second editing surface. Custom inline bodies are still part of the same row-scope-local editing slice; `mode: 'dialog'`, `clientMode.loadDataOnce`, and `syncLocation` remain deferred.
- Next step: choose whether the next Plan 138 slice should add `quickEdit.mode: 'dialog'` or pivot to `clientMode` / `syncLocation` while the inline editing baseline remains stable.

### 2026-04-25

- Started Plan 138 Phase 3 with the smallest live `quickEdit` slice instead of trying to land AMIS-style editing parity all at once. CRUD now forwards `quickSaveAction` / `quickSaveItemAction` into the internal table schema, and table-backed CRUD columns with `quickEdit: true` or `quickEdit: { saveImmediately }` render inline scalar editors bound to `record.<column.name>` in the existing row scope. The first baseline supports explicit save buttons or blur-triggered save, with focused CRUD tests for both paths.
- Link to doc or code path: `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/schemas.ts`, `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud-quick-edit.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`
- Key decision: keep the first editing slice table-backed and row-scope-local. Reuse existing input renderers and action dispatch instead of inventing a separate quick-edit owner. `mode: 'dialog'`, custom `quickEdit.body`, `clientMode.loadDataOnce`, and `syncLocation` remain explicitly deferred.
- Next step: decide whether the next Plan 138 increment should broaden `quickEdit` to custom body / richer field types or switch to the query-sync side (`clientMode` / `syncLocation`) while this inline baseline remains narrow and stable.

### 2026-04-25

- Stabilized Plan 138's request-owned CRUD proof after tracing the hanging focused test to two real runtime contract bugs instead of a flaky harness. First, `crud` refresh was delegating `onRefresh` on a synthetic child scope, which breaks runtime-owned `refreshSource` lookup; it now preserves the owner scope like the existing table refresh path. Second, the source-registry invalidation matcher treated dependency-less sources as “refresh on any scope write”, so CRUD's own `$crud` / `$_crud.*` scope publication kept retriggering upstream `data-source` refreshes. That matcher now ignores unrelated scope changes when no dependency set is tracked, and the focused request-owned CRUD test now exits normally.
- Link to doc or code path: `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-runtime/src/scope-change.ts`, `packages/flux-runtime/src/__tests__/scope-change.test.ts`, `packages/flux-renderers-data/src/__tests__/data-crud-request-owned.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`
- Key decision: fix the shared runtime invalidation semantics instead of adding a CRUD-specific escape hatch. A source with no tracked dependencies should still refresh on start and explicit `refreshSource`, but not on every unrelated scope write.
- Next step: move Plan 138 into Phase 3 and define the first narrow `quickEdit` baseline, keeping `clientMode.loadDataOnce` and `syncLocation` explicitly deferred unless they become required by that slice.

### 2026-04-25

- Added "Design Direction: ApiSchema As Ajax Action Transport Contract" section to `docs/architecture/api-data-source.md`. Clarified that `ApiSchema` is NOT compile-time sugar — it remains the internal transport descriptor used by the `ajax` action. What changes is removing the independent execution paths (submitApiCall, async validation's direct api consumption, schema-level `api` properties) that bypass action dispatch.
- Updated `docs/architecture/frontend-programming-model.md`: rule #8, hard invariant #14, and the execution-model taxonomy table now correctly describe `ApiSchema` as the internal transport descriptor for the `ajax` action.
- Updated all examples in `api-data-source.md` from `"api": { url, method }` to `"action": "ajax", "args": { url, method }`.
- Created Plan 139 (`docs/plans/139-unified-action-dispatch-for-submit-validation-and-data-access.md`) to delete all `api` properties from schema and unify all remote calls through action dispatch. Four phases: (1) delete async validation `api` variant, (2) delete form submit `submitApiCall` bypass, (3) delete `DataSourceSchema.api` field, (4) doc/type cleanup. No compatibility layer needed — project has never been published.
- Link to doc or code path: `docs/architecture/api-data-source.md:189`, `docs/architecture/frontend-programming-model.md:88`, `docs/architecture/frontend-programming-model.md:129`, `docs/architecture/frontend-programming-model.md:350`, `docs/plans/139-unified-action-dispatch-for-submit-validation-and-data-access.md`
- Key decision: `api` property is completely deleted from schema. Users write `{ action: "ajax", args: { url, method, ... } }` directly. `ApiSchema` keeps its role as HTTP request descriptor type used internally by the `ajax` action, but is invisible to schema authors. No backward compatibility needed since this is the first release.
- Next step: begin Plan 139 Phase 1 — delete async validation `api` variant.

### 2026-04-25

- Extended Plan 138 Phase 2 from “preloaded source result object” to a real upstream request-owned proof. Added a focused CRUD test where an actual `data-source` publishes `{ items, total }` into scope, CRUD consumes it through `source`, and `component:refresh` on CRUD re-enters the upstream owner through `onRefresh: { action: 'refreshSource', targetId }`. This gives the successor plan a real request-owned baseline without turning CRUD itself into a request owner.
- Link to doc or code path: `packages/flux-renderers-data/src/__tests__/data-crud-request-owned.test.tsx`, `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`, `docs/components/crud/design.md`
- Key decision: treat upstream `data-source` cooperation as the canonical first live meaning of `api -> source` for CRUD. CRUD owns composition and summary publication, while request execution remains with the upstream source owner.
- Next step: choose between continuing request-owned semantics (`clientMode` / query-sync) or starting the first explicit `quickEdit` baseline.

### 2026-04-25

- Started Plan 138 and landed the first request-owned/source-owned CRUD baseline instead of jumping directly into `quickEdit`. `crud` no longer drops common upstream source result objects to empty when `source` resolves to shapes like `{ items, total }`; it now consumes those rows, keeps the upstream total in `$crud.total`, and still applies local query filtering to visible rows. Added focused runtime coverage plus a dedicated CRUD lab scenario for this first editing/runtime successor baseline.
- Link to doc or code path: `docs/plans/138-crud-editing-and-request-owned-runtime-successor-plan.md`, `docs/components/crud/design.md`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/crud-renderer-state.ts`, `packages/flux-renderers-data/src/__tests__/data-crud-source-runtime.test.tsx`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`
- Key decision: keep the first Plan 138 request-owned slice narrow. CRUD still does not become a request owner; it only accepts the common scope-published source result object shapes produced by upstream `data-source` / source workflows, which is enough to establish a real runtime baseline without inventing a new fetch protocol inside CRUD.
- Next step: verify the new source-result baseline, then decide whether the next Plan 138 slice should extend request-owned semantics further or move into the first explicit `quickEdit` baseline.

### 2026-04-25

- Closed Plan 137 after a final independent closure audit confirmed there is no remaining plan-owned gap in the live repo. The final audit verified both the standalone `table` baseline and the CRUD bridge baseline for `columnSettings`, responsive expand, and header search/filter behavior, and only required plan bookkeeping to mark completion.
- Link to doc or code path: `docs/plans/137-crud-table-heavy-parity-successor-plan.md`
- Key decision: closure required independent evidence from live code, tests, docs, and playground coverage, not just plan checklist updates. Final closure evidence is recorded in `task_id: ses_23dbc4ba1ffeeZwc0ZiCB90OlY`.
- Next step: begin successor work only under a new owner scope; do not reopen Plan 137 unless a new live regression appears.

### 2026-04-25

- Re-ran a fresh independent closure audit for Plan 137 after the CRUD responsive fix and found two smaller blockers: the CRUD lab was still using stale `title`-based column examples that did not match the live table contract, and CRUD still lacked a direct focused forwarding test for header search/filter behavior. Fixed the CRUD lab to use the live `label`-based contract and added a dedicated CRUD header-forwarding test file so the integration evidence no longer depends on inference from standalone table coverage.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud-header-forwarding.test.tsx`, `docs/plans/137-crud-table-heavy-parity-successor-plan.md`
- Key decision: keep using the audit as a hard gate. Even small integration mismatches in docs/playground or missing direct CRUD evidence count against closure when the plan explicitly owns CRUD/table-heavy parity, so they must be fixed before the final closure decision.
- Next step: run one more independent closure audit against the updated repo state and only close Plan 137 if that audit comes back clean.

### 2026-04-25

- Added `tabsMode` and `sidePosition` properties to `TabsSchema`, enabling amis-compatible tab display modes: default, line, vertical, sidebar (left/right). Updated `TabsRenderer` to resolve `tabsMode` into the correct `orientation` and `variant` for the underlying UI Tabs component. For sidebar mode with `sidePosition: 'right'`, the layout uses `flex-row-reverse`. Updated the playground tabs lab page with scenarios for each mode (default, line, vertical, sidebar-left, sidebar-right, form tabs).
- Link to code: `packages/flux-renderers-basic/src/tabs.tsx`, `packages/flux-renderers-basic/src/schemas.ts`, `apps/playground/src/component-lab/renderers/tabs-lab-page.tsx`
- Key decision: map `tabsMode` to `orientation` + `variant` at the renderer level so the UI primitive stays generic. Add marker classes (`nop-tabs--vertical`, `nop-tabs--sidebar`, `nop-tabs--sidebar-right`) for schema-driven styling.
- Next step: verify all modes render correctly in the playground.

### 2026-04-25

- Ran an independent closure audit for Plan 137 and found a real remaining plan-owned gap instead of a documentation-only mismatch: `crud-renderer` was not forwarding `responsive` into the internal `table` schema, so the responsive more-columns baseline was only landed for standalone `table`, not for CRUD integration. Fixed that wiring, added a CRUD-focused responsive test, and added a dedicated CRUD responsive playground scenario before re-running verification.
- Link to doc or code path: `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`, `docs/plans/137-crud-table-heavy-parity-successor-plan.md`
- Key decision: treat the independent audit as authoritative. Plan 137 cannot close on standalone `table` evidence alone because the plan explicitly owns CRUD/table-heavy parity; the CRUD bridge has to carry the landed responsive baseline too.
- Next step: run a fresh independent closure audit against the updated live repo state and only close the plan if that audit finds no remaining plan-owned gap.

### 2026-04-25

- Added the missing dedicated playground scenario for the first responsive more-columns baseline. The table lab now includes a focused `responsive.mode: 'expand'` example that keeps the primary column in the main row and moves secondary columns into the expandable detail row under the configured breakpoint, which makes the already-landed Phase 3 runtime behavior visible without relying only on focused tests.
- Link to doc or code path: `apps/playground/src/component-lab/renderers/table-lab-page.tsx`, `docs/plans/137-crud-table-heavy-parity-successor-plan.md`
- Key decision: keep the scenario narrow and demonstrative rather than mixing it with the header search/filter example; Phase 3 now has one playground scenario per major baseline so later closure audit can reference them independently.
- Next step: run the focused playground verification, then decide whether Plan 137 is ready for closure-audit preparation or still needs small final docs/checklist cleanup.

### 2026-04-25

- Continued Plan 137 Phase 3 by tightening the header search/filter baseline into a more stable, testable UX instead of adding broader parity features. Header menus now expose i18n-backed active-state trigger labels and a per-column clear action that resets both keyword and option filters. Also added a dedicated table playground scenario for header search/filter controls so this baseline is visible outside focused tests.
- Link to doc or code path: `docs/plans/137-crud-table-heavy-parity-successor-plan.md`, `docs/components/table/design.md`, `docs/components/crud/design.md`, `packages/flux-renderers-data/src/table-renderer/table-header-row.tsx`, `packages/flux-renderers-data/src/table-renderer/use-table-controls.ts`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-i18n/src/locales/en-US.ts`, `packages/flux-i18n/src/locales/zh-CN.ts`, `apps/playground/src/component-lab/renderers/table-lab-page.tsx`
- Key decision: keep the shipped slice narrow and composable. Phase 3 now treats “stable header-triggered keyword/filter controls with active-state feedback and clear action” as the landed baseline, while richer option-source/search UX and broader ownership convergence remain explicit follow-up work instead of being implied by the current menu.
- Next step: decide whether to finish Plan 137 by adding a dedicated responsive-expansion playground scenario plus any remaining doc/playground polish, or to open the closure-audit path once the remaining richer parity gaps are explicitly out of scope.

### 2026-04-25

- Started the first real Plan 137 Phase 3 implementation instead of keeping responsive expansion as schema-only text. `table` now treats `responsive.mode: 'expand'` as a live baseline: below the configured `breakpoint`, primary/fixed columns stay in the main row while secondary columns move into an expandable detail row that reuses the existing expand-row path. Also hardened `@nop-chaos/ui` `useIsMobile` so missing `window.matchMedia` no longer crashes jsdom-based tests.
- Link to doc or code path: `docs/plans/137-crud-table-heavy-parity-successor-plan.md`, `docs/components/table/design.md`, `docs/components/crud/design.md`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx`, `packages/flux-renderers-data/src/table-renderer/fixed-columns.ts`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/ui/src/hooks/use-mobile.ts`, `packages/ui/src/index.ts`
- Key decision: implement the first responsive more-columns baseline by reusing the existing expandable-row mechanism instead of inventing a second row-detail system. This keeps the shipped slice small and makes the new behavior repo-observable with one focused test, while leaving richer trigger/layout parity for the remaining Plan 137 work.
- Next step: continue Plan 137 Phase 3 on the remaining header search/filter stabilization work and decide whether the playground should add a dedicated responsive-expansion scenario now that the baseline is live.

### 2026-04-25

- Continued Plan 137 Phase 2 by aligning the remaining user-facing surfaces with the already-landed `columnSettings` baseline instead of widening runtime behavior prematurely. Updated the renderer contract metadata so `columnSettings` explicitly mentions inline-vs-overlay entry and keeps drag reorder marked deferred, then expanded the playground CRUD lab to show `columnSettings.overlay: false` plus the current basic header search/filter controls.
- Link to doc or code path: `docs/plans/137-crud-table-heavy-parity-successor-plan.md`, `packages/flux-renderers-data/src/index.tsx`, `apps/playground/src/component-lab/renderers/crud-lab-page.tsx`
- Key decision: keep Plan 137 Phase 2 honest by treating playground and contract metadata as part of the live baseline; if `draggable` is not implemented, every user-facing surface should say so rather than silently carrying the field name forward as if parity already existed.
- Next step: either keep tightening Phase 2 around column-settings menu-state details or move into Phase 3 and define the first real responsive more-columns expansion baseline.

### 2026-04-25

- Started executing Plan 137 instead of leaving the CRUD follow-up only as handoff text. Phase 1 is now closed with an explicit successor baseline: richer `columnSettings` in this plan means visibility toggles, ordered-column ownership, move-up/move-down ordering, and a real `overlay: false` inline panel; `draggable` remains deferred until drag behavior exists. Also landed the first Phase 2 runtime slice by making `columnSettings.overlay: false` render an inline control panel with focused table coverage.
- Link to doc or code path: `docs/plans/137-crud-table-heavy-parity-successor-plan.md`, `docs/components/crud/design.md`, `docs/components/table/design.md`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`
- Key decision: do not treat existing `expandable` support or minimal header search/filter plumbing as automatic closure evidence for CRUD/table responsive parity; only repo-observable more-columns expansion and richer header controls will count for the remaining Plan 137 slices.
- Next step: run focused verification for the new inline column-settings slice, then continue Phase 2 by deciding whether `draggable` should stay explicitly deferred or whether the next smallest runtime win is menu-state hardening or a playground example update.
