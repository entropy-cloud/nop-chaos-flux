# Development Log — 2026-04-17

### 2026-04-17

- Fixed validation system issue 8 from `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md`:
  - **Issue 8 (FieldFrame performance)**: Migrated `FieldFrame` from `useCurrentFormState` to `useCurrentFormFieldState` for O(1) field subscription.
  - Added graceful degradation in `useCurrentFormFieldState`: falls back to whole-store subscription when `subscribeToPath` is unavailable.
  - Added empty-path skip logic: when path is empty string, skips subscription entirely and returns empty state (fixes test compatibility).
- Link to code paths: `packages/flux-react/src/field-frame.tsx`, `packages/flux-react/src/hooks.ts`.
- Key decision: The performance optimization is now complete. When field A changes, field B's `FieldFrame` is no longer awakened (O(1) vs O(n)).
- Verification: `pnpm typecheck`, `pnpm --filter @nop-chaos/flux-react test`, `pnpm --filter @nop-chaos/flux-runtime test` all passed.
- Next step: All 9 issues from the validation consistency audit are now resolved (5 Phase 3-4 planned features, 4 code fixes).

### 2026-04-17

- Revalidated `docs/analysis/2026-04-17-deep-audit/10-styling-system-compliance.md` against the live codebase, fixed the real styling ownership issues, and rewrote the audit record so it distinguishes package-owned shell CSS from spreadsheet performance exceptions.
- Fixed `flux-renderers-form` styling ownership in `packages/flux-renderers-form/src/{renderers/input.tsx,index.tsx,form-renderers.css}` by moving renderer-internal structure markers to `data-slot` and moving default control styling out of `apps/playground/src/styles.css` into the package entry.
- Fixed `spreadsheet-renderers` package style entry drift in `packages/spreadsheet-renderers/src/{index.ts,styles.d.ts,canvas-styles.css,spreadsheet-grid.tsx,cell-style-map.ts}` so spreadsheet toolbar/editor/grid shell styles ship with the package, while keeping the documented hybrid `ss-* + inline style + data-*` performance strategy for cells.
- Reduced Flow Designer shell theme debt in `packages/flow-designer-renderers/src/{index.tsx,designer-page.tsx,designer-toolbar.tsx,designer-inspector.tsx,designer-palette.tsx,designer-xyflow-canvas/DesignerXyflowCanvas.tsx,designer-xyflow-canvas/DesignerXyflowEdge.tsx,designer-theme.css,styles.d.ts,designer-context.ts}` by introducing package-owned `.fd-theme-root` / `--fd-*` shell tokens; verification also exposed that the package needed a direct `use-sync-external-store` dependency when `designer-context.ts` started using the selector shim.
- Updated docs: `docs/architecture/styling-system.md`, `docs/architecture/report-designer/spreadsheet-canvas-css.md`, `docs/analysis/2026-04-17-deep-audit/10-styling-system-compliance.md`.
- Key decisions:
  - Spreadsheet/cell-heavy surfaces remain a documented performance exception; the defect was missing package wiring and stale doc baseline, not the mere existence of package-owned structural classes.
  - Root `nop-*` wrapper markers may stay when they are semantic shell markers, but renderer-internal substructure should prefer `data-slot`, and package-owned default CSS must live with the package instead of the playground.
  - Flow Designer shell visuals should consume `.fd-theme-root` / `--fd-*` tokens, but node semantic colors and DingFlow overlays are still partial follow-up debt rather than a reason to overstate the current finding.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form typecheck`, `build`, `lint`, `test`; `pnpm --filter @nop-chaos/spreadsheet-renderers typecheck`, `build`, `lint`, `test`; `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`, `build`, `lint` passed. `pnpm --filter @nop-chaos/flow-designer-renderers test` initially failed because the package lacked a direct `use-sync-external-store` dependency required by `designer-context.ts`; the package metadata is now corrected and needs a lockfile refresh/install before that test is rerun.
- Next step: if continuing dimension-10 cleanup, focus Flow Designer node semantic colors / DingFlow overlays and the remaining playground BEM sample schemas rather than reopening the spreadsheet hybrid-style exception.

### 2026-04-17

- Closed the verification fallout left after the dimension-03 host/contract landing: fixed `page.refresh()` so `PageStoreApi.refreshTick` no longer masquerades as page-scope data change, fixed `useHostScope()` host projection recreation so host-owner pages stop infinite `replace()` loops, and added a focused `flux-react` regression test covering renderer-registered API data sources.
- Also removed the remaining package/test execution blockers discovered during full verification: `flow-designer-renderers` no longer depends on runtime resolution of `use-sync-external-store/shim/with-selector`, `flux-core` root exports now include `StructuralLoopRenderContext`, and `flux-renderers-form-advanced` tests were rewired away from package subpath test-support imports that Vitest/Vite was not resolving reliably in-workspace.
- Link to code/doc paths: `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-react/src/{workbench/hooks.ts,hooks.ts,__tests__/schema-renderer-runtime-core.test.tsx}`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/flux-core/src/index.ts`, `packages/flux-renderers-form-advanced/src/__tests__/`, `docs/analysis/2026-04-17-deep-audit/03-api-surface-and-contracts.md`.
- Key decisions:
  - Page refresh ticks must stay outside scope-data invalidation, otherwise source/reaction pipelines observe false dependency changes.
  - `useHostScope()` should preserve host projection identity unless the parent scope boundary actually changes; comparing against the derived scope id is sufficient and avoids layout-effect rerender loops.
  - For tests, prefer direct relative imports over package subpath test-support imports when the subpath is intentionally non-public and Vite workspace aliasing is not guaranteed to resolve it.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint` passed; `pnpm -r --workspace-concurrency=1 test` passed after workspace-parallel Vitest worker forks proved noisy on this machine.
- Next step: if desired, separately tighten the remaining `flux-react` / `flux-runtime` / `report-designer-renderers` public surfaces that are still documented as dimension-03 follow-up items.

### 2026-04-17

- Recalibrated the remaining dimension-09 renderer-contract findings after re-reading `docs/architecture/variant-field.md` and the tree component design. `variant-field` local selector/detection state is now classified as a valid inline live-edit owner choice rather than a defect, matching the earlier tree reclassification around dynamic indentation and local expand/collapse state.
- Updated `docs/skills/deep-audit-prompts.md` so future deep-audit agents stop mechanically treating renderer-local `style`, local `useState`, or renderer-internal UI classes as violations. The prompt now explicitly distinguishes true second-source state / hidden contract drift from legitimate UI-owner implementation details.
- Link to code/doc paths: `docs/analysis/2026-04-17-deep-audit/09-renderer-contracts.md`, `docs/skills/deep-audit-prompts.md`, `docs/architecture/{variant-field.md,renderer-runtime.md,styling-system.md}`, `docs/components/tree/design.md`.
- Key decisions:
  - `variant-field` is an inline live-edit union-value control, not a staged owner/runtime boundary; `userSelectedKey` / `detectedKey` are legitimate transient UI state, not a second field-value source.
  - Renderer-contract review must distinguish pure structural renderers from renderer-owned UI shells; dynamic style and local interaction state are sometimes the optimal implementation.
- Verification: documentation-only updates after architecture/code revalidation; no additional code changes were required in this slice.
- Next step: if the deep audit is rerun, use the revised prompt baseline and re-check whether any other dimensions still carry over-mechanical findings derived from the old wording.

### 2026-04-17

- Continued the dimension-09 renderer-contract follow-up: removed the `loop` / `recurse` package-private ambient contract by promoting structural recursion context into standard React runtime APIs (`StructuralLoopContext` / `useStructuralLoopContext`) in `@nop-chaos/flux-react`, with shared public types (`StructuralLoopBindings` / `StructuralLoopRenderContext`) in `@nop-chaos/flux-core`.
- Also fixed a real React 19 external-store edge in `packages/flux-react/src/{hooks.ts,field-frame.tsx}` by letting `useCurrentFormState()` skip subscription when a caller has no meaningful form-path dependency; this eliminates the `FieldFrame` snapshot loop exposed by the root-path/no-name test stub.
- Link to code/doc paths: `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-react/src/{contexts.ts,hooks.ts,index.tsx,field-frame.tsx}`, `packages/flux-renderers-basic/src/{loop.tsx,recurse.tsx,structural-loop.tsx}`, `docs/analysis/2026-04-17-deep-audit/09-renderer-contracts.md`.
- Key decisions:
  - `loop` / `recurse` still need an ambient recursion carrier today, but that carrier should be a standard runtime capability owned by `flux-react`, not a renderer-package-private contract.
  - For `useSyncExternalStore`, "no meaningful dependency" should short-circuit the subscription path instead of relying on unstable stub snapshots.
- Verification: `pnpm --filter @nop-chaos/flux-core typecheck`, `build`, `lint`; `pnpm --filter @nop-chaos/flux-react typecheck`, `build`, `lint`, `test`; `pnpm --filter @nop-chaos/flux-renderers-basic typecheck`, `build`, `lint`, `test` all passed.
- Next step: revisit `tree-renderer` and split `table-renderer/*` styling-contract debt, and separately decide whether `variant-field` active variant should remain renderer-local or become owner/runtime state.

### 2026-04-17

- Fixed validation system consistency issues 4 and 5 from `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md`:
  - **Issue 4 (summary-gate)**: Extended `ChildValidationContractRegistration` interface with `getState(): ChildValidationScopeState`. `computeCanSubmit()` now checks child scope's `ready`, `validating`, and `valid` state instead of just `active`.
  - **Issue 5 (recurse-submit)**: Extended `ChildValidationContractRegistration` interface with `triggerValidation(): Promise<ValidationResult>`. `executeFormSubmit()` now calls `triggerValidation()` on all active `recurse-submit` contracts in parallel and aborts submission if child validation fails.
- **Issue 8 (FieldFrame performance)**: Deferred. Migration to `useCurrentFormFieldState` requires test mock updates to support `subscribeToPath()` method. Infrastructure is ready but risk-benefit ratio suggests doing this opportunistically.
- Link to code paths: `packages/flux-core/src/types/validation.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-runtime-submit-flow.ts`, `packages/flux-runtime/src/__tests__/owner-based-validation-contracts.test.ts`, `packages/flux-runtime/src/__tests__/plan-68-69-remaining-behaviors.test.ts`.
- Key decision: Issues 4 and 5 were real functional bugs in parent-child validation coordination. Issue 8 is a performance optimization that can wait.
- Verification: `pnpm typecheck`, `pnpm test`, `pnpm lint` all passed.
- Next step: Consider implementing `ChildValidationContractRegistration` in actual renderers that create child forms (e.g., `detail-field`, `detail-view`) to leverage the new interface.

### 2026-04-17

- Revalidated `docs/analysis/2026-04-17-deep-audit/09-renderer-contracts.md` against the live codebase, fixed the real renderer-contract defects that were still present, and rewrote the audit record so it distinguishes real defects from stale or now-reclassified findings.
- Fixed reactive/render-contract issues in `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-form-advanced/src/{tag-list.tsx,tree-controls.tsx,array-editor.tsx,key-value.tsx,condition-builder/ConditionBuilder.tsx}`.
- Key decisions:
  - `page` and `tag-list` must consume reactive field/scope data through subscription hooks instead of imperative render-time reads.
  - `tree-select` clear affordance must use a real `@nop-chaos/ui` button, not a pseudo-button span.
  - `array-editor`, `key-value`, and `condition-builder` should treat form/scope store values as the single source of truth; local refs are retained only for registration/validation callbacks.
  - `detail-field` / `detail-view` are no longer tracked as renderer-contract defects because `docs/architecture/form-validation.md` now explicitly treats renderer-level draft isolation as the current Phase 2 baseline.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-basic typecheck`, `build`, `lint`, `test`; `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `build`, `lint` all passed. `pnpm --filter @nop-chaos/flux-renderers-form-advanced test` still fails due to a pre-existing unresolved test-support import (`@nop-chaos/flux-renderers-form/test-support`) in advanced renderer tests, unrelated to this change set.
- Link to code/doc paths: `docs/analysis/2026-04-17-deep-audit/09-renderer-contracts.md`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-form-advanced/src/{tag-list.tsx,tree-controls.tsx,array-editor.tsx,key-value.tsx,condition-builder/ConditionBuilder.tsx}`.
- Next step: separately re-audit `loop` / `recurse` structural context usage and the split `table-renderer/*` styling contract at the subcomponent level.

### 2026-04-17

- Rechecked the source-artifact leak path after finding stale `flux-react` outputs and confirmed the repo already had a root guard (`scripts/verify-no-src-artifacts.mjs` / `scripts/clean-src-artifacts.mjs`), but it only scanned `packages/*/src`. Expanded both scripts to cover `apps/*/src` too, updated `docs/architecture/frontend-baseline.md`, and then cleaned a broader historical leak set across `flux-core`, `flux-formula`, `flux-runtime`, `ui`, `word-editor-core`, and `flux-react`.
- Verification: `node scripts/clean-src-artifacts.mjs` removed 377 generated files and both `node scripts/verify-no-src-artifacts.mjs` and `pnpm check:src-artifacts` now pass.
- Link to code/doc paths: `scripts/{verify-no-src-artifacts.mjs,clean-src-artifacts.mjs}`, `docs/architecture/frontend-baseline.md`.
- Key decision: the right fix was not a new parallel guard but widening the existing workspace invariant so `apps/*/src` and `packages/*/src` are both treated as source-only.
- Next step: prefer root `pnpm lint` / `pnpm check` when doing workspace validation so front-door guardrails run before package-local commands.

### 2026-04-17

- Continued `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md` review with architectural decision about owner boundary design.
- **Key decision**: Owner boundary automatic partitioning (`create-owner` / `inherit-owner` / `no-owner`) is reclassified as **Phase 3 future direction**, not current implementation requirement. Rationale:
  1. Core draft isolation need is already satisfied by renderer-level `FormRuntime` creation (`detail-field` / `detail-view`)
  2. No actual "nested draft in nested draft" scenarios exist in the project that would require multi-level owner coordination
  3. Current approach is simpler and working — renderers manage their own draft lifecycle
- **Architecture doc updates** (`docs/architecture/form-validation.md`):
  - "Owner Resolution Algorithm" now marked with Implementation Status noting it's Phase 3 target
  - "Parent And Child Scope Interaction" now marked with Implementation Status noting current renderer-level implementation
  - Added "Current Implementation: Renderer-Level Draft Isolation" section explaining how `detail-field` / `detail-view` achieve isolation
  - "Implementation Phases" section updated with status labels (Phase 1: Complete, Phase 2: Current, Phase 3-4: Future)
- Link to doc paths: `docs/architecture/form-validation.md`, `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md`.
- Next step: Continue using renderer-level draft isolation; revisit compiler-level owner partitioning only if genuine multi-level owner coordination scenarios emerge.

### 2026-04-17

- Cleared the follow-up verification blockers left after the async-cancellation fixes: rewrote `packages/flux-react/src/workbench/hooks.ts` so `useHostScope()` keeps `ScopeRef` identity stable without violating React Compiler / hooks lint rules, wired `useDesignerSnapshotSelector()` to selector equality in `packages/flow-designer-renderers/src/designer-context.ts`, restored `@hufe921/canvas-editor` build-time typing in `packages/word-editor-renderers/tsconfig.build.json`, and removed leaked `packages/flux-react/src/*.js` / `*.d.ts` build artifacts that had been polluting lint.
- Also aligned workspace metadata for `@nop-chaos/word-editor-renderers` by declaring its `flux-core` / `flux-runtime` / `flux-formula` dependencies and refreshing workspace links with `pnpm install`.
- Link to code paths: `packages/flux-react/src/workbench/hooks.ts`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/word-editor-renderers/{package.json,tsconfig.build.json,src/WordEditorPage.tsx}`, `packages/flux-react/src/` leaked build artifacts.
- Verification: `pnpm --filter @nop-chaos/flux-react lint`, `pnpm --filter @nop-chaos/flux-react build`, `pnpm --filter @nop-chaos/flow-designer-renderers lint`, `pnpm --filter @nop-chaos/flow-designer-renderers build`, `pnpm --filter @nop-chaos/word-editor-renderers build`, `pnpm --filter @nop-chaos/word-editor-renderers lint` all passed.
- Key decision: treat leaked build artifacts and package-local build config drift as real verification debt worth fixing immediately once touched-package validation depends on them.
- Next step: rerun broader workspace verification only when you want the remaining untouched packages rechecked as a full batch.

### 2026-04-17

- Reviewed `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md` findings and recalibrated the audit results.
- **Classification update**: 7 of 9 findings are architecture-vs-implementation phase gaps (Phase 3-4 planned features), not code defects. 1 finding is a performance optimization opportunity with infrastructure already in place. 1 finding was a genuine code inconsistency.
- **Fixed**: `selectCurrentFormFieldPresentation()` in `packages/flux-react/src/form-state.ts` now reads compiled `showErrorOn` behavior from field/form validation config instead of hardcoding `touched || dirty || visited || submitting`. This aligns `useFieldPresentation()` with `FieldFrame` behavior.
- Link to code paths: `packages/flux-react/src/form-state.ts`.
- Link to doc paths: `docs/analysis/2026-04-17-deep-audit/08-validation-consistency.md`.
- Key decision: recognize that Phase 3 multi-owner support (draft scopes, owner boundaries, child contracts) is planned architecture, not missing code; current single-owner implementation is the correct Phase 2 baseline.
- Verification: `pnpm --filter @nop-chaos/flux-react typecheck`, `pnpm --filter @nop-chaos/flux-react build`, `pnpm --filter @nop-chaos/flux-react test` all passed.
- Next step: remaining performance optimization (migrating `FieldFrame` to `useCurrentFormFieldState`) can be done opportunistically when addressing form rendering performance.

### 2026-04-17

- Re-verified and fixed the real async-cancellation issues from `docs/analysis/2026-04-17-deep-audit/06-async-cancellation-safety.md`: DataSource refresh supersede, `submitForm` timeout/cancel signal propagation, root `xui:imports` preload cleanup/release, Flow Designer auto-layout stale-result guards, and word-preview word-count stale guard.
- Updated the async audit record and corrected the `xui:imports` lifecycle baseline in `docs/architecture/action-scope-and-imports.md`; imported namespace release is now documented as real boundary teardown rather than a no-op.
- Link to code/doc paths: `packages/flux-runtime/src/{data-source-runtime.ts,form-runtime-submit-flow.ts,form-runtime.ts,runtime-factory.ts,imports.ts,action-runtime.ts}`, `packages/flux-react/src/{schema-renderer.tsx,helpers.tsx}`, `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flow-designer-renderers/src/{designer-page.tsx,designer-toolbar.tsx,auto-layout-guards.test.tsx}`, `packages/word-editor-renderers/src/{preview/DocPreviewPage.tsx,__tests__/doc-preview-page.test.tsx}`, `docs/analysis/2026-04-17-deep-audit/06-async-cancellation-safety.md`, `docs/architecture/action-scope-and-imports.md`.
- Verification: focused tests passed for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, `@nop-chaos/flow-designer-renderers`, and `@nop-chaos/word-editor-renderers`; package `typecheck` and most touched-package `build` passed, but repo-local verification still has unrelated blockers in `packages/flux-react/src/workbench/hooks.ts` (existing React Compiler lint rule), `packages/flow-designer-renderers/src/designer-context.ts` (existing unused parameter lint), and `packages/word-editor-renderers/tsconfig.build.json`/package build ownership (`rootDir` vs workspace-source imports).
- Key decision: keep the async fixes minimal and behavior-oriented, but also correct the stale import-lifecycle doc baseline so future audits do not treat the old no-op release behavior as intentional.
- Next step: if needed, address the unrelated package verification debt separately from this async-safety slice.

### 2026-04-17

- Landed the main dimension-03 contract fixes: `flux-renderers-form` test support moved off the root barrel onto `@nop-chaos/flux-renderers-form/test-support`, `spreadsheet-page` / `report-designer-page` / `word-editor-page` now declare host contracts, schema diagnostics now derive host manifest validation from renderer definitions with `xui:version` support, and `flux-renderers-basic` no longer exports its internal layout helpers from the root barrel.
- Converted `word-editor-renderers` from a plain page export surface into a real host-owner renderer package: added `registerWordEditorRenderers()`, `word-editor-page` schema/manifest/action-provider wiring, migrated the playground page onto `SchemaRenderer`, and implemented persisted `word-editor:insertChart` / `word-editor:insertCode` placeholder insertion through self-closing template tags plus saved `charts` / `codes` document metadata.
- Updated API-surface and owner docs to match the landed behavior in `docs/analysis/2026-04-17-deep-audit/03-api-surface-and-contracts.md`, `docs/components/word-editor-page/design.md`, `docs/architecture/word-editor/design.md`, and corrected the stale `PageStoreApi` wording in `docs/architecture/flux-core.md`.
- Link to code paths: `packages/flux-runtime/src/schema-compiler/shape-validation.ts`, `packages/flux-runtime/src/schema-compiler/host-action-validation.ts`, `packages/flux-runtime/src/schema-compiler-diagnostics.test.ts`, `packages/word-editor-core/src/{canvas-editor-bridge.ts,document-io.ts,template-model.ts}`, `packages/word-editor-renderers/src/{renderers.tsx,types.ts,word-editor-manifest.ts,word-editor-action-provider.ts,WordEditorPage.tsx}`, `apps/playground/src/pages/WordEditorPage.tsx`, `packages/flux-renderers-basic/src/index.tsx`.
- Key decision: follow the docs for `word-editor-page` and make the chart/code actions real instead of shrinking the contract; keep `report-designer-renderers` root helper tightening for a later pass because this slice did not show a safe external-consumer-free cut.
- Next step: run full workspace verification (`pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`) and then revisit the remaining `flux-react` / `flux-runtime` / `PageStoreApi` public-surface cleanup items that still need narrower references or additional doc work.

- Reviewed and recalibrated `docs/analysis/2026-04-17-deep-audit/07-lifecycle-effect-ownership.md` findings after code verification.
- **P1 匿名 source 生命周期** → Reclassified as "设计合理". The `node-source-prop-controller.ts` is a framework-agnostic controller with its own subscribe/getSnapshot pattern; React only bridges via `useSyncExternalStore`. AbortController management is internal to the controller, not React lifecycle. Moving this to runtime would require Node instance tracking with unclear benefit.
- **P2 DynamicRenderer** → Downgraded to P3. DynamicRenderer's direct `executeApiObject()` call is acceptable for its limited schema-loading use case; schema loads are typically one-shot and don't need cache/dedup/retry.
- **P2 FormRenderer statusPath** → Downgraded to P3. The effect-based status summary publishing works correctly; moving it to runtime is a nice-to-have improvement, not a defect.
- Fixed line number references throughout the document (form.tsx:254-316, dynamic-renderer.tsx:17-80, designer-page.tsx:339-345, use-table-row-scope-cache.ts:64-98, schema-renderer.tsx:50-65).
- Link to doc path: `docs/analysis/2026-04-17-deep-audit/07-lifecycle-effect-ownership.md`.
- Key decision: recognize that Node-level transient source lifecycle is naturally tied to component mount; the controller pattern already achieves state management decoupling from React without requiring full runtime-layer tracking.
- Next step: apply similar calibration rigor to remaining audit dimensions.

### 2026-04-17

- Fixed subscription precision issues from `docs/analysis/2026-04-17-deep-audit/05-subscription-precision.md`:
  1. **useHostScope ScopeRef rebuild**: Removed `scopeData` from `useMemo` dependency array in `packages/flux-react/src/workbench/hooks.ts` so `ScopeRef` identity remains stable; projection updates now happen via `scope.replace()` in `useLayoutEffect`.
  2. **DesignerContext snapshot broadcast**: Split `DesignerContextValue` in `packages/flow-designer-renderers/src/designer-context.ts` to exclude `snapshot`; added `useDesignerFullSnapshot()` and `useDesignerSnapshotSelector()` hooks for fine-grained subscriptions.
  3. **useOwnedAxisValue full-scope subscription**: Modified `packages/flux-renderers-basic/src/interaction-owner.ts` to only subscribe to specific path via `getIn(scopeData, statePath)` when `ownership === 'scope'`, avoiding full-scope subscription in `local`/`controlled` modes.
- Verified finding 1 (FieldFrame) was already optimized via equality functions.
- Link to code paths: `packages/flux-react/src/workbench/hooks.ts:16-37`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/flow-designer-renderers/src/designer-*.tsx`, `packages/flux-renderers-basic/src/interaction-owner.ts`.
- Key decision: optimize subscription precision at the hook level rather than restructuring store internals.
- Next step: run full workspace verification; remaining audit findings can now reference these fixes as baseline patterns.

### 2026-04-17

- Landed the follow-up renderer splits requested after the dimension-02 calibration: `packages/flux-renderers-data/src/table-renderer.tsx` is now a thin orchestrator over `TableHeaderRow`, `TableBodyRows`, `TablePaginationBar`, and `TableLoadingOverlay`, and `packages/flux-code-editor/src/code-editor-renderer.tsx` now delegates value binding, SQL execution state, toolbar, and body composition into `packages/flux-code-editor/src/code-editor-renderer/`.
- Updated the audit records to reflect the landed state in `docs/analysis/2026-04-17-deep-audit/02-module-boundaries.md` and `docs/analysis/2026-04-17-deep-audit/index.md`.
- Link to code paths: `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/*.tsx`, `packages/flux-code-editor/src/code-editor-renderer.tsx`, `packages/flux-code-editor/src/code-editor-renderer/*`.
- Key decision: keep both renderers as thin orchestrators and only extract the stable seams already implied by the existing package structure, instead of forcing a larger redesign.
- Next step: rerun workspace verification and keep the remaining audit backlog focused on behavior/contract issues rather than file size alone.

### 2026-04-17

- Recalibrated the deep-audit guidance for API-surface reviews so agents treat `nop-chaos-flux` as an evolving in-progress repo instead of assuming every package is already fully converged.
- Link to doc paths: `docs/skills/deep-audit-prompts.md`, `docs/analysis/2026-04-17-deep-audit/03-api-surface-and-contracts.md`.
- Key decision: do not classify unconnected modules, draft host contracts, or transitional test-support exports as full defects unless current code and owner docs already present them as stable public contracts.
- Next step: use the calibrated API-surface guidance when re-reviewing dimension-03 findings so retained issues focus on real contract drift and misleading public surface, not merely unfinished convergence work.

### 2026-04-17

- Fixed the two dimension-02 findings I consider mandatory after calibration: `packages/flux-runtime/src/index.ts` is now a thin assembly/export entry that delegates runtime creation to `packages/flux-runtime/src/runtime-factory.ts`, and formula-source execution/result-mapping logic moved out of `packages/flux-runtime/src/source-registry.ts` into `packages/flux-runtime/src/data-source-runtime.ts` via `createFormulaDataSourceController(...)`.
- Link to code paths: `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/runtime-factory.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/data-source-runtime.ts`.
- Key decision: treat `index.ts` as a real assembly boundary again and keep `source-registry.ts` focused on registration/refresh/debug ownership instead of letting source execution logic drift there.
- Next step: rerun workspace verification so the boundary refactor is validated across the full monorepo, not just `@nop-chaos/flux-runtime`.

### 2026-04-17

- Cleared the unrelated workspace lint blockers in `@nop-chaos/flux-renderers-data` that were preventing verification after the dependency-boundary fixes.
- Link to code paths: `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer/use-table-row-scope-cache.ts`.
- Key decision: remove the dead render-time virtualization/ref-read path from `table-renderer.tsx` and keep the row-scope cache as a tiny external store backed by a mutable state object, which satisfies React lint rules without a larger table refactor.
- Next step: rerun workspace lint and tests so the earlier package-metadata fixes and these follow-up lint fixes are fully verified together.

### 2026-04-17

- Fixed the real dependency-boundary findings from `docs/analysis/2026-04-17-deep-audit/01-dependency-boundaries.md`: promoted `@nop-chaos/flux-formula` to a runtime dependency of `@nop-chaos/flux-code-editor`, moved `@nop-chaos/tailwind-preset` onto the workspace-standard `dist` export/build pattern, and added a proper root JS/types export for `@nop-chaos/theme-tokens` while keeping its CSS subpath export.
- Link to code paths: `packages/flux-code-editor/package.json`, `packages/tailwind-preset/package.json`, `packages/tailwind-preset/tsconfig.build.json`, `packages/theme-tokens/package.json`.
- Key decision: treat `tailwind-preset` and `theme-tokens` as normal workspace packages with explicit root exports instead of leaving them as undocumented metadata exceptions.
- Next step: verify the workspace with `pnpm typecheck`, `pnpm build`, and `pnpm lint`; run tests if the package changes affect testable package entry behavior.

### 2026-04-17

- Updated `docs/skills/deep-audit-prompts.md` to require a mandatory second-pass audit workflow: each dimension now needs an initial review agent, a separate dimension-level review agent, and separate per-finding review agents before any result can enter the final summary.
- Link to doc path: `docs/skills/deep-audit-prompts.md`.
- Key decision: deep-audit outputs are now treated as candidate findings until they pass independent re-review at both the dimension level and the individual finding level.
- Next step: when running future deep audits, the coordinator agent should record both initial findings and calibrated review outcomes instead of directly aggregating first-pass results.

### 2026-04-17

- Revised the original deep-audit result files so the archive itself no longer treats public `renderers -> flux-core/flux-formula/flux-runtime` dependencies as defects, no longer flags `report-designer-renderers -> spreadsheet-renderers` shared reuse as a boundary problem, and no longer classifies spreadsheet/native platform controls as UI violations.
- Updated audit result paths: `docs/analysis/2026-04-17-deep-audit/01-dependency-boundaries.md`, `docs/analysis/2026-04-17-deep-audit/11-ui-component-usage.md`, `docs/analysis/2026-04-17-deep-audit/18-cross-package-pattern-consistency.md`, `docs/analysis/2026-04-17-deep-audit/index.md`.
- Key decision: keep the audit archive aligned with the calibrated review so future follow-up work is not driven by stale false positives.
- Next step: use the revised archive plus `docs/analysis/2026-04-17-deep-audit-meta-review.md` as the baseline if we turn retained findings into implementation work.

### 2026-04-17

- Updated `docs/skills/deep-audit-prompts.md` to stop treating public `renderers -> flux-core/flux-formula/flux-runtime` dependencies as boundary violations by default, to explicitly allow native HTML exceptions for platform controls and spreadsheet-like high-performance surfaces, and to recognize `report-designer-renderers -> spreadsheet-renderers` as valid shared-package reuse.
- Updated `docs/analysis/2026-04-17-deep-audit-meta-review.md` to align with the clarified audit stance and remove the remaining implication that these dependencies are backlog items by default.
- Link to doc paths: `docs/skills/deep-audit-prompts.md`, `docs/analysis/2026-04-17-deep-audit-meta-review.md`.
- Key decision: future audits should focus on private-path leakage, cycles, manifest correctness, real contract drift, and real maintenance cost, not idealized dependency ladders or mechanical native-element bans.
- Next step: keep using the deep-audit archive as a discovery set, but only convert findings with concrete bug risk, doc-code mismatch, or ownership conflict into remediation work.

### 2026-04-17

- Added a calibrated follow-up review in `docs/analysis/2026-04-17-deep-audit-meta-review.md` to separate real engineering issues from architecture-purity overreach in the earlier multi-agent audit.
- Link to doc paths: `docs/analysis/2026-04-17-deep-audit-meta-review.md`, `docs/analysis/2026-04-17-deep-audit/index.md`, `docs/architecture/field-frame.md`, `docs/components/word-editor-page/design.md`.
- Key decision: keep findings that show concrete boundary drift, wrapper-contract conflicts, manifest correctness issues, or doc-code mismatch; downgrade or reject findings that mainly push uniformity or forced abstraction.
- Next step: convert only the retained high-signal items into an actionable remediation backlog, starting with `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/source-registry.ts`, and the `wrap: true` field-chrome conflicts in advanced form renderers.

### 2026-04-17

- Added a multi-agent deep audit archive under `docs/analysis/2026-04-17-deep-audit/`, with one result file per audit dimension plus a summary index.
- Link to doc paths: `docs/analysis/2026-04-17-deep-audit/index.md`, `docs/analysis/2026-04-17-deep-audit/01-dependency-boundaries.md` through `18-cross-package-pattern-consistency.md`.
- Key decision: store the deep audit as a dedicated analysis directory rather than a single report file so each subagent output remains independently traceable.
- Next step: use the summary P1 list as the remediation backlog baseline and convert the most mechanical findings into automation where possible.

### 2026-04-17

- Recorded the cross-package regression in `docs/bugs/32-react19-external-store-derived-snapshot-loop-fix.md` because the visible failure lived in `packages/report-designer-renderers` while the real root cause was an unstable external-store snapshot returned by `packages/spreadsheet-renderers/src/bridge.ts`.
- Fixed a React 19 external-store snapshot loop by caching derived bridge snapshots in `packages/spreadsheet-renderers/src/bridge.ts` so repeated `bridge.getSnapshot()` calls stay referentially stable until the spreadsheet core snapshot changes.
- Added a regression test in `packages/spreadsheet-renderers/src/bridge.test.ts` that asserts stable snapshot identity before a dispatch and a fresh snapshot after the underlying core updates.
- Verification: `pnpm --filter @nop-chaos/spreadsheet-renderers test`, `pnpm --filter @nop-chaos/report-designer-renderers test`, `pnpm typecheck`, `pnpm build`, `pnpm test`.
- Key decision: fix the instability at the spreadsheet bridge boundary instead of adding guards in `report-designer-renderers`, because `useSyncExternalStore` requires the bridge snapshot itself to be stable.
- Next step: address the unrelated workspace lint failures in `packages/flux-renderers-data/src/table-renderer.tsx` and `packages/flux-renderers-data/src/table-renderer/use-table-row-scope-cache.ts`.
