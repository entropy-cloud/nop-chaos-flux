# Development Log - 2026-09-13

### 2026-09-13

- **三分支审查 + 自动修复**（ui-review 任务）：审查 `feat-threejs-integration` / `add-web-print` / `feat-echarts-integration` 及其在 master 的落点，产出分析报告 `docs/analysis/2026-09-13-threejs-webprint-echarts-branch-review.md` 并修复报告中的 P1 问题。
- 拓扑结论：print 与 echarts(chart/recharts) 的代码主体早已进入 master；threejs 全仓零代码（仅 roadmap/design）。`add-web-print` 分支仅剩 1 个冗余提交（master 已含等价 i18n 键与路由断言），echarts/threejs 分支增量均为纯文档。
- 修复（print）：新增 `packages/flux-print-renderers/src/print-designer.css` —— 此前选中框/8 向手柄/旋转手柄/标尺/区域参考线在全仓无任何 CSS（实机确认不可见，标尺以 16px 文本流堆叠）；包 `sideEffects`/`exports`/`build` 接线仿 `flux-renderers-form`。纸张预设下拉从空操作改为真实应用预设宽高（按 direction 换算）；方向切换交换宽高；`render-html.ts` 的 `@page` 不再交换宽高（与 `.fmt-page`/PDF 三处几何同源，"width/height 即字面页面尺寸"）；`background` 简写顺序修正（网格不再被页面背景清掉）；预览警告色补 dark 变体；调色板项 Button 化；设计态元素应用模板字体样式。
- 修复（chart）：legend 标签空白 —— `chartConfig` 改为以 `dataRegionKey`（recharts legend payload 的 dataKey）为主键并保留 name 别名，`ui/chart.tsx` `ChartLegendContent` 增加 `item.value ?? item.dataKey` 回退链（实机确认修复，m4-data/chart-lab legend 显示 Revenue/Expenses）；首次异步加载先显示 loading 而非"暂无数据"（保持 canvas 事件契约）；heatmap 增加坐标轴文字标签 + 每格原生 title tooltip + aria i18n；`'Value'`/`References: `/heatmap aria 改走新 `flux.chart.*` 键（两 locale）；chart-lab 补 heatmap 演示场景。
- 顺手落地：`tests/e2e/playground-entry-pages.spec.ts` 的 `print-designer` ROUTE_ASSERTIONS（此前为工作区未提交修复，HEAD 上路由清单测试红）。
- 验证：typecheck/build/lint/check 全绿（check 的 2 个 error 为 locale 文件既有豁免）；单测 9954 passed / 1112 文件；e2e 全量 1461 passed / 4 failed / 43 skipped —— 4 个失败（c3-1 picker-host、dialog-real-schema、w4c picker ×2）经 clean-HEAD stash 复跑证实为 master 既有基线红（与 09-07 add-web-print 分支记录的 picker/dialog 家族失败一致），与本次 delta 零关系；print-designer 8/8、m4-data、chart 相关 spec 全过；路由清单测试 `domain route coverage matches playground route inventory` 转绿。UI 复验探针（系统 Chrome）程序化断言选中框/手柄/标尺/legend/heatmap 全部修复。
- 环境注意：本机 Playwright chromium headless shell 缺失，已 `npx playwright install chromium`（92 MiB，本地缓存，不涉仓库变更）。
- 遗留（报告 §5 已登记）：旋转元素 AABB 排版、autoGrow 尾片高度、printDate 跨页一致性、PDF JPEG→PNG、吸附辅助线渲染、chart-renderer 拆分（621 行）、yAxisId 收敛、echarts mission M1 排期重排、threejs roadmap 文档合入。

---

以下为 `feat-threejs-integration` 分支同期日志（plan 463–468 收口，随分支合并带入）。

## Three.js 集成 I4.1 AI 场景生成（Plan 468，roadmap I4.1）

- Plan：`docs/plans/468-threejs-i4-ai-generation-plan.md`（draft review 2 轮升 active；必须自动化档）。
- 交付：`threejs-schema.json`（draft-07 资产，覆盖 v5 形状含 primitive/groundColor/transform，**不硬编码 url XOR oneOf**——双源容忍口径入 description）；`SchemaValidator`（手写结构+语义双层为校验来源：model-id-duplicate/binding-target-missing/camera-position-invalid/model-rotation-invalid 确定性错误码 + JSON Pointer path 一次聚合；判别口径与 live renderer 一致——仅拒双空、双源容忍 primitive 优先）；`AISchemaGenerator`（确定性模板：slug 派生+纯中文回退 `model-<n>`、dataPoints 归属 models[0]、无模型不生成 bindings、kind 命中图元六类走 primitive/未知走 url 回退；`generateFromPrompt`：LlmProvider 注入 + 围栏剥离 + errors 回喂修正回路 ≤ 2 轮 + 轮次耗尽 reject 聚合错误）。
- v5 漂移回写：design-ai-generation.md §3 校验机制声明 + 判别口径；§4 primitive/url 回退与无模型不生成 bindings。
- Closure 文档义务（Deferred 裁定）：design-ai-generation.md §4「I4.1 提供 Gemini 适配与提示模板」措辞更正为实际交付面（provider 注入面）；roadmap I4.1 行内容含「Gemini API 集成」——状态置 done 与行内容不符部分标记人工评审（roadmap Rule 5）。
- 验证：typecheck 40/40、build 40/40（--force）、lint 40/40（--force）、test 73/73 任务（**12,329 passed / 0 failed**，3d 包 177 测试）、check exit 0、覆盖率四维 ≥90（94.4/90.9/93.2/96.3）。
- Closure audit：独立子 agent 审计 **approved**（0B/0M/3 Minor 均在 closure pass 修复：plan 文本 llm-provider.ts 落点更正、design §2 url format: uri 回写、义务 (a) design §4 措辞更正与 (b) roadmap 状态+行内容差异标记人工评审），证据见 plan 468 `Closure Audit Evidence`。roadmap I4.1 → done。

## Three.js 集成 I3.1 工业协议接入（Plan 467，roadmap I3.1）

- Plan：`docs/plans/467-threejs-i3-protocol-plan.md`（draft review 2 轮升 active；必须自动化档）。
- 交付：`ReconnectionManager`（指数退避 500ms 基准 ±25% 抖动 clamp maxDelay、pending 幂等——onerror+onclose 双触发只一次重连、cancel/reset、耗尽 `reconnect-give-up`、timer/random 注入）+ `IndustrialAdapter`（openSocket capability check 按 adapter 实例去重、onopen reset+subscribe 帧、数据帧 address→tag 映射 bool/int16/float32 转换 + NaN 丢弃 → `scope.update('dataSources.<name>')`、parse 失败去重上报、主动 disconnect 抑制重连、同 id 重入先断旧、disconnectAll）。
- v5 漂移回写：design-protocol.md §3（onGiveUp/random 注入、pending 幂等契约）§4（unavailable 去重粒度 + 双触发幂等语义 + connect-failed 每次连接事件一次的粒度声明）——初版提交仅落 1/3（替换锚点失配），经审计退回后补齐。
- 使用面裁定：契约级可用（mock 链路证明）；renderer/host 组件接线与 schema surface 归后继（moved to explicit successor ownership，roadmap Rule 5 需人工评审新立）。
- 验证：typecheck 40/40、build 40/40、lint 40/40、test 73/73 任务（12,329 passed / 0 failed，3d 包 158 测试）、check exit 0、覆盖率四维 ≥90。
- Closure audit：两轮独立子 agent 审计（R1 issues：漂移回写仅落 1/3 → 修复 commit `faff10b7d` → R2 approved），证据见 plan 467 `Closure Audit Evidence`。roadmap I3.1 → done。

## Three.js 集成 I2.2 TransformEngine 与图元库（Plan 466，roadmap I2.2）

- Plan：`docs/plans/466-threejs-i2-primitives-transform-plan.md`（draft review 2 轮升 active；必须自动化档，五 Phase 全 Proof 先行）。
- 交付四块：TransformEngine（range 线性映射+clamp+防除零 → convert flux 表达式注入 value → condition，顺序固定、单项失败回退+去重、成功清记录）替换 I2.1 identity 接缝；过渡动画 TweenRegistry（tween/step/spring，number/vec3/hex 色插值，`modelId::path` 单活跃、覆盖续走，easing 未知回退 linear+诊断）；图元库（ModelConfig 判别联合 url XOR primitive、六几何×四材质、同步注册即时就绪、Mesh 直接为 root 使 material.\* 绑定可达）；AnimationConfig 关键帧播放（KeyframeClip 纯逻辑播放器 + 引擎 registerClips/startClip/notifyEvent + use-animation-clips hook，state 触发合并 paths 单次 useScopeSelector，event 经 useThreeEvents onEvent 转发）。
- 测试驱动修复：step 动画未保持起点（实现为线性）、pingpong count 终止缺失、非单调 keyframes 时间游标未用首帧时间、onEvent 经 latest ref 转发丢失、TweenRegistry get-after-set 型可选链问题。
- v5 漂移回写：design-renderer.md §1 ModelConfig 判别联合（primitive 字段 + url 可选）；design-data-binding.md §5 TransformEngine onError/setErrorHandler。
- 验证：`pnpm typecheck` 40/40、`pnpm build` 40/40、`pnpm lint` 40/40、`pnpm test` 73/73 任务（**12,329 passed / 0 failed**，3d 包 144 测试）、`pnpm check` exit 0、覆盖率四维 ≥90（95.8/93.3/91.8/97.6）。
- 测试驱动修复：primitive 双源静默无诊断（补 `primitive-dual-source` 一次性诊断 + 测试去掩盖）、notifyEvent 零测试覆盖（补引擎级 event 触发测试）、probe 探针残留删除、帧序（tween→clip）注释补齐。
- Closure audit：两轮独立子 agent 审计（R1 issues 2M → 修复 → R2 approved），证据见 plan 466 `Closure Audit Evidence`。

## Three.js 集成 I2.1 three-canvas 渲染器实现（Plan 465，roadmap I2.1）

- Plan：`docs/plans/465-threejs-i2-renderer-plan.md`（draft review 2 轮升 active；Test Strategy 必须自动化档，Proof 先行）。
- 新包 `@nop-chaos/flux-renderers-3d` 落地：schemas（v5 §1 类型，结构化 prop 按 `& SchemaObject` 承载）、renderer-definitions（events 整体 prop，D3）、binding/flux-eval（五态 probe + analyzeBindingSubscriptions，D4）、engine（SceneManager + ModelLoader generation-guard，D5）、renderer 三 hook + 组件壳（D1/D6）。
- 关键实现事实：灯光五类 + environment/fog 按 v5 §5 组装；`updateProperty` 点分导航 + `.set()` 类型分派；未就绪模型更新进 pending buffer（`modelId::path` 键、插入序回放、失败丢弃）；桥接走 `useScopeSelector` paths 精确订阅 → `compileValue`/`evaluateValue` → Object.is 比较 → pending 队列 → `setFrameUpdateQueue` 帧边界 drain；错误 (表达式, code) 去重上报；deps-empty 分析期上报不受 enabled 影响；事件 onPick/onHover + `createNormalizedActionEvent` + `helpers.dispatch`，射线拾取 interactive 门控 + 拖拽阈值。
- 测试驱动发现并修复 3 个真 bug：(1) replay 用复合键当路径导航（buffer 条目补存原始 path）；(2) 相机 `position.set` 后未 `updateMatrixWorld`，拾取射线用陈旧矩阵全程 miss；(3) dispose 竞态下 `loadModels` 承诺悬挂（dispose 时落定全部 settlers）。
- 覆盖率：包级 v8 四维 96.7/92.68/94.89/99.56 ≥ 90（industrial 骨架先例阈值）；默认 GL 工厂路径 `v8 ignore` 显式标注（plan 政策：列名豁免不降阈值）。
- check 命中处置：3 个新增命中（灯光默认 hex 色 ×2、结构化诊断 message ×1）按 AGENTS.md 流程在 `find-ui-consistency-gaps.mjs` EXEMPTIONS 登记（industrial 域豁免同款判例），check exit 0。
- v5 漂移回写：`LightConfig.groundColor` 字段（hemisphere 地面色显式化，替代 target 复用）已同步 schemas.ts + 设计分册 design-renderer.md §1——无其他漂移。
- 验证：`pnpm typecheck` 40/40、`pnpm build` 40/40（--force）、`pnpm lint` 40/40（--force）、`pnpm test` 73/73 任务（**12,268 passed / 0 failed**）、`pnpm check` exit 0、bench 脚本复跑通过。
- Closure audit：由独立子 agent（fresh session）执行，证据见 plan 465 `Closure Audit Evidence`。

## Three.js 集成 I1.1 设计共识审查定稿（Plan 464，roadmap I1）

- Plan：`docs/plans/464-threejs-i1-design-review-plan.md`（draft review 2 轮升 active）。
- v5 设计文档集定稿（`docs/components/threejs-integration/`）：design.md 总览（架构 + 决策记录 D1–D6 + 分册索引）+ design-renderer / design-data-binding / design-protocol / design-ai-generation 四分册，合计 ~40.7KiB、单册 ≤ 12KB（v4 单文件 72KB 已拆分，原文归档 `docs/analysis/threejs-integration-design-v4.md`）。
- 六项决策裁定（均含理由/被拒替代/live 证据）：D1 裸 Three.js（拒 R3F，对齐 scada 命令式引擎先例）；D2 新建 `flux-renderers-3d`（拒扩展 industrial）；D3 events 整体 `kind: 'prop'` + `helpers.dispatch` 桥接——**v4 的"compiler 约束"实体已找到**：`classifyField` 仅顶层 key 精确匹配、无点号路径（industrial `renderer-definitions.ts:37-38,201` 同款先例，调研文档 §8.1#7 的 ⚠️ 就此闭环）；D4 `compileValue`/`evaluateValue` 求值路径 + probe 依赖提取；D5 模型加载 generation-guard 取消；D6 错误去重诊断通道。
- 3 轮独立 sub-agent 共识审查（plan 464 Review Rounds 留痕）：R1 fail（3M/7m：SceneManager↔hook 接线缺口、绑定初值丢失无重放、v4 砍除项零记录）→ R2 pass-with-minors（5m/2Nit：buffer 键粒度、queue 换绑双依赖、CJK slug 回退等）→ R3 终审 pass-with-minors（1m/2Nit，8/8 R2 修复复核落地、15 处 live 引用零漂移）。
- roadmap 同步：I1 `planned`（draft review 通过时置位）；I2.1/I2.2/I3.1/I4.1「设计文档」列改指 v5 对应分册。
- Closure audit：由独立子 agent（fresh session）执行，证据见 plan 464 `Closure Audit Evidence`。

## Three.js 集成 I0.1 调研收口（Plan 463，roadmap I0）

- Plan：`docs/plans/463-threejs-i0-research-plan.md`（draft review 2 轮由独立子 agent 完成，pass-with-minors 后升 active）。
- 调研文档 `docs/analysis/threejs-integration-analysis.md` 新增四节：§7 industrial-hmi 复用点确认（每项 live file:line）、§8 设计 v4 API 用法核对表（10 条，2 ❌ 2 ⚠️ 6 ✅）+ 待裁定分歧清单（R3F vs 裸 Three.js；新包 vs 扩展 industrial，留 I1.1）、§9 Three.js 核心 API 深度分析（three@0.186.0，对 `@types/three` 抽查 ≥5 处签名）、§10 性能基准框架。
- 关键发现（I1.1 输入）：
  - 设计 v4 `formulaCompiler.compileExpression()` 产物喂 `evaluateWithState` 类型不匹配（`CompiledExpression` ≠ `DynamicRuntimeValue`）；scada 实际走 `compileValue()`/`evaluateValue()`。
  - three 0.186 `GLTFLoader.loadAsync(url, onProgress)` 仅 2 参，v4 的 AbortSignal 第 4 参不存在；取消需 generation-guard。
  - 路线图「复用 scada socket 数据桥接」勘误：industrial 包内零 `openSocket` 调用；契约在 `flux-core/src/types/renderer-api.ts:136-167`，host 实现在 `apps/playground/src/env/socket-impl.ts`。
  - v4 events 注册为 `kind: 'prop'` 的理由（"compiler 约束"）未指明出处；平台标准是 `kind: 'event'` → `props.events`。
- 基准框架：`scripts/three-perf/bench-scene-graph.mjs`（7 场景，`--json` 支持），基线：位置写入 ~5.4ns、颜色写入 ~185ns、场景组装 ~60µs——绑定热路径 CPU 段对 16ms 帧预算余量充足。根 devDeps 增 `three@0.186.0` + `@types/three@0.186.0`。
- 环境修复（非本计划改动引入）：本工作区首次 `pnpm install` 后 `pnpm typecheck` 因 `tsconfig.base.json` paths 缺 `flux-renderers-form-advanced` 映射而失败（fresh checkout 无 dist 时暴露，预存在债务，仅 build-first 可绕过）；`pnpm lint` 因分支落后 origin/master 缺 4 个 i18n key 而红——已 merge origin/master（解决 package.json turbo 版本冲突，保留 three 依赖），两门禁恢复绿。tsconfig paths 缺口未修（跨包契约变更，另行处理）。
- 验证（merge origin/master 后）：`pnpm typecheck` 39/39、`pnpm build` 39/39、`pnpm lint` 39/39、`pnpm test` 72/72 任务（38 包报告测试计数，**12,185 passed / 0 failed**）、`pnpm check` exit 0（仅注册豁免）、bench 脚本跑通且 `--json` 可解析。
- Closure audit：由独立子 agent（fresh session）执行，证据见 plan 463 `Closure Audit Evidence`。

---

以下为 `feat-echarts-integration` 分支同期日志（E1.1–E5.2 收口，随分支合并带入）。

## What landed today

### E1.1 — `echarts` 渲染器骨架（plan `docs/plans/2026-09-13-2343-echarts-e1-1-renderer-skeleton-plan.md`）

**Plan review**：独立子 agent 2 轮（round 1 fail：3 Major——flux-bundle 单文件构建内联 echarts、events 声明层 lying contract、缺 new-renderer-introduction-audit；round 2 收敛至零 Major）。events 体系整体移交 E2.1；根 marker class `nop-echarts`；audit §A–F 纳入 Phase 4。

**决策**：按需引入粒度 = **按功能模块**（`echarts/core` + `echarts/charts` + `echarts/components` + `echarts/renderers` 官方 tree-shaking 入口，集中注册于 `flux-renderers-data/src/echarts-setup.ts` 单一模块；渲染器组件动态 import → echarts 全量位于懒 chunk，未挂载 echarts 渲染器的宿主零加载）。`analysis/echarts-migration-analysis.md` Open Questions 已勾选。

**代码落地**（`packages/flux-renderers-data`）：

- `echarts-setup.ts` — 22 类 chart + 通用组件 + Canvas/SVG renderer 集中注册（`use` 经 `registerEChartsModules` 别名调用，规避 eslint react-hooks 对 `use*` 顶层调用的误报）；`getECharts()` 导出
- `echarts-schemas.ts` — `EChartsSchema`（type/option/dataset/renderer/initOptions/theme/notMerge/lazyUpdate/height/componentId；`events` 移交 E2.1；`componentId`/`height:number|string` 为对齐 chart 先例的记录在案偏差；`EChartsDatasetSchema`/`EChartsInitOptions` extends `SchemaObject` 以满足 BaseSchema 索引签名）
- `echarts-schema-validation.ts` — 结构校验（`RendererSchemaValidationContext`，诊断码 `invalid-property-shape`；option 缺失 = warning，非法形态 = error）
- `echarts-renderer.tsx` — 生命周期：懒加载（模块级 promise 去重）→ init（canvas/svg、theme、initOptions）→ setOption（引用比对，notMerge/lazyUpdate 透传）→ ResizeObserver resize → dispose；空态/错误态/`data-empty` 标记；`nop-echarts` marker；ComponentHandle `resize` 能力
- `echarts-renderer-definition.ts` — RendererDefinition（schemaValidator + propContracts + resize capability + fields，无 events 声明）；并入 `dataRendererDefinitions`；barrel 导出
- 测试：`echarts-schema-validation.test.ts`（16）、`echarts-renderer.unit.test.tsx`（12）、`echarts-renderer-load-failure.unit.test.tsx`（2，独立文件规避 `vi.doMock`/`resetModules` 与 hoisted mock 的交叉污染）、`echarts-renderer-definition-contracts.test.ts`（5）；`data-package-units.test.tsx` 注册清单断言更新（manifest 契约）

**依赖接入**：`echarts@^6.0.0`（npm latest 6.1.0）→ `flux-renderers-data` optional peer + devDep；`flux-bundle` optional peer + devDep + `vite.config.ts` `hostOwnedExternal` 增加 `/^echarts(\/.*)?$/`（防单文件 bundle 内联）。

**Playground 契约强制项**：`route-matrix.test.ts` 要求每个注册 data renderer 有 route + lab page → `data-route-entries.ts` 增 `echarts` 路由、`echarts-lab-page.tsx`（3 场景：canvas bar / svg line / 显式空态）挂入 lab registry + renderers barrel。

**New Renderer Audit §A–F**：全部通过并记录于 plan「New Renderer Audit Record」（A 无外部 IO；B 数据零请求/UI/i18n 复用既有键零新增；C 域内部 state 不进 scope、resize 经 ComponentHandle projection；D 标准 RendererComponentProps 契约；E events 预留归 E2.1；F `nop-echarts` + data-slot + presence-only data-empty）。

### 实现期发现（工程记录）

- **vitest 模块 runner 竞态**：组件在首个动态 import 未决时卸载后，同文件后续 `import('./echarts-setup.js')` 会绕过 `vi.mock` 拉取真实模块（复现于 double-render-with-unmount 用例，`init returned ECharts` 实证）。渲染器层模块级 promise 去重（`loadEChartsSetup`）既是正确的生产代码（避免重复 chunk 请求）也消除了测试竞态。
- **`RendererDefinition` 泛型变体**：显式 `RendererDefinition<EChartsSchema>` 因 `authoringTransform` 逆变形不可赋给 `RendererDefinition<BaseSchema>` 数组——对齐 sparkline 先例使用非泛型 `RendererDefinition` + 上下文类型化。

## Verification（执行 session）

- `pnpm typecheck` ✅ 37/37（turbo，repo root）
- `pnpm build` ✅ 37/37（turbo，repo root；echarts 经 hostOwnedExternal 保持外部化）
- `pnpm lint`：turbo eslint ✅ 37/37；链上前置 `check-i18n-keys` ❌ 4 个未定义键（见下方既有红登记）
- `pnpm test` ✅ 68/68 tasks，约 11,501 tests passed / 0 failed（含 flux-renderers-data 147 files / 1081 tests、playground 347 tests）
- `pnpm check`：除同一 i18n 既有红外全过（`check-oversized-code-files` 2 个 error 为已豁免 locale 数据文件，plan 2026-08-07-1053-1 登记）
- e2e：本计划未跑（E5.1 统一测试覆盖），故不构成 full-green 口径

## Pre-existing red 登记（非本分支引入，本变更零新增命中）

`check-i18n-keys`：4 个 undefined keys——`flux.picker.remove`（引入提交 `44ef5188f`，picker feat，on origin/master）、`flux.validation.url` / `flux.validation.integer` / `flux.validation.format`（引入提交 `646d16ba4` 2026-09-02，format-registry feat，on origin/master）。取证：`git merge-base --is-ancestor 646d16ba4 origin/master` 成立；`git diff origin/master...HEAD -- packages/flux-i18n packages/flux-runtime packages/flux-renderers-form-advanced` 为空（本分支对涉事包零改动）。属 master 级既有红，E1.1 不处理（不在 scope），建议独立修复项补齐两 locale 的 4 键。

## Status

- E1.1 执行完成，closure audit 待独立子 agent 复核（plan 保持未关闭直至 audit 通过）
- roadmap E1.1 状态待 audit 通过后 todo → done

## What's next

- E1.1 closure audit → commit → E2.1（dataset/encode 数据映射 + 事件桥接 + 主题 token 裁决）

---

## E2.1 — ECharts dataset 绑定 + 事件桥接 + 主题 token（续）

**Plan review**：独立子 agent 2 轮（round 1 fail：2 Major——eventContracts 通道移交缺口、tooltip/legend 可观测收口缺失；round 2 `pass-with-minors`，3 残余 Minor 已合并）。

**主题 token 裁决**：CSS 变量 → `flux` 注册主题（`echarts-theme.ts` 的 `resolveFluxEChartsTheme()`：`--chart-1..5` hsl 包装 + `--foreground`/`--muted-foreground`/`--border`/`--popover`/`--popover-foreground`；`registerTheme('flux')`；`theme` 未指定时默认应用，指定时透传；SSR/jsdom 回退静态调色板；运行时切换需重挂载）。analysis Open Questions 已全部 resolved。

**代码落地**：

- `echarts-theme.ts`（新建）：flux 主题解析
- `echarts-setup.ts`：模块加载时 `registerTheme('flux', ...)`
- `echarts-renderer.tsx`：dataset 组合（`helpers.evaluate` 解析表达式/恒等透传已解析数组；schema 级 dataset 覆盖 `option.dataset`；三形态直传）+ 防御校验（source 形态/dimensions-vs-source/encode 引用 → warn 降级）+ 空 dataset 显式空态 + `empty` 插槽 + 事件桥接（`NATIVE_EVENT_BINDINGS` 9 键 on\*→native 字面清单，`events?.<key>` 显式引用满足 contract-honesty 守卫；`createNormalizedActionEvent` + `helpers.dispatch(action, { event, scope })`；`off` 清理；未知键 warn；dispatch reject 捕获）
- `echarts-schemas.ts`：补 `events`/`empty` 字段
- `echarts-renderer-definition.ts`：fields 补 `events`（prop）/`empty`（value-or-region）；`propContracts.events`；9 键 `eventContracts`
- validator：`events` 值形态/`transform` 数组诊断
- 测试：`echarts-theme.test.ts`（4）、`echarts-dataset-binding.test.tsx`（13，真实编译链 `createDataSchemaRenderer`：对象数组/列式/二维数组、覆盖优先级、空态/插槽、encode 警告、bar/line/pie/scatter dataset 形态、tooltip+legend 透传断言）、`echarts-events-bridge.test.tsx`（6：on/off/normalized event/未知键/reject 捕获/dataZoom）；`echarts-renderer.unit.test.tsx` 默认 theme 断言更新为 `'flux'`（记录在案的行为变更）
- lab page：dataset 切换按钮场景、svg line、click→showToast、dark 主题 pie、空态

**实现期发现**：flux-core `contract-honesty` 守卫（`isRendererEventKeyReferenced`）要求 `events?.<key>` 字面引用模式——动态键 map（`NATIVE_EVENT_MAP[fluxKey]`）不被识别；重构为显式 `nativeBindings` 字面清单（`['click', events?.onClick, ...]`），既满足守卫又比动态查表更诚实可读。

## Verification（E2.1，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37（修复：未用 helper 删除、eslint-disable 摘除改显式 deps——React Compiler 禁optimize 告警）
- `pnpm test` ✅ 68/68 tasks，约 11,527 tests / 0 failed（flux-renderers-data 150 files / 1107 tests）
- `pnpm lint`/`pnpm check`：同 E1.1 登记的 4 个既有 i18n 未定义键红，本计划零新增命中
- e2e 未跑（E5.1 统一覆盖），不构成 full-green 口径

**Status**：E2.1 执行完成，closure audit 待独立子 agent 复核。

---

## E3.1 — ECharts 高级图表类型落地（sankey/treemap/tree/boxplot/gauge/funnel/radar）

**Plan review**：独立子 agent 1 轮 `pass-with-minors`（4 Minor 已合并：radar 记法、条件 Fix 收口处置、boxplot 五数概括措辞、Phase 2 Targets 清理；另发现并修复 roadmap E2.1 行 todo→done 的状态滑移——prettier 表格重排导致此前 replace 未命中，教训：roadmap 表格行更新后需 grep 复核）。

**落地**：

- `echarts-advanced-charts.test.tsx`（新建，7 tests，真实编译链）：sankey（nodes/links）、treemap（层级 data）、tree（children 层级）、boxplot（二维数组 dataset 绑定预计算五数概括）、gauge（标量）、funnel（标签值）、radar（option 级 `radar` 坐标系 + indicator）——7/7 一次转绿，证明 E1.1 透传 + setup 全量注册下高级类型「缺的是专项验证」论断成立；条件 Fix 项 not-triggered（零渲染器代码改动）。
- lab page 新增 7 场景（boxplot 走 dataset 绑定、gauge 组合 onClick 事件），introDescription 更新。
- 图结构类（sankey/tree/treemap/radar）不适用 dataset+encode 的边界记录于 plan Follow-up（echarts 原生语义即 series 内嵌数据）。

## Verification（E3.1，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37；`pnpm test` ✅ 68/68 tasks，11,534 tests / 0 failed
- playground 33 files / 347 tests 全绿
- i18n 既有 4 键红同前，零新增命中；e2e 未跑（E5.1），不构成 full-green 口径

**Status**：E3.1 执行完成，closure audit 待独立子 agent 复核。

---

## E4.1 — ECharts 完整功能（map+GeoJSON/candlestick/graph/sunburst/themeRiver/custom）

**Plan review**：独立子 agent 1 轮 `pass-with-minors`（5 Minor 已合并：GeoComponent 真实分发链由 lab 承担、map 纳入 init gating、map 字段不注入 option.series、Follow-up 补 classification、map-name-missing 措辞对齐 echarts console.error）。审查者以临时探针实证 custom series 函数值（renderItem）经真实编译链保真到达 setOption。

**落地（map 为唯一代码面）**：

- `echarts-setup.ts`：`getECharts()` 增加 `registerMap` 导出
- `echarts-schemas.ts`：`EChartsMapSchema`（name + geoJson 表达式绑定）；`echarts-renderer-definition.ts` fields 增 `map`（prop）
- `echarts-schema-validation.ts`：map 形态诊断（name string / geoJson 必填）
- `echarts-renderer.tsx`：`mapInfo` 求值 memo（表达式/已解析对象）+ init gating（geoJson 就绪后重走 init+registerMap+setOption）+ 不可用时显式空态
- 测试：map 桥接单测 3（先于 init 注册/未就绪降级/晚到重走 init）+ validator 4；`echarts-full-features.test.tsx`（6，真实编译链：map/candlestick/graph/sunburst/themeRiver/custom-renderItem 保真）
- lab page：map（内联极小 GeoJSON 经表达式绑定）/candlestick/graph/sunburst/themeRiver/custom 六场景

## Verification（E4.1，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37；`pnpm test` ✅ 68/68 tasks，11,544 tests / 0 failed（flux-renderers-data 152 files / 1124 tests）
- i18n 既有 4 键红同前，零新增命中；e2e 未跑（E5.1），不构成 full-green 口径

**Status**：E4.1 执行完成，closure audit 待独立子 agent 复核。

---

## E5.1 — ECharts 集成优化（按需引入包体 / 文档和示例 / 测试覆盖）

**Plan review**：独立子 agent 2 轮（round 1 fail：2 Major——harness 可复现性矛盾、e2e gate 范围歧义；round 2 `pass-with-minors`，3 残余措辞 Minor 已合并）。

**落地**：

- `echarts-import-isolation.test.ts`（新建，2 tests）：按 import source 三形态（from / 动态 import() / side-effect import）守卫「echarts 符号仅入 setup + renderer(type-only/动态 setup)」；红侧有效性以临时违规注入验证（注入→红→移除）
- 包体实测：echarts-setup 懒 chunk **1,418.99 kB min / 399.79 kB gzip**（echarts 6.1.0 / vite 8.0.3-rolldown rc.12，es2022 minify）；harness 完整源码内嵌 design.md「按需引入与包体」节，命令自包含可复现；`_tmp/` build 输出已清
- 文档：`docs/components/echarts/design.md`（最终态设计 + schema 契约表 + Failure Paths + 包体节 + 验证锚点 + 原则审计引用）、`docs/components/echarts/example.json`（四类示例）、`docs/index.md` 路由行更新（rev 3 + 组件文档指引）、`docs/components/index.md` 数据渲染器清单与目录清单补 `echarts`
- e2e：`tests/e2e/component-lab/echarts-lab.spec.ts`（2 用例，真实浏览器 + 程序化断言：canvas 非零渲染 + dataset 切换无空态/无错误/无页面错误）
- 覆盖率：echarts 系文件全部 ≥80%（renderer 94.4 / theme 94.1 / setup 100 / validation 100 / schemas 100 / definition 100；`echarts-setup.test.ts` 真实加载测试补齐 setup 从 0% → 100%，同时实证 GeoComponent→registerMap 真实分发链）

**e2e 实证并修复的 live defect（E2.1 遗留）**：`events` 声明为普通 prop 时，编译器在渲染期深求值 args 模板——`${event.name}` 在 event 上下文不存在时每帧抛错（e2e 捕获 104 条 console.error）。修复（对齐 button onClick 的 dispatch 期求值语义）：definition `events` 字段改 `ignored`（跳过编译期深求值）+ 渲染器直读 raw schema（`props.schema.events`），`${event.*}` 模板在 dispatch 期结合 normalized event 求值。新增 events-bridge 用例锁死 raw-schema 语义 + definition-contracts 断言 `ignored`。**design.md 已同步该求值语义。**

## Verification（E5.1，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37；`pnpm test` ✅ 68/68 tasks，11,550 tests / 0 failed
- e2e（echarts spec）✅ 2/2；coverage exit 0（echarts 文件全达标）
- `pnpm check`：仅存既有 i18n 4 键红（零新增）

**Status**：E5.1 执行完成，closure audit 待独立子 agent 复核。

---

## E5.2 — nop-datav 集成评估（dashboard panel 指定渲染器机制）

**Plan review**：独立子 agent 2 轮（round 1 fail：1 Major——panel `source` 绑定路径边界缺口；round 2 `pass-with-minors`，3 残余 Minor 已合并）。

**评估结论：零包装层可用。** dashboard panel 经通用机制（`panel.type` → `registry.has` → fragment 装配 → `helpers.render`）直接挂载 `echarts` 渲染器（E1.1 起注册于 `dataRendererDefinitions`）。

**落地**：

- dashboard 包 devDependency 增 `@nop-chaos/flux-renderers-data`（仅测试用）
- `dashboard-echarts-panel.test.tsx`（3 tests）：机制证明（fragment → setOption 全等 + props 表达式求值）、panel.source 边界不变量（source 数据永不出现在 setOption option 中——closed prop model 下 unknown-property 跳过，两个分支的公共不变量）、props 表达式求值
- `dashboard-echarts-panel-failure.test.tsx`（1 test）：echarts chunk 失败降级隔离（echarts-error 占位 + text 兄弟面板存活）
- 评估文档 `docs/components/echarts/integration-nop-datav.md`（机制/前置条件/authoring 指引「echarts 面板经 props.option/dataset 传数，不用 panel.source」/边界/结论）+ design.md 验证锚点

**实现期发现**：panel.source 边界行为存在两个分支（编译期拒绝→零 init；跳过未知键→init 但 option 原样），测试断言二者的公共不变量而非耦合诊断严格度分支。

## Verification（E5.2，执行 session）

- `pnpm typecheck` ✅ 37/37；`pnpm build` ✅ 37/37；turbo eslint ✅ 37/37；`pnpm test` ✅ 68/68 tasks，约 11,554 tests / 0 failed（dashboard 6 files / 57 tests）
- `pnpm check`：workspace-manifest-deps 过链，仅存既有 i18n 4 键红（零新增）
- e2e 未新增（E5.1 已含 echarts lab spec），不构成 full-green 口径

**Status**：E5.2 执行完成，closure audit approved，roadmap E5.2 → done。**ECharts 集成 roadmap 全部 work item（E0–E5）完成。**
