# Development Log — 2026-04-11

### 2026-04-11 (12)

- Elevated `docs/analysis/2026-04-11-dynamic-schema-hot-reload-and-validation-owner-lifecycle.md` from problem statement to a concrete technical specification.
- Link: `docs/analysis/2026-04-11-dynamic-schema-hot-reload-and-validation-owner-lifecycle.md`
- Key decisions added:
  - `modelGeneration: number` counter on each `ValidationScopeRuntime` instance; increments on every model replacement.
  - Async run identity now requires `{ ownerId, modelGeneration, path, ruleId, runGeneration }` — old-generation runs must not publish.
  - `isOwnerCompatible(oldModel, newModel)` algorithm: only `ownerId` and `rootPath` must match; structural node differences are handled by model-refresh, not owner-recreation.
  - Two explicit lifecycle paths: **owner-compatible model refresh** (10-step sequence ending in `reason: 'system'` reconciliation) and **owner-boundary change** (dispose + recreate + React key strategy).
  - State retention rule: always reset materialization cache, active instance graph, async runs, closure cache; may retain touched/dirty/errors only for paths whose `ruleIdentitySet` is identical across old and new model.
  - React integration points defined: schema fingerprint comparison, React key for boundary change, `refreshCompiledModel(newModel)` for compatible refresh, field re-registration via `modelGeneration` observable.
  - Debugger exposure list: `modelGeneration`, reconciliation pass status, cancelled run count, cleared vs retained field paths.
- Also identified remaining open gaps in the normative validation docs (not yet fixed):
  - `ChildValidationContract` activation timing edge cases (parent submit races child not-yet-ready).
  - Multi-level draft commit propagation chain is undefined for 3+ owner nesting.
  - `compiledModel: null` runtime behavior is underdocumented in the architecture baseline.
  - Overlay + compiled rule priority/dedup for same path + same ruleKind is not specified.
  - `applyChangesAndRevalidate` cross-owner path behavior is unspecified in formal docs.
- Next step: create an implementation plan that covers model-generation tracking, `refreshCompiledModel` API surface, React key integration, and the reconciliation pass sequencing.

### 2026-04-11 (11)

- Completed second-round deep review of `docs/analysis/2026-04-11-flux-validation-unified-design.md` and applied all P1/P2/P3 fixes. Document status updated to "已审核，可采纳".
- Links: `docs/analysis/2026-04-11-flux-validation-unified-design.md`, `docs/architecture/form-validation.md`
- Key fixes applied:
  - **P1-1**: Added `ready: boolean` to `ScopeValidationStateSnapshot` in both the unified doc and `form-validation.md`. Updated `summary-gate` canSubmit semantics to use `ready` instead of `valid`.
  - **P1-2**: Restored the two-interface split (`RuntimeRuleOverlayDescriptor` with `ruleTemplatesByPath`, `RuntimeOpaqueValidationDescriptor` with `attachTo`) in §21. The unified `RuntimeValidationDescriptor` was missing `ruleTemplatesByPath`, which broke the "single source of truth for materialization" claim in §13.
  - **P1-3**: Added `ChildValidationContractRegistration extends ChildValidationContract` (with `active: boolean` + `unregister()`) and lifecycle rules to §22. Without `active`/`unregister`, disposed child dialogs could block parent `canSubmit` indefinitely.
  - **P2-1**: Added `registerField` / `updateFieldRegistration` to `ValidationScopeRuntime` public interface in both docs.
  - **P2-2**: Clarified that `applyChangesAndRevalidate` with array root in `changedPaths` uses `validateSubtree` semantics (not just closure expansion), ensuring all remapped row fields are re-validated after array mutation.
  - **P2-3**: Documented Phase 1 limitation: `effectiveRequired` (field `*` indicator) is also lazy in Phase 1, not just validation results.
  - **P2-4**: Added enforcement note: `kind: 'none'` contributions must not provide `collectNode`/`collectRules`/`collectChildren`.
  - **P3-1**: Fixed phasing note for `compiledModel: null` — Phase 3 non-form scopes always have compiled model; null case is Phase 4.
  - **P3-2**: Fixed §24.2 example to use `validationScope: { id, kind }` schema contract instead of bare `scopeId`.
  - **P3-3**: Changed §18.1 Step 4 reason from `'change'` to `'system'` for variant switch initial validation.
  - **P3-4**: Aligned `getScopeRootErrors()` semantics to filter by `sourceKind === 'scope-root'`.
  - **P3-5**: Fixed stale `validateForm()` reference → `validateAll()` in Phase 2 description.
  - **P3-6**: Added `ownerId: string` to `FieldValidationStateSnapshot` for debugger/multi-owner use.
  - **P3-7**: Clarified child scope registration mechanism in §7.2.
  - **P3-8**: Tightened `CompiledRuleTemplate.args` type from `CompiledRuntimeValue<unknown> | unknown` to `CompiledRuntimeValue<unknown>` (static values use `{ kind: 'static', value }` wrapper).
  - **P2-6**: Added note that `validationOrder` contains template IDs and is expanded per array instance at runtime.
- Next step: this document is ready for use as the Phase 1 implementation contract. Begin Phase 1 work following `docs/analysis/2026-04-11-flux-validation-unified-design.md` §25 Phase 1 spec.

### 2026-04-11 (10)

- Pushed `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md` from architecture principles down to compiler-interface design by defining a richer field-tree node shape and compile-time validation collector direction.
- Link: `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- form validation should compile to a real field-tree model, not just a flat rule list; flat absolute paths remain the query/storage key, but parent/children topology remains explicit.
- renderer definitions should evolve from the current leaf-oriented `renderer.validation` participation API toward compiler-integrated validation collectors that can register object/array/variant/repeated-template structure, rule templates, and dependencies.
- `object-field`, `array-field`, `variant-field`, and structural `loop` now each have explicit compile-time registration responsibilities documented in the draft.
- `array-field` and `loop` may share repeated-template substrate, but they must not collapse into the same high-level validation semantics because only `array-field` owns value-editing and array aggregate validation.
- Next step: map the existing compiler and renderer definitions onto these proposed collector hooks and identify the minimal compatibility migration path from the current `renderer.validation` API.

### 2026-04-11 (9)

- Compared `docs/analysis/2026-04-11-form-validation-expression-rules-design.md` against the owner-redesign draft and folded the strongest ideas into `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`.
- Links: `docs/analysis/2026-04-11-form-validation-expression-rules-design.md`, `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- adopted the idea that Flux should explicitly distinguish three layers inside forms: compiled field tree / validation graph, runtime field registration state, and validation result state.
- adopted the direction that renderer definitions should support compiler-integrated validation collector hooks, so component types can register field-tree nodes, aggregate structure, rules, and dependencies during compilation rather than relying only on runtime mount behavior.
- kept the owner-scoped direction and did **not** adopt “mounted field registry is the only truth” as the main architecture, because Flux still needs precompiled structure, owner boundaries, aggregate topology, and non-React-friendly semantics.
- clarified the intended split: compiled field tree defines what may exist, runtime registry reports what is currently instantiated/participating, and validation state records what currently failed or is validating.
- Next step: refine the field-tree node shape and map the current renderer `validation` contribution API toward compile-time collector hooks without breaking the existing compiler flow.

### 2026-04-11 (8)

- Refined `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md` after an independent audit of the owner execution pseudocode, tightening the phase-1 runtime rules.
- Link: `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- owner execution now uses a fixed-point preflight (`prepareExecution`) so active branch participation, hidden-state transitions, and clear-on-hide side effects stabilize before validation starts.
- the public post-write API is narrowed to a write-aware atomic owner operation (`applyChangesAndRevalidate`) instead of a loose `revalidateAffected(changedPaths)` helper.
- phase 1 explicitly allows owner-side value mutations caused by hidden participation reconciliation, while keeping hidden-policy intent sourced from the UX layer.
- phase 1 async behavior under structural edits is intentionally conservative: array reorder/remove, variant switches, subtree deactivation, and owner disposal invalidate affected subtree async runs instead of attempting async remap.
- submit validation now explicitly supersedes pending debounced change/blur runs before launching submit-owned validation runs.
- Next step: decide subtree extraction strategy and map these phase-1 algorithms against the current `FormRuntime` implementation to identify which parts can be reused directly versus moved behind a `ValidationOwner` abstraction.

### 2026-04-11 (7)

- Extended `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md` with an implementation-oriented `ValidationOwner Execution Model` section and owner API pseudocode.
- Link: `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- the redesign now defines a common execution sequence for all owner validation entry points: refresh active instance participation, reconcile hidden-state side effects, compute impacted closure, materialize effective rules, run sync rules first, then run async rules with explicit run ownership.
- `validateAt`, `validateSubtree`, `validateAll`, and `revalidateAffected` are now documented as owner-level algorithms instead of loose conceptual APIs.
- `revalidateAffected` is treated as the post-commit closure revalidation primitive for array mutation, variant switching, and draft confirm writeback.
- async validation ownership is made explicit with `path + ruleId + runId + reason`, so stale results from branch switches, row disposal, or owner disposal cannot write back into the wrong active graph.
- Next step: refine deterministic ordering rules, subtree extraction strategy, and how these owner algorithms should map onto existing `FormRuntime` and runtime-registration code paths.

### 2026-04-11 (6)

- Expanded `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md` with dynamic validation participation design for `if`, `variant-field`, `loop`, `object-field`, and `array-field`, plus a worked end-to-end example.
- Link: `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- Flux validation keeps precompiled template graphs, but runtime validation executes against an owner-scoped **active validation instance graph** so `if` branches, active variants, repeated array items, and opened draft owners can participate dynamically without reverting to a pure AMIS-style mount-only registration model.
- `variant-field` is treated as mutually exclusive branch activation: inactive branches do not participate in validation, and branch switching must clear old branch errors/validating state before activating and validating the new branch.
- `loop` remains a structural repeated-instance materializer rather than a new validation owner; value validation semantics still belong to value-bearing nodes such as `array-field` and ordinary bound fields instantiated inside the loop.
- async validation semantics are now documented as part of the same owner pipeline, with explicit deactivation/cancellation cases for `if` branch changes, variant switches, array row removal, and draft-owner disposal.
- Added a concrete complex example covering `if + variant-field + detail-field + object-field + array-field + loop + async`, with step-by-step explanation of local validation, branch activation, draft confirmation, array mutation, and submit-time active-instance traversal.
- Next step: continue refining the draft around subtree extraction mechanics, dynamic overlay precedence, and how much of the active-instance materialization contract should later move into the normative architecture doc.

### 2026-04-11 (5)

- Added and iteratively refined a non-normative redesign draft at `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`, keeping `docs/architecture/form-validation.md` unchanged as the active contract.
- Link: `docs/analysis/2026-04-11-form-validation-owner-redesign-draft.md`
- Key decisions:
- validation redesign keeps owner-scoped compiled graphs, but now separates `Value Validation Layer`, `ValidationOwner`, and `FormRuntime` UX responsibilities explicitly.
- validation rule values can be expression-backed and precompiled into rule templates that materialize effective static rules at validation time.
- phase 1 is intentionally narrowed to subtree-based draft owners only; arbitrary projection draft editing stays outside the first-class validation-owner model for now.
- phase 1 public owner APIs, dependency bookkeeping, dynamic overlays, hidden-path state, and cache invalidation are all anchored on canonical absolute paths.
- parent-side commit validation is modeled as owner-computed affected-closure revalidation (`revalidateAffected`) instead of caller-picked subtree validation.
- inherited dialog/drawer editing requires captured parent-owner context; array-row reorder during an open inherited surface is explicitly left out of phase 1 until stable row identity is designed.
- Process note: the draft was audited by multiple independent subagents and revised repeatedly until the remaining issues were narrowed to API precision/open-phase questions instead of broad structural contradictions.
- Next step: refine subtree extraction strategy, dynamic overlay precedence/dedup, and decide whether projection drafts or row-reorder-safe inherited surfaces should enter a later phase.

### 2026-04-11 (4)

- Completed Plan 67 (Hidden-Field Policy) — full implementation landed and verified.
- New types: `HiddenFieldPolicy extends SchemaObject` in `packages/flux-core/src/types/validation.ts`; `CompiledFormValidationField.hiddenFieldPolicy`, `CompiledValidationNode.hiddenFieldPolicy`, `CompiledFormValidationModel.defaultHiddenFieldPolicy`.
- New function: `resolveHiddenFieldPolicy(fieldPolicy, formPolicy)` in `packages/flux-core/src/validation-model.ts` — merges field > form > architecture default (false/false).
- `FormRuntime` interface extended with `notifyFieldHidden(path, hidden)` in `packages/flux-core/src/types/runtime.ts`; implemented at `packages/flux-runtime/src/form-runtime.ts:214` with idempotency check and `clearValueWhenHidden` trigger.
- Validation skip: `validatePath()` in `packages/flux-runtime/src/form-runtime-validation.ts:273` — hidden fields with `validateWhenHidden=false` clear their errors and return empty result.
- Renderer integration: `NodeRenderer` calls `notifyFieldHidden` in `useEffect` (`packages/flux-react/src/node-renderer.tsx:276`), covering all field renderers automatically.
- Shared hook: `useHiddenFieldPolicy(name, hidden)` added to `packages/flux-renderers-form/src/field-utils.tsx:291` for composite renderer use.
- Schema adoption: `InputSchema` and `FormSchema` in `packages/flux-renderers-form/src/schemas.ts` both carry `hiddenFieldPolicy?: HiddenFieldPolicy`.
- Tests: `packages/flux-runtime/src/__tests__/hidden-field-policy.test.ts` (15 tests) and `packages/flux-renderers-form/src/__tests__/hidden-field-policy.test.tsx` (6 renderer integration tests) — all green.
- Docs: `docs/architecture/form-validation.md` updated — Code Anchors, "Hidden And Inactive Content" section, and "Current Runtime Baseline" section all reflect live implementation.
- Key decision: `notifyFieldHidden` is called by `NodeRenderer` (not by individual renderers), so all field-type nodes get hidden-field tracking for free without per-renderer work.
- Key decision: `clearValueWhenHidden` fires in `notifyFieldHidden` on the hidden→visible transition edge only (idempotent; won't fire again if already hidden).
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test` all pass (flux-runtime: 279 tests, flux-renderers-form: 156 tests).
- Plan 67 closed: all phases `completed`, Validation Checklist fully ticked.

### 2026-04-11 (3)

- Reframed `docs/architecture/form-validation.md` around validation owner boundaries instead of treating nested validation as a flat field-rule problem.
- Link: `docs/architecture/form-validation.md`
- Key decision: nested validation should be collected by the current validation owner, not by visual nesting level. Inline `object-field` / `array-field` / table editors stay in the parent owner when directly bound, while `detail-field` / `detail-view` / dialog draft editors should use a local draft validation owner until commit.
- Key decision: `c:/can/nop/templates/yup` and `c:/can/nop/amis-react19` remain reference inputs only. Flux should keep compiled owner-scoped validation graphs plus `validateField(path)` / `validateSubtree(path)` / `validateForm()` as the primary model, instead of switching to a dynamic Yup schema or a mount-only form-item registration model.
- Next step: align runtime design and component designs around a first-class local draft validation owner for detail/dialog editors.

### 2026-04-11 (2)

- 重写 `docs/architecture/form-validation.md`，修正已知错误并补充多个架构 gap。
- 修正的错误：
  - `CompiledFormValidationModel` 中不存在的 `fields` key（已删除，实际只有 `nodes`）
  - `FormStatusSummary` 中不存在的 `lastSubmitStatus` 字段（已删除）
- 新增内容：
  - `hidden` 字段的运行时追踪机制：`sharedState.hiddenFields: Set<string>` + `FormRuntime.notifyFieldHidden(path, hidden)`
  - `CompiledFormValidationModel` 的平面字典结构说明（`nodes` 是 flat `Record<string, CompiledValidationNode>`，层级通过 `children`/`parent` 字符串表达）
  - `buildCompiledFormValidationModel` 无字段时返回 `undefined` 的边界行为
  - **`required` vs `requiredWhen` 的本质区别**：`required` 是编译期静态规则（表达式字符串被视为 truthy，永远必填）；`requiredWhen`/`requiredUnless` 在验证触发时读 scope，是真正的运行时条件规则
  - **`validateWhen` 设计**（未来方向）：在 `CompiledValidationRule` 上加 `when?: CompiledRuntimeValue<boolean>`，验证触发时一次性 `evaluateValue` 无需 state；`kind === 'static'` 时编译期直接过滤；限制：表达式用的是 form-level 平面路径
  - **嵌套 scope 下的验证路径模型**：array item 字段路径为 `items.0.name` 格式的绝对路径；验证始终在 form-level scope 的平面 key 空间里操作；动态行通过 `registerField` 补充
  - **数组状态重映射**：行插入/删除时 `transformArrayIndexedPath` 同步所有数字索引键的错误/触碰/脏状态
  - **表单边界隔离**：dialog/popover 内的 `form` 创建完全独立的 `FormRuntime`，外层 form 不知道它的存在；分级验证通过各自 form 的 submit 触发，不存在跨边界验证耦合
  - `scopePolicy: 'dialog'` 不存在的澄清
- Key decision: `validateWhen` 不需要 `RuntimeValueState`，因为验证只在触发时运行一次，当时 scope 已经就绪，`evaluateValue` 直接求值即可。这和 `requiredWhen` 用 `scope.get(path)` 的本质完全相同，只是换成了任意表达式。

### 2026-04-11 (1)

- Completed Plan 65 (Scoped Render Slots) — migrated `loop` and `tree` renderer families to the `$slot` frame contract and closed the plan.
- Code paths: `packages/flux-renderers-basic/src/loop.tsx`, `packages/flux-renderers-basic/src/structural-loop.tsx`, `packages/flux-renderers-basic/src/structural-loop-context.tsx`, `packages/flux-renderers-basic/src/recurse.tsx`, `packages/flux-renderers-basic/src/index.tsx`, `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flux-renderers-data/src/index.tsx`
- Key changes:
  - `loop` `body` region: moved from `regions: ['body']` to `fields: [{ key: 'body', kind: 'region', params: ['item', 'index'] }]`. `LoopRenderer` passes `{ bindings: slotBindings, instancePath }` through `region.render()`; no more bare-name scope injection.
  - `StructuralLoopContext`: removed `scope` and `bodyNode` fields; replaced with `renderBody(slotBindings, instancePath): ReactNode` function. `RecurseRenderer` now delegates to `loopContext.renderBody(slotBindings, instancePath)` instead of calling `helpers.render(loopContext.bodyNode, { scope })` directly.
  - `tree` `node` region: moved from `regions: ['node']` to `fields: [{ key: 'node', kind: 'region', params: ['node', 'index', 'depth', 'key', 'parentNode'], isolate: false }]`. `TreeNodeRenderer` replaced `helpers.createScope + helpers.render(templateNode)` with `owner.regions.node.render({ bindings: { node, index, depth, key, parentNode } })`.
  - Tests: loop tests updated from `${item.name}` → `${$slot.item.name}`, recurse tests updated from `${item.label}` / `${item.children}` → `$slot`-prefixed equivalents; tree test updated from `${node.label}:${depth}:${parentNode ? ...}` → `${$slot.node.label}:${$slot.depth}:${$slot.parentNode ? ...}`.
  - `docs/architecture/renderer-runtime.md`: updated `RenderRegionHandle` interface to show `params` field, `render()` as primary API, and `instantiate()` as deprecated; updated Pattern 2 example to use `render()`.
  - Plan 65 closed: all checklist items ticked, all phases marked `completed`, status changed to `completed`.
- Key decision: `recurse` reuses the `slotBindings` already computed by `renderStructuralLoop` (same bindings object the parent loop body was rendered with), because recurse re-enters the parent's body region template with the child item's data.
- Next step: no immediate follow-up required; broader renderer-family adoption (form renderers) is tracked in the Plan 65 Follow-up section.
