# Development Log - 2026-08-16

### 2026-08-16

- **Plan 457（Sundial 复刻页交互补齐）draft review 三轮通过，升级 active**：用户反馈"复刻页点击无反应"→ 全量交互元素分析（A 类已接线 11 处 / B 类静态展示 / C 类 16 个未接线交互点）→ 计划 `docs/plans/457-sundial-replica-interactions-plan.md`（4 Phase：flex/container onClick 渲染器增强 → 按钮/图标补接线 → 行级接线+动态选中态 → 收口验证）。独立子 agent review 三轮（`ses_ff503fdd4ffeNACwlLX0K0NGIY`）：round 1 `revised`（4 Major：双渲染缺初始 scope 值、activeView 变量冲突、日期行清除按钮冒泡、C 类清单遗漏；6 Minor）；round 2 `revised`（1 Major：C11/C12 双渲染同源缺值；4 Minor）；round 3 `pass-with-minors`（零 Blocker/Major）。关键修订：PAGE_DATA 注入 activeSection/activeView/mode/recur/list 初始值、activeSection vs activeView 变量拆分、日期行拆"选择区+清除按钮"两可点 flex、C 类扩至 16 项。Next step：按 plan 457 Phase 1 执行

### 2026-08-16

- **示例应用（Sundial 复刻页）过时 CSS 清理 + 文档过时修订**：
  - `sundial-replica.css`：`.sd-section` 系列从"CSS 覆盖内部 DOM + `::before` 色调条 + `.sd-section-tone-*` 类"重写为**原生 marker 选择器**（`[data-tone='...'] [data-slot='collapse-tone-bar']` 色调条、`[data-slot='collapse-count']` 等宽计数、`[data-tone]` 驱动标题色）；删除 `.sd-section-title-tone-*`/`.sd-section-count`/`::before` hack 全部死类；`.sd-checkbox` 注释明确"原生 shape:'circle' + 宿主 CSS 定制白勾指示器"
  - `sundial-workbench.json`：5 个 collapse 节点 className 去掉 `sd-section-tone-*`（tone 完全由 `item.tone` 语义字段驱动）
  - 过时文档修订：`docs/analysis/sundial-ui-reproduction-analysis.md`（§4.2 矩阵、§5.1、§5.3 重构为"已落地/剩余"、§6 结论、验证清单终版）；flux-guide 4 份（collapse.md 语义 trigger 章节、layout.md 结构级 responsive、chart.md 逐点上色、form-basic-fields.md checkbox shape）+ README 条目
  - 验证：playground 153/153（Sundial 测试全绿）、check:active-doc-code-anchors 330 docs 通过

### 2026-08-16

- **Plan 456 执行完成（responsive 结构断点 + collapse/checkbox/chart 增强，4 Phase 全绿）**：
  - **Phase 1 `responsive` 渲染器**（G7）：`ResponsiveBreakpoint`/`ResponsiveVariantSchema`/`ResponsiveSchema` 上移 flux-core（basic re-export 兼容）；ui 新增 `useBreakpoint(query)`（单 query）+ `useBreakpoints(queries)`（多 query，React-Compiler 友好：effect 依赖用 join key）；`flux-renderers-layout/responsive-renderer.tsx`（preserve-literal unwrap + 变体按序匹配 + 整树切换，body 走 region 机制）；definition + test-support + 5 个 focused 测试（matchMedia 注入，见执行偏差）；route inventory/lab registry/responsive-lab-page 补齐；Sundial workbench 外层包 responsive（默认=桌面树，`max:'lg'` 移动变体），端到端断言变体切换
  - **Phase 2 `collapse` tone/count/leading**（G1）：`CollapseItemSchema` 三字段 + definition fieldRules 接线（leading → leadingRegionKey region）+ renderer marker（`data-tone`/`collapse-tone-bar`/`collapse-count`/`collapse-leading`，未声明时行为不变）+ 2 个测试（含表达式 count）；Sundial workbench 5 个分组改用语义字段（去掉手工 CSS 标题）；styling-system.md 记录 marker 契约
  - **Phase 3 `checkbox` shape**（G2）：`CheckboxSchema.shape` 透传 ui `Checkbox`（`data-shape` 输出）+ definition fields + 2 个测试；Sundial workbench/detail 任务行全部 `shape:'circle'`
  - **Phase 4 `chart` 逐点上色**（G4）：`ChartSeriesSchema.colors`（定长色板按索引循环）/`colorRegionKey`（记录取色）+ `sanitizeSeries` 保留新字段 + bar/scatter 用 recharts `Cell` 逐点着色（key 用数据值避免 index key）+ 3 个测试；Sundial 压力图单 series 化（mock items 加 tone 字段）
  - 执行偏差记录（plan Phase 1 注）：vi.mock('@nop-chaos/ui') 在 layout 包测试链路导致 page.scope undefined，改用 window.matchMedia 注入
  - 验证：`pnpm test` 全仓 0 失败（layout 124、data 870、form 809、playground 153 等）；typecheck/build/lint 37/37；`check:schema-prop-coverage`/`check:i18n-keys`/audit/react19 全绿；`pnpm check` 既有 2 项登记红不变
  - 文档：`docs/architecture/responsive-and-renderer-enhancements.md`（最终设计归档）、quick-reference.md（responsive 条目 + openDialog className/testid）、styling-system.md（collapse marker 契约）
  - Next step：closure audit（独立子 agent）→ plan 456 标 completed

### 2026-08-16

- **Flux 渲染器改进：设计文档 + 执行计划（draft review 通过，plan 456 升级 active）**：
  - 设计文档 `docs/discussions/2026-08-16-flux-renderer-improvements-design.md`：四个改进的最终设计——D1 `responsive` 容器（结构级响应式原语，`useBreakpoint(query): boolean | null` 单 query 契约 + 变体匹配 + 整树切换，含拒绝的替代方案表）、D2 `collapse` tone/count/leading 语义化 trigger（渲染器发 marker）、D3 `checkbox` shape 透传（ui 已支持 circle）、D4 `chart` series `colors`/`colorRegionKey` 逐点上色（recharts Cell，bar 为验收主路径）
  - 执行计划 `docs/plans/456-flux-renderer-improvements-plan.md`（4 Phase，Proof 先于 Fix，Test Strategy=必须自动化，覆盖载体按门禁范围裁定：checkbox.shape 走 schema-prop-coverage（脚本 rendererFiles 硬编码清单含 form），collapse/chart/responsive 不在扫描范围以 focused 测试覆盖）
  - **独立子 agent draft review 两轮**（`ses_ff5a96d77ffebEEEKwydyel74H`）：round 1 `revised`（Major-1：schema-prop-coverage 门禁范围不符实际脚本 + Minor 1-7 全处理）；round 2 `pass-with-minors`（R2-M1/M2 措辞残留、R2-M3 默认变体二选一→方案 A：默认变体=桌面树，移动树显式 max:'lg'）。评审记录已写入 plan `## Draft Review Record`，Plan Status: draft → active
  - 关键事实修正：`check:schema-prop-coverage.mjs:326-343` rendererFiles 为硬编码清单，**不含 flux-renderers-layout 与 data-renderer-definitions.ts**；扩展脚本列为 Deferred 治理项
  - Next step：按 plan 456 Phase 1（responsive 渲染器）开始执行

### 2026-08-16

- **Sundial UI 复刻分析 + 验证页**（新任务，仅 playground + docs，无渲染器改动）：
  - 分析 `~/sources/ui/sundial`（Kotlin Compose Multiplatform 待办应用，160 Kotlin 文件），产出复刻可行性报告 `docs/analysis/sundial-ui-reproduction-analysis.md`：设计令牌提取（品牌橙 #EA7A2A、bgSecondary #F2F2F2、扁平无阴影、8dp/2dp/16dp 圆角体系）、8 个复杂页面识别、能力映射矩阵、7 项 gap（G1 collapse trigger 语义化、G2 checkbox shape 透传、G3 缺日历/日期选择器、G4 chart 逐点上色、G5 hover/选中态 schema 表达、G7 响应式断点）。
  - 验证产物（5 个 schema 页面 + CSS + mock 端点 + 测试，全部零渲染器改动）：
    - `apps/playground/src/complex-pages/page-schemas/sundial-{workbench,analytics,detail,settings,todo-dialog}.json`：复刻工作台（侧边栏 + 压力 rail + 5 个色调条折叠分组 + 圆形 checkbox 任务行）、分析（KPI 卡 + 折线/柱状/多色柱状图 + 图例 + insight）、详情（字段行 + badge + 日期/重复/列表选择对话框）、设置（rail + 同步模式卡 + 状态卡）、新建待办对话框（声明式 dialog + 表单）。
    - `apps/playground/src/sundial-replica/sundial-replica.css`：Sundial 设计令牌 CSS 变量 + 组件类（badge/按钮/圆形 checkbox/折叠分组/压力 rail/时间线/对话框），import 进 `styles.css`。
    - `apps/playground/src/complex-pages/shared/showcase-env.ts`：新增 `Sundial__summary/trend/energy/pressure/outputStructure/lists/todos/todayTasks` mock 端点。
    - `apps/playground/src/complex-pages/complex-pages-model.ts`：新增 `app-replica` 分类 + 5 个条目；`page-data.ts` 注入 checkbox 初始态。
    - `apps/playground/src/complex-pages/sundial-replica.test.tsx`：8 个测试（工作台/分析/详情/设置/对话框渲染 + 对话框打开交互）。
  - 关键发现：
    - 作用域表达式不支持 `?.[0]`（可选索引）—— **真实限制**：parser 的 `?.` 分支强制 identifier（parser.ts:337），`[` 索引分支 `optional: false`（parser.ts:361）；`${pressure?.items?.[0]?.x}` 解析失败后 compileNode 回退 static-node 原样输出。规避：顶层字段 + `?.field`。
    - 文本节点部分插值"逾期${x}"**本来就支持**（compileTemplate 混合 text/expr 段，compile/compile-node.ts:99）—— 最初观察到的"原样输出"实为 `?.[0]` 解析失败导致的整体回退，**并非"必须整个字符串"**（此归因错误已修正）。
    - openDialog 创建的 surface **曾无法传 className/testid**（action-adapter.ts open() 未传 options.meta，dialog-host.tsx:309 只读 surface.meta?.className）；**已修复**（resolveSurfaceMeta 透传，见上）。args 的 `bodyClassName`/`headerClassName`/`footerClassName` 一直有效（dialog-host.tsx:270-271 直接读 surface）。规避路径：声明式 `dialog` 节点 + `open: "${scopeBool}"` + `setValue` 驱动打开（className 经 renderer meta 生效）——两种方式现均可用。
  - **补充测试（锁定上述行为，防止误读/回归）**：
  - **问题 1 修复（`?.[0]` 可选索引支持）**：`flux-formula/src/parser.ts` parsePostfix 的 `?.` 分支新增 `?.[` 计算成员解析（computed: true, optional: true）；求值层（evaluator.ts evaluateMemberTarget）本就支持 optional null 短路，static-eval/symbol-diagnostics 对 computed 安全跳过。测试：parser.test.ts 用例反转（支持 `?.[0]`/`?.[key].name` 链式），template-exec.test.ts 增加 `?.[0]` 混合模板/纯表达式求值 + 缺失索引 null 兜底用例。端到端：sundial-analytics.json legend 表达式改回 `?.[0]` 形式验证。flux-formula 213/213。
  - **问题 2 修复（openDialog/openDrawer 支持 className/testid）**：`flux-runtime/src/action-adapter.ts` 新增 `resolveSurfaceMeta` helper，open() options 传 `meta: { className, testid, cid }`（从 invocation.args 提取，与声明式 dialog 走同一 surface.meta 通道，dialog-host.tsx:309 生效）。测试：schema-renderer-dialog-container.test.tsx 断言反转（className/testid 现在生效）。flux-runtime 1422/1423、flux-react 481/481。
  - 验证：flux-compiler 551/551、playground 152/152、typecheck/lint 全仓通过。
  - **补充测试（锁定上述行为，防止误读/回归）**：
    - `flux-formula/src/parser.test.ts`：`?.[` 支持（computed+optional AST）+ 普通 `items[0]` 索引可用
    - `flux-formula/src/template-exec.test.ts`（新文件，8 例）：部分插值执行级验证（"逾期${count}"→"逾期3"、首尾混合、nullish 段为空串）、`?.[0]` 混合模板 null 兜底、纯表达式/纯文本节点类型锁定
    - `flux-react/__tests__/schema-renderer-dialog-container.test.tsx`：openDialog args className/testid 生效 + bodyClassName 生效（3 例）
    - `flux-renderers-basic/__tests__/surface-enhancements.test.tsx`：声明式 dialog className/testid/bodyClassName 生效（3 例，对照 openDialog）
    - 验证：flux-formula 213/213、flux-react 481/481、flux-renderers-basic 489/489、flux-runtime 1422/1423、flux-compiler 551/551、typecheck/lint 全仓通过
    - G3 复核（二轮）：flux 已有 `input-date`（flux-renderers-form）—— 实为**完整的 popover 月历选择器**（trigger + Calendar single-mode 网格 + 时间数字输入 + 清除，`date-field-control.tsx`）；`input-time` 存在但默认是原生 `<input type="time">`，**无 Sundial ±1h/±5m 步进器**。已修正报告 G3。
  - **`input-time` steppers 模式（renderer 增强，本轮落地）**：`packages/flux-renderers-form/src/renderers/input-time-renderer.tsx` + `schemas.ts` 新增 `steppers`/`hourStep`/`minuteStep` 字段 —— JSON 一行 `"steppers": true` 即启用 Sundial 风格小时/分钟循环步进器（renderer 内部私有 StepperButton，非外部 React 控件，页面整体仍为纯 flux JSON）。修复 2 个实现 bug：分钟步进进位到小时（23:55+5min→00:00）、无值时用本地零点基准（`new Date(0)` 会被时区偏移成 08:00）。测试 +4（input-time.test.tsx，含 schema-prop-coverage 所需字段覆盖）。
  - **flux-guide 文档修复**：`flux-guide/design-patterns/date-fields.md` 重写 —— 原文档属性名全部错误（`format`/`min`/`max`/`showTime` 均非真实 schema 属性，真实为 `valueFormat`/`displayFormat`/`minDate`/`maxDate`/`minTime`/`maxTime`/`clearable`/`utc`），且未说明任何交互形态（input-date 是 popover 月历而非普通输入框）。重写后：真实属性表 + 各字段交互形态说明 + input-time steppers 文档 + 展示示例指向。
  - 复刻页更新：`sundial-detail.json` 日期选择对话框时间行改用真实 `input-time` steppers（±1h/±5m），测试断言 `sundial-time-stepper-display` 与步进点击行为（152/152 通过）。
  - 验证：typecheck/lint 全仓 37/37、form 包测试 807/807、playground 152/152、build 通过、`check:schema-prop-coverage`（新字段已覆盖）+ `check:i18n-keys`（复用既有 `flux.date.hour/minute` key）全绿；`pnpm check` 其余项同前（2 个既有红非本次引入）。
  - 验证：playground `pnpm test` 151/151 通过（含 8 个新测试）；typecheck/lint/build 通过；`pnpm check` 全部通过项通过，`check:oversized-code-files`（wizard-renderer.tsx 716 行）与 `check:audit-event-dispatch-ctx`（industrial 6 处）在干净 master 上同样失败（既有债务，非本次引入）。
  - Next step：按报告 G1–G4 优先级推进 flux 设计改进（日历组件、collapse trigger 语义化、chart 逐点上色）。
