# Development Log - 2026-06-27

### 2026-06-27

- **Re-verification of plan `2026-06-27-0850-3`** (MISSION_DRIVER re-execute): all 4 Phases already `[x]`/`Status: completed`; no unfinished Phase. Re-ran full workspace gates and caught a **real lint break** that invalidated the prior "full-green" claim: `packages/flux-renderers-form-advanced/src/key-value.tsx:4` had an unused `FormRuntime` type import left over from Phase 2 H25 work (`@typescript-eslint/no-unused-vars`). Removed it. Workspace now genuinely full-green: typecheck 55/55, build 29/29, lint 29/29 (0 errors; 1 pre-existing out-of-scope tree-virtualization warning in `tree-option-list.tsx`), test 55/55 (form-advanced 910 tests). Source audits already `closed` (idempotent); roadmap `Last Updated` header already documents the plan; Closure-audit gate `[ ]` correctly left for an independent fresh-session (executor must not self-audit).

- Executed plan `docs/plans/2026-06-27-0850-3-form-runtime-correctness-composite-editors-contract-honesty-async-lifecycle.md` (mission `amis-bug-driven-improvements`, audit-sourced owner plan: H3/H7/H8/H9/H13/H14/H15/H16/H24/H25/H26/H27/H28/H29/H30 + AUDIT-07/12/13/14/15). All 4 Phases completed; plan `Plan Status: completed`. Workspace `typecheck/build/lint/test` full-green.
- **Phase 1 — 契约诚实性生产隔离 (H7/H16)**: flux-core `contract-honesty.ts` 新增 `buildPerRendererSourceResolver`（按 renderer type 解析自身实现文件、传递式 import 闭包含动态 import、排除 sibling renderer 文件与 barrel、运行时 handle factory 按引用选择）+ 导出 `isLifecycleTransitional`。6 个生产 harness（form/form-advanced/basic/data/content/layout）从整包 blob 改为 per-renderer 隔离 resolver，每包加 H7 sibling-隔离 Proof。`isCapabilityHandleReferenced` 数组锚收紧到 `methods`/`listMethods`/`return` 上下文（H16 Proof：`labels:['save']` 不再 incidental 满足）。`types/node-fs.d.ts` ambient `node:path` 补 `relative`。
- **Phase 2 — 复合控件数据丢失/边界/契约对称 (H3/H9/H24/H25/H27/H29/H30)**: condition-builder `BetweenInput` 清一侧保留存活值（emit `[left,right]` 含 undefined 槽，sanitizeRight 保留存活值，H3）；date-range `commitRange` 每侧 clamp 到 `[minDate,maxDate]`（镜像 date-field-control，H9）；key-value `validateChild` 增内联重复 key 反馈（H25）；key-value `handleRemove`/`removeItem` 加 `shouldValidateOn` 门控（与 combo/array-editor/input-table 对称，H27）；`rewriteItemRight` 移至 utils.ts 导出 + DFS 短路防 id 碰撞串值（H29）；condition-builder projected form/scope/validation 按 `item.id`+`right`（deep-equal）缓存 + syncValueRef，消除每 render churn（H30）；input-table 补 `removeWhen` schema 字段 + field 声明 + 逐行 gating + handler 守卫，与 combo/array-field 对称（H24，Decision (a)）。
- **Phase 3 — 树 async 生命周期 (H8/H14/H15/AUDIT-12)**: `tree-controls.tsx` memoize `treeConfig`（deps 收敛到 5 config 原语）+ `baseOptions`（deps `options,treeConfig`）解 debounce churn（H8）；`useTreeLazyChildren.runLoad` 加 `mountedRef`+`generationRef` 守护 + `baseSnapshot` 防 stale-merge/unmount setState（H14）；`useTreeOptionListController` 展开态 effect 改合并策略（`prevExpandableRef` 跟踪，新可展开节点默认展开、保留用户显式折叠、prune 不再可展开键，H15）；`useTreeRemoteSearch` 补 `AbortController` + `executeTreeSource` 透传 `signal` 到 `dispatch`（保留既有 `cancelled` stale-response 守护，AUDIT-12）。
- **Phase 4 — runtime async/验证一致性 (H13/H26/H28/AUDIT-07/14/15/AUDIT-13)**: `api-data-source-controller-runtime.ts` 成功路径在 sequence bump 前镜像 catch 的 `requestSequence < latestSettledRequestSequence` 陈旧守护（H13，governance-absent 防御性补丁；既有 `runtime-sources-dedup` 测试覆盖可观测行为）；upload `removeExisting` 改读 `committedItems()`（`latestValueRef`，与 commitItems 一致，H26）；`validateSubtreeByNode` 签名加 `options`、两处 `validatePath` 透传 signal + 循环前 abort 检查、`validateSubtree` 调用点透传（H28）；`validateForm`/`validateSubtree` 入口加 `isLifecycleTransitional` 门控（model 存在时也等生命周期，AUDIT-14）；`revalidateDependents` 不再预清 `validating`（交给 validateField，AUDIT-15）；AUDIT-07 文档化为 caller 契约（`form-runtime-array.ts` 注释：所有复合编辑器 caller 已 validateField/validateSubtree(name) 且遵守 validateOn，运行时无条件自验会破坏 submit-only 语义）；qrcode AUDIT-13 文档化豁免（`QRCode.toCanvas` 无 signal，canvas 绘制幂等，bare flag 守护错误态）。
- New Proof tests: value-input H3（保留存活值 + both-clear 折叠）、`rewrite-item-right.test.ts`（H29 id 碰撞短路）、`condition-builder-projected-stability.test.tsx`（H30 scope 引用稳定）、date-range H9（time clamp）、form-array-validation H25（内联 dup-key）/H27（key-value remove validateOn:submit）、`h24-input-table-remove-when.test.tsx`（H24 gating+minItems floor）、`tree-async-lifecycle.test.tsx`（H8 debounce-churn/H14 unmount+stale/H15 collapse-preserve/AUDIT-12 signal-abort）、`form-runtime-phase4-lifecycle.test.ts`（H28 subtree abort / AUDIT-14 lifecycle wait）、upload-field H26（remove committed）。flux-core contract-honesty.test.ts 加 H16 incidental-array Proof + 3 个 resolver Proof。6 个生产 harness 各加 H7 sibling-隔离 Proof。
- Owner doc `docs/architecture/form-validation.md` 同步：validateOn 一致性 + validateForm/validateSubtree 入口生命周期门控 + revalidateDependents validating 生命周期归 validateField；`docs/architecture/renderer-runtime.md` 同步：contract-honesty 生产 harness per-renderer 隔离契约 + capability array 锚 methods/listMethods 上下文。
- Source audits `docs/audits/2026-06-26-1859-{open,multi}-audit-amis-bug-driven-improvements.md` already `closed` (idempotent). Roadmap 无 per-finding ❌/✅ row（audit-sourced owner plan；roadmap 记录于 `Last Updated` 日志）。Closure-audit gate 留 `[ ]` 给独立 fresh-session（executor 不自审）。

- Executed plan `docs/plans/2026-06-27-0850-2-flux-renderers-data-correctness-table-crud-a11y-races.md` (mission `amis-bug-driven-improvements`, audit-sourced owner plan: H1/H2/H4/H5/H6/H10/H11/H12/H17/H18/H19/H20/H21/H22/H23 + AUDIT-02/03). All 5 Phases completed; plan `Plan Status: completed`. Workspace `typecheck/build/lint/test` full-green (55/55 test, 29/29 build/lint, 55/55 typecheck). **Full-green verification: unit + e2e (playground vitest) all pass** — flux-renderers-data 79 files / 628 tests; playground 19 files / 88 tests (incl. `performance-table-page` single-row locality diagnostic, 0 sibling re-renders). `pnpm check:i18n-keys` ✅.
- **Phase 1 — 列宽 ownership/生命周期 (H1/H4/H5)**: `use-column-resize.ts` scope 模式实时反馈（transient `draggingWidths` overlay）+ `pointerup` 持久化 `active.next`（拖到宽度，非拖动前）；window 监听器由 React unmount-effect teardown 拥有 + `pointercancel`（H4）；`controlled` 加 `onWidthsChange` 通道 + G10 式 dev 告警（H5，镜像 drag-sort）。收紧既有 scope 持久化弱断言（`expect.any(Number)` → 精确 180）。
- **Phase 2 — drag-sort a11y/诊断/click (H6/H12/H19)**: `use-row-drag-sort.ts` `dragHandleProps` 加 `onKeyDown`（ArrowUp/Down 走 `commitReorder` 同路径）；补 `scope && !statePath` dev 告警（H12）；`onClick` stopPropagation（H19）。抽出 `commitReorder`/`resetDragState` 共享鼠标/键盘 commit。
- **Phase 3 — ownership 矩阵 + race + 行 memo (H20/H21/H11/H10)**: `table-quick-edit-controller.ts` `runSave` 发起时快照 record（`recordSnapshot`），await 后提交快照，record 突变不串记录（H20）；`use-table-selection.ts` `handleSelectAll` 在 `keepOnPageChange` 下对全量已知 key prune 幻影 key（H21）；`useTableExpand` 裁定 local-only + design.md 显式化（H11）；H10 `fixedColumnLayout` 稳定化（memo deps 收敛为 `mainColumns/rowSelection/showExpandColumn`，`createFixedColumnLayout` 首参类型放宽为 `Pick<…,'rowSelection'|'expandable'>`）消除 identity churn → 行不再陈旧渲染；row `React.memo` 保留以守 locality 契约（playground 诊断锚）。
- **Phase 4 — CRUD 类型化委托 + compile-once + defaultQuery (AUDIT-02/03/H18)**: 删除 carrier `key={`${listMode}:…`}` keyed-remount workaround（AUDIT-02），carrier 持续挂载、reactive 更新（item/card 编译一次）；`delegateTableRendererProps` 集中 cast seam（events/regions 升级 single `as`，templateNode/node 收敛为唯一文档化 `as unknown as`，AUDIT-03）；`defaultQuery` 经 `normalizedSchema` memo 稳定 identity + `useCrudRuntimeState` init effect 改 `defaultQueryRef` 且 deps 移除 defaultQuery（仅 mount/path 触发，H18）。
- **Phase 5 — 分页/图表/树/i18n (H2/H22/H17/H23)**: `pagination-renderer.tsx` `total` 改 render-time 派生（H2）；`chart-renderer.tsx` Pie `Cell` key=`name:value` 去同名碰撞 + source-array `Number.isFinite` 守护（H22）；`tree-renderer.tsx` deferred 改增量 chunking（每 tick `prev+BATCH` 并重武装，H17）；`table-loading-overlay.tsx`/`table-cell-chrome.tsx` 硬编码英文走 `t('flux.table.loading/copy/copied')` + 两 locale 补 key（H23）。
- New Proof tests: `table-e1c-column-widths-persistence`（H1/H4/H5 + 收紧断言）、`table-e1c-row-drag-sort-keyboard-a11y`（H6/H12/H19）、`table-quick-edit-record-swap`（H20）、`table-selection-phantom-prune`（H21）、`data-pagination-rendering` H2 case、`crud-list-mode` AUDIT-02 no-remount case。更新 `crud-selection-drift-hook`（H21 全量数据集模型）、`crud-item-card-compile-contract`（C-03 keyed-remount removed）。
- Owner doc `docs/components/table/design.md` 同步：§7 列宽 ownership（scope 拖到宽度 + 实时反馈 / controlled `onWidthsChange` + G10 / window 监听器生命周期）、行排序 ownership（键盘 a11y + click stopPropagation + scope 诊断）、expand local-only 裁定、T10 H21 phantom prune。
- Source audits `docs/audits/2026-06-26-1859-{open,multi}-audit-amis-bug-driven-improvements.md` already `closed` (idempotent). Roadmap has no per-finding ❌/✅ row for this plan (audit-sourced; B1–B7 roadmap is a separate completed set). 树真虚拟化 successor retained in `Deferred But Adjudicated`. Closure-audit gate left `[ ]` for an independent fresh-session per policy (executor must not self-audit).

- Executed plan `docs/plans/2026-06-27-0850-1-workspace-health-ci-gate-green-and-convention-cleanup.md` (mission `amis-bug-driven-improvements`, governance/hygiene plan sourced from AUDIT-01/04/05/06/08/09/10/11/16/17/18/19/20/21/22 + H31/H32/H33/H34/H35). All 5 Phases completed; plan `Plan Status: completed`. Workspace `typecheck/build/lint/test` full-green (55/55 typecheck, 29/29 build, 29/29 lint, 55/55 test).
- **Phase 1 — CI gate-green (oversized files)**: split 4 test files (grid-selection → 3, chart-renderer → 2, runtime-sources-refresh → 2, infinite-scroll → 2) under 700 lines; extracted `createPageStore`/`createSurfaceStore` from `form-store.ts` to `page-store.ts`/`surface-store.ts` (635 → 630 lines); added **explicit opt-in exemption** mechanism to `check-oversized-code-files.mjs` with documented decisions for `form-runtime-owner.ts` and `node-compiler.ts` (single-orchestrator cohesion). `pnpm check:oversized-code-files` now exit 0 (2 exempt, 0 non-exempt errors).
- **Phase 2 — /unstable convergence + manifest deps**: removed 14 stable-barrel-duplicate symbols from `flux-react/src/unstable.ts` (stable ∩ unstable = ∅, locked by new `public-surface.test.ts` disjointness assertion); migrated 6 test file imports from `/unstable` to stable barrel; removed 3 dead `vi.mock('/unstable')` blocks; promoted `flux-runtime` to `flux-renderers-form-advanced` production dep (`projected-scope.ts` re-export from canonical path); moved `flux-renderers-form` to `flux-code-editor` dependencies (runtime `formFieldChromeRules` value import).
- **Phase 3 — quick-reference.md resync**: added 3 missing renderer packages (content/layout/mobile); fixed `useCurrentFormError`/`useFieldError` return types (`ValidationError | undefined`, not array); fixed `useRenderInstancePath` return type (`readonly InstanceFrame[] | undefined`); fixed bundle npm name (`@nop-chaos/flux`, not `@nop-chaos/flux-bundle`).
- **Phase 4 — hygiene/convention/mobile**: AUDIT-16 dedup map `Promise<ApiResponse<any>>` → `Promise<ApiResponse<unknown>>`; AUDIT-17 `as any` → typed `ctx.scope.get('$designer')`; AUDIT-18 coverage thresholds for flow-designer-core/spreadsheet-core (baseline levels, successor to raise to 70%); AUDIT-19 removed 145 redundant `@vitest-environment happy-dom` pragmas; AUDIT-20 adjudicated out-of-scope (dev app); AUDIT-11 watch-only (SelectRenderer ~314 lines < 450 threshold); H31 notice-bar ResizeObserver for container-width re-measurement; H32 removed hardcoded `isMobile && 'flex flex-col'` from page.tsx (layout-renderer marker-only contract); H33 removed 3 event-handler `useCallback`s + 1 derived `useMemo` (compiler-redundant), kept effect-dep-coupled memos (vitest doesn't run compiler yet).
- **Phase 5 — interesting-guess adjudication**: H34 (reaction-runtime TDZ) — **no TDZ risk**, forward-reference safe via microtask invariant, added guard comment; H35 (action-core cancelled failure-class) — **intended + tested**, added design-decision comment.
- Source audits `docs/audits/2026-06-26-1859-{open,multi}-audit-amis-bug-driven-improvements.md` → `closed`. Roadmap has no per-finding ❌/✅ row for this plan (audit-sourced governance plan, not a roadmap wave item). 4 items adjudicated in Deferred But Adjudicated (AUDIT-20 out-of-scope, AUDIT-11 watch-only, AUDIT-18 optimization candidate, H33 optimization candidate).
- CI gates: `check:oversized-code-files` exit 0 (2 exempt), `check:workspace-manifest-deps` PASS, `check:active-doc-code-anchors` PASS.

- Executed plan `docs/plans/2026-06-27-1030-3-composite-field-data-lifecycle-form-control-correctness-plan.md` (mission `amis-bug-driven-improvements`, findings M-03/M-05/G9/G18/G5/G12/G17/G1/G11/M-07). All 3 Phases (A/B/C) completed; plan `Plan Status: completed`. Workspace `typecheck/build/lint/test` (unit) full-green; e2e not run this session.
- **Phase A — composite-field** (`packages/flux-renderers-form-advanced/src/`):
  - **M-03**: `combo` + `array-field` `removeWhen` field rule `kind:'ignored'` → `kind:'prop', lazyEval:true, params:['record','index','value']`; renderers read the compiled handle from `templateNode.structuralFields.removeWhen` and evaluate via `helpers.evaluateCompiled` (no per-item recompile). `remove-when-gating.ts` switched to a compiled-handle contract + `isRemoveWhenConfigured` (preserves the legacy empty/whitespace guard via static-value detection). No raw `props.schema.removeWhen` reads remain.
  - **M-05**: extracted scalar validation helpers to new `composite-field/array-field-scalar-validation.ts`; `array-field.tsx` 722 → 590 lines (under the 700 hard limit; dropped out of `check:oversized-code-files` ERROR).
  - **G9**: `childPaths`/`scalarChildPaths` derived from `items.length`+`name`; array-field registration effects rerouted to read `itemsRef.current` (no stale values); `items` removed from effect deps → no per-keystroke re-register.
  - **G18**: new shared `composite-field/composite-item-keys.ts` (`useCompatibilityItemKeys` + `buildStableObjectItemKeys`) generalized the scalar `compatibilityItemKeys` reference solution to object-kind array-field, combo, input-table, array-editor, key-value — stable ids travel with items across structural edits (remove filters, move reorders, append mints). `key-value-normalizer.ts` extracted to keep `key-value.tsx` under 700.
- **Phase B — data lifecycle** (`packages/flux-renderers-data/src/`):
  - **G5**: `use-infinite-scroll.ts` now drives `loading`/`error` from the promise returned by `onLoadMore` (list/crud `handleLoadMore`/`handleRefresh` return the dispatch promise), adds a `loadingRef` concurrent-fetch guard, treats a resolved `{ok:false}` ActionResult (or rejection) as a load error, and auto-continues when the sentinel stays in viewport (real `getBoundingClientRect` geometry — no-op in jsdom/happy-dom so existing tests stay single-fire).
  - **G12**: standalone `list-renderer` derives `effectiveSelectedKeys` at render time (evicts keys absent from the current data set) — stale keys no longer leak into `onSelectionChange` or the rendered `selected` state.
  - **G17**: `tree-renderer` adds an effect that, when the active node disappears and DOM focus has fallen to `<body>`, moves focus back onto the resolved active tree item.
- **Phase C — form controls**:
  - **G1** (`picker-renderer.tsx`): single-select `openDialog` seeds `pending` with the current value (RadioGroup preselects); `confirmSelection` keeps the existing value when nothing is pending; Confirm disabled on empty single-select; fixed the contradictory inline comment.
  - **G11** (`upload-field.tsx`): per-upload `AbortController` (cancelled on unmount + per-item Cancel button); `mountedRef` guard prevents any late `onChange`/`setItems` into an unmounted field; dispatch receives `signal`.
  - **M-07** (`period-renderers.tsx` / `date-renderer-definitions.ts`): removed `resolveKind(schemaType)` runtime switch; split into three thin wrappers (`Month/Quarter/YearPeriodRenderer`) that supply the kind directly — each renderer type maps to its own component.
- New focused regression tests: `g9-g18-composite-stability.test.tsx`, `g1-g11-picker-upload.test.tsx`, `g5-g12-g17-data-lifecycle.test.tsx`, `m07-period-dispatch.test.tsx`.
- Source audits `docs/audits/2026-06-26-1859-{open,multi}-audit-amis-bug-driven-improvements.md` already `closed` (idempotent). Roadmap has no per-finding ❌/✅ row for this plan (findings tracked in the audits + this plan). G13 (tree single-fat-node expand freeze) adjudicated `optimization candidate`, deferred with successor (not an in-scope live defect).
- Verification: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 29/29 + `pnpm check:i18n-keys` ✅ (added missing `flux.form.cancel` key to en-US/zh-CN after independent closure-audit flagged it), `pnpm test` unit full-green (form-advanced 894 / form 549 / data 612). Independent fresh-session closure-audit (`ses_0f9e9af8bffe6qV4KvGiONZuXF`): PASS-WITH-MINORS → minor remediated → all 10 findings ✓; closure-audit gate ticked.

`(audit F1/F2/F6 remediation on carousel). All 3 Phases completed; workspace`typecheck/build/lint/test` full-green.

- **F1 (layered pause)** — `packages/flux-renderers-content/src/carousel.tsx` auto-advance effect: replaced the shared `let paused` (clobbered by `resume()`) with independent `hovered` / `focused` / `visible` flags; tick derives `paused = hovered || focused || !visible`. An interleaved `mouseleave` no longer resumes an offscreen carousel (WCAG 2.2.2).
- **F2 (reactive reduced-motion)** — subscribe to `matchMedia('(prefers-reduced-motion: reduce)')` `change`; `clearInterval`/restart on change (zero tick latency). No more "read once + early-return forever".
- **F6 (React19 memo hygiene)** — removed hand-written `useMemo` for the component handle AND the no-deps ref-mirror effect. Mechanism note: plan's literal "render-time latest-ref + Compiler memoization" is blocked by the repo's own `react-hooks/refs` + `exhaustive-deps` (error) lint rules, so used the lint-recommended equivalents (handle constructed inside the register effect, like `data-source-renderer.tsx`; `activeIndexRef` updated in the embla select callback). Exit criteria (useMemo gone, mirror effect gone, handle stable across slides, fresh `getDebugData`) all met; deviation documented in the plan's Phase 3 executor note.
- New regression tests: `packages/flux-renderers-content/src/carousel-autoplay.test.tsx` (8 cases — pause-source interleaving for hover/focus/offscreen, reduced-motion bidirectional + at-mount, no-autoPlay, and a runtime O-03 no-churn test). Mocks `@nop-chaos/ui` to inject a fake `CarouselApi`; stubs `matchMedia` / `IntersectionObserver` / `setInterval`. Updated existing `carousel.test.tsx` O-03 source-grep for the removed `useMemo`.
- Owner doc: added WCAG 2.2.2 auto-advance contract (§13) to `docs/components/carousel/design.md`.
- Closed source audit `docs/audits/2026-06-26-1859-open-audit-amis-bug-driven-improvements.md` (`planned` → `closed`).
- Verification: `pnpm typecheck` 55/55, `pnpm build` (content) PASS, `pnpm lint` 29/29, `pnpm test` 55/55 (content pkg 24 files / 179 tests).

- Executed plan `docs/plans/2026-06-27-0007-2-response-adaptor-error-path-contract-plan.md` (audit F3 remediation: `responseAdaptor` error-path throw-case contract). All 3 Phases completed; closure-audit by independent fresh-session sub-agent PASS; workspace `typecheck/build/lint/test` full-green.
- **Phase 1 — Decision (a)**: on the `!response.ok` branch wrap `applyResponseAdaptor` in try/catch and fall back to raw `response.data` on throw, so `readResponseErrorMessage` still surfaces backend `message`/`msg`. Rejected (b) status-defensive requirement (breaks existing success-only adaptors; not lint-enforceable) and (c) success-only adaptor (undoes the mission's A1 contract). Decision + rejection rationale recorded in the plan.
- **Phase 2 — Proof-then-Fix**: wrote failing-first regression tests first (`packages/flux-runtime/src/__tests__/request-runtime-response-adaptor-non-ok.test.ts`, +2 throw-case tests using `payload.data.items` / `payload.records[0]` on an undefined member — the flux-formula engine throws "Cannot access member of null or undefined"; asserts backend `msg`/`message` surfaces + `status`/`response`/`responseData` preserved). Then restructured `executeApiSchema` in `packages/flux-runtime/src/async-data/request-runtime.ts:402-440`: split the adaptor call — error branch now try/catch-falls-back to raw payload then builds the error via `createApiResponseError`; success branch keeps the unconditional adaptor call (unchanged). Tests turned green; original 3 clean-return non-ok tests unmodified.
- **Phase 3 — Owner doc**: synced the final (current-design-state) contract into `docs/architecture/api-data-source.md` A1 section + pipeline steps 6/7 (adaptor runs on both OK/non-OK; on non-OK throw the runtime falls back to raw payload; adaptor not required to be status-defensive; success branch still propagates throws).
- Roadmap: `docs/components/amis-bug-driven-improvement-roadmap.md` `> Last Updated:` note appended (F3 follow-up收口; B2.1 4xx-adaptor reachability 本体早已 `done`). Source audit `docs/audits/2026-06-26-1859-open-audit-amis-bug-driven-improvements.md` already `closed` (idempotent).
- Verification: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 29/29, `pnpm test` 55/55 (flux-runtime 102 files / 1254 tests +1 skipped).

- Executed plan `docs/plans/2026-06-27-0007-3-async-lifecycle-abort-and-inflight-race-hardening-plan.md` (audit F4/F5 remediation: two pre-existing async/in-flight lifecycle races in mission-refactored paths). All 3 Phases completed; closure-audit by independent fresh-session sub-agent `ses_0faf8b99bffeY6unN7VKzI0JoD` pass-with-minors (0 blocker / 0 major); workspace `typecheck/build/lint/test` full-green.
- **Phase 1 — F4 `dynamic-renderer` per-invocation abort** (`packages/flux-renderers-basic/src/dynamic-renderer.tsx`): `run()` is reachable >1× per effect lifecycle (initial autoload + exposed `refresh` capability), so the shared `let controller` made the post-await guards unreliable — a later run reassigns `controller`, and an aborted earlier run read the still-active newer controller post-`await`, surfacing a spurious error / stale schema. Fix: `const myController = new AbortController(); controller = myController;`; dispatch passes `myController.signal`; both post-await guards (success `:135` + catch `:164`) check `myController.signal.aborted`. Shared `controller` still drives the abort closure + effect cleanup (no regression to stale-clear / loadActionKey-change teardown). Proof-then-Fix: `packages/flux-renderers-basic/src/__tests__/dynamic-renderer-refresh-race.test.tsx` — refresh-while-loading with a signal-aware fetcher; asserts no `[data-error]` false error and fresh schema wins (confirmed fails on pre-fix catch branch, green after). Note: discovered the request runtime abort-couples the parent signal to the fetcher (`request-runtime.ts:499-509`), so the bug reliably manifests in the catch branch — the test's discriminator is the `[data-error]`-null `waitFor`.
- **Phase 2 — F5 `FormRenderer` in-flight guard cleanup** (`packages/flux-renderers-form/src/renderers/form.tsx`): init-effect cleanup aborted the controller but did NOT clear the `inFlightInitKeyRef` concurrency marker; refs outlive the effect body, so a mid-init change to a NON-activation dep (e.g. `initAction` identity) stranded the marker and the next effect body bailed at `:320`, silently dropping init for that activationKey. Fix: inside the existing `initActionAbortRef.current === controller` cleanup block, clear `inFlightInitKeyRef` when `=== activationKey`; AND gate the `.finally` marker-clear on controller identity (`&& initActionAbortRef.current === controller`) so a stale aborted promise cannot clear a fresh re-run's marker (draft-review minor incorporated). Success-path (`lastInitKeyRef`) semantics unchanged; success / activationKey-change / unmount paths re-traced. Proof-then-Fix: `packages/flux-renderers-form/src/__tests__/form-init-inflight-race.test.tsx` — in-flight (never-resolving, abort-rejecting) initAction #1, then rerender changing ONLY initAction identity (stable activationKey via empty instancePath); asserts #2 (and a 2nd test: #3) re-invoked (confirmed fails on pre-fix, green after).
- **Phase 3 — pattern note + recurrence surface**: owner doc for the effect-lifecycle/abort pattern is `docs/architecture/performance-design-requirements.md` P5 (carries the AbortController-in-useEffect + "two cancellation layers" rules), NOT `renderer-runtime.md` (which carries runtime-owned teardown only). Added two rules to P5: (1) a `run()` reachable >1× per effect lifecycle must capture per-invocation controller + check it post-await; (2) an in-flight guard ref must be cleared in the same cleanup that aborts it, and any promise-settlement marker-clear must be controller-identity-guarded. source/crud same-class race recorded as watch-only residual in the plan's `## Deferred But Adjudicated` (no successor unless a future audit confirms).
- Roadmap: `docs/components/amis-bug-driven-improvement-roadmap.md` `> Last Updated:` note prepended (F4+F5 follow-up收口; B2 async/in-flight 主题 pre-existing 残留，B2.1/B2.2 本体早已 `done`). Source audit `docs/audits/2026-06-26-1859-open-audit-amis-bug-driven-improvements.md` already `closed` (idempotent).
- Verification: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 0 errors (1 pre-existing non-mission `useVirtualizer` warning), `pnpm test` 55/55 (flux-renderers-basic 39 files / 409 tests; flux-renderers-form 59 files / 547 tests).

- Ran the **dimension-driven** companion audit to the (now-closed) open-ended pass, written to `docs/audits/2026-06-26-1859-multi-audit-amis-bug-driven-improvements.md` (`Status: open`). Method: main-agent tool baselines (`check:oversized-code-files`/`workspace-manifest-deps`/`audit-runtime-raw-schema-reads`/`audit-missing-renderer-markers`/`audit-styling-suspects`/`audit-reactive-render-reads`/`audit-performance-suspects`/`audit-async-failure-paths`/`audit-react19-optimization-candidates`) → 3 parallel iterative deep-dive sub-agents (dims 04/05/06/08/19 · 02/03/09/12 · 13/14/15/16/18) → 1 independent review sub-agent (live-code re-verify per candidate) → main-agent fact-check on the `requiredRange`-wiring contradiction. Scope `packages/` over diff `0b80da30^...HEAD`, F1–F6 referenced not re-reported.
- **Result: Issues found — 0 P0, 2 P1, 3 P2, 5 P3 retained; 4 rejected.** The two P1 are both mission-introduced regressions of just-anchored contracts: **M-01** table `onSelectionChange` emits phantom keys for deleted rows (B3 P1-7 pruned the _display_ but not the _event payload_ — `use-table-selection.ts:236` reads unpruned `localSelectedRowKeys` in the default `'local'` branch; `table-selection-invariants.test.tsx` asserts display only); **M-02** `form-validation.md:706` "exactly these kind values" omits the mission-added `requiredRange` kind (validator+message+i18n+wiring all shipped via `input.tsx:466`/`date-renderer-definitions.ts:110` — confirmed NOT unwired). P2: **M-03** `removeWhen` `kind:'ignored'`+raw read forces per-item recompile (should use `lazyEval`); **M-04** new `table-expanded-row.tsx:74-80` reads `record[column.name]` not `getIn` (regresses B3.1/T6 dotted-column in responsive-expand); **M-05** `array-field.tsx` 722 lines (>700 hard rule) with extractable validation helpers. P3: M-06 dedup cache-hit ignores pre-aborted caller, M-07 period hardcoded type-dispatch (pre-existing), M-08 responseAdaptor bare `catch {}` on error throw (F3 residual — no logging/cause), M-09 V18 convergence claim unfulfilled in `applyChangesAndRevalidate`, M-10 new `useMemo` without `eslint-disable react-compiler`. Rejected: `requiredRange`-unwired claim (false positive — verified wired), bare-delimiter `','` gap (unreachable — `date-range-renderer` coerces both-empty to `undefined`), test-setup-bloat (pure style). Status update: prior **C-01 closed** by this mission (`projected-scope.ts:1` now via `flux-react/unstable`).
- This is a read-only analysis pass (no code change); no verification gate required beyond confirming HEAD `77bd50b6` matches the open-audit's snapshot (green state already recorded above).

- Executed plan `docs/plans/2026-06-27-1030-1-table-selection-drag-dotted-path-render-equivalence-convergence-plan.md` (audit-sourced remediation bundle for the table-renderer cluster findings M-01/G2/G6/G7/G8/G10/M-04/M-10 of the 2026-06-26-1859 round). All 5 Phases completed; closure-audit by independent fresh-session general sub-agent `ses_0fa792c55ffeh2FSzL9HkvHcLh` pass-with-minors (0 blocker / 0 major); workspace `typecheck/build/lint/test` full-green.
- **Phase 1 — selection 三通道等价 (M-01/G7)** (`packages/flux-renderers-data/src/table-renderer/use-table-selection.ts`): `handleSelectRow` 的 `baseSet` 改取 render-time pruner 干净化后的 `selectedRowKeys`（原先 `'local'` 分支读脏的 `localSelectedRowKeys`，删行后勾选存活行会把幻影 key 回灌进 `onSelectionChange` payload）。`localSelectedRowKeys` 从该 callback 依赖移除。display 端 render-time prune（P1-7）保留。Proof-then-Fix：`__tests__/table-selection-invariants.test.tsx` +2（删行后 payload 无幻影 key；净化后 `selectedRowKeys` 引用跨渲染稳定——SelectionProbe 每渲染重建 `rows` 使 `currentRowKeySet` churn，仅干净回写能稳定 identity）。既有 P1-7 / keepOnPageChange 断言不削弱。
- **Phase 2 — tree 子行可选 (G2)** (`packages/flux-renderers-data/src/table-renderer.tsx`): `useTableTree` 上提至 `useTableSelection` 之前，selection 接收 `treeFlattenedData`（扁平行集，含展开子行）而非顶层 `filteredData`。`currentRowKeySet`/`handleSelectAll`/`allSelected` 遂覆盖真实嵌套子行。非树表 no-op（`useTableTree` 原样回传输入）。Proof-then-Fix：新文件 `__tests__/table-tree-selection-child-selectable.test.tsx` +2（经真实 schema renderer + happy-dom：展开父行→勾选子行→`data-checked` 持久；select-all 覆盖扁平子行）。子行为真正嵌套（`rowChildrenField:'children'`，`[data-level="1"]`），非 source 顶层行。既有 no-cascade 测试不削弱。
- **Phase 3 — controlled 拖拽可观测 (G10)** (`packages/flux-renderers-data/src/table-renderer/use-row-drag-sort.ts`): 裁定保留真正受控语义（不在本地持久落序；`onReorder?.()` 通知父端）+ 当 `enabled && ownership==='controlled' && !onReorder` 时发 dev 告警（与缺 `orderField` 同通道），原先的静默空操作变为可观测。退出标准 OR 语义由告警满足。Proof-then-Fix：`__tests__/table-row-drag-sort-persist.test.tsx` +2（缺 onReorder 时告警发出；有 onReorder 时不告警）。既有 controlled-ownership 回归测试（自然序保留 + onReorder 触发 + 不写 scope）不削弱。
- **Phase 4 — 点号 + 列等价 (M-04/G6/G8)**: M-04 `table-expanded-row.tsx` 响应式隐藏列值 `record[column.name]`→`getIn(record, column.name)`；G8 `chart-renderer.tsx` pie/scatter/sr-only-summary 手建查找 `item[key]`→`getIn(item, key)`（cartesian `dataKey` 仍交 recharts 解析，不变）；G6 `table-flattened-items.ts` `areColumnsRenderEquivalent` 新增比较 `quickEdit`/`quickEditBodyRegionKey`/`copyable`（`MemoizedDataRow` 闸门）。Proof-then-Fix：`__tests__/table-dotted-column-paths.test.tsx` +2、`__tests__/chart-renderer.unit.test.tsx` +3、`__tests__/table-data-and-layout.test.tsx` +5（均先确认失败再转绿）。
- **Phase 5 — React Compiler memo 约定 (M-10)**: 本计划改动未新增任何 `useMemo`/`useCallback`；两目标文件的派生 memo 经 react-compiler 规则实跑（探针确认规则激活）未被标记，`pnpm lint` 全绿。裁定保留（`selectedRowKeys` 是 G7 identity-稳定的承重 memo；删除会使 Proof 倒退；不可加 escape 注——会变 unused directive 触发 `reportUnusedDisableDirectives`）。详见 plan `## Deferred But Adjudicated`。
- Owner doc: `docs/components/table/design.md` §7 新增“选择 / 拖拽 / 点号 等价收敛（2026-06-27）”小节，文档化 M-01/G7/G2/M-04/G6/G10 收口契约（对既有 ownership/T6/T10 契约的完成性收口，非语义改写）。
- Source audits closed (idempotent): `docs/audits/2026-06-26-1859-open-audit-amis-bug-driven-improvements.md` 与 `docs/audits/2026-06-26-1859-multi-audit-amis-bug-driven-improvements.md` `planned`→`closed`。本计划仅覆盖 table-renderer 簇 findings；其余 findings（M-02/M-03/M-05…）归姊妹 plan 1030-2/1030-3，下一轮 OPEN_AUDIT 会按需 re-discover。
- Verification: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 0 errors（1 条 pre-existing 与本 mission 无关的 `useVirtualizer` warning）, `pnpm test` 55/55（flux-renderers-data 74 files / 607 tests，+16 回归测试）。

- Executed plan `docs/plans/2026-06-27-1030-2-validation-i18n-diagnostics-contract-honesty-anchor-plan.md` (audit-sourced remediation bundle for the validation/i18n/diagnostics/contract-honesty anchor findings G3/G4/G14/G15/M-02/M-06/M-08/M-09 of the 2026-06-26-1859 round; G16 rejected as false-positive — live key is `flux.common.queryFailed`, already consumed at `crud-renderer.tsx:420`). All 5 Phases completed; closure-audit by independent fresh-session general sub-agent `ses_0fa4f062affekogSMQyZ93Pe6S` `approved` (0 defect); workspace `typecheck/build/lint/test` full-green.
- **Phase 1 — 校验消息一致性 + requiredRange 完整化 (G3/G14/M-02)** (`packages/flux-runtime/src/validation/message.ts`, `flux-i18n locales`, `form-validation.md`): G3 — `required`/`minLength`/`maxLength` 三 case 加 `rule.message ??` 守卫（与同族 minItems/maxItems/requiredRange 一致，原先最高频的 required 反而静默丢弃自定义 message）。G14 — `requiredRange` 改用专属 `validation.requiredRange` i18n key（en `{{label}} requires both ends of the range` / zh `{{label}}需要填写完整的范围`，两 locale 占位一致），消除「部分区间触发却读 generic is required」的误导；`check-i18n-keys` 门禁通过且 key 被消费。M-02 — `form-validation.md:706`「exactly these」kind 列表补 `requiredRange`，与 live `ValidationRule` union（17 kind）完全一致 + 一行区间感知说明。Proof-then-Fix：`validation-message.test.ts` +4（required/minLength/maxLength 自定义 message + requiredRange 专属 key，先失败再转绿）；`date-range.test.tsx` 部分区间断言更新为 range-aware 文案。
- **Phase 2 — 依赖再校验失败路由收敛 (M-09)** (`packages/flux-runtime/src/form-runtime-owner.ts`): `applyChangesAndRevalidate` 的裸 `await revalidateDependents(path, reason)` 收敛为 try/catch——非 Abort 失败经 `reportDependentRevalidationFailure` 诊断缝上报 + 表单级 error（`sourceKind:'form'`），Abort 重抛；不再 bare reject。三条依赖再校验入口（`validateForm`/`validateFormPath`、`executeSetValues` 经 `attachDependentRevalidationFailureHandler`、`applyChangesAndRevalidate`）现已全部收敛于同一诊断缝；代码收敛注释（V18/M-09）与 `form-validation.md:283` 如实列出三条入口。Proof-then-Fix：`form-validation-resilience.test.ts` +1（依赖字段 validateRule 抛错 → `reportFailure` 被调 + resolve（非 reject）+ 表单级 error，先失败再转绿）。
- **Phase 3 — responseAdaptor 错误诊断对称化 (M-08)** (`packages/flux-runtime/src/async-data/request-runtime.ts`): `!response.ok` 分支的 `applyResponseAdaptor` catch 从完全裸 `catch {}` 改为结构化诊断——`env.monitor?.onError?.({ phase:'api', error, details:{ url, status, adaptor:'responseAdaptor' } })` + `console.warn`，并保留既有 `errorPayload = response.data` raw-body fallback（后端 message 仍经 `createApiResponseError` 展示）。与成功路径（无 catch、直传）的静默不对称消除。Proof-then-Fix：`request-runtime-response-adaptor-non-ok.test.ts` +1（adaptor 抛错时 `onError` 被调 + 后端 msg 保留，先失败再转绿）。
- **Phase 4 — dedup/cache 已-aborted 边 (M-06)** (`packages/flux-runtime/src/runtime-action-helpers.ts`): schema-fetch cache-hit 路径（`sharing.apiCache.get` 命中）追加 `signal?.aborted` 检查 → 返回 `createCancelledResult(signal.reason)`；共享 fetch 仍只用 `registrySignal`（单订阅 abort 不取消共享 fetch 的文档化意图不变），唯一 residual（已-aborted caller 命中 cache 仍收 `{ok:true}`）消除。Proof-then-Fix：`request-runtime-schema-fetch-dedup-cache.test.ts` +1——因 dispatcher（`action-execution.ts:522`）对已-aborted signal 上游短路，该 residual 直接对 `executeRuntimeAjaxAction` 注入 primed cache + aborted signal 验证（断言 `cancelled===true`/`error===reason`/无新 fetch，先失败再转绿）。deferred：合并 caller+registry signal 属 optimization candidate（无证据调用方需单订阅 await reject），无 successor。
- **Phase 5 — 契约扫描器锚定与粒度 (G4+G15，强耦合同 phase)** (`packages/flux-core/src/contract-honesty.ts`): G4 — `isCapabilityHandleReferenced` 从近似 no-op 的 `['"]<h>['"]`（对任何带引号出现都 true，common-word 句柄如 `'submit'` 被 action type / i18n key / error msg 误判）锚定到真实 ComponentHandle 接线上下文：`case '<h>':` / `===`/`==` 比较 / method 数组字面量元素（`['<h>',` / `,'<h>']`），使其与 JSDoc 声称一致。G15 — `findUnreferencedContracts` 新增 per-definition 源 resolver（`ContractHonestySourceResolver`：`string | (def) => { componentSource, capabilityHandleSource? }`），event key 与 capability handle 都不再用整包拼接 blob 检查；新增 `stripComments`（`/* */` / 全行 `//` / JSDoc `*` 行）使注释不算引用。form 包 capability 检查从 `${formSource}\n${runtimeSource}` 拼接 blob 改为 per-definition `() => ({ componentSource: formSource, capabilityHandleSource: runtimeSource })`（句柄实际接在 runtime 工厂），消除 sibling 包对 `'submit'` 等的掩盖。Proof（新文件 `packages/flux-core/src/contract-honesty.test.ts`，8 例）：incidental 带引号字面量（action type / i18n key / error msg / label）不算引用；case/`===`/数组元素算引用；移除真实句柄实现（factory 删 `submit`）可被检出（per-renderer 源）；sibling 渲染器使用不掩盖本渲染器缺失；注释-only 引用不算；既有真负用例保留。既有 6 个 renderer 包 contract-honesty.test 全绿。
- Owner doc: `docs/architecture/form-validation.md`（M-02 kind 列表 + M-09 三入口收敛注释）；契约扫描器行为变更未引入新 owner doc（contract-honesty.ts JSDoc 自身即契约说明）。
- Roadmap: `docs/components/amis-bug-driven-improvement-roadmap.md` `> Last Updated:` 前置本 bundle 收口条目（本 plan 是 audit-remediation bundle，roadmap 工作项 B1.1–B7 均早已 `done`，无 ❌→✅ 可翻）。Source audits `docs/audits/2026-06-26-1859-open-audit-*.md` 与 `multi-audit-*.md` 均 already `closed`（idempotent）。
- Verification: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 0 errors（1 pre-existing 与本 mission 无关的 `useVirtualizer` warning）, `pnpm test` 55/55（flux-core +9 tests 含 8 contract-honesty Proof；flux-runtime 102 files / 1260 tests +1 skipped；flux-renderers-form 59 files / 547 tests）。
