# Development Log — 2026-04-19

### 2026-04-19

- Finished the active `ApiObject` -> `ApiSchema` cleanup that had been left mid-migration after the compatibility alias removal in `flux-core`. The remaining breakage was limited to active test annotations in `flux-runtime` and `flux-renderers-form-advanced`, so this pass only updated those type references and left runtime behavior unchanged.
- Re-established green workspace verification for the migration: `pnpm typecheck` passed, `pnpm build` passed, targeted `flux-runtime` request/action tests passed, and `src/__tests__/form-source-options.test.tsx` passed in `flux-renderers-form-advanced`.
- `pnpm lint` is still not globally clean, but the remaining failures are unrelated filename-case debt in `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}` and `packages/flux-renderers-form-advanced/src/condition-builder/{ConditionBuilder.tsx,ConditionGroup.tsx,ConditionItem.tsx,FieldSelect.tsx,OperatorSelect.tsx,ValueInput.tsx}` rather than fallout from the `ApiSchema` migration.
- Link to code paths: `packages/flux-runtime/src/__tests__/{request-runtime.test.ts,runtime-ajax.test.ts,runtime-actions-advanced.test.ts,runtime-actions-chained.test.ts,runtime-actions-monitor.test.ts,runtime-actions-submit.test.ts}`, `packages/flux-renderers-form-advanced/src/__tests__/form-source-options.test.tsx`.
- Key decision: historical/archive references to `ApiObject` can remain historical; active source and active tests now use `ApiSchema` as the maintained baseline.

### 2026-04-19

- Completed the two follow-up cleanup tracks that were left open after the audit slices.
- Naming/API cleanup is now closed for active source and tests: all remaining active `ApiObject` usages in package source/tests were migrated to `ApiSchema`, the deprecated compiled aliases remain only as explicitly deprecated compatibility exports, and the small set of camelCase source filenames called out in the audit were normalized to kebab-case (`use-node-imports`, `use-node-debug-data`, `use-node-scopes`, `use-source-value`, `word-editor-page`, `editor-canvas`) with imports/exports updated and packages rebuilt.
- Word-editor shell color cleanup is also closed for renderer-owned chrome: dataset/field badges and toolbar/panel shell colors were normalized to semantic utility/token usage, and the only remaining raw color literals in that package are the required hex defaults for browser `input[type=color]` controls rather than theme-owned surface styling.
- Link to code paths: `packages/flux-react/src/{use-node-imports.ts,use-node-debug-data.ts,use-node-scopes.ts,use-source-value.ts,node-renderer.tsx,index.tsx,use-node-source-props.ts,node-source-prop-controller.ts}`, `packages/word-editor-renderers/src/{word-editor-page.tsx,editor-canvas.tsx,index.ts,renderers.tsx,panels/FieldList.tsx,panels/DatasetPanel.tsx,toolbar/FontControls.tsx}`, plus the updated active tests in `packages/flux-runtime`, `packages/flux-renderers-form*`, and `packages/nop-debugger`.
- Key decision: keep legacy terminology only in historical docs/plans/logs and the intentionally deprecated compatibility aliases; active source, active tests, and maintained renderer shell styling are now on the new baseline.

### 2026-04-19

- Re-checked the remaining small styling tail after the flow-designer convergence. Confirmed `workflow-designer-schema.json` already needed no extra code change because its graph-mode node/edge inspectors were already explicit schema fragments. Landed only the safe remaining UI cleanup: word-editor dataset/field type badges now use semantic utility colors instead of gray/blue hardcodes, while color picker defaults stayed as valid hex values because `input[type=color]` cannot accept CSS variables.
- Also confirmed that `.nop-designer` / `.nop-designer-toolbar` remain the current package theme hooks in `designer-theme.css`; moving those selectors without changing the render tree would have been incorrect, so they were intentionally kept while other config-first ownership cleanup continued.
- Link to code paths: `packages/word-editor-renderers/src/{panels/FieldList.tsx,panels/DatasetPanel.tsx,toolbar/FontControls.tsx}`, `packages/flow-designer-renderers/src/designer-theme.css`, `apps/playground/src/schemas/workflow-designer-schema.json`.

### 2026-04-19

- Re-audited `apps/playground/src/schemas/workflow-designer-schema.json` after the tree-mode inspector migration and confirmed no extra code change was needed there: all graph-mode node types and the default edge type already declare explicit `inspector.body`, so the maintained playground examples are now consistently schema-first across both graph and tree designer modes.
- Linked the conclusion back into `docs/architecture/flow-designer/config-schema.md` so the architecture docs now point at all three playground examples as schema-first references.

### 2026-04-19

- Removed the last renderer-owned domain-specific inspector fallback forms from `packages/flow-designer-renderers/src/designer-inspector.tsx`. The default inspector now consists of shared base fields (`label`, `description`), schema-driven `nodeType.inspector.body` when present, and only a generic scalar-field fallback. This completes the shift away from renderer-maintained Dingtalk/Action-flow inspector DSL branches.
- Updated the flow-designer architecture docs again so they now state the stronger rule: renderer no longer owns domain-specific inspector forms; schema/config is the maintained authoring path.

### 2026-04-19

- Finished the remaining `docs/architecture/flow-designer/` sync for the config-first inspector/menu changes. Updated `config-schema.md`, `tree-mode.md`, and `dingflow-visual-spec.md` so the architecture baseline now matches the landed code: `nodeTypes[].inspector.body` is preferred by the default inspector, and tree-mode add menus derive items from `config.nodeTypes` with only narrow renderer-side filtering/fallback ordering.
- Marked the residual hardcoded node-field forms in `designer-inspector.tsx` explicitly as compatibility fallback, not the primary authoring path.
- Link to doc/code paths: `docs/architecture/flow-designer/{config-schema.md,tree-mode.md,dingflow-visual-spec.md}`, `packages/flow-designer-renderers/src/designer-inspector.tsx`.

### 2026-04-19

- Continued the flow-designer config-first convergence by migrating the playground tree examples to explicit `nodeTypes[].inspector.body` schema fragments for the previously hardcoded fallback cases (`dt-approval`, `dt-condition`, `dt-subprocess`, `action-step`). This keeps the renderer fallback available for compatibility, but the maintained examples now exercise the schema-first path the architecture docs describe.
- Added an integration regression in `packages/flow-designer-renderers/src/index.test.tsx` to verify that the default inspector really prefers `nodeType.inspector.body` when present.
- Link to code/test/example paths: `apps/playground/src/schemas/{dingtalk-workflow-tree-schema.json,action-flow-tree-schema.json}`, `packages/flow-designer-renderers/src/index.test.tsx`.
- Key decision: schema/config-provided inspector bodies are the maintained path; renderer-owned domain fallback forms remain only as compatibility fallback and should shrink over time as example/host configs move to explicit schema inspectors.

### 2026-04-19

- Removed the last renderer-owned DingFlow add-node item list as an authoritative source: tree-mode add menus now derive their item set from `config.nodeTypes`, filtered by tree semantics and a narrow fallback priority rule, instead of relying on a hardcoded renderer list of `dt-approval` / `dt-cc` / `dt-condition`.
- This means schema/config now directly controls which node types appear in the add menu, while the renderer only supplies conservative fallback ordering and excludes obvious root/terminal-only types.
- Link to code paths: `packages/flow-designer-renderers/src/{designer-node-appearance.ts,designer-canvas.tsx,dingflow/DingFlowCanvasOverlay.tsx}`.
- Key decision: since the schema already explicitly declares `config.nodeTypes`, the add-menu item set should be treated as config-owned data rather than something the renderer needs to internationalize or keep as a separate default catalog.

### 2026-04-19

- Pushed the flow-designer default domain metadata one step further toward config ownership: inspector headers and DingFlow add-node menus now prefer `nodeType.label` and `nodeType.icon` from designer config, with the old per-type tables kept only as fallbacks in `designer-node-appearance.ts`.
- This means the renderer no longer treats its own static node-type label/icon map as the authoritative source when the host/schema already defines those values in `config.nodeTypes`.
- Link to code paths: `packages/flow-designer-renderers/src/{designer-node-appearance.ts,designer-inspector.tsx,designer-canvas.tsx,dingflow/DingFlowCanvasOverlay.tsx}`.
- Key decision: keep a small fallback table only for standalone/default DingFlow usability, but route all authorable metadata through designer config first so renderer defaults do not silently override schema intent.

### 2026-04-19

- Finished another DingFlow styling cleanup slice by extracting shared default visual helpers into `packages/flow-designer-renderers/src/dingflow/dingflow-theme.ts` and moving plus-button, merge-button, add-condition badge, and edge-label visuals off scattered inline hex values/hardcoded whites into shared token-aware defaults.
- `CONNECTOR_COLOR` now defaults through the Flow Designer edge token contract (`--fd-edge-stroke`), and the remaining DingFlow overlay/button chrome now consistently uses the same default primary/token entry points instead of each component owning its own hardcoded palette.
- Link to code paths: `packages/flow-designer-renderers/src/dingflow/{dingflow-theme.ts,dingflow-constants.ts,DingFlowAddConditionOverlay.tsx,DingFlowMergeOverlay.tsx,DingFlowPlusButton.tsx,DingFlowEdge.tsx,index.ts}`.
- Key decision: keep DingFlow domain defaults as shared renderer-owned fallback visuals, but route them through one token/helper layer so schema/config/theme overrides can replace them coherently instead of fighting multiple local constants.

### 2026-04-19

- Continued the flow-designer styling audit with the explicit rule that node/edge visual appearance should prefer designer config/schema ownership over renderer-local hardcoded colors. Landed a small convergence step by centralizing default node accent fallback logic in `packages/flow-designer-renderers/src/designer-node-appearance.ts` and making canvas menus / inspector badges/icons prefer `nodeType.appearance.borderColor` when configured.
- This keeps DingFlow defaults working while letting schema/config-defined node appearance override the renderer’s previous hardcoded per-type color map instead of being silently ignored.
- Link to code/test paths: `packages/flow-designer-renderers/src/{designer-node-appearance.ts,designer-canvas.tsx,designer-inspector.tsx,dingflow/DingFlowCanvasOverlay.tsx,designer-controls.test.tsx}`.
- Key decision: treat node/edge core appearance as config-owned (`appearance`, `classAliases`, region schema) and limit renderer-owned styling to structural chrome such as toolbar shells, hit areas, and neutral overlays.

### 2026-04-19

- Replaced active-source `ApiObject` usages with `ApiSchema` in code-editor and validation-facing types where the alias had already become redundant, while keeping the public alias in `flux-core` for compatibility during the broader migration.
- Removed the hard dependency on `next-themes` from `packages/ui/src/components/ui/sonner.tsx`; the shared `Toaster` now works without a required ThemeProvider and falls back to explicit/host-provided `theme` props.
- Reduced safe styling-contract violations in this pass by moving `flux-renderers-form` control layout styling from `nop-*` marker selectors onto explicit `data-slot` wrappers, and by replacing a small set of obvious hard-coded surface colors with theme-aware semantic classes in `flow-designer-renderers` and `word-editor-renderers`.
- Link to code paths: `packages/flux-code-editor/src/{types.ts,source-resolvers.ts,code-editor-renderer/use-sql-editor-state.ts}`, `packages/flux-core/src/types/validation.ts`, `packages/ui/src/components/ui/sonner.tsx`, `packages/flux-renderers-form/src/{renderers/input.tsx,form-renderers.css}`, `packages/flow-designer-renderers/src/{designer-xyflow-canvas/DesignerXyflowNode.tsx,dingflow/DingFlowAddNodeMenu.tsx}`, `packages/word-editor-renderers/src/toolbar/{RibbonToolbar.tsx,SearchReplace.tsx,PageControls.tsx}`.
- Key decision: keep the remaining large color/token cleanup in flow-designer and word-editor as a follow-up slice, because the remaining hard-coded palettes are spread across multiple domain-specific components and need a coordinated token strategy rather than ad hoc replacement.

### 2026-04-19

- Continued the remaining deep-audit re-check for dimensions `14` and `17` after the earlier `10-18` slice, and separated stale findings from still-real gaps before changing code.
- Confirmed the former `path-binding.ts` / `instance-path.ts` test-gap finding was path-stale rather than code-gone: the modules now live under `packages/flux-core/src/utils/`, so I added focused tests there instead of treating the item as a missing-source defect. Also added a direct `schema-diagnostics/manifest.ts` contract test.
- Added non-breaking terminology cleanup in `packages/flux-core/src/types/renderer-compiler.ts` by marking legacy alias exports `CompiledSchemaMeta` and `CompiledNodeRuntimeState` as deprecated rather than removing them from the public API.
- Link to code/test paths: `packages/flux-core/src/{utils/path-binding.test.ts,utils/instance-path.test.ts,schema-diagnostics/manifest.test.ts,types/renderer-compiler.ts}`.
- Key decision: keep larger naming/API convergence work (`ApiObject` vs `ApiSchema`, file rename consistency) deferred because it requires broader public-surface coordination, while small focused tests and deprecation metadata can land safely now.
- Next step: the biggest confirmed backlog still left from `10-18` is dimension `10` theme/styling cleanup; dimension `14` now mainly contains larger missing-suite authoring rather than small isolated test gaps.

### 2026-04-19

- Re-checked deep-audit kernel dimensions `10-18` against the live repository and split them into: confirmed-small fixes landed now, and confirmed-large items deferred because they require broad theme/coverage/refactor work rather than safe patch-sized changes.
- Landed confirmed low-risk fixes across type, field-slot, cross-package, and doc drift areas: `array-field` now declares `item` region params, `detail-view` now reuses `formLabelFieldRule`, runtime navigate/timer/lifecycle typing is tightened, data/table/chart/tree empty fallbacks are localized, `flux-code-editor` now exports standard renderer registration helpers, `nop-debugger` filter labels now use i18n, and active docs/AGENTS now match the current hook/runtime contracts.
- Confirmed but intentionally not auto-fixed this pass: dimension `10` broad styling/theme violations (`form-renderers.css`, flow-designer/word-editor theme hardcodes, marker/layout mixing), dimension `14` missing direct tests/fixtures, and dimension `17` naming/dead-export cleanup. These are real but require larger coordinated refactors or net-new test suites rather than isolated bug patches.
- Link to code/doc paths: `packages/flux-runtime/src/{node-runtime.ts,action-runtime-handlers.ts,data-source-runtime.ts}`, `packages/flux-react/src/node-renderer-effects.ts`, `packages/flux-renderers-form-advanced/src/{composite-field/array-field.tsx,detail-view/detail-view.tsx}`, `packages/flux-renderers-data/src/{table-renderer.tsx,chart-renderer.tsx,tree-renderer.tsx}`, `packages/flux-code-editor/src/index.ts`, `packages/nop-debugger/src/panel.tsx`, `docs/architecture/{renderer-runtime.md,flux-runtime-module-boundaries.md}`, `AGENTS.md`.
- Key decision: treat dimensions `10`, `14`, and much of `17` as confirmed backlog slices, not automatic patch targets, because partial cleanup there would leave the repo in a mixed state without actually closing the architectural/test debt.
- Next step: if we continue after this slice, the best follow-ups are either a dedicated styling/theme cleanup pass for dimension `10` or a dedicated test authoring pass for dimension `14`.

### 2026-04-19

- Re-checked the remaining `variant-field` and `crud` dimension-09 follow-ups against the current renderer/docs baseline and landed the items that were now clearly scoped.
- `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx` now delegates field chrome back to `FieldFrame` while preserving the existing active-variant selector/body behavior; `FieldFrame` gained a narrow `rootProps` escape hatch so composite field renderers can keep stable root `data-*` markers such as `data-active-variant` without rebuilding `nop-field` manually.
- Updated active docs so CRUD explicitly documents the intended dual read path: `$crud` remains the default subtree summary binding, and `statusPath` publishes the same read-only `CrudStatusSummary` for external observers when configured.
- Link to code/doc paths: `packages/flux-react/src/{field-frame.tsx,frame-slot-meta.test.tsx}`, `packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx`, `docs/architecture/variant-field.md`, `docs/components/crud/design.md`.
- Key decision: keep `$crud` + `statusPath` as an explicit documented dual-access contract instead of treating the second publication path as a defect.
- Next step: if we later want to remove the remaining renderer-level `as any` casts, do it as a separate typing cleanup rather than mixing it into field-contract behavior work.

### 2026-04-19

- Re-checked the deep-audit findings in `docs/analysis/2026-04-18-deep-audit-kernel/{07-lifecycle.md,08-validation.md,09-renderer-contract.md}` against live code and only landed the findings that still reproduced in the current tree.
- Hardened owner-scoped validation writes in `packages/flux-runtime/src/form-runtime-owner.ts` so `applyChangesAndRevalidate()` now rejects non-owned `writes` / `changedPaths`, and dependent revalidation now preserves higher-priority reasons like `commit` across owner updates, `setValue`, `setValues`, and array mutation paths.
- Disposed transient draft `FormRuntime` instances on both confirm and cancel in `packages/flux-renderers-form-advanced/src/detail-view/{detail-field.tsx,detail-view.tsx}` so detail surfaces do not retain draft validation/debounce state after close.
- Consolidated field-error visibility logic into `packages/flux-react/src/field-error-visibility.ts` for reuse by `flux-react` and `flux-renderers-form`, and replaced hard-coded CRUD empty fallback text with `flux.common.noData` plus new `flux.common.detailDraftValidationError` locale keys.
- Link to code paths: `packages/flux-runtime/src/{form-runtime.ts,form-runtime-owner.ts,form-runtime-values.ts,form-runtime-array.ts,form-runtime-array-ops.ts}`, `packages/flux-react/src/{field-error-visibility.ts,form-state.ts,field-frame.tsx,index.tsx}`, `packages/flux-renderers-form/src/field-utils.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/{detail-field.tsx,detail-view.tsx}`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/flux-i18n/src/locales/{en-US.ts,zh-CN.ts}`.
- Link to tests: `packages/flux-runtime/src/__tests__/owner-based-validation-contracts.test.ts`, `packages/flux-renderers-data/src/__tests__/data-crud.test.tsx`, `packages/flux-renderers-form-advanced/src/{detail-view/detail-field-basic.test.tsx,detail-view/detail-view-basic.test.tsx,__tests__/composite-form-detail-and-loop.test.tsx}`.
- Key decision: keep `07` lifecycle dependency cleanup, `09` `$crud`/`statusPath` semantics, `variant-field` chrome unification, and renderer `as any` cleanup out of this pass because they are contract/architecture follow-ups rather than minimal bug fixes.
- Next step: if those remaining renderer-contract findings still matter, handle them in a separate follow-up slice with explicit API/contract scope.

### 2026-04-19

- Re-checked the deep-audit findings in `docs/analysis/2026-04-18-deep-audit-kernel/{04-state-ownership.md,05-reactive-precision.md,06-async-safety.md}` against live runtime/react code and fixed the confirmed defects only.
- Removed duplicate `statusPath` publication from `dialog`/`drawer` renderers so `SurfaceRuntime` remains the single owner for surface open-state summaries; kept React-side status publication for `page`/`tabs` and added effect cleanup to clear stale status on unmount.
- Changed form scope subscriptions to notify only on `values` snapshot changes, which prevents field-state-only writes from reusing stale `lastChange` paths and falsely waking scope subscribers; also set explicit form last-change updates on owner-driven value writes.
- Added submit/validation/import/reaction hardening: thrown submit API failures now route through `onSubmitError`, async validation requests abort stale runs, import setup no longer lands released scope results, and reaction fire-and-forget paths now report errors instead of leaking unhandled rejections.
- Link to code paths: `packages/flux-renderers-basic/src/{dialog.tsx,drawer.tsx,status-hooks.ts}`, `packages/flux-react/src/container-hooks.ts`, `packages/flux-runtime/src/{form-runtime.ts,form-runtime-owner.ts,form-runtime-validation.ts,form-runtime-submit-flow.ts,runtime-action-helpers.ts,reaction-runtime.ts,imports.ts}`, `packages/flux-core/src/types/actions.ts`.
- Link to tests: `packages/flux-runtime/src/__tests__/runtime-audit-fixes.test.ts`, `packages/flux-runtime/src/__tests__/runtime-imports.test.ts`.
- Key decision: treat `useScopeSelector` path-level pruning as a real but separate subscription-architecture optimization, not a minimal defect fix in this audit pass.
- Next step: if the remaining reactive-precision cost becomes hot in profiling, add path-aware scope subscription filtering at the `ScopeStore.subscribe` layer instead of broad hook-local workarounds.

### 2026-04-19

- Audited `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md` against live code and found the original `ImportStack` slice was too heavy for the current implementation state.
- Removed the `__imports` transport mechanism from live runtime paths and switched imported expression helpers to direct `$alias` scope bindings, which simplifies formula evaluation and keeps imported expressions aligned with ordinary scope lookup.
- Removed `SchemaRenderer` root import preloading/injection because it leaked imported aliases/actions outside their declaring lexical boundary; node-local import loading remains the active runtime boundary.
- Updated focused tests and plan/doc baseline to reflect that `ActionScope` already owns action lexical visibility, while expression visibility can use direct `$alias` bindings without a separate runtime stack.
- Landed shared `ModuleCache` in `flux-core`/`flux-runtime`, threaded it through `createRendererRuntime`, and added cross-runtime import dedupe coverage.
- Added mandatory `schemaUrl` root input plus `env.resolveImportUrl`, and now resolve `xui:imports.from` before loader/cache lookup so relative imports dedupe across schema renderers.
- Link to code/doc paths: `packages/flux-formula/src/{compile.ts,evaluator.ts,index.test.ts,evaluator.test.ts}`, `packages/flux-react/src/{schema-renderer.tsx,node-renderer.tsx,useNodeImports.ts,schema-renderer-imports-basic.test.tsx}`, `packages/flux-renderers-form/src/renderers/form.tsx`, `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md`, `docs/architecture/module-cache-and-import-stack.md`.
- Link to additional code paths: `packages/flux-core/src/types/{compilation.ts,renderer-api.ts,renderer-hooks.ts,renderer-core.ts,node-identity.ts,renderer-compiler.ts}`, `packages/flux-runtime/src/{runtime-factory.ts,imports.ts,schema-compiler.ts,__tests__/runtime-imports.test.ts}`.
- Key decision: simplify first by deleting the special `__imports` channel; re-evaluate `ImportStack` later only if direct `$alias` scope overlays prove insufficient for compile-time analysis or host integration.
- Next step: continue Plan 116 with compile-time symbol-table / diagnostics work, keeping `ImportStack` optional unless later slices prove a real runtime need.
