# Development Log — 2026-04-03

### 2026-04-03

- Moved `data-source` request orchestration out of `@nop-chaos/flux-renderers-data` and into a new runtime controller in `@nop-chaos/flux-runtime`, so polling, abort, stop conditions, dedup-backed request execution, and cache ownership now live with the runtime instead of a React effect.
- Link to doc path: `docs/articles/flux-design-introduction-revision-notes.md`, `docs/architecture/api-data-source.md`
- Link to code path: `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/flux-runtime/src/__tests__/request-runtime.test.ts`, `packages/flux-renderers-data/src/index.test.tsx`
- Key decision: keep cache ownership runtime-local rather than process-global, so independent renderer roots remain isolated while repeated data-source refreshes inside one runtime can still reuse cached responses.
- Verification: `pnpm --filter @nop-chaos/flux-runtime typecheck && test`, `pnpm --filter @nop-chaos/flux-renderers-data typecheck && test` pass.
- Next step: continue the revision-note follow-up list with debugger debug-snapshot contracts or `env` identity hardening.

### 2026-04-03

- Audited all packages for `@nop-chaos/ui` adoption: identified 4 packages needing migration (flux-code-editor, word-editor-renderers, report-designer-renderers, spreadsheet-renderers) and confirmed nop-debugger should remain self-contained due to its isolated theme architecture.
- Link to doc path: `docs/plans/30-ui-package-adoption-audit-and-migration-plan.md`
- Key decision: nop-debugger keeps its self-contained CSS variable theme; no shared ribbon/toolbar abstraction -- each package uses `@nop-chaos/ui` Button/Separator/Tabs directly.
- Next step: execute migration in order: flux-code-editor -> word-editor-renderers -> report-designer-renderers -> spreadsheet-renderers.

### 2026-04-03

- Implemented the first code follow-up from `docs/articles/flux-design-introduction-revision-notes.md`: `@nop-chaos/flux-react` now exposes `useOwnScopeSelector()` alongside lexical `useScopeSelector()`, and report-designer shell renderers moved to the own-scope hook where they only consume fragment host data.
- Link to doc path: `docs/articles/flux-design-introduction-revision-notes.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flux-core.md`
- Link to code path: `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/index.tsx`, `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-react/src/index.test.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.tsx`, `packages/report-designer-renderers/src/inspector-shell-renderer.tsx`
- Key decision: keep `readOwn()` semantics unchanged and make selector intent explicit at the hook layer instead of hiding lexical inheritance behind own-snapshot structure.
- Verification: `pnpm --filter @nop-chaos/flux-react typecheck && test && lint` and `pnpm --filter @nop-chaos/report-designer-renderers typecheck && test` pass.
- Next step: continue the revision-note implementation order with `data-source` runtime extraction, debugger debug-snapshot contracts, or `env` identity hardening as separate changes.

### 2026-04-03

- Completed the remaining structural work in `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`: `@nop-chaos/flow-designer-core` now extracts selection/history/node/edge/transaction helpers under `src/core/`, `@nop-chaos/report-designer-core` now extracts preview/codec command helpers under `src/runtime/`, and `@nop-chaos/nop-debugger` now moves inspect-mode overlay/pick behavior into `src/panel/use-inspect-mode.ts` so `panel.tsx` stays a thinner shell.
- Link to doc path: `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`
- Link to code path: `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-core/src/core/*.ts`, `packages/report-designer-core/src/core.ts`, `packages/report-designer-core/src/runtime/*.ts`, `packages/nop-debugger/src/panel.tsx`, `packages/nop-debugger/src/panel/*.ts(x)`
- Key decision: keep public core/panel entrypoints and behavior unchanged, but finish the second-pass split only for pure or local orchestration helpers that were still crowding the entry files after the first refactor pass.
- Verification: `pnpm --filter @nop-chaos/spreadsheet-core typecheck && build && lint && test`, `pnpm --filter @nop-chaos/flow-designer-core typecheck && build && lint && test`, `pnpm --filter @nop-chaos/report-designer-core typecheck && build && lint && test`, and `pnpm --filter @nop-chaos/nop-debugger typecheck && build && lint && test` all pass.
- Next step: rerun workspace-level verification separately if we want to refresh the old unrelated-blocker record after this plan closure.

### 2026-04-03

- Updated `docs/articles/flux-design-introduction.md` to align the public article with current runtime contracts: narrowed component `id` scope to page-local uniqueness, fixed namespace action examples to `namespace:method`, corrected `xui:imports` missing-loader behavior, rewrote `data-source` as a side-effect renderer, and softened debugger claims to match shipped capabilities.
- Link to doc path: `docs/articles/flux-design-introduction.md`, `docs/articles/flux-design-introduction-revision-notes.md`
- Link to code path: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/action-scope.ts`, `packages/flux-runtime/src/imports.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/panel.tsx`
- Key decision: keep the article as a design-philosophy piece while removing only the parts that misstate current external semantics or overclaim shipped tooling.
- Next step: if needed, apply the remaining implementation follow-ups from `docs/articles/flux-design-introduction-revision-notes.md` as separate code changes rather than expanding this docs-only edit.

### 2026-04-03

- Completed the second `@nop-chaos/nop-debugger` pass from `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md` by moving the overview, timeline, network, and node tab render branches out of `packages/nop-debugger/src/panel.tsx` and wiring `panel.tsx` back up as the thin state/orchestration shell.
- Link to code path: `packages/nop-debugger/src/panel.tsx`, `packages/nop-debugger/src/panel/overview-tab.tsx`, `packages/nop-debugger/src/panel/timeline-tab.tsx`, `packages/nop-debugger/src/panel/network-tab.tsx`, `packages/nop-debugger/src/panel/node-tab.tsx`
- Key decision: keep inspect overlays, drag/resize state, launcher/minimized behavior, and controller interactions in `panel.tsx`, while moving only the tab view bodies; restore the full node inspector UI instead of leaving the temporary placeholder split.
- Verification: `pnpm --filter @nop-chaos/nop-debugger typecheck`, `build`, `lint`, and `test` all pass after stabilizing the extracted inspect callback for hooks linting.
- Next step: optionally stop here with the debugger split complete, or do a later follow-up only if we want to extract more panel-local inspect helpers without changing behavior.

### 2026-04-03

- Finalized the article revision note after reviewing `docs/articles/review-reply.md`: kept `id` as a page-local absolute anchor, downgraded raw-schema `_cid` mutation from a required fix to an optional cleanup, and converged the scope-reactivity recommendation on a dual-hook design instead of hiding parent data behind `readOwn()`.
- Link to doc path: `docs/articles/flux-design-introduction-revision-notes.md`, `docs/articles/review-reply.md`
- Link to code path: `packages/flux-react/src/hooks.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/schema-renderer.tsx`
- Key decision: preserve article-level design intent where it still matches the intended architecture, and only narrow statements that are factually outdated or overstate current implementation.
- Next step: apply the agreed article text edits, then treat `useScopeSelector`, `data-source`, debugger contracts, and `env` lifetime as separate implementation tracks.

### 2026-04-03

- Executed the first implementation pass of `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`: extracted pure/helper modules from `packages/spreadsheet-core/src/core.ts`, `packages/flow-designer-core/src/core.ts`, `packages/report-designer-core/src/core.ts`, and `packages/nop-debugger/src/panel.tsx` without changing their public APIs.
- Link to code path: `packages/spreadsheet-core/src/core.ts`, `packages/spreadsheet-core/src/core/*.ts`, `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-core/src/core/*.ts`, `packages/report-designer-core/src/core.ts`, `packages/report-designer-core/src/runtime/*.ts`, `packages/nop-debugger/src/panel.tsx`, `packages/nop-debugger/src/panel/*.ts(x)`
- Key decision: keep orchestrators in the original `core.ts` / `panel.tsx` entrypoints and only extract document mutations, adapter helpers, viewer/helpers, and drag/resize hooks that could move without changing runtime semantics.
- Verification: package-level `typecheck/build/lint/test` now pass for `@nop-chaos/spreadsheet-core`, `@nop-chaos/flow-designer-core`, `@nop-chaos/report-designer-core`, and `@nop-chaos/nop-debugger`; workspace `typecheck` and `lint` still stop in `packages/flux-renderers-form` as an unrelated blocker, and workspace `build` / `test` hit recursive command timeouts after the refactored packages themselves had already completed.
- Next step: if needed, continue plan #29 with a second pass that further thins `nop-debugger/src/panel.tsx` into tab-level components and optionally extracts more mutation helpers from the remaining runtime orchestrators.

### 2026-04-03

- Finished the last remaining docs-only item in `docs/plans/23-architecture-audit-fix-plan.md` by documenting the `amis-formula` adapter boundary in the active architecture baseline.
- Link to doc path: `docs/architecture/flux-core.md`, `docs/plans/23-architecture-audit-fix-plan.md`
- Link to code path: `packages/flux-formula/src/compile.ts`
- Key decision: treat `amis-formula` as an implementation detail of `@nop-chaos/flux-formula`, keep the dependency boundary explicit in docs, and close plan #23 as completed instead of leaving a low-value dangling backlog item.
- Next step: use dedicated follow-up plans for domain-package structure work rather than reopening #23.

### 2026-04-03

- Re-reviewed `docs/plans/23-architecture-audit-fix-plan.md` against the current repository state and converted it from a stale planned backlog into a reviewed status ledger.
- Link to doc path: `docs/plans/23-architecture-audit-fix-plan.md`
- Link to code path: `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/form-store.ts`, `packages/flux-runtime/src/form-runtime-array.ts`, `packages/flux-runtime/src/utils/debounce.ts`, `packages/flux-core/src/types.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-formula/src/`
- Key decision: close the already implemented `flux-*` audit items in plan #23, keep only the low-priority docs-only `amis-formula` note as still actionable, and move domain-package follow-up tracking to dedicated plans such as `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`.
- Next step: if desired, finish the remaining `P3-2` documentation note so plan #23 becomes fully historical.

### 2026-04-03

- Revised `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md` after an independent review: corrected its relationship to plan #27, tightened spreadsheet/report-designer split boundaries, added per-package `build`/`lint` verification, and explicitly pulled `packages/flow-designer-core/src/core.ts` into this refactor round.
- Link to doc path: `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`
- Link to code path: `packages/flow-designer-core/src/core.ts`, `packages/spreadsheet-core/src/core.ts`, `packages/report-designer-core/src/core.ts`, `packages/nop-debugger/src/panel.tsx`
- Key decision: this plan now tracks four domain hotspots instead of three, and treats `flow-designer-core` as first-class scope rather than a deferred follow-up.
- Next step: execute plan #29 in four passes: `spreadsheet-core`, `flow-designer-core`, `report-designer-core`, then `nop-debugger`.

### 2026-04-03

- Audited the repository for non-behavioral refactor opportunities and added `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md` to track the highest-value remaining structure work.
- Link to doc path: `docs/plans/29-domain-runtime-and-debugger-refactor-plan.md`
- Link to code path: `packages/spreadsheet-core/src/core.ts`, `packages/report-designer-core/src/core.ts`, `packages/nop-debugger/src/panel.tsx`
- Key decision: do not duplicate `flux-*` audit items already covered by plan #23/#27; prioritize domain-package hotspots that still mix orchestration, helpers, and UI in oversized files.
- Next step: execute the plan in three focused passes: `spreadsheet-core`, `report-designer-core`, then `nop-debugger`.

### 2026-04-03

- Fixed the playground CSS import ordering so Vite/PostCSS no longer warns about `@import` appearing after other statements during production builds.
- Moved the imported stylesheet block ahead of `@source` in `apps/playground/src/styles.css`, then moved the bundled `react-json-view-lite` import to the top of `packages/ui/src/styles/base.css` because that imported file still violated the same CSS rule.
- Verified `pnpm --filter @nop-chaos/flux-playground build` and `pnpm --filter @nop-chaos/ui build` pass without the PostCSS `@import must precede all other statements` warning.
- Link to code path: `apps/playground/src/styles.css`, `packages/ui/src/styles/base.css`
- Key decision: fix CSS ordering at the originating stylesheets instead of hiding the warning in Vite, so workspace consumers keep valid imported CSS as well.
- Next step: rerun the full workspace verification suite with longer timeouts now that the report-designer snapshot loop and CSS build warning are both resolved.

### 2026-04-03

- Fixed the last React 19 external-store loop in `@nop-chaos/report-designer-core` by caching `getSnapshot()` results, matching the earlier spreadsheet and flow-designer store stabilization work.
- Verified the targeted packages now pass: `pnpm --filter @nop-chaos/report-designer-core test` and `pnpm --filter @nop-chaos/report-designer-renderers test`.
- Link to code path: `packages/report-designer-core/src/core.ts`
- Key decision: keep the fix at the store boundary so React consumers continue reading stable snapshots without adding renderer-specific memoization.
- Next step: rerun workspace verification with a longer timeout or package-level aggregation, since isolated `flow-designer-renderers`, `flux-renderers-form`, and `apps/playground` reruns passed after the recursive commands timed out.

### 2026-04-03

- Added an article review note that separates stale descriptions in `docs/articles/flux-design-introduction.md` from forward-looking design that should remain, and recorded concrete implementation follow-ups for scope reactivity, compile purity, data-source runtime ownership, debugger introspection, and runtime/env lifetime.
- Link to doc path: `docs/articles/flux-design-introduction-revision-notes.md`
- Link to code path: `packages/flux-react/src/hooks.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/nop-debugger/src/controller.ts`, `packages/flux-react/src/schema-renderer.tsx`
- Key decision: treat `docs/articles/` as rationale documents, so only clearly outdated current-state claims should be revised; future-facing architecture intent remains valid unless it conflicts with active contracts.
- Next step: apply the low-risk article edits first, then schedule the runtime fixes in implementation order instead of mixing doc corrections with code changes.

### 2026-04-03

- Audited the entire pnpm workspace for outdated dependencies and found a single safe forward upgrade in `packages/ui/package.json`.
- Updated `react-resizable-panels` from `^4.8.0` to `^4.9.0` after confirming its latest peer range still matches the repo baseline (`react`/`react-dom` 19).
- Kept the root lint toolchain on the latest compatible ESLint 9 line because `eslint-plugin-react-hooks@7.0.1` does not yet accept ESLint 10 peers, while `typescript-eslint` supports both.
- Fixed the word-editor build configs to resolve the local `@hufe921/canvas-editor` declaration file through the workspace base tsconfig path, and tightened shared Vitest file matching so generated `src/*.js` test artifacts stop running alongside source tests.
- Stabilized external-store snapshots for `@nop-chaos/flux-react` and `@nop-chaos/spreadsheet-core` so React 19 gets cached scope/runtime snapshots instead of fresh objects on every read.
- Link to code path: `packages/ui/package.json`
- Link to code path: `package.json`
- Link to code path: `packages/word-editor-core/tsconfig.build.json`, `packages/word-editor-renderers/tsconfig.build.json`, `vitest.shared.ts`, `packages/flux-react/src/hooks.ts`, `packages/spreadsheet-core/src/core.ts`
- Key decision: keep the dependency refresh minimal and compatibility-driven instead of forcing major-version churn where the workspace already resolves to current versions.
- Next step: clear the tracked `src/` build artifacts and finish the remaining `@nop-chaos/spreadsheet-renderers` React 19 render-loop fix so full lint/test verification can go green again.

### 2026-04-03

- Guarded the debugger panel drag/resize pointer-capture calls so `packages/nop-debugger/src/panel.tsx` no longer throws in jsdom when `setPointerCapture` or `releasePointerCapture` is unavailable.
- Verified `pnpm.cmd --filter @nop-chaos/nop-debugger lint`, `typecheck`, `build`, and `test` all pass, including both `src/panel.test.tsx` and `dist/panel.test.js`.
- Link to code path: `packages/nop-debugger/src/panel.tsx`
- Key decision: keep pointer-capture behavior in real browsers, but make the panel tolerate DOM environments that only implement pointer events partially.
- Next step: rerun full workspace tests to confirm the last remaining blocker is gone.

### 2026-04-03

- Added `scripts/patch-lucide-react.mjs` and wired it to the root `postinstall` script so the workspace patches the broken `lucide-react@1.7.0` publish without rolling versions backward.
- Finished the strict latest hooks/compiler lint migration by fixing the remaining playground, data renderer, form renderer, code editor, and designer integration violations across the workspace.
- Added package-local `@hufe921/canvas-editor` declaration shims in the word editor packages so package-root builds no longer depend on cross-root type resolution.
- Verified `pnpm.cmd lint` and `pnpm.cmd typecheck` pass end-to-end; `pnpm.cmd build` then narrowed to the upstream `lucide-react@1.7.0` missing-file defect before the patch was added.
- Key decision: keep `lucide-react` on the latest published version and repair the packaging defect locally instead of rolling back to an older release.
- Next step: rerun `pnpm.cmd build` after the patch and confirm the workspace is green end-to-end.
