# Development Log - 2026-09-01

### 2026-09-01

- **ui-review 审计 P1 批次三（a11y 7 条 + 测试保护 2 条）执行**（plan `docs/plans/2026-08-31-1941-3-ui-review-p1-a11y-and-test-protection.md` Phase 1–4 全部完成；批次一/批次二已收口后按跨批次执行顺序执行本批次；先红后绿纪律全程履行）。
- **Phase 1（ui command 可访问名称与空态语义 20-01/20-02）**：`ui/src/components/ui/command.tsx` `CommandDialog` 默认 title/description 改 `t('flux.command.title')`/`t('flux.command.searchPlaceholder')`（消费 ui 默认值方案，`command-palette.tsx` 消费方零传参）；`CommandEmpty` 内层 `<span role="status" aria-live="polite">`——cmdk 上游在 props spread 之后硬编码 `role="presentation"`（props 无法覆盖），内层显式 role 节点是唯一不依赖上游的 live 通道（ARIA presentation 冲突消解下显式 role 子节点语义保留）。双语登记红线履行：`flux-i18n` en-US/zh-CN 新增 `flux.command.*` 组 + ui 内置英文兜底同步。先红后绿：新 `ui/src/__tests__/command-dialog-a11y.test.tsx` 4 条（红态实测 2 failed——桥接中文断言 + role=status 断言）。
- **Phase 2（data 三组件 a11y 20-03/20-04/20-05）**：`batch-bar.tsx` 根节点 `role="status"` + `aria-live="polite"`（空集 null gate 语义不变）；`table-editable-cell.tsx` `errorId = useId()` 稳定 id——select 分支补 `aria-invalid`/`aria-describedby`（原全缺）、input 分支补 `aria-describedby`（原仅 aria-invalid）、错误 span 挂 `id`；`table-column-settings.tsx` inline 形态 `inlinePanelId = useId()` + 触发钮 `aria-expanded`/`aria-controls`（仅展开态指向，收起零悬挂 IDREF）+ 面板 `id`（overlay 形态不动）。先红后绿：`batch-bar.test.tsx` 增 live-region 2 条、`table-editable-cell.unit.test.tsx` 增 aria 接线 2 条、新 `table-column-settings-disclosure.test.tsx` 1 条（红态实测 2+2+1 failed）。
- **Phase 3（scheduling kanban + content result 状态通道 20-07/20-10）**：`kanban-column-header.tsx` WIP 超限徽章补 sr-only 后缀 `t('scheduling.kanban.wipExceeded')`（en 'WIP limit exceeded' / zh '超出 WIP 上限' 双语登记；颜色通道保留，`kanban-column.tsx` 未动）；`result.tsx` 落定 sr-only 状态词方案——section 顶部常挂 `data-slot="result-status-sr"` sr-only span（`flux.result.statusSuccess/statusError/statusWarning/statusInfo` 四 key 双语登记），status-only 配置即有非视觉语义通道。先红后绿：新 `kanban-column-header-wip.test.tsx` 3 条（真实 flux-i18n 非 mock，en+zh 双语断言；红态实测 2 failed）+ `result-renderer.test.tsx` 增 2 条（红态实测 2 failed；执行注记：flux-i18n 单例可能被同文件前置用例以默认语言 auto-init，`beforeEach` 需先 `resetFluxI18n` 再 init）。
- **Phase 4（command-palette 测试保护 23-01/23-02 + 执行期真实缺陷修复）**：23-01——既有「close-then-dispatch」用例注入 fetcher spy 断言 `/r/Executed?id=nav` + 新增「locks the close-then-dispatch order」用例（三通道全 ajax + 单数组 equality 锁定 `/r/Closed → /r/Executed?id=nav → /r/Command?id=nav` 顺序）；23-02——「skipped no-op」用例重写为 fetcher spy 计数机制（skip 点击后 onOpen ajax 零调用 + Esc 关闭后同一按钮真触发 `/r/Opened` 正向对照）。**两条 23-01 用例首跑即推翻审计"live 行为正确"结论，暴露真实缺陷：静态条目经 prop-kind 编译在渲染期对渲染 scope 求值，`action.args` 内 `${id}` 模板被预烘焙为空串（`/r/Executed?id=`；同名单名变量存在时更会烘焙错值），dispatch 期 `evaluationBindings` 永远看不到模板**（独立 explore agent 复核根因：`resolveNodeProps` 渲染期烘焙，排除时序/scope-dispose 假说；`/r/Closed` 通过属无模板平凡解析）。Fix（command-palette.tsx 渲染层，静态轨道 raw-action 保真）：`buildSections` 为静态 groups/items 轨道建立 raw schema 平行配对（`templateNode.schema`；长度对齐 + 逐项 object 校验 + 不对齐即回退 resolved 值；expression/`source` 轨道为运行时数据不加配对），`executeCommand` 优先派发 raw action——模板存活至 dispatch 期由 payload bindings（id/item/groupId）解析，非绑定名继续沿 node.scope 链解析。**mutation self-check**：删除 executeCommand 派发分支 → 恰 2 条 23-01 用例红（598 绿）；删除 handle open skip 门禁 → 恰 1 条 23-02 用例红（599 绿）；恢复后全绿。姊妹缺陷 keyboard.tsx `bindings`（同型 prop-kind 静态 action）登记 plan Non-Blocking Follow-ups（不在本 plan scope）。审计回写：audit 文件 23-01 条目补记。owner-doc 同步：`docs/references/renderer-interfaces.md` command-palette dual-track 条目补一句静态轨道 dispatch 期模板解析契约。
- **执行期非本 plan 缺陷修复（阻断 full-green，drive-by 披露）**：`flux-renderers-form/input-date-relative-wiring.test.tsx` calendar-constraints 4 例自 2026-09-01 起恒红——**月历 popover 打开在 value 的月份（硬编码 `2026-08-11` → 八月格），断言按当日 `getDate()` 数字查按钮，月份翻转后查到的是八月日期钮，全部断言反向（月度时间炸弹，非瞬时 flake；8-31 基线绿因当时 value 月=当前月）**。修复：value 改锚定当前月（`currentMonthDay(11)` 等，断言语义不变）。同日早间另观察到午夜时分 4 例红与基线 stash 复跑一致，实为同一缺陷的两种触发表象。探针脚本（`_tmp/date-flake-inspect.test.tsx`）用后即删。
- **验证状态（full-green verification，unit 侧全绿）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 任务全绿（exit 0；新增单测 17 条：ui 4 + data 5 + scheduling 3 + content 2 + basic command-palette 重写/新增 3）、`pnpm check` exit 0 零新增红（仅既有 2 exempt locale 文件登记红线）。**e2e 披露（非 full-green，全部失败经基线复跑证实与本批次无关）**：`pnpm test:e2e` 1438 passed / 5 flaky / 43 skipped / **13 failed**——13 例在 pristine 基线（stash 全部本批次改动后）复跑 **逐条同型失败**：barcode 族 4 例（本机 Chromium `BarcodeDetector` 不支持 `pdf_417` 枚举，环境缺口）+ crud-demo 6 例（页面标题不渲染，基线同败）+ cal-replica 2 例 + m4-data 1 例（基线同败）。e2e 遗留为基线既有状态，不随本批次收口；与 AGENTS full-green 提交规则的口径差异（e2e 非全绿）已在本日志与本条披露，提交标题据实标注 unit full-green。
- 测试档位：必须自动化（7 条 a11y 先红后绿 + 2 条测试保护缺口本身即交付物）。
- **Closure audit（批次三 plan 收口）**：fresh session 独立子 agent `ses_fa6f8b998ffelATdGnV6XGaIKQ`（input 仅三件套：plan + diff summary + verification output）1 轮 **APPROVED** 零 Blocker/零 Major/1 Minor/3 Informational。Auditor 独立重跑五包目标测试（ui 4、data 33、scheduling 3、content 12、basic 33——全包 600/600）+ `check:i18n-keys` ✅ + `pnpm check` exit 0 基线一致 + 双侧 key 登记 grep 实证 + 9 条 finding"删修复即红"性质逐条核对 + 23-01 raw-action 修复契约面审查 + scope/docs/honesty 三项过。1 Minor（order-lock 用例残留 `DEBUG-URLS` console.log）已随收口删除并复跑绿（18/18）。plan Status → `completed`；源审计 `2026-08-28-1659-multi-audit-ui-review.md` Audit Status `planned` → `closed`（三批次全部收口）。
