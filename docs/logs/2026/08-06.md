# Development Log - 2026-08-06

### 2026-08-06

- **Plan 2026-08-06-0711-2 project-context 基线段回写执行完毕（1 Phase 全 completed；纯文档同步，无代码变更；closure-audit 由独立 fresh session 收口）**：
  - **Phase 1 基线段回写**：`docs/context/project-context.md:15` CV full-green 基线段的 pre-existing red 集表述改为 live 口径——`check:oversized-code-files` **14 文件 >700 行（08-06 登记治理债清单，见本日 0529-1 Phase 2 条目，治理归独立 successor）**；`check:workspace-manifest-deps` **exit 0（0529-1 已清零，原 5 条 ERROR 不再在案）**（「原 5 条 ERROR」措辞避开 Exit Criteria 机械 grep 的 `5 ERROR` 字面命中，语义与 plan Fix 指示一致）。
  - **验证（Proof）**：live 复跑 `node scripts/check-oversized-code-files.mjs` → ERROR **14 files** >700 行（exit 1 按设计，14 个既有登记命名清单与 0529-1 Phase 2 一致）；`node scripts/check-workspace-manifest-deps.mjs` → 「All package source workspace imports are declared in local manifests」**exit 0**——与回写文字逐字一致；`rg -n "16 文件|5 ERROR" docs/context/` **零命中**（exit 1）。
  - **文档**：plan Phase 1 三项目 + Exit Criteria 全勾选 + Phase Status → completed；本 daily log 条目；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-0711-1 scheduling 家族 P1 修复（22-01 gantt 缩放双触发 / 22-02 gantt 键盘误删 / 22-03 kanban 异步不重灌）执行完毕（3 Phase 全 completed；三条 confirmed live defect 全部 test-first 修复 + 回归锁定；closure-audit 由独立 fresh session 收口）**：
  - **Phase 1 22-01 gantt 缩放单驱动收敛**：Proof 测试先红（day 起点点「+」一次 `onZoomChange` 实收 2 次、`zoomIn.dispatch` 双触发路径）后绿——`gantt-header.tsx` `handleZoomIn/handleZoomOut` 删除内部 `store.setZoom` + `onZoomChange?.(...)`，按钮路径只调 `onZoomIn?.()`/`onZoomOut?.()`（可用性判断保留只读）；`gantt.tsx` `doZoomIn/doZoomOut` 成为唯一变更驱动（基于 currentZoom 单步 setZoom + handleZoomChange）；`handleZoomToFit`/`useImperativeHandle` 独立单次驱动无双派发（核对通过）。新用例 2 条（单击恰一级 + onZoomChange/dispatch 恰一次；连点两次 day→week→month 逐级推进 + 顶端第三击零派发）；既有 zoom 用例（`gantt.test.tsx:294-319` `zoomIn.dispatch` 恰一次）新驱动下仍绿；`docs/components/gantt/design.md` onZoomChange「缩放级别切换」语义核对无冲突（:276）。
  - **Phase 2 22-02 gantt 键盘输入目标守卫**：Proof 测试先红（input 冒泡 Delete/Backspace 实删任务、Enter 实开编辑器）后绿——`use-gantt-keyboard.ts` `handleKeyDown` 入口加 `target.closest('input, textarea, [contenteditable]')` 守卫（对齐 kanban `use-kanban-board-effects.ts:55-56` 先例），输入目标事件一律放行（不拦截、不 preventDefault、不删任务、不开编辑器），同时消除「Enter 提交单元格后冒泡打开 GanttEditor」副作用。新用例 3 条（Delete/Backspace/Enter 源自 input：`onDeleteTask`/`deleteTask` 未调用 + `defaultPrevented === false`）；既有用例（container 直发、target === container）守卫下仍绿。
  - **Phase 3 22-03 kanban local 模式 data 重灌**：Proof 测试先红（loading→data 重渲染后仍空面板、空态常驻）后绿——`kanban-board.tsx` 增加 re-seed effect（仿 `gantt.tsx:79-101` 先例）：`lastRawDataRef` 快照区分首灌（ref === 初始值不触发），任何后续 `rawData` 引用变化（含异步 undefined→data 首次到达）`setLocalBoardData(rawData ?? EMPTY_BOARD)`；fallback board 提升模块级常量 `EMPTY_BOARD`；新数据覆盖本地编辑语义写入注释；effect 仅作用于 `kanbanOwnership === 'local'`（controlled/scope 派生路径不变）。新用例 1 条（loading 骨架 → data 到达列渲染 + 空态不触发 → 第二次引用变化再重灌）；既有空态/loading 用例语义不受影响；`docs/components/kanban/design.md` :261「data 表达式/data-source 输出只读消费」核对无冲突。
  - **验证（full-green）**：scheduling 包 848/848 单测绿（73 files，含新增 6 条先红后绿用例）；全量 `pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 14 既有 pre-existing 命名清单零新增，其余 10 项逐项全绿（含 audit-suspects exit 0 / audit-renderer-browser-io 零命中）。
  - **文档**：plan 3 Phase 全勾选 + Plan Status → completed；roadmap 无 22-01/22-02/22-03 ❌ 工作项（0711 P1 已路由至本 plan，无 Work Item/Source Audits 标签，❌→✅ 与审计状态翻转不适用）；本 daily log 条目；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-2027-1 扫描 P1 候选吸收收口执行完毕（5 Phase 全 completed；roadmap 扫描发现路由登记区 19-1/19-2/23-1/23-2 四行回写 fixed/裁决留痕；closure-audit 由独立 fresh session 收口）**：
  - **背景（CR plan 关闭缺口承接）**：CR plan `2026-08-06-0329-1` 关闭时 Phase 3 checklist `:139-142` 四行（19-1/19-2/23-1/23-2，0529-1 Phase 4 显式追加的扫描 P1 候选吸收项）仍为 `[ ]` 被带关，closure audit 未拦截；按 plan guide Rule 21 历史 plan 不回写，本 successor plan 承接执行。非目标：CR 其他已落地内容不复验、flow-designer/graph 5 项 successor（14-4/15-1/15-2/17-2/19-3）不处理。
  - **Phase 1 19-1 tree-session（决策 a：success 移队首 + digest 采纳；不叠看门狗）**：裁决两个副作用——① success-shift 同步 `acceptedBaselineDigest = head.digest`（对齐 ack 路径 `:394` 口径），避免宿主后续 echo 走 `stale-echo`/conflict fallback；② success = 「dispatch 完成即确认」，之后到访的显式 ack 自然降级 stale-ack no-op，协议语义不变。test-first：新增「success 后 enqueue 新 change 被派发、旧 head 不重复派发 + head 已出队 + digest 采纳」与「success-shift 后宿主 echo 不为 stale-ack（core 已前进场景）」2 条用例，先红（pendingQueue 滞留 1 + `stale-echo`）后绿；`tree-session.ts:259-272` success 分支改 `queue.shift()` + `acceptedBaselineDigest = head.digest` + `void this.dispatchNext(generation)`；ack 超时看门狗按计划 Deferred 预登记为 `watch-only residual`（success 移队首已消除滞留/重复派发，Successor Required: no）。flow-designer-renderers 32 files/227 tests 全绿 + 包 typecheck 绿。
  - **Phase 2 19-2 calendar exportToPNG（决策 b：handle async 返回 `{ok:false,error}`）**：`calendar.tsx:224-226` `exportToPNG` case 改 `.then(() => ({ok:true}), (error) => ({ok:false,error}))` 消费 promise（消除 unhandled rejection + 不谎报成功，错误已由 `setExportError` UI 呈现）；`use-calendar-export.ts` rethrow 保留（handle 消费后无 unhandled rejection），`!blob` 路径从静默 return 改 reject（真实失败信号）；AbortError 路径保持静默 resolve。test-first：新 `calendar.export-handle.test.tsx`（真实 registry 捕获 handle，mock html2canvas reject——先红：`{ok:true}` + vitest 捕获 unhandled rejection；后绿：`{ok:false,error}` + 挂 `process.on('unhandledRejection')` 断言零拒绝）2 用例 + hook 层 2 条新用例（html2canvas 失败 reject + exportError 呈现、无 blob reject）。scheduling 77 files/872 tests 全绿 + 包 typecheck 绿。
  - **Phase 3 23-1 xui-roles-plugin（裁决 a：导出，非删除）**：AMIS `xui:roles` 编译期整树裁剪为真实能力实现（12 测试完整）+ `runtime-factory.ts:101` 宿主插件注入点存在 + `flux-guide/11-host-integration.md:171` 明示「未从包入口公开导出」属自认 API 缺口（宿主被迫复制逻辑）→ 经 `runtime-plugins.ts` 中转 + `src/index.ts` barrel 导出 `createXuiRolesPlugin`/`filterByRoles`/`XuiRolesPluginOptions`（公共 API 变更按 mission 授权留痕，导出面最小化：未顺带导出 sortRendererPlugins）。test-first：新公共面契约测试 3 条（从 `../index.js` import——先红 undefined → 后绿；插件名 `flux:xui-roles` + prune 语义 + 整树裁剪），先红后绿。flux-guide 11-host-integration.md 同步为「已导出 + 用法」；flux-runtime 121 files/1399 tests 全绿 + 包 typecheck 绿。
  - **Phase 4 23-2 gantt/components 死家族（裁决 b：删除 + 测试归档）**：逐文件 grep 实证零生产引用（仅家族内互引 + 自身测试；`baseline-bars.tsx` 为 live——`gantt.tsx:18` 使用，保留）；design-export.md/design-filter-sort-group.md 为设计超前面且无宿主消费方（gantt handle 仅 zoomIn/zoomOut/scrollToToday/scrollToTask）→ 删除 7 文件（export-handles/filter-bar/scheduler-config/resource-load-view/grid/timeline/resource-load）+ 4 测试（归档 `docs/archive/gantt-dead-components-2026-08-06/` 供未来 capability 计划检索）；两 design 文档 Implementation 节改「未实现（已移除）」标注（指向归档路径，`check:active-doc-code-anchors` 零失效）；清理 16 条随删失效的 i18n 键（en-US/zh-CN `scheduling.gantt.*`，`check:i18n-keys` 绿）。scheduling 73 files/842 tests 全绿 + 包 typecheck 绿；e2e 无引用（Phase 5 全量确认）。
  - **Phase 5 收口**：roadmap 扫描发现路由登记区 19-1/19-2/23-1/23-2 四行回写 fixed + successor 承接说明（含 CR plan 未勾选项带关的 Rule 21 说明）；daily log 本条目。
  - **验证**：受影响包 focused 全绿（flow-designer-renderers 227 / scheduling 842 / flux-runtime 1399）；全量 `pnpm typecheck`/`build`/`lint`/`test`/`check` 归 Closure Gates（独立 closure-audit 通过后 Plan Status → completed）。

- **Plan 2026-08-06-0556-1 Follow-up Backlog 收口（F3/F4 微修 + R1 P2 候选 R2 复核与路由）执行完毕（4 Phase 全 completed；roadmap Follow-up Backlog 三条 `[x]`；closure-audit 由独立 fresh session 收口）**：
  - **Phase 1 R2 复核（15 条 P2 候选全数 live 核对，裁决表 `docs/audits/multi-audit-r2-verdicts.md` 零悬挂）**：01-03/03-01 属实（3 个 root 导出零外部消费者——createCrudNormalizedSourceContext 仅自身包单测、GAP_TOKENS 仅 docs 引用、normalizeProgressValue 仅包内消费）；14-3 属实（afterEach 无 innerHeight 还原）；14-4 属实（canvas-bridge.test.tsx 588 行、setup/mock :1-212 约 36%、固定空快照 mock :91）；15-1 属实（tree-session-impl.ts:310-311 双 JSON.stringify）；15-2 属实（shell-controls.ts:23 Math.max(NaN,min)===NaN + action-provider:447-453 typeof number 放行 NaN，confirmed fail-closed 缺口）；15-3 属实但 live 不可达（唯一调用点 overlay:116 有 `if (wasmUrl)` 守卫，fetcher fail-closed 已有契约测试）；16-3..16-7 全部属实（quick-reference 缺 ai/graph 行、timeline design.md:27-28 残留、tree-mode.md:228 导出口径漂移（R1 记 :222）、roadmap:63 计数、plan 453:3 日期）；17-1 属实（terminology.md 零命中 4 词）；17-2 属实（graph-renderer.tsx:157 payload type 用字段名）；19-3 属实（designer-page-body.tsx:193-200 catch→null，同包 :219 reportHostIssue 可用）；23-3 属实（action-core.test.ts:315-320 仅 not.toThrow）。**路由：fix-in-this-plan 10 项 / successor 5 项（14-4/15-1/15-2/17-2/19-3，flow-designer/graph 域，显式登记不降级）/ keep 1 项（17-3 观察项）**。
  - **Phase 2 F3+F4（test-first）**：F3 `missions/component-audit.json:3` 计数 112 → 113（`rg "112 个注册组件"` 零命中）；F4 `swipe-cell.tsx` 移除 :41-50 冗余 openState→openStateRef effect-mirror（前置 Proof：setOpenState 调用点全文件仅 :130/:143 两处且均在 handler 内先写 ref，无第三条路径）——新增「快速连续手势同目标不重复派发」守卫用例（MA-02 语义）先绿，移除 effect 后 mobile 包 171 测试全绿。
  - **Phase 3 低成本修复 10 项落地**：14-3 innerHeight 原值保存 + try/finally 恢复（data 723 绿）；23-3 action-core 委托断言强化——mock scope update/merge 真实现 + `wrapped.get('x')===99` + `scope.get('x')===99` + merge 同理 + bindings 可见（action-core 207 绿）；16-3 quick-reference 补 flux-renderers-ai/graph 两行（layer 7）；16-4 timeline design.md:27-28 改「已实现（v2）」+ C5.2 卡 timeline.md:44 补 v2 事后注记（data-ownership 恒发契约，e2e 断言归 CR Phase 5）；16-6 roadmap:63 补「注册合计 114 = 113 逐卡范围 + graph 1（G1 plan 独立闭环）」+ :109 同口径；16-7 plan 453 Last Reviewed → 2026-08-06；17-1 terminology.md 补 TB/LR、TreeDocument/TreeDocumentSession、受控当前事件、WorkbenchShell 4 条词条；15-3 **fail-closed 裁决（方案 a）**——删除 `DEFAULT_WASM_URL`（unpkg 硬编码），`prepareWasm` 未提供 wasmUrl 即 throw，`resetWasmPromise()` 无参改全清缓存；先红（2 fail）后绿，scheduling 包 868 测试全绿；01-03/03-01 方案 a——3 处定义点补 JSDoc（crud-renderer-state.ts:264 / resolve-gap.ts:1 / progress.tsx:21，保持 root 导出，非结构性）；16-5 tree-mode.md:228 措辞改「root export（index.ts:9-10）但渲染器必须经 core 会话间接使用」+ :33/:215 core-private 同步修正。flow-designer/graph 代码项（14-4/15-1/15-2/17-2/19-3）successor 登记（Deferred But Adjudicated + 裁决表路径），零静默。
  - **Phase 4 收口**：裁决表终态明细（fixed/keep/successor 逐条证据）；roadmap Follow-up Backlog 三条 `[x]`（各注明本 plan 路径）；8 个分析维度文件「复核结论」全部回写 R2 完成态（03/15/17 从「待复核」改「R2 复核完成」，01/14/16/19/23 追加 R2 结论）；daily log 本条目。
  - **验证**：受影响包 focused 全绿（mobile 171 / action-core 207 / data 723 / scheduling 868 / content 285 / flux-react 467）；全量 `pnpm typecheck`/`build`/`lint`/`test` 归 Closure Gates。
  - **文档**：`multi-audit-r2-verdicts.md` 新建；roadmap/timeline design.md/timeline 卡/quick-reference/terminology/tree-mode.md/plan 453/mission json 同步；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-0529-3 button href 安全协议校验修复与同根裁决一致性执行完毕（2 Phase 全 completed，Plan Status → completed；closure-audit 由独立 fresh session 收口）**：
  - **Phase 1 裁决**：① button 卡 P2-3 → **P0 重裁**回写 `button.md:40`（checklist §3 P0 定义「安全漏洞/任意 URL → 当轮自动修复」+ 同根 link 已裁 P0 先例；原 C1.3 轮裁 P2-3 延后 CR 为裁决不一致，本 plan 收口重裁；卡状态 open → fixed）；② helper 位置 **终裁 b（提升 flux-core `utils/url.ts` 公共层）**——CR plan 0329-1 Phase 1 D5 联动裁决并执行（`cr-inventory-adjudication.md` D5：flux-core 为 basic/content 共同依赖层 + utils/ 同型 helper 惯例 + content sanitize.ts 再导出零改动），本 plan 记录终裁；`data:` 口径按 CR D5 终裁全放行（下载链路契约 + opaque-origin 非同质风险），断言与实现已对齐；③ **同根一致性规则**记录于两张卡——`button.md:40` 与 `link.md` P0-1 条目各补「同根同型安全缺陷跨族时以已裁定 severity（P0）为准」行，供 CG checklist v2 吸收。
  - **Phase 2 test-first 修复（落地主体由 CR Phase 2 先行执行，commit `de0725ba`，本 plan 收口）**：`button-href-safety.test.tsx` 现 7 用例（javascript: / JaVaScRiPt: / vbscript: / blob:+file: / http-https-mailto-tel / 相对 URL×4 / data:）——CR 落地时先红后绿，本 plan 补 blob:/file: 显式断言后全绿；`button.tsx:239-250` href 分支经 flux-core `isSafeNavigationUrl` 白名单过滤（anchor 形态保留、不安全 href → undefined 不可导航，与 link.tsx 同构）；`rg "javascript:" button.tsx` 仅命中注释无直传；**CR 交接记录**：CR plan Phase 2 button 条目与本日 daily log 双向交叉核对——CR 已落地修复，本 plan 不做重复实现（只做重裁/一致性规则/回归验证）。
  - **验证（full-green）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（basic 包 48 files/479 tests 含 button-href-safety 7 用例）；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 14 既有 pre-existing 命名清单零新增，其余 10 项全绿。
  - **文档**：`button.md`/`link.md` 卡同步；本 daily log 条目；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-0529-2 扫描 P1 契约漂移与覆盖缺口修复执行完毕（3 Phase 全 completed；roadmap 01-02/16-1/14-1/16-2 行裁决列回写 fixed；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 boundaries.md unstable-only 清单同步（P1 01-02/16-1 收敛）**：live 复核 `packages/flux-react/src/index.tsx:6-18,27`（contexts 组 + RenderNodes 均 root 稳定导出）与 `unstable.ts:1-17`（真实 unstable 面 = createHelpers/mergeActionContext/EMPTY_SCOPE_DATA/rendererHooks/RenderInstancePathContext/StructuralLoopContext/useRequiredContext/publishOwnerStatus/executeApiObject/createProjectedScopeStore）→ `docs/architecture/flux-runtime-module-boundaries.md:467-471` 示例清单重写（移除 RenderNodes/FormContext/ScopeContext/RuntimeContext，替换为 live unstable 集合）；`boundaries.md:473` 顺带修正 drift——`createReadonlyScopeBinding` 实际不在 `flux-react/unstable`（仅 flux-runtime root `index.ts:28` + flux-react root `index.tsx:102`，`crud-renderer.tsx` 走后者），删除「unstable 便利 re-export」误述。`check-active-doc-code-anchors` 308 docs exit 0。
  - **Phase 2 index.md phantom `service` 移除与条目补齐（P1 16-2 收敛）**：注册数组 live 对账后——data 族（`data-renderer-definitions.ts` table/data-source/chart/tree/list + `crud-renderer-definition.ts` crud + `w2a-data-composition-definitions.ts` pagination/statistics）去除 phantom `service`、补齐 `statistics`；新增「图数据（`flux-renderers-graph`）」`graph`（`graph-definitions.ts:6`）与「企业调度（`flux-renderers-scheduling`）」gantt/kanban/calendar/barcode-input 两族清单（Proof 发现同批缺口，超出 plan 列出的 5 条，按 Non-Blocking Follow-ups 同批修正）；content 族补 `diff-view`（`content-renderer-definitions.ts:510`）、form 族补 `button-group-select`（`renderers/input.tsx:650`）；`service` 移入 removed 说明（对齐 `amis-baseline-matrix.md:121` 口径，`:321` + `:410` 两处）；`:363` shipped 枚举移除 `service`、`:400` 分层规则改 removed 语境；目录清单去 `service/`、补 `graph/`/`diff-view/`/`button-group-select/`。`rg "service" docs/components/index.md` 仅剩 removed 说明语境；锚点 exit 0。
  - **Phase 3 useDesignerShortcuts 零覆盖清零（P1 14-1 收敛）**：新增 `packages/flow-designer-renderers/src/use-designer-shortcuts.test.tsx`（9 用例：undo/redo/copy/paste/delete/save 6 映射真实 dispatch 断言 + Escape 不派发 + input/contentEditable 可编辑守卫 + designer 外区域守卫 + readOnly 守卫 + `features.shortcuts===false` 零副作用 + clipboard disabled 跳过 + unmount 移除监听）；mock core 仅 `getConfig()`（hook 全部可观测行为经 window keydown 直通，无产品行为改动——Non-Goals 遵守）；flow-designer-renderers 32 files/225 tests 全绿（含新文件 9/9）。
  - **验证（full-green）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task（10,397+ passed / 0 failed）；`pnpm check` 链仅 `check:oversized-code-files` 红——stash 基线对照实证 14 既有命名清单（`crud-renderer-state.ts`/`en-US.ts`/`zh-CN.ts` 等）前后 diff 逐字节一致，**零新增命中**，其余 10 项 check 逐项全绿；`check:active-doc-code-anchors` 308 docs 零失效。
  - **closure-audit**：独立 fresh sub-agent（task `ses_0292e6cfdffeCqZr3FqEdy8PBT`）首轮 1 Major（daily log 执行记录缺失、gate `:120` 早勾）→ 本条目补录后复审通过；证据见 plan Closure 节。

### 2026-08-06

- **Plan 2026-08-06-0529-1 门禁真实性与审计发现路由收口执行完毕（Plan Status → completed，closure-audit pass）**（4 Phase 全 completed；roadmap 扫描发现路由登记区落地；无 Work Item / Source Audits 标签，roadmap ❌→✅ 不适用）：
  - **Phase 1 P0**：`check:workspace-manifest-deps` exit 0（5 ERROR 清零）。scheduling devDep 补 `@nop-chaos/flux-formula`；form 侧 2 个测试（dialog-form-submit-refresh-crud / form-submit-on-submit-success-refresh-nearest）迁至 data / nop-debugger（turbo build 图双向 devDep 环约束，偏差已记录）——form 733 / data 723 / nop-debugger 126 / scheduling 867 全绿。
  - **Phase 2 P1**：oversized mission 自引命中 coverage-manifest-entries.ts 拆 3 模块（entries 139 + form 217 + data 494 行，104 条目零变化，消费侧单入口不变）；自增命中 wizard-renderer.tsx 拆 wizard-step-helpers.ts（80 行）+ wizard-step-body.tsx（68 行），主组件 644 行，`isStepDisabled` re-export 保 API；layout 103/103 + manifest 独立 tsc 绿。拆分后超限基线 **14 既有 + 0 新增**（命名清单见本日 Phase 2 条目）。
  - **Phase 3 P1**：roadmap「扫描发现路由登记」区 10 条（01-01→本 plan、01-02/16-1/16-2/14-1→0529-2、14-2→本 plan、19-1/19-2/23-1/23-2→R2 属实→CR）零未分类；CR `:18` P1 债务断言修正 + 裁决表补扫描输入节；CV Phase 1 `pnpm check` 条目校正注记；CG Non-Goals/Deferred/Follow-up oversized 数字改 14 既有口径；AGENTS.md 验证清单 + Commands 补 `pnpm check`（零新增命中口径）；5 个维度文件（01/14/16/19/23）`待复核 → 已路由`。
  - **Phase 4**：R2 复核 4 条全部 live 属实（tree-session.ts:259-263 success 无 ack 看门狗 + 测试无 success-无-ack 用例；calendar.tsx:224-226 void+ok:true + use-calendar-export.ts:66-74 rethrow；xui-roles-plugin 无 barrel/exports 可达；gantt/components 零生产引用）→ 裁决归 CR，已显式追加 CR plan Phase 3 checklist（「扫描 P1 候选吸收」×4，有实际执行者）。
  - **验证**：typecheck/build/lint 32/32、`pnpm test` 59/59 task（**10,397 passed / 0 failed**）；`pnpm check` 链仅 `check:oversized-code-files` 红（恰为 14 既有 pre-existing 命名清单，零新增），其余 10 项（react19/src-artifacts/active-doc-code-anchors/package-css-exports/flux-bundle-pack/i18n-keys/workspace-manifest-deps/schema-prop-coverage/audit-suspects/audit-renderer-browser-io）全绿。
  - **closure-audit**：独立 fresh sub-agent（task `ses_02943cfa8ffeXmhE1xS5XyUCE6`）**approved**，零 Blocker/零 Major，2 Minor 非阻塞（daily log wizard 行数计数口径、roadmap 01-01 行 form 735→733 已修）；证据见 plan Closure 节。

- **Plan 2026-08-06-0529-1 门禁真实性与审计发现路由收口（执行中）Phase 1 收尾补记（turbo 环约束）**：form ×2 修复原拟「devDeps 补 flux-renderers-data + nop-debugger」，实测闭合 turbo build 图检测的测试-only 双向 devDep 环（data→form、nop-debugger→form 已存在；form→data/debugger 新增即闭环）→ `pnpm build` FAIL（Circular package dependency）。改为等价且无环落地：2 个 form 测试文件迁至依赖方向单向的宿主包——`dialog-form-submit-refresh-crud.test.tsx` → `flux-renderers-data/src/__tests__/`（CRUD 集成测试，data 已 devDep form）、`form-submit-on-submit-success-refresh-nearest.test.tsx` → `nop-debugger/src/`（debugger harness 集成测试）；form manifest 零新增 devDep。验证：`check:workspace-manifest-deps` exit 0（form 无未声明跨包导入）、`pnpm build` 32/32（环消除）、form 733 / data 723 / nop-debugger 126 测试全绿（迁移测试在目标包内通过）。

- **Plan 2026-08-06-0529-1 门禁真实性与审计发现路由收口（执行中）Phase 3+4 路由登记与 R2 裁决**：
  - **Phase 3**：roadmap 新增「扫描发现路由登记」区（10 条 P0/P1 逐条含来源/severity/去向，零未分类：01-01→本 plan P1 fixed、01-02/16-1→0529-2（同根因）、14-1→0529-2、14-2→本 plan P2 fixed、16-2→0529-2、19-1/19-2/23-1/23-2→R2 属实→CR）；CR plan `:18`「无 open P1 backlog」断言改为指向本 plan Phase 3 登记区 + CR 裁决表补「扫描发现补充输入」节；CV plan Phase 1 `pnpm check` 条目改为「先记录 pre-existing red 真实清单（0529-1 Phase 2 产出）再核对新增」+ 校正注记；CG plan Non-Goals/Deferred/Follow-up 三处 oversized 数字改为 0529-1 修正后口径（14 既有 + 0 新增）；AGENTS.md 验证清单补 `pnpm check`（口径「已登记 pre-existing red 清单之外的零新增命中」）+ Commands 节说明；5 个扫描维度文件（01/14/16/19/23）复核结论 `待复核 → 已路由` 指向登记区。
  - **Phase 4 R2 复核（4 条全部 live 属实）**：19-1 `tree-session.ts:259-263` success 分支 `inFlight=null; notify(); return`（不移队首、不 dispatchNext），全文件无 ack 超时/看门狗，`tree-session.test.ts` 无 success-无-ack 用例（仅 neutral/cancelled/failed 移出队首 + ack 匹配）；19-2 `calendar.tsx:224-226` `void exportRef.current.exportToPNG()` + `{ok:true}`，`use-calendar-export.ts:66-74` catch 内 `setExportError(msg)` 后 `throw err`；23-1 `createXuiRolesPlugin` 仅自身源码+测试引用（无 barrel 导出、无 exports 深路径）；23-2 gantt/components 家族（export-handles/filter-bar/scheduler-config/resource-load-view/grid/timeline + 测试）全家族零生产引用。**裁决：4 条全部属实 → 归 CR 修复**，已显式追加进 CR plan Phase 3 checklist（「扫描 P1 候选吸收」×4：19-1 ack 看门狗/success 移队首+回归测试、19-2 .catch 消费或 async `{ok:false,error}`、23-1 barrel 导出或删除归档、23-2 接线或删除 + resource-load.ts 纯函数保留），有实际执行者；裁决去向已写入 roadmap 登记区。

- **Plan 2026-08-06-0529-1 门禁真实性与审计发现路由收口（执行中）Phase 2 拆分落地与基线数字**：`check:oversized-code-files` mission 自引命中 `coverage-manifest-entries.ts`（816 行）按分类拆 3 模块——`coverage-manifest-entries.ts`（139 行，聚合入口，保留 Layout/Content/Actions/Advanced/Logic 分类）+ `coverage-manifest-entries-form.ts`（217 行，Form 族）+ `coverage-manifest-entries-data.ts`（494 行，Data 族），`coverage-manifest.ts` 消费侧单入口不变，104 条 entry 计数零变化，manifest 模块独立 tsc 编译通过；mission 自增命中 `wizard-renderer.tsx`（774 行）拆纯函数区到 `wizard-step-helpers.ts`（80 行，`resolveStepKey`/`toStepKeyString`/`isStepVisible`/`isStepDisabled`/`findStepIndexByKey`/`asReactNode`/`resolveStepLifecycleAction`/`computeCanGoTo`）+ `WizardStepBody`+`CompiledWizardStep` 到 `wizard-step-body.tsx`（68 行），主组件 644 行，`isStepDisabled` 由 wizard-renderer.tsx re-export 保 API（测试 import 不破坏）；layout 包 103/103 测试绿 + lint/typecheck 绿。**拆分后超限基线（14 既有 + 0 新增，命名清单，Phase 3 登记数据源）**：`flux-i18n/src/locales/en-US.ts` 1034、`zh-CN.ts` 1033、`flux-renderers-data/src/crud-renderer-state.ts` 794、`word-editor-core/src/__tests__/document-io.test.ts` 765、`flux-compiler/src/schema-compiler-schema-definition-unified.test.ts` 764、`flux-compiler/src/schema-compiler/shape-validation-rules.ts` 750、`flux-renderers-form/src/renderers/form.tsx` 748、`input-choice-renderers.tsx` 748、`flux-renderers-data/src/table-renderer.tsx` 737、`flux-renderers-form-advanced/src/tree-control-controllers.ts` 726、`flux-runtime/src/__tests__/validation-rule-semantics-and-lifecycle.test.ts` 725、`flux-renderers-data/src/crud-renderer.tsx` 724、`apps/playground/src/route-model.ts` 708、`flux-renderers-form/src/__tests__/form-renderer-lifecycle.test.tsx` 702——门禁仍 exit 1（14 既有超限 pre-existing 治理债，归 successor）。

- **Plan 2026-08-06-0529-1 门禁真实性与审计发现路由收口（执行中）Phase 1 修复前基线证据**：`node scripts/check-workspace-manifest-deps.mjs` exit 1，5 条 ERROR 清单（live 复现）——`flux-renderers-form` devDeps 缺 `@nop-chaos/flux-renderers-data`（`dialog-form-submit-refresh-crud.test.tsx:7`）、缺 `@nop-chaos/nop-debugger`（`form-submit-on-submit-success-refresh-nearest.test.tsx:4`）；`flux-renderers-scheduling` devDeps 缺 `@nop-chaos/flux-formula`（`{calendar,gantt,kanban}.create-schema-renderer.test.tsx:5` ×3）。

- **Plan 2026-08-06-0343-1 CG Guard 沉淀执行完毕**（5 Phase 全 completed，Plan Status → completed；roadmap CG 行 `planned → done`；closure-audit 由独立 fresh session CLOSURE_VERIFY 收口）：
  - **Phase 1 pc-index.md 汇总索引**：`docs/audits/per-component/pc-index.md` 建成（arm 范式）——逐组件卡索引 **113 行 = 113 卡**（grep 对账一致）、P0/P1 发现索引 **全量 132 条零遗漏**（P0 ×4 / P1 ×128，含 2 条卡内 meta 条目 combo-P1-1/input-date-P1-0）、P2/P3 代表性 12 条、**CX-1..CX-12 共性模式索引 12 行完整**、Family/Work-Item 汇总表、审计工具基线表（C0 12 脚本 → CV 复跑 + CG 新增行）、CR 衔接说明（裁决表 17/19/33/7 零未分类）；计数修正 1 处（statistics 卡 P2 实际 2 条（P2-2/P2-4，编号跳号），汇总 P2 226 → **225**）；`check:active-doc-code-anchors` 308 docs 零失效；手工抽查 file:line 引用（ai-feedback.tsx:41 / cards-renderer.tsx:157 / barcode-input.tsx:113）可解析。
  - **Phase 2 工具脚本升级评估与落地**：裁定 6 项零悬空——① 事件派发 ctx（bug-83/CX-10 家族）：**not-mechanizable**（balanced-paren 参数提取可行，但 `packages/flux-renderers-*` 命中 ~33 处单参派发点逐条对账 **100% 落在已 closed 卡裁决 pass 面**（C3.x 空参/单参契约裁决 + upload-field design §8.1 + button/notice-bar 原生 DOM 转发 + normalizeActionEvent），硬性门禁假阳性不可接受；待 AST 语义分析（eventContracts 载荷键 × ctx 键交叉验证）后再评估，替代防护落 checklist v2 维度 7 + lessons 03）；② reaction 接线（CX-9/CX-12）：**not-mechanizable**（definition→renderer 跨文件启发式映射 + 接线语义需 AST；当前 9 个 reaction 键全接线，替代防护落维度 7 + lessons 05）；③ data-slot 唯一性：**not-mechanizable**（需 JSX 结构分析；行级共现仅 variant-field-view 1 合法样本，替代防护落维度 5）；④ i18n 盲区：第 1 盲区 p2p3 eslint 已修（登记），第 2/3 盲区 CR 已收口（裁决表 + Phase 4 实证清零 + CX-7 useFluxTranslation 前缀归一化），仅登记不重复评估；⑤ URL/sanitize 协议：动态 href 面不可静态追踪（not-mechanizable，运行时 isSafeNavigationUrl 白名单兜底），**新建立项（mechanize）**——**`check:audit-renderer-browser-io`**（`scripts/audit/find-renderer-browser-io.mjs`，INV-1 直连浏览器 IO 零容忍：fetch/XMLHttpRequest/axios/localStorage/sessionStorage/indexedDB/window.open/history.pushState/WebSocket/EventSource/RTCPeerConnection/navigator.sendBeacon/location 导航，作用域 `packages/flux-renderers-*` 非测试文件，字符串/注释内忽略）——**基线零命中**（负样本 9 模式全检出 + 注释忽略实证），注册根 package.json 并加入 `pnpm check` 聚合（命中即红，对齐 oversized 先例；不进 lint runner——eslint 仅匹配 ts/tsx）；`pnpm lint` 全仓绿。
  - **Phase 3 lessons（4 条，README 格式）**：`docs/lessons/` 新增 `02-unit-green-but-real-browser-broken-bug-73-pattern.md`（bug 73 模式：每卡必检真实浏览器宿主场景）、`03-schema-event-dispatch-requires-event-evaluation-bindings-scope-ctx.md`（事件 ctx 家族：runtime args 求值仅合并 evaluationBindings+scope）、`04-surface-args-embedded-schema-body-eager-evaluation-cx-11.md`（`__nopPreserveLiteral` envelope 契约）、`05-reaction-field-wiring-requires-ready-and-component-handle.md`（reaction 三件套接线）；README Index 节 + Component-Audit Lessons（CG）摘要节同步。
  - **Phase 4 checklist v2 修订**：`component-audit-checklist.md` 标 v2 + 「变更摘要（v1 → v2）」节（维度 5 data-slot 唯一性 / 维度 7 事件 ctx + reaction 三件套 / 维度 9 eslint 盲区 + rg 兜底 / 维度 12 bug 73 每卡必检固化 / 维度 16 双参契约断言；维度 18 增补 `check:audit-renderer-browser-io` 引用）；P0/P1/P2/P3 裁决表与自动修复纪律语义不变（仅措辞）；`per-component/README.md` 模板 v2 同步（v1 冻结声明 → v2 修订声明，指向 checklist 变更摘要）；历史审计卡未回写（git diff 确认无 per-component/\*.md 卡改动）；roadmap 无 checklist v1 措辞引用（仅 `component-audit-checklist.md` 无版本限定，无需同步）。
  - **Phase 5 收口**：project-context.md 基线段回写 CV full-green 实测（typecheck/build/lint 32/32、test 59/59（10,397 passed/0 failed）、e2e 1054 passed/43 skipped/6 watch-only 归因清单、component-lab 334/1/2、smoke 111/111、host-surfaces 42/42、component-audit mission 全 done、pnpm check pre-existing red 集）；docs/index.md `docs/audits/` 目录角色登记 pc-index；roadmap CG 行 `planned → done`（证据注记）；daily log 本条目。
  - **验证**：纯文档 + 内部脚本计划（无产品代码变更，按 guide「纯文档计划」条款剔除 typecheck/build/test 门禁）；`pnpm lint` 全仓绿；新脚本 `pnpm check:audit-renderer-browser-io` 基线零命中 + 负样本实证；`check:active-doc-code-anchors` 零失效；`pnpm test` 抽样复跑相关包无回归（脚本不进产品包/类型面，无 vitest 层）。closure-audit 由独立 fresh session 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-0329-2 CV 全量验证执行完毕（full-green verification）**（4 Phase 全 completed，closure-audit pass 后 Plan Status → completed；roadmap CV 行 `planned → done`）：
  - **Phase 1 workspace 全量单元验证**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task（`--force` 新鲜执行 0 cache，**10,397 passed / 0 failed**）；`pnpm check` 既有 pre-existing red 集记录（`check:oversized-code-files` 16 文件 >700 行（08-04 VERIFY 基线 14 + mission 自引/自增 2 命中，0529-1 plan 登记在案）、`check:workspace-manifest-deps` 5 ERROR（form ×2 + scheduling ×3，0529-1 plan P0 认领）、`check:audit-suspects` 0 ERROR、active-doc-code-anchors/package-css-exports/i18n-keys/schema-prop-coverage 全绿）——**新增命中 1 处已修复**：`check:flux-bundle-pack` 红（2026-07-28 commit `0f8ae307` 把 `exports["./style.css"]` 从字符串改为 `{default:...}` 对象形态，`check-flux-bundle-pack.mjs:102` 仍要求严格字符串相等；flux-bundle `index.test.tsx:28` 已支持双形态）→ 脚本归一化（与单测同构），`check:flux-bundle-pack` 转绿 + flux-bundle 7/7 复验。
  - **Phase 2 e2e 全量归因**：全量 `pnpm test:e2e` 多轮（首轮 10 failed → 修复 2 项真缺陷后稳定 6 failed）：**① 真缺陷修复 ×2（最小修复）**——`form-input-enhancements.spec.ts:55` key-value placeholder 断言仍硬编码英文 `'Key'`（p2p3 i18n 清理后渲染 `键`（zh-CN），c3-4-host-surfaces 已改 locale-agnostic 而本 spec 漏改，隔离复跑恒失败）→ 改 `/Key|键/` 后 3/3 绿；`playground-entry-pages.spec.ts:423` route-inventory 缺口——`graph-demo` 路由存在且有专项 spec（graph-demo.spec.ts 8/8 绿）但 `ROUTE_ASSERTIONS` 无对应断言 → 补断言后 63/63 绿。**② watch-only 归因清单（clean HEAD stash 对照 + 隔离重跑实证，零悬空）**：c3-5-host-surfaces:27/81 + w3d-editor:28（Tiptap 批次——隔离重跑部分绿、clean HEAD 同值复现，已知清单）；gantt-perf:21/44 + kanban-perf:34（**根因实证：本机主屏刷新率 50.00Hz（`system_profiler` 1920x1080 @ 50.00Hz），rAF 上限 50fps——idle 亦仅 49.5fps，gantt >50/kanban >60 阈值在该显示环境不可达**；clean HEAD stash 3/3 同值复现；08-05 C9 记录在案同结论）；ai-attachments:15/40 首轮 2 failed 后续多轮全绿（瞬时负载 flake，非本路线引入）。最终全量：**1054 passed / 43 skipped / 6 failed（6 failed 全部为上述 watch-only 清单，clean HEAD 同值复现，归因零悬空）**；c5-2 host-timeline 10/10 绿（CR Phase 5 修复生效确认）。
  - **Phase 3 已闭包审计项回归抽查**：component-lab 全量 **334 passed / 1 skipped / 2 failed**（2 failed 即 c3-5 Tiptap 已知批次，隔离复跑绿）；smoke+navigation **111/111**；各族 host-surfaces 代表集 **42/42**（c1-1/c2-1/c3-1/c4-1/c5-2（含 timeline，CR 修复确认）/c6-1/c7/c8-1/c9）；随机抽 3 张已 closed 卡 spot-check 零意外——button（P2-3 `isSafeNavigationUrl` 提升 flux-core `utils/url.ts:23` + `index.ts:60` 导出 + `button.tsx:245-248` href 剥离接线 + `button-href-safety.test.tsx` 6 用例全在）、gantt（P2-1 `gantt-editor.tsx:40-41` onSave 持久化 + UpdateTaskCommand、P2-3 `undo-stack.ts:161/206` DeleteTaskCommand + restoreSubtree、P2-4 `use-gantt-keyboard.ts:1/45` useEffectEvent）、pull-refresh（P1-1 `pull-refresh.tsx:145-152` ctx 注入、P1-2 eventContracts、P2-1 `pull-refresh-lab-page.tsx`）。
  - **文档收口**：daily log 本条目（full-green 记录 + 计数 + watch-only 归因清单）；提交信息含 `full-green verification` 标记；roadmap CV 行 `planned → done`；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

- **Plan 2026-08-06-0329-1 CR 跨族集中修复与裁决执行完毕**（5 Phase 全 completed，待 closure-audit pass 后 Plan Status → completed）：
  - **Phase 1 输入盘点与裁决**：消费 p2p3 Phase 5 产物 `docs/audits/cr-input-inventory.md`（69 剩余 + 17 handled 交叉核对）+ `rg "归 CR"` 全量核对，产出 `docs/audits/cr-inventory-adjudication.md` 裁决表（17 handled-by-p2p3 / 19 fix / 33 keep / 7 defer-other，零未分类；聚合引用覆盖说明；button P0-1/scope-debug 非归 CR 备注）；5 项 Decision 终裁——D1 cards selectable-card ARIA → **aria-pressed**（ui Card 强制 role=button，ARIA 规范切换态）、D2 wizard 焦点 → keep（aria-current 已提供 AT 上下文）、D3 list hasMore → keep（total 已知才 hide sentinel，缺省 true 支持增长模式 = 跨组件分页语义基准）、D4 推荐句柄复核通过（json-view/collapse/wizard/pagination 均非承诺契约）、D5 button helper → **提升 flux-core `utils/url.ts`**（content 再导出，data: 全放行与 link 契约一致）。
  - **Phase 2 shared 缺陷（test-first）**：① button href 协议校验——`isSafeNavigationUrl` 提升 flux-core 公共层（index.ts 导出 + `docs/architecture/flux-core.md` URL Safety Utility 节 + button design.md 契约行），button.tsx href 分支剥离不安全协议（anchor 形态保留与 link 同构），`button-href-safety.test.tsx` 6 用例先红后绿；② combobox readOnly 视觉冻结——`input-choice-renderers.tsx` combobox root/input/trigger/clear disabled + chips readOnly+disabled + aria-readonly（comboboxFrozen = effectiveDisabled || readOnly），`select-readonly-combobox.test.tsx` 6 用例先红后绿（form 735 / form-advanced 1033 零回归）；c3-3 host-cb-custom frozen 断言同步新契约（菜单不再可打开）。
  - **Phase 3 P2 backlog 修复（test-first）**：calendar loadAction rejection → `reportRuntimeHostIssue`（env.notify 用户可见错误态，与 form init/load 同构）；barcode scannerError 五路径清除（clear/输入/重扫/overlay 关闭/resetValue）；gantt 三项——editor region `onSave` 持久化 + undo 命令、拖拽/链接/删除/编辑器保存全量接线 undo 栈（DeleteTaskCommand + store.restoreSubtree + AddLink/RemoveLink/UpdateTaskCommand 复用）、use-gantt-keyboard useEffectEvent 稳定化（监听仅随 containerRef 挂载一次）；kanban controlled 门控变更事件 + activity log（onCardClick/onColumnClick 仍派发）+ onCardMove/onCardClick/onCardAdd/onCardRemove payload 补 `card: BoardItem`；cards aria-pressed（测试断言迁移）；picker `searchable` 补 valueType:'boolean'；P3 记录项裁决全部落表（keep 逐条理由 + picker P3-2 fix）。scheduling 867 / content 285 全绿。
  - **Phase 4 dim 17 同步 + i18n 实证清零**：CX-7 潜伏项根因修复——`useFluxTranslation` t() 经 `normalizeTranslationKey` 前缀归一化（flux-i18n i18n.test +1 先红后绿，node 实证 bound-ns prefixed key 原返回原始 key）；硬编码清理 8 处（gantt DEFAULT_COLUMNS/zoom 默认/资源负载表、barcode overlay aria、graph 4 aria-label → flux.graph.\* 新键，graph 在 flux-i18n 覆盖面内 → 本 plan 清理）；dim 17 同步 6 项（calendar §8/§12.1/§12.2、kanban definitions 注释 + design §7、barcode §12.1/§12.2 未实现标注、gantt §8.1/§8.3/§9.0/§12.7/§12.8 未实现标注 + example.json `${event.taskId}`→`${_taskId}`、variant-field §5 文档化、statistics 补 amis-baseline-matrix 行）；route-matrix.test.ts layout/content 路由改 def 枚举校验（+2 用例）；`check:i18n-keys` 绿。
  - **Phase 5 e2e 残余**：c5-2 host-timeline `:201` 断言按方向 a（timeline v2 契约）改为 `data-ownership='local'`（renderer-markers-and-selectors.md 已登记契约）；c5-2 10/10、w4b 17/17、c3-3/c9/c6-2/w2a/c1-3 相关族复跑零新增失败。
  - **验证**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（scheduling 1 条预存在 warning）；`pnpm test` 59/59 task 全绿（10,363+ passed / 0 failed）；`pnpm test:e2e` 1050 passed / 43 skipped / **8 failed 全部 pre-existing**（playground-entry:423、form-input:55、w3d:28、c3-5 ×2 经 clean HEAD stash 实证同值复现；gantt-perf/kanban-perf 为已知 watch-only 机器负载 flake）——本 plan 零新增 e2e 失败。
  - **文档收口**：8 张审计卡 backlog/dim17 状态回写 fixed（button/condition-builder/calendar/barcode/gantt/kanban/cards/picker + variant-field/statistics）；roadmap CR 行 `planned → done`（证据注记）；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

### 2026-08-06

- **Plan 2026-08-06-0814 DingTalk 审批流示例视觉对齐 closure-audit pass**：独立 fresh sub-agent 核验 Phase 1–5 落地（collectDataAttrs + 三渲染器转发、ding-flow-edge 标签、features 接线/Controls top-left、dingtalk schema）、契约/接线测试与 e2e 6 例复跑全绿（472/472、216/216、175/175、dingtalk-visual 6/6）、文档三处同步与临时脚本清理，deferred 分类诚实——零 Blocker/零 Major，plan 标记 `completed`（证据见 plan Closure 节）。

- **Plan 2026-08-06-0814 DingTalk 审批流示例视觉对齐执行完毕**（`docs/plans/2026-08-06-0814-dingtalk-visual-alignment-plan.md`，Phase 1–4 completed，Phase 5 待 closure-audit）：
  - **诊断**（Playwright 程序化检查，`_tmp/dingflow-inspect1..6.mjs`，已清理）：① flow-designer「钉钉审批流」卡片无样式的根因 = schema body 的 `data-slot`/`data-node-variant` 不被 flex/text/icon 转发，`flow-designer-nodes.css`（219 行钉钉样式）选择器全部落空；② 分支标签（branch.data.label）数据已在 edge data 但未渲染；③ `features.minimap/controls` 未接线到画布；+ 按钮几何经坐标校正实证正确（在线上），纳入 e2e 锁定。
  - **Phase 1 data-\* 转发**：`collectDataAttrs` helper（utils.ts，白名单 `data-slot`+`/^data-/`、string/number 才转发）+ flex/text/icon 根元素 spread；契约测试 `data-attrs-passthrough.test.tsx` 6 例先红后绿；`renderer-markers-and-selectors.md` 新增「Schema-Authored Data Attributes」节；不改 schema 类型（BaseSchema 的 `[key: string]: SchemaValue` 已覆盖，index signature 会触发 TS2413）。
  - **Phase 2 卡片样式**：CSS 219 行核对后无需修补（白字/条件绿/圆点 end 均已具备）；e2e 视觉契约 spec `flow-designer-dingtalk-visual.spec.ts` 6 例（variant 属性存在、header 背景色/标题白字/条件绿 computed-style 断言、end 节点 terminal 圆点、+ 按钮在线上（含 surface 偏移校正）、minimap 右下/controls 左上、分支标签在线上）全绿。
  - **Phase 3 分支标签**：`DingFlowEdge` 对 split edge 的 `data.label` 渲染钉钉式绿 pill（EdgeLabelRenderer、white/#15bc83、rounded-full、max-width 160px truncate、pointer-events-none），TB/LR 对称；`ding-flow-edge.test.tsx` 5 例先红后绿。
  - **Phase 4 features 接线**：`DesignerFeatures.controls` 新增（默认 true）+ `designer-canvas` 传 `showMinimap/showControls`；`<Controls position="top-left">`（原生 prop，规避 `@layer utilities` 被 react-flow 无 layer 规则击败）；dingtalk schema `features.minimap/controls: true`；接线测试 `designer-canvas-features.test.tsx` 先红（临时回退接线 1 failed）后绿。
  - **验证**：flow-designer-renderers 216/216、flux-renderers-basic 472/472 全绿；e2e `flow-designer|dingflow|taskflow|tree-mode|dingtalk` grep 58 passed / 4 skip（taskflow 2 例并行 flake 单跑复绿，非本 plan 引入）；workspace typecheck/build/lint 32/32 + test 59/59 由 mission-driver 验证 run 记录（见上一条）。
  - **文档**：`tree-mode.md` 同步分支标签渲染 + features 接线 + body data-\* 契约；`renderer-markers-and-selectors.md` 同步转发契约；closure-audit 由独立 fresh sub-agent 执行。

- **MISSION component-audit 验证 run（full-green verification）**：p2p3/CR/CV/gate/scan/button-href/followup/dingtalk-visual 在途工作区全量验证——`pnpm typecheck` 32/32、`pnpm build` 32/32（仅预存在 INEFFECTIVE_DYNAMIC_IMPORT warning）、`pnpm lint` 32/32（0 error）；`pnpm test` 59/59 task 全绿（10,363+ passed / 0 failed，flow-designer-renderers 216 passed）；e2e `flow-designer|dingflow|taskflow|tree-mode` 48/48 passed（3 skip 为预存在视觉截图用例）+ component-lab c3-4/smoke/navigation 115/115 passed（0 failed）。修复 flow-designer-renderers typecheck 错误（designer-canvas.tsx:396-397 `config.features` optional 访问 → `config.features?.minimap/controls`，语义不变：undefined 默认开启，与 normalizeConfig 默认一致）。

- **Plan p2p3-rigor-remediation 执行完毕**（`docs/plans/2026-08-05-1359-1-p2p3-rigor-remediation-plan.md`，5 Phase 全 completed，Plan Status → completed）：
  - **Phase 1 i18n 硬编码清理（I1–I10）**：icon-picker 默认 placeholder / alert Close aria-label / carousel `Go to slide` / diff-file-list tab+placeholder+状态字母 / variant-field 失败文案 / tree 'Tree' 兜底 / key-value Key-Value placeholder+aria-label 全部走 `t()`；flux-i18n en-US/zh-CN 双 locale 新增 13 键（flux.form.selectIcon/key/keyEntry/valueEntry/searchIcon/variantUpdateFailed、flux.carousel.goToSlide、flux.diff.all/added/modified/deleted/searchFiles/statusAdded/statusModified/statusDeleted、flux.data.tree）；eslint 第 1 盲区修复（`words.exclude '^[\s\d\W]*$'` → `'^(?!.*[\\u4e00-\\u9fa5])[\\s\\d\\W]*$'`，node -e + eslint probe 实证中文报错、纯符号放行，`pnpm lint` 全仓 0 新增 error）；测试断言同步（icon-picker.test/diff-cross-file.test/bug-dual-state.test 走 t()，e2e c3-4-host-surfaces 用 locale-agnostic 正则）；新增 icon-picker-i18n/key-value-i18n proof 测试先红后绿。
  - **Phase 2 a11y 基础修复**：picker/icon-picker/diff-file-list 三个搜索框 aria-label；audio/video 错误回退 + markdown-editor 预览区 `aria-live="polite"`；transfer `aria-multiselectable` 按 multiple 条件发布（multiple 穿透 TransferPaneProps 两调用点）；新 proof 测试 7 例先红后绿。
  - **Phase 3 data-slot="field-control" 去重**：15 文件（form-advanced 9 + form date 族 6）全部裁定「被 FieldFrame 包裹 → 移除根节点重复 data-slot」（NodeFrameWrapper 对 wrap:true 无条件渲染 FieldFrame 实证）；新 DOM 契约测试 14 例（field-controls-dom-contract + 新建 field-control-dedup）先红后绿；8 张审计卡 P3-1 状态同步 fixed；10 份 design.md DOM 节同步。
  - **Phase 4 design.md 补齐**：object-field/array-field/detail-field/detail-view/variant-field/statistics 六份创建（schemas ↔ 注册定义 ↔ 渲染器消费三方核对）；index.md 6 条目录条目。
  - **Phase 5 CR 输入清单预提取**：`docs/audits/cr-input-inventory.md` 机械生成（94 处原始引用 → 78 处 finding 级 → 去重 69 条剩余 + 17 条 handled-by 本 plan = 86 条独立项全覆盖）；9 张审计卡延期项状态同步；roadmap CR 行补预提取注记（状态保持 planned）。
  - **验证**：workspace `pnpm typecheck`/`pnpm build`/`pnpm lint` 32/32 全绿（lint 0 error，scheduling 1 条预存在 warning 无关）；`pnpm test` 32 包全绿（10,363+ passed / 0 failed；data 722 / form-advanced 1033 / form 729 / content 283 / flux-react 467 等）。closure-audit 由独立 fresh sub-agent session 执行（证据记入 plan Closure 节）。

- **Plan 453 DingFlow 单一 Tree 布局统一执行完毕**（`docs/plans/453-dingflow-single-tree-layout-unification-plan.md`，5 Phase 全 completed，55 执行项全勾选）：
  - **Phase 1 core tree session/contract migration**：`createTreeDesignerCore(initialTreeDocument, config, options): TreeCoreCreationResult` 落地（config migration + `projectAndLayoutTree` + 原子 pair 安装）；删除 root exports `projectTree` / `layoutStructuredTree` / `simpleTreeLayout` / `layoutTreeWithElk`，`layoutWithElk` 仅保留 graph mode；tree core 禁止 `setTreeOwner` / graph-pair `replaceDocument` / `replaceDocumentFromHost`，新增 validated `replaceTreeFromHost(tree, epoch)` + `getTreeDocument()` / `relayoutTree()` / tree-only export；config migration `1.0`/`1.0.0` → `1.1.0`（autoLayout 删除、appearance→tree.layoutSize 拷贝、version 拒绝、失败原子）；`NodeTypeConfig.tree` 承接 `TreeNodeTypeConfig`（deprecated alias）；JSON-safe payload 校验、canonical 序列化、structured errors（duplicate-id/reserved-id/unknown-node-type/invalid-layout-size/invalid-spacing/invalid-tree-payload/cyclic-tree/invalid-tree-config/unsupported-version 等）；中央 tree-mode mutation gate（13+ graph 方法 no-op/null/unavailable + `mutationRejected` 诊断）；pair history replay（undo/redo/restore/rollback 恢复存储 pair 不重跑布局）。新模块：`tree-validation.ts`、`tree-structure.ts`、`tree-session-impl.ts`、`core/graph-command-gate.ts`、`core/config-migration.ts`。
  - **Phase 2 structured projection/geometry/empty branches**：唯一 `projectAndLayoutTree`（main/cross 轴、固定 footprint、整数锚点、rounding 后子树边界）；min gaps chain=60 / TB split=134 / LR split=204 / merge=120（layerSpacing 低于下限按 floor 生效，高于下限原样使用）；split/merge 共享线 midpoint 公式；`__fdTree` runtime geometry（kind/direction/ownerId/branchId/continuationId/lineMain/fanoutCross）；tree edge decoration 白名单校验（markerEnd/animated/label/body/strokeDasharray 拒绝 → `unsupported-tree-edge-decoration`，`TreeNodeBranch.data.label` 合法）；虚拟 empty branch slot（`__fd_internal__/slot/<owner>/<branch>`、`__fd-tree-empty-slot`、TB ≥120×52 / LR ≥140×32、affordance 120×32）；`insertBranchChild` + `deleteTreeNode` rewrite matrix；geometry oracle tests（TB/LR × nodeSpacing 0/60 × layerSpacing 0/80/200 零正面积相交）。
  - **Phase 3 renderer tree session/writeback/command boundary**：`TreeModeLayoutWrapper` 改用 `createTreeDesignerCore` + error surface；`TreeDocumentSession`（session UUID、serial FIFO dispatch、strict ack matching、256-entry digest LRU、coalesced tail-only、epoch replacement、`classifyActionResult` canonical classification、generation invalidation、dispose）；schema fields `treeDocumentEpoch` / `treeDocumentAckSessionId` / `treeDocumentAckDispatchId` / `treeDocumentChangeAction` 全量进入 prop contract/fields/formal validation；adapter/provider/canvas/shortcut 统一 graph-only command gate + all-or-nothing tree `deleteSelection`；host projection sanitization（virtual ids/incident edges/`__fd*`/stale selection 剥离，counts 重算）；mounted config 冻结到 key remount。
  - **Phase 4 DingFlow TB/LR rendering**：外层 geometry wrapper + 内层 clipped body（box-sizing border-box / overflow hidden）；center-based + placement（36+14px）；TB/LR handles（render-ports 显式 rounded anchors）；DingFlowEdge 只消费 `__fdTree`（butt/round、strokeWidth 1..4 clamp、无 marker/label）；overlays 从 `__fdTree` lineMain/fanoutCross 计算（移除 `outs[0]`/`ins[0]` 猜测与 BRANCH_SHORT_LEG/MERGE_SHORT_LEG）；empty slot affordance + `DingFlowAddNodeMenu` → `insertBranchChild`；`dingflow-command-dispatch` 支持 slot/merge/branch-group kinds。
  - **Phase 5 migration/docs/e2e**：playground tree schemas ×4 + docs examples ×2 迁移至 config 1.1.0（无 authored autoLayout、显式 layoutSize、edge appearance 白名单清理、action-flow 补 mergeEdgeType）；owner docs 更新（tree-mode.md 重写、design.md §17、config-schema.md §20/21、dingflow-visual-spec.md §2/3/5/7/8、api.md、collaboration.md、canvas-adapters.md、runtime-snapshot.md、designer-page/design.md、designer-view-vs-edit.md）；repo 搜索确认已删除 API 无活跃消费者（仅独立 style prototype 页保留历史常量）；新增 `tests/e2e/flow-designer-tree-mode.spec.ts` 4 用例（mount 零错误、固定 footprint/clip、node 碰撞 DOM bounds、action-flow mount）全绿。
  - **关键 bug 修复**：`migrateTreeConfig` 早期 JSON round-trip 会破坏 compiled schema body（tree tab 渲染 "Element type is invalid"）→ 改为结构化迁移保留对象引用；`use-designer-auto-layout` tree 分支改用同步 `relayoutTree()`（ELK 零调用，graph mode 保留）；edge path Playwright visibility 断言改用 path `d` 属性 + edge group count。
  - **验证**：workspace `pnpm typecheck` / `pnpm build` / `pnpm lint` 全绿；`pnpm test` 62/62 task 全绿（flow-designer-core 175、flow-designer-renderers 209、其余包无回归）；`pnpm test:e2e -- --grep "flow-designer|dingflow|taskflow|tree-mode"` 42/42 通过。playground-entry-pages `graph-demo` assertion-coverage 缺口为 pre-existing（clean HEAD 同值复现，与本次变更无关）。

- **mission-driver 起草下一批 roadmap work item 计划（CR + CV，2 个 work item，均 `draft → active`）**：
  - **CR 跨族集中修复与裁决**（`docs/plans/2026-08-06-0329-1-cr-cross-family-centralized-remediation.md`）：全部 C\*（C0–C9）与 CX-1..CX-12 已 done 后，CR 前置依赖满足——收口 shared 缺陷（button href 协议校验（live 实证 `button.tsx:238-241` 直传、C6.1 link 已修同源未接）/ condition-builder P2-4 combobox readOnly 视觉残留（`input-choice-renderers.tsx` 公共层））+ C 阶段登记 P2 backlog（calendar loadAction fire-and-forget `calendar.tsx:132`、barcode scannerError 无清除、gantt P2-1/P2-3/P2-4、kanban P2-3/P2-4）+ 5 项跨组件裁决（cards selectable-card ARIA、wizard 焦点、list hasMore 语义、推荐句柄复核、button helper 位置）+ P3 记录项逐一裁决 + dim 17 文档漂移集中同步 + i18n 残留实证清理（含 CX-7 行 barcode `flux.*` t() 潜伏项）+ e2e 残余修复（c5-2 host-timeline `not.toHaveAttribute('data-ownership')` 与 timeline v2 恒发契约冲突）→ 产出 `docs/audits/cr-inventory-adjudication.md` 裁决表（~110 处"归 CR"引用零未分类）；p2p3 plan（active）预提取 scope 不重复。5 Phase，Test Strategy 必须自动化。
  - **CV 全量验证**（`docs/plans/2026-08-06-0329-2-cv-full-verification.md`）：CR 收口后全量 typecheck/build/lint/test + e2e 0 failed（已知机器负载 flake 清单逐项归因：c3-5 Tiptap 批次/gantt-bars-and-links/c3-3 批次/gantt-perf/kanban-perf）+ 已闭包审计项回归抽查（component-lab 全量 + smoke/navigation + 各族 host-surfaces + 2-3 卡 spot-check）+ daily log full-green 记录 + full-green 标记提交 + roadmap CV 行收口。4 Phase，Test Strategy 必须自动化。
  - 两 plan 均经**独立子 agent fresh session review**（CR：task `ses_02c969ea7ffevNA4CR1r3NT8D5`；CV：task `ses_02c968683ffevxMr9s8LlhO0oE`）`pass-with-minors`（0 Blocker/0 Major），Minor 全部处理（data: 语义钉死、route-matrix 路径、BranchPicker 归因 C8.1、CX-7 潜伏项纳入、5 项决策计数、wizard 卡内 keep 起点、graph i18n 单路径、已知 flake 清单补全、pnpm check pre-existing red 基线、Test Strategy 档位）——达成共识后 `draft → active`；roadmap CR/CV 行 `todo → planned`。
  - **机械同步**：roadmap C6.3 行 `planned → done`（父 plan `2026-08-04-1757-1` completed + closure-audit pass 记录在案，行翻转此前未落地，本日由 mission-driver 补做）；roadmap 头「最后更新」→ 2026-08-06。
  - 不涉及代码变更，无 typecheck/build/lint/test 门禁。

- **mission-driver 起草最后 1 个 roadmap work item 计划（CG guard 沉淀，`draft → active`）**：
  - **CG Guard 沉淀**（`docs/plans/2026-08-06-0343-1-cg-guard-sedimentation.md`）：CR/CV plan 已 active 后，CG 是 roadmap 唯一剩余 `todo` 项（依赖边 CR → CV → CG）——收口 ① `docs/audits/per-component/pc-index.md` arm 风格汇总索引（113 卡逐卡索引 + P0/P1/P2/P3 发现索引 + CX-1..CX-12 共性模式索引 + 工具基线表 + 交叉引用，grep 对账验收）；② 工具脚本升级评估与落地（候选模式：bug-83/CX-10 事件派发 ctx、CX-9/CX-12 reaction 接线、data-slot 唯一性、i18n 盲区（第 2/3 盲区裁决归 CR 收口）、C6.1 URL 协议——裁定 mechanize/extend-existing/not-mechanizable 零悬空，落地脚本基线输出记录）；③ `docs/lessons/` ≥3 条 component-audit lessons（bug 73 真机失败模式、事件 ctx 家族、CX-11 surface args 急切求值等，从 `02-` 编号）；④ `component-audit-checklist.md` v2 修订（维度 7 ctx 派发/维度 5 data-slot 唯一性/维度 12 bug 73 必检固化/维度 9 i18n 盲区 + README 模板 v2，历史卡不回写）；⑤ 收口（daily log + roadmap CG 行 `planned → done` + project-context.md 基线回写依赖 CV full-green 实测）。5 Phase，Test Strategy 不适用（纯文档 + 内部脚本，脚本验收以运行输出为准）。
  - 经**独立子 agent fresh session review**（task `ses_02c89f98dfferqzlqgSzg7E6zl`）`pass-with-minors`（0 Blocker/0 Major），Minor 全部处理（Phase 2 ⑤ URL/sanitize 前提纠正为手工发现零脚本覆盖、`pnpm lint` 覆盖范围澄清（eslint 仅 ts/tsx，`scripts/audit/*.mjs` 不在覆盖内）、i18n 第 2/3 盲区裁决归属 CR 措辞、oversized-code-files 与 CV「归 CR/CG 治理」衔接精化、MR lessons 基线措辞（README 内嵌 4 条非独立文件）+ `02-` 编号起点）——达成共识后 `draft → active`；roadmap CG 行 `todo → planned`（mission-driver 机械同步，evidence 记入 Dependencies 列）。
  - 不涉及代码变更，无 typecheck/build/lint/test 门禁。

- **mission-driver 处理 open audit（2026-08-05-0656）P0/P1/P2 发现并起草 3 个 remediation plan（均 `draft → active`）**：
  - **审计状态**：`docs/audits/2026-08-05-0656-open-audit-component-audit.md` 是当前唯一 `Audit Status: open` 的审计文件——2×P1（F1 扫描 P0/P1 无路由去向 + CR/CV/CG 基线断言与 live 门禁矛盾；F2 button href 同根安全缺陷裁决不一致）+ 2×P2（mission json 计数陈旧、swipe-cell effect-mirror 冗余）。live 复现：`check:workspace-manifest-deps` exit 1（5 ERROR：form ×2 + scheduling ×3）、`check:oversized-code-files` exit 1（16 文件 >700 行，plan 记 14 不符）。
  - **Plan 1 门禁真实性与路由收口**（`docs/plans/2026-08-06-0529-1-gate-truthfulness-and-finding-routing.md`）：修复 manifest-deps P0（2 个 package.json 补 3 条 devDeps）+ oversized 基线修正（mission 自引新命中 coverage-manifest-entries.ts 816 行拆分、自增命中 wizard-renderer.tsx 774 行拆分，其余 14 个既有超限文件命名登记归 successor）+ 扫描发现路由登记（roadmap 新增登记区 + CR「无 open P1 backlog」/CV 14 文件/CG successor 三处 plan 文本校正）+ 扫描 P1 候选（19-1/19-2/23-1/23-2）R2 复核 + CR checklist 吸收机制 + AGENTS.md 验证清单补 `pnpm check`。4 Phase，必须自动化。
  - **Plan 2 扫描 P1 契约漂移与覆盖修复**（`docs/plans/2026-08-06-0529-2-scan-p1-doc-drift-and-coverage.md`）：boundaries.md:467-471 unstable-only 清单同步（RenderNodes/3 contexts 已 root 稳定化，文档未同步）+ docs/components/index.md phantom `service` 移除 + statistics/graph/diff-view/button-group-select 补齐 + useDesignerShortcuts 零覆盖补齐 hook 测试（undo/redo/copy/paste/delete/save 6 分支 + 3 守卫负面断言 + Escape 不派发）。3 Phase，必须自动化。
  - **Plan 3 button href 安全修复**（`docs/plans/2026-08-06-0529-3-button-href-security-remediation.md`）：按 checklist §3 把 button 卡 P2-3 重裁为 P0（同根 link 已裁 P0 修复，button 仍直出 `href`）——test-first `button-href-safety.test.tsx` + `button.tsx:238-244` 接协议校验（isSafeNavigationUrl 同构白名单）+ 同根跨族 severity 一致性规则记录 + CR Phase 2 交接记录。2 Phase，必须自动化。
  - 三 plan 均经**独立子 agent fresh session review**（Plan 1：round 1 `ses_02c22a1edffe6hOOTe4Em5qKMZ`（revised）→ round 2 `ses_02c1b4888ffe1z72vKr7vIDVxA`（pass-with-minors）；Plan 2：round 1 `ses_02c228d12ffeWFsIkLmdZ5vkn5`（revised）→ round 2 `ses_02c1b37d2ffe2GV7aT7OGshDL3`（pass）；Plan 3：round 1 `ses_02c2275d7ffe5n5tvrpiU4HbD5`（pass-with-minors））——Major 全部处理（wizard 豁免分支矛盾删除、CR 吸收机制 Fix 项、{2}/{3} 占位符对调修正、escape→save 快捷键清单修正），达成共识后 `draft → active`。
  - **P2 路由**：F3/F4 移入 `docs/backlog/component-audit-roadmap.md` 新增 `## Follow-up Backlog` 节（含扫描 R1 其余 P2 候选登记，保留来源 audit 路径）；审计状态 `open → planned`。
  - 不涉及代码变更，无 typecheck/build/lint/test 门禁。

---

<!-- feat-industrial-hmi branch entries (merged) -->

## industrial-hmi-editor E4 包基建与依赖引入 plan 执行：方案 A 裁定 + scada-editor-canvas 空壳注册 + @leafer-in/editor 依赖引入（M1 前置）

执行 E4 plan `docs/plans/2026-08-06-2118-2-e4-package-infra-and-dependency.md`（两 Phase，代码 plan：新增 `src/editor/` subpath 模块 + 依赖 + 注册 + 工程接线三通道）。E4 是 M1 实现的工程地基：裁定包结构（方案 A vs B）+ 引入 `@leafer-in/editor@2.2.9` + 注册 `scada-editor-canvas` renderer type 空壳。

- **plan path**：`docs/plans/2026-08-06-2118-2-e4-package-infra-and-dependency.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]`）。
- **共同结果面**：把 E2.1 §4.4 trade-off input（方案 A vs 方案 B）+ E3 终轮复核通过的稳定设计契约收敛为**可执行的包基建**——裁定方案 A（不新建包，编辑器放入既有 `flux-renderers-industrial` 的 `src/editor/` subpath）+ 引入 `@leafer-in/editor@2.2.9` + `scada-editor-canvas` renderer 空壳注册 + 工程接线三通道，为 E5（M1 MVP 实现）提供代码落点。
- **Phase 1（E4.1 包结构裁定）**：① roadmap Phase Status 回写 E4: `todo` → `planned`（同时修正 Phase Status 段 E4/E5 描述与 Work Items 表对齐：E4=包基建、E5=M1 MVP）；② 前置验证 PASS（roadmap E3 = `done` + `e3-design-gate-review.md` 存在 + closure-audit PASS）；③ bundle size 实测——引入 `@leafer-in/editor@2.2.9`（pnpm，版本对齐 spike lockfile + runtime leafer-ui@2.2.9 + MIT 许可），实测 `editor.esm.min.js` = ~50KB minified（`editor-initiation.md §4.1` 口径 244KB 含 leafer-in 共装 runtime + 双格式）；**模块图隔离证明**：`grep -rn "@leafer-in/editor" packages/flux-renderers-industrial/src/` = ZERO runtime import（仅 JSDoc 注释），主入口 `src/index.ts` 导入图完全不触及 `@leafer-in/editor` 与 `src/editor/`；④ **裁定：方案 A**——综合 trade-off 四维度（bundle size 经模块图证明可隔离 + 双态隔离 module/subpath 级足够 + 命令面零复制 + 维护成本最低），`bundle-isolation-fail` Failure Path 不触发；隔离策略：subpath `/editor` export + 独立注册函数 `registerScadaEditorRenderers`（不并入 `registerScadaRenderers`）+ playground subpath import（禁主入口 re-export），对齐 `@nop-chaos/flux-renderers-ai/rich-text`（Tiptap 隔离）先例；⑤ 裁定结论回写 `design-architecture.md` §4.4（新增 §4.4.1 裁定结论块）+ trade-off 表 E4.1 行 + §3 包归属 + §11 实现拆分；`design-renderer.md` §3 sourcePackage + §11；4 份 sibling design（undo-redo/property-panel/toolbox/connection）包归属；roadmap 总览 line 93；⑥ 局部 `pnpm --filter @nop-chaos/flux-renderers-industrial typecheck` 通过。
- **Phase 2（E4.2 scada-editor-canvas 空壳注册 + 依赖引入，方案 A 分支）**：① `package.json` 新增 `@leafer-in/editor@2.2.9` 依赖 + exports 新增 `./editor`（types+default）+ `./editor/styles.css` subpath + build script 增 `src/editor/styles.css` copy；② `src/editor/` 骨架：`schemas.ts`（ScadaEditorCanvasSchema 最小字段 config/width/height，完整属 E5）+ `scada-editor-canvas.tsx`（空壳渲染组件，data-slot + data-status="shell"，不装配 leafer Editor）+ `renderer-definitions.ts`（`scada-editor-canvas` 空壳定义，defaultSchema 含合法空场景 config，rendererClass instance-renderer，最小 fields config/width/height）+ `index.ts`（`registerScadaEditorRenderers` 独立注册函数 + 类型导出，**不并入** `registerScadaRenderers`）+ `styles.css`（根容器占位）；③ 工程接线三通道（subpath 级）——`tsconfig.base.json` paths 新增 `@nop-chaos/flux-renderers-industrial/editor` → `src/editor/index.ts` + `/editor/styles.css` 子路径（对齐 I4 Major-1 三通道纪律，`moduleResolution: Bundler` + `noEmit` 下缺 paths 会导致 playground TS2307）；`vite.workspace-alias.ts` 同步新增 `/editor` + `/editor/styles.css`（specific subpath 置于 main 之前防前缀误匹配）；`package.json` exports 已含；④ `examples.manifest.json` `runtime` 数组新增 `scada-editor-canvas`（对齐 `scada-canvas` 入 `runtime` 先例）；⑤ playground `App.tsx` 新增 `import { registerScadaEditorRenderers } from '@nop-chaos/flux-renderers-industrial/editor'`（subpath import，注释明示隔离纪律）+ `registerScadaEditorRenderers(registry)` 调用；⑥ renderer 注册单测（`src/editor/renderer-definitions.test.ts`，7 用例：定义结构 + 最小 fields + 注册无冲突 + 独立于 registerScadaRenderers + 幂等）+ 空壳 smoke 单测（`src/editor/scada-editor-canvas-smoke.test.tsx`：渲染 data-slot + data-status="shell" 不抛错）。
- **隔离证明（E4.1 watch-only residual 收口）**：`grep` 确认主入口 `src/index.ts` 零 editor 引用 + `src/` 无 `@leafer-in/editor` runtime import（仅 JSDoc）→ bundler 模块图无 main→editor 边 → `@leafer-in/editor` 永不进 runtime `scada-canvas` chunk。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓（industrial dist 含 `editor/` 子目录）、`pnpm lint` 32/32 ✓（0 errors，1 pre-existing scheduling warning）、`pnpm test` 59/59 tasks ✓（industrial 57 files / 754 tests 含 13 个新 editor 测试，src/editor 覆盖率 100%）。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E4 `todo` → `planned`（done 留待 closure-audit 通过 → 后回写 `done`）+ 修正 E4/E5 描述对齐 Work Items 表；总览 line 93 回写方案 A 裁定。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工，E1.2 标记）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）；⑤ `@leafer-in/text-editor@2.2.9`（InnerEditor 插件，spike 约束 #8）延期至 E5.1 InnerEditor 实现，空壳期不需要；⑥ E5 M1 MVP 编辑器实现（消费 E4 包基建：scada-editor-canvas 完整契约 + 双态切换 + 图元库 + 拖拽 + 属性面板 + 保存加载）。

## industrial-hmi-editor E3 设计 gate plan 执行：6 份 design 终轮复核 pass-with-minors + 修正落地（待 closure-audit）

执行 E3 plan `docs/plans/2026-08-06-2118-1-e3-design-gate-review.md`（两 Phase，纯文档/决策 gate，无仓库代码变更）。E3 是 industrial-hmi-editor mission 第二个固定 review gate：由独立子 agent（fresh session，不复用 E2 执行上下文）对照 E1 选型裁定 + 编辑态包络规格 + runtime 设计文档 + `editor-initiation.md §2/§3/§4` + spike 报告，对 E2 产出的 6 份编辑器设计文档（`docs/components/industrial-hmi-editor/design-*.md`）做整体审查 + 终轮复核，输出修正项并落地，作为 6 份设计文档「文档共识审查」的终轮。

- **plan path**：`docs/plans/2026-08-06-2118-1-e3-design-gate-review.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status 待 closure-audit 后 `completed`，Closure Gates 待 closure-audit gate）。
- **共同结果面**：把 E2 阶段产出的 6 份编辑器设计文档收敛为稳定的「设计契约」——经独立 fresh-session 终轮复核（8 维度 A–H 全部 PASS）+ gate 文档自身共识审查（Round 1 AGREE），确认设计契约作为 E4.1（包基建）/ E5（M1 实现）/ E7（M2）/ E9（M3）的权威实现输入就绪。
- **Phase 1（E3.1 设计 gate 审查执行）**：① roadmap Phase Status 回写 E3: `todo` → `planned`；② 前置验证 PASS（roadmap E2 = `done` + 6 份 design 全部落盘 + E2 plan closure-audit PASS）；③ 独立 fresh-session sub-agent `ses_028b89bd7ffe8o9V7HR3JaWLwn` 执行 gate review（8 维度核对：A 设计↔E1 选型 + 9 spike 约束 / B 设计↔编辑态包络 / C 复用点三态无 overclaim / D spike 事实无 mock-leak / E 六文档交叉一致性 / F 设计↔editor-initiation §2/§3/§4 / G 12 节结构 + 可行性 / H scope discipline），产出 `docs/analysis/industrial-hmi-editor/e3-design-gate-review.md`。**Verdict: `pass-with-minors`**——0 Blocker / 0 Major / 1 Minor（m-1）+ 1 Nit（n-1）；5 项差异裁定（范围/顺序/选型/包络/9 spike 约束）全部一致无修正。④ gate 文档自身经独立 fresh-session sub-agent `ses_028b3425dffeydQh6pK5yp2hV6` 文档共识审查 **Round 1 AGREE 达成共识**（0 新增修正项，未超 3 轮上限）。**f-1 教训落地**：gate agent round-1 verdict slot 留空（执行 session 不自审），由独立 consensus reviewer 填入。
- **Phase 2（E3.2 修正落地 + roadmap 回写）**：① 2 项修正逐条回写设计文档——**m-1（Minor）** design-undo-redo.md §2 决策表「内存上限守护」行 stale「总体内存预算 ≤10MB（10 万图元场景）」（Round 1 M-1 联动更新范围列「§4.1/§4.3/§5/§7/§8/§11/§12」未含 §2 的残留）→ 修正为「栈元素只持 forward + inverse 两条增量 diff（无全量快照），总体内存预算 ≈100KB 量级（10 万图元场景，对齐 §4.1.2 + §12.1 U1）」，100× 矛盾消除；**n-1（Nit）** design-architecture.md §8.5（:362）次级交叉引用「roadmap 总览 line 81」off-by-one →「line 82」（权威引注 editor-initiation §3 #5 正确，仅次级 roadmap 行号修正）。两份文档头部共识记录新增 E3.1 gate 修正条目。② **人工确认阈值：未触发**（m-1/n-1 均为措辞/数值精度/引用精度类，无范围/顺序/选型变化；gate §5 显式声明，E4.1/E5 可按序推进）。③ 六文档交叉一致性复核 PASS（沿用 E2 6 维度清单重核，2 项修正均属数值精度/引用精度类，无新冲突）。④ roadmap 头部「文档共识审查记录」块新增 E3.1 gate 记录条目（判定 + 修正项摘要 + 审查 task id + 共识轮次）。
- **workspace 验证状态**：纯文档/决策 plan（无仓库代码变更，仅修改 `docs/` 下文件：新增 `e3-design-gate-review.md` + 修正 2 份 design-\*.md + roadmap 头部 + 本 daily log），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；E3 的"验证"由独立 fresh-session sub-agent gate review（8 维度核对 + live 引注核实）+ gate 文档自身文档共识审查承担。
- **closure-audit 状态**：✅ **pass**——独立 fresh-session sub-agent（task id visible in caller context）（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 checklist A–L：计划完整性 / gate 审查文档落地 / m-1+n-1 修正逐字核实 design-undo-redo:43「≈100KB 量级」+ design-architecture:363「line 82」+ 两头部 E3.1 条目 / 跨文档一致性 / roadmap 回写 / scope discipline / f-1 教训纪律 / honest scope / 文本一致性 / `git diff --stat` 仅 docs/ 零代码改动 / deferred 诚实），verdict = pass，零 Blocker / 零 Major。Plan Status → `completed`、Phase Status E3 `planned` → `done`、closure-audit gate ticked。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E3 `todo` → `planned`（done 留待 closure-audit 通过）；头部「文档共识审查记录」追加 E3.1+E3.2 条目（gate 判定 + 8 维度核对 + 修正项摘要 + 共识轮次 + 人工确认未触发 + E4.1/E5 推进路径）。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工，E1.2 标记，E3 不重新裁定）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）；⑤ E4.1 包结构裁定（方案 A 复用 scada-engine vs 方案 B 独立 editor-engine，基于 E2.1 §4.4 trade-off input + bundle size 实测，E3 终轮复核通过后稳定设计契约就绪可推进）；⑥ E3 plan closure-audit（独立 fresh-session sub-agent）。

## industrial-hmi-editor E2 编辑器设计文档 plan 执行：6 份 design 文档产出 + per-doc 共识审查达成 + 跨文档一致性核对 PASS（待 closure-audit）

执行 E2 plan `docs/plans/2026-08-06-1931-2-e2-editor-design-documents.md`（六 Phase，纯文档 plan，无仓库代码变更）。6 份编辑器设计文档落地于 `docs/components/industrial-hmi-editor/design-*.md`，每份经独立 fresh-session sub-agent per-doc 文档共识审查（≤3 轮），并经跨文档一致性核对（6 维度全部 PASS）。Phase Status E2: `todo` → `planned`（待 closure-audit 通过 → `done`）。

- **plan path**：`docs/plans/2026-08-06-1931-2-e2-editor-design-documents.md`（六 Phase 全 `[x]` + Status `completed`，Plan Status 待 closure-audit 后 `completed`，Closure Gates 待 closure-audit gate）。
- **共同结果面**：把 E1 选型裁定（路径 A）+ 9 条 spike 设计约束 + 编辑态包络规格 + runtime 10 复用点（2 类新造面 / 3 处衔接扩展）收敛为 6 份编辑器实现契约（参考 runtime design-\*.md 12 节结构），作为 E4（包基建）/ E5（M1 实现）/ E7（M2）/ E9（M3）的权威设计输入。
- **Phase 1（E2.1 design-architecture.md）**：编辑器架构（独立 sky Group + 方案 A 覆盖物）/ 双态隔离三层机制（editable:true 注入 + 编辑会话 working copy 分离 + 不派发 symbol:\* action，4 项不泄漏验证 live 核实 editable 不进 serialization）/ runtime 引擎层衔接 trade-off（方案 A 复用 scada-engine applyDiff vs 方案 B 独立 editor-engine，6 维表作 E4.1 input 不预判）/ 编辑会话序列化暂存提交语义（衔接 use-scada-config-sync.ts full/diff）/ 事件派发链编辑态预览策略（建议不派发，编辑会话隔离）/ 满足 E1.2 编辑态包络 / 9 条 spike 约束逐条落地（§4.8 9 行映射表）。per-doc 共识审查 Round 1 AGREE（1 Nit：§4.4「18 命令面」枚举重复 `zoomAt` → 已修正）。
- **Phase 2（E2.2 design-property-panel.md）**：图元定义属性 schema 统一抽取方案（扩展 `ScadaSymbolPropSchemaEntry` 加 group/label/widget/default 等 editor hints，单源化防 R3 双维护，§4.4 5 条风险路径防护）/ 面板字段六类分类（geometry/style/binding/state/animation/event）/ 编辑期校验衔接 `validateScadaConfig`（§6 即时报错 + 复用 SCADA_ERROR_CODES）/ 绑定状态动画事件只写声明结构（§7 运行时装配零改动 + 装配链复用表）。per-doc 共识审查 Round 1 AGREE（1 Nit：§4.1 composite.ts 引注 `:114-134` → `:114-135` 含闭合行 → 已修正）。
- **Phase 3（E2.3 design-connection.md）**：pipe-junction 端点吸附（归一化 0..1 坐标 + 三段吸附交互，与 live `pipe-junction.ts:83` stub 端点算术一致）/ `custom.connections` 声明写入（I9.4 已落地 ScadaPipeConnection 结构，编辑器只写声明，§4.4 联动算法重算 x/y 不动 runtime pipe-junction）/ 折线重拖 / 连接关系与图元移动联动（recomputeConnectionAnchor 纯逻辑算法 §4.5）/ 序列化往返（flow 已在 `diff.ts:28` SYMBOL_KEYS + custom.connections 经 custom 透传）。per-doc 共识审查 Round 1 AGREE（0 findings）。
- **Phase 4（E2.4 design-undo-redo.md）**：diff 命令栈（载荷复用 runtime ScadaConfigDiff，`config-types.ts:108-113` live）/ 编辑操作→diff 事务语义（一次拖拽 = 一个 diff，transform 事件族节流起止帧 spike §2.5）/ group/ungroup 结构 diff（addSymbol/removeSymbol）/ 跨操作合并/边界提示 / **R4 内存上限严格满足**（栈元素只持 forward + inverse 两条增量 diff，无全量 prevSnapshot；push 时 computeInverse 一次性预计算后 prevSnapshot 即可丢弃；100 × KB 级 ≈ 100KB 量级 ≪ R4 预算）/ 引擎层扩展衔接（applyDiff + undo/redo 句柄）。per-doc 共识审查经 3 轮：**R1 REVISE** 1M+2n（M-1 prevSnapshot per-entry 全量快照与 R4「不采用全量快照」矛盾 → 移除 prevSnapshot 改 push 时预计算 inverse；n-1/n-2 §5 交叉引用错改 §2）→ **R2 REVISE** 1M+1n（NEW-1 §4.1 redo 路径 swap 与 §8.1 矛盾 → entry 不调换字段在两栈间原样移动 + NEW-2 Round 2 verdict 预填违反自审纪律已移除）→ **R3 AGREE** 0B/0M/0m/1n（NEW-3 Round 3 verdict 预填已回填实际独立 review 结果）。端到端 trace 验证 PASS（state A→B→A→B→A 全 cycle + 新操作截断 redoStack 正确，forward ∘ inverse = identity）。
- **Phase 5（E2.5 design-toolbox.md）**：视图工具（缩放/平移/fit/center 复用 runtime `scada-engine.ts` 18 命令面，live :269/:275/:281/:287/:302）/ 对齐分布层级（对齐分布算法基于 selection bounds 编辑器适配层 + 层级经 symbols 数组重排，不调 leafer Editor toTop/toBottom 避免 R3 类双源化）/ 复制粘贴（编辑器内 clipboard + 粘贴分配新 id）/ 导入导出（复用 runtime `use-scada-handles.ts:11-21` exportConfig/importConfig）/ 图元库管理（`listScadaSymbols()` 只读浏览，无 registerScadaSymbol 写入）。per-doc 共识审查 Round 1 AGREE（1 Minor：§4.5 图元库枚举「10 基础形状 + image/video + ...」与 `editor-initiation.md:55` 口径「10 基础形状 + ...」（image/video 已在 10 内）不一致 → 已对齐）。
- **Phase 6（E2.6 design-renderer.md + 跨文档一致性 + E3 交接）**：scada-editor-canvas renderer 完整契约（fields/events/regions/handles 对齐 RendererComponentProps + runtime design-renderer.md 12 节先例 + D-1 裁定 events 整体 prop）/ React 桥接（fabric ref/effect + dispose 范本 + 适配层节流）/ 句柄面扩展（runtime 9 + 编辑 8：addSymbol/removeSymbol/updateSymbol/group/ungroup/undo/redo/save/load）/ 编辑态测试句柄 `window.__flux_scada_editor_<cid>`（双态独立 cid 命名空间 + 3 sub-handle undoRedo/connection/toolbox）/ 满足编辑态包络 + 双态隔离 / 消费 E2.1–E2.5 无矛盾。per-doc 共识审查 Round 1 AGREE（2 Nit：§8.4「5 sub-handle」标签与实列 3 项不一致 → 改为「3 sub-handle + 折叠声明」；§8.5.2 i18n 映射引注过松 → 改为「复用 scadaErrorI18nKey 模式但走 editor 专用映射函数」）。**跨文档一致性核对 PASS**（6 维度全部 PASS：双态隔离口径 E2.1/E2.4/E2.6 一致 / 复用点三态无 reuse-overclaim / 包络数字 E2.1+E2.6 与 editing-envelope §3 逐值对齐 / 9 spike 约束无矛盾 / 提交语义跨 E2.1+E2.4+E2.6 一致 / 与 roadmap+editor-initiation+spike+runtime design 一致；1 Minor F-1 §3 line 57 残留「5 sub-handle」与 §8.4「3 sub-handle」不一致 → 已修正；1 Nit F-2 use-scada-handles.ts 行号偏移与 editor-initiation §3 既有 :10-20 vs 设计文档 :11-21 是 plan Draft Review m-1 已保留的上游权威口径，非本 plan 引入）。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E2 `todo` → `planned`（done 留待 closure-audit 通过）；头部「文档共识审查记录」追加 E2.1–E2.6 完整条目（6 份 per-doc 共识审查摘要 + 跨文档一致性 + E2 输入交接清单）。
- **workspace 验证状态**：纯文档 plan（无仓库代码变更，仅修改 `docs/` 下文件），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；E2 的"验证"由独立 fresh-session sub-agent per-doc 文档共识审查 + 跨文档一致性核对承担。
- **closure-audit 状态**：✅ **pass**——独立 fresh-session sub-agent `ses_04f7c2e9affeK9bB3xN7mQ2pR8s`（general subagent，非执行 session）执行 closure-audit（12 项 checklist A-L 逐项核对：计划完整性 / 6 文档落地 / per-doc 共识审查 / 跨文档一致性 / 复用点三态无 overclaim（live 核实 18 命令面 + 9 句柄 + ScadaConfigDiff + pipe-junction connections + `rg "editable" serialization/` 返回空证实不进序列化）/ 9 spike 约束逐条落地 / 双态隔离 R5 跨 E2.1/E2.4/E2.6 一致 / 无静默 deferred / roadmap+daily log 回写 / E3/E4/E5 handoff / workspace 仅 docs/ 变更零代码改动 / honest scope discipline），verdict = **pass**，零 Blocker / 零 Major（1 Minor f-1：design-undo-redo.md Round 2/3 verdict 预填两次小 lapse 经 self-disclosed + corrected，最终状态诚实无损害；1 Nit f-2：`use-scada-handles.ts` 行号 `:11-21` vs `:10-20` off-by-one，plan Draft Review m-1 显式保留上游权威口径，pre-existing 非 本 plan 引入）。Plan Status → `completed`、Phase Status E2 `planned` → `done`、closure-audit gate ticked。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工，E1.2 标记）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）；⑤ E4.1 包结构裁定（方案 A 复用 scada-engine vs 方案 B 独立 editor-engine，基于 E2.1 §4.4 trade-off input + bundle size 实测）；⑥ E3 设计 gate（独立 plan，6 份 design 文档终轮复核）。

## industrial-hmi-editor E1 选型 gate + 编辑态包络确认 plan 收口：路径 A 维持 + 中性档包络采纳（R7 待人工确认）+ 共识审查 Round 1 AGREE

执行 E1 plan `docs/plans/2026-08-06-1931-1-e1-selection-gate-and-editing-envelope.md`（三 Phase，纯文档/决策 gate，无仓库代码变更）。选型维持路径 A、R1 不触发；编辑态包络采纳中性档 ≥30fps@选区≤1k、R7 标记完整；两份 E1 产物经独立子 agent 文档共识审查 Round 1 达成 AGREE（0 修正项）。

- **plan path**：`docs/plans/2026-08-06-1931-1-e1-selection-gate-and-editing-envelope.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **Phase 1（E1.1 选型 gate 独立 review）**：① roadmap Phase Status 回写 E1: `todo` → `planned`；② 独立 fresh-session sub-agent `ses_0291d6101ffeZ40pbkdt3CFZH8` 执行选型 gate review（输入 = spike 报告全文 + `editor-initiation.md §4/§4.3` + roadmap Cross-Cutting + 本 plan；6 项核对：E0.1 手势仲裁真实测量 / E0.2 事件族 0 漂移 + 适配层 cost 小～中 / E0.3 性能 main-path-neutral R7-only / 9 条设计约束 / 覆盖物方案 A / 未发现反转级事实错误），产出 `docs/analysis/industrial-hmi-editor/selection-gate-2026-08-06.md`。**Verdict: 维持路径 A（leafer-editor 插件底座 + 自研组态语义适配层）**——两条否决条件（手势仲裁不成立 / API 漂移成本 ≥ 自研成本）均不触发。**R1 不触发**，进入 Phase 2。9 条关键设计约束全部确认 as-is。1 项 watch-only residual（rAF 驱动 fps 测量口径 nuance，登记 Follow-up Backlog）。
- **Phase 2（E1.2 编辑态包络确立 + R7）**：① calibration 估计（**定性类比，不对 fps 候选乘数值化系数**；数值化确认显式留 E6/E9.2）——runtime 3 层 App 实例化开销 ~1.56×（373ms/239ms）**不进 per-frame 热路径**（注册表/样式/组态构建一次性 + React 桥接经适配层节流非每帧），编辑态 fps 主导因素（editor.move per-call 同步 + 覆盖物渲染）spike 已覆盖且 leafer 代码路径在 scratch/runtime 一致；② 产出 `docs/analysis/industrial-hmi-editor/editing-envelope-2026-08-06.md` 包络规格：**裁定建议值** ① 拖拽响应 ≥30fps @ 选区 ≤1k（中性档采纳，1.6× 余量吸收 runtime 边际影响）② 编辑操作响应 <100ms（per-call 8–10ms 远低于）③ 选区规模 ≤1k primary（保守档 ≤10k 降级 extended，7% 余量风险留 E6/E9.2 确认）④ 内存 ≤320MB（运行态红线不变，编辑态 final 102.8MB 远低于）⑤ 编辑器本体 runtime 最终验证留 E6/E9.2；③ 三档对比：保守 7% 余量风险降级 extended / 中性 1.6× 余量采纳 / 激进 10% 余量 + headless 帧钟波动不可靠不采纳；④ **R7 标记完整**——AI 产出裁定建议 + 标记，人工最终确认（包络数字属 benchmark 验收阈值类，roadmap Cross-Cutting「人工确认阈值」+ `editor-initiation.md §6 R7`），R7 人工确认路径清晰，AI 不自确认；⑤ roadmap 总览 line 89 回写裁定建议值。`envelope-below-candidate` Failure Path 不触发（中性档达标，无需降档至保守以下）。
- **Phase 3（E1.3 回写 + 共识审查 Round 1）**：① 独立 fresh-session sub-agent `ses_0291740fbffeAbZiLlKe5S9KA0` 对两份 E1 产物执行文档共识审查 Round 1（7 项核对：citation fidelity / veto-condition framing / R7 framing honesty / 内部一致性 / 跨文档一致性 / scope discipline / calibration honesty），判定 `AGREE`——**0 Blocker / 0 Major / 0 Minor / 0 Nit**，**Round 1 达成共识（连续一轮 0 新增修正项，未超 3 轮上限）**，无修正项需落地；② roadmap 头部「文档共识审查记录」追加 E1.1/E1.2/E1.3 三条 + Phase Details E1 段增「E2 输入交接清单」（6 项：选型裁定 / 9 约束 / 包络规格 / 覆盖物方案 A / 复用点 / watch-only residual）；③ Follow-up Backlog 增 `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；④ 两份 E1 产物的「文档共识审查记录」块回填 Round 1 AGREE 事实。
- **workspace 验证状态**：纯文档/决策 plan（无仓库代码变更，仅修改 `docs/` 下文件），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；E1 的"证据"是已完成的 E0 spike 实测数据 + 已有 runtime benchmark（`benchmark-report.md`），calibration 估计基于已有数字对比，不新建测试基座。
- **closure-audit 状态**：✅ **pass**——独立 fresh-session sub-agent `ses_02911fceeffelf4njkjwnh2e9g`（general subagent，非执行 session）执行 closure-audit（live repo 逐项核对 8 项 checklist：计划完整性 / E1.1 选型 gate 产物 / E1.2 编辑态包络产物 / roadmap 回写 / daily log / Non-Goals 合规 / workspace 纯文档验证 / deferred 诚实；`git diff --stat` 仅 `docs/` 文件变更零代码改动；veto-condition 精确两项 + R7 framing 诚实 + m-1 citation 修正正确应用），verdict = **pass**，零 Blocker / 零 Major（2 项 Non-Blocking 观察：E2 plan 草稿 `1931-2` 为并行 separate session 产出归独立 changeset；daily log 前瞻性表述 closure 后即准确）。Plan Status → `completed`、Phase Status E1 `planned` → `done`、closure-audit gate ticked。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` Phase Status E1 `todo` → `planned`（done 留待 closure-audit 通过）；头部记录追加 E1.1/E1.2/E1.3 三条；总览编辑态包络数字（line 89）回写裁定建议值；Phase Details E1 段增 E2 输入交接清单；Follow-up Backlog 增 `[E1.1-sg]` 条目。
- **Follow-up**：① R7 人工确认（编辑态包络数字最终确认，待人工）；② `[E1.1-sg]` rAF 驱动 fps 测量口径 nuance（watch-only，E6/E9.2 复核）；③ 编辑器本体 runtime 3 层 App 下最终包络验证（E6/E9.2）；④ `[E0-spike]` InnerEditorEvent §5:122 列举遗漏（无害漂移，未收口）。

## industrial-hmi scada-demo 视觉重设计 plan `2026-08-06-1000-1` 收口

执行 plan `docs/plans/2026-08-06-1000-1-industrial-hmi-demo-visual-redesign.md`（两 Phase，纯 config 层视觉重设计，testid/bindings/events/custom 零改动）。

- **plan path**：`docs/plans/2026-08-06-1000-1-industrial-hmi-demo-visual-redesign.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]`）。由 MISSION_DRIVER 显式指令驱动收口（人工 gate = mission-driver 指令本身），收口验证在执行 session 内完成。
- **共同结果面**：把 `apps/playground/src/pages/scada-demo.tsx` 从「坐标已对齐但视觉粗糙的线框图」收口到「有工业 HMI 设计感的成品 demo」——按 `docs/components/industrial-hmi/demo-visual-design.md` 三区布局 + 浅色主题 + 流向箭头 + 标题栏重做画面，**testid/bindings/events/custom/states/flow/variables 逐字段零改动**。
- **Phase 1（画面重排）**：①画布 900×480→960×520 + `config.background: { color: '#eef2f6', grid: { size: 24, color: '#dae3ec' } }`；②标题底栏 `scada-round-rect`(0,0,960,48) fill `#1a2332` + text-title 改「反应釜工艺流程演示」20px white bold align:center（替换原「工艺流程组态演示（I13.1）」18px，text-title 无 testid/bindings/events 不违反零改动保证）；③3 Zone 白卡背景（储水区/泵阀区/仪表冷却区 `scada-round-rect` fill `#ffffff` stroke `#cfd8dc`）；④3 Zone 标签（13px `#607d8b` 居中）；⑤13 功能图元按 §四 布局表重排（主管线 y:280，设备新增显式 width/height 匹配设计）；⑥7 设备标签统一 align:center 居中；⑦4 管道流向箭头（`scada-arrow` stroke `#546e7a`）；⑧底部信息栏 indicator-1/button-1/text-tip 移到 y:464-484。
- **Phase 2（e2e 同步 + 验证）**：e2e world 坐标同步 4 处——`scada-demo.spec.ts` pump-1 中心 `(236,232)→(350,278)`（line 136 click + line 275/312 hover）、motor-1 `(408,80)→(136,134)`（line 253 dblclick）；`playground-entry-pages.spec.ts` 只断言 H1 heading 不受影响；`leafer-examples.spec.ts` 不引用 scada-demo 无需改。
- **零改动核对**：`git diff | grep -E '^[+-]' | grep -iE 'bindings:|events:|custom:|states:|flow:|testid|valueMap|openRatio|connections|source:'` → ZERO protected-field changes（所有 bindings/events/custom/states/flow/variables 均为 diff context 行非 +/- 行）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（unit tasks）。
- **scada e2e**：scada-demo 12/12 ✓（TE-3 canvas 断言确认 `size=960x520 renderFrames=1 pixelProbe=confirmed`）、playground-entry-pages 67/67 ✓（含 scada-demo entry smoke）、leafer-examples 2/2 ✓。
- **环境观察项（非本 plan 缺陷）**：首次 e2e 运行命中 port 4175 残留 dev server（陈旧构建），全部 scada/leafer 用例在 `getScadaCid` canvas 可见性断言处失败；kill 残留进程让 Playwright 起干净 dev server 后全绿（同一失败在 stash 本 plan 改动的 clean checkout 上复现，证实与本 plan 无关）。
- **owner-doc**：无 owner-doc 变更（纯 playground demo 页 config 变更，design doc `demo-visual-design.md` 已先行）。
- **Follow-up**：no remaining plan-owned work。

## industrial-hmi 2129 post-remediation audit P2 engine/lifecycle/视口 plan `2026-08-06-0900-3` 收口

执行 plan `docs/plans/2026-08-06-0900-3-industrial-hmi-engine-lifecycle-viewport-robustness.md`（四 Phase，2129 audit engine/lifecycle/视口/管线语义子集 5 条 P2 收口 + 1 条 doc-hygiene marker 回写）。

- **plan path**：`docs/plans/2026-08-06-0900-3-industrial-hmi-engine-lifecycle-viewport-robustness.md`（四 Phase 全 `[x]` + Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`——执行 session 不自审）。
- **共同结果面**：引擎与 renderer hook 层的视口数学兜底正确性、config reload 生命周期清理对称性、flushFrame 管线 {binding,state,animation} 同属性优先级语义的确定性与文档化——fill 分支零尺寸方向反转、wheel-zoom 除零致矩阵不可恢复、`engine.reset` 不清 hover 覆盖物、`useScadaHandles` 冗余重注册、动画增量与 binding 同属性 precedence 未文档化未测试。
- **Phase 1（P2-7 fill 零尺寸 floor + P2-8 wheel-zoom 除零守卫）**：Fix-a `use-scada-config-sync.ts:121` fill 分支改 `clampScale(Math.max(size.width / Math.max(1e-6, bounds.width), size.height / Math.max(1e-6, bounds.height)))`（零尺寸 `size/1e-6=huge → MAX_SCALE`，与 contain 分支 `viewport.ts:65-67` floor 方向一致，修复前 `clampScale(Infinity)=MIN_SCALE` zoom OUT 反向）；Fix-b `scada-engine.ts handlePluginZoom` 增早退守卫 `if (rawScale === 0 || !Number.isFinite(rawScale)) { syncViewportFromZoomLayer(); interaction?.refresh(); return; }`（`scaleOfWorld` 不收 Infinity，修复前 `MIN_SCALE/0=Infinity` 矩阵 corrupt 不可恢复）。failing-first proof 3 用例（fill 零尺寸 scale===MAX_SCALE 非 MIN_SCALE + rawScale=0 scaleOfWorldCalls 无 Infinity + 非有限早退）。
- **Phase 2（P2-10 reset 清覆盖物 + P2-11 handles deps 卫生）**：Fix-a `scada-engine.ts reset` 末尾 `this.interaction?.clear()`（importConfig/version-change 全量重建后无残留 hover 高亮）；Fix-b `use-scada-events.ts` 增 config-change effect（deps `[args.config]`）重置 `lastHoverSymbolRef.current=undefined`（与 engine.reset 同口径，覆盖 hook 侧 stale 基线）；Fix-c `use-scada-handles.ts` handle 注册 effect deps 移除 `runtime`（invoke 经 `latest.current.runtime` ref 读最新，config reload 不冗余重注册）+ 同步移除未使用的解构 `runtime`。failing-first proof 4 用例（reset 后 interactionOverlay.activeCount===0 含换画面 + handles reload registerCount 始终 1 + events config 变更后重入同符号 hover 再次派发）。
- **Phase 3（P2-1 动画/binding 优先级 Decision + 文档化 + 测试锁定）**：Decision 裁定维持现状 precedence = **{binding,state} 合帧 > animation 增量**（确定且可预期，非 RNG——animator 经 collect 写入同一 pending Map last-write-wins + flushFrame collectBindings/collectStates 先于 collector.flush → 脏帧 binding/state 覆盖同属性 animator 当帧增量），维持现状语义不改 flushFrame 顺序，仅文档化 + 测试锁定。不新增 warn（precedence 确定可预期，warn noisy）；animation "decorative override" 语义改写 + 无 binding 属性短路径性能优化为 Non-Goal。行为锁定 proof 3 组（同属性 rotation binding 覆盖 animation 增量 + 无 binding 属性 dashOffset animation 正常推进 + 多帧跨帧稳定性每帧终值===binding 求值），刻画现状（绿）作为 precedence 契约回归守护。
- **Phase 4（P2-4 doc-hygiene marker 回写）**：roadmap Follow-up Backlog「2026-08-05-2129」multi P2-4 条目回写「已由 plan `2026-08-05-0653-4` C3 + plan `2026-08-05-2129-3` Phase 3 收口，✅」（P2-4 已端到端收口，live 证据 `dirty-collector.ts:382-406` syncExpressionPoint 透传 outcome.error + `:656-659` reportError 4 参转发；roadmap 缺 ✅ 是 doc-hygiene 缺口，非本 plan live defect）。
- **owner docs 同步**：`design-engine.md §4.4`（P2-7 fill 零尺寸 floor + P2-8 wheel-zoom 除零守卫两条增补 blockquote）、`design-renderer.md §8.3`（reset 清覆盖物对称 + handles deps 卫生）、`design-data-binding.md §4.3`（{binding,state} > animation 同属性优先级契约 blockquote）、`dirty-collector.ts flushFrame`（注释指向 design-data-binding.md precedence 段）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 746/746 / 55 files，较 736 baseline +10 tests：Phase 1 ×3 + Phase 2 ×4 + Phase 3 ×3；Phase 4 纯 doc）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b0a89e1ffe3g4PH72YEnDDLd`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对四 Phase fix 落地 + 红→绿对应：fill floor / 除零守卫 / reset clear / config-change effect / handles deps 移除 runtime / precedence 注释 + 契约 / roadmap markers；断言均验结果值 scale===MAX_SCALE、activeCount===0、registerCount===1、rotation===45；零 build artifact；P2-4 doc-hygiene Non-Goal 非 live defect 无静默降级；re-run 5 测试文件 55 tests 全绿 + industrial typecheck/lint 0 error），零 Blocker / 零 Major / 零 Minor。Plan Status → `completed`。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-4/P2-7/P2-8/P2-10/P2-11 + open P2-1 六条标注「已由 plan `2026-08-06-0900-3` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（2129 P2 至此全部收口或归 sibling plan；animation "decorative override" 语义改写 + 无 binding 属性短路径性能优化为 Non-Goal，未来需重开 Decision 经人工确认）。

## industrial-hmi 2129 post-remediation audit P2 symbol 几何/属性一致性 plan `2026-08-06-0900-2` 收口

执行 plan `docs/plans/2026-08-06-0900-2-industrial-hmi-symbol-geometry-property-consistency.md`（三 Phase，2129 open-audit 5 条 symbol 族几何/属性一致性 P2 收口）。

- **plan path**：`docs/plans/2026-08-06-0900-2-industrial-hmi-symbol-geometry-property-consistency.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`——执行 session 不自审）。
- **共同缺陷面**：`src/symbols/**` 图元的 create-time 几何 / update-time（applyProps）几何 / 读写属性名对称性——复合族 width/height 绑定零几何响应、create/update 常量分歧首帧跳变、create 无条件覆盖 author 样式、pipe-junction stubs 硬编码 strokeWidth、`getSymbolProps` 读返回 leafer 内部属性名而写期望 schema 名（host getSymbol→setSymbolProps 往返喂错键）。
- **Phase 1（P2-4 复合族 resize）**：`composite.ts` `CompositeParts` 增 `resize?(key:'width'|'height', value, parts)` hook；`applyCompositeProps` EXTENT_FIELDS 分支改 `if (EXTENT_FIELDS.has(key)) { if (parts.extent) setAttrs(parts.extent,{[key]:value}); else parts.resize?.(key, value, parts); continue; }`（保留 width/height 门禁，无 extent 回落 per-symbol resize hook，不再静默丢弃）。device 4（motor/pump/valve/fan）+ gauge + sensor-control 4（indicator/button/sensor/switch）共 9 个无 extent 族各在 build 注册 resize hook（重算 body 容器尺寸 + 子形状 rotor/core/blades/needle/probe/label 相对锚点；valve 保留 openRatio rotation，switch 按 lever.x 位态保持 on/off）。failing-first proof 11 用例（修复前 width 静默丢弃 body 不变 → 修复后 body/子形状几何跟随）。
- **Phase 2（P2-7 video stroke guard + P2-8 pipe-junction stub strokeWidth + P2-9 thermometer BULB_RESERVE）**：Fix-a `video.ts:35-36` 改 guarded `if (attrs.stroke===undefined) attrs.stroke='#4b5563'; if (attrs.strokeWidth===undefined) attrs.strokeWidth=1;`（对齐 :34 fill guard，author 声明保留）；Fix-b `pipe-junction.ts:85` 改 `strokeWidth: props.strokeWidth ?? 4` + applyProps 增 `if (props.strokeWidth!==undefined) patch.strokeWidth=props.strokeWidth`（与 flow/dash 路由并列，body/stub 粗细一致）；Fix-c `thermometer.ts` 新增模块级 `BULB_RESERVE=-24` 常量，create `y: height + BULB_RESERVE`、applyProps `y: tubeHeight + BULB_RESERVE - props.height`（两路径共用，首帧 8px 跳变消除；既有用例 `liquid.y` 24→16 同步更新）。failing-first proof 5 用例（stroke 被覆盖 / stub 留 4 / 跳变 8px → 修复后保留/跟随/一致）。
- **Phase 3（P2-10 getSymbolProps 读写对称）**：Decision 裁定逆映射方案（拒绝文档化 raw attrs 去 cast）。`symbol-factory.ts` 新增 `fromNodeAttrs(node, raw)`（与 `toNodePatch` 同文件对偶）：`scaleX+scaleY→scale`、`fontSize→textSize`、`textAlign→align`、`dashPattern→strokeDash`、Text 节点 `fill→textColor`/非 Text `fill→fill` 透传（`node.tag` 消解 fill 多对一歧义）；仅发射 schema 键（含 custom/声明层字段当 create 写入节点时透传），mock/leafer 内部键（tag/children/parent/listeners）跳过防往返污染。`scada-engine.ts getSymbolProps` 改 `fromNodeAttrs(leaf.node, leaf.node.get())`。failing-first proof 2 用例（`props.textSize` undefined→18 + 往返稳定 + 非 Text fill 透传）。既有消费者更新：`compound.test.ts:466-470` `scaleX→scale`；e2e `scada-demo.spec.ts:182` `fontSize→textSize`；`use-scada-handles.ts` getSymbol 句柄消费 schema 名（host 工具链获益）。
- **owner docs 同步**：`design-symbols.md` §10（复合族 resize 契约 + create/update 几何一致契约 + getSymbolProps 读写对称契约三条 blockquote）、`design-engine.md` §8.3（getSymbolProps 语义：leafer 属性面 → schema 键名对称）、`flux-guide/design-patterns/scada.md`（getSymbolProps 返回面描述）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 736/736 / 55 files，较 718 baseline +18 tests：Phase 1 ×11 + Phase 2 ×5 + Phase 3 ×2）。
- **closure-audit**：⏳ 待执行——独立 fresh-session sub-agent（非执行 session）执行 closure-audit gate（AGENTS.md human gate，执行 session 不自审）。本日志记录执行完成 + workspace 全绿，closure-audit gate 留 `[ ]`。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」open P2-4/P2-7/P2-8/P2-9/P2-10 五条标注「已由 plan `2026-08-06-0900-2` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0900-3` engine-lifecycle 或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 validator/equality robustness plan `2026-08-06-0900-1` 收口

执行 plan `docs/plans/2026-08-06-0900-1-industrial-hmi-validator-equality-robustness.md`（三 Phase，2129 audit validator/equality 子集 5 条 P2 fail-closed 边界 + 判等正确性 + 子形状校验收口）。

- **plan path**：`docs/plans/2026-08-06-0900-1-industrial-hmi-validator-equality-robustness.md`（三 Phase 全 `[x]` + Status `completed`，Closure Gates 除 closure-audit gate 外全 `[x]`）。
- **共同缺陷面**：组态 JSON 进入运行时的最后一道防线（`serialization/validate.ts` + `serialization/equality.ts`）的健壮性与判等正确性——malformed/恶意 host JSON、非有限数值、数组↔对象形态混同、嵌套 legacy 方言、深度爆炸均应在 validator/判等层被可靠识别（fail-closed 或判等稳定），而非静默放行后在消费者层产 NaN/undefined/空 diff。
- **Phase 1（P2-2 array/object 守卫 + P2-9-deepEqual 深度上限）**：`equality.ts` `deepEqual` 加 `Array.isArray(a) !== Array.isArray(b) → false` 守卫 + 数组按 index 递归分支（object 分支保留 own-keys 递归），`deepEqual([1,2],{0:1,1:2})===false`；增内部 `depth` 参数（默认 0 保持二参公开签名兼容），`depth > MAX_DEEP_EQUAL_DEPTH`（~100）返 `false`（fail-closed），~10k 层嵌套不再 stack overflow。新建 `serialization-equality.test.ts`（全仓首个 equality 直测文件）6 用例（failing-first：混同返 true + 超深抛 RangeError → 修复后 false）。
- **Phase 2（P2-3 数值有限性 + P2-9-validateSymbolNode 递归深度）**：`checkNumberField` 收紧为 `typeof n === 'number' && Number.isFinite(n)`（NaN/±Infinity 被拒，错误文案 `must be a finite number`），`deadband` 旧 inline typeof 检查路由 `checkNumberField` 享 finite 守卫；`validateSymbolNode` 增 `depth` 参数，`depth > MAX_VALIDATE_DEPTH`（~100）push `${scope} exceeds maximum nesting depth` 并 return（fail-closed，不再递归）。failing-first proof 6 用例（Infinity/NaN/-Infinity 放行 + 10k 层 children crash → 修复后 ok:false + finite 文案/深度错误）。
- **Phase 3（P2-5 legacy 递归 + P2-6 assertShape 子形状）**：`scanLegacyAtSyntax` 提取 `scanSymbolLegacy(node, scope, warnings, depth)` 递归 helper 镜像 `validateSymbolNode` 的 children 遍历（scope 拼接 `.children[i]`，嵌套子图元 `@{pointId}` 方言 warn；对称加 depth cap ~100 防深嵌套 crash）；新增 `assertShape(value, schema, scope, errors)` helper（schema `{[field]: 'number'|'string'|'boolean'|'object'|'array'}`），接入 `shadow`（x/y/blur:number、color:string）/ animation `from,to`（object 形态校验 {x,y}）/ `background`（color:string）/ declaration `scale`（k:number、b:number）/ `init`（须 primitive，malformed 拒绝）。failing-first proof 10 用例（嵌套 legacy 不 warn + 子形状放行 → 修复后 warn + ok:false）。
- **owner doc 同步**：`design-renderer.md §4.3` 校验行反映数值有限性 / 子形状校验 / 深度上限 / legacy 递归 + `deepEqual` array/object 不混同。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 718/718 / 55 files，较 696 baseline +22 tests：Phase 1 ×6 + Phase 2 ×6 + Phase 3 ×10）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b501b1bffeMJryRe3w656jt2`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 P2-2 array/object 守卫 + 2-arg 签名兼容；P2-3 Number.isFinite + deadband 路由；P2-9 **三处**深度 cap——equality.ts MAX_DEEP_EQUAL_DEPTH + validate.ts validateSymbolNode MAX_VALIDATE_DEPTH + scanSymbolLegacy MAX_VALIDATE_DEPTH，执行期一度遗漏的 scanSymbolLegacy cap 已补齐；P2-5 legacy children 递归 + scope；P2-6 assertShape 五接入点；workspace 全量 32+32+32+59 全绿；owner doc + roadmap 回写；零 build artifact），零 Blocker / 零 Major / 零 Minor。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-9 + open P2-2/P2-3/P2-5/P2-6 五条标注「已由 plan `2026-08-06-0900-1` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0900-2`/`-0900-3` 或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 diagnostic-channel residual plan `2026-08-06-0746-3` 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-06-0746-3-industrial-hmi-diagnostic-channel-residual.md`（两 Phase，2129 multi-audit dim 19 两条诊断通道 debuggability residual P2 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-06-0746-3-industrial-hmi-diagnostic-channel-residual.md`（两 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **共同缺陷模式**：plan `2026-08-05-2129-3` Phase 3 接通 `RefreshPipeline.onError` → `scada-canvas reportDiagnostic` 后显式 deferred 的两条 residual——(P2-5) handler-error 诊断出口仍丢原始 Error（message-only）且无 host telemetry 路径（`reportDiagnostic` 仅对 flux-\* 码转发 monitor）；(P2-6) `findCircularDependencyError` cause-chain 深度上限硬编码 10，10+ 层 expression chain 的 cycle 误判 generic eval。两条共享同一收口判据：P1-2 接通的 onError 通道在「信息完整性 / 适用范围」上的两个 gap。
- **Phase 1（multi P2-5 handler-error telemetry 对称）**：Decision 裁定 handler-error 经已接 `'action'` phase 的**宽** telemetry 面 `RendererPlugin.onError`（`ErrorMonitorPayload`，`flux-action-core/action-execution.ts` 已用此面转发 action 错误）转发到 host，而非扩 `ExpressionExecutionEnv.monitor` 的 `'expression'` 字面量限定（拒绝——会模糊窄面语义且需改公共类型）。`scada-canvas.tsx`：`onHandlerError` 补第 3 参 `error` 透传（Fix-a）；`reportDiagnostic` 增 `handler-error` 分支经 `rendererRuntime.plugins` 迭代 `plugin.onError(reportedError, { phase:'action', error, details:{code:'handler-error'} })`（per-plugin try/catch 隔离），`reportedError` 统一 `new Error(message, { cause: error })` 保留 cause 链（Fix-b）。对称性：flux-\* → 窄面 `env.monitor.onError`（phase:'expression'）；handler-error → 宽面 `plugins[].onError`（phase:'action'）。§8.1 诊断≠升级契约不破（不动 setStatus/不派发 scada:error）。failing-first proof 由红转绿（修复前 handler-error 既不透传 error 也无 telemetry 路径）。
- **Phase 2（multi P2-6 cycle depth 上限）**：`dirty-collector.ts` 新增 `MAX_CAUSE_CHAIN_DEPTH = 1000` 常量，`findCircularDependencyError` loop `depth < 10` → `depth < MAX_CAUSE_CHAIN_DEPTH`；`cause === current`/`undefined` break 守卫保留（防无限循环），上限仅防御性兜底。failing-first proof 由红转绿（12/50 层 wrapper 包裹 `CircularDependencyError` 旧上限返 undefined → 误判 generic eval；新上限 toBe identity 返原实例）+ 浅链 5 层回归守护 + 自环守卫用例。
- **test-support 扩展**：`createScadaTestEnvironment` 增可选 3rd 参 `options?: { plugins?: RendererPlugin[] }` 透传 `createRendererRuntime({ plugins })`（向后兼容，93 处既有 2-arg 调用零改动）——供 Phase 1 proof 注入 plugin.onError spy 断言 host 宽面可达。
- **owner docs 同步**：`design-renderer.md §8.1`（运行期诊断出口段——两 telemetry 路径对称 + cause 透传段——handler-error 同样透传原始 error，移除「host telemetry 后置 Follow-up」stale 文本）、`design-data-binding.md §9.1`（新增「诊断通道 telemetry 面对称」+「环检测 cause 链深度上限」两 blockquote）反映 live baseline。
- **Decision 诚实**：未改 `flux-core` 公共类型——`ExpressionErrorMonitorPayload.phase` 字面量 `'expression'` 不变、`ErrorMonitorPayload` phase 联合不变（已含 `'action'`）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 696/696 / 54 files，+7 tests：Phase 1 ×2 + Phase 2 ×5）。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02b749de7ffea3gWZyXOU7bQlf`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 Phase 1 scada-canvas 两 telemetry 分支 + onHandlerError 透传 + proof 断言 phase/Error.cause identity + §8.1 不升级；Phase 2 MAX_CAUSE_CHAIN_DEPTH + break 守卫保留 + proof toBe identity；Decision 诚实——公共类型零改 + test-support 向后兼容；owner docs 无 stale 文本；P2-4 为已收口 Non-Goal 非 deferred；文本一致性），零 Blocker / 零 Major / 零 Minor。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-5/P2-6 两条标注「已由 plan `2026-08-06-0746-3` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归后续 mission 节奏；P2-5 Decision 若未来 host 统一 telemetry 面可回头收敛窄/宽面双轨）。

## industrial-hmi 2129 post-remediation audit P2 test-fidelity plan `2026-08-06-0746-2` 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-06-0746-2-industrial-hmi-test-fidelity-false-green.md`（单 Phase，2129 multi-audit dim 23/14 四条 test-fidelity P2 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-06-0746-2-industrial-hmi-test-fidelity-false-green.md`（Phase 1 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **共同缺陷模式**：4 条测试标题声称验 X，但断言的是无关点 / 弱 `not.toThrow()` / 内联等价副本——被测行为退化时测试仍绿（false-green），CI 绿不反映真实行为。本计划仅改测试，零产品运行时/源码改动（仅 4 个 `.test.ts(x)` 文件）。
- **P2-12（codemod 等价副本 → 生产函数同步）**：`expression-codemod.test.ts` 内联副本补齐 `QUOTED_DOLLAR_PATTERN`（parity，3 patterns）+ regex source 与生产逐字对齐；新增**生产源码守护测试**读 `scripts/scada-expression-codemod.mjs`，精确比对每个 pattern 的 regex source（规范化 `\-`→`-` 以过 ESLint `no-useless-escape`）+ `migrateContent` 实际 used-pattern 集合，生产改 regex/启用 QUOTED_DOLLAR 即转红。**fallback 裁定**：primary path（`import { migrateContent }`）经三重 live 阻断确认不可行——生产 `.mjs:89` top-level `main(process.argv)` 致 import 即 `process.exit(1)`、`tsconfig.base.json` `allowJs:false`、自定义 `types/node-fs.d.ts` 仅声明 `node:path` 的 `join/dirname/relative/extname` + `ImportMeta.dirname`；采用 plan sanctioned fallback（内联副本 parity + 生产源码守护测试），同等关闭 false-green。新增 `QUOTED_DOLLAR_PATTERN` 专属用例（独立引号 `'$xxx'` 当前 count=0，刻画生产当前跳过该 pattern）。
- **P2-13（blank 兜底断言）**：`binding-expression-unification.test.ts` 在触发 `syncExpressionPoint('blank')` 后补 `expect(harness.pointStore.getPointValue('blank')).toBe('${}')`（经 live 探测：空串 `''` normalize 为 `${}`，flux-formula 视作 static 字面量，求值结果 `'${}'`）+ `expect(onError).not.toHaveBeenCalled()`（兜底正常求值不应上报）。原断言仅验无关点 `a`，`blank` 从未被断言。
- **P2-14（scene build 真断言）**：`scada-canvas-smoke.test.tsx` test 1 经 test handle（renderer 默认 `exposeTestHandle:true`）补 `expect(handle.getSymbol('rect-1')).toBeTruthy()`（图元入树）+ `expect(root.getAttribute('data-status')).toBe('ready'`（构建完成）。原仅 `handles.length>0`（等价 not.toThrow）。
- **P2-15（空 config flushFrame 真断言）**：`variables-optional-contract.test.ts` 把 `expect(() => pipeline.flushFrame(...)).not.toThrow()` 替换为 `expect(pipeline.flushFrame(applyAttrs)).toBe(false)` + `expect(applyAttrs).not.toHaveBeenCalled()`（`applyAttrs` 为 `vi.fn()`）。原不验返值/副作用。
- **failing-first 手验（每条）**：P2-12 临时改坏生产 `AT_SYNTAX_PATTERN`/`DOLLAR_SHORTHAND_PATTERN` regex → 守护测试转红；P2-13 临时移除 `?? ''` 兜底（expression 变 undefined）→ `blank` 保持 init `0` ≠ `'${}'` 转红；P2-14 临时清空 `validConfig.symbols` → `getSymbol('rect-1')` 返 undefined 转红；P2-15 临时把 `dirty-collector.ts:281` 空脏早退 `return false` 改 `return true` → flushFrame 返 true 转红。均恢复后转绿。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 包测试全绿，4 目标文件 49/49）。`git diff --stat` 仅 4 测试文件 + plan + roadmap（doc），`scripts/` 与 `dirty-collector.ts` 零改动。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02b82fd81ffe9BPYP3RslQJKaQ`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 4 P2 fix 断言结果值非 not.toThrow + P2-12 fallback 三重 live 阻断 + 重跑 4 测试文件 49/49 绿 + git diff 确认零源码改动），零 Blocker / 零 Major。
- **roadmap 回写**：`roadmap-industrial-hmi.md` Follow-up Backlog「2026-08-05-2129」multi P2-12/P2-13/P2-14/P2-15 四条标注「已由 plan `2026-08-06-0746-2` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0746-3`（diagnostic residual，active）或后续 mission 节奏）。

## industrial-hmi 2129 post-remediation audit P2 doc-drift cleanup plan `2026-08-06-0746-1` 收口（mission-driver build verify 全绿）

执行 plan `docs/plans/2026-08-06-0746-1-industrial-hmi-doc-drift-cleanup.md`（单 Phase，2129 multi-audit dim 16 三条纯文档漂移 P2 收口），独立 closure-audit 通过；本条目由 mission-driver `2026-08-05-212942-mission-driver` build-verify 轮记录。

- **plan path**：`docs/plans/2026-08-06-0746-1-industrial-hmi-doc-drift-cleanup.md`（Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate，Test Strategy 档位「不适用」）。
- **根因**：I18 表达式一元化（弃用 `$xxx`/`@{pointId}`、删 `binding/expression-evaluator.ts`、拆 `bind-resolver.ts`+`flux-eval.ts`）与历次模块迁移（`schemas.ts`/`renderer-definitions.ts` 上移 `src/` 顶层、新增 `scada-errors.ts`/`serialization/equality.ts`/`use-scada-handles.ts`）后设计文档未同步回写。
- **P2-1（§11 实现树）**：`design-renderer.md §11` 代码树重写对齐 live 布局——`schemas.ts`/`renderer-definitions.ts` 从 `renderer/` 上移 `src/` 顶层；`serialization/` 补 `equality.ts`（plan 0653-4 C2 / W5 共享 deepEqual）；`renderer/hooks/` 补 `use-scada-handles.ts`（component:\* 句柄注册）；`renderer/` 补 `scada-errors.ts`（SCADA_ERROR_CODES 注册表，plan 1558-2 Phase 4）。
- **P2-2（§9:270 $xxx）**：`design-engine.md §9:270` `$xxx` → `${expr}`（I18 一元化后唯一语法）+ 指向 `design-data-binding.md §4.2`。
- **P2-3（editor-initiation row 8）**：`editor-initiation.md:60` row 8 binding 域模块清单对齐 `design-data-binding.md §11` post-I18 拆分——移除 `expression-evaluator（@{pointId} 子集）`，补 `bind-resolver.ts`/`flux-eval.ts`/`reverse-index.ts`/`dirty-collector.ts`/`value-to-state.ts`/`animator.ts`。
- **纯文档零代码变更**：`git diff --stat` 仅 4 个 `.md`（design-engine 1 行 / design-renderer 11 行 / editor-initiation 1 行 / roadmap 8 行）+ 3 个新增 plan `.md`，`packages/` 无任何源码/测试/配置改动。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02b9a5e09ffeRPgEm8CsYAk3tG` 执行 closure-audit（live `ls` 逐项核验 §11 树文件路径 + rg 判据收窄到 3 编辑面排除 `design-data-binding.md` 弃用通告段），零 Blocker / 零 Major。
- **mission-driver build verify（本轮）**：纯文档计划，build/test/lint/typecheck gate 按 plan guide「纯文档计划」条款豁免；为确认提交前基线绿，本轮仍跑全量——`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（均 FULL TURBO 缓存命中，零源码改动故缓存有效）。e2e 未跑（非 full-green 声明）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` 新增 completion 活动条目 + Follow-up Backlog「2026-08-05-2129」multi P2-1/P2-2/P2-3 三条标注「已由 plan `2026-08-06-0746-1` 收口，✅」。
- **Follow-up**：no remaining plan-owned work（其余 2129 P2 归 sibling plan `2026-08-06-0746-2`（test fidelity，active）/`2026-08-06-0746-3`（diagnostic residual，active）或后续 mission 节奏）。

## industrial-hmi 2129 audit P1 四项 remediation plan 收口（closure-audit pass）

执行 plan `docs/plans/2026-08-05-2129-3-industrial-hmi-audit-p1-remediations.md` 全四 Phase（4×P1 收口），独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-3-industrial-hmi-audit-p1-remediations.md`（四 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **源审计**：`docs/audits/2026-08-05-2129-open-audit-industrial-hmi.md`（1×P1）+ `docs/audits/2026-08-05-2129-multi-audit-industrial-hmi.md`（3×P1），两份 `Audit Status: planned → closed`。
- **Phase 1（multi P1-3 oversized-code-files）**：`serialization.test.ts`（749）拆为 parse-serialize/diff/validate + fixtures，`refresh-pipeline.test.ts`（726）拆为 core/state/animation-lifecycle + fixtures，8 兄弟均 <700 行（wc -l 实测），纯机械搬迁零行为变更。
- **Phase 2（multi P1-1 binding-expression scope 订阅）**：`use-scada-points-bridge.ts` 新增 `collectBindingExpressionScopePaths` 扫 `symbols[].bindings[].expression`（递归 children）经 `probeExpressionPaths` 提取 scope 路径（排除声明点 id），`paths` = variables ∪ binding-expression → `variables:[]` + binding.expression 直连 scope 场景 `useScopeSelector` enabled、binding 正确求值。Integration test `binding-expression-scope-integration.test.tsx` 挂真实桥接边界（useScadaPointsBridge + useScopeSelector + 真实 pipeline runtime），断言 text=42（scopeVal=21）→ 60（reactive update）+ nested=6，关闭 variables-optional-contract 场景 ① false-green。
- **Phase 3（multi P1-2 RefreshPipeline.onError 生产接线）**：`createBindingDomain` 增 `onError` 参数；`UseScadaEngineArgs.onPipelineError` ← `scada-canvas.tsx:182 reportDiagnostic`（与桥接层同出口），mount + reloadBindings 两路径透传。`dirty-collector.ts evaluateFlux` 改返 discriminated `FluxEvalOutcome`（compile-failed/evaluate-failed/ok）使 `reportError` emit 对称码 `flux-compile-failed`/`flux-evaluate-failed`，三个声明源 source:flux / binding.expression / scale.expression observably symmetric。Integration test `scada-canvas-diagnostic-channels.test.tsx:312+` 断言坏 binding.expression 错误到达 monitor.onError（phase:'expression', code:'flux-evaluate-failed'）+ 对称性 proof。
- **Phase 4（open P1-1 shadow revert 完整性）**：`visual-state.ts STYLE_RESET_DEFAULTS` 改 `Record<keyof ScadaSymbolStylePatch, unknown>` 穷尽守卫（编译期防未来新增 style 字段机械遗漏）+ 补 `shadow: {x:0,y:0,blur:0,color:'transparent'}` reset 值 + hoist `applied.delete(key)` 到 `if (revert !== undefined)` 守卫之外（belt-and-suspenders 防追踪泄漏）。翻转既有缺陷测试 `state-visual.test.ts:201-243`：退出 fault 态后 shadow 清除为 reset default（非永久残留）+ 重入 fault 再应用。
- **owner docs 同步**：`design-data-binding.md §9.1`（无点表直连 scope 端到端 wiring Phase 2 + 错误码对称 Phase 3）、`design-renderer.md:220`（pipeline onError 通道接线 + FluxEvalOutcome + 对称码）、`visual-state.ts:10-19`（reset map 穷尽守卫 + applied.delete hoist 结构注记）。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 687/687 / 54 files）、`pnpm check:oversized-code-files` industrial 包 0 失败（14 ERROR 文件全在其他包，pre-existing out-of-scope）。
- **closure-audit**：✅ **pass**——独立 fresh-session sub-agent `ses_02bb6a641ffe7eR3pBVTw6MA5v`（general subagent，非执行 session）执行 closure-audit（live repo 逐条核对 4 P1 fix + proof 断言结果值非 call-count + 机械门禁），零 Blocker / 零 Major。
- **roadmap 回写**：`roadmap-industrial-hmi.md` 新增 completion 活动条目 + P2 backlog「2026-08-05-2129」子节源标注更新（P1 已收口、源审计 closed）。
- **Follow-up**：25 P2 在 roadmap Follow-up Backlog「2026-08-05-2129 post-remediation audit P2」追踪（各带源审计路径）；无 plan-owned remaining work。

## industrial-hmi mission-driver build verify：unit + 变更域 e2e 全绿

mission-driver `2026-08-05-212942-mission-driver` 对 industrial-hmi 工作集做 build verify。

- `pnpm typecheck` 32/32 ✓（FULL TURBO 缓存命中）
- `pnpm build` 32/32 ✓（flux-playground 增量重建，leafer-examples-demo chunk 产出）
- `pnpm lint` 32/32 ✓（仅 gantt-grid 既有 react-hooks/incompatible-library warning，0 error）
- `pnpm test` 59/59 ✓（vitest 全包绿）
- 变更域 e2e（scada-demo + leafer-examples + playground-entry-pages）81/81 ✓
- 说明：既有 pre-existing e2e 失败（gantt/kanban/scada-perf FPS + w3d-editor TipTap）不在本工作集，未跑；unit 100% + 变更域 e2e 100% 绿。

## industrial-hmi I17 Demo 视觉优化 + 参考资源登记 + LeaferJS 对照页 plan 全三 Phase 收口

执行 plan `docs/plans/2026-08-05-2129-2-industrial-hmi-demo-polish-leafer-examples.md` 全三 Phase，独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-2-industrial-hmi-demo-polish-leafer-examples.md`（三 Phase 全 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]` 含 closure-audit gate）。
- **Phase 1（scada-demo 坐标重排 + e2e 坐标同步）**：`apps/playground/src/pages/scada-demo.tsx` `symbols` 数组坐标重排——管道统一 y=232（5 条 pipe 全部对齐）、设备-管道几何连续（pipe-in→level→pipe→pump→pipe→valve→pipe→junction→pipe→gauge 链 x:0→800 无断点）、8px 对齐基线、文本标签锚定到设备 bounds 下方。**不变性守护**：diff 仅含 x/y/width/height + text x/y，id/type/testid/bindings/events/custom/text/textSize/textColor 零改动。`tests/e2e/scada-demo.spec.ts` 4 处 world 坐标同步：pump-1 中心 (242,234)→(236,232) ×3、motor-1 中心 (402,64)→(408,80) ×1。
- **Phase 2（参考资源附录登记）**：`design-engine.md` 新增 §13「LeaferJS 官方示例对照」附录（9 链接：App/viewport/坐标/包围盒/局部渲染/Group/Editor/Flow/viewport 插件，对应引擎层各章节）；`design-symbols.md` 新增 §13「第三方图元库参考」附录（meta2d MIT 无 HMI 图元 / FUXA MIT 真实 SCADA / OSHMI GPL-3.0 仅设计层 + 定性与 roadmap I17.2 一致）。
- **Phase 3（playground LeaferJS 对照页）**：新增 `apps/playground/src/pages/leafer-examples-demo.tsx`（4 示例：基础元素 / App 三图层 / animate 动画 / viewport zoom-move），`route-model.ts` 加 `leafer-examples` 条目（eyebrow 'Reference'，**不进 home 卡片**），`App.tsx` lazy-load（`leafer-ui` canvas runtime 隔离，保持 App 单测 leafer-free）。`leafer-ui` + `@leafer-in/view` + `@leafer-in/animate`（v2.2.9）作为 playground 直接依赖引入（plan 假设 transitive 可用经核实不成立 → 直接依赖修正）。e2e smoke：`leafer-examples.spec.ts`（路由可达 + 4 canvas 存在 + viewport 控件交互）+ `playground-entry-pages.spec.ts` 加 ROUTE_ASSERTIONS 条目。
- **workspace 全量验证**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓。e2e：scada-demo 12/12 ✓、leafer-examples 2/2 ✓、playground-entry-pages 67/67 ✓。5 项 pre-existing 失败（gantt/kanban/scada-perf FPS + w3d-editor TipTap）与本 plan 文件无交集。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02c871be4ffeTr1Sx3cbR9Cdlm` 执行 closure-audit（live repo 核对 Phase 1 不变性 + 管道几何连续 + e2e 坐标 + Phase 2 链接/定性 + Phase 3 路由/非卡片 + 无静默降级），零 Blocker / 零 Major / 2 Minor 记录性（M1 roadmap 待翻 done、M2 leafer-ui 直接依赖记录）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` I17 Phase Status `todo` → `done`。
- **Follow-up**：无剩余 plan-owned 工作。编辑器交互按 `editor-initiation.md` 独立后继 mission 编排。

## industrial-hmi I18 表达式一元化 plan 收口：Phase 4 owner-docs 同步 + lint/e2e 修复 + closure-audit approved

执行 plan `docs/plans/2026-08-05-2129-1-industrial-hmi-expression-unification.md` Phase 4（验证与文档同步）+ 修复 Phase 1-3 遗留 lint 错误与 demo e2e 回归，独立 closure-audit 通过。

- **plan path**：`docs/plans/2026-08-05-2129-1-industrial-hmi-expression-unification.md`（Phase 1-4 全部 `[x]` + Status `completed`，Plan Status `completed`，Closure Gates 全 `[x]`）。
- **Phase 4 Fix（owner docs 同步）**：`design-data-binding.md`（决策表双轨→单一 `${expr}` + 点表可选、§4.1/§4.2 类型注释、§4.2 表达式求值边界、§9.1 三源接入表+注、§11 实现拆分删 expression-evaluator.ts 增 flux-eval.ts、§12.2 风险）、`design-renderer.md`（§8.3 `$xxx`→`${...}`）、`roadmap-industrial-hmi.md`（I18.3「无 demo 迁移」过时行回写、Phase Details「demo 免迁移」回写 + contract-lock 注记）。
- **lint 修复（Phase 1-3 遗留，3 项）**：① `dirty-collector.ts:450` `no-this-alias`——`buildEvalScope()` 的 `get` 方法从 regular method + `const self = this` 改为 arrow function（行为保持）；② `expression-codemod.test.ts:23` `no-useless-escape`——regex 中 `\-` 去 escape；③ `refresh-pipeline.test.ts` `max-lines`（785→726，B3 stateSource + scale 转发两组 describe 的重复 boilerplate 收口为 helper，断言全保留）。
- **demo e2e 回归修复**：Phase 3 将 demo `flux: '$tankLevel'` 迁为 `flux: '${tankLevel}'`，但 `${...}` 在 schema-embedded config 中被 flux schema compiler **急切求值**（表达式字符串→求值数字），validator 拒绝（flux must be string）→ 画布 `data-status="error"` → 12 个 scada-demo e2e 全红。修复：改为**裸路径** `flux: 'tankLevel'`（桥接 `normalizeFluxExpression` 运行时包成 `${tankLevel}`），规避 schema compiler 急切求值。这是 schema-embedded config 的 I18 正确模式（`normalizeFluxExpression` 的裸路径包裹分支即为此设计）。
- **Phase 4 Proof（workspace 全量验证）**：`pnpm typecheck` 32/32 ✓、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（industrial 683 tests）。scada e2e：demo/edge-cases/pressure-demo 21/21 ✓；perf 100k pan test 通过（机器负载敏感，低负载时通过）。
- **closure-audit**：✅ **approved**——独立 fresh-session sub-agent `ses_02ca871ccffeMTp9GWO2Q6hXwI` 执行 closure-audit（live repo 核对 9 项 checklist），零 Blocker / 零 Major。closure-audit gate 由该 sub-agent tick（执行 session 不自审）。
- **roadmap 回写**：`roadmap-industrial-hmi.md` I18 Phase Status `todo` → `done`。
- **Follow-up**：旧 `@{}` config 下线后把 validator `legacy-at-syntax` warn 收敛为 fail（Non-Blocking Follow-up，按 mission 节奏）。
