# Development Log - 2026-06-26

### 2026-06-26 (B7 P2/P3 信号收口 — 99 条逐条裁定 + residual landing — amis-bug-driven mission 闭环)

- 执行 plan `docs/plans/2026-06-26-2100-1-b7-p2p3-signal-triage-residual-adjudication-plan.md`（roadmap Wave B7）：收口 amis-bug-driven roadmap 最后一个工作项 B7。本 plan 是 **triage + residual-landing** 计划，非 feature 实现计划——对 14 源 signal doc 中全部 P2/P3 signal（**99 条** = 97 单一 decision-type + 2 复合标签行 I11/D3）做一次性 triage，逐条给出显式裁定，使「B1–B6 closure 成立后 B7 可启动评估」roadmap gate 兑现。
- **Phase 1（triage）**：建立裁定工作表 `docs/components/amis-bug-driven-improvements/_b7-triage.md`（99 条 × verdict + evidence）。两轮并行独立 explore sub-agent：(1) 枚举全部 99 P2/P3 行（severity 列 padded 对齐 + 2 复合行 + 2 embedded-`|` 行 T19/AG4 + 3 blockquote 行 D8/L4/L5/DD11，reconcile 到 99）；(2) 映射 B1–B6 全部 13 plan 已落地 test/doc/deferred。逐条裁定五类归宿：`covered-by` **21**（B1–B6 主题/直接覆盖，如 V7/C7、T9/T10/B3.3、L4/CB2/MP1/AG2 已有测试、I11 clamp 已锚）、`landed-anchor` **1**（T13）、`landed-doc-note` **1**（F2）、`out-of-scope-feature` **8**（T2/T28/I10/D10/TR7/DD7/A10/M2）、`watch-only` **68**（每条含 non-blocking 理由：construct-true / low-freq / host-layer / depends-on-unimplemented-feature）。**Anti-Slacking**：无 in-scope live defect 被静默降级（B1–B6 已修全部 P0/P1 live defect）。每条在源 doc AMIS-REF cell 追加 `RESOLVED (B7): <verdict> <evidence>` 内联标记（grep 校验 = 99，无 AMIS-REF 丢失）。5 条 `covered-by` 独立 spot-check（V15/T9/L4/CB2/MP1）经 live repo 核对一致。
- **Phase 2（residual landing）**：`landed-anchor`——新建 `packages/flux-renderers-data/src/__tests__/table-tree-selection-no-cascade.test.tsx`（B7/T13 LOCK，amis #5865 lost）：tree-table 选 parent rowKey → `selectedRowKeys==['p1']`，children c1/c2 NOT 自动入选（Flux selection 是 flat `Set<rowKey>` 单键 toggle，零 cascade 代码——grep 确认）；附向上/旁向不级联 control。`landed-doc-note`——`docs/components/crud/design.md` B3.1 契约段新增 F2（cleared-field query 语义）：核对 live code 后准确写为「client-match（`applyQueryToRows`）按 F1 丢弃空值；发布的 `onQuerySubmit` `query` payload 透传 queryForm `getValues()` 经 `toRecord` 归一化、**不自动省略清空 key**；省略经 `transformOutAction`/query-adaptor 请求层处理」（`toRecord` 经 `object.contract.test.ts` 确认不 strip empty-value key）。**I11 重分类**：初判 `landed-anchor`，live 核对发现 `input-number.test.tsx:160`（clamps to max on blur，types 200 max:100 → submit 100 无 error）已锚 → 改判 `covered-by`，避免冗余重复锚。
- **Phase 3（deferred 收敛 + B7 关闭）**：plan `Deferred But Adjudicated` 在 B1–B6 已有 10 项（V6/C10-projection/T11/U5/U6/DD9/I1-schema/I4/L16/MP2-loader）基础上新增 8 项 Phase-1 裁出 feature（T2/T28/I10/D10/TR7/DD7/A10/M2），每项 `Classification: out-of-scope improvement` + non-blocking 理由 + successor 指向。roadmap B7 Phase Detail 写入完整 successor feature 清单（18 项，可反查 backlog）。
- 生产源码改动：**仅新增 1 个测试文件**（T13 锚，无生产代码变更）+ owner doc（crud/design.md F2 一条决策）。无 schema/运行时行为变更。其余为 14 源 doc RESOLVED 标记 + plan/roadmap/triage-worksheet 文档。
- **验证（full-green：unit 全绿 + workspace 全绿）**：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29，仅预存 ineffective-dynamic-import warning) + `pnpm lint`(29/29) + `pnpm test`(workspace 55/55 packages 全绿；flux-renderers-data 含 +1 T13 锚文件 2 tests，全包 591 tests 全绿)。新增 T13 锚 spot-run green。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B7 `todo`→`done`（Wave B7 列表 line 55 + 表格 Status line 139 + Phase Detail line 199）。**amis-bug-driven mission 闭环（B1–B7 全 done）。**
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Closure-audit：由独立 fresh-session 子 agent 执行 closure-audit（AGENTS.md human-gate，执行 session 不自审）。证据见 plan `Closure Audit Evidence`。
- Follow-up：18 项 successor feature（10 B1–B6 deferred + 8 B7 新裁）指向 feature roadmap / 独立 feature plan（本 roadmap 是测试/文档边界债 roadmap，不实现 feature）。无剩余 plan-owned work。

### 2026-06-26 (button disabled-expr / label & / toast strict-mode+duration / mapping source-scope / styling no-helper.css — plan B6.2)

- 执行 plan `docs/plans/2026-06-26-2016-1-b62-button-mapping-toast-styling-contract-plan.md`（roadmap Wave B6 B6.2）：收口 6 条 signal——B1（button `disabled` boolean|expr LOCK）、B3（label `&` 忠实渲染 TEST-GAP）、T2（toast imperative StrictMode 安全 TEST-GAP）、T3（toast duration 跨 variant 一致 TEST-GAP）、MP2（mapping source-scope DESIGN-GAP，含 owner-doc drift Fix + loader 裁定）、STY2（utility className 无 helper.css LOCK + DESIGN-GAP）。纯「lock + test-gap 回归锚 + owner-doc 显式化 + 裁定」，无新行为/schema surface，无生产运行期行为变更。
- **Phase 1（B1/B3）**：`packages/flux-renderers-basic/src/__tests__/button-enhancements.test.tsx` 新增 `contract regression anchors (B6.2)` describe——B1 两条：`disabled:"${!isAdmin}"` 经 scope 求值，`isAdmin=true`→DOM `button.disabled===false`、`isAdmin=false`→`===true`（两分支均断言 DOM 真实态）；B3 一条：`label:"${name}"`、`name="A & B"`→`textContent==="A & B"`（单层转义，`innerHTML` 不含 `&amp;amp;` 双逃义）。复用 `createBasicSchemaRenderer` + `data={{...}}` scope 注入（证明 `expressionCompiler.compileValue` 对所有 string prop 串 `${}` 统一编译，与 `editorType` 无关，见 `node-compiler.ts:474`）。
- **Phase 2（T2/T3）**：T3 加在既有 `packages/ui/src/components/ui/sonner.test.tsx`（mock 化，结构 guard）——断言渲染 `<Toaster/>` 的 `toastOptions` 无 `duration`（top-level 或 per-variant）、wrapper 不转发 `duration`；附源结构 guard `readFileSync('src/components/ui/sonner.tsx')` 断言无 `duration` token。T2 因需真实 sonner 全局 store（既有 mock 文件不提供 `toast` API），新建 `packages/ui/src/components/ui/sonner-strict-mode.test.tsx`（**不 mock sonner**）：`<React.StrictMode>` 挂 `<Toaster/>` + `act(()=>toast.success(...))` 一次 → `expect.poll(()=>document.querySelectorAll('[data-sonner-toast]').length).toBe(1)` + `toast.getToasts().length===1`（验证 Flux wrapper 无 effect 驱动 toast 创建，StrictMode 不双发）。
- **Phase 3（MP2）**：Fix——`docs/components/mapping/design.md` §9 重写：删去「loader 归一化结果」虚假声称，声明 `map` 仅静态对象/expression（无 `source`/`initApi`/`api`，组件级请求被请求下沉审计拒绝）；新增 §9.1 source-scope（per-row 对 row scope 求值、page-level 共享 `map` 经词法继承每行同对象无发散、renderer 无 once-per-renderer cache）、§9.2 empty-map+无 `*` wildcard fallback（`lookupMap` `hasOwnProperty(String(value))` 逐字匹配）、§9.3 loader+「loader wins」precedence → DESIGN-ACK-NOT-IMPL + successor B7。`schemas.ts` `value` 注释同步去掉误导的「/source」。Proof——`mapping.test.tsx` 新增 `MP2 source-scope / no-wildcard (B6.2)` describe：cards 行内共享 `map:"${statusMap}"`（page scope）三行按各自 `status` 解析 Active/Idle/Unknown（无 per-row 发散、pending→defaultLabel 无 wildcard 误命中）；`map:{'*':...,a:...}` value 'unknown'→miss→Default、value '_'→hit→Star（锁 `'_'` 是字面 key 非通配）。
- **Phase 4（STY2）**：Fix——`docs/architecture/styling-system.md` Architecture Guardrails 新增 STY2 LOCK 条目（utility classNames 经 Tailwind v4 `@source "../../../packages"` + `@theme inline` 颜色映射生成，仅默认 stylesheet 即生效，全仓无 helper.css / 无 `.amis-scope`；引 `apps/playground/src/styles.css:1,8,21` + `docs/bugs/14-*.md`）。Proof——新建 `packages/ui/src/__tests__/styling-no-helper-css.test.ts` repo-structure guard：walk packages+apps 源（手动递归 prune `node_modules`/`dist` 避免 pnpm store symlink 循环）断言 (a) 0 `helper.css`、(b) packages `*.css`/`*.tsx` 无 `amis-scope`、(c) `styles.css` 含 `@source "../../../packages"`。
- 生产源码改动：无行为/schema 变更（仅 `schemas.ts` 一行注释去「/source」误导措辞）。其余为测试 + owner doc。
- **验证（full-green：unit 全绿 + workspace 全绿）**：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，仅 flux-renderers-form-advanced 1 预存 `useVirtualizer` warning，git 确认 HEAD 即存在、非本改动) + `pnpm test`(workspace 55/55 packages 全绿；flux-renderers-basic 408 含 +3 button 锚、ui 139 含 +3 sonner STY2 锚、flux-renderers-content 171 含 +2 mapping MP2 锚)。新增 5 个测试文件/段全绿。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B6.2 `planned`→`done`（Wave B6 列表 line 51 + 表格 Status line 133）。**B1–B6 closure 成立，B7 可启动评估。**
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Closure-audit：由独立 fresh-session 子 agent `ses_0fc06af59ffesRpQcixHc6yLa7`（general，不复用执行 session）执行 closure-audit，VERDICT `pass`（零 Blocker/Major）——独立 spot-run B6.2 测试（button 3 / ui 11 / mapping 2 全绿，全包 408/139/171 吻合）+ 独立读 live 源（button.tsx:48,103 / sonner.tsx:21-25 / mapping.tsx:13-22,70-73 / schemas.ts:202-214 / styles.css:8）+ `git diff HEAD` 确认唯一生产改动=schemas.ts 1 行 JSDoc（button/sonner/mapping byte-identical）。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Follow-up：mapping loader-sourced map + 「loader wins」precedence → successor B7（out-of-scope distinct feature，见 plan Deferred But Adjudicated）；doc-14 余项（B2/B4/T1/MP1/CD2/CD3/CD5/CD6/ST1-ST3/STY1/STY3/STY4/DB1/DB2/AG2/AG4/AG5）→ B7 backlog。无剩余 plan-owned work。

- 执行 plan `docs/plans/2026-06-26-1030-3-b52-tabs-dynamic-chart-i18n-plan.md`（roadmap B5.2）：收口 14 条 signal——L6（drawer inner-overlay 回归锚）、L8（tabs candidate-fix Fix）、L9（mountOnEnter/unmountOnExit 回归锚）、L14（card media className Fix）、L16（iframe notRetained 裁定）、DD1（chart 空态硬契约复核）、DD2（chart in-place feasibility 锚）、DD8（markdown reactivity 锚）、DD13（dynamic-renderer lexical/per-instance 锚）、I2（no-baked-in-literal leak-guard）、I3（`t()` formula builtin Fix）、I4（reactive locale DESIGN-ACK 裁定，signal 重分类 TEST-GAP→DESIGN-ACK）。三处 Fix（L8/L14/I3）failing-test-first 先红后绿；其余为回归锚 + owner doc 显式化 + 裁定。
- **Phase 1（L6/L8/L9/L14/L16）**：L8 Fix——`tabs.tsx` 新增 `resolveCandidateValue`（keep→nearest-right→nearest-left→empty）+ candidate-fix effect（仅 local/scope ownership 经 `ownedAxis.setValue` 写回，controlled 保持受控契约；幂等：value 匹配后不再触发）；`tabs-candidate-fix.test.tsx`（keep/nearest-right/nearest-left/empty/controlled-不改写，先红后绿）。L14 Fix——`CardSchema`/content-renderer-definitions 增 `imageClassName`，`card.tsx` 用 `cn('aspect-video w-full object-cover', imageClassName)` 合并；`card.test.tsx` L14 anchor（先红后绿）。L6 锚——`dialog-host-responsive.test.tsx` L6 anchor（DrawerContent inline style 任何 size/width/height 下永不写 position/top/left/zIndex，按构造满足）。L9 锚——`tabs-owner-lifecycle.test.tsx`（mountOnEnter 延迟挂载 + unmountOnExit 干净重初始化无累积）。L16 裁定——`amis-baseline-matrix.md` 新增「Not-Retained Boundary: iframe」段（listener/clone-safety 属 host 集成）。
- **Phase 2（DD1/DD2/DD8/DD12/DD13）**：DD8 锚——`markdown-reactivity.test.tsx`（content:'${md}' mutate scope→HTML 更新）。DD13 锚——`dynamic-renderer-lexical.test.tsx`（child scope 读取 + 两 fragment 隔离 instance 不碰撞）。DD1/DD2 锚——`chart-renderer.unit.test.tsx`（DD1 `[]`/undefined/null 空态 never-error + 畸形 series 过滤；DD2 数据更新 chart-canvas 宿主节点 identity 稳定 + 无 loading flash，recharts mocked 故 by-construction + 可测部分锚）。owner doc：`chart/design.md` §6 DD1 空态硬契约 + DD2 in-place 声明（by construction 非 test-verified）；`markdown/design.md` §9 DD8 reactivity；`dynamic-renderer/design.md` §5 loadAction `kind:'prop'` 修正 + §6 DD12/DD13 lexical/per-instance。
- **Phase 3（I2/I3/I4）**：I3 Fix——`flux-formula/src/builtins.ts` 注册 `t(key, params?)` 调 `getMessageFormatter()`（call-time 读当前 locale，static-eval 不折叠 Identifier callee → runtime 评估）；`builtins.test.ts` I3 anchors（expression/template/identity-fallback，先红后绿；未破坏 validation message sink——validation-message 1252 tests 绿）。I2 leak-guard——`chart-renderer.unit.test.tsx` I2 anchor（override `flux.common.noData` → chart 空 fallback 反映 override，证明非 baked-in）。I4 裁定——新建 `docs/components/i18n/design.md` owner doc（message-key 半边 + `t()` formula 暴露 + locale 覆盖边界 + I2 硬规则 + I4 signal 重分类 TEST-GAP→DESIGN-ACK + `useFluxTranslation` hook 锁，测试 i18n.test.ts:77-101 已在）。
- 生产源码改动：`flux-renderers-basic/src/tabs.tsx`（candidate-fix）、`flux-renderers-content/src/card.tsx`+`schemas.ts`+`content-renderer-definitions.ts`（imageClassName）、`flux-formula/src/builtins.ts`（t builtin）。其余为测试 + doc。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(含 check-i18n-keys/renderer-fields/finite-prop-contracts 全通过，0 errors) + `pnpm test`(workspace 55/55 packages 全绿；flux-formula 190、flux-i18n 26、flux-renderers-data 589、flux-renderers-content 169、flux-renderers-basic 405、flux-react 436、flux-runtime 1252 全绿)。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B5.2 `planned`→`done`（Wave B5 列表 + 表格 Status）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Closure-audit：执行 session 不自审（AGENTS.md human-gate），Closure Gates 末项 `[ ]` 留待 fresh-session 独立子 agent。Plan Status 已置 `completed`（三 Phase 全执行 + tick）。
- Follow-up：DD2 recharts 真实 ResponsiveContainer identity 断言（watch-only，unmock 后可补）；item-level visible/hidden candidate-fix（独立维度，Non-Blocking Follow-ups）；I4 reactive locale 全量接线（successor roadmap B7，若产品判定硬需求）；iframe host 集成（host 职责）。无剩余 plan-owned work。

### 2026-06-26 (action-graph reload target / condition-builder disabled umbrella / cards selection+itemAction scope — plan B6.1)

- 执行 plan `docs/plans/2026-06-26-1030-2-b61-action-graph-reload-condition-builder-disabled-cards-plan.md`（roadmap B6.1）：收口 6 条 signal——AG1（多步 async 顺序 LOCK）、AG3（reload 目标解析 doc 显式化 + 负向回归锚）、CB1（disabled umbrella 扇出）、CB3（showNot runtime-added-group + 序列化往返）、CD1（selectionMode none off-disables-both）、CD4（onItemClick per-row itemScope）。经 live 审计六条行为均已落地，本计划交付 owner doc 显式化 + 聚焦回归锚 + 一处 residual 裁定（CB1 dnd-kit sensor watch-only）。纯「lock + doc + 回归锚」，无新 runtime feature。
- **Phase 1（AG1/AG3）**：复核 AG3 doc 映射声明已存在（`action-scope-and-imports.md:664-671` amis `reload`→`refreshSource`/`component:refresh` 定向重载、不调 `ctx.page?.refresh()`、整页独立 `refreshTable`；`button/design.md:80`）；AG3 负向回归锚 `runtime-actions-advanced.test.ts:222`（`refreshSource` 后 `page.refreshTick` 不变 + source 已刷新）；AG1 LOCK 复核既有分维度用例（顺序/async/分支链接，flux-action-core 203 tests 绿）。Phase 1 doc + 锚均已落地，本会话复核 + tick。
- **Phase 2（CB1/CB3）**：新增 `condition-builder-disabled-umbrella.test.tsx`（CB1 单例 umbrella：NOT/if/AND-OR/add-condition/add-group 全 disabled；item delete、group delete、drag handle 不渲染；disabled click 不 mutate onChange；含 control 用例证明 enabled 态 affordance 在场）；新增 `condition-builder-cb3-not.test.tsx`（CB3：runtime add-group → 新子组也现 NOT toggle + toggle 写 `value.not`；`sanitizeNode` 保留 `not` 单测；AMIS `{combinator,not,rules}` 输入经 `convertAmisRule` 往返渲染 `NOT ✓`；write-back toggle off 写回）。owner doc `condition-builder/design.md` §7.6 新增 disabled umbrella 扇出矩阵 + CB1-residual 裁定（dnd-kit sensor 无条件实例化但 drag handle 在 disabled 时不渲染 → watch-only residual，successor no）+ §10 marker 约定补 disabled 不改命名。
- **Phase 3（CD1/CD4）**：新增 `cards-selection-itemaction.test.tsx`——CD1：`selectionMode:'none'`（及默认 unset）→ 点击卡片无 `data-selected`、不触发 `onSelectionChange`；`interactive` 仅绑 `onItemClick` 时为真（cursor-pointer/tabindex=0）但无 selection 高亮；无 onItemClick+none → 非 interactive。CD4：`onItemClick` action 读 `${item.label}` 经 namespace-capture provider 断言解析到**被点行**（点 Beta→"Beta"、点 Gamma→"Gamma"），证明 per-row itemScope 求值。owner doc `cards/design.md` §4/§7（selectionMode none off-disables-both + `selectionMode` vs amis `selectable` NOT-ADOPTED）+ §6/§8（onItemClick per-row itemScope，引 `cards-renderer.tsx:125,151-154`）；`card/design.md` §8（独立 card 无 per-row itemScope，行级 scope 属 cards 集合）。
- 生产源码改动：无（纯测试 + doc）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm test`(workspace 55/55；flux-action-core 203、flux-runtime 1252、flux-renderers-form-advanced 882、flux-renderers-content 167 全绿)。`pnpm lint`：flux-renderers-form-advanced 有 1 预存 error（`tree-select` `react-hooks/incompatible-library` @ 270-271）+ 1 预存 warning，**git stash 确认 HEAD 即存在**、非本改动；三个新测试文件单独 eslint 全绿。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B6.1 `planned`→`done`（Wave B6 Phase Status 列表 + 表格 Status）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Closure-audit：执行 session 不自审（AGENTS.md human-gate），Closure Gates 末项 `[ ]` 留待 fresh-session 独立子 agent。Plan Status 已置 `completed`（三 Phase 全执行 + tick）。
- Follow-up：CB1-residual（dnd-kit sensor 短路）watch-only、successor no；AG1 合并权威用例（>2 步+async+result-data）既有分维度已覆盖，缺则记 Non-Blocking Follow-ups；B6.2 独立推进；B5.2 现可推进（依赖 B6.1 已 done）。无剩余 plan-owned work。

### 2026-06-26 (surface teardown GC gate / markdown content+security / tabs controlled matrix / remote-schema i18n boundary — plan B5.1)

- 执行 plan `docs/plans/2026-06-26-1030-1-b51-surface-gc-markdown-xss-remote-schema-i18n-plan.md`（roadmap B5.1）：收口 5 条 signal——L1（surface teardown GC 回归门）、L7（tabs 受控值语义 LOCK）、DD9（markdown content-only 裁定）、DD10（markdown sanitize 双策略）、I1（远程 schema 本地化边界裁定）。纯「lock + 裁定 + 回归门 + owner doc 显式化」，无新 runtime feature，无生产源码改动（仅测试 + doc）。
- **Phase 1（DD9/DD10）**：DD9 裁 content-only——`markdown.tsx:11-56` + `schemas.ts:124-132` 确无 `src`/fetch/effect，循环属性按构造（vacuously）成立；`markdown/design.md` §9 新增「renderer 仅渲染 content，不持有 src/fetch；远程内容经 `content:"${...}"` loader 注入」+ §5 澄清 source-enabled 措辞。DD10 双策略——§12 新增 (a) allowHtml→DOMPurify allowlist+rehypeRaw（已实现+已测引 `sanitize.ts:28-42`）+ (b) 代码块逐字保留（按构造满足）；新增 `markdown.test.tsx` fenced-code-block 逐字保留锚（`"he said 'hi'` + `<tag>` 字面保留 + `not.toContain('&#39;'/'&quot;'/'&amp;amp;')` entity 双转义）。
- **Phase 2（L1）**：新增 `packages/flux-runtime/src/__tests__/surface-teardown-gc.test.ts`——50× open→close 循环断言 entries 归零 + disposeScope 计数=50（可证伪代理：entry 计数+scope dispose 调用计数，不依赖 jsdom GC）；root `runtime.dispose()` 断言 ownedSurfaceRuntimes 驱动 stacked entries 全清；`close(id)` 路径。复核 `surface-owner.md:277,411-417` 与 live `disposeEntry`（`surface-runtime.ts:102-116`）一致（未改动）。
- **Phase 3（L7）**：新增 `packages/flux-renderers-basic/src/__tests__/tabs-controlled-value-matrix.test.tsx`——local+defaultValue、numeric-index authoring（`0:0`→`1:1`）vs string-key（`a:0`→`b:1`）等价（同 activeIndex 位置）、controlled+mutating-expr 单一真源（含 controlled 点击不持久化→点 First 仍 `second:1`）。`tabs/design.md` §8.1 补「local 下 `value` 充当初值（`defaultValue ?? value ?? fallback`，`interaction-owner.ts:32-34`）；受控无歧义仅 `valueOwnership:'controlled'`」。scope writeback 既有 `basic-page-and-tabs-status.test.tsx:127-162` 覆盖不重复。
- **Phase 4（I1）**：裁定 schema-string i18n 边界——message-key 半边（renderer `t('flux.*')`/`useFluxTranslation`，reactive 于 locale）= Flux runtime 契约已锁；schema-string 翻译半边（schemaApi/dynamic schema 字符串翻译）= Loader/组装层职责 DESIGN-ACK（引 `frontend-programming-model.md` 平台分层表）。证据：`getMessageFormatter` 唯一生产消费者 `validation/message.ts:9`、flux-formula 无 `t` builtin、dynamic-renderer 无 i18n pass。`frontend-programming-model.md` 新增「i18n 边界裁定（locale 解析覆盖）」段。新增 `i18n.test.ts` I1 双向 locale 锚（保存→Save→保存，无 remount）。
- 生产源码改动：无（纯测试 + doc）。DD9 markdown `src` / I1 schema-string 翻译 pass 均 out-of-scope distinct feature，successor roadmap B7（见 Deferred But Adjudicated）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(55/55) + `pnpm lint`(29/29，0 errors) + `pnpm test`(workspace 55/55；content 162 passed | +2 锚、runtime 1251 passed | 1 skipped +3 新测试、basic 全绿 +4 新测试、i18n 26 passed +1 新测试)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0fddd6ebeffesgzRUitvSnwDiO` 执行（执行 session 不自审），VERDICT `pass`（零 Blocker/Major/Minor）：独立核对 markdown 确无 src、disposeEntry 链释放 validation owner+scope、interaction-owner.ts:32-34 初值语义、getMessageFormatter 唯一消费 validation/message.ts:2,9、flux-formula 无 t builtin；spot-run 4 新测试文件全绿；DD9/I1 裁定诚实（legitimately-absent distinct feature 非 live bug）；doc↔code 一致（行号准确）；`git diff --stat` 仅 docs+测试（108 insertions）匹配范围。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B5.1 `planned`→`done`（Wave B5 列表 + 表格 Status + Last Updated）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：markdown 远程 `src` fetch、runtime schema-string i18n 翻译 pass（均 distinct feature / Loader 层职责 → successor roadmap B7）。无剩余 plan-owned work。

### 2026-06-26 (date/tree/file lifecycle & boundary contract — plan B4.2)

- 执行 plan `docs/plans/2026-06-26-0830-3-b42-date-tree-file-lifecycle-boundary-plan.md`（roadmap B4.2）：逐条裁定 date（D1/D2/D4/D5/D6/D7/D9/D12）、tree（TR1-TR6）、file（U1-U6）共 20 条 in-scope signal。三类工作：2 确认 live 缺陷 Fix（D6 range required 单端通过 P1、D9 input-datetime time-typing 绕过 min/max P1）、3 feature 缺口裁定（U1 lifecycle doc-only / U5 deleteAction 裁定 B / U6 maxSize 裁定 B）、15 TEST-GAP/DOC-GAP 锁 + owner doc 显式化。failing-test-first（Phase 1 D6/D9 先红→Phase 2 转 green）。
- **Phase 1（裁定 + failing/locking test）**：D6 裁 Fix——range 值单 delimited 串 `'2024-06-01,'` 经 `isEmptyValue` 非空→required 通过；裁 option-a validator 注册（不采用 option-b 归一化，避免丢合法半选）。D9 裁 Fix——`handleTimeChange` 仅 clamp digit 0..23/0..59 后 commit，无 `isWithinRange`→typing 时间使 datetime 越界不受检（calendar 路径经 disabled matcher 已约束）。U1 裁 doc-only（pending 兼任 uploading by design + form onChange 仅 success 刻意）。U5/U6 裁定 B（genuinely-absent features，successor B7）。新增 D6 failing test（partial→required 失败 / 两端→通过 / 全空→失败）+ D9 failing test（min/max clamp below/above/within）先红；D1（valueFormat commit vs displayFormat display 分离）/D2（utc 仅 commit bridge，display/picker 本地）/D4（datetime-range 设一端时间另一端存活）/TR1（空 children[]→叶）/TR4（lazy echo 经 re-render 重算 deferred 子节点→checked）+U2（error DOM 显 server msg）/U3（successive append）/U4（init existing merge）Proof 全 direct green。
- **Phase 2（Fix D6/D9）**：D6——`flux-core/src/types/validation.ts` 增 `{kind:'requiredRange';delimiter?;message?}` 规则类型；`flux-runtime/src/validation/validators.ts` 增 `requiredRange` validator（仅 partial delimited 串触发，全空留 `required`、全填不触发）；`flux-runtime/src/validation/message.ts` 复用 `validation.required`；`flux-renderers-form/src/renderers/input.tsx` 增 `createRangeFieldValidation()` 工厂；`date-renderer-definitions.ts` date-range 改用它。D9——`date-field-control.tsx` `handleTimeChange` 经新 `clampToRange`(isWithinRange) 将最终 datetime clamp 到 `[minDate,maxDate]`（calendar 路径不变；input-time 既有 clamp 不回归）。Phase 1 D6/D9 failing test 全转 green + 单值 date required 回归 green + requiredRange 5 unit 用例。
- **Phase 3（TEST-GAP 锁 + doc 显式化）**：D7（range 选立即 commit→display==committed 无 leak）/D12（time sub-field 顺序 digit `1`→`14` 无 doubling）/D5（auto-swap 确认锚）/TR2（cascade down+indeterminate up 确认锚）；TR3（反复 toggle 5× 无重复）/TR5（Object.freeze 不抛不改）/TR6（valueField:'code' remap）于 `tree-options.test.ts`。owner doc 显式化：`date-range/design.md` §7 immediate-commit(D7) + §7.1 D4 bound 独立 + D6 required 两端(option-a) + D5 auto-swap；`input-date/design.md` §4.1 D1/D2/D9/D12 语义；`input-tree/design.md` option model 不变量 TR1/TR4/TR5/TR6；`input-file/design.md` §8.1 U1 lifecycle 契约 + §8.2 U5 裁定B + §8.3 U6 裁定B + §8.4 U2/U3/U4 行为契约。
- **Phase 4（owner doc 收口）**：抽查四 owner doc 与 live code 一致（`date-field-control.tsx:142-169` clamp / `date-range-renderer.tsx:143-177` commitRange+setTimeOn / `validators.ts:126-143` requiredRange / `tree-options.ts` leaf+cascade / `upload-field.tsx` lifecycle），无 "Proposed vs Current" 叙事。
- 生产源码改动：`packages/flux-core/src/types/validation.ts`（requiredRange 类型）、`packages/flux-runtime/src/validation/{validators,message}.ts`（requiredRange validator+msg）、`packages/flux-renderers-form/src/renderers/input.tsx`（createRangeFieldValidation）、`date-renderer-definitions.ts`（date-range 用 range validation）、`date/date-field-control.tsx`（D9 clamp）。D1/D2/D4/D5/D7/D12/TR*/U*/U1/U2/U3/U4/U5/U6 仅测试 + doc。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，0 errors，1 预存 warning `select-combobox-lists.tsx:87` useVirtualizer 非本改动，git stash 确认 HEAD 即存在) + `pnpm test`(workspace 55/55)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0fe3bd036ffefa5anVHLoRx7gB` 执行（执行 session 不自审），VERDICT `approved`（零 Blocker/Major/Minor）：独立核对 D6 Fix real（requiredRange 仅 partial 触发、单值 date 不受影响）、D9 Fix real（handleTimeChange clamp、calendar 不变）、failing-test-first 断言行为而非「无错」、U5/U6 grep 确认 `deleteAction`/`maxSize`/`onReject`/`onDelete*` genuinely ABSENT（裁定 B 诚实非漂移）、doc↔code 一致（行号准确）、D6/D9 仅有的确认 live 缺陷均已 landed 无静默 deferred、无 stray 构建产物入 `src/`。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B4.2 `planned`→`done`（Wave B4 列表 + 表格 Status）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：U5 deleteAction action-ref、U6 maxSize+onReject（裁定 B non-goal → successor roadmap B7 或独立 feature plan）；D3/D8/D10/D11、TR7 enableNodePath、U7/U8、date unix token grammar 归 B7 / candidate future（`Non-Blocking Follow-ups` 已记）。无剩余 plan-owned work。

- 执行 plan `docs/plans/2026-06-26-0830-2-b41-select-input-controlled-value-echo-event-plan.md`（roadmap B4.1）：逐条裁定 `03-select.md` 7 条（S1/S3/S4/S5/S6/S8/S12）+ `05-input-fields.md` 7 条（I1/I2/I3/I4/I5/I6/I8）共 14 条 in-scope signal。三类工作：1 确认 live 缺陷 Fix（S3 echo-fallback P0）、1 feature 缺口裁定（S4 remote 搜索裁定 B out-of-scope）、12 TEST-GAP/DOC-GAP 锁 + owner doc 显式化。failing-test-first（Phase 1 S3 先红→Phase 2 转 green）。
- **Phase 1（裁定 + failing/locking test）**：S3 裁 Fix——value 设置但 options 无匹配时 trigger 空白（desktop single `comboboxValue` 得 null 显 placeholder / multi chips 静默丢无对应 option 的 primitive / mobile `?? ''`）。S4 裁定 B——select renderer 不内置 `searchSource`（远程搜索显式归组合层 `filterOption:false` + data-source），redirect tree-select/picker 或 successor，value 真值源 + option 刷新不丢值（仅影响 echo=S3）。新增 S3 failing test（`select-controlled-value-echo.test.tsx`：desktop single non-searchable/searchable、multi chips、mobile single/multi、numeric、backfill 共 8 条，先红）+ S1 LOCK（`,`/`@`/unicode whole primitive + 逗号 value 选中写单 primitive）+ I2（`input-reset-resync.test.tsx`：component:reset 还原 DOM `input.value` + runtime，form-level reset 显示跟随）。owner doc 裁定记录：`select/design.md` §2 决策表（echo-fallback 行 + S4 裁定理据）+ §4 `noMatchText?` + §7 echo-fallback 契约 + §12 边界。
- **Phase 2（Fix S3）**：`packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx` 引入 echo-fallback——`noMatchText` 解析；`comboboxValue` single 无匹配时降级为 synthetic `{label: noMatchText ?? String(value), value}`（非 null），multi 追加无匹配 primitive 为 synthetic chip（不丢显示）；`mobileTriggerText` single 降级 `noMatchText ?? String(value)`、multi 含无匹配 primitive 的 `String(...)`；`mobileHasSelection` single 改判 value 在场（非 option-match）。form/scope 真值源不变（echo 仅影响显示，`handleValueChange` 仍写 raw primitive）。`schemas.ts` SelectSchema 增 `noMatchText?: string`。Phase 1 S3 failing test 全转 green + 补 disabled/readOnly 降级不空白 + noMatchText 覆盖 + multi-retain（option 回填恢复 label 不丢值）+ chip remove（移除 echoed primitive）共 13 条 green。
- **Phase 3（TEST-GAP 锁 + doc 显式化）**：S5 `api-cache-key-dependencies.test.ts`（dep 变/keyword 同→key 变、keyword 变→key 变、全不变→key 稳定、resolveCacheKey cacheTTL、caching disabled→null）；S6 `select-virtual-filter.test.tsx`（深 offset 渲染 + stale 越界 index null-guard 不崩）；S8 `programmatic-setvalue-reaction.test.tsx`（setValues/component:setValue 驱动依赖 `disabled`/`visible` 表达式重算）；S12 `select-option-template-click.test.tsx`（plain nested click-anywhere 选中 + virtual+multiple toggle + **stopPropagation 边界实测**：commit 在冒泡 click，nested `<a>` stopPropagation 会吞 commit→不选中，契约文档化）；I1（null→'' + edit round-trip）、I3（clear→submit `count===undefined`）、I4（component:setValue 后 type/backspace/paste 可编辑）、I6（native/clear/reset/setValue 四路径同 canonical observable）、I8（bare `$catId` 字面绑定 vs `${x}` 求值）。九条 Proof 全 direct green（S12 stopPropagation 为诚实边界裁定，非缺陷）。owner doc 显式化：`select/design.md` §12 增 S1 LOCK/S5 cache-key/S8 程序式 set/S12 click-anywhere+nested-anchor 边界；`input-text/design.md` §12 增 I2/I5 同步写无 flush 契约 + I6 统一 onChange + I8 字面 vs `${}`；`input-number/design.md` §13 增 I2/I5 同步写 + numberAdapter.out 空→undefined。
- **Phase 4（owner doc 收口）**：抽查三 owner doc 与 live code 一致（`input-choice-renderers.tsx:255-299` echo-fallback、`value-adapter.ts:185` stringAdapter null→''、`form-runtime.ts:501-518` reset lastChange、`formula-compiler.ts:107` hasExpression `${`），无 "Proposed vs Current" 叙事。
- 生产源码改动：`packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx`（S3 echo-fallback single/multi/desktop/mobile + `noMatchText`）、`packages/flux-renderers-form/src/schemas.ts`（SelectSchema `noMatchText?`）。S1/S4/S5/S6/S8/S12/I1/I2/I3/I4/I5/I6/I8 仅测试 + doc。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(55/55) + `pnpm lint`(29/29，0 errors，1 预存 warning `select-combobox-lists.tsx:87` 非本改动) + `pnpm test`(workspace 55/55；flux-renderers-form 528 passed/58 files +29 新测试；flux-runtime 1241 passed | 1 skipped +5 新测试)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0fe725b22ffeFp1ZIs5hmcWXku` 执行（执行 session 不自审），VERDICT `pass`（零 Blocker）：独立核对 S3 Fix real（synthetic fallback single/multi/desktop/mobile、value 不回写、`noMatchText` 覆盖）、测试断言行为而非「无错」（S3 raw 可见/not placeholder、S5 不同 dep→不同 key、I3 `count===undefined`、I8 bare `$catId` 字面 vs `${x}` 求值、I2 DOM 重同步）、S4 裁定落地（doc §2 + Deferred successor→B7）、无 in-scope 缺陷静默降级、doc↔code 一致（行号准确）、无 stray 构建产物入 `src/`。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B4.1 `planned`→`done`（Wave B4 列表 + 表格 Status）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：S4 select renderer 内置 `searchSource`（feature successor B7 或独立 feature plan，镜像 input-tree）；S2/S7/S9/S10/S11/S13 + I7/I9/I10/I12/I13 归 B7 backlog（P2/P3）；I11 min/max clamp BY-DESIGN P3 已降级。无剩余 plan-owned work。

### 2026-06-26 (table tree lazy-children / nested crud isolation / hover-focus render / column resize-summary-fixed-selection — plan B3.3)

- 执行 plan `docs/plans/2026-06-26-0830-1-b33-table-advanced-tree-aggregate-perf-plan.md`（roadmap B3.3）：逐条裁定 `02-table-and-crud.md` 8 条 in-scope signal（T11/T15/T18/T23/T24/T29 + 自 B3.1 deferred 接管的 T9/T10）并落地。T11（tree lazy-load 每节点）= feature 缺口裁定 B（DESIGN-ACK-NOT-IMPL + doc）；T24（嵌套 crud 隔离）/T29（hover/focus 不重渲染兄弟行）/T15（列 resize 多次/溢出）/T18（summary 切列重对齐）/T9（fixed-left + 选择列）/T10（setSelection 跨页）/T23（source 刷新）= TEST-GAP/DOC-GAP 锁 + owner doc 显式化（实现已正确，全 direct green）。三类工作交织：1 feature 缺口裁定、6 TEST-GAP/DOC-GAP、1 已覆盖确认锚。
- **Phase 1（裁定 + Proof）**：T11 裁定 B——table 树模式按设计即预加载递归 flatten（`use-table-tree.ts:62` `flattenTreeRows` 同步读 `record[rowChildrenField]`、`handleToggleTreeExpand:126-136` 仅翻本地 Set，无 `childrenSource`/`deferApi`/`hasChildrenField`，grep 零命中），lazy-children 是 feature 非「已声称属性」，DESIGN-ACK-NOT-IMPL + candidate future（镜像 input-tree `childrenSource`，用户交互驱动 pattern #3，successor B7）。T24 id-collision guard 裁定 doc-only non-goal（隔离 by construction `crud-renderer-ownership.ts:54-61`，运行时 guard 跨 ownership 边界 + HMR/strict-mode 误报风险，改 authoring 契约「CRUD id 必须唯一」）。新增 7 条 Proof（全 direct green）：`table-b33-advanced-boundary.test.tsx`（T15 re-resize 从当前 width 累积 + delta-based clamp / T18 summary 运行时 toggle B hidden 重对齐 A-C / T9 纯选择列 `left:0` + 首列 `left:40px` / T10 setSelection 跨页 keepOnPageChange / T29 `<Profiler>` hover+focus 零 commit）；`crud-b33-nested-isolation.test.tsx`（createCrudOwnerPaths unit + sibling crud per-instance `$crud` 选择/翻页隔离）；`data-table-b33-source-refresh-anchor.test.tsx`（T23 source+refreshSource 行刷新锚）。
- **Phase 2（条件 Fix）**：跳过——Phase 1 全 direct green，无失败项升级；T24 guard 裁定 doc-only non-goal（无代码 Fix）。
- **Phase 3（TEST-GAP 锁 + doc 显式化）**：`table-e1c-tree-table.test.tsx` 增「B3.3 T11 preload-only tree」锚（展开有预加载 children 的节点→子节点出现；展开无 children 字段节点→仍 1 行，证明无 lazy fetch；handleToggleTreeExpand 同步 void + api 无 load/fetch callback，锁定裁定 B）。owner doc 显式化：`table/design.md` §7 新增「B3.3 裁定」段（T11/T18/T9/T29/T10）+ §12 T11 lazy-children DESIGN-ACK-NOT-IMPL note；`crud/design.md` §7 新增「B3.3 裁定」段（T24 containment + id-uniqueness 契约 + guard doc-only non-goal）。
- 生产源码改动：无（纯测试 + doc）。8 条 signal 全部为 TEST-GAP/DOC-GAP/feature-裁定，无 in-scope live defect。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(0 errors，1 预存 warning 非本改动) + `pnpm test`(workspace 55/55；flux-renderers-data 584 passed | 72 files，+15 新测试)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0fea8cd58ffezpFbwJiINFXmWK` 执行（执行 session 不自审），VERDICT `approved`（零 Blocker / 零 Major）：独立 re-run 4 个新/改测试文件 → 27 passed、数据包全量 584 passed、typecheck+lint clean；interface-vs-semantics 抽查 T11（展开无 children 节点不产生新行=真无 lazy fetch）+ T24（createCrudOwnerPaths id 命名空间隔离 + 集成 A 不泄漏 B）；owner doc file:line 引用核对准确；deferred 诚实（T11 out-of-scope improvement + successor B7；T29 selection-cascade 在 Non-Blocking Follow-ups + owner doc）；5 处文本一致性通过。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B3.3 `planned`→`done`（Phase Status 块 + 表格 + Last Updated）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：T11 per-node lazy/on-expand children fetch（feature successor B7 或独立 feature plan）；T29 selection-cascade 性能项（`handleSelectRow` `useCallback` identity 依赖 `selectedRowKeys` → 选一行破坏所有行 `React.memo`，归 B7 perf backlog）；T17 autoFillHeight×fixed-column（candidate future，autoFillHeight 本身暂不实现）；T2/T4/T7（P2 → B7 backlog）。无剩余 plan-owned work。

### 2026-06-26 (array/combo nested isolation / per-row delete when / hidden submit / row-local addressing — plan B3.2)

- 执行 plan `docs/plans/2026-06-26-0520-2-b32-array-combo-nested-isolation-validation-addressing-plan.md`（roadmap B3.2）：逐条裁定 `04-combo-and-array-field.md` 9 条 signal（C1/C2/C3/C5/C7/C9/C10/C12/C13）并吸收 predecessor B1.2 显式 deferred 的 V6（array 行本地相对跨字段寻址）。failing-test-first（Phase 1 C3 先红→Phase 2 转 green）。
- **Phase 1（裁定 + failing/locking test）**：C10 裁定 B——hidden 字段默认保留值并包含进提交（`form-runtime-submit-flow.ts:360` 直读 `store.getState().values` 无投影），`clearValueWhenHidden` opt-in 清值排除，submit-payload-projection 为 successor B7。C3 裁定 A（Fix）——per-row delete `removeWhen`。V6 裁定 B（DESIGN-ACK-NOT-IMPL）——`getChildFieldPathPrefix` 返回 false 有意，行本地相对引用 successor B7，关闭 B1.2 deferred。新增 C10 locking test（`form-runtime-submit-flow.test.ts` 默认 payload 含 hidden 值，green）+ C3 failing test（`b32-combo-remove-when.test.tsx`，先红）+ V6 locking test（`b32-v6-row-relative.test.tsx`：getChildFieldPathPrefix=false + path-binding 静态 owner-only + 行内相对规则不按 row-local 作用，green）。owner doc 三处裁定记录：`form-validation.md`（V6 + C10 adjudication 段）+ `array-field.md`（C3/C10/V6 段）+ `combo/design.md` §4/§8。
- **Phase 2（Fix C3）**：新增 `composite-field/remove-when-gating.ts`（`isRemoveBlockedByWhen` 纯求值，fail-open + console 报错）。combo-renderer.tsx + array-field.tsx 两路径同步：schema `removeWhen`（`kind:'ignored'` 保留 raw，避免被 parent scope 解析报错），parent per-item 经 `createItemScope` 投影求值生成 `removeBlockedByIndex`，传 `removeBlocked` boolean 给 item（按钮 disabled 态并入 `readOnly || atMinItems || removeBlocked`），`handleRemove` + combo `removeItem` handle 均经 `isRemoveBlockedAt` 门控。`composite-schemas.ts` 增 `removeWhen?: string`（ComboSchema + ArrayFieldSchema）。Phase 1 C3 failing test 全转 green + 负向（无 removeWhen/minItems 地板/array-field 路径）green。
- **Phase 3（TEST-GAP 锁 + doc 显式化）**：C1 `b32-array-combo-nested-isolation.test.tsx`（2 级 combo-in-combo，编辑 row0 子项不串扰 row1）；C2（item 内 `visible` 读同行 sibling field 经投影 owner scope 级联）；C5 `form-runtime-external-errors-addressing.test.ts`（行级 `items.1`/叶级 `items.0.sku`/穿容器 `items.2.tabs.t.sku` 寻址 + undefined-target `items.999.unknown` 不崩 + remove 后 index remap）；C7 `b32-array-submit-and-validate.test.tsx`（array-editor add 空 required 行无错→touch+clear 错→submit 全行）；C9（item select options 来自 per-item data-source，提交值仅含声明字段，无 transient option payload 泄漏）；C12（30 items array-field + render-count probe，编辑 row15 仅该行重渲染，兄弟 row0/14/16/29 count 不增）；C13（per-item setFlag action 经投影 form 回写 `rows.0.flag`，兄弟行不禁用）。七条 Proof 全 direct green，无 Fix 升级。
- **Phase 4（owner doc 收口）**：`array-field.md` 新增 8 段行为契约（C3 removeWhen / C10 hidden submit / V6 row-local / C1 nested write isolation / C5 external error addressing / C7 add-vs-validate / C9 submit declared-only / C12 per-item re-render / C13 per-item action writeback）；抽查三 owner doc 与 live code 一致（`array-field-runtime.ts` prefix-projection、`form-runtime-submit-flow.ts:360` 直读 values、`array-field.tsx` getChildFieldPathPrefix false、C3 removeWhen 实现），无 "Proposed vs Current" 叙事。
- 生产源码改动：`packages/flux-renderers-form-advanced/src/composite-field/remove-when-gating.ts`（新）、`combo-renderer.tsx`（removeWhen 门控）、`composite-field/array-field.tsx`（removeWhen 门控）、`composite-field/composite-schemas.ts`（schema 类型）。C1/C2/C5/C7/C9/C12/C13/V6/C10 仅测试 + doc，无其他生产代码改动。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(0 errors，1 预存 warning 非本改动) + `pnpm test`(workspace 55/55；flux-renderers-form-advanced 855 passed/95 files；flux-runtime 1236 passed)。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B3.2 `planned`→`done`（Phase Status 块 + 表格 + Last Updated；并修正 B3.2 描述里「hidden 默认排除提交」为裁定 B 的「默认保留包含 + clearValueWhenHidden opt-in」）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：V6 行本地相对寻址 + C10 submit-payload-projection（均 successor B7，out-of-scope improvement）；C4/C6/C8/C11/C14/C15 归 B7/backlog；successor B3.3（table 高级能力）依赖 B3.1+B3.2，可推进。无剩余 plan-owned work。

### 2026-06-26 (table row identity / data-shrink page clamp / dotted sort-selection — plan B3.1)

- 执行 plan `docs/plans/2026-06-26-0520-1-b31-table-row-identity-pagination-clamp-sort-selection-plan.md`（roadmap B3.1）：逐条裁定 `02-table-and-crud.md` 7 条 in-scope signal（T1/T3/T5/T6/T8/T27/F1）并落地。T5（数据收缩后页码不钳制）= 确认 P0 live 缺陷→Fix；T6（dotted 列名 path-binder）裁定 A→Fix；T1/T3/T8/T27/F1 = TEST-GAP 锁定 + owner doc 显式化（实现已正确，预期均 green）。failing-test-first（Phase 1 T5+T6 先红→Phase 2 转 green）。
- **Phase 1（裁定 + failing test）**：T5 裁 Fix——table/CRUD 引入 render-time `currentPage` 钳制到 `[1, totalPages]`（镜像 `list-pagination.ts:157`）。T6 裁定 A——sort/cell/filter/combine 升级 `getIn` path-binder（blast-radius 审计：in-repo/playground 无 table 列依赖字面含点键，命中的 `record.status`/`record.name` 均为 quick-edit dialog form 字段名，故零回归）。T1 裁——`__rowKey` hydrate 为 Flux-idiomatic、expression rowKey 记 candidate future、dotted 单路径 + `__rowKey`/`id` 回退为 supported baseline。新增先红测试 `data-table-pagination-clamp.test.tsx`（T5：client shrink→非空末页 / in-range 保持 / 空数据不卡 / CRUD→table 路径）+ `table-dotted-column-paths.test.tsx`（T6：dotted sort/filter/display + flat 回归）。owner doc 三处裁定记录：`table/design.md` §7 B3.1（T5/T6/T8/T3）+ `crud/design.md` §7 B3.1（T5/T27/F1/T3）+ `table-row-identity-and-scope-performance.md`（T1 复合 rowKey）。
- **Phase 2（Fix T5 + T6）**：T5——`packages/flux-renderers-data/src/table-renderer.tsx` 于 `treeFlattenedData` 后算 `totalPages=max(1,ceil(len/pageSize))` 与纯渲染期 `resolvedCurrentPage=min(max(1,currentPage),totalPages)`（不回写 scope/local），用于 `paginateTableData`/`TablePaginationBar`/`useTableHandle`；filter-path「强制 page 1」收敛为 `clampPage(currentPage,…)`（单一 clamp 语义）。T6——`table-data.ts`（sort `getIn(a.record,entry.column)` + filter `getIn(row.record,columnName)`）、`table-cell-chrome.tsx`（cell/copyable/popOver rowValue 经 `getIn`）、`combine-cells.ts`（`getCellValue` 经 `getIn`）；flat 名行为不变。Phase 1 failing tests 全转 green。
- **Phase 3（TEST-GAP 锁 + doc 显式化）**：T1 `table-row-key-resolution.test.ts`（normalizeRowKey dotted + `__rowKey`/`id`/legacy 回退 + dedup cache key）；T3 `use-table-controls.pagination.test.tsx` 增（翻页写回携带当前 pageSize，不踩默认 10）；T8 `table-click-dispatch-priority.test.tsx`（checkbox/copyable/popOver 不触发 onRowClick、纯 cell 触发）；T27+F1 `crud-renderer-state.unit.test.tsx` 增（`{items:null}`→`[]`、`{total:null}`→rows.length、空行 totalPages=1 无循环；filter 值 `0`/`"0"` 保留为有效 filter、`null`/`''` 丢弃）。五条 Proof 全 direct green，无 Fix 升级。
- **Phase 4（owner doc 收口）**：抽查三 owner doc 与 live code 一致（`table-renderer.tsx:260-270` 钳制、`table-data.ts` sort/getIn、`normalizeRowKey` dotted 回退、`normalizeCrudSourceValue` nullish），无 "Proposed vs Current" 叙事。
- 生产源码改动：`packages/flux-renderers-data/src/table-renderer.tsx`（T5 clamp + filter 收敛）、`table-renderer/table-data.ts`（T6 sort+filter getIn）、`table-renderer/table-cell-chrome.tsx`（T6 cell+copyable+popOver getIn）、`table-renderer/combine-cells.ts`（T6 getCellValue getIn）。T1/T3/T8/T27/F1 仅测试 + doc，无生产代码改动。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，含 active-doc-anchor 校验) + `pnpm test`(workspace 55/55；flux-renderers-data 569 passed | 69 files)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0ff2cd4a1ffe4w2uQwKv9XWQqM` 执行（执行 session 不自审），VERDICT `pass`（零 Blocker / 零 Major）：独立 re-run flux-renderers-data 69 files/569 passed、typecheck/build clean、无 src 产物泄漏；T5 clamp 为纯渲染派生（无 scope 回写，镜像 list）；T6 getIn 升级在声明 scope 内完整（剩余 `record[field]` 命中为 B3.3 tree 聚合 / quick-edit form 字段绑定，正交 out-of-scope）；T1/T3/T8/T27/F1 锚断言正确；Deferred 仅 T2（显式 out-of-scope → B7）。2 minor（plan Failure Path T5-server 措辞 + T5-server-shrink 测试实为 client-side CRUD）已处理：Failure Path 行按 owner doc 收口表述。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B3.1 `planned`→`done`（Phase Status 块 + 表格 + Last Updated；roadmap 自身规则：closure audit 通过方可 `planned`→`done`）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：T2 字面含点/符号字段名经 bracket-key 路径解析（P2 → B7 backlog）；T4/T7/T9/T10（P2）归 B7/backlog 或 B3.3；successor B3.3（table 高级能力：tree/aggregate/perf，T11/T15/T18/T23/T24/T29）依赖本工作项，可推进。无剩余 plan-owned work。

### 2026-06-26 (scope propagation / isolation / reaction triggering — plan B2.2)

- 执行 plan `docs/plans/2026-06-26-0406-2-b22-scope-propagation-isolation-reaction-plan.md`（roadmap B2.2）：收口 `11-api-data-and-scope.md` 的 scope 传播/隔离/reaction 主题（A5/A9/A14-A16/A19）+ 吸收 predecessor B2.1 deferred 的 A4/A7/A8（request 层边界）。failing-test-first（Phase 1 A8 先红→Phase 2 转 green）。
- **Phase 1（裁定 + failing test）**：owner doc `docs/architecture/api-data-source.md` 新增 7 段裁定（Array Parameter Serialization A8 / Template Interpolation Boundary A7 / Request Adaptor Shallow-Merge No-Op Contract A5 / Adaptor Abort Boundary A4 / Polling Loading Semantics A9 / Per-Runtime-Instance Namespace A15 / Route-Location Parameter Binding A16）。新增 A8 先红测试（`request-runtime.test.ts` 全管道 `prepareApiRequestForExecution` 数组参数断言单一 repeated-key 形式 + 负向）。
- **Phase 2（Fix A8 + A9/A7 裁定 B）**：A8——`packages/flux-runtime/src/async-data/request-runtime.ts` 抽出共享 `appendParamValues(searchParams,key,value)` helper（array→repeated same-name key / object→JSON / primitive→string），`buildUrlWithParams` 与 `canonicalizeUrlWithParams` 复用之；`canonicalizeUrlWithParams` 额外 `delete(`${key}[]`)` 兜底清除遗留 bracket 项。消除原双形式输出（`buildUrlWithParams` 出 `ids[]=`、`canonicalizeUrlWithParams` 不删 `key[]` 又追加逗号串 `ids=1,2,3`，全管道并存两种）。统一为 `ids=1&ids=2`（repeated-key，RESTful，故意与 amis `bar[0]` 分歧）。A9 裁定 B（文档化当前行为，非缺陷）——理由：`deriveDataSourceState`（`data-source-state.ts:67-75`，经 `api-data-source-controller-state.ts:65` 每次 update 应用）耦合 `loading=inFlightCount>0||fetchStatus==='fetching'` 与 `isRefreshing=loading&&hasData`，"isRefreshing true ∧ loading false" 不重写共享推导不可达， disproportionate to UX-polish signal；`isInitialLoading`/`isRefreshing` 细分已可避免 poll 闪烁。A7 裁定 B（已知限制，非缺陷）——`${` 必为插值起点、无转义语法、未闭合当文本（`template.ts:74`）；P2 特征缺失，语言级 implications。补 `finalizeApiRequest` 全管道数组测试 + 更新既有 bracket 断言；`template.test.ts`/`request-runtime.test.ts` 锁定 A7。
- **Phase 3（TEST-GAP 锁 + doc）**：A5——`action-scope-and-adaptor.test.ts` undefined/null no-op + partial `{headers}` 合并保留 url/data/params。A14——`scope-ownership-lexical-and-nested.test.ts` 祖父写→孙级 composite 订阅者收到通知（钉 `scope.ts:278-287` 链式传递，#3562 类）。A15——`runtime-sources-refresh.test.ts` 两个完整 `createRendererRuntime` 实例 + 同名源 → refreshSource 只命中各自 instance。A19——`runtime-sources-lifecycle.test.ts` refresh 路径 sendOn falsy→`{skipped:true}` 不发请求 + sendOn 读祖先（cross-owner）值。`scope-ownership-and-isolation.md` Default Inheritance 显式「无限深度」。
- **Phase 4（owner doc 收口）**：抽查两 owner doc 与 live code 一致——`request-runtime.ts` 序列化、`scope.ts` 递归（`resolveScopePath:362`/`createCompositeScopeStore:278-287`）、`runtime-factory.ts` instance registry（`runtimeId:99` + 闭包局部 `sourceRegistryRef/reactionRegistryRef:121-122` + `:641,646` 创建 + component-handle registry `:182-185`）。
- 生产源码改动仅 `packages/flux-runtime/src/async-data/request-runtime.ts`（A8 Fix：共享 helper + 两 builder 统一 + canonicalize 删 bracket）。A9/A7/A4/A16 为 doc-only，无生产代码改动。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(workspace 55/55；flux-runtime 1229 passed | 1 skipped；flux-formula 187 passed)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0ff5e885dffeldxtNvyVFSbJPc` 执行（执行 session 不自审），VERDICT `approved`（零 Blocker / 零 Major）：A8 经隔离脚本端到端 trace 确认 final url 仅 `ids=1&ids=2&ids=3`；A9 Decision B 耦合声明核对 `data-source-state.ts:67-75`+`controller-state.ts:65` 成立；A5/A14/A15/A19 锚测试均验证正确结果；无 in-scope live defect 被静默降级。已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B2.2 `planned`→`done`（Phase Status 块 + 表格 + Last Updated）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：A16 route/location→scope 绑定 watch-only residual，待 app/navigation 层落地后评估（successor required = yes，归 B7 或新工作项）。Non-Blocking Follow-ups（A6/A10/A12/A13/A17/A18）归 B7 backlog。无剩余 plan-owned work。

### 2026-06-26 (validation rule semantics / expression params / full-pipeline entry — plan B1.2)

- 执行 plan `docs/plans/2026-06-26-0406-1-b12-validation-rule-semantics-expression-params-plan.md`（roadmap B1.2，最大单一工作项）：逐条裁定 `01-form-validation.md` V3-V6/V8-V10/V13-V15/V17-V21/V23 共 16 条 signal 并落地——特征缺口裁定 + 回归锚 + V18 真实分叉收敛 + owner doc 收口。非功能新建（V10/V13/V3/V6 仅裁定缺席），V18 裁为缺陷并 landed。
- **Phase 1（V3/V10/V13/V6 特征缺口裁定 + V10 相邻属性锚）**：`docs/architecture/form-validation.md` Dependency Model 新增 V3（派生/计算字段不进校验依赖图，NOT-ADOPTED）+ 数组行本地相对寻址 V6（deferred B3.2）；Rule Template Model 新增 V10（live 无 `min`/`max`/`isInt` 数值范围 kind，length/items 规则 `typeof`/`Array.isArray` 守卫保证非矛盾）+ V13（表达式参数=doc 声称的目标态、live 未落地；`CompiledRuleTemplate.args` 等 sketch 全部标 target-state only）。新增聚焦测试 `packages/flux-runtime/src/__tests__/validation-rule-semantics-and-lifecycle.test.ts`（V10 非串/非数组/number/boolean/object/null 输入 → length/items 全安全短路、单一非矛盾结果；不弱化 supported path）。
- **Phase 2（V4/V5/V9/V15/V17/V20/V21 回归锚 + V8/V14/V19 stale 再核）**：同文件补七条 Proof——V4 动态 requiredness（gating + 指示器共享 `Object.is(getIn,path,equals)` 谓词不可分歧）；V5 数组兄弟列默认值不抑制 A 规则物化；V9 多 required + 嵌套 combo 空提交 `onValidateError` 恰 1 次；V15 pattern author message 端到端（字段错误文本==author message、不含正则源）；V17 async A→B→C 执行时快照用值 C（abort-aware mock）；V20 init/setValues/reset 空值不产可见错误；V21 `applyChangesAndRevalidate` 清 stale required + `canSubmit` true + 一次可提交。V8（`plan-68-69-remaining-behaviors.test.ts:205-243`）/V14（`validators.test.ts:301-383`）/V19（`hidden-field-policy.test.ts:136-574`）再核仍 green → stale TEST-GAP = 已覆盖、无需新增。所有 Proof 一次 green（V4 requiredUnless 谓词方向 + V17 abort-aware mock 在编写期校正，非 live defect）。
- **Phase 3（V18 失败路由裁定 = A 统一收敛 + Fix）**：live `validateFormPath` catch（`form-runtime-owner.ts:399-415`）把 async/validator throw 转字段错误，与直接 change/blur 路径（`validateCompiledField` finally 只提交 sync 错误）及依赖重校验路径（`createDependentRevalidationFailureHandler` 诊断 seam）分叉。裁定 A：因 `validateCompiledField` 已把非 Error 归一化为 `Error(...,{cause})`，到 catch 处不可靠区分 transport vs throw，故统一走诊断 seam（`reportDependentRevalidationFailure ?? defaultReportDependentRevalidationFailure` → `env.monitor.onError` + 单次 `env.notify`），**不发**字段错误；abort 仍重抛；form result 仍 `ok:false`（form-level error 进 `errors[]` 不进 `fieldErrors[path]`）以阻 submit 在未知态静默继续。failing-test-first（V18 两例先红 → 修复后 green）。同步更新 `form-validation-resilience.test.ts`（原 77-109/111-132）+ `form-runtime-owner-lifecycle.test.ts`「error fidelity」（原 276）至新契约。
- **Phase 4（owner doc 收口）**：`form-validation.md` Async Validation Semantics 新增「Async / Validator Failure Routing (V18 adjudication)」小节；Key Types Summary 新增「FormValidationResult.fieldErrors Failure-Path Contract (V23)」小节（path-keyed 失败路径下游分支契约）。抽查 length 守卫 / validateFormPath 路由 / `FormValidationResult` 形状与 live code 一致，无 "Proposed vs Current" 叙事。
- 生产源码改动仅 `packages/flux-runtime/src/form-runtime-owner.ts`（V18 Fix：catch 改走诊断 seam + import `defaultReportDependentRevalidationFailure`）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(workspace 55/55；flux-runtime 1219 passed | 1 skipped)。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B1.2 `planned`→`done`（Phase Status 块 + 表格 + Last Updated）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：V6 显式 deferred 到 successor B3.2（`docs/architecture/array-field.md`，吸收入 `04` C1-C13 跟踪集）；V3/V10/V13 若将来落地数值范围规则 / 表达式参数 args / 派生字段 revalidation 属新功能，另立 plan，不在本计划实现。

### 2026-06-26 (async request 4xx/notify/dedup/cache — plan B2.1)

- 执行 plan `docs/plans/2026-06-26-0234-2-b21-async-request-4xx-dedup-cache-plan.md`（roadmap B2.1）：收口 Flux 请求执行层两处确认 P0 live 缺口（A1 responseAdaptor non-OK 不可达 / A11 schema-fetch 无跨订阅者去重且不参与缓存）+ A2 单一 error→notify/backend msg + A3 LOCK 复核。failing-test-first（Phase 1 先红→Phase 2/3 转 green）。
- **Phase 1（裁定 + failing test）**：owner doc `docs/architecture/api-data-source.md` 新增 A1（Error response adaptor reachability）/A2（Single error→notify translation）/A11（Schema-Fetch Cross-Subscriber Dedup And Cache）三段 normative 裁定 + 更新 Required Request Execution Flow（step 6 responseAdaptor 在 non-OK 也执行；step 7 message = `data.message ?? data.msg ?? generic`）。新增三份先红测试：`request-runtime-response-adaptor-non-ok.test.ts`（A1）/`request-runtime-error-notify.test.ts`（A2）/`request-runtime-schema-fetch-dedup-cache.test.ts`（A11）。
- **Phase 2（Fix A1 + A2）**：`request-runtime.ts` `executeApiSchema` 改为在 `!response.ok` 抛错**之前**先 `applyResponseAdaptor`（含 `status`），adapted payload 经 `createApiResponseError({ ...response, data: adaptedData })` 喂入 error message；non-OK 仍抛、不恢复成功。`request-runtime-adaptor.ts` `applyResponseAdaptor` 新增 `status?` 入参并暴露到 adaptor context。A2：新增 `readResponseErrorMessage`（读 `message` 后 `msg`）；`runtime-action-helpers.ts` `executeRuntimeAjaxAction` 对 HTTP 业务失败改为返回 `{ok:false}`（不再 re-throw）→ dispatcher 单次 error→notify 恰好一次；基础设施错误（network/compile）仍 re-throw 以触发 `onActionError`/plugin `onError`（`runtime-actions-chained.test.ts` 不回归）。
- **Phase 3（Fix A11）**：新增 `packages/flux-runtime/src/async-data/request-in-flight-registry.ts`（按 executable identity 去重、registry 自有 `AbortController` 与订阅者 signal 解耦、dispose 回收）。`executeRuntimeAjaxAction` 仅在 `sharing && control.cacheTTL>0 && safe method(get/head/options)` 时启用：先查 `apiCache`（命中即返 adapted data），否则 `inFlight.acquire(identityKey, runOnce)`（第二订阅者复用 in-flight、settled 后两订阅者均收结果），settled 后写 `apiCache`。经 `action-adapter.ts`（`ActionAdapterInput.sharing`）+ `runtime-factory.ts`（创建/dispose）接入。owner-local cancel-previous/ignore-new/parallel 未改动（`runtime-sources-refresh.test.ts` 不回归）。补充负向测试（不同 identity / 非 safe method 不误去重）+ React 级 Proof（`basic-dynamic-renderer.test.tsx` 两个 co-mounted DynamicRenderer 共享一次 in-flight、均渲染）。
- **Phase 4（A3 LOCK + doc 收口）**：复核 `request-runtime.test.ts:308-333` LOCK 锚（断言 `finalUrl === '/api/users?page=1&token=secure-token'` + `request.url` + `params undefined`）仍 green、覆盖充分，无需补锚。抽查 owner doc A1/A2/A3/A11 四处与 live code 一致，无 "Proposed vs Current" drift。
- 验证（full-green）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(workspace 55/55；flux-runtime 1201 passed | 1 skipped；flux-action-core 203；flux-react 435；flux-renderers-basic 390 含新 DynamicRenderer A11 Proof)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0ffaf4f0cffeYdgSEud7FmZwzs` 执行（执行 session 不自审），VERDICT `PASS`：独立 re-run flux-runtime(1201)+renderers-basic DynamicRenderer(15) 全绿、typecheck(55/55)+lint(29/29)、逐条 re-verify A1/A2/A3/A11 live source 命中、`Deferred But Adjudicated` 空、doc-code 无 drift；已 tick closure-audit gate + 置 `Plan Status: completed` + 填 Closure Audit Evidence。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B2.1 `planned`→`done`（summary 块 + 表格 + Last Updated）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：no remaining plan-owned work。successor B3.1（table 行身份/页码钳制/排序选择）显式依赖本工作项先行；B2.2（scope 传播/隔离/reaction）同 wave 独立，另立 plan。

### 2026-06-26 (validation dependency-closure + async-cancel property locks — plan B1.1)

- 收尾 plan `docs/plans/2026-06-26-0234-1-b11-validation-dependency-closure-async-cancel-plan.md`（roadmap B1.1）：TEST-GAP 锁定 + owner-doc 显式化，非功能新建。钉住四个已建模但缺聚焦回归的 P0/P1 校验属性。
- **Phase 1（V1/V2 依赖闭包错误清除）**：新增 `packages/flux-runtime/src/__tests__/validation-dependency-closure.test.ts`。V1——真实 required 规则产生错误 → renderer 变更路径（`setValue`+`validateField('change')`）使规则通过 → 断言 `getFieldState().errors` 清空、`canSubmit` 反映、`showErrorOn:'touched'` 延迟路径下底层状态仍清除。V2——真实相互约束（A`lt B`/B`gt A`，自定义 `validateRule` 读 `scope.get` 对端，不 mock `validateField`），改一端通过依赖闭包清除对端错误；显式 cycle-safe（`validateRule` 调用计数有界）。
- **Phase 2（V16 负向锚 + V22 全管道）**：新增 `packages/flux-runtime/src/__tests__/validation-async-cancel-and-full-pipeline.test.ts`。V16——快速连续变更使旧 async run 过期，断言旧 run 不发布任何字段错误，且字段错误消息永不出现 "cancel"/"stale"（与 async-governance diagnostics 的 cancelled/stale-dropped 形成对照）。V22——required+minLength+pattern 三规则经 `validateAt('manual')`/`validateForm`(action 入口)/`submit` 三入口，非空违规值仍报 minLength+pattern（证伪 required-only 弱化），空值报 required+minLength（证 required 不抑制后续）。
- **Phase 3（owner doc 显式化）**：`docs/architecture/form-validation.md` 新增「Error Clearing On Rule Pass」子节（紧邻 Dependency Model）+ Rule Template Model 规则 5（任何入口跑同一全管道、无 required-only 弱化）。抽查与 live code 一致（`validateCompiledField` 规则循环无短路、`commitPathValidationState:114-116` 空 errors 删除、`validateForm` 合并清除已通过路径、`rg "required-only|requiredOnly|weaken"` 生产代码 0 命中），无 "Proposed vs Current" 叙事。
- **所有 Proof 在 live code 上直接 green，无 Fix、无 in-scope 残留缺陷。** 未改任何生产源码（`packages/flux-runtime/src` 排除 `__tests__`）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(workspace 55/55；flux-runtime 1192 passed | 1 skipped)。
- Closure-audit 由独立 fresh-session 子 agent `ses_0ffd3db8fffeM4RbPjDrqsgYoc` 执行（执行 session 不自审），VERDICT `pass`（零 Blocker / 零 Major；2 minor：B2.1 同树暂存未计入 diff 摘要、校验计数 stale——均不影响结论）。
- Roadmap 翻转：`docs/components/amis-bug-driven-improvement-roadmap.md` B1.1 `planned`→`done`（Phase Status 块 + 表格 + Last Updated；roadmap 自身规则：closure audit 通过方可 `planned`→`done`）。
- 源审计关闭：N/A——本 plan front matter 无 `> Source Audits:` 行（roadmap-sourced plan）。
- Follow-up：no remaining plan-owned work。successor B1.2（V3-V6/V8-V10/V13-V15/V17-V21/V23）与 B3.2（array/combo 嵌套隔离 C1-C13）显式依赖本工作项先行，另立 plan。

### 2026-06-26 (component styling/doc/hygiene cleanup — plan 450)

- 收尾 plan `docs/plans/450-component-styling-doc-and-hygiene-cleanup-plan.md`：收口 2026-06-24 components 审计 [C-06]/[C-07]/[C-08]/[C-09]/[C-12]..[C-23]/[O-02]/[O-03]/[O-04]——契约/能力/消息诚实、选择器与样式契约、设计系统一致性、文档新鲜度、诊断与测试卫生。5 Workstream 全部完成。
- **发现**：plan baseline 过时——多数 finding 已由前置 plan（0630-1/448/449）修复；本轮逐条核对 live baseline，仅改动 genuinely outstanding 项（C-08 cast 裁定 + C-13/C-14 token + C-15 marker-only + C-17 四处 slot + C-18 picker + C-19 editor + O-03/O-04 carousel）。
- **WS-A（C-06/C-07/O-02/C-22）**：全部前置已修，本轮核对——transfer 无 `nop-transfer__*` BEM（grep 0）；crud query 失败用 `flux.common.queryFailed`+`cause`（`crud-renderer.tsx:418-428`）；chart `resize` 实装 option A（`handleResize`→`measureContainerWidth`，能力面诚实）；qrcode dev `console.warn`。Proof 既有：`crud-query-and-pagination.test.tsx:274`（C-07 notify warning）、`data-chart-handles.test.tsx:38`（O-02 `hasMethod('resize')`）。
- **WS-B（C-08/C-09/C-12/C-20/C-21）**：全部前置已修。**C-08 裁定**：`as RendererDefinition[]` cast 经实测为结构必需（`ValidationContributor<S>` invariant，移除→TS2322 build 失败），非冗余；substantive（共享 `registerRendererDefinitions`）已成立，cast 保留 + 源码注释说明（防未来误删）。
- **WS-C（C-13..C-19）**：C-13 alert success/warning→`bg-success-bg`/`bg-warning-bg` token（移除 emerald/amber/red + `dark:` 变体）；C-14 timeline dots→`bg-success`/`bg-warning`/`bg-info`，**info 裁定引入新 `--info` token**（`theme-tokens` 4 块 + `styles.css @theme inline` 新增 `--color-success`/`--color-success-bg`/`--color-warning-bg`/`--color-info`）；C-15 dynamic loading 分支 marker-only（移除 `flex flex-col gap-3`）；C-16 `classNames` 前置已删；C-17 四处命名 wrap:true 渲染器内层 `field-control`→专属 slot（`array-editor-control`/`tag-list-control`/`upload-field-control`/`period-control`，FieldFrame 已提供外层）；C-18 picker raw label/input→`Label`+`Checkbox`（多）/`RadioGroup`+`RadioGroupItem`（单）；C-19 editor raw `<button>`→`<Button variant="ghost" size="sm">`（wizard/upload 前置已 `<Button>`）。Proof：alert DOM className 断言（`bg-success-bg`/`bg-warning-bg`，无 emerald/amber）。
- **WS-D（O-03/O-04 carousel）**：O-03 `activeIndex`/`autoPlay`/`loop`→ref（effect 同步以满足 `react-hooks/refs`），handle useMemo deps 现 `[api, props.id, slotProps.name, items.length]`（`items.length` 保留供 `setValue` 边界钳制）；O-04 autoplay 加 `prefers-reduced-motion` 门控 + hover/focus（mouseenter/leave/focusin/out）暂停 + `IntersectionObserver` 离屏暂停。Proof：`carousel.test.tsx` 源码级 regression guard（jsdom 下 embla api 为 null 无法行为驱动）。
- **picker test 适配**：迁 Base UI 后视觉控件 + 隐藏 input 双元素均被 `getByLabelText` 命中→改 `getByRole('radio|checkbox', {name})`（排除 `aria-hidden`，更精确，保留 a11y 覆盖）。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：受影响包全绿（content 160 含新 carousel/alert Proof / layout / basic 390 / form 499 / form-advanced 842）；closure gates `pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29，1 non-blocking warning) + `pnpm test`(workspace 55/55)。
- Closure-audit 由独立 fresh-session 子 agent `ses_100039e36ffeEFSVBDsAaxO339` 执行（执行 session 不自审），VERDICT `approved`：19 条独立 re-verify + 独立 re-run content(160)/form-advanced(842)+两包 `tsc -p` clean；确认 C-08 cast 裁定合法、C-17 deferral（其余 14 处既有 pattern，FieldFrame 提供外层 slot，非阻塞）诚实。
- Roadmap ❌→✅：N/A——本 plan findings 来自 components 审计，`docs/audits/2026-06-24-2213-{multi,open}-audit-components.md` `Audit Status` 已 `closed`（前序关闭，本轮 idempotent），无 per-finding ❌ roadmap 条目可翻转。
- Follow-up：C-17 广义残余 14 处（combo/input-table/key-value/transfer/picker/object-field/array-field/detail-field 等 wrap:true 内层 `field-control`）→ 后续 styling-consistency plan；C-24 条件性；`>500` 行收敛独立治理。本计划无剩余 plan-owned work。

### 2026-06-26 (oversized renderer-file governance split — plan 449 closure)

- 收尾 plan `docs/plans/449-oversized-renderer-file-governance-split-plan.md`：收口 2026-06-24 components 审计 [C-04]/[C-05]——两个越过「>700 行硬规则」的渲染器文件拆分到聚焦模块。**纯物理提取、行为零变化**。
- **发现**：前序 run 已落地全部提取代码并提交（git working tree clean），但未回填 plan 复选框/状态——本轮逐文件核对提取完整性、复跑全量验证、执行独立 closure-audit，然后关闭 plan。
- **WS-1（C-04 tree-renderer.tsx）**：772→591 行，拆出 `tree-search.tsx`（`computeTreeSearch`/`collectTreeNodeIds*`/`renderHighlightedLabel`/`EMPTY_TREE_SEARCH`/`TreeSearchState`，147 行；落点 `.tsx` 因 `renderHighlightedLabel` 含 JSX）+ `tree-node-helpers.ts`（`toNodeKey`/`createTreeNodeId`/`toTreeNodes`/`shouldExpandInitially`/`TreeNodeRecord`/`isTreeNodeRecord`/`DEFAULT_*`，47 行）+ `tree-focus-nav.ts`（`getVisibleTreeItems`/`getTreeItemDepth`/`getTreeItemNodeId`/`TreeNavigationKey`，22 行）。`tree-renderer.tsx` 仅留 React 组件 + focus hooks。
- **WS-2（C-05 table-body-row-rendering.tsx）**：723→442 行，拆出 `table-cell-chrome.tsx`（`CellContentWithPopOver`/私有 `CopyButton`/`asReactNode`/`indentStyle`，103 行）+ `table-flattened-items.ts`（`FlattenedRow`/`FlattenedItem`/`FlattenedExpandedRow`/`buildFlattenedItems`/`areColumnsRenderEquivalent`，96 行）+ `table-expanded-row.tsx`（`renderExpandedRow`，103 行）。主文件 lines 18-20 re-export `buildFlattenedItems`/`renderExpandedRow`/`Flattened*`，故 `table-body-rows.tsx:8` 的 import 仍可解析。**`MemoizedDataRow` 比较器逐字迁移**（O-01 归 448）。
- **验证（full-green）**：`pnpm typecheck`(55/55) + `pnpm build`(29/29) + `pnpm lint`(29/29) + `pnpm test`(55/55 task 全绿；`pnpm --filter @nop-chaos/flux-renderers-data test` 548/548)。`pnpm check:oversized-code-files`：两目标文件均退出 ERROR 列表（仅剩 4 个 out-of-scope 文件：`grid-selection.test.tsx`/`form-store.ts`/`infinite-scroll.test.tsx`/`node-compiler.ts`；脚本整体仍 exit 1，符合本计划判据）。
- Closure-audit 由独立 fresh-session 子 agent `ses_1002c65f0ffeCueguErnYnjmSs` 执行（执行 session 不自审），VERDICT `PASS`：7 项客观主张全部独立核对——`MemoizedDataRow` 比较器 vs 拆分前 commit `42b490d6^` diff IDENTICAL、无 src 内 stray 构建产物、import 解析 OK。审计附 1 条非阻塞 doc-accuracy nit（REJ-1 称 `layout-renderer-definitions.ts` 为 701/ERROR，现 live 为 681/WARN，out-of-scope 不影响判据，已记入 plan Closure）。
- Roadmap ❌→✅：N/A——本 plan findings 来自 components 审计，`docs/audits/2026-06-24-2213-{open,multi}-audit-components.md` `Audit Status` 已 `closed`，无 per-finding ❌ roadmap 条目可翻转；状态由本 daily log + plan Closure 记录。
- Follow-up：`>500` 行 WARN 级别文件的系统性收敛（独立治理 plan）。本计划无剩余 plan-owned work。

### 2026-06-26 (runtime-compile contract + composite-item identity correctness — plan 448)

- Executed plan `docs/plans/448-runtime-compile-contract-and-composite-item-identity-correctness-plan.md`：收口 2026-06-24 components 审计的 [O-01]/[C-01]/[C-02]/[C-03]/[C-10]/[C-11]——渲染器绕过 compile-once / 依赖边界漂移 / 组合项身份传播。4 Phase 全部执行完成（C-03 显式裁定 residual）。
- **Phase 1（O-01 组合项身份传播）**：三处 `React.memo` 比较器（`combo-renderer.tsx`、`composite-field/array-field.tsx`、`input-table-renderer.tsx`）补 `itemInstancePath` 判定。**关键修正**：审计建议的引用相等（`===`）会破坏 array-item locality——`itemInstancePath` 数组在集合任意项变更时被整体重建（新引用），导致未变更兄弟项也被判不等而重渲染（`apps/playground` `performance-table-page` "array item locality" 回归，stash 验证 baseline 通过 / 改后 fail）。改用新增 `composite-field/instance-path-equal.ts` 的**值相等** `instancePathEqual`（逐帧比 `repeatedTemplateId`+`instanceKey`）：祖先路径漂移→内容变→不等→重渲染（O-01 修）；单项值变更→兄弟路径内容稳定→相等→跳过（locality 保留）。三处导出 `ComboItem`/`ArrayItem`/`InputTableRow` 供直接渲染级 Proof。Proof：`__tests__/composite-item-instance-path-drift.test.tsx`（修复前 3 fail / 修复后 3 pass；定位性能回归后改为值相等）。
- **Phase 2（C-02 dynamic-renderer compile-once）**：`dynamic-renderer.tsx` 改消费编译期 propsProgram 预编译、运行期由 prop 通道反应式解析的 `props.props.loadAction`，移除 `useScopeSelector(()=>helpers.evaluate(props.schema.loadAction))` + effect 内重 evaluate + `useRenderScope`/`useScopeSelector` 导入；`loadActionRef`（effect 同步）供 `run()`/refresh 取最新解析值。Decision B 而非 A：事件通道 handler 稳定引用无法暴露「解析后 loadAction 变化」做 reload change-detection（reload-reactivity 测试要求作用域驱动重载）。Proof：`__tests__/dynamic-renderer-compile-once.test.ts`（源码契约断言消费 `props.props.loadAction` 且无 `helpers.evaluate(props.schema` / `props.schema.loadAction`）；定义文件注释同步）；既有 14 测试全绿（含 reload-reactivity）。
- **Phase 3（C-03/C-10 CRUD）**：C-10 已落地（`crud-renderer.tsx:178` `createCrudQueryFormId(props.id, props.path)`，与 `:60` 一致；无 `props.schema.id`）。C-03 **residual**：carrier `list`/`cards` 委托契约（既有 `crud-list-mode.test.tsx` 锁定 `[data-slot="list-root"]`/cards carrier 标记）+ React Compiler（`reactCompilerPreset`）子树 memoize 是硬约束；(A) CRUD 自迭代 `props.regions.item.render` 会移除委托→回归测试；(B) 把已编译 TemplateNode 作 carrier `item`/`card` 字段——`isSchema(TemplateNode)===true`（TemplateNode 有 string `type`），carrier 编译器会重/错编译；`normalizeNodeInput`（`render-nodes.tsx:186-188`）仅复用 TemplateNode 作为 render **直接**输入（等价 A）。完整修复需 carrier reactive-scope 重设计，超出最小修复范畴。Proof：`__tests__/crud-item-card-compile-contract.test.ts`（源码裁定：carrier 委托 + keyed-remount 保留为 intentional，锁定 residual 边界）；既有 6 测试全绿。
- **Phase 4（C-01/C-11）**：前序 run 已落地并复核——C-01 `detail-view/projected-scope.ts:1` 从 `@nop-chaos/flux-react/unstable` re-export `createProjectedScopeStore`，`package.json` `flux-runtime` 在 devDependencies（生产代码零 `from '@nop-chaos/flux-runtime'`）；C-11 `page.tsx:56-59` aside 走 `props.regions.aside?.templateNode`（无 `props.schema.aside`）。Proof：`pnpm check:workspace-manifest-deps` 通过。
- Closure re-verification 修复：复跑 `pnpm lint` 暴露执行期 `dynamic-renderer.tsx:83` `loadActionRef.current = loadAction`（render 期写 ref，`react-hooks/refs`）——改为 `useEffect` 同步（与 `array-field.tsx:355` 同模式；event/effect 在 commit 后触发，ref 始终最新，行为不变）。修复后 lint 全绿。
- 验证（unit-green，未跑 e2e——不在本 plan scope/gates）：受影响包 `pnpm test` 全过（form-advanced 842 含新 O-01 Proof / basic 390 含新 C-02 Proof + dynamic 14 / data 548 含新 C-03 Proof + crud-list-mode 6 / playground performance-table-page 5/5 locality 不回归）；closure gates `pnpm typecheck`(55/55) + `pnpm build` + `pnpm lint`(29/29，0 errors) + `pnpm test`(workspace full-green 55/55) + `pnpm check:workspace-manifest-deps`。
- Roadmap ❌→✅：N/A——本 plan findings 来自 components 审计，audit-level `Audit Status` 已 `closed`、无 per-finding ❌ roadmap 条目可翻转；状态由本 daily log + plan Closure 记录。
- 源审计关闭：`docs/audits/2026-06-24-2213-{open,multi}-audit-components.md` `Audit Status` 已 `closed`（前序 run 关闭，本轮 idempotent）。
- Follow-up：C-03 carrier reactive-scope 重设计 + O-01 深层重构（`docs/skills/react19-best-practices-review.md` 方向）——另立 plan。Closure-audit 由独立 fresh-session 子 agent 执行（执行 session 不自审），见 plan `Closure Audit Evidence`。
