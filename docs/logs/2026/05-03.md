# Development Log — 2026-05-03

### 2026-05-03

- Executed Plan 188 summary remediation across runtime validation owners, cleanup-safe `statusPath` publication, word-editor page-margin owner sync, and workspace manifest hygiene. Code/tests: `packages/flux-runtime/src/form-runtime-field-ops.ts`, `packages/flux-runtime/src/form-runtime-owner.ts`, `packages/flux-runtime/src/form-runtime-submit-flow.ts`, `packages/flux-runtime/src/__tests__/owner-registration-contracts.test.ts`, `packages/flux-runtime/src/__tests__/owner-validation-lifecycle-contracts.test.ts`, `packages/flux-runtime/src/__tests__/owner-submit-contracts.test.ts`, `packages/flux-react/src/status-path.ts`, `packages/flux-renderers-data/src/crud-renderer-state.ts`, `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/spreadsheet-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/word-editor-renderers/src/word-editor-page.tsx`, `packages/word-editor-renderers/src/toolbar/page-controls.tsx`, `packages/word-editor-renderers/src/toolbar/page-controls.test.tsx`.
- Validation owner baseline is now explicit and test-backed: foreign registration paths are rejected before entering owner-local maps, scheduled async debounce windows count as pending owner work for `validating`/`ready`, and submit under `bootstrapping` / `refreshing` returns an explicit failure before touching submit state. The old oversized `owner-based-validation-contracts.test.ts` surface was split into focused successor files.
- Cleanup-safe publication baseline is now shared through `packages/flux-react/src/status-path.ts`; the audited CRUD/tree/designer/spreadsheet/report/word `statusPath` publishers all clear their host summary on unmount. Focused publish/unmount regressions were added in the owning renderer test suites.
- Word editor page margins no longer keep a disconnected local draft: opening the dialog hydrates from `editor-store.paperSettings`, and Apply writes back to both the store and the bridge command path.
- Workspace manifest hygiene is now machine-checkable through `scripts/check-workspace-manifest-deps.mjs` and root script `pnpm check:workspace-manifest-deps`; `packages/flux-renderers-form/package.json` and `packages/flux-renderers-data/package.json` now declare their live test-only `@nop-chaos/flux-compiler` dependency.
- Updated owner docs to the final live baseline only: `docs/architecture/form-validation.md`, `docs/references/form-validation-execution-details.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flux-runtime-module-boundaries.md`, `docs/components/word-editor-page/design.md`, and `docs/references/audit-rules/workspace-manifest-dependency-hygiene.md`.
- Key decision: keep ordinary validation entrypoints waiting through transitional owner lifecycle, but make submit use an explicit early failure baseline; reuse one shared cleanup-safe publication hook instead of six more ad hoc effects; keep manifest verification scoped to package test-source workspace imports only.
- Verification so far: focused runtime/renderer/word-editor status and margin tests plus `pnpm check:workspace-manifest-deps` are green; next step is full workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`, then independent closure audit and final Plan 188 closure bookkeeping.

### 2026-05-03

- Continued Plan 187 and closed the remaining semantic convergence work across validation owners, built-in action timeout cancellation, and Flow Designer tree/port contracts.
- Phase 1 baseline is now live end to end: projected child editors rebase validation metadata into owner-local relative paths, and `bootstrapping` / `refreshing` validation requests wait for `active` instead of returning a fake clean success. Code/tests: `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.ts`, `packages/flux-runtime/src/form-runtime-validation.ts`, `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.test.ts`, `packages/flux-runtime/src/__tests__/form-runtime-owner-lifecycle.test.ts`.
- Phase 2 baseline is now live end to end: `timeout` composes with parent abort instead of replacing it, so timed actions still observe upstream cancellation while preserving structured timeout results. Code/tests: `packages/flux-action-core/src/operation-control.ts`, `packages/flux-action-core/src/action-dispatcher/action-execution.ts`, `packages/flux-action-core/src/__tests__/operation-control.test.ts`, `packages/flux-runtime/src/__tests__/runtime-actions-submit.test.ts`.
- Phase 3 baseline is now live end to end: tree mode keeps one `DesignerCore` alive and updates projected tree docs through `core.replaceDocument(...)`, preserving `selection` / `history` / `snapshot` continuity across tree edits; port identity now survives command dispatch, connect/reconnect callbacks, duplicate-edge validation, manifest/action payloads, persistence, and Xyflow render-back. Code/tests: `packages/flow-designer-renderers/src/designer-page.tsx`, `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-renderers/src/designer-action-provider.ts`, `packages/flow-designer-renderers/src/designer-manifest.ts`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/xyflow-utils.ts`, `packages/flow-designer-renderers/src/designer-page.tree.test.tsx`, `packages/flow-designer-renderers/src/index.xyflow.test.tsx`, `packages/flow-designer-renderers/src/designer-provider-and-manifest.test.tsx`, `packages/flow-designer-core/src/core.test.ts`.
- Updated owner docs to match the final baseline only: `docs/architecture/form-validation.md`, `docs/architecture/object-field.md`, `docs/architecture/variant-field.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `docs/architecture/action-scope-and-imports.md`, `docs/references/action-payload-matrix.md`, `docs/architecture/flow-designer/design.md`, `docs/architecture/flow-designer/tree-mode.md`, `docs/architecture/flow-designer/canvas-adapters.md`, `docs/architecture/flow-designer/collaboration.md`, `docs/architecture/flow-designer/config-schema.md`, `docs/architecture/flow-designer/api.md`.
- Key decision: keep Plan 168 `formId` semantics unchanged, treat timeout composition as the actual Phase 2 runtime gap, and narrow the old Flow Designer tree-mode clipboard claim instead of pretending it was part of the proved closure baseline.
- Verification so far: focused Flow Designer package typechecks/tests passed after the Phase 3 fixes; full workspace verification is the remaining closure gate for Plan 187.
- Next step: run repo-wide `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`, then record independent closure audit evidence before marking Plan 187 completed.

### 2026-05-03

- Completed Plan 185 by landing the two remaining large-file hotspot splits: `packages/flux-runtime/src/async-data/api-data-source-controller.ts` now delegates to `api-data-source-controller-types.ts`, `api-data-source-controller-state.ts`, and `api-data-source-controller-runtime.ts`; `packages/spreadsheet-renderers/src/spreadsheet-toolbar.tsx` now composes focused modules under `packages/spreadsheet-renderers/src/spreadsheet-toolbar/`.
- Added/kept focused verification aligned with those splits: runtime polling/refresh coverage stayed green in `packages/flux-runtime/src/__tests__/request-runtime-polling.test.ts` and `runtime-sources-refresh.test.ts`, and a dedicated toolbar regression test now covers the split shell in `packages/spreadsheet-renderers/src/spreadsheet-toolbar.test.tsx`.
- Updated owner docs and plan state to match the live baseline instead of the earlier partially-landed snapshot: `docs/architecture/flux-runtime-module-boundaries.md`, `docs/components/spreadsheet-page/design.md`, and `docs/plans/185-large-file-hotspot-split-plan.md`.
- Restored honest full-workspace verification by fixing three unrelated baseline blockers surfaced during closure verification: replaced a render-time mutable `useRef(...)` initializer with `React.useId()` in `packages/flux-renderers-form-advanced/src/composite-field/array-field.test.tsx`, narrowed `packages/flux-renderers-data/src/crud-renderer.tsx` memo deps to stable `tableEmpty` input for React Compiler lint compatibility, and aligned the `ManagedFormRuntimeSharedState` fixture in `packages/flux-runtime/src/__tests__/form-runtime-values.test.ts` with the required `lifecycleWaiters` field.
- Verification: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all pass on the final baseline; focused package checks for `@nop-chaos/flux-runtime`, `@nop-chaos/spreadsheet-renderers`, `@nop-chaos/flux-renderers-form-advanced`, and `@nop-chaos/flux-renderers-data` also passed while closing the loop.
- Key decision: close Plan 185 only after both hotspot splits, owner docs, focused tests, and full workspace verification converged; treat unrelated lint/type blockers as baseline debt to fix narrowly rather than documenting around a red workspace.
- Independent closure audit `ses_214a78e56ffeF52ABH5hfnWN6C` confirmed no remaining in-scope code/doc split work; the only closure blocker had been missing audit bookkeeping, which is now recorded in `docs/plans/185-large-file-hotspot-split-plan.md`.
- Next step: move back to the remaining open owner plans.

### 2026-05-03

- Fixed the residual `submitForm` `formId` dispatcher bug so built-in actions no longer require a local `ctx.form` before the runtime adapter can resolve a targeted form through `componentRegistry`.
- Added dispatcher-level regression coverage in `packages/flux-action-core/src/__tests__/action-dispatcher.test.ts` to prove `submitForm` with `formId` is forwarded to the built-in adapter even when the current action context has no local form runtime.
- Kept the fix intentionally narrow in `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`: only non-targeted submit actions still require local `ctx.form`; targeted `formId` submit now follows the already-supported runtime adapter path in `packages/flux-runtime/src/action-adapter.ts`.
- Verification: `pnpm --filter @nop-chaos/flux-action-core test -- --runInBand` and `pnpm --filter @nop-chaos/flux-runtime test -- --runInBand` passed after the regression fix.
- Key decision: preserve the `Plan 168` baseline that `formId` remains a supported cross-form targeting carrier for `submitForm`; this change restores dispatcher/runtime alignment instead of redefining the public contract.
- Next step: run workspace `pnpm typecheck`, `pnpm build`, and `pnpm lint`, then continue with the remaining `Plan 187` residuals if no new verification failures surface.

### 2026-05-03

- Re-audited Plans `183`, `184`, and `185` against the live repo and rewrote their status/checklists to match the actually landed slices instead of the earlier placeholder `planned` baseline.
- Corrected the stale large-file split claims in `docs/architecture/flux-runtime-module-boundaries.md`, `docs/components/spreadsheet-page/design.md`, and the 2026-05-02 log: `field-utils.tsx` is split, but `api-data-source-controller.ts` and `spreadsheet-toolbar.tsx` are still unsplit hotspots.
- Synced `docs/architecture/renderer-runtime.md` with the live `SchemaRendererProps.strictValidation` surface while keeping the active Plan 183/184 owner-doc baseline intact.
- Verification follow-up: the first full `pnpm test` rerun exposed two workspace-only Vitest timeout hotspots rather than deterministic behavior regressions: `packages/report-designer-renderers/src/renderers.integration.test.tsx` and `packages/flux-renderers-form-advanced/src/__tests__/form-double-edit-regression.test.tsx`.
- Applied the smallest test-only fix by giving each affected `describe(...)` block a file-local `{ timeout: 15000 }` budget instead of changing product code or global Vitest defaults.
- Re-verified both affected packages directly: `pnpm --filter @nop-chaos/report-designer-renderers test` and `pnpm --filter @nop-chaos/flux-renderers-form-advanced test` now pass on the updated baseline.
- Final workspace verification is green again: `pnpm test` now passes across the full turbo run after the two narrow timeout adjustments.
- Independent closure audits: fresh subagent review `ses_214dbcd48ffezBE0lfXXRLf1gR` confirmed Plan 184 can now close with no remaining in-scope work; fresh subagent review `ses_214dbcddeffe4vtDtM4eH7HOXO` kept Plan 183 open but confirmed Phases 1-3 are landed and that the remaining gap is closure evidence rather than implementation debt.
- Updated `docs/plans/184-reactive-hot-path-precision-and-notification-scaling-plan.md` to `completed`, and tightened `docs/plans/183-renderer-props-and-host-neutral-typing-convergence-plan.md` so Phase 3 and the green verification items are checked without overstating closure.
- Key decision: close Plan 184 now that code, docs, focused verification, workspace verification, and independent closure audit all align; keep Plan 183 open only for its final closure pass. Plan 185 remains `partially completed` because only the field-utils slice is landed.
- Next step: either run the final closure pass for Plan 183 or start the remaining Plan 185 hotspot split work.
