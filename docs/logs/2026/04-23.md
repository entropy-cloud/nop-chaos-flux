# 2026-04-23

### 2026-04-23

- Recovered the `/#/flux-basic` debugger e2e regression by removing the route-level `React.lazy` boundary for `FluxBasicPage`, keeping the page module eagerly imported, tightening the Playwright heading locator to the page-level `h1`, and recording the incident as a historical bug note.
- Link to doc or code path: `apps/playground/src/app.tsx`, `apps/playground/src/pages/flux-basic-page.tsx`, `tests/e2e/debugger.spec.ts`, `docs/bugs/33-flux-basic-lazy-route-suspense-hang-fix.md`
- Key decision: treat the blocker as a route-level Suspense/lazy-boundary failure instead of continuing to patch renderer registration paths, since direct route mounting made the page render immediately and restored the debugger coverage.
- Next step: if we want the `flux-basic` route code-split again later, reproduce the lazy-boundary stall in isolation first and only reintroduce `React.lazy` after the underlying child render suspension is understood.

### 2026-04-23

- Executed plan 133 end to end by separating import-owned `ActionScope` creation from renderer-owned `actionScopePolicy`: `xui:imports` still always creates a child capability scope, but that rule now lives as import boundary semantics instead of a renderer-policy fallback in `NodeRenderer`.
- Link to doc or code path: `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/use-node-scopes.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-react/src/schema-renderer-imports-boundaries.test.tsx`, `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/module-cache-and-import-stack.md`, `docs/plans/133-node-renderer-runtime-stack-and-import-boundary-refactor-plan.md`
- Key decision: preserve the user-confirmed runtime rule that every `xui:imports` node gets its own child `ActionScope` and falls back through the parent chain, while making it explicit that this is import-owned runtime behavior rather than renderer metadata.
- Next step: if future work changes import capability ownership again, start from the now-explicit import-owned boundary model instead of reintroducing `xui:imports` via renderer `actionScopePolicy` mixing; note that workspace-wide `pnpm typecheck/build/lint/test` were attempted in this session but not fully observed to completion because of timeout/external workspace failures, while focused `flux-react` verification and the independent closure audit task `ses_247543496ffewQNC5a7dGJXmwE` both passed.

### Lint Fixes for React Hooks Exhaustive Deps

Fixed pre-existing lint errors in dialog/drawer renderers and code-editor:

1. **dialog.tsx / drawer.tsx**: `surfaceStackSnapshot` was in useMemo deps but not used in computation body
   - Added `isDeclarativeSurfaceActiveInSnapshot(id, snapshot)` helper that uses snapshot directly
   - Changed from using global `isDeclarativeSurfaceActive(id)` to snapshot-based version
   - Now lint-compliant: dependency is actually used in the computation

2. **use-sql-editor-state.ts**: `props.helpers` in deps but `props` accessed via `buildExecutionParams(props, ...)`
   - Changed dependency from `[props.helpers, sqlConfig, view]` to `[props, sqlConfig, view]`
   - Correctly reflects that the callback accesses multiple parts of props

**Files Modified:**
- `packages/flux-renderers-basic/src/declarative-surface-stack.ts` - added `isDeclarativeSurfaceActiveInSnapshot()`
- `packages/flux-renderers-basic/src/dialog.tsx` - use snapshot-based active check
- `packages/flux-renderers-basic/src/drawer.tsx` - use snapshot-based active check
- `packages/flux-code-editor/src/code-editor-renderer/use-sql-editor-state.ts` - fix deps array

**Verification:** `pnpm typecheck && pnpm build && pnpm lint` all pass

---

### Plan 132 Phase 3 Completed: Runtime Migration to Compiled Data

Completed Phase 3 of Plan 132 (Runtime Schema Dependency Elimination) - Reactions and Data Sources now use only compiled data at runtime.

**Key Changes:**

1. **Reactions fully migrated to compiled-only:**
   - `CompiledReaction.watch` type changed: `readonly string[]` → `CompiledRuntimeValue<unknown>` (stores compiled expression, not raw template string)
   - `CompiledReaction.when` type changed: `CompiledRuntimeValue<boolean>` → `CompiledExpression<boolean>` (raw expression, not template)
   - `reaction-compiler.ts` refactored to compile watch expressions and treat when as formula
   - `reaction-runtime.ts` uses only `compiledReaction`
   - `runtime-factory.ts` `registerReaction` requires `compiledReaction`
   - `ReactionRenderer` requires `compiledReaction`

2. **Data Sources fully migrated to compiled-only:**
   - **`resultMapping` fix**: Changed from eager evaluation at registration time to deferred evaluation with `payload` context
     - Added `compiledResultMapping?: CompiledRuntimeValue<unknown>` to controller interface
     - `applyResultMapping` now accepts `CompiledRuntimeValue` and evaluates with `payload` in scope
   - **API config fix**: Changed from pre-evaluated `ApiSchema` to `CompiledApiConfig` that evaluates at request time
     - Added `createApiConfigRuntimeState()` and `evaluateCompiledApiConfig()` helpers
     - API URL/params/data/headers re-evaluated on each refresh, enabling proper dependency tracking
   - Exported `compileApiConfig()` from `@nop-chaos/flux-compiler`
   - `RendererRuntime.createDataSourceController` interface updated to require `compiledApi`

3. **Public API changes:**
   - `RendererRuntime.registerDataSource({ compiledSource })` - requires compiled source
   - `RendererRuntime.registerReaction({ compiledReaction })` - requires compiled reaction
   - `RendererRuntime.createDataSourceController({ compiledApi })` - requires compiled API config

**Files Modified:**
| File | Change |
|------|--------|
| `packages/flux-core/src/types/compilation.ts` | `CompiledReaction.watch/when` type changes |
| `packages/flux-core/src/types/renderer-core.ts` | Updated `createDataSourceController` interface |
| `packages/flux-compiler/src/reaction-compiler.ts` | Compile watch as expression, when as formula |
| `packages/flux-compiler/src/source-compiler.ts` | Export `compileApiConfig` |
| `packages/flux-compiler/src/index.ts` | Export `compileApiConfig` |
| `packages/flux-runtime/src/async-data/data-source-runtime.ts` | Accept `CompiledApiConfig`, evaluate at request time |
| `packages/flux-runtime/src/async-data/data-source-runtime-utils.ts` | Add `evaluateCompiledApiConfig`, `createApiConfigRuntimeState`, fix `applyResultMapping` |
| `packages/flux-runtime/src/async-data/source-registry.ts` | Pass compiled values directly to controllers |
| `packages/flux-runtime/src/__tests__/request-runtime-polling.test.ts` | Updated to use `compileApiConfig` |

**Verification Passed:**
- `pnpm typecheck` - all packages pass
- `pnpm build` - all packages build
- `pnpm --filter @nop-chaos/flux-runtime test` - 355 tests pass
- `pnpm --filter @nop-chaos/flux-compiler test` - 84 tests pass

**Next Steps:**
- Phase 4: DevTools compatibility
- Phase 5: Cleanup and documentation

---

### 2026-04-23

- Updated the runtime architecture docs to separate `Host Projection`, `ActionScope`, `ComponentHandleRegistry`, and `ImportFrame` as four distinct boundary layers, and clarified that `xui:imports` is an import-owned lexical boundary rather than a generic host-boundary flag.
- Link to doc or code path: `docs/architecture/action-scope-and-imports.md`, `docs/architecture/module-cache-and-import-stack.md`, `docs/architecture/renderer-runtime.md`
- Key decision: stop treating the old `NodeRenderer` refactor plan as the active owner plan; the remaining live problem is runtime stack minimization and import-boundary convergence, so a new successor plan now owns that work.
- Next step: execute `docs/plans/133-node-renderer-runtime-stack-and-import-boundary-refactor-plan.md` against the live `NodeRenderer` / import boundary code paths instead of reviving the old file-shape-oriented checklist.

### 2026-04-23

- Finished closing the remaining `flux-code-editor` SQL plan gap by fixing the React hook dependency lint issue in `use-sql-editor-state.ts`, reran focused package verification, and updated the plan evidence/checklists so the completed status matches the live repo.
- Link to doc or code path: `packages/flux-code-editor/src/code-editor-renderer/use-sql-editor-state.ts`, `docs/plans/22-code-editor-sql-enhancement-plan.md`
- Key decision: keep the runtime fix minimal by making `handleExecuteSQL` depend on the full renderer props object it already closes over, rather than introducing a larger refactor just to satisfy React Compiler linting.
- Next step: continue reconciling the remaining completed plan docs that still have stale unchecked execution text despite already having closure evidence.

### 2026-04-23

- Refined `docs/articles/ai-assisted-large-framework-best-practices.md` to make the argument more even and internally smooth: softened the over-governance phrasing, clarified that benchmark evidence is a targeted pressure mechanism rather than an already-universal default gate, and wove multi-stage independent review into the article's main governance narrative instead of leaving it as an appended aside.
- Link to doc or code path: `docs/articles/ai-assisted-large-framework-best-practices.md`, `docs/skills/deep-audit-prompts.md`, `docs/logs/2026/04-15.md`, `docs/logs/2026/04-17.md`, `docs/logs/2026/04-20.md`
- Key decision: keep the article opinionated, but distinguish more clearly between always-needed executable pressure (`live repo`, focused verification, playground, structured diagnostics) and benchmark's current role as a pressure requirement mainly for performance-sensitive topics.
- Next step: if the article is revised again, check whether the workflow section should explicitly reference periodic deep-audit style re-review without turning the essay into a process manual.

### 2026-04-23

- Re-audited `docs/plans/` against the live repo, started closing stale completed-plan checklists/closure notes, and repaired one real code gap discovered during the audit: `flux-code-editor` SQL execution now honors `execution.params` scope mappings and suppresses the result preview when `showPreview: false`.
- Link to doc or code path: `docs/plans/43-tanstack-query-inspired-runtime-improvement-plan.md`, `docs/plans/112-capability-projection-manifest-implementation-plan.md`, `docs/plans/116-module-cache-import-stack-compile-symbol-resolution-plan.md`, `docs/plans/132-runtime-schema-dependency-elimination-plan.md`, `packages/flux-code-editor/src/code-editor-renderer/use-sql-editor-state.ts`, `packages/flux-code-editor/src/code-editor-renderer.tsx`
- Key decision: treat plan closure as a live repo audit problem rather than a checkbox cleanup problem; plans whose code surface is complete should be documented as complete with explicit evidence, while plans whose text over-claimed completion should be reclassified instead of forcing code to match stale plan prose.
- Next step: finish rewriting the remaining misclassified plans (`22-code-editor-sql`, `36-node-renderer-refactor`, `126`, `132`) so their status, scope, and follow-up ownership match the current repository baseline.

### 2026-04-23

- Strengthened `docs/articles/ai-assisted-large-framework-best-practices.md` by adding a new principle that elevates exploratory prototypes, playground probes, benchmarks, and structured diagnostics from auxiliary techniques into first-class semantic adjudication mechanisms for large-framework AI development.
- Link to doc or code path: `docs/articles/ai-assisted-large-framework-best-practices.md`, `docs/logs/2026/04-15.md`, `docs/logs/2026/04-13.md`, `docs/logs/2026/04-20.md`, `docs/logs/2026/04-17.md`
- Key decision: reject the framing that this repo's risk is over-governance; instead, make explicit that strong plans/audits/docs must stay under pressure from executable probes, focused verification, live code paths, and observable playground/diagnostics evidence.
- Next step: if this article is further expanded, add a follow-up section on keeping candidate abstractions deliberately unpromoted until repeated executable evidence shows they deserve entry into the long-term baseline.

### 2026-04-23

- Extended the AI-native development article with an explicit default operating workflow: route through owner docs first, re-audit the live repo before execution, externalize progress into development logs during execution, use multi-agent reconciliation for complex questions, verify in focused-then-full order, and require an independent closure audit before declaring completion.
- Link to doc or code path: `docs/articles/ai-native-large-system-development-lessons.md`, `docs/index.md`, `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/logs/index.md`
- Key decision: turn the article from a retrospective summary into a more executable working method, so future AI sessions can treat it as a default workflow rather than only as a historical essay.
- Next step: if needed, extract this workflow section into a dedicated checklist/reference doc that future agents can follow directly.

### 2026-04-23

- Closed plan 132 after landing declarative `dialog` / `drawer` renderer-path `statusPath` publication end to end: both renderers now publish live open state, write closed snapshots on cleanup, default omitted `open` / `defaultOpen` to closed, and keep `active` aligned with the top declarative surface via a minimal renderer-local stack helper.
- Link to doc or code path: `packages/flux-renderers-basic/src/dialog.tsx`, `packages/flux-renderers-basic/src/drawer.tsx`, `packages/flux-renderers-basic/src/declarative-surface-stack.ts`, `packages/flux-renderers-basic/src/__tests__/basic-page-layout.test.tsx`, `docs/architecture/surface-owner.md`, `docs/components/dialog/design.md`, `docs/components/drawer/design.md`, `docs/plans/132-declarative-surface-statuspath-support-plan.md`
- Key decision: keep declarative surfaces on the existing renderer-local path and add only the smallest shared stack helper needed to make top-surface `active` semantics stable, instead of widening this slice into `SurfaceRuntime` migration work.
- Next step: keep any future declarative-surface `SurfaceRuntime` migration or `data` own-scope work in separate plans; separately fix the unrelated workspace blockers currently failing `pnpm typecheck` / `pnpm build` in `packages/flux-compiler/src/source-compiler.test.ts` and `pnpm lint` in `packages/flux-core/src/types/compilation.ts`.

### Plan 132 Phase 2 Completed: Source and Reaction Compilers

Implemented Phase 1-2 of Plan 132 (Runtime Schema Dependency Elimination):

**Phase 1: Type Definitions**
- Added `CompiledApiConfig` interface to `flux-core/src/types/compilation.ts`
- Added `CompiledOperationControl` interface
- Added `CompiledDataSource` interface (kind: 'api' | 'formula')
- Added `CompiledReaction` interface
- Added `compiledSources` and `compiledReactions` fields to `TemplateNode`

**Phase 2: Compiler Implementation**
- Created `packages/flux-compiler/src/source-compiler.ts`:
  - `compileDataSource()` - compiles DataSourceSchema to CompiledDataSource
  - `isDataSourceFullyStatic()` - checks if all fields are static
  - Compiles API config (url, method, data, params, headers)
  - Compiles merge options (strategy, key, mapping)
  - Compiles timing options (interval, stopWhen)
  
- Created `packages/flux-compiler/src/reaction-compiler.ts`:
  - `compileReaction()` - compiles ReactionSchema to CompiledReaction
  - `isReactionFullyStatic()` - checks if when/action are static
  - Normalizes watch paths to array
  - Compiles when condition
  - Uses existing `compileActions()` for action compilation

- Integrated into `schema-compiler.ts`:
  - Detects `type: 'data-source'` and `type: 'reaction'` nodes
  - Populates `compiledSources` and `compiledReactions` on TemplateNode

**Unit Tests Added**
- `packages/flux-compiler/src/source-compiler.test.ts` (14 tests)
- `packages/flux-compiler/src/reaction-compiler.test.ts` (12 tests)

**Files Created/Modified**
| File | Change |
|------|--------|
| `packages/flux-core/src/types/compilation.ts` | Added compiled types |
| `packages/flux-core/src/types/node-identity.ts` | Added compiledSources/Reactions to TemplateNode |
| `packages/flux-compiler/src/source-compiler.ts` | New file |
| `packages/flux-compiler/src/reaction-compiler.ts` | New file |
| `packages/flux-compiler/src/source-compiler.test.ts` | New test file |
| `packages/flux-compiler/src/reaction-compiler.test.ts` | New test file |
| `packages/flux-compiler/src/schema-compiler.ts` | Integrated compilers |
| `packages/flux-compiler/src/index.ts` | Exported new compilers |

**Verification Passed**
- `pnpm typecheck` - all packages pass
- `pnpm build` - all packages build
- `pnpm --filter @nop-chaos/flux-compiler test` - 82 tests pass (23 new)

**Next Steps**
- Phase 3: Migrate runtime to use compiled data instead of raw schema
- Phase 4: DevTools compatibility
- Phase 5: Cleanup and documentation

---

### 2026-04-23

- Refined the AI-native development article again to explicitly elevate two recurring repo practices into first-class guidance: automatic development-log writing during execution, and multi-subagent review loops that keep reconciling findings until they converge on an evidence-backed consensus.
- Link to doc or code path: `docs/articles/ai-native-large-system-development-lessons.md`, `docs/logs/index.md`, `docs/logs/2026/04-22.md`, `docs/analysis/2026-04-17-deep-audit/index.md`, `docs/analysis/2026-04-17-deep-audit-meta-review.md`, `docs/analysis/2026-04-19-deep-audit-unresolved-issues.md`
- Key decision: treat development logs as execution-time external memory rather than post-hoc summaries, and treat independent subagents as a consensus-forming review system rather than a one-shot parallel search trick.
- Next step: if this workflow is formalized further, codify log-writing and consensus-audit expectations into a dedicated internal checklist or agent workflow guide.

### 2026-04-23

- Revised the AI-native engineering article after a broader multi-agent audit of `docs/logs/`, adding underrepresented historical lessons around DOM-first UI debugging, event-driven E2E waits, timeout/concurrency control, focused bug-fix loops, bug-fix note discipline, warn-to-error lint hardening, compatibility layers during boundary migration, and executable oversized-file governance.
- Link to doc or code path: `docs/articles/ai-native-large-system-development-lessons.md`, `docs/logs/2026/04-15.md`, `docs/logs/2026/03-20.md`, `docs/logs/2026/04-18.md`, `docs/logs/2026/04-13.md`, `docs/bugs/00-bug-fix-note-writing-guide.md`
- Key decision: present these as historically validated practices and calibrated governance patterns, while explicitly qualifying which checks are already enforced in the repo versus which remain recommended future automation.
- Next step: if needed, split the article into a shorter public-facing essay plus a repo-internal checklist/playbook variant.

### 2026-04-23

- Added an AI-native engineering article summarizing how this repository's history converged on a more effective large-system development workflow: doc-first baseline control, narrow owner plans, independent subagent closure audits, reverse requirement extraction, multi-solution design exploration, and strong lint/check gates.
- Link to doc or code path: `docs/articles/ai-native-large-system-development-lessons.md`, `docs/index.md`, `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/discussions/README.md`, `docs/analysis/2026-04-17-deep-audit/index.md`, `docs/analysis/2026-04-17-deep-audit-meta-review.md`, `docs/analysis/2026-04-19-deep-audit-unresolved-issues.md`, `docs/logs/2026/04-22.md`, `package.json`, `eslint.config.js`
- Key decision: frame the article around historically validated workflow patterns rather than abstract AI productivity advice, and emphasize that lint/check automation should absorb as many high-signal mechanical review rules as possible without turning architecture preference into noisy enforcement.
- Next step: if the repo later formalizes more workflow automation, the most natural follow-up is to turn the article's recommended plan-closure, package-boundary, FieldFrame, raw-HTML, and subscription-scope checks into calibrated scripts or lint rules.

### Plan 131 Completed: Static Analysis Optimization

**All phases completed:**
- Phase 1: Type definitions (staticCapable, StaticAnalysisResult)
- Phase 2: Renderer definitions updated
- Phase 3: Compiler implementation (bottom-up analysis)
- Phase 4: Unit tests (17 test cases)
- Phase 5: Documentation (`docs/architecture/static-analysis.md`)

**Files Created/Modified:**
| File | Change |
|------|--------|
| `packages/flux-core/src/types/renderer-core.ts` | Added `staticCapable` |
| `packages/flux-core/src/types/node-identity.ts` | Added `StaticAnalysisResult`, `staticAnalysis` |
| `packages/flux-compiler/src/schema-compiler.ts` | Added `computeStaticAnalysis()`, `isMetaProgramStatic()` |
| `packages/flux-compiler/src/schema-compiler-static-analysis.test.ts` | New test file (17 tests) |
| `packages/flux-renderers-basic/src/index.tsx` | Added `staticCapable: true` to display renderers |
| `packages/flux-runtime/src/import-stack.ts` | Fixed `reportImportFailure` import (unrelated bug) |
| `docs/architecture/static-analysis.md` | New architecture doc |
| `docs/plans/131-static-analysis-optimization-plan.md` | Status: completed |
| `docs/index.md` | Added routing entry for static-analysis.md |

---

### Static Analysis Implementation - Phase 4 Completed

Added comprehensive unit tests for static analysis in `packages/flux-compiler/src/schema-compiler-static-analysis.test.ts`:

**Test Categories (17 tests total)**:
- `staticCapable renderer detection`: text static/dynamic, input not static, button not static
- `container with children`: all static children, any non-static child, child with expression, empty container
- `name binding detection`: marks node with name as not static
- `event handler detection`: marks node with events as not static
- `scope creation detection`: marks form as not static
- `nested static analysis`: deep nesting propagation
- `meta expressions`: visible/disabled expressions make node non-static
- `array root compilation`: each node has own static analysis

**Bug Fix**: Fixed meta program static check - added `isMetaProgramStatic()` to check visible/disabled expressions.

**Unrelated Fix**: Fixed import error in `flux-runtime/src/import-stack.ts` - `reportImportFailure` was incorrectly imported as type.

---

### Static Analysis Implementation (Plan 131)

Implemented Phase 1-3 of Plan 131 (Static Analysis Optimization):

**Phase 1: Type Definitions**
- Added `staticCapable?: boolean` to `RendererDefinition` in `flux-core/src/types/renderer-core.ts`
- Added `StaticAnalysisResult` interface to `flux-core/src/types/node-identity.ts`
- Added `staticAnalysis?: StaticAnalysisResult` to `TemplateNode`

**Phase 2: Renderer Definitions**
- Updated basic renderers with `staticCapable: true`:
  - `container`, `fragment`, `flex`, `text`, `icon`, `badge`
- Interactive renderers default to `staticCapable: false` (no change needed)

**Phase 3: Compiler Implementation**
- Added `computeStaticAnalysis()` function to `flux-compiler/src/schema-compiler.ts`
- Implements bottom-up post-order traversal
- Static analysis computed after regions (children) are compiled
- Static conditions checked:
  1. Renderer `staticCapable: true`
  2. Props program `isStatic: true`
  3. No `name` binding
  4. No event handlers
  5. `scopePlan.kind === 'inherit'`
  6. All children static

**Verification Passed**
- `pnpm typecheck` - all packages pass
- `pnpm build` - all packages build
- `pnpm lint` - all packages pass
- `pnpm test` - all tests pass

**Files Changed**
- `packages/flux-core/src/types/renderer-core.ts` - added `staticCapable`
- `packages/flux-core/src/types/node-identity.ts` - added `StaticAnalysisResult`, `staticAnalysis`
- `packages/flux-compiler/src/schema-compiler.ts` - added static analysis computation
- `packages/flux-renderers-basic/src/index.tsx` - added `staticCapable` to display renderers

**Next Steps**
- Phase 4: Add unit tests for static analysis
- Phase 5: Documentation updates

---

### V11 Design Architectural Revision

- **Identified fundamental flaw**: V11's execution strategies (Resumability, Islands, ExecutionStrategy per node) incorrectly assumed DSL can control framework-level execution
- **Root cause**: DSL layer cannot override React's hydration model, Resumability requires Qwik's architecture
- **Corrected design**: 
  - DSL declares **intent** via hints, not execution directives
  - Framework adapter layer decides how (or whether) to honor hints
  - Execution control (hydration, server/client split) stays with framework layer

### Follow-up Correction: Static Analysis Design

User pointed out issues with static analysis approach:

1. **Renderer should declare `staticCapable`** - e.g., `input` is never static even with static props
2. **Bottom-up computation** - post-order traversal, compute children first, avoid redundant recursion
3. **Region handling** - all children in all regions must be static for parent to be static
4. **name binding** - any `name` in schema means data binding, not static

Corrected static analysis rules:
| Condition | Result |
|-----------|--------|
| Renderer `staticCapable: false` | Not static |
| Props contain expressions | Not static |
| Schema has `name` binding | Not static |
| Has event handlers | Not static |
| Creates scope (data/sources) | Not static |
| Any child not static | Not static |
| All pass | **Static** |

### Created Implementation Plan

Created `docs/plans/131-static-analysis-optimization-plan.md`:

- Phase 1: Type definitions (`staticCapable`, `StaticAnalysisResult`)
- Phase 2: Update all renderer definitions
- Phase 3: Compiler implementation (bottom-up computation)
- Phase 4: Testing
- Phase 5: Documentation

### Documents Updated

1. `docs/experiments/v11-final-design.md` (v1.0.0 → v1.1.0)
   - Removed `ExecutionStrategy` from TemplateNode
   - Added `RendererDefinition.staticCapable`
   - Redesigned static analysis as bottom-up post-order computation
   - Replaced Part V with Framework Adapter Layer boundary definition
   - Added Appendix C (Lessons Learned)

2. `docs/experiments/v11-design.md` (v0.2.0 → v0.2.1)
   - Added architectural warning

3. `docs/experiments/v11-lowcode-design.md` (v0.2.0 → v0.2.1)
   - Added architectural warning

4. `docs/plans/131-static-analysis-optimization-plan.md` (new)
   - Implementation plan for static analysis in flux

### Key Lessons

```
DSL Layer:
- Schema compilation (including static analysis)
- Expression evaluation, Scope, Actions, Validation

Renderer Definition:
- Declares staticCapable (renderer knows its own nature)

Compiler:
- Bottom-up static analysis (no hints needed)
- Post-order traversal for efficiency

Framework Adapter Layer:
- Uses staticAnalysis for optimization decisions
- DSL doesn't dictate how to use it
```
