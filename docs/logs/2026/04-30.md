# Development Log — 2026-04-30

### 2026-04-30 (follow-up session)

- Closed Plan 159 (Code Refactor Discovery Remediation). All phases complete:
  - P2.3 descoped: `useSyncExternalStore` on `table-quick-edit-controller.ts` creates feedback loop (internal `rowScope.update` → snapshot change → useEffect resets savedValue). `record` prop already propagates external changes.
  - P2.5: 6 adapter focused tests added to `field-utils.unit.test.tsx` covering sync (`__syncIn`), sync-via-microtask, async resolve, stale cancellation, error warning.
  - P6.2: `selection`/`target` compatibility aliases removed from `report-designer-manifest.ts`; only canonical `selectionTarget` remains.
  - P3.2: confirmed `resolveGap` dead export already absent from `flux-renderers-basic/src/index.tsx`.
  - All 3 `flux-runtime-module-boundaries.md` gaps filled (Phase 1 AbortController, Phase 3 package boundaries, Phase 6 compat cleanup).
- `pnpm typecheck && pnpm build && pnpm test` all pass across the repo.
- Link: `docs/plans/159-code-refactor-discovery-remediation-plan.md`, `packages/flux-renderers-form/src/__tests__/field-utils.unit.test.tsx`, `packages/report-designer-renderers/src/report-designer-manifest.ts`

### 2026-04-30

- Ran the first real Stryker mutation pass on the narrowed validation pilot (`pnpm audit:mutants`) after the dry-run stabilization work. The pilot completed successfully in about 20 seconds and produced a mutation score of `18.16%` (`65` killed, `39` survived, `254` no coverage, `0` errors).
- The mutation results are already useful: the current validation test slice exercises registry wiring well (`registry.ts` reached `100%`), but most surviving and no-coverage mutants are concentrated in `packages/flux-runtime/src/validation/validators.ts`, with additional low-coverage gaps in `message.ts`, `errors.ts`, and `rules.ts`.
- Cleaned one real playground dependency issue surfaced by the split `knip` pass: removed the direct `@nop-chaos/word-editor-core` dependency from `apps/playground/package.json` because the playground only consumes it transitively through `@nop-chaos/word-editor-renderers` and Vite chunking logic.
- Kept `@nop-chaos/theme-tokens` in `apps/playground/package.json` intentionally. The package is imported through CSS (`apps/playground/src/styles.css`), and the current repo preference is to retain this future-facing styling dependency instead of removing it just to satisfy `knip`'s JS/TS dependency scanner.
- Updated `knip.json` so the playground workspace ignores the known CSS-import false positive for `@nop-chaos/theme-tokens`; after that change `pnpm audit:knip:playground` runs cleanly while the package-focused scan continues surfacing real package-level cleanup candidates.
- Focused verification after the cleanup passed: `pnpm --filter @nop-chaos/flux-playground typecheck`, `pnpm --filter @nop-chaos/flux-playground build`, `pnpm audit:knip:playground`, and `pnpm audit:mutants`.
- Link to doc or code path: `apps/playground/package.json`, `apps/playground/src/styles.css`, `knip.json`, `packages/flux-runtime/src/validation/validators.ts`, `packages/flux-runtime/src/validation/registry.ts`
- Key decision: do not remove style/token dependencies such as `theme-tokens` just because `knip` cannot see CSS-side imports or because the dependency may be kept intentionally for near-future playground evolution. Remove only dependencies proven unnecessary in the live app graph.
- Next step: decide whether to spend the next pass on adding validation regression tests to kill the highest-value surviving Stryker mutants in `validators.ts`, or on reducing the broader package-level `knip` noise now that the playground slice is clean.

- Hardened the repo audit-tool baseline after the initial install pass. `package.json` now exposes split `knip` entry points (`pnpm audit:knip:packages`, `pnpm audit:knip:playground`) and `pnpm audit:mutants` now passes `@stryker-mutator/vitest-runner` explicitly instead of relying on implicit plugin discovery.
- Narrowed the active Stryker pilot from all of `packages/flux-runtime/src/**/*.ts` to the non-DOM `packages/flux-runtime/src/validation/*.ts` slice, added `packages/flux-runtime/vitest.stryker.config.ts`, and constrained Stryker to validation test files only.
- Verified the narrowed mutation baseline with `pnpm audit:mutants --dryRunOnly`: Stryker found 5 mutable files, 2 validation test files, and completed the initial dry run successfully without the earlier `document is not defined` failure.
- Added local tool-output hygiene so audit runs stop polluting repository scans: `.gitignore` and `knip.json` now ignore `.stryker-tmp/`, `stryker.log`, and `.venv/`.
- Added a compatibility fallback in `vitest.workspace.ts` so tools that evaluate the file outside Vitest's normal loader path can still consume the workspace list without crashing on `defineWorkspace`.
- Verified the split `knip` commands after the config changes. `pnpm audit:knip:playground` now produces a focused result (`apps/playground/package.json` still reports unused `@nop-chaos/theme-tokens` and `@nop-chaos/word-editor-core`), and `pnpm audit:knip:packages` now runs against package workspaces instead of silently resolving to zero package targets.
- Link to doc or code path: `package.json`, `knip.json`, `stryker.runtime.conf.mjs`, `packages/flux-runtime/vitest.stryker.config.ts`, `vitest.workspace.ts`, `.gitignore`, `docs/architecture/frontend-baseline.md`
- Key decision: treat mutation testing as an incremental pilot, not a repo-wide gate. The current success criterion is a stable, non-DOM slice with deterministic dry runs; broader mutation scope can expand later from this baseline.
- Next step: run a real non-dry Stryker mutation pass on the validation slice, then decide whether the next cleanup effort should target the remaining `knip` package noise or the concrete unused playground dependencies.

- Added repo-level code-audit tooling baselines at the workspace root: `.dependency-cruiser.cjs`, `knip.json`, `stryker.runtime.conf.mjs`, `.semgrep.yml`, and `.semgrepignore`, plus root scripts `pnpm audit:deps`, `pnpm audit:knip`, `pnpm audit:mutants`, and `pnpm audit:semgrep` in `package.json`.
- Installed new dev tools: `dependency-cruiser`, `knip`, `@stryker-mutator/core`, and `@stryker-mutator/vitest-runner`.
- Executed the new tools against the live repo baseline:
- `pnpm audit:deps` ran and reported real findings, including cross-package internal test-support imports and multiple circular dependency clusters in `flux-core`, `flux-react`, `flux-formula`, `flux-renderers-form`, and `nop-debugger`.
- `pnpm audit:knip` ran and produced a large result set (unused dependencies, unlisted workspace dependencies in `apps/playground`, unused exports/files, duplicate exports), though the current generic config still over-reports in this monorepo and needs narrowing before it can act as a CI gate.
- `pnpm audit:mutants` reached instrumentation and sandbox creation for `packages/flux-runtime`, but failed before test execution because Stryker could not load the `vitest` test-runner plugin in the current environment; trace output suggests an environment/plugin-loading incompatibility rather than a missing install.
- Attempted local `Semgrep` installation via `pip`, but the host Python 3.14 environment could not resolve/build Semgrep's dependency chain (`ruamel.yaml.clib` / `pydantic` compatibility), so `pnpm audit:semgrep` is wired but not yet executable on this machine.
- Updated `docs/architecture/frontend-baseline.md` to record the new audit-tool entry points and their current status.
- Link to doc or code path: `package.json`, `.dependency-cruiser.cjs`, `knip.json`, `stryker.runtime.conf.mjs`, `.semgrep.yml`, `.semgrepignore`, `docs/architecture/frontend-baseline.md`
- Key decision: install and keep the tools even if all of them are not yet CI-ready. `dependency-cruiser` already provides immediately useful architectural findings; `knip` and `Stryker` need repo- or environment-specific hardening before they should become enforced gates; `Semgrep` remains environment-blocked.
- Next step: narrow dependency-cruiser rules to separate production/package boundary violations from test-support exceptions, teach `knip` the workspace dependency graph more precisely, investigate Stryker Vitest plugin loading under Node 25, and run Semgrep from a supported Python/toolchain environment.

### 2026-04-30

- Fixed 8 swallowed-exception defects across 6 files in `docs/plans/160-swallowed-exception-remediation-plan.md`:
  - **Bug #1 (高)**: `packages/flux-runtime/src/async-data/formula-data-source-controller.ts:164` — `publish()` failure now calls `updateState()` setting `fetchStatus: 'idle'`, `status: 'error'`, `error`, `failureReason` instead of permanently sticking in `fetching`.
  - **Bug #2 (高)**: `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx:191` — `transformOut` failure now logs `[object-field] transformOut failed` with stale-sequence guard.
  - **Bug #3 (中)**: `object-field.tsx:140` — `transformIn` failure now falls back to rawValue and logs `[object-field] transformIn failed` with abort guard.
  - **Bug #4 (中)**: `packages/flux-renderers-form/src/field-utils.tsx:245` — `adapter.in()` failure now logs `[field-utils] adapter.in failed` with abort guard.
  - **Bug #4b (中)**: `field-utils.tsx:111,123,134` — `adapter.out()` failure in onChange now logs `[field-utils] adapter.out failed in onChange`.
  - **Bug #5 (低)**: `packages/flow-designer-renderers/src/use-designer-auto-layout.ts:76` — ELK layout failure now logs `[flow-designer] Auto-layout failed`.
  - **Bug #6a/6b (低)**: `packages/word-editor-renderers/src/editor-canvas.tsx:118` and `preview/doc-preview-page.tsx:47` — word count failure now caught silently (decorative feature).
- Enhanced deep-audit dimension 06 (`docs/skills/deep-audit-prompts.md`) with explicit swallowed-exception check patterns (empty catch blocks, `void .then()` without `.catch()`, `.then().finally()` without `.catch()`, `new Promise` executor exceptions, severity grading).
- Added feature deprecation protocol to `AGENTS.md` referencing `docs/skills/deprecated-feature-cleanup.md` (renamed from `deprecated-feature-cleanup-prompt.md`).
- Link to plan: `docs/plans/160-swallowed-exception-remediation-plan.md`
- Key decision: use `console.warn` with descriptive prefix for error logging in packages without `@types/node` (flow-designer-renderers, flux-renderers-form, flux-renderers-form-advanced); use `updateState()` for state-machine fixes in packages with direct state access (flux-runtime).
- `pnpm typecheck && pnpm build` passed; lint on modified files passed; `pnpm test` passed (word-editor-renderers/spreadsheet-renderers worker crash is pre-existing).

- Tightened shared abort semantics after the plan-158 closure work: `packages/flux-action-core/src/action-core.ts` now treats both DOM `AbortError` exceptions and existing runtime-style `{ name: 'AbortError' }` / `{ code: 'ABORT_ERR' }` objects as shared abort signals so runtime packages can consume one cancellation classifier instead of keeping a parallel helper-only rule.
- Fixed two runtime abort paths that were still inconsistent with that shared model. `packages/flux-runtime/src/async-data/reaction-runtime.ts` now treats a dispatched `{ cancelled: true }` action result as a cancelled reaction run instead of incorrectly counting it as success, and `packages/flux-runtime/src/async-data/source-registry.ts` now allocates its `AbortController` before subscribing so synchronous store subscriptions cannot trip a TDZ during data-source registration.
- Extended the same result-style cancellation alignment to async validation: `packages/flux-runtime/src/runtime-action-helpers.ts` now treats a dispatched `{ cancelled: true }` async validation result as a cancelled validation run, matching the existing thrown-abort path instead of silently treating it as a normal success-without-errors case.
- Added focused regression coverage in `packages/flux-action-core/src/__tests__/action-core.test.ts`, `packages/flux-runtime/src/__tests__/reaction-runtime.test.ts`, and `packages/flux-runtime/src/__tests__/source-registry.test.ts`, then re-ran focused `flux-action-core` / `flux-runtime` test, typecheck, and lint verification successfully.
- Added one more focused runtime regression in `packages/flux-runtime/src/__tests__/runtime-owned-helpers.test.ts` and re-ran focused runtime test/typecheck/lint after the validation helper change.
- Performed a broader runtime sweep for remaining result-style cancellation gaps across form submit, validation aggregation, source refresh, and action-monitor paths. No new concrete blocker surfaced after the latest helper/reaction/source fixes: the remaining higher-level aggregators now either consume `ValidationResult` where cancellation is already folded away downstream, or they already classify `cancelled` through the existing submit/action result helpers.
- Re-ran an expanded `flux-runtime` focused suite covering submit, validation, source refresh, and action monitor flows after the sweep, plus `flux-runtime` and `flux-action-core` typecheck, and all remained green.
- Extended the audit across neighboring packages. `flux-react` and the renderer packages do not currently maintain independent abort/cancelled interpretation paths, so they did not require changes. `packages/nop-debugger/src/adapters.ts` did have one drift: fetcher failures were still classified as aborted only for `error.name === 'AbortError'`. That logic now uses the same broader abort-like shape (`name === 'AbortError'` or `code === 'ABORT_ERR'`) already honored by runtime/action-core cancellation flows, and `packages/nop-debugger/src/adapters.test.ts` now locks that behavior in.
- Re-ran focused `nop-debugger` test/typecheck/lint verification after that cross-package consistency fix, and all passed.
- Updated architecture docs to make cancellation ownership explicit. `docs/architecture/action-scope-and-imports.md` now states that `cancelled` / `timedOut` are shared action/operation-control semantics rather than renderer/debugger-local hints; `docs/architecture/form-validation.md` now explains that validation owners consume both thrown-abort and structured cancelled-result semantics from lower helpers before folding them into validation-facing APIs; `docs/architecture/debugger-runtime.md` now clarifies that debugger abort presentation follows runtime semantics instead of defining a parallel vocabulary.
- Link to doc or code path: `packages/flux-action-core/src/action-core.ts`, `packages/flux-runtime/src/runtime-action-helpers.ts`, `packages/flux-runtime/src/action-adapter.ts`, `packages/flux-runtime/src/async-data/reaction-runtime.ts`, `packages/flux-runtime/src/async-data/source-registry.ts`
- Key decision: abort classification is a shared operation/action-core concern; runtime paths should consume the shared classifier and shared cancelled-result semantics instead of maintaining narrower local abort heuristics.
- Next step: if needed, continue auditing other runtime async owners for places that still interpret cancellation only via thrown exceptions and not via `{ cancelled: true }` results.

- Completed the post-execution closure pass for `docs/plans/158-code-quality-redundancy-and-duplication-remediation-plan.md` after a second implementation slice further reduced the remaining `flux-react/src/hooks.ts` duplication. The final pass moved repeated form/validation store resolution plus error-query / field-path subscription wiring behind local helpers while keeping `useChildFieldState(...)` as an explicit public alias.
- Final verification remained green after the last hooks cleanup: workspace `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed. A later re-run of `typecheck`/`build` used the same green code state but hit tool timeouts during long workspace execution windows; the earlier successful runs remain the recorded verification evidence for the same landed tree.
- Independent closure-audit rounds then re-checked live code, docs, and plan ownership. The first fresh audit found two real blockers: Phase 2 hook dedup was overstated, and the plan could not legally close before recording fresh closure evidence. Those blockers were resolved in the same execution pass before the final closure update.
- Link to doc or code path: `docs/plans/158-code-quality-redundancy-and-duplication-remediation-plan.md`, `packages/flux-react/src/hooks.ts`, `docs/architecture/renderer-runtime.md`, `docs/logs/2026/04-30.md`
- Key decision: keep plan 158 closed only for the narrowed residual duplication scope. Public API removals and any broader boundary changes remain outside this plan and require their own fresh consumer/boundary audit.
- Next step: no remaining plan-owned work.

- Re-audited and narrowed `docs/plans/158-code-quality-redundancy-and-duplication-remediation-plan.md` after repeated independent subagent review. The original umbrella scope was replaced with a smaller residual-cleanup plan that only keeps still-valid, low-risk duplication items in `flux-runtime`, `flux-react`, `flux-renderers-basic`, and `flux-core`.
- Landed the first implementation slice of revised plan 158: `packages/flux-runtime/src/action-adapter.ts` now delegates ajax execution to `executeRuntimeAjaxAction(...)` while preserving the adapter-level cancelled-abort result; `packages/flux-renderers-basic/src/use-surface-renderer.ts` now owns the shared declarative dialog/drawer lifecycle and `statusPath` publication flow; `packages/flux-react/src/hooks.ts` now uses one internal form-store selector helper for the remaining duplicated error/field-state subscription wiring; `packages/flux-core/src/named-action-provider.ts` now imports `XUI_ACTIONS_NAMESPACE` from `constants.ts`.
- Added a focused adapter regression test for ajax abort handling in `packages/flux-runtime/src/__tests__/action-adapter.unit.test.ts`, and refreshed architecture docs so the current live baseline now explicitly documents the declarative surface shared helper and the fact that `useChildFieldState(...)` remains an intentional public alias rather than dead code.
- Link to doc or code path: `docs/plans/158-code-quality-redundancy-and-duplication-remediation-plan.md`, `packages/flux-runtime/src/action-adapter.ts`, `packages/flux-runtime/src/runtime-action-helpers.ts`, `packages/flux-runtime/src/__tests__/action-adapter.unit.test.ts`, `packages/flux-renderers-basic/src/use-surface-renderer.ts`, `packages/flux-renderers-basic/src/dialog.tsx`, `packages/flux-renderers-basic/src/drawer.tsx`, `packages/flux-react/src/hooks.ts`, `packages/flux-core/src/named-action-provider.ts`, `docs/architecture/surface-owner.md`, `docs/architecture/renderer-runtime.md`
- Key decision: keep plan 158 narrow. Public API removals and package-owner baseline changes stay out of scope unless a fresh successor plan proves them against live consumers.
- Next step: run focused verification, then full workspace verification, then perform repeated post-execution independent audits before closure.

- Executed the first implementation slices of `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md` and narrowed the supported live owner-family set to `form`, `SchemaRenderer` page-owned root, and detail child owners (`detail-field` / `detail-view`).
- Landed compiler/runtime owner metadata for supported families: `packages/flux-core/src/types/{validation.ts,renderer-core.ts,node-identity.ts}` now carry explicit validation owner boundary metadata, and `packages/flux-compiler/src/schema-compiler.ts` now emits `validationOwnerPlan` so detail renderers consume a real owner-boundary contract instead of relying only on doc intent.
- Landed real detail child contracts in live code: `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.ts`, `detail-field.tsx`, `detail-view.tsx`, and `detail-surface.tsx` now register/unregister active child contracts against the parent validation owner and provide the draft validation owner through `ValidationContext` while the detail session is open.
- Key decision: `detail child owner` belongs to the detail value-editing semantic boundary, not to the dialog/drawer shell. The surface remains a `Surface Owner`; the detail renderer owns the staged data/validation boundary and only uses the surface as UI host.
- Updated owner docs to final-design wording for the supported scope: `docs/architecture/form-validation.md`, `docs/architecture/data-domain-owner.md`, and `docs/architecture/value-adaptation-and-detail-field.md` no longer describe detail child owners as merely renderer-local temporary exceptions in the supported paths.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-compiler test -- validation-collection.test.ts`; `pnpm --filter @nop-chaos/flux-core typecheck`; `pnpm --filter @nop-chaos/flux-compiler typecheck`; `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`; `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- --maxWorkers=1 detail-draft-controller.test.tsx`; `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- --maxWorkers=1 composite-form-detail-and-loop.test.tsx`.
- Final repo-wide verification passed after the owner-doc cleanup: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Independent closure audit result: `ses_2225c72ffffe7rkOLk8DE95DBD` first found two remaining Phase 4 gaps (`form-validation.md` still carried exception wording in compile-time collection/final decision sections, and plan/log closure evidence was not yet recorded). Those doc/log gaps were then resolved in the same execution pass.
- Closure decision: keep the supported family set explicit and narrow. Plan 157 is closed only for `form`, page-owned root, and detail child owners; generalized filter/search/wizard or row-local staged owners still require successor plans.
- Link to doc or code path: `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md`, `docs/architecture/form-validation.md`, `docs/architecture/data-domain-owner.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `packages/flux-core/src/types/validation.ts`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/validation-collection.test.ts`, `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.ts`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-surface.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/composite-form-detail-and-loop.test.tsx`
- Next step: no remaining plan-owned work in the supported scope.

- Re-audited the earlier `submitForm -> args` assumption against live source and corrected the conclusion: `submitForm` is currently a semantic built-in submit command, not an `ajax`-style payload action.
- Evidence chain: `packages/flux-core/src/types/actions.ts` defines `SubmitFormActionSchema` without `args`; `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts` dispatches `submitForm` with no payload; `packages/flux-runtime/src/action-adapter.ts` delegates directly to `ctx.form.submit(...)`.
- Updated `docs/references/action-payload-matrix.md` and `docs/references/flux-json-conventions.md` so `submitForm` is no longer described as `args: ApiSchema` target-state convergence.
- Narrowed `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md` to the real remaining implementation gap: validation owner boundary / child-contract convergence. The previous `submitForm -> args` premise was removed because it does not reflect the current authoritative contract.
- Link to doc or code path: `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md`, `docs/references/action-payload-matrix.md`, `docs/references/flux-json-conventions.md`, `packages/flux-core/src/types/actions.ts`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-runtime/src/action-adapter.ts`
- Key decision: stop treating `submitForm` as an unfinished `args` migration. If a future design wants alternative form targeting, model that explicitly instead of inheriting the `ajax` payload contract.
- Next step: continue auditing validation-owner implementation gaps separately from action payload unification.

### 2026-04-30

- Re-audited the outcomes of `docs/plans/155-architecture-owner-doc-convergence-plan.md` and `docs/plans/156-reference-doc-sync-and-audit-consensus-plan.md` against the current doc-governance rules in `docs/index.md`, `docs/architecture/README.md`, and `docs/plans/00-plan-authoring-and-execution-guide.md`.
- Conclusion: plan 155's scoped fixes still hold, but plan 156 preserved a now-incompatible exception by allowing `docs/architecture/form-validation.md` to remain a live-vs-target mixed owner doc. That does not satisfy the current repo rule that `docs/architecture/` should describe the latest final design state only.
- Confirmed remaining code-lagging areas that should be implemented rather than papered over with doc downgrades: `submitForm -> args` is still not fully landed in `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts` and `packages/flux-runtime/src/action-adapter.ts`, and validation owner docs still depend on unlanded compiler-owned boundary / child-contract semantics.
- Added `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md` to own the next implementation slice: land `submitForm -> args`, land the supported validation owner/child-contract semantics, and remove the remaining plan-scoped current-vs-target exception wording from the validation owner docs.
- Link to doc or code path: `docs/plans/157-validation-owner-and-submitform-implementation-alignment-plan.md`, `docs/index.md`, `docs/architecture/README.md`, `docs/architecture/form-validation.md`, `docs/architecture/data-domain-owner.md`, `docs/architecture/value-adaptation-and-detail-field.md`, `docs/references/action-payload-matrix.md`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-runtime/src/action-adapter.ts`
- Key decision: stop treating validation owner mixed wording and `submitForm` doc downgrades as stable end states. If these architecture decisions remain active, code now has to catch up.
- Next step: execute plan 157 as the code-owned convergence pass, and keep any larger Report Designer future-contract implementation in a separate successor plan.

### 2026-04-30

- Reduced `@nop-chaos/flux-react` test startup cost by replacing several broad test imports (`./index`, `../index`, and `test-support`) with narrower module imports such as `schema-renderer`, `hooks`, `contexts`, `test-support-core`, and `test-support-runtime`.
- Verified the slow-test diagnosis locally: `pnpm --filter @nop-chaos/flux-react test -- --reporter=verbose --maxWorkers=1` now completes cleanly (`29` files / `267` tests), confirming the issue was import/jsdom startup cost rather than an infinite loop or OOM.
- Cleared the existing workspace blockers in `@nop-chaos/flux-renderers-data` by tightening `vi.fn` promise mock typing in `packages/flux-renderers-data/src/__tests__/table-quick-edit-controller.test.tsx` and removing the stale unused import from `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx`.
- Package verification passed for `@nop-chaos/flux-renderers-data`: `pnpm --filter @nop-chaos/flux-renderers-data typecheck`, `build`, `lint`, and `test`.
- Link to doc or code path: `packages/flux-react/src/test-support-core.tsx`, `packages/flux-react/src/test-support-runtime.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-react/src/__tests__/schema-renderer-dialog-container.test.tsx`, `packages/flux-renderers-data/src/__tests__/table-quick-edit-controller.test.tsx`, `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx`
- Key decision: prefer narrower internal entry points over creating a new workspace package when test slowness is caused by overly broad imports rather than package-boundary coupling.
- Next step: rerun repo-scope verification from the updated baseline and only continue splitting `flux-react` test support if future profiling still shows import-time dominance.

### 2026-04-30

- Closed `docs/plans/155-architecture-owner-doc-convergence-plan.md` after repeated fresh post-execution audits and full repo verification.
- Final closure-audit evidence: `ses_22342d974ffef0vdxeis4rSqyT` first found two remaining plan-owned gaps (an unqualified future-doc reference in `docs/architecture/action-scope-and-imports.md` and missing CRUD validation-block regression coverage). Those were resolved by qualifying the Report Designer draft reference and adding a focused invalid-submit regression in `packages/flux-renderers-data/src/__tests__/data-crud-state-interactions.test.tsx`.
- Final independent audits then passed: `ses_2230ab8c3ffeOkAf2sjqUV7SGF` confirmed the CRUD query-submit fix is contract-safe with no remaining `packages/**` `\$form.values` usage, and `ses_2230ab8d6ffeugnf1CYh2cCUGH` confirmed all four plan phases are closed against live repo state.
- Final verification passed: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Key decision: keep plan 155 closed. Any future architecture drift outside this audited scope should move to a new analysis or successor plan instead of reopening the completed convergence slice.
- Next step: no remaining plan-owned work.

- Executed `docs/plans/156-reference-doc-sync-and-audit-consensus-plan.md` against the live repo baseline instead of the earlier audit text alone.
- Synced scoped reference docs to live code: `docs/references/terminology.md`, `renderer-interfaces.md`, `action-payload-matrix.md`, `form-validation-runtime-types.md`, `form-validation-execution-details.md`, `architecture-doc-status-matrix.md`, `complex-component-design-process.md`, `integrating-third-party-components.md`, and `refactoring-guidelines.md`.
- Narrow owner-doc cleanup landed in `docs/architecture/form-validation.md`: stale summary/example type names were removed or downgraded to target-state sketches, and the remaining live-vs-target split is now explicitly labeled as a narrow existing exception under cleanup rather than a general owner-doc policy.
- `submitForm -> args` was adjudicated as **not fully landed** in live code: `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts` still creates `submitForm` invocations with `args: undefined`, and `packages/flux-runtime/src/action-adapter.ts` still executes `ctx.form.submit(...)` directly. Plan 156 therefore downgraded docs/log wording instead of widening into runtime implementation.
- Clarified the historical log evidence in `docs/logs/2026/04-20.md` and `docs/logs/2026/04-21.md` so the earlier convergence notes are preserved as history but no longer misread as live contract evidence.
- Link to doc or code path: `docs/plans/156-reference-doc-sync-and-audit-consensus-plan.md`, `docs/references/action-payload-matrix.md`, `docs/references/form-validation-runtime-types.md`, `docs/references/form-validation-execution-details.md`, `docs/architecture/form-validation.md`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-runtime/src/action-adapter.ts`
- Key decision: keep plan 156 doc-owned. No successor runtime implementation plan was created because this execution pass only needed doc/log downgrade, not code expansion.
- Next step: run repeated independent audits plus closure audit, then mark plan 156 complete only if the updated docs, logs, and scoped exit criteria stay aligned.
- Closure audit completed: three independent subagent rounds verified all Phase 1-4 exit criteria MET and all validation checklist items SATISFIED.
  - Post-update audit 1 (`ses_222aad6f3ffeYZt1qLDbvynFd1`): PASS WITH MINOR ISSUES. All exit criteria MET. Spot-checked `RendererComponentProps`, `ValidationScopeRuntime`, `ActionContext` against live code — all match. Fictional types (`ScopeValidationResult`, `FormSubmitResult`, `FieldRegistrationState`) confirmed absent from reference docs and live code.
  - Post-update audit 2 (`ses_222a615a4ffe1rcZU7u6A35485`): PASS WITH MINOR ISSUES. `submitForm` consistency VERIFIED across docs/logs/code. AMIS framing VERIFIED removed. New finding: `architecture-doc-status-matrix.md` missing family sub-documents — pre-existing gap outside plan scope.
  - Closure audit (`ses_222a1f3ebffe9Gb3wxWZLGu7Ax`): READY FOR CLOSURE. All Phase 1-4 exit criteria re-checked and PASS. Follow-up items recorded: (1) `submitForm -> args` runtime implementation if needed, (2) family sub-doc coverage in status matrix, (3) continued tightening of `form-validation.md` live-vs-target split.
- Plan 156 marked `completed`. No remaining plan-owned work.

### 2026-04-30

- Added `docs/plans/156-reference-doc-sync-and-audit-consensus-plan.md` to own the follow-up synchronization work for the 2026-04-30 references audit.
- The new plan is intentionally narrow: it covers only the audited `docs/references/` drift, the small `docs/architecture/form-validation.md` cleanup needed to stop feeding stale types back into references, and any required clarification in `docs/logs/2026/04-20.md`, `04-21.md`, and `04-30.md` for the `submitForm -> args` landing claim.
- Re-checked the plan against `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/index.md`, `docs/architecture/README.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/form-validation.md`, and the related daily logs before freezing the plan scope.
- Independent plan-audit rounds:
  - Round 1: `ses_223110346ffe7uNm2dP8vAICkP` found 4 issues: soft `submitForm` code-scope ownership, over-broad phased-owner-doc wording, contradictory validation wording, and incomplete `Source:` provenance.
  - Round 2: `ses_2230ece1effeDYxfvvbShRU8Go` found 3 remaining issues: mutable-scope leakage through review inputs, still-too-broad `Goals` wording for owner docs, and closure-audit criteria not explicit enough.
  - Round 3: `ses_2230bad15ffeROTtVkWt3rVg1O` confirmed the revised plan is acceptable as-is with no remaining findings.
- Key decision: this plan does not silently own a `submitForm -> args` runtime implementation. If the correct conclusion after doc sync is “implement, not downgrade,” that code work must move to a narrow successor implementation plan.
- Link to doc or code path: `docs/plans/156-reference-doc-sync-and-audit-consensus-plan.md`, `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/analysis/2026-04-30-references-doc-code-consistency-audit.md`, `docs/index.md`, `docs/architecture/README.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/form-validation.md`, `docs/logs/2026/04-20.md`, `docs/logs/2026/04-21.md`
- Next step: execute plan 156 phase by phase and keep the repeated independent audit evidence log updated instead of closing the work on the plan draft alone.

### 2026-04-30

- Re-audited `docs/analysis/2026-04-30-references-doc-code-consistency-audit.md` against the actual owner-doc precedence chain (`docs/index.md` -> `docs/architecture/frontend-programming-model.md` -> `docs/architecture/flux-design-principles.md` -> narrower owner docs) plus the related daily logs.
- Corrected the adjudication model for the validation references: `docs/references/form-validation-runtime-types.md` and `form-validation-execution-details.md` are not just "docs lagging behind code". They mix live-code reference material with target/pseudotype content that belongs in `docs/architecture/form-validation.md`, while the owner doc itself still has a few stale summary/example type names to clean up.
- Tightened the `submitForm` conclusion using both logs and source: 2026-04-20 / 2026-04-21 logs claim `submitForm -> args: ApiSchema` landed, but live built-in dispatch still invokes `submitForm` with `args: undefined` and `flux-runtime` submits only through `ctx.form`, so this is a real code-vs-doc/log convergence gap.
- Expanded the complex-component finding: `docs/references/complex-component-design-process.md` is not only stale on status/CSS tokens; its "AMIS JSON is the core DSL" framing also lags behind the current Flux `Final Execution Schema` / SchemaRenderer architecture baseline.
- Link to doc or code path: `docs/analysis/2026-04-30-references-doc-code-consistency-audit.md`, `docs/index.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/form-validation.md`, `packages/flux-core/src/types/{runtime.ts,validation.ts,actions.ts,renderer-core.ts,renderer-hooks.ts}`, `packages/flux-action-core/src/action-dispatcher/built-in-actions.ts`, `packages/flux-runtime/src/action-adapter.ts`
- Key decision: reference docs must describe the latest live baseline only; phased target-vs-current discussion is allowed only in owner architecture docs when that split is made explicit.
- Next step: execute the follow-up convergence pass by rewriting `docs/references/form-validation-runtime-types.md` to the live exported types, cleaning stale summary type names out of `docs/architecture/form-validation.md`, and then deciding whether `submitForm args` should be implemented or downgraded from "LANDED" in docs/log status.

### 2026-04-30

- Refined `docs/references/renderer-implementation-guidelines.md` with a split decision table that distinguishes the main implementation patterns now used in the repo: keep-one-file, pure helper module, local controller hook, thin orchestration renderer plus shared hooks/helpers, shared family helper/runtime, and domain core.
- Key decision: make the guidance pattern-based instead of slogan-based. The docs now explicitly separate renderer-local interaction complexity from shared family lifecycle problems and cross-shell domain behavior, so `headless` is no longer used as a catch-all for all refactoring decisions.

- Updated `docs/components/table/design.md` and `docs/components/dialog/design.md` with explicit implementation-splitting guidance as contrasting examples. `table` is now documented as an orchestration-shell plus shared-hooks/helpers case, while `dialog` is documented as a surface-family shared-helper/runtime case rather than a local-controller case.
- Key decision: document multiple split patterns, not a single headless recipe. Complex renderer docs should distinguish renderer-local interaction controllers from shared family/runtime helpers so later implementations do not force every complexity problem into the same abstraction shape.

- Updated `docs/components/tree-select/design.md` and `docs/components/input-tree/design.md` with explicit implementation-splitting guidance. Both docs now state how to divide renderer/view responsibilities, shared pure tree-option helpers, and local controller-hook boundaries, using the current tree-control refactor direction as the concrete baseline.
- Key decision: document tree helper sharing before popup-shell sharing. `tree-select` and `input-tree` should share option-model helpers first, while keeping their interaction shells separate unless a later refactor proves the controller boundary is truly common.

- Updated `docs/components/index.md` to make renderer implementation split guidance explicit for component design docs. Complex component design docs should now state which logic stays in the renderer/view layer, which logic belongs in pure helpers, and when a local controller hook or lower-layer helper is the recommended implementation boundary.
- Key decision: keep this requirement in `docs/components/index.md` as a component-doc authoring rule rather than promoting it to a new architecture-wide hard mandate. The normative architecture docs still define boundary constraints, while the split decision itself continues to live in `docs/references/renderer-implementation-guidelines.md`.

- Refactored `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx` and `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx` to share a new local draft controller module `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.ts`.
- The shared controller now owns the repeated draft-form lifecycle pieces: initial draft value shaping, reserved `$form` stripping, mount-safe draft state updates, draft open/close/dispose flow, confirm lifecycle flags, and adaptation-action dispatch wiring. The renderer files keep their distinct viewer layout, writeback semantics, and surface composition.
- Added focused tests in `packages/flux-renderers-form-advanced/src/detail-view/detail-draft-controller.test.tsx`, while existing detail-field/detail-view suites continue to cover transform, validation, commit, owner update, and unmount-safety behavior.
- Verification for the refactor itself passed: `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- detail-draft-controller.test.tsx detail-field.test.tsx detail-view.test.tsx`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, plus repo-scope `pnpm lint`.
- Key decision: keep writeback ownership split. `detail-field` still owns field writeback into the current form field, while `detail-view` still owns patch/updates/object commit application against scope or form state. Only the draft-session controller flow was shared.
- Next step: if the repo wants to continue this pattern, the remaining candidates are lower priority and should be judged by maintenance pain rather than mechanically sweeping every control.

- Refactored `packages/flux-renderers-data/src/table-renderer/table-quick-edit-cell.tsx` into a thinner view layer plus `packages/flux-renderers-data/src/table-renderer/table-quick-edit-controller.ts`, applying the same local-controller pattern used for the earlier tree-control cleanup.
- Moved quick-edit control state and save-flow behavior into the controller hook: dirty tracking, draft/saved value state, custom-body dirty capture, dialog open/close restore semantics, inline value writeback, and save lifecycle now live outside the JSX file while the view keeps dialog/inline structure and renderer composition.
- Added focused controller tests in `packages/flux-renderers-data/src/__tests__/table-quick-edit-controller.test.tsx` and kept the existing unit + CRUD integration coverage (`table-quick-edit-cell.unit.test.tsx`, `data-crud-quick-edit.test.tsx`) as regression protection for dialog, blur-save, fallback action, and custom-body paths.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-data test -- table-quick-edit-controller.test.tsx table-quick-edit-cell.unit.test.tsx data-crud-quick-edit.test.tsx`, `pnpm typecheck`, `pnpm build`, `pnpm lint`.
- Key decision: keep `resolveTableQuickEditConfig()` in `table-quick-edit-cell.tsx` as the stable public entry used by existing tests/callers, while moving only runtime interaction state into the controller module. This preserves external shape and avoids unnecessary file churn.
- Next step: the next strong local-controller candidates are `detail-field.tsx` and `detail-view.tsx`, where draft-form lifecycle and transform/validate/commit flow are still mixed with surface rendering.

- Applied the new renderer implementation guideline to a live control by refactoring `packages/flux-renderers-form-advanced/src/tree-controls.tsx` into a thinner view layer plus a new local controller module `packages/flux-renderers-form-advanced/src/tree-control-controllers.ts`.
- Moved tree-control interaction concerns into local hooks without changing external renderer contracts: node expand/select keyboard handling, searchable filter projection, and tree-select trigger label derivation now live in controller hooks, while the renderer file keeps `RendererComponentProps` wiring, `@nop-chaos/ui` composition, and slot markup.
- Added focused regression coverage in `packages/flux-renderers-form-advanced/src/tree-control-controllers.test.tsx` for source error normalization, searchable parent-preserving filtering, and joined trigger labels / placeholder fallback.
- Verification: `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- tree-control-controllers.test.tsx form-tree-control-source-states.test.tsx form-tree-checkbox-fields.test.tsx form-double-edit-regression.test.tsx`, `pnpm typecheck`, `pnpm build`, `pnpm lint`.
- Key decision: keep the extraction renderer-local. The new controller module is an internal implementation detail, not a new public renderer protocol, and tree option pure helpers remain in `tree-options.ts` rather than being wrapped in unnecessary React hooks.
- Next step: use this tree-control refactor as the reference slice for the next local-controller candidate, likely `table-quick-edit-cell.tsx` or the detail-view draft controllers.

- Added `docs/references/renderer-implementation-guidelines.md` as a focused implementation guide for renderer authors. It clarifies when to keep a renderer in one file, when to extract pure helpers, when to extract a local controller hook, and when logic should move to domain core instead.
- The guide intentionally lives in `docs/references/` rather than `docs/architecture/` because it is an implementation-structure playbook inside existing owner-doc boundaries, not a new top-level architecture contract.
- Updated `docs/index.md` routing so renderer refactors that need a single-file vs local-controller vs domain-core decision now have a dedicated first-stop doc.
- Key decision: do not turn local controller extraction into a platform-wide headless mandate. The default remains minimal renderers plus standard `RendererComponentProps` and existing runtime hooks; local controller hooks are a narrow tool for behavior-heavy controls such as `tree-controls` and `table-quick-edit-cell`.
- Next step: use the new guide when touching the next behavior-heavy renderer, and only promote any rule into `docs/architecture/` if repeated implementation experience shows it has become a stable contract rather than a refactoring preference.

- Fixed pagination duplicate page number bug in `packages/flux-renderers-data/src/table-renderer/table-pagination-bar.tsx`.
- Root cause: the middle-section page generation used clamping (`if (page < 1) page = 1`) which produced duplicate numbers at edges (e.g., page 1 of 20 → `[1, 1, 2]`, page 20 of 20 → `[19, 20, 20]`).
- Fix: extracted `computePaginationPages()` using a Set-based approach that shifts the window instead of clamping, guaranteeing unique sorted page numbers. Simplified rendering to iterate computed pages with automatic ellipsis insertion for gaps.
- Added `whitespace-nowrap` to the "1-50 of 1000" row info text to prevent wrapping.
- Added 14 unit tests in `packages/flux-renderers-data/src/__tests__/table-pagination-pages.test.ts` covering all edge cases (first/last page, duplicates invariant, sorted invariant).
- Key decision: compute pages via Set + sorted array rather than conditional JSX branches, eliminating the class of overlap bugs entirely.
- Next step: visual verification in playground that the pager no longer shows duplicate numbers or text wrapping.

- Fixed the playground performance page `Scenario B: Nested loop card list` so it now renders the intended card rows plus nested child lines instead of effectively collapsing to the later editable form block during live inspection.
- Updated `apps/playground/src/pages/performance-table/schema.ts` to follow the live `loop` authoring contract with explicit `itemName` bindings and `$slot.*` access for both outer and inner loops; this matches the existing Component Lab loop examples and avoids relying on unsupported bare `item.*` expressions.
- Added e2e regression coverage in `tests/e2e/performance-table.spec.ts` that asserts Scenario B content appears in `Full Stress`, disappears in `Table Only`, and comes back when switching modes again.
- Verification: `pnpm exec playwright test tests/e2e/performance-table.spec.ts --reporter=list`, `pnpm typecheck`, `pnpm build`, `pnpm lint`.
- Key decision: keep the fix schema-local instead of changing runtime loop compatibility behavior; the current repo baseline already documents loop item access through explicit binding names / `$slot`.
- Next step: if more playground fixtures still use bare `item.*` loop access, align them the same way rather than introducing a second authoring convention.

- Executed Phase 3 of `docs/plans/155-architecture-owner-doc-convergence-plan.md` and removed the remaining package-level `${$form.values}` contract violation from `packages/flux-renderers-data/src/crud-renderer.tsx`.
- Reworked CRUD query submit ownership so `packages/flux-renderers-data/src/crud-renderer-ownership.ts` now validates and reads query-form values through the registered form handle (`validate` + `getValues`) and then updates CRUD-owned query state directly, instead of relying on `$form` as a values object or double-submitting through the embedded form runtime.
- Focused regression verification passed: `pnpm --filter @nop-chaos/flux-renderers-data test -- data-crud-state-interactions.test.tsx data-crud-request-owned.test.tsx data-crud-source-runtime.test.tsx data-crud-rendering.test.tsx`.
- Repo-scope evidence: `packages/**` search for `\$form.values` now returns no matches.
- Key decision: keep the query form as a normal embedded form for field state and validation, but keep query-state ownership in CRUD so the search button follows one submit path and the `$form` reserved-binding contract remains summary-only.
- Next step: run full repo verification (`pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm test`) and then complete the fresh post-execution closure audit for plan 155.

- Completed a repo-wide `docs/architecture/` vs live code consistency audit and saved the synthesized result to `docs/analysis/2026-04-30-architecture-doc-code-consistency-audit.md`.
- Audit consensus was reached after repeated independent subagent review (`ses_223d55629ffe4Pw94JjtdK6xEj`, `ses_223d55619ffeb6W7A23UQ5pIVg`, `ses_223d555f5ffe9PNH1vDZXoqnch`, `ses_223d55591ffet9DtqLEBC4gY7q`, `ses_223ccd29dffeIgMgp1tz3CITbE`, `ses_223ccd218ffeDGGzozwsF9JEwI`, `ses_223c865f8ffe3oLOCL7IxHVMca`). The main confirmed gaps are: `word-editor` document-model doc drift, Report Designer family-doc drift around renderer schema shape / owner-doc precedence, styling owner-doc conflict around root markers vs shipped default-spacing CSS, several stale Flow/Playground live-behavior claims, and lag in `docs/references/architecture-doc-status-matrix.md`.
- Key evidence paths recorded in the audit include `docs/architecture/word-editor/design.md`, `docs/architecture/report-designer/design.md`, `docs/architecture/report-designer/config-schema.md`, `docs/architecture/renderer-markers-and-selectors.md`, `docs/architecture/flow-designer/api.md`, `docs/architecture/flow-designer/runtime-snapshot.md`, `packages/word-editor-core/src/template-model.ts`, `packages/report-designer-renderers/src/types.ts`, `packages/flux-react/src/default-spacing.css`, `packages/flow-designer-renderers/src/designer-command-adapter.ts`, `packages/flow-designer-renderers/src/designer-context.ts`, and `apps/playground/src/route-model.ts`.
- Refined the audit to explicitly judge whether each gap means `docs lagging behind code`, `code lagging behind docs`, or `mixed-owner drift`, using `docs/architecture/frontend-programming-model.md`, `docs/architecture/flux-design-principles.md`, `docs/architecture/flux-dsl-vm-extensibility.md`, and `docs/architecture/complex-control-host-protocol.md` as the precedence chain. Final independent adjudication consensus was reached via `ses_223c19f0effexRkdC879a4G86a`, `ses_223c19f03ffePk7WK9x5DnTgKs`, and `ses_223be7684ffefTkf0h481fYP8V`.
- Next step: use `docs/analysis/2026-04-30-architecture-doc-code-consistency-audit.md` as the convergence baseline for follow-up doc fixes and any paired code/doc cleanup passes.

- Independent closure audit for `docs/plans/154-complex-control-code-doc-convergence-implementation-plan.md` passed via fresh subagent review `ses_22413d2c4ffetva7mhJ1BpjOge`. The audit re-checked owner docs against live source/test paths and confirmed the Report Designer inspector precedence (`byProfile -> byTarget -> body -> explicit empty state`), `selectionTarget` as the canonical host field with `selection` / `target` left only as compatibility aliases, the default `report-inspector-shell` runtime path, and the Word Editor save/back host-action behavior without page-local `window.confirm`.
- Evidence paths: `packages/report-designer-core/src/runtime/inspector-panels.ts`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/report-designer-inspector.tsx`, `packages/report-designer-renderers/src/inspector-shell-renderer.tsx`, `packages/report-designer-renderers/src/host-data.ts`, `packages/word-editor-renderers/src/word-editor-page.tsx`, plus focused tests in `packages/report-designer-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/report-designer-inspector.test.tsx`, `packages/flow-designer-renderers/src/designer-page-shell.test.tsx`, and `packages/word-editor-renderers/src/__tests__/word-editor-page.test.tsx`.
- Key decision: keep plan 154 closed. The only remaining issue found in the closure pass is non-blocking repo hygiene drift in stale `dist/` artifacts that still mention pre-convergence inspector symbols; that cleanup does not change the live source contract and should be handled separately if needed.
- Next step: no remaining plan-owned work for 154; only optional cleanup is removing stale generated `dist/` residue in a separate narrow pass.

- Executed `docs/plans/154-complex-control-code-doc-convergence-implementation-plan.md` and landed the code-side convergence for complex-control host boundaries. Report Designer inspector runtime now follows the DSL-first path (`byProfile -> byTarget -> body -> explicit empty state`) instead of the legacy provider/panel model, and the old public inspector-provider exports/helpers were removed from `@nop-chaos/report-designer-core` and its renderer consumers/tests.
- Updated code paths: `packages/report-designer-core/src/types.ts`, `packages/report-designer-core/src/core.ts`, `packages/report-designer-core/src/adapters.ts`, `packages/report-designer-core/src/__tests__/*`, `packages/report-designer-renderers/src/page-renderer.tsx`, `packages/report-designer-renderers/src/report-designer-inspector.tsx`, `packages/report-designer-renderers/src/renderers.integration.test.tsx`, `packages/report-designer-renderers/src/bridge.test.ts`, `apps/playground/src/pages/report-designer-demo.tsx`.
- Hardened the shared host checkpoints without changing the owner-doc baseline: Word Editor save now goes through the `word-editor` namespace/provider path from the page component, page-local `window.confirm` leave handling is gone, and new renderer tests lock `statusPath`, override-region host scope, save shortcut wiring, and direct `onBack` host callback behavior. Flow Designer and Spreadsheet renderer tests were extended to cover host-scope/statusPath facts plus the edge-selection fallback qualifier.
- Verification passed after reconciliation work: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all completed successfully.
- Key decision: for this convergence pass, `selectionTarget` remains the canonical report-designer selection field, `selection`/`target` stay only as documented compatibility aliases, and the runtime no longer treats `InspectorProvider` / `InspectorPanelDescriptor` / `InspectorValueAdapter` / `providerIds` / `panelIds` as public contract surface.
- Next step: record the independent closure-audit evidence for plan 154 after the fresh audit pass, then close any remaining compatibility-only cleanup as a separate narrow plan if needed.

- Refined execution plan `docs/plans/154-complex-control-code-doc-convergence-implementation-plan.md` after independent subagent review: added owner-doc precedence, fixed Report Designer inspector schema resolution order (`byProfile -> byTarget -> body -> empty state`), narrowed Phase 1 to mostly test hardening plus the remaining Word Editor save/leave-guard convergence, and made the Report Designer public-API cleanup rules explicit.
- Added successor execution plan `docs/plans/154-complex-control-code-doc-convergence-implementation-plan.md` to drive live code into full conformance with the newly aligned host-protocol and inspector DSL-first docs.
- Key decision: the new code plan is intentionally narrower than the earlier broad platform-convergence work; it treats Flow/Spreadsheet/Word as checkpoint-hardening work and makes Report Designer inspector runtime migration the main implementation focus.
- Aligned shared host-protocol, Flow Designer, and Report Designer owner docs around the same baseline: Flux remains the execution/runtime core, while complex-control families reuse host boundaries, host projection, namespaced actions, `WorkbenchShell`, and `statusPath` publication.
- Link to docs: `docs/architecture/complex-control-host-protocol.md`, `docs/architecture/flow-designer/design.md`, `docs/architecture/flow-designer/config-schema.md`, `docs/architecture/flow-designer/api.md`, `docs/architecture/flow-designer/runtime-snapshot.md`, `docs/architecture/report-designer/design.md`, `docs/architecture/report-designer/config-schema.md`, `docs/architecture/report-designer/api.md`, `docs/architecture/report-designer/contracts.md`, `docs/architecture/report-designer/inspector-design.md`, `docs/architecture/report-designer/nop-report-profile.md`
- Key decision: inspector is not a second architecture model; the normative path is plain Flux `SchemaInput` + form runtime + action-based writeback, and any live provider/panel structures in report-designer code are documented only as implementation lag / compatibility detail.
- Key decision: Flow Designer docs now consistently record `statusPath`, `config.toolbar.items` vs `designer-page.toolbar` boundary, renderer-owned default palette/canvas UI, region-scoped host fields (`doc`, `selection`, `activeNode`, `activeEdge`, `runtime.*`), and the current gap where edge inspector schema is designed ahead of renderer support.
- Closure audit: independent review task `ses_2245411bdffeBADAIH75eQtYKP` passed after re-checking the final docs; evidence confirmed the shared host protocol wording, Flow Designer toolbar/statusPath alignment, and Report Designer DSL-first inspector wording all match the intended baseline.
- Next step: if the repo later migrates report-designer live code away from provider/panel legacy structures, track that in a separate successor plan instead of reopening this doc-alignment plan.
