# 维度 11：UI 组件使用合规性 — 审计报告

## 第 1 轮（初审）

### [维度11-01] condition-builder value-input 使用原生 input (中)

- **文件**: `packages/flux-renderers-form-advanced/src/condition-builder/value-input.tsx`
- **现状**: 广泛使用原生 `<input>`（68,93,143,211,260,346,362 行）
- **建议**: 使用 `Input` from `@nop-chaos/ui` 以获得一致的 ARIA、样式和禁用处理

### 导入架构干净

- 所有 `@base-ui/react` 导入仅存在于 `packages/ui/src/components/ui/` ✅
- 无 renderer 直接依赖 `@base-ui/react` 或 `@radix-ui` ✅

### 豁免项正确

- spreadsheet `<table>/<thead>/<tbody>`: styling-system.md §650-695 明确豁免 ✅
- ui 包内部原生 HTML: 设计如此 ✅

# 维度 12：表单字段与 Slot 建模 — 审计报告

## 第 1 轮（初审）

### 字段分类完整性审计通过 ✅

### [维度12-01] data-source renderer 缺少 fields 声明 (低)

- **文件**: `packages/flux-renderers-data/src/data-renderer-definitions.ts:145-150`
- **现状**: 无 fields 声明，编译器将所有 schema 字段分类为默认 prop
- **建议**: 补充 fields 声明

### Slot 分类全部正确

- region 只读 slot: body, header, footer, actions, toolbar, items
- value-or-region: title, empty, loadingSlot
- 参数化 region: loop.body, recurse.body, table.columns[].item

# 维度 16：文档-代码一致性 — 审计报告

## 第 1 轮（初审）

### [维度16-01] 陈旧行号引用 (低)

- **文件**: `action-scope-and-imports.md:1192`、`report-designer/design.md:127`、`flow-designer/design.md:117`
- **建议**: 使用函数/符号名代替行号

### [维度16-02] field-metadata-slot-modeling.md 类型位置过时 (低)

- **文件**: `field-metadata-slot-modeling.md:89-93`
- **现状**: SchemaFieldRule 已在 `types/renderer-core.ts` 而非 `index.ts`
- **建议**: 更新锚点

### AGENTS.md 路由表验证：全部通过 ✅

# 维度 17：命名与术语一致性 — 审计报告

## 第 1 轮（初审）

### 关键术语一致性全部通过 ✅

ScopeRef、RendererRuntime、RendererComponentProps、RendererDefinition、TemplateNode、FormRuntime、value-or-region 等均一致

### [维度17-01] dataPath 遗留字段 (低)

- **文件**: `packages/flux-code-editor/src/types.ts:191`
- **现状**: 遗留 dataPath 字段，有 JSDoc 注释
- **判定**: 已文档化的向后兼容载体，可接受

### [维度17-02] 表格子组件文件命名不一致 (低)

- **现状**: table-body-row-rendering.tsx, table-quick-edit-cell.tsx, table-quick-edit-controller.tsx 等命名模式不统一
- **建议**: 非缺陷，但产生可发现性摩擦

### 函数命名模式全部通过（create*、register*、use*、is*/has\*）✅

# 维度 18：跨包模式一致性 — 审计报告

## 第 1 轮（初审）

### 渲染器注册模式一致 ✅

所有包使用 XxxRendererDefinitions: RendererDefinition[] + registerRendererDefinitions()

### Domain Core/Renderers 分层一致 ✅

flow-designer、report-designer、spreadsheet、word-editor 均使用 hostContract 模式

### Hook 使用模式一致 ✅

所有渲染器从 `@nop-chaos/flux-react` 导入 hooks，无直接 store 访问

### 国际化一致 ✅

所有用户可见字符串使用 `t()` from `@nop-chaos/flux-i18n`，无硬编码

# 维度 20：可访问性 (WCAG) — 审计报告

## 第 1 轮（初审）

### ARIA 属性覆盖全面 ✅

aria-label、aria-required、aria-describedby、aria-disabled、aria-live、aria-expanded、aria-selected 均正确使用

### [维度20-01] 缺少验证后焦点导航 (中)

- **现状**: 验证错误通过 `aria-live="assertive"` 播报，但键盘焦点不自动移到第一个错误
- **判定**: Plan 195/226 已规划此修复

### 键盘可操作性良好 ✅

表格行、树节点、图表、电子表格表头、条件构建器均有键盘操作模式

### 语义化 HTML 良好 ✅

`role="tree"`、`role="grid"`、`role="alert"`、`role="status"`、`role="tab"`、`role="dialog"` 均正确使用
