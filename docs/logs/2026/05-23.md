# Development Log - 2026-05-23

### 2026-05-23

- Closed `docs/plans/431-deep-audit-2026-05-23-maintenance-surface-remediation-plan.md` after the final verification pass and independent closure audit. The last retained blocker (`14-03`) was finished in `packages/report-designer-renderers/src/field-panel-renderer.test.tsx` by replacing the module-top mutable Flux React mock state container with per-test `vi.fn()` hook stubs.
- Final verification for Plan `431` is now green on the live repo: `pnpm check:oversized-code-files` reports `0 errors` for the plan-owned hard gate, and `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Independent closure audit `ses_1ad9490ebffe1eY6TrpV8VZ8Fu` passed and confirmed there are no remaining in-scope live blockers; `docs/plans/431-deep-audit-2026-05-23-maintenance-surface-remediation-plan.md` is now marked `completed`.

### 2026-05-23

- Executed `docs/plans/433-open-ended-adversarial-review-2026-05-23-report-designer-host-projection-and-manifest-plan.md`, `434-open-ended-adversarial-review-2026-05-23-flow-designer-tree-recovery-plan.md`, and `435-open-ended-adversarial-review-2026-05-23-spreadsheet-search-host-contract-plan.md` after re-verifying each residual against live code and confirming they do not reopen completed Plans `409`, `428`, or `392`.
- Fixed Report Designer host-contract residuals in `packages/report-designer-renderers/src/{host-data.ts,report-designer-manifest.ts}` and focused tests: top-level `activeSheet` now mirrors the active-sheet baseline instead of a selection-target subset, and nested `spreadsheet.selection` manifest publication now exposes the structured row/column/cell/range/sheet vocabulary already emitted by runtime host data.
- Fixed Flow Designer tree recovery residual `R23-02` in `packages/flow-designer-core/src/core.ts` and `core.test.ts`: tree-mode `save()` now snapshots the paired owner `TreeDocument`, and `restore()` replays owner tree plus projected graph together instead of restoring only the graph baseline.
- Fixed Spreadsheet search host-contract residual `R23-04` in `packages/spreadsheet-renderers/src/spreadsheet-manifest.ts` and `src/__tests__/schema-integration.test.tsx`: the published search option vocabulary now matches live runtime/provider semantics (`matchWholeCell` / `useRegex`), and the stale `wholeCell` / `includeFormulas` surface is no longer accepted as the public contract.
- Updated owner docs to the new live baseline in `docs/components/report-designer-page/design.md`, `docs/architecture/report-designer/{design.md,api.md,inspector-design.md,contracts.md}`, `docs/components/designer-page/design.md`, `docs/architecture/flow-designer/{tree-mode.md,api.md,runtime-snapshot.md}`, and `docs/components/spreadsheet-page/design.md`.

### 2026-05-23

- Executed `docs/plans/431-deep-audit-2026-05-23-maintenance-surface-remediation-plan.md` across all four in-scope code/doc surfaces owned by this plan.
- Fixed the active deep-audit handbook drift in `docs/skills/deep-audit-prompts.md`: the archive example now lists all `20` dimension files, including `19-error-propagation.md` and `20-accessibility.md`, so the handbook no longer publishes an 18-file example while claiming a 20-dimension audit.
- Split the three oversized hard-fail test files back to owner-aligned surfaces: Flow Designer tests now live in `designer-page-status.test.tsx`, `designer-page-failures.test.tsx`, and `designer-page-rendering.test.tsx`; Report Designer tests now live in `page-renderer-shell.test.tsx`, `page-renderer-init.test.tsx`, and `page-renderer-host-projection.test.tsx`; compiler analyze coverage now lives in `schema-compiler-shape-validation-action-source.test.ts`, `schema-compiler-shape-validation-region.test.ts`, and `schema-compiler-shape-validation-value-shape.test.ts`.
- Removed the module-top mutable mock leak pattern from `packages/report-designer-renderers/src/field-panel-renderer.test.tsx` by switching the mocked `@nop-chaos/flux-react` hook state to a hoisted per-test state container reset in `beforeEach`, so mocked hooks no longer close over cross-case shared module variables.
- Contained `scope-debug` subscription cost in `packages/flux-renderers-basic/src/scope-debug.tsx`: it now stays collapsed by default, only subscribes/serializes while expanded, and supports explicit `dataPaths` for narrower subscription input. Existing component-lab/perf pages that intentionally rely on live debug output now opt into `defaultExpand: true` explicitly.
- Closed the spreadsheet canvas styling drift in `packages/spreadsheet-renderers/src/canvas-styles.css` by anchoring the previously bare `spreadsheet-*` `data-slot` selectors back under `.nop-spreadsheet-page [data-slot='spreadsheet-grid']` or `[data-slot='report-designer-spreadsheet-canvas'] [data-slot='spreadsheet-grid']`.
- Owner-doc adjudication for this plan: `scope-debug` and spreadsheet canvas changes restore live code to the existing contracts already described in `docs/architecture/performance-design-requirements.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/report-designer/spreadsheet-canvas-css.md`, and `docs/architecture/styling-system.md`, so no architecture doc text change was required. The optional handbook structure-check script remains a non-blocking follow-up, not a closure requirement.
