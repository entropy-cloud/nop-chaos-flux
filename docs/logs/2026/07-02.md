# Development Log - 2026-07-02

### 2026-07-02

- **flux-guide CRUD 准确性修复**：核查发现 `flux-guide/` 对 CRUD 的描述与真实源码严重漂移——示例用了 CRUD **不存在**的 `api` / `perPage` 字段，类型定义只覆盖真实接口（~50 字段）的 6 个。照文档写的 CRUD 无法取数。
  - 根因：`flux-types/schema.d.ts` 是手工提取的精简副本，非源码生成，已与 `packages/flux-renderers-data/src/crud-schema.ts` 漂移；README 却宣称其为"绝对准确的知识源"。
- 修正内容（均经测试用例与设计文档交叉验证）：
  - `flux-types/schema.d.ts`：`CrudSchema` 从 6 字段补全到 ~50 字段（对齐 `crud-schema.ts:133`），新增 `CrudColumnSchema`/`CrudQueryFormConfig`/`CrudSelectionConfig`/`CrudQuickEditConfig`/`CrudColumnFilterConfig`/`CrudClientModeConfig`/`CrudPaginationConfig`/`CrudResponsiveConfig` 等子接口；`index.ts` 导出这些子接口。删除虚构的 `api`/`perPage`。
  - `design-patterns/crud.md`：重写为可运行示例——`source` 路径（data-source + `refreshSource`，推荐）与 `loadAction` 路径；补充 `$crud` 摘要、`queryForm`、selection + selection-aware `listActions`、cards/list `listMode`、常用能力表（快编/列设置/响应式/轮询/折叠查询/无限滚动）。
  - `01-quickstart.md` #3：去掉 `api`/`perPage`，改为 data-source + `source` + `onRefresh`/`refreshSource`，`footerToolbar` 用结构化对象而非字符串数组。
  - `02-reference.md`：数据来源表加 `crud` 的 `source`/`loadAction`；组件通信示例从错误的 `refreshTable`+`api` 改为 `refreshSource`/`component:refresh`。
  - `README.md`：保留"绝对准确的知识源"权威定位，补一句"修改 schema 时须同步 flux-types 以保持一致（定期维护）"，把维护责任写明确。
- 关键事实记录（供后续校验）：
  - CRUD 取数两路径：`source`（消费 `{items,total}`，推荐请求下沉 data-source）/ `loadAction`（自带编排，接收 pagination/query/sort/filters 绑定）。
  - CRUD 无 `api` 字段；`perPageField` 已被 `data-schema-validation.ts:231` 明确拒绝，改用 `pageSizeField`。
  - `$crud` 摘要字段：`itemCount`/`total`/`query`/`selectionCount`/`hasSelection` 等（见 `crud-renderer-definition.ts:424` scopeExportContracts）。
- 验证：`npx tsc -p flux-guide/tsconfig.json` 退出码 0（含新增子接口导出）。
- 仅文档变更，无 CODE 改动；未跑 typecheck/build/lint（flux-guide 不在根 tsconfig references 内，独立可编译）。

### 2026-07-02（续）— flux-guide 全量准确性多轮审查

用户要求"用独立子 agent 反复核查改进 flux-guide 直到达成共识"。执行 4 轮 fresh-context 独立审查（每轮对照真实源码），发现问题逐轮收敛：R1~17 项系统性错误 → R2 3 项 → R3 2 项 → R4 0 项（达成共识）。

- **R1 发现 + 修复**：除 CRUD 外，`flux-types/schema.d.ts` 与几乎所有组件接口漂移，且 markdown 示例遍布错误字段。
  - `schema.d.ts` **全文重写**，对齐各 `packages/flux-renderers-*/src/*-schemas.ts` + `flux-core/src/types/schema.ts`：Tabs(`items`/`value`/`valueOwnership`，非 `tabs`/`activeKey`)、Button(`variant`，非 `level`)、Table(`rowSelection`/`pagination{enabled,currentPage,pageSize}`，非 `selectable`/`api`)、List(`items`+`item`)、Cards(`items`+`card`)、Service(`body`/`data`/`items`/`statusPath`)、Tree(`data`/`node`/`childrenKey`)、Pagination(`pageSize`/`mode`)、Form(`initAction`/`rules`，非 `initApi`/`validations`)、Combo(`reorderable`/`minItems`，非 `draggable`/`scaffold`)、input-file(`maxFiles`+`uploadAction`，非 `maxSize`)、Layout 全套(Collapse/Steps/Timeline/Wizard/Grid/ButtonGroup)。
  - `index.ts` 同步：`QrcodeSchema`→`QrCodeSchema`，month/quarter/year 合并为 `InputPeriodSchema`。
  - markdown 示例系统性修正（quickstart/reference/design-patterns/mobile）：Button `level`→`variant`、`component:method` `args._target`→顶层 `componentId`、`refreshSource`/`refreshTable` 顶层 `targetId`/`componentId`、data-source `action` 字符串+同级 args（非嵌套对象）、data-source 不渲染故 body 改兄弟节点、Page 无 `initApi`、select/cards 用 `options`/`items`（非 `source`）、Tabs `items`、wizard `onStepCommit`/`onComplete`（非 `submitAction`）、过滤器去 includes/match 加 ARRAYINCLUDES/CONTAINS/ISARRAY/MOD/CONCAT。
- **R2 修复**：`pull-refresh.md` 基础用法遗漏（`refreshTable`+`list.api`→data-source+`refreshSource`）、`02-reference` data-source `action` 对象→字符串、内联 JSON 括号修复；LOW amis 残留清理（§5 `validations` map→字段属性+表单 `rules`、§2 顶层 `api`→action args）。
- **R3 修复**：`infinite-scroll.md` `setValues`+`args.value`→`setValue`（`setValues` 需 `args.values` 对象，见 `action-core.ts:361`）；表单 `rules` 取值从误列 9 种瘦身为 `equalsField`/`notEqualsField`（见 `FormCrossFieldRuleKind` + `form-rules.ts`）。
- **R4**：13 项扫描清单 0 命中，95 个 JSON 块括号全平衡 → **达成共识**。
- 验证：每轮修复后 `npx tsc -p flux-guide/tsconfig.json` 退出码 0。
- 仍仅文档变更。`flux-types` 是手工维护的权威知识源（README 已注明须与源码同步维护）；本次将其全面对齐源码，是"定期维护"的一次执行。
