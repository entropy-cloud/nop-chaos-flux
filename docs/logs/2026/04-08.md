### 2026-04-08

- Closed the React 19 migration verification loop: workspace `pnpm check:react19`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` all passed after the `flux-react` import lifecycle fix and test-file split, so the repository-level hang is no longer blocking the migration plan.
- Recorded codemod audit evidence: `types-react-codemod` `preset-19` dry-run reported `960 unmodified / 4 ok / 0 errors`, while the official `codemod@latest react/19/migration-recipe --dry-run` entrypoint could not start on this Windows host because the optional `@codemod.com/cli-win32-x64-msvc` package was unavailable.
- Link to code/docs: `docs/plans/43-react-18-to-19-best-practices-migration-plan.md`, `packages/flux-react/src/useNodeImports.ts`, `packages/nop-debugger/src/panel.tsx`
- Key decision: treat the Windows codemod launcher failure as a tool-distribution limitation, not a remaining repository migration gap, because AST audit, lint guardrails, and full test verification already show the codebase is operating on a React 19-only baseline.
- Next step: no further React 19 closure work is required for this plan; future use of APIs such as `useEffectEvent` or `startTransition` should be handled as normal ROI-driven feature work instead of plan debt.

### 2026-04-08

- Fixed the `flux-react` import-binding render loop that surfaced while splitting `packages/flux-react/src/index.test.tsx`: `useNodeImports` now keeps imported expression bindings versioned and local to the owning node subtree, and import readiness no longer triggers `page.refresh()`, so import effects no longer thrash across the whole page on every readiness transition.
- Link to code/tests: `packages/flux-react/src/useNodeImports.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/schema-renderer-imports-basic.test.tsx`
- Key decision: imported namespace readiness should only rerender the node subtree that owns the import instead of forcing `page.refresh()`, because page-wide refreshes create avoidable import re-registration churn and obscure test/runtime isolation.
- Next step: rerun `@nop-chaos/flux-react` package tests and then retry workspace-level verification to confirm the oversized-test split no longer blocks React 19 plan execution.

### 2026-04-08

- Split `packages/flux-react/src/index.test.tsx` into focused test files so `flux-react` no longer relies on one oversized cross-domain Vitest file for renderer runtime, imports/provider boundaries, frame/slot helpers, and reactive dialog behavior.
- Link to code/tests: `packages/flux-react/src/test-support.tsx`, `packages/flux-react/src/schema-renderer-runtime.test.tsx`, `packages/flux-react/src/schema-renderer-imports-basic.test.tsx`, `packages/flux-react/src/schema-renderer-imports-boundaries.test.tsx`, `packages/flux-react/src/schema-renderer-imports-errors.test.tsx`, `packages/flux-react/src/frame-slot-meta.test.tsx`
- Key decision: keep a shared test-support module for render helpers and common probe renderers, but split execution units by behavior domain so hangs or slow imports are easier to isolate and package tests can be run in smaller batches.
- Next step: finish validating each split import-related test file independently and only then retry the package-wide `@nop-chaos/flux-react` test command.

### 2026-04-08

- Executed the first React 19 migration guardrail slice from `docs/plans/43-react-18-to-19-best-practices-migration-plan.md`: `eslint.config.js` now blocks legacy React DOM imports/property access, `package.json` adds `pnpm check:react19`, and `lint` now runs the React 19 audit script before package lint.
- Added the AST-based workspace audit script `scripts/check-react19-legacy-apis.mjs` to catch legacy context APIs, string refs, `element.ref`, function-component `defaultProps` / `propTypes`, and implicit ref callback returns that grep or import-only lint rules can miss.
- Updated baseline docs to make the current policy explicit: `docs/architecture/frontend-baseline.md` now documents React 19 as the only supported baseline, while `docs/plans/28-workspace-latest-dependency-and-lint-upgrade-plan.md` and `docs/logs/2026/04-04.md` now mark the earlier React 18 compatibility wording as historical rather than current.
- Link to doc/code paths: `eslint.config.js`, `package.json`, `scripts/check-react19-legacy-apis.mjs`, `docs/architecture/frontend-baseline.md`, `docs/plans/43-react-18-to-19-best-practices-migration-plan.md`, `docs/plans/28-workspace-latest-dependency-and-lint-upgrade-plan.md`
- Key decision: keep the React 19 closure work focused on durable guardrails and documentation convergence first; adoption of newer APIs such as `useEffectEvent`, `startTransition`, and `useDeferredValue` stays incremental and ROI-driven.
- Next step: run workspace verification with the new guardrails and then continue the plan with targeted correctness/adoption work only where the audit finds real gaps.

### 2026-04-08

- Tightened `docs/plans/43-react-18-to-19-best-practices-migration-plan.md` after a follow-up audit against the current workspace baseline and the current official React migration tooling guidance.
- Link to doc/code paths: `docs/plans/43-react-18-to-19-best-practices-migration-plan.md`, `eslint.config.js`, `package.json`
- Key decision: treat React 19 migration closure as a guardrail problem as much as a version/codemod problem, so the plan now explicitly covers both named-import and namespace/property-access regressions instead of relying on grep alone.
- Next step: when execution starts, implement Phase 5 with AST-aware lint/script checks first, then use the codemod and correctness phases to clear any remaining real migration gaps.

### 2026-04-08

- Finished the follow-through verification for the selective `wip/2026-04-08` hardening backport: fixed the `createActionDispatcher(...)` assembly so it receives the real `RendererRuntime`, restored the public `createDataSourceController({ dataPath })` to internal `targetPath` wiring, and updated stale runtime tests that still used the old controller field name.
- Link to code/tests/docs: `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/__tests__/request-runtime.test.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-core/src/types/renderer-core.ts`, `docs/architecture/flux-runtime-module-boundaries.md`
- Key decision: keep `index.ts` as a thin assembly layer by fixing the runtime wiring at the composition boundary instead of weakening `action-runtime` back to a partial-runtime contract.
- Next step: if we continue hardening action/data-source control flow, move more execution-policy helpers beside `operation-control.ts` without expanding the public runtime surface prematurely.

### 2026-04-08

- Migrated the useful `wip/2026-04-08` hardening slices without regressing the active Plan 40 node-instance path: `SchemaRenderer` now treats root page-data replay as an update-only reconciliation path, table row-scope materialization now uses an effect-driven external cache instead of mutating render-owned maps, and `action-runtime` timeout/retry control was extracted into a shared `operation-control` helper while keeping the existing behavior/tests intact.
- Link to code/tests/docs: `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/operation-control.ts`, `packages/flux-renderers-basic/src/index.test.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `docs/architecture/renderer-runtime.md`, `docs/architecture/table-row-identity-and-scope-performance.md`
- Key decision: keep the current branch's `NodeInstance` / owner-instance render path as the source of truth and only port the `wip` fixes that improve render-phase purity or execution structure without reverting Plan 40 progress.
- Next step: if table row rendering needs more cleanup, extract the row-scope cache into a dedicated helper hook so `table-renderer.tsx` can keep shrinking without changing row identity semantics again.

## 2026-04-08

- Completed Plan 42 by tightening the remaining top-level programming-model gaps: `data-source` is now `name`-first with explicit `mergeToScope` behavior and a first `statusPath` summary DTO, `refreshSource` accepts resource names, duplicate `componentName` dispatch now fails ambiguously instead of silently overriding, and report/spreadsheet hosts now create local `ActionScope` boundaries like Flow Designer.
- Link to code/tests/docs: `packages/flux-core/src/types/schema.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/component-handle-registry.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/index.test.ts`, `packages/report-designer-renderers/src/renderers.tsx`, `packages/spreadsheet-renderers/src/renderers.tsx`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/api-data-source.md`, `docs/architecture/action-scope-and-imports.md`, `docs/articles/flux-design-introduction.md`, `docs/plans/42-frontend-programming-model-alignment-remediation-plan.md`
- Key decision: keep legacy `id` and `dataPath` paths available as compatibility-only runtime affordances, but move the author-visible contract and documentation fully to `name`-first publication and explicit local capability boundaries.
- Next step: continue Plan 40 for the remaining live-node identity migration; Plan 42 no longer owns `NodeInstance` convergence.

- Continued Plan 40 past the React-only helper layer by threading `nodeInstance` through `ActionContext`, `PageRuntime.openDialog(...)`, dialog state, and `DialogHost`, so dialog actions now preserve both the compiled owner node and live owner instance across the action-to-render boundary.
- Link to code/tests/docs: `packages/flux-core/src/types/actions.ts`, `packages/flux-core/src/types/runtime.ts`, `packages/flux-react/src/helpers.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: keep compile-time CID/path derivation anchored to the compiled owner node for now, but carry `ownerNodeInstance` alongside it so downstream dialog rendering and future action/import consumers can rely on live locator identity without reopening the dialog API again.
- Next step: continue Plan 40 by pushing live-node identity into the remaining compiled-node-first helper and import/runtime seams.

- Finished the remaining practical work needed to treat Plan 41 as complete rather than just partially landed: `form` and `table` now contribute renderer-owned `schemaValidator` checks, the user-management docs example was updated from the stale `tpl` baseline to current `text` renderer usage, and the example is now exercised through a shared `validateSchema(...)` regression test.
- Link to code/tests/docs: `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-renderers-form/src/renderers/form.schema-validator.test.ts`, `packages/flux-renderers-data/src/index.tsx`, `packages/flux-renderers-data/src/schema-validator.test.ts`, `apps/playground/src/schema-examples.test.ts`, `docs/examples/user-management-schema.md`, `docs/architecture/schema-file-validator.md`, `docs/plans/41-compiler-integrated-schema-diagnostics-implementation-plan.md`
- Key decision: Plan 41's original wording that implied all basic/form/data renderers needed immediate validator rollout was too broad for the current metadata surface; the implemented baseline instead targets the renderer families where richer structural checks add immediate signal, while leaving open-ended/basic renderers on the shared compiler substrate until their contracts justify tighter local validation.
- Next step: keep Plan 40 as the active frontier and continue moving live-node identity from compatibility layers into the main render path.

- Marked Plan 39 and Plan 41 as completed after the verified dependency-tracking and compiler-diagnostics deliveries, and advanced Plan 40's React integration by exposing the current `NodeInstance` through renderer metadata contexts/hooks.
- Link to code/tests/docs: `docs/plans/39-dependency-tracking-root-scope-implementation-plan.md`, `docs/plans/41-compiler-integrated-schema-diagnostics-implementation-plan.md`, `packages/flux-core/src/types/renderer-hooks.ts`, `packages/flux-react/src/contexts.ts`, `packages/flux-react/src/hooks.ts`, `packages/flux-react/src/node-renderer.tsx`, `docs/architecture/renderer-runtime.md`
- Key decision: keep `CompiledSchemaNode` compatibility in place for existing renderers, but make live-node ownership first-class in React hooks so fragment/helper flows can migrate toward `NodeInstance` without another public-contract expansion later.
- Next step: continue Plan 40 by threading `NodeInstance` through fragment ownership and render-path APIs until compiled nodes stop acting as the implicit live node.

- Continued Plan 40 debugger/runtime identity migration by adding locator-based debugger inspection and tightening DOM bridge semantics: `inspectNode(locator)` now resolves through registry/runtime structural lookup, while `inspectByElement()` climbs to the nearest inspectable owner marker instead of requiring the clicked descendant to carry `data-cid` directly.
- Link to code/tests/docs: `packages/nop-debugger/src/controller.ts`, `packages/nop-debugger/src/types.ts`, `packages/nop-debugger/src/automation.ts`, `packages/nop-debugger/src/controller-inspect.test.ts`, `packages/nop-debugger/src/panel.test.tsx`, `docs/architecture/debugger-runtime.md`
- Key decision: DOM remains only the bridge from an element back to a live node; mounted-state and locator semantics now come from registry/runtime resolution, including valid-but-not-materialized locator targets.
- Next step: keep migrating debugger-facing inspect APIs toward typed `InspectResult` surfaces while preserving the current convenience return shapes for automation and panel consumers during the compatibility window.
