# Development Log - 2026-08-05

### 2026-08-05

- **C9 scheduling 族审计执行完毕**（plan `docs/plans/2026-08-05-1721-2-c9-scheduling-family-audit.md`，4 Phase 全 completed，4 卡 closed；**closure-audit 独立 fresh sub-agent session 已 pass**（task `ses_02d58a525ffe8vUs0IQnYTw0Mq`：4 卡 closed 复核 + fixed 声明代码 spot-check 全过 + host e2e 接线验证 + scheduling 854/854 + workspace typecheck 32/32 + test 59/59，证据见 plan Closure Audit Evidence 与全文一致性））：
  - **审计**：4 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{gantt,kanban,calendar,barcode-input}.md`），P0×1/P1×15/P2×13/P3×13 裁决留痕。维度重点：值所有权三态（calendar viewOwnership/dateOwnership 状态分层、kanban collapsedStatePath、barcode-input 表单 value 参与与写回、gantt 交互态/编辑态）pass/发现；5 DOM 契约（根 marker nop-gantt/nop-kanban/nop-calendar/nop-barcode-input + data-slot/data-state/data-testid）对齐 + 跨态根 slot 补齐；7 事件 ctx（~25 派发点全族单参缺口）；12 组合宿主（4 场景 Phase 3 补）；18 注册/IO 红线（barcode WASM fetch 直调浏览器 fetch = INV-1 P0；getUserMedia/BarcodeDetector 为浏览器本地能力非网络 IO 裁定）；**4 组件均无 component-lab lab 页——维度 18 缺口 Phase 3 补齐**。
  - **P0×1 修复（test-first）**：barcode INV-1 红线——`prepareWasm` 直调浏览器 `fetch(url)` 加载 ZXing wasm（默认 CDN URL）绕过 RendererEnv → 改为注入式 `WasmFetcher`（无注入即抛错）+ `barcode-input.tsx` 经 `useRendererEnv().fetcher` 构建 env 版 fetcher 传入 overlay；`prepare-wasm.test.ts` 10 用例先红后绿 + 新增 `src/contract-honesty.test.ts` 全包扫描禁裸 fetch/XHR/storage（ai 包先例）。
  - **P1×15 修复（test-first，CX-10/bug-83 家族 + 三态/异步契约）**：① **事件派发缺 `{ event, evaluationBindings, scope }` ctx**——gantt×14 + kanban×9 + calendar×9 + barcode×4 派发点全量补 `eventCtx` 第二参（action args `${_taskId}`/`${cardId}`/`${eventId}`/`${barcode}` 先前解析为空）+ barcode payload 补 `type: 'scan'`/`'scan-error'`；② **gantt/calendar reaction 字段接线**（zoomIn/zoomOut/scrollToToday/scrollToTask + print/exportPNG 等——渲染器不消费 `props.reactions`、无 ComponentHandle 注册，`component:*` 不可解析）→ `reactions[key].ready()` 挂载激活 + `useCurrentComponentRegistry` 句柄注册（gantt 4 句柄/calendar 7 句柄）+ gantt header +/-/Today 按钮派发 reaction；③ gantt 运行时 `tasks/links/resources/assignments` prop 更新重解析（`store.parse` + `recalcLayout`）；④ barcode 表单校验 contributor（`createBarcodeInputFieldValidation` 挂定义 `validation:` + `useCurrentFormError` 展示 + scanNow/clear readOnly 守卫 + 连续扫描 consume-once（含 open 守卫关闭态伪派发修复））；⑤ kanban：undo 添加列（新增 `addColumn`/`removeColumn` 命令——原以 moveColumn 入栈 undo 变移首非删除）、`filterCardFn` render 期 setState（useMemo + ref + effect 同步）、dropIndex/closestEdge（`resolveDropIndex` 纯函数提取）、Space/Enter 按键拆分（Enter=click、Space=drag pickup）；⑥ calendar 死代码 `_resourceOpenMap`/`_handleGroupToggle` 清理（onGroupToggle 标 `@reserved`）。
  - **P2×13**：data-slot 跨态根一致性（calendar 正常态根/gantt loading-empty 分支/kanban 空态根 `data-empty`）、i18n 提取（`scheduling.gantt/kanban/calendar.*` + `flux.barcode.*` 双语 ~40 key，含 barcode overlay Escape 关闭）、barcode 假绿测试改写 5 用例真实扫描模拟 + 断言反转修正、overlay Escape 关闭、lab 页 ×4 补维度 18 缺口（`{gantt,kanban,calendar,barcode-input}-lab-page.tsx` + `data-c9-host.ts` 198 行 4 host schema + probe 注册 + `RENDERER_LAB_REGISTRY` `:230-233` + `scheduling-renderer-routes.ts`（route-model 接入）+ `COMPONENT_LAB_COVERAGE_MANIFEST` ×4 + route-matrix 自动核对）。P2 backlog 归 CR（gantt editor onSave/undo 栈、kanban controlled 事件、calendar loadAction 错误处理、barcode scannerError 清除）。
  - **共性缺陷裁决（CX-12 插入）**：事件 ctx 缺口 + reaction 未接线为 4 组件家族共性（CX-10/bug-83 家族模式延伸），根因在各渲染器派发/接线点（非公共层）→ 按 roadmap「自动修复机制」§7b 当前 plan 内修复 + 事后回写插入 **CX-12**（roadmap 行，closure-audit pass 后与 C9 一并标 done）。无结构性重构。
  - **宿主场景 4/4**（`tests/e2e/component-lab/c9-host-surfaces.spec.ts`，programmatic DOM 断言，禁截图）：host-gantt-dialog（**bug 73 模式**：gantt 在 openDialog 内渲染任务条 + 点击派发 onTaskClick + `${_taskId}` 真机解析）、host-kanban-drag（**bug 73 模式**：kanban 在 openDialog 内卡片点击 `${cardId}|${index}` 解析 + 跨列拖拽 onCardMove 派发 + 焦点不逃逸）、host-cal-load（**bug 73 模式**：calendar 在 openDialog 内 loadAction 加载事件渲染 + 点击 onEventClick + `${eventId}` 真机解析）、host-barcode-form（form 内手动输入 + 模拟扫码写回 form 值 + required 校验错误展示 + 提交 payload 含 barcode）。
  - **C0 裁定项复验**：`gantt-bars-and-links.spec.ts:132` 隔离重跑 15/15×3 绿 + 全量跑 45 绿——flake 未复现，维持 **machine-load watch-only** 归因（非时序断言缺陷，无需稳定化修复，successor 无）；`calendar-demo.spec.ts` 导航按钮 locale 修复（C5.1）回归确认全绿。
  - **验证**：scheduling 包 **854/854** tests 绿（新增 prepare-wasm×10 + contract-honesty + barcode-schemas×4 + 各组件 regression + kanban-undo/helpers 用例）；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59/59 task 全绿；e2e c9-host-surfaces 4/4 + gantt-demo 20 + gantt-editor-and-keyboard 12 + gantt-bars-and-links 15/15×3 + kanban-demo 7 + calendar-demo 6 = 45 绿 + component-lab smoke/navigation **111/111**——**本 plan 零新增 e2e 失败**（gantt-perf/kanban-perf 3 failed 在 clean HEAD 完全同值复现 → pre-existing 机器负载，非 C9 回归）。
  - **roadmap**：C9 行 + CX-12 行已由 mission-driver 翻转 `planned → done`（closure-audit pass 后收口）；No owner-doc update required 决策已记录（4 卡维度 17 漂移项留痕，文档同步归 CR 跨族集中处理）。
  - 文档：4 审计卡（closed）+ plan/roadmap（C9 + CX-12）/daily log。

- **C8.3 ai 增强族审计执行完毕**（plan `docs/plans/2026-08-05-1721-1-c8-3-ai-enhancement-family-audit.md`，4 Phase 全 completed，4 卡 closed；closure-audit 由 fresh sub-agent session 执行，证据见 plan Closure 节）：
  - **审计**：4 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{ai-prompts,ai-suggestions,ai-voice-input,ai-welcome}.md`），P0×0/P1×3/P2×8/P3×1 裁决留痕。维度重点核对：注册定义 ↔ schema ↔ 渲染器消费一致（ai 包无 propContracts/eventContracts 声明系全族惯例，C8.1/C8.2 closed 先例，payload 形状以 renderers.md §13 + schema JSDoc 为准）；值所有权三态（4 组件均为 display-only widget 无 value 写回路径、无表单泄漏）pass；DOM 契约 4 marker + data-slot/data-cid/data-align/data-layout/data-state/data-overflow/data-unsupported 全对齐（`check:audit-missing-renderer-markers` 0 命中）；i18n pass（`flux.ai.voiceInput/voiceListening/voiceUnsupported/suggestionsTitle` 双语键存在，无硬编码文案）；dim 17 核对：§13 onAction 行归属 ai-feedback（schema 确有 onAction）、无 ai-welcome 族残留——ai-bubble 卡 P3 观察闭环；另发现 2 处文档漂移（见 P2）。
  - **P1 修复 ×3（test-first，CX-10 家族同型缺口）**：事件派发缺 `event`/`evaluationBindings`/`scope` ctx——ai-prompts onSelect + ai-suggestions onSelect + ai-voice-input onResult/onError ×3 派发点（共 5 处）——C8.1/C8.2 修复 14+10 处后增强族漏接；action args `${item.label}`/`${index}`/`${transcript}`/`${reason}` 先前解析为空。按 C8.1/C8.2 模式补 `dispatchCtx`（ai-voice-input 额外 `nodeScopeRef` 供 mount effect 路径，ai-chat.tsx:156-158 先例）；test-first 先红后绿（新增 `ai-prompts.test.tsx` 9 用例 + `ai-welcome.test.tsx` 9 用例 + ai-suggestions/ai-voice-input/p1-renderers 断言收紧为 payload+ctx 双参契约，初跑 5 失败全转绿）。
  - **P2 ×8**：独立测试文件 ×2（ai-welcome/ai-prompts 维度 16 盲区）、ai-voice-input 测试 ctx 断言 ×4、renderers.md §11 phantom `trigger` 字段移除（schema 无此字段，live grep 零命中消费）、renderers.md §13 补 ai-voice-input onResult/onError 事件行、lab 页 ×4 补维度 18 缺口（`ai-{prompts,suggestions,voice-input,welcome}-lab-page.tsx` + RENDERER_LAB_REGISTRY + AI_RENDERER_ROUTES + COMPONENT_LAB_COVERAGE_MANIFEST ×4 + route-matrix 自动核对，playground 141/141）。
  - **共性缺陷裁决（CX-11 插入 + 当前 plan 修复）**：宿主场景 host-prompts-dlg 暴露**公共层缺陷**——`openDialog`/`openDrawer` 表面 args 内嵌事件 args 的成员访问模板（`${item.label}`）被急切求值（flux-compiler `action-compiler.ts` `compilePayload` 用通用 compileValue 编译整个 args 树，`compileNode` 下钻 schema body；派发时 flux-action-core `evaluateSurfaceArgs` 先急切求值全 args 再以 isSchema 覆盖为 raw——成员访问在 dispatch scope 抛 TypeError → dialog 静默打不开；裸键 `${index}` 不抛故潜伏）。根因在公共层（flux-compiler + flux-action-core）→ 按 roadmap §7b 插入 **CX-11** 并在本 plan 修复：`compilePayload` 对 isSchemaInput 顶层 arg 经 `__nopPreserveLiteral` envelope 编译为 static-node（与 dispatcher 侧 isSchema 保留契约 option ① 对齐）；test-first `action-compiler.test.ts` 先红后绿 + `docs/bugs/89` + c8-3 host 实证。
  - **宿主场景 4/4**（`tests/e2e/component-lab/c8-3-host-surfaces.spec.ts`，programmatic DOM 断言，禁截图）：host-prompts-dlg（**bug 73 专项**：ai-prompts 在 openDialog 内点击 prompt → `${item.label}|${index}`=`Translate|1` 经 ctx 真机解析——同时实证 CX-11 修复）、host-suggest-pop（popover 溢出 +N 展开 → 溢出项点击 `${item.text}|${index}`=`Refine|3` 全局索引）、host-voice-degrd（addInitScript 确定性删除 SpeechRecognition → disabled + data-unsupported + `${reason}`=`unsupported` 单次派发不崩溃）、host-welcome-reg（footer region 内嵌组件渲染 + 内嵌按钮 action probe 值 0→1 递增证明 region host scope 求值正常）。
  - **验证**：ai 包 **509/509** tests 绿（新增 18 用例 + 断言收紧）；flux-compiler **550/550**（新增 CX-11 契约测试）；flux-action-core 207/207 + flux-runtime 1396/1396 + flux-react 467/467 + playground 141/141 全绿；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；e2e c8-3 4/4 + smoke **110/110** + ai-widgets-demo/ai-p4-widgets 3/3（113 全绿）+ component-lab 全量 326 passed/2 failed/1 skipped + ai 族回归 34/34——**本 plan 零新增 e2e 失败**（2 failed 均为 C7 记录在案 pre-existing：c5-2 host-timeline data-ownership 冲突 clean HEAD 复现、c3-5 editor Tiptap 批次 flake 单跑绿）。
  - **pre-existing 顺带修复（manifest 一致性）**：C8.2 期遗留 `coverage-manifest-entries.ts` primaryScenario 漂移 ×3（ai-tool-call/ai-attachments/ai-citations 与 lab 页场景标题不一致 → smoke 恒失败，clean HEAD 复现确认非本 plan 引入）——已对齐实际标题，smoke 110/110 恢复全绿。
  - **roadmap**：C8.3 行执行证据待回写 + CX-11 行已按 §7b 插入（`planned`，依赖 C8.3）；`planned → done` 翻转待 closure-audit pass 后收口（执行 session 不自审）。
  - 文档：4 审计卡（closed）+ `docs/bugs/89` + renderers.md（§11 phantom trigger 移除 + §13 补行）+ plan/roadmap（C8.3 + CX-11 行）/daily log。

- **mission-driver 最终全量验证（full-green verification）**（plan `docs/plans/2026-08-05-1540-1-workbench-shell-panel-resize-plan.md` workbench shell panel-resize）：uncached 强制重跑 `pnpm typecheck --force` 32/32、`pnpm build --force` 32/32、`pnpm lint --force` 32/32、`pnpm test --force` 59/59 task 全绿（含 flux-react workbench-shell 467 用例、flow-designer-core 103、flow-designer-renderers 184）；e2e 受影响族全绿——flow-designer 族 **30 passed/3 skipped**（resizable 5/5 + collapsible 7/7 零回归 + edge-creation/label-text/minimap-pan/ui）＋ WorkbenchShell 其它消费方 report/word/taskflow designer 43 passed（report-designer-demo + word-editor×4 + taskflow-designer-ui）＝ **73 passed/3 skipped**，零新增失败；unit + e2e 双绿，提交含 full-green 标记。
- **WorkbenchShell 面板拖拽调宽 + Flow Designer 宽度状态落地**（plan `docs/plans/2026-08-05-1540-1-workbench-shell-panel-resize-plan.md`，4 Phase 全 completed）：
  - **Phase 1（flux-react WorkbenchShell）**：opt-in 调宽能力落地——`WorkbenchShellProps` 新增 `leftResizable/rightResizable/leftWidth/rightWidth/onLeftWidthChange/onRightWidthChange/leftMinWidth/leftMaxWidth/rightMinWidth/rightMaxWidth`（默认 width 240/352px 等价 15rem/22rem、min 200、max 600，与 page.aside 一致）；handle 贴中心侧边缘（左面板右缘 `right-0`、右面板左缘 `left-0`），`role="separator"` + `aria-orientation="vertical"` + `aria-valuenow/min/max` + tabIndex；pointer 拖拽（setPointerCapture + 右侧 dx 反转 + clamp）与键盘 `ArrowLeft/ArrowRight` 步长 16px（stopPropagation 防短路经 window 快捷键链）；受控 props 优先、未传时内部 local state 起步；宽度经 inline `gridTemplateColumns` 应用，仅 `>=1024px` 宽视口生效（`useWideWorkbenchViewport` matchMedia，jsdom 无 matchMedia 时回退宽视口），窄视口仍走既有响应式类，零回归；collapsed 不渲染 handle。单测 +9（a11y 契约/handle clamp 双向/右测反转/intermediate payload/键盘步长+camp/collapsed 无 handle/缺省零回归/uncontrolled 起步）。
  - **Phase 2（core + renderers 全链路）**：`DesignerShellConfig`（`{ palette?: { resizable?, width?, minWidth?, maxWidth? }, inspector?: {...} }`）挂 `DesignerConfig.shell` + `NormalizedDesignerConfig.shell`；`DesignerShellState.paletteWidth/inspectorWidth`（init 自 config.shell，缺省 240/352）；`shell-controls.setPaletteWidth/setInspectorWidth`（clamp 到 [minWidth,maxWidth] 缺省 [200,600] + 幂等 + 事件 `paletteWidthChanged/inspectorWidthChanged`）；snapshot 两字段 + 变更检测；`DesignerCore.setPaletteWidth/setInspectorWidth` 公开 API；renderers：`DesignerCommand` 加 `{ type:'setPanelWidths', paletteWidth?, inspectorWidth? }` + adapter case + `designer:setPanelWidths` action（listMethods + invoke + manifest 条目，args paletteWidth/inspectorWidth optional）。core 单测 +6、renderers adapter/provider/manifest 单测 +4；`designer-canvas.tsx` snapshot selector 同步补两字段。宽度不进入 undo/redo 历史与 dirty 状态（既有 core-ui-state 全零回归）。
  - **Phase 3（接线 + playground + e2e）**：`designer-page-body.tsx` 从 snapshot 读宽度、从 `config.shell` 读 resizable/min/max，透传 WorkbenchShell，`onLeftWidthChange/onRightWidthChange` → `dispatch({type:'setPanelWidths',...})`；`workflow-designer-schema.json` designer config 增 `shell` 段（palette/inspector `resizable:true`，默认宽度不变）；新 e2e `tests/e2e/flow-designer-resizable.spec.ts` 5 用例（mouse 拖拽改宽 + canvas 变窄、拖远 clamp ≤600、键盘步长 16、调宽→折叠→展开宽度保持、右测 dx 反转）全绿；`flow-designer-collapsible.spec.ts` 7/7 零回归（含 `toBe(initialWidth)` 等式断言）。
  - **Phase 4（文档）**：`designer-workbench-shell.md` Panel Resize Contract 与 live 实现核对一致（handle 位置/键盘等价/宽度 family-owned/默认宽度不变/collapsed 无 handle/响应式抑制不变）；`flow-designer/design.md` §5 补 `config.shell` 段说明 + §10 action 清单补 `designer:setPanelWidths`；daily log 本条目。
  - **验证**：flux-react 467/467、flow-designer-core 103/103、flow-designer-renderers 184/184 tests 全绿；e2e resizable 5/5 + collapsible 7/7；workspace `pnpm typecheck/build/lint` + `pnpm test` 全绿（见 plan Closure Gates）。
  - **closure-audit（独立子 agent fresh session，task `ses_02e6a3eb6ffeHUV5lp4y3eKXCY`）已 pass**：全部实现语义逐项实证 PASS（handle a11y/pointer+键盘+clamp/dx 反转/collapsed 无 handle/受控优先、core shell 宽度状态+事件+snapshot、command/adapter/action/manifest 链、designer-page 接线、e2e 12/12 实测重跑、Panel Resize Contract 与 live 一致）；唯一 Major 为 plan 文本 Phase 4 勾选滞后（审计指示由执行者补齐后收口，无代码影响）；workspace typecheck/build/lint 32/32 + test 59/59 task 全绿；Plan Status → `completed`，roadmap/审计卡无对应行（本 plan 为 analysis-sourced，非 roadmap work item）。

- **C8.2 ai 工具内容族审计执行完毕**（plan `docs/plans/2026-08-05-1314-3-c8-2-ai-tool-content-family-audit.md`，4 Phase 全 completed，5 卡 closed；closure-audit 待 mission-driver CLOSURE_VERIFY fresh session 执行）：
  - **审计**：5 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{ai-tool-call,ai-attachments,ai-citations,ai-feedback,ai-token-usage}.md`），P0×0/P1×4/P2×8 裁决留痕（ai-tool-call 全维度 pass——onApproval ctx 已在 C8.1 修复）。维度重点核对：值所有权三态（ai-feedback voted/copied local 回显、ai-tool-call 工具调用状态受控、ai-token-usage 无 value 纯展示）pass；5 DOM 契约 marker + data-slot/data-cid/data-tool-status/data-mode/data-citation-index/data-empty 全对齐（`check:audit-missing-renderer-markers` 0 命中）；事件 payload 形状全核对（onAction/onChange/onError/onUpload/onSourceClick/onClick/onApproval）发现 3 处契约问题（见 P1 修复）；**安全红线核查 pass**：附件 URL 仅入 `<img src>` 不成 anchor、文件名仅 React 文本节点渲染（XSS 不可达）、无路径穿越面、无浏览器网络 IO 直调（URL.createObjectURL/clipboard/SpeechRecognition 均为用户手势浏览器 API，非网络 IO——ai-voice-input 先例）。
  - **P1 修复 ×4（test-first）**：① **事件派发缺 `event`/`evaluationBindings`/`scope` ctx**（ai-feedback onAction + ai-attachments onChange/onError×2/onUpload + ai-citations onSourceClick + ai-token-usage onClick 共 10 处——CX-10/bug-83 家族约定在工具内容族漏接；action args `${action}`/`${reason}`/`${source.title}` 等先前解析为空；ai-token-usage onClick 原为**空 payload**，补 `{ type:'ai:token-usage-click', usage }` + ctx）；② **ai-attachments 附件 `status`（uploading/error）不渲染**（renderers.md §9.1 模型契约 vs AttachmentItemView 忽略状态——上传失败/进行中不可见，四态错误态不成立）→ 新增 `AttachmentStatus`（data-status + spinner/失败文案 + i18n 键 `flux.ai.uploading`/`uploadFailed` 双语）。
  - **P2 ×8**：ai-token-usage aria-hidden → aria-label=`flux.ai.tokenUsage`（键原闲置）+ role=group；renderers.md 文档同步 ×4（ai-attachments schema 注释/Events 总览 payload 形状 `{file?,error?}` → live `{attachments}`/`{reason}`、§14.3 `items` 残留 → `value`、ai-citations sanitizeHtml 过期声明 → 受控文本节点模型、§13 Events 总览补 ai-token-usage 行 + §11b DOM 列表补 `-text`）；测试加固（ai-feedback like/dislike/sources 派发 + data-active 回显 + 全 payload/ctx 断言、ai-token-usage onClick 假绿收紧、ai-attachments onUpload payload + 安全回归 ×2）。
  - **共性缺陷裁决**：ctx 缺失根因跨 4 组件 10 处派发——同一根因家族（C7 bug-83 / docs/bugs/86），按 roadmap §7b 当前 plan 内修复 + **CX-10 事后回写延伸覆盖**（roadmap CX-10 行描述与依赖补 C8.2 覆盖证据 + `docs/bugs/88` 记录），不插入新 CX-n。
  - **宿主场景 7/7**（`tests/e2e/component-lab/c8-2-host-surfaces.spec.ts`，programmatic DOM 断言，禁截图）：host-tool-dialog（**bug 73 专项**：ai-tool-call 在 openDialog 内 running→success 状态流转 + 参数展开/折叠 DOM 契约）、host-hitl-dead（**HITL 死点击专项**：wired 快速双击仅 1 次派发 `approve|call_c8_2` + host setValue 翻转 decided → 徽章替换按钮；no-handler 卡按钮禁用）、host-attach-dialog（**bug 73 专项**：ai-attachments 在 openDialog 内 setInputFiles 缩略图渲染/移除/超限 onError `${reason}` 解析）、host-attach-safety（**URL/文件名安全专项**：javascript: URL 仅 `<img src>` 不成 anchor（Chromium ERR_UNKNOWN_URL_SCHEME 已按预期噪声入 KNOWN_NOISE_PATTERNS）、恶意文件名文本化）、host-citation-clk（引用触发 → Popover 来源卡标题/URL + `${index}|${source.title}`=2|Doc B 解析）、host-feedback（like/dislike data-active 互斥回显 + `${action}|${message.id}`=dislike|m_fb 解析）、host-token-usage（metadata.usage 计数/ring + data-empty 占位 + `${usage.total_tokens}`=42 解析）。
  - **验证**：ai 包 **495/495** tests 绿（新增 feedback ×6 + attachments ×9 + token-usage ×2 + citations ctx ×1 + i18n 键；p1-renderers 断言收紧）；`flux-i18n` 26/26；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；playground route-matrix 34/34；e2e c8-2 7/7 + smoke **93/93**（自动纳入 5 新 lab 页）+ **ai 族回归 42/42 全绿**（ai-attachments 2 + ai-citations 2 + ai-tools 1 + ai-hitl 2 + ai-branches-linkage 3 + ai-p4-widgets + ai-persistence 2 + ai-chat 10 + ai-conversations 4 + ai-component-handle 2 + ai-virtual-scroll 1 + ai-widgets-demo 13）——**本 plan 零新增 e2e 失败**。
  - **roadmap C8.2 行**：执行证据已回写（`docs/backlog/component-audit-roadmap.md`，`planned → done` 翻转待 CLOSURE_VERIFY closure-audit pass 后收口——执行 session 不自审）；CX-10 行事后回写 C8.2 延伸覆盖证据。
  - 文档：5 审计卡 + `docs/bugs/88` + renderers.md（§9.2/§11b/§13/§14.3/§11b 安全节）+ fixtures.ts KNOWN_NOISE_PATTERNS + plan/roadmap（C8.2 + CX-10 行）/daily log。

- **C8.1 ai 会话主链审计执行完毕**（plan `docs/plans/2026-08-05-1314-2-c8-1-ai-conversation-main-chain-audit.md`，4 Phase 全 completed，5 卡 closed；closure-audit 待 mission-driver CLOSURE_VERIFY fresh session 执行）：
  - **审计**：5 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{ai-chat,ai-message-list,ai-bubble,ai-sender,ai-conversations}.md`），P0×0/P1×3（onConversationChange 静默 no-op + 事件派发缺 evaluationBindings ctx 跨 4 渲染器，ai-tool-call onApproval 同型）/P2×6/P3×1 裁决留痕。维度重点核对：值所有权三态（ai-chat messages/status turn-boundary structuredClone 投影、ai-sender 输入受控、ai-conversations 会话选择 scope-owned）pass；5 DOM 契约 marker + data-slot/data-cid 全对齐（`check:audit-missing-renderer-markers` 0 命中）；i18n key 双语核对 pass（唯一缺口 BranchPicker 硬编码英文 aria-label → P2 修复）；事件 payload 形状全核对发现 2 处契约问题（见 P1 修复）。
  - **P1 修复 ×3（test-first）**：① `onConversationChange` 声明（schema+definitions+renderers.md）但从不派发 → 静默 no-op（`ai-chat-conversation-change.test.tsx` 先红后绿，activeConversationId prop 变化时派发 `{ conversationId }` 含清空到 null）；② **事件派发缺 `event`/`evaluationBindings`/`scope` ctx**（ai-chat×5 + ai-sender×3 + ai-bubble×1 + ai-conversations×4 + ai-tool-call onApproval×1 共 14 处——action args 模板 `${id}`/`${text}`/`${action}` 等先前解析为空字符串，bug 83 / diff-view P1-10 同族先例；实测文档示例 `${event.x}` 亦不可解析——`getEvaluationScope` 仅合并 evaluationBindings+scope（flux-action-core/src/action-core.ts:206-208），`event` 绑定只用于 preventDefault 求值；修复补 ctx 后 c8-1 host 场景实证 `${id}|${conversation.title}`=c2|Second chat 等解析）。
  - **P2 ×6**：BranchPicker aria-label i18n（`flux.ai.branchGroups/branchPrevious/branchNext` 双语 key + p2-a11y-i18n 断言）；renderers.md §1.2 ai-chat schema 漂移（phantom `conversationId`/`onSend` 移除 + 10 个 live 字段补齐 + Events 总览 onSend 行删除 + `onConversationChange` 触发语义补注）；`ai-conversations-example.json`/renderers.md 14.2 `${event.*}` → 裸键同步；lab 页 ×5 补维度 18 缺口（`ai-{chat,message-list,bubble,sender,conversations}-lab-page.tsx` + `RENDERER_LAB_REGISTRY` + `AI_RENDERER_ROUTES`（route-model 接入）+ `COMPONENT_LAB_COVERAGE_MANIFEST` ×5 + route-matrix 更新 + multi-scenario-lab-page 注册 ai renderers）。
  - **共性缺陷裁决（CX-10 事后回写）**：事件 ctx 缺失根因跨 5 渲染器（含 C8.2 的 ai-tool-call onApproval）——按 roadmap §7b 当前 plan 内修复后回写 CX-10（`component-audit-roadmap.md` 新行，`planned`，父 plan closure audit pass 后标 done）；跨包文档口径校正：renderer-runtime.md Event Passthrough Contract 补注（evaluationBindings 裸键约定）+ layout-selection-guide/graph design.md 示例改裸键（`${event.value.id}` → `${value.id}`、`${event.node}` → `${node}`）。
  - **e2e pre-existing 复验**：C0 基线归属本 work item 的 `ai-chat.spec.ts:112` timestamp + `ai-rich-text-sender.spec.ts` ×5（Tiptap）——live repo 已由 commit `abcecfa9`（2026-08-04，本 plan HEAD 祖先）修复，本 plan 复验全绿（10/10 + 5/5，programmatic 断言），记录为 successor 修复证据（plan Phase 2 项「修复」以复验代实现——缺陷在基线快照后已被修复，非降级）。
  - **宿主场景 5/5**（`tests/e2e/component-lab/c8-1-host-surfaces.spec.ts`，programmatic DOM 断言，禁截图）：host-ai-dialog（**bug 73 专项**：ai-chat 在 openDialog 内流式对话 + 发送，mock connector 流式回复渲染于 dialog surface）、host-ai-stream（流式 DOM 契约：data-state processing→completed、气泡 data-streaming presence-only、aria-busy 翻转、cancel→aborted→sender 恢复可用）、host-ai-hitl（**HITL 死点击专项**：wired handler 快速双击仅 1 次派发 `approve|call_c8_1` + host setValue 翻转 decided → 徽章替换按钮（第二次点击不可达）；no-handler 卡按钮禁用）、host-ai-cc（会话侧栏 item 点击 → `${id}|${conversation.title}`=c2|Second chat + ai-chat onConversationChange=`c2`）、host-ai-sender（Enter 提交 trim 后 `${text}`=hello from lab + 字数计数）。
  - **验证**：ai 包 479/479 tests 绿（新增 conversation-change×3 + branch i18n×1；p1-renderers 3 断言更新为 payload+ctx 双参契约）；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；playground 141/141（route-matrix 新行校验）；e2e c8-1 5/5 + smoke 91/91 + ai 族回归 29/29 全绿（ai-chat 10 + rich-text 5 + conversations 4 + component-handle 2 + hitl 2 + virtual-scroll 1 + persistence 2 + branches-linkage 3）。
  - **roadmap C8.1 行**：执行证据已回写，`planned → done` 翻转待 CLOSURE_VERIFY closure-audit pass 后执行（执行 session 不自审）。**CLOSURE_VERIFY fresh session closure-audit 已 pass**（2026-08-05，独立审计子 agent：5 卡 closed 复验、P1 修复（onConversationChange 派发 + 事件 ctx）×14 处代码实证（ai-chat `:321`/ai-sender `:210/:230/:237/:243`/ai-bubble `:265-268`/ai-conversations `:31-134`/ai-tool-call `:285-298`）、宿主 spec 5/5 fresh 重跑 + ai-chat 10/10 + ai-rich-text-sender 5/5 全绿、ai 包 479/479 + workspace typecheck/build/lint 32/32 + test 59/59 全绿、plan-check --strict 全勾选）；**roadmap C8.1 行已由 mission-driver 翻转 `planned → done`，CX-10 依赖项同步标 done**（证据见 plan Closure Audit Evidence 与 plan 全文一致性）。
  - 文档：5 审计卡 + renderers.md §1.2/§13/§14.2 + renderer-runtime.md + layout-selection-guide + graph design.md + ai-conversations-example.json + plan/roadmap（C8.1 + CX-10 行）/daily log。

- **mission-driver 最终全量验证（full-green verification）**（plan `docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md` C7）：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；mobile 包 170/170（uncached 重跑）；e2e `c7-host-surfaces.spec.ts` 6/6 + mobile 族（mobile-components/m5-mobile-showcase/m2-touch）37 passed/1 skip 全绿；unit + e2e 双绿，提交含 full-green 标记。
- **C7 mobile 交互族审计执行完毕**（plan `docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md`，4 Phase 全 completed，5 卡 closed；closure-audit 待 mission-driver CLOSURE_VERIFY fresh session 执行）：
  - **审计**：5 组件 × 18 维增量审计卡产出（`docs/audits/per-component/{pull-refresh,infinite-scroll,swipe-cell,countdown,notice-bar}.md`），P0×0/P1×9/P2×8/P3×1 裁决留痕。维度重点：值所有权三态（countdown time/paused、infinite-scroll hasMore/loading/error 运行时受控）pass；DOM 契约 5 marker + data-slot/data-state 全对齐（`check:audit-missing-renderer-markers` 0 命中）；**i18n key 核对为 pass**（`flux.mobile.*` 键与 locale 一致，en-US 解析经新增测试实证——早前 draft 中疑似 `n.` 前缀问题经 live repo 复核不存在）；notice-bar onClick/onClose 原生事件转发与 link/card 同族惯例一致（不强制 eventContracts）。
  - **P1 修复 ×9（test-first）**：① 事件派发缺 `event`/`evaluationBindings`/`scope` ctx（pull-refresh/infinite-scroll/swipe-cell×3/countdown 共 6 处——action args `${direction}`/`${source}`/`${side}` 先前不可解析，bug 83 同族先例，`docs/bugs/86`）；② 注册定义缺 eventContracts ×7（onRefresh/onLoadMore/onAction/onOpen/onClose/onFinish，carousel P1-1/diff-view P1-7 先例）；③ **NEW-C7-02 swipe-cell 操作区真实浏览器不可见**（bug 73 模式：region 恒 `translateX(-100%)/+100%` 位于行 overflow:hidden 裁剪盒外，open 时仅 content 平移露出空档——单测断言 content offset 从不断言 region 屏位；宿主场景 elementFromPoint 探针定位，修复为 region transform 跟随 openState + 同曲线 transition，`docs/bugs/87`）。
  - **P2 ×8**：lab 页 ×5 补维度 18 缺口（`pull-refresh/infinite-scroll/swipe-cell/countdown/notice-bar-lab-page.tsx` + `RENDERER_LAB_REGISTRY` + `MOBILE_RENDERER_ROUTES`（route-model 接入）+ `COMPONENT_LAB_COVERAGE_MANIFEST` ×5 + route-matrix 更新 + multi-scenario-lab-page 注册 mobile renderers）；design.md payload 文档 ×4 + countdown prefix/suffix「value-or-region」漂移更正。
  - **宿主场景 6/6**（`tests/e2e/component-lab/c7-host-surfaces.spec.ts`，programmatic DOM 断言）：host-pr-dialog（bug 73 专项：pull-refresh dialog 内 CDP 触摸下拉 → `${direction}|${threshold}`=down|50）、host-is-dialog（infinite-scroll dialog 内 immediateCheck → `${source}`=immediate）、host-is-retry（error 态文本 + 重试 → `${source}`=retry）、host-sw-action（行内滑动 → 点击操作按钮 → `${side}|${$slot.index}`=open-left|0 + 自动回弹）、host-cd-finish（1.5s 归零 → data-finished=true + `${type}`=finish）、host-nb-close/click/static。**触摸注入技术沉淀**：CDP `Input.dispatchTouchEvent` 是唯一可靠路径（JS 合成 TouchEvent 与 page.mouse-as-touch 均不可达 React 委托监听，详见 `docs/bugs/87` Notes）。
  - **验证**：mobile 包 168→**170 tests** 绿（新增 evalCtx×4 + i18n 锁定×3 + eventContracts×4 + NEW-C7-02×2）；workspace `pnpm typecheck/build/lint` 32/32 + `pnpm test` 59 task 全绿；e2e c7 6/6 + smoke 86 + mobile 7/1skip + m5 21 + m2 9 全绿（本 plan 零新增 e2e 失败）。
  - **共性缺陷裁决**：evalCtx/eventContracts 影响 4 组件但均为组件单点派发代码（无公共层根因）——按 roadmap §7 当前 plan 内修复，不插入 CX-n（bug 83/diff-view P1-10 先例一致）。
  - **pre-existing e2e 观察（非本 plan 引入，供 CLOSURE_VERIFY 核对）**：① `c5-2-host-surfaces.spec.ts:189` host-timeline 失败——timeline v2 commit `af7e89e7` 起 timeline 恒发 `data-ownership`，与 c5-2 display-only 断言冲突（clean HEAD 复现确认；该 commit 验证仅跑 w4b 9/9 未跑 c5-2；归属 timeline-v2 successor/CR）；② `c3-5-host-surfaces.spec.ts` editor/link 批次 flake（Tiptap 同族 pre-existing，单跑绿批次红，clean HEAD 复现确认）。
  - **roadmap C7 行**：执行证据已回写（`docs/backlog/component-audit-roadmap.md`），`planned → done` 翻转待 CLOSURE_VERIFY closure-audit pass 后执行（执行 session 不自审）。**CLOSURE_VERIFY fresh session closure-audit 已 pass**（2026-08-05，独立审计子 agent：5 卡 closed 复验、P1 修复 + eventContracts 代码实证、宿主 spec 6/6 fresh 重跑、mobile 170/170 + workspace typecheck/build/lint 32/32 + test 59/59 全绿、plan-check --strict 39/39 checked）；**roadmap C7 行已由 mission-driver 翻转 `planned → done`**（证据见 plan Closure Audit Evidence 与 plan 全文一致性）。
  - 文档：5 审计卡 + `docs/bugs/86` + `docs/bugs/87` + 4 份 design.md + plan/roadmap/daily log。

- **P2/P3 修正计划补充：i18n lint 三盲区实证 + Phase 1 范围修订**（plan `docs/plans/2026-08-05-1359-1-p2p3-rigor-remediation-plan.md`，保持 active）：
  - **核验结论**：`eslint.config.js:185-306` 实际已启用 `i18next/no-literal-string`（error 级，`mode:'jsx-text-only'`，覆盖 packages 源码，`pnpm lint` 生效）；`scripts/check-i18n-keys.mjs` 随 `check:i18n-keys` 校验 t() 用 key 定义完整性。但 3 盲区使 I1–I10 全部漏检：① `words.exclude '^[\s\d\W]*$'` 的 `\W` 匹配 CJK（实测 `选择图标`/`上一页` 被放行）；② `jsx-attributes.exclude 'aria-.*'` 放行 aria-label；③ `jsx-text-only` 不查 JS 字符串字面量（I1/I4–I6/I9–I10 均属 JS 非 JSX 文本）。
  - **修复验证**：修复版 `^(?!.*[\\u4e00-\\u9fa5])[\\s\\d\\W]*$`（文件层双反斜杠）下纯符号/数字仍放行、中文报错；临时 patch 后全仓 `eslint packages` 跑测 **0 新增 error**（零风险收益）。注意 JS 转义陷阱：sed 单反斜杠会把 `\s` 解析为 `s` 破坏规则（已通过 require 真实配置 + node 复现确认根因）。
  - **Phase 1 修订**：新增 eslint 第 1 盲区修复 item（`words.exclude` 正则 + Preflight 实证 + Exit Criteria 增加 `pnpm lint` 0 新增 error 与 `node -e` 复现记录）；Non-Goals 明确 ②③ 盲区不修（牵连 C7/C8 族 ~16 处 aria-label / 32 个含中文文件，超本 plan 审计范围，随 C7/C8 审计后统一评估）；`analysis` §8.5 对应更新"①落地、②③等待审计"。
  - 文档变更（plan/analysis 各一处），无代码变更，无 typecheck/build/lint/test 门禁。

- **mission-driver 起草下一批组件审计计划**（C7/C8.1/C8.2，3 个 work item，均 `draft → active`）：
  - **C7 mobile 交互族**（`docs/plans/2026-08-05-1314-1-c7-mobile-interaction-family-audit.md`）：pull-refresh/infinite-scroll/swipe-cell/countdown/notice-bar 5 组件增量 18 维审计（roadmap C7 定位"已有密集审计，仅增量——组件卡回填、DOM 契约维度、真实浏览器触摸场景"）；基线实测：definitions 127 行（注册 `:17/:41/:62/:82/:101`）、renderer 256/255/338/233/265 行、单测 144 `it(`、e2e mobile-components 8 + m5-mobile-showcase 21 + m2-touch 9；**5 组件均无 component-lab lab 页**（维度 18 缺口待补）。
  - **C8.1 ai 会话主链**（`docs/plans/2026-08-05-1314-2-c8-1-ai-conversation-main-chain-audit.md`）：ai-chat/ai-message-list/ai-bubble/ai-sender/ai-conversations 5 组件增量审计；**承接 C0 基线 e2e pre-existing 裁定项（successor C8.1）**：`ai-chat.spec.ts:112` timestamp + `ai-rich-text-sender.spec.ts` ×5（Tiptap）——计划内 test-first 修复并复验（`Fix` 非降级）。
  - **C8.2 ai 工具内容族**（`docs/plans/2026-08-05-1314-3-c8-2-ai-tool-content-family-audit.md`）：ai-tool-call/ai-attachments/ai-citations/ai-feedback/ai-token-usage 5 组件增量审计；重点安全红线（附件 URL/文件名校验）与 HITL 死点击场景专项。
  - 三 plan 均经**独立子 agent fresh session review**（task `ses_02fa4cbbcffeHDGkVhB9R8eBSz`）`pass-with-minors`（0 Blocker/0 Major）：Minor 仅 2 类——C8.1/C8.2 `src/index.ts` import 行号引用错误（已修正为 renderer exports `:39-43`/`:46-51`）与 `it(` 计数措辞（已用"约"）——达成共识后 `draft → active`；roadmap C7/C8.1/C8.2 行 `todo → planned`。
  - 不涉及代码变更，无 typecheck/build/lint/test 门禁；e2e pre-existing 修复在 plan 执行期进行。

- **timeline v2 受控当前事件落地**（plan `docs/plans/2026-08-04-2030-2-timeline-v2-controlled-current-event-plan.md`，Phase 1-3 全部完成，独立 closure-audit `approved`）：
  - timeline 由展示型扩展为支持受控当前事件：`value`/`defaultValue`/`valueOwnership`(local|controlled|scope)/`valueStatePath`/`onChange` 五字段落地于 `flux-renderers-layout`（`schemas.ts` TimelineItemSchema/TimelineSchema + `process-display-definitions.ts` propContracts/fields/eventContracts + `timeline-renderer.tsx`）。
  - value 解析链与 steps 同构（key 匹配 → 数字 clamp → 未匹配 -1），本地实现（`resolveEventIndex`）；**渲染层裁定**：未匹配 → 回退 defaultValue → 仍 -1 → 无 active（不回退首项，与 steps →0 兜底显式不同，design §2.1-1 已记录裁定：首版不抽共享 helper，第三个同族消费者出现时再提升 flux-core）。
  - 点击 seek 仅 `onChange` 声明时事件项可点（`data-clickable` + tabindex + Enter/Space），payload `{ value, index, item }`（含 `type:'timeline:change'`，steps 同构）；controlled 只派发不 mutate、scope 写 valueStatePath（缺路径降级 local + dev 告警）；active marker `data-state="active"` + 根节点 `data-active-index` + `data-ownership`；reverse 下 active 按逻辑顺序解析。
  - 关键排障：scope/degraded 单测首版忘记声明 onChange 导致点击不可达（timeline v2 可点态以 onChange 声明为门）——补 onChange 后三态写回用例通过。
  - focused 单测 +10（timeline-renderer.test.tsx 共 17 条）+ playground 演示（w4b 页新增 4 个 v2 demo：scope 写回/controlled 只派发/reverse+active/未匹配无 active）+ e2e +5（w4b-process-display-family.spec.ts 共 9 条全绿）。
  - 状态同步：flux-guide v2 标注翻「已实现」；design.md §2.1-1 裁定 + §3 实现状态；roadmap 新增 timeline-v2 work item 标 done；`renderer-markers-and-selectors.md` 登记 timeline v2 marker（data-state=active/data-active-index/data-clickable/data-ownership）。
- 全量验证（full-green verification）：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（layout 包 103/103、playground 141/141 uncached 重跑）；e2e `w4b-process-display-family.spec.ts` 9/9（4 既有 + 5 新增）。
- closure-audit（独立子 agent，fresh session `ses_02fe894b8ffeZ1OugDo16g4WGL`）`approved`：0 Blocker/Major，2 Minor（roadmap 前置声明「closure audit 通过」需与 Plan Status 翻转同步、daily log 需记录本条目——均在本条目中收口）。Plan Status → `completed`；roadmap timeline-v2 标 done；`examples.manifest.json` timeline 保持 `runtime`（v2 为能力增强，非新组件）。

- **G1 graph 图查看器落地**（plan `docs/plans/2026-08-04-2030-1-g1-graph-viewer-plan.md`，Phase 1-4 全部完成，待独立 closure-audit 翻转 `completed`）：
  - 新包 `@nop-chaos/flux-renderers-graph` bootstrap：`@xyflow/react ^12.10.2` + `dagre ^0.8.5` + zustand/use-sync-external-store；`vite.workspace-alias.ts`/`tsconfig.base.json`/根 `tsconfig.json` 三处 monorepo 集成；`flux-i18n` 增 `flux.graph.searchPlaceholder/clearSearch` 键。
  - `graph` renderer 全链路：`schemas.ts`（GraphNode/GraphEdge/GraphSchema + DEFAULT_LEVEL_MAP）、`graph-layout.ts`（dagre LR/TB 分层投影 + 畸形数据过滤硬契约）、`graph-search.ts`（子串匹配 + 循环索引）、`graph-store.ts`（Zustand vanilla per-instance：视口/布局/搜索/选中）、`xyflow-canvas.tsx`（只读适配层：nodesDraggable/nodesConnectable/selectionOnDrag 全禁用 + elementsSelectable:false + 受控 viewport）、`graph-node.tsx`（node region 编译 + label 回退 + data-level/data-selected/data-matching）、`graph-renderer.tsx`（7 句柄 + 单选模型 + empty slot）、`graph-definitions.ts`（propContracts/eventContracts/componentCapabilityContracts）。
  - 关键实现裁定（排障所得）：
    - **受控 nodes 下不得在 node 上写 `selected` 字段**——会触发 xyflow elevateNodesOnSelect z-index 更新风暴（`Maximum update depth exceeded`）；单选态改经 node `data` 下发（renderer 自持），xyflow 层 `elementsSelectable:false` 禁用内部选中状态机。
    - **回调 identity 稳定性**：xyflow 的 StoreUpdater/SelectionListener 依赖回调引用，canvas 全部回调经 `canvasHandlersRef` 镜像 + `useCallback([])` 稳定化，否则每次渲染触发额外事件派发。
    - **受控视口写回守卫**：`onMove` 值相等时跳过 `setViewport` 写回，避免 prop↔store 更新风暴。
    - region 模板表达式须用 `$slot.<绑定>` 语法（同 list `item` region）：`${$slot.node.label}`（design §4.3 已补注，`example.json` 已同步）。
  - playground `#/graph-demo` 演示页（hierarchy+flow 双布局、搜索、单选联动、畸形/空数据降级、5 个 component handle 触发按钮）+ e2e `tests/e2e/graph-demo.spec.ts`（8 条程序化断言全绿）。
  - 单测 42 条（layout/search/畸形数据/只读画布配置/node 视图/单选 payload/句柄失败路径/searchable:false 句柄共存）。
  - 状态同步：`examples.manifest.json` graph `targetContract→runtime`；roadmap G1 `proposed→planned`（`done` 待 closure audit）；design.md §3/§12/Checklist G 实现状态翻转。
- 全量验证：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；`tests/e2e/graph-demo.spec.ts` 8/8。
- closure-audit（独立子 agent）Major 修复：**悬垂边 dev 告警补发**（`computeGraphLayout` 现返回 `skippedEdges`，renderer 数据变化时 `console.warn` 一次，focused 单测断言告警 + 跳过行为）；design.md §10 边容器 marker 按首版范围裁掉（默认 xyflow 边，无自定义 slot）；`searchable:false` + `component:search` 句柄共存场景补 focused 单测（句柄注册无条件、UI 框按 searchable 门控；e2e 仅覆盖 searchable:true 图的内置框）。
- **closure-audit 通过（3 轮独立 fresh-session 审计链）**：首轮 ses_0300735f6ffekxS3YUIyEUXBK9 发现 Major-1（悬垂边 dev 告警缺失）+ 2 Minor；修复后复审 ses_030029e93ffekTC9mjIpng7Bj0 批准 Major 修复、指出 Minor-2 日志声称不实；补单测 + 修正日志后终审 ses_02ff7df1effeXnWmcg46OSg7gf `approved`。Plan Status → `completed`；roadmap G1 `planned→done`；`examples.manifest.json` graph 已 `runtime`。
- **mission-driver 最终全量验证（full-green verification）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿；graph 包 fresh（uncached）重跑 typecheck + 42 单测通过；e2e `tests/e2e/graph-demo.spec.ts` 8/8 通过（19.6s）。unit + e2e 双绿，提交含 full-green 标记。

- **WorkbenchShell 面板调宽决策分析与计划起草**（文档产出，无代码变更）：
  - **分析报告**：`docs/analysis/2026-08-05-page-vs-workbench-layout-analysis.md` —— 裁定：(1) **不扩展 page 吸收 workbench 布局**（语义错位 + WorkbenchShell 已共享三 family + 折叠态须留 designer core + 契约膨胀）；(2) 左右收缩已有（WorkbenchShell rail + core store），保留在 workbench 侧；(3) 上下不收缩/不调高（top 是 chrome，bottom 当前无面板）；(4) 左右拖拽调宽**需要且 opt-in**，落 WorkbenchShell，状态归 family。
  - **文档补充**：`docs/architecture/designer-workbench-shell.md` 新增 Panel Resize Contract 节（opt-in、handle 贴中心侧、pointer + 键盘 Arrow 等价、role=separator + aria-valuenow/min/max、宽度 family-owned、collapsed 无 handle、默认宽度不变）+ "Relationship To Page Regions" 补 page 边界说明（page.aside 保持业务页单侧栏，page 不吸收 workbench）。
  - **计划起草**：`docs/plans/2026-08-05-1540-1-workbench-shell-panel-resize-plan.md`（draft，待独立子 agent plan review 后 `draft → active`）——4 Phase：① WorkbenchShell opt-in 调宽（Decision：保持 grid + pointer handle，拒绝 RRP 迁移；test-first 7 组单测）；② core shellState 宽度 + designer:setPanelWidths 命令/action 全链路（types/shell-state/shell-controls/snapshot/core.ts/command-types/adapter/action-provider/manifest）；③ designer-page 接线 + playground schema + e2e（resizable spec 新 + collapsible 8/8 零回归）；④ 文档同步。Test Strategy：必须自动化（共享组件 + public action 契约）。

---

<!-- feat-industrial-hmi branch entries (merged) -->

## industrial-hmi-editor E0 三项 spike 全绿收口：主路径 A 成立（手势仲裁 / 事件族 0 漂移 / 编辑态性能候选达标）+ 共识审查 3 轮 AGREE

执行 E0 spike plan `docs/plans/2026-08-05-1645-1-editor-spike-three-verifications.md`（4 Phase）全部完成。三项否决条件均不触发，R1/R7 均不触发，spike 报告经独立子 agent 文档共识审查 3 轮达成 AGREE。

- **plan path**：`docs/plans/2026-08-05-1645-1-editor-spike-three-verifications.md`（Phase 1 E0.1 / Phase 2 E0.2 / Phase 3 E0.3 / Phase 4 整合+共识审查 全部 `[x]` + Status `completed`）。
- **Phase 1 E0.1（手势仲裁，前置 run 已完成本会话核对 tick）**：scratch `~/sources/industrial-hmi-research/spike-editor/`（不入仓库）真实 leafer-ui@2.2.9 + `@leafer-in/editor`@2.2.9 + `@leafer-in/viewport`@2.2.9，深度探针 P1 证 `tree:viewport + move:drag:'auto' + editable:true + 真实点击` 下 Editor 完整接管拖拽（rect 移动 70×60px / viewport 不变 / `editor.move × 9 帧`）。**主路径 A 成立**，R1 不触发。spike-mock-leak 教训复现并纠正（初测误判根因 = demo 缺 `editable:true` + `editor.select()` API 替代真实点击）。
- **Phase 2 E0.2（事件族载荷）**：扩展 demo（+ `@leafer-in/text-editor@2.2.9` 触发 InnerEditor），真实手势触发六大事件族 + 多选 + InnerEditor + 框选，`page.evaluate` 抽真实 JSON 载荷（`results-e0.2-2026-08-05.json`）。**spike-event-drift 不触发**（六大族类名与 `research-render-engines.md §5:122` 完全一致；InnerEditorEvent 在 §5:122 未枚举 = ⚠️ 无害漂移，登记 Follow-up）。flux 衔接：leafer 事件携带循环 Leaf 引用**不能直传** `createNormalizedActionEvent`，需薄适配层抽纯 payload + nodeId；move/scale/rotate/skew 高频需节流；group/ungroup 需结构 diff（中 cost）。新发现：框选 selectArea 拖拽过程渐进式选择成功（峰值 listLen=5）但**默认 release 清空选区**（selectKeep 配置细节，E2 落点）。
- **Phase 3 E0.3（编辑态覆盖物性能）**：10 万图元组态（mulberry32 固定种子 42，buildMs 239ms / final 内存 102.8MB 远低于 320MB 红线），方案 A（leafer Editor 内置）vs 方案 B（独立 sky Group 自研）内部 rAF 驱动 fps 矩阵（规避 page.mouse IPC 污染）。实测（headless+swiftshader 下界）：两方案 ≤1k 选区 ~50fps；10k 选区 A=32.2fps / B=36.4fps（均 ≥30 候选）；editor.move per-call 8–10ms（n≤1k）/ 20.7ms（n=10k）；复合（n=1000 选区+视口平移）drag 49.9fps + pan 41.4fps。**编辑态包络候选三档位**（保守 ≥30fps@≤10k / 中性 ≥30fps@≤1k / 激进 ≥45fps@≤1k）有实测支撑；**覆盖物建议方案 A**（方案 B 作 fallback 路径 B）。**R7 不触发**（最低 32.2fps ≥ 30；最终包络经 E1.2 + R7 人工确认）；spike-perf-fail 不触发。
- **Phase 4 整合 + 共识审查**：spike 报告 `docs/analysis/industrial-hmi-editor/spike-2026-08-05.md` 全文整合（头节 + E0.1/E0.2/E0.3 三章 + 选型路径建议 + 包络候选 + R1/R7 + 局限性 + 产物附录）。**独立子 agent 文档共识审查 3 轮达成 AGREE**：R1（`ses_02dcb36b9…`）REVISE 3 Minor+3 Nit（文档卫生）→ 全部落地；R2（`ses_02dc63c28…`）REVISE 1 Nit n-4（选型路径建议否决条件口径，m-5 跨文档遗漏）→ 落地；R3（`ses_02dc2bd72…`）AGREE（n-4 LANDED + 0 新增 + 跨文档否决条件七处一致），未超 3 轮上限。
- **roadmap 回写**：`docs/components/roadmap-industrial-hmi-editor.md` 头部「文档共识审查记录」追加 E0.2/E0.3/E0 收口 4 条；Phase Status E0 保持 `planned`（待 closure-audit 通过后 → `done`，对齐 plan Phase 4 item 156）。
- **包级/workspace 验证状态**：纯文档 + scratch spike plan（无仓库代码变更），`pnpm typecheck/build/lint/test` 不适用（plan Closure Gates 按「纯文档计划」条款移除）；spike 实测脚本（measure-e0.1/e0.2/e0.3.mjs）即核心验证，全绿（六大事件族 + 多选 + InnerEditor + 性能矩阵全部 fired/达标）。
- **closure-audit 状态**：✅ **PASS**——独立 fresh-session sub-agent `ses_02dbd68e6fferdFWkfmfT0k3q5` 执行 closure-audit（2026-08-05），6 Closure Gates 全满足 + 跨文档否决条件 7 处一致 + 数字一致 + 无 stale placeholder。审计 verdict 许可 Plan Status → `completed`、E0 Phase Status `planned` → `done`、勾选 closure-audit gate，均已落地。
- **Follow-up**：① InnerEditorEvent 在 `research-render-engines.md §5:122` 未枚举（无害漂移，建议补文档，登记 editor mission Follow-up Backlog）；② 框选 selectArea 默认 release 清空选区（E2 设计落点，需 `selectKeep:true` 或自定义 release）；③ scratch 单层 App vs runtime 3 层 App 差异（E1.2 编辑态包络确立时在 runtime 下复测对照）。

## industrial-hmi 表达式收口推进：讨论文档经人工确认（含用户修正 `$xxx` 冲突）+ roadmap 增 I18 work item

承接上方讨论文档起草，本日完成确认与 roadmap 落地：

- **用户修正**：平台唯一合法表达式形式是 `${expr}`，**不存在 `$xxx` 简写**——`$` 开头标识符是保留内置命名空间（`$Math`/`$JSON`/`$Date`，flux-formula.md:196,205），桥接层 `normalizeFluxExpression` 接受的 `$xxx`/裸路径是 scada 域本地扩张，不经规整直交公式解析会落入内置命名空间路径，与内置变量规范冲突。讨论文档 §一/§二/§三-6/§五-D1 + 日志全部同步修正。
- **人工确认（D1-D6）**：对话「继续」= 决策项确认；讨论文档状态改「已确认」，附用户两条修正注记。
- **roadmap 修改（Rule 3 结构性调整，人工确认已落实）**：`docs/components/roadmap-industrial-hmi.md` 增 **I18 work item**（表达式一元化与点表可选间接层，`todo`）——Phase Status 行 + Work Items I18.1-I18.4（语法一元化/点表可选/迁移兼容/验证）+ Phase Details 段 + Dependency Graph 节点（`I15 --> I18`）+ Cross-Cutting 复用表 formula compiler 消费方补 I18。同步回写：总览「数据模型」行（点表可选间接层 + 唯一 `${expr}`，标注 `$xxx` 冲突与 I18 出处）、I2.2 行（移除 `$xxx` 表述）、头部文档共识审查记录追加 I18 立项登记条目。**I0-I17 状态与契约不动**。
- **下一步**：起草 I18 execution plan（Must automate 档，failing-first Proof 先行）→ 实施 → 文档同步（design-data-binding/design-symbols/design-renderer）+ 闭环审计。
- **未跑独立子 agent 共识审查 / 未跑 typecheck/build/lint/test**：纯编排层文档变更（讨论文档 + roadmap + 日志，无产品代码），workspace 验证不适用；I18 execution plan 起草时按 plan 共识审查条款执行（独立子 agent 审查直到共识）。

## industrial-hmi 表达式收口讨论文档起草（待人工确认）：`@{}` 弃用 → 单一 flux 语法，点表降为可选间接层

用户在对话中对 SCADA 图元表达式设计提三条质疑：`@{pointId}` 自定义语法的来源、为何不直接复用 flux-formula/flux-compiler（手写 404 行 expression-evaluator.ts 与平台复用表「禁止重复实现」相悖）、是否需要 `points.` 固定命名约定（可否从 data-source 取数后直连绑定、无内置固化约定）。经核对 live code 与 design 文档，三条质疑均成立：

- **成因**：I6.2（2026-08-03 Wave 2）早于 I10.3 桥接层；设计文档记录了三项支持理由——双轨自包含（Q3）、`$`/`@{}` 前缀隔离（design-data-binding.md:340）、INV-3「表达式能力以 flux-formula 为主」的能力收窄（:343）。其中「无 React 依赖复用障碍」事后不成立（flux-formula 仅依赖 flux-core、src 零 React 引用）。
- **同源可行性已证明**：`use-scada-points-bridge.ts:366` `createPrivateEvalScope({...pointValues, ...scopeData})` 已把点表值注入求值 scope 供 flux-formula 求值——`@{x}` 可归一化为普通标识符，无技术障碍。
- **点表职能拆解**（6 项）：map/scale/format 换算、状态判定、表达式求值可直接承接；高频遥测 out-of-band 通道（INV-4）、自包含离线、协议适配器归口不可替代 → 点表降为**可选间接层**而非语法强制；`points` 不固化、非保留名（`points.x` 只是普通 scope 标识符，访问形式为 `${points.x}`）。
- **`$xxx` 简写本身也是设计债（用户修正）**：平台唯一合法表达式形式是 `${expr}`，`$` 开头标识符是保留的内置命名空间（`$Math`/`$JSON`/`$Date`，flux-formula.md:196,205）；桥接层 `normalizeFluxExpression` 接受的 `$xxx`/裸路径是 scada 域本地扩张，不经规整直交公式解析会落入内置命名空间路径，与内置变量规范冲突——随 `@{}` 一并弃用，demo `flux: '$flow'` 改写为 `flux: '${flow}'`。
- **决策项 D1-D6**（§七 待人工确认）：D1 **唯一 `${expr}` 语法**弃用 `@{}` 与 `$xxx` / D2 点表可选 / D3 无保留命名空间（root 合并 scope 胜出契约沿用）/ D4 移除 expression-evaluator.ts（依赖收集改 flux 探针 + generation 失效，环检测保留）/ D5 INV-4 高频通道不变 / D6 迁移不做大爆炸（validator 可选容忍 + codemod，playground 已确认无 `@{}` 生产配置）。
- **流程（已于同日完成）**：本节起草后经人工确认（见上方「表达式收口推进」条目）——讨论文档状态改「已确认」，roadmap 按 Rule 3 结构性调整增 I18 work item，下一步起草 I18 execution plan。

- **未跑独立子 agent 共识审查 / 未跑 typecheck/build/lint/test**：纯讨论文档起草（无产品代码变更，workspace 验证不适用）；共识审查与 roadmap 修改待人工确认后按流程启动。

## industrial-hmi-editor E0.1 spike 完成：主路径 A（leafer-editor）成立 + 关键变因 editable:true 识别 + spike-mock-leak 教训复现

按 spike plan `docs/plans/2026-08-05-1645-1-editor-spike-three-verifications.md` Phase 1（E0.1 viewport+Editor 共存手势仲裁）执行完毕。scratch 工程 `~/sources/industrial-hmi-research/spike-editor/`（不入仓库）含 demo.js + measure.mjs（5 场景）+ measure-variants.mjs（3 配置变体）+ measure-deep-probe.mjs（3 深度探针）+ 3 个 results JSON。

- **初测 5 场景**：S1 wheel 平移 ✅ / S2 API select+拖拽 ❌ / S3 空白拖拽 ✅ / S4 框选 ❌ / S5 双向切换 ⚠️——S2/S4/S5 误判，根因是 demo 用法不完整。
- **变体测试 3 配置**：runtime default / `move:drag:false` / 无 move——全部 FAIL（API select 替代真实点击 + Rect 缺 editable:true）。
- **深度探针 3 探针**：P1 `tree:viewport + move:auto + editable:true + 真实点击` ✅ PASS（rect 移动 70×60px / viewport 不变）/ P2 `tree:default`（对照组）✅ PASS / P3 `tree:viewport` 无 move:auto ✅ PASS——**主路径 A 成立**。
- **关键变因识别**：Editor 接管拖拽的两个充要条件——① Rect 显式 `editable: true`；② 真实 pointerdown→pointerup 触发选中（`editor.select(target)` API 单独使用不触发完整接管）。这是 spike plan Failure Path `spike-mock-leak` 的真实复现（"禁以 mock 推断真实 API" + demo 配置不完整 = 误判风险）。
- **真实 API 锚点（15 项）**：14 项与 `research-render-engines.md §5` 一致，1 项漂移（`editor.list = []` 初次报 getter-only → 用 `editor.cancel()` 规避）。事件流真实抽取：`editor.select → tree.pointer.down → tree.pointer.up → tree.pointer.down → editor.move × 9 → tree.pointer.up`（首次点击 + 拖拽期）。
- **R1（选型变更）人工确认不触发**：主路径 A 成立，无需转路径 B。
- **适配层 cost = 极小**：仅需双态切换 `editable:true`（编辑态开 / 运行态关，R5 双态隔离落点）+ 监听 `editor.move` 转 diff（E2.4 undo-redo 设计落点）。无手势仲裁适配层。
- **spike 报告**：`docs/analysis/industrial-hmi-editor/spike-2026-08-05.md` §1（E0.1）完成，含 7 小节：测试矩阵 / 关键发现 / 主路径裁定 / 适配层 cost / 真实 API 锚点 / R1R7 标记 / Exit Criteria 核对。E0.2/E0.3 + Phase 4 待后续会话（fresh context）。
- **roadmap 头部记录追加**：`docs/components/roadmap-industrial-hmi-editor.md` 头部追加「E0.1 spike 完成」条目。
- **未跑 workspace 验证**：纯 scratch + 文档变更（spike 报告新文件 + roadmap 头部追加），无产品代码 / 无 plan / 无 audit 文件变更；typecheck/build/lint/test 不适用。

## industrial-hmi-editor mission 共识审查循环全绿：roadmap + E0 spike plan 经 3 轮独立 sub-agent 审查达成 AGREE

按 editor mission Cross-Cutting「文档共识审查」+ Rule 5，对刚起草的 `docs/components/roadmap-industrial-hmi-editor.md` + `docs/plans/2026-08-05-1645-1-editor-spike-three-verifications.md` 启动独立 fresh-session sub-agent 共识审查循环：

- **Round 1（独立子 agent `ses_02ec81a79ffe0qNKQPVRiY7FWm`，REVISE）**：0 Blocker / 1 Major / 4 Minor / 1 Nit 共 6 项修正——M-1 否决条件过宽（应仅手势仲裁 + API 漂移成本两项触发，E0.3 性能不否决，对齐 editor-initiation §4.3）/ m-1 runtime 复用点表遗漏 3 项「需扩展」标注（#1/#4/#9）/ m-2 包结构裁定归属不一致（E2 vs E4.1）/ m-3 M1 边界错误标 R8（§6 R8=否）/ m-4 E5.1 依赖缺 E2.6 / N-1 Phase 3 标题格式。6 项全部对照 live 上游依据落地。
- **Round 2（独立子 agent `ses_02ebf5257ffeeXf5npPaV6zmyO`，REVISE 轻量轮）**：R1 6 项全部 ✅ 真实落地无回退；全文复扫发现 1 项新增 Minor **m-5**——M-1 修正未同步到 spike plan Purpose line 14（"任一项否决 → 转路径 B"），跨文档不一致。m-5 落地：line 14 改为精确条件 + line 19 补「§4.3 否决条件仅两项」防误读。
- **Round 3（独立子 agent `ses_02eb8c9a0ffetLu1ZasS657b0v`，AGREE）**：m-5 两处修正 ✅ 真实落地且与 roadmap 三处精确化条件（line 29/96/191）+ editor-initiation §4.3 line 95 + spike plan 自身 Failure Paths（line 69-71）全链跨文档一致；关键改善：m-5 修正前 Purpose「任一项否决」与 spike-perf-fail（E0.3 → R7）矛盾 → 修正后与三条 Failure Paths 完全自洽。全文复扫 0 新增修正项，R1 修正无回退。
- **共识循环**：R1 REVISE 6 项 → R2 REVISE 1 项 m-5 → R3 AGREE，未超 3 轮上限（Cross-Cutting「文档共识审查」+ Rule 5），无人工裁决触发。
- **roadmap + spike plan 头部记录全部回填**：roadmap 头部 Round 1/2/3 三轮记录 + spike plan Draft Review Record 均含 fresh-session sub-agent task id + verdict + 修正项摘要，可追溯。
- **下一步选项**：① 启动 E0 spike 工程 scratch（`~/sources/industrial-hmi-research/spike-editor/`）开始真实 leafer 验证；② 暂停编辑器 mission，先推进当前 runtime mission I17（demo 优化）；③ 起草 E1 选型 gate plan（依赖 E0 结论，暂不启动）。
- **未跑 workspace 验证**：纯文档共识审查循环，无产品代码变更；typecheck/build/lint/test 不适用。

## industrial-hmi-editor mission 启动：3 份编排层产物起草完成（mission 配置 + roadmap + E0 spike plan）

follow-up 决策锁定 ①「I16 编辑器后继 mission — 启动 + 先跑三项 spike」落地。3 份编排层产物全部起草完成，按 industrial-hmi runtime mission 先例格式：

- **`missions/industrial-hmi-editor.json`**：mission 配置，commit format `feat(industrial-hmi-editor):`，对齐 industrial-hmi.json 字段集；description 明确「leafer-editor 插件底座（spike 验证后裁定）+ 自研组态语义适配层 + 3 里程碑 M1/M2/M3 + 10 复用点 + 5 review gate」。
- **`docs/components/roadmap-industrial-hmi-editor.md`**：编辑器 mission roadmap，11 阶段 E0–E10——E0 三项 spike / E1 选型 gate + 编辑态包络确认（R1+R7 人工确认）/ E2 编辑器设计 6 份 design-\*.md / E3 设计 gate / E4 包基建（包结构裁定）/ E5 M1 MVP 实现 / E6 M1 gate / E7 M2 连线+undo-redo / E8 M2 gate / E9 M3 工具箱+收尾 / E10 M3 gate + 整体收尾。**5 个固定 review gate**（E1/E3/E6/E8/E10）对齐 runtime mission 4-gate 先例；**10 项 runtime 复用点**全部 live 核对入 Cross-Cutting 复用表（含需扩展的 2 处：图元定义属性 schema 统一抽取 / 编辑态交互覆盖物族 + undo 事务语义）；**双态隔离**强约束（编辑态覆盖物不得在运行态启用，运行态包络不变，编辑态包络另立 E1.2 裁定）；**spike 先行纪律**（禁以 mock 推断真实 API，对齐 gate-3 §3 + bugs/76 教训）；roadmap 头部「文档共识审查记录」待 Round 1（本会话为起草阶段，共识审查未启动）。
- **`docs/plans/2026-08-05-1645-1-editor-spike-three-verifications.md`**：E0 spike plan（4 Phase）——E0.1 viewport+Editor 共存手势仲裁（决定选型主路径成立与否）/ E0.2 Editor 事件族载荷（六大事件族 + 多选框选 + InnerEditor 场景，对齐 gate-3 M-1 载荷面核对口径）/ E0.3 编辑态覆盖物密集场景性能（10 万图元 + 覆盖物挂载形态对比，决定编辑态包络数字候选 + R7 人工确认）/ Phase 4 整合 + 文档共识审查 + roadmap 回写。**scratch 目录** `~/sources/industrial-hmi-research/spike-editor/`（不入仓库）；**Failure Paths** 含 spike-gesture-fail（→ 路径 B + R1 人工确认）/ spike-event-drift / spike-perf-fail（→ R7 人工确认）/ spike-mock-leak（→ 立即回滚真实 leafer）；**纯文档 + scratch plan**，Closure Gates 按 plan guide「纯文档计划」条款移除 typecheck/build/lint/test。
- **当前 industrial-hmi runtime mission 头部记录追加「编辑器后继 mission 启动」条目**（`docs/components/roadmap-industrial-hmi.md:31`）：3 份编排层产物起草完成，未跑独立子 agent 共识审查（roadmap + spike plan 均为首版 draft，留下一会话或人审决策）；I0–I17 状态全部不动（I17 demo 优化仍 `todo`，可继续推进）。
- **未跑独立子 agent 共识审查 / 未跑 typecheck/build/lint/test**：三份产物均为编排层文档（mission json + roadmap md + plan md），无产品代码变更；按 editor mission Cross-Cutting「文档共识审查」+ Rule 5，**roadmap 与 spike plan 起草完成后、E0 spike 执行前必须先达成共识**（独立子 agent fresh session，≤3 轮），本会话为起草阶段，共识审查未启动；workspace 验证不适用。

## industrial-hmi follow-up 决策锁定：7 项延迟项逐一确认（6 项保持现状 + 1 项启动编辑器 mission）

对 `docs/components/roadmap-industrial-hmi.md` 全文 follow-up / deferred / watch-only residual / optimization candidate / Deferred But Adjudicated 类延迟项做完整盘点（Follow-up Backlog 84 条已全部带「已由 plan XXX 收口」标记，无未收口项；真正待决策的延迟项分布在 7 处），经人工逐一确认锁定（roadmap 头部记录 + line 31 详细登记）：

- **① I16 编辑器后继 mission — 启动 + 先跑三项 spike**（`editor-initiation.md §4.3`：viewport+Editor 共存手势仲裁 / Editor 事件族衔接 / 编辑态覆盖物密集场景性能）。编辑器 mission 独立编排（新 mission 文件 + 新 roadmap），不在当前 industrial-hmi mission 加 work item。
- **② mock bounds API 确定性建模 — 保持抛错防御现状**（plan `2026-08-04-2243-3` T2 optimization candidate）。未来编辑器交互层大量调 leafer bounds API 时再合并升级。
- **③ 平台 collector 复杂表达式支持 — 保持薄包装现状**（plan `2026-08-05-0325-1` out-of-scope improvement）。scada 域内 `analyzeFluxSubscriptions` 可消费平台能力即可，不在本 mission 改 flux-runtime 包。
- **④ D2 浏览器像素验证 — 保持 watch-only 状态**（plan `2026-08-04-2243-2` watch-only residual）。attrs 级单测 + happy-dom measureText fallback 已覆盖，浏览器像素级 e2e ROI 低。
- **⑤ scada-image 画布级诊断 — 维持现状**（open-audit `[P2]` Deferred But Adjudicated 后置 I16）。与 P1-8「不升级 status」契约一致，编辑器 mission 时代统一资源面诊断。
- **⑥ lastError 跨配置完整语义 — 维持表达式级去重现状**（plan `{2}` Phase 5 residual）。主体缺陷已收口，plan `2026-08-04-2243-1` L5 补 reload 清空后影响更小。
- **⑦ batch.add 性能观察项 — 确认 I14 已复测、关闭此项**（gate-3 m-8 归属 I14）。benchmark-report.md §3.1 已含实例化 88.5ms 拆解 + batch-add-probe 项目，复测对照已隐隐完成。
- **锁定效果**：②–⑦ 锁定为「保持现状」= 不再独立 plan 推进，相关条目保持在 Follow-up Backlog / plan 共识审查记录的可追溯位置；未来若上游变化（如 leafer-ui v3 升级 / 编辑器 mission 推进触发依赖）需重开决策，按 Rule 3 结构性调整经人工确认。① 编辑器 mission 启动为本次决策唯一推进项。
- **未跑独立 sub-agent 共识审查 / 未跑 typecheck/build/lint/test**：纯 roadmap 编排层决策记录（roadmap 头部记录追加一条「follow-up 决策锁定（2026-08-05，✅ 7 项逐一确认）」），无产品代码 / 无 plan / 无 audit 文件变更；Rule 5 不触发，workspace 验证不适用。

## industrial-hmi roadmap 结构性调整：新增 I17 work item（Demo 视觉优化 / 参考资源登记 / LeaferJS 对照页）— Rule 3 人工确认已落实

scada-demo 演示页美观度 review（用户反馈「画面画乱」），对照 LeaferJS 官方示例库与本地 meta2d.js / FUXA 工业组态参考资源后，发现 4 类轻量问题。**经人工确认（Rule 3 = 新增 work item 须人工确认）**，4 类问题合并为 **I17 work item** 立项，写入 `docs/components/roadmap-industrial-hmi.md` 编排层 5 处（roadmap 头部记录 / Phase Status / Work Items / Phase Details / Dependency Graph）；同日先登记的 Follow-up Backlog 临时子节已删除避免双写，保留 HTML 注释指针可追溯。

- **I17.1 scada-demo 坐标重排** — `apps/playground/src/pages/scada-demo.tsx:69-262` 工艺流程组态画面手写硬编码坐标（pipe / 设备 / 文本标签 x/y 全字面量、无对齐辅助 → 设备-管道错位、文本漂移、整体「画乱」）→ 参考 LeaferJS 官方 Playground（`https://www.leaferjs.com/playground/`）+ meta2d.js `packages/core/src/diagrams/` 图元形态，对坐标做一次重排（8px 对齐基线、统一管道路径轨迹、设备间距规范、文本标签锚定到设备 bounds），**保留所有现有 testid/bindings/events 不变**；scoped e2e 回归确认点击/联动/视口命令仍绿。依赖 I13.1。
- **I17.2 文档参考资源登记** — ① `design-engine.md` 增补「官方示例对照」附录（6-8 个 LeaferJS 官方示例链接：创建 App / 缩放平移 / 坐标转换 / 包围盒 / 局部渲染 / Group / Editor / Flow / viewport 插件）；② `design-symbols.md` 增补「第三方图元库参考」附录（meta2d.js diagrams / FUXA / OSHMI 三项及定性，**关键定性**：LeaferJS 官方示例是通用 Canvas 能力展示无 HMI 行业示例、meta2d 亦无 HMI 设备图元、FUXA 是 MIT SCADA/HMI 平台、OSHMI GPL-3.0 仅设计层参考）。依赖 I15.2。
- **I17.3 playground LeaferJS 官方示例对照页** — 新增独立路由 `#/leafer-examples`（**不进 home 卡片**，对齐 `#/scada-perf-scale` 先例），直接跑 LeaferJS 官方示例代码（App / Rect / 动画 / 视口 / Editor / Flow）。**价值**：① 团队学习 LeaferJS 原生能力；② 编辑器后继 mission 立项前对照 LeaferJS 官方 Editor/Flow 插件能力（评估「直接复用 leafer-editor vs 自研」决策）；③ 长期作为 LeaferJS 升级版本回归对照基线。**约束**：落地需复核 leafer-ui 依赖在 flux-renderers-industrial 包内（无需 playground 重复引入）；新增路由需同步 `route-model.ts` + `App.tsx`。依赖 I4.1。
- **范围裁定**：编辑器实现**不在此 work item**——按 `missions/industrial-hmi.json`「editor deferred to a successor mission」描述与 `editor-initiation.md` 立项材料，编辑器后继 mission 独立编排；本次不动 I16 范围与立项入口状态（I16 仍 `done`）。
- **未跑独立 sub-agent 共识审查 / 未跑 typecheck/build/lint/test**：纯 roadmap 编排层结构性调整（roadmap 头部记录 + Phase Status 新增 I17 行 + Work Items 新增 I17 表格 + Phase Details 新增 I17 段落 + Dependency Graph 新增 I17 节点 + Follow-up Backlog 删除临时子节保留 HTML 注释指针），无产品代码 / 无 plan / 无 audit 文件变更；Rule 5「文档共识审查」覆盖范围为 contract / 设计文档 / plan / review gate / 立项材料，**编排层 roadmap 结构性调整不在覆盖范围**（且 Rule 3 人工确认已落实）；若后续 I17 起草 execution plan，按 plan 共识审查条款执行。workspace 验证因零代码变更不适用。

## industrial-hmi plan-2026-08-05-0653-5 收口：0653 multi-audit P2-2 renderer 测试样板抽取两 Phase 全绿 + closure-audit approved（multi P2-2 收口 = 0653 audit 全部 P2 已收口）

执行 `docs/plans/2026-08-05-0653-5-hmi-renderer-test-boilerplate-extraction.md`（0653 multi-audit `[P2-2]` dim 14：8 个 scada-canvas renderer 测试文件近重复 `makeProps`/`configProp`/`scadaTestHandle`/`validConfig` 样板抽取到既有 `test-support/renderer-test-support.tsx`，消除 `cid` 漂移 7/9/11/19/21/31。纯测试 hygiene 重构，无产品代码变更）。两 Phase 先前 run 已落地，本 run 完成独立 closure-audit：

- **Phase 1（共享工厂落地，已 `[x]`）**：`test-support/renderer-test-support.tsx:86-137` 导出 4 个权威工厂 + `ScadaCanvasConfigProp` 类型——`makeScadaCanvasProps(overrides?)`（`cid` sentinel 默认 1，`meta.cid` 由之）、`configProp(config)`、`scadaTestHandle(cid)`（复用 `scadaTestHandleKey` 返规范 `ScadaTestHandle`）、`validCanvasConfig(overrides?)`（最小 1-symbol 默认 + 浅合并）；新建 `renderer-test-support.test.ts`（11 tests / 4 describe）断言默认值 + override 合并 + handle mount/remove round-trip + symbols 整体替换。
- **Phase 2（8 文件迁移，已 `[x]`）**：lifecycle/hardening/wiring/diagnostic-channels/events/handles/hover-overlay/event-actions 八文件删除全部局部 `makeProps`/`configProp`/`scadaTestHandle`/`validConfig`/`ScadaCanvasConfigProp` 定义（`rg` 在 `src/renderer/` 零命中），改 import 共享工厂；cid 经 `makeScadaCanvasProps({ cid })` 显式可见（lifecycle/hardening/wiring=7, diagnostic=19, events=11, handles=9, hover-overlay=31, event-actions=21），无 intra-file 漂移；lifecycle/wiring 2-symbol 默认（rect-1+rect-2）经 `validCanvasConfig({ symbols:[rect-1,rect-2] })` 显式传入，保 wiring rect-2 断言（`:98/:100/:103`）不丢（Draft Review R1 Major 正确 operationalize）；文件专有 helper（`textConfig`/`alwaysAnimConfig`/`warnReported`/`warnReportCount`/`makeRegion` 等）保留。
- **结果**：包级 668 tests / 47 files 全绿（baseline 657 + 11 factory 自测，无测试丢失/新增产品测试），workspace 全量验证（typecheck/build/lint 32/32 + test 全绿）。`git status` 仅 test 文件 + test-support + roadmap + plan 变更，产品代码未触。
- **closure-audit（独立 fresh-session 子 agent `ses_02ef03b19ffe8Qpp2W4fY1Gcxt`）**：判定 `approved`——9 项 checklist 逐条 live 核对（Phase 1/2 工厂签名 + 自测断言、`rg` ZERO_HITS、8 文件 import 共享工厂、per-file 唯一 cid 匹配、文件专有 helper 保留、**关键 rect-2 回归核查 PASS**：lifecycle `lifecycleConfig()` 保 rect-1+rect-2 `:27-30` 断言 `:60`、wiring `wiringConfig()` 保 rect-1+rect-2 `:25-31` rect-2 断言 `:98/:100/:103`、独立复跑包级 test 668/668 + typecheck/build/lint 32/32、deferred 诚实、roadmap:496 ✅），零 Blocker/Major，零 in-scope defect 降级。
- **plan 收口**：两 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 勾选（closure-audit gate 由独立 sub-agent 通过后勾选）+ `Plan Status: completed`。无 `> Source Audits:` 行（roadmap-sourced），源审计 `2026-08-05-0653-multi-audit` `Audit Status: closed`（idempotent，sibling plan `0653-2` P1 收口阶段已关闭）。`roadmap-industrial-hmi.md:496` multi `[P2-2]` 已标 `**已由 plan 2026-08-05-0653-5 收口（✅）**`（先前 run 回写，idempotent）。**Follow-up**：multi P2-2 为 0653 audit 15×P2 最后一条 backlog 项，本 plan 收口后 0653 audit 全部 P2 已收口（1×P1 由 `0653-2`、9×open P2 由 `0653-1/0653-3/0653-4`、15×multi P2 由 `0653-2/0653-4/0653-5` 及 sibling 各 plan 分担）。无剩余 plan-owned work。

## industrial-hmi plan-2026-08-05-1253-2 收口：0653 audit 剩余 2×P2 公共面/死模块 hygiene 单 Phase 全绿（closure-audit gate 待独立 sub-agent）

执行 `docs/plans/2026-08-05-1253-2-hmi-public-surface-dead-code-hygiene.md`（0653 multi-audit 剩余 2 条 doc/code drift P2 finding 收口：multi P2-1 `serializeScadaConfig` §11 公共面枚举遗漏 + multi P2-3 `scada-canvas.types.ts` 零导入者死模块被 §11 实现布局认可）。单 Phase 全部落地：

- **Phase 1（multi P2-1 + P2-3）**：
  - **Fix（P2-1 枚举）**：`design-renderer.md §11` 公共导出面枚举行补 `serializeScadaConfig`（标注为序列化契约函数 / design-contract 导出供 host 工具链直调，与 §4.3 已有 Decision 对齐），与 `src/index.ts:54` 实际导出逐项一致。
  - **Fix（P2-3 死模块）**：`packages/flux-renderers-industrial/src/renderer/scada-canvas.types.ts` 删除（`git rm`，1 行 re-export；全仓 rg 实核零导入者；`index.ts:16` 已直连 `./schemas.js` 再导出同名类型，无契约 break）。
  - **Fix（§11 布局）**：`design-renderer.md §11` 实现布局代码块移除 `scada-canvas.types.ts` 行（renderer/ 目录现仅 `scada-canvas.tsx` + `schemas.ts` + `renderer-definitions.ts` + `hooks/`）。
- **结果**：workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿；industrial 包 typecheck/build cache miss 重编译通过，证明删除死模块无隐藏消费者（无 `from './scada-canvas.types'` 或 `from '../scada-canvas.types'` 残留导入）。无新增 focused 测试（无可测行为——纯文档修正 + 零导入者死模块删除，Test Strategy 「不适用」档）。
- **plan 收口**：Phase 1 Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit gate 保留 `[ ]` 待独立 fresh-session sub-agent（执行 session 不自审，AGENTS.md human gate）。无 `> Source Audits:` 行（roadmap-sourced），源审计 `2026-08-05-0653-multi-audit` `Audit Status: closed`（idempotent，P1 收口阶段已关闭）。`roadmap-industrial-hmi.md` 两条 backlog bullet（multi P2-1 + P2-3）追加 `**已由 plan 2026-08-05-1253-2 收口（✅）**` marker。Follow-up：0653 audit 15×P2 仅剩 multi P2-2（renderer 测试样板 DRY）一条 doc/test-hygiene 类，留 backlog 按后续 round 节奏处理。

## industrial-hmi plan-2026-08-05-1253-1 收口：0653 audit 剩余 3×P2 引擎/渲染器热路径正确性与性能三 Phase 全绿（closure-audit gate 待独立 sub-agent）

执行 `docs/plans/2026-08-05-1253-1-hmi-hot-path-correctness-and-performance.md`（0653 post-remediation audit 剩余 3 条引擎/渲染器热路径 P2 finding 收口：multi P2-5 fill 分支未钳制 scale / open P2-1 subtreeIds O(N×subtree) / open P2-2 points-bridge 全量快照重建 + 合并优先级未文档化）。三 Phase 全部落地：

- **Phase 1（multi P2-5，视口 fill 分支 clamp-before-center）**：`use-scada-config-sync.ts` `applyInitialViewport` fill 分支 `scale` 经 `clampScale(...)`（从 `engine/viewport.ts` 导入）钳制后再算居中 `x/y`，与 contain 分支（`viewport.ts:67` 已 `clampScale`）对齐。Decision 裁定不抽共享 `fitFill` helper（fill max-scale 与 contain min-scale 语义不同，强行共享会模糊语义），保持内联 + 复用 `clampScale`。failing-first proof（构造 bounds 宽高极小 → rawScale=800 越界，断言 `setViewport` 收到已钳 scale=20 且 x/y 按钳制 scale 算 = -19.5/-14.5，修复前传 800 + x=0 漂移），自验修复前 RED、修复后 GREEN。既有 contain/center 用例不回归。`design-engine.md §4.4` 补 P2-5 增补注记。
- **Phase 2（open P2-1，TreeRegistry subtreeIds O(subtree)）**：`engine/tree-registry.ts` 增 `private childrenOf = new Map<string, Set<string>>()`，`add` 维护 parentId→子集索引（懒初始化 Set），`remove` 子树断链（`childrenOf.delete(id)` + 从 parent Set 移除 id，Set 空时删 key），`clear` 清空索引，`subtreeIds(id)` 改 pre-order DFS（`[id]` 起栈 + `childrenOf.get(cur)` 下推），O(subtree) 且不依赖「父先于子插入」隐式不变式。failing-first proof（深度≥2 逆序插入 `gc→c→g`，旧实现 forward + `ids.includes` 漏收 `gc` 仅返 `['g','c']`，DFS 返完整三层 `['g','c','gc']`），自验修复前 RED、修复后 GREEN。既有正序用例 + remove/clear 索引一致性新增单测（remove 后不含已删节点 / 子树根删后断链 / clear 后重插正常）全绿。caller（ConfigAdapter）先捕 `subtreeIds` 列表再逐个 remove 的遍历逻辑不变。
- **Phase 3（open P2-2，points-bridge generation-memoize + 合并优先级文档化）**：
  - **Decision**：裁定不走表达式 AST 过滤 point 引用（flux bare 标识符无法在无 AST 时安全区分 point 引用 vs 函数名/常量，误删被引用点值风险高于性能收益），改走 change-generation memoize。
  - **Fix（generation）**：`binding/point-store.ts` 增 `private generation = 0` + `getGeneration(): number`；所有改写点值/点集入口 bump——`applyValue`（仅命中真实变更，`Object.is`/deadband 不变不 bump）、`loadDeclarations`（直写 entries）、`restoreValues`（同直写）、`reset`（清空点集）；只读/订阅路径不 bump。
  - **Fix（memo）**：`use-scada-points-bridge.ts` effect 内读 `runtime.pointStore.getGeneration()` 与 `useRef` 缓存 `(generation, snapshot)` 比较，不变复用、变化重建；bridge 自身 `setPointValues` 写入后更新缓存到 post-write generation + 合并刚写入值（使 scope-only 变更仍命中 memo，不破坏反馈环）；config reload 时缓存对称重置（强制下次重建）。effect deps 保留 `scopeData`（scope 变更仍触发求值，仅跳过快照重建）。
  - **Proof**：① 合并优先级 contract lock-in（同 id point=1 与 scope=2 冲突 → evalScope[id]===2，scope 遮蔽 point，非 red-on-current 但守护未文档化既定行为）；② generation-memo skip 方向 red-on-current（spy pointIds，scope-only 变更后断言不重调，修复前 RED 修复后 GREEN）；③ generation-memo rebuild 方向回归守护（host 写 base=5 bump generation → 下次 scope 变更重建快照，新值进入 evalScope，守护 memo 不静默吞新值）。
  - **Fix（doc）**：`design-data-binding.md §9.1` 增 flux 求值 scope 构造与合并优先级段（`{...pointValues, ...scopeData}`，scope 在 id 冲突时遮蔽 point 为既定契约）+ point-values 快照 generation-memoize 段。
- **结果**：包级 657 tests / 46 files 全绿（较基线 647 增 10：Phase 1 fill proof + contain 不回归 2 / Phase 2 DFS proof + remove/clear 索引一致性 5 / Phase 3 合并优先级 + memo skip/rebuild 3）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **plan 收口**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit gate 保留 `[ ]` 待独立 fresh-session sub-agent（执行 session 不自审，AGENTS.md human gate）。无 `> Source Audits:` 行（roadmap-sourced），且仅关闭 0653 审计 15×P2 中的 3 项（剩 multi P2-1/P2-2/P2-3 + 其余 doc/test-hygiene 类），审计整体未全收口故 `Audit Status` 保持。`roadmap-industrial-hmi.md` 三 backlog bullet（multi P2-5 + open P2-1/P2-2）追加 `**已由 plan 2026-08-05-1253-1 收口（✅）**` marker。Follow-up：I14 benchmark 复测对照 generation-memo 实际帧时间收益（non-blocking，留 I14 节奏）。

## industrial-hmi plan-2026-08-05-0653-4 收口：0653 audit 剩余 4×P2 silent-defect（config-build/equality/诊断通道）三 Phase 全绿（closure-audit gate 待独立 sub-agent）

执行 `docs/plans/2026-08-05-0653-4-hmi-config-build-equality-diagnostic-fidelity.md`（0653 audit 剩余 4 项 P2 silent-defect C1-C4 收口——均「静默丢失信息」：config-build Group 降级丢 leaf attrs / 序列化相等 key 序敏感产冗余 override / 诊断通道丢 error stack/cause / probe 塌缩产 flux-deps-empty 误报）。三 Phase 全部落地：

- **Phase 1（C1，open P2-3）**：Decision 裁定方案 (a)+(b) 双层——`validate.ts` 增 `children` 仅 `scada-group` 约束（fail-fast 拒绝，错误消息 `<scope>.children is only allowed on scada-group nodes`，继续递归 children 最大化 diagnostic）；`config-adapter.ts:84` `isContainer` 收紧为 `node.type === GROUP_CONTAINER_TYPE`（defense-in-depth 守护 `engine.reset` 直调旁路，叶子带 children 按 leaf 构建保 `fill`/`stroke`/`width`/`height`，children 字段被忽略）。failing-first proof 2 用例（config-adapter buildNode 保 leaf attrs + validator fail-fast 拒绝），自验修复前 RED、修复后 GREEN。
- **Phase 2（C2，open P2-6）**：新建 `serialization/equality.ts` 导出 `deepEqual(a, b)`（own-keys 递归 stable，从 `diff.ts valuesEqual` 提取）；`diff.ts valuesEqual` 改为引用共享实现（行为不变，W5 修复保留）；`compound.ts deepEquals`（旧 `JSON.stringify` key 序敏感实现）删除并改为引用共享 `deepEqual`。消除 compound 路径的 W5 同类隐患（第三方 `registerScadaSymbol` 带 object-typed defaults key 序不同时 `diffInstanceProps` 不再产冗余 override）。failing-first proof 2 用例（key 序重排 object-typed defaults 判等稳定 + 嵌套数组混合反向验证），自验修复前 RED、修复后 GREEN。
- **Phase 3（C3 multi P2-4 + C4 multi P2-6）**：
  - **C3（诊断通道 cause 透传）**：`UseScadaPointsBridgeArgs.onError` 签名 `(code, message)` → `(code, message, error?)`（第三参可选，向后兼容现有 2-arg 桩；参数序刻意保留 `(code, message)` 前缀以稳定桩位置，仅追加 `error?`）；`reportOnce` 透传原始 error；`scada-canvas reportDiagnostic(code, message, error?)` 在 `env.monitor.onError` 处用 `new Error(message, error ? { cause: error } : undefined)` 包装（保留原始 stack/cause 链），host 监控可经 `Error.cause` 定位 formula evaluator 源。failing-first proof（注入 throw compiler 断言 onError 第三参为原始 Error 实例含 stack），自验修复前 RED、修复后 GREEN。
  - **C4（消双报）**：`extractExpressionDepsViaProbe` 返回类型由 `string[]` 改为 discriminated result `{ status: 'ok'; paths: string[] } | { status: 'compile-failed' | 'create-state-failed' | 'evaluate-failed' } | { status: 'deps-empty' }`；`analyzeFluxSubscriptions` 仅在 `status === 'deps-empty'` 且 `expressionReadsScope` 为真时入 `depsEmptyExpressions`，`status === 'ok'` 取 paths（含空 paths：静态表达式走此分支不诊断），compile/createState/evaluate 失败跳过（由 bridge effect 的 `flux-compile-failed`/`flux-evaluate-failed` 真报覆盖）。既有 `scada-points-bridge.test.tsx:110-122` 直测同步迁移消费 discriminated result。failing-first proof（mock throwing compiler 断言 `flux-compile-failed` 真报保留 + `flux-deps-empty` 误报消除），自验修复前 RED、修复后 GREEN。注：flux-formula 对畸形 `${...}` 一律按 static 字面量处理（不抛 compile 错），生产 compiler 下畸形表达式走 ok 空集分支（不诊断）；C4 修复主要覆盖 host 自定义 compiler / 未来 compiler 版本 / 真实 createState/evaluate 失败等可触发 compile-failed 的路径。
- **owner-doc 同步**：`design-renderer.md` 增 `children` 字段契约段（C1，Group 降级契约）+ §4.3 校验行补「`children` 仅 `scada-group`」+ §8.1 诊断出口段补 cause 透传语义（C3）；`design-data-binding.md §9.1` 增 probe result 语义段（C4，discriminated result + 跳过规则 + flux-formula static 字面量限制注记）。
- **结果**：包级 647 tests / 46 files 全绿（较基线 640 增 7：C1 buildNode defense + validator reject 2 用例 + C2 key-order stable 2 用例 + C3 cause transparency + C4 消双报）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **plan 收口**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit gate 保留 `[ ]` 待独立 fresh-session sub-agent（执行 session 不自审，AGENTS.md human gate）。源审计 `2026-08-05-0653-{open,multi}-audit` 已由 sibling plan `2026-08-05-0653-2` 关闭（P1 收口），本 plan 仅回写 roadmap Follow-up Backlog 对应 per-line P2 条目（multi P2-4/P2-6 + open P2-3/P2-6 四项追加 `**已由 plan 2026-08-05-0653-4 收口（✅）**` marker）。Follow-up：剩余 6 项 P2（multi P2-1/P2-2/P2-3/P2-5 + open P2-1/P2-2）为 doc/test-hygiene/perf 类，留 backlog 按 mission 节奏择期处理。

## industrial-hmi plan-2026-08-05-0653-3 收口：0653 open-audit 5×P2 binding/state silent-defect 三 Phase 全绿 + closure-audit approved（B5 re-entrant pointId 归属为本 run 实际代码变更）

执行 `docs/plans/2026-08-05-0653-3-hmi-binding-state-resolution-correctness.md`（open-audit 5 项 P2 silent-defect B1-B5 收口——均「通过 validate 但运行期静默产出错误视觉/状态/数据归属结果」）。Phase 1（B1+B2）、Phase 2（B3）由先前 run 落地（已 `[x]`）；本 run 执行 Phase 3（B4+B5）。三 Phase 全部落地：

- **Phase 1（B1+B2，先前 run）**：`bind-resolver.ts` `resolveBinding` 文本类属性集合（`text`/`fill`/`stroke`/`textColor`）仅对这些 property 施加 format（B1，open P2-4，`visible:false` 绑定真正隐藏）；line/arrow/pipe `applyProps` 从 width/height 重算 points 写回 `node.points`（B2，open P2-8，pipe 补齐 `defaultGeometryPoints`，Decision 裁定方案 (a)）。
- **Phase 2（B3，先前 run）**：`value-to-state.ts` `defaultState` 命名偏好链（run→normal→off→首键）+ `ScadaStateDeclaration.stateSource?`（`"pointId"` 或 `"pointId.property"`）显式 state-driver，`dirty-collector.ts` 优先 consult `declaration.stateSource`（B3，open P2-5）。
- **Phase 3（B4+B5，本 run）**：
  - **B4（open P2-7）**：Decision 裁定方案 (b) validator 拒绝 declaration 级 `scale.expression` 并指向 binding-scale（binding 层 `applyScale` 已消费 expression），同时关闭 `point-store.convert` 与 `value-to-state.applyLinearScale` 两处 drop site。`validate.ts:299-306` 拒绝 + `serialization.test.ts:129-151` failing-first proof 由红转绿（先前 run 已落地，本 run 确认 + tick）。
  - **B5（open P2-9）——本 run 实际代码变更**：`point-store.ts` 删除可变字段 `lastNotifyPointId`，`EventHub` 构造不再传 `onListenerError`（原读 `lastNotifyPointId`），`applyValue` 改用 `this.events.emitWith('point:change', (error) => this.reportSubscriberError(pointId, error), payload)`——per-emit 闭包捕获当前 `pointId`，消除 re-entrant `setPointValue`（listener 回写另一点）下字段被覆盖的归属竞态，错误始终归属正在派发的 pointId。`scada-robustness-hardening.test.ts` 新增 failing-first proof（listener A for p1 re-entrant 写 p2 翻字段、listener B throw on p1-emit → 断言归属 `'p1'` 非 `'p2'`）；自验修复前 RED、修复后 GREEN。
- **结果**：包级 640 tests / 46 files 全绿（较 639 增 1：B5 proof）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **plan 收口**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit gate 由独立 fresh-session sub-agent（task `ses_0300d86c3ffeYQJYXK2oqgkuD3`）执行，verdict `approved`（逐条 file:line citation 经 live repo 核对）。`roadmap-industrial-hmi.md` open-audit P2-4/P2-5/P2-7/P2-8/P2-9 五 backlog bullet 追加 `**已由 plan 2026-08-05-0653-3 收口（✅）**` marker + 头部追加收口记录。无 `> Source Audits:` 行（roadmap-sourced），且仅关闭 open-audit 9×P2 中的 5 项，审计整体未全收口故 `Audit Status` 保持（sibling plan `2026-08-05-0653-4` + 剩余 backlog 待续）。Follow-up：line/arrow/pipe width/height 绑定几何响应的浏览器像素验证为 watch-only residual（attrs/逻辑级单测已覆盖）。

## industrial-hmi plan-2026-08-05-0653-2 收口：0653 审计 4×P1 remediations 四 Phase 全绿（oversized 门禁恢复 + flow diff/indicator z-order/W3 revert-vs-binding 三 live defect 修复）

执行 `docs/plans/2026-08-05-0653-2-industrial-hmi-audit-p1-remediations.md`（两份 0653 审计——open-ended adversarial + multi-dimensional——共 4 条 P1 收口）。四 Phase 全部落地：

- **Phase 1 拆分 2 个超限测试文件（multi P1-2，恢复 oversized 硬门禁）**：`scada-points-bridge.test.tsx` 769→主（427，bridge 主路径：订阅/求值/点值下发）+ `scada-points-bridge-diagnostics.test.tsx`（391，flux 错误上报/去重/reload-clear/deps-empty 诊断类）；`scada-engine.test.ts` 718→主（344，lifecycle/test handle/commands）+ `scada-engine-plugin-sync.test.ts`（190，插件 zoom/move 同步/视口钳制/交互覆盖层 P1-7/P1-9/D3）+ `scada-engine-events-declarations.test.ts`（250，事件桥接线/getSymbolDeclarations/image cache）。5 文件均 <700，纯机械按 `describe` 域边界切分，不改测试语义/断言。`pnpm check:oversized-code-files` industrial 包 0 失败（16→14 ERROR）。
- **Phase 2 修复 `diffScadaConfig` 丢弃 `flow` 字段（open P1-1）**：`serialization/diff.ts:3-30` `SYMBOL_KEYS` 在 `'textSize'` 与 `'custom'` 之间机械补 `'flow'`（与 `config-types.ts:78` 声明序对齐）。failing-first proof 5 用例（仅 flow.enabled 变更、flow.speed/dash 子字段、同值判等、undefined→定义、定义→undefined）+ 集成 proof 2 用例（`diffScadaConfig` → `engine.applyDiff` → `pipe-junction.applyProps` 链路：flow.enabled true→false 时 stub `dashPattern` 由 `[6,4]`→`[]`，flow.dash 变更切到新 dash 序列）。
- **Phase 3 修复 `scada-sensor-control-indicator` 绘制顺序（open P1-2）**：`symbols/sensor-control/indicator.ts:42-45` 复合子序 swap `[lamp(body), housing]` → `[housing, lamp(body)]`——leafer `Group` 后入子在上层渲染，原序使不透明 housing（`#455a64`，覆盖全 bounds）绘制在 lamp 之上完全遮挡灯体；swap 后 housing 先绘作背景层、lamp 后绘于上层状态色可见。`name:'body'` 仍在 lamp 确保 `applyCompositeProps` 状态色路由不变。z-order 可观测 proof 3 用例（mock `children` 数组按入序保留即 render order，断言 `[housing, lamp]` 入序 + 状态色 fill 在 lamp 上层可见 + 与兄弟复合图元 motor 子序语义对齐消除离群点）；自验 swap 前 3 例全红、swap 后全绿。
- **Phase 4 修复 W3 revert 覆盖同帧 binding 值（multi P1-1）**：`symbols/visual-state.ts` `StateVisualApplier.applyState` revert 分支（line 61-74）revert 某字段前查 `instanceProps.bindings?.[key]`，若有 binding 跳过该字段的 revert collect（同帧 `collectBindings` 已把 binding 解析值写入 pending，Map last-write-wins 是正确终值；revert 会覆盖为 base/reset 空值）。无 binding 时 revert 行为不变。failing-first 扩展 alarm-storm frame 3 fill-value 断言（`frame3Arg['s0']?.fill === 2` 即 binding 解析值，修复前为 `''` base/reset）+ 聚焦回归 3 用例（alarm→unstyled 退出停在 binding 值、无 binding revert 行为不变、active 态 state-style 仍胜出）。W3 Decision 注记补 binding-vs-revert 三路裁定；`design-data-binding.md §4.3` 同步。
- **结果**：包级 628 tests / 46 files 全绿（较 615/43 增 13 tests / 3 files：flow diff 5 + 集成 2 + z-order 3 + W3 revert 3）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿、`pnpm check:oversized-code-files` industrial 包 0 失败。
- **plan 收口 + 源审计关闭**：四 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit 项保留 `[ ]` 待独立 fresh-session sub-agent（执行 session 不自审，AGENTS.md human gate）。源审计 `2026-08-05-0653-open-audit` / `2026-08-05-0653-multi-audit` `Audit Status: planned → closed`。`roadmap-industrial-hmi.md` 头部追加本 plan 收口记录。15 P2 已在 roadmap Follow-up Backlog「2026-08-05-0653 post-remediation audit P2」子节追踪，按 mission 节奏择期处理。

## industrial-hmi plan-2026-08-05-0653-1 收口：roadmap Follow-up Backlog 标注漂移 hygiene pass（纯文档，无代码变更）

执行 `docs/plans/2026-08-05-0653-1-hmi-roadmap-backlog-annotation-consistency.md`（plan `2026-08-05-0325-1` Non-Blocking Follow-ups line 156 登记的 deferred：roadmap Follow-up Backlog `2026-08-04-2242` post-remediation 子节 State & lifecycle（L1-L6，roadmap:444-449）+ Wiring W3（roadmap:462）共 7 条 per-line 条目底层缺陷实际已由 plan `2026-08-04-2243-1` 三 Phase 收口，但 per-line `已由 plan ... 收口` marker 从未回写）。纯文档 hygiene pass，不触碰任何代码：

- **标注回写**：逐条补齐 7 条 marker（格式与同节兄弟条目 D1-D4/W4/W5/T1-T6/A1/Doc1-Doc3 对齐：`plan + Phase + (Lx/W3) + 一句话落地摘要 + Proof`）——L1 RefreshPipeline destroyed 门控、L2 DirtyCollector collect/flush/flushFrame 守卫对称、L3 releaseRuntime collector 单一 owner、L4 pendingSkipNonce per-import nonce、L5 lastReportedErrors 与 compiledCache 对称清空、L6 reloadConfig useCallback 稳定身份、W3 collectStates/StateVisualApplier 合帧 owner 裁定（方案 a）。
- **全节扫描兜底**：Follow-up Backlog 全节（roadmap:366-483）69 条 backlog bullets 全部带 marker，无其他缺口。
- **roadmap 头部共识审查记录**：追加一条本 hygiene pass 的 closure 记录（对齐既有 closure 记录格式）。
- **结果**：纯文档计划，Test Strategy = 不适用；无 Phase Status / Work Items / Dependency Graph / Cross-Cutting / Rule 正文语义改动。workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿（FULL TURBO，确认文档变更未扰动既有 green baseline）。

## industrial-hmi plan-2026-08-05-0325-1 收口：复杂 flux 表达式订阅 `flux-deps-empty` 诊断（W1 successor）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-05-0325-1-hmi-flux-deps-empty-diagnostic.md`（roadmap Follow-up Backlog W1 / 源审计 `2026-08-04-2242-multi-audit` §Wiring `[P2]`：复杂表达式（`${analog.temp + 1}` 类）在平台依赖收集 probe 失败时 `extractExpressionDepsViaProbe` 静默返 `[]` → `useScopeSelector` disabled → 表达式永不随 scope 更新且对 author 不透明；successor 从未创建，本 plan 即该 successor）。两 Phase 全部落地 + 独立 closure-audit 通过：

- **Phase 1 Proof 先行（failing-first）**：`scada-points-bridge.test.tsx` 增两例——(1) 注入 collector 返空 deps 的 `ExpressionCompiler` stub（`createState` 返 root 无 `dependencies`、`evaluateValue` 返值），渲染 `useScadaPointsBridge`，断言 `onError` 收**一次性** `flux-deps-empty`（message 含 `${analog.temp + 1}` 片段）、重复 scope 更新不重报、仅 flux-deps-empty 无 status-upgrading 码（实现前红）；(2) 纯字面量 `${1 + 2}`（无标识符）不触发 flux-deps-empty 的负向断言。typecheck 通过，红例确认「flux-deps-empty 未上报」。
- **Phase 2 检测+上报+注册表+i18n+文档**：(a) `scada-errors.ts` `SCADA_ERROR_CODES` 增 `'flux-deps-empty'`，注释块归入「不升级画布 status」组；(b) i18n en-US/zh-CN 对称位置增 `industrial.scada.error.flux-deps-empty` 文案；(c) 新增 `expressionReadsScope(candidate)` 谓词（含标识符即为真，排除 `${1 + 2}`）；(d) 引入 `analyzeFluxSubscriptions(config, options) → { paths, depsEmptyExpressions }`（复杂表达式分支 probe 返空 deps 且 `expressionReadsScope` 为真时入 `depsEmptyExpressions`），`extractFluxScopePaths` 降为薄包装返 `.paths`（保留既有签名/导出/测试不破坏）；(e) `useScadaPointsBridge` `useMemo` 改调 `analyzeFluxSubscriptions`，新增 effect 对 `depsEmptyExpressions` 每项经 `reportOnce(expression, 'flux-deps-empty', 合成 Error)` 一次性上报（不升级 status）；(f) `scada-canvas.tsx` `reportDiagnostic` 把 `flux-deps-empty` 纳入 `monitor.onError` expression-phase 分支；(g) `scada-errors.test.ts` 增注册表 + i18n key 断言；(h) `scada-canvas-diagnostic-channels.test.tsx` 增 reportDiagnostic 分支 focused 用例（stub compiler 触发 flux-deps-empty → monitor phase `expression`/details.code `flux-deps-empty` + 画布 ready + 零 scada:error）。
- **owner-doc 同步**：`design-data-binding.md §9.1` 增 `flux-deps-empty` 诊断语义段（触发/一次性/非升级/不派发 `scada:error` + 静默 disable 残留已 surfaced 注记 + `expressionReadsScope` 启发式限制注记）；`design-renderer.md §8.1` 诊断出口段 + 错误码枚举行补 `flux-deps-empty`。
- **结果**：包级 615 tests / 43 files 全绿（+3 focused 用例：bridge flux-deps-empty 一次性 + 字面量负向 + reportDiagnostic 分支；+2 既有测试断言扩展）、workspace 全量验证全绿（typecheck 32/32、build 32/32、lint 32/32、test 59/59）。
- **closure-audit（独立 fresh-session 子 agent）**：判定 `approved`——见 plan `Closure Audit Evidence`。
- **plan 收口 + roadmap 回写**：两 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`；`roadmap-industrial-hmi.md` Wiring dim 22 W1（roadmap:459）回写「已由 plan 2026-08-05-0325-1 收口」。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：平台 collector 扩展复杂表达式支持面（out-of-scope improvement，独立包演进）、roadmap 标注漂移（L1-L6/W3 缺收口 marker，roadmap hygiene 轮统一回写）。

## industrial-hmi plan-2026-08-04-2243-3 收口：HMI 验证 fidelity / 公共面 / 文档漂移十一项 finding 三 Workstream 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2243-3-hmi-verification-fidelity-public-surface-doc-drift.md`（multi-audit dim 14/23 六项 P2 T1-T6 验证 fidelity + dim 03 一项 P2 A1 公共面 + dim 16 三项 P2 Doc1-Doc3 文档漂移 + dim 22 一项 P2 W2 死代码，共十一项 in-scope finding），三 Workstream 全部落地 + 独立 closure-audit 通过：

- **WM1 mock fidelity（T1+T2）**：`leafer-ui-mock.ts` `MockLeafer` 改 `extends MockZoomLayer` + `get zoomLayer(): this`（对齐真实 leafer `tree.zoomLayer === tree` 身份，消除独立实例掩蔽产线 viewport transform 漂移）；`MockApp.destroy()` 移除原 `this.zoomLayer.destroy()`（身份变 `this` 后会无限递归）；`MockLeaf` 加 `getBoundsToWorld()`/`getBounds()`/`get worldBox` 抛「mock 不建模 bounds API」错。新增 `leafer-ui-mock.test.ts` mock-invariant 单测（身份 `app.tree.zoomLayer === app.tree` + zoom 能力写回 + bounds 抛错 + toJSON 不含 getter）。
- **WM2 e2e 断言加固（T3+T4+T5+T6）**：T3 `scada-pressure-demo.spec.ts` 10k/overview-restore 两测试补 `assertScadaCanvasRendered`；T4 `scada-perf.spec.ts` memory + 10k-refresh 两测试补硬门；T5 `scada-canvas-assert.ts` 像素探测根因修复——旧实现 `canvas.evaluate` 只读 `[data-slot="scada-canvas-canvas"]`（`querySelector('canvas')` 返回的首个 ground 透明背景层，漏掉 tree 层已绘内容），改扫 `[data-slot="scada-canvas"]` 容器内**全部** canvas 任一命中非零像素即 confirmed，非空场景（symbols>0）`fallback-all-zero` 判失败（新增 `allowZeroPixels` 例外通道用于已知 off-content 视口，如 perf throughput 循环）；T6 五处 `not.toThrow()` 弱断言换副作用负向断言（robustness `isPlaying===false` ×2 + 无 animator 直接调用；lifecycle `getPointValue('level')≠999` 证销毁 guard 不写；points-bridge `readOwn()` 不变证只读 scope；definitions 直接调用 + `toHaveLength(1)` idempotency）+ 删重复 `computeSymbolBounds([])` 测试。
- **WM3 公共面/死代码/文档（A1+W2+Doc1+Doc2+Doc3）**：A1 `index.ts` 移除 `export type { IndustrialRendererSchema }` + `renderer-definitions.ts` 删类型别名及未用 import（仓内零代码消费者）；W2 `config-adapter.ts` 删 `setConfig()` 死方法；Doc1 `design-renderer.md §10` 表 loading/error 行 marker 列 `—` → `nop-scada-canvas-loading`/`nop-scada-canvas-error`；Doc2 §6 retire `scada-canvas-overlay` slot 引用（与 §10 一致）；Doc3 `design-engine.md §8.3` ScadaTestHandle 补 `setPointValues?`/`measureAddStrategies?` + 非公共契约注（对齐 `test-handle.ts` 9 成员）。
- **结果**：包级 612 tests / 43 files 全绿（+7 mock-invariant 回归用例）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿、scada e2e 全量全绿（pressure-demo 3/3 + perf 5/5 + demo/edge-cases 18/18；非空场景 pixelProbe=confirmed、空场景正确允许 fallback-all-zero）。
- **closure-audit（独立 fresh-session 子 agent `ses_031cbf1bcffeusatLqH7U1VFfY`）**：判定 `approved`——11 项 finding 逐项 live 核对（interface↔semantics trace：`MockLeafer extends MockZoomLayer` + `get zoomLayer()` 返 this + destroy 无递归；bounds 三方法/getter 抛错；e2e 硬门全覆盖；T5 全 canvas 扫描 + 非空严格门；T6 五目标文件 `not.toThrow()` 清零 + 替换为副作用负向断言；A1/W2 仓内零消费者；Doc1-3 文档↔live code 对齐），deferred 诚实（T6-residual 实际未触发，5 处全清零）。
- **plan 收口 + roadmap 回写**：三 Workstream Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；`roadmap-industrial-hmi.md` 回写收口记录（dim 14/23 T1-T6 + dim 03 A1 + dim 16 Doc1-Doc3 + dim 22 W2 标注「已由 plan 2026-08-04-2243-3 收口」+ 共识审查记录加 ✅ 条目）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：W1 表达式订阅诊断 deferred（successor 见 plan `{2243-1}`）、mock bounds API 升级确定性建模为 optimization candidate。

## industrial-hmi plan-2026-08-04-2243-2 收口：HMI 显示几何/视口数学/数据路径守卫六项 finding 三 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2243-2-hmi-geometry-viewport-data-path-correctness.md`（multi-audit dim 21 四项 P2 D1-D4 显示几何/视口 + open-audit dim 22 两项 P2 W4/W5 数据路径守卫，共六项 in-scope finding），三 Phase 全部落地 + 独立 closure-audit 通过：

- **Phase 1 默认几何 + base text 居中（D1+D2）**：`symbol-types.ts` `ScadaSymbolDefinition` 增 `defaultGeometryPoints?: (node) => unknown[]` resolver；`polygon.ts` 导出 `DEFAULT_TRIANGLE` + resolver、`line.ts`/`arrow.ts` 增由 width/height 派生的 `[0,0,w,h]` resolver（与 create 几何同源，抽默认值常量）；`use-scada-config-sync.ts` `boundsOfNode` 无显式 custom.points 时 consult 定义默认几何算包围盒（重构 `boundsFromCustomPoints` → 通用 `boundsFromPoints(node, raw)`），fit 不冲 MAX_SCALE；`text.ts` 增 `measureTextWidth`（canvas 2d `measureText` + happy-dom/jsdom 0.6em/char fallback）+ 自动宽下 center/right 按测量设 width 并把原点平移到对齐锚（center→文本中心、right→文本右缘），居中生效。Proof：D1 focused 单测（default-triangle polygon bounds {100,200,100,86}、零高 line {0,0,100,0}、fit scale<MAX_SCALE、显式 points 优先、非 points 族回退）+ D1 e2e 几何断言（`scada-edge-cases.spec.ts` 默认几何 fit scale<20 且>1，playground 加 `default-geometry-fit` 载体）；D2 attrs 级单测（width=60/x=70 center、x=38 right、left 不触发、显式 width 不覆盖）。
- **Phase 2 wheel 锚点 + viewport 契约（D3+D4）**：核实 leafer-in viewport 源码 `getZoomEventData` 透传指针 `event.x/y`（screen 坐标），`scada-engine.ts` `handlePluginZoom` 钳制改用 `readZoomAnchor(event)` 光标锚（`scaleOfWorld(cursor, clamped/raw)`）→ 光标下内容点保持固定、超界无视觉偏移；事件缺 x/y（程序触发/旧消费方）回落 `{0,0}`（P1-9 命令路径不变式：视口 x/y 不变）。D4 Decision 维持 viewport prop 不对称现状（`design-renderer.md §8.3` 已显式标注「policy 仅 full/reset 路径应用」，复核确认，不新接 effect）。Proof：D3 focused 单测（光标 {200,100} 钳制 25→20 → 视口 {140,75,20} + `getViewportPoint({150,80})==={200,100}` 内容固定；缺坐标回落非回归）。
- **Phase 3 数据路径守卫（W4+W5）**：`use-scada-points-bridge.ts` `isScadaPrimitive` 改导出（flux 桥接与 host 句柄共用）；`use-scada-handles.ts` `setPointValue` case 复用校验，非原始值（对象/数组/null）返 `{ok:false}` 且点表不变（移除 `as ScadaPrimitive` 强转）；`diff.ts` `valuesEqual` 由 `JSON.stringify` 改 own-keys 递归比较（按 a 的 keys 在 b 查表 + 递归、数组按 index），key 插入序不再影响判等。Proof：W4 focused 单测（`use-scada-handles.test.ts` 新建——对象/数组/null→{ok:false}+点表不变、primitive 接受）；W5 focused 单测（异源 key 序不同 shadow/custom/scale 判等不假阳性、真实变更仍检出、缺键检出、数组序敏感）。
- **owner-doc 同步**：`design-engine.md §4.4` 增 D3 增补（wheel clamp 光标 screen 锚，与 P1-9 命令路径 screen 原点锚区分）；`design-renderer.md §8.5` `setPointValue` 行补 W4 `invalid-point-value` 失败路径 + `isScadaPrimitive` 注记。
- **结果**：包级 606 tests / 42 files 全绿（+26 focused 回归用例跨三 Phase：D1 8 + D2 4 + D3 2 + W4 5 + W5 7）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **closure-audit（独立 fresh-session 子 agent `ses_031eb2b3dffeqK21aYXa7Wgvy0`）**：判定 `approved`——六项 finding 逐项 live 核对（interface↔semantics trace：`boundsOfNode` 消费 resolver、`handlePluginZoom` 消费 `readZoomAnchor`、`setPointValue` 消费 `isScadaPrimitive`、`diffScadaConfig` 消费 `valuesEqual`）、focused 测试断言正确结果、deferred 诚实（D2-browser-pixel watch-only residual 非阻塞）、owner-doc 一致、独立复跑包级 typecheck/test 606/606 全绿。
- **plan 收口 + roadmap 回写**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；`roadmap-industrial-hmi.md` 回写收口记录（Display & positioning dim 21 D1-D4 + Wiring dim 22 W4/W5 六项标注「已由 plan 2026-08-04-2243-2 收口」+ 共识审查记录加 ✅ 条目）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：D2 浏览器像素验证 watch-only residual（无 successor）、W1 表达式订阅诊断 deferred（successor 见 plan `{2243-1}`）。

## industrial-hmi plan-2026-08-04-2243-1 执行：HMI 生命周期/销毁/重载/合帧流水线 hardening 三 Phase 全绿（closure-audit 待独立 sub-agent）

执行 `docs/plans/2026-08-04-2243-1-hmi-lifecycle-destruction-pipeline-hardening.md`（multi-audit dim 04/07 六项 P2 L1-L6 + open-audit W3 状态样式双写，共七项 in-scope finding），三 Phase 全部落地，包级 + workspace 全量验证全绿：

- **Phase 1 销毁门控对称（L1+L2+L3）**：`dirty-collector.ts` `DirtyCollector` 的 `destroyed` 守卫扩展到 `collect`/`flush`/`flushFrame`（与 `requestRender` 对称）；`RefreshPipeline` 增自身 `destroyed` flag，`requestRender`/`flushFrame` 入口守卫，`destroy()` 置位 + 幂等；`use-scada-engine.ts` `releaseRuntime` collector 销毁单一 owner（移除冗余 `collector.destroy()`，`pipeline.destroy()` 内部销毁为唯一路径）。Proof 三组（`dirty-collector.test.ts` collector destroyed 全 no-op；`refresh-pipeline.test.ts` pipeline destroyed requestRender/flushFrame no-op + `pipeline.destroy()` 销毁 collector 计数=1 幂等）。
- **Phase 2 重载泄漏收口（L4+L5）**：`use-scada-config-sync.ts` `pendingSkipRef` 计数器改 per-import nonce（`importSeqRef` 单调 + `pendingSkipNonceRef` single-use，effect 首次运行即消费，消除「identity 不匹配不递减 → 残留 → 后续巧合匹配误 skip」泄漏）；`use-scada-points-bridge.ts` 既有 `compiledCache.clear()` effect 补 `lastReportedErrors.current.clear()`（与 compiledCache 对称，config reload 后同表达式错误重报）。Proof 两组（`use-scada-config-sync.test.ts` renderHook：nonce 无泄漏 + self-induced re-run 仍 skip；`scada-points-bridge.test.tsx`：config reload 后同表达式错误重新上报）。
- **Phase 3 handle 稳定 + 合帧 owner（L6+W3）**：`scada-canvas.tsx` `reloadConfig` 内联箭头包 `useCallback`（依赖 `syncImported` 稳定身份）→ `useScadaHandles` effect 不每渲染重登；W3 裁定**方案 (a)**（单一合帧 owner）：active-state 样式 owner=`collectStates`（保持不变，每帧批量写、状态色胜出 binding），退出恢复 base（revert）owner=`StateVisualApplier` 经同一 `collector.collect` 汇入帧尾单次写（`visual-state.ts` 不再 immediate 写 active，构造增可选 `collector` 参数，生产装配恒传、既有单测省略时 revert 退回 immediate 向后兼容）；`dirty-collector.ts` 未改（refresh-pipeline 状态样式断言无需变更）。Proof 两组（`scada-handles.test.tsx`：同 props re-render register 计数不递增；`state-visual.test.ts`：alarm-storm N 图元同帧状态切换 engine.applyAttrs=1 次/帧 + collectStates 仍为 active owner）。
- **owner-doc 同步**：`design-engine.md §4.2` 销毁门控对称 + 单一销毁 owner 注记；`design-data-binding.md` 帧尾批量写段补状态样式合帧 owner 裁定（active=collectStates / revert=StateVisualApplier 经 collector 合帧）。
- **结果**：包级 580 tests / 41 files 全绿（+10 focused 回归用例跨三 Phase）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。closure-audit 待独立 fresh-session sub-agent 执行（执行 session 不自审，Closure Gates 中该项 `[ ]` 保留为 human gate）。
- **plan 收口 + roadmap 回写**：三 Phase Status completed + 全部 items/Exit Criteria 勾选 + `Plan Status: completed`（Closure Gates 验证项 typecheck/build/lint/test/findings-landed 勾选，closure-audit 项保留 human gate）；`roadmap-industrial-hmi.md` 回写收口记录（State & lifecycle dim 04/07 L1-L6 + Wiring W3 七项）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。W1（复杂表达式订阅诊断）deferred adjudicated（successor）。

## industrial-hmi plan-2026-08-04-2242-2 收口：x/y 三层契约收敛（消除静默 NaN viewport）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2242-2-hmi-config-xy-bounds-contract-plan.md`（open-audit `2026-08-04-2242` `[P1]` x/y 三层契约漂移：TS 类型 `x`/`y` 必填 ↔ JSON validator 可选 ↔ runtime bounds consumer 无默认 → JSON 省略 x/y + viewport fit → NaN viewport → 画布空白且 `data-status="ready"` 无错误信号），两 Phase 全部完成 + 独立 closure-audit 通过：

- **Phase 1 Decision + Proof 失败用例先行（不动产线）**：Decision 裁定三层收敛到「x/y 可选 + 默认 0」（依据：open-audit 明示意图为 default-to-0；validator 已当可选；`interaction-overlay.ts:43-44` 已默认 `?? 0`；同接口 width/height/rotation/scale 已可选——类型改动归属 Phase 2）。Proof-1/2/3 三组失败用例入库（`use-scada-config-sync.test.ts` bounds 有限/原点 0 + viewport fit 有限；`viewport.test.ts` clampViewport NaN/Infinity→0），经 `as ScadaSymbolNode` 绕过当前必填类型构造省略 x/y 节点；产线未动，三组用例实跑红。
- **Phase 2 Fix 一并落地恢复 typecheck**：Fix-0 `config-types.ts` `x?`/`y?` 改可选；Fix-1/2 `use-scada-config-sync.ts` `boundsOfNode`/`boundsFromCustomPoints` `?? 0`（镜像 interaction-overlay.ts:43-44）；Fix-3 `viewport.ts` `clampViewport` 加 `Number.isFinite` 防御非有限 x/y（关闭仅钳 scale 的 position 放大器，与 clampScale 对称）；Fix-4 `config-adapter.ts` Group 构造 `?? 0`（leaf 路径经 symbol defaults `x:0,y:0` + `deepMergeInstanceProps` undefined-skip 已安全，裁定不需补，扫描清单 5 点入 plan）。
- **结果**：三层契约读法收敛一致（type 可选 + validator 可选 + runtime 默认 0），两「node geometry」consumer（bounds/interaction-overlay）不再矛盾；Proof 三组由红转绿；包级 570 tests / 41 files 全绿、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **owner-doc 同步**：`docs/components/industrial-hmi/design-renderer.md §4.2` `ScadaSymbolNode.x/y` 改可选 + 默认 0 语义注记。
- **closure-audit（独立 fresh-session 子 agent `ses_0322c7a16ffeRLPgu2ZntwSI0z`）**：判定 `approved`——Phase 1/2 Exit Criteria 逐项 live 核对、interface↔semantics trace（`{id,type,width,height}` → `computeSymbolBounds` → `fit` → 有限 viewport；`clampViewport({x:NaN})` → 0）、Fix-4 扫描诚实性（leaf 路径 31 symbol defaults + undefined-skip 兜底 sound）、两 consumer 一致性、plan 文本一致性、独立复跑包级 typecheck/build/lint/test 570/570 全绿。
- **plan 收口 + 源审计关闭**：Phase 1–2 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；open-audit `2026-08-04-2242-open-audit-industrial-hmi.md` `Audit Status: planned → closed`；`roadmap-industrial-hmi.md` 回写收口记录。

## industrial-hmi plan-2026-08-04-2242-1 收口：HMI 诊断通道接通（P1-1 onError + P1-2 onHandlerError）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2242-1-hmi-diagnostic-channel-wiring-plan.md`（multi-audit `2026-08-04-2242` P1-1/P1-2：flux 编译/求值失败去重上报通道与用户侧图元事件处理器 throw 去重上报通道在 engine/hook 层已建好、React renderer 层从未订阅），两 Phase 全部完成 + 独立 closure-audit 通过：

- **Phase 1 选定诊断出口 + 接通 onError（flux 数据错误）**：`scada-canvas.tsx:120-136` 新增统一 `reportDiagnostic(code, message)` 非升级出口——(1) `console.warn('[scada-canvas]', code, message)` 保底可见（dev+prod，去重已在上游 hook/engine 层完成故每唯一错误仅 fire 一次）；(2) flux 编译/求值错误额外复用既有 host telemetry 钩子 `RendererEnv.monitor.onError`（`ExpressionExecutionEnv.monitor`，phase:'expression'）。出口整体 try/catch 自保护（通道自身 throw 不得回流 engine/hook）。`scada-canvas.tsx:217` `useScadaPointsBridge({...})` 加 `onError: reportDiagnostic`。Decision 裁定 option (b)（option (a) `onDiagnostic` schema 事件不采纳——新增即 schema 契约变更）；不违反 §8.1（不动 `setStatus`/`setErrorInfo`/`notifyError`，画布保持 ready，不派发 `scada:error`——诊断 ≠ status 升级）。Proof 4 例（`scada-canvas-diagnostic-channels.test.tsx`）：flux evaluate 失败经出口可见 + 画布 ready + 零 scada:error 派发、同表达式多次触发去重（上报计数不增长）、出口自身 throw 自隔离、handler-error（Phase 2 共享出口）。
- **Phase 2 接通 onHandlerError（用户侧 action throw）**：`use-scada-engine.ts:42` `UseScadaEngineArgs` 加 `onHandlerError?: (error: unknown) => void`；`:147` `ScadaCanvasEngine.create({...})` 经 `latest` ref 转发 `onHandlerError`（对齐既有 `onSymbolEvent`/`getPointValuesFor` 转发模式）；`scada-canvas.tsx:163-164` `useScadaEngine({...})` 加 `onHandlerError` 订阅与 Phase 1 同一 `reportDiagnostic` 出口。`event-bridge.ts` `safeRun`/`reportHandlerError` 去重 machinery 未改动（仅消费者接通）。Proof 覆盖 handler throw → 诊断出口被调（`handler-error`）+ 画布 ready + 后续点击仍可达 + 去重仅一次上报。
- **owner-doc 同步**：`docs/components/industrial-hmi/design-renderer.md §8.1` 新增「运行期诊断出口（非升级，plan 2026-08-04-2242-1）」段落，明确 `reportDiagnostic` 出口语义（console.warn 保底 + flux 错误复用 `env.monitor.onError`、不升级画布 status、try/catch 自保护、handler-error host telemetry 后置为 Non-Blocking Follow-up）。
- **closure-audit（独立 fresh-session 子 agent）**：判定 `approved`——两 Phase items/Exit Criteria 逐项 live 核对（`scada-canvas.tsx` onError+onHandlerError 订阅行号、`UseScadaEngineArgs.onHandlerError` 字段、`ScadaCanvasEngine.create` latest ref 转发、`reportDiagnostic` try/catch 自保护、`event-bridge.ts` safeRun 去重 machinery 未改、测试 4 例实跑、`design-renderer.md §8.1` 同步段落）；anti-hollow 抽查（`reportDiagnostic` 运行期经两条订阅链路可达，非空实现）；deferred 诚实（`lastReportedErrors` 跨 config 不清 + host telemetry 基础设施均登记 Non-Blocking Follow-ups，非 in-scope live defect 降级）；五处状态自洽（Plan Status / Phase Status / Exit Criteria / Closure Gates / 本日志收口记录）。
- **plan 收口**：Phase 1–2 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 已回填）。
