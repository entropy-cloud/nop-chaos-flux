# Development Log — 2026-05-14

### 2026-05-14

- Added follow-up deep-audit automation so repeated manual findings can be surfaced before the next audit pass. `scripts/audit/rules.mjs` now reports additional suspect buckets for broad `useScopeSelector` calls without `paths`, `.then()` chains without local catch paths, catch blocks that flatten/swallow failures, `JSON.stringify` change-detection/keying, and package CSS selectors that target bare `[data-slot]` globally.
- Extended the audit scanner substrate in `scripts/audit/shared.mjs` to include CSS files and added focused entry points `scripts/audit/find-performance-suspects.mjs` / `scripts/audit/find-styling-suspects.mjs`, exposed as `check:audit-performance-suspects` and `check:audit-styling-suspects` in `package.json`.
- Calibration note: these are heuristic suspect reports, not hard lint gates. Current focused runs intentionally return candidates for review: async failure paths (`100` suspects), reactive/render reads (`10` suspects), performance suspects (`7` suspects), and styling suspects (`37` suspects).

### 2026-05-14

- Closed Plan `279` after the final live verification and closure audit pass. The last verification blockers were resolved by excluding `packages/flux-renderers-form-advanced/src/**/*.test-support.ts(x)` from `packages/flux-renderers-form-advanced/tsconfig.build.json` and by updating `packages/flux-react/src/node-renderer-resolved.tsx` to compare `resolvedProps.value` identity instead of the outer `ResolvedNodeProps` wrapper identity, which restored imported custom-renderer `props.props` refresh after multi-field form writes.
- Final Plan `279` verification baseline is now fully green: workspace `pnpm typecheck` passed, `pnpm build` passed, `pnpm lint` passed, and `pnpm test` passed with Turbo reporting `49 successful, 49 total`. This final `pnpm test` run captured the previously slow packages to completion, including `@nop-chaos/flux-renderers-form-advanced` (`66` files, `599` tests), `@nop-chaos/flux-renderers-data` (`31` files, `273` tests), `@nop-chaos/flux-playground` (`13` files, `59` tests), `@nop-chaos/flux-code-editor` (`9` files, `50` tests), `@nop-chaos/nop-debugger` (`17` files, `132` tests), and the already rerun `@nop-chaos/flux-react` (`41` files, `362` tests).
- Independent closure audit `ses_1d8f7b3cbffe4WoXwQ5qjUVg96` found no remaining code-level blockers for Plan `279`; it only required the stale blocked-state text in `docs/plans/279-resolved-boolean-props-contract-plan.md` and this daily log to be updated to the live green baseline.

- Continued Plan `279` from the live repo instead of trusting the earlier draft worktree state. Landed the resolved-boolean props contract across `packages/flux-core/src/types/{schema.ts,renderer-core.ts,renderer-compiler.ts,node-identity.ts}`, `packages/flux-compiler/src/schema-compiler/{fields.ts,node-compiler.ts,runtime-value-compilation.ts}` and `packages/flux-runtime/src/node-runtime.ts`: boolean-like runtime props now normalize to `boolean | undefined`, non-boolean `${expr}` results collapse to `undefined` instead of truthiness coercion, and renderer-facing meta (`disabled`, `className`, `frameClassName`, `testid`, `cid`, `readOnly`, `required`) is projected into `props.props` while `visible` / `hidden` / `when` remain `NodeRenderer`-owned control fields.
- Completed the renderer-consumer cleanup for Plan `279` across `packages/flux-react/src/{auto-renderer.tsx,__tests__/defaults-and-auto-renderer.test.tsx,test-support-core.tsx,use-node-source-props.ts,node-source-prop-controller.ts}`, `packages/flux-renderers-form/src/{renderers/input.tsx,field-utils/{field-handlers.tsx,field-presentation.tsx}}`, `packages/flux-renderers-form-advanced/src/{composite-field/{array-field.tsx,object-field.tsx},detail-view/{detail-view.tsx,detail-field.tsx},tree-controls.tsx,tag-list.tsx,condition-builder/condition-builder.tsx,key-value.tsx,array-editor.tsx}`, `packages/flux-code-editor/src/{code-editor-renderer.tsx,code-editor-renderer/use-code-editor-binding.ts}`, `packages/flux-renderers-data/src/{crud-renderer.tsx,__tests__/crud-selection-and-features.test.tsx}`, `packages/report-designer-renderers/src/{renderers.tsx,schemas.ts,report-designer-toolbar.tsx,report-designer-toolbar-helpers.ts}`, and `packages/flow-designer-renderers/src/{index.tsx,designer-controls.test.tsx}` so concrete renderers consume final resolved booleans directly instead of `Boolean(...)`, `=== true`, stale `props.meta.disabled`, or local boolean-expression evaluators.
- Fixed rollout regressions discovered during focused reruns: `packages/flux-renderers-form/src/field-utils/field-handlers.tsx` now uses optional `RuntimeContext` access so lightweight tests outside `SchemaRenderer` keep working while still reporting host issues when runtime exists; `packages/flux-core/src/types/actions.ts`, `packages/flux-runtime/src/form-runtime-submit-flow.ts`, and `packages/flux-action-core/src/action-dispatcher/action-execution.ts` now mark form lifecycle failures as already user-handled so form-owned `onSubmitError` / `onValidateError` no longer trigger duplicate fallback `notify('error', ...)` toasts. Added a small workspace ambient shim `types/node-fs.d.ts` so `packages/flux-renderers-form/src/__tests__/form-renderers-css.test.ts` typechecks under the package tsconfig.
- Focused verification for Plan `279`: `pnpm exec vitest run src/__tests__/form-submit-actions.semantic.test.tsx --reporter=verbose` in `packages/flux-renderers-form` passed (`5` tests), `pnpm exec vitest run src/index.test.tsx --reporter=dot` in `packages/flux-renderers-form` passed (`44` tests), `pnpm --filter @nop-chaos/flux-renderers-form test -- --run src/index.test.tsx` passed (`26` files, `182` tests), and `pnpm exec vitest run src/index.test.tsx --reporter=verbose` in `packages/flux-renderers-data` passed (`63` tests, including the earlier `silent: true` notify case).
- Package-level verification for touched owners passed: `pnpm --filter @nop-chaos/flux-core typecheck && pnpm --filter @nop-chaos/flux-core build && pnpm --filter @nop-chaos/flux-core lint`, `pnpm --filter @nop-chaos/flux-runtime typecheck && pnpm --filter @nop-chaos/flux-runtime build && pnpm --filter @nop-chaos/flux-runtime lint`, `pnpm --filter @nop-chaos/flux-action-core typecheck && pnpm --filter @nop-chaos/flux-action-core build && pnpm --filter @nop-chaos/flux-action-core lint`, and `pnpm --filter @nop-chaos/flux-renderers-form typecheck && pnpm --filter @nop-chaos/flux-renderers-form build && pnpm --filter @nop-chaos/flux-renderers-form lint` all passed after the local `node:fs` shim landed.
- Workspace verification is still blocked outside Plan `279` ownership: `pnpm typecheck` passed, but `pnpm build` currently fails in `packages/flow-designer-renderers/src/designer-page.test-support.tsx` (portable Vitest spy typing / spread / React child typing issues) and `pnpm lint` currently fails in `packages/flux-react/src/__tests__/hook-contracts.test.tsx` (React purity lint against render-time mutation in tests). Full workspace `pnpm test` was therefore not rerun after those unrelated repo-level gate failures. Plan `279` remains `partially completed` pending those external blockers plus an independent closure audit.

### 2026-05-14

- Implemented a minimal live Plan `281` truth-surface slice on retained Word Editor items without touching historical completed plans. `packages/word-editor-renderers/src/hooks/use-word-editor-state.ts` now keeps the empty host `document` fallback as a pure persisted/autosave-shaped snapshot instead of mixing in live `charts` / `codes`; `packages/word-editor-renderers/src/editor-canvas.tsx` no longer clears canonical dirty on autosave; and `packages/word-editor-renderers/src/word-editor-action-provider.ts` now captures the current document snapshot first but persists the local recovery baseline only after the host `saveEvent` succeeds.
- Added focused regression proof in `packages/word-editor-core/src/__tests__/document-io.test.ts` and `packages/word-editor-renderers/src/__tests__/{editor-canvas.test.tsx,word-editor-action-provider.test.ts,word-editor-page-host-scope.test.tsx,word-editor-page-actions.test.tsx}`. Coverage now proves persisted-only host document fallback, autosave no longer clearing dirty, and failed host save no longer advancing the recovery baseline.
- Re-audit note: the broader retained lifecycle/validation items from `docs/analysis/2026-05-14-deep-audit-batch1/{07-lifecycle.md,08-validation.md}` are still live Plan `281` scope, but they were not safe to fold into this minimal slice because they live in `packages/flux-react/src/**`, `packages/flux-runtime/src/**`, and Report Designer owner-construction paths rather than the targeted Word Editor files. No owner-doc update was required for this slice because the fixes align with the existing `docs/architecture/word-editor/design.md` truth-surface baseline.
- Verification: `pnpm --filter @nop-chaos/word-editor-core test -- src/__tests__/document-io.test.ts` passed (`11` files, `220` tests). Direct focused Vitest runs from the workspace root also passed for `packages/word-editor-renderers/src/__tests__/{editor-canvas.test.tsx,word-editor-action-provider.test.ts,word-editor-page-host-scope.test.tsx,word-editor-page-actions.test.tsx}` (`4` files, `32` tests). Package-script scoped `pnpm --filter @nop-chaos/word-editor-renderers test -- ...` was not reliable evidence in this environment because Vitest pulled unrelated suite execution / hanging workers, so direct file-targeted Vitest invocation was used for the final focused proof.

- Implemented a minimal live execution slice of Plan `284` without touching historical completed plans. Split retained omnibus hotspots into owner-aligned suites for `packages/flux-react/src/__tests__/` (`hook-contracts.test.tsx`, `surface-lifecycle-contracts.test.tsx`, `schema-renderer-contracts.test.tsx`, `form-owner-cleanup.test.tsx`, with the existing `error-boundary.test.tsx` remaining the dedicated owner for boundary coverage) and `packages/flow-designer-renderers/src/` (`designer-page.tree.test.tsx`, `designer-page.graph-regression.test.tsx`, `designer-page.tree-history.test.tsx`, plus shared `designer-page.test-support.tsx`).
- Constraint noted during verification: a first extraction attempt for the `designer-page.tree` runtime-prop sub-slice did not match the current live baseline, while `packages/flow-designer-renderers/src/designer-page.resolved-props.test.ts` already owns resolved-props proof. I left that runtime-prop split out of this minimal slice instead of widening the refactor.
- Tightened retained item `14-01` in `tests/e2e/component-lab/simple-form.spec.ts` so the input-password lab test no longer implies confirm-password validation enforcement; it now asserts the scenario description and the currently supported masked-input baseline explicitly.
- Owner docs: no owner-doc update required beyond this log because the work only narrows test ownership and fixes a false-green-style test claim to match the already documented playground baseline.

- Executed a plan/doc-only routing pass for Plan `287` without modifying historical completed plans. Re-audited the in-scope structural hotspots `02-07/09/10/11/12/13/14` against the live repo plus active Plans `280/281/282/284/286/288`, then updated `docs/plans/287-deep-audit-2026-05-14-structural-hotspots-owner-routing-plan.md` with an explicit owner matrix and allowed-outcome adjudication.
- Routing decision: `02-09` (`packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`) stays under Plan `280`; `02-10` (`packages/report-designer-renderers/src/page-renderer.tsx`) stays under Plan `282`; `02-11` (`packages/report-designer-core/src/core.ts`) stays under Plan `281`; `02-13` (`packages/flux-renderers-form-advanced/src/variant-field/variant-field.tsx`) stays under Plan `286`; `02-07` (`packages/flux-runtime/src/runtime-factory.ts`) and `02-12` (`packages/flux-runtime/src/async-data/reaction-runtime.ts`) are recorded as `watch-only residual`; `02-14` (`packages/flow-designer-core/src/tree-layout.ts`) is recorded as an `optimization candidate`.
- Owner-doc decision: no architecture/component doc update was required for this routing pass because the task only clarified active plan ownership and residual classification. Verification: docs-only pass; no code verification commands were required or run.

- Implemented a minimal retained slice of Plan `282` without overlapping Plans `279/280`: `packages/report-designer-renderers/src/index.ts` now re-exports `createReportDesignerActionProvider` and related host-action helpers from the package root; `packages/report-designer-renderers/src/types.ts` now aligns `report-designer-page` region surfaces with the live renderer contract by allowing generic region schema inputs for `toolbar` / `fieldPanel` / `inspector` and removes stale unsupported `emptyLabel` from `ReportInspectorShellSchema`; `packages/flux-renderers-basic/src/basic-renderer-definitions.ts` now registers `text.tag`; and `packages/flow-designer-renderers/src/designer-page.tsx` now preserves root meta on config/document fallback branches.
- Added focused proof in `packages/report-designer-renderers/src/{schemas.test.ts,renderers.integration.test.tsx}`, `packages/flow-designer-renderers/src/designer-page-shell.test.tsx`, and `packages/flux-renderers-basic/src/__tests__/basic-renderer-contracts.test.ts`. Updated owner-doc drift in `docs/components/report-designer-page/design.md` so the host projection path now matches live `reportDocument.spreadsheet` and the page-region contract explicitly states these override surfaces accept generic region schema input.
- Focused verification pending for this Plan `282` slice; full workspace verification was not run in this step.

- Executed the code slice for Plan `280` across spreadsheet/renderers, detail-view, data-source structural publication, Flow Designer xyflow sync, and table filtered pagination. `packages/spreadsheet-renderers/src/page-renderer.tsx` now mounts a real default spreadsheet host via new `packages/spreadsheet-renderers/src/default-page-body.tsx`; readonly/root-scoped interaction guards landed across shared spreadsheet UI and `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`; `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx` replaced serialized viewer remounting with a commit-only revision bump; `packages/flux-compiler/src/source-compiler.ts` plus shape-validation now treat `data-source.name` / `statusPath` as structural literal paths; `packages/flow-designer-renderers/src/designer-xyflow-canvas/use-xyflow-sync.ts` now merges acknowledged snapshot data instead of keeping stale local nodes; and `packages/flux-renderers-data/src/table-renderer{.tsx,/table-data.ts,/use-table-filter.ts,/use-table-pagination.ts}` now derives pagination totals/body from the same filtered row universe.
- Added focused regression proof in `packages/spreadsheet-renderers/src/{__tests__/schema-integration.test.tsx,sheet-tab-bar.test.tsx,spreadsheet-toolbar.test.tsx,spreadsheet-interactions/async-handlers.test.tsx}`, `packages/flux-renderers-form-advanced/src/{detail-view/detail-view-owner-updates.test.tsx,condition-builder/condition-builder.test.ts}`, `packages/flux-compiler/src/{source-compiler.test.ts,schema-compiler-shape-validation-analyze.test.ts}`, `packages/flow-designer-renderers/src/designer-xyflow-canvas/use-xyflow-sync.test.tsx`, and `packages/flux-renderers-data/src/__tests__/{table-data-and-layout.test.tsx,use-table-controls.pagination.test.tsx,use-table-controls.sort-filter-expand.test.tsx,use-table-controls.test-support.tsx}`.
- Owner-doc sync: updated `docs/architecture/report-designer/{api.md,design.md}` to record the live default `spreadsheet-page` host/readOnly behavior and `docs/architecture/api-data-source.md` to make `data-source.name` / `statusPath` structural literal paths explicit. For Phase 2, Phase 4, and Phase 5 there was no additional owner-doc update required beyond the daily log because the fixes align with the existing renderer/canvas/table ownership baselines without introducing a new public contract surface.
- Verification: focused/package checks passed for the plan-owned surfaces, including `pnpm --filter @nop-chaos/spreadsheet-renderers test -- --runInBand` (`11` files, `93` tests), `pnpm --filter @nop-chaos/flux-renderers-form-advanced test -- --runInBand` (`65` files, `595` tests), `pnpm --filter @nop-chaos/flux-compiler test -- --runInBand` (`28` files, `444` tests), `pnpm --filter @nop-chaos/flow-designer-renderers test` (`19` files, `125` tests across three batches), `pnpm --filter @nop-chaos/flux-renderers-data test -- --runInBand` (`31` files, `273` tests), `pnpm --filter @nop-chaos/flux-react test -- --runInBand` (`36` files, `362` tests), plus workspace `pnpm typecheck`, `pnpm build`, and `pnpm lint` all passed after the plan-owned fixes landed.
- Closure blocker noted: full workspace `pnpm test` is still red on pre-existing independent `@nop-chaos/flux-action-core` failures in `src/__tests__/{action-dispatcher-control-flow.test.ts,contract-control-flow-retry-and-extras.test.ts,action-dispatcher-error-guard.test.ts,action-dispatcher-monitoring.test.ts}`. Those files and the underlying `packages/flux-action-core/src/action-dispatcher/{action-execution.ts,action-runners.ts}` changes were already present in the worktree and are outside Plan `280` ownership, so Plan `280` cannot be marked completed until that unrelated blocker is fixed or successor ownership is explicitly assigned.

- Followed up Plan `280` Phase 2 against the live repo after the earlier closure text drift. Root cause was not `detail-view` remounting itself but shared parent-change dedupe state inside `packages/flux-runtime/src/scope.ts:createCompositeScopeStore(...)`: child composite scopes were sharing one `lastVisibleForParent` cache across all subscribers, so the first imported-viewer subscriber consumed the parent update and later sibling schema-node subscribers were incorrectly short-circuited. The fix moved that dedupe cache into each `subscribe(...)` closure, preserving lifecycle while letting all imported region/viewer subscribers observe the same parent change.
- Added regression proof in `packages/flux-runtime/src/__tests__/scope-ownership-lexical-and-nested.test.ts` for multi-subscriber parent updates on child composite scopes, refreshed the focused `detail-view`/import-region proofs in `packages/flux-renderers-form-advanced/src/detail-view/detail-view-transform.test.tsx`, and tightened `packages/flux-react/src/__tests__/scope-and-reactivity-imports.test.tsx` to assert both probe and schema text updates without brittle single-match `getByText(...)` queries.
- Updated verification baseline after the Phase 2 root-cause fix: `pnpm --filter @nop-chaos/flux-runtime typecheck`, `pnpm --filter @nop-chaos/flux-runtime build`, `pnpm --filter @nop-chaos/flux-runtime lint`, `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/scope-ownership-lexical-and-nested.test.ts`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced build`, `pnpm --filter @nop-chaos/flux-renderers-form-advanced lint`, focused `detail-view` regression trio, `pnpm --filter @nop-chaos/flux-react test -- --runInBand`, and full workspace `pnpm typecheck` / `pnpm build` / `pnpm lint` all passed. Full workspace `pnpm test` no longer failed in `@nop-chaos/flux-action-core`; the remaining closure issue is operational: the repo-wide run exceeded the command timeout after already proving direct package passes for `@nop-chaos/flux-playground` (`13` files, `59` tests) and the previously failing `@nop-chaos/flux-react` import-region suite. `@nop-chaos/flux-renderers-form-advanced test -- --runInBand` still needs a longer uninterrupted run or a recorded known-good full-green baseline before Plan `280` Phase 6 can be closed.
- Final closure-gate rerun completed successfully: workspace `pnpm test` passed with Turbo reporting `49 successful, 49 total`. This captured the previously slow packages to completion, including `@nop-chaos/flux-renderers-form-advanced` (`66` files, `599` tests), `@nop-chaos/flux-renderers-data` (`31` files, `273` tests), `@nop-chaos/flux-playground` (`13` files, `59` tests), `@nop-chaos/flux-code-editor` (`9` files, `50` tests), `@nop-chaos/nop-debugger` (`17` files, `132` tests), and the earlier rerun of `@nop-chaos/flux-react` (`41` files, `362` tests). The remaining test output was limited to known runtime warnings (`--localstorage-file` path warnings, chart zero-size warnings in tests, and existing React warning stderr in data CRUD tests), not failing assertions.

- Implemented retained live fixes from Plan `288` without touching historical completed plans: `packages/word-editor-core/src/document-io.ts` now preserves structured recovery failure classification for document/dataset local-storage read/parse failures, `packages/flux-runtime/src/action-adapter.ts` keeps targeted `setValues` on the batched form path, `packages/flux-runtime/src/async-data/formula-data-source-controller.ts` skips unchanged-value republish/dataUpdatedAt churn, and `packages/flux-action-core/src/action-dispatcher/{action-runners.ts,action-execution.ts}` now make `monitor.onActionEnd` fail-safe while adding the default observable fallback only for returned failure-class results with no `onError` handler.
- Added focused regression coverage in `packages/word-editor-core/src/__tests__/document-io.test.ts`, `packages/flux-runtime/src/__tests__/{action-adapter.unit.test.ts,formula-data-source-recovery.test.ts,form-runtime-values.test.ts}`, and `packages/flux-action-core/src/__tests__/{action-dispatcher-monitoring.test.ts,action-dispatcher-control-flow.test.ts}`.
- Verification: `pnpm --filter @nop-chaos/word-editor-core test -- document-io.test.ts` passed (`11` files, `218` tests), `pnpm --filter @nop-chaos/flux-runtime test -- src/__tests__/action-adapter.unit.test.ts src/__tests__/formula-data-source-recovery.test.ts src/__tests__/form-runtime-values.test.ts` passed (`82` files passed, `1` skipped; `1052` tests passed, `1` skipped), and `pnpm --filter @nop-chaos/flux-action-core test -- src/__tests__/action-dispatcher-monitoring.test.ts src/__tests__/action-dispatcher-control-flow.test.ts src/__tests__/action-dispatcher-error-guard.test.ts src/__tests__/cancelled-class-and-error-guard.test.ts src/__tests__/contract-control-flow-retry-and-extras.test.ts` passed (`13` files, `189` tests).
- Constraint noted: retained item `15-05` was only partially tightened. `FormRuntime.setValue()` now shares the existing catch-and-warn pattern used by batched dependent revalidation, but there is still no narrow owner-level env/monitor sink on `FormRuntime` to promote these failures into a stronger formal diagnostics channel without widening runtime contracts.

### 2026-05-14

- Implemented a minimal live slice of Plan `286` on retained non-`280/282/284` surfaces: removed unused dialog-host whole-scope subscriptions in `packages/flux-react/src/dialog-host.tsx`, added observable async failure handling in `packages/{flow-designer-renderers/src/designer-page-helpers.tsx,report-designer-renderers/src/{field-panel-renderer.tsx,report-designer-toolbar.tsx},flux-renderers-form/src/field-utils/field-handlers.tsx}`, and added real SQL execute abort wiring in `packages/flux-code-editor/src/code-editor-renderer/use-sql-editor-state.ts`.
- Added focused owner-aligned proof in `packages/flux-react/src/__tests__/dialog-host.test.tsx`, `packages/flow-designer-renderers/src/designer-page-helpers.test.ts`, `packages/report-designer-renderers/src/{field-panel-renderer.test.tsx,report-designer-toolbar.test.tsx}`, and `packages/flux-code-editor/src/code-editor-renderer/use-sql-editor-state.test.tsx`.
- Owner docs: no owner-doc update required beyond the daily log because the changes align with the existing `docs/architecture/renderer-runtime.md` reactive/visible-failure baseline rather than changing it.

### 2026-05-14

- Updated `docs/plans/00-plan-authoring-and-execution-guide.md` to make one plan-execution boundary explicit: already-`completed` plans are historical records by default and should not be rewritten just because newer guide rules or later code evolution would make their text look stale.
- Key decision: new audits should record findings in fresh analysis / active plans / daily logs instead of retrofitting old completed plans for template consistency, unless the user explicitly asks for historical plan revision.

- Added `docs/plans/280-open-ended-adversarial-review-2026-05-14-remediation-plan.md` to own the confirmed findings from `docs/analysis/2026-05-14-open-ended-adversarial-review-01/round-01.md` through `round-05.md`: spreadsheet default canvas/readonly/root-scoped shortcuts, `detail-view` serialized viewer remounting, data-source structural publication paths, Flow Designer post-drag canvas reconciliation, and table filtered pagination.
- Independent plan review iteration 1 found blockers: `ses_1da93f1a3ffePLBtjUzKQytpJX` flagged spreadsheet docs-downgrade and vague test gates, `ses_1da93f03fffeknQAAh6TAGpw6w` flagged missing spreadsheet consumers/default-shell details, Plan `279` dependency, and validation targets, and `ses_1da93f028ffejocyH3oupqVLDk` flagged sheet-tab/field-drop readonly gaps.
- Revised Plan `280` to require a real default spreadsheet canvas, concrete bridge/root shell wiring, Report Designer consumer coverage, sheet-tab and field-drop readonly gates, static data-source structural path validation, dedicated Flow Designer sync proof, table filter/page event-order proof, and explicit `pnpm test` closure gating.
- Independent plan review iteration 2 reached consensus: `ses_1da8a5691ffeDcxvqgs6lsarbi`, `ses_1da8a54dbffeUxLYO6FuCr9IZ5`, and `ses_1da8a54c0ffeiHqokhHW4cTZjk` reported no blocking findings. Verification: docs/plan-only update; code verification commands were not run.

### 2026-05-14

- Updated the resolved boolean props architecture baseline in `docs/architecture/renderer-runtime.md`, `docs/architecture/field-binding-and-renderer-contract.md`, and `docs/architecture/flux-core.md`: authoring allows boolean literals or `${expr}`, validation rejects ordinary boolean strings, non-boolean expression results resolve to `undefined`, and concrete renderers consume final `props.props` values directly.
- Added `docs/plans/279-resolved-boolean-props-contract-plan.md` to implement the contract across core types, compiler/runtime prop assembly, renderer consumers, nested toolbar/tabs boolean fields, and regression tests.
- Independent plan review iterations: `ses_1dad6af95ffeT4IJXsDPnP414r` found contract blockers, `ses_1dad26abbffeah5n4pDgn1gsxZ` found stale normative snippets, and `ses_1dacf56cdffeusOalVziexp4sJ` reached consensus after the docs and plan were revised.
- Verification: docs-only update; code verification commands were not run for this planning step.

### 2026-05-14

- Closed Plan `240` after live re-auditing the current happy-dom baseline. The plan itself was stale: `docs/logs/2026/05-10.md` already recorded the migration landing, and today’s recheck confirmed `pnpm exec node scripts/scan-jsdom-usage.mjs` reports no real jsdom dependency/config/pragma residue while `pnpm exec node scripts/validate-happy-dom-migration.mjs` still returns `ALL CHECKS PASSED`.
- Closed Plan `231` after live re-auditing the source-substrate/code-editor convergence baseline. The plan text still described pre-landing drift, but the live repo now routes anonymous source props/hooks through `runtime.createSourceObserver()` (`packages/flux-react/src/{use-node-source-props.ts,node-source-prop-controller.ts,use-source-value.ts}`), and `packages/flux-code-editor/src/{types.ts,source-resolvers.ts}` no longer carry any code-editor-specific `loadAction` / `source: 'action'` loading contract.
- Updated `docs/plans/{240-jsdom-to-happy-dom-upgrade-plan.md,231-source-substrate-and-code-editor-convergence-plan.md}` so their Current Baseline, phase checklists, closure gates, and status/closure notes match the live repo instead of preserving stale `planned` state.
- Independent closure audit `ses_1db6ddd42ffe4voeaaBk1AGbt9` agreed both plans are substantively complete and found only closure-text issues: both plans were missing recorded audit evidence, and Plan `240` also needed its historical `pnpm lint` verification gate restored. Those text-consistency fixes are now recorded in the plan files.

- Closed Plan `221` after re-auditing its stale closure blocker against the current repo. The plan still accurately reflects the build/public-surface/docs convergence work landed on 2026-05-07, and the old note about 10 pre-existing `flux-renderers-form-advanced` detail-view failures is no longer true because the current workspace verification baseline is fully green.
- Updated `docs/plans/221-deep-audit-2026-05-07-confirmed-defect-remediation-plan.md` so Workstream 4, Closure Gates, Validation Checklist, and Closure evidence now cite the current `pnpm typecheck` / `pnpm build` / `pnpm lint` / `pnpm test` green baseline instead of preserving the stale test-failure disclaimer.

- Closed Plan `277` after finishing the retained async/lifecycle backlog it owned from Plan `266`. `packages/flux-runtime/src/async-data/source-observer.ts`, `packages/flux-renderers-data/src/crud-renderer.tsx`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-keyboard.ts`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/use-field-drop.ts`, and `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx` now cover `06-05`, `06-07`, `06-09`, `06-12`, and `06-15` with focused proof in `packages/flux-runtime/src/async-data/source-observer.test.ts`, `packages/flux-renderers-data/src/__tests__/crud-query-and-pagination.test.tsx`, `packages/spreadsheet-renderers/src/spreadsheet-interactions/async-handlers.test.tsx`, and `packages/flux-renderers-form-advanced/src/composite-field/object-field-runtime.test.ts`.
- Fixed the two stale closure blockers discovered by the independent audit: `packages/flux-renderers-form-advanced/src/detail-view/detail-view.tsx` and `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx` now notify on open-time transform failures (`06-14`), and `packages/flux-react/src/render-nodes.tsx` no longer uses `queueMicrotask(...)` for fragment-scope commit gating (`07-04`). Added focused proof in `packages/flux-renderers-form-advanced/src/detail-view/detail-view-transform.test.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field-commit.test.tsx`, and `packages/flux-react/src/__tests__/schema-renderer-runtime-scope.test.tsx`.
- Repaired the last workspace test blocker in `@nop-chaos/flux-renderers-data`: `packages/flux-renderers-data/src/data-renderer-definitions.ts` now registers the `chart` renderer definition again, restoring chart entry/handle/event contracts with proof in `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-package-units.test.tsx`, and `packages/flux-renderers-data/src/__tests__/data-renderer-definition-contracts.test.ts`.
- Verification: `pnpm typecheck` passed, `pnpm build` passed, `pnpm lint` passed, and `pnpm test` passed. Full-green test baseline included `@nop-chaos/flux-renderers-data` (`31` files, `272` tests), `@nop-chaos/flux-renderers-form-advanced` (`65` files, `593` tests), `@nop-chaos/flux-playground` (`13` files, `59` tests), `@nop-chaos/nop-debugger` (`16` files, `132` tests), `@nop-chaos/flux` (`1` file, `4` tests), `@nop-chaos/flux-code-editor` (`7` files, `48` tests), `@nop-chaos/flux-renderers-form` (`25` files, `181` tests), plus the remaining workspace package suites under `pnpm test`.
- Independent closure audit: task `ses_1dbb2e846ffe6wDmswF0iN37Ie` initially caught stale `06-14` / `07-04` plan claims, and the final Plan `277` closure note now records the corrected live baseline and proof set.

- Independent closure audit task `ses_1db9dfb9dffeD2UEnCHWQAkcNA` rechecked the final plan set (`262`, `263`, `264`, `265`, `266`, `268`, `273`, `276`) plus successor plans `274-278` and passed after the final text-consistency fixes. Residual risk is now limited to the explicitly recorded non-blocking structural residuals in Plan `273`; Plan `277` is now closed with no remaining plan-owned async backlog.
- Finalized structural successor-plan closure for `262` and `273`. After the live re-audit, `02-01` is now actually fixed in code, while `02-02`, `12-03`, `02-07`, `02-08`, `02-09`, `02-11`, `02-12`, and `02-13` were explicitly reclassified as optimization candidates or watch-only residual structural debt instead of still-live plan-owned defects.
- Updated `docs/plans/262-deep-audit-2026-05-13-structural-owner-successor-plan.md` and `docs/plans/273-deep-audit-2026-05-13-structural-hotspots-execution-successor-plan.md` so both plans now reflect the actual live baseline and closure chain instead of leaving stale `planned` state behind.
- Re-audited plans `262` through `273` with the rule that already-`completed` plans are skipped and non-completed plans must be checked against the live repo rather than trusted at face value.
- Closed successor-routing plans `263`, `264`, `265`, `266`, and `268` after confirming their owner-routing/re-audit responsibilities were complete and that successor ownership was explicit in plans `274`, `275`, `276`, `277`, and `278`. Those successor execution owners are now also closed where applicable, including Plan `277` for the async backlog it inherited from Plan `266`.
- Continued Plan `273` with the first real structural slice for retained item `02-01`: extracted runtime-value tree compilation out of `packages/flux-compiler/src/schema-compiler/node-compiler.ts` into new file `packages/flux-compiler/src/schema-compiler/runtime-value-compilation.ts` and re-exported the helper from `packages/flux-compiler/src/schema-compiler/index.ts`.
- This structural extraction reduced `packages/flux-compiler/src/schema-compiler/node-compiler.ts` from `757` lines to `504` lines, so `pnpm check:oversized-code-files` no longer reports `node-compiler.ts` as a `>700` hard-gate failure. The remaining hard-gate failures are elsewhere and outside Plan `273` item `02-01`.
- Verification: `pnpm --filter @nop-chaos/flux-compiler typecheck` passed and `pnpm --filter @nop-chaos/flux-compiler test` passed (`28` files, `440` tests). `pnpm check:oversized-code-files` was rerun as a live audit baseline and now reports `6` `>700` files, with `packages/flux-compiler/src/schema-compiler/node-compiler.ts` removed from the error list.
