# Development Log - 2026-05-26

### 2026-05-26

- Full-green verification is now re-established on the current branch for both workspace unit tests and full Playwright coverage. `pnpm test` is green, and the full browser suite `pnpm exec playwright test --reporter=list --workers=1` is also green with `322 passed`, `45 skipped`, `0 failed`.
- The last two full-E2E blockers were resolved as follows. `packages/flux-renderers-basic/src/dynamic-renderer.tsx` now subscribes to scope-driven `loadAction` key changes via `useScopeSelector(...)` and dispatches the currently resolved `loadAction`, so runtime schema switches such as the component-lab `schemaType` button flow actually reload the dynamic fragment instead of leaving the previously loaded schema mounted. Focused regression proof was added in `packages/flux-renderers-basic/src/__tests__/basic-dynamic-renderer.test.tsx` for scope-driven `loadAction` changes and is green.
- The CRUD radio-selection browser failure was a combination of stale E2E expectations and a real console-warning regression. `tests/e2e/component-lab/crud-editing-and-selection.spec.ts` now targets the live shared radio primitive (`[data-slot="radio-group-item"]`) instead of the removed checkbox shim, and `packages/flux-renderers-data/src/table-renderer/table-body-rows.tsx` now keeps `RadioGroup` controlled from first render by passing `value={radioSelectionValue ?? ''}`, eliminating the uncontrolled-to-controlled React warning that the page-error fixture correctly promoted to an E2E failure.
- Full workspace unit-test verification is green after the Flow Designer / TaskFlow repair slice and the follow-up `flux-code-editor` test stabilization: `pnpm test` now passes across the current branch. The only newly exposed failure during that run was `packages/flux-code-editor/src/code-editor-styles.test.ts`, where the stylesheet contract was asserting one exact single-line `color-mix(...)` declaration. The test now normalizes whitespace before checking that the default chrome tokens still derive from shared theme variables, so CSS formatting changes no longer create false negatives while the actual token contract remains covered.
- Re-ran the required workspace verification after the Flow Designer / TaskFlow toolbar-scope fix and the follow-up playground import cleanup. The current branch is green for `pnpm typecheck`, `pnpm build`, and `pnpm lint`. An earlier workspace `turbo` run briefly reported `@nop-chaos/tailwind-preset` type/build failures, but isolated package runs for that package were already green and the subsequent full workspace rerun confirmed the branch now passes without additional `tailwind-preset` code changes.
- Fixed the TaskFlow/Flow Designer toolbar template regression on the real live path instead of rewriting schema strings away. `packages/flow-designer-renderers/src/designer-context.ts`, `designer-page-helpers.tsx`, and `designer-page-body.tsx` now carry the already-created designer host scope through `DesignerContext`, and `packages/flow-designer-renderers/src/designer-toolbar.tsx` resolves `toolbar.items` expressions against that live `designerScope` rather than the toolbar component's local renderer scope. This restores runtime values such as `Container: ${doc.name}` on the TaskFlow designer page while keeping the compiler-side rule that `toolbar.items` template leaves are preserved for delayed resolution.
- Focused regression proof for the toolbar/template fix is green: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/designer-controls.test.tsx src/designer-page.resolved-props.test.ts src/edge-label-expression.test.tsx` passed (`3` files, `24` tests). Live page smoke and TaskFlow UI proof are also green: `pnpm exec playwright test "tests/e2e/playground-entry-pages.spec.ts" --grep "flow-designer|taskflow-designer" --reporter=list --workers=1` (`2` passed) and `pnpm exec playwright test "tests/e2e/taskflow-designer-ui.spec.ts" --reporter=list --workers=1` (`8` passed). The `apps/playground/src/pages/flow-designer-page.tsx` cleanup that removes the two TaskFlow examples from the Flow Designer playground is now verified against the live entry page instead of remaining as an unverified edit.

- Fixed the manual-testing regression on `#/lab/input-tree`: `packages/flux-renderers-form-advanced/src/tree-controls.tsx` now declares `input-tree` with `frameRootTag: 'div'` so the wrapped field shell no longer uses a native `<label>` wrapper that forwards node-label clicks into the first internal control. This preserves the expected tree interaction where clicking a node label selects/focuses without collapsing the branch. Regression coverage was added in `src/__tests__/tree-structure.test.tsx`.
- Fixed the visible Report Designer playground toolbar/editing regressions on the real `/report-designer` page path: the live demo uses `@nop-chaos/spreadsheet-renderers` `SpreadsheetToolbar`, not the schema `report-toolbar` renderer. `packages/spreadsheet-renderers/src/canvas-styles.css` now keeps the live toolbar on a single horizontal row via `flex-wrap: nowrap` plus horizontal overflow, and marks toolbar groups/status as non-shrinking so actions no longer wrap into many short rows that steal vertical canvas space. The toolbar shell in `packages/spreadsheet-renderers/src/spreadsheet-toolbar.tsx` no longer renders a separate cell-value editor below the toolbar; the supported Excel-like baseline is now selection-first with inline grid editing entered through double-click, `Enter`, `F2`, or direct text entry. Focused proof was updated in `packages/spreadsheet-renderers/src/{spreadsheet-toolbar.test.tsx,__tests__/grid-selection.test.tsx}`, `apps/playground/src/pages/report-designer-demo.test.tsx`, and `tests/e2e/report-designer-demo.spec.ts`.
- Follow-up fix for the inline editing baseline: the grid cell editor no longer reuses the shared `@nop-chaos/ui` `Input` size contract because that 32px-class control height was inflating the fixed 22px spreadsheet row box and causing row jitter on edit focus. `docs/components/spreadsheet-page/design.md` now documents spreadsheet cell inline editing as a tightly scoped canvas-density exception, `packages/spreadsheet-renderers/src/spreadsheet-grid/inline-controls.tsx` uses a dedicated compact input with an explicit comment explaining the exception, and `canvas-styles.css` constrains the editor to inherit the cell font/line-height and stay box-sized within the existing row height. Focused proof was extended in `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx`.
- Follow-up fix for keyboard edit continuity: entering spreadsheet inline edit mode now transfers DOM focus into the inline editor input so typing continues to update the active cell draft instead of getting swallowed by the grid shell after `editingCell` becomes truthy. `packages/spreadsheet-renderers/src/spreadsheet-grid/inline-controls.tsx` now focuses/selects the inline editor on mount, and `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx` adds focused proof that `F2` places focus into the editor and accepts subsequent typing.
- Follow-up fix for live keyboard navigation after mouse selection: `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx` now updates its local keyboard anchor immediately on cell mouse interactions and synchronously restores focus to the grid root on click selection, so the real click-then-`Arrow*` and click-then-`Enter` path no longer depends on the asynchronous core selection round-trip before keyboard input starts. Focused proof now includes click-then-keyboard regression coverage in `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx`, and verification is green for `pnpm --filter @nop-chaos/spreadsheet-renderers exec vitest run src/__tests__/grid-selection.test.tsx` (`1` file, `28` tests), `pnpm --filter @nop-chaos/flux-playground exec vitest run src/pages/report-designer-demo.test.tsx` (`1` file, `4` tests), and `pnpm exec playwright test "tests/e2e/report-designer-demo.spec.ts" --reporter=list --workers=1` (`8` passed).
- Follow-up fix for sticky spreadsheet headers on the real `/report-designer` page path: the playground live canvas wrapper in `apps/playground/src/pages/report-designer-demo.tsx` had drifted from the documented spreadsheet/report canvas contract and was missing `data-slot="report-designer-spreadsheet-canvas"`. Because `packages/spreadsheet-renderers/src/canvas-styles.css` intentionally scopes sticky row/column header rules under that marker, the live page was bypassing the shipped sticky geometry CSS even though the spreadsheet package already implemented it. The live root marker is restored, page-level proof now asserts the marker exists in `apps/playground/src/pages/report-designer-demo.test.tsx`, and live sticky behavior is verified in `tests/e2e/report-designer-demo.spec.ts` by scrolling the grid and checking that row/column header positions stay pinned while content moves.
- Owner-doc sync for the spreadsheet interaction baseline: `docs/components/spreadsheet-page/design.md` now contains an explicit `Excel-Like Cell Interaction Contract` section that documents selection-first behavior, edit entry paths (`double-click`, `Enter`, `F2`, direct text entry), focus handoff into the inline editor, in-editor key semantics, and the Report Designer rule that rich cell metadata editing belongs to the right inspector rather than a second toolbar-lower editor.
- Focused verification for this turn is green: `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/tree-structure.test.tsx` (`1` file, `9` tests) and `pnpm --filter @nop-chaos/report-designer-renderers exec vitest run src/report-designer-toolbar.test.tsx` (`1` file, `7` tests) both passed.

### 2026-05-26

- Re-ran repo-level verification on the current branch after closing plans `441`, `442`, and `443`: `pnpm typecheck`, `pnpm build`, `pnpm lint`, `pnpm check`, and `pnpm test` are all green.
- `pnpm check:oversized-code-files` no longer reports hard failures on this branch; current status is warning-only (`91 warnings, 0 errors`), so the earlier log note claiming workspace `lint` / `check` remained blocked is no longer accurate.
- Full workspace unit-test proof is green from `pnpm test`: `49` turbo test tasks succeeded, covering `572` test files and `5959` tests total across the workspace; representative package totals include `@nop-chaos/flux-runtime` (`88` files / `1122` tests with `1` skipped), `@nop-chaos/flux-renderers-form-advanced` (`71` / `649`), `@nop-chaos/flux-renderers-data` (`36` / `325`), and `@nop-chaos/flux-playground` (`19` / `84`).
- Residual verification notes: the full `pnpm test` run still emits the existing non-fatal `--localstorage-file` warnings in several Vitest packages, and React logs the existing `showClear` DOM-prop warning from `packages/flux-renderers-form-advanced/src/condition-builder/condition-item.test.tsx`; neither warning failed the suite.

### 2026-05-26

- Final closure verification for plan `436` is now recorded on the live repo baseline: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test` all passed.
- Required closure static/audit commands also passed: `pnpm audit:deps`, `pnpm check:active-doc-code-anchors`, `pnpm check:workspace-manifest-deps`, `pnpm check:package-css-exports`, `pnpm check:flux-bundle-pack`, `pnpm check:schema-prop-coverage`, `pnpm check:oversized-code-files`, `pnpm check:audit-suspects`, `pnpm check:audit-reactive-render-reads`, `pnpm check:audit-async-failure-paths`, `pnpm check:audit-fieldframe-bypasses`, `pnpm check:audit-test-global-leaks`, `pnpm check:audit-missing-renderer-markers`, `pnpm check:audit-performance-suspects`, `pnpm check:audit-styling-suspects`, `pnpm check:audit-non-retained-renderer-references`, `pnpm check:audit-react19-optimization-candidates`, `pnpm check:audit-runtime-raw-schema-reads`, and `pnpm check:audit-hardcoded-type-dispatch`.
- The `check:audit-*` scripts still emit suspect inventories, but they exited successfully and did not establish a still-open retained finding under plan `436`; for this closure they are treated as passed source guards, not as a zero-results gate.
- Relevant supported E2E closure coverage is green when rerun serially on an isolated port to avoid Playwright `webServer` reuse collisions: `PLAYWRIGHT_PORT=4185 pnpm exec playwright test "tests/e2e/flow-designer-edge-creation.spec.ts" "tests/e2e/flow-designer-ui.spec.ts" "tests/e2e/code-editor.spec.ts" "tests/e2e/word-editor.spec.ts" "tests/e2e/component-lab/crud-query-and-ownership.spec.ts" "tests/e2e/report-designer-demo.spec.ts" --reporter=list --workers=1` => `43 passed`, `4 skipped`, `0 failed`.
- The four skipped cases in that closure suite remain the intentionally isolated diagnostic specs inside `tests/e2e/flow-designer-edge-creation.spec.ts`, `tests/e2e/flow-designer-ui.spec.ts`, and `tests/e2e/code-editor.spec.ts`; the supported closure surface itself is fully green.
- While rerunning closure E2E, `tests/e2e/report-designer-demo.spec.ts` exposed stale proof text rather than a product regression: the spec still expected all `30` spreadsheet row headers to be mounted even though the live spreadsheet shell is virtualized. The proof was corrected honestly to assert mounted visible row/column headers exist and the first visible labels remain `1` / `A`.
- Independent closure audit `ses_19d08a29bffeNkCb61zTy3oZXF` rechecked the post-verification repo and confirmed the remaining blocker is procedural, not technical: plan `436` still cannot be marked `completed` until the repository full-green protocol is satisfied with a `full-green verification` commit.
- Because commit creation still requires explicit user permission under the assistant's higher-priority operating rules, plan `436` remains `partially completed` / Workstream 10 `blocked` pending that approval, even though the code/docs/test closure surface is otherwise green.
- User approval was subsequently provided in the same session, so this execution now proceeds through the repository full-green protocol: the current branch records the full-green verification baseline for plan `436`, and the corresponding closure commit is created with a subject that explicitly includes `full-green verification`.

- Re-audited the remaining open Flow Designer renderer-contract slice in plan `436` (`维度09-06`) against the live compiler/runtime seam. The verified result is that `designer-page.config` still lacks a supported compiler-owned transport for schema-bearing nested leaves such as `nodeType.body`, `quickActions`, `edgeType.body`, `createDialog.body`, and inspector bodies; the issue is not yet honestly closable by a small renderer-only patch.
- A focused implementation attempt in `packages/flow-designer-renderers/src/renderer-definitions.ts` tried to lower those nested schema leaves through `compileDesignerConfig()` into compiled template nodes, with matching renderer/helper widening in `packages/flow-designer-renderers/src/{designer-page-helpers.tsx,designer-page-body.tsx,designer-inspector.tsx,designer-xyflow-canvas/designer-xyflow-node.tsx,designer-xyflow-canvas/designer-xyflow-edge.tsx,dingflow/ding-flow-edge.tsx}`.
- Focused verification showed that approach is not safe on the current seam: the compiled values still travel through the ordinary `kind: 'prop', compile:` props path, which then fails in `packages/flux-runtime/src/node-runtime.ts` because `resolveNodeProps()` expects a normal runtime-value program and not embedded template nodes (`propsProgram.createState is not a function`).
- Reverted that experiment immediately, restored the prior stable Flow baseline, and re-ran focused proof successfully: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/edge-label-expression.test.tsx src/designer-page-rendering.test.tsx src/designer-page-failures.test.tsx` and `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` are green.
- Key decision: keep `维度09-06` open in `docs/plans/436-deep-audit-2026-05-24-full-remediation-plan.md` until Flow Designer gets an explicit renderer-owned compiled schema transport or a compiler/runtime extension that can carry nested compiled fragments without routing them through ordinary prop-program evaluation.
- Re-audited the remaining Workstream 3 renderer-contract queue against live code instead of the stale parent checklist. Confirmed `维度09-07` is already closed live: `packages/report-designer-renderers/src/renderers.tsx` now models `report-inspector.body` as a compiled `region`, while `packages/report-designer-renderers/src/report-designer-inspector.tsx` keeps host-provided `inspectorPanels` / `resolvedSchema` on a separate dynamic schema channel.
- Closed `维度18-10` on the live code path: `packages/flux-code-editor/src/code-editor-renderer/use-code-editor-binding.ts` now registers the editor with the current form or validation owner through `registerField(...)` whenever `code-editor` publishes `validation.kind = 'field'`, so field participation no longer diverges from the declared validation contract. Focused proof is green in `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor-renderer/use-code-editor-binding.test.tsx`, and package `typecheck` / `build` / `lint` are green for `@nop-chaos/flux-code-editor`.
- Re-audited `维度12-05` by attempting the smallest honest migration of `detail-field` / `detail-view` value-adaptation actions from `prop` metadata to the compiled `event` channel. That attempt exposed a deeper invocation-semantics mismatch between the current detail value-adaptation helper and the compiled event-handler path, causing the focused detail transform/commit regressions to stop honoring the existing import-action payload contract. The attempted migration was reverted immediately, leaving the prior stable behavior in place and confirming `维度12-05` remains a real open blocker rather than stale text drift.
- Focused validation for this turn is green for `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/detail-view/value-adaptation-helper.test.ts` and `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor-renderer/use-code-editor-binding.test.tsx`. Touched-package `typecheck` / `build` / `lint` is green for `@nop-chaos/flux-code-editor`. `@nop-chaos/flux-renderers-form-advanced` still has unrelated pre-existing verification blockers on the current branch (`build` missing `@nop-chaos/flux-react` root exports such as `FormContext` / `ScopeContext` / `ValidationContext`, and `lint` still reports the existing `variant-field-controller.ts` hook-deps issue), so this turn does not establish a new package-green baseline for that package.
- Closed `维度12-05` on a second pass without forcing the DOM `event` channel. `packages/flux-core/src/types/schema.ts` and `packages/flux-compiler/src/schema-compiler/node-compiler.ts` now let custom field compilers emit precompiled owner action programs, `packages/flux-compiler/src/schema-compiler/runtime-value-compilation.ts` now preserves `CompiledActionProgram` values as atomic static leaves so custom-compiled action props are not recursively rewritten by `propsProgram` compilation, and `packages/flux-renderers-form-advanced/src/detail-view/{detail-field.tsx,detail-view.tsx,detail-draft-controller.ts,value-adaptation-helper.ts}` now compile and consume `transformInAction` / `validateValueAction` / `transformOutAction` through that honest compiled owner-action channel while preserving the documented default-payload vs explicit-args behavior for value-adaptation owners.
- Focused proof for the closure is green: `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/detail-view/value-adaptation-helper.test.ts src/detail-view/detail-field-commit.test.tsx src/detail-view/detail-view-transform.test.tsx src/detail-view/detail-view-basic.test.tsx src/detail-view/detail-revalidation.test.tsx` and `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck` both pass. The package still has the same unrelated branch-local blockers for full package `build` / `lint`, so this closes `维度12-05` specifically but still does not establish a new all-green package baseline for `@nop-chaos/flux-renderers-form-advanced`.
- Closed `维度09-06` on a smaller compiler/runtime seam than the first failed experiment. `packages/flow-designer-renderers/src/renderer-definitions.ts` now precompiles schema-bearing `designer-page.config` leaves such as `nodeTypes[].body`, `quickActions`, `edgeTypes[].body`, and nested inspector/create-dialog bodies through `context.compileSchema(...)`, while the earlier `packages/flux-compiler/src/schema-compiler/runtime-value-compilation.ts` atomic-leaf rule now preserves those compiled fragments inside `config` instead of recursively rewriting them through ordinary `propsProgram` object compilation. Existing Flow consumption sites keep using `RenderNodes` / `helpers.render(...)`, but they now receive compiled `TemplateNode` fragments instead of raw authored schema on the audited paths.
- Focused Flow proof is green after that closure: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/edge-label-expression.test.tsx src/designer-page-rendering.test.tsx src/designer-page-failures.test.tsx` and `pnpm --filter @nop-chaos/flow-designer-renderers typecheck` both pass.
- Re-audited the remaining stale unchecked Workstream 3 findings in plan `436` against the live repo and confirmed they are now closed on current code rather than genuinely open defects. `packages/flux-renderers-form-advanced/src/variant-field/{variant-field.tsx,variant-field-controller.ts}` already consume normalized slot/event/action channels for `维度09-01`, `维度09-02`, `维度09-03`, and `维度12-01`; `packages/flux-renderers-form-advanced/src/composite-field/array-field.tsx` already uses compile-time `scalarItemValidation` metadata for `维度09-04` / `维度12-06`; `packages/flux-renderers-data/src/{crud-renderer-definition.ts,crud-renderer.tsx}` and table renderer tests already cover the compiled `queryFormRegion`, `quickEditBodyRegionKey`, and `buttonsRegionKey` seams for `维度09-05`, `维度09-08`, and `维度09-09`; `packages/flux-renderers-basic/src/use-surface-renderer.ts` already allocates declarative surface scopes after commit for `维度07-01`; and the wrapped-field identity drift items (`维度12-02`, `维度12-03`, `维度12-04`, `维度12-07`) are already closed live because `code-editor`, `input-number`, and picker-mode `condition-builder` keep node-level `testid/cid` on the wrapped field root while `dynamic-renderer.loadAction` is modeled on the event channel.
- Focused closure-sync proof is green for `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/variant-field/variant-field-owner-contract.test.tsx src/variant-field/variant-field-detection.test.tsx src/variant-field/variant-field-transform.test.tsx src/composite-field/array-field.test.tsx src/composite-field/array-field-schema-coverage.test.tsx src/condition-builder/condition-builder-renderer.test.tsx` (`6` files, `44` tests), `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-renderer-definition-contracts.test.ts src/__tests__/table-quick-edit-cell.unit.test.tsx src/__tests__/table-data-and-layout.test.tsx` (`3` files, `34` tests), `pnpm --filter @nop-chaos/flux-renderers-basic exec vitest run src/__tests__/basic-dynamic-renderer.test.tsx src/__tests__/basic-page-layout-surfaces.test.tsx` (`2` files, `21` tests), `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor.integration.test.tsx src/code-editor-renderer/use-code-editor-binding.test.tsx` (`2` files, `14` tests), and `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/input-number.test.tsx` (`1` file, `22` tests).
- Closed plans `441`, `442`, and `443` at the code/focused-proof level. `scripts/check-package-css-exports.mjs` now recursively validates direct and object-form CSS export leaves and reports `11 CSS export subpaths across 11 resolved targets`; regression coverage was added in `scripts/__tests__/check-package-css-exports.test.ts` and `pnpm test:scripts` is green.
- `ValidationScopeRuntime` public validation entry points now accept `options?: { signal?: AbortSignal }`, and that carrier is threaded through `packages/flux-runtime/src/{form-runtime.ts,form-runtime-owner.ts,runtime-owned-factories.ts}` plus `packages/flux-renderers-form-advanced/src/detail-view/projected-validation-runtime.ts`. Focused proof is green in `packages/flux-runtime/src/__tests__/{owner-validation-lifecycle-contracts.test.ts,source-observer-action-context.test.ts}` and `packages/flux-renderers-form-advanced/src/detail-view/projected-form-runtime.test.ts`.
- Source-enabled prop execution now preserves caller renderer action context through `packages/flux-react/src/{use-node-source-props.ts,node-source-prop-controller.ts,node-renderer-resolved.tsx}` and `packages/flux-runtime/src/async-data/source-observer.ts`; focused proof is green in `packages/flux-react/src/__tests__/{node-source-prop-controller.test.ts,use-node-source-props.test.tsx}` and `packages/flux-runtime/src/async-data/source-observer.test.ts`.
- Removed one-off runtime-owned event scope creation from the retained `tabs`, report field panel, and table refresh paths by switching those payloads to `evaluationBindings` overlays instead of disposable child scopes.
- Tightened form editability and boolean contract handling: `packages/flux-renderers-form/src/renderers/fieldset.tsx` now declares `collapsible` / `collapsed` as boolean props and uses strict boolean checks after normalization; `input`, `textarea`, and `input-number` now pass native `readOnly`, and choice controls now stop treating read-only state as editable interaction. Focused proof is green in `packages/flux-renderers-form/src/__tests__/{fieldset-renderer-contracts.test.ts,input-number.test.tsx}`.
- Projected composite owner scopes now hard-block `update` / `merge` / `replace` when `readOnly` is published, covering array/variant projected child scopes. Focused proof is green in `packages/flux-renderers-form-advanced/src/{composite-field/array-field-runtime.test.ts,variant-field/variant-field-runtime.test.ts}`.
- Verification status for this turn: touched-package `typecheck` is green for `@nop-chaos/flux-runtime`, `@nop-chaos/flux-react`, `@nop-chaos/flux-renderers-form`, and `@nop-chaos/flux-renderers-form-advanced`; workspace `build` is green; `pnpm test:scripts` and all focused regression suites listed above are green.
- Workspace `lint` and `check` are still blocked by pre-existing repository-wide oversized-file gates rather than this change set. Current blockers include `packages/flux-runtime/src/__tests__/action-adapter.unit.test.ts` exceeding the 700-line hard limit (also reported by `pnpm check:oversized-code-files`), along with other existing oversized files already listed by the gate output.
