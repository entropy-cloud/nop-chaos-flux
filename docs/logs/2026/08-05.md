# Development Log - 2026-08-05

## industrial-hmi plan-2026-08-05-0653-3 收口：0653 open-audit 5×P2 binding/state silent-defect 三 Phase 全绿 + closure-audit approved（B5 re-entrant pointId 归属为本 run 实际代码变更）

执行 `docs/plans/2026-08-05-0653-3-hmi-binding-state-resolution-correctness.md`（open-audit 5 项 P2 silent-defect B1-B5 收口——均「通过 validate 但运行期静默产出错误视觉/状态/数据归属结果」）。Phase 1（B1+B2）、Phase 2（B3）由先前 run 落地（已 `[x]`）；本 run 执行 Phase 3（B4+B5）。三 Phase 全部落地：

- **Phase 1（B1+B2，先前 run）**：`bind-resolver.ts` `resolveBinding` 文本类属性集合（`text`/`fill`/`stroke`/`textColor`）仅对这些 property 施加 format（B1，open P2-4，`visible:false` 绑定真正隐藏）；line/arrow/pipe `applyProps` 从 width/height 重算 points 写回 `node.points`（B2，open P2-8，pipe 补齐 `defaultGeometryPoints`，Decision 裁定方案 (a)）。
- **Phase 2（B3，先前 run）**：`value-to-state.ts` `defaultState` 命名偏好链（run→normal→off→首键）+ `ScadaStateDeclaration.stateSource?`（`"pointId"` 或 `"pointId.property"`）显式 state-driver，`dirty-collector.ts` 优先 consult `declaration.stateSource`（B3，open P2-5）。
- **Phase 3（B4+B5，本 run）**：
  - **B4（open P2-7）**：Decision 裁定方案 (b) validator 拒绝 declaration 级 `scale.expression` 并指向 binding-scale（binding 层 `applyScale` 已消费 expression），同时关闭 `point-store.convert` 与 `value-to-state.applyLinearScale` 两处 drop site。`validate.ts:299-306` 拒绝 + `serialization.test.ts:129-151` failing-first proof 由红转绿（先前 run 已落地，本 run 确认 + tick）。
  - **B5（open P2-9）——本 run 实际代码变更**：`point-store.ts` 删除可变字段 `lastNotifyPointId`，`EventHub` 构造不再传 `onListenerError`（原读 `lastNotifyPointId`），`applyValue` 改用 `this.events.emitWith('point:change', (error) => this.reportSubscriberError(pointId, error), payload)`——per-emit 闭包捕获当前 `pointId`，消除 re-entrant `setPointValue`（listener 回写另一点）下字段被覆盖的归属竞态，错误始终归属正在派发的 pointId。`scada-robustness-hardening.test.ts` 新增 failing-first proof（listener A for p1 re-entrant 写 p2 翻字段、listener B throw on p1-emit → 断言归属 `'p1'` 非 `'p2'`）；自验修复前 RED、修复后 GREEN。
- **结果**：包级 640 tests / 46 files 全绿（较 639 增 1：B5 proof）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **plan 收口**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit gate 由独立 fresh-session sub-agent（task `ses_0300d86c3ffeYQJYXK2oqgkuD3`）执行，verdict `approved`（逐条 file:line citation 经 live repo 核对）。`roadmap-industrial-hmi.md` open-audit P2-4/P2-5/P2-7/P2-8/P2-9 五 backlog bullet 追加 `**已由 plan 2026-08-05-0653-3 收口（✅）**` marker + 头部追加收口记录。无 `> Source Audits:` 行（roadmap-sourced），且仅关闭 open-audit 9×P2 中的 5 项，审计整体未全收口故 `Audit Status` 保持（sibling plan `2026-08-05-0653-4` + 剩余 backlog 待续）。Follow-up：line/arrow/pipe width/height 绑定几何响应的浏览器像素验证为 watch-only residual（attrs/逻辑级单测已覆盖）。

## industrial-hmi plan-2026-08-05-0653-2 收口：0653 审计 4×P1 remediations 四 Phase 全绿（oversized 门禁恢复 + flow diff/indicator z-order/W3 revert-vs-binding 三 live defect 修复）

执行 `docs/plans/2026-08-05-0653-2-industrial-hmi-audit-p1-remediations.md`（两份 0653 审计——open-ended adversarial + multi-dimensional——共 4 条 P1 收口）。四 Phase 全部落地：

- **Phase 1 拆分 2 个超限测试文件（multi P1-2，恢复 oversized 硬门禁）**：`scada-points-bridge.test.tsx` 769→主（427，bridge 主路径：订阅/求值/点值下发）+ `scada-points-bridge-diagnostics.test.tsx`（391，flux 错误上报/去重/reload-clear/deps-empty 诊断类）；`scada-engine.test.ts` 718→主（344，lifecycle/test handle/commands）+ `scada-engine-plugin-sync.test.ts`（190，插件 zoom/move 同步/视口钳制/交互覆盖层 P1-7/P1-9/D3）+ `scada-engine-events-declarations.test.ts`（250，事件桥接线/getSymbolDeclarations/image cache）。5 文件均 <700，纯机械按 `describe` 域边界切分，不改测试语义/断言。`pnpm check:oversized-code-files` industrial 包 0 失败（16→14 ERROR）。
- **Phase 2 修复 `diffScadaConfig` 丢弃 `flow` 字段（open P1-1）**：`serialization/diff.ts:3-30` `SYMBOL_KEYS` 在 `'textSize'` 与 `'custom'` 之间机械补 `'flow'`（与 `config-types.ts:78` 声明序对齐）。failing-first proof 5 用例（仅 flow.enabled 变更、flow.speed/dash 子字段、同值判等、undefined→定义、定义→undefined）+ 集成 proof 2 用例（`diffScadaConfig` → `engine.applyDiff` → `pipe-junction.applyProps` 链路：flow.enabled true→false 时 stub `dashPattern` 由 `[6,4]`→`[]`，flow.dash 变更切到新 dash 序列）。
- **Phase 3 修复 `scada-sensor-control-indicator` 绘制顺序（open P1-2）**：`symbols/sensor-control/indicator.ts:42-45` 复合子序 swap `[lamp(body), housing]` → `[housing, lamp(body)]`——leafer `Group` 后入子在上层渲染，原序使不透明 housing（`#455a64`，覆盖全 bounds）绘制在 lamp 之上完全遮挡灯体；swap 后 housing 先绘作背景层、lamp 后绘于上层状态色可见。`name:'body'` 仍在 lamp 确保 `applyCompositeProps` 状态色路由不变。z-order 可观测 proof 3 用例（mock `children` 数组按入序保留即 render order，断言 `[housing, lamp]` 入序 + 状态色 fill 在 lamp 上层可见 + 与兄弟复合图元 motor 子序语义对齐消除离群点）；自验 swap 前 3 例全红、swap 后全绿。
- **Phase 4 修复 W3 revert 覆盖同帧 binding 值（multi P1-1）**：`symbols/visual-state.ts` `StateVisualApplier.applyState` revert 分支（line 61-74）revert 某字段前查 `instanceProps.bindings?.[key]`，若有 binding 跳过该字段的 revert collect（同帧 `collectBindings` 已把 binding 解析值写入 pending，Map last-write-wins 是正确终值；revert 会覆盖为 base/reset 空值）。无 binding 时 revert 行为不变。failing-first 扩展 alarm-storm frame 3 fill-value 断言（`frame3Arg['s0']?.fill === 2` 即 binding 解析值，修复前为 `''` base/reset）+ 聚焦回归 3 用例（alarm→unstyled 退出停在 binding 值、无 binding revert 行为不变、active 态 state-style 仍胜出）。W3 Decision 注记补 binding-vs-revert 三路裁定；`design-data-binding.md §4.3` 同步。
- **结果**：包级 628 tests / 46 files 全绿（较 615/43 增 13 tests / 3 files：flow diff 5 + 集成 2 + z-order 3 + W3 revert 3）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿、`pnpm check:oversized-code-files` industrial 包 0 失败。
- **plan 收口 + 源审计关闭**：四 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 技术项勾选 + `Plan Status: completed`；closure-audit 项保留 `[ ]` 待独立 fresh-session sub-agent（执行 session 不自审，AGENTS.md human gate）。源审计 `2026-08-05-0653-open-audit` / `2026-08-05-0653-multi-audit` `Audit Status: planned → closed`。`roadmap-industrial-hmi.md` 头部追加本 plan 收口记录。15 P2 已在 roadmap Follow-up Backlog「2026-08-05-0653 post-remediation audit P2」子节追踪，按 mission 节奏择期处理。

## industrial-hmi plan-2026-08-05-0653-1 收口：roadmap Follow-up Backlog 标注漂移 hygiene pass（纯文档，无代码变更）

执行 `docs/plans/2026-08-05-0653-1-hmi-roadmap-backlog-annotation-consistency.md`（plan `2026-08-05-0325-1` Non-Blocking Follow-ups line 156 登记的 deferred：roadmap Follow-up Backlog `2026-08-04-2242` post-remediation 子节 State & lifecycle（L1-L6，roadmap:444-449）+ Wiring W3（roadmap:462）共 7 条 per-line 条目底层缺陷实际已由 plan `2026-08-04-2243-1` 三 Phase 收口，但 per-line `已由 plan ... 收口` marker 从未回写）。纯文档 hygiene pass，不触碰任何代码：

- **标注回写**：逐条补齐 7 条 marker（格式与同节兄弟条目 D1-D4/W4/W5/T1-T6/A1/Doc1-Doc3 对齐：`plan + Phase + (Lx/W3) + 一句话落地摘要 + Proof`）——L1 RefreshPipeline destroyed 门控、L2 DirtyCollector collect/flush/flushFrame 守卫对称、L3 releaseRuntime collector 单一 owner、L4 pendingSkipNonce per-import nonce、L5 lastReportedErrors 与 compiledCache 对称清空、L6 reloadConfig useCallback 稳定身份、W3 collectStates/StateVisualApplier 合帧 owner 裁定（方案 a）。
- **全节扫描兜底**：Follow-up Backlog 全节（roadmap:366-483）69 条 backlog bullets 全部带 marker，无其他缺口。
- **roadmap 头部共识审查记录**：追加一条本 hygiene pass 的 closure 记录（对齐既有 closure 记录格式）。
- **结果**：纯文档计划，Test Strategy = 不适用；无 Phase Status / Work Items / Dependency Graph / Cross-Cutting / Rule 正文语义改动。workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿（FULL TURBO，确认文档变更未扰动既有 green baseline）。

## industrial-hmi plan-2026-08-05-0325-1 收口：复杂 flux 表达式订阅 `flux-deps-empty` 诊断（W1 successor）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-05-0325-1-hmi-flux-deps-empty-diagnostic.md`（roadmap Follow-up Backlog W1 / 源审计 `2026-08-04-2242-multi-audit` §Wiring `[P2]`：复杂表达式（`${analog.temp + 1}` 类）在平台依赖收集 probe 失败时 `extractExpressionDepsViaProbe` 静默返 `[]` → `useScopeSelector` disabled → 表达式永不随 scope 更新且对 author 不透明；successor 从未创建，本 plan 即该 successor）。两 Phase 全部落地 + 独立 closure-audit 通过：

- **Phase 1 Proof 先行（failing-first）**：`scada-points-bridge.test.tsx` 增两例——(1) 注入 collector 返空 deps 的 `ExpressionCompiler` stub（`createState` 返 root 无 `dependencies`、`evaluateValue` 返值），渲染 `useScadaPointsBridge`，断言 `onError` 收**一次性** `flux-deps-empty`（message 含 `${analog.temp + 1}` 片段）、重复 scope 更新不重报、仅 flux-deps-empty 无 status-upgrading 码（实现前红）；(2) 纯字面量 `${1 + 2}`（无标识符）不触发 flux-deps-empty 的负向断言。typecheck 通过，红例确认「flux-deps-empty 未上报」。
- **Phase 2 检测+上报+注册表+i18n+文档**：(a) `scada-errors.ts` `SCADA_ERROR_CODES` 增 `'flux-deps-empty'`，注释块归入「不升级画布 status」组；(b) i18n en-US/zh-CN 对称位置增 `industrial.scada.error.flux-deps-empty` 文案；(c) 新增 `expressionReadsScope(candidate)` 谓词（含标识符即为真，排除 `${1 + 2}`）；(d) 引入 `analyzeFluxSubscriptions(config, options) → { paths, depsEmptyExpressions }`（复杂表达式分支 probe 返空 deps 且 `expressionReadsScope` 为真时入 `depsEmptyExpressions`），`extractFluxScopePaths` 降为薄包装返 `.paths`（保留既有签名/导出/测试不破坏）；(e) `useScadaPointsBridge` `useMemo` 改调 `analyzeFluxSubscriptions`，新增 effect 对 `depsEmptyExpressions` 每项经 `reportOnce(expression, 'flux-deps-empty', 合成 Error)` 一次性上报（不升级 status）；(f) `scada-canvas.tsx` `reportDiagnostic` 把 `flux-deps-empty` 纳入 `monitor.onError` expression-phase 分支；(g) `scada-errors.test.ts` 增注册表 + i18n key 断言；(h) `scada-canvas-diagnostic-channels.test.tsx` 增 reportDiagnostic 分支 focused 用例（stub compiler 触发 flux-deps-empty → monitor phase `expression`/details.code `flux-deps-empty` + 画布 ready + 零 scada:error）。
- **owner-doc 同步**：`design-data-binding.md §9.1` 增 `flux-deps-empty` 诊断语义段（触发/一次性/非升级/不派发 `scada:error` + 静默 disable 残留已 surfaced 注记 + `expressionReadsScope` 启发式限制注记）；`design-renderer.md §8.1` 诊断出口段 + 错误码枚举行补 `flux-deps-empty`。
- **结果**：包级 615 tests / 43 files 全绿（+3 focused 用例：bridge flux-deps-empty 一次性 + 字面量负向 + reportDiagnostic 分支；+2 既有测试断言扩展）、workspace 全量验证全绿（typecheck 32/32、build 32/32、lint 32/32、test 59/59）。
- **closure-audit（独立 fresh-session 子 agent）**：判定 `approved`——见 plan `Closure Audit Evidence`。
- **plan 收口 + roadmap 回写**：两 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`；`roadmap-industrial-hmi.md` Wiring dim 22 W1（roadmap:459）回写「已由 plan 2026-08-05-0325-1 收口」。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：平台 collector 扩展复杂表达式支持面（out-of-scope improvement，独立包演进）、roadmap 标注漂移（L1-L6/W3 缺收口 marker，roadmap hygiene 轮统一回写）。

## industrial-hmi plan-2026-08-04-2243-3 收口：HMI 验证 fidelity / 公共面 / 文档漂移十一项 finding 三 Workstream 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2243-3-hmi-verification-fidelity-public-surface-doc-drift.md`（multi-audit dim 14/23 六项 P2 T1-T6 验证 fidelity + dim 03 一项 P2 A1 公共面 + dim 16 三项 P2 Doc1-Doc3 文档漂移 + dim 22 一项 P2 W2 死代码，共十一项 in-scope finding），三 Workstream 全部落地 + 独立 closure-audit 通过：

- **WM1 mock fidelity（T1+T2）**：`leafer-ui-mock.ts` `MockLeafer` 改 `extends MockZoomLayer` + `get zoomLayer(): this`（对齐真实 leafer `tree.zoomLayer === tree` 身份，消除独立实例掩蔽产线 viewport transform 漂移）；`MockApp.destroy()` 移除原 `this.zoomLayer.destroy()`（身份变 `this` 后会无限递归）；`MockLeaf` 加 `getBoundsToWorld()`/`getBounds()`/`get worldBox` 抛「mock 不建模 bounds API」错。新增 `leafer-ui-mock.test.ts` mock-invariant 单测（身份 `app.tree.zoomLayer === app.tree` + zoom 能力写回 + bounds 抛错 + toJSON 不含 getter）。
- **WM2 e2e 断言加固（T3+T4+T5+T6）**：T3 `scada-pressure-demo.spec.ts` 10k/overview-restore 两测试补 `assertScadaCanvasRendered`；T4 `scada-perf.spec.ts` memory + 10k-refresh 两测试补硬门；T5 `scada-canvas-assert.ts` 像素探测根因修复——旧实现 `canvas.evaluate` 只读 `[data-slot="scada-canvas-canvas"]`（`querySelector('canvas')` 返回的首个 ground 透明背景层，漏掉 tree 层已绘内容），改扫 `[data-slot="scada-canvas"]` 容器内**全部** canvas 任一命中非零像素即 confirmed，非空场景（symbols>0）`fallback-all-zero` 判失败（新增 `allowZeroPixels` 例外通道用于已知 off-content 视口，如 perf throughput 循环）；T6 五处 `not.toThrow()` 弱断言换副作用负向断言（robustness `isPlaying===false` ×2 + 无 animator 直接调用；lifecycle `getPointValue('level')≠999` 证销毁 guard 不写；points-bridge `readOwn()` 不变证只读 scope；definitions 直接调用 + `toHaveLength(1)` idempotency）+ 删重复 `computeSymbolBounds([])` 测试。
- **WM3 公共面/死代码/文档（A1+W2+Doc1+Doc2+Doc3）**：A1 `index.ts` 移除 `export type { IndustrialRendererSchema }` + `renderer-definitions.ts` 删类型别名及未用 import（仓内零代码消费者）；W2 `config-adapter.ts` 删 `setConfig()` 死方法；Doc1 `design-renderer.md §10` 表 loading/error 行 marker 列 `—` → `nop-scada-canvas-loading`/`nop-scada-canvas-error`；Doc2 §6 retire `scada-canvas-overlay` slot 引用（与 §10 一致）；Doc3 `design-engine.md §8.3` ScadaTestHandle 补 `setPointValues?`/`measureAddStrategies?` + 非公共契约注（对齐 `test-handle.ts` 9 成员）。
- **结果**：包级 612 tests / 43 files 全绿（+7 mock-invariant 回归用例）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿、scada e2e 全量全绿（pressure-demo 3/3 + perf 5/5 + demo/edge-cases 18/18；非空场景 pixelProbe=confirmed、空场景正确允许 fallback-all-zero）。
- **closure-audit（独立 fresh-session 子 agent `ses_031cbf1bcffeusatLqH7U1VFfY`）**：判定 `approved`——11 项 finding 逐项 live 核对（interface↔semantics trace：`MockLeafer extends MockZoomLayer` + `get zoomLayer()` 返 this + destroy 无递归；bounds 三方法/getter 抛错；e2e 硬门全覆盖；T5 全 canvas 扫描 + 非空严格门；T6 五目标文件 `not.toThrow()` 清零 + 替换为副作用负向断言；A1/W2 仓内零消费者；Doc1-3 文档↔live code 对齐），deferred 诚实（T6-residual 实际未触发，5 处全清零）。
- **plan 收口 + roadmap 回写**：三 Workstream Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；`roadmap-industrial-hmi.md` 回写收口记录（dim 14/23 T1-T6 + dim 03 A1 + dim 16 Doc1-Doc3 + dim 22 W2 标注「已由 plan 2026-08-04-2243-3 收口」+ 共识审查记录加 ✅ 条目）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：W1 表达式订阅诊断 deferred（successor 见 plan `{2243-1}`）、mock bounds API 升级确定性建模为 optimization candidate。

## industrial-hmi plan-2026-08-04-2243-2 收口：HMI 显示几何/视口数学/数据路径守卫六项 finding 三 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2243-2-hmi-geometry-viewport-data-path-correctness.md`（multi-audit dim 21 四项 P2 D1-D4 显示几何/视口 + open-audit dim 22 两项 P2 W4/W5 数据路径守卫，共六项 in-scope finding），三 Phase 全部落地 + 独立 closure-audit 通过：

- **Phase 1 默认几何 + base text 居中（D1+D2）**：`symbol-types.ts` `ScadaSymbolDefinition` 增 `defaultGeometryPoints?: (node) => unknown[]` resolver；`polygon.ts` 导出 `DEFAULT_TRIANGLE` + resolver、`line.ts`/`arrow.ts` 增由 width/height 派生的 `[0,0,w,h]` resolver（与 create 几何同源，抽默认值常量）；`use-scada-config-sync.ts` `boundsOfNode` 无显式 custom.points 时 consult 定义默认几何算包围盒（重构 `boundsFromCustomPoints` → 通用 `boundsFromPoints(node, raw)`），fit 不冲 MAX_SCALE；`text.ts` 增 `measureTextWidth`（canvas 2d `measureText` + happy-dom/jsdom 0.6em/char fallback）+ 自动宽下 center/right 按测量设 width 并把原点平移到对齐锚（center→文本中心、right→文本右缘），居中生效。Proof：D1 focused 单测（default-triangle polygon bounds {100,200,100,86}、零高 line {0,0,100,0}、fit scale<MAX_SCALE、显式 points 优先、非 points 族回退）+ D1 e2e 几何断言（`scada-edge-cases.spec.ts` 默认几何 fit scale<20 且>1，playground 加 `default-geometry-fit` 载体）；D2 attrs 级单测（width=60/x=70 center、x=38 right、left 不触发、显式 width 不覆盖）。
- **Phase 2 wheel 锚点 + viewport 契约（D3+D4）**：核实 leafer-in viewport 源码 `getZoomEventData` 透传指针 `event.x/y`（screen 坐标），`scada-engine.ts` `handlePluginZoom` 钳制改用 `readZoomAnchor(event)` 光标锚（`scaleOfWorld(cursor, clamped/raw)`）→ 光标下内容点保持固定、超界无视觉偏移；事件缺 x/y（程序触发/旧消费方）回落 `{0,0}`（P1-9 命令路径不变式：视口 x/y 不变）。D4 Decision 维持 viewport prop 不对称现状（`design-renderer.md §8.3` 已显式标注「policy 仅 full/reset 路径应用」，复核确认，不新接 effect）。Proof：D3 focused 单测（光标 {200,100} 钳制 25→20 → 视口 {140,75,20} + `getViewportPoint({150,80})==={200,100}` 内容固定；缺坐标回落非回归）。
- **Phase 3 数据路径守卫（W4+W5）**：`use-scada-points-bridge.ts` `isScadaPrimitive` 改导出（flux 桥接与 host 句柄共用）；`use-scada-handles.ts` `setPointValue` case 复用校验，非原始值（对象/数组/null）返 `{ok:false}` 且点表不变（移除 `as ScadaPrimitive` 强转）；`diff.ts` `valuesEqual` 由 `JSON.stringify` 改 own-keys 递归比较（按 a 的 keys 在 b 查表 + 递归、数组按 index），key 插入序不再影响判等。Proof：W4 focused 单测（`use-scada-handles.test.ts` 新建——对象/数组/null→{ok:false}+点表不变、primitive 接受）；W5 focused 单测（异源 key 序不同 shadow/custom/scale 判等不假阳性、真实变更仍检出、缺键检出、数组序敏感）。
- **owner-doc 同步**：`design-engine.md §4.4` 增 D3 增补（wheel clamp 光标 screen 锚，与 P1-9 命令路径 screen 原点锚区分）；`design-renderer.md §8.5` `setPointValue` 行补 W4 `invalid-point-value` 失败路径 + `isScadaPrimitive` 注记。
- **结果**：包级 606 tests / 42 files 全绿（+26 focused 回归用例跨三 Phase：D1 8 + D2 4 + D3 2 + W4 5 + W5 7）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **closure-audit（独立 fresh-session 子 agent `ses_031eb2b3dffeqK21aYXa7Wgvy0`）**：判定 `approved`——六项 finding 逐项 live 核对（interface↔semantics trace：`boundsOfNode` 消费 resolver、`handlePluginZoom` 消费 `readZoomAnchor`、`setPointValue` 消费 `isScadaPrimitive`、`diffScadaConfig` 消费 `valuesEqual`）、focused 测试断言正确结果、deferred 诚实（D2-browser-pixel watch-only residual 非阻塞）、owner-doc 一致、独立复跑包级 typecheck/test 606/606 全绿。
- **plan 收口 + roadmap 回写**：三 Phase Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；`roadmap-industrial-hmi.md` 回写收口记录（Display & positioning dim 21 D1-D4 + Wiring dim 22 W4/W5 六项标注「已由 plan 2026-08-04-2243-2 收口」+ 共识审查记录加 ✅ 条目）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。Follow-up：D2 浏览器像素验证 watch-only residual（无 successor）、W1 表达式订阅诊断 deferred（successor 见 plan `{2243-1}`）。

## industrial-hmi plan-2026-08-04-2243-1 执行：HMI 生命周期/销毁/重载/合帧流水线 hardening 三 Phase 全绿（closure-audit 待独立 sub-agent）

执行 `docs/plans/2026-08-04-2243-1-hmi-lifecycle-destruction-pipeline-hardening.md`（multi-audit dim 04/07 六项 P2 L1-L6 + open-audit W3 状态样式双写，共七项 in-scope finding），三 Phase 全部落地，包级 + workspace 全量验证全绿：

- **Phase 1 销毁门控对称（L1+L2+L3）**：`dirty-collector.ts` `DirtyCollector` 的 `destroyed` 守卫扩展到 `collect`/`flush`/`flushFrame`（与 `requestRender` 对称）；`RefreshPipeline` 增自身 `destroyed` flag，`requestRender`/`flushFrame` 入口守卫，`destroy()` 置位 + 幂等；`use-scada-engine.ts` `releaseRuntime` collector 销毁单一 owner（移除冗余 `collector.destroy()`，`pipeline.destroy()` 内部销毁为唯一路径）。Proof 三组（`dirty-collector.test.ts` collector destroyed 全 no-op；`refresh-pipeline.test.ts` pipeline destroyed requestRender/flushFrame no-op + `pipeline.destroy()` 销毁 collector 计数=1 幂等）。
- **Phase 2 重载泄漏收口（L4+L5）**：`use-scada-config-sync.ts` `pendingSkipRef` 计数器改 per-import nonce（`importSeqRef` 单调 + `pendingSkipNonceRef` single-use，effect 首次运行即消费，消除「identity 不匹配不递减 → 残留 → 后续巧合匹配误 skip」泄漏）；`use-scada-points-bridge.ts` 既有 `compiledCache.clear()` effect 补 `lastReportedErrors.current.clear()`（与 compiledCache 对称，config reload 后同表达式错误重报）。Proof 两组（`use-scada-config-sync.test.ts` renderHook：nonce 无泄漏 + self-induced re-run 仍 skip；`scada-points-bridge.test.tsx`：config reload 后同表达式错误重新上报）。
- **Phase 3 handle 稳定 + 合帧 owner（L6+W3）**：`scada-canvas.tsx` `reloadConfig` 内联箭头包 `useCallback`（依赖 `syncImported` 稳定身份）→ `useScadaHandles` effect 不每渲染重登；W3 裁定**方案 (a)**（单一合帧 owner）：active-state 样式 owner=`collectStates`（保持不变，每帧批量写、状态色胜出 binding），退出恢复 base（revert）owner=`StateVisualApplier` 经同一 `collector.collect` 汇入帧尾单次写（`visual-state.ts` 不再 immediate 写 active，构造增可选 `collector` 参数，生产装配恒传、既有单测省略时 revert 退回 immediate 向后兼容）；`dirty-collector.ts` 未改（refresh-pipeline 状态样式断言无需变更）。Proof 两组（`scada-handles.test.tsx`：同 props re-render register 计数不递增；`state-visual.test.ts`：alarm-storm N 图元同帧状态切换 engine.applyAttrs=1 次/帧 + collectStates 仍为 active owner）。
- **owner-doc 同步**：`design-engine.md §4.2` 销毁门控对称 + 单一销毁 owner 注记；`design-data-binding.md` 帧尾批量写段补状态样式合帧 owner 裁定（active=collectStates / revert=StateVisualApplier 经 collector 合帧）。
- **结果**：包级 580 tests / 41 files 全绿（+10 focused 回归用例跨三 Phase）、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。closure-audit 待独立 fresh-session sub-agent 执行（执行 session 不自审，Closure Gates 中该项 `[ ]` 保留为 human gate）。
- **plan 收口 + roadmap 回写**：三 Phase Status completed + 全部 items/Exit Criteria 勾选 + `Plan Status: completed`（Closure Gates 验证项 typecheck/build/lint/test/findings-landed 勾选，closure-audit 项保留 human gate）；`roadmap-industrial-hmi.md` 回写收口记录（State & lifecycle dim 04/07 L1-L6 + Wiring W3 七项）。源审计关闭步骤省略（plan front matter 为 `> Source:` 非 `> Source Audits:`，roadmap-sourced）。W1（复杂表达式订阅诊断）deferred adjudicated（successor）。

## industrial-hmi plan-2026-08-04-2242-2 收口：x/y 三层契约收敛（消除静默 NaN viewport）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2242-2-hmi-config-xy-bounds-contract-plan.md`（open-audit `2026-08-04-2242` `[P1]` x/y 三层契约漂移：TS 类型 `x`/`y` 必填 ↔ JSON validator 可选 ↔ runtime bounds consumer 无默认 → JSON 省略 x/y + viewport fit → NaN viewport → 画布空白且 `data-status="ready"` 无错误信号），两 Phase 全部完成 + 独立 closure-audit 通过：

- **Phase 1 Decision + Proof 失败用例先行（不动产线）**：Decision 裁定三层收敛到「x/y 可选 + 默认 0」（依据：open-audit 明示意图为 default-to-0；validator 已当可选；`interaction-overlay.ts:43-44` 已默认 `?? 0`；同接口 width/height/rotation/scale 已可选——类型改动归属 Phase 2）。Proof-1/2/3 三组失败用例入库（`use-scada-config-sync.test.ts` bounds 有限/原点 0 + viewport fit 有限；`viewport.test.ts` clampViewport NaN/Infinity→0），经 `as ScadaSymbolNode` 绕过当前必填类型构造省略 x/y 节点；产线未动，三组用例实跑红。
- **Phase 2 Fix 一并落地恢复 typecheck**：Fix-0 `config-types.ts` `x?`/`y?` 改可选；Fix-1/2 `use-scada-config-sync.ts` `boundsOfNode`/`boundsFromCustomPoints` `?? 0`（镜像 interaction-overlay.ts:43-44）；Fix-3 `viewport.ts` `clampViewport` 加 `Number.isFinite` 防御非有限 x/y（关闭仅钳 scale 的 position 放大器，与 clampScale 对称）；Fix-4 `config-adapter.ts` Group 构造 `?? 0`（leaf 路径经 symbol defaults `x:0,y:0` + `deepMergeInstanceProps` undefined-skip 已安全，裁定不需补，扫描清单 5 点入 plan）。
- **结果**：三层契约读法收敛一致（type 可选 + validator 可选 + runtime 默认 0），两「node geometry」consumer（bounds/interaction-overlay）不再矛盾；Proof 三组由红转绿；包级 570 tests / 41 files 全绿、workspace 全量验证（typecheck 32/32、build 32/32、lint 32/32、test 59/59）全绿。
- **owner-doc 同步**：`docs/components/industrial-hmi/design-renderer.md §4.2` `ScadaSymbolNode.x/y` 改可选 + 默认 0 语义注记。
- **closure-audit（独立 fresh-session 子 agent `ses_0322c7a16ffeRLPgu2ZntwSI0z`）**：判定 `approved`——Phase 1/2 Exit Criteria 逐项 live 核对、interface↔semantics trace（`{id,type,width,height}` → `computeSymbolBounds` → `fit` → 有限 viewport；`clampViewport({x:NaN})` → 0）、Fix-4 扫描诚实性（leaf 路径 31 symbol defaults + undefined-skip 兜底 sound）、两 consumer 一致性、plan 文本一致性、独立复跑包级 typecheck/build/lint/test 570/570 全绿。
- **plan 收口 + 源审计关闭**：Phase 1–2 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 回填）；open-audit `2026-08-04-2242-open-audit-industrial-hmi.md` `Audit Status: planned → closed`；`roadmap-industrial-hmi.md` 回写收口记录。

## industrial-hmi plan-2026-08-04-2242-1 收口：HMI 诊断通道接通（P1-1 onError + P1-2 onHandlerError）两 Phase 全绿 + closure-audit approved

执行 `docs/plans/2026-08-04-2242-1-hmi-diagnostic-channel-wiring-plan.md`（multi-audit `2026-08-04-2242` P1-1/P1-2：flux 编译/求值失败去重上报通道与用户侧图元事件处理器 throw 去重上报通道在 engine/hook 层已建好、React renderer 层从未订阅），两 Phase 全部完成 + 独立 closure-audit 通过：

- **Phase 1 选定诊断出口 + 接通 onError（flux 数据错误）**：`scada-canvas.tsx:120-136` 新增统一 `reportDiagnostic(code, message)` 非升级出口——(1) `console.warn('[scada-canvas]', code, message)` 保底可见（dev+prod，去重已在上游 hook/engine 层完成故每唯一错误仅 fire 一次）；(2) flux 编译/求值错误额外复用既有 host telemetry 钩子 `RendererEnv.monitor.onError`（`ExpressionExecutionEnv.monitor`，phase:'expression'）。出口整体 try/catch 自保护（通道自身 throw 不得回流 engine/hook）。`scada-canvas.tsx:217` `useScadaPointsBridge({...})` 加 `onError: reportDiagnostic`。Decision 裁定 option (b)（option (a) `onDiagnostic` schema 事件不采纳——新增即 schema 契约变更）；不违反 §8.1（不动 `setStatus`/`setErrorInfo`/`notifyError`，画布保持 ready，不派发 `scada:error`——诊断 ≠ status 升级）。Proof 4 例（`scada-canvas-diagnostic-channels.test.tsx`）：flux evaluate 失败经出口可见 + 画布 ready + 零 scada:error 派发、同表达式多次触发去重（上报计数不增长）、出口自身 throw 自隔离、handler-error（Phase 2 共享出口）。
- **Phase 2 接通 onHandlerError（用户侧 action throw）**：`use-scada-engine.ts:42` `UseScadaEngineArgs` 加 `onHandlerError?: (error: unknown) => void`；`:147` `ScadaCanvasEngine.create({...})` 经 `latest` ref 转发 `onHandlerError`（对齐既有 `onSymbolEvent`/`getPointValuesFor` 转发模式）；`scada-canvas.tsx:163-164` `useScadaEngine({...})` 加 `onHandlerError` 订阅与 Phase 1 同一 `reportDiagnostic` 出口。`event-bridge.ts` `safeRun`/`reportHandlerError` 去重 machinery 未改动（仅消费者接通）。Proof 覆盖 handler throw → 诊断出口被调（`handler-error`）+ 画布 ready + 后续点击仍可达 + 去重仅一次上报。
- **owner-doc 同步**：`docs/components/industrial-hmi/design-renderer.md §8.1` 新增「运行期诊断出口（非升级，plan 2026-08-04-2242-1）」段落，明确 `reportDiagnostic` 出口语义（console.warn 保底 + flux 错误复用 `env.monitor.onError`、不升级画布 status、try/catch 自保护、handler-error host telemetry 后置为 Non-Blocking Follow-up）。
- **closure-audit（独立 fresh-session 子 agent）**：判定 `approved`——两 Phase items/Exit Criteria 逐项 live 核对（`scada-canvas.tsx` onError+onHandlerError 订阅行号、`UseScadaEngineArgs.onHandlerError` 字段、`ScadaCanvasEngine.create` latest ref 转发、`reportDiagnostic` try/catch 自保护、`event-bridge.ts` safeRun 去重 machinery 未改、测试 4 例实跑、`design-renderer.md §8.1` 同步段落）；anti-hollow 抽查（`reportDiagnostic` 运行期经两条订阅链路可达，非空实现）；deferred 诚实（`lastReportedErrors` 跨 config 不清 + host telemetry 基础设施均登记 Non-Blocking Follow-ups，非 in-scope live defect 降级）；五处状态自洽（Plan Status / Phase Status / Exit Criteria / Closure Gates / 本日志收口记录）。
- **plan 收口**：Phase 1–2 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（Closure Audit Evidence 已回填）。
