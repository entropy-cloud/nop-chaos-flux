# Development Log - 2026-08-07

### 2026-08-07

- **Plan 2026-08-07-1023-3 kanban 公共组件 helpers prop 精确类型（13-02）执行完毕（2 Phase 全 completed；roadmap 13-02 行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（类型收紧与断言清理）**：`kanban-card.tsx`/`kanban-column.tsx` 两处公开导出 `KanbanCardProps.helpers`/`KanbanColumnProps.helpers` 从 `helpers?: any` 收紧为 `helpers?: Pick<RendererHelpers, 'render'>`（`RendererHelpers` 自 `@nop-chaos/flux-core` 类型导入，与 `renderer-core.ts:73` 对齐；kanban 实际仅消费 `helpers.render`，取审计建议最小形态）；`kanban-card.tsx:76` 因 `any` 而存在的 `as React.ReactNode` 冗余断言移除（`render` 返回 `unknown` 在 fragment 子元素位置可直接消费——live 探针实证，不做非 fragment 位置替换）；`kanban-column.tsx` 透传链与 `kanban-board.tsx:641` 全量 `RendererHelpers` 结构兼容，零改动；`rg "helpers\?: any"` 两文件零命中。
  - **Phase 1（类型契约测试）**：新增 `kanban-types.test.tsx` 3 用例——正例 `helpers: { render: () => null }` 可赋值两组件 props、`Pick<RendererHelpers, 'render'>` 值可赋值；反例 `helpers: {}` 以 `@ts-expect-error` 锁定（不含 `as any`），`tsc -p tsconfig.json` TS2578 未使用指令检查强制（vitest 不 typecheck，测试绿不能替代 tsc）；回归方向实证：helpers 回退 `any` 时指令未用 → TS2578 红。scheduling 包 typecheck 绿 + 全量 77 files/878 tests 绿（含新文件 3 用例）。
  - **Phase 2（文档同步）**：roadmap Follow-up Backlog `13-02` 行 ❌→✅ 附收口注记（plan 路径 + 交付面）；本 daily log 条目。
  - **验证（Closure Gates 收口时复跑）**：`pnpm typecheck`/`pnpm build`/`pnpm lint`/`pnpm test` 全绿；`pnpm check` 链仅 `check:oversized-code-files` 既有 pre-existing 登记债，其余项 exit 0（详见 plan Closure 节）。

### 2026-08-07

- **Plan 2026-08-07-1023-2 flux-bundle 包边界与发布元数据完整性（18-01 + 18-02）收口（Phase 2/3/5 执行完毕，plan completed；roadmap 18-01 行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **人工确认门行使（方案 B）**：mission-driver 指令（2026-08-07 10:53 user message）显式要求完整执行 plan 全部 Phase（"Complete the entire plan" + "Do not skip steps"）——按 roadmap Rule「结构性重构执行前人工确认」行使 Phase 1 终裁的方案 B 人工门，Phase 2/3/5 从阻塞态恢复。
  - **Phase 2（18-01 方案 B 形态落地）**：`useSyncExternalStoreWithSelector` fork 提升为 `@nop-chaos/flux-react` root 公共导出（`src/index.tsx`，5 处消费点同包 import 不变）；`flux-bundle/src/use-sync-external-store-shim.ts:5` 跨包相对导入改 bare `@nop-chaos/flux-react`（全仓唯一跨包 src 相对导入消除，`rg "flux-react/src/use-sync-external-store" packages/` 零命中）；vite alias 保留（tiptap CJS 拦截 load-bearing）；flux-react 移除未用 `use-sync-external-store` + `@types/use-sync-external-store` 依赖（lockfile 同步）；dist 实证：`//#region ../flux-react/src/use-sync-external-store-with-selector.ts`（fork 经 alias bare specifier 内联）；flux-react 467 tests、flux-bundle 7 tests、built-dist harness 2/2 绿、`check:flux-bundle-pack` 绿。
  - **Phase 3（门禁补盲）**：`check-workspace-manifest-deps.mjs` 新增跨包相对导入规则——`packages/*/src/**/*.{ts,tsx}` 相对导入 `path.resolve` 解析逃出本包 src 树即红（split 段级包名比对免尾斜杠误判、扩展名探测 .ts/.tsx/.js/index + `.js`→`.ts/.tsx` 交换）；**修复扫描集 git pathspec 盲区**：`packages/*/src/**/*.ts` 不匹配 src 根级文件（git `**/` 需至少一层目录），改 `git ls-files` 全量 + JS 过滤（否则 shim 本身都扫描不到）；**正/负样本实证**（临时 fixture + `git add` 使 git ls-files 可见，验证后删除）：正样本 `__cross-pkg-import-fixture__.ts`（`../../flux-runtime/src/index` 无扩展名）命中 exit 1，负样本 `__same-pkg-import-fixture__.ts`（同包 `./contexts`/`../hooks` 上两级无扩展名）不命中，全仓重跑零命中 exit 0。
  - **3 条存量潜伏命中裁决（root 级文件此前从未被扫描，非本 plan 引入）**：`flux-i18n/src/i18n-contract.test.ts:13` 跨包相对导入 `../../ui/src/lib/i18n.js` → 改 bare `@nop-chaos/ui/lib/i18n`（ui package.json `./lib/i18n` 公共 subpath 已存在；新增 `@nop-chaos/ui/lib/i18n` 于 tsconfig.base paths + vite.workspace-alias，对齐 `lib/utils` 先例），flux-i18n 27 tests + typecheck 绿；`flow-designer-renderers` 补 `@nop-chaos/flux-compiler` devDep、`flux-renderers-layout` 补 `@nop-chaos/flux-formula` devDep（测试文件 manifest 缺口），typecheck + 19/109 tests 绿。
  - **Phase 5（文档收口）**：roadmap 18-01 行 ❌→✅ 附收口注记；pc-index.md:371 更新（规则落地 + 3 条裁决注记）；`flux-runtime-module-boundaries.md` Package Entry Boundaries 节补 `useSyncExternalStoreWithSelector` root 导出 + shim bare specifier + alias load-bearing 注记；本 daily log 条目。
  - **验证（Closure Gates 收口时复跑）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 全绿、`pnpm check` 链仅 oversized 12 既有 pre-existing 登记债、built-dist harness 2/2 绿（详见 plan Closure 节）。

### 2026-08-07

- **Plan 2026-08-07-1023-2 flux-bundle 包边界与发布元数据完整性（18-01 + 18-02）执行（Phase 1/4 completed，Phase 2/3 blocked——方案 B 人工确认等待态，plan 保持 in progress；18-02 roadmap 行 ❌→✅，18-01 行收口中）**：
  - **Phase 1 Proof-1（built-dist 浏览器 harness 落地，基线绿 2/2）**：新增 `tests/e2e/flux-bundle-built-dist.spec.ts` + `tests/e2e/flux-bundle-built-dist/{index.html,main.tsx}`——consumer 式 vite build（无 workspace src alias，`@nop-chaos/flux` 直指 `packages/flux-bundle/dist/index.js` 产物；peer-external 由 harness build 从 flux-bundle node_modules 解析，宿主等价），静态 http server 加载；断言 6 族 13 代表 renderer 可注册 + page/container/text + form/editor（tiptap）真实渲染零错误；spec beforeAll 先 `pnpm --filter @nop-chaos/flux build` 保证被测产物新鲜。基线（alias+shim+fork 内联）2/2 绿。
  - **Phase 1 Proof-2（方案 A 探针证伪，复现 commit 2aebc5f3 失效模式）**：临时移除 `vite.config.ts` use-sync-external-store alias + flux-react 5 处消费点临时改 npm shim → build 成功（rolldown `__commonJSMin` 插桩 CJS 入 dist）→ harness **红**：`PAGEERROR: Calling require for "react" in an environment that doesn't expose the require function`（模块级崩溃）。产物实证 dist 内 `with-selector.production.js` region `var React$2 = __require("react")`；rolldown 文档确认非 node platform external `require` 原样保留（仅 `inject` hack 可桥接，plan 外不采）。tiptap 单一路径即成立（`use-sync-external-store/shim/index.js` 模块初始化即 `__require("react")`）。
  - **Phase 1 Decision（终裁）**：方案 A(a)（npm shim 直入发布 bundle）**证伪** → 方案 A(b) → **方案 B 形态**：fork 保留（5 消费点不改）、shim WithSelector 改 bare `@nop-chaos/flux-react`、alias 保留（tiptap CJS 拦截 load-bearing 实证）；fork 提升公共导出 = 结构性变更（公共 API）→ **按 roadmap Rule 人工确认门**，plan 保持 in progress；Phase 2/3/5 18-01 相关项 blocked 待恢复。终裁路径验证：build + harness 2/2 + `check:flux-bundle-pack` 全绿（基线即方案 B bundle 形态）。
  - **Phase 4（18-02 发布面修正）**：`flux-bundle/package.json` description 更新为 6 族 + mobile/scheduling/ai/graph 按需注册（live 核对 `registerDefaultFluxRenderers` 6 族、flux-bundle/src 零 mobile/scheduling/ai/graph 引用）；README 补注册面说明；**附带修复 `scripts/pack-flux-bundle.mjs` releaseManifest 缺 `description` 字段**（打包 tarball package.json 现含 description 实况）；`check:flux-bundle-pack` 绿 + tarball 内 README/description 实证。
  - **Phase 5 部分（决策无关项）**：roadmap 18-02 行 ❌→✅（18-01 行注明收口中/人工门等待态，保持 `[ ]`）；`pc-index.md:371` 修正——`check:workspace-manifest-deps` 现 exit 0（0529-1 已清零，不再误标 pre-existing red，跨包相对导入规则随 Phase 3 落地后补登记）；本 daily log 条目。
  - **阻塞声明**：Phase 2（18-01 代码落地）/Phase 3（门禁规则——落地即命中 shim 跨包导入致全仓 check 红，exit「全仓零命中」以 18-01 修复为前提）/Phase 5 18-01 项 blocked：fork 公共导出需人工确认（方案 B）。恢复路径：人工确认 → Phase 2 → Phase 3 → Phase 5 收口 → closure-audit。

### 2026-08-07

- **Plan 2026-08-07-1023-1 `check:audit-renderer-browser-io` 门禁覆盖补全（O-P2-2）执行完毕（3 Phase 全 completed；roadmap O-P2-2 行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 Proof（作用域测试先红）**：新增 committed 测试 `scripts/__tests__/find-renderer-browser-io.test.ts`（execFile 实跑脚本，对齐 `check-active-doc-code-anchors.test.ts` 先例）+ fixture `scripts/__tests__/fixtures/inv1-browser-io/`（命名避开 `isTestFile` 过滤）；测试内临时 copy 到扫描根真实包目录 `packages/<pkg>/__inv1_scan_fixture__/` 执行（afterEach 清理）。首跑 2 fail（作用域正样本 + 远程 import 正样本——当前 `:122` 正则不覆盖 flow-designer-renderers、RULES 无 import 规则），负样本 2 用例绿。
  - **Phase 1 Fix（作用域放宽 14 包）**：`scripts/audit/find-renderer-browser-io.mjs:122` 正则放宽为 `/^packages\/(flux-renderers-|flow-designer-renderers|spreadsheet-renderers|report-designer-renderers|word-editor-renderers)\//`（保持 isTestFile 过滤）；作用域正/负样本用例转绿；全仓重跑零命中；**4 个新覆盖包正样本实证**（临时 `__inv1_empirical__/inv1-scope-probe.ts` 各放 `fetch('/api/x')` → 门禁 4 命中 exit 1 → 删除后归零）；`pc-index.md:362` 作用域描述更新为 14 包 + `import()` 规则 + committed 测试登记。
  - **Phase 2 Proof（import() 测试先红）**：`remote-dynamic-import-fixture.ts`（`await import('https://cdn.example.com/mod.js')`）先红；`import-negatives-fixture.ts`（type import `import('@nop-chaos/flux-core').X` / `import('./local.js').X` / 本地懒加载 `await import('html2canvas')` / `await import('@zxing/library')` / 字符串内 `"await import('https://x')"` / 注释内 `// import('https://x')`）断言不命中。
  - **Phase 2 Fix（`renderer-remote-dynamic-import` 规则）**：RULES 增补第 10 条规则，形态裁定写死于 plan——只匹配「参数为 URL 字符串字面量（http(s)://、//、data:、blob:）」的动态 import，正则 `\bimport\s*\(\s*(?:['"`](?:https?:)?\/\/|['"`]data:|['"`]blob:)`，配合 `isCodePosition` 排除字符串/注释出现；type import 与本地包懒加载不命中；message 对齐 INV-1「动态加载远程模块」口径（RendererEnv.importLoader）。转绿后全仓重跑零命中——现有 60+ 处 type import / 15 处本地懒加载（2 包名 + 13 相对路径）零误报实证。
  - **Phase 3 文档同步**：roadmap Follow-up Backlog `O-P2-2` 行 ❌→✅ 附收口注记（plan 路径 + 交付面）；本 daily log 条目。
  - **附带修复（pre-existing 红）**：`scripts/__tests__/check-package-css-exports.test.ts` 计数断言过期（脚本实报 17 目标、断言仍写 11）——clean HEAD 复现确认 pre-existing，为满足 Phase 2 exit「`pnpm test:scripts` 绿」修正断言为 17（保留测试语义）；`pnpm test:scripts` 3 files/6 tests 全绿。focused 复验：`check:audit-renderer-browser-io` exit 0 + `check:workspace-manifest-deps` exit 0。
  - **验证**：`pnpm test:scripts` 全绿；`pnpm check:audit-renderer-browser-io` 全仓零命中；全量 `pnpm typecheck`/`pnpm build`/`pnpm lint`/`pnpm test`/`pnpm check` 在 Closure Gates 收口时复跑。

### 2026-08-07

- **Plan 2026-08-07-0819-3 渲染器样式契约族修复（10-01/10-02/10-03 三条 P2）执行完毕（4 Phase 全 completed；roadmap 三行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（10-01）spreadsheet 包 CSS 去 playground 锚定**：`canvas-styles.css:349,363,371,381,391,400,409` 7 处 `.report-designer-demo` 变体经 live 复核为与 `[data-slot='spreadsheet-default-toolbar']` 锚定规则**同声明体的第二锚点**（demo 页直接挂载 toolbar 无 default-toolbar wrapper，变体是 demo 样式唯一来源）——并入独立 `[data-slot='spreadsheet-toolbar']`（及 toolbar-group/separator/status/cell-address/frozen-badge/`[data-toolbar-active]`）裸选择器，声明体不变；包 CSS 零 `.report-designer-demo`（rg 零命中）；playground `styles.css` 经复核无差异可下沉（`.report-designer-demo` 类为 demo 页自身布局，非包 CSS 锚定对象，保留）；**demo 视觉保持程序化断言**：report-designer-demo e2e toolbar 用例扩展计算样式断言（display flex / 背景色非透明 / border-bottom 1px）——9/9 绿。
  - **Phase 2（10-02）rd-toolbar 死类名清理 + e2e 基准更新**：`spreadsheet-toolbar.tsx:11`（rd-toolbar + rd-toolbar--single-row）、`toolbar-status.tsx`（3 处）、`toolbar-groups.tsx`（separator + 11 组 div，13 处）共 16 处 `rd-*` 全删仅留 data-slot（DOM 契约不变，全仓无 `rd-*` CSS 定义）；**playground 单测断言同步**：`report-designer-demo.test.tsx:43,50` `.rd-toolbar` + `className` 包含断言改 `[data-slot='spreadsheet-toolbar']` 定位 + data-slot 存在性 + 子槽位（group/status）可观测断言；e2e 定位基准更新：`report-designer-demo.spec.ts:25,168,186` 与 `subagent-a-independent-review.spec.ts:414,417`（该 describe 为既有 `test.describe.skip`，定位同步但不参与回归）改 `[data-slot='spreadsheet-toolbar']`；`rg "rd-toolbar|rd-toolbar--" packages/ tests/e2e/ apps/` 零命中，`rd-` 仅剩 word-editor/wizard 等非 BEM 前缀命中逐一核对；report-designer-demo e2e 9/9 全绿。
  - **Phase 3（10-03）diff-view token 化**：`diff-view.css` 补齐家族 token——`--nop-diff-stat-modified-text`（modified 对应色）+ `--nop-diff-stat-{added,removed,modified}-bg`（状态徽标背景，oklch 值与现 hex 视觉等价）+ 文件列表家族 token（`--nop-diff-active-bg`/`--nop-diff-hover-bg`/`--nop-diff-muted-text`/`--nop-diff-accent`，替代非家族 `--nop-bg-active`/`--nop-bg-hover`/`--nop-text`/`--nop-accent` 与裸 hex）；`diff-file-list.tsx` 10 处硬编码色全改 `var(--nop-diff-*)`（状态徽标 fg/bg、+/- 计数、tab 激活态、hover/active 背景、未访问圆点、空列表文案）；`rg "#16a34a|#dc2626|#ca8a04|#dcfce7|#fef2f2|#fefce8|#999|#1677ff" .../components/` 零命中（fallback hex 仅留 diff-view.css token 定义处）；content 286 tests 绿。
  - **验证**：spreadsheet-renderers 140 tests、flux-renderers-content 286 tests、playground 143 tests 全绿（含 canvas-styles/既有 diff 测试回归）；`check:package-css-exports` exit 0；`pnpm typecheck` 32/32 绿；report-designer-demo e2e 9/9 绿（含新计算样式断言）；全量 `pnpm build`/`pnpm lint`/`pnpm test`/`pnpm check` 在 Closure Gates 收口时复跑。
  - **文档**：plan 4 Phase 全勾选 + Closure Gates（audit gate 由独立 fresh session 执行，证据见 plan Closure 节）；roadmap 10-01/10-02/10-03 三行 ❌→✅ 附收口注记；styling-system/theme-compatibility 契约未变经核实无需同步（10-03 文件列表 token 归属已在 diff-view.css 文件头 token 家族注记）；本 daily log 条目。

### 2026-08-07

- **Plan 2026-08-07-0819-2 variant-field/FieldFrame 家族修复（12-01/12-02/12-03 三条 P2）执行完毕（2 Phase 全 completed；roadmap 三行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 Proof（region label 测试先红）**：`variant-field-owner-contract.test.tsx` 新增 region label 用例（`props.label` 构造为 region key 占位串 `'label'` + `regions.label` render handle 手工模拟编译产物——单测不跑编译器，对应 `node-compiler-helpers.ts:270` 的 `item[fieldKey] = regionKey` 行为）+ 值 label 回归用例；同时按「green 可观测」更新 harness mock：`FieldFrame` mock 改渲染 `label`（原只渲染 children）、`resolveRendererSlotContent`/`resolveFieldLabelContent` mock 委托 regions→props 解析链（原均返回 undefined）。首跑 region label 用例红（断言找不到 `自定义`，失败根因为 label 解析通道缺失而非 mock 链返回 undefined），值 label 用例 + 既有 5 用例绿。
  - **Phase 2 修复（12-01/12-02/12-03）**：12-01 按 hint/description 先例 prop-threading——`variant-field.tsx` 用 `resolveFieldLabelContent(props)`（region/值双形式）解析后经新增 `VariantFieldViewProps.labelContent` 下传，`variant-field-view.tsx` `FieldFrame label={labelContent}`（原 `schemaProps.label` 只读值通道，region 形式被编译器替换为 region key 占位串静默丢失）；region label 用例转绿，form-advanced 1049 tests 绿。12-02 `scripts/audit/rules.mjs` allowlist 登记路径 `variant-field.tsx` → `variant-field-view.tsx`（实际 FieldFrame 使用位于 view），`pnpm check:audit-fieldframe-bypasses` 零候选命中 exit 0（原 3 处候选消失）。12-03 `docs/architecture/field-frame.md` 接口列表补 `renderer?: string` + Key props 表行（`data-renderer` debugger 锚点，`node-frame-wrapper.tsx:62` 实传 `templateNode.rendererType`），与 `field-frame.tsx:47` 一致。
  - **文档**：owner doc `docs/architecture/variant-field.md` 补 label value-or-region 解析注记（`resolveFieldLabelContent` → `labelContent` 下传链路，12-01 收口说明）；roadmap Follow-up Backlog 12-01/12-02/12-03 三行 ❌→✅ 附收口注记；本 daily log 条目。
  - **验证（full-green 单测/静态链）**：form-advanced 包 1049/1049 绿（含新 region label + 值 label 用例）；全量 `pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（FULL TURBO）；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 12 既有 pre-existing 命名清单零新增（variant-field-owner-contract.test.tsx 429→571 行进入 >500 WARN 区不超 700 硬限，未登记不新增红），其余 11 项逐项 exit 0（含 audit-event-dispatch-ctx 零命中、audit-suspects 431 登记基线）；e2e 未复跑（本 plan 单测聚焦验证，Closure Gates 不要求，08-06 CV watch-only 归因清单仍挂账）。

### 2026-08-07

- **Plan 2026-08-07-0819-1 flow-designer/graph 域 successor 承接与修复执行完毕（5 Phase 全 completed；verdicts 5 项 successor 收口（4 fixed + 1 adjudicated）；roadmap O-P2-1/13-01 两行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 Proof（5 组 focused 测试先红）**：15-2 core（`core-ui-state.test.ts` 2 条 NaN fail-closed 先红——当前 `Math.max(NaN,min)===NaN` 后 emit）、15-2 action-provider（新 `designer-action-provider.test.ts` 3 条契约测试，NaN 请求 `{ok:false, reason:'invalid-width'}` 先红）、13-01（新 `designer-xyflow-node.keyboard.test.tsx` 3 条——Enter/Space 键盘坐标有限且等于槽位元素中心，先红于 `clientX` undefined）、19-3（新 `designer-page-json-export.test.tsx` 2 条——parse 失败 `reportHostIssue` 未调用先红 + 正常路径行为保持断言）、17-2（`graph-renderer.test.tsx` 3 条 payload type 命名空间契约先红——当前为事件字段名）；3 包 typecheck 全绿。
  - **Phase 2 flow-designer-core（15-2 + 15-1）**：`shell-controls.ts` `clampShellWidth` 加 `Number.isFinite` fail-closed 守卫（非有限数返回当前已存宽度、不改变状态不 emit，`setPaletteWidth`/`setInspectorWidth` 经守卫自然拒绝）；`shell-state.ts` `resolveShellWidth` 同守卫（配置 NaN 兜底默认宽）；`designer-command-types.ts` `DesignerCommandReason` 增 `invalid-width`；`designer-command-adapter.ts:256-264` 校验升级 `Number.isFinite` + `createFailure(..., 'invalid-width')`；`designer-action-provider.ts` payload 校验前拦截 NaN 返回 `mapCoreResult({ok:false, reason:'invalid-width'})` + `:450/:451` typeof 校验升级 Number.isFinite。**15-1 裁决（Decision）：watch-only residual**——`version` 为 semver 字符串非修订计数（`normalizeTreeDocumentVersion` 只归一化、relayout/命令路径不 bump），浅引用/长度比较无法检测坐标漂移 → 计数比较不可等价；变更检测语义正确且被既有测试锁定，非热路径（用户触发 relayout 非渲染循环）不阻塞。新增双分支等价测试 3 条（`tree-session.test.ts` 15-1 describe：no-op 同步对不 emit / changed 失同步对 emit presentationChanged 并恢复投影坐标 / version 不变仍 emit 实证计数比较不可等价），flow-designer-core 175→180 tests 绿。
  - **Phase 3 flow-designer-renderers（13-01 + 19-3）**：`designer-xyflow-node.tsx` onKeyDown 不再 `KeyboardEvent as unknown as MouseEvent`，拆 `openSlotMenuFromElement`（槽位元素 `getBoundingClientRect` 中心点定位菜单），鼠标路径 `handleSlotAffordanceClick` 行为不变；`designer-page-body.tsx` JSON 导出 parse 改 `jsonResult` 判别联合 memo（纯推导）+ 失败经 useEffect 走 `reportHostIssue`（reason `designer-json-export-parse-failed`）+ dialog 内 `data-slot="designer-json-panel-error"` role="alert" 错误文案（新增 i18n key `flux.flowDesigner.flowJsonParseError` en/zh），不再静默空内容；正常路径 DataViewer 行为不变；test-first 5 条先红后绿，flow-designer-renderers 235 tests 绿（含既有 xyflow/canvas-bridge 回归）。
  - **Phase 4 graph（17-2）**：`graph-renderer.tsx` `GRAPH_EVENT_PAYLOAD_TYPES` 映射（`graph:node-click`/`graph:node-double-click`/`graph:selection-change`），`fireNodeEvent` payload `type` 命名空间化（事件键名 `onNodeClick` 等作 `props.events` 索引不变，与 `normalizeActionEvent` 字符串 type 契约对齐）；`graph-definitions.ts` eventContracts 三事件 payload 补 `type` 字面量字段；`docs/components/graph/design.md` §8.1 payload 表补 type 字段 + 命名空间注记（§2.1-4 引用同步）；既有 ctx 契约断言更新；graph 48 tests 绿。
  - **Phase 5 14-4 + 台账收口**：`canvas-bridge.test.tsx` mock/渲染辅助区提取为 `canvas-bridge-test-support.tsx`（227 行：@xyflow/react mock、ResizeObserver、flux-react mock、designer-context mock、i18n hooks、`createSnapshot` 真实 fixture），`:91` 固定空快照 mock 改走真实 fixture `createSnapshot()`（非空文档快照），主文件 588→325 行、setup 区 36%→~18.5%（<40% 达标），8 个既有用例语义保持 + 新增 `bridgeProps`/`renderBridge`/`renderTaskNode` 渲染辅助；`multi-audit-r2-verdicts.md` 5 行 successor 终态回写（14-4/15-2/17-2/19-3 → fixed + plan 引用；15-1 → adjudicated watch-only residual）+ 终态核对节更新；roadmap `O-P2-1`/`13-01` 两行 ❌→✅ 附收口注记；本 daily log 条目。
  - **验证（full-green 单测/静态链）**：flow-designer-core 180 tests、flow-designer-renderers 235 tests、flux-renderers-graph 48 tests 全绿；全量 `pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（11,xxx passed / 0 failed）；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 12 既有 pre-existing 命名清单零新增（en-US/zh-CN +1 行为已登记超限文件内 i18n key 增量），其余 11 项逐项 exit 0（audit-suspects 431 命中为登记扫描基线含新 catch 点、event-dispatch-ctx 零命中）；e2e 未复跑（本 plan 触及路径 focused 验证 + 既有 08-06 CV watch-only 归因清单挂账，Closure Gates 不要求 e2e 全量）。
  - **文档**：plan 5 Phase 全勾选 + Deferred But Adjudicated 15-1 裁决记录 + Closure Gates（audit gate 由独立 fresh session 执行，证据见 plan Closure 节）；verdicts/roadmap/design.md 同步；本 daily log 条目。

### 2026-08-07

- **Plan 2026-08-07-0421-3 文件结构治理与所有权文档同步（02-01/02-02/02-03/02-04）执行完毕（4 Phase 全 completed；roadmap 四行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1（02-04）diff-view/utils 合并**：`diff-stats.ts` + `diff-template.ts` 合并为 `packages/flux-renderers-content/src/diff-view/utils.ts`（62 行），7 处 import 更新（5 生产 + 2 测试），`utils/` 二级目录删除；content 包 typecheck 绿 + 286 tests 绿。
  - **Phase 2（02-01）crud-renderer-state.ts 拆分**：`useCrudLoadAction` + `CrudLoadActionResult` 及 load 专属依赖（343 行）迁至新 `crud-renderer-load.ts`，state 文件保留纯 state 规约函数落至 **477 行**（<500 目标达成，退出 >700 命中区）；`crud-renderer.tsx` 消费侧改从新文件导入，re-export 面零破坏（index.tsx 的 `createCrudNormalizedSourceContext` 不变）；data 包 typecheck 绿 + 753 tests 绿。
  - **Phase 3（02-02）table-renderer.tsx responsive 提取**：`resolveResponsiveBreakpoint`/`splitResponsiveColumns`/`useIsBelowResponsiveBreakpoint` + 消费逻辑收拢为 `useResponsiveExpandState` hook（122 行）迁至 `table-renderer/responsive.ts`（子目录 hook 先例对齐），主文件 **740→645 行**；`data-responsive-expand` attr、`expandAllByDefault`/`showExpandColumn`/`expandRowByClick` 接线原样保留，responsive 语义零变更；data 包 typecheck 绿 + 753 tests 绿（含 table-e1b/enhancements 等 responsive 测试原样通过）。
  - **Phase 4（02-03）flux-runtime-module-boundaries.md 补全**：补齐 **28 个 live 模块条目**（flux-runtime 根 13：form-store-owned/form-runtime-owner-validation-utils/form-store-diagnostics-bridge/page-store/surface-store/projected-scope-store/composite-field-handle/form-component-handle/input-component-handle/surface-component-handle/refresh-nearest/abort-signal-helpers + surface-hooks；async-data 3：request-in-flight-registry/blob-download/source-observer；schema-compiler 8：shape-validation-analyze/node-fields/predicates/traversal/flux-value-shape-validation/action-selector-validation/runtime-value-compilation/validation-compiler；flux-compiler 根 4：schema-compiler-helpers/reaction-compiler/source-compiler/validation-lowering），scope-change/runtime-plugins 散文改 bullet 条目；全量生产模块条目循环 **MISSING:NONE**（抽查口径 `ls packages/flux-runtime/src/*.ts` 逐项对照）；doc 33,396 B < 40 KB；anchors 零失效（`rg "flux-runtime-module-boundaries.md#" docs/` 零引用）。
  - **验证**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（data 包 `useCallback` 未用导入清理 1 处后全绿）、`pnpm test` 59/59 task 全绿（content 286 / data 753）；`pnpm check` 链仅 `check:oversized-code-files` 红——**12 文件**（较 14 既有 pre-existing 基线**减 2**：`crud-renderer-state.ts`/`table-renderer.tsx` 均退出 >700 命中区，零新增命名），其余 11 项 check 逐项全绿；e2e 未复跑（纯结构/文档治理，plan Closure Gates 不要求，08-06 CV watch-only 归因清单仍挂账）。
  - **文档**：plan 4 Phase 全勾选 + Closure Gates 全绿（audit gate 由独立 fresh session `ses_02673dda9ffeq5O426HztlMV6R` 执行——首轮发现 scope-change/runtime-plugins 缺 bullet 条目 1 项 Minor，修复后复检 approved，证据已写入 plan Closure 节）；roadmap 02-01/02-02/02-03/02-04 四行 ❌→✅ 附收口注记；本 daily log 条目。

### 2026-08-07

- **Plan 2026-08-07-0421-2 作用域订阅门控与状态同步治理（05-01/05-02/05-03/04-01，14 处 useScopeSelector 门控 + stableColumns 双镜像收敛）执行完毕（3 Phase 全 completed；roadmap 04-01/05-01/05-02/05-03 四行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 scheduling 族（05-01/05-02）**：`kanban-board.tsx:61,72` 两处 useScopeSelector 补 `enabled: <ownership> === 'scope' && !!statePath` + `paths: [statePath]` 收窄（board 订阅门控 kanbanOwnership、collapsed 订阅门控 collapsedOwnership——独立 ownership prop 不可混用）；`use-calendar-ownership.ts:21,29` 补 `enabled: isScopeView/isScopeDate`（scope + path 双条件）+ `paths` 收窄。先红后绿 2 新测试文件（`kanban-ownership-gating.test.tsx` 5 用例 + `use-calendar-ownership.test.ts` 6 用例，首跑 10 fail），spy 断言 options.enabled/paths 契约；controlled 模式行为不变断言。kanban/calendar design.md 三态文档核对与实现一致（无需同步）。
  - **Phase 2 data 控制 hooks 族（05-03）**：10 处 useScopeSelector 全数补 `enabled` 门控——pagination/selection/filter 按 `<ownership> === 'scope' && !!statePath`；sort 单/多模式分别门控（`:118` 门控 `!multiSortEnabled`、`:129` 门控 `multiSortEnabled`，消除单模式下的整 scope 死订阅）；visible-columns/crud 无独立 ownership prop 按路径存在性判定；column-resize 按 `effectiveOwnership === 'scope' && !!statePath`；list-pagination 按 `ownership === 'scope'`。**消费侧兼容修正**：`list-pagination.ts` equality 补 undefined 安全（enabled:false 时 fallback undefined 进 equality）；`crud-renderer-ownership.ts` `useCrudVisibleColumnNames` 补 `fallback: EMPTY_VISIBLE_COLUMN_STATE` 保持 selector 零值形状契约（真 hook 下 enabled:false 返回 fallback，直接解引用不再崩）。先红后绿 `scope-subscription-gating.test.tsx` 8 用例（首跑 8 fail，spy 模拟真 hook enabled/fallback 语义）；data 750/750 + typecheck 绿。
  - **Phase 3 table stableColumns（04-01）**：双镜像（`useState stableColumns` + `prevRawRef` + `useEffect` + `startTransition`）收敛为 **last-good render 期派生**——React 19「adjust state during render」模式（`lastGoodColumns`/`prevRawColumns` useState + 变更守卫条件 setState + 初始 lastGood 从 rawColumns 种子），移除 useEffect/startTransition 镜像与 stale-value 窗口（lint `react-hooks/refs` 禁止 render 期读写 ref，故弃 ref 方案）；无效格式到达仅 dev 警告（isDevRuntime），有效值立即应用。回归测试 `table-t29-dynamic-columns-last-good.test.tsx` 3 用例（createSchemaRenderer host harness + data rerender：表达式变更渲染新列、无效格式 last-good 保留 + dev 警告、初始无效回退空列）——04-01 在 Draft Review 已裁定现链无真回环（prevRawRef 防重入），故为行为保持守卫（改造前后全绿）；live 抽查经 host harness 覆盖动态 columns 运行时路径。
  - **验证（full-green）**：scheduling 76 files/875 tests、data 107 files/753 tests 全绿；`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` exit 0 全绿；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 14 既有 pre-existing 命名清单零新增（table-renderer.tsx 735→741 为已登记超限文件），其余 11 项逐项全绿；`check:audit-reactive-render-reads` 14 处修复点 broad-scope-selector 零命中（全量收窄 paths）。
  - **文档**：plan 3 Phase 全勾选 + Closure Gates（audit gate 由独立 fresh session 执行，verdict approved 0 Blocker/0 Major）；roadmap 04-01/05-01/05-02/05-03 四行 ❌→✅ 附修复注记；本 daily log 条目。
  - **verify-run 复核（mission-driver 2026-08-07，提交后同 HEAD 复跑）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（FULL TURBO）；e2e 全量两轮均 **1054 passed / 43 skipped / 6 failed**——6 项失败与 08-06 CV watch-only 归因清单逐项一致（Tiptap 批次 c3-5-host-surfaces:27/:81 + w3d-editor:28，已知 input-race flake——c3-5:27 隔离复跑绿、w3d:28 隔离复跑 2/3 绿；gantt-perf:21/:44 + kanban-perf:34，本机 50Hz 主屏 rAF 上限 50fps 实测 avg=50.0，阈值不可达），clean HEAD 同值复现（HEAD 无任何后续改动）零新增失败，本 plan 触及包（scheduling/data）e2e 全绿——**非 full-green 状态，不作 full-green 声明**。

### 2026-08-07

- **Plan 2026-08-07-0421-1 渲染器 a11y 键盘与 ARIA 语义契约族修复（11-01/11-02/20-01..20-10，12 条 P2）执行完毕（4 Phase 全 completed；roadmap 12 行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 data 族（20-01/20-02/20-08/20-10）**：`use-column-resize.ts` 新增 `stepResize(column, index, delta)` 键盘步进 API（min/max 钳制 + 与 pointer drag 同 commit 路径，镜像 use-row-drag-sort H6 先例）；`table-header-row.tsx` resize handle（`role="separator"`）补 `tabIndex={0}` + ArrowLeft/ArrowRight 10px 步进；`crud-infinite-scroll-area.tsx` 状态文本容器补 `role="status"` + `aria-live="polite"`（对照 mobile infinite-scroll 先例）；`list-renderer.tsx` listitem 移除 aria-selected（ARIA 1.2 不支持），选中项改 `aria-current="true"`；`crud-renderer-toolbar.tsx` PaginationPrevious 首页与 Next 对称输出 `aria-disabled` + onClick 首页边界 return。先红后绿 4 新测试文件（`table-column-resize-keyboard`/`crud-infinite-scroll-live-region`/`list-selection-aria`/`crud-toolbar-pagination-a11y`，首跑 10 fail）+ data-list-rendering 2 处旧断言改新契约；data 742/742 + typecheck 绿；table/crud design.md 同步。
  - **Phase 2 layout/mobile 族（20-03/20-04/20-09）**：`notice-bar.tsx` hover/focus 暂停 JS 轮播 tick（setTimeout 不调度）+ marquee `animation-play-state: paused`，prefers-reduced-motion 经 matchMedia change 监听响应式停用轮播（含 JS tick 层，不只 CSS——审计 20-03 表述修正）；pause 监听经 effect + ref 挂载（carousel.tsx 先例，规避 no-static-element-interactions lint）；`steps-renderer.tsx` 指示按钮补 `aria-label` = `${t('flux.steps.step')} ${index+1}: ${title}`（新增 i18n key `flux.steps.step` en/zh）；`wizard-renderer.tsx` 步骤切换后（初始 mount 除外）焦点移入新步骤体首个可聚焦元素 + `role="status"`/`aria-live="polite"` sr-only 播报当前步骤标题。先红后绿 3 新测试（`notice-bar-pause-a11y`/`steps-a11y`/`wizard-a11y`，首跑 6 fail）；mobile 174/174 + layout 109/109 + flux-i18n typecheck 绿；notice-bar/steps/wizard design.md 同步。
  - **Phase 3 form/form-advanced 族（20-05/20-07）**：`transfer-renderer.tsx` 移除 `role="listbox"`/`aria-multiselectable`（审计建议最小形态，Checkbox 保留勾选语义）；`markdown-editor-renderer.tsx` **Decision（20-07）：去 `role="toolbar"` 角色退化形态**——12 按钮（live 核对非审计所述 15）独立 tab stop 全键盘可达（无 2.1.1 失败），含 3 组分隔符布局 roving tabindex 收益低；同步删除孤立 i18n key `markdown.toolbarLabel`（en/zh）。先红后绿 2 新测试 + 旧断言更新 4 处（transfer-renderer 2 条 listbox 断言、markdown-editor-i18n 2 条 toolbar role 断言）；form 736/736 + form-advanced 1047/1047 + typecheck 绿；transfer/markdown-editor design.md 同步（含 Decision 记录）。
  - **Phase 4 ai/content 族（11-01/11-02/20-06）**：`ai-attachments.tsx` 容器去 `role="button"`/`tabIndex`/`aria-label`/`onKeyDown`（拖放面不再伪装按钮、不再劫持内部 Pick Button 键盘激活；lint 要求拖放面带 role，改 `role="region"` 对齐 report-designer 先例），内部 Button 为键盘入口；图片模式移除按钮补 `focus-visible:opacity-100`；`diff-file-list.tsx` 原生 input 换 ui `<Input className="h-8 w-full text-xs" />` 删手写内联样式（type 改 search）。先红后绿 2 新测试（`ai-attachments-a11y`/`diff-file-list-a11y`，首跑 4 fail）；ai 512/512 + content 286/286 + typecheck 绿。
  - **验证（full-green）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（notice-bar/ai-attachments 两处 no-static-element-interactions 经 effect+ref / role="region" 修正后全绿）、`pnpm test` 59/59 task 全绿；e2e 全量 1054 passed / 43 skipped / 0 failed（w1c-list aria-selected 旧断言改 aria-current 后绿）；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 14 既有 pre-existing 命名清单零新增（en-US.ts 1017→1020、zh-CN.ts 1016→1019 为登记超限文件内 +3 行 steps key），其余 11 项逐项全绿（含 audit-event-dispatch-ctx——notice-bar 3 条 allowlist 行号随 20-03 修复位移，已同步 160/166/172 → 198/204/210）。
  - **文档**：plan 4 Phase 全勾选 + Deferred But Adjudicated 20-07 Decision 记录 + Closure Gates（audit gate 由独立 fresh session 执行）；roadmap 11-01/11-02/20-01..20-10 十二行 ❌→✅；七份 design.md（table/crud/notice-bar/steps/wizard/transfer/markdown-editor）a11y 契约同步；本 daily log 条目。

### 2026-08-07

- **Plan 2026-08-06-2306-3 scheduling/graph 接线与 phantom 契约裁决（22-04/22-05/22-06/22-07/22-08/22-10/22-12）执行完毕（5 Phase 全 completed；7 条发现全数裁决落地；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 裁决 + 测试先红（Proof）**：7 条裁决表写入 plan（22-04/22-05/22-06/22-07/22-10/22-12 → 实现；22-08 → design.md @reserved 文档降级）；16 条 focused 测试全部先红（kanban onColumnAdd controlled 不派发、kanban component:\* 七句柄注册可解析（新 `kanban-handle.test.tsx` 7 用例）、calendar print/exportPNG 句柄 invoke 派发、graph layout prop 运行时同步、gantt onTaskEdit 三路径（editor 保存 ×2 / grid 行内提交 / 键盘 Delete）派发、gantt 配置 prop 运行时同步（cellWidth/taskBarHeight/zoomLevels ×3））。
  - **Phase 2 kanban（22-04 + 22-12 + 22-08）**：`kanban-board.tsx` onColumnAdd 补 `if (!isControlled)` 守卫；22-12 句柄实现抽离为 `kanban-handle.ts`（`registerKanbanHandle` + `KANBAN_HANDLE_METHODS` 七方法 + `KanbanHandleSurface` 操作面接口），`kanban-board.tsx` 每次渲染刷新 `kanbanSurfaceRef` 镜像（calendar navRef 模式）后经 `useCurrentComponentRegistry` 注册（gantt/calendar 同模式，且避免 kanban-board 超 700 行红线）；handle 驱动与 UI 驱动共用同一 mutation 通道（`handleCardAddAt`/`handleCardMoveViaHandle`/`handleCollapseColumn`，undo + 事件 + controlled 拒绝语义）；`scheduling-renderer-definitions.ts` kanban 条目补 7 条 componentCapabilityContracts；22-08 按裁决 design.md columnsOrder\* 三处标注 @reserved；`docs/components/kanban/example.json:40` toolbar `component:addCard` 补 `componentId` 恢复可运行；两处既有 test mock 补 `useCurrentComponentRegistry: () => undefined`。
  - **Phase 3 calendar + graph（22-05 + 22-06）**：`calendar.tsx` 句柄 invoke 路径补 `reactionsRef.current.exportPNG/print?.dispatch()`（对齐 gantt.tsx:417-419 先例；importICal/exportToICal 维持 @reserved 注释澄清）；`graph-renderer.tsx` 补 layout prop → store 单向同步 effect（值不等才 `setLayoutMode`，避免与 `component:setLayout` 双向覆盖）。
  - **Phase 4 gantt（22-07 + 22-10）**：`gantt-store.ts` 补 `taskBarHeight`/`zoomLevels` setter；`gantt.tsx` re-seed effect 扩展配置字段同步（cellWidth/taskBarHeight/zoomLevels，变更后 recalcLayout 使渲染订阅生效）；新增 onTaskEdit schema 事件（`gantt.types.ts` 契约 + definitions 注册 + gantt-editor `onCommit` / gantt-grid `onCellCommit` 回调 prop + gantt.tsx `dispatchTaskEdit` 统一派发，键盘 Delete 带 `deleted: true` 标记，全量 ctx 对齐 CX-12）。
  - **Phase 5 文档收口**：kanban/gantt/calendar/graph 四份 design.md 契约描述与 live 对齐（kanban §4.2/§5/§7/§8/§11.2 columnsOrder @reserved + 七句柄已注册注记；gantt §5/§8.1 onTaskEdit 契约；calendar design-export.md 派发接线注记；graph §7 layout 双向收敛说明）；per-component kanban 卡 dim 17 留痕更新；全仓 grep 复验 `columnsOrder` packages/src 零命中（仅 design.md @reserved 语境）、`component:addCard` 有注册可解析。
  - **验证（full-green）**：scheduling 包 864/864 单测绿（含 16 条新用例先红后绿）、graph 包 45/45 绿；全量 `pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 task 全绿（FULL TURBO）；`pnpm check` 链仅 `check:oversized-code-files` 红——恰为 14 既有 pre-existing 命名清单零新增，其余 10 项（react19/src-artifacts/active-doc-code-anchors/package-css-exports/flux-bundle-pack/i18n-keys/workspace-manifest-deps/schema-prop-coverage/audit-suspects/audit-renderer-browser-io/audit-event-dispatch-ctx）逐项全绿。barcode-input 单测 1 条环境性 flake（clean HEAD 同值复现，与本 plan 无关）。
  - **文档**：plan 5 Phase 全勾选 + 裁决表；roadmap 22-04/22-05/22-06/22-07/22-08/22-10/22-12 七行 ❌→✅；审计文档 7 条 finding 回写 fixed；本 daily log 条目；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。

### 2026-08-07

- **Plan 2026-08-06-2306-2 createScope/disposeScope 配对纪律（09-01 + 09-02，10 处一次性求值 scope 泄漏）执行完毕（4 Phase 全 completed；roadmap 09-01/09-02 两行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 测试先红（Proof）**：10 处泄漏点全数落地 focused 测试——hook 级 spy（use-table-selection checkableWhen 行判定、use-table-lazy-children、use-select-remote-search）、纯函数单测（variant-field-matching、executeTreeSource、createTableEventContext）、组件级 runtime spy（loop/recurse itemData、condition-builder 公式 source、picker autoFill，经 `onRuntimeChange` 捕获 runtime 后 `vi.spyOn(createChildScope/disposeScope)`）；13 条用例全部先红（dispose 未调用/不必要 createScope 断言失败）后经修复转绿。**修复通道分型 Decision**：render 期同步求值点 7 处（use-table-selection/loop×2/recurse/variant-field/condition-builder/picker）→ 求值后 try/finally 配对 disposeScope；dispatch 点 3 处（use-table-lazy-children/tree-control-controllers/use-select-remote-search）scope 流入异步 action 且 host fetcher 读 `ctx.scope` → 保留 scope 于 promise settle 后 `.finally()` dispose（弃用 evaluationBindings 纯迁移，因 tree 测试实证 fetcher 读 scope 可见性契约）；table-event-context → 不建子 scope，改 `{ event, evaluationBindings, scope: rootScope }` 约定（kanban-board.tsx:148-152 先例）。
  - **Phase 2 render 期 4 处修复**：use-table-selection.ts checkableWhen 每行 scope try/finally 配对；loop.tsx 两处 evaluateItemData 闭包 + recurse.tsx 求值后 finally dispose；variant-field-matching.ts 扩展 `disposeScope` 注入参数（matchesVariant/detectMatchedVariant/resolveInitialVariant 透传），controller 传 `props.helpers.disposeScope`；data 731/731、basic 487/487 全绿。
  - **Phase 3 交互/事件路径 6 处修复**：use-table-lazy-children/use-select-remote-search/tree-control-controllers（公式路径同步 evaluate 后立即 dispose，action 路径 settle 后 finally dispose）；condition-builder 公式 source scope 求值后 finally dispose；picker-renderer autoFill row scope finally dispose；table-event-context 签名收敛为 `{ event, scope }`（去除 helpers/scopeKey/pathSuffix），9 个直接调用点 + pagination 包装 helper 3 处调用（pagination/filter/sort/selection 四 hook）改传 `renderScope`，事件 ctx 契约同步更新 3 个既有 hook 级测试断言；form-advanced 1046/1046、form 735/735 全绿。
  - **Phase 4 owner-doc 补强与收口**：`renderer-runtime.md` 新增「One-shot evaluation scope discipline (09-01/09-02)」节——一次性查询语义不得留 runtime-owned scope（ownedScopeDisposers 累积机制）+ evaluationBindings 优先 + 同步求值 finally dispose / 异步 dispatch settle 后 dispose 三条规则 + 配对先例清单；全仓 live grep 复验 10 点 createScope 全部有配对 dispose（table-event-context 零 createScope）；roadmap 09-01/09-02 ❌→✅；审计文档 09-01/09-02 条目回写「修复状态: fixed」。
  - **验证（full-green）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（scheduling 1 条预存在 warning）、`pnpm test` 59/59 task 全绿；`pnpm check` 链仅 `check:oversized-code-files` 红——14 既有 pre-existing 命名清单零新增（tree-control-controllers.ts 725→733、table-renderer.tsx 736→735 均为 HEAD 已超限登记文件），其余 10 项逐项全绿（含 audit-suspects/audit-renderer-browser-io/audit-event-dispatch-ctx exit 0）。
  - **文档**：plan 4 Phase 全勾选 + Closure Gates 勾选至「独立子 agent closure-audit」前一项（该 gate 由独立 fresh session 执行，证据见 plan Closure 节）；roadmap/审计/architecture/renderer-runtime.md 同步；本 daily log 条目。

- **Plan 2026-08-06-2306-1 事件派发 ctx 全量扫描（dialog/drawer surface + upload-field + graph + wizard + carousel/tabs type 命名空间）执行完毕（5 Phase 全 completed；roadmap O-P2-3/O-P2-4/22-09/22-11/09-03 五行 ❌→✅；closure-audit 由独立 fresh session 收口，证据见 plan Closure 节）**：
  - **Phase 1 机械扫描门禁与基线登记**：新增 `scripts/audit/find-event-dispatch-without-ctx.mjs`（`check:audit-event-dispatch-ctx`）——扫描 `packages/flux-renderers-*` 非测试源文件的 schema 事件派发点，三形态覆盖（直接 `props.events.xxx?.(`/`eventHandlers.xxx?.(`、动态索引 `props.events[type]` 经局部 handler 间接派发（graph fireNodeEvent）、可选链调用）；命中条件 = 第二参缺失或对象缺 `event`/`evaluationBindings` 键；零参通知事件与 `undefined` 首参 lifecycle 派发天然排除；`eventCtx(payload)`/`ctx` 变量形态判定合规。**live 基线 23 点** = 本 plan 19 点全数 + **4 点同根因扫描发现并入**（list-renderer.tsx:368 onSelectionChange、pagination-renderer.tsx:163,178,188 onChange/onPageSizeChange——与 22-09 graph 同型 payload 派发缺 ctx）；allowlist 7 条原生 DOM 转发（button/notice-bar/chart，C3.x 裁决）留档。
  - **Phase 2 basic surface 事件 ctx（5 点）**：`use-surface-renderer.ts` `eventCtx(payload)` helper（event 合成 `type: 'custom'`，evaluationBindings=payload，scope=node.scope）应用于 :176/:181/:224/:225/:226 五处派发；test-first 3 条契约用例（先红：URL `/confirm-` 空替换；后绿）实证 `${surfaceId}` 解析 + onConfirm/onClose 同 id 一致性 + drawer onClose；basic 485/485 全绿。
  - **Phase 3 upload-field 家族 ctx（7 点）**：`upload-field.tsx` `eventCtx` helper（event 保留 payload 既有 type）应用于 onUploadSuccess/onUploadError/onReject/onDelete/onDeleteSuccess/onDeleteFail 7 处；test-first 5 条契约用例（先红：`/success-`、`/error-`、`/reject-`、`/delete-` 空替换；后绿）实证 `${item.url}`/`${file.url}`/`${error}`/`${reason}` 解析；form-advanced 1038/1038 全绿。
  - **Phase 4 graph + wizard + 同根因并入（11 点）**：`graph-renderer.tsx:158` 补 `event: fullPayload, evaluationBindings: fullPayload`（动态索引形态被门禁显式覆盖）；`wizard-renderer.tsx` 6 点补全量 ctx；test-first graph 2 条（mock events 断言 ctx 三键 + `${nodeId}`）+ wizard 2 条（先红 `/commit-`、`/change-`、`/step-error-` 空替换；后绿）实证 `${currentStepKey}` 解析（onChange/onStepCommit/onComplete/onStepError 四类事件）；list/pagination 4 点同根因修复 + 2 条契约测试（`${selectionMode}`/`${currentPage}` 解析）；graph 44/44、layout 105/105、data 725/725 全绿。
  - **Phase 5 carousel/tabs type 命名空间 + 门禁收口**：`carousel.tsx` → `'carousel:change'`、`tabs.tsx createTabsChangePayload` → `'tabs:change'`；tabs 两处派发补 `scope: props.node.scope` 键；源文锁断言（carousel.test.tsx 正则）同步；carousel/tabs/wizard 三份 design.md 事件契约节同步命名空间 type + ctx 描述；**Decision：`check:audit-event-dispatch-ctx` 并入 `pnpm check` 聚合**（零容忍契约门禁 + `check:audit-renderer-browser-io` 并入先例；project-context.md 工具基线登记）；content 285/285、basic 485/485 全绿。
  - **验证（full-green）**：`pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32（scheduling 1 条预存在 warning）、`pnpm test` 59/59 task 全绿；`node scripts/audit/find-event-dispatch-without-ctx.mjs` **全仓零命中 exit 0**（allowlist 7 条原生 DOM 转发）；`pnpm check` 链仅 `check:oversized-code-files` 红——14 既有 pre-existing 命名清单（新增契约测试拆分出 `surface-event-ctx.test.tsx`/`wizard-event-ctx.test.tsx` 独立文件避免自增超限，baseline 14 零新增）其余全绿。
  - **文档**：plan 5 Phase 全勾选 + Deferred But Adjudicated 裁决表更新（4 点并入 + 零参/原生转发 allowlist + 脚本排除形态留档）；roadmap O-P2-3/O-P2-4/22-09/22-11/09-03 五行 ❌→✅；open/multi 两份审计文档 5 条 finding 补「修复状态: fixed」；carousel/tabs/wizard design.md 同步；project-context.md 工具基线登记 `check:audit-event-dispatch-ctx`；本 daily log 条目；closure-audit 由独立 fresh sub-agent 执行（证据见 plan Closure 节）。
