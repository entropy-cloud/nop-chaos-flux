# Development Log - 2026-08-29

### 2026-08-29

- **R2 全量 UI 一致性审查收口**（plan `docs/plans/2026-08-28-1701-1-r2-consistency-audit.md` 全 Phase 完成，closure audit 通过，roadmap R2 → `done`）。执行中断恢复：前次 session 中断于 round-05 G7 派发前，本 session 从断点续跑（round-05-g7 → round-06/07/08 递归收敛 → 独立复核 → 汇总 → closure）。
- **递归收敛**：8 轮发现 127→63→34→29→16→5→2→**0**（round-06 G4/G5/G7、round-07 G1/G3/G6、round-08 G2 零发现收敛，未触 10 轮上限）。全部轮次文件与台账落 `docs/analysis/ui-review/r2-audit/`。G7（R6）与 G1/G5/G6（R6/R7 复核轮）共 4 次子 agent 速率限制失败，均按 Failure Path audit-subagent 重试 1 次成功。
- **独立复核**：7 组 fresh-session 复核 agent（先 grep live code 自行判断、后对发现结论），终判 **273 保留 / 3 降级（G3 focus ring 三条 M→L：UA 默认 outline 在位）/ 0 驳回**；HIGH 17 条全维持。G3 组用 Playwright 独立探针复核 2 条 HIGH（`_tmp/r2-g3-review-probe*.mjs`）。终判严重度 H17 / M172 / L87。
- **产出**：owner doc `docs/analysis/ui-review/R2-consistency-audit.md` 终稿（P0 5 / P1 12 / P2 172 / P3 87 映射、共性族 10 类、给 R3 的 8 个同根因预授权修复批、dark token 抽查结论、ma5-ux 零交集对照、转 C2 候选 6 项）；`r2-audit/summary.md`（Quick Wins 12、Top 3、全量清单）；`r2-audit/review.md`（276 行终判表 + scope-conflict 裁定 4 条）。
- **dark token 抽查（R0 §3 遗留 / C2 G-I "R2 顺带"）**：4 调色板（classic/glass × light/dark）逐变量结构完全对称（4/4 块 × 56 变量），抽样全为真暗色；复杂页暗色 2/4 合格（sundial 两张复刻页为自述 light-only，非回归）。探针 `_tmp/dark-token-symmetry-inspect.mjs`。结论已回写 `C2-capability-gaps.md` §3（首个回写"回写 ①"，授权链注明）。
- **closure audit**：首轮 `ses_fb61dd754ffe6f6GDqqo5XgTba`（A–H/J pass，I fail——summary.md 占位符残留 + 2 处 UTF-8 截断，系 awk 字节级 substr 截断多字节字符所致）；修复（python3 码点安全重建 + 占位符原位替换）后 focused 复审 `ses_fb615a240ffeK6hio7bQeyL5ki` 全 pass。教训：**生成中文 markdown 表格禁止字节级截断（awk substr），用码点感知处理**。
- **验证状态（本日，纯文档变更）**：改动面仅 docs/（plan/roadmap/owner doc/C2/r2-audit 41 个 .md，`git status --porcelain` 核对）+ `_tmp/` 探针；无代码变更，plan Test Strategy = 不适用（纯文档档）。`pnpm test` 全量：64 包成功，仅存 08-28 已登记的两处既有红（focused 复验：`flux-renderers-form` 809/813、4 红均 `input-date-relative-wiring` date 族已知 bug；`nop-debugger` 123/126、3 红均 network 断言期望 `status: 200` 的既有漂移），与本日变更无关，不声明 full-green。
- **验证状态（build verify 步跟进：两处既有红已修复，`pnpm test` 68/68 任务全绿）**：① `nop-debugger` 3 红 = ApiResponse 契约迁移（ok 移除、success = `status === 0`，01770f770/db49efebf）遗漏的测试断言漂移——`adapters/index/controller-helpers` 三测试断言对齐新契约，并移除 `NopDebugEventNetworkSummary` 死字段 `ok?`，126/126；② `flux-renderers-form` 4 红根因是测试定位歧义而非渲染器缺陷：2026-08 网格渲染 7 月尾随日（Jul 26–31），`dayButton()` 按文本 find "29" 命中上月尾随日——改为过滤 `closest('[data-outside]')` 后 813/813。react-day-picker v9 matcher 为日历日粒度（`differenceInCalendarDays`），`minDate:'now'` 当日可选、`maxDate:'now-1d'` 当日禁用，渲染器行为正确。e2e 未运行（mission 命令集无 e2e 项），故不声明 unit+e2e full-green；typecheck/build/lint/check 全绿。
- Next step: R3（一致性 P0/P1 修复与共性归类收口）按 roadmap 顺序启动，入口 `R2-consistency-audit.md` §给 R3 的输入清单；修复前先读 `r2-audit/review-g1..g7.md` 的 9 处证据精度修正。
- **R3 P0/P1 修复与共性归类收口执行**（plan `docs/plans/2026-08-29-0419-1-r3-consistency-p0p1-remediation.md` Phase 1–4 完成）。修复前按 owner doc 义务先读 `r2-audit/review-g1..g7.md` 的 9 处证据精度修正，全部批次 test-first 先红后绿。
- **Phase 1（P0 ×5）**: ② `ui` 新增 `useDirtyCloseGuard` 原语 + detail-view/detail-field 三关闭通道（X/ESC/遮罩）脏态确认（i18n 四键 en/zh）；④ VirtualBody 补 RadioGroup 受控包裹 + autoFill×virtual ref 互斥路由拆双绑；⑥ 会话删除 destructive 确认层；⑦ link 渲染器 `download` 透传 + `crud-views-export.json` 补 `"download": true`。
- **Phase 2（P1 ×12 + P2 随批 ×3）**: ① input-time steppers 双层门禁 / period 快捷钮门禁 / barcode-input `locked` 收敛五通道；P2 随批修 upload-field 完成写入+取消钮、scada 三面板 `inert` 门控、gantt/kanban/calendar 四分支根容器接 `meta.disabled`；③ ui cva 补 `primary` 键 + basic schema union/propContract union 补键（静态检查 `check-finite-prop-contracts.mjs` 登记 pattern 同步更新——schema 与 contract 仍同步，非放宽）；⑤ `__row_save_bar__`/`__drag__` 补表头 th + colgroup col + fixed 左固定条目 + resize 手柄 `relative` 包含块；⑧ DrawerBody 补 `min-h-0 flex-1 overflow-y-auto` + Drawer resize 变量消费到 Popup 几何（width/height/max 钳制）；⑨ 锚点 disabled `preventDefault`+href 置空+禁用视觉 / button-group `data-selected:` 消费类。
- **类别清扫**: 每批 grep 全 14 包 + ui（模式与命中清单见 owner doc §R3 收口节 §3），新命中 P2 同根因 3 条全部随批修 + 补回归测试；同根因不同落点文件者全部登记候选。
- **Phase 3（裁决）**: P2 172 条台账 `r2-audit/r3-p2-adjudication.md`（172 行全覆盖：随批修 3 / 候选 169 / 拒绝 0；族分布 族1×9 族2×7 族3×6 族4×1 族5×12 族6×7 族7×5 族8×8 族9×17 族10×11 单点 89）；抽查 10/10 可从台账行追溯到 round 原文。P3 87 条登记终态归 roadmap backlog。R2 owner doc 追加「R3 收口节」（P0/P1 落点清单 + 测试↔ID 映射 + 清扫记录 + 裁决汇总 + P3 声明）。
- **Phase 4（回写）**: C2 §3 追加「回写 ②」（族 2 佐证 G-F 加厚、族 7 维持 G-B2、族 1 全通道门禁经验为 D1 契约素材行）；R2 owner doc「R3 收口节」补全 P0/P1 落点清单（17 条批次①–⑨ 逐条落点文件+方式）与回归测试清单（测试名 ↔ 发现 ID 映射）。
- **R3 全量验证结果（2026-08-29，R3 修复后，owner doc R3 收口节 §6 引用处）**: `pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37 全绿；`pnpm test` **68/68 任务全绿**（R3 新增 15 个回归测试文件全部随包通过）；`pnpm check` exit 0（零新增命中）。全量 e2e **1315 passed / 11 failed / 3 flaky / 43 skipped（20.3m）**——对比 08-25 登记 baseline red（14 条）**零新增红**：11 条失败全部归因既有面，barcode 家族 ×4（08-25 登记 ×5 中 4 条触发，根因同为本地 Chromium `BarcodeDetector` 能力面缺陷）+ crud-demo ×6（08-25 登记同 6 条，标题渲染问题归 crud-demo owner）+ scada-perf:155 fps 吞吐 ×1（**stash 实证**：干净树 HEAD `1129772fe` 聚焦复跑同红、R3 工作树聚焦复跑通过——纯机器负载波动型既有红，与 08-15 登记「scada-perf 全量并行性能波动×1」同型，失败断言为吞吐阈值 36.9<45 而功能性断言 TE-1 视口位移/指针路径 ≥45 均过，且 R3 未触及 perf 页渲染路径）；08-25 登记的 sundial ×2 本次全过未触发、scada-edge-cases ×2 转 flaky 复跑通过；watch-only gantt-perf ×2 / kanban-perf ×1 未触发。3 条 flaky（c3-5 link echo 陈旧值断言时序、scada-edge-cases hover ×2）复跑全过，与 R3 变更面无关。e2e 总红数 11 < 登记 baseline 14。
- **R3 closure audit 通过并收口**：fresh session 独立子 agent `ses_fb4589abaffeqrHt1MkZyRtBw7` verdict **approved**（零 Blocker/Major，3 Minor——「16 个回归测试文件」实为 15 已订正两处；In Scope 未点名 content/layout 落点包由 round 条目条款覆盖；程序性收尾已执行）。审计覆盖 A–H：plan 文本一致性 / 17 条修复测试逐字核对 6 文件 + live 源码抽查 3/3 / P2 台账 172 行 + 5/5 追溯 / C2 回写 ② / 三方一致性 / deferred 诚实性 / diff 范围纪律。plan `Plan Status: completed`，roadmap R3 `planned`→`done`。审计证据落 plan §Closure。
- **P1 参考应用线上调研与复刻工程规范执行**（plan `docs/plans/2026-08-29-0419-2-p1-reference-app-research-and-replication-spec.md` WS1–WS7 全 Workstream 完成，closure audit 通过，roadmap P1 `planned`→`done`）。产出 `docs/analysis/ui-review/P1-reference-apps/` 7 篇：README 复刻工程规范总篇（6 应用 slug/CSS 前缀/mock 端点分配表、目录命名规范、mock 后端与 e2e 模板、分析篇文档模板、验收维度：测试档位 + AI 模板感治理 + 样式契约）+ 6 篇应用分析篇（`ant-design-pro.md` / `cal-booking.md` / `linear.md` / `notion-database.md` / `airtable-grid.md` / `stripe-dashboard.md`），每篇四要素（设计令牌结构 / 页面清单 ★ 分级 / 核心交互清单 / 可复刻边界）+ 能力映射表（逐行 C2 对照）+ 5 个闭源应用差异声明。
- **调研执行方式**：6 个 fresh-session 研究子 agent 并行（各 ≤12 fetch，官方文档/源码优先）；关键实证：AntD Pro `config/routes.ts`+`defaultSettings.ts`+v5 令牌全表、Cal.com `--cal-*` CSS 变量表+Booker reservation 语义、Linear keycombiner 快捷键全表+官方 select-issues（chord/多选/⌥↑↓ 重排）、Notion help 视图族+10 色三套色盘、Airtable 28 型别/键盘快捷键官方文章全文、Stripe dashboard/search/reports docs+色彩系统博客（Badge 色阶对规则）。失效链接（preview.pro.ant.design JS 壳、linear docs/shortcuts 404、airtable docs/{slug} 404 等）均按 plan Failure Paths 记录替代来源；未核实值全部显式标注（📊/拟定/📁常识档/未能核实），Stripe **无原生批量栏**如实记录（G-B3 回写素材）。
- **转 C2 候选（各篇 §7，随各 Pi-b closure 回写裁决，本计划不做 C2 变更）**：富文本描述编辑器（Linear）、预约槽位联动容器+slot 刷新策略（Cal.com）、个人视图偏好存储层+board 列聚合（Notion）、grid 分组聚合+fill-handle 选区模型（Airtable）、筛选状态 URL 同步+语法搜索解析（Stripe）。
- **验证与收口**：纯文档变更（Test Strategy 不适用档），改动面仅 `P1-reference-apps/` 7 文件 + plan + roadmap + 本日志；`pnpm test` 全仓 68/68 任务全绿（无测试面变更，不声明 full-green——e2e 未运行）。独立 closure audit：fresh session `ses_fb43c6d5bffeL4kz0gnJfK3aWM` verdict **approved-with-minors**（零 Blocker/零 Major，3 Minor：README 验证命令 filter 名 `@nop-chaos/playground`→`@nop-chaos/flux-playground`、sundial-replica.css 引用区间 1–46→19–46、README 模板映射表备注列与各篇实例对齐为四列——均已随收口修正，修正经审计者授权"fix during closure recording"）。
- **P2a Ant Design Pro 页面模板族静态复刻执行**（plan `docs/plans/2026-08-29-1240-1-p2a-antdpro-template-static-replica.md` Phase 1–6 全部完成，closure audit 通过，roadmap P2a `planned`→`done`）。产出：9 张 `antdpro-*` 页面 schema（list 标杆 / form basic·grouped·dialog·step 四布局 / detail basic·advanced / dashboard / result，category `app-replica`）+ `apps/playground/src/antdpro-replica/antdpro-replica.css`（令牌声明于 `.adp-root/.adp-dialog` 双作用域，light-only；差异裁定：圆角 6→8px 建立视觉差异、保留 `#1677ff` 语义锚点，记入 plan §差异声明）+ `shared/mock-backend-antdpro.ts`（36 行订单确定性数据集、分组详情、看板聚合）+ showcase-env 3 个 `AntdPro__` get-only 读端点分支 + `COMPLEX_PAGE_ENTRIES` 9 条注册 + `tests/e2e/antdpro-replica-visual.spec.ts` 10 条初屏结构用例 + 12 条 mock 单测。steps 受控 value 首次 schema 用例：`value` 表达式驱动进度需 `valueOwnership: 'controlled'`（local 档忽略 value prop，registry 已文档化契约，非缺陷）。
- **执行中治理线冲突就地修复**：AntdPro 分支体最初内联使 `showcase-env.ts` 涨至 704 行触发 `check:oversized-code-files` 700 行 MUST-split 红线——分支体下沉 `mock-backend-antdpro.ts`（`createAntdProFetcherBranch` 工厂），showcase-env.ts 收敛 680 行，`pnpm check` 恢复 exit 0（仅剩 2 个已登记 exempt i18n 红行）。
- **验证状态（full-green verification，本日 P2a 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 任务全绿、`pnpm check` exit 0 零新增命中；目标 e2e `npx playwright test tests/e2e/antdpro-replica-visual.spec.ts` 10/10 全绿（含空态/禁用态/令牌 getComputedStyle 断言）。**unit + e2e 双全绿成立**（本 spec 为新增文件，不触碰既有 e2e 基线红清单）。改动面：playground In Scope 文件 + 本 spec + 截图 artifacts（sundial 先例同轨）+ plan + roadmap + 本日志，零 `packages/` 触碰，`mock-backend.ts` 维持 463 行。
- **P2a closure audit 通过**：fresh session 独立子 agent `ses_fb3e7a832ffe1Sy9XzXORpZMg9` verdict **approved**（零 Blocker/零 Major，2 Minor 均随关闭编辑消化：Closure Gates 勾选时机、审计交接简报单测计数口误 15→12）。审计独立复跑全部验证命令 + 逐 Phase Exit Criteria 对照 live repo + 品牌资产 grep 零命中 + deferred 诚实性核对。plan `Plan Status: completed`，roadmap P2a `planned`→`done`。Next: P2b（交互接线与测试）按 roadmap 顺序取队首。
- **P2b Ant Design Pro 交互接线与测试执行**（plan `docs/plans/2026-08-29-1413-1-p2b-antdpro-interaction-wiring-and-tests.md` Phase 1–5 全部完成，closure audit 通过，roadmap P2b `planned`→`done`）。Phase 1–2（前次 session 落盘）：5 个 `AntdPro__` post 写端点契约落字 + 9 条写操作单测先红后绿（saveOrder upsert-unshift / deleteOrders 空集防御 / approveOrder 状态翻转 / submitForm / selectOrder 会话指针 + orderDetail 回退链 id→指针→A1001）、list 页 I1–I9/I14 接线与锁定 11 条 e2e（含 I4 列设置实测修正预测缺口 + 静态重复按钮移除、I3 页码回 1 schema 级补齐 + 2 条 renderer 级 finding 登记）。
- **Phase 3–4（本次执行）**：form 族接线（basic/grouped `submitAction`→`AntdPro__submitForm` + `onSubmitSuccess` navigate + `component:reset`；dialog `submitScope:'surface'`+`closeOnSubmit` 闭环；step wizard `mountOnEnter:true` 修 valuesPath 卸载清发布值实测缺口 + `onComplete` 末步提交）+ detail/result 接线（approve/reject→`approveOrder`→`refreshSource targetId`（action 级，args 形态实测不读）+ 状态翻转详情/列表双端断言；result 主/次按钮与 detail back navigate）。实测裁定两条：①页级 host 每页挂载 `<Toaster/>`，立即 navigate 卸载 toast——navigate 动作加 `control:{debounce:1200}` 对齐 AntD Pro「message→延迟跳转」；②`valuesPath` 发布于 form dispose 时回写 undefined（`form-runtime.ts` setupExternalPublication 清理分支）——分步数据暂存必须声明 `mountOnEnter`。e2e 先红后绿：首轮 8 红修复后 23/23。新增 opt-in 端点计数钩子 `window.__antdproEndpointCalls`（生产 no-op）支撑 I17「零写请求副作用」断言。
- **Phase 5（回写与自查）**：C2 §3 追加「回写 ③」（G-B3 可达面/降级点、G-E 密度缺口成立 + 列显隐维度预测修正、ProLayout 三布局无实测证据不新增行、4 条 D1 素材行）；分析篇 `ant-design-pro.md` §4.1 I1–I18「预测 vs 实测」逐条对照表 + §7 回写结论 + 3 条事实勘误；AI 模板感自查（Current Baseline 静态清单 14 项逐钮终态：接线 11/裁决移除 1/静态保留 3 归因宿主能力族与 G-E 缺口，零 demo 占位残留）；样式契约自查（零 `.css`、零 `packages/` 变更）。
- **验证状态（full-green verification，本日 P2b 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 任务全绿（playground 209/209 含 21 条 antdpro mock 单测）、`pnpm check` exit 0 零新增命中；目标 e2e `npx playwright test tests/e2e/antdpro-replica-visual.spec.ts tests/e2e/antdpro-replica-interactions.spec.ts` **33/33 全绿**（P2a 10 条初屏零回归 + interactions 23 条）。**unit + e2e 双全绿成立**（interactions spec 为本计划新增文件，不触碰既有 e2e 基线红清单）。治理线：`mock-backend.ts` 463、`showcase-env.ts` 680 零触碰，`mock-backend-antdpro.ts` 466<500，interactions spec 499 行。
- **P2b closure audit 通过**：fresh session 独立子 agent `ses_fb34349c2ffesT42CsbGgFKTwi` verdict **approved-with-minors**（零 Blocker/零 Major；1 Minor = Phase 5 自查「7 张 schema」实为 8 张的文本计数漂移，已随收口修正）。审计独立复跑单测 209/209、interactions 23/23、visual 10/10、typecheck 37/37；逐文件核对 5 写端点契约与 8 张 schema 接线形状、C2 回写 ③ 与分析篇对照在盘、deferred 诚实性（I2/I4/I10/I16/print 均附 non-blocking 理由，console 噪声登记非静默豁免）。plan `Plan Status: completed`，roadmap P2b `planned`→`done`。Next: P3a（Cal.com 静态复刻）按 roadmap 顺序取队首。
- **P3a Cal.com 预约流程静态复刻执行**（plan `docs/plans/2026-08-29-1413-2-p3a-cal-booking-static-replica.md` Phase 1–5 全部完成，closure audit 通过，roadmap P3a `planned`→`done`；P2b 虚线时序约束已于执行启动时 live 复核满足——P2b 已 done、HEAD `a77683a32`、working tree clean）。产出：3 张 `cal-*` 页面 schema（cal-booking 入口区 + 时长 tabs + calendar 月视图 + 时区 select(searchable) + 12h/24h switch + loop 分组槽位网格三态按钮 + 骨架样本 / cal-confirm reschedule 条 + 摘要卡 + 姓名邮箱电话(无 input-phone 型别，input-text 承载)+备注 + 嘉宾行形态 + select/radio-group/checkbox-group 自定义问题 + 校验错误形态静态样本 + Confirm 黑按钮 / cal-success 绿✓ + 摘要 + Add to calendar 四占位外链 + Copy link + 重排/取消 + pending 徽章，category `app-replica`）+ `apps/playground/src/cal-replica/cal-replica.css`（令牌 `.cal-root/.cal-dialog` 双作用域，8 组语义 + radius/spacing 双基准，light-only；差异裁定：品牌黑换名 `--cal-action*`、radius 10px 经官方 embed 文档实证维持不微调、Cal Sans→Inter/系统栈，记入 plan §差异声明）+ `shared/mock-backend-cal.ts`（确定性槽位数据集：hash%7===3 空日路径、时长档 trim 递减、almost-full/expired/selected 状态样本、calDateText 中文周几）+ showcase-env `Cal__event`/`Cal__slots` 两个 get-only 读端点分支（分支体下沉，showcase-env.ts 691 行 ≤700 红线）+ `COMPLEX_PAGE_ENTRIES` 3 条注册 + `tests/e2e/cal-replica-visual.spec.ts` 3 条初屏结构用例 + 12 条 mock 单测（`cal-mock-backend.test.ts`）。
- **执行期发现与就地登记**：① complex-pages 共用 registry（`render-host.tsx`）从未注册 scheduling 包渲染器，plan 点名的 calendar 型别不可达——追加 `registerSchedulingRenderers(registry)` 一行并同步 plan Scope 清单（packages/ 零改动）；② calendar 月视图实测为资源时间轴横条（单行全月格）而非 Booker 式 6 周竖网格——today 强调/导航内建可用，形态差距落 G-C 实测证据，联动可模拟性实测结论落字 plan（`dateOwnership:'scope'+dateStatePath` scope 写入 `calendar.tsx:109-115` + data-source url 逐次按 scope 物化 `api-data-source-controller-runtime.ts:279-283` + `refreshSource` by data-source name → P3b 接线路径成立；interval 轮询内建、窗口聚焦 refetch 无内建支持→候选 2 半保留）；③ loop 体 item 访问走 `$slot.item`（既有 schema 先例模式）；④ tabs 选中态 attr 为 `data-active=""`（非 "true"）。
- **验证状态（full-green verification，本日 P3a 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` exit 0（仅 flux-renderers-scheduling 1 条 0-error 既有警告，未触碰包）、`pnpm test` 72 任务全绿（playground 29 files/221 tests 含 12 条 cal 单测）、`pnpm check` exit 0 零新增命中；目标 e2e `npx playwright test tests/e2e/cal-replica-visual.spec.ts` **3/3 全绿**（含 `--cal-action`/`--cal-radius` getComputedStyle 令牌断言、槽位三态 CSS 抽查、品牌字样零引用断言）。**unit + e2e 双全绿成立**（本 spec 为新增文件，不触碰既有 e2e 基线红清单）。改动面：playground In Scope 文件 + render-host.tsx（已登记）+ 本 spec + 截图 artifacts（antdpro 先例同轨）+ plan + roadmap + 本日志，零 `packages/` 触碰；`mock-backend.ts` 零触碰、`showcase-env.ts` 691 行。
- **P3a closure audit 通过**：fresh session 独立子 agent `ses_fb3036002ffe63FU6OjzSqBCF5` verdict **approved-with-minors**（零 Blocker/零 Major；Minor1 = cal-success 注册 description 缺端点名 `Cal__event`，已随收口修复并复跑单测/e2e 全绿；Minor2 = 执行交接简报单测计数 9→实为 12 的报告口径勘误，无仓库改动）。审计六维清单（plan 一致性/P1 硬规则/品牌边界/测试有效性/执行偏差合法性/mock 数据集与 e2e 断言手工复算抽查）逐项 PASS 附 file:line。plan `Plan Status: completed`，roadmap P3a `planned`→`done`。Next: P3b（Cal.com 交互接线与测试）按 roadmap 顺序取队首，起点即 plan 实测结论节的接线路径。
- **P3b Cal.com 预约交互接线与测试执行**（plan `docs/plans/2026-08-29-1819-1-p3b-cal-booking-interaction-wiring-and-tests.md` Phase 3–5 本次收口；Phase 1–2 为前次 session 落盘）。Phase 3（cal-confirm）：I6 嘉宾增删 = `Cal__addGuest/removeGuest` 会话端点 + loop 渲染 `${selected?.guests}`（`$slot.index` 删行）+ `then → component:refresh`（componentId `cal-selected-source`）——实测撞墙：form 内按钮 `refreshSource` 对页面级 source 报 `Source not found`（scoped lookup 只查所在 scope 桶不走父链，`source-registry.ts`），跨树刷新须走 `component:refresh`+`componentId`（素材行随 C2 回写 ④ 登记）；I7 校验锁定（`data-field-invalid` + `[data-slot="field-error"]` + 空提交零写请求端点计数断言；校验失败走 `onValidateError` 不触发 `onSubmitError`——留页关键）；I8 提交链（form `submitAction`→`Cal__book`+`includeScope:'*'` → `onSubmitSuccess` navigate，pending 变体裁定恒走确认态 + 成功页徽章表达式兜底）；I9 失效分支（opt-in `__calTestHooks.selectSlot` 端点同路径钩子承载 expired 强制失效，`messages.failed` toast + `onSubmitError` 返回槽位视图该槽维持置灰）；I13 回退状态保留（会话指针回填时长档/选中日/选中槽位，时区回 PAGE_DATA 默认如实落字）。Phase 4（cal-success）：I10 四外链 href 精确断言 + 可点性；I11 裁定候选 ①语义模拟（copy-link 静态 flex→button，`Cal__shareLink` + 「链接已复制」toast；实际剪贴板写入不做，clipboard 通道候选随 C2 回写 ④ 登记）；I12 重排 button+navigate 预填 / 取消 `openDialog`（reason textarea + `[保留预约, 确认取消]` secondary→primary 序 + 新 `.cal-btn-outline` scope 类）→`Cal__cancelBooking` → 状态翻转会话可观察（成功页新增 `booking` data-source 读 `Cal__latestBooking`，pending 徽章 `${booking?.status}` 三态绑定直接访问零回归，摘要三字段 booking→preview 兜底绑定）。提交/失败/取消 navigate 全部 `control:{debounce:1200}`（页级 host Toaster 随页卸载，回写 ③ 同源补丁复现）。
- **先红后绿证据（P3b Phase 3/4）**：新增 5+3 条 schema/钩子单测与 10 条交互 e2e（07–16）先行跑红（unit 7 failed/233 passed；e2e 9 failed/7 passed——01–06 与 I10 href 锁定即时绿，符合既有内建锁定口径），接线后 unit 240/240、e2e interactions 16/16 全绿。`mock-backend-cal.ts` 增 `__calEndpointCalls` 计数钩子（沿 antdpro 先例，生产 no-op）+ `__calTestHooks`，注释压缩维持 499 行 <500 治理线；`showcase-env.ts`/`mock-backend.ts` 零触碰。
- **Phase 5（回写与自查）**：C2 §3 追加「回写 ④」（G-F 三态联动实测、G-C 月视图形态差 + 降级承载、候选 1 降级组合技巧级不新增行、候选 2 裁定落字、input-phone/timezoneSelector 注记、I11 剪贴板素材行 + refreshSource scope 桶限定新 finding）；分析篇 `cal-booking.md` §4.1 I1–I15「预测 vs 实测」逐条对照 + 5 条事实勘误 + §7 两候选收口结论；AI 模板感自查通过（零 demo 占位按钮、五类状态成对可见、数据全经端点）；样式契约自查通过（零 `packages/` 改动，新 CSS 仅 `.cal-btn-outline`）。
- **验证状态（full-green verification，本日 P3b 后）**：`pnpm typecheck` 37/37、`pnpm build` 37/37、`pnpm lint` 37/37、`pnpm test` 68/68 任务全绿（playground 240/240 含 29 条 cal 单测）、`pnpm check` exit 0 零新增命中；目标 e2e `npx playwright test tests/e2e/cal-replica-interactions.spec.ts tests/e2e/cal-replica-visual.spec.ts` **19/19 全绿**（interactions 16 条 + P3a visual 3 条零回归）。**unit + e2e 双全绿成立**（interactions spec 为本计划 Phase 2 新增文件，不触碰既有 e2e 基线红清单）。附带修复 Phase 1 遗留 typecheck 红一处（cal 测试 `post` helper body 型别 `Record<string, unknown>`→`Record<string, SchemaValue>`，对齐 antdpro 同款 helper）。
- **P3b closure audit 通过**：fresh session 独立子 agent `ses_fb21272a7ffennzqXt26oRIoxX` verdict **approved-with-minors**（零 Blocker/零 Major；Minor 1 = cal-confirm.json 嘉宾删除钮 onClick 缩进漂移，已随关闭编辑修复并复跑单测 240/240；Minor 2 = interactions spec 529 行超 ~500 启发线（门禁未标记），登记 plan Non-Blocking Follow-ups 观察项）。审计 A–H 清单全 PASS：plan 文本一致性（执行期未自勾 Closure Gates、未提前标 done）/ Phase 3/4 落地 file:line 复核（submitAction/includeScope/debounce/onValidateError 缺省/钩子生产 no-op）/ 先红后绿诚实性（契约↔单测映射 + 程序化断言核查）/ Phase 5 文档 append-only diff 核验（C2 初版裁决表零改动）/ 变更面纪律（零 packages/、mock-backend.ts 与 showcase-env.ts 零触碰）/ deferred 诚实性 / roadmap 与 daily log 状态核对。审计者独立复跑：unit 240/240、e2e 19/19、typecheck 37/37、check exit 0、行数门 499/691/463。plan `Plan Status: completed`，roadmap P3b `planned`→`done`。Next: P4a（Linear 静态复刻）按 roadmap 顺序取队首。
