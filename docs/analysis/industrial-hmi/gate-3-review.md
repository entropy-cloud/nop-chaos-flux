# I7 Gate 结论：实现对照 review（I5 引擎 Wave 1 + I6 数据绑定与动画 Wave 2）

> 日期：2026-08-04
> 版本：v1（I7.1 产出）
> 上游：plan `docs/plans/2026-08-03-2307-2-i7-implementation-gate-review.md`（I7.1 本文件）；讨论文件 `docs/discussions/2026-08-03-industrial-hmi-scada-mission-scope-discussion.md` §八 8 条最终决策；4 份设计文档 `docs/components/industrial-hmi/design-engine.md`（§4.1-§4.4/§8.2/§8.3/§11）、`design-data-binding.md`（§4.1-§4.5/§7/§8.1/§8.2/§9/§11）、`design-symbols.md`（§4.1/§4.2/§10/§11/§12.1 V5）、`design-renderer.md`（§4.3/§8.2/§12.2）；调研汇总 `docs/analysis/industrial-hmi/research-summary.md`（I0.5）；实现 plan `docs/plans/2026-08-03-2113-3-i5-engine-core-wave1.md`（completed）与 `docs/plans/2026-08-03-2307-1-i6-data-binding-and-animation-wave2.md`（completed）及二者 Closure/Closure Audit Evidence；roadmap `docs/components/roadmap-industrial-hmi.md` 全文（执行必读/Phase Status/Work Items/Cross-Cutting 性能红线与测试纪律/Rule 4）；前一 gate 结论 `docs/analysis/industrial-hmi/gate-2-review.md`（§6 I7 gate 注意项 8 条/§4 A1-A5 约束）；leafer 真实 API 抽查锚点 spike 工程 `/Users/abc/sources/industrial-hmi-research/spike-leafer/src/demo.js`（leafer-ui@2.2.9 + @leafer-in/viewport@2.2.9）与 leafer 系列本地 clone 源码（`leafer/packages/`、`leafer-ui/packages/`、`leafer-in/packages/viewport/`）逐项核对
> 下游：`docs/plans/2026-08-03-2307-2-i7-implementation-gate-review.md`（I7.2 修正落地 + 回归测试）、`docs/plans/2026-08-03-2307-3-i8-basic-symbol-library-wave3.md`（I8，依赖 I7 done）
> 依据：roadmap I7.1 + Cross-Cutting（review gate 纪律/文档共识审查不叠加/人工确认阈值/Rule 4/性能红线/测试纪律）；`docs/references/new-renderer-introduction-audit.md` 不作审查输入依据（I10.2 五边界审计属 I10，本 gate 仅记录归属，见 §10）

## 文档共识审查记录（本文件）

> 依据 roadmap Cross-Cutting「文档共识审查」条款，本文件定稿前须经独立子 agent（fresh session）反复审查直到共识（判据：连续一轮 0 新增修正项；≤3 轮，超限升级人工）。轮次记录如下（由编写者于每轮落地后回填事实，判定由审查 agent 给出）：

- **Round 1（2026-08-04）**：独立 agent（fresh session，task `ses_0376801a9ffebSC6hADmT6VtEd`）审查，判定 `REVISE`——0 Blocker/0 Major、2 Minor + 1 Nit：`F1`（Minor）m-2 证据引注错位——「透传 leafer config」实际在 design-engine.md §4.1:73 + §7:141 而非 :79，m-2 结论本身成立；`F2`（Minor）§2 核对项 22 引注指向错误函数——point:change 发射区域不在 :159-161（该处为订阅注册方法），已按发射区域重写；`F3`（Nit）全文 10+ 处 file:line 引注疑似 ±1~2 行偏移（该轮列出的「修正值」经 Round 2 复核证实有误——原值即 live 正确行号，见 Round 2 记录）。F1/F2 落地；F3 曾按该轮建议修改，Round 2 复核后全部恢复为原值。
- **Round 2（2026-08-04）**：独立 agent（fresh session，task `ses_0375f1128ffeSIortl6TsAo20z`）审查，判定 `REVISE`——F1 落地正确；F2 部分落地（引注已移至发射区域，但内部行号需精确化）；**F3 全面反向落地**：该轮逐一对照 live 证实 Round 1 提出的 12 处「偏移修正」全部有误，原值恰为正确行号（如 design-engine.md §4.2:96/:97/:98 = 创建/销毁/重建、§4.4:114 = 程序式视口 API、design-renderer.md §8.5:237 = component:setPointValue、§12.1:302 = A3、design-symbols.md §8:197/§4.4:161/§10:206、design-data-binding.md §6:208、i6-plan:19、i5-plan:248），已全部恢复原值。新增 3 项：`N1`（Minor）12 处引注恢复 live 正确行号；`N2`（Minor）核对项 22 内部行号精确化——`point-store.ts:187-198（去重 :192 → 死区 :193 → 发射 :198）`；`N3`（Nit）event-bridge.test.ts 发射形状引注精确化——tap 发射 :40、double_tap :54、pointer.move :61、:68-69/:76-78 为发射行（:79 为断言行）。N1/N2/N3 全部落地。
- **Round 3（2026-08-04）**：独立 agent（fresh session，task `ses_03754ab18ffeb3fL4MvQ0Z4spZ`）确认轮，判定 `AGREE`——N1 12 处引注全部与 live 吻合、N2 point-store 内部行号精确、N3 两处落点精确化、头部记录如实（R1 反向落地说明 + R2 恢复动作）、全文通读自洽（3 Major + 8 Minor 与 §9 判定 revise 匹配、§11 收口动作一一对应、核心代码引注无偏移），**零新增修正项，达成共识**（共识循环：R1-R2 修正 2 轮 + R3 确认轮，未超轮次上限）。

---

## 1. 审查输入

### 1.1 证据集

| 类别                 | 文件                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ------- | -------------------------------------- | ------- | --------------------------------------------------------- |
| 主审对象（I5）       | `packages/flux-renderers-industrial/src/engine/`：scada-engine.ts / tree-registry.ts / viewport.ts / config-adapter.ts / hit.ts / test-handle.ts；`src/serialization/`：config-types.ts / validate.ts / parse.ts / serialize.ts / diff.ts；`src/symbols/`：symbol-types.ts / symbol-registry.ts / style-resolver.ts / symbol-factory.ts / base-shapes/ / register-builtin.ts；`src/test-support/leafer-ui-mock.ts` |
| 主审对象（I6）       | `packages/flux-renderers-industrial/src/binding/`：point-store.ts / reverse-index.ts / dirty-collector.ts / expression-evaluator.ts / bind-resolver.ts / value-to-state.ts / animator.ts；`src/engine/`：hit.ts / event-bridge.ts（I6.4）                                                                                                                                                                          |
| 主审对象（I4 壳）    | `src/renderer-definitions.ts` / `scada-canvas-placeholder.tsx` / `schemas.ts`（仅检查注册机制对齐与 cid 通道，I10 完整注册后置）                                                                                                                                                                                                                                                                                   |
| 设计契约             | `docs/components/industrial-hmi/design-engine.md` / `design-data-binding.md` / `design-symbols.md` / `design-renderer.md`（共识定稿版，gate-2 m-1/m-2 已落地）                                                                                                                                                                                                                                                     |
| 调研结论             | `docs/analysis/industrial-hmi/research-summary.md`；按需对照 `research-render-engines.md` / `research-scada-apps.md` / `research-download.md`                                                                                                                                                                                                                                                                      |
| 实现 plan            | `docs/plans/2026-08-03-2113-3-i5-engine-core-wave1.md`（completed，deferred 5 项裁定 + closure audit 3 Minor）；`docs/plans/2026-08-03-2307-1-i6-data-binding-and-animation-wave2.md`（completed，deferred 5 项裁定 + createNormalizedActionEvent 归属 Decision）                                                                                                                                                  |
| roadmap              | `docs/components/roadmap-industrial-hmi.md` 全文（执行必读/Phase Status/Work Items I0-I16/Phase Details/Dependency Graph/Cross-Cutting/Rule）                                                                                                                                                                                                                                                                      |
| 前一 gate            | `docs/analysis/industrial-hmi/gate-2-review.md`（§6 注意项 8 条、§4 A1-A5）                                                                                                                                                                                                                                                                                                                                        |
| leafer 真实 API 锚点 | `/Users/abc/sources/industrial-hmi-research/spike-leafer/src/demo.js`（真实 leafer-ui@2.2.9 + @leafer-in/viewport@2.2.9 用法）+ 本地 clone 源码（`leafer/packages/interface                                                                                                                                                                                                                                        | event | display | partner/\*`、`leafer-ui/packages/event | display | interaction/_`、`leafer-in/packages/viewport/_`）逐项核对 |
| 验证命令             | `pnpm --filter @nop-chaos/flux-renderers-industrial test`（268 passed / 20 files，coverage statements 96.77/branches 90.78/functions 97.57/lines 98.44 复现）；`pnpm --filter @nop-chaos/flux-renderers-industrial typecheck` 通过                                                                                                                                                                                 |

### 1.2 任务范围摘要（讨论文件 §八 8 条最终决策）

1. **范围**：运行时渲染优先（引擎 + 图元库 + 数据驱动 + 画面渲染）；编辑器后置为后继 mission（I16 仅立项入口，不实现）。
2. **选型**：LeaferJS（leafer-ui）为渲染底座；组态语义层（点表/绑定/动画/图元模型/组态 JSON/React 桥接）自研。
3. **数据模型**：双轨——组态内点表自包含 + flux 表达式 `$xxx` 桥接。
4. **组件形态**：单 renderer type `scada-canvas`，props 内嵌组态 JSON；`scada-symbol` 图元级 type 后置。
5. **测试**：纯逻辑 Vitest 单测 + Playwright e2e 程序化断言（测试句柄 `window.__flux_scada_<cid>`；无截图、无 node-canvas）。
6. **回顾机制**：roadmap 固定 4 个 review gate（I1/I3/I7/I12），本文件为 I7.1。
7. **调研下载**：`~/sources/industrial-hmi-research/`（6 组 clone + 浅调研补充）。
8. **产物**：missions/roadmap/讨论/调研/设计文档。

---

## 2. 契约一致性核对

逐项核对表（设计契约 → 实现位置 → 结论）。核对口径：实现 vs 4 份设计文档契约 + gate-2 §6 注意项 8 条 + A1-A5。

| #   | 核对项（契约锚点）                                                                                                                                                                                                              | 实现位置                                                                                                                                                                                                                                                                  | 结论                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **A1 固化**：`ScadaCanvasEngine.create` 中 App/tree 创建参数写死 `tree: { type: 'viewport' }` 且不暴露配置项（design-engine.md §4.2:96）                                                                                        | scada-engine.ts:83-91（appConfig 内 `tree: { type: 'viewport' }` 硬编码，`...options.performance` 展开无法覆盖 tree 键；`ScadaEngineOptions`（:36-52）无 tree 配置项）                                                                                                    | ✅ 一致。单测断言 `app.config.tree.type === 'viewport'`（scada-engine.test.ts:43）                                                                                                                                                                                              |
| 2   | **A2 帧事件挂 tree 层**：tree 层 `render` 事件 → `onRender({ frame, dirtyBlocks })`（design-engine.md §4.1:90/§4.5:120/§8.1:177）                                                                                               | scada-engine.ts:94（`this.app.tree.on('render', this.handleRender)`）、:120-123（frameCount 自增，**dirtyBlocks 恒 0**）                                                                                                                                                  | ⚠️ 部分一致：帧事件挂 tree ✅；`dirtyBlocks` 恒 0（见修正项 m-1）。`onRender` 签名与 §4.1 一致                                                                                                                                                                                  |
| 3   | **A2 测试句柄含 tree 引用**：`window.__flux_scada_<cid>` 结构 engine/tree/app/getSymbol/getPointValue/getViewport/forceRender（design-engine.md §8.3:194-205；design-renderer.md §8.4:228）                                     | test-handle.ts:3-23（键名 `__flux_scada_${cid}`，含全部 6 方法）；scada-engine.ts:254-265（mount）、:134-136（destroy 移除）；`getPointValue` 经 `ScadaEngineOptions.pointStore` 只读投影（:47,260；I5 deferred 项已由 I6.1 落地）                                        | ✅ 一致。句柄挂载/移除所有权在引擎侧（mountScadaTestHandle/removeScadaTestHandle 唯一调用点均引擎）；单测覆盖挂载/移除/快照（scada-engine.test.ts:120-179）                                                                                                                     |
| 4   | **cid 传递链**：`RendererResolvedProps.cid → ScadaEngineOptions.cid`（design-renderer.md §8.4）                                                                                                                                 | `RendererComponentProps.cid`（flux-core/src/types/renderer-component.ts:19,41）、`RendererResolvedProps.cid`（renderer-core.ts:100）；引擎 `options.cid ?? nextCid++`（scada-engine.ts:72）；占位组件 `data-cid={props.meta.cid}`（scada-canvas-placeholder.tsx:14）      | ✅ 一致。cid 通道类型与传递链就绪；实际 React 桥接（I10.1）后置                                                                                                                                                                                                                 |
| 5   | **applyAttrs 唯一批量写入口**：不得泄漏逐点写 API；`setSymbolProps` = `applyAttrs({[id]: patch})` 便捷封装（design-engine.md §8.2:183-184；design-symbols.md §8:197；design-renderer.md §8.5:237 component:setPointValue 收敛） | scada-engine.ts:143-153（applyAttrs 唯一写入口；unknown id skip）、:169-171（setSymbolProps 委托）；DirtyCollector.flush 帧尾单次调用（dirty-collector.ts:78-87）；RefreshPipeline.flushFrame → collector.flush 唯一写点（dirty-collector.ts:201）                        | ✅ 一致。引擎侧无逐点写 API 泄漏；绑定/动画/状态样式全覆盖到 applyAttrs 路径                                                                                                                                                                                                    |
| 6   | **lazySpeard / changedThreshold 键名拼写**（design-engine.md §4.1:78-79）                                                                                                                                                       | scada-engine.ts:32-33（键名拼写正确）、:76-82（缺省 100/100）、:90（透传）                                                                                                                                                                                                | ⚠️ `lazySpeard` ✅ 真实 leafer 键（leafer-ui/packages/display/src/Leafer.ts:71,225）；`changedThreshold` ✗ **非 leafer 真实配置键**（leafer + leafer-ui 源码零命中；Watcher 硬编码 `changed < 100`，leafer/packages/partner/watcher/src/Watcher.ts:57）——透传无害但无效，见 m-2 |
| 7   | **注册机制对齐**：`registerScadaRenderers`/`registerScadaSymbol` 对齐既有 `registerSchedulingRenderers`/`registerRendererDefinitions`（gate-2 注意项 7；禁重复实现）                                                            | index.ts:80-82（registerScadaRenderers → registerRendererDefinitions(registry, industrialRendererDefinitions)）vs flux-renderers-scheduling/src/index.ts:22-24（同构）；symbol-registry.ts 自研「注册表 + 工厂」（映射 leafer tag 工厂，design-engine.md §3:56 边界约定） | ✅ 一致。无重复实现注册机制                                                                                                                                                                                                                                                     |
| 8   | **引擎命令句柄 17 项 API 面**（design-engine.md §8.2）                                                                                                                                                                          | scada-engine.ts：applyAttrs/getSymbol/getSymbols/getSymbolProps/setSymbolProps/fit/center/setViewport/zoomAt/setSize/getViewport/getWorldPoint/getViewportPoint/applyDiff/exportConfig/importConfig/destroy/reset 全齐（:143-232,139-141,125-137）                        | ✅ 一致（17 项命令面 + destroy/reset 生命周期，与 I5 plan Closure 计数一致）                                                                                                                                                                                                    |
| 9   | **destroy 逆序销毁 + 幂等**（design-engine.md §4.2:97）                                                                                                                                                                         | scada-engine.ts:125-137（事件桥注销 → tree off render → adapter.destroy → registry.clear → app.destroy → 移除句柄；`destroyed` 守卫幂等）                                                                                                                                 | ✅ 一致。单测断言幂等与监听移除（scada-engine.test.ts:81-97）                                                                                                                                                                                                                   |
| 10  | **reset 重建语义 / applyDiff 增量**（design-engine.md §4.2:98；§8.2）                                                                                                                                                           | scada-engine.ts:139-141（reset → adapter.build 全量替换）；:214-217（applyDiff + setConfig）                                                                                                                                                                              | ✅ 一致。单测覆盖 rebuild/applyDiff（config-adapter.test.ts:136-168）                                                                                                                                                                                                           |
| 11  | **场景树组织与 tree-registry**：id→Leaf 索引、z 序 = 树序遍历顺序（design-engine.md §4.3:102-104）                                                                                                                              | tree-registry.ts:15-20（add 按序 zIndex=byId.size）、:54-62（subtreeIds）；config-adapter.ts:63-94（递归建树 + 注册索引）                                                                                                                                                 | ✅ 一致                                                                                                                                                                                                                                                                         |
| 12  | **视口纯逻辑工具**：worldToViewport/viewportToWorld/fit/center/zoomAt/缩放钳制 0.1-20（design-engine.md §4.4:109-114）                                                                                                          | viewport.ts 全部实现（:36-89，MIN_SCALE/MAX_SCALE = 0.1/20）                                                                                                                                                                                                              | ✅ 一致。纯逻辑单测互逆/比例/钳制/锚点缩放全绿（viewport.test.ts）                                                                                                                                                                                                              |
| 13  | **引擎视口命令 ↔ zoomLayer 矩阵对接**（design-engine.md §4.4:114；I5 plan Phase 2）                                                                                                                                             | scada-engine.ts:238-252（applyViewportState：scaleOfWorld 锚点缩放 + zoomLayer.move）                                                                                                                                                                                     | ❌ **不一致（live defect）**：`move` 平移符号反了——见 §3 抽查项 4 与修正项 M-3                                                                                                                                                                                                  |
| 14  | **三源点表模型**（design-data-binding.md §4.1：static/expression/flux + scale/deadband/init）                                                                                                                                   | config-types.ts:5-16（ScadaPointDeclaration 全字段）；point-store.ts:95-103（三源声明加载）、:187-217（去重/死区/换算归口）                                                                                                                                               | ✅ 一致。点表类型 I5 本地结构形状 → I6.1 由 binding/ 再导出统一（config-types.ts 为唯一定义源，index.ts:7-18 再导出）                                                                                                                                                           |
| 15  | **绑定表达式 → 属性映射**：可绑定属性集 13 项（design-data-binding.md §4.2:105）                                                                                                                                                | bind-resolver.ts:14-28（BINDABLE_PROPERTIES 13 项逐字对齐）；:90-123（point→expression→map→scale→format 优先级）                                                                                                                                                          | ✅ 一致                                                                                                                                                                                                                                                                         |
| 16  | **统一归口**：绑定只引用点表变量；flux 源声明于点表层级（design-data-binding.md §4.2 注:103）                                                                                                                                   | 绑定层无 flux 字段（config-types.ts:18-24 ScadaBinding 无 flux）；`source: 'flux'` 在点表声明（:7）                                                                                                                                                                       | ✅ 一致（I6 plan Decision 归口落地）                                                                                                                                                                                                                                            |
| 17  | **刷新流水线**：合并帧 + 脏属性收集 + 帧尾单次 applyAttrs（design-data-binding.md §4.3:109-136，A5 固化）                                                                                                                       | dirty-collector.ts:37-104（帧内脏收集 + requestRender 帧对齐 + flush 单次批量写）；RefreshPipeline（dirty-collector.ts:134-349）编排：脏点 → 表达式依赖链重算 → 反向索引 → 绑定求值 → 状态判定 → 帧尾 flush                                                               | ✅ 一致。1 帧内多写入收敛单次 applyAttrs（refresh-pipeline.test.ts:73-105,255-283）；1000 点批量单帧断言（:84-105，I6 plan 注明 1 万点全量留 I14）                                                                                                                              |
| 18  | **绑定反向索引**：pointId → [{symbolId, property}]（design-data-binding.md §4.1:41/§6:208）                                                                                                                                     | reverse-index.ts:46-131（递归构建/增量维护/lookup/lookupSymbol/getBindings）                                                                                                                                                                                              | ✅ 一致                                                                                                                                                                                                                                                                         |
| 19  | **表达式求值器**：`@{pointId}` 子集 + 依赖链 + 缓存 + 环检测（design-data-binding.md §4.2:106；binding-cycle Failure Path）                                                                                                     | expression-evaluator.ts:344-404（缓存/失效/环检测 active 栈）；tokenize/parse 子集实现（:75-334）                                                                                                                                                                         | ✅ 一致。环检测/未知点/迭代预算均有单测（refresh-pipeline.test.ts:161-232,306-329）                                                                                                                                                                                             |
| 20  | **多状态呈现**：ranges/booleanMap/valueMap 判定链 + 默认状态（design-data-binding.md §4.5:169-191）                                                                                                                             | value-to-state.ts:11-51（优先级 ranges→booleanMap→valueMap；量程换算先行；默认 run/首键）                                                                                                                                                                                 | ✅ 一致                                                                                                                                                                                                                                                                         |
| 21  | **动画生命周期 + 合帧调度**：rotate/blink/flow/move、start/stop/pause/resume/isPlaying、30ms 限频、增量汇入帧内脏收集（design-data-binding.md §4.4:157-167）                                                                    | animator.ts:65-269（时钟注入 now/scheduleTick、interval 30ms、四类插值、loop、collect 汇入脏收集、requestFrame 帧尾写请求）                                                                                                                                               | ✅ 一致。30ms 限频/合帧/方波/loop 单测齐备（animator.test.ts:172-290）                                                                                                                                                                                                          |
| 22  | **数据层事件**：point:change/state:change/animation:start/stop（design-data-binding.md §8.1:225-229）                                                                                                                           | point-store.ts:187-198（Object.is 去重 :192 → 死区 :193 → 发射 :198）；dirty-collector.ts:125-127,181-183（state:change 统一发射出口）；animator.ts:10-13,97,108,116（animation:start/stop）                                                                              | ✅ 一致。订阅/退订协议齐备（I11 触发器体系输入源）                                                                                                                                                                                                                              |
| 23  | **数据层句柄**：setPointValues/getPointValue/getPointState/subscribe/flushFrame（design-data-binding.md §8.2:233-237）                                                                                                          | point-store.ts:130-134,116-124,140-157；dirty-collector.ts:90-95（flushFrame）；RefreshPipeline.flushFrame（:185-202）                                                                                                                                                    | ✅ 一致                                                                                                                                                                                                                                                                         |
| 24  | **引擎事件桥 + 载荷规范化**：symbol:click/dblclick/hover 载荷 `{ symbolId, world, viewport }`（design-engine.md §8.1:176）；`ScadaSymbolEventPayload`（design-renderer.md §8.2:202-210）                                        | event-bridge.ts:3-12（ScadaSymbolEventName 3 事件 + Payload 形状逐字段对齐）、:23-32（buildSymbolEventPayload 只读快照）、:55-109（attach/destroy 幂等）；`createNormalizedActionEvent` **零调用**（归属 I11.1，I6 plan Decision 记录）                                   | ❌ **不一致（live defect）**：事件载荷**读取面**错误——`pointOf` 读 `event.point`（真实 leafer 事件无该属性，为 `x`/`y`），且命中结果未解包 `IPickResult.target`——真实 leafer 上 symbol 事件永不发射。见 §3 抽查项 2/3 与修正项 M-1/M-2                                          |
| 25  | **命中解析**：`selector.getByPoint` O(候选) + 屏外 0ms 预检，无空间索引（A3；design-renderer.md §12.1:302）                                                                                                                     | hit.ts:14-26（屏外预检 + getByPoint + idOf 反查）                                                                                                                                                                                                                         | ⚠️ 屏外预检与 A3 无索引 ✅；**返回值处理面漂移**（未解包 target）见 M-2                                                                                                                                                                                                         |
| 26  | **图元注册契约**：`registerScadaSymbol` 幂等 + override（design-symbols.md §4.1:85）                                                                                                                                            | symbol-registry.ts:5-24（type/create/props 校验 + 重复注册抛错 + override 覆盖）                                                                                                                                                                                          | ✅ 一致                                                                                                                                                                                                                                                                         |
| 27  | **样式解析**：defaults ∪ 实例 ∪ statePatch，实例 > defaults、statePatch 覆盖实例（design-symbols.md §10:206）                                                                                                                   | style-resolver.ts:3-14                                                                                                                                                                                                                                                    | ✅ 一致                                                                                                                                                                                                                                                                         |
| 28  | **8 种基础形状 + type 命名**：scada-rect/round-rect/ellipse/line/arrow/pipe/text/polygon（design-symbols.md §4.4:161；V5 目标 §12.1）                                                                                           | base-shapes/ 8 定义（index.ts:1-8 注册 8 型）；type 命名形状族无前缀对齐（config-adapter.test.ts:45-69 断言 8 形状全实例化）；V5 24 个自定义图元全路径（v5-custom-registration.test.ts:45-50）                                                                            | ✅ 一致                                                                                                                                                                                                                                                                         |
| 29  | **组态 JSON 唯一事实源**：引擎导出不从 leafer toJSON 反向依赖（design-renderer.md §4.3:158）                                                                                                                                    | scada-engine.ts:219-223（exportConfig 从 adapter.currentConfig structuredClone 输出）                                                                                                                                                                                     | ✅ 一致                                                                                                                                                                                                                                                                         |
| 30  | **renderer 空壳注册**：`scada-canvas` type/sourcePackage/defaultSchema/fields 空（I4.2，fields 随 I10.2）                                                                                                                       | renderer-definitions.ts:9-21（type/sourcePackage/component 就位，fields: [] 留空）；schemas.ts:12-38（ScadaCanvasSchema 对齐 §4.1）                                                                                                                                       | ✅ 一致（I10.2 后置）                                                                                                                                                                                                                                                           |

**gate-2 §6 注意项 8 条核验汇总**：① A1 ✅（第 1 行）；② 测试句柄 ✅（第 3/4 行）；③ applyAttrs 唯一写入口 ✅（第 5 行）；④ lazySpeard 拼写 ✅ / changedThreshold 无效键 ⚠️（第 6 行，m-2）；⑤ 序列化 perf 基线口径 ✅ 记录于 design-engine.md §4.6:132（parse 23.4 + 实例化 88.5 + 渲染 ~67，gate-2 m-2 已落地），实现侧无 perf 断言属 I14 既定顺序（无缺口）；⑥ I10.2 五边界审计——**不属本 gate**，归属记录见 §10；⑦ 注册机制对齐 ✅（第 7 行）；⑧ e2e 测试锚点——句柄机制存在且被单测覆盖 ✅（e2e 本体属 I15.1，本 gate 只确认句柄可用）。

**A1-A5 核验**：A1 ✅；A2 ✅（tree 帧事件 + 句柄含 tree；dirtyBlocks 恒 0 见 m-1）；A3 ✅（无空间索引，屏外预检）；A4（测量口径）设计文档声明完整，实现期不固化基准方法（I14 后置）——无实现义务；A5 ✅（刷新流水线 + 动画合帧全链收敛，无逐点直刷）。

---

## 3. leafer 真实 API 抽查结论

抽查锚点：spike demo.js（真实 leafer-ui@2.2.9 + @leafer-in/viewport@2.2.9）+ 本地 clone 源码逐项核对。结论标注：✅ 一致 / ❌ 漂移（live defect）/ ⚠️ 漂移（无害）。

| #   | 抽查项（spike/源码锚点）                                                                                                                                                                                                                                                                                                                                                                             | 实现位置                                                                                                                                                                               | 结论                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **App/tree 构造参数**：`new App({ view, ..., tree: { type: 'viewport' } })`（demo.js:7-14）；wheel/move/zoom 交互配置由 viewport 插件默认提供（leafer-in viewport addViewportConfig：wheel preventDefault 等）                                                                                                                                                                                       | scada-engine.ts:83-91 写死 `tree: { type: 'viewport' }`；未配置 wheel/move/zoom（交互输入属 I11.2 既定后置）                                                                           | ✅ 一致                                                                                                                                                          |
| 2   | **事件载荷形状**：leafer 指针事件数据为 `{ ..., x, y, ... }`（`PointerEventHelper.convert`：`{ ...base, x, y, ... }`，leafer-ui/packages/interaction/interaction-web/src/PointerEventHelper.ts:9-15；`UIEvent` x/y readonly 无 point 字段，leafer-ui/packages/event/src/UIEvent.ts:10-11；IUIEvent 接口仅 x/y/getPagePoint，leafer/packages/interface/src/event/IUIEvent.ts:8-10,33）                | event-bridge.ts:91-96 `pointOf` 读 `(event as {point}).point`——**真实事件无 `point` 属性，恒 undefined** → symbol:click/dblclick/hover 在真实 leafer 上永不发射                        | ❌ **漂移（M-1）**。mock/测试发射形状 `{ point: { x, y } }`（event-bridge.test.ts:40 等）与真实事件形状不符，掩蔽缺陷                                            |
| 3   | **`selector.getByPoint` 返回值**：`IPickResult { target, path, throughPath? }`（leafer/packages/interface/src/selector/ISelector.ts:6-10；Picker.getByPoint 恒返回对象，leafer/packages/partner/selector/src/Picker.ts:46）；spike 用法 `result.target` 解包（demo.js:177-179）                                                                                                                      | hit.ts:22-24 将 `getByPoint` 原始返回值直接传给 `idOf`（`registry.findByNode`，WeakMap 按节点实例反查，tree-registry.ts:38-40）——**真实返回 `{ target, path }` 而非节点，反查必失败**  | ❌ **漂移（M-2）**。mock 面 `selector.getByPoint` 直接返回叶子/`null`（leafer-ui-mock.ts:201-203；hit.test.ts:9,21 等），掩蔽缺陷                                |
| 4   | **`zoomLayer` 用法**：`zoomLayer.move({x,y})`（屏幕像素平移，viewport 插件自身用法 `zoomLayer.move(getValidMove(e.moveX, e.moveY))`，leafer-in/packages/viewport/src/type/viewport.ts:31；`move` = `moveLocal` 直接累加 x/y，leafer/packages/display-module/helper/src/LeafHelper.ts:119-127）、`zoomLayer.scaleOfWorld(worldPoint, factor)`（viewport 插件 :46）、`zoomLayer.scaleX`（demo.js:185） | scada-engine.ts:238-252：scaleOfWorld 锚点缩放 ✅（锚点 = 视口原点世界点）；**`move` 平移符号反了**：需要 `-(Δx)*scale`（推导：zoomLayer.x = -vx*s，见 §5 说明），当前传 `+(Δx)*scale` | ❌ **漂移（M-3）**。mock 的 `MockZoomLayer.move` 只记录调用不校验矩阵（leafer-ui-mock.ts:167-174），单测只断言调用次数（scada-engine.test.ts:220-234），掩蔽缺陷 |
| 5   | **`lazySpeard` 键名**：真实 leafer 配置键（leafer-ui/packages/display/src/Leafer.ts:71,225，gate-2 已核实）                                                                                                                                                                                                                                                                                          | scada-engine.ts:32,80 拼写正确且透传                                                                                                                                                   | ✅ 一致                                                                                                                                                          |
| 6   | **`changedThreshold` 键名**：leafer + leafer-ui 源码**零命中**；Watcher 帧内节流为硬编码 `changed < 100`（leafer/packages/partner/watcher/src/Watcher.ts:57）                                                                                                                                                                                                                                        | scada-engine.ts:33,81,90 透传——**无效键（无害）**；design-engine.md §4.1:73 + §7:141 声称「透传 leafer config」不实                                                                    | ⚠️ 漂移（m-2）                                                                                                                                                   |
| 7   | **事件名**：`tap`/`double_tap`/`pointer.move`（PointerEvent.TAP/DOUBLE_TAP/MOVE，leafer-ui/packages/event/src/PointerEvent.ts:17,26-27）、tree 层 `render`（demo.js:37）、`leafer.ready`（demo.js:280）                                                                                                                                                                                              | event-bridge.ts:63-65（tap/double_tap/pointer.move 真实名 ✅）；scada-engine.ts:94（tree render ✅）；`leafer.ready` 未用（onReady 属 I10 桥接职责，非 I5/I6 缺口）                    | ✅ 一致（事件名）；载荷读取面见 #2                                                                                                                               |
| 8   | **batch add**：`batch.add(nodes)` 批量实例化入树（demo.js:94-100,217-219）                                                                                                                                                                                                                                                                                                                           | config-adapter.ts:63-94 逐节点 `parent.add(leafNode)`（:78,93）——同一帧内多次 add 由 watcher 合并渲染，语义等价；性能口径（实例化 88.5ms）含 batch 路径，I14 复测对照                  | ⚠️ 漂移（m-8，性能观察项，非功能缺陷）                                                                                                                           |
| 9   | **`forceRender`**：`ILeaf.forceRender(bounds?, sync?)` 真实 API（leafer/packages/interface/src/display/ILeaf.ts:643）                                                                                                                                                                                                                                                                                | scada-engine.ts:234-236 `this.app.tree.forceRender()`；句柄 forceRender 接线                                                                                                           | ✅ 一致                                                                                                                                                          |
| 10  | **`resize`**：App.resize({width, height})                                                                                                                                                                                                                                                                                                                                                            | scada-engine.ts:197-200 setSize → app.resize                                                                                                                                           | ✅ 一致（mock 面 MockApp.resize 对应）                                                                                                                           |

**抽查结论摘要**：10 项抽查，3 项 live-defect 漂移（#2 事件载荷读取面、#3 getByPoint 返回值、#4 zoomLayer move 符号）——均被 mock 面错误建模掩蔽（mock↔真实漂移核查命中），另有 1 项无害漂移（#6 changedThreshold 无效键）与 1 项性能观察（#8 逐节点 add vs batch.add）。

---

## 4. 序列化完整性

| 契约（design-renderer.md §4.3）                                                                                                                                                                          | 实现                                                                                                                                 | 结论                                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `validateScadaConfig(json): { ok: true } \| { ok: false; errors: string[] }`——version 检查、symbol 树结构（id 唯一/children 递归/type 已注册）、点表声明（id 唯一/三源字段合法）、绑定/状态/动画字段类型 | validate.ts:141-184（version/symbol 树/children 递归/重复 id/未知 type/点表三源字段全量）；:77-88 绑定/状态/动画仅**容器级**类型检查 | ⚠️ 容器级检查达标；**内部字段语义未校验**（`bindings: { fill: {} }`、`animations: [{ kind: 'spin' }]`、`states: { states: {} }` 均通过）——I6 plan 声称「绑定/状态/动画字段的语义校验属本 plan 增量」（i6-plan:19）未落地，见 m-4 |
| `parseScadaConfig(input: string \| object)`——字符串 JSON.parse 失败错误信号；对象浅拷贝防御外部突变                                                                                                      | parse.ts:3-25（字符串失败抛 `invalid scada config JSON`；symbols/variables 浅拷贝）                                                  | ✅ 一致（onError 映射属 I10 桥接）                                                                                                                                                                                               |
| `serializeScadaConfig(config): string`——version 恒定 1；唯一事实源                                                                                                                                       | serialize.ts:3-6                                                                                                                     | ✅ 一致（round-trip 单测通过）                                                                                                                                                                                                   |
| `diffScadaConfig(prev, next): ScadaConfigDiff`——symbols 增删/属性变更/点表变更收敛 `{ added, removed, updated }`                                                                                         | diff.ts:49-111（SYMBOL_KEYS/POINT_KEYS 全字段比较、children 变更入 updated.patch、variables 变更收敛）                               | ✅ 一致                                                                                                                                                                                                                          |
| **diff 消费**：`engine.applyDiff` 增量应用（避免全量重建）                                                                                                                                               | config-adapter.ts:48-61（added 逐节点 buildNode/removed 子树摘除/updated 属性 patch + children 重建）；scada-engine.ts:214-217       | ✅ 一致（单测覆盖增删改/子树移除/children 重建）                                                                                                                                                                                 |

### design-renderer.md:155 复核（`ScadaConfigDiff.added` 类型）

- **设计文档现状**：design-renderer.md:155 写 `{ added: string[], removed: string[], updated: Array<{ id, patch }> }`——`added` 标注为**字符串数组**。
- **实现语义**：`ScadaConfigDiff.added: ScadaSymbolNode[]`（config-types.ts:93-97）；diff.ts:53 收集完整节点对象；config-adapter.applyDiff 需要完整节点才能实例化入树（config-adapter.ts:52-54 `this.buildNode(node, this.root, undefined)`）；config-adapter.test.ts:156-159 亦以节点对象断言。
- **复核结论**：**设计文档为笔误，实现语义正确**——`added` 必须是节点数组（字符串数组无法驱动增量实例化）。与 I5 closure audit 记录一致（i5-plan:248「design-renderer.md:155 diff 类型笔误（added 应为节点数组，实现语义正确，交 I7 记录）」）。落地为修正项 m-3（设计文档修正，非代码修正）。

---

## 5. 性能路径

| 红线/策略（roadmap Cross-Cutting + design-\*.md）                                      | 实现                                                                                                                                                                                                            | 结论                                                                                                  |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **点表刷新合并帧 + 脏属性收集**，禁逐点直刷                                            | point-store 去重/死区后置 dirty 标记（point-store.ts:187-203）→ RefreshPipeline 帧内脏收集（reverse-index lookup → 绑定求值 → 状态判定 → collector.collect）→ 帧尾**单次** applyAttrs（dirty-collector.ts:201） | ✅ 成立。单帧多写入收敛单次批量写断言（refresh-pipeline.test.ts:255-283）；1000 点批量单帧（:84-105） |
| **动画合帧调度**，禁每帧全量重建                                                       | animator tick（30ms 限频）→ 增量 `collect({symbolId, property, value})` 汇入脏收集 → requestFrame 帧尾写（animator.ts:203-230）                                                                                 | ✅ 成立。动画增量与点表刷新同一帧批量写收敛断言（animator.test.ts:281-290）                           |
| **render 帧事件**（A2，I14 测量锚点）                                                  | scada-engine.ts:94 tree 层 render 监听 + frameCount                                                                                                                                                             | ✅ 成立；dirtyBlocks 恒 0 见 m-1                                                                      |
| **引擎侧批量写语义**：`applyAttrs` 单次调用合并全部脏属性（design-engine.md §8.2:183） | scada-engine.ts:143-153（单次调用内遍历所有 symbol patch 各一次 set）                                                                                                                                           | ✅ 成立（leafer watcher 帧内节流作第二道防线，design-data-binding.md §12.1:284）                      |
| **视口命令路径**：fit/center/setViewport/zoomAt（I11.2 消费）                          | 纯逻辑状态计算 ✅ + zoomLayer 矩阵应用 **符号错误**（M-3）                                                                                                                                                      | ❌ 见 M-3（I11.2 与 I14 视口吞吐测量前必须修正）                                                      |
| **1 万点端到端基线**（16.9-19.7ms，design-data-binding.md §4.3:131）                   | 实现无 perf 断言；1000 点批量语义单测 + I14 全量基准后置（I6 plan Non-Blocking Follow-up 记录）                                                                                                                 | ✅ 按 plan 既定顺序（无 in-scope 缺口）                                                               |
| **组态 JSON 加载基线**（parse 23.4 + 实例化 88.5 + 渲染 ~67）                          | 设计文档已含分解（design-engine.md §4.6:132）；config-adapter 逐节点 add（m-8 观察项）                                                                                                                          | ⚠️ I14 复测对照 batch.add 差异                                                                        |

**M-3 符号推导说明**（供 I7.2 落地复核）：屏幕映射 `screen(w) = zoomLayer.x + w * scaleX`（zoomLayer 为 tree 内普通 Group 子层，其自身 x/y 不受自身 scale 影响，viewport 插件用法佐证：`zoomLayer.move(拖拽 delta)` = 内容随拖拽右移）；视口状态语义 `viewport.x` = 屏幕原点处的世界坐标（viewport.ts:36-48），故 `zoomLayer.x = -viewport.x * scale`。从状态 (x1,s1) → (x2,s2)：先 `scaleOfWorld(x1, s2/s1)`（锚定视口原点世界点保持不动）后 `zoomLayer.x' = -x1*s2`，再平移需 `Δm = -(x2-x1)*s2`。当前代码传 `+(x2-x1)*s2`（scada-engine.ts:246-249），方向反且偏移量翻倍。示例：s=1 时 setViewport({x:50})，世界 100 处应位于屏幕 50，当前实现将其置于屏幕 150。

---

## 6. 测试覆盖

### 6.1 覆盖面与断言非空洞核查

- **测试现状**：268 passed / 20 files（含 I5+I6 全部模块）；coverage statements 96.77 / branches 90.78 / functions 97.57 / lines 98.44（阈值 90 达标，本 gate 复现）。包级 typecheck 通过。
- **断言质量抽样**（验证正确行为而非仅无错误）：
  - 引擎生命周期：A1 viewport 类型、A2 帧计数、destroy 幂等 + 句柄移除（scada-engine.test.ts:35-105）；
  - 测试句柄：结构/快照/未注入 undefined 兜底（scada-engine.test.ts:119-179）；
  - 刷新流水线：帧内合并单次批量写、依赖链传播、环检测（报错且保上次有效值）、迭代预算安全网、状态切换仅过渡时发射（refresh-pipeline.test.ts:73-329,332-418）；
  - 动画：30ms 限频、方波/插值、loop、合帧收敛（animator.test.ts:172-290）；
  - 命中/事件桥：屏外预检不调用 getByPoint、未命中不发射、幂等 attach、destroy 注销（hit.test.ts:18-32；event-bridge.test.ts:65-110）；
  - 载荷规范化：逐字段形状 + 只读快照（event-bridge.test.ts:113-138）；
  - 序列化：非法 JSON/重复 id/未知 type/三源字段/round-trip/增量 diff 收敛（serialization.test.ts:31-253）；
  - V5：24 个自定义图元注册→校验→实例化→场景树加载→卸载全路径（v5-custom-registration.test.ts:45-50）。
- **非空洞结论**：纯逻辑与 wiring 断言均验证正确行为，未发现「仅无错误」的空洞测试。

### 6.2 mock↔真实 API 漂移核查（重点）

`src/test-support/leafer-ui-mock.ts` 与真实 leafer API 的漂移面核查（对照 §3 抽查锚点）：

| mock 面                                                                                                               | 真实 API                                                                         | 漂移结论                                       |
| --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------- |
| `MockLeafer.selector.getByPoint` 返回叶子/null（mock 默认 `() => null`，测试注入叶子）                                | 返回 `IPickResult { target, path }`（对象）                                      | ❌ **漂移**（掩蔽 M-2）                        |
| 测试事件发射形状 `{ point: { x, y } }`（event-bridge.test.ts:40,54,61,68-69,76-78；scada-engine.test.ts:272,294,311） | 事件数据 `{ x, y }`（无 point 属性）                                             | ❌ **漂移**（掩蔽 M-1）                        |
| `MockZoomLayer.move` 直接累加 x/y，不校验矩阵语义                                                                     | `zoomLayer.move(dx)` = 内容右移 dx 屏幕像素；`zoomLayer.x = -viewport.x * scale` | ❌ **漂移**（掩蔽 M-3，mock 需补矩阵断言能力） |
| `MockApp.resize`/`MockLeafer.on('render')` 计数                                                                       | App.resize / Leafer.on('render') 真实存在                                        | ✅ 一致                                        |
| `MockLeafer.forceRender` 发 render 事件                                                                               | `ILeaf.forceRender(bounds?, sync?)` 真实存在（ILeaf.ts:643）                     | ✅ 一致                                        |
| 构造器面（App/Group/Rect/Line/Text/… 与 `tree: {type}` 记录）                                                         | 与 spike demo.js 一致                                                            | ✅ 一致                                        |

**核查结论**：3 处 mock 面与真实 API 漂移，全部集中在事件/视口矩阵路径（恰为 I6.4/I5.2 的核心交付），且全部掩蔽 live defect——单测全绿但真实 leafer 上事件不发射、视口平移反向。**I6 plan Failure Paths `leafer-event-api-drift`（i6-plan:73）触发**：实现期未对照 spike/源码核对事件载荷与命中返回值形状，本 gate 落地为 M-1/M-2/M-3。

---

## 7. 修正项清单

### Blocker

无。

### Major

#### M-1：事件桥读取 `event.point`——真实 leafer 事件无 `point` 属性，symbol 事件永不发射

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/event-bridge.ts:91-96`（`pointOf` 读 `(event as { point?: { x?: unknown; y?: unknown } }).point`）；mock 面 `src/test-support/leafer-ui-mock.ts` + 受影响测试（event-bridge.test.ts:40,54,61,68-69,76-78 等、scada-engine.test.ts:272,294,311）
- **描述**：leafer 指针/tap 事件数据为 `{ x, y, ... }`（`PointerEventHelper.convert` 构造，leafer-ui/packages/interaction/interaction-web/src/PointerEventHelper.ts:9-15；`UIEvent` 仅 x/y + `getPagePoint()`，leafer-ui/packages/event/src/UIEvent.ts:10-11,55-57；IUIEvent:8-10,33）。`pointOf` 读 `event.point` 在真实 leafer 上恒 undefined → 命中路径不执行 → **symbol:click/dblclick/hover 在真实浏览器上永不发射**（I6.4 核心交付不可用）。
- **建议改法**：`pointOf` 改读 `event.x`/`event.y`（canvas-local = 视口坐标，与 `getByPoint` 期望空间一致、与 §8.1 载荷 `viewport` 语义一致）；world 保持 `viewportToWorld` 换算（scada-engine.ts:279 接线不变）；mock 面事件形状改为真实形状 `{ x, y }`（可补 `getPagePoint`），测试发射形状同步更新；新增回归测试：以真实形状 `{ x: 10, y: 20 }` 发射 tap/double_tap/pointer.move 断言事件发射 + 载荷字段。
- **验证方式**：包级单测（真实形状断言）+ typecheck；I13.1/I15.1 真实浏览器 e2e 经测试句柄断言点击联动。

#### M-2：命中解析未解包 `IPickResult.target`——`getByPoint` 返回值处理面漂移，命中反查必失败

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/hit.ts:17-25`（`resolveSymbolId` 将 `getByPoint` 原始返回值直接交 `idOf`）；`scada-engine.ts:272-273`（`idOf = (leaf) => this.registry.findByNode(leaf as object)`，WeakMap 按节点实例反查）；mock 面 `leafer-ui-mock.ts:201-203` + hit.test.ts/event-bridge.test.ts/scada-engine.test.ts 的 getByPoint 注入
- **描述**：leafer `selector.getByPoint` 恒返回 `IPickResult { target, path, throughPath? }`（ISelector.ts:6-10；Picker.ts:46）；spike 用法即 `result.target` 解包（demo.js:177-179）。实现把结果对象而非节点传给 `findByNode` → 反查恒失败（与 M-1 叠加：即使事件点读取修正，id 解析仍失败）。
- **建议改法**：`resolveSymbolId` 中先解包 `(hit as { target?: unknown }).target`（`target == null` → 未命中返回 undefined），再交 `idOf`；mock 面 `MockLeafer.selector.getByPoint` 返回 `{ target, path }` 形状（并加 default `{ target: null, path: [] }`）；回归测试：注入 `{ target: leaf, path: [] }` 断言解析成功、`{ target: null }` 断言未命中。
- **验证方式**：包级单测（真实形状断言）+ typecheck。

#### M-3：`applyViewportState` 平移符号反了——视口命令（setViewport/zoomAt/fit/center）在真实 leafer 上反向平移且偏移量翻倍

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/scada-engine.ts:245-250`（`this.app.tree.zoomLayer.move({ x: (clamped.x - cur.x) * clamped.scale, y: ... })`）
- **描述**：`zoomLayer.move(dx)` = 内容右移 dx 屏幕像素（viewport 插件自身用法 `zoomLayer.move(getValidMove(e.moveX, e.moveY))`，leafer-in/packages/viewport/src/type/viewport.ts:31；`move` = `moveLocal` = x/y 直接累加，LeafHelper.ts:119-127）。屏幕映射 `zoomLayer.x = -viewport.x * scale`（推导见 §5），故平移应传 `-(Δx)*scale`；当前传 `+(Δx)*scale`，方向反且偏差翻倍。mock 只记录调用不校验矩阵（leafer-ui-mock.ts:167-174），单测只断言调用次数（scada-engine.test.ts:220-234），缺陷被掩蔽。
- **建议改法**：改为 `move({ x: -(clamped.x - cur.x) * clamped.scale, y: -(clamped.y - cur.y) * clamped.scale })`；`MockZoomLayer` 补矩阵语义断言能力（move 后 `x -= ...` 不可行——改为断言引擎调用参数或让 mock 记录期望的 `zoomLayer.x = -vx*s` 关系）；回归测试：`setViewport({x:50, y:30, scale:2})` 后断言 zoomLayer 调用 `move({x: -100, y: -60})`（当前实现传 +100/+60，测试先行失败）。
- **验证方式**：包级单测（move 参数符号断言）+ typecheck；I11.2 真实浏览器视口交互 e2e 兜底。

### Minor

#### m-1：`onRender` 的 `dirtyBlocks` 恒为 0

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/scada-engine.ts:120-123`（`handleRender` 固定 `dirtyBlocks: 0`）
- **描述**：契约 `onRender({ frame, dirtyBlocks })`（design-engine.md §4.1:90）；leafer `render` 事件携带 `times`/bounds（RenderEvent.ts:35-40，事件参数被引擎忽略）。dirtyBlocks 字段存在但无实际值，I14 帧测量只能取 frame。
- **建议改法**：二选一——① 从 leafer render 事件数据映射（事件回调参数带 times/bounds 时以脏块数或占位填充并注释来源）；② 设计文档明示 `dirtyBlocks` 为预留字段（I14 固化口径），代码注释标注恒 0 原因。
- **验证方式**：单测断言 onRender 载荷形状；文档核对。

#### m-2：`changedThreshold` 不是 leafer 真实配置键（透传无效）

- **落点**：设计文档 `design-engine.md` §4.1:73 + §7:141（「透传 leafer config」表述）；代码 `scada-engine.ts:33,81,90`（透传无害）
- **描述**：leafer + leafer-ui 源码对 `changedThreshold` 零命中；Watcher 帧内节流为硬编码 `changed < 100`（leafer/packages/partner/watcher/src/Watcher.ts:57）。键透传不报错但无效果，属设计文档表述失真。
- **建议改法**：design-engine.md §4.1 该行改写为「`changedThreshold`（自研占位键，语义对齐 leafer Watcher 硬编码 changed<100 帧内节流；非 leafer 配置键，透传无害）」；引擎注释同步说明；I14 调参不依赖此键。
- **验证方式**：文档核对 + leafer 源码 grep 复核。

#### m-3：design-renderer.md:155 `ScadaConfigDiff.added` 类型笔误（设计写 `string[]`）

- **落点**：设计文档 `design-renderer.md` §4.3 增量 diff 行（:155）
- **描述**：设计写 `{ added: string[], ... }`；实现 `added: ScadaSymbolNode[]`（config-types.ts:93-97）且语义正确（applyDiff 需完整节点实例化，§4 复核）。设计文档笔误需修正。
- **建议改法**：design-renderer.md:155 改为 `{ added: ScadaSymbolNode[], removed: string[], updated: Array<{ id, patch }> }`（引用 config-types.ts 类型）；头部共识记录补本 gate 修正条目。
- **验证方式**：文档核对。

#### m-4：validate 对 bindings/states/animations 仅容器级类型检查

- **落点**：代码 `packages/flux-renderers-industrial/src/serialization/validate.ts:77-88`；I6 plan（i6-plan:19 声称「绑定/状态/动画字段的语义校验属本 plan 增量」）
- **描述**：`bindings: { fill: {} }`、`animations: [{ kind: 'spin' }]`、`states: { states: {} }` 均通过校验；design-renderer.md §4.3 校验行要求「绑定/状态/动画字段类型」。I6 plan 的增量承诺未落地。
- **建议改法**：二选一——① validate.ts 补内部结构校验（ScadaBinding 字段类型：point/expression/map/scale/format；ScadaStateDeclaration：states 映射 + ranges/booleanMap/valueMap 形状；ScadaAnimation：kind 枚举 + when 形状），单测覆盖非法形态；② 若裁定运行时兜底（onError 路径），在 plan/文档记录裁定并删除增量承诺。倾向①（契约 §4.3 明列校验项）。
- **验证方式**：新增单测断言非法绑定/动画/状态形态被拒绝。

#### m-5：`ScadaSymbolProps` 增 `dashOffset` 字段未同步设计文档

- **落点**：代码 `packages/flux-renderers-industrial/src/symbols/symbol-types.ts:26`（`dashOffset?: number`）；设计文档 `design-symbols.md` §4.2（:88-141 无 dashOffset）
- **描述**：I6 为 flow 动画新增 `dashOffset`（docs/logs/2026/08-03.md:13 有记录），design-symbols.md §4.2 未同步——组态 JSON 字段与设计 schema 漂移（design-renderer.md §4.2 图元字段表亦未列）。
- **建议改法**：design-symbols.md §4.2 管线行补 `dashOffset?: number`（注明 flow 动画消费，design-data-binding.md §4.4 flow 行 dash-offset 语义）；design-renderer.md §4.2 同步核对（可选，图元字段表未穷举属性子集，SymbolProps 为准）。
- **验证方式**：文档核对。

#### m-6：`getSymbolProps` 返回 leafer 原生属性面（`fontSize` 而非 `textSize`）

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/scada-engine.ts:163-167`（`leaf.node.get()`）
- **描述**：design-engine.md §8.2 getSymbolProps 语义为「图元属性读取」；实现返回 leafer 原生 attrs（如 text 节点 fontSize/dashPattern 等映射后键名），与 `ScadaSymbolProps` 键名不完全一致，e2e 断言（design-renderer.md §8.4 例 `getSymbol('pump-1').fill` 可用）需知悉。
- **建议改法**：二选一——① 文档明示 getSymbolProps 返回 leafer 属性面（I15.1 e2e 断言指南注意）；② 实现做 scada 键名投影（toNodePatch 逆映射），I10.1 桥接时评估。
- **验证方式**：文档核对 / 桥接层消费时复核。

#### m-7：`interactionLayer` 构造选项声明但未接线

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/scada-engine.ts:43`（`interactionLayer?: boolean` 声明，构造器未使用）
- **描述**：design-engine.md §4.1:83-84 交互覆盖层开关「缺省 true」；引擎未实现 sky 层交互覆盖物（hover 高亮等运行时反馈属 I8.2/I11.2 语义）。选项惰性（I5 closure audit 已记录「interactionLayer 惰性选项（I6.4/I8.2 前向兼容）」），但无注释/文档标注。
- **建议改法**：代码补注释标注 reserved（I8.2/I11.2 启用）；或 design-engine.md §4.1 补「本期惰性保留」说明。
- **验证方式**：文档核对。

#### m-8：config-adapter 逐节点 add vs spike batch.add 批量入树

- **落点**：代码 `packages/flux-renderers-industrial/src/engine/config-adapter.ts:63-94`（buildNode 递归中逐节点 `parent.add`）
- **描述**：spike 性能路径用 `batch.add(nodes)` 批量实例化入树（demo.js:94-100,217-219）；设计 §4.6「Group.add 批量入树」。实现逐节点 add 同一帧内由 watcher 合并渲染（语义等价），性能口径（实例化 88.5ms）含 batch 路径——功能无缺口，但 I14 复测时需对照该差异。
- **建议改法**：不改代码（本 gate 不认定功能缺陷）；记录为 I14 复测观察项（I6 plan Non-Blocking Follow-up 已有「1 万点批量合并全量断言留 I14」呼应）。
- **验证方式**：I14 实测对照。

---

## 8. 差异清单裁定

| #   | 差异（任务范围 ↔ 设计文档 ↔ 实现）                                                                                                                           | 裁定                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| 1   | **事件载荷读取面**：设计 §8.1 载荷 `{ symbolId, world, viewport }` 未规定事件坐标读取字段；实现按 mock 假设读 `event.point`                                  | **实现缺陷（M-1）**：设计无冲突，实现未按真实 leafer 事件形状（x/y）实现——mock 面错误建模掩蔽。非设计差异，落地为 M-1 |
| 2   | **getByPoint 返回值处理**：设计 A3 约定 `selector.getByPoint` 命中解析（design-renderer.md §12.1:302）；spike 实测返回 `{ target, path }`（demo.js:177-179） | **实现缺陷（M-2）**：实现未解包 `IPickResult.target`，与 spike 实测用法不一致。落地为 M-2                             |
| 3   | **zoomLayer move 符号**：设计 §4.4「对齐 zoomLayer 矩阵」（design-engine.md:114）未规定符号；推导目标 `zoomLayer.x = -vx*s`                                  | **实现缺陷（M-3）**：设计语义（世界原点↔视口映射）推导符号为负，实现取正。落地为 M-3                                  |
| 4   | **`ScadaConfigDiff.added` 类型**：设计写 `string[]`（design-renderer.md:155）vs 实现 `ScadaSymbolNode[]`                                                     | **设计文档笔误（m-3）**：实现语义正确（applyDiff 需节点实例化），设计修正                                             |
| 5   | **`changedThreshold` 键**：设计声称透传 leafer config（design-engine.md §4.1:73 + §7:141）vs leafer 源码零命中                                               | **设计表述失真（m-2）**：键透传无害但无效；设计修正表述                                                               |
| 6   | **`dashOffset` 字段**：实现新增（symbol-types.ts:26）vs design-symbols.md §4.2 未列                                                                          | **文档同步缺口（m-5）**：实现正确（flow 动画消费），设计补列                                                          |
| 7   | **批量入树**：设计 §4.6「Group.add 批量入树」vs 实现逐节点 add                                                                                               | **性能观察项（m-8）**：语义等价（watcher 同帧合并）；I14 复测对照，非缺陷                                             |
| 8   | **`createNormalizedActionEvent` 归属**：roadmap 复用表列 I6.4 消费方 vs I6 plan Decision 裁定「载荷形状对齐、调用归 I11.1」                                  | **一致，无修正**：event-bridge.ts 零调用（§2 核对项 24 验证），裁定成立，I11.1 落地时核对                             |
| 9   | **I5 deferred 项**：getPointValue/hit.ts 由 I6 吸收                                                                                                          | **一致，无修正**：getPointValue 已接线（scada-engine.test.ts:151-179）、hit.ts 已落地（I6.4），I5 plan 裁定兑现       |
| 10  | **范围一致性**：编辑器→I16、报警/趋势不内置、`scada-symbol` deferred、e2e 后置 I13.1/I15.1、perf 复测后置 I14                                                | **一致，无修正**：全链与 roadmap/讨论 §八/§九 吻合                                                                    |

---

## 9. 判定

**判定：`revise`**——0 Blocker、**3 Major**（M-1 事件载荷读取面漂移、M-2 getByPoint 返回值未解包、M-3 视口平移符号错误）、8 Minor（m-1…m-8）。

- 3 项 Major 均为「单测全绿但真实 leafer 上行为不可用」的 live defect，共同根因是 `src/test-support/leafer-ui-mock.ts` 对事件载荷形状、`getByPoint` 返回值形状、zoomLayer 矩阵语义的错误建模掩蔽（I6 plan Failure Paths `leafer-event-api-drift` 触发）。I6.4 事件系统与 I5.2 视口命令为 roadmap I11 的直接上游，修正须在 I7.2 完成并回归后再推进 I8。
- 已确认 live defect 未降级为 follow-up：M-1/M-2/M-3 全部列入 I7.2 落地清单（§7）。
- 人工确认阈值：**未触发**——修正项均为实现缺陷/文档笔误类，不涉及引擎选型变更、`scada-canvas` 公共契约重大变更、benchmark 不达标、共识循环超限或编辑器提前启动。

---

## 10. I8+ 域问题归属记录（不落地的显式记录）

| 观察项                                                                          | 归属        | 说明                                                                                                                 |
| ------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| I10.2 五边界审计（gate-2 注意项 6）                                             | I10.2       | roadmap I10 强制原则审计；本 gate 不重叠审查（审查输入声明），renderer-definitions fields 规则留 I10.2 落地          |
| `onReady`/`onError` 引擎事件（design-engine.md §8.1 ready/error）               | I10.1/I10.3 | 引擎当前仅 onRender/onSymbolEvent；ready（首帧）/error（校验/构建失败 → empty region + onError）属 renderer 桥接消费 |
| `component:*` 组件句柄（design-renderer.md §8.5）                               | I10.2       | ComponentHandleRegistry 注册；fit/getSymbols/setPointValue 等经既有注册通道收敛到引擎句柄                            |
| wheel/pinch 用户交互配置（zoom min/max 等）                                     | I11.2       | 引擎依赖 viewport 插件默认配置；交互输入路径与 zoom 边界（引擎状态钳制 0.1/20 vs 插件无钳制）I11.2 落地核对          |
| 视觉状态交互层（hover 高亮等 sky 覆盖物）与 `interactionLayer` 接线（m-7 关联） | I8.2/I11.2  | design-engine.md §6 交互覆盖层语义                                                                                   |
| flow 语义消费（base shapes 无 applyProps，`flow`/`dashOffset` 直写节点属性）    | I9.4/I8.1   | 管道图元实现时经 applyProps 消费 flow 参数                                                                           |
| 1 万点全量批量合并断言 + 组态 JSON 加载 batch.add 对照（m-8）                   | I14         | I6 plan Non-Blocking Follow-up 记录在案                                                                              |
| e2e 程序化断言（测试句柄）补强                                                  | I15.1       | 句柄机制已就绪且单测覆盖（§2 注意项 8）                                                                              |

---

## 11. 收口动作清单（本文件定稿后由 I7 plan 执行）

1. **I7.2 修复 M-1**：event-bridge.ts `pointOf` 改读 `event.x`/`event.y`；mock 面事件形状改真实形状；受影响测试（event-bridge.test.ts、scada-engine.test.ts）发射形状同步；新增真实形状回归测试（tap/double_tap/pointer.move → 事件发射 + 载荷字段断言）。
2. **I7.2 修复 M-2**：hit.ts `resolveSymbolId` 解包 `IPickResult.target`（null → 未命中）；mock 面 `selector.getByPoint` 返回 `{ target, path }` 形状；回归测试覆盖解包成功/未命中两态。
3. **I7.2 修复 M-3**：scada-engine.ts `applyViewportState` move 参数取 `-(Δx)*scale`；MockZoomLayer 补矩阵语义断言；回归测试断言 `setViewport({x:50,y:30,scale:2})` 触发 `move({x:-100,y:-60})`。
4. **I7.2 落地 m-1**：onRender dirtyBlocks 填充或文档明示预留。
5. **I7.2 落地 m-2**：design-engine.md §4.1 changedThreshold 表述修正 + 引擎注释。
6. **I7.2 落地 m-3**：design-renderer.md:155 added 类型修正为 `ScadaSymbolNode[]` + 头部共识记录补条目。
7. **I7.2 落地 m-4**：validate.ts 补 bindings/states/animations 内部结构校验（或记录运行时兜底裁定）+ 单测。
8. **I7.2 落地 m-5**：design-symbols.md §4.2 补 `dashOffset` 字段。
9. **I7.2 落地 m-6/m-7**：getSymbolProps 语义文档化；interactionLayer 标注 reserved。
10. **I7.2 回归**：M-1/M-2/M-3 修正后包级 typecheck/lint/test 全绿；仓库级 `pnpm typecheck`/`build`/`lint`/`test` 全绿（M-1/M-2/M-3 涉及 mock 面与测试修改，须全量回归）。
11. **roadmap 回写**（Rule 4）：I7 gate 结论与修正项摘要回写 roadmap 头部「文档共识审查记录」块；I7.2 修正落地且回归通过后 roadmap Phase Status I7 `planned → done`（前置：独立 closure-audit）。
12. **gate-3-review.md 自身文档共识审查**：独立子 agent（fresh session）审查 ≤3 轮至 0 新增修正项。
13. **docs/logs/2026/08-04.md**（或当日日志）记录本 gate 产出摘要与 I7.2 落地结果。
14. **人工确认项**：无（§9 声明）。
