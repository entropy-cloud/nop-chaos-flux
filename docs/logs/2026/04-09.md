# Development Log — 2026-04-09

### 2026-04-09

- Refined the target form-state design so author-visible pending/status reads no longer point toward raw store exposure: form-internal expressions now standardize on a readonly `$form` binding, cross-form observers standardize on explicit `statusPath`, and docs/examples reject `$store` as a public schema surface.
- Link to docs: `docs/architecture/form-validation.md`, `docs/architecture/action-interaction-state.md`, `docs/components/form/design.md`, `docs/examples/user-management-schema.md`, `docs/plans/47-form-status-visibility-and-reserved-form-binding-plan.md`
- Key decision: keep ordinary form values on normal lexical scope paths, but publish form meta state through semantic summary surfaces (`$form` inside, `statusPath` outside) instead of implicit by-name lookup or store-shaped bindings.
- Next step: implement `$form` and `statusPath` through the new focused plan rather than continuing to rely on ad hoc host flags in examples.

### 2026-04-09

- Generalized the same idea beyond forms: active docs now treat loading/pending/status as owner-specific readonly summaries rather than per-control store leaks, clarifying that CRUD is a composition of form + source/query + table + tracked operations instead of one monolithic status owner.
- Link to docs: `docs/architecture/action-interaction-state.md`, `docs/components/table/design.md`, `docs/components/data-source/design.md`, `docs/components/index.md`, `docs/plans/48-semantic-owner-status-surface-unification-plan.md`
- Key decision: unify external owner-state reads on `statusPath`, keep `<axis>StatePath` for writable interaction axes, and reserve local bindings such as `$form` only for owners with strong subtree-local authoring needs.
- Next step: if table/dialog local bindings are still desired after implementation planning, add them as narrow follow-ups on top of the generalized owner-status model instead of inventing ad hoc component-specific state contracts.

### 2026-04-09

- Clarified page versus surface ownership: `page` is now documented as a shell owner only, while `dialog` and `drawer` are treated as one surface-owner family that should expose external readonly state through `statusPath` and, if local bindings are ever needed, converge on a shared `$surface` rather than splitting into `$dialog` and `$drawer`.
- Link to docs: `docs/architecture/action-interaction-state.md`, `docs/components/page/design.md`, `docs/components/dialog/design.md`, `docs/components/drawer/design.md`, `docs/plans/49-page-and-surface-status-ownership-plan.md`
- Key decision: prefer the most specific owner instead of lifting child status into `page`; keep surface control (`component:open` / `component:close`) separate from inner form/source semantic owners.
- Next step: decide in a narrow follow-up whether `$surface` is actually justified by subtree-local authoring demand before adding any new reserved binding.

### 2026-04-09

- Extended the same owner model to structured containers: `tabs` is now explicitly treated as an interaction owner, future `wizard` is positioned as a likely composite owner, and future `sheet` is constrained to reuse an existing owner family instead of spawning a fresh `$sheet` binding by name alone.
- Link to docs: `docs/architecture/action-interaction-state.md`, `docs/components/tabs/design.md`, `docs/components/index.md`, `docs/plans/50-structured-container-owner-model-plan.md`
- Key decision: keep `valueStatePath` for writable interaction axes, reserve `statusPath` for readonly owner summaries, and require future local bindings like `$tabs` to justify themselves against concrete subtree-local authoring demand.
- Next step: if wizard design starts, model step-switching and step-commit as two separate owner surfaces instead of collapsing both into one vague wizard state object.

### 2026-04-09

- Consolidated the owner model into quick-reference taxonomy tables so future component docs can classify status ownership without reopening the same naming debate; the docs now explicitly distinguish producer, semantic lifecycle, interaction, surface, shell, and explicit tracked-operation owners.
- Link to docs: `docs/architecture/action-interaction-state.md`, `docs/components/index.md`, `docs/plans/48-semantic-owner-status-surface-unification-plan.md`
- Key decision: treat ownership classification as a first-class doc artifact rather than a scattered set of component-specific notes.
- Next step: when new stateful container docs are added, require them to cite the taxonomy table and justify any new local binding against it.

### 2026-04-09

- Added a dedicated `wizard` design baseline and positioned it as a composite owner: step switching is now documented as interaction state, while step commit/validation is a separate semantic lifecycle surface. The new doc also freezes `statusPath` as the first external summary surface and explicitly rejects eager `$wizard` exposure.
- Link to docs: `docs/components/wizard/design.md`, `docs/architecture/action-interaction-state.md`, `docs/plans/50-structured-container-owner-model-plan.md`
- Key decision: do not model wizard as either a dressed-up `tabs` or a mini `page`; keep navigation and commit semantics separate from the start.
- Next step: if a real wizard renderer is prioritized later, derive its runtime API from this split instead of introducing a single ambiguous wizard status object.

### 2026-04-09

- Rewrote `docs/architecture/action-interaction-state.md` into a tighter normative structure centered on core rules, owner taxonomy, naming rules, canonical scenarios, and rejected directions so future component docs can cite one stable status-ownership baseline instead of inheriting exploratory discussion text.
- Link to docs: `docs/architecture/action-interaction-state.md`
- Key decision: keep the settled design but remove duplicated discussion paths, making `statusPath`, owner families, local-binding rules, and auto-disable guidance the primary reference surface.
- Next step: when future component docs add state surfaces, cite this document's taxonomy and naming rules directly instead of re-explaining the full rationale.

### 2026-04-09

- Extracted dialog/drawer shared rules into `docs/architecture/surface-owner.md` so surface-family components can reuse one narrow contract for open-state ownership, `statusPath`, and future `$surface` direction instead of duplicating the same explanation in each component doc.
- Link to docs: `docs/architecture/surface-owner.md`, `docs/components/dialog/design.md`, `docs/components/drawer/design.md`, `docs/plans/51-surface-owner-shared-contract-plan.md`
- Key decision: treat surface ownership as a reusable family-level contract, not a per-component local convention.
- Next step: when future `sheet`-like components are designed, classify them against `surface-owner.md` before introducing any new binding or status surface names.

### 2026-04-09

- Reconciled the cross-store discussion with the current architecture baseline by introducing `Domain Host Owner` into the interaction-state taxonomy, clarifying that complex hosts read internally through reactive `Host Projection` and should publish any host-external readonly summary through `statusPath` rather than a separate `publishScope` field.
- Link to docs: `docs/discussions/06-cross-store-data-driven-design.md`, `docs/architecture/action-interaction-state.md`, `docs/architecture/complex-control-host-protocol.md`, `docs/plans/52-domain-host-status-publication-plan.md`
- Key decision: keep one read-story for domain hosts: internal `Host Projection`, external `statusPath`, writes through namespaced actions.
- Next step: if domain-host status publication is implemented later, keep the external DTO narrow and readonly instead of lifting full host projection data into page-global scope.

### 2026-04-09

- Added a focused scope-ownership doc to freeze the default data model as “lexical inheritance plus own-patch shadowing,” align `data` as the current node's scope-init patch, keep `isolate` as the sole isolation control term, and explicitly reject `$parentScope` as a general escape hatch.
- Link to docs: `docs/architecture/scope-ownership-and-isolation.md`, `docs/components/page/design.md`, `docs/plans/53-scope-ownership-and-isolation-alignment-plan.md`
- Key decision: keep authoring simple by making inheritance the default, isolation explicit, and external parent data access in isolated subtrees an explicit projection/copy problem rather than a hidden lexical backdoor.
- Next step: if dialog/drawer later gain `data`, align them to the same “own scope init patch” semantics instead of inventing a second local-data model.

### 2026-04-09

- Carried the same scope-init rule into `dialog` and `drawer` target docs, and tightened `renderer-runtime.md` so fragment/region authoring now explicitly prefers inheritance by default, `data` for narrow own-scope patches, and `isolate: true` only for real boundary/performance cases rather than as a cue to add `$parentScope` backdoors.
- Link to docs: `docs/components/dialog/design.md`, `docs/components/drawer/design.md`, `docs/architecture/renderer-runtime.md`, `docs/plans/53-scope-ownership-and-isolation-alignment-plan.md`
- Key decision: `data` means the same thing across page/form/dialog/drawer, while fragment isolation remains an explicit narrow tool rather than a second default scope model.
- Next step: if future fragment-heavy container docs appear, require them to state whether they are ordinary inherited scopes or explicit isolated carriers.

### 2026-04-09

- Tightened the table side of the same story: docs now keep `isolate` as the preferred term, define a future `rowData` projection surface for isolated rows, and distinguish table shell `data` from per-row projected extras so row-local performance does not depend on `$parentScope` fallback.
- Link to docs: `docs/architecture/scope-ownership-and-isolation.md`, `docs/components/table/design.md`, `docs/plans/54-table-row-projection-and-isolation-plan.md`
- Key decision: isolated row scopes should receive any needed parent/table values through explicit projection (`rowData`), not through a generalized lexical escape hatch.
- Next step: if table runtime follow-up is scheduled, implement `rowData` evaluation against the table lexical scope plus row-local roots before publishing the final isolated row patch.

### 2026-04-09

- Added the hot-path constraint for future `rowData`: it must be evaluated once per row owner, cached by `rowKey`, and incrementally published as row-local roots rather than recomputed inside each cell or materialized as a full parent-scope clone.
- Link to docs: `docs/architecture/table-row-identity-and-scope-performance.md`, `docs/components/table/design.md`, `docs/plans/54-table-row-projection-and-isolation-plan.md`
- Key decision: preserve row-local performance by treating `rowData` as a narrow additive projection on top of the existing row-scope cache, not as a second dynamic row-state system.
- Next step: when implementation starts, keep `rowData` diffing at root-key granularity and avoid per-cell evaluation paths.

### 2026-04-09

- Added a dedicated `loop` design baseline and froze it as a structural node with inherited item scope by default, `itemName`/`indexName` naming, repeated-instance identity aligned to table rows, and a separate `itemData` surface instead of prematurely promoting table `rowData` into a global repeated-projection primitive.
- Link to docs: `docs/components/loop/design.md`, `docs/components/index.md`, `docs/plans/55-loop-structural-node-and-item-scope-plan.md`
- Key decision: `loop` and `table` share repeated-instance mechanics but not the same default scope behavior; table row isolation stays a performance-specific exception.
- Next step: if loop implementation starts, keep item scope inherited by default and only add an isolation optimization later if real high-frequency loop cases justify it.

### 2026-04-09

- Folded the new `loop` baseline back into the architecture entry docs so repeated-instance identity and scope-ownership docs now both state the same rule: loop items inherit parent lexical scope by default, while table rows remain the isolated high-frequency exception.
- Link to docs: `docs/architecture/scope-ownership-and-isolation.md`, `docs/architecture/template-instantiation-and-node-identity.md`, `docs/index.md`
- Key decision: keep the repeated-instance model shared, but keep scope defaults owner-specific instead of forcing table's performance exception onto all repeated structures.
- Next step: if list/grid-style collection renderers are added later, classify whether they behave more like general loop items or more like table rows before reusing either default blindly.

### 2026-04-09

- Clarified the boundary between `loop` and `list`: `loop` is now explicitly documented as a no-UI structural expansion node, while `list` is reaffirmed as a visual collection renderer that may reuse the same repeated-item substrate internally without collapsing its external schema into `loop`.
- Link to docs: `docs/components/loop/design.md`, `docs/components/list/design.md`, `docs/index.md`
- Key decision: keep structure and visual collection shells separate so authors can choose `loop` for raw structural expansion and `list` for opinionated sequential presentation.
- Next step: if `list` implementation starts, reuse loop/repeated-item substrate under the hood without reintroducing a second template protocol.

### 2026-04-09

- Added `fragment` as the explicit no-UI structural grouping node so grouped `when` / `data` / `isolate` use cases no longer need either a parallel `if` component or `container` masquerading as a virtual wrapper. The loop design now points to `fragment` for grouped conditional content.
- Link to docs: `docs/components/fragment/design.md`, `docs/components/loop/design.md`, `docs/components/index.md`, `docs/plans/56-fragment-structural-grouping-node-plan.md`
- Key decision: keep `when` as the only condition mechanism and solve grouped structural control with `fragment`, not with a second condition DSL.
- Next step: if fragment implementation starts, preserve its no-UI baseline and avoid forcing a default DOM wrapper just to mirror visual container semantics.

### 2026-04-09

- Added a grouped architecture-doc entry (`docs/architecture/README.md`) instead of immediately reshuffling the now-crowded flat `docs/architecture/` directory. The new guidance recommends logical grouping first and family-by-family migration later, keeping top-level anchor docs stable until a narrower move is justified.
- Link to docs: `docs/architecture/README.md`, `docs/index.md`, `docs/plans/57-architecture-docs-grouping-and-gradual-migration-plan.md`
- Key decision: the architecture directory is crowded enough to need grouped navigation, but not yet worth a one-shot physical reorganization that would churn links across the repo.
- Next step: if one doc family becomes especially dense and self-contained, move that family first under a dedicated subdirectory rather than renaming the entire architecture tree.

### 2026-04-09

- Added `recurse` as the lexical recursive structural node on top of the new `loop` + `fragment` baseline. The docs now treat recursive JSON rendering as “re-instantiate the nearest enclosing loop body with new items” rather than as a global template-registry problem.
- Link to docs: `docs/components/recurse/design.md`, `docs/components/loop/design.md`, `docs/architecture/template-instantiation-and-node-identity.md`, `docs/plans/58-recursive-structural-rendering-plan.md`
- Key decision: keep recursive authoring lightweight by combining `loop`, `fragment`, and `recurse`, while reusing the same repeated-instance identity model already defined for table rows and repeated structures.
- Next step: if recursive renderer implementation starts, validate nearest-enclosing-loop resolution and `instancePath` accumulation before considering any higher-level tree or schema-driven domain wrappers.

### 2026-04-09

- Verified that AMIS does have strong tree-related references (`input-tree`, `tree-select`, and the underlying Tree UI), then documented a separate Flux `tree` baseline as a visual hierarchical renderer rather than collapsing those form-control references into the new structural `loop + recurse` layer.
- Link to docs: `docs/components/tree/design.md`, `docs/components/roadmap.md`, `docs/plans/59-tree-visual-renderer-boundary-plan.md`
- Key decision: keep tree UI, structural recursion, and form tree selection as three related but distinct layers.
- Next step: if future form tree controls are designed, derive them from the tree/UI layer and form field semantics together instead of overloading either plain `tree` or plain `recurse`.

### 2026-04-09

- Completed that follow-up at the doc layer: added `input-tree` and `tree-select` design baselines so the tree family is now split cleanly into structural recursion (`loop + recurse`), visual tree UI (`tree`), and form tree controls (`input-tree` / `tree-select`).
- Link to docs: `docs/components/input-tree/design.md`, `docs/components/tree-select/design.md`, `docs/components/tree/design.md`, `docs/plans/60-form-tree-controls-boundary-plan.md`
- Key decision: do not overload either `tree` or `select`; form tree selection remains its own field family.
- Next step: when implementation work starts, share option/tree model and UI substrate where useful, but keep external renderer contracts separate.

### 2026-04-09

- Clarified the action authoring baseline by adding `docs/architecture/action-graph-authoring.md` and `docs/architecture/action-interaction-state.md`, then updated `docs/architecture/action-algebra-formal-spec.md`, `docs/components/button/design.md`, and Plan 46 so visual-designer concerns no longer pressure the exported DSL into a `steps` rewrite.
- Link to docs: `docs/architecture/action-graph-authoring.md`, `docs/architecture/action-interaction-state.md`, `docs/architecture/action-algebra-formal-spec.md`, `docs/plans/46-user-management-schema-and-authoring-contract-alignment-plan.md`
- Key decision: keep `then` / `onError` object-or-array and explicit `parallel` as the exported Action Algebra baseline; solve future designer UX through an authoring-layer graph projection and solve generic async button pending through owner-first state plus future explicit tracked interactions rather than hidden inference.
- Next step: when pending-state implementation work starts, add an explicit tracked-interaction contract instead of teaching generic buttons to guess which async step is the blocking owner.

### 2026-04-09

- Retired `docs/architecture/frontend-programming-model-improvement-design.md` after merging its surviving final-design rules back into `docs/architecture/frontend-programming-model.md` and relying on narrower active docs for subsystem details (`action-algebra-formal-spec.md`, `api-data-source.md`, `form-validation.md`, `complex-control-host-protocol.md`).
- Updated live doc routing and active plan/discussion references so `docs/architecture/` now reflects only the current architecture baseline instead of keeping a separate follow-up improvement companion beside the root programming-model document.
- Key decision: architecture docs should describe the latest final design only; follow-up or convergence notes belong either in the current owning doc or in historical logs/plans, not as a second active architecture baseline file.
- Next step: when later cleanup happens, continue collapsing any remaining “current baseline + follow-up baseline” pairs into one owning architecture document per topic.

### 2026-04-09

- Refactored `docs/architecture/frontend-programming-model.md` from a 2000-line mixed top-level-plus-subsystem document into a slimmer normative root focused on macro layering, the seven primitives, primitive composition, derived-system boundaries, and hard invariants.
- Added `docs/architecture/action-algebra-formal-spec.md` to own the detailed `Action Algebra` contract (`when`, `then`, `onError`, `parallel`, result classes, chained result context) that no longer belongs in the top-level programming-model document.
- Updated `docs/index.md` routing so readers can now enter through either the macro programming model or the narrower action-control-flow spec depending on the question they are answering.
- Key decision: keep overall architecture and primitive-composition content in the top-level programming-model doc, but push detailed subsystem rules into narrower architecture docs instead of letting the top-level contract absorb every convergence detail.
- Next step: if later doc cleanup continues, move any remaining subsystem-dense sections still duplicated elsewhere toward the same top-level-versus-narrow-doc split.

### 2026-04-09

- Re-audited historical `docs/plans/*` and recent `docs/logs/2026/*` against the new plan template, then made three targeted adjustments instead of a larger rewrite: restored `Goals` / `Non-Goals` as template-level required sections, expanded allowed plan statuses to match real historical usage (`proposed`, `superseded`, `deferred`), and allowed `Workstream` as an alternative to sequential `Phase` slices.
- Key decision: keep the template lightweight, but align it with actual repository history so it does not fight the plan shapes that have already worked well here; the only hard status requirement remains plan-level plus execution-slice-level status, not a mandatory top-level status table for every plan.
- Next step: if future active plans repeatedly miss the same field, tighten that specific template point only; do not grow the guide back into a long prose rulebook without concrete historical evidence.

### 2026-04-09

- Simplified `docs/plans/00-plan-authoring-and-execution-guide.md` into a template-first guide so plan drafting relies on a short enforced structure instead of a long prose rule set.
- The guide now makes two execution rules explicit in the template itself: every plan needs plan-level status plus phase-level status, while explanatory sections such as `Purpose`, `Scope`, and `Risks` do not require separate completion markers.
- Key decision: keep the guide lightweight and make the template carry the discipline through `<<说明>>` placeholders, phase status fields, exit criteria checkboxes, and a required closure/follow-up block.
- Next step: use the simplified template as the default starting point for future active plans and update older active plans only when they are next touched.

### 2026-04-09

- Added `docs/plans/00-plan-authoring-and-execution-guide.md` to standardize how plans are drafted, executed, re-audited, and closed, based on recurring plan drift and completion-audit misses seen in recent development history.
- Updated `AGENTS.md` and `docs/index.md` so future plan drafting/execution work is explicitly routed through the new guide, and `docs/plans/` is described as active execution documentation with required status discipline rather than a loose historical bucket.
- Key decision: require every active plan to start from an audited current baseline, define hard scope/non-goals, use phase exit criteria plus validation checklists, and explicitly move any leftover work to out-of-scope or successor-plan ownership before calling a plan complete.
- Next step: use the new guide for future `docs/plans/*` authoring and completion reviews, especially when re-auditing broad migration/remediation plans.

### 2026-04-09

- Expanded the `README.md` license section to state Flux's AMIS inspiration more explicitly and document that some code paths were re-expressed through AI-assisted analysis of existing AMIS implementations under Flux's own architecture and coding rules.
- Link to docs/assets: `README.md`
- Key decision: keep the provenance note near the license section so repository readers can see both the MIT license and the high-level implementation lineage in one place.
- Next step: if we later add a dedicated provenance or acknowledgements section, keep it aligned with this README statement.

### 2026-04-09

- Tightened `README.md` wording around Flux's lineage and project-status framing so the introduction stays direct: the repo overview now stays focused on Flux's own architecture claims and concrete entry points.
- Link to docs/assets: `README.md`
- Key decision: keep lineage references lightweight in the top-level repository narrative and prefer direct statements about Flux's model, package shape, and evaluation surface.
- Next step: keep future README edits centered on Flux's model and use lineage references only where they add orientation.

### 2026-04-09

- Restructured `README.md` to present Flux in its own right: clarified the inspiration relationship with AMIS without adversarial framing, added a stronger design-goals section, tightened architecture depth to README scope, and improved evaluator-oriented quick-start/doc routing.
- Link to docs/assets: `README.md`
- Key decision: treat the README as both an evaluation entry point and a contributor-facing repo overview, but lead with Flux's own design goals rather than comparative critique.
- Next step: only revisit README structure again if we later want a more product-marketing landing page instead of a contributor-oriented repository front page.

### 2026-04-09

- Improved the README architecture section by replacing the old ASCII dependency flow with two Mermaid views: one for the execution pipeline and one for the workspace package topology.
- Link to docs/assets: `README.md`
- Key decision: separate conceptual runtime architecture from monorepo package relationships instead of forcing both concerns into one table plus one text arrow block.
- Next step: if Mermaid rendering is reliable on the target hosting surface, consider adding one more diagram for the seven primitives vs derived systems boundary.

### 2026-04-09

- Refreshed the repository landing page by adding live README preview images for both the playground home page and the Flow Designer workspace.
- Link to docs/assets: `README.md`, `docs/images/readme-playground-home.png`, `docs/images/readme-flow-designer.png`
- Key decision: use Playwright-captured screenshots from the real playground surfaces instead of mock images so the README reflects the current product surface.
- Next step: regenerate the screenshots when the playground home layout, Flow Designer workspace, or capability cards change materially.

### 2026-04-09

- Plan 45 fully complete. Final lint fixes: removed redundant `itemsRef`/`pairsRef` prop from `ArrayEditorRow` and `KeyValueRow` child components (React Compiler flagged mutation of a ref passed as a prop argument). The mutations were already covered by `syncItems`/`syncField` in the parent — child copies were dead code. Removed two stale `eslint-disable` comments for `react-compiler/react-compiler` that were no longer needed (`use-dialog-drag.ts:152`, `use-selection.ts:66`).
- All verification gates pass: `pnpm typecheck` ✅, `pnpm build` ✅, `pnpm lint` ✅, `pnpm test` ✅ (1134 tests across 20 packages).
- Marked `docs/plans/45-react19-compiler-and-high-frequency-interaction-refactor-plan.md` as ✅ DONE.

### 2026-04-09

- Executed Plan 45: React 19 Compiler & high-frequency interaction refactor.
- **Step 1 — React Compiler**: Installed `babel-plugin-react-compiler@1.0.0`, `@rolldown/plugin-babel`, `@babel/core`, and `eslint-plugin-react-compiler`. Updated `apps/playground/vite.config.ts` to activate the compiler via `babel({ presets: [reactCompilerPreset({ target: '19' })] })`. Added `react-compiler/react-compiler: error` to `eslint.config.js` as a regression guard. Added `manualChunks` function to `rollupOptions` to split large packages into dedicated chunks.
- **Step 2 — Unified async cancel model**: Replaced boolean `cancelled`/`disposed` flag patterns with `AbortController` in `packages/flux-react/src/useSourceValue.ts`, `packages/flux-react/src/useNodeImports.ts`, and `packages/flux-code-editor/src/source-resolvers.ts` (4 hooks). Cleanup functions now call `controller.abort()` before releasing namespaces.
- **Step 3 — TableRenderer split**: Decomposed `packages/flux-renderers-data/src/table-renderer.tsx` (906 lines) into `table-renderer/types.ts`, `table-renderer/table-data.ts`, `table-renderer/use-table-row-scope-cache.ts`, `table-renderer/use-table-controls.ts` (pagination, selection, sort, filter, expand — each with `startTransition`), `table-renderer/use-table-handle.ts`. The orchestrator `table-renderer.tsx` is now ~290 lines.
- **Step 4 — Spreadsheet interactions split**: Decomposed `packages/spreadsheet-renderers/src/use-spreadsheet-interactions.ts` (918 lines) into 10 independent modules under `spreadsheet-interactions/`: `use-snapshot`, `use-selection`, `use-editing`, `use-fill-handle`, `use-resize`, `use-clipboard`, `use-style-commands`, `use-sheet-commands` (with `startTransition` on rename), `use-find-replace` (with `startTransition` on results), `use-comments`, `use-field-drop`, `use-keyboard`. The orchestrator is now ~220 lines.
- Key decision: `startTransition` is applied only to non-urgent derived state (pagination, selection bulk update, find/replace results, sheet rename) — not to primary pointer/keyboard capture handlers.
- Link to docs: `docs/architecture/performance-design-requirements.md` — added React Compiler, `startTransition`, and module boundary rules.
- Next step: After profiling confirms compiler adoption on table/debugger/designer components, retire redundant manual `useMemo`/`useCallback` boundaries that the compiler now covers.

 that still mention internal `__` selectors as legacy snapshots, so they no longer read like current architecture guidance.
- Link to docs: `docs/bugs/14-tailwind-v4-monorepo-content-scan-canvas-invisible-fix.md`, `docs/plans/19-bem-to-tailwind-migration-plan.md`, `docs/plans/27-comprehensive-code-remediation-plan.md`
- Key decision: preserve historical records, but annotate them explicitly instead of silently rewriting old steps into present-tense guidance.
- Next step: only do a broader historical-doc rewrite if we decide the remaining archive/plan text is causing real maintenance confusion.

### 2026-04-09

- Continued the styling-system cleanup after the main BEM-to-`data-slot` migration by removing misleading old `__` examples from current architecture guidance and updating the remaining flow-designer CSS diagnostic test to query the active `data-slot` canvas marker instead of an obsolete BEM selector.
- Link to code/docs: `docs/architecture/field-frame.md`, `tests/e2e/flow-designer-css-diag.spec.ts`
- Key decision: keep historical bug/plan records as history, but update any living architecture example or active diagnostic test that still teaches or queries obsolete internal BEM markers.
- Next step: if needed, do a separate documentation-only sweep for historical plans/bugs that should be explicitly marked as legacy selector snapshots.

### 2026-04-09

- Landed the form-first semantic lifecycle baseline: `FormSchema` now exposes `initAction`, `submitAction`, `onSubmitSuccess`, `onSubmitError`, and `onValidateError`, and the form renderer/runtime now route those fields through the existing `FormRuntime.submit(...)` semantic boundary instead of button-local business graphs.
- Link to code paths: `packages/flux-renderers-form/src/schemas.ts`, `packages/flux-renderers-form/src/renderers/form.tsx`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-core/src/types/runtime.ts`
- Key decision: keep one semantic submit path in `FormRuntime.submit(...)`; external UI still enters through `component:submit`, while activation/request follow-ups stay form-owned and reuse the existing `Action Algebra` result-binding model.
- Next step: continue broader lifecycle-entry convergence only after this form-first baseline is fully re-verified and any remaining page/dialog follow-up scope is split into a separate plan slice.

### 2026-04-09

- Aligned `Action Algebra` runtime behavior with the current frontend-programming-model baseline: `then` now runs only for `success-class`, `onError` now runs for `failure-class`, and chained branch evaluation now exposes transient `result` / `error` / `prevResult` bindings without publishing them into ordinary scope.
- Link to code paths: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/reaction-runtime.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: reaction-triggered action evaluation and chained-action result context reuse the same transient evaluation-binding mechanism so expression reads can see reserved names while writes still go to the original business scope.
- Next step: continue the remaining top-level convergence work through semantic lifecycle entry schema ownership rather than reopening the now-closed action/runtime gaps.

### 2026-04-09

- Tightened Host Projection to DTO-style snapshot replacement across workbench hosts: `useHostScope` now replaces host snapshots, rejects writes to projected fields, spreadsheet moved off region-data leakage, and flow/report/spreadsheet host scopes no longer expose core objects.
- Link to code paths: `packages/flux-react/src/workbench/hooks.ts`, `packages/flux-react/src/workbench/hooks.test.tsx`, `packages/flow-designer-renderers/src/designer-context.ts`, `packages/report-designer-renderers/src/host-data.ts`, `packages/spreadsheet-renderers/src/page-renderer.tsx`
- Key decision: projected host fields must remain readonly snapshot data, while host-private bridge/core objects stay in React/context wiring only.
- Next step: keep protocol/docs current so future audits do not mistake old compatibility notes for current host behavior.

### 2026-04-09

- Refreshed top-level remediation planning after the runtime/host fixes: Plan 42 now reflects the actual remaining gaps, and Plan 44 was added to own semantic lifecycle entry alignment for form-first schema surfaces.
- Link to doc paths: `docs/plans/42-frontend-programming-model-alignment-remediation-plan.md`, `docs/plans/44-semantic-lifecycle-entry-alignment-plan.md`, `docs/architecture/frontend-programming-model.md`, `docs/architecture/complex-control-host-protocol.md`
- Key decision: do not keep semantic lifecycle schema ownership mixed into a generic runtime-gap remediation plan once the runtime gaps are already closed.
- Next step: implement the form-first lifecycle surface (`submitAction`, `onSubmitSuccess`, `onSubmitError`, `onValidateError`, `initAction`) under Plan 44.
