### 2026-05-04 (closure audit remediation pass)

- Reverted plans 192–200 from `completed` to `in progress` after recognizing premature closure without independent audit.
- Ran 9 independent closure audit agents; all but 199 returned FAIL due to missing focused tests and some semantic gaps.
- **Plan 192**: Added focused tests for collector try/finally, action dispatcher dispose, ComponentHandleRegistry dispose. Source/reaction cascade depth limits noted as integration-test-only gaps.
- **Plan 193**: Added security proof tests for DANGEROUS_MEMBER_KEYS (member access + computed + object literals), Object.create(null) verification, $JSON.parse sanitization (shallow + recursive), setIn/getIn dangerous path segments.
- **Plan 194**: Strengthened validateForm rejection test to assert `rule:'async'` error semantics. Added non-Error normalization test. Fixed real semantic gap: hidden field skipping now covers runtime-registration child validation path (`form-runtime-validation.ts`). Confirmed non-form validation owner design is correct (not a bug).
- **Plan 195**: Implemented array-editor focus management (add → focus new input, remove → focus previous/add button). Added 5 FieldFrame ARIA tests (role="alert", aria-required, aria-invalid, aria-describedby). Added 2 label htmlFor tests. Tree/table ARIA tests deferred (require full runtime context).
- **Plan 196**: Fixed `action-scope-and-imports.md` — removed `validate` from built-in action references. Added 3 buildUrlWithParams array/object serialization tests.
- **Plan 197**: Wired FormulaRegistry DI into `createFormulaCompiler` and `createRendererRuntime` (optional param, backward compatible). Added registry isolation test.
- **Plan 198**: Added 8-test readVisible() scope stability suite (inheritance, rapid updates, interleaved mutations, three-level chains, cache stability, isolate mode). Back button test confirmed already covered.
- **Plan 200**: Added 9 unit tests for canonical isAbortError (6 cases) and buildScopeChain (4 cases).
- Key decision: source/reaction cascade depth limits and tree/table ARIA tests deferred to integration testing (unit-level scaffolding prohibitive).
- Updated plan guide rule 18: checklist打勾是closure前置条件.
- Full verification: typecheck ✅ build ✅ lint ✅ test ✅ (48/48 turbo tasks).

### 2026-05-04

- Continued the surface-family doc convergence pass so active owner docs now treat `dialog` / `drawer` as public DSL over one shared surface runtime instead of as a long-term dual runtime model.
- Link to doc paths: `docs/architecture/surface-owner.md`, `docs/architecture/action-algebra-formal-spec.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/action-scope-and-imports.md`, `docs/architecture/flux-core.md`, `docs/references/action-payload-matrix.md`, `docs/references/flux-json-conventions.md`
- Key decision: formal built-in surface actions are `openDialog`, `openDrawer`, and `closeSurface`; `dialog` / `drawer` open aliases and `closeDialog` / `closeDrawer` close aliases are compatibility-only and should not remain the active authoring baseline.
- Context: this removes lingering contradictions where some docs still described `surfaceRuntime` as managed-only or still taught `closeDialog` as the normal close contract.
- Next step: finish the remaining active-doc sweep for stale examples/references that still mention the old close aliases as normative behavior.

### 2026-05-04

- Executed plans 192–200 (9 plans, ~50 code changes across 30+ files). All changes pass full verification: `pnpm typecheck` ✅ `pnpm build` ✅ `pnpm lint` ✅ `pnpm test` ✅ (25 packages).
  - **Plan 192** (runtime/compiler residuals): evaluate.ts collector try/finally, submit-flow full-scope try/finally, action dispatcher `dispose()`, ComponentHandleRegistry `dispose()`, compiler diagnostics configurable via `CompileNodeOptions.diagnostics`, source cascade depth limit (100), reaction cascade depth limit (200), withRetry failure counting fix.
  - **Plan 193** (evaluator security): `DANGEROUS_MEMBER_KEYS` blocklist, `Object.create(null)` for object literals, `instanceof` blocked, `deepSanitize` for `$JSON.parse`, recursive `sanitizeSnapshot`, `DANGEROUS_PATH_SEGMENTS` guard in `setIn`/`getIn`.
  - **Plan 194** (form safety): rejected validator → `rule:'async'` error, abort controllers aborted before clear, non-Error normalization, surface cleanup writes `undefined`.
  - **Plan 195** (a11y): field-frame aria-describedby/invalid/required, error role="alert", label.tsx → `<label>`, condition-builder/array-editor/key-value aria-labels, tree ARIA roles, table aria-sort, loading overlay role="status".
  - **Plan 196** (interface alignment): `RendererHookApi` 6 hooks aligned, `settledError` in `ActionResult`, `XUI_ACTIONS_NAMESPACE` deduped, `buildUrlWithParams` array/object fix.
  - **Plan 197** (architecture): `createFormulaRegistry()` factory pattern, global wrappers preserved, `tsconfig.base.json` cleanup.
  - **Plan 198** (renderer/workbench): report inspector selector narrowed + shallowEqual, node-frame-wrapper meta passthrough, report-field-panel.css → CSS variables.
  - **Plan 199** (docs/verification): active doc broken refs fixed (5 docs), spreadsheet interaction test file created.
  - **Plan 200** (duplicate convergence): Phase 1: `resolveGap`/`GAP_TOKENS` removed from renderers-basic. Phase 2: `isAbortError` + `buildScopeChain` canonical in `flux-core/runtime-inspection.ts`, `NopScopeChainEntry` → deprecated alias.
- Key decisions: `instanceof` fully blocked (not filtered); validator crash treated as `rule:'async'` error; formula registry factory pattern with backward-compatible global wrappers; `ignoreDeprecations: "6.0"` kept in tsconfig.base.json.
- All 9 plan files updated to `status: completed`.
- Next step: independent closure audit before final sign-off.

- Added `frameClassName` as a new meta field to the schema/compiler/runtime/renderer pipeline. `className` now exclusively targets the renderer component (control), `frameClassName` targets the FieldFrame wrapper (`.nop-field`). This is a clean break from the previous behavior where `className` was passed to the FieldFrame.
  - Files: `flux-core/types/schema.ts`, `flux-core/types/node-identity.ts`, `flux-core/types/renderer-compiler.ts`, `flux-core/constants.ts`, `flux-compiler/schema-compiler/fields.ts`, `flux-runtime/node-runtime.ts`, `flux-react/node-frame-wrapper.tsx`, `flux-renderers-form-advanced/variant-field/variant-field.tsx`
  - Key decision: clean break — no backward compat fallback. Schemas that intend to style the FieldFrame wrapper must use `frameClassName` instead of `className`.
  - Fixed `sanitizeSnapshot` regression: recursive sanitization was eagerly creating new plain objects for any scope data containing nested objects, which broke prototype-chain-based scope inheritance in `readVisible()`. Fix: only create a new object when dangerous keys are actually present.
  - Updated `docs/architecture/styling-system.md` with the `frameClassName` vs `className` distinction.

- Merged `Validation Checklist` into `Closure Gates` in `docs/plans/00-plan-authoring-and-execution-guide.md`. The two sections had significant overlap (pnpm four-pack, doc sync, focused verification) — now consolidated into one section that serves both planning-time closure definition and final validation checklist.
- Link to doc path: `docs/plans/00-plan-authoring-and-execution-guide.md`
- Key decision: Closure Gates + Validation Checklist described the same completion conditions from two angles; merging eliminates redundancy without losing governance coverage. Updated all cross-references (Minimum Rules rule 4, When Closing The Plan step 3, Closure Audit Rule rule 6).
- Next step: new plans should use the merged `Closure Gates` section; existing active plans don't need retroactive restructuring.

- Ran the closure audit for `docs/plans/192-199` against `docs/analysis/2026-05-04-deep-audit-full/summary.md` and the retained adversarial findings, then tightened owner coverage where the rewritten plans still drifted.
- Link to doc paths: `docs/plans/194-form-submit-validation-timing-and-lifecycle-safety-plan.md`, `docs/plans/196-interface-contract-alignment-and-api-hygiene-plan.md`, `docs/plans/199-doc-and-verification-closure-plan.md`, `docs/analysis/2026-05-04-deep-audit-full/summary.md`
- Key decision: keep `194` limited to retained submit/validation/lifecycle defects only, route active-doc broken-link ownership explicitly through `199`, and include `docs/architecture/frontend-baseline.md` in the `DA16-1` docs-closure scope instead of leaving it implicit.
- Next step: re-run one last owner-map read-through on `192-199` and only then mark the plan set as pass/fail for closure readiness.

- Updated `scripts/code-stats.mjs` to classify `apps/playground/**` as a standalone reporting bucket instead of mixing it into source files, and added a dedicated largest-playground-files section to keep source rankings focused on package/app implementation code.
- Link to code/doc paths: `scripts/code-stats.mjs`, `docs/logs/2026/05-04.md`
- Key decision: preserve the existing tracked-file / ignored-path baseline, but treat playground as example/demo surface in reporting so source-vs-test discussions no longer overcount playground code as production source.
- Follow-up: refined the package table with explicit `Pgd` and `Oth` columns so package-level stats now show source/playground/test separation without hiding total package size.
- Next step: if more consumers need machine-readable stats, add a JSON output mode rather than parsing the console table.

- Revised `docs/skills/open-ended-adversarial-review-prompt.md` to restore its original open-ended role after it drifted too close to `docs/skills/deep-audit-prompts.md`.
- Link to doc paths: `docs/skills/open-ended-adversarial-review-prompt.md`, `docs/skills/deep-audit-prompts.md`
- Key decision: keep adversarial review discovery-first and non-checklist-driven; retain only lightweight de-dup (`docs/analysis/` scan) and blind-spot reflection, while downgrading viewpoint rotation and missing-object checks to optional heuristics.
- Context: `deep-audit` already owns structured dimensions, independent re-review, and evidence calibration, so `open-ended` now explicitly avoids becoming a second light audit framework.
- Next step: if repeated confusion returns, extract a short "when to use deep-audit vs open-ended" note under `docs/skills/` or `docs/references/`.

- Completed deep audit full-6 (18 dimensions). Results: 0 P0, 0 P1, 7 P2, 3 P3, 10 Low. Output: `docs/analysis/2026-05-04-deep-audit-full-6/`
- Verified adversarial review findings against live code: 12/14 confirmed, 1 already fixed (#12 readVisible memoization), 1 partial (#13 hardcoded strings — 2/3 confirmed).
- Created consolidated remediation plan: `docs/plans/192-deep-audit-full-6-and-adversarial-review-remediation-plan.md`
- Key decisions: Phase 1 prioritizes 5 critical runtime defects (collector corruption, scope-change semantics, submitForm error swallowing, ActionDispatcher lifecycle, child registry leak). Security findings (#9) classified as watch-only given trusted schema assumption.
- Added 2 new audit dimensions to `docs/skills/deep-audit-prompts.md`: dim 19 (错误传播保真度) and dim 20 (可访问性/WCAG). Based on analysis of 6 historical adversarial reviews showing these categories account for 12+ recurring findings not covered by existing 18 dimensions.
- Upgraded `docs/skills/open-ended-adversarial-review-prompt.md`: added perspective rotation (12 viewpoints), absence detection, progressive deep-dive strategy, and blind-spot self-assessment. Designed to force novel angles on each successive review.
- Next step: Begin Phase 1 implementation starting with formula evaluator try/finally fix.
