# Development Log - 2026-07-24

### 2026-07-24

- **A6 (P6 flux-renderers-ai) — Tiptap Rich-Text Sender Extension completed**: all 4 phases of `docs/plans/2026-07-24-2200-1-a6-flux-renderers-ai-p6-tiptap-rich-text.md` executed. Closes the last P6 work item on the AI roadmap.
  - **Phase 1 — Schema contract + sender delegation + subpath scaffolding**: `AiSenderSchema` + `AiChatSchema` gained `senderExtensions?: SchemaValue` (expression resolves to `React.ComponentType<AiSenderExtensionProps>`); `AiSenderExtensionProps` interface defined in `schemas.ts` (draft value/onChange/onSubmit/onCancel/loading/placeholder/maxLength/showWordLimit/submitType/disabled/refocusAfterSubmit) and re-exported via `export type` from `src/index.ts` (compile-erased — does not pull Tiptap into the main entry runtime graph). `AiSenderView` accepts `extensionComponent?: ComponentType<AiSenderExtensionProps>`; when present, delegates the input area (root gets `data-extension=""`); when absent, the existing `<Textarea>` path runs unchanged (zero regression — all prior sender tests stay green). `AiChatRenderer` threads `senderExtensions` through to its internal `AiSenderView`. `package.json` gained the `./rich-text` subpath export (`types→dist/rich-text/index.d.ts`, `default→dist/rich-text/index.js`), `@tiptap/react`+`@tiptap/starter-kit`+`@tiptap/core` as optional peerDeps (`peerDependenciesMeta.optional=true`) + devDeps. `vite.workspace-alias.ts` + `tsconfig.base.json` got the `@nop-chaos/flux-renderers-ai/rich-text` alias/path. Created `src/rich-text/{index.ts,types.ts}` (type-only entry for Phase 1). Extended `contract-honesty.test.ts` with a `@tiptap` import scan over `src/engine/`+`src/renderers/`+`src/adapters/` (bundle-isolation guard — INV-1 invariant). Phase 1 Proof: `renderers/__tests__/ai-sender-extensions.test.tsx` (stub component via `extensionComponent` + Textarea fallback).
  - **Phase 2 — Tiptap editor + plain-text serialization + submit/abort**: `src/rich-text/tiptap-sender.tsx` implements the concrete editor (`useEditor`+`EditorContent`+`StarterKit`, mirroring `flux-renderers-form-advanced/editor-renderer.tsx`). Plain-text extraction via `editor.getText()` → `onChange(text)`/`onSubmit()` (engine contract `sendMessage(string)` unchanged). Custom submit keymap extension (`aiSenderSubmitKeymap`, priority 1000) maps Enter/Ctrl+Enter/Shift+Enter to the `submitType` mode (reads latest via a stable `callbacksRef` — mirrors the `handlersRef` pattern; deferred ref reads satisfy `react-hooks/refs`). Placeholder via `editorProps.attributes` + CSS `data-placeholder:empty::before` (no `@tiptap/extension-placeholder` dep). `loading`/`disabled` → `editor.setEditable(false)`. External clear (parent `value=''` while unfocused) → `setContent('', {emitUpdate:false})`. `index.ts` switched from type-only stub to runtime `createTiptapSender(options)` factory. Added `onReady` callback to `TiptapSenderOptions` (host imperative integration + test seam). Proof: `rich-text/__tests__/tiptap-sender.test.tsx` (8 tests: surface render, initial-value round-trip, onChange plain-text, Enter submit, Shift+Enter no-submit, loading disables, external clear, Ctrl+Enter).
  - **Phase 3 — built-in @mention / template / slash extensions**: `src/rich-text/extensions/{mention,template,slash-command}.ts` — trigger detection (`detectMentionQuery`/`detectSlashQuery` scan the text before the caret for `@word` / `/word`), data-source filtering (`filterMentions`/`filterSlashCommands`), insertion primitives (`insertMention`/`insertTemplate`/`runSlashCommand`). Integrated into `tiptap-sender.tsx`: `onSelectionUpdate` detects triggers → opens a filtered `SuggestionPopup` (anchored below the editor, `@nop-chaos/ui` `Button` items, keyboard nav via editor `handleKeyDown` intercepting Arrow/Enter/Escape when popup active). `TemplateBar` renders clickable template buttons. Graceful degradation (`extension-data-missing`): empty data source → no popup. All three extensions opt-in via `TiptapSenderOptions.extensions: ('mention'|'template'|'slash')[]`; empty/omitted → StarterKit only. Proof: `rich-text/__tests__/tiptap-extensions.test.tsx` (10 tests: template bar render + insertion, @ mention popup + filtering + insertion as `@label`, /slash popup + insertText, extension-data-missing no-popup, empty-extensions StarterKit-only).
  - **Phase 4 — playground + e2e + owner-doc sync**: `apps/playground/src/pages/ai-rich-text-demo.tsx` + `ai/ai-rich-text-example.json` — host creates `createTiptapSender({extensions:['mention','template','slash'], mentions, templates, slashCommands})`, exposes it via augmented `createExpressionHelpers` in the `ai` namespace; schema binds `senderExtensions: '${$ai.tiptapSender}'` on `ai-chat`. Route `#/ai-rich-text` registered in `App.tsx` + `route-model.ts`. e2e `tests/e2e/ai-rich-text-sender.spec.ts` (5 tests: Tiptap surface renders with `data-extension`, template bar + insertion, @ mention popup + select→`@label`, /slash popup, submit→plain-text in message list). Docs synced: `design.md` §3 (Tiptap non-goal → "已落地"), §5.1 (P6 row ✅ + status line), §6 (`src/rich-text/` subtree + bundle-isolation note), §10.6 (new subsection: senderExtensions field + subpath injection + contract invariants + Failure Paths); `renderers.md` §4.1/§4.2 (`senderExtensions` field + `AiSenderExtensionProps` interface + `createTiptapSender` usage); `roadmap-ai.md` A6 → `done`; `docs/components/index.md` P6 ✅.
  - **Verification**: `flux-renderers-ai` 274 unit tests (30 files) pass; `flux-playground` typecheck green; full workspace typecheck + build + lint green; e2e pending Playwright run.

### 2026-07-24

- **ai-chat external-engine injection (A4 deferred follow-up) completed**: all 4 phases of `docs/plans/2026-07-24-0751-1-ai-chat-external-engine-injection.md` executed. Closes the "ai-chat ↔ useConversation 引擎统一" deferred item from the A4 plan.
  - **Phase 1 — public `useEngineView` hook**: extracted the external-engine binding (formerly a private helper in `ai-persistence-demo.tsx:135-155`) into `packages/flux-renderers-ai/src/adapters/use-engine-view.ts` (binds an existing `MessageEngine` via `useSyncExternalStore`, returns the full view incl. `send`). Refactored `use-message.ts` to reuse `useEngineView` internally and added an `engine?: MessageEngine | null` option (rules-of-hooks safe — the self-built engine is always created via lazy `useState`; the condition falls on the value, not the call). Exported `useEngineView` + `UseEngineViewReturn` from `index.ts` (Group 3 host utilities). Proof: `adapters/__tests__/use-engine-view.test.ts`.
  - **Phase 2 — `ai-chat.engine` prop**: `AiChatSchema` gained `engine?: SchemaValue` (resolved prop, symmetric with `connector`/`conversationController`; not a reactive scope snapshot so §11.5 line 467 is honoured). `ai-chat.tsx` duck-types `resolved.engine`; valid → bind external engine; `engine-prop-not-engine` (non-engine value) → `console.warn` + self-built fallback; `engine-null-switch` (`activeEngine` null mid-switch) → render emptyState (`flux.ai.selectConversation` i18n key added). Connector hot-swap now guarded to the self-built engine only (external engines own their connector). ActionScope namespace `ai` + ComponentHandle bind to whichever engine wins. Proof: `renderers/__tests__/ai-chat-external-engine.test.tsx` (5 tests: external bind, send routing, self-built regression, non-engine fallback+warn, null-switch emptyState).
  - **Phase 3 — persistence demo via `ai-chat` + e2e**: `apps/playground/src/pages/ai-persistence-demo.tsx` now renders the chat surface through `SchemaRenderer` with `engine: "${engine}"` bound to `conversations.activeEngine` (removed the manual `AiChatProvider`+`AiMessageListView`+`AiSenderView` assembly + the private `useEngineView`). Sidebar stays React (drives `conversations.create/switch/delete` directly). Schema: `apps/playground/src/ai/ai-persistence-example.json`. e2e `tests/e2e/ai-persistence.spec.ts` rewritten to assert the `nop-ai-chat` marker (refresh-recovery + conversation-switch re-hydration, both through `ai-chat`).
  - **Pre-existing bug discovered (out of scope, not fixed)**: `normalizeActionEvent` (`packages/flux-react/src/renderer-helpers.ts`) drops custom event payloads that lack a `type` string, so `${event.id}` resolves to undefined for custom-payload events (`onItemClick`/`onItemDelete`/`onSelect` …) across all AI renderers. The persistence demo sidesteps it by keeping the sidebar in React (direct manager calls). Needs its own bug-fix plan with regression tests.
  - **Docs synced**: `design.md` §11.2 (engine data-flow row) + §11.5 (engine-instance row note: injectable via `engine` prop, resolved value not scope snapshot); `renderers.md` §1.2/§1.4 (`engine` prop + external-engine injection notes); `roadmap-ai.md` (Follow-ups 已收口: ai-chat ↔ useConversation 引擎统一 ✅).
  - **Verification**: `flux-renderers-ai` 249 unit tests (27 files) pass; `flux-playground` 142 unit tests pass; full workspace `pnpm typecheck` (58 tasks) green; 10 AI e2e green (incl. 2 rewritten persistence specs).

### 2026-07-24

- **A5 (P4 flux-renderers-ai) completed**: all 5 phases of `docs/plans/2026-07-24-1800-2-a5-flux-renderers-ai-p4-advanced-integration.md` executed.
  - **Phase 1 — 平台联动 (two Decisions)**: Decision-A (messages→form) adjudicated **path (a) host paradigm** — host calls Layer C `component:getMessages` then **serializes (deep-copy)** before `setValue`; `engine.messages` stays domain-internal (INV-17 kept — the live ref the engine returns would otherwise be mutated). Decision-B (data-source linkage) adjudicated **path (a) no new method** — `onResponseComplete` payload already carries the last message so the host wires `data-source:reload/insert` directly off the event. Both conclusions recorded in `design.md` §3 (A) / §11.2 (B). Proof: `packages/flux-renderers-ai/src/renderers/__tests__/phase4-platform-linkage.test.tsx` (getMessages serialization preserves engine ownership; event drives a data-source reload).
  - **Phase 2 — `ai-voice-input` (A-15)**: new Widget `src/renderers/ai-voice-input.tsx` (`nop-ai-voice-input`, `data-slot`, `data-state=idle|listening`, `data-unsupported`). `SpeechRecognition`/`webkitSpeechRecognition` called **directly** (INV-1 adjudication — mic input is user-gesture browser API, not network IO; `contract-honesty.test.ts` FORBIDDEN_GLOBAL_IO scan does not flag it). CSS waveform (`styles.css` `@keyframes flux-ai-voice-wave`, zero deps). Three Failure Paths: `voice-unsupported` (mount-detect → disabled button + Tooltip + `onError('unsupported')`), `voice-permission-denied`, `voice-no-result`. Detection via `useState` initializer + ref-guarded one-shot onError (avoided `react-hooks/exhaustive-deps` disable which trips React Compiler).
  - **Phase 3 — message branches (A-16)**: `engine.regenerate(branchId?)` added (`engine/types.ts` + `create-engine.ts`) — drops the trailing assistant turn (back to last user) and re-runs the request, stamping `metadata.branchId` on the produced assistant message (incrementing `branch-N` or host-supplied); engine stores NO branch set (host owns full history). Exposed via Layer C `component:regenerate`. `AiBubbleView` renders a prev/next + counter picker (`ai-bubble-branches`/`-branch-prev`/`-counter`/`-next`) when the message id is in the host-injected `branches` set; `branch-no-host-data` → omitted. Threaded through `AiChatContext` (`ai-chat` schema `branches`/`activeBranchId`/`onBranchChange`).
  - **Phase 4 — `ai-token-usage` (A-17) + `ai-suggestions`**: `ai-token-usage` reads `message.metadata.usage` (explicit prop > metadata), renders an SVG context-limit ring + prompt/completion/cost counts; `token-no-usage` → muted placeholder. `ai-suggestions` renders suggestion pills with `overflowMode` expand/scroll/popover (+`N` Popover collapse beyond `maxVisible`) + `onSelect`.
  - **Phase 5 — playground + e2e + docs**:
    - 2 playground pages + routes (`ai-p4`, `ai-linkage`): schema-driven voice/token/suggestions; engine-driven branches (`engine.regenerate` + host `setMessages` on `onBranchChange`) + Decision-A/B linkage (messages → form-field view + data-source reload counter off `requestState==='completed'`). Used `createReactMessageAdapter` so `useSyncExternalStore` sees a stable snapshot (native adapter returns a new object each `getState`).
    - e2e: `ai-p4-widgets.spec.ts` (token counts + ring, suggestions expand + popover `+3`, voice marker), `ai-branches-linkage.spec.ts` (regenerate stamps `branch-1`, picker `2/2`→prev`1/2`, Decision-A form field + Decision-B reload counter). Also fixed pre-existing `playground-entry-pages` ROUTE_ASSERTIONS gaps (added `ai-persistence`/`ai-citations`/`ai-hitl` + the two new routes so the coverage-equality test passes).
  - **Verification (full-green)**: `flux-renderers-ai` 241 unit tests (25 files) pass; full workspace `pnpm typecheck` (58) + `pnpm build` + `pnpm lint` (31) + `pnpm test` (58 unit tasks) green; 60 `playground-entry-pages` e2e green + 6 new P4 e2e green + all 15 AI e2e green.
  - **Docs synced**: `design.md` §5.1 (P4 four rows ✅) + §3/§11.2 (two Decisions); `renderers.md` (§3.1b branches + §11 suggestions + §11b voice + §11c token-usage); `docs/components/index.md`; `roadmap-ai.md` A5 → `done` + Renderer Coverage P4 ✅.

### 2026-07-24

- **A4 (P3 flux-renderers-ai) completed**: all 4 phases of `docs/plans/2026-07-24-1800-1-a4-flux-renderers-ai-p3-persistence-citations-hitl.md` executed.
  - **Phase 1 — useConversation storage sync (real persistence)**: rewrote `packages/flux-renderers-ai/src/adapters/use-conversation.ts` — mount bootstrap calls `storage.loadConversations()` (seeds list + selects first active); `switchConversation` re-hydrates via A3's `engine.setMessages(await loadMessages(id))` (replaced the hollow 111-121 dead code); turn-complete auto-save subscribes to engine `requestState` at `buildEngine` time (`processing`→`completed|aborted|error` → `storage.saveMessages(id, snapshot)`), with subscription teardown on evict/unmount. Storage failures non-fatal (`storage-load-error` / `storage-save-error`, `console.warn`, no throw). `storage` stays **optional** (live type wins over `design.md` §11.3 "必填" drift — Phase 4 doc-sync corrected).
  - **Phase 1 INV-1 guard (Major-3)**: extended `src/__tests__/contract-honesty.test.ts` FORBIDDEN_GLOBAL_IO scan to cover `src/adapters/` (was engine/renderers only) — `use-conversation.ts` touches no `localStorage`/`fetch`/`IndexedDB` directly.
  - **Phase 2 — `ai-citations` (A-13, Decision-C)**: new Widget `src/renderers/ai-citations.tsx` (`nop-ai-citations`, `data-slot="ai-citations"`, per-marker `data-citation-index`). Parses `[N]`/`[N,M]` from `message.content` → hoverable `<sup>` opening a `@nop-chaos/ui` `Popover` source card. Independent-widget composition (host places in an `ai-chat` region; `ai-bubble` `[1]` stays literal — no overlap; in-bubble inline variant out-of-scope). Sources resolve `sources` prop > `metadata.sources` > `data-sources` part. Security: sanitize via shared `sanitizeHtml` then controlled-React rendering (never `dangerouslySetInnerHTML` — XSS-safe). Registered in `schemas.ts`/`ai-renderer-definitions.ts`/`index.ts`.
  - **Phase 3 — HITL approval (A-14)**: `ai-tool-call.tsx` reads `state.approval`; `pending` renders approve/reject buttons + `data-requires-approval=""` + a focus trap (Tab cycles approve↔reject, Esc restores prior focus, approve auto-focused); `approved`/`rejected` render a decided badge (A-12 palette). `onApproval` event payload `{ action, toolCall, toolCallId }`; engine only holds the field (no pause/resume). `hitl-no-handler` is a no-op.
  - **Phase 4 — host + playground + e2e + docs**:
    - Host `apps/playground/src/ai/local-storage-storage.ts` (`createLocalStorageStorage`, `structuredClone` deep-copy, host-side not in package — INV-13).
    - 3 playground pages + routes: `ai-persistence-demo` (`useConversation({storage, autoSaveMessages})` bound to `activeEngine` via `AiMessageListView`+`AiSenderView` — single engine → real save/re-hydrate; exported `AiMessageListView`/`AiSenderView` from the package for host composition), `ai-citations-demo` (sample sourced message + inline/list), `ai-hitl-demo` (host-managed approval state via `AiToolCallView`).
    - **A2 `${controller}` follow-up**: the `ai-chat` renderer owns its own engine (independent of `useConversation`), so unifying it with the conversation manager is a host-level integration larger than this plan; the persistence demo sidesteps the dual-engine split by binding `ai-message-list`+`ai-sender` directly to `activeEngine`. Tracked as a non-blocking follow-up.
    - e2e: `ai-persistence` (send → wait turn-complete → reload → conversation + `persist-me` bubble recover via auto-rehydrate), `ai-citations` (4 markers + Popover source card + list mode), `ai-hitl` (approve→badge+log, reject→badge+log). Diagnosed via `page.evaluate` only (no screenshots); root-caused two flakes (stale dev-server HMR after edits; reloading before turn-complete skipped autoSave).
  - **Verification (full-green)**: `flux-renderers-ai` 200 unit tests (19 files) pass; full workspace `pnpm typecheck` (58 tasks) + `pnpm build` (31) + `pnpm lint` (31) + `pnpm test` (58 unit tasks) green; 17 AI e2e green (12 existing + 5 new: persistence refresh-recovery, citations hover+list, HITL approve/reject).
  - **Docs synced**: `design.md` §5.1 (P3 rows ✅) + §11.3 (storage "必填"→optional drift corrected, Major-2); `renderers.md` (§10 ai-tool-call HITL + new §10b ai-citations + events table); `docs/components/index.md`; `roadmap-ai.md` A4 → `done`.

### 2026-07-24

- **A3 (P2 flux-renderers-ai) completed**: all 6 phases of `docs/plans/2026-07-24-1400-1-a3-flux-renderers-ai-p2-tools-attachments-deepening.md` executed (Phases 1-3 were already done by a prior run; Phases 4-6 executed this session).
  - **Phase 4 — Layer C ComponentHandle**: live `useCurrentComponentRegistry()` + `ComponentCapabilities.invoke(method, payload, ctx)` dispatch model was already wired (`adapters/ai-component-handle.ts`, `renderers/ai-chat.tsx`). Added lifecycle + `component:sendMessage` action-routing tests in `renderers/__tests__/ai-namespace.test.tsx`. Corrected `design.md` §11.1 Layer C to match live API (was stale `runtime.componentRegistry?.register` + flat methods).
  - **Phase 5 — renderer deepening**:
    - **A-9**: `useAutoScroll` contract finalized (`isPinned`→`isAtBottom`); `ai-message-list` shares the hook.
    - **A-8**: virtual scroll via `@tanstack/react-virtual` when `messages.length > 200` (added dep `^3.13.24`); inlined the virtualizer in `AiMessageListView` (hook + scroll ref same component) after a cold-start fix.
    - **A-10**: reasoning duration — `thinkingPlugin` records `startedAt`/`endedAt`; `reasoning` renderer shows "Thought for Xs".
    - **A-11**: streaming cursor `▍` (CSS blink in `styles.css`) appended to markdown while `message.loading`.
    - **Message editing (§4.7)**: `ai-bubble/user-edit.tsx` — user-message edit affordance; resubmit truncates the conversation at the edited message and re-sends.
    - **Decision A-7 Streamdown**: NOT introduced (path C buffer sufficient); recorded in `design.md` §10.4 + plan Deferred.
    - **Decision LaTeX/KaTeX**: NOT built-in (~20KB); host injects via custom `BubbleContentRenderer`; recorded in `design.md` §10.4 + plan Deferred.
    - **Bug fix**: `defaultBubbleContentRenderers` markdown matcher now requires non-empty content — previously empty-string `content` shadowed the tools/reasoning matchers (round-1 tool_calls assistant rendered empty). New focused tests in `renderers/__tests__/phase5-deepening.test.tsx` (11 tests).
  - **Phase 6 — host + playground + e2e + docs**:
    - Playground: `ai-tools-demo` (mock tool_calls stream + `toolExecutor` + `tools` schema injection via `useMessage`), `ai-attachments-demo`, `ai-component-handle-demo`, `ai-virtual-scroll-demo` (1000 seeded messages). `ai-chat` now forwards `tools`/`toolExecutor`/`maxToolRounds`. New host helper `apps/playground/src/ai/tool-mock.ts`; `mock-ai-env.ts` import loader exposes `tools`/`toolExecutor`. 4 routes registered.
    - e2e: `ai-tools` (tool_calls→executor→follow-up reply + `data-tool-status`), `ai-attachments` (upload/preview/remove + maxFiles validation), `ai-component-handle` (`component:sendMessage`), `ai-virtual-scroll` (DOM bubble count < 100 via `page.evaluate`). Fixed the route-coverage gate (`playground-entry-pages.spec.ts`): added missing assertions for `env-stream`/`ai-chat`/`ai-conversations` + the 4 new AI routes + a pre-existing `gantt-perf-scale` gap. Diagnosed via `page.evaluate` only (no screenshots).
  - **Verification**: `flux-renderers-ai` 167 unit tests (17 files) pass; full workspace `pnpm typecheck` (58 tasks) + `pnpm lint` (31) + `pnpm test` (58) green; 12 AI e2e pass + 7 AI route smoke tests + route-coverage gate green.
  - **Docs synced**: `design.md` §5.1 (P2 renderers ✅ + A-6~A-12), §10.4 (A-7/LaTeX decisions), §11.1 Layer C (live API); `docs/components/index.md`; `roadmap-ai.md` A3 → `done`.
- **A2 (P1 flux-renderers-ai) completed**: all 5 phases of `docs/plans/2026-07-23-2143-3-a2-flux-renderers-ai-p1-conversations-markdown.md` executed.
  - **Phase 1 — ActionScope namespace `ai` (Layer B)**: `ai-chat` registers via `useCurrentActionScope()` + `useNamespaceRegistration(actionScope, 'ai', provider)`. Provider implements 7 actions (`send`/`abort`/`clear`/`createConversation`/`switchConversation`/`deleteConversation`/`renameConversation`). Engine state projected to host scope via `useHostScope({ isProcessing, messages, activeConversationId }, path, 'ai')` so descendants read reactively via `useScopeSelector`. Capability check: no ActionScope → no-op, no crash.
    - New: `src/adapters/ai-action-provider.ts`, `src/adapters/ai-conversation-controller.ts`, `src/renderers/__tests__/ai-namespace.test.tsx`, `src/ai-test-support.tsx`.
    - Engine gained `clear()` method (`src/engine/types.ts`, `src/engine/create-engine.ts`).
  - **Phase 2 — A-2 streaming Markdown buffer + A-3 code copy + A-1 data part**: `src/renderers/ai-bubble/markdown-buffer.ts` (pure string processor: UTF-16 surrogate hold-back + code-fence/`$$`/`\(` balance). `MarkdownContentRenderer` wraps `react-markdown` with copy button on fenced code blocks (`clipboardAdapter`). New `DataPartContentRenderer` for `data-${string}` content parts.
  - **Phase 3 — 4 P1 renderers + A-4 timestamp + A-5 error state + `useConversation`**: `ai-conversations`/`ai-welcome`/`ai-prompts`/`ai-feedback` renderers. `src/adapters/use-conversation.ts` (double-layer model: conversations array + engines Map, lazy creation, evict non-processing on switch, keep streaming in background). Timestamp + error renderers in `ai-bubble/renderers/`.
  - **Phase 4 — a11y baseline**: `ai-message-list` root `role="log"` + `aria-live="polite"` + `aria-busy` (presence-only). `ai-sender` refocuses textarea after submit via `requestAnimationFrame`.
  - **Phase 5 — host connector + playground + e2e**: `apps/playground/src/ai/openai-connector.ts` (OpenAI/DeepSeek compatible, reads `VITE_OPENAI_API_KEY`/`VITE_DEEPSEEK_API_KEY`). `apps/playground/src/pages/ai-conversations-demo.tsx` + route `#/ai-conversations`. 4 e2e tests in `tests/e2e/ai-conversations.spec.ts`.
  - **Verification**: 104 unit tests pass (12 files), 4 e2e pass, `pnpm typecheck` (58 tasks) + `pnpm build` + `pnpm lint` all green.
  - **Docs synced**: `design.md` §5.1 (P1 renderers ✅), §11.1 Layer B (live API: `useCurrentActionScope` + `useNamespaceRegistration`); `roadmap-ai.md` A2 → `done`; `docs/components/index.md` updated.
  - **Known follow-up**: `design.md` §11.1 stale `runtime.actionScope?.registerNamespace` phrasing fully corrected in-place. Playground demo `${controller}` expression binding for `conversationController` has a timing issue in the full state cycle; renderer-package unit tests prove the controller wiring works — host-level integration pass deferred to A3.
- Closure audit gate left unchecked per collaboration discipline (executor must not self-audit). A fresh sub-agent session should run the closure audit.
