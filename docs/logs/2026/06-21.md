# Development Log - 2026-06-21

### 2026-06-21 (X5 Flux 决策表 — Phase 1 审计与规范)

- **X5 design.md Flux 决策表 — Phase 1（审计与格式规范裁定）完成。** Plan: `docs/plans/2026-06-21-0255-x5-flux-decision-tables-plan.md`。纯文档计划，无代码变更。X3（`docs/references/naming-conventions.md`）已 `done`，硬依赖满足。
- **审计结果**：14 个 P0/P1 组件 design.md **当前都已有** `### Flux 决策表` 节，但**全部**仍是 3 列窄表 `能力 | 决定 | 理由`（"决定"列混合"实现/计划实现/暂不实现/不采纳"）。需统一翻转为 4 列 `能力 | 采纳 | 不采纳 | 理由`。其中：
  - `input-email` / `input-password` 当前**无独立表**（§2 文字 deferral 到 input-text）—— Phase 3 需补独立小 4 列表覆盖各自特化面（email validator / password reveal）。
  - `crud` / `input-tree` / `tree-select` 经 E0b/E0c 扩面后**已覆盖全能力面**（不止漂移字段），Phase 2/3 仅翻转 4 列、不扩面。
- **Flux 决策表规范（4 列蓝图）写入 plan Phase 1 输出 B**：列定义（能力 / 采纳 / 不采纳 / 理由）、行粒度（一个 E1/E2 工作项最小决策单元 = 至少一行；amis 不采纳项归到最相关行）、特殊符号（`—` em dash 表空；`(E1a)` 标计划工作项）、能力覆盖范围（analysis §1.2/§3 全部能力 + analysis §5/X3 §3 该组件相关全部不采纳）、命名对齐 X3 规则、input-email/password 特化组件沿用 input-text 共享面 + 独立小表只列差异。
- **owner-doc**：Phase 1 仅裁定，**No owner-doc update required**（裁定结果驱动 Phase 2-3）。

### 2026-06-21 (X5 Flux 决策表 — Phase 2 P0 组件)

- **X5 Phase 2（P0 组件 select/table/crud 决策表 3 列 → 4 列翻转）完成。** 纯文档变更，无代码改动。
- **3 个 P0 design.md 决策表统一翻转为 4 列** `能力 | 采纳 | 不采纳 | 理由`：
  - `select/design.md` §2 Flux 决策表：12 行覆盖基线（单选+静态/异步 options+placeholder/disabled/readOnly/required）+ E1a 全部能力（searchable/multiple/clearable/虚拟滚动/分组/option 模板/远程异步搜索）+ 暂不实现（creatable/editable/removable）+ 不采纳（amis selectMode 多模式/值编码 valueField-joinValues-extractValue-delimiter-simpleValue/皮肤 borderMode-overlay-mobileUI-hideSelected-showInvalidMatch/checkAll 归 checkbox-group）。命名对齐 X3 §2（`{label,value}`、`searchable`、`clearable`）+ §3 amis 不采纳清单。
  - `table/design.md` §2 Flux 决策表：28 行覆盖当前基线 11 行（列定义/分页/选择/固定列/expandable/排序/search-filter/虚拟滚动/quick edit/columnSettings/responsive/empty+loading+stripe+bordered/事件+句柄）+ E1b 计划 4 行（resize/sticky header/聚合行/单元格合并）+ E1c 计划 6 行（树表/行拖拽/多列排序/多级表头/copyable/popOver）+ 暂不实现 4 行（showIndex/itemActions/autoFillHeight/lazyRenderAfter）+ 不采纳 3 行（export Excel/CSV、amis rowClassNameExpr、amis tableLayout）。
  - `crud/design.md` §2 Flux 决策表：26 行覆盖 source/queryForm/toolbar/listActions/$crud/clientMode/quickSave/autoClear/events/ownership 基线 10 行 + E0c 收口 3 行（keepOnPageChange 实现/maxSelectionLength 实现/checkableWhen 实现）+ E0c 删字段 1 行（maxKeepSelectionLength 不采纳）+ E1d 计划 4 行（轮询/折叠查询/无限滚动/cards-list 依赖主 roadmap W1c/W2a）+ 暂不实现 4 行 + 不采纳 3 行（syncLocation/amis filter as dialog-drawer/export）。
- **覆盖核对**：每个表的"能力"行覆盖 analysis §1.2/§3 对应组件的全部能力 + analysis §5/X3 §3 中该组件相关的全部不采纳项，无遗漏。
- **owner-doc 更新**：3 个 P0 design.md 即 owner-doc 本身，已更新。
- **验证**：纯文档计划，依 plan-authoring-guide 不跑 `pnpm typecheck`/`build`/`lint`/`test`；anchor 前置脚本 `check-active-doc-code-anchors.mjs` 通过（233 active docs，无断链）。

### 2026-06-21 (X5 Flux 决策表 — Phase 3 P1 组件)

- **X5 Phase 3（P1 组件 11 个决策表 3 列 → 4 列翻转 + input-email/input-password 新增独立小表）完成。** 纯文档变更，无代码改动。
- **11 个 P1 design.md 决策表统一翻转为 4 列** `能力 | 采纳 | 不采纳 | 理由`：
  - `input-tree/design.md` §2：15 行覆盖 treeMode 三模式/options source/自定义 keys/本地 searchable/showPathLabel/onlyLeaf/全键盘/cascade+indeterminate（E0b 收口）+ showIcon/showOutline 删字段（不采纳）+ E2d 计划（异步懒加载/远程搜索/虚拟滚动）+ 暂不实现（节点 CRUD/nodeBehavior/enableNodePath）+ 不采纳（amis api/mobileUI）。
  - `tree-select/design.md` §2：13 行覆盖 popover 触发/treeMode/onlyLeaf/showPathLabel/自定义 keys/clearable/placeholder/searchable/cascade+indeterminate + showIcon 删字段（不采纳）+ E2d 计划 + 暂不实现 + 不采纳（amis api/mobileUI）。
  - `form/design.md` §2：21 行覆盖 body/actions/data/mode/statusPath/hiddenFieldPolicy/events/handles 基线 + E2g 计划（columnCount/inline/submitOnChange/preventEnterSubmit/autoFocus/scrollToFirstError/static/rules）+ 不采纳（组件级 api/wizard/persistData/promptPageLeave/redirect/debug）。
  - `dialog/design.md` §2：20 行覆盖 regions/data/open/events/surface 基线 + E2f 计划（closeOnEsc/size/width-height/header-footer region/confirm/showCloseButton）+ 暂不实现（draggable/allowFullscreen/dialogType/showErrorMsg-showLoading/lazyRender/动画钩子）+ 不采纳（amis msg-confirmText-cancelText-inputParams/api/mobileUI）。
  - `drawer/design.md` §2：14 行覆盖 regions/side/data/open/events/surface 基线 + E2f 计划（closeOnOutside 修不对称/closeOnEsc/size/width-height/header-footer/resizable/className）+ 不采纳（amis msg/api/mobileUI）。保留 E2f 不对称 bug 警示注。
  - `button/design.md` §2：16 行覆盖基线（label/disabled/onClick）+ variant 实现（不采纳 amis level）+ size 实现 + E2e 计划（icon/loading/tooltip/block/active）+ 不采纳（amis actionType 判别树/api/hotKey/countDown/isMenuItem/requireSelected/feedback-messages-payload/onMouseEnter-Leave）。
  - `input-text/design.md` §2：15 行覆盖字段基线 + minLength/maxLength/pattern 双重生效（实现）+ E2a 计划（prefix/suffix/clearable/trimContents/showCounter/autoComplete/nativeAutoComplete）+ 暂不实现（input-mask/transform）+ 不采纳（amis addOn/borderMode/clearValueOnEmpty）。明确声明本表是文本输入族共享面。
  - `input-email/design.md` §2：**新增独立小表** 4 行，覆盖 email 特化差异（HTML input type=email/默认 email validator/远端邮箱可用性校验走 validate.api/邮箱特化字段不采纳）。共享面引用 input-text §2。
  - `input-password/design.md` §2：**新增独立小表** 5 行，覆盖 password 特化差异（HTML input type=password/E2a-bis revealPassword 计划/密码强度暂不实现/自动生成不采纳/密码管理器专用字段不采纳）。共享面引用 input-text §2。
  - `textarea/design.md` §2：12 行覆盖多行基线/disabled-readOnly/placeholder-className-aria/events + E2b 计划（minRows-maxRows/showCounter/clearable/trimContents/原生 maxLength）+ 不采纳（amis borderMode/markdown 模式/api）。共享面引用 input-text §2。
  - `checkbox-group/design.md` §2：13 行覆盖 options `{label,value}`/source/group 级 disabled-readOnly-required 基线 + E2c 计划（checkAll+半选/max-min selected/per-option disabled）+ 暂不实现（columnsCount/optionType button/menuTpl/creatable-editable）+ 不采纳（amis 值编码/api/mobileUI）。
- **覆盖核对**：E2a–E2h 各工作项能力面均在对应组件决策表中有"采纳/不采纳+理由"行；analysis §5/X3 §3 该组件相关全部不采纳项均落位到组件级表。
- **owner-doc 更新**：11 个 P1 design.md 即 owner-doc 本身，已更新。
- **验证**：anchor 前置脚本 `check-active-doc-code-anchors.mjs` 通过（233 active docs，无断链）。

### 2026-06-21 (X5 Flux 决策表 — Phase 4 跨组件一致性)

- **X5 Phase 4（跨组件一致性与验证）完成。** 纯文档变更，无代码改动。
- **格式一致性（14/14 通过）**：全部 14 个 `### Flux 决策表` 节均以 4 列 `能力 | 采纳 | 不采纳 | 理由` 开头；无残留 3 列 `能力 | 决定 | 理由`；prelude blockquote 统一引用 X3 基线。
- **命名对齐 X3（无不一致）**：`variant` 非 amis `level`（button ✓）；`options {label,value}` 非 amis 值编码（select/checkbox-group ✓）；`clearable`/`searchable` 肯定式布尔（select/input-tree/tree-select/input-text/textarea/checkbox-group ✓）；`disabled`/`readOnly` 肯定式（多组件 ✓）；`prefix`/`suffix`/`placeholder`（input-text ✓）；`when` 统一条件渲染非 amis `visibleOn`/`hiddenOn`/`disabledOn`（crud `checkableWhen` 用 raw expression ✓）。amis 名字仅出现在"不采纳"列（grep `joinValues`/`extractValue`/`borderMode`/`mobileUI`/`actionType`/`level` 验证无泄露）。
- **不采纳项落位（analysis §5 全部回填到组件级）**：前端导出（table/crud）、组件级请求（form/crud/select/input-tree/tree-select/input-text/textarea/checkbox-group/button/dialog/drawer）、散落条件属性（crud `checkableWhen` 用 `when`）、button amis 化（button 9 行）、值编码 amis 化（select/checkbox-group）、样式 amis 化（input-text/textarea/table）、mobileUI 双实现（dialog/drawer/button/input-tree/tree-select/checkbox-group/select）、路由/持久化（crud syncLocation、form persistData/promptPageLeave/redirect）。echarts/JS 函数串/杂项重 SDK 不在 14 组件范围（P2/P3 随 E3，本计划 Non-Goals）。
- **amis-baseline-matrix.md 一致性**：本计划未改变任何 retained/not-retained 决策（14 组件均保持 `runtime`/`landed`）；**No owner-doc update required**。
- **文档锚点**：`node scripts/check-active-doc-code-anchors.mjs` 通过（233 active docs，无断链）。
- **Closure Gate 抽查**：14 个决策表覆盖全能力面（与 analysis §1.2/§3 对齐）；E0b/E0c 已补的窄表（crud/input-tree/tree-select）已扩面至全能力面；与 analysis §5/X3 §3 无矛盾。

### 2026-06-21 (X3 命名规范基线)

- **X3 命名规范基线（naming-conventions.md）— 执行完成。** Plan: `docs/plans/2026-06-21-0254-x3-naming-conventions-baseline-plan.md`。纯文档计划，无代码变更。
- **Q1 裁决 = yes**（analysis §9.1）：产出 `docs/references/naming-conventions.md` 作为 X3 依据，消除"尚未成文"悬置状态。统一命名基线是 X5（Flux 决策表）与 E1/E2 实现的硬前置。
- **`docs/references/naming-conventions.md` 成文**（从 15 行占位 stub → 完整基线），五节齐全：
  1. **命名原则** — 提炼 analysis §0.2 六条 Flux 设计原则的命名含义。
  2. **shadcn/ui 命名映射表** — 15 行映射，每行出处可查（`variant`/`size`/`options`/`clearable`/`searchable`/`disabled`/`readOnly`/`name`/`label`/`on*` 事件/region/`when`/`prefix`/`suffix`/`placeholder`），引用 `packages/ui/src/components/ui/button.tsx`、`select.tsx`、`combobox.tsx` 与 `field-binding-and-renderer-contract.md` Frozen Matrix。
  3. **amis 不采纳清单** — 迁移 analysis §5（前端导出/echarts/组件级请求/散落条件属性/button amis 化/值编码 amis 化/样式 amis 化/mobileUI/JS 函数串/路由持久化/杂项重 SDK），每条附 Flux 理由。
  4. **按字段类型的命名规则** — 布尔（肯定式）、枚举（受控词表）、选项集（`{label,value}`）、事件（`on*` vs schema `events`）、region/fragment、表达式字段（不造 `xxxExpr` 平行命名）。
  5. **新增字段审查 Checklist** — 8 条可勾选项，供 X5/E1/E2 过审。
- **Scope boundary 明确**：本文档只管 schema 属性命名；CSS 类名归 `styling-system.md`；通道归属（props/meta/regions/events）归 `field-binding-and-renderer-contract.md` Rule 10 + Frozen Matrix（互补不重复）。
- **owner-doc 同步**：`existing-components-improvement-analysis.md` §9.1 Q1 改"已裁决 = yes / 已由 X3 落地"；§10"局限"行改"已由 X3 落地"，原"尚未成文"悬置表述消除。
- **一致性验证**：`scripts/check-active-doc-code-anchors.mjs` 通过（233 active docs，无断链）；`input-number/design.md:13-31` 决策表与本基线相容（`borderMode` 拒绝对应 §3 样式 amis 化；`prefix`/`suffix`/`placeholder` 对应 §2 映射；该表是 X5 格式翻转前的范例）。
- 验证：纯文档计划，依 plan-authoring-guide 不跑 `pnpm typecheck`/`build`/`lint`/`test`；anchor 前置脚本通过。
- Next: roadmap X3 状态由 `todo` 改 `done`；等待独立 closure audit 复核。

- **Plan 446 (Form Store Debugger Bridge) — Phase 1 完成。** Plan: `docs/plans/446-form-store-debugger-bridge-plan.md`。落地 debugger automation 与 runtime-owned form-store diagnostics 之间的最小桥接，承接 plan 445 收敛的 runtime-owned capture 契约。
- **新增 runtime-owned bridge 类型**（`packages/flux-core/src/types/runtime.ts`）：`FormStoreDiagnosticsOwnerQuery`（`formId` / `formName` / `scopeId` 三种 selector）、`FormStoreDiagnosticsOwnerSummary`、`FormStoreDiagnosticsBridge`（`listOwners` + `startSession` / `stopSession` / `clearSession` / `getSnapshot`）。
- **`RendererRuntime` 新增 `getFormStoreDiagnosticsBridge?()`**（`packages/flux-core/src/types/renderer-core.ts`）：runtime 暴露的唯一 bridge 入口，debugger 不直接接触 capture。
- **bridge 实现**（`packages/flux-runtime/src/form-store-diagnostics-bridge.ts`）：owner 选择语义 = 空查询仅当 owner 数 = 1 时匹配，多 owner 必须显式 selector；所有 session 方法 forward 给底层 `FormStoreApi`，运行时无新 capture 逻辑。`runtime-factory.ts` 在 runtime 创建时一次性绑定 `ownedFormRuntimes` → bridge，并在 runtime 对象上暴露 `getFormStoreDiagnosticsBridge()`。
- **debugger automation API 扩展**（`packages/nop-debugger/src/{types,automation,controller}.ts`）：新增 `listFormStoreDiagnosticsOwners`、`startFormStoreDiagnosticsSession`、`stopFormStoreDiagnosticsSession`、`clearFormStoreDiagnosticsSession`、`getFormStoreDiagnosticsSnapshot`；controller + automation 同时暴露；runtime 缺失或未实现 bridge 时返回空/false/undefined，不抛错。
- **focused proof（必须自动化）**：
  - `packages/flux-runtime/src/__tests__/form-store-diagnostics-bridge.test.ts`（12 用例）：owner 列表、空/未知/歧义 selector 行为、start/stop/clear/snapshot 路由、selector 独立匹配、snapshot defensive copy、bounded payload forwarding；包含纯 helper (`matchFormStoreDiagnosticsOwnerForTesting` / `pickFormStoreDiagnosticsOwnerForTesting`) 的语义证明。
  - `packages/flux-runtime/src/__tests__/runtime-form-store-diagnostics-bridge.test.ts`（6 用例）：runtime 端到端，含 `bridge.listOwners` 反映 `runtime.createFormRuntime`、`start/stop/clear/snapshot` 捕获 `form.store.setValue` 的 commit、bounded retention（`maxRecentCommits: 2` → 第 3 次提交后 `droppedCommitCount=1`，`recentCommits` 长度 = 2，sequence = [2,3]）。
  - `packages/nop-debugger/src/controller-form-store-diagnostics-bridge.test.ts`（4 用例）：runtime 缺失 bridge 时所有方法返回安全默认；runtime 暴露 bridge 时 controller + automation 双侧都正确 forward 参数；snapshot 防御性 copy；automation API shape 含全部新方法。
- **owner-doc 同步**：`docs/architecture/form-store-diagnostics.md` 的 "Integration With `nop-debugger`" 改为反映 bridge 已落地（替换原 "successor ownership" 文案）；`docs/architecture/debugger-runtime.md` 的 "Automation API" 列表新增 5 个 bridge 方法，"Current store-diagnostics routing baseline" 改为反映 bridge 落地。
- **验证（全绿）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49 tasks 全过（含新增 22 用例）。（注：未运行 e2e/Playwright，故非 AGENTS.md 定义的 unit+e2e full-green。）

### 2026-06-21 (E0d Phase 1)

- **E0d condition-builder 漂移修复 — Phase 1（Q3 决策与契约固化）完成。** Plan: `docs/plans/2026-06-21-0010-e0d-condition-builder-drift-fix-plan.md`。
- **3 处漂移逐项裁定**：
  - `showIf`: **实现**。值槽 `ConditionGroupValue.if?: string` 已存在；组级 `if` 条件是规则引擎常见能力；实现成本低，符合 declarative schema 原则。`showIf: true` 时在组头部渲染 `if` 表达式输入，绑定 `value.if`；`showIf: false`/缺省不渲染；求值属消费方（E3 formula 或宿主）。
  - `selectMode: tree`/`chained`: **不采纳（整体删字段）**。list 模式已通过 `ConditionFieldGroup` 扁平化 + `searchable` 覆盖；tree/chained 边际价值低、非高频；按 §0.2 "核心已简化" 收敛 list-only 并从 `ConditionBuilderSchema` 整体删字段。
  - `formulas`/`formulaForIf`: **进 types.ts**（DESIGN-ACK-NOT-IMPL）。提供 E3 稳定契约；遵循 crud `matchFunc` 先例（声明 + deferred）；运行时静默忽略，实际求值属 E3 P2 批。最小类型 `ConditionFormulaConfig { enabled?; formula?; source? }`。
- 更新 `docs/components/condition-builder/design.md`：删除 §2 漂移注记 + "计划实现（E0d）"过渡措辞；Flux 决策表三行改为最终状态；§4 schema 示例移除 `selectMode`，新增 `ConditionFormulaConfig` 最小形状；§5 字段分类移除 `selectMode`；新增 §7.4 固化 `showIf` 最终语义。

### 2026-06-21 (E0d Phase 2)

- **E0d Phase 2（Focused Proof / Red）完成。** 新增 `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder-drift.test.tsx`（13 用例）。
- **showIf proof（实现方向，Red）**：4 用例断言 `showIf:true` 渲染 `[data-slot="condition-group-if-input"]`、填写写入 `value.if`、回显、缺省空串 —— 当前实现无该渲染，全 Red。
- **selectMode proof（删字段方向，Red）**：1 用例源码 grep `ConditionBuilderSchema` 接口块不含 `selectMode`（当前仍声明 → Red）；1 BASELINE 用例 `selectMode:'tree' as any` 无渲染效果（持续绿）。
- **formulas proof（进 types.ts 方向，Red）**：3 用例源码 grep `types.ts` 声明 `ConditionFormulaConfig` + `formulas?`/`formulaForIf?`（当前无 → Red）；1 BASELINE 用例 `formulas:{...} as any` 不崩溃、组正常渲染（DESIGN-ACK-NOT-IMPL 静默忽略，持续绿）。
- **negative baseline**：2 用例 `showIf:false`/缺省不渲染 if 输入 + conjunction 正常渲染（持续绿）。
- Note：`SchemaObject` 基类带 `[key: string]: SchemaValue` 索引签名，无法用 `@ts-expect-error` 类型层证明字段进/出 types.ts，改为源码 grep + 运行时结构验证（同 E0b 裁定）。
- 验证 Red：8 failed | 5 passed（13 tests）。5 passed 全部为 baseline/negative proof；8 failed 全部为漂移字段正向语义（drift confirmed）。

### 2026-06-21 (E0d Phase 3)

- **E0d Phase 3（实现裁定落地 / Green）完成。**
- `types.ts`：从 `ConditionBuilderSchema` 移除 `selectMode?` 字段（收敛 list-only，Phase 1 裁定）；新增 `export interface ConditionFormulaConfig { enabled?; formula?; source? }`（extends SchemaObject）+ `formulas?: ConditionFormulaConfig` / `formulaForIf?: ConditionFormulaConfig`（进 types.ts，DESIGN-ACK-NOT-IMPL）。
- `condition-group.tsx`：解构 `showIf`（缺省 false）；新增 `handleIfChange`（空输入写 `undefined`）；`showIf: true` 时在组头部行右侧渲染 `<Input data-slot="condition-group-if-input">`，绑定 `value.if`；`disabled` 透传；`showIf` 加入头部行渲染条件；if 输入 `ml-auto`、remove 按钮在 showIf 时退为 `ml-1` 保持布局。
- `flux-i18n`：新增 `conditionBuilder.ifExpressionPlaceholder` / `ifExpressionLabel`（en-US + zh-CN）。
- `formulas`/`formulaForIf` 运行时 DESIGN-ACK-NOT-IMPL：condition-builder.tsx 主 renderer 与子组件均不消费这两个字段（静默忽略），实际求值属 E3 P2 批。
- 验证 Green：新增 proof 13/13 全过；condition-builder 目录 17 test files / 243 tests 全过（无回归）。
- **最终验证（全绿）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49 tasks 全过。（注：未运行 e2e/Playwright，故非 AGENTS.md 定义的 unit+e2e full-green。）

### 2026-06-21 (E0c Phase 1)

- **E0c CRUD 选择漂移修复 — Phase 1（Q3 决策与契约固化）完成。** Plan: `docs/plans/2026-06-21-0010-e0c-crud-selection-drift-fix-plan.md`。
- **4 个漂移字段逐项裁定**：
  - `keepOnPageChange`: **实现**。`true` 时 select-all 合并当前页而非替换；select-all(false) 仅移除当前页。翻页不清空；refresh 按 `autoClearSelectionOnRefresh`。
  - `maxSelectionLength`: **实现**。达上限未选行 disabled；select-all 截断到上限；radio 模式忽略。
  - `maxKeepSelectionLength`: **不采纳（删字段）**。超限策略（LRU/FIFO/block-new）属 feature 级设计，需 `selectionRetentionStrategy` 字段，明确超出 Non-Goals。由 E1d 后续评估。
  - `checkableWhen`: **实现**。Flux 表达式（raw expression，不包裹 `${}`）按行求值；falsy disabled；求值失败按 `when` 语义视为 falsy。不包裹 `${}` 是因为 schema 编译期会在 CRUD scope 解析 `${}`，而 `record` 在行 scope 才存在。
- 更新 `docs/components/crud/design.md`：Flux 决策表对应行从"计划实现（E0c）"改为逐项"实现"/"不采纳（删字段）"+理由；新增 §7.1 选择字段语义节固化最终行为；§8 迁移映射表 `maxKeepSelectionLength` 行改为"不采纳（删字段）"。

### 2026-06-21 (E0c Phase 2)

- **E0c Phase 2（Focused Proof / Red）完成。** 新增 3 个测试文件：
  - `packages/flux-renderers-data/src/__tests__/crud-selection-drift-hook.test.tsx`（hook-level：keepOnPageChange 合并/移除/替换基线、maxSelectionLength 拒绝/截断/toggle-off、checkableWhen isRowCheckable + select-all 排除、radio 不受影响）
  - `packages/flux-renderers-data/src/__tests__/crud-selection-drift-renderer.test.tsx`（renderer-level：maxSelectionLength DOM disabled、checkableWhen DOM disabled、negative baseline、maxKeepSelectionLength 无效果）
  - `packages/flux-renderers-data/src/__tests__/crud-selection-drift-deletion.test.ts`（source-level 结构守卫：`maxKeepSelectionLength` 不再声明；`keepOnPageChange`/`maxSelectionLength`/`checkableWhen` 仍声明）
- 验证 Red：8 failed | 7 passed（15 tests）。7 passed 全部为 baseline/negative proof；8 failed 全部为漂移字段正向语义（drift confirmed）。

### 2026-06-21 (E0c Phase 3)

- **E0c Phase 3（实现选择路径消费漂移字段 / Green）完成。**
- `crud-schema.ts`：从 `CrudSelectionConfig` 移除 `maxKeepSelectionLength`；`checkableWhen` 类型从 `SchemaValue` 改为 `string`（raw expression，不包裹 `${}`，避免 schema 编译期在 CRUD scope 解析失败）。
- `schemas.ts`：`TableSchema.rowSelection` 扩展 `keepOnPageChange` / `maxSelectionLength` / `checkableWhen` 字段。
- `crud-renderer.tsx`：`base.rowSelection` 透传 `keepOnPageChange` / `maxSelectionLength` / `checkableWhen`。
- `use-table-selection.ts`：
  - `keepOnPageChange: true` → `handleSelectAll(true)` 合并当前页 + 截断到 maxSelectionLength；`handleSelectAll(false)` 仅移除当前页。
  - `maxSelectionLength` → `handleSelectRow` 在上限时拒绝；`handleSelectAll` 截断到上限；radio 忽略。
  - `checkableWhen` → 按 row scope 求值 raw expression（`${expr}` 包裹后 `helpers.evaluate`），falsy 行 `isRowCheckable=false`，select-all 排除；求值失败视为 falsy。
  - 新增返回 `isRowCheckable(rowKey)` / `isAtMaxSelection` / `checkableRowKeys`。
- `table-body-row-rendering.tsx` / `table-body-rows.tsx`：`DataRowView` 接收 `isRowCheckable` / `isAtMaxSelection`，checkbox `disabled = !isRowCheckable(rowKey) || (isAtMaxSelection && !isSelected)`；radio 的 `RadioGroupItem` 也消费 `isRowCheckable`。
- `table-header-row.tsx`：select-all checkbox 接收 `selectAllDisabled`（`isAtMaxSelection && !allSelected` 时 disabled）。
- `table-renderer.tsx`：从 `useTableSelection` 解构 `isRowCheckable` / `isAtMaxSelection`，透传给 `TableHeaderRow` / `TableBodyRows`。
- 验证 Green：15/15 全过（3 test files）；既有 `crud-selection-and-features.test.tsx` / `use-table-controls.selection.test.tsx` / `data-table-pagination-selection.test.tsx` 26/26 全过（无回归）。
- **最终验证（全绿）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49 tasks 全过。（注：未运行 e2e/Playwright，故非 AGENTS.md 定义的 unit+e2e full-green。）

### 2026-06-21

- **E0b 树级联漂移修复 — Phase 1（Q3 决策与契约固化）完成。** Plan: `docs/plans/2026-06-21-0001-e0b-tree-cascade-and-icon-drift-fix-plan.md`。
- **showIcon / showOutline 裁定：删除字段。** 依据：当前 schema 未定义图标/引导线的数据来源（无 `iconField`/`iconKey`），实现需一整套图标解析设计，属 feature（E2d 范围或独立 feature plan）而非漂移修复。保留"声明了但设了无效"的字段正是 E0b 要修的漂移本身。依据 Flux 原则"不以 amis 为标尺"（analysis §0.2 / §5）。后续如需图标，须以独立 feature plan 提供完整图标来源设计后再加字段。落地：从 `InputTreeSchema` 移除 `showIcon` + `showOutline`；从 `TreeSelectSchema` 移除 `showIcon`（Phase 3 执行）。
- **cascade 最终语义固化（写入两份 design.md）**：
  - 仅多选（`treeMode: 'checkbox'`）生效；单选（`radio`/`normal`）不受影响。
  - 向下传播：点击父节点翻转全部"可选后代"（`onlyLeaf` 决定叶子还是全部）+ 父本身（`onlyLeaf: false` 时）。
  - 向上派生：父节点 `checked`/`indeterminate` 由可选后代选中态派生，非独立存储。
  - `onlyLeaf` 优先于 `cascade`：父值永不写入 value；cascade 仅向叶子传播。
- 更新 `docs/components/input-tree/design.md`、`docs/components/tree-select/design.md`：删除全部"漂移待 E0b"措辞；Flux 决策表 cascade 改"实现"、showIcon/showOutline 改"不采纳（删字段）"。

### 2026-06-21 (E0b Phase 2)

- **E0b Phase 2（Focused Proof / Red）完成。** 新增 `packages/flux-renderers-form-advanced/src/__tests__/tree-cascade.test.tsx`（18 用例：cascade helper 父子传播 / onlyLeaf 优先 / indeterminate 派生 / input-tree renderer 选中+取消+半选+onlyLeaf / tree-select renderer 半选 / cascade:false 负向 / treeMode:radio 负向）。
- 为解析 import 在 `tree-options.ts` 加 Phase 2 占位签名壳（`cascadeSelectParent` / `cascadeDeselectParent` / `deriveCheckedState`，返回占位值不含真实 cascade 逻辑）。
- 验证 Red：`pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/tree-cascade.test.tsx` = 15 failed | 3 passed（3 个 passed 为 cascade:false / radio 负向用例，符合预期；15 failed 为 cascade 正向语义，失败原因为"未实现 cascade"而非 import/配置错误）。`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck` 全过（占位签名使 import 解析正常）。

### 2026-06-21 (E0b Phase 3)

- **E0b Phase 3（实现 cascade + 删 showIcon/showOutline 字段 / Green）完成。**
- `tree-options.ts`：实现纯 helper `cascadeSelectParent` / `cascadeDeselectParent` / `deriveCheckedState` + 内部 `collectAllDescendantValues` / `collectLeafDescendantValues` / `collectCascadeGroupValues`。语义与 Phase 1 裁定一致：向下传播翻转发"可选后代"集合（由 `onlyLeaf` 决定叶子/全部）+ 父本身（`onlyLeaf: false`）；向上派生父节点 checked/indeterminate，非独立存储；叶子节点 cascade 退化为单点翻转；`onlyLeaf: true` 下父值永不入数组。
- `tree-control-controllers.ts`：`useTreeOptionNodeController` 接 `cascade` / `onlyLeaf`；`cascadeEnabled = cascade && multiple`；checked/indeterminate 在 cascade 模式下由 `deriveCheckedState` 派生，否则保留原 `isTreeSelectionChecked`；`handleSelect` 在 cascade 模式下按当前派生态走 `cascadeSelectParent` / `cascadeDeselectParent`，否则原 `toggleTreeSelection`。返回新增 `indeterminate`。
- `tree-controls.tsx`：`TreeOptionNode` / `TreeOptionList` 串接 `cascade` / `onlyLeaf`；checkbox 新增 `indeterminate` prop；treeitem 新增 `aria-checked="mixed"`（indeterminate 时）；两个 renderer 从 schema 读 `cascade` / `onlyLeaf` 传入。保留现有键盘 / roving focus / searchable 行为。
- `schemas.ts`：从 `InputTreeSchema` 删除 `showIcon` + `showOutline`；从 `TreeSelectSchema` 删除 `showIcon`（Phase 1 裁定：删字段，因为 schema 未定义图标数据来源，实现需整套图标解析设计，属 feature 而非漂移修复）。
- **deletion proof 说明**：`SchemaObject` 基类带 `[key: string]: SchemaValue` 索引签名，故 `keyof InputTreeSchema` 必然含任意 string，无法用 `K extends keyof T` 类型层断言证明"字段已从类型消失"。改为运行时结构验证（renderer 不发布 `tree-option-icon`/`tree-option-outline` slot）+ 源码 grep（schemas.ts 不再声明、renderer 不再消费）把关。
- 验证 Green：`pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/__tests__/tree-cascade.test.tsx` = 19/19 全过（含 helper 父子传播/onlyLeaf/indeterminate 派生、input-tree renderer 选中+取消+半选+onlyLeaf、tree-select renderer 半选、cascade:false 负向、radio 负向、showIcon/showOutline deletion 结构守卫）。
- 全 form-advanced 测试 = 73 files / 700 tests 全过（含既有 tree-structure / tree-values / form-tree-\* 全绿，无回归）。
- **最终验证（全绿）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49 tasks 全过。（注：未运行 e2e/Playwright，故非 AGENTS.md 定义的 unit+e2e full-green。）

### 2026-06-21 (E0a)

- **E0a 输入校验漂移修复 — 执行完成。** Plan: `docs/plans/2026-06-21-0001-e0a-input-validation-drift-fix-plan.md`。
- **关键基线校正（执行 Phase 2 时发现）**：计划 `Current Baseline` 称 `minLength`/`maxLength`/`pattern` "既不收集也不传原生"。经 live repo 核对，**校验规则收集已由 schema 编译期完成**：
  - `packages/flux-compiler/src/validation-lowering.ts:43-49,97-103` 的 `collectSchemaValidationRules` 已从 schema 收集 `{ kind: 'minLength'|'maxLength'|'pattern', value }`。
  - `packages/flux-compiler/src/schema-compiler/validation-collection.ts:136-139` 经 `mergeValidationRules` 将其与 renderer `collectRules`（email / async）合并。
  - 既有测试 `packages/flux-renderers-form/src/__tests__/form-validation-ui.test.tsx:360-396`（`minLength: 5` → "Email must be at least 5 characters"）已验证端到端生效。
  - 因此计划 Phase 3 item 1（在 renderer `collectRules` 中再加这些 rule）**未执行** —— 会导致 rule 重复（每个字段产生两条同名 rule），是回归而非修复。新增 focused test 显式断言错误文案只出现一次，证明无重复。
- **真正剩余的漂移 = 原生 `<input>` 属性透传**（`minlength`/`maxlength`/`pattern`）。修复：`packages/flux-renderers-form/src/renderers/input.tsx` `createInputRenderer` 按 schema 条件透传这三个原生属性（值未声明时不传）。
- **Phase 1（owner docs）**：更新 `docs/components/input-text/design.md`、`input-email/design.md`、`input-password/design.md` —— 删除全部"漂移（待/随 E0a 修复）"措辞；Flux 决策表条目改为"实现"；明确双重生效路径（编译期 rule 收集 + renderer 原生属性透传）。
- **Phase 2（focused proof）**：新增 `packages/flux-renderers-form/src/__tests__/input-validation-drift.test.tsx`（5 用例）。先红后绿：rule 收集项修复前即绿（证明编译期已收集）；原生属性项修复前为红（`getAttribute('minlength'|'maxlength'|'pattern')` 返回 `null`），修复后转绿。
- **验证**：`pnpm --filter @nop-chaos/flux-renderers-form test` = 29 files / 208 tests 全过（含新增 5 例）；`pnpm test`（全 workspace）= 49 tasks 全过；`pnpm typecheck` = 49 tasks 全过；`pnpm build` = 26 tasks 全过；`pnpm turbo run lint` = 26 tasks 全过。
- **lint 失效锚点已修复（verify+commit 阶段）**：`pnpm lint` 前置脚本 `check-active-doc-code-anchors.mjs` 原报 2 处失效锚点（`existing-components-improvement-roadmap.md:87 -> docs/references/naming-conventions.md` 属 X3；`mobile-roadmap.md:59 -> docs/architecture/mobile-responsive-baseline.md` 属移动端 M0）—— 均为 roadmap 对**计划中尚未成文**的工作项产出文件的前向引用。本次 verify 阶段新建两份明确的 `Status: planned` 占位文档（`docs/references/naming-conventions.md`、`docs/architecture/mobile-responsive-baseline.md`），使 `pnpm lint` 全绿（含前置脚本 + `turbo run lint` 26/26）。占位文档待 X3 / M0 owner plan 执行时回填正式内容。非 E0a 引入、亦非 E0a 交付物，仅为满足 active-doc 锚点检查的 workspace 文档基建。
- **最终验证（verify+commit 阶段，全绿）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 前置脚本通过 + `turbo run lint` 26/26、`pnpm test` = 49 tasks 全过。（注：未运行 e2e/Playwright，故非 AGENTS.md 定义的 unit+e2e full-green。）
- Next: roadmap E0a 状态由 `todo` 改 `done`（已完成）；等待独立 closure audit 复核。
