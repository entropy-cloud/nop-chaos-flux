# HCA3 Binding Layer 包级深审记录

> Audit Date: 2026-08-08
> Mission: industrial-hmi-component-audit
> Work Item: HCA3. Binding 层审计
> Plan: `docs/plans/2026-08-08-0748-3-industrial-hmi-hca3-binding-layer-audit.md`
> Scope: `packages/flux-renderers-industrial/src/binding/` 7 文件（数据绑定管线核心）
> Baseline: HEAD 2026-08-08，`pnpm --filter @nop-chaos/flux-renderers-industrial typecheck/lint` 全绿；binding 目录行数 `wc -l`：point-store 330 / reverse-index 126 / dirty-collector 665 / value-to-state 59 / animator 269 / bind-resolver 139 / flux-eval 111
> Dimension Set: `docs/skills/deep-audit-prompts.md` 23 维（binding 为复杂交互层，**维度 21 显示与定位 / 22 集成接线 / 23 测试有效性 必选**）

## 审计方法

逐文件过 23 维重点维度（脏收集合帧顺序 collect → collectBindings → flushFrame 确定性 / animation vs binding 同属性优先级 P2-1 契约复核 / flux 求值订阅收集并集 + 解绑对称 + 无泄漏 / point-store 监听器清理 subscribe/unsubscribe 对称 + dispose / reverse-index 解析正确性 + 增量更新 / value-to-state 阈值边界 / animator rAF 生命周期 start/cancel 对称 + 无孤儿帧 + 无 binding 属性推进短路径 / bind-resolver 绑定图构建空/重复/循环引用），抽查边界值（空点表 / 单点 / 超大点表 10k+ / NaN/Infinity 值 / 同属性 binding+animation 冲突 / 快速 setPointValue 风暴 / dispose 后 setPointValue / 订阅泄漏多次 subscribe/unsubscribe / deadband 边界 / scale NaN 传播 / expression cycle）。每条 finding 带 `文件:行` 证据，所有 finding 已 triage 为 P0/P1/P2/P3。

维度 22 边界：本 plan dim 22 = binding 内部子模块接线（point-store↔reverse-index↔dirty-collector↔refresh-pipeline↔animator 接合面）+ pipeline 公共 API 可操作性（flushFrame/requestRender/destroy 对称 + destroyed 门控）；完整 schema→store→DOM→event 链路可操作性在 HCA1（renderer hook 层），本 plan 不重复。

## 逐文件 finding 表

### point-store.ts（330 行）— PointStore 点表 store + EventHub 事件基元

| 维度                                      | 结论                                                                                                                                                                                                                                                                                                                                                                                                                                    | 证据                                     | Triage                                                                                                                              |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------ |
| 22 集成接线（subscribe/unsubscribe 对称） | `subscribe`（:208-225）按 pointId 集合注册，跳过未声明点（`!this.entries.has(pointId) continue`），返回退订函数清理 targets（Set delete + size=0 时 delete map entry）。重复订阅同一 pointId+cb 幂等（Set add）。`reset`（:170-178）清 entries/subscribers/dirtyPointIds/events/reportedSubscriberErrors，全量清理对称。`point-store.test.ts:173-195` 覆盖 subscribe/unsubscribe/idempotent/unknown-skip 断言 cb call count + payload。 | `point-store.ts:208-225,170-178`         | 零发现                                                                                                                              |
| 04 状态所有权（applyValue 单一写入归口）  | `applyValue`（:284-308）为唯一写入路径（setPointValue/setPointValues 均转发）。Object.is 变化检测 → deadband → 写入 + dirty 标记 + generation++ → emit + subscriber 通知。subscriber 异常经 `reportSubscriberError`（:310-315）去重上报不中断循环（emitWith per-emit 闭包捕获正确 pointId，:299）。无 props-to-state 同步链 / 无双状态。                                                                                                | `point-store.ts:284-315`                 | 零发现                                                                                                                              |
| 21 显示与定位（NaN/deadband/scale 边界）  | `applyLinearScale`（:113-118）k\*value+b，非 number 原样返回；NaN 输入 → NaN 输出但 `Object.is(NaN,NaN)===true` 使连续 NaN 不重复派发（:289），`point-store.test.ts:158-162` 显式测试 NaN unchanged。deadband（:324-329）仅对 number 生效（非 number 返 false），`                                                                                                                                                                      | Δ                                        | <deadband` 不派发，`point-store.test.ts:105-114` 断言 deadband 边界值。expression 源点 convert 直接返回 raw（:319，不二次 scale）。 | `point-store.ts:113-118,284-329` | 零发现 |
| 19 错误传播（subscriber 异常隔离）        | emitWith（EventHub :70-82）+ subscriber 循环 try/catch 双重隔离：单 subscriber throw 不中断剩余 subscriber 与批量写入循环；异常经 `reportSubscriberError` 按 `${pointId}:${message}` 去重上报（:311）。`onSubscriberError` 回调为可选注入通道（bridge 层装配）。                                                                                                                                                                        | `point-store.ts:299-315, EventHub:55-82` | 零发现                                                                                                                              |
| 15 性能（generation 计数器）              | `generation`（:147-156）仅在 applyValue 命中真实变更 / loadDeclarations / restoreValues / reset 时 bump，只读/订阅路径不 bump。bridge 层据此 memoize 快照（scope-only 变更不触发快照重建）。`snapshotValues`（:260-266）/`restoreValues`（:272-282）仅记录已写入值（undefined 不入快照），restoreValues 绕过 convert 避免二次 scale。                                                                                                   | `point-store.ts:143-282`                 | 零发现                                                                                                                              |

### reverse-index.ts（126 行）— 绑定反向索引

| 维度                                               | 结论                                                                                                                                                                                                                                                                                                                                                                                                                        | 证据                      | Triage |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------ |
| 22 集成接线（build/add/remove/update 增量维护）    | `build`（:50-55）clear+遍历 addSymbol；`addSymbol`（:57-62）递归 children；`removeSymbol`（:64-76）filter byPoint 清理（remaining 空 delete / 否则 set filtered）+ delete bySymbol/bindingsBySymbol；`updateSymbol`（:78-81）remove+add。`indexNode`（:109-125）按 property→collectBindingPointIds（Set 去重）→byPoint+bySymbol 双索引。`reverse-index.test.ts` 覆盖 build/lookup/remove/update/clear/children 断言结果集。 | `reverse-index.ts:50-125` | 零发现 |
| 21 显示与定位（collectBindingPointIds 表达式路径） | `collectBindingPointIds`（:24-33）binding.point 直接取 + binding.expression 经 `probeExpressionPaths` 提取 scope paths（fluxEvalContext 不可用时 expression 分支不产出 refs，纯点表绑定仍工作）。Set 去重。`path && path !== '*'` 过滤通配符。                                                                                                                                                                              | `reverse-index.ts:24-33`  | 零发现 |
| 13 类型安全                                        | `IndexEntry extends BindingTarget` + pointId 字段扩展。`lookup`（:83-85）/`lookupSymbol`（:87-93）返回新对象（不暴露内部引用）。getBindings 返内部 Record（只读消费契约）。无 any/类型逃逸。                                                                                                                                                                                                                                | `reverse-index.ts:83-97`  | 零发现 |

### dirty-collector.ts（665 行）— DirtyCollector 帧内脏收集 + RefreshPipeline 刷新流水线

| 维度                                                          | 结论                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 证据                                         | Triage                                 |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | -------------------------------------- |
| 22 集成接线（合帧顺序确定性）                                 | `flushFrame`（:267-305）顺序确定：① destroyed gate → ② scopeDirty 快照 → ③ 首同步(synced=false)/增量(drainDirtyPointIds) → ④ 早退检查(changed.length===0 && !collector.hasPending) → ⑤ recomputeExpressionPoints → ⑥ collectBindings(含 collectStates) → ⑦ collector.flush。collect → collectBindings → flush 顺序固定，last-write-wins per (symbolId,property)。animation 增量经 `animator.collect→collector.collect` 在 flushFrame 前（animator tick）或 flushFrame 内（collectBindings 覆盖同属性）写入同一 pending Map。`refresh-pipeline-core.test.ts:18-27` 断言全链路收敛 + 单次批量写。 | `dirty-collector.ts:267-305`                 | 零发现                                 |
| 22 集成接线（destroyed 门控对称）                             | DirtyCollector（:49-111）collect/flush/flushFrame/requestRender/destroy 全部 destroyed gate；RefreshPipeline（:267-333）flushFrame/requestRender/destroy 全部 destroyed gate。destroy 幂等（pipeline :322 if(destroyed) return; collector :105-111）。`refresh-pipeline-animation-lifecycle.test.ts:126-185` 三组测试覆盖 destroy 后 no-op + collector 单一 owner + 幂等。                                                                                                                                                                                                                      | `dirty-collector.ts:49-111,267-333`          | 零发现                                 |
| 21 显示与定位（{binding,state} > animation 优先级 P2-1 复核） | `flushFrame` :270-273 注释固化 P2-1 契约：animator 经 collect 写同一 pending Map，collectBindings/collectStates 先于 collector.flush → 脏帧 binding/state 覆盖同属性 animator 当帧增量。`refresh-pipeline-animation-lifecycle.test.ts:260-322` 三组覆盖：(a) 同属性 binding 覆盖 animation(45 vs 180) / (b) 无 binding 属性 animation 正常(dashOffset=50) / (c) 多帧跨帧稳定(rotation 恒=45)。断言结果值非 call-count。                                                                                                                                                                         | `dirty-collector.ts:270-273`                 | 零发现（先验修复回归点，Phase 3 抽查） |
| 21 显示与定位（evaluateFlux 表达式归一化）                    | `evaluateFlux`（:445-468）normalized = trim+`${...}`包装。`${v1}+${v2}` startsWith('${') → 原样；`v1` → `${v1}`。compiledCache 按 normalized key 缓存。compile/evaluate catch 区分 phase 返 FluxEvalOutcome（compile-failed/evaluate-failed/ok）。CircularDependencyError 经 findCircularDependencyError 守卫重新抛出（:453-454,464-465）。`binding-expression-unification.test.ts`覆盖`${}` / `$xxx`/`@{}` 全语法路径。                                                                                                                                                                        | `dirty-collector.ts:445-468`                 | 零发现（先验修复回归点，Phase 3 抽查） |
| 19 错误传播（cause-chain 端到端保留 P2-4）                    | `findCircularDependencyError`（:149-163）沿 Error.cause 链解包 CircularDependencyError（MAX_CAUSE_CHAIN_DEPTH=1000，覆盖多层嵌套求值包装深度）。`reportError`（:660-664）按 key 去重(lastError Set) + 透传原始 error。`syncExpressionPoint`（:379-411）返 FluxEvalOutcome → 对称错误码 flux-compile-failed/flux-evaluate-failed。`refresh-pipeline-core.test.ts:106-131` 断言 cycle onError 含 'circular dependency' message + error 透传。                                                                                                                                                     | `dirty-collector.ts:149-163,379-411,660-664` | 零发现（先验修复回归点，Phase 3 抽查） |
| 21 显示与定位（collectStates 状态色 > binding 合帧）          | `collectBindings`（:551-566）先 collect binding 值 → `collectStates`（:569-593）后 collect 状态样式 patch → 同帧同属性 state 覆盖 binding（design-data-binding.md §4.3 状态色胜出契约）。`resolveStateScale`（:622-631）仅在声明级无 scale 或同对象时转发 binding.scale，避免双重换算。`refresh-pipeline-state.test.ts:29-56,122-153` 断言状态样式 + scale 转发结果值。                                                                                                                                                                                                                         | `dirty-collector.ts:551-631`                 | 零发现                                 |
| 21 显示与定位（resolveStateDriver stateSource B3）            | `resolveStateDriver`（:600-615）优先 `declaration.stateSource`（格式 "pointId" 或 "pointId.property"，lastIndexOf('.') 分割），缺省回落 reverse-index [0]。`refresh-pipeline-state.test.ts:176-210` 三组覆盖 stateSource 覆盖 / 缺省回落 / pointId.property 限定。                                                                                                                                                                                                                                                                                                                              | `dirty-collector.ts:600-615`                 | 零发现（先验修复回归点）               |
| 22 集成接线（applyAnimationLinkage 启停对称）                 | `applyAnimationLinkage`（:634-658）：always→start / when:{state} 匹配 current→start / 不匹配且 prev defined→stop；prev state 声明 animations→stop / current state 声明 animations→start。animator.start 同 kind 幂等（animator.ts:84-100 existing 替换 animation 对象）。animator.stop 不存在 kind no-op（animator.ts:112-113）。双启动（symbol-level when:{state} + declaration.states[current].animations 同 kind）经 start 幂等收敛。`refresh-pipeline-animation-lifecycle.test.ts:70-104` 覆盖 fault 态 auto blink start/exit stop + when:always 持续 + when:{state} 联动。                 | `dirty-collector.ts:634-658`                 | 零发现                                 |
| 15 性能（buildEvalScope 每 expression 调用重建）              | `buildEvalScope`（:512-549）每次 evaluateFlux 调用时遍历 pointStore.pointIds() 构建全量 data 对象（O(P) per expression eval）。E 个 expression 点首次同步 O(E×P)。bridge 层 generation-memoize 缓存快照（owner doc §9.1 Deferred 已裁定不走 AST 过滤）。非 live defect，属已记录性能特征。                                                                                                                                                                                                                                                                                                      | `dirty-collector.ts:512-549`                 | 零发现（Deferred But Adjudicated）     |
| 02 模块职责（665 行 > 500 WARN）                              | dirty-collector.ts 承担三职责：① DirtyCollector 帧内脏收集（:38-112）② CircularDependencyError 环检测（:114-163）③ RefreshPipeline 刷新流水线编排（:165-665）。`check:oversized-code-files` WARN 桶（500<665≤700，非 ERROR）。                                                                                                                                                                                                                                                                                                                                                                  | `dirty-collector.ts` 全文                    | **P2-BND-1（拆分 Decision 见下）**     |

### value-to-state.ts（59 行）— 值→状态判定纯逻辑

| 维度                               | 结论                                                                                                                                                                                                                                                                                                                              | 证据                      | Triage |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------ |
| 21 显示与定位（判定链 + 阈值边界） | `resolveState`（:11-33）scale → ranges(仅 number，inclusive min/max) → booleanMap(仅 boolean) → valueMap(defined value) → defaultState。range 首匹配胜出（overlapping ranges 取序）。defaultState（:52-58）偏好链 run→normal→off→首键。`value-to-state.test.ts` 覆盖 ranges/booleanMap/valueMap/default/empty states/scale 边界。 | `value-to-state.ts:11-58` | 零发现 |
| 13 类型安全                        | `applyLinearScale`（:35-44）expression-scale 不线性换算（`'expression' in scale` 返原值），与 point-store convert + dirty-collector resolveStateScale 三处一致（declaration expression-scale 由 validator 拒绝，design-data-binding.md §4.2 B4）。                                                                                | `value-to-state.ts:35-44` | 零发现 |

### animator.ts（269 行）— 动画时钟 + 生命周期

| 维度                                                | 结论                                                                                                                                                                                                                                                                                                                                                                                                                    | 证据                          | Triage |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------ |
| 22 集成接线（start/stop/pause/resume 生命周期对称） | `start`（:84-100）同 kind 幂等（existing 替换 animation），仅新 kind emit animation:start。`stop`（:102-119）支持单 kind / 全 kind，size=0 清 map + stopClock。`pause`（:121-127）/`resume`（:129-136）elapsed 累计 + runStartAt 重置。`destroy`（:153-162）spread 快照 keys 后 emit animation:stop + clear + stopClock + removeAll。`animator.test.ts` 覆盖 start/stop/pause/resume/isPlaying/getActiveKinds/destroy。 | `animator.ts:84-162`          | 零发现 |
| 22 集成接线（rAF 生命周期无孤儿帧）                 | `ensureClock`（:186-194）clockScheduled gate + playing.size>0 gate；`stopClock`（:196-201）cancel + reset lastTickAt=NEGATIVE_INFINITY。tick（:203-230）限频检查 + loop 完成自动 stop + playing.size>0 时 requestFrame+ensureClock / 否则自然停钟。destroy 后 stopClock 确保无孤儿 rAF。`refresh-pipeline-animation-lifecycle.test.ts:70-83` 覆盖 fault enter/exit blink start/stop。                                   | `animator.ts:186-230`         | 零发现 |
| 21 显示与定位（computeIncrements 插值正确性）       | rotate: from+(to-from)*progress（:239-241）；blink: progress<0.5→on, from/to number→opacity / 否则→visible（:243-251）；flow: dashOffset from+(to-from)*progress（:253-255）；move: x/y 双轴插值（:257-266）。`animator.test.ts` 覆盖四类 kind progress 增量值。                                                                                                                                                        | `animator.ts:236-268`         | 零发现 |
| 06 异步安全（tick 中 stop 修改迭代集合）            | tick（:210-225）迭代 `this.playing` Map 嵌套 `byKind` Map；loop 完成调 `this.stop(symbolId,kind)` 删 byKind.delete(kind) + 可能 playing.delete(symbolId)。JS Map/Set 删除当前迭代元素安全（迭代器跳到下一元素）。`[...byKind.keys()]` spread 在 destroy 中快照。tick 内 stop 修改 Map 迭代安全（JS spec 保证）。                                                                                                        | `animator.ts:203-230,153-162` | 零发现 |

### bind-resolver.ts（139 行）— 绑定表达式求值 + 属性映射

| 维度                                                          | 结论                                                                                                                                                                                                                                                                                                                                                        | 证据                             | Triage                       |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ---------------------------- |
| 21 显示与定位（求值优先级 point→expression→map→scale→format） | `resolveBinding`（:108-128）point 优先 / expression 次 / base undefined→return undefined。map[String(value)] 有则覆盖。scale applyScale。format 仅 isFormatTargetProperty(text/fill/stroke/textColor)（B1 修复，:124）。`bind-resolver.test.ts` 覆盖 point/expression/map/scale/format 优先级 + format-target-property 限定 + applyScale expression-scale。 | `bind-resolver.ts:108-128`       | 零发现（先验修复 B1 回归点） |
| 13 类型安全                                                   | BINDABLE_PROPERTIES（:14-28）readonly const tuple + isBindableProperty 类型守卫。resolveBindings（:130-138）跳过 non-bindable property。formatValue（:66-96）%s/%d/%f/%% token 解析，未识别 token 原样输出。                                                                                                                                                | `bind-resolver.ts:14-96,130-138` | 零发现                       |

### flux-eval.ts（111 行）— isScadaPrimitive + createPrivateEvalScope + 依赖探针

| 维度                                                    | 结论                                                                                                                                                                                                                                                                                                                                                                                | 证据                 | Triage |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------ |
| 21 显示与定位（extractExpressionDepsViaProbe 判别结果） | `extractExpressionDepsViaProbe`（:64-95）返 discriminated result（ok/compile-failed/create-state-failed/evaluate-failed/deps-empty）。compiled.kind!=='dynamic' → ok 空集（静态表达式不诊断）。wildcard/deps 空 → deps-empty。tolerant probe scope（:36-51）Proxy 宽容求值使依赖收集可达。`flux-eval.ts` 经 reverse-index collectBindingPointIds + dirty-collector probeDeps 消费。 | `flux-eval.ts:36-95` | 零发现 |
| 13 类型安全                                             | `createPrivateEvalScope`（:17-34）返 ScopeRef（INV-4 非 schema-visible）。has/get 基于 getIn。`__proto__`/`constructor`/`prototype` 守卫（tolerant probe :40）。无 any。                                                                                                                                                                                                            | `flux-eval.ts:17-51` | 零发现 |

## 维度 21 显示与定位正确性 专项节

| 子项                                      | 结论                                                                                                                                                                   | 证据                                                                                       |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 脏收集合帧顺序确定性                      | collect → collectBindings(含 collectStates) → collector.flush 固定顺序；last-write-wins per (symbolId,property)；animation 增量在 flushFrame 前/内写入同一 pending Map | `dirty-collector.ts:267-305` + `refresh-pipeline-core.test.ts:18-50`（1k 点单帧合并断言）  |
| animation vs binding 同属性优先级（P2-1） | {binding,state} > animation 契约成立 + 三组 focused 测试锁定（同属性覆盖 / 无 binding 属性正常 / 多帧稳定）                                                            | `dirty-collector.ts:270-273` + `refresh-pipeline-animation-lifecycle.test.ts:260-322`      |
| value→state 判定边界                      | ranges inclusive + booleanMap + valueMap + defaultState 偏好链；binding.scale 转发避免双重换算                                                                         | `value-to-state.ts:11-58` + `refresh-pipeline-state.test.ts:102-153`                       |
| animator 插值数学                         | rotate/blink/flow/move 四类 progress 公式手算正确；blink opacity/visible 分支正确                                                                                      | `animator.ts:236-268` + `animator.test.ts`                                                 |
| flux 表达式归一化 + 环检测                | `${expr}` 唯一语法；compiledCache 缓存；CircularDependencyError cause-chain 解包；active 栈环检测                                                                      | `dirty-collector.ts:420-468,149-163` + `binding-expression-unification.test.ts`            |
| scale 换算一致性                          | declaration scale linear（convert 施加）/ binding scale linear 或 expression（applyScale 消费）/ state scale 转发避免双重 — 三处一致                                   | `point-store.ts:113-118,317-322` + `bind-resolver.ts:49-63` + `dirty-collector.ts:622-631` |

**结论**：维度 21 零 live defect。合帧顺序、优先级、判定边界、插值数学、表达式求值、scale 换算均经结果值断言覆盖。先验修复（P2-1 优先级 / P2-4 cause-chain / B1 format-target / B3 stateSource / B4 scale / 表达式一元化）回归点 Phase 3 抽查。

## 维度 22 集成接线与可操作性 专项节

| 子项                                                          | 结论                                                                                                                                                                      | 证据                                                                                         |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| point-store ↔ reverse-index ↔ dirty-collector ↔ pipeline 接线 | setPointValues → drainDirtyPointIds → collectBindings(reverseIndex.lookup) → resolveBinding → collector.collect → flush 全链路通断                                        | `dirty-collector.ts:267-305,551-566` + `refresh-pipeline-core.test.ts:18-27`                 |
| subscribe/unsubscribe 对称 + dispose 清理                     | subscribe 按 pointId Set 注册 + 退订清理；reset 全量清；pipeline.destroy 幂等 + collector 单一 owner 销毁                                                                 | `point-store.ts:208-225,170-178` + `dirty-collector.ts:319-333`                              |
| animator start/stop + rAF 生命周期                            | start 幂等 / stop 单/全 kind / ensureClock+stopClock 对称 / destroy 无孤儿帧                                                                                              | `animator.ts:84-162,186-201` + `refresh-pipeline-animation-lifecycle.test.ts:70-104`         |
| destroyed 门控对称                                            | DirtyCollector + RefreshPipeline 所有公共入口 destroyed gate no-op                                                                                                        | `dirty-collector.ts:49-111,267-333` + `refresh-pipeline-animation-lifecycle.test.ts:126-185` |
| flushFrame 早退正确性                                         | changed.length===0 && !collector.hasPending → return false（无变更无 pending 不触发渲染）；animation-only pending → collectBindings 空集 no-op + collector.flush 正确应用 | `dirty-collector.ts:300-304` + `refresh-pipeline-core.test.ts:194-205`                       |

**结论**：维度 22 零 live defect。全链路接线通断、订阅/销毁对称、生命周期门控、早退正确性均经 focused 测试覆盖。

## 维度 23 测试有效性与假绿 专项节

| 子项                                    | 结论                                                                                                                                                                                                                                                    | 证据                                                                                                         |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 断言结果值（非 not.toThrow/call-count） | refresh-pipeline-core 断言 applied attrs 结果对象（:24-26,48-49）/ pipeline-state 断言 fill 色值（:47,52）/ animation-lifecycle 断言 isPlaying boolean + rotation 终值（:275,294,320）/ cycle 断言 onError message 含 'circular dependency'（:127-128） | 全部 binding test 文件                                                                                       |
| 固化缺陷断言识别                        | 无固化缺陷断言：P2-1 precedence 测试断言 binding 胜出值（45 非 180），非 animator 值；B1 format-target 测试断言 visible:false 绑定原值透传（非字符串化 true）；deadband 测试断言 deadband 边界不派发（非派发）                                          | `refresh-pipeline-animation-lifecycle.test.ts:275` / `bind-resolver.test.ts` / `point-store.test.ts:105-114` |
| mock↔真实漂移                           | 使用真实 expressionCompiler（createExpressionCompiler(createFormulaCompiler())）+ createDefaultEnv（refresh-pipeline-fixtures.ts:13-14），非 mock 被测边界。scheduleTick 手动注入确定性调度器（非 mock rAF）。Animator now/interval 注入确定性时间源。  | `refresh-pipeline-fixtures.ts:10-15`                                                                         |
| 先验修复回归锁定                        | P2-1 优先级三组 / P2-4 cause-chain cycle onError / 表达式一元化全语法 / B3 stateSource / B4 scale 转发 — 均有 focused 测试                                                                                                                              | 各先验 plan 测试文件                                                                                         |
| 测试覆盖缺口                            | binding-expression-unification.test.ts 直接驱动 pipeline + 手工注入 scopeData（variables-optional-contract 场景①false-green 已由 bridge 层 integration test 收口，非 binding 层 gap）                                                                   | `variables-optional-contract.test.ts` + `binding-expression-scope-integration.test.tsx`（renderer 层）       |

**结论**：维度 23 零假绿。全部断言为结果值（applied attrs / isPlaying / onError message / pointValue），无固化缺陷断言、无 mock 掉被测边界、无同义反复。先验修复回归点均有 focused 锁定。

## Finding Triage 汇总

| ID       | 严重程度 | 文件                      | 一句话摘要                                                                                       | 处置                                      |
| -------- | -------- | ------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| P2-BND-1 | P2       | `dirty-collector.ts` 全文 | 665 行承担三职责（DirtyCollector / CircularDependencyError / RefreshPipeline），超 500 WARN 阈值 | **拆分 Decision：本 plan 落地**（见下节） |

**零 P0/P1 live defect**。binding 层经先验修复（P2-1 优先级 / P2-4 cause-chain / 表达式一元化 / 订阅并集 / B1-B4 系列）后行为正确性扎实，23 维深审（维度 21-23 必选）未发现新的 live defect。

## dirty-collector.ts 拆分 Decision

**裁定：本 plan 落地拆分。**

依据：

1. 拆分是机械操作（按职责切文件 + 更新 import 路径），全量回归可证无行为漂移。
2. 本 plan 深审已逐行过 dirty-collector.ts，拆分同时提升后续审计可审计性。
3. 顺带把 WARN 桶清零（665 → 各模块 ≤ 500），HCA-CG 可跳过此项。
4. WARN 非硬门禁，但本 plan 工作量可控（3 文件切分 + 20 个 import 更新 + 全量回归）。

**切分方案**：

| 新文件                       | 职责                                                                                                              | 来源行           | 预估行数 |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------- | -------- |
| `dirty-collector.ts`（保留） | DirtyCollector 帧内脏收集 + 帧类型（ApplyAttrs/CollectedEntry/TickScheduler/FrameScheduler）+ createTickScheduler | :11-112          | ~105     |
| `expression-errors.ts`（新） | MAX_CAUSE_CHAIN_DEPTH + CircularDependencyError + findCircularDependencyError + FluxEvalOutcome 类型              | :114-163,165-173 | ~60      |
| `refresh-pipeline.ts`（新）  | MAX_EXPRESSION_ITERATIONS + RefreshPipelineOptions + RefreshPipelineEvents + RefreshPipeline 类                   | :114,165-665     | ~500     |

**公共面不变**：RefreshPipeline 类 API（flushFrame/requestRender/destroy/on/updateScopeData/getResolver）+ DirtyCollector 类 API（collect/hasPending/flush/flushFrame/requestRender/destroy）均不变。import 路径更新（20 个文件从 dirty-collector.js → refresh-pipeline.js），不影响包外部消费者（index.ts 不导出 RefreshPipeline/DirtyCollector）。

## 先验修复回归抽查（Phase 3 抽查点）

以下先验修复构成本 plan Current Baseline，Phase 3 抽查其行为仍成立（不重做）：

- P2-1 {binding,state} > animation 同属性优先级（`dirty-collector.ts:270-273` precedence 注释 + `design-data-binding.md §4.3`）。
- P2-4 evaluateFlux cause-chain 端到端保留（`dirty-collector.ts:382-406` syncExpressionPoint 透传 outcome.error + `:660-664` reportError 转发）。
- binding 表达式一元化（2129-1，`@{}` 拒识 + `${expr}` 唯一语法）。
- binding-expression scope 订阅并集（2129-3 multi P1-1，bridge 层 collectBindingExpressionScopePaths + useScopeSelector paths 并集）。
- B1 format 仅文本类属性（`bind-resolver.ts:42,124`）。
- B3 stateSource 显式 state-driver（`dirty-collector.ts:600-615`）。
- B4 declaration expression-scale validator 拒绝（`value-to-state.ts:35-44` + `point-store.ts:317-322`）。

## owner doc 一致性核对（Phase 3 item）

核对 `docs/components/industrial-hmi/design-data-binding.md` 与 live binding：

| 契约                                               | doc 位置         | live 位置                                                                          | 结论                                                                                                                |
| -------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 模块清单 §11（7 文件）                             | §11（:359-368）  | `src/binding/` 7 文件                                                              | **drift（拆分后）**：拆分后新增 `expression-errors.ts` + `refresh-pipeline.ts`，模块清单 7→9 文件。Phase 3 同步 §11 |
| 合帧顺序 §4.3（collect → collectBindings → flush） | §4.3（:113-137） | `dirty-collector.ts:267-305`                                                       | 一致                                                                                                                |
| {binding,state} > animation 优先级契约 §4.3        | §4.3（:130）     | `dirty-collector.ts:270-273`                                                       | 一致                                                                                                                |
| 错误码对称 §9.1                                    | §9.1（:271-276） | `dirty-collector.ts:387-406,660-664`                                               | 一致                                                                                                                |
| 环检测 cause 链深度 §9.1                           | §9.1（:288-296） | `dirty-collector.ts:114-163`                                                       | 一致（拆分后移至 expression-errors.ts）                                                                             |
| scale 消费语义 §4.2                                | §4.2（:107）     | `point-store.ts:317-322` + `bind-resolver.ts:49-63` + `dirty-collector.ts:622-631` | 一致                                                                                                                |
| format 适用域 §4.2                                 | §4.2（:106）     | `bind-resolver.ts:42,124`                                                          | 一致                                                                                                                |
| stateSource §4.5                                   | §4.5（:186-188） | `dirty-collector.ts:600-615`                                                       | 一致                                                                                                                |

**结论**：发现 1 处 drift（模块清单 §11 拆分后 7→9 文件），Phase 3 拆分后同步。其余契约一致。

## 喂入 HCA-BL（bug 候选）

> 正式归档动作在 HCA-BL，本 plan 不产出 `docs/bugs/` 卡片。以下为本层 finding 喂入清单。

| 候选   | 复杂/跨层 | 归档建议                                                        |
| ------ | --------- | --------------------------------------------------------------- |
| （无） | —         | binding 层零 P0/P1 live defect，无复杂/跨层 bug 候选喂入 HCA-BL |

**说明**：binding 层经多轮先验修复后行为正确性扎实，23 维深审未发现新的 live defect。P2-BND-1（文件行数治理）为 governance 项非 bug，本 plan 内拆分落地。
