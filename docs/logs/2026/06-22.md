# Development Log - 2026-06-22

### 2026-06-22 (E3 code-editor colorize 只读高亮 — 全 3 Phase 完成)

- **E3 code-editor colorize（只读代码高亮显示）— 全 3 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0740-2-e3-code-editor-colorize-plan.md`。把 E2h plan 显式 Deferred 到 E3 的 code-editor **只读代码高亮显示（colorize）**收口：为 code-editor 增加一条独立于可编辑 `EditorView` 的静态语法高亮渲染路径，用于只读展示代码片段（无需编辑器实例、更轻量）。`colorize?: boolean` 标志短路 EditorView/MergeView 创建，改用 `@lezer/highlight` `highlightTree` + `@codemirror/language` `HighlightStyle` 渲染静态高亮 `<pre>`。
- **Phase 1（机制裁定 + design.md 决策表）**：
  - 裁定 A：`colorize?: boolean` 独立标志（`mode` 偏编辑行为 expression/template/code 三态，colorize 偏渲染范式，语义正交不宜混入 `EditorMode` 枚举）。
  - 裁定新增 `getLanguageParser(language): Parser | null` 并行映射（live repo 证据：`javascriptLanguage`/`typescriptLanguage`/`jsonLanguage`/`cssLanguage`/`htmlLanguage`/`pythonLanguage`/`markdownLanguage`/`yamlLanguage`/`xmlLanguage` 均导出 `*Language`（含 `.parser`）；`@codemirror/lang-sql` 不导出 `sqlLanguage`，导出 `StandardSQL` `SQLDialect` 实例，SQL 走 `StandardSQL.language.parser`；`expression` 复用 `javascriptLanguage.parser`；`plaintext` → null）。
  - `code-editor/design.md` §2 line 35 翻转为「实现（E3）」+ 触发形态 + colorize vs readOnly editor 选择指引表；§4 schema 加 `colorize?: boolean` 字段定义；§12 baseline 增补 colorize 条目；§13「未来可选扩展」节 colorize 行更新为「E3 已落地」。`types.ts` `CodeEditorSchema` 同步加 `colorize?: boolean`。
- **Phase 2（静态高亮渲染实现 + focused 单测）**：
  - `extensions/base.ts`：新增 `getLanguageParser(language): Parser | null`，11 语言映射（SQL 走 `StandardSQL.language.parser`），plaintext→null。新增 `@lezer/common` devDependency（Parser 类型）。
  - `code-editor-renderer/colorize.tsx`（新增）：`ColorizeView` 用 `parser.parse(code)` + `highlightTree` + `defaultHighlightStyle`/`oneDarkHighlightStyle`（light/dark 对齐 `editorTheme`）渲染静态 `<pre><code>`；CSS rules 经 `highlightStyle.module.getRules()` 注入 `<style>`；根节点 markers：`data-colorize` + `data-colorize-language` + `data-colorize-theme` + `data-colorize-fallback`；plaintext/parser-null → 纯文本降级；parse 抛错 try-catch → 纯文本降级（Failure Path `colorize-parse-error`）。
  - `code-editor-renderer.tsx`：`isColorizeMode = colorize === true && !isDiffMode`；成立时 early-return 渲染 `ColorizeView`（跳过 toolbar/fullscreen/SQL chrome）；hooks 仍按顺序调用但 `containerRef.current` 为 null 故不创建 EditorView（`useCodeMirror`/`useMergeView` 均在 useEffect 内 guard）；`diffValue` 有值时 `isColorizeMode=false` 走 MergeView（colorize 不与 diff 组合）。新增 `colorize` field rule + propContract。
  - `code-editor-styles.css`：新增 `[data-colorize-container]` 边框/圆角/背景 + dark 变体 + `.nop-code-editor-colorize` 等宽字体/white-space/tab-size。
  - `__tests__/code-editor-colorize.test.tsx`（新增 21 cases）：`getLanguageParser` 12 语言对齐 + plaintext→null；`ColorizeView` javascript/json/sql 产出 token spans + plaintext 降级 + style 注入 + dark theme marker；integration：colorize:true 无 `.cm-editor` + token spans + 非 colorize 仍渲染 editor + diffValue 覆盖 colorize 走 MergeView + plaintext colorize 降级。
- **Phase 3（playground demo + e2e）**：
  - `apps/playground/src/pages/code-editor-page.tsx`：新增 3 个 colorize demo（JS/SQL/JSON-dark），form `data` 初始化值（form 不从 field `value` prop 初始化，走 `data`）。
  - `tests/e2e/code-editor.spec.ts`：新增 3 个 e2e（colorize JS/SQL/JSON-dark），程序化断言（`page.evaluate`/`page.locator`）：`[data-colorize-container]` 可见 + `.cm-editor` count=0 + `span[class]` count>0 + code 文本含预期 token + `data-colorize-theme` 对齐。不用截图诊断。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-code-editor/playground/tests/e2e），属 AGENTS.md「Cross-module changes require explicit human gates」分类。Roadmap E3 P2 code-editor colorize 子项 ✅ done。
- **验证输出（full-green）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-code-editor 15 files / 98 tests 全绿（含新增 21 colorize cases）；code-editor e2e 18 passed / 1 skipped（含新增 3 colorize e2e）。

### 2026-06-22 (E3 select option-template region — 全 3 Phase 完成)

- **E3 select option-template region — 全 3 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0740-1-e3-select-option-template-region-plan.md`。把 E1a 显式 Deferred 到 E3 的 select **option 模板渲染**收口：select 每个 option 项支持自定义展示（icon / 描述 / 双行 / 任意 schema 驱动内容），通过受控参数化 region（`params: ['option', 'index']`）+ per-option scope 绑定实现。底层对齐 `loop` renderer 的 per-item `.render({ bindings })` 先例，`ComboboxItem value={option}` 选中值匹配契约不变。
- **Phase 1（机制裁定 + design.md 决策表）**：裁定选 A（`optionTemplate?: BaseSchema[]` 受控参数化 region），live repo 证据 = `loop.tsx:123-134` 已证 Flux region 编译通道支持 per-item scope 绑定。`docs/components/select/design.md` §2 line 26 从「暂不实现」翻转为「实现」+ Flux 理由；§4 加 `optionTemplate` 字段定义（类型 + `$slot` 引用约定 + groups/virtual/multiple 兼容性）；§5 字段分类加 `optionTemplate: region`；§6 regions 节翻转为「已落地」；§12 风险节翻转 + 引用 Failure Path。
- **Phase 2（schema + 渲染实现 + focused 单测）**：
  - `schemas.ts` `SelectSchema` 加 `optionTemplate?: BaseSchema[]`；`input.tsx` select definition `fields` 加 `{ key: 'optionTemplate', kind: 'region', params: ['option', 'index'] }`。
  - `input-choice-renderers.tsx`：`ChoiceOption` 加 `[key: string]: SchemaValue` 索引签名 + `sanitizeChoiceOptions` spread 原始 entry 以保留额外字段（icon/description 等）供 region 引用；`renderComboboxItem` 新增第三参数 `renderOptionTemplate?: OptionTemplateRenderer`（try/catch + console.warn 覆盖 Failure Path `option-template-region-compile-fail`）；`StaticComboboxList`（flat + groups）与 `VirtualizedComboboxList` 均透传 `renderOptionTemplate`；`SelectRenderer` 从 `props.regions.optionTemplate` 构造 per-option `.render({ bindings: { option, index } })`。
  - 新增 `select-option-template.test.tsx` 5 用例（custom content renders / value-matching preserved / fallback to label / missing-field graceful degradation / virtualized + custom content），全通过。
- **Phase 3（playground demo + e2e）**：`select-lab-page.tsx` 新增第三个 scenario「Custom option template (icon + label + description + badge)」（region 嵌套 container + icon + 双行 text + badge，option 数据带 `role`/`badge` 额外字段）；`simple-form.spec.ts` select describe 新增 e2e 用例（程序化断言：option 富文本可见 + 选中后 trigger `toHaveValue('Alice Chen')`），通过。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-renderers-form/playground/tests/e2e），属 AGENTS.md「Cross-module changes require explicit human gates」分类。Roadmap E3 P2 select option-template 子项 ✅ done。
- **验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26（0 errors，1 pre-existing `useVirtualizer` warning）；`pnpm --filter @nop-chaos/flux-renderers-form test` = 40 files / 371 tests 全绿（含新增 5 用例）；e2e 新增 select optionTemplate 用例 1/1 GREEN。注：`simple-form.spec.ts:231` 既有 select e2e 用例在 clean baseline（git stash 验证）已失败（asserts `toContainText('uk')` 但 trigger 正确显示 label `'United Kingdom'`），非本次回归。

### 2026-06-22 (E3 chart 视觉配置增强 — 全 4 Phase 完成 + 独立 closure audit pass)

- **E3 chart 视觉配置增强（area/legend/stacked/grid/colors）— 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0528-2-e3-chart-visual-configuration-plan.md`。把 `chart`（`flux-renderers-data`，P3）从「4 类型 + 固定视觉参数」补齐为覆盖常见 dashboard 视觉配置：显式 `legend` 开关、`stacked` 堆叠、`grid` 网格开关、自定义 `colors` 调色板、新增 `'area'` 图表类型。全部在 recharts 原生能力范围内（分析报告 §0.2/§5 拒绝 echarts config 透传/扩展/geo/主题）。
- **Phase 1（design.md Flux 决策表 + 字段裁定）**：`docs/components/chart/design.md` §2 从叙述性 bullet 重写为 Flux 决策表（列：能力/采纳/不采纳/理由，13 行：5 项采纳 + echarts config 透传/扩展/geo/主题/双轴/数据缩放/复杂 tooltip slot/组件级 api 不采纳 + `component:resize` watch-only）；§4 字段清单加 `legend`/`stacked`/`grid`/`colors` + `chartType` 扩展 `'area'`；§5 字段分类同步；§12 colors override 不改双入口语义。
- **Phase 2（schema + definition 字段声明）**：`chart-schemas.ts` `ChartType` 加 `'area'`，`ChartSchema` 加 `legend?/stacked?/grid?/colors?: string[]`；`data-renderer-definitions.ts` chart `fields` 加 4 个 `{kind:'prop'}`。局部 typecheck 双绿。
- **Phase 3（视觉配置实现）**：`chart-renderer.tsx`：
  - 导入 `AreaChart`/`Area`；`isChartType` 含 `'area'`；新增 area 分支（`AreaChart` + 受 gate 的 `CartesianGrid`/`ChartLegend` + `Area type="monotone" dataKey stroke/fill stackId?(stacked)`）。
  - `showLegend = legend ?? hasMultipleSeries`（显式值覆盖启发式），gate 全部 5 处 `ChartLegend`。
  - `showGrid = grid ?? true`，gate 全部 4 处 cartesian `CartesianGrid`（pie 无）。
  - `stacked` 对 Bar/Area 系列加 `stackId="a"`；pie/scatter 忽略。
  - `palette = (colors 非空数组) ? colors : COLORS` 替换全部 9 处 `COLORS[i%...]`/`COLORS[0]` 引用（仅余声明 + 回退）。
- **执行期裁定（live repo truth，诚实记录）**：recharts v3.8.1 的 `Line.d.ts` **不再暴露 `stackId`**（仅 `Area.d.ts`/`Bar.d.ts` 含 `stackId?: StackId`）。plan 原文「bar/line/area」按 live recharts 类型修正为「bar/area」：Line 不加 stackId（类型不支持；recharts 内部亦会忽略），design.md §2/§4 同步该 live 事实。这是忠于「recharts 原生能力」design intent 的忠实偏差，非 defect、非静默降级。
- **Phase 4（focused 单测 + playground demo + e2e）**：
  - `chart-renderer.unit.test.tsx`：recharts mock 补 `AreaChart`/`Area`；新增嵌套 describe「visual configuration」10 用例（area 类型 / legend off / legend on / stacked bar / stacked area / stacked-pie-ignored / grid off / grid default / colors override / colors-empty 回退），全用 `simplifyProps` 精确断言 recharts 组件 props（含 `"stackId":"a"` / 自定义色 / 默认 `hsl(var(--chart-1))`）。
  - Playground：`chart-lab-page.tsx` 从 2 场景扩到 6（bar / line / area / stacked-bar / custom-colors+grid-off / legend-toggle 单 series）。
  - e2e：`data-renderers.spec.ts` chart describe 新增 area + stacked bar 两条（scenario slug 对齐 lab 标题）。
- **Closure 裁定（透明记录）**：Plan Status → `completed`；Closure Gates 全 `[x]`。**本次独立 closure-audit 由 fresh general 子 agent 执行**（ses_113bc4c71ffe4YtwA6pYx2vWAm，未参与实现、fresh context、独立重跑测试 + 读 live 源码），verdict `pass`、1 轮；满足 AGENTS.md「executor must spawn a fresh sub-agent session to audit」要求（执行 session 未自审）。Roadmap E3 P2 chart 子项 ⏳ planned → ✅ done。
- **验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26（0 errors）；`pnpm --filter @nop-chaos/flux-renderers-data test` = 50 files / 449 tests 全绿（含新增 10 用例）；e2e `data-renderers.spec.ts` 9/9 GREEN（含新增 area + stacked）；`pnpm test`（workspace unit）一次出现 1 个与 chart 无关的 flaky playground row-unmount 用例失败，重跑 88/88 全绿。未单独跑全量 e2e 套件（仅 chart 相关子集），故未触发 AGENTS.md「full-green commit」规则。

### 2026-06-22 (E3 dynamic-renderer autoLoad gate + component:refresh — 全 4 Phase 完成)

- **E3 dynamic-renderer 加载门控 — 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0528-1-e3-dynamic-renderer-autoload-gate-plan.md`。把 `dynamic-renderer`（`flux-renderers-basic`）从「mount 即自动触发 `loadAction`、无句柄」补齐为「作者可控加载时机」：新增 `autoLoad?: boolean`（缺省 `true`，向后兼容）门控 mount 自动加载，并落地 `component:refresh` 句柄（design.md §8 承诺但未实现），让作者能按需触发 schema 重新加载。模式对偶于 data-source 的 `initFetch: false` + `component:refresh`（X4），是同一 Flux 原生门控在不同 owner 上的投影。
- **Phase 1（design.md Flux 决策表 + 范围裁定）**：`dynamic-renderer/design.md` §2 新建 Flux 决策表（6 行：`autoLoad` 采纳 / `component:refresh` 采纳 / amis 组件级 api 族不采纳 / fallback/empty/errorMode/onError 后续 / component:cancel 不采纳 / loadWhen 后续）；§4 `autoLoad` 加入正式字段清单；§5 字段分类加 `autoLoad`（prop, valueType boolean）；§7 `autoLoad:false` 初始态裁定（loading:false + error:undefined，停留 body/空态）；§8 `component:refresh` 升级为正式契约 + 4 条 failure paths。
- **Phase 2（schema + definition 字段声明）**：`packages/flux-core/src/types/schema.ts` `DynamicRendererSchema` 新增 `autoLoad?: boolean`（含 JSDoc）；`packages/flux-renderers-basic/src/basic-renderer-definitions.ts` `dynamic-renderer` definition 新增 `{key:'autoLoad', kind:'prop', valueType:'boolean'}` field + `componentCapabilityContracts: [{handle:'refresh', displayName:'Refresh', description:'...'}]`。局部 typecheck 双绿。
- **Phase 3（autoLoad gate + component:refresh 实现）**：`packages/flux-renderers-basic/src/dynamic-renderer.tsx` 重构：
  - `createDynamicRendererState(loadAction, autoLoad)`：`autoLoad:false` 时返回 idle state（loading:false + error:undefined），否则维持既有 `loading: Boolean(loadAction)` + required 错误行为。
  - mount `useEffect` gate：`autoLoad && loadActionKey` 才自动 `run()`；`autoLoad:false` 跳过，组件渲染 body region 不进 loading 态。
  - `loadSchemaRef` 共用 load pipeline：提取 `run()` 为 ref 持有的稳定回调（mount effect 与 component:refresh 句柄共用），`run()` 内部 re-evaluate loadAction（try-catch 包裹，覆盖 refresh-eval-error failure path）+ abort 在途旧请求（`loadSchemaRef.current?.abort()`）+ 复用既有 abort/状态机/stale-clear 行为。
  - `component:refresh` 句柄注册：`useCurrentComponentRegistry()` + `componentRegistry.register(handle, {cid})`；`invoke('refresh')` 调 `loadSchemaRef.current.run()`；`hasMethod`/`listMethods` 对齐 `['refresh']`；不支持方法返回 `{ok:false, error}`。
- **Phase 4（focused 单测 + playground demo + e2e）**：
  - `packages/flux-renderers-basic/src/__tests__/basic-dynamic-renderer.test.tsx` 新增 7 用例（14 总）：契约发布（autoLoad valueType + refresh contract）/ `autoload-false-no-fire`（fetcher call count=0 + 无 spinner + body 可见）/ `refresh-triggers-load`（click refresh → fetcher 调用 + schema 出现）/ `refresh-no-loadaction`（refresh onError caught + fetcher=0 + state 不变）/ `refresh-while-loading`（二次 refresh abort 旧请求 + 新 schema 胜出）/ `refresh-eval-error`（dispatch 失败 → onError caught + fetcher=1）/ 缺省 autoLoad:true 无回归。
  - Playground：`apps/playground/src/component-lab/renderers/dynamic-renderer-lab-page.tsx` 新增「On-demand load via autoLoad:false + component:refresh」场景（button 触发 component:refresh，演示按需加载）。
  - e2e：`tests/e2e/component-lab/action-logic.spec.ts` 新增「autoLoad:false defers load until component:refresh is triggered」（mount → body 可见 + loaded schema 不可见 → click Load Schema → schema 出现 + body 消失）。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-core/flux-renderers-basic/playground/tests/e2e），属 AGENTS.md「Cross-module changes require explicit human gates」分类。
- **验证输出（full-green）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-renderers-basic 26 files / 331 tests 全绿（含新增 7 用例）；e2e dynamic-renderer 3/3 GREEN（含新增 1 用例）。

### 2026-06-22 (E3 tree display search/icon/guide-line — 全 4 Phase 完成)

- **E3 树展示组件 UX 增强（tree 搜索/过滤 + 节点图标 + 缩进引导线）— 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0330-3-e3-tree-display-search-icons-plan.md`。把 `tree`（展示树，`flux-renderers-data`）从「仅展开/收起 + 节点模板」补齐为覆盖常见 P2 树展示 UX：本地子串搜索过滤（自动展开匹配祖先链 + 高亮 + 清空恢复）、节点图标（`showIcon`/`iconField`）、缩进引导线（`showGuideLine`）。选择/值绑定归 input-tree/tree-select（E0b/E2d 已收口），拖拽裁定 Deferred（无共享 dnd 工具 + 层级 DnD 复杂）。
- **Phase 1（X5 决策表 + 4 条 Decision）**：
  - `tree/design.md` §2 新建 Flux 决策表子节（保留现有「与 AMIS 的关系」叙述，按 X5 4 列模式补：能力/采纳/不采纳/理由），列：searchable/showIcon+iconField/showGuideLine 实现、选择/值绑定/勾选不采纳（归 input-tree/tree-select）、节点拖拽后续（Deferred + 无共享 dnd 工具 + 层级 DnD 复杂）、节点 CRUD 后续、异步懒加载后续、虚拟滚动不采纳（E2d out-of-scope）、远程搜索不采纳（归 data-source）、amis nodeBehavior/itemActions/enableNodePath/api/mobileUI 不采纳。
  - 新建 §2.1 关键裁定（E3 实现依据）4 条：搜索 open-state 管理（受控展开覆盖 `searchForcedOpenNodeIds`，搜索期间不写本地态，清空自然恢复快照）/ `showIcon` 与 node region 关系（region 自定义优先，showIcon 仅在无 region 的 label 回退路径生效）/ 引导线实现（按 depth 渲染 `border-l` spacer，每节点 depth 个）/ 拖拽裁定（Deferred，`optimization candidate`，`Successor Required: no`）。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-data/src/__tests__/tree-display-ux.test.tsx`（8 用例）。覆盖：searchable 渲染搜索框/缺省无搜索框、搜索过滤+祖先链自动展开（Failure Path `search-deep-tree`）、高亮 marker（`search-highlight`）、无匹配 empty 提示（`search-no-match`）、清空恢复快照（`search-clear`）、showIcon+iconField 渲染图标/节点无 iconField 留空（`iconfield-missing`）/缺省无图标、showGuideLine 按 depth 渲染引导线 marker/缺省无引导线。RED baseline = 8 failures，既有 431 tests 全过。
- **Phase 3（schema + runtime GREEN）**：
  - `schemas.ts`：`TreeSchema` 新增 `searchable?: boolean` / `showIcon?: boolean` / `iconField?: string` / `showGuideLine?: boolean`。
  - `tree-renderer.tsx`：
    - 新增 `computeTreeSearch`（递归 walk，返回 `{query, active, visibleNodeIds, forcedOpenNodeIds}`；matchedNodeIds=子串命中，forcedOpenNodeIds=匹配节点的所有祖先链；visibleNodeIds=matched∪forcedOpen）+ `renderHighlightedLabel`（大小写不敏感子串高亮，多命中全标 `<mark data-slot="tree-search-highlight">`，保留原大小写）+ `renderNodeIcon`（`getIn(node, iconField)` → `resolveLucideIcon`，缺字段返回 null 不渲染 marker）。
    - TreeRenderer 新增 `searchQuery` useState + searchState（searchable 时 compute，否则 EMPTY_TREE_SEARCH）+ 顶部搜索框（`@nop-chaos/ui` Input `data-slot="tree-search-input"` type=search）+ 无匹配时渲染 `tree-empty`（复用 empty region 或默认 noData，搜索框保持可见）。
    - TreeNodeRenderer 新增 `searchState`/`showIcon`/`iconField`/`showGuideLine` props；`effectiveOpen = searchActive ? forcedOpenNodeIds.has(id) : localOpen`；搜索期间 `handleOpenChange` no-op（不写本地态，保快照）；搜索期间非 visible 节点返回 null（DOM 隐藏）；`childRenderCount = searchActive ? childNodes.length : renderedChildCount`（搜索绕过批量挂载）；引导线 spacer（`showGuideLine && depth>0` 时渲染 `depth` 个 `data-slot="tree-guide-line"` `w-4 border-l`，同时切 row 缩进为 spacer-based）；图标在无 region 的 label 回退路径 prepend（region 优先）。**Hooks 顺序安全**：所有 useState/useEffect 先无条件调用，再 `if (searchActive && !visible) return null`。
  - `data-renderer-definitions.ts`：tree fields 注册 `searchable`/`showIcon`（valueType boolean）/`iconField`/`showGuideLine`（valueType boolean）为 prop。
- **Phase 4（owner-doc + playground + e2e + roadmap）**：
  - `tree/design.md` §4（schema 新字段）/§5（字段分类含新 prop）/§9（新增 §9.1 icon 与 region 关系 + §9.2 DOM markers 表）/§10（Empty State 补搜索无匹配）同步落地。
  - Playground：`apps/playground/src/pages/tree-display-ux-demo.tsx`（4 棵树演示：纯 searchable、纯 showIcon、纯 showGuideLine、三能力叠加），注册到 App.tsx + route-model.ts（id `tree-display-ux`）。
  - e2e：`tests/e2e/tree-display-ux.spec.ts`（6 用例：搜索过滤+祖先链自动展开+高亮、清空恢复快照、无匹配 empty、showIcon 图标、showGuideLine 引导线、三能力叠加）。
  - `docs/components/tree/example.json` 增强为演示新能力（searchable+showIcon+iconField+showGuideLine）。`examples.manifest.json` 裁定：tree 已登记 runtime，本 plan 未引入新 renderer type。
  - Roadmap：E3 P2 注记 tree display ✅ done。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-renderers-data/playground/tests/e2e），属 AGENTS.md「Cross-module changes require explicit human gates」分类。
- **验证输出（full-green）**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-renderers-data 50 files / 439 tests 全绿（含新增 8 用例）；playground route-matrix 19 files / 88 tests 全绿；e2e tree-display-ux 6/6 GREEN。

### 2026-06-22 (E3 form input controls input-number/array-editor/key-value — 全 5 Phase 完成)

- **E3 表单输入控件增强 — 全 5 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0330-2-e3-form-input-number-array-keyvalue-plan.md`。把 `input-number`（长按连续步进）与 `array-editor`/`key-value`（min/max + 上下移动重排）三个表单输入控件从最小可用补齐为覆盖常见 P2 输入交互。
- **Phase 1（X5 决策表 + 3 Decision）**：
  - `input-number/design.md` §2 从「AMIS 对照表」重命名为「Flux 决策表（X5 扩展，E3）」（列主语改为能力/首版决定/理由），新增「长按连续步进」实现行 + formatter/parser 不采纳行。
  - `array-editor/design.md` / `key-value/design.md` 各新建 §2 Flux 决策表节（minItems/maxItems 实现、reorder 实现、component:moveItem 从 future 翻转、多列/类型化 schema/拖拽/复制/deleteConfirm 不采纳或后续 + 理由）。
  - 3 条 Decision：长按时序（初始延迟 400ms + 重复间隔 80ms + clamp 停止 + pointerup/leave/blur/ESC 取消 + steppedViaLongPressRef 守卫）/ reorder 机制（上下移动按钮非 drag，对接 moveValue，scope owner 回退走 scope.update 保持 enabled）/ maxItems UX（新增按钮 disabled 非隐藏）。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-form/src/__tests__/input-number-long-press.test.tsx`（6 用例：连续步进/clamp 停止/pointer-up 取消/pointer-leave 取消/短按 onClick 单步/showStepper=false 无回归，用 vi.useFakeTimers + advanceTimersByTimeAsync）+ `packages/flux-renderers-form-advanced/src/__tests__/array-keyvalue-min-max-reorder.test.tsx`（12 用例：minItems 禁删/maxItems 禁增/缺省回退/上移下移 moveValue/首末行禁用/scope owner 回退）。RED = 4 input-number + 12 array/key-value failures。
- **Phase 3（input-number 长按步进 GREEN）**：`input-number-renderer.tsx` 新增 `commitStep`（读 latestValueRef，复用 clamp+precision）+ `startLongPress`/`cancelLongPress`（initialDelay setTimeout 400ms → interval setInterval 80ms，clamp 时 clearInterval）+ `handleStepperPointerDown/Up/Click`（onPointerDown preventDefault 启动、onPointerUp/Leave 取消、onClick 用 steppedViaLongPressRef 守卫抑制长按后的多余单步）+ `latestValueRef`（useEffect 同步外部变更 + commitStep 内 optimistic update 满足 react-hooks/refs lint）。stepper 按钮 onPointerDown/onPointerUp/onPointerLeave/onClick 全接线。
- **Phase 4（array-editor/key-value min/max + reorder GREEN）**：
  - `schemas.ts`：`ArrayEditorSchema`/`KeyValueSchema` 各新增 `minItems?: number`（缺省 1）/`maxItems?: number`（缺省无上限）。
  - `array-editor.tsx`/`key-value.tsx`：validation `collectRules` 读 schema minItems（替代硬编码 1）+ 新增 maxItems 规则；每行新增上移/下移按钮（`data-slot="array-editor-move-up/down"`、`key-value-move-up/down"`，调 `currentForm.moveValue(name, from, to)`）；边界 disabled（首行禁上移、末行禁下移、达到 minItems 禁删、达到 maxItems 禁增）；无 form runtime 时镜像 append/remove 回退（调 `scope.update` 重排，按钮保持 enabled，Failure Path `movevalue-scope-fallback`）。
- **Phase 4 无回归调整**：`key-value.test.tsx` 一处旧用例（scope-sync on remove-to-empty）显式配 `minItems: 0` 保留其验证意图；`fluxBasicPageSchema.json` 的「Submit array demo」（单 reviewer）同理配 `minItems: 0`；`flux-basic-page.debugger.test.tsx` 的 remove 按钮选择器改 locale-agnostic regex `/^(?!Move (up|down)).*Reviewer 1$/`（排除新增 move 按钮，兼容 zh-CN locale 下「删除 Reviewer 1」）。
- **Phase 5（owner-doc + playground + e2e）**：三 design.md §4（schema）/§5（字段分类）/§10（DOM marker）同步落地；playground 新增 `apps/playground/src/pages/form-input-enhancements-demo.tsx`（演示长按步进 + min/max + 上下移动），注册到 App.tsx + route-model.ts（id `form-input-enhancements`）；e2e `tests/e2e/form-input-enhancements.spec.ts`（3 用例：input-number 长按连续步进 + clamp、array-editor move-up 重排 + 首行禁用、key-value move-down 重排 + 末行禁用，读 input value 而非 `${array}` 表达式避免对象序列化）。`examples.manifest.json` 裁定：三组件已登记 runtime，本 plan 未引入新 renderer type，demo 页经 route-model 注册（与既有 E3 demo 一致）。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-renderers-form/form-advanced/playground/tests/e2e），属 AGENTS.md「Cross-module changes require explicit human gates」分类。
- **验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-renderers-form 39 files / 366 tests、flux-renderers-form-advanced 80 files / 761 tests、playground 19 files / 88 tests 全绿；e2e form-input-enhancements 3/3 GREEN。

### 2026-06-22 (E3 layout family flex/page/tabs — closure audit 收口)

- **Closure audit 收口**：独立 closure-audit 子 agent（fresh session）已完成对 `docs/plans/2026-06-22-0330-1-e3-layout-family-flex-page-tabs-plan.md` 的独立核对（非信任 `[x]` 标记，直接读 live repo）：`schemas.ts`（FlexSchema direction reverse/justify evenly/align baseline/alignContent、PageSchema aside/asidePosition/asideClassName/subTitle/remark、TabsItemSchema badge/icon/mountOnEnter/unmountOnExit）、`flex.tsx`（FLEX_ALIGN_CONTENT_CLASS_MAP + cn()）、`page.tsx`（aside region + subTitle + remark Tooltip + 空 aside 折叠）、`tabs.tsx`（resolveTabBadge/resolveTabIcon/resolveTabKeepMounted + activated-set）、`basic-renderer-definitions.ts`（aside region + props 注册）、22 单测 + 9 e2e specs、三 design.md §2 Flux 决策表 + §4/§5/§10。Closure Gates 全 `[x]`（含独立审计项），Closure Audit Evidence 已填实证据（file:line 引用），无 `<<...>>` 占位。Plan 五点一致：Plan Status `completed` / 4 Phase 全 `completed` / Exit Criteria 全 `[x]` / Closure Gates 全 `[x]` / 本日志记录一致。
- **再验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过（FULL TURBO，全 cached）。

### 2026-06-22 (E3 layout family flex/page/tabs — 全 4 Phase 完成)

- **E3 布局族能力补齐（flex 枚举扩展 + page aside + tabs per-tab badge/icon/mountOnEnter）— 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0330-1-e3-layout-family-flex-page-tabs-plan.md`。把 `flex`/`page`/`tabs` 三个布局族组件（同属 `flux-renderers-basic`）从最小可用补齐为覆盖常见 P2 布局场景。
- **Phase 1（X5 决策表 + 3 条 Decision）**：
  - `flex/design.md` §2 新建 Flux 决策表（direction reverse 实现、justify evenly 实现、align baseline 实现、alignContent 实现、flex-item per-child 不采纳/后续、自由 style 不采纳 styling contract、amis draggable 不采纳）。
  - `page/design.md` §2 新建 Flux 决策表（aside region 实现、asidePosition 实现、subTitle/remark 实现、initApi/interval/polling 不采纳请求下沉 data-source、toolbar 独立 region 不采纳 header subsume、pullRefresh 归 mobile-roadmap、css/cssVars 不采纳）。
  - `tabs/design.md` §2 新建 Flux 决策表（per-tab badge 实现、per-tab icon 实现、per-tab mountOnEnter/unmountOnExit 实现、addable/closable/draggable/editable/hash/source 不采纳引用 §3/§17/§19）。
  - 3 条 Decision 裁定写入三个 design.md（alignContent 与 align 语义边界 / aside 布局与空 aside 折叠 / mountOnEnter 与 unmountOnExit 优先级）。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-basic/src/__tests__/layout-family-enhancements.test.tsx`（22 用例）。覆盖：flex row-reverse/column-reverse/justify-evenly/items-baseline/content-center/content-between/缺省无回归；page aside 渲染/left DOM 顺序/right DOM 顺序/空 aside 折叠/无 aside 无回归/subTitle/remark/无 subTitle/remark 无回归；tabs badge=5/icon=user/badge 非数字非字符串 String 兜底/无 badge/icon 无回归/mountOnEnter 懒挂载/unmountOnExit 卸载/缺省 keepMounted=true 无回归。RED baseline = 16 feature failures + 6 baseline passes。
- **Phase 3（Fix GREEN）**：
  - `schemas.ts`：`FlexSchema` direction 加 `'row-reverse' | 'column-reverse'`、justify 加 `'evenly'`、align 加 `'baseline'`、新增 `alignContent?`；`PageSchema` 新增 `aside?`/`asidePosition?`/`asideClassName?`/`subTitle?`/`remark?`；`TabsItemSchema` 新增 `badge?`/`icon?`/`mountOnEnter?`/`unmountOnExit?`。
  - `utils.ts`：`resolveDirection` 扩展 `column-reverse → flex-col-reverse`、`row-reverse → flex-row-reverse`。
  - `flex.tsx`：FlexRenderer 用 `FLEX_ALIGN_CLASS_MAP`/`FLEX_JUSTIFY_CLASS_MAP`/`FLEX_ALIGN_CONTENT_CLASS_MAP` 映射新枚举到 Tailwind class（`items-baseline`/`justify-evenly`/`content-*`），缺省回退不应用 class。
  - `page.tsx`：PageRenderer 读 `props.schema.aside` 判定空 aside 折叠（`Array.isArray && length > 0`）；渲染 aside region（`data-slot="page-aside"`）按 `asidePosition`（left/right）控制 DOM 顺序；渲染 subTitle（`data-slot="page-subtitle"`）+ remark（`@nop-chaos/ui` Tooltip + `data-slot="page-remark"` + info icon）。
  - `tabs.tsx`：TabsRenderer per-tab 渲染 badge（`@nop-chaos/ui` Badge + `data-slot="tab-badge"`，`String()` 兜底）+ icon（`resolveLucideIcon` + `data-slot="tab-icon"`）；`resolveTabKeepMounted` 按 Phase 1 Decision 优先级（mountOnEnter 优先 + unmountOnExit 正交）覆盖全局 `keepMounted`；用 `useState<ReadonlySet<string>>` 追踪已激活 tab（React 19 setState-during-render 模式，满足 `react-hooks/refs` lint 规则）。
  - `basic-renderer-definitions.ts`：page fields 注册 `aside` region + `subTitle`/`remark`/`asidePosition` prop。
- **Phase 4（Owner-doc + playground + e2e + roadmap）**：
  - design.md §4（schema）/§5（字段分类）/§10（DOM marker）同步落地，与 runtime 一致。
  - Playground：`apps/playground/src/pages/layout-family-enhancements-demo.tsx`（演示 flex row-reverse/evenly/baseline/content-center、page aside left+subTitle+remark/right、tabs badge+icon+mountOnEnter+unmountOnExit），注册到 App.tsx + route-model.ts（id `layout-family-enhancements`）。
  - e2e：`tests/e2e/layout-family-enhancements.spec.ts`（9 用例：flex row-reverse/justify-evenly/items-baseline/content-center class 应用、page aside + subTitle + remark 渲染、page asidePosition=right DOM 顺序、tabs badge+icon 渲染、tabs mountOnEnter 懒挂载、tabs unmountOnExit 卸载）。
  - Roadmap：E3 P2 注记 flex/page/tabs 布局族 ✅ done。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-renderers-basic/playground/tests/e2e），属于 AGENTS.md「Cross-module changes require explicit human gates」分类。
- **验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-renderers-basic 26 files / 317 tests 全绿（含新增 22 用例）。

### 2026-06-22 (E3 text/icon visual fields — 全 4 Phase 完成)

- **E3 基础展示组件视觉字段 — 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0149-3-e3-basic-display-visual-fields-plan.md`。把 `text` 与 `icon` 两个基础展示组件从最小可用补齐为覆盖常见 P2 场景：text 支持 `copyable`（一键复制 + clipboard + toast）与 `maxLine`（line-clamp 截断），icon 支持 schema 级 `size`（替代 `icon.tsx:18` 硬编码 `size={16}`）与 `color`（inline style）。
- **Phase 1（X5 决策表 + 3 条 Decision）**：
  - `text/design.md` §2 新建 Flux 决策表（copyable 实现、maxLine 实现纯 line-clamp 无 toggle、富文本/markdown 不采纳归 markdown 组件、amis tpl 不采纳、click/hover 事件不采纳走 X2 通用事件）。
  - `icon/design.md` §2 新建 Flux 决策表（size 实现 number 像素缺省 16、color 实现 inline style、decorative/title 后续、预设 token follow-up、echarts/自定义 SVG 不采纳）。
  - 3 条 Decision 裁定写入两个 design.md（maxLine 不带 toggle / size 用 number / color 用 inline style）。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-basic/src/__tests__/text-icon-visual-fields.test.tsx`（11 用例）。覆盖：copyable 渲染按钮/clipboard writeText + toast/copyable 缺省无按钮/clipboard-unavail 降级；maxLine=2 line-clamp-2/maxLine=3 line-clamp-3/缺省无 line-clamp；icon size=24 覆盖硬编码 16/缺省 16；icon color inline style/缺省无 color。RED baseline = 7 feature failures + 4 baseline passes（无回归验证）。
- **Phase 3（Fix GREEN）**：
  - `schemas.ts`：`TextSchema` 新增 `copyable?: boolean` / `maxLine?: number`；`IconSchema` 新增 `size?: number` / `color?: string`。
  - `text.tsx`：TextRenderer 读 copyable（渲染 `TextCopyButton`，`@nop-chaos/ui` Button `variant="ghost" size="icon-xs"` + `data-slot="text-copy-button"` + `copyToClipboard` + `toast.success`/`toast.error`）与 maxLine（`resolveMaxLineClass` → `line-clamp-{N}`，`>100` 截断为 `line-clamp-100`）。
  - `copy-to-clipboard.ts`：新建本地 clipboard helper（navigator.clipboard.writeText + execCommand 降级 + Failure Path `clipboard-unavail` toast.error），因 `flux-renderers-basic` 不能依赖 `flux-renderers-data`。
  - `icon.tsx`：IconRenderer 读 size（缺省 16，`Math.max(1, Math.floor)` 防 NaN/负数）传 lucide `size` prop；color 映射 `style={{ color }}`（缺省 `undefined`，保留 `meta.className` 优先级）。
  - `basic-renderer-definitions.ts`：text fields 注册 `copyable`/`maxLine`；icon fields 注册 `icon`/`size`/`color`（此前 icon 无 fields 声明）。
  - `basic-renderer-contracts.test.ts`：更新 text fields 断言包含新字段。
- **Phase 4（Owner-doc + playground + e2e + roadmap）**：
  - design.md §4（schema）/§5（字段分类）/§10（DOM marker）同步落地，与 runtime 一致。
  - Playground：`apps/playground/src/pages/text-icon-visual-fields-demo.tsx`（演示 copyable 复制邮箱、maxLine 2/3 截断长文本、icon default/24/32+color/heart+red），注册到 App.tsx + route-model.ts（id `text-icon-visual-fields`）+ pages/index.ts。
  - e2e：`tests/e2e/text-icon-visual-fields.spec.ts`（6 用例：copyable clipboard 复制 + grantPermissions/readText 验证、maxLine=2 line-clamp-2、maxLine=3 line-clamp-3、icon size=24 覆盖 16、icon color inline style rgb 归一化、icon 缺省 size=16）。
  - Roadmap：E3 P2 注记 text+icon 视觉字段 ✅ done。
- **Closure audit**：独立子 agent（fresh session, ses_114601d55ffeKKBSWjQ1ywYpwk）16/16 checklist 全 PASS，zero blockers。
- **验证输出**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26、`pnpm test` = 49/49 tasks 全过；flux-renderers-basic 25 files / 295 tests 全绿（含新增 11 用例）。e2e：`text-icon-visual-fields.spec.ts` 6/6 全过。

### 2026-06-22 (E3 boolean control value contract — 全 4 Phase 完成)

- **E3 布尔控件值契约规范化 — 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0149-2-e3-boolean-control-value-contract-plan.md`。把 `checkbox`/`switch` 的布尔值映射从硬编码 `true`/`false`（`input-choice-renderers.tsx` 的 `Boolean(checked)`）规范化为 schema 可配的 `trueValue`/`falseValue` 契约，使布尔控件可存 `1/0`、`"yes"/"no"`、`"Y"/"N"` 等业务值。同时补齐 checkbox/switch/radio-group 三个组件缺失的 X5 Flux 决策表。
- **Phase 1（X5 决策表 + radio-group 裁定）**：
  - `checkbox/design.md` §2 新建 Flux 决策表（trueValue/falseValue 实现、indeterminate 不采纳/后续、amis option 数组语法不采纳）。
  - `switch/design.md` §2 新建 Flux 决策表（trueValue/falseValue 实现、与 checkbox 语义边界、amis 旧字段不采纳）。
  - `radio-group/design.md` §2 新建 Flux 决策表。
  - **radio-group 裁决：不采纳 trueValue/falseValue** — `RadioGroupRenderer` 已用 `stringValueAdapter` + options 任意值，boolean-radio 场景已被 `options: [{label:'是', value:'Y'}, ...]` subsume；引入 trueValue/falseValue 会产生双真值源。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-form/src/__tests__/boolean-control-value-contract.test.tsx`（11 用例）。覆盖：checkbox 自定义 1/0 映射、缺省 true/false 无回归、initial-value-custom、value-neither（值原样保留）、only-trueValue-set（falseValue 缺省为 false）；switch 自定义 yes/no 映射、onLabel/offLabel 与值映射正交、缺省无回归、initial-value-custom、value-neither。RED baseline = 9 failures（所有 trueValue/falseValue 相关用例）。
- **Phase 3（Fix GREEN）**：
  - `packages/flux-core/src/value-adapter.ts`：新增 `booleanMappingAdapter(trueValue, falseValue)` —— `in` 判 `Object.is(value, trueValue)`，`out` 按 `checked ? trueValue : falseValue`；缺省 `true`/`false`（与旧 `booleanStringAdapter.out` 字节兼容）。
  - `packages/flux-renderers-form/src/schemas.ts`：`CheckboxSchema`/`SwitchSchema` 新增 `trueValue?: SchemaValue`/`falseValue?: SchemaValue`（X3 扁平标量）。
  - `packages/flux-renderers-form/src/renderers/input-choice-renderers.tsx`：CheckboxRenderer/SwitchRenderer 读取 schema 的 trueValue/falseValue（缺省 true/false），构造 `booleanMappingAdapter` 实例传给 `useFormFieldController`；移除不再使用的模块级 `booleanValueAdapter` 常量和 `booleanStringAdapter` 导入。
- **Phase 4（Owner-doc + playground + e2e + roadmap）**：
  - design.md §4/§5 同步 trueValue/falseValue 字段落地内容。
  - Playground：`apps/playground/src/pages/boolean-control-value-contract-demo.tsx`（演示 checkbox trueValue:1/falseValue:0 + switch trueValue:"yes"/falseValue:"no" + 缺省回退对照 + 实时值显示），注册到 App.tsx + route-model.ts（id `boolean-control-value-contract`）。
  - example.json：`checkbox/example.json` 改用 trueValue:1/falseValue:0；`switch/example.json` 改用 trueValue:"yes"/falseValue:"no"。
  - e2e：`tests/e2e/boolean-control-value-contract.spec.ts`（4 用例：checkbox 1/0 映射、switch yes/no 映射、checkbox 缺省 true/false、switch 缺省 true/false）。
  - e2e 修复：`tests/e2e/playground-entry-pages.spec.ts` 补齐 `boolean-control-value-contract` + `condition-builder-formula`（前序 plan 遗漏）route assertions，使 `domain route coverage` 数据测试通过。
  - Roadmap：E3 P2 注记布尔控件 trueValue-falseValue 子项 ✅ done。
- **E3 收口验证**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26（1 pre-existing `useVirtualizer` warning 来自 E1a，与本 plan 无关）、`pnpm test` = 49/49 tasks 全过；flux-renderers-form 38 files / 360 tests（含新增 11 boolean-contract 用例）全绿。e2e：`boolean-control-value-contract.spec.ts` 4/4 全过、`playground-entry-pages` route coverage + boolean-control-value-contract smoke 全过。
  - **Pre-existing e2e failures（非本 plan 引入）**：`component-handles.spec.ts` 6 failures + `playground-entry-pages` condition-builder-formula smoke 1 failure（formula eval console errors）—— git stash 验证确认这些在本 plan 工作前已存在。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-core/flux-renderers-form/playground/tests/e2e），属于 AGENTS.md「Cross-module changes require explicit human gates」分类。

### 2026-06-22 (E3 condition-builder formula — 全 4 Phase 完成)

- **E3 condition-builder formula & 表达式补全 — 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-22-0149-1-e3-condition-builder-formula-completion-plan.md`。把 condition-builder 的 `formulas` / `formulaForIf` 从 **DESIGN-ACK-NOT-IMPL**（类型已声明、runtime 静默忽略）收口为 **实现**：condition-item right 侧表达式值槽、`formula` 种子串默认值、`formulaForIf.enabled` 时组级 `if` 标注为可求值表达式、`evaluateFormula` 回调走 `helpers.evaluate` + `helpers.executeSource` + try/catch 降级。
- **Phase 1（Decision + design.md）**：3 条 Decision 落地（`ConditionFormulaConfig` 三字段运行期语义 / formula 求值入口职责分离 `helpers.evaluate` vs `source` executeSource / formula 值槽 UI 形态）。`design.md` §2 决策表 `formulas`/`formulaForIf` 行由 `进 types.ts（DESIGN-ACK-NOT-IMPL，E0d 收口）` 翻转为 `实现（E3 收口）`；§4 补运行期语义；§7.5 新增求值对接契约；§10 补 DOM marker（`condition-formula-value` / `condition-group-if-formula` / `condition-formula-preview`）；§7.4 引用更新。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder-formula.test.tsx`（16 用例）。覆盖：formula 值槽渲染/回写、formula 种子串默认值、enabled=false 无回归、formulaForIf 标记/写入、evaluateFormula 预览/eval error 降级/throw 降级、source integration、DESIGN-ACK-NOT-IMPL eliminated、SchemaRenderer integration。
- **Phase 3（Fix GREEN）**：
  - `condition-builder.tsx`：从 schema 读 `formulas`/`formulaForIf`；导出 `EvaluateConditionFormula` 类型 + `createFormulaEvaluator`（走 `helpers.evaluate` + `helpers.executeSource` + try/catch console.warn 降级）；threading 到 ConditionGroup/PickerModeContent。
  - `condition-group.tsx`：接受 `formulas`/`formulaForIf`/`evaluateFormula` props；`handleAddCondition` 使用 `formulas.formula` 种子串作为新增 item 默认 right；`if` 输入 data-slot 在 `formulaForIf.enabled` 时切换为 `condition-group-if-formula`。
  - `condition-item.tsx`：透传 `formulas`/`evaluateFormula` 到 ValueInput。
  - `value-input.tsx`：新增 `FormulaValueSlot` 组件（`data-slot="condition-formula-value"` + `fx` 视觉前缀 + evaluateFormula 异步预览 + `condition-formula-preview` marker + eval error 降级显示）；`formulas.enabled` 时在 op/between/custom 分派前插入 formula 值槽渲染。
  - `config-test-support.tsx`：`renderGroup` 支持 `formulas`/`formulaForIf`/`evaluateFormula` options。
  - i18n：`flux-i18n` locales（en-US + zh-CN）新增 `formulaValuePlaceholder`/`formulaValueLabel`/`formulaValuePrefix`/`formulaPreview`/`formulaEvalError`。
- **Phase 4（Owner-doc + playground + e2e + roadmap）**：
  - Playground：`apps/playground/src/pages/condition-builder-formula-page.tsx` + `condition-builder-formula-schema.json`（演示 formulas.enabled + formulaForIf + formula seed + 字面量无回归对照），注册到 App.tsx + route-model.ts（id `condition-builder-formula`）。
  - e2e：`tests/e2e/condition-builder-formula.spec.ts`（4 用例：formula 值槽渲染/回写、if formula 标记、字面量无回归）。
  - E0d plan `Follow-up` + `Non-Blocking Follow-ups` 注记「已由 E3 condition-builder formula plan 收口」。
  - Roadmap：E3 P2 注记 condition-builder formula 子项 ✅ done。
- **Lint 修复**：`FormulaValueSlot` 初版在 `useEffect` 内同步 `setPreview(null)` 触发 `react-hooks/set-state-in-effect` rule；重构为 `canEvaluate` 门控 + `displayPreview` 派生值（`canEvaluate ? preview : null`），避免 effect 内同步 setState。
- **E3 收口验证**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26（1 pre-existing `useVirtualizer` warning 来自 E1a，与本 plan 无关）、`pnpm test` = 49/49 tasks 全过；form-advanced 79 files / 749 tests（含新增 16 formula 用例）全绿。e2e 未运行（mission-driver 未要求 full-green e2e；unit + typecheck + build + lint 全绿）。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-renderers-form-advanced/flux-i18n/playground/tests/e2e），属于 AGENTS.md「Cross-module changes require explicit human gates」分类。

### 2026-06-22 (X1 doAction 命令族统一 — 全 4 Phase 完成)

- **X1 doAction 命令族统一 — 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-21-2146-1-x1-doaction-command-family-unification-plan.md`。把 Flux `component:<method>` 句柄族从「少数 owner-renderer 已注册 + 大量 input/surface 控件 design.md 仅写 future」收敛为「全量补齐 + design.md 决策表一致 + 命名统一」。同时收口 E1a/E2e/E2f/X4/E2h 五个前序 plan 显式路由到 X1 作为 successor 的全部 deferred 项。
- **Phase 1（词汇表裁定）**：已在 2026-06-21 完成（`docs/references/component-handle-vocabulary.md` 新建，data-source/code-editor/surface family 裁定写入对应 design.md + `docs/architecture/surface-owner.md` + `docs/architecture/capability-contract-model.md`）。
- **Phase 2（Input 控件 handle 全量补齐）**：
  - 共享 input handle 工厂：`packages/flux-runtime/src/input-component-handle.ts`（`createInputComponentHandle` + `InputHandleBindings`/`InputHandleMethod` 类型）。
  - React hook：`packages/flux-react/src/hooks/use-input-component-handle.ts`（`useInputComponentHandle`）。
  - 11 个 input 控件全部接入并发布 `componentCapabilityContracts`：
    - 标量（clear/reset/focus）：`input-text`/`input-email`/`input-password`/`textarea`/`input-number`。
    - Select（clear/focus/open）：复用 `openMenu` binding。
    - Choice（focus only）：`switch`/`radio-group`/`checkbox-group`（boolean/choice 字段不暴露 clear/reset，语义不清）。
    - Tree（clear/focus）：`input-tree`/`tree-select`（reset 不暴露，tree state 复杂）。
  - Button 接入 `useInputComponentHandle`（focus only），收口 E2e deferred。
  - Tests：`packages/flux-renderers-form/src/__tests__/component-handles-input.test.tsx`（17 用例）、`packages/flux-renderers-form-advanced/src/__tests__/component-handles-tree.test.tsx`（4 用例）、`packages/flux-renderers-basic/src/__tests__/component-handles-button.test.tsx`（2 用例）、`packages/flux-runtime/src/__tests__/input-component-handle.test.ts`（11 用例，覆盖 4 个 failure paths：`x1-focus-not-mounted`/`x1-clear-disabled`/`x1-reset-no-initial`/`x1-focus-hidden`）。
  - 12 个 design.md Flux 决策表更新（input-text/input-email/input-password/textarea/input-number/select/checkbox-group/radio-group/switch/input-tree/tree-select/button）。
- **Phase 3（Surface family handle 补齐）**：
  - 共享 surface handle 工厂：`packages/flux-runtime/src/surface-component-handle.ts`（`createSurfaceComponentHandle` + `SurfaceHandleBindings`/`SurfaceHandleMethod`）。Failure paths：controlled surface → `{ok:true, skipped:true}`；open on open / close on closed → `{ok:true, skipped:true}`。
  - React hook：`packages/flux-react/src/hooks/use-surface-component-handle.ts`（`useSurfaceComponentHandle`）。
  - `packages/flux-renderers-basic/src/use-surface-renderer.ts` 接入 hook：`isOpen` 查 `surfaceRuntime.store.getState().entries`，`setOpen` 调 `store.setUncontrolledOpen(id, next)` —— handle 与既有 `openDialog`/`openDrawer`/`closeSurface` action API **共享同一 `SurfaceRuntime`**，无双状态源。
  - dialog/drawer renderer definition 发布 `componentCapabilityContracts: ['open', 'close', 'toggle']`（`packages/flux-renderers-basic/src/surface-renderer-definitions.ts`）。
  - Tests：`packages/flux-renderers-basic/src/__tests__/component-handles-surface.test.tsx`（9 用例：open/close/toggle on dialog/drawer + `x1-open-no-target` + `x1-close-not-open` + capability contract assertion）。
  - E2f plan `Deferred But Adjudicated` 注记「已由 X1 plan 收口」。
  - `docs/components/dialog/design.md` §8、`docs/components/drawer/design.md` §8、`docs/architecture/surface-owner.md` §Surface Handle Coexistence 同步到「实现」基线。
- **Phase 4（naming alignment + playground + e2e + roadmap）**：
  - data-source naming audit 裁定 (a) 落地（保留双入口 + 文档分层；`docs/components/data-source/design.md` §refresh、`docs/architecture/api-data-source.md` 已含裁定）。
  - code-editor naming audit 裁定「保持现状」（`docs/components/code-editor/design.md` §决策表已含 X1 audit note），收口 E2h watch-only residual。
  - Playground：`apps/playground/src/pages/component-handles-demo.tsx`（演示 14 个 renderer 的 handle 调用：input 族 clear/reset/focus、select open、switch/radio-group/checkbox-group focus、tree clear、form submit/reset、dialog/drawer open/toggle/close、button focus），注册到 playground 路由 `#/component-handles`。
  - e2e：`tests/e2e/component-handles.spec.ts`（6 个 test：input clear/reset/focus、button focus、dialog open+toggle、drawer open+close）；`tests/e2e/playground-entry-pages.spec.ts` 补 component-handles route assertion。
  - Roadmap：`docs/components/existing-components-improvement-roadmap.md` X1 状态 `planned` → `done`，工作项表加 ✅。
  - 5 个 source plan deferred/follow-up 注记收口：E1a、E2e、E2f、X4、E2h。
- **Lint 修复**：`packages/flux-react/src/hooks/use-input-component-handle.ts` + `use-surface-component-handle.ts` 添加 `// eslint-disable-next-line react-hooks/refs` —— `bindingsRef` 是 mutable invoke-time callbacks holder（只在 invoke() event-handler context 读 `.current`，从不在 render 时读），静态 lint rule 无法识别此 pattern，按 ref-as-holder 惯例 disable。两文件对称处理。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项（typecheck/build/lint/test/design sync/roadmap/no-silent-defect/source-plan annotation）全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次为执行 agent 自检 + 跨 Phase 跨模块代码变更（flux-runtime/flux-react/flux-renderers-form/flux-renderers-form-advanced/flux-renderers-basic/playground），属于 AGENTS.md「Cross-module, contract, or architecture changes require explicit human gates between stages」分类。
- **X1 收口验证**：`pnpm typecheck` = 49/49、`pnpm build` = 26/26、`pnpm lint` = 26/26（1 pre-existing `useVirtualizer` warning 来自 E1a，与 X1 无关）、`pnpm test` = 49/49 tasks 全过；新增/扩展测试：`input-component-handle.test.ts`(11)、`component-handles-input.test.tsx`(17)、`component-handles-tree.test.tsx`(4)、`component-handles-button.test.tsx`(2)、`component-handles-surface.test.tsx`(9)。e2e 未运行（mission-driver 未要求 full-green e2e；unit + typecheck + build + lint 全绿）。
- **Deferred 但已裁定**：P2/P3 组件 handle（pagination/wizard/collapse/carousel/cards/alert/service/dynamic-renderer/reaction/dropdown-button/picker）— `out-of-scope improvement`，归 E3 P2 批启动时的新 plan。共享 input handle 工厂推广到 form/table/crud 既有 handle 去重 — Non-Blocking Follow-up。`componentCapabilityContracts` 的 `args`/`result` FluxValueShape 自动从 zod schema 推导 — Non-Blocking Follow-up（adapter only，按 `capability-contract-model.md`）。

### 2026-06-22 (X2 可阻止事件 — 全 4 Phase 完成)

- **X2 可阻止事件（schema-driven preventDefault / stopPropagation）— 全 4 Phase 执行完成，Plan Status → completed。** Plan: `docs/plans/2026-06-21-2146-2-x2-preventable-events-plan.md`。把 Flux 事件系统从「runtime contract 已暴露 `preventDefault`/`stopPropagation`，但 author 无法在 schema 声明式请求 + timing hazard」收敛为「schema-level 声明 + 同步阻塞 native default + 既有命令式路径继续工作」。
- **Phase 1（Decision）**：4 条裁定落地。
  - 字段命名：`ActionShapeFields.preventDefault?: boolean | string` + `stopPropagation?: boolean | string`（与 DOM API 一致，与既有 `when` 同形；拒绝 amis `rendererEvent`）。
  - Timing 模型：sync prevention 发生在 `node-renderer-resolved.tsx` 的 per-event-key handler 内、`helpers.dispatch` 之前。求值顺序 normalize event → preventDefault expression → sync 调 native `event.preventDefault()` → stopPropagation expression → sync 调 native `event.stopPropagation()` → dispatch。`when:false ∧ preventDefault:true` 裁定：prevention 在 `when` 之前/独立求值生效，即 `when:false` 不 RUN action 但仍 sync 阻止 native default。
  - `preventEnterSubmit` 关系：选 (a) 保留作为 form 便捷 shorthand（form `preventEnterSubmit` 控制 form 自己的 enter-submit 逻辑，action 上的 `preventDefault` 控制 native default，二者层级不同、互不冲突）。
  - `stopImmediatePropagation` 不引入（roadmap scope 最小化）。
  - Docs：`docs/architecture/renderer-runtime.md` 新增 "Schema-Driven Prevention" 子节（含 timing contract、orthogonality with when、5 条 failure paths、out-of-scope）、`docs/components/form/design.md` 新增 "`preventEnterSubmit` 与 schema-driven `preventDefault` 的关系" 段落、`docs/references/naming-conventions.md` §4.4 X2 裁定行更新。
- **Phase 2（Proof RED baseline）**：新建 `packages/flux-react/src/__tests__/event-prevention.test.tsx`，11 个 test case 覆盖：boolean preventDefault on click、form submit、expression truthy/falsy、backward-compat (field absent)、stopPropagation（DOM 冒泡验证）、非事件上下文 lifecycle + dev warn、expression eval error fallback、与 `when` 正交性、imperative `ctx.event.preventDefault()` backward-compat。RED 基线确认：7 fail / 4 pass（含 backward-compat 既有路径，本应 always green）。
- **Phase 3（Fix GREEN）**：
  - `packages/flux-core/src/types/actions.ts`：`ActionShapeFields` 新增两字段；`CompiledActionNode` 新增 `preventDefault?` / `stopPropagation?` 为 `CompiledRuntimeValue<boolean>`。
  - `packages/flux-compiler/src/action-compiler.ts`：`compileActionNode` 编译两字段（同 `when` 路径）；`isNodeFullyStatic` 同步检查两字段。
  - `packages/flux-compiler/src/schema-compiler/shape-validation-rules.ts`：`validateActionShape` 紧邻 `when` 规则追加两条 type-validation 规则（boolean-or-string，错误码 `invalid-action-shape`）。
  - `packages/flux-action-core/src/action-core.ts`：新增 `shouldPreventDefault(action, ctx, evaluator)` + `shouldStopPropagation(...)` helper（复用 `shouldRunActionWhen` 的 expression 求值 path，含 try-catch fallback falsy + dev console error）；导出至 `index.ts`。
  - `packages/flux-react/src/renderer-helpers.ts`：新增 `applySchemaDrivenPrevention({...})` —— 集中化 sync 求值 + 调用 native event method（idempotent、含 `x2-no-native-event` warn、含 `x2-expr-eval-error` error、含 `withEventBinding` 把 `event` 绑入 evaluation scope 支持 expression 内 `event.key` 等 access）。
  - `packages/flux-react/src/node-renderer-resolved.tsx`：per-event-key handler 在 dispatch 前 sync 调用 `applySchemaDrivenPrevention`。
  - `packages/flux-react/src/node-renderer-effects.ts`：`useNodeLifecycleActions` 在 lifecycle dispatch 前调 `warnIfPreventionRequestedOnLifecycle`（兼容 `CompiledActionProgram` + `ActionSchema` 两种 shape）。
  - Compiler validation tests：`packages/flux-compiler/src/schema-compiler-shape-validation-action-source.test.ts` 加 3 个 case（invalid preventDefault shape、invalid stopPropagation shape、valid boolean+expression 通过）。
- **Phase 4（Owner-doc + playground + e2e + roadmap + log）**：
  - Playground：`apps/playground/src/pages/event-prevention-demo.tsx`（3 demo：native form submit 阻止、native link 跳转阻止、native keydown 阻止；每个 demo 有 toggle 对比 `preventDefault: true/false`），注册到 `apps/playground/src/App.tsx` + `apps/playground/src/route-model.ts` 的 `DOMAIN_RENDERER_ROUTES`（id `event-prevention`）。
  - e2e：`tests/e2e/event-prevention.spec.ts`（4 个 test：form submit 不提交、link 不跳转、keydown 全键阻止、toggle off 后允许 typing）；`tests/e2e/playground-entry-pages.spec.ts` 补 event-prevention route assertion。
  - Roadmap：`docs/components/existing-components-improvement-roadmap.md` X2 状态 `planned` → `done`。
  - amis-baseline-matrix：无 retained 决策变化，按 Minimum Rule 17 不写 "No update required" 凑条目。
- **X2 收口验证**：`pnpm typecheck` = 49/49、`pnpm build` = 49/49、`pnpm lint`（无新增问题）、`pnpm test` = 49/49 unit tasks 全过；e2e event-prevention 4/4 + playground-entry event-prevention smoke 1/1 通过（pre-existing component-handles smoke 失败与本 plan 无关）。新增/扩展测试：`event-prevention.test.tsx`(11 unit)、`schema-compiler-shape-validation-action-source.test.ts`(+3 compiler)、`event-prevention.spec.ts`(4 e2e)。
- **Deferred 但已裁定**：`stopImmediatePropagation`（同 element listener 顺序控制）— `out-of-scope improvement`，如出现具体用例独立加字段。硬编码 `preventDefault()` 迁移（input-number ArrowUp/ArrowDown、tree 键盘导航、chart resize）— `out-of-scope improvement`，归各 renderer P2 enhancement（E3 批）独立评估。amis `rendererEvent` 兼容层 — roadmap §5 明确拒绝。
- **Closure 裁定（透明记录）**：Plan Status 按 mission-driver 指令置 `completed`；Closure Gates 技术验证项（typecheck/build/test/design sync/roadmap/no-silent-defect）全 `[x]`；**「独立子 agent closure-audit」按 AGENTS.md「Human gates: Agent MUST NOT self-approve closure audit」留 `[ ]`** —— 本次跨模块（flux-core/flux-compiler/flux-action-core/flux-react/flux-playground/tests/e2e），属于 AGENTS.md「Cross-module, contract, or architecture changes require explicit human gates between stages」分类。
