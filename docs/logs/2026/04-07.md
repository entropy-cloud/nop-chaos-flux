# Development Log — 2026-04-07

### 2026-04-07

- Aligned normative architecture docs to the `name` + `mergeToScope: true` Resource publication convergence direction. Updated `docs/architecture/api-data-source.md` (interface, binding target, examples, status notes), `docs/references/terminology.md` (DataSourceSchema entry), `docs/articles/flux-design-introduction.md` (data-source example and description), `docs/examples/user-management-schema.md` (clarified ActionSchema.dataPath vs DataSourceSchema.dataPath).
- Key decision: `dataPath` on `DataSourceSchema` is now documented as legacy compatibility-only across all normative docs; new schema should use `name` as the sole publication path with optional `mergeToScope: true` for object-result scope merge.
- Next step: continue converging runtime code toward `name`-first identity and registration.

### 2026-04-07

- Centralized field-level `type: 'source'` prop resolution through renderer metadata instead of renderer-local hooks.
- Link to code paths: `packages/flux-core/src/types/schema.ts`, `packages/flux-core/src/types/renderer-compiler.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/use-node-source-props.ts`, `packages/flux-react/src/node-renderer.tsx`, `packages/flux-renderers-form/src/renderers/input.tsx`
- Link to doc update: `docs/architecture/field-metadata-slot-modeling.md`
- Key decision: keep source support explicit with `SchemaFieldRule.allowSource` so only renderer-owned value fields opt into anonymous source execution.
- Next step: expand `allowSource` to additional proven value fields after validating behavior for form options and basic text content.

### 2026-04-07

- Added `SchemaFieldRule.sourceStateKey` so source-enabled props can inject one companion transient-state object into renderer props.
- Link to code paths: `packages/flux-core/src/types/schema.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/use-node-source-props.ts`, `packages/flux-renderers-form/src/schemas.ts`, `packages/flux-renderers-form/src/renderers/input.tsx`
- Link to tests: `packages/flux-react/src/index.test.tsx`, `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: model loading/error/status as one narrow UI-facing state object instead of scattering multiple sibling props.
- Next step: keep the transient-state contract narrow and only adopt it where a renderer actually needs loading/error chrome.

### 2026-04-07

- Extended `optionsSourceState` consumption from `select` to `radio-group` and `checkbox-group` so all source-backed option-group controls show loading chrome consistently.
- Link to code paths: `packages/flux-renderers-form/src/renderers/input.tsx`, `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: keep option-group loading UX minimal and consistent: inline spinner plus temporarily disabled option controls.
- Next step: only add error-specific inline chrome if a concrete renderer needs it; for now the transient state contract remains available without forcing every renderer to render errors.

### 2026-04-07

- Verified concrete source error messages surface to option controls when the fetcher fails by throwing, and covered `select` / `radio-group` / `checkbox-group` with regression tests.
- Link to tests: `packages/flux-renderers-form/src/index.test.tsx`
- Key decision: preserve the typed fetcher contract and treat thrown fetcher errors as the canonical path for renderer-visible source failure messages.
- Next step: if needed, formalize non-throwing API failure payload semantics separately rather than overloading `ApiResponse<T>` with ad-hoc `error` fields.

### 2026-04-07

- Aligned request execution with the fetcher contract: non-OK `ApiResponse` values are now converted into thrown errors inside `executeApiSchema`, so runtime callers observe successful `data` or an exception.
- Link to code paths: `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/index.test.ts`
- Key decision: `ApiResponse` remains a fetcher boundary object; renderer/runtime callers should not branch on `ok === false` and instead rely on thrown failures.
- Next step: document the fetcher contract in a stable reference doc if this behavior will be exposed to host integrators directly.

### 2026-04-07

- Updated architecture/reference docs to make the fetcher boundary explicit: `ApiResponse` belongs to `env.fetcher(...)`, while runtime consumers receive adapted data or a thrown error.
- Link to docs: `docs/architecture/api-data-source.md`, `docs/references/renderer-interfaces.md`, `docs/references/terminology.md`
- Key decision: keep the non-OK `ApiResponse` fallback as an internal compatibility path, but document thrown failures as the intended host integration contract.
- Next step: if external integrator docs are added later, reuse this same wording so host fetcher implementations do not depend on deprecated non-OK response handling.
