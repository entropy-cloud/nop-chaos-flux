# Development Log - 2026-08-28

### 2026-08-28

- **master 合入 ui-review**（merge commit `6a920a58f`，master `01770f770`）：唯一冲突文件 `sundial-workbench.json`（16 处）全部取分支 plan 460 B8 侧——B8 是带完整交互（task 行 `taskDetailOpen` 真打开、picker 取消/确定 + testid、中文值语义）的专版实现，master 侧为 bug-162 会话的临时简化版，已被覆盖；`page-data.ts` 初值统一到 B8 语义（`taskDetailRecur: '每周'`、`taskDetailListLabel: '工作'`，删除孤儿 `taskDetailList`）。master 的 bug-162 框架层守卫（`dialog-host.tsx` 非 topmost 外点/Esc 惰性）与 B8 的 schema 层 `closeOnOutsideClick:false` 共存（纵深防御，测试证实无冲突）。
- **合并语义验证**：playground complex-pages 45/45、flux-react `dialog-host-close-behavior` 10/10（master bug-162 覆盖）、flux-renderers-basic `surface-event-ctx` 3/3（**master HEAD 上一直红的该测试经合并转绿**——B8 的 confirm-first capture 修复是 master 缺失的）。
- **master 既有红修复（`01770f770` ApiResponse 去 `ok` 契约的漏网家族，master HEAD 上实测同红）**：flux-runtime 3 个测试文件 release 回调注解（commit `c05190aed`）；playground `stream-impl.ts` 4 处 response 字面量 + `stream-impl.test.ts` 2 断言 + `env-stream-demo.tsx` 显示串 + `coverage-connectors.ts` mock；`flux-renderers-ai` `ai-connector-factory.ts` 消费条件改 `status !== 200 && status !== 0` + 测试 mock 6 处；`flux-renderers-content` `markdown.tsx` 成功判断改 `status !== 0`；`nop-debugger` `adapters.ts` level 判定 + `controller-helpers.ts` 快照字段删除 + 测试 1 处。统一口径：`ApiResponse` 成功 = `status === 0`，stream response = `Omit<ApiResponse, 'data'>`（HTTP 200 直通）。
- **依赖同步教训**：合并带入 lockfile 大改（+318 行）后 worktree node_modules 仍为 08-19 旧装（TS 5.9.3），`pnpm typecheck` 报 `flux-core` TS5103 `ignoreDeprecations` 无效；`pnpm install` 升级 TS 6.0.3 后消除。**worktree 合并 master 后必须先 `pnpm install` 再验证。**
- **快照政策放开（AGENTS.md）**：原"NEVER diagnose UI failures via screenshots"改为——多模态模型可用页面快照做 UI 诊断/视觉评审（ui-review mission 2026-08-28 起生效）；测试通过判据仍必须程序化（`page.evaluate()` 等），快照不作为测试证明。docs-for-ai 目录不在本仓（仅 nop-entropy 仓存在），本仓的快照禁令只在 AGENTS.md。
- **mission `missions/ui-review.json` 更新**：`model` → `opencode-go/glm-5.3-flash`（沿用本仓 `opencode-go/<model>` 惯例），`variant: max`（思考级别）不变，新增 `multimodal` 字段声明模型可读快照、双轨验证口径。
- **全量验证终态（08-28）**：`pnpm typecheck` 37/37 ✓、`pnpm build` ✓、`pnpm lint` ✓、`pnpm check` exit 0；`pnpm test` 唯一残余红 = `flux-renderers-form` `input-date-relative-wiring.test.tsx` 4 用例（minDate/maxDate 相对表达式日期禁用语义——`now` 未按日粒度取整导致"今天"被禁；**master HEAD 同红实测确认**，属 date 族产品逻辑 bug，非本合并/迁移引入，建议立 bug note + 独立修复 plan）。不声明 full-green。
- Next step: 全量验证链（typecheck/build/lint/test/check）结果回填本日志；随后按 roadmap 顺序启动 R2（全量 UI 一致性审查）。
