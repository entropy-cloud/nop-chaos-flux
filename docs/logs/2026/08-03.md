# Development Log - 2026-08-03

### 2026-08-03

- **DingFlow 单一 Tree 布局实施计划草稿（docs-only）**: 新建 `docs/plans/453-dingflow-single-tree-layout-unification-plan.md`，将已共识的分析契约收敛为单一 owner plan 草稿，分为 core tree session/migration、structured projection/geometry、renderer host writeback/command gate、TB/LR DingFlow UI、authoring/owner-doc/E2E 收口五个执行阶段。计划仍为 `draft`，未执行、未进入独立 plan review。

- **DingFlow 单一 Tree 布局契约达成独立共识（docs-only）**: 对 `docs/analysis/2026-08-03-dingflow-tree-layout-unification.md` 执行多轮独立架构、布局数学与 implementation-contract 审查，最终架构/几何均 `PASS`，implementation-contract 为 `contract-ready`（零 P1/P2）。主文档现 38,917 bytes，详细审查历史归档在 `docs/audits/2026-08-03-1111-document-audit-dingflow-tree-layout-unification.md`（closed）。未改代码、未创建实施计划。
  - **冻结结论**: tree mode 仅保留 core-private `projectAndLayoutTree` 的 structured 算法；ELK 仅保留 graph mode；tree session 使用 validated core factory 与原子 tree/graph pair；TB/LR/empty slot/geometry oracle/严格 FIFO host writeback/epoch+sessionId ack/JSON payload 验证/删除矩阵均已定义，并绑定 proof obligations。

- **DingFlow 分析文档压缩（docs-only）**: 将逐轮独立审查历史从 `docs/analysis/2026-08-03-dingflow-tree-layout-unification.md` 提取到 `docs/audits/2026-08-03-1111-document-audit-dingflow-tree-layout-unification.md`；主文档保留全部技术契约和 proof obligations，并以审查收敛节保留最终独立共识审查门禁。未改技术结论或代码。

- **DingFlow 布局算法统一分析文档完成（docs-only）**: 分析 tree mode 目前并存的三套布局算法（`layoutStructuredTree` / `simpleTreeLayout` / `layoutTreeWithElk`）并给出收敛方案，落地 `docs/analysis/2026-08-03-dingflow-tree-layout-unification.md`。核心结论：DingFlow 是嵌套树布局问题，tree 模式只保留 `layoutStructuredTree` 一种算法；方案分六阶段（引擎收敛 / 布局输出几何元数据 branchGroups / 渲染器与 overlay 消费元数据 / 空分支语义 / 常量口径统一 / 测试改造），并含逐文件改动清单与验证方案。未改代码。
  - **实测证据（一次性 vitest 复现后删除）**: 同一条件树 structured after.x=140 vs ELK after.x=152（发散）；非对称深度下 `simpleTreeLayout` 的 merge 覆盖使 continuation 与同层 right3 重叠（x=-110 vs 30）；默认 layerSpacing=120 下分支线（sourceBottom+88）与合并线（sourceBottom+84）差 4px 不对齐；空分支投影产生 owner→continuation phantom merge 边（`tree-projection.ts:113-115`）。
  - **现状**: `flow-designer-core` tree-layout 11 tests 全绿（对称场景仅覆盖）；改动点汇总于分析文档 §6 文件改动清单。

## C3.5 form-advanced 媒体与富文本族逐组件审计执行：plan-2026-08-03-1616-2 四 Phase 全绿（editor/input-file/input-image/tree-select/input-tree 5 组件 18 维 + P1×5 类/P2 自动修复 + 宿主场景 7/7 + closure 前置）

### C3.5 mission-driver VERIFY build 复核（2026-08-03 晚）

- 触发：mission-driver 对已完成 plan 的 build 验证 + 收口提交。本次 VERIFY 修复 2 个并发 session artifact 并全量复核：**typecheck/build/lint/test 全绿（31/31 包 + playground 141/141）**。
- **[并发 artifact 1 修复] flux-bundle typecheck**：`pur-order-real-schema.test.tsx`（未跟踪 WIP）缺 `formulaCompiler`（SchemaRendererProps required）→ 补 `createFormulaCompiler()` prop；typecheck 31/31 绿。
- **[并发 artifact 2 修复] hidden route-matrix 收口**（a3841ff8 遗留，基线记录归 owner session）：`form-route-entries.ts` 补 hidden 路由 + 新建 `hidden-lab-page.tsx`（3 场景：提交表单内 hidden 值随提交、schema value 播种 scope、clearValueWhenHidden 策略）+ `renderers/index.ts`/`renderer-lab-registry.ts` 注册 → route-matrix 2 用例绿（hidden 路由覆盖 + 计数 64→65），playground **141/141**。
- e2e 全量：**828 passed / 9 failed / 43 skipped / 15 did not run**（含本族 c3-5-host-surfaces 7/7 + w3d-editor + w3d-upload-family + tree-display-ux 全绿）。9 失败 = C0 pre-existing 裁定项（ai-chat timestamp ×1、ai-rich-text-sender ×5（ai 包 Tiptap，C8.1）、calendar-demo ×1、diff-perf ×1（机器负载敏感阈值）、gantt-bars-and-links ×1（已知 flaky 拖拽类）），与 C3.4/C3.5 基线记录完全一致、无新增；另有 simple-form select ×1 + performance-table ×1 满载运行 flake（isolated 复跑 2/2 绿）+ w3d-editor bold 间歇 flake（isolated 复跑绿，日志既有裁定）——均非行为回归。未达仓库级 full-green，不声明（pre-existing 项归属 C8.1/C9/CV）。
- 提交：feat 收口（form-advanced 修复 + i18n + lab 页/宿主场景 + flux-bundle 测试 + hidden 路由补齐 + flux-guide types 重新生成）+ docs 收口（5 卡/plan/roadmap/bug 76/77/daily log）。

执行 `docs/plans/2026-08-03-1616-2-c3-5-form-advanced-media-rich-text-family-audit.md`（媒体与富文本族 5 组件 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 媒体与富文本族执行完成（roadmap C3.5 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.1-C3.4 先例一致；执行 session 不自审、不标 done）**。

执行 `docs/plans/2026-08-03-1616-2-c3-5-form-advanced-media-rich-text-family-audit.md`（媒体与富文本族 5 组件 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 媒体与富文本族执行完成（roadmap C3.5 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.1-C3.4 先例一致；执行 session 不自审、不标 done）**。

### Phase 1 — 5 张审计卡（`docs/audits/per-component/{editor,input-file,input-image,tree-select,input-tree}.md`，editor/tree-select/input-tree 追加 deep-audit 21-23 维）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×5 类（P1-1 editor link 按钮零行为（命令未注册）、P1-2 editor URL 协议校验缺失（随 P1-1 落地必须同步的安全红线）、P1-1 树 fields 注册缺口（input-tree ×8 + tree-select ×10 消费键未注册）、P1-2 pathSeparator 零行为（设计 TR7 主张已实现、代码硬编码 '/'、既有测试伪覆盖）、P1-3 树懒加载 StrictMode 假死（bug 73 模式，宿主场景实证））、P2 ×7（editor i18n 硬编码 ×11 按钮 + 3 文案、upload Remove aria-label、input-image alt 兜底、tree-select placeholder fallback、popover 高度、input-image 专属测试缺口、editor 组件测试缺口、coverage-manifest 缺 3 组件登记）、P3 ×12**。
- 重点维度：dim 1（**P1-1 树 fields 注册缺口：InputTreeSchema/TreeSelectSchema 消费的 treeMode/childrenKey/labelField/valueField/cascade/searchable/onlyLeaf/showPathLabel（+tree-select clearable/placeholder）零注册 → flux-guide 生成类型丢键（schema.d.ts:662-682）——C3.4 P1-1 同型**）；dim 1/17/22/23（**P1-2 pathSeparator：已注册 + design TR7 主张「pathSeparator 默认为 /」但 valuePath 硬编码 '/'（tree-options.ts:73-75），tree-enable-node-path.test.tsx:156「uses pathSeparator」单根节点无分隔符伪覆盖**）；dim 18（**P1-1 editor link：DEFAULT_EDITOR_TOOLBAR 含 link + TOOLBAR_BUTTONS.link run 引用 setLink（editor-renderer.tsx:116-130）但 StarterKit v3 无 Link 扩展——命令未注册、isActive 恒 false、点击 no-op/抛错；lab 文案宣称 link 能力**；**P1-2：无 URL 协议校验点——Link 扩展未配置 protocols/validate，输出 getHTML 未二次 sanitize，javascript: href 可经粘贴进入存储 HTML（宿主回显未净化即 XSS，安全红线）**）；dim 11（upload 请求下沉 + abort/失败不污染 + deleteAction 生命周期全 pass；树远程搜索 debounce+abort+懒加载 generation 守卫 pass——**但宿主实证 P1-3：mountedRef 双挂载无复位，懒加载 resolve 全被丢弃**）；dim 16（**input-image 专属断言仅 1 例（thumbnail），fill/multiple/readOnly/回显零断言**；editor 零组件渲染测试（仅纯函数））。
- 工具脚本：`check:audit-missing-renderer-markers`/`styling-suspects`/`runtime-raw-schema-reads`/`fieldframe-bypasses`/`hardcoded-type-dispatch` 本族 0 命中；`async-failure-paths` 命中 upload-field/editor-renderer 均为已处理 catch/finally（合规）；`performance-suspects`/`react19-optimization-candidates` 命中为 H8/H14/H15 既有守卫（C3.x 同裁定）。
- 08-02 机制复验：pass（searchSource/childrenSource/uploadAction/deleteAction 均 actionValue 声明（tree-controls.tsx:486-498/532-544、upload-schemas.ts:14-26），无 deepFields 残留——live grep 零命中，c3-5-schema-contract-honesty.test.ts 冻结断言）。
- 共性裁决：pathSeparator 零行为根因单点（共享 tree-options.ts 单文件）→ 卡内共享修复不插 CX-n；P1-3 StrictMode 根因单点（tree-control-controllers.ts 共享控制器）→ 卡内共享修复不插 CX-n；fields 注册缺口根因单点（定义文件）→ 不插 CX-n；onlyLeaf 非 cascade 语义未文档化 → P2 backlog 归 CR。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 991 → 1014 tests）

- **[P1-1 editor link]**：新增 `@tiptap/extension-link@^3.27.1`（deps）+ `@tiptap/core`（devDeps 供 headless 测试）+ `buildEditorExtensions()`（StarterKit + Link.configure({ protocols: ['http','https','mailto','tel'], validate: isSafeLinkUrl, openOnClick:false, autolink:false })）+ link 按钮 run 重写（prompt null→unsetLink / 安全→setLink(trim) / 不安全→静默忽略）。test-first：editor-link.test.tsx 9 用例（7 先红：setLink 命令缺失 + isSafeLinkUrl 纯函数不存在；headless @tiptap/core Editor 直接覆盖被破坏的扩展接线面）。
- **[P1-2 editor URL 协议校验（安全红线）]**：protocols + validate 双闸 + **输出防线**：onUpdate html 分支提交前二次 `sanitizeEditorHtml(getHTML())`（design §W3d「输出只含安全子集」主张兑现）——javascript:/data:/vbscript: 从 UI/paste/输出三路全堵；`isSafeLinkUrl` 纯函数导出（12 分支表断言）。
- **[P2-1 editor i18n ×15 key]**：flux-i18n 新增 `editor.*` 双 locale（toolbarLabel/richTextEditor/bold/italic/strike/heading1/heading2/bulletList/orderedList/code/blockquote/link/undo/redo/linkPrompt）；TOOLBAR_BUTTONS 硬编码 title → `toolbarButtonTitle(id)` switch 静态 key（check:i18n-keys 可解析）；editor aria-label fallback → t()。
- **[P1-1 树 fields 注册缺口 ×8+10]**：tree-controls.tsx 定义补注册 input-tree `treeMode/childrenKey/labelField/valueField/cascade/searchable/onlyLeaf/showPathLabel` + tree-select 追加 `clearable/placeholder`；flux-guide/flux-types/schema.d.ts 重新生成（两类型补齐全部消费键，114 types 不变）；test-first：c3-5-schema-contract-honesty.test.ts 4 用例（schema 声明 ↔ 定义注册冻结 + actionValue 契约 + 无 deepFields）先红（phantom 8/10 键）后绿。
- **[P1-2 pathSeparator 零行为]**：`getTreeOptionConfig` 增 pathSeparator（默认 '/'）+ `buildTreeOptionMeta` valuePath 消费（tree-options.ts）+ 两 renderer 传参（tree-controls.tsx，memo 依赖补 pathSeparator）；test-first：tree-enable-node-path.test.tsx 新用例（嵌套节点 + `pathSeparator: ' > '` → 'root > child'）先红（'root/child'）后绿——伪覆盖用例（单根节点）被真断言取代。
- **[P2-2 tree-select]**：triggerLabel fallback 'Select tree option' → `flux.form.treeSelectPlaceholder` 双 locale；桌面 PopoverContent 包 `max-h-[60vh] overflow-y-auto`（C3.3 picker 先例，mobile Sheet 已有 max-h-[65vh]）。
- **[P2-1 input-file/input-image]**：`Remove ${name}` aria-label → `flux.form.removeItem`；alt 兜底 'uploaded image' → `flux.form.uploadedImage` + alt 回退链 name→url→i18n。
- **[P2-2 input-image 测试缺口]**：新增 input-image-preview.test.tsx 6 用例（既有值回显/multiple 数组/fill+thumbnail 尺寸断言/readOnly 移除隐藏+trigger disabled/失败无缩略图不污染值/字符串值 alt）。
- **[P2-2 测试加固]**：tree-structure.test.tsx +2（自定义 childrenKey/labelField/valueField + showPathLabel/onlyLeaf 真实映射断言）——同时补齐 check:schema-prop-coverage 新注册 8 键（180 declared/100%）；editor-link.test.tsx 渲染壳 2 用例（默认工具栏含 link 按钮/readOnly 隐藏工具栏）。
- **[P2-2 coverage-manifest]**：editor/input-file/input-image 补登记（C3.4 icon-picker 同型缺口）；5 组件 primaryScenario 同步为 bug 73 宿主场景。
- **[P3 裁定]**：input-tree data-testid 透传尝试后回退——实证 FieldFrame（wrap:true）已在 frame 输出 data-testid（field-frame.tsx:229），根透传重复（complex-pages Tree+CRUD 测试实证 2 元素冲突）；tree-select 既有根透传为同型潜在重复登记归 CR（行为无害）。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**1014 tests**）；`check:i18n-keys` 绿（729 used/全定义，unused 警告 126 项 pre-existing）；`check:schema-prop-coverage` 绿；flux-i18n 26 tests 绿；flux-guide schema.d.ts 重新生成。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c3-5-host-surfaces.spec.ts` 7/7 全绿 + lab 页 5 组件 ×+1 宿主场景）

- **host-mr-editor（bug 73 模式）pass**：form 内 editor 追加输入 → Submit → echo `MR-EDITOR:` 含追加文本 + `<strong>rich</strong>`——HTML 值真机进 store、提交形状正确（单测绿 + 真机一致）。
- **host-mr-sanitize（XSS 红线）pass**：存储值含 `<script>alert(1)</script>` + `javascript:` 链接 → ProseMirror DOM 零 script/alert(1) → 键入触发提交 → echo 无 `<script`/`javascript:`/`alert(1)`——输入 sanitize + 输出 sanitize 双防线真机成立。
- **host-mr-editor-link（P1-1/P1-2 真机证明）pass**：link 按钮 dialog 接受 `https://example.com/doc` → 提交值含 href；dialog 接受 `javascript:alert(1)` → 拒绝不落值；dialog dismiss → unsetLink → 提交值无 `<a>`。
- **host-mr-upload-ok/fail（bug 73 模式）pass**：input-file 上传成功（值回写 `"ok":"https://cdn.example.com/contract.pdf"`）+ 失败端点（`data-item-status=error` 展示、值不污染、无 missing-action 误报）→ Submit echo 正确。
- **host-mr-image-ok/fail（bug 73 模式）pass**：input-image 上传成功缩略图 src 断言 + 失败 error item + 值不污染。
- **host-mr-tree-lazy（bug 73 模式 + 异步生命周期）pass**：input-tree 展开 deferChildren 节点 → childrenSource 子节点加载渲染并选中 → 提交 `"node":"a-a"`；失败节点 inline error + retry → 重试成功 → 提交 `"nodeFail":"c-r"`；tree-select 同场景双组件（popover portal 内操作 + 独立 fail 节点 'd'）同样成立。
- **bug 73 模式专项检查结论：pass（带证据 fail→已修复）**——宿主场景实证并修复 1 个"单测绿但真机失败"类缺陷：**P1-3 树懒加载 StrictMode 假死**（`useTreeLazyChildren` mountedRef cleanup 置 false 无复位——playground StrictMode 双挂载后懒加载 resolve 全被当「已卸载」丢弃：env fetcher 已调用、dispatch ok:true、子节点永不渲染、零错误；单测无 StrictMode 全绿假绿）→ 修复（mount effect 补 `mountedRef.current = true` 复位，与 upload-field G11 模式对齐）+ StrictMode 回归用例先红后绿 + bug 77 记录。
- 回归：w3d-editor **3/3**、w3d-upload-family **2/2**、tree-display-ux **6/6**；component-lab 全量 **211 passed / 1 skipped（pre-existing simple-form skip）**。
- lab 页新场景：editor-lab-page +3（编辑提交/sanitize/link）、input-file-lab-page +1、input-image-lab-page +1、input-tree-lab-page +1（懒加载+失败重试）、tree-select-lab-page +1；coverage-manifest 5 组件条目 primaryScenario 同步为 bug 73 模式场景 + editor/input-file/input-image 补登记。

### Phase 4 — closure 前置

- 5 卡全部 `closed`（P0/P1 清零；P1×5 类 + P2×7 fixed；P2 backlog（onlyLeaf 非 cascade 语义）+ P3 ×12 卡内记录归 CR）；全卡 18 维 + 21-23 维表结论与最终代码一致。
- 全量验证（closure gates 前置证据）：`pnpm typecheck` **30/31 包绿**（flux-bundle 1 失败 = **并行 session 并发提交 artifact**：`packages/flux-bundle/src/__tests__/pur-order-real-schema.test.tsx`（未跟踪 WIP，20:14 生成，非本 plan 改动）缺 formulaCompiler prop）；`pnpm build` 31/31、`pnpm lint` 31/31（1 pre-existing warning）；`pnpm test` **30/31 包绿 + playground 139/141**（2 失败 = **并行 session 并发提交 artifact**：`hidden` renderer type 提交（a3841ff8，20:06，HEAD 基线内）注册了新类型但 lab 路由/清单未同步 → route-matrix 2 用例（hidden 缺路由 + 计数 64vs65）——归属 hidden renderer 的 owner session/C2.1 收口，本 plan 不触碰）。**本 plan 全部改动路径验证全绿**（form-advanced 1014 tests、flux-i18n 26、playground 139/141 中复杂页 Tree+CRUD 已随 data-testid 回退修复）。
- 文档：5 卡 closure 汇总；bug 76（editor link 零行为 + URL 协议红线）、bug 77（树懒加载 StrictMode 假死）记录；flux-guide schema.d.ts 重新生成核验（tree 两类型补齐 8/10 键）。
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（editor-link 7 red、honesty 2 red、pathSeparator 1 red、input-image-preview 1 red、**StrictMode 回归 1 red（无修复实测）**；宿主 spec 3 轮定位修正后 7/7）。
- **bug 73 模式专项检查结论：pass**（host-mr-editor/host-mr-editor-link/host-mr-upload/host-mr-image/host-mr-tree-lazy 真机全部成立；过程中实证并修复 1 个"单测绿但真机失败"类缺陷：P1-3 StrictMode 假死——既有单测全绿（无 StrictMode 环境）但真机懒加载静默不加载）。
- **Non-blocking follow-up（CR）**：P2 backlog（onlyLeaf 非 cascade 单选父节点可选语义裁决）；P3 ×12（editor focus-visible outline、工具栏 aria-pressed boolean、tree clear '' vs undefined 一致性、valuePathMap baseOptions-only、aria-checked 仅 indeterminate、upload 单选并发旧上传不取消、重复 key、cancel defaultValue 冗余、crop 扩展点、img src 协议归宿主、tree-select 根 testid 潜在重复、onlyLeaf 渲染全节点）；e2e pre-existing 8 项（C0 裁定归 C8.1/C9/CV）未达 full-green 不声明。

### C3.5 并发基线说明（执行 session 记录，非本 plan 责任）

- 执行期间 HEAD 被并行 session 推进 2 次：`565c6e8a`（18:47 CRUD 测试）→ `a3841ff8`（20:06 hidden renderer type）。hidden 提交引入 route-matrix 2 失败（新类型未配 lab 路由）；flux-bundle 未跟踪 WIP 测试文件引入 typecheck 1 失败——均与 C3.5 改动无依赖关系（本 plan 改动路径全部单独验证绿），归各自 owner session 收口，C3.5 closure-audit 时点另行复核。

### C3.5 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（独立复核 live repo：5 卡 closed 且 18/21-23 维结论与最终代码一致——editor-renderer.tsx（buildEditorExtensions/isSafeLinkUrl/输出 sanitize/toolbarButtonTitle）、tree-options.ts（pathSeparator 消费于两 renderer）、tree-controls.tsx（fields 注册 ×18）、tree-control-controllers.ts（mountedRef 复位 + treeSelectPlaceholder i18n）、upload-field.tsx（removeItem aria-label）、input-image-renderer.tsx（uploadedImage alt 兜底）、flux-i18n 双 locale（editor.\*/removeItem/uploadedImage/treeSelectPlaceholder）；新测试 editor-link 9 + c3-5-schema-contract-honesty 4 + input-image-preview 6 + tree-lazy-children StrictMode + tree-structure +2 + tree-enable-node-path 断言正确行为；实测复核 `c3-5-host-surfaces.spec.ts` 7/7 + w3d-editor 3/3 + w3d-upload-family 2/2 + tree-display-ux 6/6（首跑 2 项为页面加载超时 flake，error-context 快照含目标 heading，重跑全绿，非行为回归）+ `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck/test` 绿（1014）+ flux-i18n 26 + plan-check --strict pass + flux-types schema.d.ts 重新生成核验 + bug 76/77；deferred 分类诚实（P2 backlog=optimization candidate、机制未落地=watch-only residual、e2e pre-existing 归 C8.1/C9/CV，无静默降级 live defect）；五处一致性核对通过）
- Verdict: **pass（approved）**——审计 session 复核未见残留 owner-doc 漂移（icon-picker §7/§8 同型问题本次无对应残留）；plan/roadmap/daily log 收口动作同步完成
- 收口动作（由审计 session 完成）: Closure Gates closure-audit 项勾选 + plan `## Closure` Audit Evidence 回填（`Plan Status: completed`）+ **roadmap C3.5 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.x/C3.1-C3.4 先例一致）

### C3.5 EXECUTE visit 2 复核（mission-driver retry，2026-08-03 晚）

- 触发：CLOSURE_AUDIT 输出 issues 后 flow 重试 EXECUTE（visit 2）。复核结论：**plan 已完整收口，无需任何改动**——四 Phase 全部 `Status: completed` + 全部 `- [x]`（39/39），`Plan Status: completed`，Closure Gates 11/11 含 closure-audit 项（由审计 session 勾选，执行 session 不自审合规）。
- 复核证据（本次 fresh 实测）：`plan-check.mjs --strict` **pass（39 checked / 0 unchecked / completed / closure evidence 存在）**；live 代码核验（buildEditorExtensions:233 + isSafeLinkUrl:216 + 输出 sanitize:311 + toolbarButtonTitle:156、tree-options.ts pathSeparator:55,77 被两 renderer 消费、tree-controls.tsx fields 注册、tree-control-controllers.ts:201 mountedRef 复位、flux-i18n removeItem/uploadedImage/treeSelectPlaceholder）；`pnpm --filter @nop-chaos/flux-renderers-form-advanced test` **1014/1014** + typecheck 绿；flux-i18n **26/26** + typecheck 绿；e2e `c3-5-host-surfaces` **7/7** + w3d-editor 3/3 + w3d-upload-family 2/2 + tree-display-ux 6/6 全绿；roadmap C3.5 行 `done`、5 卡 `closed`、bug 76/77 存在。
- 结论：audit 反馈中的 1 个 unchecked 项（closure-audit gate）已由独立审计 session 勾选；EXECUTE visit 2 无剩余 plan-owned 工作。

## C3.4 form-advanced 轻量编辑族逐组件审计执行：plan-2026-08-03-1616-1 四 Phase 全绿（tag-list/key-value/array-editor/icon-picker 4 组件 18 维 + P1×2 类/P2 自动修复 + 宿主场景 4/4 + closure 前置）

执行 `docs/plans/2026-08-03-1616-1-c3-4-form-advanced-lightweight-editor-family-audit.md`（轻量编辑族 4 组件 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 轻量编辑族执行完成（roadmap C3.4 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.1/C3.2/C3.3 先例一致）**。

### Phase 1 — 4 张审计卡（`docs/audits/per-component/{tag-list,key-value,array-editor,icon-picker}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×2 类（P1-1 定义 fields 未注册消费键 ×3 组件、P1-1 icon-picker required 静默 no-op ×1）、P2 ×9（i18n 硬编码消息 ×3 组件、icon-picker ARIA listbox ×1、phantom onChange 主张 ×1、测试加固 ×2、design.md 同步 ×3）、P3 ×15**。
- 重点维度：dim 1（**P1-1 fields 注册缺口：TagListSchema `tags`（schemas.ts:269）/KeyValueSchema `addLabel/uniqueKeys/minItems/maxItems`（:272-277）/ArrayEditorSchema `itemLabel/minItems/maxItems`（:285-289）全部被渲染器+校验消费但定义 fields 仅 formFieldRules → flux-guide 生成类型丢键（schema.d.ts TagListSchema/KeyValueSchema/ArrayEditorSchema 仅 value）——C3.1/C3.2/C3.3 phantom 同类反方向（已消费未注册）**）；dim 4（**P1-1 icon-picker required 静默 no-op：定义无 validation contributor + 组件无 registerField，collectValidationModel 仅对 kind:'field' contributor 建校验字段（validation-collection.ts:111-113）→ schema.required 永不下降，FieldFrame aria-required/星号与 enforcement 缺口，probe 实证 submit 空值零错误**）；dim 10（**CX-8 同型复验 pass：本族 4 组件行/项内嵌均为直接 Input/Button disabled 传播（非嵌套 schema 字段），全链冻结无缺口，宿主 host-le-readonly 实证**）；dim 3（行内编辑写回 form/scope 双路径 + 受控 echo 路径订阅 + 数组相等比较器，host-le-submit/host-le-icon/host-le-tag 实证）；dim 15（行增删 O(n) map/splice 有界、icon-picker ICON_NAMES 模块级 + visibleCount 200/批分页，无 O(n²)）；dim 16（**array-editor 无专属单测——覆盖来源为 composite-field-handles/min-max-reorder/validation 复合测试，行编辑写回/readOnly 冻结/焦点恢复缺直属断言 → P2 补**；icon-picker 既有 8 用例未覆盖选择写回路径 → P2 补）。
- 工具脚本：`check:audit-missing-renderer-markers`/`styling-suspects`/`runtime-raw-schema-reads`/`fieldframe-bypasses`/`performance-suspects`/`async-failure-paths`/`hardcoded-type-dispatch` 4 组件 0 命中；`react19-optimization-candidates` 命中 useCallback 簇（array-editor/key-value/icon-picker 行事件处理与 Popover 回调——C3.x 同裁定）。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 971 → 991 tests）

- **[P1-1 定义 fields 注册缺口 ×3]**：tag-list 补 `{key:'tags', kind:'prop', valueType:'array'}`、key-value 补 `addLabel/uniqueKeys/minItems/maxItems`、array-editor 补 `itemLabel/minItems/maxItems`；**flux-guide/flux-types/schema.d.ts 重新生成**（三类型补齐丢失键，114 types 不变）。test-first：c3-4-schema-contract-honesty.test.ts 5 用例（4 schema 接口键 ↔ 定义 fields 注册三方契约冻结 + contributor 声明断言，先红后绿）。
- **[P1-1 icon-picker required 静默 no-op]**：定义补 `validation: {kind:'field', valueKind:'scalar', getFieldPath}` contributor → schema.required 经 validation-lowering 自动下降 + buildValidationMessage i18n 回退。test-first：icon-picker-validation.test.tsx 2 用例先红后绿。**跨包 bug 按 Bug Fix Test Coverage Rule 记录 `docs/bugs/74-icon-picker-required-validation-silent-noop-fix.md`**。
- **[P2 i18n 硬编码 ×3]**：tag-list registerField validate 消息 → `t('validation.required', {label})`；key-value/array-editor collectRules 去硬编码 message 走 buildValidationMessage `validation.minItems/maxItems/uniqueBy` 回退（uniqueKeys 自定义 message 语义保留）；validateChild 行内消息 → `t('validation.required')`。受影响测试断言同步（key-value.test.tsx collectRules 形状、form-runtime-fields/form-array-validation 消息文本、playground flux-basic-page.debugger 正则化）。
- **[P2 icon-picker ARIA listbox]**：网格 `role="listbox"` + aria-label（新增 `flux.form.iconList` en/zh 键）+ 图标按钮 `role="option"` + `aria-selected` + `aria-label`——aria-haspopup="listbox" 主张兑现。test-first：icon-picker-selection.test.tsx 6 用例（选择写回/搜索/清空/readOnly/disabled/受控 echo，先红后绿）。
- **[P2 测试加固 ×2]**：array-editor-row-editing.test.tsx 6 用例（行编辑写回/append 写回/readOnly/disabled 冻结/删除焦点恢复/新增焦点落新输入）；icon-picker-selection.test.tsx 6 用例（上述）。
- **[P2 文档 ×4]**：tag-list design.md §3/§4/§5（`tags` = 候选集语义，非初始化别名 + fields 现状）；key-value/array-editor design.md §3（fields 现状 + i18n 回退说明）；icon-picker design.md §5（移除 `onChange: event` phantom 主张 + 值变更写 store 语义注记）。
- **[共性裁决]**：fields 注册缺口根因单点（各组件定义文件遗漏），非公共层机制 → 不插 CX-n（C3.1/C3.2 根因单点先例）；CX-8 同型复验本族 pass（无缺口消费）；honesty 契约测试为族级 guard，跨族 guard 归 CG/CR。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**991 tests**）；`check:i18n-keys`/`check:schema-prop-coverage` 绿（172 declared/100% covered）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c3-4-host-surfaces.spec.ts` 4/4 全绿 + lab 页 +4 场景）

- **host-le-submit（bug 73 模式）pass**：form 内 array-editor（行 0 就地改名 alice→alice-x + 新增行 bob）+ key-value（env/prod→staging + 新增 region/us）→ Submit → echo `LE-SUBMIT:` 含 `"value":"alice-x"`/`"value":"bob"`/`"key":"env","value":"staging"`/`"key":"region","value":"us"`——行编辑真机写 store + 提交形状正确（单测绿 + 真机一致）。
- **host-le-tag（bug 73 模式）pass**：tag-list 受控 echo——react 切 off + vite 切 on → Submit → `LE-TAG:` 含 `"vite"` 不含 `"react"`——增删无重复/丢失。
- **host-le-readonly（CX-8 同型复验）pass**：readOnly 表单同载 4 组件——tag 按钮 disabled、array-editor 输入/Add disabled、key-value 输入/Add disabled、icon-picker trigger disabled → Submit → `LE-RO:` 原值全保留（tags/values/keys/icon 未变化）。
- **host-le-icon（bug 73 模式）pass**：icon-picker 弹层搜索 accessibility → option 选择 → trigger 回显 accessibility → Submit → `LE-ICON:"accessibility"`——选择写回成立。
- 回归：composite-editor-handles **5/5**；component-lab 全量 **209 passed / 1 skipped（pre-existing simple-form skip）**；w4c-composite-form-family **8/8**。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：tag-list-lab-page +2（tag echo + readOnly 全家冻结）、key-value-lab-page +1（行编辑提交）、array-editor-lab-page +1（array+key-value 组合提交）、icon-picker-lab-page +1（选择提交）；coverage-manifest 4 组件条目 primaryScenario 同步为 bug 73 模式场景 + **icon-picker 补登记**（此前缺失）。

### Phase 4 — closure 前置

- 4 卡全部 `closed`（P0/P1 清零；P1-1×3 + P1-1 icon + P2×9 fixed；P3 ×15 卡内记录归 CR）；全卡 18 维表结论与最终代码一致（fields 注册/validation contributor/listbox ARIA/i18n 消息均可核验）。
- 全量验证（closure gates 前置证据）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` **58/58 tasks 全绿（form-advanced 991）**；flux-i18n 26 tests 绿（新增 key）。
- 文档：4 卡 closure 汇总；4 份 design.md 同步；flux-guide/flux-types/schema.d.ts 重新生成；bug 74 记录；历史 Deferred 复验（icon-picker iconTemplate/component:open = out-of-scope/optimization 归 CR；E3 non-goals copyable/deleteConfirmDialog/addable-removable toggle 复验裁定成立归 CR）。
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（4 个新测试文件 20 用例：honesty 5 red（收口 6）、icon validation 1 red（收口 2）、icon selection 6 red、row-editing 通过即绿；宿主 spec 2 轮 portal/listbox 定位修正后 4/4）。
- **bug 73 模式专项检查结论：pass**（host-le-submit/host-le-tag/host-le-icon 三场景真机成立——行编辑/受控 echo/选择写回真机提交值形状正确；过程中实证并修复 1 个"单测绿但真机/静默失败"类缺陷：P1-1 icon-picker required 静默 no-op——既有测试全绿（无人断言错误出现）但真机用户提交空值零提示）。
- **Non-blocking follow-up（CR）**：P3 ×15（嵌套 data-slot=field-control ×2（key-value/icon-picker）、react19 useCallback 簇 ×3 组件既有裁定、icon-picker 搜索 input aria-label、icon-picker 缺省 placeholder 硬编码中文、array-editor labelBase 无 i18n key、tag-list \_fieldState 未消费订阅、tag-list/icon-picker handles/iconTemplate Deferred、E3 non-goals 复验登记等）。

### C3.4 BUILD_VERIFY 复验（mission-driver，commit 前）

- 全量复跑：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 tasks 全绿（form-advanced 991）。
- 仓库级 e2e 全量：**825 passed / 9 failed / 43 skipped / 7 did not run**。9 项失败全部落在 mission 未触及包（ai-chat ×1、ai-rich-text-sender ×5、calendar-demo ×1、diff-perf ×1、gantt-bars-and-links ×1（已知 flaky 拖拽类））；与 C0 pre-existing 裁定一致（归属 C8.1/C9/CV，diff-perf 为机器负载敏感阈值、gantt 拖拽为已知 flaky），C3.4 改动（fields 注册/validation contributor/listbox ARIA/i18n 消息/lab 页/coverage-manifest）无新增失败。未达 full-green，不声明（与 Phase 4 记录一致）。

### C3.4 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（独立复核 live repo：4 卡 closed 且 18 维结论与最终代码一致；4 新测试文件 20 用例断言正确行为；实测复核 `c3-4-host-surfaces.spec.ts` 4/4 + `composite-editor-handles.spec.ts` 5/5 + `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck/build/lint/test` 绿（991）+ workspace typecheck/build/lint 31/31 + test 58/58 + check:i18n-keys/schema-prop-coverage 绿 + plan-check --strict pass + flux-types 重新生成核验 + bug 74；deferred 分类诚实；五处一致性核对通过）
- Verdict: **pass（approved）**——审计 session 补修复 1 处残留 owner-doc 漂移：icon-picker design.md §7/§8 仍声明零注册零派发的 `onChange` 事件（与 §5 修复同型 phantom），已移除；plan/roadmap/daily log 收口动作同步完成
- 收口动作（由审计 session 完成）: Closure Gates closure-audit 项勾选 + plan `## Closure` Audit Evidence 回填 + **roadmap C3.4 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.x/C3.1-C3.3 先例一致）

## C3.3 condition-builder 逐组件审计执行：plan-2026-08-03-0921-3 四 Phase 全绿（单组件 18 维 + deep-audit 21-23 + P1×3/P2×3 自动修复 + 宿主场景 4/4 + closure 前置）

执行 `docs/plans/2026-08-03-0921-3-c3-3-condition-builder-audit.md`（condition-builder 单组件 18 维逐组件审计 + 复杂交互渲染器必选 deep-audit 21-23 维 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 族最后一个复杂交互渲染器收官（C3.3 执行完成，roadmap C3.3 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.1/C3.2 先例一致）**。

### Phase 1 — 1 张审计卡（`docs/audits/per-component/condition-builder.md`，18 维 + 21-23 维）

- 18 维逐项核对 + deep-audit 21（显示与定位）/22（集成接线与可操作性）/23（测试有效性与假绿），`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3 类（P1-1 readOnly 不进 umbrella ×1、P1-2 disabled/readOnly 不传播到自定义值编辑器 ×1、P1-3 phantom 声明簇 ×22 键）、P2 ×3（硬编码 aria-label、死 i18n key、guide 示例词汇）、P3 ×6**。
- 重点维度：dim 3（受控 echo audit-probe E PASS——外部 scope 变更 'a'→'b' 树回显；写回经 valueRef/syncValueRef + rewriteItemRight）；dim 11（**live 核对：`2026-06-22-1343-1` 组件级 async metadata 加载 abandoned——设计拒绝组件级 auto-fetch（请求必须下沉）；设计路线 = 外部 data-source 载入 scope + `fields` 表达式读取，audit-probe D PASS 实证 `${fieldDefs}` 表达式 live**；组件无 IO（INV-1 合规）；formula 预览 AbortController + 失败降级）；dim 10（**P1-1：readOnly（组件 schema + 表单级 static:true 经 FormLayoutContext.staticReadOnly）不并入 disabled umbrella——`condition-builder.tsx:170` 仅 effectiveDisabled||submitting，readOnly 时全链 chrome 可点、scope 模式自定义编辑器写投影 scope 直接 throw（崩溃面）**，audit-probe A/B 先红）；dim 6/12（**P1-2：CustomValueEditorHost 无 FormLayoutContext 注入——内嵌 schema 编辑器 disabled 下保持交互，form 模式写泄漏、scope 模式 throw**，audit-probe C 先红）；dim 1（**P1-3 phantom 声明簇：schema 级 addBtnVisibleOn/addGroupBtnVisibleOn（design.md §4/§7.4 主张行为但零实现）/searchable（Base UI combobox 恒搜索 toggle 无实现）+ 字段级 defaultValue/valueTypes/minLength/maxLength/minimum/maximum/step/precision/date-time format 族/autoComplete/maxTagCount/searchable/disabled 共 22 键——C3.2 P1-2 同型三方契约漂移**）。
- 工具脚本：`check:audit-missing-renderer-markers`/`styling-suspects`/`runtime-raw-schema-reads`/`fieldframe-bypasses`/`performance-suspects`/`hardcoded-type-dispatch` 0 命中；`async-failure-paths` 命中 :111（formula fail-open 设计，合规）；`react19-optimization-candidates` 命中 useCallback 簇（H30 投影缓存既有设计，C3.x 同裁定）。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 958 → 971 tests）

- **[P1-1 readOnly umbrella]**：`condition-builder.tsx:170` umbrella 补 `presentation.readOnly`（含组件级 + FormLayoutContext.staticReadOnly 表单级）——readOnly 与 disabled 同链冻结，杜绝 form 模式「点击无效果」误导与 scope 模式投影写 throw。test-first：condition-builder-readonly-umbrella.test.tsx 5 用例先红后绿（audit-probe A/B 落为回归）。
- **[P1-2 自定义值编辑器冻结（CX-8 机制消费）]**：CustomValueEditorHost 以 `FormLayoutContext.Provider staticReadOnly` 包裹（value-input.tsx，useMemo 防 context 重建）——内嵌 schema 编辑器子字段渲染 readonly（C3.1/C3.2 复合族同机制）。test-first：condition-builder-custom-editor-freeze.test.tsx 4 用例先红后绿（input-text 编辑器 readonly 属性 + 程序化赋值不落地 + readOnly 折叠 + 正常态写回对照）。**共性裁决：CX-8 机制消费（roadmap CX-8 行已 done——C3.1 证据，本 plan 完成消费留痕，不重复插 CX-n）**。
- **[P1-3 phantom 声明簇]**：types.ts 移除 22 键（addBtnVisibleOn/addGroupBtnVisibleOn/searchable（schema+field 级）/defaultValue/valueTypes/minLength/maxLength/minimum/maximum/step/precision/date-time format 族 ×7/autoComplete/maxTagCount/disabled（field 级））；`removeConditionLabel` 低成本实现（ConditionGroup → ConditionItem aria-label/title，design §4 主张兑现）；消费键补注册定义 fields（formFieldRules + fields/operators/formulas/formulaForIf（value）+ builderMode/embed/showAndOr/showNot/showIf/draggable/uniqueFields/maxDepth/maxItemsPerGroup/placeholder/addConditionLabel/addGroupLabel/removeConditionLabel/removeGroupLabel（prop），validation 提取为命名 const 使 coverage 提取器可解析——顺带修复 check:schema-prop-coverage 对含内联函数定义文件的解析盲区（注释中引号/反引号会破坏朴素括号扫描，已规避））；design.md §2/§3/§4/§5/§7.4/§7.6/§10 同步（phantom 主张移除 + readOnly 折叠 + 自定义编辑器冻结文档化）；addGroupBtnVisibleOn 表达式门控能力登记 CR backlog。test-first：c3-3-schema-contract-honesty.test.ts 4 用例（ConditionBuilderSchema/字段族/嵌套类型声明 ↔ 定义注册 ↔ 消费三方契约冻结 + 全 props 可渲染 fixture，覆盖 check:schema-prop-coverage 新增 18 注册键）。
- **[P2-1] 硬编码 aria-label**：MultiSelect 移除 badge `Remove value ${label}` → flux-i18n 新增 `conditionBuilder.removeValue` zh/en（既有测试同步为译文案断言）。
- **[P2-2] 死 key**：`conditionBuilder.searchField` 双 locale 移除。
- **[P2-3] guide 词汇**：flux-guide form-advanced-fields.md §5 示例 `contains` → `like` + vocabulary 注释。
- **[P3-2] `ConditionCustomOperator.values`**：design §7.2 明示「尚未消费…后续可扩展」——文档诚实的未来扩展点，保留（honesty test allowlist）；P3-4 dnd-kit sensor 为 b61 既有裁定（watch-only）；P3-3 title 为 BaseSchema 通用键。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**971 tests**）；`check:i18n-keys`/`check:schema-prop-coverage` 绿（172 declared/100% covered，condition-builder +18 键强制覆盖）；flux-guide/flux-types/schema.d.ts 重新生成（ConditionBuilderSchema +18 props）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c3-3-host-surfaces.spec.ts` 4/4 全绿 + lab 页 +4 场景）

- **host-cb-submit（bug 73 模式）pass**：form 内 condition-builder 种子规则值切 inactive + 添加条件 + 嵌套分组 → Submit → echo 含 `CB-HOST:`、`"right":"inactive"`、`"id":"group-`、`"op":"select_equals"`、`"conjunction":"and"`——条件构建真机提交成立（单测绿 + 真机一致）。
- **host-cb-disabled（b61 复验）pass**：disabled 全链冻结（add/add-group/AND/OR disabled、值控件 disabled、删除 chrome 不渲染）→ Submit → echo `CB-DISABLED:{"...","right":"locked"}` 未变化。
- **host-cb-readonly（P1-1 真机证明）pass**：readOnly 与 disabled 同链冻结 → Submit → `CB-READONLY:{"...","right":"frozen"}` 未变化——**修复前 chrome 可点、form 模式写入被静默丢弃（缺陷实证）**。
- **host-cb-custom（P1-2 真机证明 + P2-4 实证）pass**：select 自定义值编辑器正常态 Editor→Viewer 写回（echo `"right":"viewer"`）；disabled 副本 chrome 禁用 + 弹层选择 Viewer 无操作（onValueChange unwired）→ echo frozen 侧 `"right":"editor"` 未变化——写阻断成立；**顺带实证 P2-4：combobox 编辑器 readonly 冻结仅解除接线、输入视觉仍可交互（登记 P2 backlog 归 CR，`shared:` 根因在 select renderer form 包公共层）**。
- 回归：condition-builder-formula e2e **4/4**；component-lab 全量 **196 passed / 1 skipped（pre-existing simple-form skip）**；w4c-composite-form-family + component-handles/composite-editor-handles/dialog-real-schema/dialog-edit-submit/dialog-dropdown-row-edit/form-ajax-includescope/surface-form-input **19/19**。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：condition-builder-lab-page +4（嵌套提交 bug 73 模式 / disabled 提交 / readOnly 提交 / 自定义编辑器写回+冻结）；coverage-manifest condition-builder primaryScenario 同步为 bug 73 模式场景。

### Phase 4 — closure 前置

- 审计卡 `closed`（P0/P1 清零；P1-1 + P1-2 + P1-3 + P2×3 fixed；P2-4 `shared:` backlog 归 CR；P3 ×6 卡内记录）；18 维表 + 21-23 维结论与最终代码一致。
- 全量验证（closure gates 前置证据）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` **58/58 tasks 全绿（form-advanced 971）**。
- 文档：审计卡 closure 汇总；design.md §2/§3/§4/§5/§7.4/§7.6/§10 同步；flux-guide schema.d.ts 重新生成 + 示例词汇修正；CX-8 机制消费留痕（roadmap CX-8 行已 done，本 plan 消费记录于卡 + log）；P2-4 shared 登记 CR。
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（audit-probes 3 red（A/B/C）→ readonly-umbrella 5 + custom-editor-freeze 4 + honesty 4 全绿；宿主 spec 3 轮 selector/形状修正后 4/4）。
- **bug 73 模式专项检查结论：pass**（host-cb-submit 真机成立——条件构建真机提交值形状正确；过程中实证并修复 2 个"单测绿但真机/多实例失败"类缺陷：P1-1 readOnly 静默不写（单测仅断言不写、未断言 UI 冻结）、P1-2 自定义编辑器禁用态写泄漏（既有测试未覆盖自定义编辑器 × disabled 组合））。
- **Non-blocking follow-up（CR）**：P2-4 `shared:`（select renderer combobox readOnly 视觉残留）；P3 ×6（fields 补注册前依赖默认 prop 分类、values 文档化扩展点、title 通用键、dnd-kit sensor 既有裁定、picker conditionCount 仅计顶层、H30 useCallback 既有设计）；addGroupBtnVisibleOn 表达式门控能力登记。

### C3.3 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（执行证据已交付：1 卡 closed + 13 新测试用例先红后绿 + c3-3 4/4 + formula 4/4 + component-lab 196/1skip + w4c 等 19/19 + workspace typecheck/build/lint/test 全绿 + i18n/schema-prop-coverage 绿 + flux-types 重新生成）
- Verdict: **pass（approved）**——审计 session 独立复核 live repo：1 卡 closed 且 18 维 + 21-23 维结论与最终代码一致（P1-1 umbrella readOnly（condition-builder.tsx:177 含 presentation.readOnly）、P1-2 value-input.tsx FormLayoutContext.Provider staticReadOnly（:151-166）、P1-3 phantom 移除 22 键（types.ts grep 零残留）+ 定义补注册 18 键 + c3-3-schema-contract-honesty.test.tsx 4 用例、P2×3 fixed 均在代码可核验）；13 新测试断言正确行为；审计 session 实测重跑 `c3-3-host-surfaces.spec.ts` 4/4 + `condition-builder-formula.spec.ts` 4/4 + form-advanced typecheck/build/lint/test 全绿（971 tests 119 files）+ 仓库级 `pnpm typecheck` 31/31 + `check:i18n-keys`/`check:schema-prop-coverage` 绿；deferred 分类诚实（P2-4 shared 归 CR、P3 ×6 卡内记录、addGroupBtnVisibleOn 登记 CR；metadata 异步路径为设计拒绝，`${fieldDefs}` 表达式路线实证 live，非缺陷降级）；五处一致性（Plan Status/Phase Status/Exit Criteria/Closure Gates/Closure evidence）核对通过
- 收口动作（已由审计 session 完成）: ✅ Closure Gates closure-audit 项勾选 + ✅ plan `## Closure` Audit Evidence 回填 + ✅ **roadmap C3.3 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.x/C3.1/C3.2 先例一致）

### C3.3 BUILD_VERIFY 复验（mission-driver，commit 前）

- 全量复跑：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 tasks 全绿（form-advanced 强制非缓存重跑 119 files/971 tests 全过，含 c3-3 新增 3 个测试文件）；`c3-3-host-surfaces.spec.ts` 4/4。
- 仓库级 e2e 全量：**818 passed / 10 failed / 43 skipped / 7 did not run**。10 项失败全部落在 mission 未触及包（ai-chat ×1、ai-rich-text-sender ×6、calendar-demo ×1、diff-perf ×1、gantt-bars-and-links ×1、w3d-editor ×1）；**clean baseline（stash 本 mission 全部改动后）复跑 ai-chat/calendar-demo/diff-perf 同路径失败——pre-existing 确认**，w3d-editor 复跑通过（flaky）。其中 diff-perf 为性能基线（机器负载敏感）、gantt 拖拽为已知 flaky 类；与 C3.3 改动（condition-builder + i18n 增键 + lab 页）无依赖关系。未达 full-green，不声明（与 Phase 4 记录一致，pre-existing 项归属 C8.1/C9/CV）。

## industrial-hmi mission 立项（范围讨论 + mission 配置 + roadmap）

为 `nop-chaos-flux` 新增工业组态（HMI/SCADA）**运行时渲染**能力的 mission 立项，两轮 Q&A 范围讨论全部采纳推荐项（11 问），共识后产出 4 份立项产物：

- **讨论记录**：`docs/discussions/2026-08-03-industrial-hmi-scada-mission-scope-discussion.md`——两轮 Q&A（Q1–Q7 + Q8–Q11 完整问题/选项/选择）+ 最终决策 + 总结（关键决策表 + 显式待定事项）。
- **Mission 配置**：`missions/industrial-hmi.json`（roadmapPath=docs/components/roadmap-industrial-hmi.md，commitFormat `feat(industrial-hmi):`，对齐 scheduling 格式）。
- **Roadmap**：`docs/components/roadmap-industrial-hmi.md`——17 阶段 I0–I16：调研（6 组项目 clone 到 `~/sources/industrial-hmi-research/` + 5 份调研报告）→ 4 个固定 review gate（I1/I3/I7/I12）→ 引擎设计（4 份 design-\*.md）→ 包基建（`@nop-chaos/flux-renderers-industrial` + leafer-ui）→ 实现 Wave1-4（场景图适配/绑定动画/基础图元/设备图元）→ React/flux 集成（`scada-canvas`）→ 事件联动 → Playground → Benchmark（10 万图元 ≥45fps / 首屏 <2s / 内存 ≤320MB / 1 万点刷新 <200ms）→ 收尾 → I16 编辑器后继 mission 立项入口（不实现）。
- **关键决策**：运行时优先编辑器后置；LeaferJS 底座 + 自研组态语义层；双轨数据模型（点表 + flux 表达式桥接）；单容器 type `scada-canvas` 内嵌组态 JSON；图元事件→flux action 全做、编辑器交互后置；逻辑层单测 + e2e 程序化断言（测试句柄读场景树）。
- **文档共识审查要求**：roadmap Cross-Cutting/Rule 固化——本 mission 全部 AI 编写文档必须经独立子 agent（fresh session）反复审查直到共识（判据：连续一轮零新增项；裁决：不得单方拒绝；上限 3 轮超限升级人工；review gate 充当对应阶段文档共识终轮复核，不叠加）。
- **立项产物共识审查**：R1 判定 `REVISE`（20 条修正项）已全部落地：mission JSON 验收口径 1k→10k 点对齐、I11.2 收缩为浏览交互（拖拽/多选后置 I16 对齐 Q8）、讨论文件补 Q1–Q7 问题正文 + 总结节 + 状态定稿、文档共识条款与既有 plan 审查体系边界切分（不叠加 + 裁决规则 + 轮次上限）、补 new-renderer-introduction-audit 五边界审计（I10.2）、补每日日志、leafer 5 仓统一、性能数字加"官方自报待 I0 校准"注记、work items 表去 Status 列（Phase Status 唯一状态面）、依赖引用统一、平台复用表、测试句柄机制、flux-guide 收尾同步等。R2 `REVISE`（轻量）3 项（日志置顶/预写判定/措辞张力）、R3 `REVISE`（轻量）2 项（Round 3 占位预写 AGREE 复发/§九 Round 2 过期引用）均落地；**R4 `AGREE`——达成共识**（R1–R3 修正 3 轮 + R4 确认轮，未超上限），两份文件头部记录块已回填终态。
- **导航**：`docs/index.md` 新增 industrial-hmi 组件设计路由行。
- **验证**：`missions/industrial-hmi.json` JSON 语法校验通过；roadmap 表格列一致性检查通过。

## SPC 控制图支撑：chart 控件增强（referenceLines/band/markers）+ inventory 文档同步

nop-app-erp 质量域 SPC 控制图调研结论落地（三方开源 ERP 无 SPC——erp-survey 战略差距表 `SPC / Control charts ★LEAD ○...`，无法参考；按 `docs/analysis/2026-07-20-complex-ui-controls-inventory-for-flux.md` §2.6 既有规划——chart 增强而非新渲染器）：

- **实现**（`packages/flux-renderers-data/src/chart-renderer.tsx` + `chart-schemas.ts`）：chart 控件新增三个通用增强字段——
  - `referenceLines: { value, label?, color?, dashed? }[]`：UCL/LCL/CL 水平参考线（recharts `ReferenceLine`，标签 `insideTopRight`，虚线可配，默认色 chart-5），line/bar/area 生效，pie/scatter 忽略
  - `band: { upper, lower, color?, opacity? }`：上下界阴影带（`ReferenceArea`，默认 chart-2/0.08 透明度），缺任一边界即忽略
  - `markers: { dataKey?, indices?, color? }`：异常点标记——`dataKey` 按源行布尔标志挑点（SPC `isOutOfControl`/`violatedRules` 字段），`indices` 按下标挑点；有标记时 Line `dot` 换自定义圆点（默认红 #ef4444），无标记保持 `dot={false}` 无点折线
  - 参考线同步进 sr-only 无障碍文本摘要；非有限数值/非法结构 sanitize 丢弃
- **定义/类型**：`data-renderer-definitions.ts` chart fields +3；重生成 `flux-guide/flux-types/schema.d.ts` ChartSchema +3（114 types 不变）；`check:schema-prop-coverage` 绿（新字段 9 用例覆盖）
- **测试**：`__tests__/chart-renderer-config.unit.test.tsx` +9 用例（ReferenceLine y/label/dash、ReferenceArea 阴影带、非法 band 忽略、markers dataKey/indices → dot function、无标记回归 dot=false、无 overlay 回归、pie 忽略、sr-only 摘要）；flux-renderers-data 全量 691 tests 绿
- **文档**：`flux-guide/design-patterns/chart.md` +SPC 控制图示例与字段参考；nop-app-erp `complex-ui-controls-inventory-for-flux.md` §2.6/§3 标 ✅ 已覆盖（2026-08-03），并修正过时引用 `spc-analysis.md` → `spc.md`
- **验证**：data 包 typecheck/build/lint 绿；仓库级 `pnpm typecheck`/`build`/`test`（58/58 tasks）全绿（并发 session C3.2 收口后不再阻塞）

## C3.2 form-advanced 组合字段族逐组件审计执行：plan-2026-08-03-0921-2 四 Phase 全绿（object-field/array-field/detail-field/detail-view/variant-field 5 组件 18 维 + P0/P1 自动修复 + 宿主场景 4/4 + closure 前置）

执行 `docs/plans/2026-08-03-0921-2-c3-2-form-advanced-composite-field-family-audit.md`（5 组件 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 组合字段族执行完成（roadmap C3.2 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.1 先例一致）**。

### Phase 1 — 5 张审计卡（`docs/audits/per-component/{object-field,array-field,detail-field,detail-view,variant-field}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3 类（P1-1 object-field transformOut 跨实例干扰 ×1、P1-2 phantom 声明 ×15（schema 声明零注册零实现）、P1-3 readOnly/disabled 不传播 ×5 组件（CX-8 同型））、P2 ×2（design.md 缺口 ×5 裁定、surface size/placement 文档化零行为）、P3 ×5**。
- 重点维度：dim 1（**P1-2 phantom 声明簇：composite-schemas.ts 声明 `trigger`/`openAction`/`confirmAction`/`cancelAction`（detail-field :84-88 / detail-view :200-204）、array-field `sortable`+transform\*×3（:28-32）、object-field `validateValueAction`（:18）、VariantOption `transformOutAction`/`validateValueAction`（:51-52）——定义零注册、渲染器零引用、flux-guide 零文档（C3.1 P1-1 同模式）；另 `surface.size/placement` 被 flux-guide §3/§4 文档化但实现零读取**）；dim 3（**P1-1：transformOut stale 抑制按共享 owner（parentForm ?? parentScope）键控——同表单两个 transformOut object-field，一个 unmount 会 invalidate 共享 sequence，另一个在途 writeback 被静默丢弃（bug 73 模式：单测绿单实例、多实例失败）**）；dim 10（**P1-3：array-field/object-field/detail-field/detail-view/variant-field 的 readOnly/disabled 不传播到子字段——child 字段 presentation 读 useFormLayout().staticReadOnly（field-presentation.tsx:50-51），复合组件未提供 FormLayoutContext.Provider；form 模式子字段可编辑写回、scope 模式写 projected scope 直接 throw（projected-owner-scope.ts:125-141）——CX-8 同型缺口，roadmap 已注明 C3.2 按同机制修复**）；dim 11（object-field transformIn/Out abort+sequence、detail open/confirm sequencer+mountedRef+dispose、variant detect/switch abort+requestId——均 pass，本族无远程数据 n-a）；dim 17（**5 组件无 design.md——裁定：登记 P2 backlog 归 CR**（flux-guide composite-fields.md 为族级文档基线，组件级拆分由 CR 集中处理，成本 >15 分钟/份））。
- 工具脚本：`check:audit-missing-renderer-markers`/`check:audit-styling-suspects`/`check:audit-runtime-raw-schema-reads` 0 命中；`check:audit-fieldframe-bypasses` 仅 variant-field-view.tsx:200-222（**主动合规**：variant-field 自行管理 frameWrap 的 FieldFrame 全参数渲染，非绕过）；`check:audit-react19-optimization-candidates` 命中 ArrayItem memo/useCallback 为复合字段族既有设计（b32 C12 先例）；`check:audit-async-failure-paths` 命中 remove-when-gating:56（fail-open 设计）/value-adaptation-helper:283（catch→valid:false）/variant-field-controller:141（abort-aware catch）均合规。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 939 → 958 tests）

- **[P1-1 object-field transformOut 跨实例干扰]**：sequence/pending 键改 per-instance `transformOutOwner`（object-field.tsx :170-183），unmount 只 invalidate 自身实例。test-first：object-field-transform-owner-isolation.test.tsx（旧代码实测丢 writeback——submitValues 缺 profileB；修复后提交值完整，真红真绿）。
- **[P1-2 phantom 声明×15]**：composite-schemas.ts 移除 `trigger`/`openAction`/`confirmAction`/`cancelAction`（detail-field ×4 + detail-view ×4）、array-field `sortable`/`transformInAction`/`transformOutAction`/`validateValueAction`（×4，sortable 排序能力登记 CR）、object-field `validateValueAction`（×1）、VariantOption `transformOutAction`/`validateValueAction`（×2）。test-first：c3-2-schema-contract-honesty.test.ts 7 用例（解析 composite-schemas.ts 接口键 vs 定义 fields 注册，防未来 phantom 回归；5 用例先红后绿）。
- **[P2 surface size/placement 兑现]**：flux-guide §3/§4 文档化的 `surface.size`（dialog → DialogContent data-size，'sm'|'default'|'lg'|'xl' 未知值回退 default）与 `surface.placement`（drawer → Drawer direction，未知值回退 bottom）实现（detail-surface.tsx + detail-field/detail-view 传参）。test-first：detail-surface-size-placement.test.tsx 3 用例先红后绿。
- **[P1-3 CX-8 同型修复 ×5 组件]**：array-field（ArrayItemView FormLayoutContext.Provider staticReadOnly :94-105）、object-field（bodyLayout :238-246）、detail-field/detail-view（DetailDraftBody staticReadOnly prop + FormLayoutContext.Provider）、variant-field（readOnly content 回退分支 readOnlyLayout :162-177）——均按 C3.1 combo/input-table 同机制。test-first：c3-2-readonly-propagation.test.tsx 7 用例（array object/scalar + object + detail-field/detail-view draft + variant content 回退，先红后绿）。**顺带 UX 修复**：variant-field select 模式 trigger 显示原始 key 而非 label（SelectValue 渲染 activeLabel）。
- **[共性裁决]**：P1-3 为 **CX-8 机制消费**（roadmap CX-8 行已 done——C3.1 证据，本 plan 完成 C3.2 消费，卡内 + daily log 留痕，不重复插 CX-n）；P1-1 根因单点（object-field.tsx 模块级 WeakMap 键控）不插 CX-n；P1-2 根因单点（composite-schemas.ts 类型声明文件）不插 CX-n；P2 design.md 缺口裁定登记 backlog 归 CR。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**958 tests**）；`check:i18n-keys`/`check:schema-prop-coverage` 绿。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c3-2-host-surfaces.spec.ts` 4/4 全绿 + lab 页 +4 场景）

- **host-objarr-submit（bug 73 模式）pass**：form 内 object-field（street/city 编辑）+ array-field（行 0 改名 + 新增行）→ Submit → `ObjArr: {"address":{"street":"9 Main St","city":"Amesville","zip":"62701"},"contacts":[{"name":"AliceX","phone":"P-100"},{"name":"Bob","phone":"P-200"},{"name":"Carol","phone":"P-300"}]}`——行 scope 隔离 + 提交数据形状正确。
- **host-detail-proj（bug 73 模式）pass**：detail-field 投影 dialog 编辑（cancel 不写回实证 → 重开编辑 confirm）→ Submit → `Detail: {"street":"1 Main St","city":"Boston","state":"DC","zip":"20500"}`——投影 scope 与宿主 form 解耦（draft 仅 confirm 时写回，未确认键保留）。
- **host-variant-switch（bug 73 模式 + 值所有权三态）pass**：select 模式切 'Multiple Contacts' → 分支值写回（scope 实证 `contactMode:[...]` dirty/touched）→ 分支编辑器跟随 → 新增行 → Submit → `Variant: ["a@example.com","b@example.com","c@example.com"]`——无 stale 无循环。
- **host-obj-disabled（P1-3 修复真机证明）pass**：readOnly object-field/array-field 子输入 `readonly` 属性、Add/Remove chrome 隐藏、DOM 注入不改值 → Submit → `RO: {"address":{"street":"FIXED-ST","city":"RO-CITY"},"contacts":[{"name":"RO-1","phone":"R-100"},{"name":"RO-2","phone":"R-200"}]}`——**修复前子字段可编辑写回（CX-8 缺口实证）**。
- 回归：w4c-composite-form-family **8/8**；component-lab 全量 **200 passed / 1 skipped（pre-existing simple-form skip）**；component-handles/composite-editor-handles/boolean-control-value-contract **15/15**；dialog-real-schema/dialog-edit-submit/dialog-dropdown-row-edit/form-ajax-includescope/surface-form-input **10/10**。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：object-field-lab-page +1（嵌套提交）、array-field-lab-page +1（readOnly 提交）、detail-field-lab-page +1（投影提交）、variant-field-lab-page +1（变体切换提交）；coverage-manifest 5 组件条目 primaryScenario 同步为 bug 73 模式场景。

### Phase 4 — closure 前置

- 5 卡全部 `closed`（P0/P1 清零；P1-1 + P1-2×15 + P1-3×5 + P2 surface 兑现 fixed；P2 backlog + P3 ×5 卡内记录归 CR）；全卡 18 维表结论与最终代码一致。
- 全量验证（closure gates 前置证据）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test --force` 58/58 tasks 全绿（form-advanced 958）。
- 文档：5 卡 closure 汇总；CX-8 机制消费留痕（roadmap CX-8 行已 done，本 plan 消费记录于卡 + log）；design.md 缺口裁定（backlog 归 CR）；flux-guide composite-fields.md 为族级文档基线确认。
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（4 个新测试文件 19 用例：schema-contract-honesty 5 red、readonly-propagation 7 red、surface-size-placement 3 red、transform-owner-isolation 1 red——修复后 958 全绿；宿主 spec 2 轮 selector/locale/portal 修正后 4/4）。
- **bug 73 模式专项检查结论：pass**（host-objarr-submit/host-detail-proj/host-variant-switch 三场景真机成立 + P1-1 多实例丢 writeback 实证修复；P1-3 宿主实证修复）。
- **Non-blocking follow-up（CR）**：P2 backlog（5 组件 design.md 拆分）；P3 ×5（嵌套 data-slot=field-control ×3、variant-field reportVariantFieldFailure 硬编码 fallback、variant 顶层 transform\* ignored 文档化）；array-field `sortable` 排序能力 feature 登记。

### C3.2 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（closure-audit 独立复核执行于 2026-08-03，非执行 session 上下文）
- Verdict: **pass（approved）**——审计 session 独立复核 live repo：5 卡 closed 且 18 维结论与最终代码一致（P1-1/P1-2/P1-3/P2 均 diff/测试可核验；phantom 声明实际移除 15 处：object ×1 + array ×4 + variant ×2 + detail-field ×4 + detail-view ×4）；4 个新测试文件 19 用例断言正确行为；实测复核 `c3-2-host-surfaces.spec.ts` 4/4 + `w4c-composite-form-family.spec.ts` 8/8 + `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck/build/lint/test` 全绿（958）+ `check:i18n-keys`/`check:schema-prop-coverage` 绿 + `plan-check.mjs --strict` pass；deferred 分类诚实（无静默降级 live defect）；五处一致性（Plan Status/Phase Status/Exit Criteria/Closure Gates/Closure evidence）核对通过
- 收口动作（由审计 session 完成）: ✅ Closure Gates closure-audit 项勾选 + ✅ plan `## Closure` Audit Evidence 回填 + ✅ plan Status Note phantom 计数修正（×12 → ×15）+ ✅ **roadmap C3.2 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.x/C3.1 先例一致）

# Development Log - 2026-08-03

## C3.5 form-advanced 媒体富文本族逐组件审计执行：plan-2026-08-03-1616-2 Phase 1+2（editor/input-file/input-image/tree-select/input-tree 5 组件 18 维 + deep-audit 21-23 + P1×2 类/P2×7 自动修复 + bug 76 记录）

执行 `docs/plans/2026-08-03-1616-2-c3-5-form-advanced-media-rich-text-family-audit.md`（媒体富文本族 5 组件 18 维逐组件审计 + 复杂交互渲染器必选 deep-audit 21-23 维 + test-first 自动修复）。**Phase 1（5 张审计卡）+ Phase 2（P0/P1 自动修复）完成；Phase 3（宿主真实浏览器场景）/Phase 4（closure 前置）待续**。

### Phase 1 — 5 张审计卡（`docs/audits/per-component/{editor,input-file,input-image,tree-select,input-tree}.md`，18 维 + editor/tree-select/input-tree 21-23 维）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×2 类（P1-1 editor link 按钮零行为 + 树 fields 注册缺口 input-tree ×8 / tree-select ×10 消费键未注册、P1-2 editor URL 协议校验缺失（安全红线）+ pathSeparator 零行为）、P2 ×7（editor i18n 硬编码 ×14、upload Remove aria-label、input-image alt 兜底、tree-select placeholder fallback、popover 高度、input-image 专属测试缺口、editor 组件测试缺口、coverage-manifest 缺 3 组件登记）、P3 ×12**。
- 08-02 nested-schema 机制复验：**pass**（searchSource/childrenSource/uploadAction/deleteAction 均 actionValue 声明，无 deepFields 残留——live grep 零命中）。
- 工具脚本：`check:audit-missing-renderer-markers`/`styling-suspects`/`runtime-raw-schema-reads`/`fieldframe-bypasses`/`hardcoded-type-dispatch` 本族 0 命中；`async-failure-paths` 命中 upload-field/editor-renderer 均为已处理 catch/finally（合规）；`performance-suspects`/`react19-optimization-candidates` 命中为 H8/H14/H15 既有守卫（C3.x 同裁定）。
- 共性裁决：pathSeparator 零行为根因单点（共享 `tree-options.ts` 单文件，非跨包机制）→ 卡内共享修复，不插 CX-n；editor link/URL 安全为组件内修复；树 fields 注册缺口根因单点（`tree-controls.tsx` 定义文件）→ 不插 CX-n。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 991 → 1013 tests）

- **[P1-1 editor link 按钮零行为]**：新增 `@tiptap/extension-link@^3.27.1`（deps）+ `@tiptap/core`（devDeps 供 headless 测试）+ `buildEditorExtensions()`（StarterKit + Link.configure protocols 白名单 + validate）+ `isSafeLinkUrl` 纯函数 + link 按钮 run 重写（prompt null→unset / 安全→setLink(trim) / 不安全→忽略）。test-first：`editor-link.test.tsx` 9 用例（7 先红：setLink 命令缺失 + 纯函数不存在；headless Editor 直接覆盖被破坏的扩展接线面）。
- **[P1-2 editor URL 协议校验（安全红线）]**：Link.configure protocols `['http','https','mailto','tel']` + validate=isSafeLinkUrl（防粘贴 `javascript:`）+ **输出防线**：onUpdate html 分支提交前二次 `sanitizeEditorHtml(getHTML())`（design §W3d「输出只含安全子集」主张兑现）——`javascript:`/`data:`/`vbscript:` 从 UI/paste/输出三路全堵。
- **[P2-1 editor i18n ×14 key]**：flux-i18n 新增 `editor.*` 双 locale（toolbarLabel/richTextEditor/bold/italic/strike/heading1/heading2/bulletList/orderedList/code/blockquote/link/undo/redo/linkPrompt）；TOOLBAR_BUTTONS titleKey → `toolbarButtonTitle(id)` switch 静态 key（`check:i18n-keys` 静态解析）；editor aria-label fallback → `t()`。
- **[P1-1 树 fields 注册缺口 ×8+10]**：`tree-controls.tsx` 定义补注册 input-tree `treeMode/childrenKey/labelField/valueField/cascade/searchable/onlyLeaf/showPathLabel` + tree-select 追加 `clearable/placeholder`；`flux-guide/flux-types/schema.d.ts` 重新生成（两类型补齐 8/10 键）；test-first：`c3-5-schema-contract-honesty.test.ts` 4 用例（schema 声明 ↔ 定义注册三方冻结 + actionValue 契约 + 无 deepFields）先红（phantom 8/10 键）后绿。
- **[P1-2 pathSeparator 零行为]**：`getTreeOptionConfig` 增 pathSeparator（默认 '/'）+ `buildTreeOptionMeta` valuePath 消费 + 两 renderer 传参；既有「uses pathSeparator」测试为伪覆盖（单根节点无分隔符，dim 23 案例）——补嵌套节点真断言（'root > child'）。test-first：`tree-enable-node-path.test.tsx` 新用例先红（'root/child' vs 期望）后绿。
- **[P2-2 tree-select placeholder fallback]**：'Select tree option' 硬编码 → `flux.form.treeSelectPlaceholder` 双 locale。
- **[P2-2 桌面 popover 高度]**：PopoverContent 包 `max-h-[60vh] overflow-y-auto`（data-slot="tree-select-popover-options"；C3.3 picker 先例；mobile Sheet 已有 max-h-[65vh]）。
- **[P3-1 input-tree data-testid/data-cid 透传]**：与 tree-select 对齐。
- **[P2-1 input-file Remove aria-label]**：`Remove ${name}` 硬编码 → `flux.form.removeItem` 双 locale（t 参数插值）。
- **[P2-1/P2-2 input-image]**：alt 兜底硬编码 'uploaded image' → `flux.form.uploadedImage` + alt 回退链 name→url→i18n；新增 `input-image-preview.test.tsx` 6 用例（既有值回显/multiple 数组/fill 尺寸/thumbnail 尺寸/readOnly 冻结移除+trigger disabled/失败无缩略图不污染值）。
- **[P2-2 测试加固]**：`tree-structure.test.tsx` +2（自定义 childrenKey/labelField/valueField + showPathLabel/onlyLeaf 真实映射断言——同时补齐 `check:schema-prop-coverage` 新注册 8 键覆盖，180 declared/100%）。
- **[P2-2 coverage-manifest]**：editor/input-file/input-image 补登记（此前缺失，C3.4 icon-picker 同型）；input-tree/tree-select primaryScenario 同步为 bug 73 宿主场景。
- **[共性裁决]**：pathSeparator 零行为根因单点（共享 `tree-options.ts` 单文件）→ 卡内共享修复（两组件同时生效），不插 CX-n；editor link/URL 为组件内修复；fields 注册缺口根因单点（`tree-controls.tsx` 定义文件）→ 不插 CX-n；onlyLeaf 非 cascade 模式父节点可选语义未文档化 → 卡内 P2 backlog 归 CR。
- **bug 76 记录**：`docs/bugs/76-editor-link-toolbar-zero-behavior-unregistered-command-fix.md`（复杂跨面 bug：契约主张漂移 + 安全红线双根因）。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**1013 tests**）；`check:i18n-keys` 绿（729 used/全定义，unused 警告 126 项为 pre-existing）；`check:schema-prop-coverage` 绿（180 declared/100%）；flux-i18n 26 tests 绿；flux-guide schema.d.ts 重新生成。

## flux-guide 文档全量审计更新：scheduling 4 篇修订 + AI 家族新增 ai.md + flux-types 补全（3 轮独立审查达成共识）

审计发现 flux-guide 滞后：scheduling 文档停在 7-22（其后 28+ commits 行为变更），AI 家族（14 组件）零文档。本轮更新全部内容：

### 文档更新（5 篇）

- **scheduling 4 篇修订**（对照 `scheduling-renderer-definitions.ts` + 各组件 types + 实现逐字段核对）：
  - `gantt.md`：`zoomLevels` 替代废弃 `scales`；补 `taskBarHeight`/`onEmptyCellClick`/`onMount`/`onUnmount`/className props；链接 4 类型 + `lag`；segments/baselines；废弃字段清单（`scales`/`startDate`/`endDate`/`childrenField`/`initiallyExpanded`/`progressBarHeight`/`calendar`）
  - `kanban.md`：修正 4 处错误——`columnsConfig` 运行时仅 controlled 模式 `collapsed` 生效（title/cardLimit/wipStrict 实际取列 `data`）；`filterCard` 是表达式（作用域 `{card,text}`）非属性路径；`statusPath` 已注册未接线；`kanbanOwnership` 已实现三态（非保留）
  - `calendar.md`：补 `locale`（默认 `navigator.language`）/`statusPath`/className props/资源嵌套 `title`（`text` 废弃）/事件 `status`；`onImportError`/`onBatchSchedule`/`onImport`/`onTimezoneChange` 标注保留未接线
  - `barcode-input.md`：补 `validate`（仅 `message` 生效，action/debounce 未接线）/`wasmUrl`（默认 unpkg CDN，原生 BarcodeDetector 优先）/`onMount`/`onUnmount`；队列 `error` 为预留状态
- **新增 `design-patterns/ai.md`**（AI 家族首个指南文档）：14 组件总览 + ai-chat 字段/Regions/Events/component handle 6 方法（`sendMessage`/`abort`/`clear`/`getMessages`/`setMessages`/`regenerate`）+ `ai` 命名空间 7 动作 + 工具循环（`tools`/`toolExecutor`/`maxToolRounds` 默认 8）+ 宿主接线（`env.stream` 必配、`createStreamBasedAiConnector`、`xui:imports` ai 命名空间）+ MessageEngine/plugins/hooks + 注意事项 4 条

### 类型生成补全

- `flux-guide/scripts/shared.mjs` REGISTER_PACKAGES 补 `flux-renderers-scheduling`/`flux-renderers-ai`；`generate-types.mjs` 补 scheduling/ai 分类；`css-stub.mjs` stub atlaskit DnD 子路径（CJS 无 exports 目录导入，Node ESM 无法解析）
- 重新生成：`flux-types/schema.d.ts` 93 → **114 个 type**（Gantt/Kanban/Calendar/BarcodeInput + 14 个 Ai\*Schema），README "共 114 个 type" 同步
- **代码侧微修**：`scheduling-renderer-definitions.ts` kanban 补注册 `{ key: 'wipStrict', kind: 'prop' }`（运行时已消费但未注册 → 生成类型缺口，终审发现）；scheduling 包 typecheck + build + 832 tests 全绿

### 独立子 agent 审查（3 轮，达成共识）

- R1：scheduling 审查 4 文档（gantt 通过；kanban/calendar/barcode 各 1-4 处问题，其中 3 处"文档宣称行为但代码零消费"最严重）+ AI 审查（仅 1 处 region 归类措辞）
- R2：修正验证通过 + 3 条一致性建议（calendar statusPath 未接线标注、barcode 简介"异步校验"矛盾、ai-welcome footer value-or-region）
- R3 终审：README 两处 "异步校验" → "静态校验" 矛盾（修正）+ wipStrict 注册缺口（代码侧修复）→ 独立确认 **【达成共识】**

- 收尾：`pnpm --filter @nop-chaos/flux-renderers-scheduling typecheck && build && test` 全绿（74 files/832 tests）；flux-types 重新生成
- 未跑仓库级全量（仅脚本 + 1 行注册变更，scheduling 包验证已覆盖受影响范围）

## C3.1 form-advanced 复合输入族逐组件审计执行：plan-2026-08-03-0921-1 四 Phase 全绿（combo/input-table/transfer/picker 4 组件 18 维 + P0/P1/P2 自动修复 + 宿主场景 6/6 + closure 前置）

执行 `docs/plans/2026-08-03-0921-1-c3-1-form-advanced-composite-input-family-audit.md`（4 组件 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**form-advanced 复合输入族收官（C3.1 执行完成，roadmap 标 done 待独立 closure-audit pass 后收口，与 C2.x 先例一致）**。

### Phase 1 — 4 张审计卡（`docs/audits/per-component/{combo,input-table,transfer,picker}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3 类（P1-1 removeWhen 零行为×1、P1-1 onSelectAll 事件漂移×1、P1-1 picker CRUD 状态路径跨实例共享×1、P1-2 readOnly/disabled 不传播×2（宿主实证，共性））、P2 ×3（transfer 硬编码 aria-label + checkAllLabel 零行为、design.md 漂移 ×2）、P3 ×7**。
- 重点维度：dim 1（**P1-1 input-table removeWhen 声明于 composite-schemas.ts:135 但定义未注册 → test-first 实证 schema 声明即 props 解析崩溃**（`${value.locked !== "true"}` 在父 scope 求值抛错，行渲染 0——比 combo/array-field 实现缺口更严重）；transfer onSelectAll 三方声明（schema+design）定义零注册零派发）；dim 6/12（**P1-1 picker CRUD `$_picker.<id>.*` 状态路径按 templateNode.id 共享——同一模板节点重复实例（combo item/CRUD 行）互相覆盖选择与数据，bug 73 模式：单测全绿（单实例）但多实例共享状态**；dim 3（transfer 受控 echo 三态、picker 选中值回写 + G1 防误清空、combo staged owner）；dim 11（**P2-1 picker labelResolveAction 失败后 requestKey 永不复位 → 同值永不再重试**）；dim 9（P2-1 transfer 硬编码 "Select all" + checkAllLabel 零行为）；dim 17（combo/input-table design.md §10 BEM marker 漂移 + combo §4 `multiple` phantom）。
- 工具脚本：`check:audit-missing-renderer-markers`/`check:audit-styling-suspects`/`check:audit-runtime-raw-schema-reads`/`check:audit-fieldframe-bypasses` 4 族文件 0 命中；`check:audit-performance-suspects` 仅 picker:220（requestKey ref，P2-1 修复后为有界去重）；`check:audit-react19-optimization-candidates` 命中 ComboItem/InputTableRow memo（b32 C12 验证的复合字段族既有设计）。

### Phase 2 — P0/P1/P2 自动修复（test-first，form-advanced 929 → 939 tests）

- **[P1-1 input-table removeWhen]**：fields 补 `{key:'removeWhen',kind:'prop',lazyEval:true,params:['record','index','value']}` + InputTableRow `removeBlocked` 门控（复用 remove-when-gating.ts）+ removeItem handle 拦截。test-first：input-table-remove-when.test.tsx 3 用例先红后绿（首轮复现 props 解析崩溃 → 修复后绿）。
- **[P1-1 transfer onSelectAll]**：fields 注册 `onSelectAll` + toggleAllCandidates 派发（全选分支，反选不派发）。test-first：transfer-on-select-all.test.tsx 2 用例先红后绿。
- **[P1-1 picker `$_picker.<id>` 跨实例共享（bug 73 模式）]**：状态路径键改按实例唯一 `props.meta.cid`（`$_picker.${cid}.selection/rows` + crud id `${cid}-picker-crud`）——combo item / input-table 行 / CRUD 行重复模板各自的弹层选择互不覆盖。test-first：picker-crud-row-isolation.test.tsx 2 用例先红后绿（双弹层同时 open：行 0 confirm 不被行 1 open 种子覆盖——**数据丢失路径**：旧代码行 0 pending 选择被行 1 seed 清空）。
- **[P1-2 readOnly/disabled 不传播到项字段（共性 → CX-8）]**：combo/input-table 的 readOnly/disabled 仅锁 chrome，item/单元格字段仍可编辑写回（**宿主场景 host-itable-disabled 实证**；根因 = 复合字段族 createItemScope 仅发布 scope readOnly 路径、子字段 presentation 只看自身 schema props）→ ComboItem/InputTableRow 以 `FormLayoutContext.Provider staticReadOnly` 经既有 form-layout 机制传播（combo-renderer.tsx:117-129,167-173 + input-table-row.tsx:137-144,191-201）。test-first：composite-readonly-propagation.test.tsx 4 用例先红后绿。**CX-8 事后回写 roadmap（planned，§7b；array-field/object-field/detail-field 同型缺口由 C3.2/C3.3 按同机制修复）**。
- **[P2-1 transfer 硬编码 + checkAllLabel 零行为]**：toggle-all aria-label = `checkAllLabel ?? t('flux.transfer.selectAll')`；flux-i18n 新增 `flux.transfer.selectAll` en/zh。test-first：transfer-checkall-i18n.test.tsx 3 用例先红后绿。
- **[P2-1 picker labelResolve 失败重试]**：失败/取消分支清除 `labelResolveRequestedRef` → 同值可重试（成功分支由缓存兜底不重复派发）。test-first：picker-label-resolve-retry.test.tsx 先红后绿（attempts 1→2→3 断言：失败→u2 成功→u1 重试成功）。
- **[P2 文档]**：combo design.md §4/§5（移除 phantom `multiple`）+ §10（`nop-combo__item` → `data-slot="combo-item"`）；input-table design.md §4/§8（removeWhen）+ §10（`nop-input-table__row` → `data-slot="input-table-row"` + `data-row-index`）；transfer design.md §2/§4（onSelectAll 已实现）。
- **[共性裁决]**：P1-2 根因在复合字段族项布局传播模式（combo/input-table 2 组件 + array-field/object-field 同型）→ **CX-8 事后回写（planned，§7b）**；P1-1×3 根因单点（input-table/transfer/picker 各自），卡内 fixed 不单独插 CX-n；P3 ×7（嵌套 data-slot=field-control、aria-multiselectable 恒 true、picker 搜索 input 无 aria-label、searchable 无 valueType boolean 等）卡内记录归 CR。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck && build && lint && test` 全绿（**939 tests**）；`check:i18n-keys`/`check:schema-prop-coverage` 绿。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c3-1-host-surfaces.spec.ts` 6/6 全绿 + lab 页 +7 场景）

- **host-combo-submit（bug 73 模式）pass**：form 内 combo 2 行预置 + 行 0 就地编辑 + 新增行填充 → Submit → valuesPath 回显 `Combo: [{"name":"AliceX","phone":"P-100"},{"name":"Bob","phone":"P-200"},{"name":"Carol","phone":"P-300"}]`——行 scope 隔离 + 提交值形状正确（单测绿 + 真机成立）。
- **host-itable-submit（bug 73 模式）pass**：input-table 2 行编辑 + 新增行 → Submit → `Table: [{"sku":"A1","amount":42},{"sku":"B2","amount":5},{"sku":"C9","amount":7}]`。
- **host-itable-disabled（P1-2 真机实证 + 修复证明）pass**：readOnly 表单元格 `readonly` 属性、操作列/添加行隐藏、Submit 回显未变化 `RO: [{"sku":"FIXED-1","amount":9},{"sku":"FIXED-2","amount":11}]`——**修复前单元格无 readonly 且可编辑写回（缺陷实证）**。
- **host-transfer-echo pass**：外部 setValue → 右栏/回显同步（T:["admin"] → T:["viewer"]），无 stale。
- **host-transfer-sa（P1-1 真机证明）pass**：toggle-all → `SA:true`（onSelectAll 派发）。
- **host-picker-row（bug 73 模式专项）pass**：CRUD 模式 picker 双行重复模板——行 0 选择 Alpha confirm → `PR:[{"name":"R0","owner":"a0"},{"name":"R1"}]`；行 1 选择 Beta confirm → `PR:[{"name":"R0","owner":"a0"},{"name":"R1","owner":"b1"}]`——逐行写回隔离（modal dialog 阻塞同开场景，跨实例 clobber 确定性回归由单测 picker-crud-row-isolation 覆盖，宿主验证逐行值隔离）。
- 回归：w4c-composite-form-family **8/8**；component-lab 全量 **187 passed / 1 skipped（pre-existing simple-form skip）**；surface-form-input/form-ajax-includescope/dialog-real-schema/dialog-dropdown-row-edit/component-handles/composite-editor-handles/boolean-control-value-contract **15/15**；smoke 49/49（coverage-manifest +4 新条目 combo/input-table/transfer/picker）。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：combo-lab-page +1（嵌套提交）、input-table-lab-page +2（表格提交 + readOnly 提交）、transfer-lab-page +2（受控 echo + onSelectAll）、picker-lab-page +1（CRUD 行隔离，env fetcher 注入）。

### Phase 4 — closure 前置

- 4 卡全部 `closed`（P0/P1 清零；P1-1×3 + P1-2 + P2-1×2 fixed；P3 ×7 卡内记录归 CR）；全卡 18 维表结论与最终代码一致。
- 全量验证（closure gates 前置证据）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test --force` **58/58 tasks 全绿（form-advanced 939）**。
- 文档：4 份 design.md 同步（combo/input-table marker + removeWhen、transfer onSelectAll、combo §4）；coverage-manifest +4；**CX-8 插入 roadmap（planned，§7b 事后回写，引用本 plan 为执行证据；array-field/object-field/detail-field 同型由 C3.2/C3.3 消费）**；依赖图注释同步（CX-8 依赖 C3.1）。
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（6 个新测试文件 13 用例全先红后绿；宿主 spec 2 轮 selector/shape 修正后 6/6）。
- **bug 73 模式专项检查结论：pass**（host-combo-submit/host-itable-submit/host-picker-row 三场景真机成立；过程中实证并修复 2 个"单测绿但真机/多实例失败"类缺陷：P1-2 readOnly 传播（宿主实证）、P1-1 picker 跨实例共享（多实例实证））。
- **Non-blocking follow-up（CR）**：P3 ×7（combo/input-table/picker 嵌套 data-slot=field-control、transfer aria-multiselectable 恒 true、picker 搜索 input aria-label、picker searchable valueType、统计全角括号等）；C3.2/C3.3 同型 readOnly 传播缺口（CX-8 机制）。

### C3.1 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（执行证据已交付：4 卡 closed + 6 新测试文件先红后绿 + c3-1 6/6 + w4c 8/8 + component-lab 187 + workspace typecheck/build/lint/test 全绿 + CX-8 回写）
- Verdict: **pass（approved）**——独立复核 live repo：4 卡 closed 且 18 维结论与最终代码一致（P1-1×3/P1-2/P2-1×2 fixed 均在代码可核验：combo/input-table FormLayoutContext.staticReadOnly、transfer onSelectAll 注册+派发+checkAllLabel i18n、picker cid 状态路径+labelResolve 重试）；输入族 line-ref 漂移修正（InputTableRow 提取至 input-table-row.tsx 后卡内引用同步）；plan Closure Gates closure-audit 项已由审计 session 勾选 + Audit Evidence 回填（39/39 unchecked 0，plan-check --strict pass）。
- 收口动作（由审计 session 完成）: ✅ Closure Gates closure-audit 项勾选 + ✅ plan `## Closure` Audit Evidence 回填 + ✅ **roadmap C3.1 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.x 先例一致）+ ✅ CX-8 按 §7b 标 done

## component-audit mission-driver：C3.x 计划起草（3 份 draft → active）

- 自 roadmap 剩余 `todo` 项选取下一批 3 份 work item（均仅依赖 C0，C0 已 done）：**C3.1 form-advanced 复合输入族**（combo/input-table/transfer/picker）、**C3.2 form-advanced 组合字段族**（object-field/array-field/detail-field/detail-view/variant-field）、**C3.3 condition-builder**（复杂交互渲染器，追加 deep-audit 21-23 维）。
- 计划文件：`docs/plans/2026-08-03-0921-1-c3-1-form-advanced-composite-input-family-audit.md`、`2026-08-03-0921-2-c3-2-form-advanced-composite-field-family-audit.md`、`2026-08-03-0921-3-c3-3-condition-builder-audit.md`（顺序即执行顺序）。
- 每份经独立子 agent fresh session review（task `ses_03ac7d39cffeiJV5mJbjZQLtMb` / `ses_03ac7befdffeulMf2l6QZD6kCS` / `ses_03ac7ab35ffeJn3h3yyz1HkQUg`），均 `pass-with-minors`（零 Blocker/Major），Minor（行号校准/文件位置/引用名）已修正 → 三份均标 `active`。
- 前置 deferred 项核对：各 C2.x 计划 deferred 的 P2 backlog 归 CR、e2e pre-existing 归 C8.1/C9/CV、「机制落地后复验」归 CR——均依赖全部 C\* 完成，本轮不可重触发；无其它可重触发 deferred 项。
- 待执行：C3.1 → C3.2 → C3.3（并行独立，各自主 plan 执行 + closure audit + roadmap 标 `done`）。

## C2.5 mission-driver BUILD_VERIFY verification (plan-2026-08-03-0517-3)

- `pnpm typecheck` 31/31 ✓ · `pnpm build` 31/31 ✓ · `pnpm lint` 31/31 ✓ · `pnpm test` 58/58 tasks ✓（turbo 全量；受影响包 fresh 重跑：flux-renderers-form 85 files/**711 tests** 全绿、flux-i18n 2 files/26 tests 全绿）。
- 受影响 e2e：`tests/e2e/component-lab/c2-5-host-surfaces.spec.ts` 4/4 ✓（host-md-submit bug 73 模式 / host-md-xss sanitize / host-md-echo / host-md-disabled）+ `tests/e2e/w3d-markdown-editor.spec.ts` 2/2 ✓。
- 全量 e2e：**804 passed / 9 failed / 43 skipped**。9 失败 = C0 pre-existing 8 项（ai-chat timestamp、ai-rich-text-sender ×5、calendar-demo nav、diff-perf 200ms，归属 C8.1/C9/CV，与 C2.1/C1.3 基线一致，C2.2 已修复 input-suggest 不在列）+ **w3d-editor bold**（已知间歇性 flake，isolated 复跑 3/3 ✓，与本次改动无关）。
- 未达仓库级 full-green（pre-existing 8 项 C0 裁定），不声明；本 plan 改动全量验证通过（form 711 tests + 宿主 4/4 + w3d 回归 2/2），与 C2.5 closure gates 证据一致。

## C2.5 form markdown-editor 逐组件审计执行：plan-2026-08-03-0517-3 四 Phase 全绿（18 维审计卡 + P2 自动修复 + 宿主场景 4/4 + closure）——**form 族 C2.x 全部收官**

执行 `docs/plans/2026-08-03-0517-3-c2-5-markdown-editor-audit.md`（单组件 18 维逐组件审计 + P2 自动修复 + 真实浏览器组合宿主场景 + closure）。**form 族 C2.x 至此全部完成（C2.1..C2.5 done 前置全部满足，roadmap C2.5 待独立 closure-audit pass 后标 done）**；form 包审计剩余为 C3.x（form-advanced 复合族）。

### Phase 1 — 1 张审计卡（`docs/audits/per-component/markdown-editor.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×0、P2 ×3（P2-1 i18n 硬编码 14 处、P2-2 design.md `mode`→`viewMode` 漂移、P2-3 四态/handle 测试加固）、P3 ×4（预览 aria-live、rows prop、example.json、大文档预览性能阈值）**。
- 重点维度：dim 18 **安全红线专项 pass**——预览经 `props.helpers.render` 组合 content 包 `markdown` 节点（不传 allowHtml）→ markdown renderer 默认 allowHtml=false（`markdown.tsx:90-96`）→ react-markdown 转义原始 HTML、schema 无 allowHtml prop 无法绕过门禁；与 C6.1 共享 ReactMarkdown+remark-gfm 依赖链，**无共性缺陷（不插 CX-n）**，交接 C6.1 核对 allowHtml=true DOMPurify 路径；dim 3/4 值所有权三态与表单参与全路径经 useFormFieldController（textarea 同源宿主契约，仅增量核对）；dim 5 DOM 契约全 pass（根 `nop-markdown-editor` + data-slot 编辑/预览/fallback + FieldFrame data-field-\*）；dim 17 design.md §4 `mode` 漂移（schemas.ts:387-390 已记录改名原因）。
- 工具脚本：`check:audit-missing-renderer-markers`/`check:audit-styling-suspects`/`check:audit-performance-suspects`/`check:audit-async-failure-paths`/`check:audit-react19-optimization-candidates`/`check:audit-runtime-raw-schema-reads`/`check:audit-fieldframe-bypasses` markdown-editor 0 命中。

### Phase 2 — P2 自动修复（test-first，form 包 705 → 711 tests）

- **[P2-1] i18n 硬编码**：工具栏 12 标签 + toolbar aria-label 'Markdown formatting' + 默认 placeholder 'Enter markdown…' → flux-i18n 新增 `flux.markdown.*` 14 keys（zh/en）；TOOLBAR_GROUPS 改 `titleKey` 渲染期 `useFluxTranslation().t()` 解析（命名空间相对 key `'markdown.bold'`；t() 不模块作用域调用——C2.3 教训）。test-first：markdown-editor-i18n.test.tsx 5 用例（zh-CN 工具栏/aria/placeholder + schema placeholder 覆盖 + en-US 契约，先红后绿 3 red → 全绿）。
- **[P2-2] 文档漂移**：design.md §4/§5 `mode` → `viewMode` + `toolbar` 字段补齐（§4 注释保留改名原因）。
- **[P2-3] 测试加固**：markdown-editor.test.tsx +6 用例（disabled/readOnly 阻断 + readOnly 不写值 + required 空值阻断提交 + component:clear/component:focus handles）。
- **[共性裁决]**：无共性缺陷（sanitize 链核对 pass，根因非公共层）→ 不插 CX-n；P3 ×4 卡内记录归 CR；C6.1 交接记录于卡 dim 18。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form typecheck && build && lint && test` 全绿（**711 tests**，85 files）；`check:i18n-keys` 绿；flux-i18n 2 files/26 tests 绿。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c2-5-host-surfaces.spec.ts` 4/4 全绿）

- **host-md-submit（bug 73 模式专项）pass**：form 内 markdown-editor 真机 typing → 预览 h1 跟随 → Submit → valuesPath 回显 "MD-SUBMIT:# Host markdown"——单测绿 + 真机行为成立。
- **host-md-xss（sanitize 门禁真机）pass**：script 标签/img onerror/javascript: 链接载荷 → 预览无 script/img 元素、无 `a[href^="javascript:"]`（react-markdown 默认 urlTransform 剥除危险协议留空 href——**真机核对后按真实行为校准断言**，初版 `a[href]` 计数 0 过严）、原始 HTML 以转义文本呈现、`window.__mdXssExecuted` 未定义（无执行）。
- **host-md-echo pass**：外部 setValue → textarea + 预览 h1 + 页面 echo 全部更新（无 stale/循环）。
- **host-md-disabled pass**：disabled/readOnly 真机阻断 textarea + 隐藏工具栏 + 预览仍渲染不崩溃。
- 回归：w3d-markdown-editor 2/2、component-lab 全量 **178 passed / 1 skipped（pre-existing simple-form skip）**；无本族相关失败。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：markdown-editor-lab-page +4（复合提交/XSS sanitize/受控 echo/disabled+readOnly）。

### Phase 4 — closure

- 审计卡 `closed`（P0/P1 清零；P2 ×3 fixed；P3 ×4 卡内记录归 CR）；18 维表结论与最终代码一致。
- 全量验证（closure gates）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` **58/58 tasks 全绿（--force 全量重跑，form 711）**。
- e2e 基线：本族新增 4/4 全绿；w3d-markdown-editor 2/2；component-lab 178 passed/1 skipped；无本族相关失败。
- 文档：`docs/components/markdown-editor/design.md` §4/§5（viewMode/toolbar 同步）；quick-reference 无组件级词条（n-a，与 form 卡同裁定）。

### C2.5 收口证据

- 审计卡：`docs/audits/per-component/markdown-editor.md`（closed，P0/P1 清零）
- 回归测试：markdown-editor-i18n.test.tsx（+5）、markdown-editor.test.tsx（+6 四态 + handles）——均先红后绿（in-session，工作区未提交属设计内）
- e2e 宿主场景：`tests/e2e/component-lab/c2-5-host-surfaces.spec.ts`（+4，含 bug 73 模式专项 host-md-submit + XSS sanitize host-md-xss）
- 代码：`markdown-editor-renderer.tsx`（useFluxTranslation + titleKey 渲染期 t() + toolbar aria-label + 默认 placeholder）、flux-i18n（flux.markdown.\* 14 keys zh/en）、lab 页 +4 场景
- 文档：design.md §4/§5 同步
- CX-n：无插入（sanitize 链无共性缺陷，决策留痕）；C6.1 交接：content markdown allowHtml=true DOMPurify 路径待 C6.1 审计核对
- **Non-blocking follow-up（CR）**：P3 ×4（预览 aria-live、rows 固定 8 无 prop、design.md example.json 缺失、大文档实时预览性能阈值）

### C2.5 closure-audit（独立子 agent fresh session 已 pass，2026-08-03）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（2026-08-03，与执行 session 隔离）
- 独立复核证据：form 包 85 files/711 tests 全绿（现场重跑）、c2-5-host-surfaces 4/4 + w3d-markdown-editor 2/2 全绿（现场重跑）、workspace typecheck/build/lint 31/31 + test 58/58 全绿（turbo 验证）、`check:i18n-keys` pass；审计卡 18 维表与 live 代码逐项核对一致，无 hollow 实现；deferred 项分类诚实（P3×4 optimization candidate 归 CR、C6.1 交接为跨 plan 显式交接、e2e pre-existing 8 项 C0 裁定）；design.md §4/§5 已同步
- 收口动作（已由审计 session 完成）：Closure Gates closure-audit 项勾选 + plan `## Closure` Audit Evidence 回填 + **roadmap C2.5 行标 `done`**（roadmap 规则「closure audit 通过 → done，不得提前」，C2.3/C2.4 先例一致）——**form 族 C2.x 全部收官，roadmap 侧完成**

## C2.4 form 日期族逐组件审计执行：plan-2026-08-03-0517-2 四 Phase 全绿（input-date/input-datetime/input-time/date-range/input-month/input-quarter/input-year 7 组件 18 维 + 自动修复 + 宿主场景 + closure）

执行 `docs/plans/2026-08-03-0517-2-c2-4-form-date-family-audit.md`（7 组件 18 维逐组件审计 + P0/P1/P2 自动修复 + 真实浏览器组合宿主场景 + closure）。**form 族至此全部完成（C2.1..C2.4 done），仅剩 C2.5（markdown-editor）**。

### Phase 1 — 7 张审计卡（`docs/audits/per-component/{input-date,input-datetime,input-time,date-range,input-month,input-quarter,input-year}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×2 类（P1-1 input-datetime `timeFormat` 零行为、P1-2 period 族 `shortcuts` 零行为——均"注册/类型/design 三方声明 vs 渲染器零引用"契约漂移）、P2 ×6、P3 ×8**。
- 7 组件根 marker 全齐（`nop-input-date`/`nop-input-datetime`/`nop-input-time`/`nop-date-range`/`nop-input-month`/`nop-input-quarter`/`nop-input-year`）——与 C2.2/C2.3 的 marker 缺失模式不同，维度 5 全 pass。
- 重点维度：dim 1/3/4（值形状契约收敛：单值=valueFormat 字符串、date-range=delimiter 连接串 + requiredRange 两端校验、period=`YYYY-MM`/`YYYY-Qq`/`YYYY`；min/max 写路径 clamp 全覆盖）；dim 9（**P2 共享：日历网格 locale 未随 flux 语言**——react-day-picker 默认 en-US，zh-CN 运行时英文网格，根因在共享 Calendar 使用点 → CX-7；硬编码文案 Clear/Hour/Minute/Select range/Start time/End time 等）；dim 8（P3：onFocus/onBlur 挂 PopoverContent 非 trigger）；dim 17（**P1-1/P1-2 文档对照同源**：design.md 声明 timeFormat/shortcuts 但实现零行为）。
- 工具脚本：`check:audit-missing-renderer-markers`/`check:audit-styling-suspects`/`check:audit-async-failure-paths` form 包 0 命中；`check:audit-react19-optimization-candidates` 仅 playground demo 页宿主 useMemo（非 renderer）。

### Phase 2 — P0/P1/P2 自动修复（test-first，form 包 677 → 700 tests）

- **[P1-1] input-datetime `timeFormat` 零行为**：实现为弹层时间粒度驱动——`timeFormat` 含 `ss` 渲染秒输入（`date-field-control.tsx` withSeconds），选日/时/分编辑按粒度保留或清零秒；`input-datetime-renderer.tsx` 传 `timeFormat`（默认 `HH:mm`）；存储始终由 valueFormat 决定。test-first：input-datetime.test.tsx +5 用例（秒输入渲染/默认无秒输入/秒提交/选日保留秒/默认清零，先红后绿）。
- **[P1-2] period 族 `shortcuts` 零行为**：实现 inline shortcut chips（single 取 `start` 经 commitSingle clamp；range 双端经 `normalizePeriodRange` 归一，`end` 必填缺失过滤）；`period-renderers.tsx` + `period-shortcut-${kind}` testid。test-first：input-period.test.tsx +3 用例（range 快捷项归一、single 快捷项 clamp、畸形条目过滤，先红后绿）。
- **[P2 批量]**：`flux-i18n` 新增 `flux.date.*` 14 keys zh/en（hour/minute/second/selectRange/start/end/startTime/endTime/rangeStartTime/rangeEndTime/rangeStart/rangeEnd/quarter/quarterYear）；共享控件与三 renderer 硬编码文案全部走 t()（`flux.common.clear` 复用）；**注意：useFluxTranslation 的 t() 为命名空间相对 key 语义（`'date.hour'` 非 `'flux.date.hour'`），本 session 实测踩坑并修正（barcode-input 的 `flux.*` 前缀用法为同型潜伏问题，记 CR 归集）**。
- **[共享 P2（CX-7）] 日历 locale**：`useFluxTranslation().i18n.language` → `react-day-picker/locale` enUS/zhCN 映射传入 Calendar（date-field-control + date-range-renderer）；flux-renderers-form 新增 react-day-picker 直接依赖声明。test-first：date-calendar-locale.test.tsx 4 用例（zh-CN 周一起始 `一二三四五六日`/en-US `SuMoTuWeThFrSa`/date-range zh/period 无网格，先红后绿）。
- **[P2 测试加固]**：date-i18n.test.tsx 11 用例（zh/en 文案契约 + 四态 disabled/readOnly 断言，先红后绿）。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form typecheck && build && lint && test` 全绿（**700 tests**）；`check:i18n-keys`/`check:schema-prop-coverage` 绿。
- **[共性裁决]**：日历 locale 根因在共享 Calendar 使用点（3 组件受影响）→ **CX-7 事后回写 roadmap（planned，§7b，引用本 plan）**；P1-1/P1-2 根因在共享 date-field-control/period-renderers 工厂但为单点行为修复，卡内标 fixed 不单独插 CX-n；P3 项（focus/blur 挂 PopoverContent、valueFormat token 无 schema 校验、sr-only data-range-kind 钩子、外部越界值只读展示、原生 placeholder 不渲染、period 逐键即时提交）卡内记录归 CR。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c2-4-host-surfaces.spec.ts` 4/4 全绿）

- **host-family-submit（bug 73 模式专项）pass**：日期族 7 控件同一 form——input-date 日历选日 15、input-datetime 选日 20（保留 14:30）、input-time 原生输入 09:15、date-range 快捷项、input-month 2024-11、input-quarter year 改 2025→2025-Q3、input-year 2031 → submit → valuesPath 回显 "Date: 2024-06-15 | 2024-06-20 14:30 | 09:15 | 2024-06-03,2024-06-10 | 2024-11 | 2025-Q3 | 2031"——单测绿 + 真机行为成立（**date-trigger testid 在 input-date/input-datetime 共享，宿主按 marker 类 scope 处理**）。
- **host-family-markers pass**：7 个根 type marker 真机断言。
- **host-range-calendar（bug 73 风险面：react-day-picker 真机交互）pass**：日历选日 18 → 范围 end 延展 → range-display "2024-06-01 , 2024-06-18"（D7 immediate-commit：display==committed；真机确认 react-day-picker range 模式在完整范围上点击 end 之后日期为延展方向，宿主断言按此校准）。
- **host-period-range pass**：period 范围复合提交（month range + quarter range + year）→ "Period: 2024-02,2024-05 | 2024-Q1,2025-Q4 | 2031"。
- 回归：w2b-date-family 6/6、component-lab 全量 **180 passed / 1 skipped**、simple-form/complex-form/dialog-\*/crud/form-ajax 7/7；无本族相关失败。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。
- lab 页新场景：input-date-lab-page +1（7 控件复合提交）、input-month-lab-page +1（period 范围复合提交）。

### Phase 4 — closure

- 7 卡全部 `closed`（P0/P1 清零；P2 fixed；P3 卡内记录归 CR）；全卡 18 维表结论与最终代码一致。
- 全量验证（closure gates）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 tasks 绿（form 700）。
- e2e 基线：本族新增 4/4 全绿；component-lab 180 passed/1 skipped；无本族相关失败。
- 文档：`docs/components/{input-datetime,input-time,input-date,date-range,input-month,input-quarter,input-year}/design.md` 同步（timeFormat/shortcuts/displayFormat 语义收敛、日历 locale 行为）。

### C2.4 收口证据

- 审计卡：`docs/audits/per-component/{input-date,input-datetime,input-time,date-range,input-month,input-quarter,input-year}.md`（closed，P0/P1 清零）
- 回归测试：input-datetime.test.tsx（+5 timeFormat）、input-period.test.tsx（+3 shortcuts）、date-calendar-locale.test.tsx（+4）、date-i18n.test.tsx（+11 文案+四态）——均先红后绿
- e2e 宿主场景：`tests/e2e/component-lab/c2-4-host-surfaces.spec.ts`（+4，含 bug 73 模式专项 host-family-submit）
- 代码：`date/date-field-control.tsx`（timeFormat 秒粒度 + Calendar locale + i18n labels）、`input-datetime-renderer.tsx`（timeFormat）、`date-range-renderer.tsx`（Calendar locale + i18n labels）、`input-time-renderer.tsx`（i18n clear）、`period-renderers.tsx`（shortcuts + i18n labels）、`flux-renderers-form/package.json`（react-day-picker 依赖声明）、flux-i18n（flux.date.\* 14 keys）、lab 页 +2 场景
- 文档：7 份 design.md §4/§4.1/§7.1 同步（timeFormat/shortcuts/displayFormat 语义、locale 行为）；quick-reference 无组件级词条（n-a，与 form 卡同裁定）
- CX-n：roadmap **CX-7 插入（planned，§7b 事后回写，引用本 plan 为执行证据）**；依赖图附注「依赖以表 Dependencies 列为准」
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（timeFormat 5 用例、shortcuts 3、calendar-locale 4、date-i18n 11——首轮 16 red → 700 全绿）
- **Non-blocking follow-up（CR）**：P3 项（date-field-control 焦点/失焦挂 PopoverContent、valueFormat 非法 token 无 schema 校验、date-range sr-only data-range-kind 钩子、外部越界值只读展示、input-time placeholder 原生不渲染、period 逐键即时提交）；barcode-input useFluxTranslation t() `flux.*` 前缀同型潜伏问题

### C2.4 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: mission-driver CLOSURE_VERIFY 阶段 fresh session（2026-08-03 执行）
- Verdict: **pass**（approved）
- 复核范围: plan 文本一致性（Phase 1-4 `completed` + 全部 `[x]`、Plan Status completed、Closure Gates 11/11——closure-audit 项由本审计 session 勾选）、7 卡 18 维表 + 证据抽查、代码修复落点（timeFormat 秒粒度/shortcuts/CX-7 locale/i18n labels）、回归测试断言正确性、form 包 700/700 与 c2-4 e2e 4/4 独立重跑、deferred 诚实性（P3 卡内记录、无 P0/P1 降级、CX-7 done 证据成立）、owner-doc 同步
- 独立重跑: `pnpm --filter @nop-chaos/flux-renderers-form test` 84 files/700 tests 绿；`npx playwright test tests/e2e/component-lab/c2-4-host-surfaces.spec.ts` 4/4 绿（bug 73 模式 host-family-submit 在内）
- 收口动作（本审计 session 完成）: Closure Gates closure-audit 项勾选 + plan `## Closure` Audit Evidence 回填；CX-7 按 §7b 标 `done`

## C2.3 form 选择控件族逐组件审计执行：plan-2026-08-03-0517-1 四 Phase 全绿（select/checkbox/checkbox-group/radio-group/switch/button-group-select 6 组件 18 维 + 自动修复 + 宿主场景 + closure）

执行 `docs/plans/2026-08-03-0517-1-c2-3-form-choice-control-family-audit.md`（6 组件 18 维逐组件审计 + P0/P1/P2 自动修复 + 真实浏览器组合宿主场景 + closure）。**form 族至此仅剩 C2.4（日期族）/C2.5（markdown-editor）**。本次执行跨两次 session（前次 session 产出审计卡 + 部分修复，本次收尾：剩余 P2 修复、宿主场景、closure）。

### Phase 1 — 6 张审计卡（`docs/audits/per-component/{select,checkbox,checkbox-group,radio-group,switch,button-group-select}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3 类（P1-A native value echo 共性 ×3 组件、P1-B 根 marker 共性 ×4 组件、P1-C form Enter 排除 ×4 组件）、P2 ×10、P3 ×7**。
- roadmap 一致性核对：CX-1..CX-4（事后回写 planned）父 plan 均 completed + closure audit pass → 按 §7b 标 `done`；**combobox-item data-value 已知缺陷样本 live 复验 pass**（`select-combobox-lists.tsx:67` 已输出 + 契约测试 :293 绿 → 裁定 fixed，回写 roadmap 基线节）。
- 重点维度：dim 3（**P1-A：select/radio-group/button-group-select single 用 stringAdapter 把表单值字符串化 vs `Object.is(option.value, value)` 匹配契约 → 数值/布尔选项永无选中态**——实测 probe 复现，共性根因 → CX-6）；dim 5（**P1-B：checkbox/switch/radio-group/checkbox-group 根 marker 缺失**（design.md §10 承诺 vs 仅 `-wrapper`），C1.3 badge/C2.2 input-text 同模式 → CX-5；P2：combobox-item/mobile option data-value 仅 string）；dim 8（**P1-C：form Enter 提交排除清单未含 role=checkbox/switch/radio + 不检查 defaultPrevented**（C2.1 交棒项））；dim 9（P2：switch On/Off 与远程搜索失败文案硬编码）；dim 10/11（P2：dict 加载失败无错误展示，select/button-group-select 共用根因）；dim 17（**button-group-select 无 design.md**（2026-08-02 注册缺口）→ 裁定：注册契约以 DOM 契约测试为准 + 本 plan 补齐 design.md）。
- 工具脚本：`check:audit-missing-renderer-markers` 0 命中系脚本假阴性（C1.3/C2.2 已证，CG 升级）。

### Phase 2 — P0/P1/P2 自动修复（test-first，form 包 669 → 677 tests）

- **[P1-A/CX-6] single 模式 native value echo**：新增 `choiceSingleAdapter` 值保真适配器（input-choice-renderers.tsx:70-89；select/radio-group/bgs 三处替换 stringAdapter）；radio-group `selectedValue` 仅对 null/undefined 归一为 `''` 保持 Base UI 受控（**防 uncontrolled→controlled 翻转——simple-form.spec.ts 回归真机发现 React 警告并修复**）；P3-1 类型断言随修。test-first：choice-native-value-echo.test.tsx（7 用例：0/true echo、数值写回、bgs data-selected、受控翻转回归，先红后绿）。**CX-6 事后回写 roadmap（planned，引用本 plan）**。
- **[P1-B/CX-5] 根 type marker 补齐**：checkbox/switch/radio-group/checkbox-group 根同时输出 `nop-<type>` + `nop-<type>-wrapper`（input-choice-renderers.tsx:582/:638/:701、checkbox-group-renderer.tsx:125-127）；DOM 契约 data-slot 不变。test-first：choice-markers-contract.test.tsx（5 用例先红后绿）。**CX-5 事后回写 roadmap（planned，引用本 plan）**。
- **[P1-C] form Enter 排除清单扩展**（C2.1 交棒项闭合）：`form.tsx` handleSectionKeyDown 增 `role === 'checkbox'/'switch'/'radio'` 排除 + `event.defaultPrevented` 兜底（:650-674）。test-first：form-shell-enhancements.test.tsx +3 用例（checkbox/switch Enter 不提交 + defaultPrevented 先红后绿）；radio 侧 Base UI Enter preventDefault 双动作风险同兜底。
- **[P2 批量]**：noMatchText 入 select fields；checkbox/switch fields 补 option/trueValue/falseValue；ButtonGroupSelectSchema 补 `dict`；combobox-item/mobile option `data-value={String(option.value)}` 全 primitive；switch On/Off → `flux.form.switchOn/Off` keys；远程搜索失败文案 → `flux.form.searchFailed` + `searchFailureMessage()` 惰性 t()（**注意：t() 不得在模块作用域调用**——模块加载期创建 zh-CN i18n 实例会使 test-support 的 en-US init 幂等失效，本 session 实测踩坑并改惰性调用）；dict 失败 → `DictOptionsState.errorMessage` + select/button-group-select error 槽合并。test-first：choice-error-i18n.test.tsx（4 用例先红）、choice-dict-error.test.tsx（2 用例先红）、select-remote-search-params.test.tsx（params URL 规范化契约）、select-dict-loading.test.tsx（+errorMessage 断言）。
- **[共性裁决]**：P1-A/P1-B 共性根因 → CX-6/CX-5 事后回写（§7b）；P1-C 根因在 form.tsx 单一排除清单（form 属本族前置已闭，直接修复）；P2-4 dict 根因在 use-dict-options.ts 共享 hook（2 组件受影响，单点 hook 修复，不入 CX-n）；P3 项（loading 时 clearable 可用、checkbox-group aria-errormessage、check:audit-missing-renderer-markers 假阴性升级）卡内记录归 CR/CG。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form typecheck && build && lint && test` 全绿（**677 tests**）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c2-3-host-surfaces.spec.ts` 5/5 全绿）

- **host-choice-submit（bug 73 模式专项 + host-bgs-submit）pass**：select/checkbox/switch/radio-group/checkbox-group/bgs(single+multiple) 同一 form 真实选择 → submit → valuesPath 回显 "Choice: us | yes | on | pro | stable,beta | main | admin,editor"——单测绿 + 真机行为成立，数组/布尔原生形状进 store。
- **host-combobox-value pass**：falsy option value（0/''）选择写回——value 0 真机显示 label 'Zero'（非 echo-fallback '0'）+ 写回 native-zero；'' 选项可选；随后 1 正常。
- **host-select-remote（bug 73 模式实例：单测绿但真机契约不同）pass**：搜索 'ap' → 远程选项加载；'fail' → error 槽 '搜索失败。'（zh-CN 默认 locale）+ 无 stale 选项；恢复查询正常。**过程中发现并修复宿主 fetcher 参数契约**：ajax executor 把 action params 规范化进请求 URL（`/api/choice-search?q=ap`）而非 `api.args.params`——单测只断言 dispatch 发生未断言参数位置，真机 fetcher 读错位置返回全量（bug 73 模式）；以 select-remote-search-params.test.tsx 冻结该契约。
- **host-controlled-echo pass**：外部 setValue → select echo 'L2'（无 stale）；switch aria-checked=true + label '开'（P2-2 i18n 真机证明）。
- **host-choice-enter（P1-C 修复真机证明）pass**：checkbox/switch 聚焦 + Enter → 800ms 后无 "Submitted:"（修复前会提交）；Enter 仅切换选中态（Base UI 原生键盘路径）；显式 Submit 按钮正常提交。
- 回归：component-lab 全量 **171 passed / 1 skipped**；c2-1/c2-2 宿主 + simple-form/complex-form/field-controls/form-input-enhancements **12/12**；**simple-form radio 用例回归发现 CX-6 引入的 Base UI uncontrolled→controlled 警告（console.error 门禁捕获）→ 修复 + 单测回归**。
- lab 页新场景：select-lab-page +4（family 复合提交、falsy 写回、远程搜索 env fetcher、受控 echo）、checkbox-lab-page +1（Enter no-submit，form submitAction 驱动）。

### Phase 4 — closure

- 6 卡全部 `closed`（P0/P1 清零；P2 fixed；P3 卡内记录归 CR）；全卡 18 维表结论与最终代码一致。
- 全量验证（closure gates）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 tasks 绿（form 677）。
- e2e 基线：本族新增 5/5 全绿；component-lab 171 passed/1 skipped；无本族相关失败。仓库级 e2e 剩余 pre-existing 8 项（C0 裁定，ai/scheduling/content 包，归属 C8.1/C9/CV）未达 full-green，不声明。

### C2.3 收口证据

- 审计卡：`docs/audits/per-component/{select,checkbox,checkbox-group,radio-group,switch,button-group-select}.md`（closed）
- 回归测试：`choice-native-value-echo.test.tsx`（+7）、`choice-markers-contract.test.tsx`（+5）、`choice-error-i18n.test.tsx`（+4）、`choice-dict-error.test.tsx`（+2）、`select-remote-search-params.test.tsx`（+1）、`form-shell-enhancements.test.tsx`（+3 Enter 排除）、`select-dict-loading.test.tsx`（+errorMessage）
- e2e 宿主场景：`tests/e2e/component-lab/c2-3-host-surfaces.spec.ts`（+5）
- 代码：`input-choice-renderers.tsx`（choiceSingleAdapter/selectedValue 归一/marker/switch i18n/dict error 合并）、`use-select-remote-search.ts`（searchFailureMessage）、`use-dict-options.ts`（errorMessage 惰性 t()）、`select-combobox-lists.tsx`/`select-mobile-renderer.tsx`（data-value）、`input.tsx`（noMatchText/checkbox/switch fields）、`form.tsx`（Enter 排除 role + defaultPrevented）、`checkbox-group-renderer.tsx`（marker）、`button-group-select-renderer.tsx`（choiceSingleAdapter/dict error）、`schemas.ts`（bgs dict）、flux-i18n（searchFailed/switchOn/switchOff 3 keys）
- 文档：`docs/components/button-group-select/design.md`（新建，维度 17 缺口关闭）、`docs/components/radio-group/design.md` §2（stringValueAdapter → choiceSingleAdapter 同步）；lab 页 +5 场景
- CX-n：roadmap **CX-5/CX-6 插入（planned，§7b 事后回写，引用本 plan 为执行证据）**；CX-1..CX-4 按 §7b 标 `done`（2026-08-03 C2.3 核对）
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（choice-native-value-echo 7 用例、markers 5、error-i18n 4、dict-error 2、Enter 排除 3；宿主 spec 远程搜索 2 轮 selector/fetcher 修正后 5/5）
- **Non-blocking follow-up（CR）**：P3 项（select loading 时 clearable 可用、checkbox-group aria-errormessage 未接、required=false 平台语义）登记 CR 归集

### C2.3 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: task `ses_03b60f3d0ffe0WzxJ5baVcQWAW`
- Verdict: `approved`（零 Blocker/Major；1 Minor 已处理：遗留 debug spec `tests/e2e/component-lab/c2-3-debug.spec.ts` 已删除，删除后 c2-3 5/5 复跑绿）
- 复核范围：plan 文本一致性（Phase 1-4 `completed` + 全部 `[x]`、Plan Status completed、Closure Gates 11 项）、6 卡 18 维表 + ~20 处 `文件:行` 证据抽查、代码修复落点（choiceSingleAdapter/selectedValue 归一/三 marker/form.tsx Enter 排除 + defaultPrevented/searchFailureMessage/dict errorMessage 惰性 t()/fields 补齐/data-value/schemas dict/i18n keys/design.md 补齐）、回归测试断言正确性（7 个测试文件）、form 包 677/677 与 workspace typecheck 31/31 与 c2-3 e2e 5/5 独立重跑、deferred 诚实性（P3 卡内记录、无 P0/P1 降级、CX-5/CX-6 planned 证据成立、C2.3 未提前标 done）、owner-doc 同步
- 收口后：roadmap C2.3 标 `done`；CX-5/CX-6 按 §7b 标 `done`（父 plan completed + closure audit pass）；plan `2026-08-03-0517-1` Plan Status `completed`；closure-audit 记录落 plan `## Closure` 节与 6 卡 Closure 节

## C2.2 form 文本输入族逐组件审计执行：plan-2026-08-03-0105-3 四 Phase 全绿（含 e2e pre-existing input-suggest 修复）

执行 `docs/plans/2026-08-03-0105-3-c2-2-form-text-input-family-audit.md`（input-text/input-password/input-email/input-number/textarea 5 组件 18 维审计 + input-suggest 子特性专项 + 自动修复 + 宿主场景 + closure）。**C0 裁定的 e2e pre-existing 失败 `input-suggest.spec.ts`（suggest 选择写回，successor: C2.2）在本 plan 内复现、归因并修复（3/3 全绿）**。

### Phase 1 — 5 张审计卡（`docs/audits/per-component/{input-text,input-password,input-email,input-number,textarea}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3（P1-1 marker 共性 ×4 组件、P1-2 suggest popover 焦点窃取、P1-3 combobox ARIA）、P2 ×8、P3 ×7**。
- 重点维度：dim 5（**P1-1：input-text/email/password/textarea 四组件根 marker 缺失** vs 各自 design.md §10 承诺——C1.3 badge P1-1 同模式，input-number 有 `nop-input-number` 而其余缺，共性根因 → CX-4 回写；`check:audit-missing-renderer-markers` 0 命中系脚本假阴性（C1.3 已记录））；dim 8/11/12（input-suggest 专项）；dim 3（值所有权三态全路径经 useFormFieldController，input-controlled-value/input-reset-resync 锚定）；dim 17（textarea design.md §11 phantom 引用 `input-choice-renderers.tsx` 实际为 `textarea-renderer.tsx`；input-number design.md §4 缺 inputMode；input-text §10 "suggest 不新增 DOM 节点" 漂移）。
- input-suggest 专项（并入 input-text 卡 dim 5/11/12）：**P1-2 e2e pre-existing 失败根因初查**——复现于本 session（1 failed），根因为 **Base UI Popover `FloatingFocusManager` 默认 `initialFocus:true` 打开时把焦点移入浮层（role=dialog）→ input blur → `BLUR_CLOSE_DELAY` 150ms 关闭浮层 → Base UI 把焦点还给 trigger span → 浮层不再打开**（bug 73 模式：单测绿真机挂——jsdom 下浮层焦点行为与真机不同，既有单测全绿）。另发现 demo 页 mock fetcher `ctx.scope.readOwn()` 读 data-source 自身 scope（q 恒空 → 建议恒全量 26 条）——demo 宿主缺陷，一并修复为 `readVisible()`。

### Phase 2 — P0/P1/P2 自动修复（test-first，form 包 649 → 655 tests）

- **[P1-2] suggest popover 焦点窃取修复（e2e pre-existing 修复）**：`Popover modal={false}` + `PopoverContent initialFocus={false}`（`input-suggest.tsx`）——打开时不窃取输入框焦点，blur-close 计时器不再误触发；test-first：e2e 写回用例先红（本 session 复现 1 failed）后绿（**3/3**）。
- **[P1-3] combobox ARIA 契约**：`useId()` listboxId + popup `role="listbox"` + 每条 item `id`；input `role="combobox"`/`aria-autocomplete="list"`/`aria-expanded`/`aria-controls`/`aria-haspopup="listbox"`/`aria-activedescendant`（随键盘高亮更新、关闭时清除）。test-first：input-suggest.test.tsx +2 条（先红后绿）。
- **[P1-1/CX-4] 四组件根 marker 补齐（共性根因，§7b 当前 plan 内修复 + 事后回写 CX-4 planned）**：`nop-input-text`/`nop-input-email`/`nop-input-password`（`input.tsx` 共享工厂 `nop-input-${inputType}`，bare/InputGroup 双路径）、`nop-textarea`（`textarea-renderer.tsx` bare/footer 双路径）。test-first：input-classname-contract.test.tsx +3 条（marker 裸路径/增强路径/全部 4 类，先红后绿）。
- **[P2-1] i18n 化**：clear/reveal/stepper aria-label + suggest 空态缺省文案走 `t()`（新增 `flux.common.increase/decrease/showPassword/hidePassword/noSuggestions` 5 keys，zh/en 同步）；`check:i18n-keys` 绿。
- **[P2-2] suggest min-length 门控**：`popoverOpen` 增 `!belowMinInputLength`——输入长度低于 `suggestMinInputLength` 时浮层关闭（design.md §4 "长度不足时不派发且关闭浮层" 契约兑现，stale 建议不再残留）。test-first：input-suggest.test.tsx +2 条（先红后绿）。
- **[P2-3 裁定]** demo 页 mock fetcher `readOwn()` → `readVisible()`（q 正确过滤建议；e2e 断言语义更真实）。textarea design.md §11 phantom 引用修正；input-number design.md §4 补 inputMode；4 份 design.md §10 marker 契约与 suggest 焦点/ARIA 契约同步。
- **[共性裁决]** 无跨包公共层根因；marker 缺失根因在文本输入族共享工厂（input.tsx `createInputRenderer` + textarea-renderer）→ 按 §7b 当前 plan 内修复 + **CX-4 事后回写（planned）**；P3 项（attemptedFetch 不重置、dispatch rejection 静默、嵌套重复 data-slot=field-control、validate 未注册 fields）卡内记录归 CR。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form typecheck && build && lint && test` 全绿（**655 tests**）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c2-2-host-surfaces.spec.ts` 5/5 全绿）

- **host-family-submit（bug 73 模式专项）pass**：5 控件（input-text/email/password/number/textarea）同一 form 真实输入（含多行 notes、数字 42）→ submit → valuesPath 回显 "Family: alice | alice@example.com | s3cret | 42 | hello\\nworld"——单测绿 + 真机行为成立。
- **host-family-markers（P1-1 修复真机证明）pass**：`nop-input-text/email/password`（input 元素）+ `.nop-input-number`（wrapper）+ `nop-textarea` 真机存在。
- **host-suggest-writeback（P1-2 修复真机证明）pass**：form 内 suggest 输入 'ap' → 浮层开（role=listbox）→ 点击 item → input 值 + live echo 写回 → 浮层关闭（修复前 item 永不可点击）。复现测试 = `tests/e2e/input-suggest.spec.ts` 写回用例（先红后绿）。
- **host-suggest-minlength（P2-2 修复真机证明）pass**：'ap' 浮层开 → 缩短为 'a'（min=2）→ 浮层关闭且 400ms 后无 stale 复活。
- **host-password-reveal pass**：reveal toggle type=password↔text + aria-pressed 切换、form 值全程不变（"Set: hunter2"）。
- lab 页新场景：input-text-lab-page（family 复合提交 + suggest 写回，per-scenario env fetcher 注入）、input-password-lab-page（reveal）。
- 回归：component-lab 全量 **165 passed / 1 skipped**；input-suggest 3/3；form-input-enhancements 3/3（stepper 断言改 `[data-slot="stepper-increase"]` 契约——zh-CN 默认 locale 下英文 aria-label 不再成立，i18n 化导致的既有 e2e 断言同步）；simple-form/complex-form/surface-form-input/form-ajax-includescope/dialog-edit-submit/c2-1-host-surfaces **9/9**。

### Phase 4 — closure

- 5 卡全部 `closed`（P0/P1 清零；P2 fixed；P3 卡内记录归 CR）。
- 全量验证（closure gates）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 tasks 绿（flux-renderers-form 655）。
- e2e 基线改善：**C0 pre-existing 9 失败中 1 项（input-suggest 写回）已修复**（3/3 绿），剩余 8 项归属 C8.1/C9/CV 不变；未达仓库级 full-green，不声明。

### C2.2 收口证据

- 审计卡：`docs/audits/per-component/{input-text,input-password,input-email,input-number,textarea}.md`（closed）
- 回归测试：`input-classname-contract.test.tsx`（+3 marker 断言）、`input-suggest.test.tsx`（+combobox ARIA 2 条 + min-length gate 2 条）
- e2e 宿主场景：`tests/e2e/component-lab/c2-2-host-surfaces.spec.ts`（+5）
- 代码：`input-suggest.tsx`（modal/initialFocus/role=listbox/comboboxAria/min-length 门控/i18n 空态）、`input.tsx`（marker + i18n + comboboxAria spread）、`textarea-renderer.tsx`、`input-number-renderer.tsx`、`flux-i18n`（5 keys）、`input-suggest-demo.tsx`（fetcher readVisible）
- 文档：5 份 design.md 同步；lab 页 +3 场景
- CX-n：roadmap **CX-4 插入（planned，§7b 事后回写，引用本 plan 为执行证据）**——文本输入族根 marker 缺失共性修复
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（marker/ARIA/min-length 单测 7 条先红后绿；e2e 写回用例本 session 复现 1 failed → 修复后 3/3；宿主 spec 2 轮 selector 修正后 5/5）
- **Non-blocking follow-up（CR）**：P3 项（input-suggest attemptedFetch 不随输入重置、dispatch rejection 静默记录、input-number 嵌套重复 `data-slot="field-control"`、input-number `validate` 未注册 fields 与 input-text 族定义契约差异、`check:audit-missing-renderer-markers` 脚本假阴性升级（CG））登记 CR 归集

### C2.2 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: task `ses_03bb1a612ffe2K2XqYNpitL1gv`
- Verdict: `approved`（零 Blocker/Major；2 Minor 文本项已修正：plan Closure Status Note 从 draft 陈旧文本改写、audit 证据回填 plan Closure 节与 5 卡 Closure 节）
- 复核范围：plan 文本一致性（Phase 1-4 `completed` + 43/43 `[x]`、Closure Gates 11 项）、5 卡 18 维表 + ~15 处 `文件:行` 证据抽查、代码修复落点（input.tsx marker/comboboxAria/i18n、input-suggest.tsx useId/initialFocus/modal/listbox/min-length gate、textarea/input-number i18n）、契约测试断言正确性、input-suggest e2e 3/3 + c2-2 e2e 5/5 独立重跑、form 包 655/655 与 workspace typecheck 31/31 与 check:i18n-keys 独立重跑、deferred 诚实性（P3 非阻断、CX-4 planned 证据成立）、owner-doc 同步（5 份 design.md）
- 收口后：roadmap C2.2 标 `done`；plan `2026-08-03-0105-3` Plan Status `completed`；closure-audit 记录落 plan `## Closure` 节与 5 卡 Closure 节

## C2.1 form shell 族逐组件审计执行：plan-2026-08-03-0105-2 四 Phase 全绿

执行 `docs/plans/2026-08-03-0105-2-c2-1-form-shell-family-audit.md`（form/fieldset + hidden-field 策略 2 组件 18 维审计 + 自动修复 + 宿主场景 + closure）。form 包 21 个注册组件中 shell 族完成（C2.2-C2.5 后续）。

### Phase 1 — 2 张审计卡（`docs/audits/per-component/{form,fieldset}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×1、P2 ×5、P3 ×4**（form 卡 P1-1/P2-1~4/P3-1~3；fieldset 卡 P1-1 联动/P2-1~2/P3-1）。
- 重点维度：dim 1（form fields 缺 gap、eventContracts 缺 loadAction、propContracts 缺 7 项；fieldset fields 缺 title/gap/bodyClassName/titleClassName——与 C1.1 page P2-1 同模式）；dim 4（提交 payload 形状 + 隐藏字段投影：form-runtime-submit-flow.ts:423-460）；dim 7（onSubmitSuccess/onSubmitError evaluationBindings prevResult:undefined 冗余但分支绑定覆盖、行为正确，P3）；dim 8（**P1-1：collapsible fieldset legend（role="button"）上 Enter 双触发**——form section keydown 排除清单无 role="button"，fieldset.tsx:50-76 legend 为 CollapsibleTrigger host）；dim 11（initAction/loadAction abort+竞态守卫齐备）；dim 13（布局 marker-only；legend trigger chrome 属 widget 自样式豁免）；dim 17（form design.md 缺 loadAction/autoLoad；form design.md:179 columnCount"子 fieldset 不消费"与 fieldset.tsx:27-31 实际消费矛盾）。
- hidden-field 策略并入 form 卡（维度 1/4/6/17）：`validateHiddenFieldPolicySchema`（hidden-field-policy-schema.ts:25-57）+ runtime 语义（form-runtime-field-ops.ts:416-449 notifyFieldHidden、form-runtime-submit-flow.ts:159-196 payload 投影）与 form-validation.md §873-900 契约一致；hidden-field-policy.test.tsx 7 条 focused 全绿。
- 工具脚本：`check:audit-missing-renderer-markers`/`check:audit-styling-suspects`/`check:audit-react19-optimization-candidates`/`check:audit-performance-suspects` form 包 0 命中。

### Phase 2 — P0/P1/P2 自动修复（test-first，form 包 645 → 649 tests）

- **[form/fieldset P1-1] Enter on collapsible legend 双触发**：`form.tsx` handleSectionKeyDown 增补 `target.getAttribute('role') === 'button'` 排除（与其他交互宿主同一语义）；test-first：form-shell-enhancements.test.tsx 新增回归（legend role=button 断言 + aria-expanded 展开 + 400ms await 后 submitCalls 为 0，先红后绿：初始 1 failed → 修复后绿）。真机证明见 Phase 3 用例 4。
- **[form P2-1/2/3] 定义契约补全**：fields 补 `gap`；eventContracts 补 `loadAction`；propContracts 补 gap/labelWidth/bodyClassName/actionsClassName/autoInit/autoLoad/submitScope 7 项。test-first：form-renderer-definition-contracts.test.ts 新增 2 条断言（先红后绿）。
- **[fieldset P2-1] fields 补全** title/gap/bodyClassName/titleClassName；fieldset-renderer-contracts.test.ts fields 基线同步更新（先红后绿）；gap 行为断言（body region gap-\* 类）+ schema-prop-coverage 130/130。
- **[form/fieldset P2-4/P2-2] 文档同步**：form/design.md §4/§5/§9 补 loadAction/autoLoad；§13.1.1 columnCount 修正（子 fieldset 可声明独立 columnCount）；fieldset/design.md §4 schema 表 + §5 分类补 4 字段。
- **[共性裁决]** 无共性根因：fields 缺失为逐定义独立缺口（根因非公共层），不入 CX-n；P3 项（loadAction 错误消息措辞、evaluationBindings 冗余、static valueType、fieldset 无 propContracts）卡内记录归 CR/不处理。
- 验证门禁：`pnpm --filter @nop-chaos/flux-renderers-form typecheck && build && lint && test` 全绿（**649 tests**）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c2-1-host-surfaces.spec.ts` 4/4 全绿）

- **host-form-submit（bug 73 模式专项）pass**：form 真实输入 → store 更新 → submit → valuesPath 发布 `ui.formValues` → form 外 text 真机回显 "Echo: alice (valuesPath)"——单测绿但真机行为成立（无行 scope 污染路径）。
- **host-hidden-field pass**：`hiddenFieldPolicy: { clearValueWhenHidden: true }` 真机——输入 secret → 隐藏字段 → 再次提交 payload 无 stale secret（"Done. secretCode=" 无 S3CR3T）。
- **host-fieldset-isol pass**：form 内双 fieldset 嵌套（Profile + collapsible Advanced）→ legend 点击展开（aria-expanded）+ 两组字段提交值隔离正确（"Submitted: alice / tok-123"）。
- **host-fieldset-enter（P1-1 修复真机证明）pass**：legend focus + Enter → 展开且 800ms 后无 "Submitted:" 文本（修复前会提交）；随后正常提交 "Submitted: bob / tok-9"。
- lab 页新场景：form-lab-page（valuesPath 发布、clearValueWhenHidden）、fieldset-lab-page（collapsible + submit）。
- 回归：simple-form/complex-form/form-ajax-includescope/surface-form-input/dialog-edit-submit/form-input-enhancements **40/40 全绿**。

### Phase 4 — closure

- 2 卡全部 `closed`（P0/P1 清零；P2 fixed；P3 卡内记录）。
- 全量验证（closure gates）：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31（schema-prop-coverage 130/130）、`pnpm test` 58/58 tasks。
- 全量 e2e：**778 passed / 10 failed / 43 skipped / 7 did not run**。10 失败 = C0 pre-existing 9（ai-chat timestamp、ai-rich-text-sender ×5、calendar-demo nav、diff-perf 200ms、input-suggest，归属 C8.1/C9/C2.2/CV）+ **gantt-bars-and-links resize-right**（C1.3 日志已记录的机器状态相关间歇 flake，与本次改动无关）；未达 full-green，不声明。

### C2.1 收口证据

- 审计卡：`docs/audits/per-component/{form,fieldset}.md`（closed）
- 回归测试：`form-shell-enhancements.test.tsx`（+P1-1 Enter-legend 回归）、`form-renderer-definition-contracts.test.ts`（+2 契约断言）、`fieldset-renderer-contracts.test.ts`（fields 基线更新）、`fieldset-interaction.test.tsx`（+gap 行为）
- e2e 宿主场景：`tests/e2e/component-lab/c2-1-host-surfaces.spec.ts`（+4）
- 代码：`form.tsx`（role="button" 排除）、`form-definition.ts`（gap field/loadAction eventContract/7 propContracts）、`fieldset.tsx`（fields 补全）
- 文档：form/fieldset design.md 同步；lab 页 +3 场景
- test-first 证据说明：工作区未提交（本次执行不产生 commit），test-first 以 in-session 先红后绿实测记录（P1-1 回归 1 failed → green；契约断言 3 failed → green）
- **Non-blocking follow-up（CR）**：form Enter 排除清单现覆盖 textarea/button/a/contenteditable/role="button"；后续族审计（C2.3 checkbox/switch 等 role="checkbox"/"switch"、tabs role="tab"）若在 form body 内出现同类交互宿主，需同步扩展排除语义——登记 CR 归集。

### C2.1 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: task `ses_03bfd1b1fffeEVLLWcv832K2N6`
- 第一轮 verdict `issues`：fieldset-interaction.test.tsx gap 测试 TS2322 破坏 workspace typecheck（30/31）、daily log 缺失、roadmap done 时机过早——已全部修复/补写/回退（typecheck 回 31/31；daily log 本节；roadmap C2.1 回 planned 待复审后 done）
- 复审 verdict **`approved`**（2 Minor 已解：test-first commit 证据以 in-session 先红后绿实测替代——工作区未提交属设计内；卡内测试计数修正 649）
- 复核范围：plan 文本一致性（Phase 1-4 `completed` + 33 `[x]`、Closure Gates 10 项）、2 卡 18 维表 + ~12 处 `文件:行` 证据抽查、代码修复落点（form.tsx role="button"、form-definition gap/loadAction/7 propContracts、fieldset fields 4 项）、契约测试断言正确性、c2-1 e2e 4/4 独立重跑、649 单测独立重跑、workspace typecheck 31/31 独立重跑、deferred 诚实性（P3 卡内记录、无 P0/P1 降级、CX-n 不必要裁决成立）、owner-doc 同步
- 收口后：roadmap C2.1 标 `done`；plan `2026-08-03-0105-2` Plan Status `completed`；closure-audit 记录落 plan `## Closure` 节与两卡 Closure 节

## C2.1 mission-driver verification (plan-2026-08-03-0105-2)

- `pnpm typecheck` 31/31 ✓ · `pnpm build` 31/31 ✓（flux-renderers-form/flux-playground/flux 增量执行）· `pnpm lint` 31/31 ✓（schema-prop-coverage 130/130，仅 1 例 pre-existing gantt-grid react-hooks/incompatible-library warning）· `pnpm test` 58/58 tasks ✓（flux-renderers-form 649 tests 全绿）。
- `pnpm test:e2e`：786 passed / **9 failed** / 43 skipped。9 失败与 C0/C1.3 基线完全一致（ai-chat timestamp、ai-rich-text-sender ×5、calendar-demo nav、diff-perf 200ms、input-suggest，归属 C8.1/C9/C2.2/CV，不在本族 Non-Goals）；本次无 gantt flake、无新失败。
- **pre-existing 验证**：`git stash` 工作区改动后 HEAD 对照复跑 9 项失败 spec —— 9/9 同样失败，证实与本 plan 改动无关；stash 已 pop 还原。
- 新增 `tests/e2e/component-lab/c2-1-host-surfaces.spec.ts` 4/4 ✓（valuesPath 发布、clearValueWhenHidden、fieldset 隔离、Enter legend 不提交）。
- 未达仓库级 full-green（pre-existing 9 例），不声明 full-green；本 plan 改动全量验证通过。

## C1.3 mission-driver verification (plan-2026-08-03-0105-1)

- `pnpm typecheck` 31/31 ✓ · `pnpm build` 31/31 ✓ · `pnpm lint` 31/31 ✓ · `pnpm test` 58/58 tasks ✓。
- `pnpm test:e2e`：新增 `tests/e2e/component-lab/c1-3-host-surfaces.spec.ts` 5/5 ✓；component-lab 全量 156 passed / 1 skipped ✓。
- **e2e 回归修复**：plan 的 maxLine 修复（`line-clamp-${N}` → `line-clamp-(--nop-line-count)` CSS 变量机制）未同步更新 `tests/e2e/text-icon-visual-fields.spec.ts`，旧断言 `line-clamp-2/3` + 展开态 `/line-clamp-\d/` 全量跑失败 3 例。已按新契约重写断言（root class + inline `--nop-line-count` 数值、展开态无 line-clamp 类）——修复后该 spec 8/8 ✓。
- 全量 e2e 剩余 9 例 pre-existing（与 C0 基线一致，归属 successor）：ai-chat timestamp、ai-rich-text-sender ×5、calendar-demo nav、diff-perf 200ms 阈值、input-suggest popover。另有 2 例间歇性 flake 与本次改动无关：w3d-editor bold（isolated 复跑通过）、gantt-bars resize-right（HEAD stash 对照 3/3 同样失败，机器状态相关）。
- 未达仓库级 full-green（pre-existing 9 例），不声明 full-green。

## C1.3 basic 原子显示族逐组件审计执行：plan-2026-08-03-0105-1 四 Phase 全绿

执行 `docs/plans/2026-08-03-0105-1-c1-3-basic-atomic-display-family-audit.md`（text/button/badge/icon 4 组件 18 维审计 + 自动修复 + 宿主场景 + closure）。basic 包 16 组件组件级审计全部完成（C1.1/C1.2/C1.3）。

### Phase 1 — 4 张审计卡（`docs/audits/per-component/{text,button,badge,icon}.md`）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×1、P1 ×2、P2 ×6、P3 ×3**。
- 重点维度：text/icon name binding 三态只读所有权（useScopeSelector paths 限定订阅）；button 事件 payload 形状（normalizeActionEvent → `{...event, type:'custom'}` 与 eventContracts 一致）；disabled meta 投影路径（`flux-runtime/src/node-runtime.ts:283-315`）；DOM/marker 契约；i18n；嵌套 schema 分类（无 deepFields 残留）。
- **工具假阴性发现**：`check:audit-missing-renderer-markers` 对 basic 包 0 命中不可信——扫描只查文件是否含 `"nop-"` 子串，`@nop-chaos/ui` 包名即满足（badge.tsx 缺 `nop-badge` marker 未被捕获；record 于 badge 卡，CG 阶段可升级脚本）。

### Phase 2 — P0/P1/P2 自动修复（test-first 13 failed → 466 passed）

- **[button P0-1] INV-1 env IO 边界违反**：countDown 直调 `localStorage`（button.tsx 4 处）——硬红线。修复：新增 `CountDownStorage` adapter（`schemas.ts`，schema prop 类型 `SchemaValue`——ai-chat `engine` 同模式）+ renderer 全部经 adapter（try/catch 兜底），**无 adapter 时 session-only**；lab 页作为 host 注入 localStorage 实现（B 档模式，`renderer-env.md` §9 不扩 env）。test-first：button-count-down.test.tsx 重写为 adapter 驱动（5 条新断言）。
- **[text P1-1] maxLine 视觉失效（bug 73 模式）**：`line-clamp-${N}` 动态类名对 Tailwind v4 静态扫描不可见——实测 built CSS 仅含 1/2/3/100（字面量偶然存在），**maxLine 4..99 真机无钳制**。修复：静态类 `line-clamp-(--nop-line-count)`（Tailwind v4 官方动态 CSS 变量语法）+ renderer 内联 `--nop-line-count`；`>100` 回退 `line-clamp-100`。test-first：text-icon-visual-fields/text-maxline-toggle 断言重写（含 maxLine=5 非邻接值回归锚）。
- **[badge P1-1] 根 marker `nop-badge` 缺失** vs design.md §10 与 root marker 契约；widget-markers-contract.test.tsx 冻结了错误基线 → `cn('nop-badge', ...)` + 冻结测试改为断言 marker 存在。
- **[共性裁决]** badge/icon 注册项缺 defaultSchema（badge 另缺 fields）——与 C1.2 CX-3 同一根因模式 → 当前 plan 内补齐（badge `{type:'badge',text:'Badge'}`+fields、icon `{type:'icon',icon:'star'}`），**回写 CX-3 记录（planned，§7b 追加 C1.3 证据）**。
- **[P2 批量]** button fields 补 label/variant/size/countDownStorage；text copy 反馈/aria 文案 i18n 化（新增 `flux.common.copyFailed`/`copyToClipboard`，zh/en 同步）；button onClick rejection 路径加 try/catch（**实测确认原实现产生 vitest Unhandled Rejection**——test-first 测试先红后绿）；flux-guide/flux-types 再生成（BadgeSchema 补 text/level、ButtonSchema 补 countDownStorage；validate 27 errors 与 pre-existing 基线一致）。
- **[P2-3 裁定]** button href 无 URL 协议校验（content `link` 同源，无既有 house pattern）→ `shared:` 归 CR 集中裁决（根因公共但无已知可利用路径，作者信任 schema）。
- 文档同步：`docs/components/text/design.md` §4/§10（maxLine 机制 + i18n）、`docs/components/button/design.md` §2（countDown adapter + INV-1）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-basic typecheck/build/lint/test` 全绿（**466 tests**）。

### Phase 3 — 组合宿主真实浏览器场景（新增 `tests/e2e/component-lab/c1-3-host-surfaces.spec.ts` 5 用例全绿）

- **host-button-act（bug 73 模式专项）pass**：button onClick → setValue 写 scope → name-bound text 真机回显（'Initial'→'Updated'，无 stale）。
- **countDown 持久化真机证明（P0-1）pass**：lab 页 host 注入 adapter → 点击后 disabled + data-countdown>0 → `page.reload()` → **countdown 从 adapter 恢复**（仍 disabled、恢复值 ≤10）——INV-1 合规持久化路径真机成立。
- **text maxLine 真机钳制（P1-1 bug 73 修复证明）pass**：`getComputedStyle().webkitLineClamp === '5'`、`--nop-line-count === '5'`、CSS 规则 `.line-clamp-\(--nop-line-count\)` 含 `-webkit-line-clamp: var(--nop-line-count)`——修复前该规则不存在（探测发现规则位于 `@layer` 块内，断言用递归 rule walk）。
- **host-badge-count pass**：count badge '0'→'1'→'2' + `.nop-badge` marker 真机存在（P1-1 修复证明）。
- **host-icon-aria pass**：未知图标名回退渲染不崩溃；svg `aria-hidden='true'` + `focusable='false'`。
- lab 页新增场景：text（name binding、maxLine clamp）、badge（count）、icon（fallback）、button（countDown + adapter）。
- 回归：action-logic/data-renderers/smoke/layout-content/navigation **89/89 全绿**。

### Phase 4 — closure

- 4 卡全部 `closed`（P0/P1 清零；P2-3 shared 归 CR backlog 登记）；roadmap C1.3 待独立子 agent closure-audit pass 后标 `done`；CX-3 已追加 C1.3 证据（planned，§7b 事后回写）。
- 全量验证（closure gates）：`pnpm typecheck`/`build`/`lint` 31/31；`pnpm test` 58/58 tasks 绿（basic 466、flux-i18n locale keys 双语言完整）。

### C1.3 收口证据

- 审计卡：`docs/audits/per-component/{text,button,badge,icon}.md`（closed）
- 回归测试：`button-count-down.test.tsx`（adapter 驱动重写）、`button-rejection-path.test.tsx`（新）、`renderer-contract-smoke.test.ts`（+4）、`text-icon-visual-fields.test.tsx`/`text-maxline-toggle.test.tsx`（maxLine 断言）、`widget-markers-contract.test.tsx`（badge marker）
- e2e 宿主场景：`tests/e2e/component-lab/c1-3-host-surfaces.spec.ts`（+5）
- 文档：text/button design.md 同步；flux-guide/flux-types 再生成
- CX-n：roadmap CX-3 追加 C1.3 证据（badge/icon defaultSchema 同根因补齐）

### C1.3 closure-audit（独立子 agent fresh session，执行 session 不自审）

- Auditor / Agent: task `ses_03c655ea6ffeZUBM6m56E4eKJe`
- Verdict: `approved`（2 Minor 非阻塞：P2-3 href 校验归 CR 而非 CX-n（已显式登记后继）；卡内行号为审计时点，cosmetic）
- 复核范围：4 卡 18 维表 + `文件:行` 证据抽查（~15 处）、Phase/Exit Criteria/Closure Gates 文本一致性、代码修复落点（button adapter 4 处 touchpoint + grep 零 localStorage 调用、text maxLine var、badge nop-badge marker、定义 defaultSchema/fields）、测试断言行为正确性、e2e programmatic DOM 断言、deferred 分类诚实性（P2-3 shared→CR）；亲自重跑 basic 466 单测、typecheck/build/lint、playwright c1-3 5/5 + action-logic/data-renderers 16/16、flux-guide validate 27 pre-existing
- 收口后：roadmap C1.3 标 `done`；plan `2026-08-03-0105-1` Plan Status `completed`；CX-3 追加 C1.3 证据（待 mission-driver 按 §7b 标 done）
