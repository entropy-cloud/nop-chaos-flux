# Development Log — 2026-04-14

### 2026-04-14

- Continued Plan 87 Workstream 3 across the composite/detail form renderer family by locking the already-landed slot structure in focused renderer tests.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/array-field.test.tsx`, `packages/flux-renderers-form/src/renderers/variant-field-selector.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-field-basic.test.tsx`, `packages/flux-renderers-form/src/renderers/detail-view-basic.test.tsx`
- Changes landed:
  - Extended `array-field.test.tsx` so the scalar array renderer now explicitly proves `array-field-item` and `array-field-item-body` slot structure under the semantic `.nop-field` root.
  - Extended `variant-field-selector.test.tsx` so the variant renderer now explicitly distinguishes editable `variant-field-selector` structure from read-only `variant-field-readonly-body` structure.
  - Extended the detail-field/detail-view suites so they now explicitly prove the draft-body and draft-error slot markers appear only when the staged surface is active and validation fails.
  - Updated the architecture audit wording so the selector-contract evidence now includes the composite/detail form family, not just the earlier tabs/condition-builder/basic/data/react-host slices.
- Key decision: once the structural/basic/data families were covered, the next highest-value Workstream 3 step was to lock the composite/detail editors because they are the form renderers most likely to regress toward ad hoc internal wrappers during future surface/draft work.
- Verification:
  - `pnpm vitest run src/renderers/array-field.test.tsx src/renderers/object-field.test.tsx src/renderers/variant-field-selector.test.tsx src/renderers/detail-field-basic.test.tsx src/renderers/detail-view-basic.test.tsx` in `packages/flux-renderers-form` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form typecheck` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 3 across the data renderer family by tightening the table/tree selector contract and eliminating the last obvious conditional class-string merges in that slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-data/src/table-renderer.tsx`, `packages/flux-renderers-data/src/__tests__/data-table.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`
- Changes landed:
  - Normalized the remaining conditional icon/filter button classes in `table-renderer.tsx` onto `cn()` instead of template-string concatenation.
  - Extended `data-table.test.tsx` so the table renderer now explicitly proves the root `.nop-table` marker coexists with `table-header-region`, `table-container`, `table-header`, `table-empty-row`, `table-empty-cell`, `table-footer`, `table-row`, and `table-pagination` slot structure.
  - Extended `data-tree-and-chart.test.tsx` so the tree renderer now explicitly proves the root `.nop-tree` marker coexists with `tree-node`, `tree-children`, and `tree-empty` slot structure.
  - Updated the architecture audit wording so Workstream 3 evidence reflects the newly landed `FieldFrame`, dialog/drawer host, page/container, and table/tree coverage rather than the older narrower tabs/condition-builder-only baseline.
- Key decision: after the shared form/basic/React host slices, the next highest-value Workstream 3 target is the data renderer family because its root markers and internal slots are reused by host integration and regression tests across pagination, row actions, and empty/loading states.
- Verification:
  - `pnpm vitest run src/__tests__/data-tree-and-chart.test.tsx src/__tests__/data-table.test.tsx` in `packages/flux-renderers-data` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-data typecheck` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-data lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 3 by expanding focused structural coverage for the basic page/container renderer family.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-renderers-basic/src/__tests__/basic-page-layout.test.tsx`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-basic/src/container.tsx`
- Changes landed:
  - Extended the existing basic layout regression suite so `page` now explicitly proves the root `.nop-page` marker coexists with `data-slot="page-header"`, `page-toolbar`, `page-body`, and `page-footer`.
  - Extended the same suite so `container` now explicitly proves the root `.nop-container` marker coexists with `data-slot="container-header"`, `container-body`, and `container-footer`.
  - Kept the assertions focused on structure rather than visual class strings, so the tests lock the selector protocol without overfitting to presentation details.
- Key decision: after the tabs and condition-builder slices, the next most valuable Workstream 3 tests are the shared structural renderers that host many nested schemas and therefore need stable root-marker plus internal-slot coverage.
- Verification:
  - `pnpm vitest run src/__tests__/basic-page-layout.test.tsx` in `packages/flux-renderers-basic` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-basic typecheck` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-basic lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 3 by tightening the shared React host marker protocol around `FieldFrame` and surface-host internals.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-react/src/frame-slot-meta.test.tsx`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-dialogs.test.tsx`
- Changes landed:
  - Added focused `FieldFrame` coverage for both sides of the presence-only state-attribute contract: dirty/touched/visited/invalid attributes appear as empty-string presence markers only when active, and stay absent for clean fields while required/hint content remains addressable through `data-slot` markers.
  - Replaced the React host-only `nop-dialog-card` and `nop-drawer-card` internal classes in `dialog-host.tsx` with `data-slot="dialog-surface"` and `data-slot="drawer-surface"`.
  - Extended the focused React dialog tests to assert the new host surface slots and to guard against reintroducing the removed internal `nop-*-card` classes.
- Key decision: once a marker is purely host-internal structure and not renderer identity, `data-slot` is the stable contract; reserve `nop-*` classes for semantic renderer roots.
- Verification:
  - `pnpm vitest run src/frame-slot-meta.test.tsx src/__tests__/schema-renderer-runtime-dialogs.test.tsx` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react typecheck` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 3 by finishing the focused condition-builder marker regression coverage and normalizing the last obvious conditional class merges in that renderer slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-renderers-form/src/renderers/condition-builder/config-markers.test.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-test-support.tsx`
- Changes landed:
  - Fixed the new `config-markers` test so it now asserts the live selector contract directly: root `.nop-condition-builder` stays as the semantic renderer marker, `condition-group` and `condition-item` are exposed through `data-slot`, and the removed `.nop-cb-group` / `.nop-cb-item` classes stay absent.
  - Normalized the condition-builder conjunction/not segmented-toggle buttons in `ConditionGroup.tsx` onto `cn()` instead of inline template-string class concatenation.
  - Updated the local mocked `@nop-chaos/ui` test support to export `cn()` so the focused condition-builder suites keep matching the real renderer implementation surface.
- Key decision: focused DOM tests should lock the actual documented contract, not incidental visible text that can vary independently of the marker protocol.
- Verification:
  - `pnpm vitest run src/renderers/condition-builder/config-markers.test.tsx src/renderers/condition-builder/config-actions.test.tsx src/renderers/condition-builder/ConditionBuilder.test.tsx` in `packages/flux-renderers-form` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form typecheck` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form build` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form lint` ✓

### 2026-04-14

- Started Plan 87 Workstream 3 with the current clarified styling-contract baseline: semantic renderers may provide basic format-complete default structure, but internal non-semantic marker classes and inconsistent class-merging helpers still need cleanup.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-basic/src/tabs.tsx`, `packages/flux-renderers-basic/src/page.tsx`, `packages/flux-renderers-basic/src/container.tsx`, `packages/flux-renderers-basic/src/flex.tsx`
- Changes landed:
  - Re-audited Workstream 3 under the updated rule that semantic layout renderers like `flex` are allowed to provide baseline structure when that structure is part of the renderer's meaning.
  - Removed the extra internal `nop-tabs-root` and `nop-tabs-content` classes in `tabs.tsx` and replaced them with `data-slot` markers so the internal structure now matches the selector protocol more closely.
  - Normalized the touched basic renderers (`page`, `container`, `flex`, `text`, `icon`, `dynamic-renderer`, `scope-debug`) onto `cn()` instead of the local `classNames()` helper.
- Key decision: Workstream 3 should target non-semantic internal marker drift and inconsistent renderer merging patterns first, not treat every default semantic layout capability as a styling-contract violation.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/__tests__/basic-page-layout.test.tsx" "src/__tests__/basic-structural.test.tsx" --config vitest.config.ts` in `packages/flux-renderers-basic` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-renderers-basic` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-renderers-basic` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-basic lint` ✓

### 2026-04-14

- Continued Workstream 3 by removing the last obvious non-semantic internal marker classes from the condition-builder renderer while keeping its default visual structure intact.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionItem.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/ConditionGroup.tsx`
- Changes landed:
  - Replaced `nop-cb-item` with `data-slot="condition-item"`.
  - Replaced `nop-cb-group` with `data-slot="condition-group"`.
  - Kept the condition-builder's explicit default visual classes because they are part of its format-complete editor presentation rather than hidden semantic-marker styling.
- Key decision: Workstream 3 should remove redundant internal marker classes first, while preserving explicit default presentation for complex editor-like controls that are expected to render coherently with no extra schema styling.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/renderers/condition-builder/config-actions.test.tsx" "src/renderers/condition-builder/ConditionBuilder.test.tsx" --config vitest.config.ts` in `packages/flux-renderers-form` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-renderers-form` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-renderers-form` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form lint` ✓

### 2026-04-14

- Finished the last Workstream 2 F3 provider-wrap convergence slice by moving React node-boundary publication onto the compiler-emitted wrapper closure.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`
- Changes landed:
  - Extended `TemplateNode` to carry the compiled provider wrapper closure alongside the already-threaded provider metadata.
  - Preserved the compiler-emitted `renderPlan.wrapProviders` closure in `schema-compiler` and exposed it through the template artifact.
  - Replaced the manual three-branch provider assembly in `NodeRendererProviders` with direct execution of the compiled wrapper closure, so action-scope/component-registry/class-alias publication is now fully compiler-owned in the live React path.
  - Closed the corresponding F3 audit note and updated the affected architecture-doc status rows from remaining drift to current consistency.
- Key decision: once the explicit `SurfaceRuntime` extraction was landed, the next most-correct Workstream 2 step was to finish the provider-wrap convergence completely instead of stopping at compiler-owned boolean flags.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-core` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-runtime.test.tsx" "src/schema-renderer-imports-basic.test.tsx" "src/schema-renderer-imports-boundaries.test.tsx" "src/schema-renderer-imports-errors.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓
  - `pnpm --filter @nop-chaos/flux-runtime lint` ✓

### 2026-04-14

- Finished the clean-slate SurfaceRuntime extraction for Plan 87 Workstream 2 and moved live surface ownership fully off `PageRuntime`.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/surface-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-react/src/dialog-host.tsx`, `packages/flux-react/src/schema-renderer.tsx`
- Changes landed:
  - Added an explicit shared `SurfaceRuntime` contract plus unified `SurfaceEntry` stack in `flux-core`, replacing the old split `dialogs[]` / `surfaces[]` shape.
  - Added `packages/flux-runtime/src/surface-runtime.ts` as the owner runtime for dialog/drawer/sheet-style surfaces and removed surface ownership from `PageRuntime`.
  - Updated `RendererRuntime`, `ActionContext`, `SchemaRenderer`, helper dispatch assembly, and `DialogHost` so built-in dialog/drawer actions and root host rendering flow through `surfaceRuntime` rather than `page.openDialog` / `page.surfaceStore`.
  - Updated the focused runtime/react surface tests to validate the new explicit surface owner boundary.
- Key decision: without compatibility constraints, the most correct owner model is one explicit shared `SurfaceRuntime` with one unified stack-based substrate; `PageRuntime` should remain page-shell-only and should not proxy surface ownership.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/__tests__/runtime-dialogs-scope.test.ts"` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-runtime.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-imports-basic.test.tsx" "src/schema-renderer-imports-boundaries.test.tsx" "src/schema-renderer-imports-errors.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-core` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-runtime lint` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 2 by aligning the import/action-scope provider-publication path with compile-owned metadata and dropping an obsolete release-direction assumption.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/flux-core/src/types/node-identity.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-react/src/node-renderer-providers.tsx`, `packages/flux-react/src/useNodeImports.ts`
- Changes landed:
  - Re-audited the current `xui:imports` baseline against the docs and confirmed the updated rule: imported libraries stay resident after load, so no new `releaseImportedNamespaces()` teardown behavior was added.
  - Threaded compile-owned provider publication flags into `TemplateNode.providerPlan` during schema compilation.
  - Updated `NodeRendererProviders` to use the compiled provider plan for action-scope/component-registry/class-alias publication instead of re-deriving that decision from live schema/component policy.
  - Narrowed the architecture audit so F3 and the related docs now reflect the true remaining gap: React still wraps providers imperatively instead of executing the compiler-emitted wrap closure directly, but the provider-publication decision itself is now compiler-owned.
- Key decision: with the current doc baseline, the correct Workstream 2 move is not to reintroduce import release semantics, but to reduce the real remaining drift by moving provider-publication decisions onto compiled metadata.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-imports-basic.test.tsx" "src/schema-renderer-imports-boundaries.test.tsx" "src/schema-renderer-imports-errors.test.tsx" "src/schema-renderer-runtime.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-core` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Continued Plan 87 Workstream 2 by tightening the surface-owner implementation boundary without forcing a wider `PageRuntime` contract break mid-slice.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/__tests__/runtime-dialogs-scope.test.ts`, `packages/flux-runtime/src/action-runtime-handlers.ts`, `packages/flux-react/src/dialog-host.tsx`
- Changes landed:
  - Kept the public `PageRuntime` API stable, but extracted the dialog/drawer open-close/store handling inside `page-runtime.ts` behind a dedicated local surface controller so the surface-owner logic is no longer spread across the page-runtime object body.
  - Re-checked the runtime and React call sites so dialog and drawer actions, disposal, and host rendering still route through the stable page surface API.
  - Recorded in Plan 87 that the remaining surface-owner gap is now narrower: an explicit shared `SurfaceRuntime` contract is still missing, but the live implementation has a cleaner single owner-controlled surface substrate than before.
- Key decision: for Workstream 2, prefer an internal ownership extraction that preserves the stable page-facing API over a wider cross-package contract rename that would interrupt convergence progress without finishing the full surface-runtime design.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/__tests__/runtime-dialogs-scope.test.ts"` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-runtime` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-runtime` ✓
  - `pnpm --filter @nop-chaos/flux-runtime lint` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Advanced Plan 87 Workstream 2 by closing the stale `NodeLocator` identity debt and narrowing the remaining owner gap to surface/runtime substrate work.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `docs/analysis/2026-04-12-architecture-doc-consistency-audit.md`, `packages/nop-debugger/src/controller-inspect.test.ts`, `packages/nop-debugger/src/controller-component-inspector.ts`, `packages/flux-runtime/src/node-resolver.ts`
- Changes landed:
  - Re-audited the live runtime/debugger/action code paths and confirmed active package code no longer uses `NodeLocator`; mounted lookup and inspection already converge on `cid` plus optional `instancePath`.
  - Updated the architecture audit so F4 and the affected per-document notes no longer claim there are still active locator-based runtime/debugger/action paths in package code.
  - Renamed the last leftover debugger test id string that still used `locator` terminology so tests and logs reflect the current identity model.
- Key decision: Workstream 2 should stop treating `NodeLocator` removal as still-open code debt; the real remaining mismatch is repeated-template/runtime-publication cleanup plus the separate surface-owner substrate gap.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/controller-inspect.test.ts" --config vitest.config.ts` in `packages/nop-debugger` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/nop-debugger` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/nop-debugger` ✓
  - `pnpm --filter @nop-chaos/nop-debugger lint` ✓

### 2026-04-14

- Finished Plan 87 Workstream 1 by landing the root execution-boundary convergence on top of the already-landed Host Projection shared-runtime contract.
- Links: `docs/plans/87-remaining-architecture-convergence-successor-plan.md`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-react/src/render-nodes.tsx`, `packages/flux-react/src/workbench/hooks.ts`
- Changes landed:
  - Updated `SchemaRenderer` so the root path now compiles `props.schema` through `runtime.compile(...)` and passes the resulting `CompiledTemplate` into `RenderNodes` instead of relying on `RenderNodes` to normalize raw root schema input.
  - Kept `RenderNodes` flexible for fragment/helper entrypoints by leaving its broader input normalization behavior intact while narrowing the steady-state root contract to compiled input.
  - Reworked the runtime-core regression test to intercept the live `RenderNodes` boundary and assert that the root input is a compiled template carrying the original text node schema, which directly locks the Workstream 1 execution-boundary behavior.
- Key decision: the root renderer boundary should own compile-once normalization, while `RenderNodes` keeps schema fallback support only as a compatibility path for non-root fragment/helper callers.
- Verification:
  - `node "C:/can/nop/nop-chaos-flux/node_modules/vitest/vitest.mjs" run "src/schema-renderer-runtime.test.tsx" --config vitest.config.ts` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.json` in `packages/flux-react` ✓
  - `node "C:/can/nop/nop-chaos-flux/node_modules/typescript/bin/tsc" -p tsconfig.build.json` in `packages/flux-react` ✓
  - `pnpm --filter @nop-chaos/flux-react lint` ✓

### 2026-04-14

- Restored full workspace verification after the post-Plan-84 cleanup regressions by fixing runtime action evaluation, React render-boundary tests, condition-builder test support, and chart renderer test isolation.
- Links: `packages/flux-runtime/src/action-runtime-handlers.ts`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-react/src/__tests__/schema-renderer-runtime-core.test.tsx`, `packages/flux-renderers-form/src/renderers/condition-builder/config-actions.test.tsx`, `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx`
- Changes landed:
  - Restored `ajax` and `submitForm` API evaluation to use action-context compiled evaluation in `flux-runtime`, which cleared the broken action, monitor, timeout, and submit test paths.
  - Earlier in the day the `SchemaRenderer` root boundary still routed raw schema into `RenderNodes`; that temporary regression was later corrected by the dedicated Plan 87 Workstream 1 slice above, so the live baseline is again root compile-once into `CompiledTemplate`.
  - Switched the remaining `condition-builder` config action tests to the shared mocked support path so drag/remove assertions no longer bypass the package's UI mocks.
  - Added an ECharts test mock in the chart behavior suite so jsdom no longer crashes on real canvas rendering while still verifying chart handle registration.
- Key decision: keep the product runtime behavior intact and solve the remaining failures with owner-local fixes; use test-side isolation for ECharts instead of weakening the chart renderer.
- Verification:
  - `pnpm --filter @nop-chaos/flux-runtime typecheck` / `build` / `test` ✓
  - `pnpm --filter @nop-chaos/flux-react typecheck` / `build` / `test` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-form test` ✓
  - `pnpm --filter @nop-chaos/flux-renderers-data test` ✓
  - `pnpm typecheck` ✓
  - `pnpm build` ✓
  - `pnpm lint` ✓
  - `pnpm test` ✓
