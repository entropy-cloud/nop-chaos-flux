# Development Log — 2026-04-02

### 2026-04-02

- Cleaned leaked `.js` / `.d.ts` / `.js.map` artifacts from `packages/*/src/` after the historical source-shadowing regression reappeared locally during strict-lint migration.
- Updated `package.json` so the root `lint` script runs `scripts/verify-no-src-artifacts.mjs` before recursive package lint, which prevents ordinary package failures from hiding a source-artifact regression.
- Key decision: src-artifact verification is now a front-door repository invariant instead of a trailing post-lint check.
- Next step: continue strict-latest lint remediation from the next real failing package once the workspace is artifact-clean.

### 2026-04-02

- Updated `eslint.config.js` to switch the workspace from `eslint-plugin-react-hooks` recommended rules to the flat `recommended-latest` preset.
- Key decision: the strict latest hooks/compiler-oriented rule set is now the actual enforced baseline in code, not just a planning target.
- Next step: fix the newly surfaced lint failures package by package, starting with `packages/ui/`.

### 2026-04-02

- Revised `docs/plans/28-workspace-latest-dependency-and-lint-upgrade-plan.md` to explicitly choose the strict latest lint path instead of preserving traditional hooks semantics.
- Key decision: the repo now treats `eslint-plugin-react-hooks` `recommended-latest` as the target baseline and allows breaking rewrites of shadcn-derived or custom code rather than adding compatibility exceptions.
- Next step: execute the plan as a strict-baseline migration, starting with `packages/ui/` and other shared hook-heavy packages.

### 2026-04-02

- Added `docs/plans/28-workspace-latest-dependency-and-lint-upgrade-plan.md` to document a workspace-wide upgrade path for latest dependency versions and latest lint tooling.
- Key decision: dependency freshness and lint-baseline modernization should be tracked together, but the plan makes `eslint-plugin-react-hooks` 7.x policy explicit instead of silently inheriting its broader recommended ruleset.
- Next step: execute the plan in phases, starting with root toolchain upgrades and an explicit choice between traditional hooks rules and full compiler-oriented hooks enforcement.

### 2026-04-02

- Marked `docs/plans/27-comprehensive-code-remediation-plan.md` as `completed` and recorded a completion date after closing the verified remediation scope and splitting leftover work into separate follow-up categories.
- Key decision: plan #27 is complete as an execution plan even though a few broader refactors and styling migrations remain for separate tracking.

### 2026-04-02

- Updated `docs/plans/27-comprehensive-code-remediation-plan.md` with a post-execution conclusion so the remaining unchecked items are split into concrete follow-up refactors (`P1-16`, `P2-2`), items to stop treating as standalone remediation (`P1-15`, `P2-7`), and a broader styling migration that should move to its own issue.
- Corrected the outdated P0-1 status in the plan after re-verifying `node scripts/verify-no-src-artifacts.mjs` passes in the current workspace.
- Key decision: plan #27 should no longer act as a catch-all backlog for generic coverage work or broad future refactors once the verified runtime defects have been closed.

### 2026-04-02

- Removed residual `nop-icon--*` modifier classes from `packages/flow-designer-renderers/src/` by moving the stable `nop-icon` root marker into `DesignerIcon` and keeping icon identity on `data-icon` instead.
- Updated icon call sites in the flow designer canvas, toolbar, and palette so visual styling stays explicit (`text-white`) without duplicating icon identity in CSS class names.
- Added regression coverage in `packages/flow-designer-renderers/src/index.test.tsx` and verified `pnpm.cmd --filter @nop-chaos/flow-designer-renderers test` passes; `pnpm.cmd --filter @nop-chaos/flow-designer-renderers lint` still reports only the pre-existing `designer-page.tsx` hook dependency warning.
- Key decision: renderer icon wrappers follow the same marker contract as basic icons: root semantic class on the component, identity in `data-icon`, no modifier-style identity classes.

### 2026-04-02

- Added a cross-reference in `docs/architecture/styling-system.md` so readers of the umbrella styling doc can find the detailed `role` vs `data-slot` explanation in `docs/architecture/renderer-markers-and-selectors.md`.

### 2026-04-02

- Clarified documentation ownership between `docs/architecture/styling-system.md` and `docs/architecture/renderer-markers-and-selectors.md`.
- Key decision: keep them as separate docs rather than merging; `styling-system.md` remains the umbrella styling architecture doc, while `renderer-markers-and-selectors.md` remains the focused marker/selector protocol for root markers, `data-slot`, and `data-*` / `aria-*` state semantics.

### 2026-04-02

- Completed plan #27 item P2-9 by extracting the input renderer family (`SelectRenderer`, `TextareaRenderer`, `CheckboxRenderer`, `SwitchRenderer`, `RadioGroupRenderer`, `CheckboxGroupRenderer`) to module-level named components in `packages/flux-renderers-form/src/renderers/input.tsx`.
- Key decision: keep renderer definitions declarative and keep component implementations separately named so DevTools and targeted tests can identify them directly.

### 2026-04-02

- Marked plan #27 item P1-13 complete based on the existing `packages/flux-react/src/node-renderer.tsx` warning path and the regression coverage already present in `packages/flux-react/src/index.test.tsx` for import setup failures.
- Key decision: no further code change was needed because import namespace failures are already observable without crashing render.

### 2026-04-02

- Completed plan #27 item P1-10 by introducing `useFieldHandlers` in `packages/flux-renderers-form/src/field-utils.tsx` and switching the input renderer family to stable memoized handler objects.
- Added regression coverage in `packages/flux-renderers-form/src/index.test.tsx` to verify handler object identity stays stable across host rerenders when renderer inputs are unchanged.
- Key decision: keep handler memoization in shared form field utilities so the fix applies uniformly across all input-family renderers without duplicating `useMemo` logic at each call site.

### 2026-04-02

- Completed plan #27 item P2-1 by splitting `packages/flux-core/src/types/renderer.ts` into focused renderer type modules and keeping `renderer.ts` as a compatibility barrel.
- Added `renderer-component.ts`, `renderer-api.ts`, `renderer-compiler.ts`, `renderer-plugin.ts`, `renderer-hooks.ts`, and `renderer-core.ts` under `packages/flux-core/src/types/`.
- Key decision: preserve the public export surface through `renderer.ts` so downstream packages do not need import churn during the refactor.

### 2026-04-02

- Completed plan #27 item P1-9 by removing heuristic `relatedPaths` prefixing in `packages/flux-runtime/src/form-path-state.ts`.
- Added focused coverage in `packages/flux-runtime/src/__tests__/form-path-state.test.ts` to preserve relative related paths and still remap already-qualified array member paths.
- Key decision: `relatedPaths` should only be transformed when they are already explicit array-local absolute paths; relative paths remain relative.

### 2026-04-02

- Completed plan #27 item P1-17 by extracting shared field binding/presentation/handler wiring into `useFormFieldController` in `packages/flux-renderers-form/src/field-utils.tsx`.
- Refactored `packages/flux-renderers-form/src/renderers/input.tsx` to use the shared hook across text, select, textarea, checkbox, switch, radio-group, and checkbox-group renderers without changing renderer contracts.
- Key decision: keep the abstraction local to form renderer utilities so it can be adopted incrementally by other renderers instead of forcing a broader API rewrite.

### 2026-04-02

- Clarified the styling/marker architecture docs so the boundary is explicit: keep root `nop-*` semantic markers, migrate renderer-internal regions to `data-slot`, and express state with `data-*` / `aria-*` instead of BEM modifiers.
- Updated docs: `docs/architecture/styling-system.md`, `docs/architecture/renderer-markers-and-selectors.md`.
- Key decision: the renderer marker guidance is about semantic consistency and shadcn/radix alignment, not about raw DOM performance.

### 2026-04-02

- Completed plan #27 item P2-8 by removing the empty-path field/error selection path in `packages/flux-react/src/field-frame.tsx` when `name` is undefined.
- Added `EMPTY_FORM_FIELD_STATE` in `packages/flux-react/src/form-state.ts` and a regression test in `packages/flux-react/src/index.test.tsx` to prevent root-level form errors from leaking into unnamed field chrome.
- Key decision: unnamed field chrome should behave as unbound presentation, not as a subscription to the root form path.

### 2026-04-02

- Completed plan #27 item P2-6 by correcting the brace indentation mismatch in `packages/flux-runtime/src/schema-compiler/validation-collection.ts`.
- Key decision: keep consistency-only fixes tightly scoped when they do not carry runtime behavior changes.

### 2026-04-02

- Completed plan #27 item P2-10 by validating dynamic renderer API payloads before treating them as schemas.
- Updated `packages/flux-renderers-basic/src/dynamic-renderer.tsx` to reject malformed payloads with an explicit error state and added a regression test in `packages/flux-renderers-basic/src/index.test.tsx`.
- Key decision: dynamic schema loading should fail closed on malformed payloads instead of pushing unchecked data into the render pipeline.
- Next step: continue remaining low-risk plan items that still have observable runtime or subscription impact.

### 2026-04-02

- Finished plan #27 item P2-3 by removing module-level mutable registry state from `packages/flux-react/src/schema-renderer.tsx`.
- Replaced the implicit global access pattern with explicit lifecycle wiring through `SchemaRenderer` props and updated `apps/playground/src/pages/FluxBasicPage.tsx` to register the debugger component registry through `onComponentRegistryChange`.
- Added regression coverage in `packages/flux-react/src/index.test.tsx` to verify the root component registry is reported on mount and cleared on unmount.
- Key decision: root component registries are renderer-instance state, not process-global state.
- Next step: if plan #27 continues, the remaining work is now outside the already confirmed registry/global-state issue.

### 2026-04-02

- Executed the confirmed items from `docs/plans/27-comprehensive-code-remediation-plan.md` across Flux React, Flux Runtime, Flux Formula, Flux Core, spreadsheet renderers, and data renderers.
- Updated code paths: `packages/flux-react/src/node-renderer.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-runtime/src/form-runtime-validation.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-runtime/src/schema-compiler.ts`, `packages/flux-runtime/src/validation-runtime.ts`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-formula/src/evaluate.ts`, `packages/flux-formula/src/scope.ts`, `packages/flux-core/src/utils/object.ts`, `packages/flux-runtime/src/form-runtime-array.ts`, `packages/flux-runtime/src/api-cache.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`
- Added/updated regression coverage in `packages/flux-react/src/index.test.tsx`, `packages/flux-runtime/src/__tests__/bug-validate-overwrite.test.ts`, `packages/flux-runtime/src/api-cache.test.ts`, `packages/flux-formula/src/scope.test.ts`, and verified the existing cache regression in `packages/flux-renderers-data/src/index.test.tsx` stays green.
- Key decision: keep render phase side-effect free, but seed initial page data when creating the page runtime; reserve effects for later prop reconciliation so cached `data-source` writes are not overwritten during mount.
- Validation: workspace `test` passes; `typecheck` and `build` remain blocked by the existing `packages/word-editor-core` declaration gap for `@hufe921/canvas-editor`; `lint` had already passed earlier in the remediation cycle with pre-existing warnings only.
- Next step: address the remaining P2 work around module-level `SchemaRenderer` registry state if plan #27 is continued.

### 2026-04-02

- Cleared the outdated repository exception that treated `packages/ui/src/` as allowed to contain generated `.js` / `.d.ts` / `.js.map` files.
- Updated enforcement and active references: `.gitignore`, `scripts/verify-no-src-artifacts.mjs`, `docs/plans/25-code-audit-bug-fix-plan.md`, `docs/plans/26-performance-and-completeness-remediation-plan.md`, `docs/plans/27-comprehensive-code-remediation-plan.md`, `docs/bugs/23-stale-js-artifacts-shadow-source-in-vitest-fix.md`.
- Key decision: all `packages/*/src/` directories are source-only with no package-level exceptions; generated output belongs in `dist/` only.
- Next step: when stale artifacts are detected again, treat them as build leakage to eliminate, not as intentional `ui` package state.

### 2026-04-02

- Completed the still-relevant items from plan #25 and plan #26 across Flow Designer, Flux Runtime, debugger, playground, and workspace verification.
- Code paths updated: `packages/flow-designer-core/src/core.ts`, `packages/flow-designer-core/src/types.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/utils/debounce.ts`, `packages/flow-designer-renderers/src/designer-page.tsx`, `apps/playground/src/flow-designer/useFlowCanvasStore.ts`, `packages/nop-debugger/src/panel.tsx`
- Architecture docs updated to match implementation: `docs/architecture/flow-designer/config-schema.md`, `docs/architecture/flow-designer/runtime-snapshot.md`
- Key decision: Flow Designer no longer owns runtime permission evaluation or string-based connection validation; host/schema preprocessing must decide visibility and allowed operations before the document reaches the designer runtime.
- Key decision: dirty-state tracking now uses revision baselines instead of `JSON.stringify` comparisons in hot paths.
- Validation: workspace `typecheck`, `build`, `lint`, and `test` all pass after the remediation work.
- Next step: if plan files are still used as execution tracking, mark the completed items in `docs/plans/25-code-audit-bug-fix-plan.md` and `docs/plans/26-performance-and-completeness-remediation-plan.md` in a follow-up docs-only pass.

### 2026-04-02

- Re-checked all documentation changes made today and corrected one governance issue in `docs/plans/`:
	- the first bulk status pass had mixed file-lifecycle and execution-state semantics
	- plan headers now use more accurate execution-oriented states such as `superseded`, `planned`, `pending`, `in-progress`, and `completed`
	- corrected stale top-of-file status text in debugger-related plans where header metadata and body summary diverged
- Validation after correction:
	- all 29 plan files now have a normalized `Plan Status` line
	- active docs internal reference scan still passes with no missing targets

- Completed docs governance cleanup for structure consistency and latest-baseline policy:
	- aligned Flow Designer architecture docs with security boundary (runtime does not evaluate permission semantics)
	- fixed active-doc broken references in architecture/references/log index
	- rewrote `docs/standardization.md` to keep only current baseline rules
	- removed archive-history routing language from `docs/index.md` and clarified non-plan/non-bug docs should describe latest design
	- added explicit status metadata to all files under `docs/plans/` (`Plan Status`, `Last Reviewed`)
- Updated files:
	- `docs/architecture/flow-designer/config-schema.md`
	- `docs/architecture/flow-designer/design.md`
	- `docs/index.md`
	- `docs/references/maintenance-checklist.md`
	- `docs/logs/index.md`
	- `docs/standardization.md`
	- `docs/plans/*.md`
- Key decision: `docs/plans/` carries execution status locally per file; no separate active-plans routing section is introduced.

- Added bug-derived architecture guardrail routing with a split structure:
	- architecture docs keep concise principles only
	- detailed anti-pattern mapping and review checks live in `docs/references/architecture-guardrails-from-bugs.md`
- Updated architecture summaries (minimal additions):
	- `docs/architecture/renderer-runtime.md`
	- `docs/architecture/flux-runtime-module-boundaries.md`
	- `docs/architecture/styling-system.md`
- Updated navigation and maintenance routing:
	- `docs/index.md`
	- `docs/references/maintenance-checklist.md`
- Key decision: keep architecture docs focused on boundaries/intent; centralize executable guardrails in references for easier incremental updates from `docs/bugs/`.

- Added dedicated normative design requirement docs for security and performance under architecture:
	- `docs/architecture/security-design-requirements.md`
	- `docs/architecture/performance-design-requirements.md`
- Key decision: treat both docs as active architecture constraints (not analysis/planning notes), with explicit review checklists and prohibited patterns.
- Updated documentation navigation and maintenance routing:
	- `docs/index.md` (Read This First + Active Source Of Truth)
	- `docs/references/maintenance-checklist.md` (fast path additions + new change triggers sections 11/12)

- Updated remediation plan #26 to align with `docs/articles/flux-design-introduction.md` architecture boundary: permissions must not be handled in renderer runtime.
- Key decision: upgrade "remove runtime permission system" to P0 and treat it as architecture-conformance requirement, not just a safe-eval bug fix.
- Added generalized similar-issue inventory to plan #26:
	- all source `new Function` locations,
	- all runtime permission semantics in contracts/core/schema/tests,
	- CI guardrail gap and follow-up static checks.
- Plan doc updated: `docs/plans/26-performance-and-completeness-remediation-plan.md`

- Added a consolidated remediation plan for performance, stability, completeness, and engineering consistency issues found in the latest audit.
- New plan document: `docs/plans/26-performance-and-completeness-remediation-plan.md`
- Key decision: execute in three phases (P0 immediate safety/correctness, P1 high-impact performance and observability, P2 completeness and governance).
- The plan now includes per-issue problem statements, concrete fix方案, acceptance criteria, and execution order.

- Redesigned `DataSourceSchema`: removed `body` region; `data-source` now injects data into the current scope and renders `null`.
- Key decision: `data-source` purpose is purely data enrichment — it writes to the current scope so sibling nodes can access the fetched data without needing a wrapping `body` container.
- Scope injection rules: if `dataPath` is set, writes response to `scope[dataPath]`; otherwise treats response as a plain object and merges all keys into current scope.
- Architecture doc updated: `docs/architecture/api-data-source.md`
- Type change: removed `body?: SchemaInput` from `DataSourceSchema` in `packages/flux-core/src/types/schema.ts:54`
- Renderer definition: removed `regions: ['body']` from `packages/flux-renderers-data/src/index.tsx:25`
- Renderer rewritten to use `useRenderScope()` and write to scope via `scope.update(dataPath, data)` or `scope.store.setSnapshot({...})`: `packages/flux-renderers-data/src/data-source-renderer.tsx`
- Loading/error UX removed from renderer: no inline skeleton or error alert; errors reported via `env.notify`.
- Tests updated to use sibling text nodes instead of `body` region: `packages/flux-renderers-data/src/index.test.tsx`
- All 11 tests pass; typecheck and build clean.

### 2026-04-02 (session 2)

- Added `ScopeRef.merge(data)` method for shallow-merging a `Record<string, unknown>` into scope's own data with change detection.
- Key decision: `merge` performs per-key `Object.is` comparison before writing — skips `setSnapshot` call entirely when no values actually changed, avoiding unnecessary reference changes and downstream reactive re-renders.
- Empty merge data (`{}`) is treated as a no-op and returns immediately.
- Interface change: added `merge(data: Record<string, unknown>): void` to `ScopeRef` in `packages/flux-core/src/types/scope.ts:24`
- Implementation: `packages/flux-runtime/src/scope.ts:192-214` — checks each incoming key against current snapshot; only creates new snapshot object if at least one value differs.
- Updated `DataSourceRenderer` to use `scope.merge()` instead of raw `scope.store?.setSnapshot({...})`: `packages/flux-renderers-data/src/data-source-renderer.tsx:31`
- Added 10 tests covering: merge new keys, overwrite, no-op on unchanged, change detection, reference stability, child scope isolation, empty data: `packages/flux-runtime/src/__tests__/scope-merge.test.ts`
- Fixed all downstream mock ScopeRef objects across 4 test files (`flux-formula`, `flux-react`, `flux-runtime`) to include `merge` stub.
- All 148 runtime tests pass; build clean.

### 2026-04-02 (session 3)

- Applied `merge` to `flow-designer-renderers`: replaced raw `setSnapshot(scopeData)` in `useDesignerHostScope` with `scope.merge(scopeData)` for change detection: `packages/flow-designer-renderers/src/designer-context.ts:120`
- Reviewed all other `setSnapshot` call sites in the codebase: `render-nodes.tsx:171` already guards with `shallowEqual` before calling — no fix needed. All other calls are in test stubs or non-scope contexts (React `useState`, debugger store).
- Removed unused `shallowEqual` import from `packages/flux-runtime/src/scope.ts:3`.
