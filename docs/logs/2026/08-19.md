# Development Log - 2026-08-19

### 2026-08-19

- **Roadmap R1/C1/C2 完成（对标分析 + 复杂页面构想 + 差距裁决）**：
  - **R1** `docs/analysis/ui-review/R1-framework-benchmark.md`——五组评分卡（AMIS 2.5/4.5、AntD+Pro 4/5、shadcn+blocks 4.5/3、Retool 系 2.5~3.5/4~4.5、Vant 4/4.5）+ flux 自评 **美观 3.5 / 完善 3.0**（分维算术均值可复算，D2 复评同口径）。核心判断：组件底座与审美已站 shadcn 一线（对 AMIS 的代际优势），两级台阶 = AntD Pro 式页面模板层（P2）+ Linear 式键盘优先交互（P4）。web 调研来源记 §7。
  - **C1** `docs/analysis/ui-review/C1-complex-page-conceptions.md`——10 页构想（三栏运维工作台/⌘K 命令面板/高密度交易表/多视图数据库/看板拖拽/预约流程/审批中心/图表门户/移动 shell/结果终态），逐页 ★ 分级 + 原语映射 + 缺口预判（显式对照 G3/G5 与 R1 G-A~G-I）；新发现 G-J（resizable schema 化）、G-K（流程图数据着色）；4 页构想库存不入 P 系列。
  - **C2** `docs/analysis/ui-review/C2-capability-gaps.md`——L1~L4 四级裁决滚动文档初版（G-A~G-M 15 条含预登记 5 候选；B8 语义发现 #2/#3 落 G-L/G-M 观察项）；D1 产品化输入预清单 6 项（option-row 首位）。
  - **closure audit（独立 sub-agent，两轮）**：round 1 R1 `issues`（1 Major：flux 综合分 3.5/3.5 与分维分不自洽）+ C1/C2 `approved`（6 Minor）→ 整改（综合分改 3.5/3.0 并写明算术、§2.4 补分维拆解、G3 交叉引用、三处重叠承认、resizable 措辞、G-F 证据引用）→ round 2 三项全 `approved`（2 cosmetic 已顺手修）。R1/C1/C2 Phase Status → `done`。

- **Roadmap R0 完成（UI 资产盘点与基线实测）**：产出 `docs/analysis/ui-review/R0-baseline-inventory.md`——注册 renderer **122 个**（运行时 registry 口径：逐包 register+list；industrial/editor/ai 因 leafer canvas 改静态口径复核，逐包明细 basic16/form22/form-advanced19/content19/data10/layout8/mobile5/scheduling4/ai14/其余各1）；`@nop-chaos/ui` **62 组件模块 + 16 工具**；theme-tokens **4 调色板 324 CSS 变量**；19 复杂页逐页行数/特征/复杂度分级（C1 去重基线：企业向 14 页集中 ★★~★★★，Sundial 5 页占据 ★★★★+）。roadmap Current Baseline 已按实测回写（88→122、ui 64 行→62+16、plan 460 在途→completed）。R0 closure audit（独立 sub-agent）round 1 `approved`（2 Minor：复测命令 glob、complex-form 描述——已当场修正）。

- **Plan 460 收口（B8 批次，ui-review worktree 恢复执行）**：live 审计发现 Phase 5-7 主体已随 B4-B7 落地、plan 文本滞后；本批次 test-first（8 断言先红后绿）收口真实剩余缺口——P7 保存真实写后端（`Sundial__updateSettings` + `db.sundialSettings`）、P8 子任务删除写后端（`Sundial__deleteSubtask`）、P9 移动写后端+关闭（任务行补 `activeTaskId` 映射 db id）、P10 详情页 X 从 toast 改真实 `navigate` 回工作台、Phase 3 遗留的 task-detail-dialog 4 字段行 picker 化。sundial schema 零「占位反馈」残留。
- **B8 runtime 语义发现（复刻=能力发现器）**：① `closeDialog/closeSurface` 定向关闭走 action **顶层 `surfaceId`**（targeting），`args.surfaceId` 无效；② declarative dialog body 内 `setValue` 影子写自身 scope 不回落页面（行隐藏等页面状态须页面级承载）→ G5 类缺口证据；③ onSubmitSuccess hook 时刻裸 scope 变量求值不可靠，跨 dialog 传参应走 openDialog `data` → form → `$formData`；④ 嵌套 picker portal 点击对未设 `closeOnOutsideClick:false` 的外层是 outside-press（458 撤销根因完整版）。
- **B1 逃逸回归修复**：`flux-renderers-basic/surface-event-ctx.test.tsx` 在 master HEAD 确定性失败（双 worktree 对照确认）——测试假设「X 关闭受控 dialog 保持挂载」，与 459/460-B1 userClosed 语义冲突；等价强度重写为 confirm-first 采集（onConfirm/onClose 两 URL 同 surfaceId 断言不变）。
- **验证（ui-review worktree）**：`pnpm typecheck`/`build`/`lint` 37/37、`pnpm test` 66/66 tasks（playground 171 含 sundial 29：+P11 设置图标导航断言）、sundial e2e spec 24/24（新增 21-24）；`pnpm check` 零新增红（预存登记红 2 项：oversized `wizard-renderer.tsx:716`、`audit-event-dispatch-ctx` industrial 6 hits，08-04/08-15 已登记）；B8 自引 `mock-backend.ts:519` 超限当场拆分（463 行 + `mock-backend-sundial.ts` 89 行，API 不变）。
- **全量 e2e 补记（PLAYWRIGHT_PORT=4190，14.4m）**：1175 passed / 43 skipped / 11 failed——11 失败全数预存：scada-edge-cases ×2（project-context 已登记 watch-only：edge line 几何 + edge polygon leafer 渲染时序）；crud-demo ×4 + route-inventory ×1 + barcode smoke ×1 经 master worktree 对照复跑（4191 端口）失败完全一致，确认为 master HEAD 预存，非本分支引入。故本分支 e2e 零新增失败，但**不声称 full-green verification**（全量套件存在预存红）。
- **环境观察项（候选治理）**：① 包脚本 `tsc` 依赖环境解析（仓库无根级 typescript 依赖声明），新 worktree 需 PATH 前置 `node_modules/.pnpm/node_modules/.bin`（lockfile TS 6.0.2）且先 `pnpm build`，否则 playground typecheck 误报 TS5103/TS2307——master 工作区仅因旧安装残留 `.bin/tsc` 通过；② playwright 4175 端口可能被其他 worktree 残留 dev server 占用，`reuseExistingServer` 会复用旧 schema 导致假失败/假通过，需 `PLAYWRIGHT_PORT` 换端口。
- **Closure audit（独立 sub-agent，fresh session）**：round 1 verdict `issues`（1 Blocker：P11 零测试覆盖，Gate 2 声称失实；3 Minor：日志缺 B8 条目、plan 文本 117→89 行漂移、错字）→ 整改（+P11 单测 13/13 绿、文本修正、本条日志）→ round 2 复核 approved（证据记入 plan Closure 节）。
- 新建 worktree `nop-chaos-flux-ui-review`（分支 `ui-review`，基点 master `0078f2a40`），UI review 专题全部工作隔离在该分支，不落 master 工作区。
- 拟制 `docs/backlog/ui-review-roadmap.md` + `missions/ui-review.json`（按 `docs/skills/roadmap-and-mission-authoring-with-consensus-review.md` 流程）：R0–R3 对标分析+一致性审查（ux-design-pattern-audit skill）、C1–C2 复杂页面构想与缺口裁决、P1–P7 参考应用复刻（Ant Design Pro / Cal.com / Linear / Notion database / Airtable grid / Stripe dashboard，沿 sundial 五步模板）、D1–D2 缺口产品化与门禁沉淀；已在 `docs/index.md` 登记。
- 关键决策：复刻方法论以 `docs/analysis/sundial-ui-reproduction-analysis.md` 为规范源（"外部应用复刻作为能力发现器"经 456/457/459 落地验证，460 在途重做深化段，458 已 superseded-reverted 不作先例）；法律边界定为"仿布局/交互/令牌结构，不复制品牌资产"；每个复刻 plan closure 强制回写 C2 滚动文档（缺口回流闭环）。
- 共识审查两轮：round 1 verdict `revised`（1 Blocker：Current Baseline 误称 sundial 先例"已完成"，live 实为 460 drafting 在途 + 458 撤销；3 Major：Phases 表 Status 列第二动态区、C2 done 判据未定义、拆分阀缺失）→ 修正（含 R0 以 plan 460 收口为前置的三处落地）→ round 2（fresh session）verdict `approved`，余 2 Minor（B1–B7 编号改 Phase 口径、本日志 sundial 链措辞）已随手修正。
- 基线修正待办：注册 renderer 数 88 为 `*-definitions.ts` 口径下限（漏 form-advanced 散装注册），R0 以运行时 registry 口径实测后修正 roadmap Current Baseline。
- Next step: 人工 review/合并决策后，按 Phase Status 顺序启动——首个动作是恢复并收口 plan 460（R0 前置），随后 R0 资产盘点。
