# Development Log - 2026-05-20

### 2026-05-20 (AGENTS full-green commit-title rule)

- Updated `AGENTS.md` commit-message guidance so any commit that records a full-green state with both unit tests and e2e passing must mention `full-green verification` directly in the commit title/subject, not only in the body.

### 2026-05-20 (full-green unit + e2e verification and test contract refresh)

- Refreshed several Playwright expectations to match current UI/runtime contracts instead of older assumptions: button-based `Select` interactions now use a shared helper in `tests/e2e/select-helpers.ts`, playground route coverage now includes `taskflow-designer`, report-designer inspector assertions accept cell-focused state, and word-editor dataset persistence verifies the saved localStorage contract after an explicit page save.
- Updated affected E2E specs: `tests/e2e/debugger-meta-diagnostic.spec.ts`, `tests/e2e/debugger.spec.ts`, `tests/e2e/exploratory/keyboard-focus-and-teardown.spec.ts`, `tests/e2e/playground-entry-pages.spec.ts`, `tests/e2e/report-designer-demo.spec.ts`, and `tests/e2e/word-editor-dataset.spec.ts`.
- Root cause summary: the repo's Select controls now render Base UI button/combobox triggers rather than native `<select>` elements, route inventory gained `taskflow-designer`, report-designer inspector text legitimately shifts from sheet-level to cell-level selection, and word-editor dataset persistence is committed by the page save flow rather than dialog close alone.
- Full-green verification passed after the test refresh:
- `pnpm test`: 49/49 successful tasks
- `pnpm exec playwright test`: 323 passed, 41 skipped, 0 failed
- `pnpm typecheck`: 49/49 successful tasks
- `pnpm build`: 26/26 successful tasks
- `pnpm lint`: 26/26 successful tasks

### 2026-05-20 (business AI practice article attractor recalibration)

- Re-read the attractor methodology sources before finalizing `docs/articles/ai-practices-for-business-application-development.md`: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`, `docs/discussions/2026-04-28-ai-development-attractor-model-discussion.md`, `docs/architecture/README.md`, `docs/architecture/flux-design-principles.md`, and `docs/architecture/frontend-programming-model.md`.
- Re-centered the article around `状态空间 -> 吸引子 -> 轨迹 -> 控制` rather than artifact mapping: AGE is now framed as attractor definition and trajectory convergence, with `docs/architecture/` owner docs serving as the precedence-bearing equation layer and plan/verification/audit/logs/bugs serving as harness after that layer exists.
- Added explicit guardrails against common misreadings: attractor is not a fully enumerated endpoint, project roadmap, allowed activity region, stronger boundary, or OpenSpec-style change package; it is an implicitly defined stable structure that makes later execution evidence meaningful.
- Tightened the business-project takeaway so owner docs define a business attractor through fact sources, state transitions, ownership boundaries, and rejected misunderstandings; Flux plans are described as local trajectory closure contracts, not richer todo lists.
- Added the spec-driven task-orientation cost argument: spec/change workflows can improve task inputs but easily become formal task dispatch; maintaining specs to Flux's owner-doc / plan / log / bug / testing / analysis / audit precision usually costs more because heterogeneous warehouse memory must be translated into requirement/scenario and change-package form.
- Polished the article after follow-up review: restored the precise `docs/architecture/` precedence-bearing owner-doc wording, removed brittle numeric summaries of architecture principles/hard rules, qualified plan verification requirements for code versus docs-only changes, restored the repository link, and kept the `361` plan reference as a concrete plan-shape example.
- This was a docs-only article/log update; project typecheck/build/lint/test were not re-run.

### 2026-05-20 (plan closure sync for 371 / 376 / 388 / 389 / 395 / 396 / 403)

- Re-ran live closure gates after the final flow-designer fidelity and audit-rule split work: `pnpm check:oversized-code-files` now passes with `80 warnings, 0 errors`; `pnpm typecheck` passed (`49/49` tasks); `pnpm build` passed (`26/26` tasks); `pnpm lint` passed (`26/26` tasks); `pnpm test` passed (`49/49` tasks).
- Closed `docs/plans/376-deep-audit-2026-05-19-spreadsheet-context-menu-test-decomposition-plan.md` with independent closure audit `ses_1bce29d43ffeuzWeLBl4keTTrn`; the spreadsheet context-menu suite remains decomposed, `SpreadsheetGridHarness` reuse remains live, and the oversized hard gate no longer blocks closure.
- Closed `docs/plans/388-deep-audit-2026-05-19-form-tree-widget-accessibility-plan.md` with independent closure audit `ses_1bce29d24ffeQ6YYSLwkIGVLHb`; shared tree keyboard/status semantics are landed and no owner-doc update was required.
- Closed `docs/plans/389-deep-audit-2026-05-19-runtime-async-listener-and-stale-promise-fidelity-plan.md` with independent closure audit `ses_1bce29ca2ffelWf7dD0qZl9N8b`; listener cleanup, fallback provider cleanup, stale request eviction, and debug-summary fidelity remain fixed with no owner-doc update required.
- Completed the remaining host-facing fidelity gap for `docs/plans/395-deep-audit-2026-05-19-flow-designer-error-fidelity-plan.md`: `packages/flow-designer-renderers/src/designer-command-types.ts`, `src/designer-command-adapter-helpers.ts`, `src/designer-command-adapter-graph.ts`, `src/designer-context.ts`, and `src/designer-page-inner.tsx` now preserve original thrown values through action-result and monitor-reporting surfaces; refreshed focused proof passed in `src/designer-command-adapter.test.ts`, `src/designer-provider-and-manifest.test.tsx`, and `src/designer-page-shell.test.tsx`, and independent closure audit `ses_1bcc0fe73ffejud0lGbX4ZinB4` found no remaining in-scope blocker after plan sync.
- Closed `docs/plans/396-deep-audit-2026-05-19-flow-designer-accessibility-interaction-plan.md` with independent closure audit `ses_1bcc6058dffeBfzJI6miyyp9lv`; the DingFlow menu keyboard model plus stable node/edge accessible name/state contract are landed and the owner docs remain synced.
- Closed `docs/plans/403-deep-audit-2026-05-19-tree-renderer-performance-plan.md` with independent closure audit `ses_1bcc6054dffeJ7Thc94w3aoR03`; bounded staged subtree expansion remains live and no owner-doc update was required.
- Closed the docs-only umbrella `docs/plans/371-deep-audit-2026-05-19-owner-routing-plan.md` after synchronizing its surviving-queue wording and historical log references to the merged-away `381` / `390` reality. The final routing review remains `ses_1c1a8efacffeFpK21DOAlZoJPa`, and the final live-tree sync audit `ses_1bcc603b1ffevG0J1BP1bRB0ML` reduced the remaining blockers to text consistency, which is now resolved.

### 2026-05-20 (business AI practice article OpenSpec source correction)

- Re-audited `docs/articles/ai-practices-for-business-application-development.md` against OpenSpec source instead of relying on workflow names or docs phrasing.
- Source-backed correction: `openspec status` artifact completion is file-existence based; `openspec validate` treats main specs and delta specs differently; `openspec archive` validates and applies delta specs by default but can skip validation and can continue past incomplete task warnings; `/opsx:sync` and `/opsx:verify` are AI workflow prompts rather than hard CLI semantic closure.
- Updated the article to state those layers explicitly while preserving the requested title, upfront `nop-chaos-flux` introduction, file-in/file-out practice, and the distinction between OpenSpec behavior-spec evolution and AGE's broader owner-doc / plan / log / bug / test / audit convergence model.
- Rechecked Flux plan evidence against `docs/plans/00-plan-authoring-and-execution-guide.md`, `docs/plans/382-deep-audit-2026-05-19-table-and-crud-owner-state-and-event-contract-plan.md`, and `docs/plans/388-deep-audit-2026-05-19-form-tree-widget-accessibility-plan.md`: plan closure still hinges on `Current Baseline`, `Non-Goals`, `Closure Gates`, repo-wide gates where applicable, and independent closure audit.
- Independent review `ses_1bcd75fc3ffeWEfXzOIuadjA9d` first found one factual over-bundling of main-spec and delta-spec validation; after correction, the same review returned `acceptable` with no remaining blocking findings.
- After further user challenge, re-audited OpenSpec from its own `openspec/specs/` and sampled active/archived changes instead of only CLI source. Key correction: the article now treats OpenSpec as a behavior-spec evolution workflow, not just a hard-constraint/prompt split, and explicitly notes that OpenSpec concepts do not map one-to-one to Flux owner docs, plans, logs, bugs, tests, and audit.
- Added the Flux-side counterfactual: if Flux handled a similar change, stable behavior would go to owner docs, the execution slice would go to a plan with `Current Baseline`, `Goals`, `Non-Goals`, `Closure Gates`, and independent closure audit, while logs/bugs/testing/tests/audit keep separate evidence roles.
- Clarified tasks: useful for AI execution and progress tracking, but not a final fact source; checklist completion must be proven against current code, tests, owner docs, and independent review.
- Independent comparison review `ses_1bcca946cffeB5LB2iPKHNNpz7` returned `acceptable` with no blocking findings; applied its non-blocking precision suggestions around `project.md`/`AGENTS.md`/config context/rules and schema artifact fields.
- Reworked the comparison again around documentation-routing freedom versus structured artifact workflows: Flux can directly route to owner docs, analysis, logs, bugs, testing issues, or plans depending on the problem, while OpenSpec projects work through a more structured schema/artifact graph. This also removed the misleading impression that OpenSpec `tasks.md` corresponds to Flux `plans`; it now states tasks only resemble the execution-checklist slice of a Flux plan or a session todo.
- Added the OpenSpec delta-apply distinction: OpenSpec lowers spec-update cost through fixed delta sections and header matching, not by asking AI to freely rewrite requirements each time; Flux changes can come from requirements, code reading, analysis, tests, bug notes, audits, or manual findings and do not always start as spec deltas.
- Focused review `ses_1bcbf0de4ffe7VMiGnXzSd2coI` returned `acceptable`; applied non-blocking wording changes to avoid overloading "rigid" and to clarify `design.md` is needed only when warranted.

### 2026-05-20 (plan 403 - tree renderer performance execution slice)

- Executed the retained tree-renderer performance fix in `docs/plans/403-deep-audit-2026-05-19-tree-renderer-performance-plan.md`: `packages/flux-renderers-data/src/tree-renderer.tsx` now bounds the large expanded-subtree hot path by rendering a first child batch immediately and completing the rest of the expanded children in a follow-up tick instead of eagerly recursing the whole subtree in one commit.
- Kept the supported tree contract intact while bounding the hotspot: keyboard navigation, tree expand/collapse semantics, region rendering, and final expanded content all remain unchanged; only the large-subtree expansion work is staged.
- Added focused proof in `packages/flux-renderers-data/src/__tests__/data-tree-and-chart.test.tsx` for both sides of the contract: the initial expanded subtree is bounded for large child lists, and the full subtree still appears after the deferred fill completes.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-renderers-data exec vitest run src/__tests__/data-tree-and-chart.test.tsx` (`1` file / `18` tests) and `pnpm --filter @nop-chaos/flux-renderers-data typecheck`.
- Plan `403` is now honestly `partially completed`: the in-scope code and focused proof are complete, but repo-wide closure gates and the independent closure audit still need to be re-run before it can move to `completed`.

### 2026-05-20 (plan 396 - flow-designer accessibility interaction execution slice)

- Executed the retained flow-designer accessibility interaction fixes in `docs/plans/396-deep-audit-2026-05-19-flow-designer-accessibility-interaction-plan.md`: `packages/flow-designer-renderers/src/dingflow/ding-flow-add-node-menu.tsx` now implements roving menu focus with `ArrowLeft` / `ArrowRight` / `Home` / `End` plus `Escape`, matching its declared `menu` / `menuitem` contract.
- Updated `packages/flow-designer-renderers/src/designer-xyflow-canvas/designer-xyflow-node.tsx` and `packages/flow-designer-renderers/src/designer-xyflow-canvas/designer-xyflow-edge.tsx` so focusable canvas node/edge roots publish stable `aria-label` values and selected state through `aria-pressed` instead of relying on visual-only styling or arbitrary inner schema text.
- Synced `packages/flow-designer-renderers/src/designer-page-inner.tsx` with the widened lifecycle-hook error contract from the adjacent `395` slice so host issue monitoring still reports designer hook failures correctly after `lifecycleHookError.error` moved from `string` to `unknown`.
- Added focused proof in `packages/flow-designer-renderers/src/dingflow/ding-flow-add-node-menu.test.tsx`, `packages/flow-designer-renderers/src/canvas-bridge.test.tsx`, `packages/flow-designer-renderers/src/edge-label-xyflow.test.tsx`, and kept `src/designer-page-shell.test.tsx` green; the focused suite now explicitly covers menu keyboard navigation plus node/edge accessible name/state publication.
- Updated `docs/components/designer-page/design.md` and `docs/architecture/flow-designer/design.md` so the supported interaction contract now explicitly requires menu-model keyboard behavior for DingFlow add-node overlays and stable label/state semantics for focusable canvas node/edge roots.
- Focused verification passed: `pnpm --filter @nop-chaos/flow-designer-renderers exec vitest run src/dingflow/ding-flow-add-node-menu.test.tsx src/canvas-bridge.test.tsx src/edge-label-xyflow.test.tsx src/designer-page-shell.test.tsx` (`4` files / `20` tests) and `pnpm --filter @nop-chaos/flow-designer-renderers typecheck`.
- Plan `396` is now honestly `partially completed`: the in-scope code, focused proof, and owner-doc updates are complete, but repo-wide closure gates and the independent closure audit still need to be re-run before it can move to `completed`.

### 2026-05-20 (plan 395 - flow-designer error fidelity execution slice)

- Executed the retained flow-designer error-fidelity fixes in `docs/plans/395-deep-audit-2026-05-19-flow-designer-error-fidelity-plan.md`: `packages/flow-designer-core/src/types.ts` now allows `DesignerEvent` `lifecycleHookError.error` to preserve the original thrown value as `unknown` instead of forcing a string-only surface.
- Updated `packages/flow-designer-core/src/core-node-commands.ts` and `packages/flow-designer-core/src/core-edge-commands.ts` so node-create, edge-connect, and delete lifecycle-hook failures emit the original thrown error object instead of collapsing it through `String(err)`.
- Added focused propagation proof in `packages/flow-designer-core/src/__tests__/core-error-fidelity.test.ts`, covering node hook, edge hook, and shared delete hook failure paths through the public `createDesignerCore(...)` owner surface.
- Focused verification passed: `pnpm --filter @nop-chaos/flow-designer-core exec vitest run src/__tests__/core-error-fidelity.test.ts src/core.test.ts src/__tests__/core-graph.test.ts` (`3` files / `27` tests) and `pnpm --filter @nop-chaos/flow-designer-core typecheck`.
- Plan `395` is now honestly `partially completed`: the in-scope code and focused proof are complete, but repo-wide closure gates and the independent closure audit still need to be re-run before it can move to `completed`.

### 2026-05-20 (plan 388 - form tree accessibility execution slice)

- Executed the retained tree-widget accessibility fixes in `docs/plans/388-deep-audit-2026-05-19-form-tree-widget-accessibility-plan.md`: `packages/flux-renderers-form-advanced/src/tree-control-controllers.ts` now owns shared roving focus, visible-tree navigation, and `ArrowUp` / `ArrowDown` / `Home` / `End` movement for both `input-tree` and `tree-select`.
- Updated `packages/flux-renderers-form-advanced/src/tree-controls.tsx` so tree nodes share one active `tabIndex=0` item at a time, expanded-state wiring is driven by the shared controller instead of per-node local state, and loading states now publish `aria-busy` plus loading-status ids through `aria-describedby` on the tree root.
- Added focused accessibility proof in `packages/flux-renderers-form-advanced/src/tree-control-controllers.test.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/form-tree-control-source-states.test.tsx`, and `packages/flux-renderers-form-advanced/src/__tests__/tree-structure.test.tsx`; the new regressions explicitly cover shared keyboard navigation, tree-select popup keyboard behavior, and busy/loading association.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/tree-control-controllers.test.tsx src/__tests__/form-tree-control-source-states.test.tsx src/__tests__/tree-structure.test.tsx` (`3` files / `19` tests) and `pnpm --filter @nop-chaos/flux-renderers-form-advanced typecheck`.
- Plan `388` is now honestly `partially completed`: the in-scope code and focused proof are complete, but repo-wide closure gates and the independent closure audit still need to be re-run before it can move to `completed`.

### 2026-05-20 (plan 389 - runtime lifecycle/debug fidelity execution slice)

- Executed the retained runtime fidelity items in `docs/plans/389-deep-audit-2026-05-19-runtime-async-listener-and-stale-promise-fidelity-plan.md`: `packages/flux-runtime/src/action-scope.ts` now performs namespace-provider cleanup through both `dispose?.()` and fallback `release?.()`, and `packages/flux-core/src/types/actions.ts` now exposes optional `release?(): void` so runtime-owned providers can advertise that cleanup surface explicitly.
- Completed the stale-request lifecycle fix in `packages/flux-runtime/src/async-data/request-runtime.ts`: the executor now evicts aborted stale active requests before `ignore-new` reuse and always detaches parent `AbortSignal` listeners in the request `finally` path after normal settlement.
- Locked the remaining proof gaps with focused regressions in `packages/flux-runtime/src/__tests__/action-scope-and-adaptor.test.ts`, `packages/flux-runtime/src/__tests__/request-runtime.executor.test.ts`, and `packages/flux-runtime/src/__tests__/async-governance.test.ts`; these now explicitly prove provider fallback cleanup, parent abort-listener cleanup, stale aborted request eviction, and async-governance debug summaries retaining error `stack` plus nested `cause`.
- Focused verification passed: `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/action-scope-and-adaptor.test.ts src/__tests__/async-governance.test.ts src/__tests__/request-runtime.executor.test.ts` (`3` files / `30` tests) and `pnpm --filter @nop-chaos/flux-runtime typecheck`.
- Plan `389` is now honestly `partially completed`: the in-scope code and focused proof are complete, but repo-wide closure gates and the independent closure audit still need to be re-run before it can move to `completed`.

### 2026-05-20 (business AI practice article)

- Added `docs/articles/ai-practices-for-business-application-development.md`, a short article explaining which `nop-chaos-flux` AI-development practices ordinary business applications can borrow without adopting the full heavyweight framework/project governance model.
- Key framing: business projects should reuse lightweight owner docs, real-path completion evidence, implementation/verification separation, focused test-proof review, small plans for complex changes, logs, and limited adversarial review; they should not mechanically copy full deep-audit and successor-plan machinery.
- Revised the article after review to clarify the distinction between ordinary harness and repo-truth practice, show concrete log and plan examples, distinguish plan-driven convergence from spec-first development, and explain why design docs should capture durable semantic constraints rather than volatile implementation details.
- Reworked the article again after external spec-driven AI workflow review (`github/spec-kit`, Claude Code workflows, GitHub/VS Code Copilot customization, and Aider conventions) plus an independent subagent review of this repository's practice. The article now contrasts spec-blueprint development with Flux's code-as-implementation-truth / owner-doc-as-semantic-truth model, and documents how plans, logs, sparse comments, audit scripts, audit prompts, hard gates, suspect scanners, and closure audits form a non-duplicative convergence structure.
- Added the missing institutional practices to the article: `AGENTS.md` turns daily logs and verification into default agent behavior, logs are one file per day under `docs/logs/{year}/{month}-{day}.md`, plan completion requires independent closure audit evidence, `docs/testing/` records manual test issues that can feed E2E/regression coverage, and `docs/bugs/` preserves complex defect diagnostics and future-refactor guardrails.
- Renamed the article framing to `从 Spec-Driven Development 到 Attractor-Guided Engineering`, added a concise `nop-chaos-flux` project introduction, and included the repository URL `https://gitee.com/canonical-entropy/nop-chaos-flux` at the end.
- Reworked the article for the real internal-sharing context: restored the original `从 Spec-Driven Development 到 Attractor-Guided Engineering` title, kept the upfront `nop-chaos-flux` project introduction, removed the invented order-cancel/refund scenario, and reframed the article around the observed misunderstanding that AGE is not just writing fuller specs for task execution.
- Added the user's repeated file-in/file-out practice to the article: messy inputs should first become demand/analysis/plan files and be referenced in OpenCode with `@`, while important outputs should be written into classified `docs/` locations instead of being left in the chat window.
- Sharpened the contrast with task-oriented AI development: task cycles tend to separate docs from code, drift code style over time, leave global rules implicit, and degrade code quality during evolution, while `nop-chaos-flux` has kept module boundaries, docs categories, tests, audits, and code quality improving through persistent repo memory and attractor constraints.
- Reorganized the article again around one explicit concept chain: spec-driven improves one task's input but can still become a task-dispatch system; file-in/file-out persists conversational context into repo memory; classified docs assign each fact to one truth source; closure gates and audits keep those truth sources aligned over time.
- Ran focused logic/style review `ses_1bd0ca0a0ffepH1naQB2uRp4yG`, which returned `acceptable` with no blocking findings; applied non-blocking polish to replace half-English `live defect` / `follow-up` wording and clarify `Testing issues` as the manual testing issue pool.
- Re-audited `C:/can/nop/OpenSpec` with subagents `ses_1bd015a08ffepYow84WMPO5n3F` and `ses_1bd0159b8ffeXFw18825fQuDhW`; corrected the article so OpenSpec is no longer treated as a task-development strawman but as a behavior-spec evolution workflow with `openspec/specs/`, `openspec/changes/`, delta specs, optional `/opsx:sync`, archive history, and expanded `/opsx:verify`.
- Updated the AGE comparison accordingly: OpenSpec now represents behavior specification evolution, while AGE is framed as the additional multi-truth-source alignment layer across owner docs, plans, logs, bugs, testing issues, tests, and audit rules; follow-up review `ses_1bcf9a56affejFpR4XsfN6GSg2` found one remaining `/opsx:verify` wording issue, which was corrected.
- Final OpenSpec accuracy check `ses_1bcf7caddffeHDIUUGQIIidYtQ` passed with no blocking findings.
- Superseded the earlier WeChat-reader draft/review cycle (`ses_1bd1e0ad1ffedz5ySUemvzexCE`, `ses_1bd1bc8c8ffespbBVaJgCI73MD`) because that discarded draft changed the requested title and used an invented order-cancel/refund scenario; those reviews are retained only as history and are not acceptance evidence for the current article.

### 2026-05-20 (AI tone review skill rewrite)

- Rewrote `docs/skills/ai-tone-and-filler-review.md` to remove its own checklist/template flavor and make it a sharper editing tool for killing filler, chat residue, AI-safe phrasing, fake-force language, over-even structure, pseudo-rebellious tone, and over-safe hedging without flattening strong prose.
- Ran independent adversarial review `ses_1bd2e9eeeffeRVLbHS4fmNnhYP`, which returned `needs revision`; addressed the blocking findings by adding structure-level AI smell checks, pseudo-rebellious tone handling, over-safety checks, evidence-based review output, and non-engineering examples.
- Ran independent second review `ses_1bd2cd3d9ffeBDwgm1LvcZSARO`, which returned `acceptable` with no blocking findings; applied its non-blocking output-format polish so review items now include `原句 -> 问题 -> 原因 -> 建议改法`.
- Applied the rewritten skill to `docs/articles/ai-practices-for-business-application-development.md`: compressed the article from an even 15-section explanatory shape into a sharper article structure, removed filler and posture words, preserved the core AGE distinctions, and ran independent tone review `ses_1bd2524b3ffeLRNlY7c9e2Ldh2`, which returned `acceptable` with no blocking findings.
- Added explicit translationese checks to the skill and reusable prompt: reviewers now flag English word order, noun-string stacking, unnecessary half-English, passive-heavy phrasing, and unnatural Chinese such as `这种开发方式并不一定马上坏`.

### 2026-05-20 (plan 391 / 406 closure)

- Executed `docs/plans/391-deep-audit-2026-05-19-report-field-panel-contract-plan.md` to completion and closed `R02-03` / `R04-02` plus retained field-panel contract items `06-01`, `10-01`, `11-01`.
- Restored the live report field-panel contract in `packages/report-designer-renderers/src/report-field-panel.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.tsx`, `packages/report-designer-renderers/src/page-renderer.tsx`, and `packages/report-designer-renderers/src/report-spreadsheet-canvas.tsx`.
- Key fixes:
  - default report-designer left panel now uses the live `ReportFieldPanel` surface instead of the old static-list fallback
  - standalone/public field rows keep drag behavior without faux button semantics and preserve a dedicated keyboard-accessible insert button
  - `report-field-panel` renderer now limits keyboard insert to supported `cell | range` targets and emits canonical `dropFieldToTarget` payloads with `type`
  - spreadsheet canvas now accepts canonical field drag payloads from `dataTransfer` while remaining compatible with existing legacy test state
- Added focused regression coverage in `packages/report-designer-renderers/src/report-field-panel.test.tsx`, `packages/report-designer-renderers/src/field-panel-renderer.test.tsx`, and `packages/report-designer-renderers/src/page-renderer.test.tsx`.
- Re-audited owner docs `docs/components/report-field-panel/design.md` and `docs/architecture/report-designer/design.md`; no text change was needed because they already described the restored live contract.
- Closed `docs/plans/406-open-ended-adversarial-review-2026-05-19-25-round-remediation-routing-plan.md` after verifying all routed owner plans (`391`, `400`, `407`, `408`, `409`, `410`, `411`, `412`) were honestly completed.
- Independent closure audits for `391` and `406` both returned `Verdict: acceptable` / `Findings: none`.

**Full-green verification (2026-05-20):**

- `pnpm --filter @nop-chaos/report-designer-renderers typecheck`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers build`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers lint`: passed
- `pnpm --filter @nop-chaos/report-designer-renderers test`: 14/14 files, 146/146 tests passed
- `pnpm typecheck`: 49/49 successful tasks
- `pnpm build`: 26/26 successful tasks
- `pnpm lint`: 26/26 successful tasks
- `pnpm test`: 49/49 successful tasks

### 2026-05-20 (plan 415 - React 19 redundant memoization cleanup)

- Executed `docs/plans/415-react19-redundant-memoization-cleanup-plan.md`: removed redundant manual `React.memo`, `useCallback`, and `useMemo` calls across the codebase where React Compiler (enabled at error level) auto-memoizes.
- Successfully cleaned up leaf renderers and utility components: `flux-code-editor`, `flux-renderers-data` (chart, crud), `flux-renderers-form` (field-handlers, field-presentation, fieldset), `flux-renderers-basic` (loop, recurse), `nop-debugger`, `spreadsheet-renderers` (sheet-tab-bar, spreadsheet-grid), `ui` (field, json-viewer, slider), `word-editor-renderers` (outline-panel), and `flux-react/workbench` (extracted `resolveWorkbenchGridCols` to module-level pure function).
- **Key finding**: many scanner-flagged sites require manual memoization for correctness, not just performance. Reverted removals in core infrastructure:
  - `schema-renderer.tsx` — stateful singletons (`runtime`, `page`, `ownedSurfaceRuntime`) must not be recreated per render
  - `dialog-host.tsx` — `handleClose` and `surfaceContext` need stable references for dialog/drawer lifecycle
  - `node-renderer.tsx` — `memo()`, `mountedCid`, import scope allocation require stable identities
  - `form.tsx` — form runtime is a stateful singleton
  - `array-field.tsx` / `object-field.tsx` — items, validation owners, projected values used in `useEffect`/`useLayoutEffect` deps
  - `designer-page-body.tsx` and all `flow-designer-renderers` — interdependent provider/registration chains
  - `report-designer-renderers` — stateful core/bridge objects
  - `spreadsheet-renderers/page-renderer.tsx` — stateful `spreadsheetCore`/`spreadsheetBridge`
  - `apps/playground` — cascading `useCallback` dependency chains
  - `table-renderer` files — performance table tests failed
- Kept `useMemo` for Context.Provider values (carousel, dialog, drawer, toggle-group, chart) to satisfy `react/jsx-no-constructed-context-values` lint rule.
- Kept `useCallback` in carousel and sidebar-context for `react-hooks/exhaustive-deps` compliance.
- Phase 3 (derived-state-in-effect, 13 candidates) deferred for individual analysis.
- Scanner still reports 224 candidates — all in "kept for correctness" categories (reverted packages, lint-required, context-provider, or derived-state-in-effect).
- Net reduction: `-209` lines across 22 source files. 224 scanner candidates remain in deferred/reverted categories.
- Plan 415 closed as `completed` after scope change: Phase 3 (derived-state-in-effect, 13 candidates) moved to successor plan. All 46 checkboxes `[x]`. Second independent closure audit (ses_1bdae720fffeHLPBBZLooKrWhY): `acceptable`.

**Full-green verification (2026-05-20):**

- `pnpm typecheck`: 49/49 successful tasks
- `pnpm build`: 26/26 successful tasks
- `pnpm lint`: 26/26 successful tasks
- `pnpm test`: 49/49 successful tasks

Commits:

- `29580bda` chore: add React 19 redundant memoization scanner and plan 415
- `2507654b` refactor: remove redundant memo/useCallback/useMemo where React Compiler handles it
- `852edd5b` docs: update skills, plans, and daily log for React 19 memoization review
- `1a13c031` test: update import-stack test and e2e tests for React 19 changes
- `743e37f4` docs: update plan 415 with execution results and deferred items

### 2026-05-20 (scanner and skills updates)

- Created `scripts/audit/find-react19-optimization-candidates.mjs` with 3 rule buckets: `redundant-react-memo`, `redundant-use-callback`, `redundant-use-memo`, `derived-state-in-effect`.
- Added `react19OptimizationRules` to `scripts/audit/rules.mjs` with `hasUseNoMemoDirective()` exclusion.
- Registered `check:audit-react19-optimization-candidates` in root `package.json`.
- Updated `docs/skills/react19-best-practices-review.md` with "React Compiler 自动记忆化" section.
- Updated `docs/skills/deep-audit-prompts.md` and `docs/skills/open-ended-adversarial-review-prompt.md` with React 19 best practices references.
