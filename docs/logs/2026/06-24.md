# Development Log - 2026-06-24

### 2026-06-24

- Closed plan `docs/plans/2026-06-24-0040-1-w1b-feedback-family-content-package-bootstrap-plan.md` (status → `completed`): 首次落地 NEW 包 `@nop-chaos/flux-renderers-content` + W1b 容器与反馈组 5 个 renderer（separator/spinner/progress/empty/card），解锁 W1a/W2a-content/W3c/W4a。
- **Phase 1 包骨架**: `packages/flux-renderers-content/{package.json,tsconfig.json,tsconfig.build.json,vitest.config.ts,src/index.ts,src/styles.css}`（deps flux-core/flux-i18n/flux-react/@nop-chaos/ui；`./styles.css`→`./dist/styles.css` 导出）；`vite.workspace-alias.ts` 双别名；根 `tsconfig.json` project ref；`apps/playground` 加 workspace dep。
- **Phase 2 5 renderer**: `src/{separator,spinner,progress,empty,card}.tsx` + `schemas.ts` + `test-support.ts`，均消费 `RendererComponentProps`（读 props.props/meta/regions/events；用 `resolveRendererSlotContent`/`hasRendererSlotContent`；无直接 store 访问）；marker `nop-separator`/`nop-spinner`/`nop-progress`/`nop-empty`/`nop-card`；复用 `@nop-chaos/ui` Separator/Spinner/Progress/Empty*/Card*。`progress.tsx` 导出纯函数 `normalizeProgressValue`（value>max 归一化不溢出，5 个专属单测）。`spinner.visible` 走 frozen META_FIELDS `meta` 通道（`field-binding-and-renderer-contract.md` 一致）。
- **Phase 3 注册+演示+e2e+doc 同步**: `content-renderer-definitions.ts` 声明 5 个 `RendererDefinition`（separator/spinner/progress/empty category `content`、card category `layout`；card header/body/footer/actions region + onClick event；empty title/description value-or-region + actions region）；`src/index.ts` 导出组件 + `contentRendererDefinitions` + `registerContentRenderers`；playground App.tsx 注册 + 新增 `w1b-content-feedback-demo.tsx` + route `w1b-content`；`styles.css` 加 separator 取向尺寸 + progress 变体主题；e2e `tests/e2e/w1b-feedback-family.spec.ts`（5 程序化断言，不靠截图）；5 份 design.md §3 归属 `flux-renderers-basic`→`flux-renderers-content`；`amis-baseline-matrix.md` 5 组件 `targetContract`→`runtime`；roadmap W1b `planned`→`done`。
- 全量 full-green 验证：`pnpm typecheck` 53/53、`pnpm build` 28/28、`pnpm lint` 28/28、`pnpm test` 53/53（content 42 单测 + playground 88 单测）；e2e 5/5。
- 独立 fresh-session closure audit `approved`（task `ses_10a757996ffeM8YJPAVsKbxI8O`）：零 Blocker/Major；5 项审计全 PASS（包集成、契约、注册、deferred 诚实、interface-vs-semantics）；判定 category `display/container`→`content/layout` 映射为唯一正确选择（代码库无 display/container 类目）。
- Non-blocking follow-ups（未阻断 closure）：progress 圆形/仪表盘变体、card 展开/选中状态机、spinner/skeleton 语义边界文档化。

---

- **MM-17 (Decision: correct doc)**: `docs/components/notice-bar/design.md` §2 决策表/§3/§4/§5/字段分类 — removed phantom `body`/`icon` region claims; `icon?: IconSchema` → `icon?: string` (lucide name), matching `schemas.ts:103` (`kind:'prop'`), `notice-bar.tsx:41-42` (`resolveLucideIconStrict`). No `body` region exists; `body` row moved to 不采纳.
- **MM-18 (Decision: fix doc)**: `notice-bar/design.md §6` reconciled with the NEW-MM-06 `styles.css:24-31` comment — now states package-local fixed hsl literals + `--nop-notice-bar-*` override vars, NOT derived from shared theme tokens.
- **MM-19 (Decision: correct doc)**: `infinite-scroll/design.md §4` deleted the non-existent scroll-math fallback; replaced with an honest IntersectionObserver precondition (v1 ships no fallback; all evergreen browsers have IO).
- **MM-20 (Decision: implement)**: `packages/flux-renderers-mobile/src/infinite-scroll.tsx` — added `findScrollableAncestor()` (walks `parentElement` chain, matches `getComputedStyle(ancestor).overflowY === 'auto'|'scroll'`); observer effect now sets `observerOptions.root = scrollableAncestor` when found, omits `root` (viewport, prior behavior) otherwise. `design.md §5` "容器滚动 vs 视口滚动" row updated to describe the implemented auto-detect. +2 focused tests (one asserts `options.root === scrollableAncestor`, which fails against pre-fix code; one asserts viewport fallback when no scrollable ancestor).
- **MM-21 (Decision: fix code)**: `packages/flux-renderers-mobile/src/pull-refresh.tsx` — resting rebound `transition` changed from `ease` to `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (matching `pull-refresh/design.md §5:94` and `swipe-cell.tsx:11`). +1 focused test asserting the cubic-bezier in the resting `transition` style.
- Mobile package tests 154→157 (+3 regression proofs). Full workspace `pnpm typecheck` (51/51) / `pnpm build` (27/27) / `pnpm lint` (27/27) / `pnpm test` (51/51) green.
- Independent fresh-session closure audit `approved` (task `ses_10ab5115dffes9d5EuijsdmHtz`): all 9 Closure Gates re-verified against live repo; scope confirmed via git diff (7 files; countdown/swipe-cell/pull-refresh design docs correctly untouched); no "Proposed vs Current" residuals.
- Updated `docs/components/mobile-roadmap.md` M5 with the plan-7 design-doc-reconciliation note.

---

- Closed plan `docs/plans/2026-06-23-2235-1-mobile-runtime-correctness-and-test-rigor-plan.md` (status → `completed`): 18:24 mobile re-audit runtime-correctness + missing-test findings NOT owned by sibling 2031-1.
- **Countdown wall-clock unification (MM-14, OA-21)**: `packages/flux-renderers-mobile/src/countdown.tsx` — replaced the branched tick (`targetTime - Date.now()` / subtractive `prev - interval`) with a unified wall-clock derivation `remaining = max(0, remainingAtStart - (Date.now() - startTimestamp))`, anchored via `remainingRef`/`startTimestampRef`/`remainingAtStartRef` on every start/resume/config-change. Effect: `targetTime` pause/resume no longer swallows the pause window (was 50→39 jump); `time` mode no longer drifts under throttled `setInterval`. MA-16 clamp / OA-13 reset / MA-02 finish-dispatch / MM-08 autoStart contracts preserved.
- **MM-22 doc reconciliation**: `docs/components/countdown/design.md` §6 now honestly states `setInterval` + wall-clock (rAF deferred to §11 as smoothness-only); §6↔§11 no longer contradict.
- **MM-15**: `notice-bar.tsx` carousel effect adds `if (!visible) return;` + `visible` dep → no 3s `setCurrentIndex` churn after close.
- **MM-16**: `infinite-scroll.tsx` in-flight guard now releases only on actual `loading`/`error` change (`prevLoadingErrorRef`) → React 19 StrictMode double-setup no longer double-dispatches `onLoadMore`; OA-16/MA-13 preserved.
- **MM-25**: `infinite-scroll.tsx` retry `<Button>` forwards `disabled` → honest WCAG 4.1.2 operability (was a dead button under `disabled + error`).
- **MM-23 / MM-24**: added countdown + infinite-scroll StrictMode dispatch tests; locked `direction:'right'→'normal'` and `'left'`/default→`'reverse'` marquee branches.
- **OA-22**: Decision A (lock mapping, public schema stable) — `notice-bar/design.md` + code comment make the "value names the inverse of motion direction" semantics explicit.
- Mobile package tests 147→154 (+7 behavior tests: throttled-tick, targetTime pause, StrictMode×2, close-no-churn, direction×2, disabled+error). Full workspace `pnpm typecheck` (51/51) / `pnpm build` / `pnpm lint` (27/27) / `pnpm test` (51/51) green.
- Independent fresh-session closure audit `approved` (task `ses_10acae81fffebNY4S17eaUulNr`): zero Blocker/Major; all 5 regression proofs verified to fail pre-fix; contracts preserved.
- Updated `docs/components/mobile-roadmap.md` M5 with the plan-6 remediation note.
- Next: real-device CDP touch-emulation + `getComputedStyle` verification successor (deferred, `out-of-scope improvement`) pairs with sibling 2031-1's OA-18/OA-20 device follow-up.
