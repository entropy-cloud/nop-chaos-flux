# Development Log - 2026-05-27

### 2026-05-27

- Full-green verification is recorded for the current branch after the theme-token, UI surface, Flow Designer, and Report Designer interaction updates. `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test`, and `pnpm exec playwright test` are all green on this snapshot.
- `packages/theme-tokens/src/{index.ts,styles.css}` and `packages/tailwind-preset/src/index.ts` now expose host token extension helpers plus shared surface/background tokens so host applications can extend the theme contract without forking the baseline preset.
- Shared UI surfaces now consume the tokenized surface contract instead of hardcoded overlays/backgrounds: `packages/ui/src/components/ui/{alert-dialog.tsx,dialog.tsx,drawer.tsx,sheet.tsx,slider.tsx,table-row-class-name.ts,radio-group.tsx,textarea.tsx}` with matching regression coverage in the colocated tests and `packages/ui/src/components/ui/theme-contract.test.tsx`.
- Spreadsheet and report-designer interaction fixes remain verified on the live page path: `packages/spreadsheet-renderers/src/{spreadsheet-grid.tsx,spreadsheet-toolbar.tsx,canvas-styles.css,spreadsheet-grid/inline-controls.tsx}` keep toolbar actions on one row, remove the extra toolbar cell editor, preserve sticky headers, and restore Excel-like inline editing focus/keyboard behavior. Proof lives in `packages/spreadsheet-renderers/src/__tests__/grid-selection.test.tsx`, `apps/playground/src/pages/report-designer-demo.test.tsx`, and `tests/e2e/report-designer-demo.spec.ts`.
- Follow-up test hardening was required before the branch could be committed: `packages/flow-designer-renderers/src/designer-controls.test.tsx` now uses a non-`eval` template resolver for toolbar text assertions, and `packages/flux-renderers-basic/src/__tests__/basic-dynamic-renderer.test.tsx` now keeps `fetcher` mocks compatible with the generic `RendererEnv['fetcher']` contract while still asserting request history.
