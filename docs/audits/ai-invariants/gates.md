# AI Engine 不变式门禁清单（Gates Registry）

> Status: active（Cycle 1 / I1 产出 + I4 扩展 + Cycle 2 / I1 追加 ⑥-⑩ + Cycle 2 / I4 补强 ⑥⑦⑨⑩②④ + 2026-08-10 双审计 P1 追加 ⑪ 新族 + ⑩ dangling 成员 + ④ fan-out 源 + 2026-08-10 双审计 P2 追加 ⑧ break/throw 扩面 + 注册红锚点行号校准（multi P2-16，2026-08-10 live 反查），棘轮单调追加登记处）
> Last Updated: 2026-08-10
> Source: `docs/audits/ai-invariants/invariant-catalog.md`（I0 不变式目录 + I4 §7 扩展 + Cycle 2 §9 + I4 §10 + P1 §11 + P2 ⑧ 扩面）+ plan `docs/plans/2026-08-09-1826-2-i1-invariant-gate-sedimentation.md` + plan `docs/plans/2026-08-09-2007-2-cycle1-i4-fix-execution.md`（②③④⑤ 补强）+ plan `docs/plans/2026-08-09-2229-2-cycle2-i1-invariant-sedimentation.md`（⑥-⑩ 第二批门禁）+ plan `docs/plans/2026-08-10-0925-2-cycle2-i4-fix-execution.md`（13 条 K 修复 + 门禁补强 + 注册红清零）+ plan `docs/plans/2026-08-10-1301-1-engine-adapter-p1-remediation.md`（⑩ dangling 成员 + ⑪ 新族 + ④ fan-out 源）+ plan `docs/plans/2026-08-10-1606-1-engine-adapter-p2-remediation.md`（⑧ break/throw 扩面 + 运行时成员 +3）
> 下游消费: I2（门禁运行审计）、I5（全量验证）、Loop Rule（新族派生 → 追加新门禁到此）

## 门禁清单

| 不变式                                                              | 覆盖方法                                                                                                                                                                                                                                                                                                                                                                                                                                            | 检测方式                                                                                                                                                                                                                                                                                                         | 运行命令                                                                                                                                                                 | 棘轮状态                                                |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| ① 异步变更入口 `isProcessing` 守卫                                  | `sendMessage`/`send`/`regenerate`（异步）+ `clear`/`setMessages`（同步 in-flight）                                                                                                                                                                                                                                                                                                                                                                  | 参数化穷举测试（方法表驱动）                                                                                                                                                                                                                                                                                     | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/engine/__tests__/engine-invariants.test.ts`                                                              | 基线绿（536 tests 含不变式套件）                        |
| ② await 后状态读取用 `activeIdRef`/`conversationsRef`（非闭包捕获） | `switchConversation`/`deleteConversation`（adapter 异步方法）+ **变更方法 sync 读取（I4/K4：create/rename/delete/clearAll 全方法，首语句豁免）** + **镜像写面（I4 Cycle 2：create/rename/delete/clearAll 全部同步写 `conversationsRef`；switch 入口 exists 检查读镜像）**                                                                                                                                                                           | 参数化测试 + 静态扫描器（`scanPostAwaitClosureReads` + `scanAdapterSyncClosureReads` + **`scanMirrorWriteSurface`（Cycle 2 / I4 新增）**）                                                                                                                                                                       | 同上 + `pnpm check:ai-engine-invariants`                                                                                                                                 | 基线零命中（Cycle 2 / I4 补强后 live 零命中）           |
| ③ catch/finally controller 身份守卫                                 | `runTurn`/`runOnce` 的 catch/finally + **成功路径完成 mutate（I4/K1：`requestState='completed'` recipe）**                                                                                                                                                                                                                                                                                                                                          | 参数化测试（abort→send race）+ 静态扫描器（`scanControllerIdentityGuard` + `scanCompletionIdentityGuard`）                                                                                                                                                                                                       | 同上 + `pnpm check:ai-engine-invariants`                                                                                                                                 | 基线零命中                                              |
| ④ storage 变更经 `reportStorageError`                               | `createConversation`/`renameConversation`/`deleteConversation`/`clearAll`（adapter storage 调用）+ **save-after-delete/clearAll 时序守卫（I4/K3：排空→删除顺序）** + **元数据写入排空链（I4 Cycle 2：create/rename 的 saveConversation 链入 `pendingSavesRef` + settlement-time 镜像再校验）** + **clearAll fan-out 源（P1-3/P1-4：枚举 `engineCache.keys()` ∪ `pendingSavesRef` ∪ `conversationsRef` 全集 + saveMessages settlement 镜像再检查）** | 参数化测试（mock rejection → onStorageError；**时序守卫 = 运行时参数化测试，扫描器不扩展**；**元数据排空臂 = 运行时参数化测试（create 与 rename 两臂）**；**fan-out 源 = 运行时参数化测试 ×2 + 静态扫描器 `scanClearAllFanOutSource`**）                                                                         | 同上 + `pnpm check:ai-engine-invariants`                                                                                                                                 | 基线零命中                                              |
| ⑤ abort 路径清理 controller                                         | `abort()`（同步复位）+ `deleteConversation`/`clearAll`（adapter in-flight engine abort）+ **abort 强制终结在途 generator（I4/K2：`activeGenerator` + 每迭代 abort 检查，纯行为不静态化）**                                                                                                                                                                                                                                                          | 参数化测试（含 signal-ignoring 有限 yield connector）                                                                                                                                                                                                                                                            | 同上                                                                                                                                                                     | 基线绿                                                  |
| ⑥ active 位移完整性（N1）                                           | `createConversation`/`deleteConversation`/`clearAll`（位移面，全部必须 bump `switchVersionRef` + 重置 `switchTargetRef`）+ `switchConversation`（post-await 提升/复水以 ref/version 裁决 + **id-aware guard（同 id 重 switch 不丢 hydration）** + 入口 exists 检查读镜像 + delete active 后 build-on-demand）+ **bootstrap/delete-fixup build-on-demand（I4 Cycle 2：`ensureEngineAndHydrate`，建引擎 + loadMessages + id-aware version guard）**   | 参数化测试（`conversation-invariants-cycle2.test.ts` Invariant ⑥ 块 **8 成员全 `it`（I4 翻转 + 3 新成员）**）+ 静态扫描器（`scanDisplacementVersionBumps`）                                                                                                                                                      | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/adapters/__tests__/conversation-invariants-cycle2.test.ts` + `pnpm check:ai-engine-invariants`           | **全绿（I4 修复翻转 + 注册红清零，live 零命中）**       |
| ⑦ storage bootstrap 列表合并（N2）                                  | mount bootstrap（`use-conversation.ts` `loadConversations` post-await `setConversations`）                                                                                                                                                                                                                                                                                                                                                          | 参数化测试（**3 成员全 `it`：create 合并 ×2 + clearAll 守卫 ×1（I4 翻转 + 新成员）**，`conversation-invariants-cycle2.test.ts`）；**不静态化**（functional updater 为行为面，静态误报高）                                                                                                                        | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/adapters/__tests__/conversation-invariants-cycle2.test.ts`                                               | **全绿（I4 修复翻转 + clearAll 成员）**                 |
| ⑧ branch 戳消费/清除（N3）                                          | `runTurn` 提前退出路径（`pendingBranchId` 不得泄漏：提前 return + **abort while-head break + onTurnStart 抛错 catch（multi P2-1 扩面）**）；`runOnce` 消费面                                                                                                                                                                                                                                                                                        | 参数化测试（**泄漏成员 4 全 `it`：connector-missing + break 臂 + throw 臂 + regenerate 序列臂（multi P2-1 追加 3）+ 消费控制 1**）+ 静态扫描器（`scanBranchStampReset` **三条规则：return / break / catch-clear（multi P2-1 扩面）**）                                                                           | 同上 + `pnpm check:ai-engine-invariants`                                                                                                                                 | **全绿（I4 修复翻转 + 注册红清零 + multi P2-1 扩面）**  |
| ⑨ plugin 错误隔离（N4）                                             | `runTurn`/`runOnce` 的 plugin hook 调用面（onTurnStart/onBeforeRequest/onError/onAfterRequest/onTurnEnd）                                                                                                                                                                                                                                                                                                                                           | 参数化测试（**4 成员全 `it`：3 注册成员翻转 + abort 变体成员（I4 新增）**，`engine-invariants.test.ts`）；**不静态化**（plugin 回调交错为行为面，静态误报高）                                                                                                                                                    | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/engine/__tests__/engine-invariants.test.ts`                                                              | **全绿（I4 修复翻转 + abort 变体成员）**                |
| ⑩ 失败轮产物清理（N5）                                              | `runOnce` catch/终态提交（失败/中止/退化轮产物提交面）+ `buildContext`（请求历史排除面）+ autoSave 快照面 + **dangling tool_calls 清理面（P1-2：runOnce abort 分支 / tool-no-executor / abort-mid-executor 返回 × buildContext 投影 × autoSave 臂；strip 保留文本 / drop 空内容 / 部分配对仅 strip 无配对条目）**                                                                                                                                   | 参数化测试（**6 成员全 `it`：排除翻转 + 正常轮控制 + 4 新成员（I4）**，`engine-invariants-i4.test.ts` + `conversation-invariants-i4.test.ts` autoSave 臂）+ **dangling 成员 5 + autoSave 臂（P1-2，`engine-invariants-i4.test.ts` + `conversation-invariants-i4.test.ts`）**；**不静态化**（行为面，静态误报高） | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/engine/__tests__/engine-invariants-i4.test.ts src/adapters/__tests__/conversation-invariants-i4.test.ts` | **全绿（I4 修复翻转 + 4 新成员 + P1-2 dangling 成员）** |
| ⑪ plugin ctx 写隔离 + 请求载荷白名单（open P1-1 + P1-5，新族）      | `buildContext`（全部 plugin hook 的 `ctx.request.messages` 隔离面 + `AiConnectorRequest.messages` wire 投影面）                                                                                                                                                                                                                                                                                                                                     | 参数化测试（**5 hook 穷举 `ctx.request.messages !== engine.getState().messages` + push 不写穿 + payload 元素隔离 + P1-5 白名单双用例**，`engine-invariants-p1.test.ts`）；**不静态化**（数组隔离行为面，静态误报高）                                                                                             | `pnpm --filter @nop-chaos/flux-renderers-ai exec vitest run src/engine/__tests__/engine-invariants-p1.test.ts`                                                           | **全绿（P1 修复后 8 成员全 `it`）**                     |

## 表完备性门禁

| 断言面                              | 机制                                                                                         | 运行位置                                                  |
| ----------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| engine 公共成员集 ⊆ 测试表 ∪ 白名单 | `Object.keys(createMessageEngine())` 运行时枚举                                              | `engine-invariants.test.ts` Table completeness gate       |
| adapter 公共成员集 ⊆ 测试表         | `renderHook(useConversation)` 返回值函数字段                                                 | `conversation-invariants.test.ts` Table completeness gate |
| 白名单                              | `getState`/`subscribe`/`setConnector`/`registerPlugin`/`getMessages`（非变更，不写会话状态） | 同上                                                      |
| `runTurn` 不入表                    | 内部管道，非 `MessageEngine` 公共成员（I0 §4.2 裁定）                                        | 同上                                                      |

**新增/重命名公共变更方法 → 运行时枚举发现新键但不在表/白名单 → 测试红。** 这是「治反应式盲区」的核心机制——不再依赖人工记住把新方法加入测试。

## 静态扫描器

| 命令                              | 覆盖不变式          | 扫描目标                                                                                      |
| --------------------------------- | ------------------- | --------------------------------------------------------------------------------------------- |
| `pnpm check:ai-engine-invariants` | ②③④⑥⑧（静态可检测） | `packages/flux-renderers-ai/src/engine/create-engine.ts` + `src/adapters/use-conversation.ts` |

- 纯行为不变式（①⑤ + I4 K2 强制终结 + I4 K3 时序守卫 + **⑦⑨⑩（Cycle 2：functional merge / plugin 回调交错 / buildContext 排除谓词均为行为面）** + **④ 元数据排空臂（Cycle 2 / I4：运行时时序）** + **⑪（P1：plugin ctx 数组隔离为行为面）**）不静态化（误报率高，运行时参数化测试已穷举覆盖；K3 时序守卫静态误报理由记录于 invariant-catalog §7.3；⑦⑨⑩ 理由记录于 invariant-catalog §9；④ 排空臂理由记录于 invariant-catalog §10.5；⑪ 理由记录于 invariant-catalog §11.2）。
- **Cycle 2 / I4 新增静态规则**：`scanMirrorWriteSurface`（② 镜像写面——create/rename/delete/clearAll 函数体必须含 `conversationsRef.current =` 写，缺失即违例；K-K4/②-1/2 镜像写缺失静态可检性评估落地为 yes，扫描形状对齐 `scanDisplacementVersionBumps`）。
- **P1（2026-08-10）新增静态规则**：`scanClearAllFanOutSource`（④ fan-out 源——clearAll 函数体必须含 `conversationsRef.current.map(` 枚举，缺失即违例；P1-3/P1-4 未打开/逐出会话逃逸清空静态可检性评估落地为 yes，扫描形状对齐 `scanMirrorWriteSurface`）。
- **Cycle 2 注册红（⑧ 静态可行性已验证）**：`scanBranchStampReset` 静态化成立——`pendingBranchId`/`runTurn`/`runOnce` 同属 `createMessageEngine` 闭包作用域（create-engine.ts），同函数扫描 sound；豁免面 = `isProcessing` 入口早退（设戳路径不可达：regenerate 设戳前自有 isProcessing 守卫 + runTurn 在设戳后同步调用），connector-missing 早退（`create-engine.ts:219-237`，live 校准）为扫描目标——**I4 修复（早退前清戳）后 live 零命中**。
- **multi P2-1（2026-08-10）⑧ 扩面**：`scanBranchStampReset` 从「仅 `return;`」扩展到三条规则——(1) 提前 `return;` 路径前缀必须含 `pendingBranchId` 清除（原规则）；(2) `break;` 早退（abort while-head break）路径前缀必须含清除（loop-head clear 或 break 前置 clear；tool-loop-max break 在运行时位于首轮 runOnce 消费之后，前缀清除保守满足）；(3) 含 pre-runOnce plugin await（onTurnStart/onBeforeRequest）的 try 区域，其 catch 必须清除（throw 路径）。棘轮单调：规则只增不减。committed 回归 fixture +4（⑧ break 违规/清洁 + ⑧ throw 违规/清洁，`scripts/__tests__/find-ai-engine-invariant-violations.test.ts` 13→17 用例）。
- committed 回归：`scripts/__tests__/find-ai-engine-invariant-violations.test.ts`（**17 用例**：清洁 fixture（含 K1 守卫 + K4 豁免 + ⑥ bump + ⑧ 清除样例）exit 0 + ④/③/③-K1/②/②-K4/**⑥/⑧** 违规 fixture 各 exit 1 + **⑧ 清洁 fixture** exit 0 + **② 镜像写面违规/清洁 fixture ×2（I4 追加）** + **④ fan-out 源违规/清洁 fixture ×2（P1 追加）** + **⑧ break/throw 违规/清洁 fixture ×4（multi P2-1 追加）**）。
- `FLUX_AUDIT_SCAN_ROOT` env 覆盖扫描根（对齐 `find-event-dispatch-without-ctx.mjs` 先例）。

## 注册红（本 plan 显式登记，非 CI 新增）

> Cycle 2 / I1 沉淀 ⑥-⑩ 门禁时，live 代码仍违反 ⑥⑧（N1-N5 全部已复现 RED）——静态扫描器对 live 的预期命中在此显式注册。**Cycle 2 / I4（2026-08-10）已全部清零：`check:ai-engine-invariants`（扩展后）live 零命中**（验证见 plan `2026-08-10-0925-2` Phase 5 + `pnpm check` 收口轮）。**注册命中行号已按 multi P2-16 校准（2026-08-10 live 反查）——锚点随代码演化漂移，以 live 复核为准。**

| 规则                                | 注册命中（live 行号）                                                                                | 状态                                                                     |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `scanDisplacementVersionBumps`（⑥） | 3：`use-conversation.ts:401`（createConversation）/ `:510`（deleteConversation）/ `:629`（clearAll） | **已清零（2026-08-10，create/delete/clearAll bump `switchVersionRef`）** |
| `scanBranchStampReset`（⑧）         | 1：`create-engine.ts:228`（connector-missing 早退；live 校准 `:219`）                                | **已清零（2026-08-10，早退前清 `pendingBranchId`）**                     |

## 棘轮规则

- 门禁**只增不减**：弱化或移除任一门禁需人工确认（在 plan 中记录裁决理由）。
- 新不变式族由 Loop Rule 派生（I2 审计发现新族 → Cycle 2 / I1 追加新门禁到此）。
- 表完备性门禁白名单的新增同样需裁决（在 catalog §4 记录理由）。
