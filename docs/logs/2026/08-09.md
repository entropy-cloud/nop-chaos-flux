# Development Log - 2026-08-09

### 2026-08-09（plan 2026-08-09-0121-2 industrial SCADA P2 polish — runtime lifecycle & symbols author safety 执行）

- **plan `2026-08-09-0121-2`（roadmap Follow-up Backlog runtime+symbols 层 P2 收口轮）执行完成**——收口 1712 两份 deep-audit 的 14 个 P2 findings（runtime engine/binding/renderer-hooks + symbols 层），2 Workstream 全交付。源：roadmap Follow-up Backlog（显式预期的「后续 polish 轮」）。与 plan `{0121-1}`（e2e healing）独立可并行。
  - **Workstream A（Engine 生命周期 + Renderer Hooks + Binding 资源安全）9 项**：
    - **本轮-2**（`engine/scada-engine.ts`）：`interactionOverlay` getter 加 `if (this.destroyed) return undefined` 早退，防销毁后惰性重建 overlay 到已 destroy 的 app。
    - **本轮-3**（`renderer/scada-canvas.tsx`）：engineRef/runtimeRef 同步 effect 加 cleanup（`return () => { engineRef/runtimeRef.current = null/undefined }`），闭合本轮-2 可达链（unmount 后异步闭包不再触达死引擎）。
    - **本轮-1**（React Compiler 纪律对齐）：逐处裁定 scada-canvas.tsx 的 useCallback/useMemo——**移除 4 处纯优化 memo**（handleReady/handleError/reportDiagnostic/getPointValuesForLatest，消费者均经 latest-ref 转发，身份不进 effect deps）；**保留 3 处**（useMemo(parsedConfig) 身份流入 config-sync/points-bridge effect deps；handleDestroyed/reloadConfig 进 useScadaHandles effect deps）。hooks 内部 memo 链（eventsApi/reportOnce 等）裁定保留——身份流入公共返回或 effect deps，**test 环境无 React Compiler**（vitest.shared 不走 babel/react-compiler，仅 playground vite 走）故移除有 churn 风险。
    - **本轮-10**（`hooks/use-scada-config-sync.ts`）：config 身份未变时 effect 早退（`config === prevRef.current` → return），消除 reloadBindings→setRuntime(新对象身份)→effect 再跑空 diff 的冗余第二轮。
    - **本轮-13**（`hooks/use-scada-points-bridge.ts`）：compiledCache/lastReportedErrors/pointSnapshot 清理 effect deps 增 `expressionCompiler`（旧编译产物喂新 evaluator 错配）。
    - **F7**（`binding/animator.ts`）：pause 全 active 时 `stopClock`（+ `allPaused` helper）+ tick 跳过 paused 项 collect + 仅 advanced 时续帧，消除 pause 后 rAF 每帧空转重算冻结增量。
    - **F8**（`binding/refresh-pipeline.ts`）：建反向索引 `reverseDeps: Map<depPointId, Set<exprPointId>>`（`setExpressionDeps` 单一入口同步维护 lastDeps + 反向边，撤旧加新），`recomputeExpressionPoints` 由 O(n²) lastDeps 线性扫描降为 O(扇出) 查找。
    - **本轮-4/F9**（`engine/event-bridge.ts` + `binding/point-store.ts`）：去重键加 call-site 维度（event-bridge：`${site}:${message}`，point-store 已含 pointId）+ Set 256 上限（超限整体清空，report-once-until-reset）。
    - **F10**（`serialization/validators/binding.ts`）：`binding.scale` 校验从仅 `isPlainObject` 升级为与 declaration scale 同形 finite k/b 校验（复用 1809-1 `isFiniteNumber` via assertShape；expression-scale 分支校验非空字符串）。
  - **Workstream B（Symbols 作者安全 + 几何 Polish）5 项**：
    - **本轮-5**（`symbols/visual-state.ts`）：revert 仲裁的 `instanceBindings` 从读 raw 实例改为合并 `defaults.bindings ∪ instance.bindings`，防自定义符号 defaults 级 binding 经 revert 被覆盖。
    - **本轮-6**（`symbols/base-shapes/round-rect.ts` + `symbol-types.ts` + `config-types.ts` + `diff.ts`）：cornerRadius 从固定 8 改为 `min(w,h)*0.08`（100×100 仍 8 向后兼容）+ per-instance `cornerRadius` override（钳到 min(w,h)/2）；同步 ScadaSymbolProps/ScadaSymbolNode/SYMBOL_KEYS（过 check-scada-symbol-keys guard）。
    - **本轮-7**（`symbols/pipe/pipe-junction.ts`）：bidirectional 连线补 `startArrow`（旧仅 endArrow，与 'out' 同形语义误导）。
    - **本轮-8**（`symbols/composite.ts`）：`createCompositeGroup` 空 children 守卫——console.warn 结构化上报 + fallback 透明 Rect body（visible:false），防 `body=undefined` 致 applyCompositeProps BODY_FIELDS 路由 setAttrs(undefined) 崩溃。
    - **本轮-9**（`symbols/composite.ts`）：Group 节点 attrs 仅承接 root 级字段（x/y/rotation/visible/opacity/scale），过滤 width/height/fill/stroke/strokeWidth/shadow/text/fillStyle（无渲染意义，写 width/height 改变 leafer bounds 语义）；re-grep 确认 A1 P0 around:'center' 修复未触碰 toShapeAttrs。
  - **focused 测（每 Fix 配，断言正确结果非 not-throw）**：animator F7×2（pause 停时钟 + partial pause 不停）、refresh-pipeline F8（4 级链 + 50 无关点只重算受影响子集）、event-bridge/point-store 本轮-4/F9×3（异 site/pointId 不互吞 + 256 cap reset）、binding F10（NaN/Infinity/expression 非空 + 合法 linear/expression）、symbols round-rect 本轮-6（缩放 + override + cap）、pipe-junction 本轮-7（双向箭头）、composite 本轮-8/9×2（空 children fallback + Group attrs）、engine 本轮-2（destroyed getter）、visual-state 本轮-5（defaults binding revert 保留）、config-sync 本轮-10（同身份不重跑）、points-bridge 本轮-13（compiler 换身份清 cache）。2 处旧测断言对齐新行为（round-rect cornerRadius 50×50→4 / 60×40→3.2）。
- **验证**：`pnpm typecheck` 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32（check-scada-symbol-keys guard passed；1 预存 scheduling warning 非阻塞）· `pnpm --filter @nop-chaos/flux-renderers-industrial test` **1409 tests** 零回归（1394 baseline + 15 new）/ coverage 96.56%（branches 90.1%+，过 90% threshold）· `pnpm test` workspace **59/59 任务**全绿。
- **plan 收口**：`2026-08-09-0121-2` Plan Status `active` → `completed`，2 Workstream status + items + Exit Criteria 全勾；Closure Gates 全勾（含 closure-audit）。roadmap Follow-up Backlog 增「runtime+symbols polish 轮执行进度」块登记 14 findings ✅ + 剩余 P2（F5/F6/F11/本轮-11·12/P2-1～P2-11）留待 editor+contract polish plan。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤省略。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 104 files / 1409 tests，1394 baseline + 15 new，零回归）。
- **Closure audit PASS**：独立子 agent fresh session `ses_01d30f469ffe5eiZOIzZ0mkj03`，pass-with-minors，0 Blocker / 0 Major / 1 Minor（m-1 本轮-1 scope 缩窄至 canvas 级 4 处 useCallback，hooks 内部 memo 链裁定保留并记为 follow-up；非正确性缺口，认可文档化判断）。
- **successor**：no remaining plan-owned work（14/14 findings fix landed + focused 测 landed）。剩余 ~16 editor/serialization/export/docs 层 P2 留待后续 mission-driver 轮的 editor+contract polish plan（与本 plan Non-Goals 一致）。

### 2026-08-09（plan 2026-08-09-0121-1 industrial SCADA e2e residual healing 执行）

- **plan `2026-08-09-0121-1`（HCA-CV deferred e2e successor + 1809-3 P1-5 e2e 验证延期收口）执行完成**——重跑 6 scada e2e spec，诊断 + healing HCA-CV 显式「Successor Required: yes」的 8 个 e2e 残留（7 hover/click + 1 perf guard），3 Phase 全交付。源：HCA-CV plan `2026-08-08-1527-1` Deferred #1/#2 + 1809-3 plan P1-5 e2e 验证延期。
  - **Phase 1（baseline re-verification）**：重跑 scada-demo / scada-edge-cases / scada-perf。P1-5 landed 后 baseline：demo 11 pass / 4 fail（click pump-1 @world 350,278、dblclick motor-1 @136,134、hover pump-1 ×2）；edge-cases 4 pass / 2 fail（hover line @200,240、hover polygon @480,180）；perf :155 TE-1 fail（pointer-drag viewport 未变）。已知 8 失败点中 P1-5 先行修好 1（demo 5→4），baseline 见 7 fail。
  - **Phase 2（diagnose & fix）**：经 page.evaluate / elementFromPoint / getComputedStyle / 临时 console.error 探针程序化诊断（**零截图**，守 AGENTS.md e2e 诊断纪律），定位**两个独立根因**，落地 2 Fix + 1 failing-first 回归测试：
    - **Fix-1（核心）：ready 态占位 DIV 拦截 pointer 事件**（`renderer/scada-canvas.tsx:328`）。ready 分支渲染 `<div class="nop-scada-canvas-canvas" />`，leafer 也在同 container 创建 `<canvas>`（marker effect 同加该 class）；React 在 leafer mount 后重渲染该 DIV 使其叠在 leafer canvas 之上 → 拦截 pointermove/tap → EventBridge 永不收事件 → hover/click overlay/dialog/navigate 全失效。证据：`elementFromPoint(pump 目标)` 返回该 DIV（非 leafer canvas）；handlePointerMove 不触发；即便 viewport settle 后 overlay 仍 `{has:false,n:0}`。Fix：ready 占位 DIV 加 `pointer-events-none`，事件穿透到 leafer sky 层（leafer canvas 同名 class 不受影响）。**此 Fix 同时修好 scada-perf:155 TE-1**（pointer-drag 现达 leafer → zoomLayer 变 → guard pass）。
    - **Fix-2：width/height prop effect 把 schema 尺寸当 DOM 尺寸**（`renderer/hooks/use-scada-engine.ts` width/height effect）。旧 `latest.current.width ?? container.clientWidth` 把 schema width（960，world design space）当 leafer DOM 尺寸 → mount 期 setSize(960,520) 覆盖构造器 container 尺寸(303) → applyInitialViewportState 用错配 size(960) fit → scale≈1，ready 在 ResizeObserver 修正前触发 → pointer 落画布外。证据（探针）：ctor containerWidth=303→size{303,544}，紧接 effect setSize{960,520} 覆盖，fit size{960,520}→scale 1。Fix：container 优先（container-driven DOM sizing，P1-5「container 真实尺寸」），schema 仅 container=0（jsdom）fallback；setSize 后调 `refitViewportOnResize`（与 ResizeObserver handler 对称）。
    - **Fix-1 + Fix-2 合计消减 5/7**：demo click/dblclick/hover×2（4）+ perf :155 TE-1（1）。demo 15/15、perf 5/5。
    - **failing-first 回归测试**：`tests/e2e/scada-pointer-events-regression.spec.ts` 断言 ready 占位 DIV `pointer-events:none` 且 elementFromPoint 不落在该 DIV（防 Fix-1 静默回退）。
  - **Phase 3（full suite + triage）**：6 scada e2e spec 全量 + 回归 spec = **32 passed / 2 failed**（2 watch-only residual，零 unexplained、零 regression：pressure/editor 全绿；industrial unit 1394/1394；typecheck/build/lint 32/32）。scada-perf:155 裁定视口相关，随 Fix-1 收口（非 perf 包络退化）。
  - **残留 2 诚实裁定（Deferred But Adjudicated R1/R2，证据可复现）**：①**R1 edge line**（geometric，EP-3 类）— container-driven 后 edge canvas DOM=1214×288，line-poly content bounds 480×120 fit height-binding scale 2.4，line（content 底沿 world y=240）映射 screen y=288=canvas height 贴底边 sub-pixel 离屏；DIAG probe 实测 `line-overlay-after-settle=0`（geometric 非时序）；根因在 edge 页面布局（canvas 容器仅 288 高），out-of-scope。②**R2 edge polygon**（leafer 渲染时序）— screen-switch reset rebuild scene 后 leafer 渲染需一帧，ready 信号在渲染前触发，test 单次 mouse.move at ready 被 miss + 静止鼠标不重触发 hover → overlay 永不出现；DIAG probe 实测 `polygon-overlay-after-settle=1`（时序非几何）；demo fresh mount 因 mount 链路更长 test 动作前 leafer 已渲染故命中。
- **验证**：`pnpm typecheck` 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32（industrial）· `pnpm --filter @nop-chaos/flux-renderers-industrial test` **1394 tests** 零回归 · 6 scada e2e 32 pass / 2 watch-only residual。fit padding 实验（修 R1）全面 regressed perf+empty+default-geometry，已 revert（证据记录于 plan）。
- **plan 收口**：`2026-08-09-0121-1` Plan Status `active` → `completed`，3 Phase status + Exit Criteria + Closure Gates 全勾（closure-audit gate 留 [ ]——执行 session 不自审，待后续独立 fresh-session closure-audit 覆盖）。roadmap HCA-CV 行 + header 登记 e2e successor 执行（5/7 消减）；project-context.md C0 baseline 补 industrial scada e2e 残留收口记录。无 `> Source Audits:` 行（plan 无此 label），source audit closure 步骤省略；plan `> Work Item: e2e-residual-healing` 是 HCA-CV deferred successor（roadmap 无独立 ❌/✅ 行，经 HCA-CV 行 + header 注记 tracking）。
- **successor**：no remaining plan-owned work（5/7 fixed landed；2 watch-only residual Successor Required: no）。可选（非义务）：①playground edge 页面给 canvas 容器显式高度 ≥ schema（治 R1，playground 布局 mission）；②engine lifecycle reset 后 gate ready on leafer render（治 R2，独立 concern）。

### 2026-08-09

- Closed plan `docs/plans/2026-08-08-1910-3-industrial-scada-viewport-guard-meta-contract.md`（A3 + A4，second-wave remediation 第二波第 3 卡）。
- **A3 零缩放守卫自毁收口**（`packages/flux-renderers-industrial/src/engine/scada-engine.ts`）：`readZoomLayerScale` 显式拒 0（`&& scaleX !== 0` → 回退 `this.viewport.scale`），闭合 P2-8 早退守卫的「自毁环路」（守卫检测到 0 调 `syncViewportFromZoomLayer` 再读回 0 落地 `viewport.scale=0` → `viewportToWorld` 除 0 产 Infinity → EventBridge world 坐标 corrupt → zoomLayer 矩阵不可逆）；`handlePluginMove` 补与 `handlePluginZoom` 同形零守卫（defense-in-depth）。
- **A4 runtime scada-canvas 遵守 meta 契约**（`packages/flux-renderers-industrial/src/renderer/scada-canvas.tsx`）：与同包编辑器画布 `scada-editor-canvas.tsx:217` 同纪律——disabled 时不下挂图元事件回调（EventBridge 闭包经 optional chaining 早退 → dispatch 不执行、hover 覆盖物不跟随）+ wrapper `aria-disabled`/`inert`；visible:false 时 renderer 层 return null（不创建引擎）。
- 新增 failing-first 7 测：`engine/scada-engine-plugin-sync.test.ts`（+2，zoom/move scaleX=0 → viewport.scale 非零有限）+ `renderer/scada-canvas-meta-contract.test.tsx`（新文件，5 测：disabled 不派发 + sanity 派发 + aria-disabled/inert + sanity 无 aria + visible:false 不渲染）。
- Owner doc 同步：`docs/components/industrial-hmi/design-engine.md` §4.4（A3 增补）+ `docs/components/industrial-hmi/design-renderer.md` §5（A4 契约增补）；roadmap `docs/backlog/industrial-hmi-component-audit-roadmap.md` 第二波执行进度登记 1910-3 ✅ completed。
- **Full-green verification**：typecheck 32/32、build 32/32、lint 32/32、test 59/59 tasks（industrial 104 files / 1394 tests，1387 baseline + 7 new，零回归）。
- Closure audit PASS：独立子 agent fresh session `ses_01da66e23ffeKtPSwMpflIsUTB`，0 Blocker / 0 Major / 0 Minor，一轮共识。
