## UI family class 嵌套重复清理（Plan 2026-09-09-0001）

- 起因：nop-chaos-next F4 规则 `.nop-table { background }` 意外命中 tr/td（`695c5f539` 批量给家族内所有元素挂了同一 `nop-*` class），unlayered 规则压过 Tailwind hover 导致行 hover 失效回归。
- 修复：28 个 ui 组件文件、139 处 family class 从嵌套内部件移除（保留每个家族的渲染根与并列根，如 Trigger/Content 并列、Kbd/KbdGroup 双根）；`data-slot` 体系不变。
- 消费面复核：两仓元素级选择器仅 `.nop-table`（data-bordered 只在容器）与 host `.nop-field`（渲染器体系，无关）；JS 查询仅 `.nop-debugger`（独立体系）。
- 顺手修复 pre-existing i18n 债务：补齐 `flux.picker.remove`、`flux.validation.format/integer/url` 定义（zh-CN + en-US），`check-i18n-keys` 全绿。
- 验证：`pnpm build` 39/39、`pnpm lint` 39/39、`pnpm test` 72/72；ui 包 42 files / 169 tests 全绿。
- 同步：`sync-flux-lib.sh` + tgz 更新至 nop-chaos-next（注意：sync 为镜像式覆盖，host 侧 flux-lib/ui 的独有文件需在同步后用 git 恢复，详见 host 侧日志）。
- Closure audit：待独立子 agent 复核后关闭 plan。
