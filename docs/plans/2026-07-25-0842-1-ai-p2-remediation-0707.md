# AI-0707 P2 Remediation — Code, Interaction & Doc Consistency

> Plan Status: completed
> Last Reviewed: 2026-07-25
> Source: `docs/components/roadmap-ai.md` → Follow-up Backlog（`2026-07-25-0707` 批）；`docs/audits/2026-07-25-0707-multi-audit-ai.md`（P2-1..P2-6）；`docs/audits/2026-07-25-0707-open-audit-ai.md`（P2-1..P2-3）
> Mission: ai
> Work Item: 0707 P2 follow-up backlog
> Related: `docs/plans/2026-07-25-0730-1-ai-p1-remediation.md`（同批 P1 已收口）；`docs/plans/2026-07-25-0117-1-ai-p2-code-and-behavior-remediation-2151.md`（前序 P2 代码批范式）

## Purpose

收口 `flux-renderers-ai` 包在 `2026-07-25-0707` 两份审计中残留的全部 P2 代码/行为/文档项（7 条），使两份源审计可标记为 `closed`，并消除该批次在 roadmap follow-up backlog 的最后一批未勾选 `- [ ]`。

## Current Baseline

> 已核对 live repo（2026-07-25）。下列每条均为当前 repo 可观测的未修复状态。

- **A1–A6 全部 done**；`roadmap-ai.md` Phase Status 顶部已全绿。本计划不触及任何 work item，仅收口 follow-up backlog。
- **0707 批 P1 已收口**：`docs/plans/2026-07-25-0730-1-ai-p1-remediation.md`（4 条，`completed`，closure-audit `pass`）；两份源 0707 审计的 P1 项已 closed。剩余仅为本计划的 7 条 P2。
- **multi-audit P2-1（live）**：`packages/flux-renderers-ai/src/renderers/ai-bubble/user-edit.tsx:106` 编辑（铅笔）按钮 `aria-label={t('flux.ai.copy')}`，语义为"复制"而非"编辑"。`flux.ai.copy` 定义于 `packages/flux-i18n/src/locales/en-US.ts:55` 与 `zh-CN.ts`（`ai:` block）。
- **multi-audit P2-2（live）**：`packages/flux-renderers-ai/src/renderers/ai-bubble/renderers/error.tsx:33` 重试按钮 `ctx?.sendMessage(lastUserText);` 为裸 promise，与 `ai-sender.tsx:70`（`void`）/`ai-attachments.tsx:178`（`await`）的事件派发约定不一致；返回的 floating promise 在 sendMessage reject 时产生未处理 rejection。该 renderer 目前无单测（`renderers/ai-bubble/renderers/__tests__/` 不存在）。
- **multi-audit P2-3（live）**：`packages/flux-renderers-ai/src/schemas.ts` 仍声明 7 个无消费者的 schema TYPE 字段：`conversationId`(L21)、`onSend`(L67)、`itemRegion`(L98)、`avatarRegion`(L109)、`actions`(L132, SchemaInput)、`menuItems`(L212)、`trigger`(L409)。核对结果：`conversationId`/`onSend`/`itemRegion`/`avatarRegion`/`menuItems` 在 `src/renderers/**` 零消费；`trigger`(L409, popover) 仅被一个测试 `Probe` 的本地 prop 使用（与 schema 字段无关）；`actions` 出现两处——L132（SchemaInput，疑似 sender/旧字段，dead）与 L249（SchemaValue，`ai-feedback.tsx:21` 经 `resolved.actions` 消费，live），**必须逐字段确认无消费后再删，`actions` 仅删 L132 dead 一处、保留 L249 live 一处**。注：对应的 field-metadata 已在 2151 批从 `ai-renderer-definitions.ts` 移除（✅），本项仅收口 TS interface 侧残留。`contract-honesty.test.ts` 已有 dead-field guard（`DEAD_FIELDS` 正则 `groupStrategy|dividerRole|maxGroupSize`，L130），可作为扩展范式。
- **multi-audit P2-4（live）**：`packages/flux-renderers-ai/src/rich-text/components/suggestion-popup.tsx:44`（`'Mentions' | 'Slash commands'`）、`:65`（`'Close suggestions'`）、`template-bar.tsx:22`（`'Insert template'`）共 4 处硬编码英文 aria-label，未走 i18n。
- **multi-audit P2-5（live）**：`docs/components/roadmap-ai.md:282` Renderer Coverage 表 `ai-message-list` 行仍写「消息列表（分组、自动滚动、注册制渲染）」，与 `:113`（已移除「分组」）、`design.md §74`、`renderers.md:95`、`contract-honesty.test.ts:130` guard 均矛盾。（审计原文记 L253，当前文件实际为 L282，属行号漂移，缺陷实体一致。）
- **multi-audit P2-6（live）**：`docs/references/terminology.md:535` 称 `ReactMessageAdapter`「Publicly exported from `flux-renderers-ai/src/index.ts`」；实际 `packages/flux-renderers-ai/src/index.ts:95` 仅导出工厂 `createReactMessageAdapter`，`ReactMessageAdapter` 为 module-local（未再导出）。`:531` 起的 `## ReactMessageAdapter` 段整体把它当作公开导出类来叙述。
- **open-audit P2-2（live）**：`packages/flux-renderers-ai/src/renderers/ai-voice-input.tsx:146-162`：`onerror` 分支在 `no-speech`（L151）与其余错误（L153）均 `fireError('no-result')`，随后 `onend`（L160）在 `!gotFinal` 时再 `fireError('no-result')` 一次 → 单次 `no-speech` 失败会话 `no-result` 被 emit 两次（onerror + onend）。已有测试 `renderers/__tests__/ai-voice-input.test.tsx:163` 断言 `onend` 路径 emit 一次，但未覆盖 onerror→onend 双触发去重。
- **i18n 落点**：新 key 需同时加到 `packages/flux-i18n/src/locales/en-US.ts`（`ai:` block L47+）与 `zh-CN.ts`（`ai:` block L48+）。

## Goals

- 7 条 P2 全部修复并有 focused proof（移除项附回归零失败证据）。
- 两份 0707 源审计的 P2 项可标 `closed`；`roadmap-ai.md` follow-up backlog 该批 7 个 `- [ ]` 全部转 `- [x]`。
- 全量 `pnpm typecheck && pnpm build && pnpm lint && pnpm test` 绿。

## Non-Goals

- 不做 `ai-bubble` 编辑态从组件 `useState` 迁移到引擎 `message.state.editing` 的 setter（2151 批 P2-4 已裁定为 deferred successor / watch-only，超 P2 polish 范围）。
- 不重构 `ai-voice-input` 的 `onerror` 分类语义（仅去重 `no-result` 双 emit，不改变各 reason 的含义）。
- 不审计/收口 `2026-07-25-0117-2-...` 计划的 closure-audit gate（属另一计划义务）。
- 不回写已 `completed` 的历史计划文本（Minimum Rule 21）。
- open-audit P2-3（ai-citations sanitize DROP marker）不另立 scope——随 `0730-1` P1-2 sanitizeHtml 双编码修复的 Proof（含 `<script>x[1]</script> plain [2]` 断言）一并验证，本计划不含该项。

## Scope

### In Scope

- multi-audit P2-1：`user-edit` 编辑按钮 aria-label 改用新增 `flux.ai.editMessage` key。
- multi-audit P2-2：`error.tsx` 重试改 `void ctx?.sendMessage(lastUserText);`。
- multi-audit P2-3：移除 `schemas.ts` 7 个 dead TYPE 字段（`actions` 仅删 L132 dead 处）。
- multi-audit P2-4：`suggestion-popup` / `template-bar` 4 处 aria-label 走新增 i18n key。
- multi-audit P2-5：修 `roadmap-ai.md:282` Renderer Coverage「分组」措辞。
- multi-audit P2-6：修 `terminology.md:531-537` `ReactMessageAdapter` 公共导出误述。
- open-audit P2-2：`ai-voice-input` `no-result` 双 emit 去重。

### Out Of Scope

- 编辑态引擎迁移（见 Non-Goals）。
- voice `onerror` reason 语义重构。
- 其它历史审计批次（1757/2157）的遗留 gate。

## Failure Paths

> 本计划涉及错误传播一致性（P2-2）与交互降级（open-audit P2-2），故填写。

| 场景编号              | 触发                                      | 行为                                                                                   | 可重试 | 用户可见表现                             |
| --------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------- | ------ | ---------------------------------------- |
| `retry-unhandled-rej` | 流式失败后点 Retry，connector 再次 reject | `void` 前缀使 promise 不浮空；reject 由既有 error 路径收口，不产生 unhandled rejection | 是     | 错误气泡仍显示，控制台无未处理 rejection |
| `voice-no-result-dup` | `no-speech` 错误后 `onend` 触发           | `onerror` 已置 `errorAlreadyFired` 门，`onend` 不重复 emit `no-result`                 | 是     | host 仅收到一次 `voice-no-result` hint   |

## Test Strategy

档位选择：`建议有测`

本档选择：建议有测。理由：均为 P2 polish（a11y/i18n、错误传播一致性、死字段清理、doc rot），非鉴权/对外 API 契约/核心回归路径；但 P2-2 与 open-audit P2-2 属行为正确性、P2-1/P2-4 属 a11y 回归点，需 focused proof 锁定；P2-3 死字段移除需回归证据防回潮。

## Execution Plan

### Phase 1 — a11y / i18n polish（P2-1, P2-4）

Status: completed
Targets: `packages/flux-i18n/src/locales/en-US.ts`、`packages/flux-i18n/src/locales/zh-CN.ts`、`packages/flux-renderers-ai/src/renderers/ai-bubble/user-edit.tsx`、`packages/flux-renderers-ai/src/rich-text/components/suggestion-popup.tsx`、`packages/flux-renderers-ai/src/rich-text/components/template-bar.tsx`、`packages/flux-renderers-ai/src/renderers/__tests__/p2-a11y-i18n.test.tsx`

- Item Types: `Fix | Proof`

- [x] **Fix**：在 `en-US.ts` / `zh-CN.ts` 的 `ai:` block 新增 key——`editMessage`（如 `Edit message` / `编辑消息`）、`mentions`（`Mentions`/`提及`）、`slashCommands`（`Slash commands`/`斜杠命令`）、`closeSuggestions`（`Close suggestions`/`关闭建议`）、`insertTemplate`（`Insert template`/`插入模板`）。
- [x] **Fix**：`user-edit.tsx:106` 改 `aria-label={t('flux.ai.editMessage')}`（不再复用 `copy`）。
- [x] **Fix**：`suggestion-popup.tsx:44` 改 `aria-label={t(kind === 'mention' ? 'flux.ai.mentions' : 'flux.ai.slashCommands')}`；`:65` 改 `aria-label={t('flux.ai.closeSuggestions')}`。
- [x] **Fix**：`template-bar.tsx:22` 改 `aria-label={t('flux.ai.insertTemplate')}`。
- [x] **Proof**：扩展 `p2-a11y-i18n.test.tsx`——新增 `user-edit` 编辑按钮 aria-label 命中 `editMessage`（非 `copy`）断言；新增覆盖 `rich-text/components/`（suggestion-popup mention/slash/close + template-bar insert）的 aria-label 命中翻译值断言。

Exit Criteria:

- [x] `user-edit.tsx` 铅笔按钮 aria-label 为 `t('flux.ai.editMessage')`（grep `flux.ai.copy` 在 user-edit 内 0 hit）。
- [x] suggestion-popup / template-bar 4 处 aria-label 均来自 `t()`，无硬编码英文字面量。
- [x] `en-US.ts` 与 `zh-CN.ts` 的 `ai:` block 均含 5 个新 key（两文件 key 集合一致）。
- [x] `p2-a11y-i18n.test.tsx` 新增用例聚焦断言上述 aria-label，且 `pnpm --filter @nop-chaos/flux-renderers-ai test -- p2-a11y-i18n` 绿。

### Phase 2 — 错误传播 / 交互正确性（P2-2, open-audit P2-2）

Status: completed
Targets: `packages/flux-renderers-ai/src/renderers/ai-bubble/renderers/error.tsx`、`packages/flux-renderers-ai/src/renderers/ai-voice-input.tsx`、`packages/flux-renderers-ai/src/renderers/__tests__/ai-voice-input.test.tsx`、（新增）error renderer focused test

- Item Types: `Fix | Proof`

- [x] **Fix**：`error.tsx:33` 改 `void ctx?.sendMessage(lastUserText);`（与 `ai-sender.tsx` 事件派发约定对齐，消除 floating promise）。
- [x] **Fix**：`ai-voice-input.tsx` 引入 `errorAlreadyFired` 门（`onerror` 内 `fireError` 时置位；`onend` 的 `no-result` 仅在 `!errorAlreadyFired` 时 emit）。保留 `onend` 作为「无任何 error、纯无结果」的唯一合法 emit 源。
- [x] **Proof**：扩展 `ai-voice-input.test.tsx`——新增用例：`onerror(no-speech)` 后 `onend` 触发，`onError` 对 `reason:'no-result'` 仅被调用一次（断言 `toHaveBeenCalledTimes(1)`）。
- [x] **Proof**：为 `error.tsx` 重试路径新增 focused test（建 `renderers/ai-bubble/renderers/__tests__/error.test.tsx` 或并入相邻现有测试）——断言点 Retry 时 `ctx.sendMessage` 被 `void` 包裹调用（不返回 thenable、不抛 unhandled rejection），且派发 retry event。

Exit Criteria:

- [x] `error.tsx:33` retry 为 `void ctx?.sendMessage(...)`（grep `sendMessage(lastUserText)` 前缀为 `void`）。
- [x] `ai-voice-input.tsx` `onend` 的 `fireError('no-result')` 受 `errorAlreadyFired` 门控（双触发路径仅 emit 一次）。
- [x] voice 双触发去重新增用例与 error retry focused test 均存在且绿（`pnpm --filter @nop-chaos/flux-renderers-ai test`）。

### Phase 3 — 公共面诚实（P2-3）

Status: completed
Targets: `packages/flux-renderers-ai/src/schemas.ts`、`packages/flux-renderers-ai/src/__tests__/contract-honesty.test.ts`

- Item Types: `Fix | Proof`

- [x] **Decision**：逐字段确认无消费后移除：`conversationId`(L21)、`onSend`(L67)、`itemRegion`(L98)、`avatarRegion`(L109)、`actions`(L132 SchemaInput dead 处，**保留 L249 ai-feedback live `actions?: SchemaValue`**)、`menuItems`(L212)、`trigger`(L409)。移除前对每个字段跑 `rg` 确认 `src/renderers/**` 与 `src/rich-text/**` 零消费。
- [x] **Fix**：从 `schemas.ts` 对应 interface 删除上述 dead 字段声明及其 JSDoc（若有）。
- [x] **Proof**：扩展 `contract-honesty.test.ts`——新增 `schemas.ts` 专属断言确认不再声明 `conversationId|onSend|itemRegion|avatarRegion|menuItems|trigger`（`actions` 因 L249 live 保留，不入此断言）。**注意 collision**：既有 `DEAD_FIELDS` 正则同时扫描 `schemas.ts` + `ai-message-list.tsx` + 另一文件；`ai-message-list.tsx:40` 有无关的局部 `const trigger`，因此 `trigger` 必须用 **schemas.ts-only** 扫描（新断言限定源文件），**不可**并入会扫到 `ai-message-list.tsx` 的共享正则。`actions` 单独断言「sender 相关 interface 不再声明 `actions?: SchemaInput`」。

Exit Criteria:

- [x] `schemas.ts` 不再声明 `conversationId`/`onSend`/`itemRegion`/`avatarRegion`/`menuItems`/`trigger`；`actions` 仅保留 `ai-feedback` 的 `SchemaValue` 一处。
- [x] `contract-honesty.test.ts` dead-field guard 覆盖新增字段，绿。
- [x] 全部 renderer/rich-text 既有测试零回归（`pnpm --filter @nop-chaos/flux-renderers-ai test` 绿）。

### Phase 4 — 文档 / 契约 rot（P2-5, P2-6）

Status: completed
Targets: `docs/components/roadmap-ai.md`、`docs/references/terminology.md`、`docs/components/flux-renderers-ai/renderers.md`（核对一致性）

- Item Types: `Fix`

- [x] **Fix**：`roadmap-ai.md:282`（Renderer Coverage 表 `ai-message-list` 行）将「消息列表（分组、自动滚动、注册制渲染）」改为「消息列表（自动滚动、注册制渲染、A-8 虚拟滚动）」，与 `:113`/`design.md §74`/`renderers.md:95`/`contract-honesty.test.ts:130` 对齐。
- [x] **Fix**：`terminology.md:535` 删除「Publicly exported from `flux-renderers-ai/src/index.ts`」误述（或改为如实描述：仅工厂 `createReactMessageAdapter` 为公共导出，`ReactMessageAdapter` 类型为 module-local）；复查 `:531-537` 整段叙述不暗示其为公共导出类。
- [x] **Fix**（owner-doc 一致性）：将本批 roadmap follow-up backlog 7 个 `- [ ]` 转为 `- [x]` 并附本 plan 引用（在 Phase 收口、Closure Gate 通过后执行）。

Exit Criteria:

- [x] `roadmap-ai.md` Renderer Coverage `ai-message-list` 行不再含「分组」。
- [x] `terminology.md` `## ReactMessageAdapter` 段不再声称该类型为 `index.ts` 公共导出（与 `index.ts:95` 实际一致）。
- [x] 两份 0707 源审计 P2 项可标 `closed`（roadmap follow-up backlog 7 条勾选）。

## Draft Review Record

> 起草后、执行前的独立审查证据。详见 guide 的 `Plan Review Rule`。由独立子 agent（fresh session）填写。

- Reviewer / Agent: independent general sub-agent（fresh session `ses_069449755ffe7uDwrO4dqt0jWK`，不复用起草者上下文）
- Verdict: `pass-with-minors`
- Rounds: 1
- Findings addressed: 零 Blocker / 零 Major。19 条引用逐条核对 live repo 全部 TRUE。2 条 Minor 已在起草者侧修正——(1) `DEAD_FIELDS` 正则内容误引为 `ln|...`，实为 `groupStrategy|dividerRole|maxGroupSize`（L130），已改正；(2) `trigger` 与 `ai-message-list.tsx:40` 局部 `const trigger` collision，Phase 3 Proof 已显式要求 `trigger` 走 schemas.ts-only 扫描、不并入共享 3 文件正则。granularity（7 项同一 audit 批、单 owner plan 4 phase）经核确认为正确粒度，未过度拆分；`actions` L132/L249 拆分处理比 roadmap backlog 更精确；Non-Goals 诚实（编辑态迁移/open-audit P2-3 均非静默丢弃）。

## Closure Gates

- [x] 7 条 P2 全部修复（live defect/contract drift 收敛，非 deferred）。
- [x] 所有新增/扩展 focused proof 存在且绿（P2-1/P2-4 a11y-i18n、P2-2 error retry、open-audit P2-2 voice dedupe、P2-3 dead-field guard）。
- [x] 不存在被静默降级到 deferred / follow-up 的 in-scope live defect 或 contract drift（编辑态迁移、open-audit P2-3 均为本计划外、已显式声明 non-goal）。
- [x] 受影响 owner docs（`roadmap-ai.md`、`terminology.md`）已同步到 live baseline；两份 0707 源审计 P2 标 `closed`。
- [x] 由独立子 agent（fresh session）执行的 closure-audit 已完成并记录证据；执行 session 不得自审勾选本项。
- [x] `pnpm typecheck`（58/58 tasks green）
- [x] `pnpm build`（31/31 tasks green）
- [x] `pnpm lint`（31/31 tasks green）
- [x] `pnpm test`（flux-renderers-ai 57 files / 463 tests green；flux-i18n 26 tests green；`check:i18n-keys` pass）

## Deferred But Adjudicated

> 本计划无新增 deferred 项。以下为来自前序计划、与本计划边界相关的既存 watch-only 项，列出以明确「不在本 scope」。

### ai-bubble 编辑态组件 useState → 引擎迁移

- Classification: `out-of-scope improvement`
- Why Not Blocking Closure: 2151 批 P2-4 已以文档裁定收口（design.md §11.5 如实记录「组件 useState 当前实现 + 虚拟回收限制 + 引擎无 editing-state setter」）；影响仅限 >200 消息虚拟回收场景，属 polish 范畴，非当前 supported baseline 的 contract 缺口。
- Successor Required: `yes`
- Successor Path: 待 future polish plan（非 0707 批）。

## Non-Blocking Follow-ups

- F3.1 残留 6 处手写 `useMemo`/`useCallback`（各带 justification 注释）→ watch-only periodic re-review（非正确性，来自 2151 批）。

## Closure

Status Note: 7 条 P2（multi-audit P2-1..P2-6 + open-audit P2-2）全部修复——P2-1/P2-4 a11y-i18n 5 个新 key 落地 + 4 处 aria-label 走 `t()`；P2-2 `error.tsx` 重试改 `void` 前缀（与 ai-sender/ai-attachments 约定对齐）；open-audit P2-2 voice `onerror`→`onend` 双 emit 经 `errorAlreadyFired` 门去重；P2-3 移除 7 个 dead schema TYPE 字段（`actions` 仅删 sender dead `SchemaInput`、保留 ai-feedback live `SchemaValue`）；P2-5/P2-6 owner-doc rot（roadmap「分组」措辞、terminology `ReactMessageAdapter` 公共导出误述）修正。每条均有 focused proof（p2-a11y-i18n 12 用例、ai-voice-input dedup 2 用例、error-retry focused test 5 用例、contract-honesty dead-field guard 3 用例）。workspace 全量 typecheck(58)/build(31)/lint(31)/test(flux-renderers-ai 463 + flux-i18n 26) + `check:i18n-keys` 全绿。两份 0707 源审计已在 P1 收口批标 `Audit Status: closed`（本批 P2 为其 follow-up，roadmap backlog 7 条已勾选）。

Closure Audit Evidence:

- Auditor / Agent: independent closure-audit sub-agent（fresh session，不复用执行者上下文；opencode closure-audit flow）
- Evidence: 独立审计在 fresh session 内逐条核对 live repo——(1) `user-edit.tsx:106` 实测 `aria-label={t('flux.ai.editMessage')}`，`flux.ai.copy` 在 user-edit 内 0 hit；(2) `suggestion-popup.tsx:45/66` 与 `template-bar.tsx:23` 实测均走 `t()`；(3) `en-US.ts:96-100` + `zh-CN.ts:97-101` 实测均含 5 新 key 且集合一致；(4) `error.tsx:33` 实测 `void ctx?.sendMessage(lastUserText);`；(5) `ai-voice-input.tsx:140` 实测 `errorAlreadyFired` 门、`:165` 实测 `onend` 受 `!errorAlreadyFired` 门控；(6) `schemas.ts:243` 实测仅保留 `AiFeedbackSchema.actions?: SchemaValue`，无 `conversationId/onSend/itemRegion/avatarRegion/menuItems/trigger` 字段声明（`actions` 的 sender dead `SchemaInput` 已删）；(7) `contract-honesty.test.ts:164-193` 实测含 3 条 0707 P2-3 专属断言（schemas.ts-only `trigger` 扫描 + AiSender/AiFeedback `actions` 拆分校验）；(8) `roadmap-ai.md:282` 实测 Renderer Coverage `ai-message-list` 行为「自动滚动、注册制渲染、A-8 虚拟滚动」（无「分组」）；(9) `terminology.md:535` 实测已改为「class itself is module-local (not re-exported from `src/index.ts`); only its factory `createReactMessageAdapter()` is part of the public surface」；(10) `roadmap-ai.md` follow-up backlog 7 条均 `[x]` 且引用本 plan；(11) 两份 0707 源审计均 `> Audit Status: closed`；(12) `docs/logs/2026/07-25.md:5` 收口记录含 4 Phase 摘要 + 全量验证结果。Anti-hollow：voice 门控逻辑、`void` 前缀、dead-field 移除均在 live source 落地（非壳函数）；proof 用例断言具体行为结果（`noResultCalls.length === 1`、`not.toMatch(SCHEMAS_DEAD_FIELDS)`、`not.toMatch(/\bactions\b\s*\??:/)`）。执行证据——`pnpm typecheck` 58/58、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm --filter @nop-chaos/flux-renderers-ai test` 57 files/463 tests、`pnpm --filter @nop-chaos/flux-i18n test` 26 tests、`pnpm check:i18n-keys` pass；新增/扩展 proof 文件：`src/renderers/__tests__/p2-a11y-i18n.test.tsx`（+5 用例）、`src/renderers/__tests__/ai-voice-input.test.tsx`（+2 dedup 用例）、`src/renderers/__tests__/error-retry.test.tsx`（新增 5 用例）、`src/__tests__/contract-honesty.test.ts`（+3 dead-field guard 用例）。五点一致性已核对：Plan Status / 4 Phase Status / 4 Phase Exit Criteria / Closure Gates / `docs/logs/2026/07-25.md` 收口记录全部一致。

Follow-up:

- open-audit P2-3（ai-citations sanitize DROP marker）随 `2026-07-25-0730-1` P1-2 sanitizeHtml 双编码修复的 Proof 一并验证，不在本 scope（已在 Non-Goals 声明）。
- ai-bubble 编辑态组件 `useState` → 引擎迁移（2151 批 P2-4 deferred successor）待 future polish plan。
