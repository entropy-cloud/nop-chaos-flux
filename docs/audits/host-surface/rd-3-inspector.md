# 审计卡：rd-3 inspector（report-designer-renderers，D3.3 面）

> 状态: closed
> 审查日期: 2026-08-08
> 审查 plan: `docs/plans/2026-08-08-1315-2-round2-d33-report-designer-surface-audit.md`
> 面定义: `docs/audits/host-surface/surface-inventory.md` rd-3 | 注册定义: `renderers.tsx:204-222`（report-inspector-shell + report-inspector）| 渲染器: `report-designer-inspector.tsx`（ReportInspectorRenderer）+ `inspector-shell-renderer.tsx`（ReportInspectorShellRenderer）
> 契约基准: `docs/audits/host-surface/README.md` §1 report-designer 行（`inspector-design.md` 承载 inspector 契约：shell 边界/空态规则/宿主 scope/表单提交模型/性能原则/错误与只读策略）

## 面身份

rd-3 inspector 面：`ReportInspectorShellRenderer`（shell：header + 空态/加载/错误三态 + 内嵌 report-inspector）+ `ReportInspectorRenderer`（body 选择：authoredBody → dynamicBody(resolvedSchema) → 空态；noSelection 态）。表单参与：经内嵌 schema 的 form runtime（inspector-design.md §7 表单与提交模型——提交目标只走 action）。布局 or widget：widget。

## 维度审查记录（18 维降维 + Designer 特有维度）

| #   | 维度                        | 结论                                                                                                                                                                                                                                                                                                            | 证据                                                                                     | 发现 |
| --- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---- |
| 1   | Schema 契约                 | report-inspector-shell 注册 fields（title/noSelectionLabel/errorLabel）+ report-inspector（body region/emptyLabel/noSelectionLabel）；shell 将 runtime inspector 状态组装为内嵌 report-inspector schema（type: 'report-inspector'，body = resolvedSchema）                                                      | renderers.tsx:204-222、inspector-shell-renderer.tsx:30-35                                | —    |
| 2   | RendererComponentProps 合规 | 双渲染器均从 props.props/meta/regions/helpers 取数；useOwnScopeSelector（shell: selectionTarget+inspector 引用比较；inspector: selectInspectorSlice + shallowEqual）；无 store 直访、无 ad-hoc context                                                                                                          | inspector-shell-renderer.tsx:20-26、report-designer-inspector.tsx:18-28                  | —    |
| 3   | 值所有权三态                | inspector runtime（open/mode/resolvedSchema/loading/error）为 core 状态（local）；scope 派生投影（designer.inspector + inspector 顶层）；activeMeta 派生（selectionTarget → getTargetMeta）                                                                                                                     | core.ts:57-76、core-dispatch.ts:154-170、host-data.ts:188-205                            | —    |
| 4   | 表单参与                    | 经内嵌 schema 的 form 组件参与（inspector-design.md §7.1 复用 form runtime；§7.2 提交只走 action——updateMeta/replaceMeta 命令）；inspector 自身不建 form 状态（shell 只订阅 active target）                                                                                                                     | inspector-design.md:131-155、inspector-shell-renderer.tsx:64-66                          | —    |
| 5   | DOM 与选择器契约            | data-slot：report-designer-inspector-shell/section-header/empty/stack + nop-report-inspector(nop-)/nop-report-inspector-shell marker 类；无 BEM 死类                                                                                                                                                            | inspector-shell-renderer.tsx:37-68、report-designer-inspector.tsx:37-74                  | —    |
| 6   | 嵌套 schema 分类            | dynamicBody 为内嵌 schema（region 语义）经 helpers.render(pathSuffix 'inspector-body'/'inspector') 渲染；authoredBody region 优先于 dynamicBody（schema 分类正确）                                                                                                                                              | report-designer-inspector.tsx:30-31,67-72                                                | —    |
| 7   | 事件与 action 契约          | 无 schema 事件派发；内嵌表单的提交 action（report-designer:updateMeta 等）经宿主 actionScope namespace 解析（page-renderer 已注册）；reaction 三件套不适用（无 kind:'reaction' 声明）                                                                                                                           | field-panel-renderer.tsx:79-107 同模式、page-renderer.tsx:347-361                        | —    |
| 8   | a11y                        | 空态/加载/错误为 `<p>` 静态文本（**无 aria-live 播报，P3-1**）；section 语义容器；target kind header span 文本                                                                                                                                                                                                  | inspector-shell-renderer.tsx:51-63、report-designer-inspector.tsx:37-58                  | P3-1 |
| 9   | i18n                        | 全部 t()（noSelection/loadingPanels/loadPanelsFailed/noPanels/inspectorTitle/Subtitle）+ props 覆盖（emptyLabel/noSelectionLabel/errorLabel）；双 locale 在案；零硬编码中文                                                                                                                                     | inspector-shell-renderer.tsx:44-62、zh-CN.ts:676-688                                     | —    |
| 10  | 四态覆盖                    | 空态（无 selection / 无 body）+ 加载（loading）+ 错误（error → stack + errorLabel）+ 正常（body 渲染）全分支有测试                                                                                                                                                                                              | report-designer-inspector.test.tsx、inspector-shell（page-renderer-shell.test.tsx 间接） | —    |
| 11  | 异步生命周期                | resolvedSchema 由 refreshDerivedState 异步解析（AbortController + error 态写回）；openInspector 经 setSelectionTarget 触发 loading → refresh 完成 loading:false；无裸 Promise                                                                                                                                   | core.ts:208-263、core-dispatch.ts:154-161                                                | —    |
| 12  | 组合宿主场景                | 既有用例 2/5（inspector 文案 + cell 点击保持 active）真机通过；Phase 5 宿主页补 shell 空态/选中态切换场景                                                                                                                                                                                                       | tests/e2e/report-designer-demo.spec.ts:66-70,151-163                                     | —    |
| 13  | 样式契约                    | widget 自样式（面板 frame 由 page-renderer renderPanelFrame 提供：flex/border/scroll）；无 ThemeProvider；CSS 变量                                                                                                                                                                                              | page-renderer.tsx:151-187                                                                | —    |
| 14  | React 19 规范               | 无冗余 memo/callback；useOwnScopeSelector 引用比较；无 effect 镜像                                                                                                                                                                                                                                              | inspector-shell-renderer.tsx、report-designer-inspector.tsx                              | —    |
| 15  | 性能边界                    | shell 只订阅 selectionTarget+inspector（inspector-design.md §16.1）；body schema 渲染路径由 helpers.render 缓存（schema 编译层）；无监听器泄漏                                                                                                                                                                  | inspector-design.md:235-259                                                              | —    |
| 16  | 测试质量                    | report-designer-inspector.test.tsx（空态/无 body/body 渲染/noSelection 覆盖）+ page-renderer-shell.test.tsx（byProfile 优先/byTarget/无 schema 隐藏/关闭）+ manifest-and-helpers 间接；inspector shell 独立行为（loading/error 分支）经 page-renderer 集成路径覆盖——loading/error 分支无独立 shell 单测（P3-2） | report-designer-inspector.test.tsx、page-renderer-shell.test.tsx:11-141                  | P3-2 |
| 17  | 文档对照                    | inspector-design.md §5 shell 边界 + §5.1 空态规则与实现逐条一致（noSelection → empty → loading → error → body）；§8 expression editor / §13 reference 模型属 adapter 面（本面无直接实现，无 phantom）                                                                                                           | docs/architecture/report-designer/inspector-design.md:82-104                             | —    |
| 18  | 注册/边界/IO                | INV-1 零命中；core 无 react 依赖（inspector 状态纯 core）                                                                                                                                                                                                                                                       | D3.3 live 跑（2026-08-08）                                                               | —    |
| H1  | host 契约                   | inspector 投影（designer.inspector + inspector 顶层 + inspectorPanels 字段）↔ manifest reportDesignerProjection.inspector/inspectorPanels 形状一致（inspectorShape/inspectorPanels union unknown）；openInspector/closeInspector 方法 ↔ provider ↔ dispatch 三向一致                                            | report-designer-manifest.ts:233-243,329-339,424-436                                      | —    |
| H2  | 事务 undo                   | openInspector/closeInspector 在 MUTATION_COMMAND_TYPES（readonly 拦截）；openInspector 变更 selectionTarget（经 setSelectionTarget）不入 undo 栈（非文档变更，语义正确）；closeInspector changed = wasOpen                                                                                                      | core.ts:356-366、core-dispatch.ts:154-170                                                | —    |
| H3  | 拖拽                        | 不适用（inspector 面无拖拽）                                                                                                                                                                                                                                                                                    | —                                                                                        | —    |
| H4  | 键盘                        | shell 无独立键盘面（面板滚动/折叠按钮在 page-renderer renderPanelFrame，aria-label t()）；内嵌表单键盘归 form runtime                                                                                                                                                                                           | page-renderer.tsx:160-171                                                                | —    |
| H5  | 剪贴板                      | 不适用（无剪贴板路径）                                                                                                                                                                                                                                                                                          | —                                                                                        | —    |
| H6  | e2e 可操作性                | 既有用例 2/5 断言 inspector 文案与存活；Phase 5 宿主页补 target 切换（cell 点击 → shell 内容切换）场景                                                                                                                                                                                                          | tests/e2e/report-designer-demo.spec.ts:66-70,151-163                                     | —    |
| H7  | MA4.3 缺口回归              | 无 inspector 专属缺口（MA4.3 清单无 inspector 项）——inspector 状态路径经 page-renderer 集成测试覆盖                                                                                                                                                                                                             | —                                                                                        | —    |

## 发现清单

- [P3-1] inspector loading/error 变化无 aria-live 播报（inspector-shell-renderer.tsx:51-63 静态 p 文本）→ 状态: 卡内记录（a11y 增强候选，DR 可选）
- [P3-2] report-inspector-shell loading/error 分支无独立 shell 单测（经 page-renderer 集成路径间接覆盖；错误分支由 report-designer-inspector.test.tsx 的 noSelection 覆盖非等价）→ 状态: 卡内记录（H7 记录维持，间接覆盖可接受）

## 组合宿主场景（真实浏览器验证，bug 73 模式专项）

- 场景（既有）: 用例 2（inspector 面板 'Inspector' 标题 + /Sheet selected/loading 文案）+ 用例 5（cell 点击后 inspector 保持 active 且文案含 cell）| 断言: data-slot 文本 | 结果: pass
- 场景（Phase 5 新增）: 宿主页 inspector shell 选中态切换（初始 sheet → 点 cell → shell 标题 target kind 变化 + body 内容切换）| 断言: data-slot 文本 + target kind | 结果: pass（`report-designer-host.spec.ts` 2026-08-08 全绿）

## 修复记录

- 本面无行为修复（P3 卡内记录）

## Closure

- 独立 closure audit: pass | fail + 记录位置（fresh session）
