# Development Log - 2026-08-04

## C6.5 diff-view 逐组件审计执行：plan-2026-08-04-1757-3 四 Phase 全绿（1 卡 closed + P1×10 修复 + 宿主 5/5 含 bug 73 专项 + 全量验证绿 + closure-audit 已 pass 收口）

执行 `docs/plans/2026-08-04-1757-3-c6-5-diff-view-audit.md`（diff-view 1 组件 × 18 维逐组件审计 + deep-audit 21-23 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**content diff-view C6.5 执行完成，closure-audit 由 mission-driver CLOSURE_VERIFY fresh session（2026-08-05）pass 后 roadmap C6.5 行标 `done` 正式收口（与 C6.1-C6.4 先例一致）**。

### Phase 1 — 审计卡（`docs/audits/per-component/diff-view.md`）

- 18 维逐项核对 + 注册定义三方核对（content-renderer-definitions.ts diff-view fields `:516-534` ↔ DiffViewSchema ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×10**（P1-1 hunk header 缺 `data-diff-type="hunk"`；P1-2 inline diff `data-diff-inline` 值漂移 insert/delete vs design add/delete；P1-3 three-column 不接线 onLineClick；P1-4 CX-9 reaction 通道死接线——4 个 kind:'reaction' 字段声明但渲染器不消费 props.reactions（无 ready()）→ 句柄 initial-paused 永不 fire；P1-5 activeFileIndex 越界钳制缺下界；P1-6 假绿测试 ×3（选择器与真实 DOM 契约脱节，断言从不执行/平凡通过）；P1-7 注册定义缺 onLineClick/onHunkExpand eventContracts；P1-8 ComponentHandle 运行时不可达——useImperativeHandle 句柄不注册进 componentRegistry（flux-react 运行时不传 ref）；P1-9 onHunkExpand payload 语义反转（expanded 实为 isHidden 新值）；P1-10 两事件派发缺 event/evaluationBindings/scope ctx 注入）+ **P2 ×6**（lab 页缺失——P2 低成本当场补页 Phase 3 落地；diff-file-list 硬编码英文归 CR backlog——alert P2-2/carousel P2-4 先例同裁；cross-file prev/next 按钮仅 title 无 aria-label；design.md 文档漂移 ×3（§4 events 嵌套 vs 平铺、§12 虚拟化未实现、§11 模块清单含不存在文件）；memo/useCallback 复核裁定 keep；viewType/activeFileIndex 无受控 echo 裁定有意设计）+ **P3 keep ×5**。
- 重点维度：**dim 1/3/7/22 反应接线**——CX-9 通道（C4.2 已收口机制）在 diff-view 的 4 个 kind:'reaction' 字段（toggleViewType/setViewType/expandAll/collapseAll）为死接线（无 ready() 激活）→ P1-4；**dim 3 值所有权三态**——viewType local 语义（design §7 有意设计 ✓）+ activeFileIndex 上界钳制存在但下界缺失（P1-5）；**dim 5 DOM 契约**——`nop-diff-view` marker + data-view/data-diff-type/data-slot diff-hunk-header/diff-gutter/data-expanded 与 design.md §10 核对出 P1-1/P1-2 两处漂移；**dim 11 异步生命周期**——无外部 IO（纯客户端计算 + lowlight 同步 highlight，LRU 限 50 条）、debounce/keyboard 清理 ✓；**dim 14 React 19**——07-28 记录 memo/useCallback 模式复核裁定 keep（Compiler 基线无害，custom comparator 因 unstable onLineClick 闭包不生效——观察记录）；**dim 15/16/23 测试有效性**——diff-perf 5000ms 校准阈值复验（live 实证 `diff-perf.spec.ts:59` 仍为 5000ms，未回退 200ms）✓ + diff-view-renderer.test.tsx 假绿 ×3（P1-6）。
- 工具脚本：missing-renderer-markers/styling-suspects 本族 0 命中；设计文档对照（dim 17）出 P2-4 文档漂移。

### Phase 2 — P0/P1 自动修复（test-first，flux-renderers-content 268 → 279 tests，+11）

- **test-first 证据**：新增/重写测试先行运行 13 failed（P1-1/P1-2/P1-3/P1-4/P1-5/P1-6/P1-7/P1-8 各复现用例 + 假绿重写），随后实现修复全部转绿。
- **P1-8（handle 注册，跨组件同型——carousel O-03/F6 先例）**：diff-view-renderer.tsx 迁 `useCurrentComponentRegistry` + `componentRegistry.register(handle, {cid})`（effect 内构造、viewTypeRef 读态、句柄身份稳定），invoke 四方法（setViewType 校验 viewType ∈ split/unified）；移除 useImperativeHandle/DiffViewHandle/ref。**P1-4（CX-9 reaction ready）**：mount effect `reactions[key]?.ready()`（CRUD 先例 crud-renderer-state.ts:710）。
- **P1-5**：clamp `Math.max(0, Math.min(initialIndex, files.length-1))`（diff-view-renderer.tsx:407）。**P1-1**：diff-hunk.tsx 两 header 补 data-diff-type="hunk"。**P1-2**：diff-template.ts 'insert'→'add' 映射。**P1-3**：diff-three-column-view.tsx 三 pane 行接线（paneRowHandlers：role/tabIndex/Enter-Space + side 对应 lineNum）。**P1-9**：diff-hunk.tsx onHunkExpand 报 `!next`（expanded 语义）。**P1-10**：两派发点补 `{event, evaluationBindings, scope: node.scope}` ctx（alert P1-1/C6.3 bug 83 同族先例）。
- **P1-7**：content-renderer-definitions.ts 补 onLineClick/onHunkExpand eventContracts（契约测试「diff-view declares eventContracts」先红后绿）。**P2-3**：diff-header.tsx prev/next 挂 aria-label（t('flux.diff.prevFile'/'nextFile') key 既有）。**P2-4**：design.md §4/§5/§7/§8/§9/§10/§11/§12 多处同步。
- **bug 记录**：`docs/bugs/85-diff-view-handle-and-reaction-dead-wiring-fix.md`（P1-8/P1-4 死接线，按 AGENTS.md Bug Fix Test Coverage Rule）。
- **共性缺陷裁决（Decision）**：P1-8（handle 注册不可达）与 P1-10（事件派发缺 ctx）均为**组件单点根因**（同类问题已在 carousel O-03/alert P1-1 各自 plan 内修复，无新的跨包公共层根因）——**不插入新 CX-n**（CX-9 机制本身 C4.2 已收口，本 plan 仅修复 diff-view 消费接线）。决策记录于卡与本 log。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 全绿（279 tests）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c6-5-host-surfaces.spec.ts` 5/5 + 回归全绿）

- lab 页新增 1 个（补上维度 18 缺口——P2-1 低成本当场补页裁决）：`diff-view-lab-page.tsx` + `data-c6c5-host.ts` 宿主 schema 模块（probe namespace `__c6c5LineClick` 独立 window 槽位）+ **CONTENT_RENDERER_ROUTES 追加 1 条**（`content-renderer-routes.ts` 18 → 19 条——与 C6.1-C6.4 协调结论：C6.1 5 条、C6.2 至 11、C6.3 至 14、C6.4 至 18、本 plan 追加至 19 条，route-model 公共导入面不变；route-matrix liveTotal 动态求和自动一致）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 1 条（smoke.spec.ts 自动覆盖）+ `RENDERER_LAB_REGISTRY` 1 条。
- 5 场景全绿（programmatic DOM 断言，无截图诊断）：
  - **host-diff-dialog（bug 73 模式专项——diff-view 在 dialog 内渲染 + 行点击）**：openDialog 打开 diff 面板 → split 视图渲染 → add 行点击派发 onLineClick，`__c6c5LineClick` probe 收到 `${lineNumber}|${side}|${type}`（evaluationBindings + payload 解析实证——P1-10 真机验证）→ **pass**
  - **host-diff-crosfile**：文件列表导航驱动内容切换；activeFileIndex 99 → 钳到 last、-5 → 钳到 first（P1-5 真机实证）→ **pass**
  - **host-diff-reaction**：schema 声明的 toggleViewType reaction（dependsOn: [toggle]）经 CX-9 通道 scope 投影后 data-view 翻转；setViewType reaction（dependsOn: [viewMode]）驱动 split/unified（P1-4 真机实证——`reactions[key]?.ready()` 激活后 ReactionHandle 不再 initial-paused）→ **pass**
  - **host-diff-expand**：component:expandAll/collapseAll handles 驱动 data-expanded 状态（P1-8 真机实证——componentRegistry 注册后 `component:expandAll` 动作可达）→ **pass**
  - **host-diff-empty**：old/new 相同内容渲染 noChanges 空态不崩溃 → **pass**
- 回归：**diff-demo.spec.ts 5/5 + diff-perf.spec.ts 1/1 全绿**（perf 实测 1067ms < 5000ms 校准阈值，未回退 200ms 预算）。
- **bug 73 模式专项检查结论：pass**（host-diff-dialog——diff-view 在 dialog 内渲染 + 真实行点击，证据见 spec `:24-57`）。

### Phase 4 — 组件回归与审计卡 closure

- 全卡复查：18 维 + 21-23 表结论与最终代码一致；P0 ×0 / P1 ×10 fixed / P2 当场修复 ×4（P2-2 i18n backlog 归 CR）/ P3 keep ×5；卡状态 `fixed-pending-closure → closed`。
- 本组件范围回归：`pnpm --filter @nop-chaos/flux-renderers-content test` 279/279 + diff-demo 5/5 + diff-perf 1/1 + c6-5-host-surfaces 5/5 全绿零新增失败；workspace 全量 `pnpm typecheck`/`build`/`lint`/`test` 31/31/31/58 全绿（Closure Gates 项）。
- 协调结论：CONTENT_RENDERER_ROUTES 与 C6.1-C6.4 共用常量模块，本 plan 追加至 19 条；route-matrix liveTotal 动态求和自动一致。
- closure-audit 证据位置：本 plan Closure 节 + 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C6.5 行标 done 的前置即该 audit pass。
- **closure-audit pass（2026-08-05，mission-driver CLOSURE_VERIFY fresh session `MISSION_DRIVER:2026-08-05-065620-mission-driver`）**：live repo 逐项核对——卡 closed（P0 ×0 / P1 ×10 fixed / P2 全裁定）；P1-1..P1-10 各修复实证（diff-hunk.tsx data-diff-type、diff-template.ts 'insert'→'add'、diff-three-column-view.tsx 三 pane 接线、diff-view-renderer.tsx reactions ready() + componentRegistry.register + clamp + ctx 注入、content-renderer-definitions.ts eventContracts + 契约测试、diff-view-renderer.test.tsx 假绿重写）；lab 页 + registry 1 条 + data-c6c5-host.ts + routes 19 条 + manifest 1 条 + 宿主 spec 全在；实测 flux-renderers-content 279/279、c6-5-host-surfaces 5/5（含 bug 73 模式 host-diff-dialog）、diff-demo 5/5、diff-perf 1/1（1067ms < 5000ms）全绿；workspace typecheck/build/lint/test 31/31/31/58 绿；design.md 同步实证；bug 85 记录实证；roadmap C6.5 行已标 `done`。**C6.5 正式收口。**

## C6.4 content 媒体族逐组件审计执行：plan-2026-08-04-1757-2 四 Phase 全绿（4 卡 closed + P1×1 修复 + 宿主 4/4 含 bug 73 专项 + 全量验证绿 + closure-audit 待 CLOSURE_VERIFY）

执行 `docs/plans/2026-08-04-1757-2-c6-4-content-media-family-audit.md`（audio/video/carousel/qrcode 4 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**content 媒体族 C6.4 执行完成（roadmap C6.4 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.x/C4.x/C5.x/C6.1/C6.2/C6.3 先例一致；执行 session 不自审）**。

### Phase 1 — 4 张审计卡（`docs/audits/per-component/{audio,video,carousel,qrcode}.md`）

- 18 维逐项核对 + 注册定义三方核对（content-renderer-definitions.ts audio `:417`/video `:434`/carousel `:454`/qrcode `:479` ↔ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×1**（carousel P1-1 注册定义缺 onChange eventContracts——onChange 有承载 payload `{activeIndex, item}`（design §8 单一规范键）但定义无 eventContracts 声明，编辑器/设计器契约面缺口（cards/alert/steps 同族均声明））+ **P2 ×10**（lab 页缺失 ×4——P2 低成本当场补页 Phase 3 落地；audio/video design.md 缺 onLoadError 事件记录 + video §4 缺 width/height——文档缺口 ×2；audio/video/qrcode 测试盲区 ×3（src 变更恢复、poster/width-height 透传、value 变更重绘 + 失败后恢复）；carousel onChange 派发缺 `scope: props.node.scope` ctx 注入 + 行为断言盲区；carousel indicator aria-label 硬编码英文归 CR backlog——alert P2-2 先例同裁）+ **P3 keep ×5**（媒体回退无 aria-live、qrcode aria-label 暴露 value 原文、carousel data-slot 嵌套同值（ui primitive 先例同裁）、carousel api null 时 handle ok、carousel onSelect 初始调用无 onChange 误报）。
- 重点维度：**dim 3 值所有权三态**——carousel 活动项 local 状态经 embla api 桥接（setApi → onSelect 桥接 activeIndex，`carousel.tsx:45-46/:59-81`）——「缺 valueOwnership」裁定为有意设计（design §7，plan 裁决一致）；qrcode value 变化 canvas 重绘 echo（`qrcode.tsx:49-71` effect deps 驱动）；audio/video 无 value 字段；**dim 6 08-02 机制复验**——audio/video title + qrcode label value-or-region、carousel items 数组 prop、无 deepFields 残留（live grep 零命中）；**dim 7** carousel onChange payload 单一规范键 activeIndex 无冗余 index（`carousel.tsx:70`，设计 §8 Decision A + 既有源文契约测试）；**dim 11** audio/video src 变更即清除 errored（`audio.tsx:26-28`/`video.tsx:36-38`——image P1-1 sticky-errored 同型缺陷在此不存在）；carousel autoplay 完整清理（F1/F2 8 用例实证）；qrcode AbortController + aborted 短路（AUDIT-13 豁免注明）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/performance-suspects 本族 0 命中；react19-optimization-candidates 命中 ui carousel primitive（ui 包）+ 4 组件 useEffect 外部同步正常；i18n-keys 通过。

### Phase 2 — P0/P1 自动修复（test-first，flux-renderers-content 259 → 268 tests，+9）

- **carousel P1-1（onChange eventContracts）**：test-first——`content-renderer-definitions.test.tsx` 新增「carousel declares onChange eventContracts with the canonical activeIndex/item payload」先红（eventContracts undefined）后绿；实现——`content-renderer-definitions.ts` carousel 注册项补 eventContracts onChange `{activeIndex: number, item: unknown}`（cards onSelectionChange/alert onClose 同族惯例）。
- **carousel P2-3（scope ctx 注入）**：test-first——`carousel-autoplay.test.tsx` 新增 2 行为用例（fake api triggerSelect 驱动 onChange：payload `{type:'change', activeIndex:1, item}` 单一规范键 + ctx event/evaluationBindings/scope 断言；同 slide 不重派）先红（ctx.scope undefined——mock props node 空对象需注入 scope stub 实证）后绿；实现——`carousel.tsx:71-75` 补 `scope: props.node.scope`（steps/cards/alert 同族惯例；运行时事件包装器本有 nodeInstance.scope fallback，功能等价、惯例补齐）+ effect deps 补 `props.node.scope`（exhaustive-deps 门禁）。
- **P2 当场修复 ×6**：audio/video 各 +2 用例（src 变更错误恢复 + poster 渲染 / width-height 样式透传）；qrcode +2 用例（value 变更重绘——`qrMock.renderedValues` 记录 toCanvas 调用序列、失败后恢复——`resolveSilently` 绕过 happy-dom canvas 2d null 限制，断言调用序列而非像素）；design.md 文档补齐 ×2（audio §8 onLoadError、video §4 width/height + §8 onLoadError；carousel §6「items 每项可带 body」aspirational 表述更正为 image/title/caption）。
- **共性缺陷裁决（Decision）**：两 P1/P2 均为组件单点根因（carousel 注册项 eventContracts 缺口、carousel 派发调用点 ctx、audio/video/qrcode 文档与测试盲区）——**不插入 CX-n**。决策记录于各卡与本 log。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 全绿（268 tests）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c6-4-host-surfaces.spec.ts` 4/4 + 回归全绿）

- lab 页新增 4 个（补上维度 18 缺口——P2 低成本当场补页裁决）：`audio-lab-page.tsx`/`video-lab-page.tsx`/`carousel-lab-page.tsx`/`qrcode-lab-page.tsx` + `data-c6c4-host.ts` 宿主 schema 模块（probe namespace `probe:record`/`probe:carouselChange` 独立 window 槽位 + BROKEN_VIDEO_DATA_URI 确定性错误触发源——data URI 资源可加载无网络 console 噪音但无法 demux → 原生 error 事件）+ **CONTENT_RENDERER_ROUTES 追加 4 条**（`content-renderer-routes.ts` 14 → 18 条——**与 C6.1/C6.2/C6.3 协调结论：C6.1 建常量模块 5 条、C6.2 追加 6 条至 11 条、C6.3 追加 3 条至 14 条、本 plan 追加至 18 条**，route-model 公共导入面不变）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 4 条（smoke.spec.ts 自动覆盖 → **80/80**）+ `RENDERER_LAB_REGISTRY` 4 条 + route-matrix 计数测试同步（liveTotal 动态求和 18 条 content routes 自动一致，playground 141 tests 全绿）。
- 4 场景全绿（programmatic DOM 断言，无截图诊断）：
  - **host-media-dialog + host-media-error（bug 73 模式专项——媒体在 dialog 内加载 + 错误回退）**：openDialog 打开媒体面板 → data URI audio 正常加载（audio-media 元素 + src 断言）；broken video data URI → data-state=error 回退 + onLoadError probe `video-error-fired`；dialog 关闭重开媒体生命周期无异常 → **pass**
  - **host-carousel-ctrl**：component:next/setValue(2)/prev 驱动 data-active-index 0→1→2→1；onChange probe `1|3|Second`/`2|3|Third`（**`${activeIndex}` 经 evaluationBindings、`${slides.length}` 经 scope、`${item.title}` 经 payload——P1-1 eventContracts + P2-3 scope 注入真机实证**）→ **pass**
  - **host-carousel-auto**：autoPlay scope 切换——interval 驱动 data-active-index 无交互推进（stage scrollIntoView 规避 offscreen 暂停契约）→ 关闭后 1.5s 稳定 → 重开恢复推进 → **pass**
  - **host-qrcode-update**：value A/B 切换 canvas toDataURL 逐次变化（重绘 echo 真机实证）；清空 → data-state=empty + fallback + canvas 移除；再设有效值 → canvas 恢复（失败后恢复路径）→ **pass**
- 回归：**component-lab 全量 279 passed / 2 failed / 1 skipped**（2 failed 为 c3-5-host-surfaces media-rich **editor** 既有机器负载 flake——`git stash` HEAD 基线复现一致，非本族引入；C6.3 VERIFY 已记录同型）+ smoke **80/80** + w4a-multimedia-family **9/9** + c6-1 5/5 + c6-2 7/7 + c6-3 4/4 + navigation 7/7。
- **bug 73 模式专项检查结论：pass**（host-media-dialog——媒体元素在 dialog 内加载 + 错误回退，真实浏览器宿主，证据见 spec `:22-57`）。

### Phase 4 — 全卡复查 + 收口前置

- 4 卡全复查：18 维表结论与最终代码一致（live 核验——content-renderer-definitions.ts eventContracts onChange 实证、carousel.tsx:71-75 scope 注入实证、lab 页 + 宿主 spec 可核验）；**P0 ×0 / P1 ×1 fixed（carousel onChange eventContracts，test-first + 宿主实证）/ P2 ×10（lab 页 ×4 fixed、文档 ×3 fixed、测试盲区 ×3 fixed、aria-label ×1 backlog 归 CR）/ P3 ×5 keep** 全部裁定；卡状态全部 `closed`（carousel：open → fixing → fixed-pending-closure → closed；audio/video/qrcode：Phase 1 closed → Phase 4 复查）。
- 全量验证：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 268 含新增回归、playground 141 含 route-matrix 18 条 content routes 动态求和）全绿；相关 e2e 零新增失败（见 Phase 3；c3-5 editor 2 项为 HEAD 基线复现的 pre-existing 机器负载 flake）。
- **08-02 机制复验结论**：dim 6 全 pass——audio/video title、qrcode label value-or-region、carousel items 数组 prop 分类与 08-02 声明一致；无 deepFields 残留。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。
- closure-audit 证据位置：本 plan Closure 节 + 4 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C6.4 行标 done 的前置即该 audit pass。
- **closure-audit pass（2026-08-05，mission-driver CLOSURE_VERIFY fresh session `MISSION_DRIVER:2026-08-05-065620-mission-driver`）**：live repo 逐项核对——4 卡 closed（P0 ×0 / P1 ×1 fixed / P2 全裁定）；carousel eventContracts onChange 实证（content-renderer-definitions.ts:468-481 + 契约测试 :294-310）；scope 注入实证（carousel.tsx:71-75）；4 lab 页 + registry 4 条 + data-c6c4-host.ts + routes 18 条 + manifest 4 条 + 宿主 spec 全在；实测 flux-renderers-content 268/268、c6-4-host-surfaces 4/4（含 bug 73 模式）、w4a-multimedia 9/9 全绿；design.md 补齐实证；roadmap C6.4 行已标 `done`。**C6.4 正式收口。**

## Timeline v2 受控当前事件扩展立约（ArbiterOS demo 播放需求驱动）

核对 `timeline-renderer.tsx` 实际代码：静态展示（items/mode/orientation/reverse，time/title/detail/icon/level），无 owner 状态/事件——**不满足 demo「播放 + 进度条 + 当前步骤高亮」**。裁定：timeline 扩展受控当前事件（播放引擎不内置，归宿主编排）。

- `docs/components/timeline/design.md` 更新：v2 扩展（value/defaultValue/valueOwnership/valueStatePath/onChange），四件套镜像 steps 同族先例（key 匹配 → 索引 clamp → 未命中无 active；scope 缺 valueStatePath 降级 local + dev 告警）；点击 seek 仅 onChange 声明时可点（`data-clickable`）；active 定位按逻辑顺序（reverse 仅影响渲染）；播放引擎明确不采纳（timer 经 `xui:imports` + `setValue` 递增 + `progress` 展示）；新增 INV-1..5 原则审计。
- `docs/components/timeline/example.json`：受控 seek 示例（valueOwnership=scope + onChange setValue）。
- `flux-guide/design-patterns/steps-timeline.md`：新增「Timeline 受控当前事件」示例 + 字段参考表补 5 字段（value/defaultValue/valueOwnership/valueStatePath/onChange）+ item `value` key。
- `examples.manifest.json`：修正漂移——timeline 从 targetContract 移入 runtime（实际已注册，W4b done 后未同步）；graph 保持 targetContract（上一会话立约被 git checkout 回退后重新补上）。
- 注意：manifest targetContract 仍有同类过期标注（markdown/json-view/steps 等实际 runtime），待统一修正。
- 下一步：实现扩展时同步 schemas.ts / process-display-definitions.ts / timeline-renderer.tsx / 单测（三态 + reverse + 未匹配降级）+ playground + e2e。

## G1 graph 图查看器 design 立约：docs/components/graph/{design.md,example.json} + research + manifest/roadmap 注册（proposed）

调研 ArbiterOS 治理界面（`~/ai/arbiteros`，Langfuse 定制 trace 图）需求后确定 Flux 唯一组件缺口为**只读交互式图查看器**（markdown/json-view/steps/timeline 等实际均已注册于 `flux-renderers-content`/`flux-renderers-layout`，`examples.manifest.json` targetContract 标注已过时）。

- 产出：`docs/analysis/complex-controls/research-graph.md`（选型：`@xyflow/react` 复用 flow-designer 同版 12.10.2 + dagre 分层布局；Cytoscape/G6 不采纳；与 flow-designer 编辑语义边界裁定）；`docs/components/graph/design.md`（target contract：`layout: flow|hierarchy` 双模式、`searchable` 本地搜索 + Enter 循环、`levelField`+`levelMap` 语义级高亮、`onNodeClick`/`onSelectionChange` 事件、7 个 component handle、node/empty region、`data-slot` marker 表、INV-1..5 原则审计）；`example.json`（trace 场景：node region + badge + levelMap + 选中联动分析面板）。
- 关键裁定：独立包 `flux-renderers-graph`（NEW）隔离 xyflow+dagre 依赖（data 包已有 recharts 不再叠加）；多观察点循环是宿主组合职责（事件+scope），graph 不内置；请求零下沉（nodes/edges 经 data-source 注入）。
- 注册：`examples.manifest.json` targetContract 加 `graph`；`roadmap.md` Phase Status 加 G1（proposed）+ Work Items Proposed 表。
- 下一步：人工确认 G1 → 转 todo → 实现计划（包 bootstrap + canvas 桥接 + 布局/搜索 helper + 单测）。

## graph/timeline 文档两轮独立审查 + 共识达成 + 修改计划草稿

对上两条（Timeline v2 立约 / G1 graph 立约）文档发起两轮独立 sub-agent 审查（fresh session，只读），逐轮修复至零 Blocker 达成共识。

- round-1：graph 3 Major + 12 Minor，timeline 2 Major + 6 Minor——全部修复（graph：example.json text `content` 字段、React Flow 视口裁剪断言、7 句柄失败路径表、node.level 词汇区分、GraphSchema.label、§2.1-4 data 一致性、畸形数据硬契约、包名 typo、roadmap 日期等；timeline：「镜像 steps」失实改三层表述、example.json 转 v1 可验证 + v2 移 flux-guide 标注、level 漂移注、marker 词汇、flux-guide steps 表 statusPath/status 词汇修正）。
- round-2：graph 2 Major + 4 Minor，timeline 2 Major(1 误报) + 5 Minor——全部修复（graph：**§4.1 代码块围栏断裂**（round-1 引入，GraphNode/GraphEdge 合回单一代码块）、**选择模型契约缺口**（裁定单选 + 禁用 React Flow 多选/框选）、§5 补 label、elkljs→elkjs、删误导性 kanban/gantt 先例引用；timeline：Major-1 `component:SetValue` 经核实文件实为小写 `component:setValue` 属误报不改、Major-2 flux-guide defaultValue 语义对齐 design.md（逐渲染回退链非 seed-only）、helper 引用范围、item.value 标 v2、漂移注补 default/primary、§10 补 data-slot=timeline-root、flux-guide Steps 表补 onChange）。graph manifest barcode-input/diff-view 双标为既有问题，记入计划清单非本轮范围。
- 自检全绿：三个 JSON 有效；graph design.md 围栏 10 配对、timeline 2 配对；残留（elkljs/content/SetValue/steps 旧状态词）清零。共识达成。
- 拟制两份修改计划**草稿**（Status: draft，按用户指令不 review）：`docs/plans/2026-08-04-2030-1-g1-graph-viewer-plan.md`（G1：新包 flux-renderers-graph bootstrap + xyflow 只读画布 + dagre 布局 + 单选 + 7 句柄 + 畸形数据 + playground/e2e/单测，4 Phase）；`docs/plans/2026-08-04-2030-2-timeline-v2-controlled-current-event-plan.md`（timeline v2：schemas+contracts+renderer 三态+seek+marker，含 resolve helper 抽取裁定（首版不抽共享），3 Phase）。两份 Draft Review Record 标 pending（执行前补独立 review）。

## C6.3 content 值映射族逐组件审计执行：plan-2026-08-04-1757-1 四 Phase 全绿（3 卡 closed + P1×2 修复 + 宿主 4/4 含 bug 73 专项 + 全量验证绿 + closure-audit 待 CLOSURE_VERIFY）

执行 `docs/plans/2026-08-04-1757-1-c6-3-content-value-mapping-family-audit.md`（alert/mapping/status 3 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**content 值映射族 C6.3 执行完成（roadmap C6.3 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.x/C4.x/C5.x/C6.1/C6.2 先例一致；执行 session 不自审）**。

### Phase 1 — 3 张审计卡（`docs/audits/per-component/{alert,mapping,status}.md`）

- 18 维逐项核对 + 注册定义三方核对（content-renderer-definitions.ts alert `:271`/mapping `:328`/status `:370` ↔ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×2**（alert P1-1 onClose 派发缺 `event`/`evaluationBindings` payload 注入——`${level}` 在 onClose action args 不可解析（cards P1-2 同族先例 bug 83）；status P1-1 propContracts `record/string` 窄于 StatusSchema `Record<string, SchemaValue>`——shape-validation 整键跳过（shape-validation-node-fields.ts:63-65）→ 非字符串映射值整表静默丢失）+ **P2 ×5**（lab 页缺失 ×3——P2 低成本当场补页 Phase 3 落地；alert aria-label="Close" 硬编码英文归 CR backlog；mapping 对象命中 JSON.stringify 分支零断言）+ **P3 keep ×4**（alert closable+actions 视觉重叠 nit、alert 空态仅 icon、mapping 'use no memo' 同族模式、status 空值 data-state="miss" 与 mapping "empty" 不对称——既有测试冻结）。
- 重点维度：**dim 3 值所有权三态**——mapping/status display-only 无写回路径（无 valueOwnership/valueStatePath 声明、grep 零写面，design.md §7「无复杂 owner 状态」）——「缺 valueOwnership」裁定为有意设计非契约漂移（plan Draft Review 裁决一致）；alert 无 value 字段；**dim 6 08-02 机制复验**——alert title/body value-or-region + actions region + onClose event、mapping item region、status 全 prop——与 08-02 声明一致，无 deepFields 残留（live grep 零命中）；**dim 7** alert onClose payload 形状与 eventContracts 一致但派发链路缺 evaluationBindings（P1-1，steps/button-group/cards 同族惯例比对确认）；**dim 16 假绿核查**——status.test.tsx 既有 10 用例全走 schema 管线但 labelMap 均为字符串值（P1-1 盲区：新增非字符串降级用例经管线实证才暴露）；alert onClose payload 零断言（P2-3）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/hardcoded-type-dispatch/non-retained/performance-suspects/async-failure-paths 本族 0 命中；react19-optimization-candidates 命中仅 playground demo 页（非 renderer）。

### Phase 2 — P0/P1 自动修复（test-first，flux-renderers-content 254 → 259 tests，+5）

- **alert P1-1（evaluationBindings payload）**：test-first——`alert-payload-action.test.tsx` CD6 用例先红（close 后 capture 空——`${level}` 未解析）后绿；实现——`alert-renderer.tsx:68-77` 补 `event: payload` + `evaluationBindings: payload`（steps `steps-renderer.tsx:196-200`/button-group `button-group-renderer.tsx:82-86`/cards `cards-renderer.tsx:157-161,:220-224` 同族惯例）。
- **status P1-1（propContracts 契约漂移）**：test-first——status.test.tsx「keeps non-string labelMap/levelMap/iconMap values through the compile pipeline」先红（`{ok:123}` labelMap 整键跳过 → miss）后绿；实现——`content-renderer-definitions.ts:383-401` 三契约（labelMap/levelMap/iconMap）value kind `'string'`→`'unknown'`，与 mapping `map` 契约（record/unknown `:342`）族内一致；渲染器既有降级逻辑（status.tsx:71-74 typeof 守卫）零行为改动。诊断方法：直渲（createMockRendererProps）通过而 schema 管线失败 → console.log `slotProps.labelMap` 实证 undefined → 定位 compile 期 skippedPropKeys 机制。
- **bug 记录**：`docs/bugs/84-status-map-contract-narrowing-silent-drop-fix.md`（契约漂移 + 编译期静默整键丢弃）。
- **共性缺陷裁决（Decision）**：两 P1 均为组件单点根因（alert 派发调用点、status propContracts 声明；编译期 skip 机制为既有共享设计、本次根因是 status 契约自身声明过窄）——**不插入 CX-n**。决策记录于各卡与本 log。
- **P2 当场修复 ×2**：mapping「renders an object hit value as JSON string」（toTextNode 对象分支断言）；status「renders no icon when iconMap names an unknown lucide icon」（非法图标名降级断言）+「falls back to the raw key when labelMap value is not a string」（并入 P1-1 回归面）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 全绿（259 tests）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c6-3-host-surfaces.spec.ts` 4/4 + 回归全绿）

- lab 页新增 3 个（补上维度 18 缺口——P2 低成本当场补页裁决）：`alert-lab-page.tsx`/`mapping-lab-page.tsx`/`status-lab-page.tsx` + `data-c6c3-host.ts` 宿主 schema 模块（含 probe namespace 注册，4 个独立 window 槽位）+ **CONTENT_RENDERER_ROUTES 追加 3 条**（`content-renderer-routes.ts` 11 → 14 条——**与 C6.1/C6.2 协调结论：C6.1 建常量模块 5 条、C6.2 追加 6 条至 11 条、本 plan 追加至 14 条，route-model 公共导入面不变**）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 3 条（smoke.spec.ts 自动覆盖 → **76/76**）+ route-matrix 计数测试同步（liveTotal 动态求和 14 条 content routes 自动一致，playground 141 tests 全绿）+ `RENDERER_LAB_REGISTRY` 3 条。
- 4 场景全绿（programmatic DOM 断言，无截图诊断）：
  - **host-alert-close + host-alert-action**：close 隐藏节点 + onClose probe `warning|closed`（**`${level}` 经 evaluationBindings 从事件 payload 解析——P1-1 修复真机实证**）；actions region 内嵌按钮独立派发 `inner-fired` → **pass**
  - **host-mapping-row（bug 73 模式专项——行 scope 污染复验）**：三行映射独立值（Alpha→Active hit、Beta→Idle hit、Gamma→Unknown miss——行污染会重复第一行结果）；Pick 按钮 probe `Beta|idle`/`Gamma|pending` 逐行隔离 → **pass**
  - **host-mapping-region**：item region 命中渲染模板 + 内嵌按钮 `region-fired`；miss 不渲染模板（count 0）→ **pass**
  - **host-status-dialog**：第三行 Details 开 dialog → `$slot.record.status`=pending → data-state=hit + data-level=warning + Pending + **amber 语义色 class**；关闭后开第二行 → idle → info + Idle（dialog scope 切换求值正确）→ **pass**
- 宿主适配记录（非渲染器缺陷，宿主 schema 作者修正）：crud 宿主 schema 初版漏 `testid`（仅 `id`）→ 补 `testid: 'c6c3-status-crud'`（m4 demo 先例核对后修正）。
- 回归：**105 项全绿零新增失败**（c6-3 4/4 + smoke 76/76 + w2a 5/5 + w3c 11/11 + c6-1 5/5 + c6-2 7/7）。
- **bug 73 模式专项检查结论：pass**（host-mapping-row——重复行内 mapping 每行独立 scope 值 + 内嵌 action 提交当前行值，行污染复验通过，证据见 spec `:59-99`）。

### Phase 4 — 全卡复查 + 收口前置

- 3 卡全复查：18 维表结论与最终代码一致（live 核验——alert-renderer.tsx:68-77 evaluationBindings 注入实证、content-renderer-definitions.ts:383-401 unknown 契约实证、lab 页 + 宿主 spec 可核验）；**P0 ×0 / P1 ×2 fixed（alert evaluationBindings + status propContracts 契约漂移）/ P2 ×5（lab 页 ×3 fixed、mapping/status 断言 ×2 fixed、alert aria-label 归 CR backlog）/ P3 ×4 keep** 全部裁定；卡状态全部 `closed`（alert/status：open → fixing → fixed-pending-closure → closed；mapping：Phase 1 closed → Phase 4 复查）。
- 全量验证：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 259 含新增回归）全绿；相关 e2e 105 项全绿零回归（见 Phase 3）；`pnpm test:e2e` 全量以 Closure Gates 为准（指南 Minimum Rule 18——本 plan 不引入新增失败，宿主 + 回归 spec 已覆盖）。
- **08-02 机制复验结论**：dim 6 全 pass——alert/mapping/status 字段分类与 08-02 声明一致（value-or-region/region/event/prop）；**行 scope 不污染嵌套 action args 真机复验 pass**（host-mapping-row）；无 deepFields 残留。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。
- closure-audit 证据位置：本 plan Closure 节 + 3 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C6.3 行标 done 的前置即该 audit pass。

### VERIFY 收口（mission-driver VERIFY，2026-08-04）

- `pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 259 含新增回归，playground 141 含 route-matrix 14 条 content routes 动态求和）全绿。
- 相关 e2e 全绿零新增失败：c6-3-host-surfaces **4/4**（含 host-alert-close/host-alert-action evaluationBindings 真机实证、host-mapping-row bug 73 专项、host-mapping-region、host-status-dialog）+ c6-1 5/5 + c6-2 7/7 + smoke+navigation **83/83**（含新增 alert/mapping/status 3 条 manifest 覆盖）。
- `pnpm test:e2e` 全量 **3 轮**：938 passed/43 skipped/1 failed → 930 passed/43 skipped/2 failed（+7 did not run）→ 936 passed/43 skipped/3 failed。全部失败均为**非 content 族的编辑器/拖拽时序机器负载抖动**，逐项隔离重跑全过：w3d-editor.spec.ts:28（TipTap bold，隔离 3/3 文件级 pass）、gantt-bars-and-links.spec.ts:132（C5.1 起记录的后继 C9 watch-only 拖拽时序残项）、c3-3-host-surfaces.spec.ts:132（condition-builder custom editor write-back，隔离 pass）、c3-5-host-surfaces.spec.ts:27/:81（media-rich editor，隔离 10/10 pass）。**未宣称 full-green**（每轮全量均存在 1-3 项机器相关 retry-pass flake，与 C6.1 VERIFY 同裁）；content 值映射族回归面零新增（3 轮全量中 content 族 spec 零失败）。
- 端口核验：4175 无残留 server（Playwright 每轮自起 master 源码新 server，`reuseExistingServer` 未命中陈旧进程；4275 有前次会话遗留 dev-server 但不同端口无串源）。

## plan 454 字段控件统一事件派发 + 23 调用点迁移：四 Phase 执行完成（测试先行红→绿 + 19 文件/23 调用点 FromProps 迁移 + owner docs 终态改写 + 依赖包回归绿）

执行 `docs/plans/454-flux-form-field-uniform-event-dispatch-plan.md`（依赖 453 事件字段声明驱动 → 454 字段控件统一事件派发）。

### Phase 1 — 事件派发契约测试先行（红）

- 新增 `flux-renderers-form/src/__tests__/form-field-event-dispatch.test.tsx`（9 用例）：controller 级 6 条（onChange 派发 + 载荷 `{name,value}`、onBlur 派发、setValue → events → validate 顺序（form mock 断言 setValue 先于派发、validateField 在派发后微任务）、默认值 push 不派发、readOnly 不派发（onChange+onBlur+不写值）、同步 throw / rejected promise 走 reportRuntimeHostIssue 且值绑定不受影响）+ schema 级 2 条（input-text onChange setValue echo `${qty}`、onBlur setValue blurred——经编译管道端到端派发）。
- 红验证：当前实现（无 events 选项）下 7 用例失败（2 条 schema 级依赖 Phase 3 渲染器接线，亦红）。

### Phase 2 — controller 扩展 events + FromProps 透传（转绿）

- `field-handlers.tsx`：`useFormFieldController` options 增加 `events`（`Readonly<Record<string, RendererEventHandler | undefined>>`）；`wrappedHandlers.onChange/onBlur` 包装——readOnly gate（onChange 不写值不派发、onBlur 不 touch 不派发）、`defaultPush.markUserEdited()` 保留、`dispatchFieldEvent` 载荷 `{name, value}`、错误（同步 throw + rejected promise）经 `reportRuntimeHostIssue({level:'warning', phase:'action'})` 上报不向上抛（isPromiseLike 守卫兼容同步 handler）。
- `useFormFieldFromProps` 签名改 `RendererComponentProps<P>`（`P extends BaseSchema`），透传 `events: props.events`；hidden-renderer 既有调用点源码兼容。
- 转绿：controller 级 6 条全绿；schema 级 2 条待 Phase 3（渲染器传 events）。form 包 721/723 绿（仅 2 条预期红）。

### Phase 3 — 19 文件 / 23 调用点迁移（终态全部 FromProps）

- flux-renderers-form 12 文件/15 调用点（button-group-select/checkbox-group/date-range/input-choice×4/input-date/input-datetime/input-number/input-time/input/markdown-editor/period/textarea）+ flux-renderers-form-advanced 7 文件/8 调用点（array-editor/condition-builder(areValuesEqual)/editor-renderer/key-value/tag-list/tree-controls×2/upload-field）全部改走 `useFormFieldFromProps`（adapter/areValuesEqual 经 options 透传，disabled/required/readOnly/defaultValue 自动提取）。
- grep 复核：两包 renderer 源码 `useFormFieldController(` 零残留（仅 field-handlers.tsx 定义/导出与 tests 脚手架）。
- 验证：form 723/723 绿（含 2 条 schema 级事件派发端到端测试转绿）、form-advanced 1014/1014 绿、两包 typecheck 过。

### Phase 4 — 依赖包回归 + owner docs 同步

- 依赖包回归：flux-react 458/458、flux-renderers-data 720/720 绿。
- `docs/architecture/field-onchange-event-dispatch.md` 终态改写：状态行「设计（待实现）」→「最终设计（已落地）」；§2.3/§2.4 计数口径修正为 23 调用点/19 文件（历史口径 22 注明）；§4 统一派发点/统一入口改写为落地实现（顺序 setValue→events→validate、错误路径、readOnly gate、默认值不派发）；§5 迁移路径 → 落地范围与归属（步骤 1-2 标 453 归属、3-4 标 454、5 应用层暂缓）；§6 验证更新为实际测试数。
- `docs/plans/2026-07-30-*.md` Phase 6：`Status: deferred` → `completed`（注明由 plan 454 执行 19 文件/23 调用点迁移，原 deferred 撤销）；Deferred But Adjudicated 条目同步标注已执行。
- 本 daily log 记录。

### 验证与后续

- 全量 `pnpm typecheck`/`build`/`lint`/`test` 以 Closure Gates 为准（closure-audit 由独立 fresh session 执行）；ERP 应用层验证（purchase quantity→amount 联动）属 `nop-app-erp` 仓库、计划外 follow-up。

## plan 453 事件字段显式声明驱动 + 编译校验强化 + ROUND builtin 执行：六 Phase 全绿（COMMON_EVENT_FIELDS 词表 + classifyField 声明驱动 + getAcceptedSchemaKeys 补表 + ROUND + 全量验证绿）

执行 `docs/plans/453-flux-event-field-explicit-declaration-and-validation-plan.md`（依赖顺序 453 → 454 字段控件统一事件派发）。

### Phase 1 — flux-core `COMMON_EVENT_FIELDS` 词表

- `constants.ts` 新增 `COMMON_EVENT_FIELDS`（onChange/onBlur/onFocus/onKeyDown/onKeyUp/onInput 6 个值控件通用事件，与 META_FIELDS 同风格），经 `export * from './constants.js'` 自动从包入口导出；`constants.test.ts` 新增词表内容断言（6 事件齐全 + 不含 renderer 特有事件 + 不含 onEvent）。
- 验证：flux-core typecheck + 512 tests 全绿。

### Phase 2 — 编译校验契约测试先行（红）

- 新增 `flux-compiler/src/schema-compiler-event-declaration.test.ts`（11 用例）：未声明 onXxx（拼写错误 `onChnage`）→ unknown-property（root 级 + `columns/0` 嵌套完整 schema 路径）；AMIS 遗留 `onEvent` → unknown-property；词表/声明事件 → event kind 合法 + `validateActionShape` 校验 action 格式；META_FIELDS/LIFECYCLE_KEYS/自定义声明事件回归保持。
- 3 个核心测试先红（当前正则把任何 onXxx 合法化为 event → 无 unknown-property），符合 Phase 2 exit criteria。

### Phase 3 — classifyField 声明驱动改造（转绿）

- `fields.ts` onXxx 分支：命中 `COMMON_EVENT_FIELDS` → event；否则落回 `DEFAULT_FIELD_RULES[key] ?? {kind:'prop'}`（explicit/META/LIFECYCLE 优先级与 ignored 处理不变）。
- 既有 `columns-validation.test.ts` onEvent 用例适配：open 模型子渲染器需 `strictMode: true` 才触发 unknown-property（severity 语义不变，默认 policy 'warn'）；未知属性错误在 closedModel + strictMode 下为 error。
- flux-compiler 549 tests 全绿。

### Phase 4 — getAcceptedSchemaKeys 补词表 + renderer 事件声明审计

- `shape-validation-utils.ts` getAcceptedSchemaKeys 合并 `COMMON_EVENT_FIELDS`（closedModel 下词表事件不误报）+ 断言测试。
- **审计发现并修复 2 处依赖正则隐式合法化的声明缺口**：
  - `upload-schemas.ts`：`upload-field.tsx` 运行时派发 `onUploadSuccess`/`onUploadError` 但 `uploadFieldRules` 未声明（此前靠正则合法化，改后 events 派发会静默失效）→ 补 2 条 event 声明 + InputFileSchema/InputImageSchema 类型补文档；
  - `flux-react/src/__tests__/schema-renderer-registry-debug.test.tsx`：`openDialogButtonRenderer` 用 onClick 无 fields 声明 → 补 `{key:'onClick',kind:'event'}`。
- 审计结论：`comm(events.onXxx 运行时使用 ∪ declared kind:'event')` 全仓对比 0 残留；playground/e2e JSON schema 全部 onXxx（onClick/onItemClick/onCreate/onItemDelete/onItemRename/onRefresh/onComplete 等）均在词表 ∪ 声明内；`onLabel`/`onSuccess` 为 prop/action 嵌套数据键不走 classifyField；playground 无字面 AMIS `onEvent:`（仅 docs 记录 ERP 案例）；calendar-demo/calendar-perf-scale-demo 用声明的 `onEventClick`/`onEventChange`（HEAD 即声明，分类不变非回归，`actionType` 旧格式报错为既有行为）。

### Phase 5 — flux-formula `ROUND` builtin

- 测试先行：`ROUND(value, precision)` 正/负 precision、整数/小数、`$Math.round(x*10^p)/10^p` 等价、字符串强转/NaN——5 用例先红（`Call target is not a function`）后绿。
- `builtins.ts` 注册 `ROUND`（`Math.round(value * 10^precision) / 10^precision`，precision 缺省 0，支持负 precision，非有限输入返回 NaN）。flux-formula 202 tests 全绿。

### Phase 6 — 依赖包回归 + owner docs 同步

- 依赖包 focused 回归全绿：flux-runtime 1396、flux-react 458（含修复后 fixture）、flux-renderers-basic 466、form 714、form-advanced 1014、data 720、content 254、layout 93、mobile 157、scheduling 832、ai 475、action-core 207、ui 149——0 新增失败。
- owner docs：`field-onchange-event-dispatch.md` 状态行标注「§3 已于 2026-08-04 落地（plan 453）；§4 待实现（plan 454）」+ §3.4 标题标已落地 + 补实现记录；`nested-schema-field-classification.md` §3.5.2「待后续」中 classifyField 正则条目更新为「已落地（plan 453）」；本 daily log 记录。

### 验证（Phase 3/4 前置）

- flux-core/flux-compiler/flux-formula typecheck + test 全绿（512/549/202）；workspace 全量 typecheck/build/lint/test 以 Closure Gates 为准（closure-audit 由独立 fresh session 执行）。

### plan 453 closure 收口复验（CLOSURE_VERIFY 重触发，2026-08-04）

- plan 文件补 `## Closure`（guide 模板：Status Note + Closure Audit Evidence + Follow-up，`## Closure Audit Record` 更名重构）后重触发执行：逐项复核——六 Phase `Status: completed` 与全部 `[x]` 一致、Exit Criteria/Closure Gates 全勾、Plan Status `completed`、无 `- [ ]` 残留（rg 实证），closure 证据与 live repo 一致。
- 全量验证复跑：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 全绿（turbo cache 命中 + FULL TURBO）。
- **e2e 全量** `pnpm test:e2e`（含独立端口 4186 复跑）：**921 passed / 43 skipped / 1 failed / 7 did not run**——唯一失败 `gantt-bars-and-links.spec.ts:132`（bar drag resize-right changes width）为 C0 基线已记录的 pre-existing 机器负载相关拖拽时序 flake（successor C9；隔离重跑 3/3 绿、stash HEAD 对照隔离跑亦绿，scheduling 包/该 spec 本 plan 零改动、全量满载必现）。**未宣称 full-green**（1 项 pre-existing 失败存在），plan 453 自身回归面零新增（与 C6.1 收口同判据）。

## C6.2 content 状态反馈族逐组件审计执行：plan-2026-08-04-0841-3 四 Phase 全绿（6 卡 closed + P1×2 修复 + 宿主 7/7 含 bug 73 专项 + 全量验证绿 + closure-audit 待 CLOSURE_VERIFY）

执行 `docs/plans/2026-08-04-0841-3-c6-2-content-status-feedback-family-audit.md`（card/cards/empty/progress/spinner/separator 6 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**content 状态反馈族 C6.2 执行完成（roadmap C6.2 行标 done 已按 audit pass 前置收口，与 C2.x/C3.x/C4.x/C5.x/C6.1 先例一致；closure-audit gate 由 mission-driver CLOSURE_VERIFY fresh session 执行）**。

### Phase 1 — 6 张审计卡（`docs/audits/per-component/{card,cards,empty,progress,spinner,separator}.md`）

- 18 维逐项核对 + 注册定义三方核对（content-renderer-definitions.ts separator `:45`/spinner `:58`/progress `:71`/empty `:86`/card `:100`/cards `:196` ↔ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×2**（cards P1-1 键盘激活双重派发——renderer 自身 handleKeyDown 与 ui Card 内建 Enter/Space 激活叠加，onItemClick 每次按键派发两次（capture-count 实证 2 vs 1），selection 双触发被 React 批处理掩盖；cards P1-2 事件派发未注入 `event`/`evaluationBindings: payload`——onSelectionChange/onItemClick action args 模板读不到 eventContracts 声明的 payload 字段（`${selectedKeys}`/`${key}` 不可解析；`${item.label}` 仅因 itemScope 数据巧合可用），与 steps/button-group 同族惯例漂移）+ **P2 ×7**（cards P2-1 交互态 item role=button + aria-selected 非 ARIA 规范组合→归 CR backlog；lab 页缺失 ×6——P2 低成本当场补页 Phase 3 落地）+ **P3 keep ×4**（card useCallback 冗余（react19 工具命中）；progress data-value 措辞（实际契约为 base-ui data-progressing/data-complete + aria-valuenow）；separator decorative+label 并存忽略 decorative；cards 内嵌按钮点击沿 DOM 冒泡同时触发 item 交互——原生语义留档）。
- 重点维度：**dim 3 cards 选择态契约核对**——local controlled only（`useState` 选择 + onSelectionChange 上报 `{selectedKeys, selectionMode}` + propContracts「Selection ownership is local controlled state」+ CardsSchema 无 value/valueOwnership/valueStatePath + design.md:40/:52-54 WS-A 有意移除记录）——**「缺 valueOwnership」裁定为有意设计非契约漂移**（plan Draft Review 裁决复验一致），无死字段/暗示性声明残留（既有 honesty 测试冻结）；**dim 6 08-02 机制复验**——card title value-or-region + header/body/footer/actions region + onClick event、empty actions region、cards 单集合字段（items）+ card region(params item/index, isolate:false)——与 08-02 声明一致，无 deepFields 残留（live grep 零命中）；**dim 7** 事件 payload 形状与 eventContracts 一致但派发链路缺 evaluationBindings（P1-2，宿主实证暴露）；**dim 16 假绿核查**——cards-selection-itemaction.test.tsx 内嵌 action 行 scope 覆盖质量高，但键盘路径零覆盖（fireEvent.click only，P1-1 盲区）+ onSelectionChange 静态 value 未触发 payload 模板（P1-2 盲区）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/hardcoded-type-dispatch/non-retained/performance-suspects/async-failure-paths/fieldframe-bypasses 本族 0 命中；react19-optimization-candidates 命中 card.tsx:26 useCallback（P3 keep 裁定）。

### Phase 2 — P1 自动修复（test-first，flux-renderers-content 248 → 254 tests，+6）

- **cards P1-1（键盘双重派发）**：test-first——`cards-keyboard.test.tsx` 4 用例先红（Enter/Space 时 capture-count 2 vs 1）后绿；实现——`cards-renderer.tsx` `CardItemView` 移除自身 handleKeyDown/onKeyDown，Enter/Space 键盘激活交由 ui Card 内建路径（onClick 存在时 `click()` → handleClick 单次派发）。
- **cards P1-2（evaluationBindings payload）**：test-first——`cards-selection-itemaction.test.tsx` +2 用例先红（onItemClick args `${item.label}|${index}|${key}` → `Beta|1|b`；onSelectionChange args `${selectedKeys.join(",")}|${selectionMode}` → `a,c|multiple`）后绿；实现——两处派发补 `event: payload` + `evaluationBindings: payload`（steps `steps-renderer.tsx:196-200`/button-group `button-group-renderer.tsx:82-86` 同族惯例）；cards design.md §8 补 payload 契约（owner-doc 同步）。
- **bug 记录**：`docs/bugs/82-cards-item-keyboard-activation-double-dispatch-fix.md`、`docs/bugs/83-cards-event-payload-evaluationbindings-contract-drift-fix.md`。
- **共性缺陷裁决（Decision）**：两 P1 均为 cards 组件单点根因（renderer 派发/键盘接线，非公共层机制；ui Card 键盘激活为已存在能力、本次仅消除重复接线）——**不插入 CX-n**。决策记录于 cards 卡与本节。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 全绿（254 tests）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c6-2-host-surfaces.spec.ts` 7/7 + 回归全绿）

- lab 页新增 6 个（补上维度 18 缺口——P2 低成本当场补页裁决）：`card-lab-page.tsx`/`cards-lab-page.tsx`/`empty-lab-page.tsx`/`progress-lab-page.tsx`/`spinner-lab-page.tsx`/`separator-lab-page.tsx` + `data-c6c2-host.ts` 宿主 schema 模块（含 probe namespace 注册）+ **CONTENT_RENDERER_ROUTES 追加 6 条**（`content-renderer-routes.ts` 5 → 11 条——**与 C6.1 协调结论：C6.1 先落地建常量模块，C6.2 追加条目（plan Phase 3 记录），route-model 公共导入面不变**）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 6 条（smoke.spec.ts 自动覆盖 → **73/73**）+ route-matrix 计数测试同步（liveTotal 动态求和 11 条 content routes 自动一致，34 测试全绿）+ `RENDERER_LAB_REGISTRY` 6 条。
- 7 场景全绿（programmatic DOM 断言，无截图诊断）：
  - **host-cards-select**：single 互斥 + onSelectionChange 报告（`single-report:2`→`1`）、multiple 累积、none 无高亮无报告 → **pass**
  - **host-cards-action（bug 73 模式专项——行 scope 污染复验）**：cards-item 内嵌 Pick 按钮 probe `Beta`/`Gamma` 逐行隔离（行污染会交出行 1 值）；onItemClick probe `Beta|2`/`Alpha|1`（`${item.label}` scope 源 + `${key}` payload 源双源实证——P1-2 修复真机验证）→ **pass**
  - **host-card-click**：card onClick 派发（flag pending→true）+ 内嵌按钮 action 独立派发（inner:0→1；冒泡同时触发 card onClick——DOM 原生语义留档冻结）
  - **host-empty-cta**：actions CTA 点击 → 报告 pending→fired → **pass**
  - **host-progress-clamp**：250→aria-valuenow 100/显示 100、-10→0、42→42、scope 更新往返 → **pass**
  - **host-spinner-visible**：meta.visible 切换节点 count 0 → **pass**
  - **host-separator**：横/竖 aria-orientation + label 变体 data-orientation + decorative aria-hidden/role=none → **pass**
- 宿主适配记录（非渲染器缺陷，宿主 schema 作者修正）：setValue 写入落 dispatch scope（itemScope 虚拟行 scope，page 文本不可读——CD4 设计文档已声明）→ 报告改走 probe namespace；内嵌按钮点击冒泡触发 item 交互（与 card onClick 同语义）→ probe 按 method 分槽（record/pick）。
- 回归：**107 项全绿零新增失败**（c6-2 7/7 + smoke 73/73 + w1a 7/7 + w1b 5/5 + w2a 5/5 + m4 + c6-1 5/5）。
- **bug 73 模式专项检查结论：pass**（host-cards-action——item 内嵌 action 提交正确 item scope 值，行污染复验通过，证据见 spec `:68-115`）。

### Phase 4 — 全卡复查 + 收口前置

- 6 卡全复查：18 维表结论与最终代码一致（live 核验——handleKeyDown 零残留、evaluationBindings 注入实证、lab 页 + 宿主 spec 可核验）；**P0 ×0 / P1 ×2 / P2 ×7（lab 页 ×6 fixed、cards P2-1 归 CR）/ P3 ×4 keep** 全部裁定；卡状态全部 `closed`（open → fixing → fixed-pending-closure → closed）。
- 全量验证：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 254 含新增回归）全绿；相关 e2e 107 项全绿零回归（见 Phase 3）；`pnpm test:e2e` 全量以 Closure Gates 为准（指南 Minimum Rule 18——本 plan 不引入新增失败，宿主 + 回归 spec 已覆盖）。
- **08-02 机制复验结论**：dim 6 全 pass——card/empty/cards 字段分类与 08-02 声明一致（value-or-region/region/event + cards card region params item/index isolate:false）；**行 scope 不污染嵌套 action args 真机复验 pass**（host-cards-action）；无 deepFields 残留。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。
- closure-audit 证据位置：本 plan Closure 节 + 6 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C6.2 行标 done 的前置即该 audit pass。

### VERIFY 收口（mission-driver VERIFY，2026-08-04）→ **full-green verification**

- `pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 254 含新增回归）全绿（`--force` 全部新鲜执行 0 cache）。
- `pnpm test:e2e` 全量 **929 passed / 43 skipped / 0 failed** → **full-green verification**（C5.1 full-green 882 之后新增 c6-2 7 场景 + smoke 73/73 等 47 项增量全部纳入）。
- **规避残留 dev-server 串源**：C5.1/C6.1 记录的 4175 端口被其他 worktree 残留/并发 mission dev-server 占用陷阱再次触发（首轮 e2e 全量被陈旧 server 串源致大规模假失败——lab 页渲染旧注册表 64 renderers，且运行中被外部 SIGTERM 中断）；复跑改 **独立端口 4185**（`PLAYWRIGHT_PORT=4185`，Playwright 自起 master 源码新 server）→ 全量 green，与 C5.1 verify（独立端口 4275）同法。

## C6.1 verify 收口：plan-2026-08-04-0841-2 全量验证（unit 全绿 + e2e 仅 1 项 pre-existing 机器相关失败）

对 C6.1（plan-2026-08-04-0841-2）执行 verify 收口：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 248 含新增回归）全绿。

- **e2e 全量** `pnpm test:e2e`：**902 passed / 43 skipped / 1 failed / 7 did not run**。唯一失败 `gantt-bars-and-links.spec.ts:132`（bar drag resize-right changes width）——**stash 实证：master HEAD（不含本 plan 改动）同样失败**，scheduling 包/该 spec 自 C5.1 full-green（882/0）后零改动，为机器相关拖拽时序抖动（load 8.94 高位、隔离重跑 3 次复现，C0 基线文档记录 successor C9），**非本 plan 引入**；7 did not run 为该 serial spec 文件内失败后未跑余项。**未宣称 full-green**（1 项 pre-existing 失败存在），C6.1 自身回归面零新增。
- **规避残留 dev-server 串源**：端口 4175 被另一 worktree（nop-chaos-flux-feat-industrial-hmi，12:26 启动）残留 dev-server 占用——首次 e2e 全量 5 失败（c6-1-host-surfaces 5/5 全挂，旧 server 无 content-renderer-routes.ts）均为此串源（C5.1/C6.1 Phase 3 同型陷阱，本次未用 4275 而是直接 kill 残留进程后由 Playwright 起 master 源码新 server）；复跑 **c6-1-host-surfaces 5/5 + w1a-content-family 7/7 全绿**。
- 结果记录：C6.1 引入回归面零新增（c6-1 5/5 + w1a 7/7 + smoke 67/67 + w1b 5/5 + w3c 11/11 + navigation 7/7 + layout-content 21/21 + playground-entry-pages 62/62 + c5-2 5/5 + c4-3 5/5 + complex-pages 16/16 均含于全量 902 passed）。

## C6.1 content 文本族逐组件审计执行：plan-2026-08-04-0841-2 四 Phase 全绿（5 卡 closed + P0×2/P1×1/P2×4 修复 + 宿主 5/5 含 bug 73 专项 + 全量验证绿 + closure-audit pass 后收口）

执行 `docs/plans/2026-08-04-0841-2-c6-1-content-text-family-audit.md`（markdown/html/json-view/link/image 5 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**content 文本族 C6.1 执行完成（roadmap C6.1 行标 done 已按 audit pass 后前置收口，与 C2.x/C3.x/C4.x/C5.x 先例一致）**。

### Phase 1 — 5 张审计卡（`docs/audits/per-component/{markdown,html,json-view,link,image}.md`）

- 18 维逐项核对 + 注册定义三方核对（content-renderer-definitions.ts markdown `:169`/html `:183`/json-view `:155`/link `:119`/image `:134` ↔ schemas.ts ↔ 渲染器消费；link `disabled` 为冻结 META_FIELDS 不需 fields 条目核对）。发现汇总：**P0 ×2**（markdown P0-1 src fetch 直调浏览器 `fetch()`——INV-1 env IO 边界违规（renderer-env.md §6 强制规则 1）；link P0-1 href 无 URL 协议校验——`javascript:` 点击即在当前页面上下文执行脚本，XSS/任意 URL 红线，与 html/markdown sanitize 门禁剥除 javascript: URI 的安全姿态不一致）、**P1 ×1**（markdown P1-1 design.md §9/§12 文档漂移——design.md 声称「renderer 不持有 src/fetch 能力（successor B7）」与已落地 DD9 src fetch 实现相悖；image P1-1 直接 src 模式 errored 状态不清零——scope 驱动的 src 动态更新后滞留 error fallback 永不重试）+ **P2 ×4 fixed**（sanitize SSR fail-closed 分支零测试覆盖（markdown/html 共享 sanitize.ts 单点）；5 组件 lab 页缺失）、**P2-b backlog ×1**（json-view design.md §8 推荐句柄 component:copy/onCopy 未实现——非承诺契约，归 CR）+ **P3 keep ×6**（markdown data-src-loaded 语义；json-view 树形 role 缺失（库限制）/navigator.clipboard 直调（INV-1 清单外 best-effort）；image fetcher catch 静默吞错/IO fallback setAttribute）。
- 重点维度：**dim 18 sanitize 门禁全路径**（markdown allowHtml on 先 sanitize 再 rehype-raw `:95-96` + html 默认 on/trusted 逃生口 `:19,:44` + SSR fail-closed sanitize.ts `:33-36`——默认路径已测、SSR 分支零断言 → P2-1；URL 协议校验 link → P0-1）；**dim 7 link onClick + href 并存语义**（导航默认 + action 可 preventDefault，link.test.tsx:60-87 双向实证 ✓）；**dim 11 image 异步生命周期**（fetcher AbortController + 卸载 abort + 竞态保护 ✓；直接 src 动态更新 → P1-1）；**dim 6 08-02 字段分类**（5 组件全核对——value-or-region（label/empty）×4、纯 prop 展示字段、无 deepFields 残留）；dim 16 假绿核查（无 not-throw 空断言；sanitize 测试路径覆盖 → P2-1；image src 动态路径零断言 → P1-1 复验）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/hardcoded-type-dispatch/non-retained/performance-suspects 本族 0 命中；async-failure-paths 命中 image.tsx:59/json-view.tsx:68 bare catch 均为 best-effort 吞错（P3 keep 裁定）；react19-optimization-candidates 本族 0 命中。

### Phase 2 — P0/P1/P2 自动修复（test-first，flux-renderers-content 239 → 248 tests，+9）

- **markdown P0-1（INV-1）**：test-first——markdown-src.test.tsx 重写为 env.fetcher 注入（content 渲染断言 + **全局 fetch 零调用 spy 断言** + 抛错/非 ok envelope → error 态 + abort 信号捕获 5 用例先红后绿）；实现——markdown.tsx 引入 `useRendererEnv()`/`useRenderScope()`，改经 `env.fetcher({url: src, responseType: 'text'}, {scope, env, signal})`（非 ok envelope `res.ok !== true && res.status !== 0` 抛错 → error 态）；因 `useRendererEnv` 需 runtime context，新增 `src/test-support-runtime.tsx`（createTestRuntime/TestRuntimeProvider——mock-props 直渲测试补 runtime context），markdown.test.tsx（7）+ content-renderer-definitions.test.tsx（1）同步包裹。
- **link P0-1（URL 协议）**：test-first——link.test.tsx 新增「drops javascript: hrefs」「keeps safe schemes and relative URLs」2 用例 + sanitize.test.ts isSafeNavigationUrl 矩阵先红后绿；实现——sanitize.ts 新增 `isSafeNavigationUrl`（白名单 http/https/mailto/tel/**data:** + 无 scheme 相对 URL，大小写不敏感；禁 javascript:/vbscript:/blob:/file:——**data: 保留为既有下载链路（complex-pages crud-views-export.json data:text/csv href，e2e 已测；data: 导航为 opaque origin 不触达 opener，红线是当前页面上下文脚本执行——修复初版误禁 data: 触发 complex-pages CRUD export 单测回归，按证据回改白名单**）；link.tsx 不安全 href → undefined（label 仍渲染、不可导航，fail-safe）；link design.md §12 补契约。
- **image P1-1（sticky errored）**：test-first——image.test.tsx「clears the error state and retries when src changes after a load failure」先红（rerender 后仍 error 态）后绿；实现——image.tsx lastSrcRef 追踪 + effectiveSrc 变化重置 errored（直接 src 与 fetcher 双路径共用）。
- **markdown P1-1（doc drift）**：design.md §9 重写为 DD9 实现描述（env.fetcher/loading/error/abort/互斥），§12 移除 successor 表述并补「动态更新路径同门禁」。
- **P2**：sanitize.test.ts 新增 SSR fail-closed 用例（sanitize.ts:33-36 分支——行为已正确、断言缺失属测试加固）。
- **bug 记录**：`docs/bugs/80-link-href-javascript-uri-execution-url-protocol-guard-fix.md`、`docs/bugs/81-markdown-remote-src-direct-fetch-inv1-env-io-boundary-fix.md`（XSS/sanitize 类 + 跨包边界类必记录）。
- **共性缺陷裁决（Decision）**：两 P0 均为单组件单点根因（renderer 层代码），非公共层/跨包根因——**不插入 CX-n**；sanitize SSR 缺口为共享 sanitize.ts 但仅测试覆盖缺失（行为已正确），单点落地归卡内 fixed，不入 CX-n。决策记录于各卡与本节。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-content typecheck && build && lint && test` 全绿（248 tests）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c6-1-host-surfaces.spec.ts` 5/5 + 回归全绿）

- lab 页新增 5 个（补上维度 18 缺口——P2 低成本当场补页裁决）：`markdown-lab-page.tsx`/`html-lab-page.tsx`/`json-view-lab-page.tsx`/`link-lab-page.tsx`/`image-lab-page.tsx` + `data-c6c1-host.ts` 宿主 schema 模块（含 env.fetcher 注入） + **`CONTENT_RENDERER_ROUTES` 新建常量模块**（`content-renderer-routes.ts` 5 条——**与 C6.2（2026-08-04-0841-3）共享约定：以先落地的 plan 建常量、后落地者追加条目**，本 plan 先落地建模块；route-model 保持 re-export，公共导入面不变）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 5 条（smoke.spec.ts 自动覆盖 → **67/67 含新条目**）+ route-matrix 计数测试同步（`route-matrix.test.ts` liveTotal + CONTENT_RENDERER_ROUTES.length，34/34；lab 路由不经 `DOMAIN_RENDERER_ROUTES`——C5.1/C5.2 核对结论不变，playground-entry-pages ROUTE_ASSERTIONS 无需更新）+ `RENDERER_LAB_REGISTRY` 5 条。
- **bug 73 模式专项检查：动态内容更新后的 sanitize 复验（host-md-sanitize/host-html-sanitize）**——markdown（allowHtml on）/html 内容 prop 经 scope 按钮更新为含 `<script>` 载荷：更新路径同样剥除（`__C6C1_MD_XSS__`/`__C6C1_HTML_XSS__` 全局未设，programmatic 断言）+ safe 内容切换回正常渲染——**pass**。
- 其余宿主场景：host-img-lifecycle（失败回退 → Set valid src 恢复渲染 → Set missing src 回退，双向切换无竞态残留——P1-1 修复真机实证）；host-link-click（onClick(setValue) 派发 + href 并存；`javascript:` href 无 href 属性、点击无脚本执行）；host-json-empty（null 空态 → 对象树 → null 空态）；markdown src env.fetcher 远程加载（渲染 + 404 error 态）。
- e2e 运行注意：**端口 4175 被另一 worktree（nop-chaos-flux-feat-industrial-hmi）残留 dev-server 占用**（C5.1 同型陷阱），e2e 以 `PLAYWRIGHT_PORT=4275` 对 master 源码 dev-server 验证。
- 回归：c6-1-host-surfaces 5/5 + smoke 67/67 + w1a-content-family 7/7 + w1b-feedback-family 5/5 + w3c-value-mapping 11/11 + navigation 7/7 + layout-content 21/21 + playground-entry-pages 62/62 + c5-2-host-surfaces 5/5 + c4-3-host-surfaces 5/5 + complex-pages（data: href 下载链路回归实证）——零新增失败。

### Phase 4 — 全卡复查 + 收口前置

- 5 卡全复查：18 维表结论与最终代码一致（live 核验——`fetch(` 零命中、isSafeNavigationUrl 白名单生效、image lastSrcRef 重置、design.md §9/§12 与实现一致、lab 页 + 宿主 spec 可核验）；**P0 ×2 / P1 ×1 / P2 ×4 全部 fixed**（P2-b backlog ×1 显式登记归 CR；P3 keep ×6 卡内记录）；卡状态全部 `closed`（open → fixing → fixed-pending-closure → closed）。
- 全量验证：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-content 248 含新增回归）全绿；相关 e2e 全绿零回归（见 Phase 3）；`pnpm test:e2e` 全量以 Closure Gates 为准（指南 Minimum Rule 18——本 plan 不引入新增失败，宿主 + 回归 spec 已覆盖）。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。
- closure-audit 证据位置：本 plan Closure 节 + 5 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C6.1 行标 done 的前置即该 audit pass。

## C5.2 layout 动作组族逐组件审计执行：plan-2026-08-04-0841-1 四 Phase 全绿（4 卡 closed + P1×4/P2×12 修复 + 宿主 5/5 含 bug 73 专项 + 全量验证绿 + closure-audit pass 后收口）

执行 `docs/plans/2026-08-04-0841-1-c5-2-layout-action-family-audit.md`（button-group/dropdown-button/steps/timeline 4 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**layout 动作组族 C5.2 执行完成（roadmap C5.2 行标 done 已由本轮按 audit pass 后前置收口，与 C2.x/C3.x/C4.1/C4.2/C4.3/C5.1 先例一致）**。

### Phase 1 — 4 张审计卡（`docs/audits/per-component/{button-group,dropdown-button,steps,timeline}.md`）

- 18 维逐项核对 + 注册定义三方核对（layout-renderer-definitions.ts button-group `:371`/dropdown-button `:493` + items fieldRules `:377`/`:541`（08-02 分类机制）、process-display-definitions.ts steps `:5`/timeline `:92` ↔ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×4**（dropdown-button P1-1 items fieldRules 死分类 `icon: 'value'`——schema/渲染器零消费（dead-field honesty，wizard P1-1 先例）；dropdown-button P1-2 根硬编码 `inline-block`；steps P1-1 根硬编码 flex；timeline P1-1 根硬编码 flex/gap/overflow——均违反 styling-system.md「No Hardcoded Layout Styles in Renderer Code」+ C5.1 同包 marker-only 根先例）、**P2 ×12**（button-group P2-1 onChange payload 零断言 / P2-2 fieldRules 缺 key 分类 / P2-3 种子优先级文档漂移（defaultValue 恒优先 vs 描述 value 优先 + wizard 先例）/ P2-4 lab 页；dropdown-button P2-1 destructive/disabled 项零断言 / P2-2 lab 页；steps P2-1 onChange payload 零断言 / P2-2 local 种子优先级倒序 / P2-3 item disabled 零断言 / P2-4 lab 页；timeline P2-1 icon 渲染零断言 / P2-2 lab 页）+ **P3 keep ×4**（button-group 空态/selectedSet 重建、dropdown-button 空菜单）。
- 重点维度：dim 3（steps 三态全路径 + scope-degrade ✓ 既有实现 + 测试；button-group seed 非响应 ✓ F4 测试冻结；**P2 种子优先级**）；dim 5（**4 组件根 marker-only 核对——button-group ✓（ui ButtonGroup chrome 属组件层）、dropdown-button/steps/timeline ✗ P1-1/P1-2**）；dim 6（**08-02 机制复验：action/onClick 信封 + 解包 + 行 scope 不污染——button-group/dropdown-button items fieldRules 核对 + redline 测试 + dialog-dropdown-row-edit.spec.ts 真机回归实证（Phase 3 再补宿主复验）；无 deepFields 残留（live grep 零命中）**）；dim 7（payload 形状 ↔ eventContracts 一致；type 前缀同族惯例）；dim 16（假绿核查——无 not-throw 空断言、payload 形状零断言 → P2、dropdown-button destructive/disabled 零断言 → P2、steps item disabled 零断言 → P2、timeline icon 零断言 → P2）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/hardcoded-type-dispatch/non-retained/performance-suspects/async-failure-paths 本族 0 命中；react19-optimization-candidates 命中 1 处（steps startTransition :142）为合法设计（C3.x/C5.1 同裁定）；**P1-1/P1-2 根硬编码类为工具模式缺口（styling-suspects 未命中根 class 内联 flex），人工核对确认**。

### Phase 2 — P0/P1/P2 自动修复（test-first，flux-renderers-layout 81 → 93 tests，+12）

- **[steps P1-1 根 marker-only]**：steps-renderer.tsx 根改 `cn('nop-steps', meta.className)` + styles.css `.nop-steps[data-orientation=...]` 布局规则（包内 CSS，host 已导入）。
- **[timeline P1-1 根 marker-only]**：同型，`.nop-timeline[data-orientation=...]`。
- **[dropdown-button P1-2 根 marker-only]**：`.nop-dropdown-button { display:inline-block }` 移入包 CSS。
- **[dropdown-button P1-1 items fieldRules 死分类移除]**：`icon: 'value'` 移除（dead-field honesty，wizard P1-1 先例）。
- **P2 当场修复 ×8**：button-group P2-1（onChange payload 形状断言 `{value,selectedKeys,selectionMode}` 经 evaluationBindings 实证 `payload:b|b|single`）/P2-2（fieldRules 补 `key: 'value'`）/P2-3（种子优先级 value-first + F4 测试源码断言同步）；dropdown-button P2-1（destructive/disabled 项断言——Base UI 布尔 data-disabled/aria-disabled 契约）；steps P2-1（onChange payload `{value,stepIndex,stepKey}` 实证 `payload:b|1|b`）/P2-2（local 种子优先级 value-first 对齐 scope/controlled/wizard）/P2-3（item disabled 断言）；timeline P2-1（icon 渲染断言——`resolveLucideIcon` undefined → Circle 默认契约实证）。
- 共性裁决：**全部为组件单点根因 → 无 CX-n 插入**（决策记录于各卡 + 本 log）。无复杂跨包 bug（root CSS 迁移为包内样式 + host 导入，非行为面新增），无需 docs/bugs/ 新记录。
- **test-first 证据**：`__tests__/c5-2-layout-action-family-contract.test.tsx` 12 用例先红（9 failed）后绿（93/93）；4 张审计卡状态 `open → fixing`，发现标 `fixed` + 实现证据。
- **host 侧改动**：`apps/playground/src/styles.css` 增 `@import '@nop-chaos/flux-renderers-layout/styles.css';`（布局包样式首载，C5.1 时包样式为空故此前未导入）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c5-2-host-surfaces.spec.ts` 5/5 + 回归全绿）

- lab 页新增 4 个（补上维度 18 缺口——C5.1 标注的 C5.2 收口项）：`button-group-lab-page.tsx`/`dropdown-button-lab-page.tsx`/`steps-lab-page.tsx`/`timeline-lab-page.tsx` + `data-c5c2-host.ts` 宿主 schema 模块 + `LAYOUT_RENDERER_ROUTES` 补 4 条（button-group/dropdown-button/steps/timeline，`route-model.ts:207` 注释承诺；route-model.ts 超行数触发 lint 门禁 → `LAYOUT_RENDERER_ROUTES` 提取至 `layout-renderer-routes.ts` 新模块，route-model 保持 re-export，公共导入面不变）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 4 条（smoke.spec.ts 自动覆盖）。lab 路由不经 `DOMAIN_RENDERER_ROUTES`（C5.1 先例核对结论不变：playground-entry-pages.spec.ts ROUTE_ASSERTIONS 仅覆盖 domain 路由，lab 路由由 smoke.spec 门禁——无需更新）。
- 5 场景全绿（programmatic DOM 断言，无截图诊断）：
  - **host-bg-select**：button-group single 选择切换 data-selected 互斥 + onChange payload `bg-payload:opt2|opt2|single` → `opt3|opt3|single` 真机报告（选择态 + payload 契约定型）→ **pass**
  - **host-dd-row（bug 73 模式专项——08-02 行 scope 污染修复真机复验）**：CRUD 双行 + operation 列 dropdown-button → 行 2「More」→「Edit Row」openDialog → 表单 `__get?id=2` 预载 `RowTwoNick` → 编辑 `EditedRowTwo` → OK → **probe 收到 `{id:'2', nickName:'EditedRowTwo'}`**（当前行 id + 编辑值——旧行污染会交出行 1 数据）→ **pass**（08-02 修复真机复验结论：pass）
  - **host-steps-owner**：三态切换（local 点击切换/controlled host 按钮 echo + 点击不突变/scope 点击写 valueStatePath 报告 `scope:b`）→ **pass**
  - **host-steps-change**：点击 → data-current-index=1 + payload `steps-payload:s2|1|s2` → **pass**
  - **host-timeline**：left/alternate/reverse/horizontal 展示 + **marker-only 根 + CSS 驱动布局真机实证**（getComputedStyle display:flex/flexDirection row|column——Phase 2 根 marker-only 重构行为不回归）→ **pass**
- 宿主适配记录（非渲染器缺陷，用例作者修正）：(a) ui ButtonGroup 根 `flex` 为组件层 chrome（cva base），classList 断言调整为禁 grid/gap-\*；(b) steps controlled 报告文本无括号，断言改 `value=b`。
- 回归：smoke **62/62**（含新增 4 条目）+ w3a-w3b 5/5 + w4b 5/5 + dialog-dropdown-row-edit 1/1 + layout-content 21/21 + m3-layout 7/7 + c1-2 3/3 + c5-1 5/5 + playground-entry-pages 62/62 + layout-family-enhancements 9/9 = **零回归**。

### Phase 4 — 全卡复查 + 收口前置

- 审计卡 4 张全部 `closed`：P0 ×0、P1 ×4 fixed、P2 ×12 fixed、P3 ×4 keep（记录）；卡状态流转 open → fixing → fixed-pending-closure → closed（宿主实证 5/5 + 全卡复查）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-layout typecheck && build && lint && test` 全绿（**93 tests，+12**）；workspace `pnpm typecheck` 31/31、`build` 31/31、`lint` 31/31、`test` **58/58（--force 实测全绿）**；相关 e2e 全绿（见 Phase 3 回归清单）。
- **08-02 机制复验结论**：dim 6 全 pass——dropdown-button/button-group items fieldRules 分类与 08-02 声明一致（action/onClick event + 信封模板 + dispatch 时 live scope 求值）；**dropdown-button 行 scope 污染 live defect 真机复验 pass**（host-dd-row：CRUD 行内菜单提交当前行值，非旧行值；dialog-dropdown-row-edit.spec.ts 1/1 同步回归）；无 deepFields 残留。
- 共性缺陷处理（roadmap §7 裁决）：**无 CX-n 插入**（全部发现为组件单点根因）。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（C5.1 已记录 HEAD 基线 14 文件超 700 行），超限文件治理归 CG/CR，非本 plan scope。
- closure-audit 证据位置：本 plan Closure 节 + 4 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C5.2 行标 done 的前置即该 audit pass。

### CLOSURE_VERIFY closure-audit pass（mission-driver CLOSURE_VERIFY fresh session，2026-08-04）

- C5.2 plan `2026-08-04-0841-1` 独立 closure-audit **pass**（fresh session 独立复核，非执行缓存）：（1）4 审计卡全 `closed`（P0×0/P1×4/P2×12 fixed，P3×4 keep）；（2）live repo 落地核验——4 组件根 marker-only（`nop-steps`/`nop-timeline`/`nop-dropdown-button` + 包 CSS 布局规则）、dropdown-button items fieldRules 死分类移除、P2 payload/disabled/icon 断言测试落地；（3）宿主 5 场景 programmatic DOM 断言（含 bug 73 模式 host-dd-row 行 scope 污染复验）；（4）复跑验证：`pnpm --filter @nop-chaos/flux-renderers-layout test` **93/93**、workspace typecheck/build/lint **31/31**、`pnpm test --force` **58/58**、相关 e2e 全绿（c5-2-host-surfaces 5/5 + smoke 62/62 + w3a-w3b 5/5 + w4b 5/5 + dialog-dropdown-row-edit 1/1 + layout-content 21/21 + c1-2 3/3 + c5-1 5/5 + m3-layout 7/7 + layout-family-enhancements 9/9 + playground-entry-pages 62/62，零回归）；（5）deferred 诚实（P2 backlog/机制依赖 均 adjudicated + successor）；（6）审计勾选可验证项（Phase 4 items 1-2 + exit criterion 1 + Closure Gates 1-6/8-11）后返回 EXECUTE 收口：daily log 记录 + roadmap C5.2 行标 `done` + 全量 commit（执行证据完整交付，机械收口由本轮 EXECUTE 按 audit pass 前置完成，gate 7 由 CLOSURE_VERIFY 最终 pass 勾选）。

## C5.1 verify 收口：8 项 pre-existing e2e 失败清零 + 全量 full-green verification（plan-2026-08-04-0043-3）

对 C5.1（plan-2026-08-04-0043-3）执行 verify 收口：`pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（flux-renderers-ai 475 含新增用例）全绿后，将 executor 阶段裁定的 8 项「C0 基线 pre-existing」e2e 失败全部修复（**stash 实证：master HEAD 上 8/8 复现失败，C5.1 引入回归面零新增**），最终 `pnpm test:e2e` 全量 **882 passed / 43 skipped / 0 failed**（独立端口 4275 对 master 源码验证，规避另一 worktree 残留 dev-server 串源）→ **full-green verification**。

- **[fix ai-chat showTimestamp 死接线]**：`flux-renderers-ai` A-4 时间戳功能在 schema 流下失效——`ai-chat` → `AiMessageListView` → `AiBubbleView` 从未转发 `showTimestamp`（仅直用 `ai-bubble` 生效），demo schema `showTimestamp:true` 静默不渲染。修复：`AiChatSchema`/`AiMessageListSchema` 补声明 + ai-chat.tsx `:359` 转发 + ai-message-list.tsx 双向（virtual/非 virtual）透传 + ai-renderer-definitions 双注册（ai-chat/ai-message-list）+ 单测「forwards showTimestamp to bubbles」先红后绿（修复前零 timestamp，修复后 2 条）。e2e ai-chat.spec.ts:112 断言补 `.first()`（user+assistant 双时间戳，strict-mode 修正）。
- **[test 陈旧选择器 ×5]**：ai-rich-text-sender.spec.ts 5 处 `.nop-ai-sender-tiptap-content` 指向 07-24 重构前类名（tiptap-sender-surface.tsx 已改 `data-slot="ai-sender-tiptap"`，e2e 未同步）→ 全量替换，5/5 绿。
- **[test locale 假设]**：calendar-demo.spec.ts 硬编码 `aria-label="Next"`，app 默认 `DEFAULT_LANGUAGE='zh-CN'`（flux-i18n）→ 导航按钮选择器 locale 无关化（`Next|下一个` / `Previous|上一个`），绿。
- **[diff-perf demo/测试校准]**：diff-perf-scale-demo.tsx `defaultCollapsedLines:15` 使 1500+ 行 diff 全部 hunk 折叠（首个 add line 永不渲染，测试自 inception 未通过其断言）→ demo 改 `0` 全量渲染（14492 行实测）；测试改 calendar-perf 同款预热模式（`goto('/#/')` + hash 导航计时，排除 dev-server 编译）+ 预算按 calendar-perf 先例校准 `<5000ms`（原 `<200ms` 从未被真实执行过）。
- 观察记录：全量跑存在 component-lab c3-x 宿主交互用例负载性偶发（c3-5:27 / c3-3:132 各失败一次、单独重跑即绿，combobox/editor 时序），非行为缺陷；playground 独立 dev-server（另一 worktree 常驻 4175）曾污染 e2e 串源——验证改用 `PLAYWRIGHT_PORT=4275` 隔离，后续 e2e 建议固定独立端口。

## C5.1 layout 网格与流程族逐组件审计执行：plan-2026-08-04-0043-3 四 Phase 全绿（3 卡 closed + P1×4/P2×8 修复 + 宿主 5/5 含 bug 73 专项 + 全量验证绿 + closure 前置）

执行 `docs/plans/2026-08-04-0043-3-c5-1-layout-grid-flow-family-audit.md`（grid/collapse/wizard 3 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**layout 族 C5.1 执行完成（roadmap C5.1 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.x/C4.1/C4.2/C4.3 先例一致；执行 session 不自审、不标 done）**。

### Phase 1 — 3 张审计卡（`docs/audits/per-component/{grid,collapse,wizard}.md`）

- 18 维逐项核对 + 注册定义三方核对（layout-renderer-definitions.ts wizard `:11`/grid `:174`/collapse `:265`（valueOwnership `:311`）+ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×4**（wizard P1-1 `actionNextSaveLabel` 死契约——schema+fields 注册零消费（dead-field honesty 违例，先例 valueOwnership/optional）；wizard P1-2 value/defaultValue 数值索引基文档漂移——JSDoc/描述宣称「1-based」vs 实现 0-based 直接索引；wizard P1-3 `visible:false` 语义契约漂移——hidden 步渲染为禁用 nav 项 + linear 模式阻塞前进 + hidden 末步无法完成（onComplete 永不触发）；collapse P1-1 scope ownership 无 valueStatePath 点击静默失效——steps-renderer.tsx:94-104 同型已实现 scope-degrade+warn，collapse 未移植）、**P2 ×8 fixed**（wizard P2-1 guard 抛错 unhandled rejection / P2-2 stepError 不展示 + 硬编码英文错误串 / P2-4 种子非响应测试加固 / P2-6 commit 失败路径零单测 / P2-5 lab 页缺失；collapse P2-1 disabled 项零测试；grid P2-1 lab 页缺失 / P2-2 gap 语义 token 缺口）+ **P2-b backlog ×2**（wizard/collapse design.md §7/§8 推荐句柄未实现——「推荐支持」非承诺契约，实现需 capability 注册面，归 CR）+ **P3 keep ×9**（wizard 恒等三元/焦点管理/双击竞态/G3 过时/重复 key、collapse startTransition 无害/collapsible 语义、grid 空态/testid 透传）。
- 重点维度：dim 3（collapse 三态全路径 + **P1-1 scope 无 path 静默失效**；wizard stepIndex 交互态 vs commit 生命周期态分离——两个独立 useState + closure-gate 测试 grep 锁定）；dim 5（布局 renderer 仅 marker 类——grid/collapse/wizard 根仅 `nop-*` + meta.className，样式来自 schema）；dim 6（**08-02 机制复验**：wizard steps fieldRules title value-or-region/body+actions region/disabled literal/beforeEnter+beforeLeave event（`__nopPreserveLiteral` 信封 → unwrapPreservedLiteral 解包 + live scope 求值测试实证）、collapse items title value-or-region/body region/disabled literal、grid items body region、无 deepFields 残留、params/isolate 与声明一致）；dim 7（payload 形状 ↔ eventContracts 逐字段一致）；dim 16（假绿核查：wizard formId 校验路径单测盲区（harness 无 form renderer）→ Phase 3 真机补上；collapse disabled 零断言；grid 无 not-throw 空断言）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/hardcoded-type-dispatch/non-retained/performance-suspects 本族 0 命中；react19-optimization-candidates 命中为合法设计（wizard useMemo/callback 族，C3.x 同裁定）；async-failure-paths 命中 wizard-renderer.tsx:559（导航点击 `void goToStep` 无兜底）→ P2-1。

### Phase 2 — P0/P1/P2 自动修复（test-first，flux-renderers-layout 73 → 81 tests，+8）

- **[P1-1 wizard actionNextSaveLabel 死契约]**：schemas.ts:59-60 + definitions fields:166 移除（AMIS「下一步并保存」在 Flux 模型中被 commitStep 合并承载，无独立行为面）；design.md §4 字段表删除 + §10.2 移除留痕（沿用 optional/valueOwnership 先例）；flux-guide schema.d.ts regenerate（自动生成源验证）。test-first：`__tests__/wizard-c5-1-contract.test.tsx`「does NOT advertise dead actionNextSaveLabel」先红后绿。
- **[P1-2 wizard 索引基文档漂移]**：schemas.ts JSDoc + definitions 描述「1-based」→「0-based」（实现 0-based 直接索引，测试 :337 `defaultValue:1` → index 1 实证）。test-first：wizard-c5-1-contract「treats numeric value as a 0-based step index seed…」锁定 0-based + **种子非响应**（rerender 换 data 步进不动）。
- **[P1-3 wizard visible:false 语义契约]**：三处修复——(a) computeCanGoTo 线性门改为「furthestReached 与 target 之间无 visible 步」放行（hidden 中间步不再阻塞 linear 前进）；(b) stepNav 过滤 hidden 步（不再渲染禁用 nav 项）；(c) isLastStep 按最后 visible 步判定（hidden 末步不再导致 wizard 不可完成）。test-first：wizard-c5-1-contract「skips hidden steps…」先红后绿（hidden nav 项存在 + Next 停 A + onComplete 不触发 → 修复全绿）。
- **[P1-1 collapse scope-degrade]**：移植 steps-renderer 同型降级模式（`valueOwnership='scope'` 无 `valueStatePath` → 降级 local + `console.warn` + warnedRef，消除点击静默失效）。test-first：collapse-renderer.test.tsx「degrades scope ownership…」先红后绿（修复前点击无 data-open 且无 warn）。
- **P2 低成本修复 ×8**：wizard P2-1（runStepTransitionGuards try/catch 抛错中止导航——throwing namespace 用例先红后绿，消灭 unhandled rejection）；P2-2（lastCommitStatus error/validationError 渲染内联 `wizard-step-error`（role=alert）+ `flux.wizard.validationFailed/commitFailed` 双 locale key + commitStep 错误串 i18n 化）；P2-4/P2-6（种子契约测试 + onStepCommit {ok:false} 错误态用例）；collapse P2-1（disabled 项用例——**测试揭示 ui CollapsibleTrigger 的 disabled 映射契约为 Base UI aria-disabled（非原生 disabled 属性），断言按实际 DOM 契约锁定**）；grid P2-2（gap 对齐 flex/container/form 的 `resolveGap` 语义：number→px、xs/sm/md/lg/xl→Tailwind 类、其他字符串→raw CSS——layout-selection-guide `gap:"md"` 示例恢复有效 + design.md §4 gap 语义同步；test-first：gap-token 用例先红后绿）；grid/collapse/wizard P2-5（lab 页，Phase 3 落地）。
- 共性裁决：全部发现为**组件单点根因**（wizard 内部语义/死字段/文档漂移、collapse 单点移植、grid 单点 gap 解析；i18n 增 key 为纯增量）→ **无 CX-n 插入**（决策记录于各卡 + 本 log）。
- 无复杂跨包 bug（i18n key 增量非行为修复），无需 docs/bugs/ 新记录。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c5-1-host-surfaces.spec.ts` 5/5 + 回归 85 passed + smoke 58/58）

- lab 页新增 3 个（补上维度 18 缺口——P2 低成本当场补页裁决）：`grid-lab-page.tsx`/`collapse-lab-page.tsx`/`wizard-lab-page.tsx` + `data-c5c1-host.ts` 宿主 schema 模块 + `LAYOUT_RENDERER_ROUTES`（grid/collapse/wizard 3 条，C5.2 successor 补 button-group/dropdown-button/steps/timeline 4 条）+ `COMPONENT_LAB_COVERAGE_MANIFEST` 3 条（smoke.spec.ts 自动覆盖 → **58/58 含新条目**）+ route-matrix 计数测试同步 + `MultiScenarioLabPage` 增 `onActionScopeChange` 透传（gate probe 需要）。lab-renderer 路由不经 `DOMAIN_RENDERER_ROUTES`（playground-entry-pages.spec.ts ROUTE_ASSERTIONS 仅覆盖 domain 路由；lab 路由由 smoke.spec 门禁）——**核对结论：无需 ROUTE_ASSERTIONS 更新**。
- 5 场景全绿：
  - **host-grid**：外层 grid `data-columns=2`（桌面 lg 桶）无 data-responsive + classList 仅 `nop-grid`/`gap-4`/meta（marker-only 契约真机核验）+ `repeat(2` + 嵌套 grid 4 items + colSpan=2 clamp → **pass**
  - **host-collapse**：local 点击展开；controlled 外部 scope 按钮驱动 echo（点击不突变）+ host 更新翻转；scope 点击 → valueStatePath 写回 scope 报告 → **pass**
  - **host-wizard-step（内嵌表单步进校验，单测盲区真机补上）**：step.formId 内嵌 form 空值 → Next → `validationError` + `wizard-step-error` 展示 + 不前进；填值 → 前进；末步 → onComplete → **pass**
  - **host-wizard-gate**：beforeEnter probe 返回 {ok:false} → 步进被拒 + beforeLeave of A 已报告 → **pass**
  - **host-wizard-dialog（bug 73 模式专项）**：openDialog 内嵌 wizard 步进 ×2 + 末步提交 + onComplete host probe=1 → **pass**
- 宿主阶段适配记录（非渲染器缺陷，用例作者修正）：(a) dialog 内容渲染于**页级 portal**（`data-slot="dialog-surface"`，不在 scenario stage 内）→ 断言按 portal 定位；(b) dialog 内容 scope 写为**本地分支语义**（openDialog 建 child scope（runtime-factory.ts:598-619，isolate:false 读父写本地），setValue 写不进页级 scope——scope 模型设计行为）→ onComplete 信号改用 host window probe（dialog-edit-submit.spec.ts 同款跨 scope 模式）→ 结论记录。
- 回归：m3-layout 7/7 + w3a-w3b-layout-action-family 5/5 + layout-family-enhancements 9/9 + w2a-data-composition 3/3（wizard 交互含 statusPath 双层 + linear 门控 + onComplete）+ c5-1-host-surfaces 5/5 + smoke 58/58（含新增 grid/collapse/wizard 条目）**85+58 全绿**。

### Phase 4 — 全卡复查 + 收口前置

- 审计卡 3 张全部 `closed`：P0 ×0、P1 ×4 fixed、P2 ×8 fixed + P2-b backlog ×2（显式登记归 CR）、P3 ×9 keep（记录）；卡状态流转 open → fixing → fixed-pending-closure（宿主实证 5/5）→ closed（全卡复查）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-layout typecheck && build && lint && test` 全绿（**81 tests，+8**：wizard ×5 + collapse ×2 + grid ×1）；workspace `pnpm typecheck` 31/31、`build` 31/31、`lint` 31/31、`test` **58/58（--force 实测全绿）**；`pnpm test:e2e` 全量 **874 passed / 43 skipped / 8 failed**——8 failed 全部为 C0 基线 pre-existing 裁定项（ai-chat ×1 + ai-rich-text-sender ×5 + calendar-demo ×1 + diff-perf ×1，successor C8.1/C9/CV），**本 plan 引入回归面零新增失败**（c5-1-host-surfaces 5/5 + 本族回归全绿 + smoke 58/58）。
- **08-02 机制复验结论**：dim 6 全 pass——wizard steps/collapse items/grid items fieldRules 分类与 08-02 声明一致（value-or-region/region/literal/event 语义 + params/isolate）；beforeEnter/beforeLeave 信封模板保持 + dispatch 时 live scope 求值（测试实证）；无 deepFields 残留。
- 共性缺陷处理（roadmap §7 裁决）：**无 CX-n 插入**（全部发现为组件单点根因）。
- 观察（供 CLOSURE_VERIFY 核对）：`pnpm check:oversized-code-files` 为 **pre-existing 红**（HEAD 基线 14 文件超 700 行，本 plan 使 wizard-renderer.tsx 734→774 进入清单；plan gates 为 typecheck/build/lint/test 均绿；超限文件治理归 CG/CR，非本 plan scope）。
- closure-audit 证据位置：本 plan Closure 节 + 3 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C5.1 行标 done 的前置即该 audit pass。

## C4.3 data 其余 6 组件逐组件审计执行：plan-2026-08-04-0043-2 四 Phase 全绿（6 卡 closed + P1×3/P2×8 修复 + 宿主 5/5 含 bug 73 专项 + 全量验证绿 + closure 前置）

执行 `docs/plans/2026-08-04-0043-2-c4-3-data-rest-family-audit.md`（tree/chart/list/pagination/statistics/data-source 6 组件 × 18 维逐组件审计 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**data 族 C4.3 执行完成（roadmap C4.3 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.x/C4.1/C4.2 先例一致；执行 session 不自审、不标 done）**。

### Phase 1 — 6 张审计卡（`docs/audits/per-component/{tree,chart,list,pagination,statistics,data-source}.md`）

- 18 维逐项核对 + 注册定义三方核对（data-renderer-definitions.ts data-source `:272`/chart `:345`/tree `:379`/list `:408` + w2a-data-composition-definitions.ts pagination `:13`/statistics `:108` ↔ schemas.ts ↔ 渲染器消费）。发现汇总：**P0 ×0、P1 ×3**（statistics P1-1 phantom lab 页——`source`/`statistics` 幽灵 props 恒渲染 Total 0；statistics P1-2 缺 `nop-statistics` 根 marker；data-source P1-1 操作控制层 `control`/`retry` 缺注册——design §4 + compiler 消费 + runtime 消费 vs fields 清单）、**P2 ×8**（tree label/title 缺类型 + multiple 缺注册；chart componentId 缺注册 + onClick/onHover eventContracts 未声明且 payload 零断言；list onLoadMore/onPageChange payload 零断言；pagination statusPath 零测试；statistics 渲染器零测试）+ **P2 backlog ×2**（list P2-3 infinite 无显式 total 时 hasMore 恒 true——design §9 两条款张力，需跨组件分页语义裁决归 CR；statistics P2-4 缺 design.md + amis-baseline-matrix 零提及，归 CR/CG）+ **P3 keep ×6**（tree aria-selected 活动节点语义/aria-label fallback 硬编码、chart sr-only 前缀硬编码/component:resize 语义、statistics i18n key 复用、data-source dev throw、pagination design §8 推荐句柄未实现）。
- 重点维度：dim 3（pagination total 渲染期跟随 H2 ✓/tree 展开 local + 搜索受控覆盖 ✓/list 三态 ownership ✓）；dim 6（**08-02 机制复验**：data 包 tree 无 childrenSource/searchSource——08-02 分类的该类字段属 form-advanced input-tree/tree-select（nested-schema-field-classification.md:289，C3.5 已闭），plan baseline 归属偏差卡内裁定留痕；data-source 请求层 action/args/onSuccess/onError 分类正确 + 无 deepFields 残留）；dim 7（pagination payload 形状 ↔ eventContracts 逐字段一致；chart 事件首参为 DOM event——与 button 族模式一致）；dim 11（data-source abort/竞态/失败/重试全链路 + **INV-1 合规：请求经 env.fetcher（request-runtime.ts:422/558）**）。
- 工具脚本：missing-renderer-markers/styling-suspects/runtime-raw-schema-reads/async-failure-paths/hardcoded-type-dispatch/non-retained/performance-suspects 本族 0 命中；react19-optimization-candidates 命中为合法设计（chart useCallback 族 + tree useMemo，C3.x 同裁定）；test-global-leaks 命中为测试环境 mock（合规）。

### Phase 2 — P0/P1/P2 自动修复（test-first，flux-renderers-data 712 → 720 tests）

- **[P1-1 statistics phantom lab 页]**：statistics-lab-page.tsx 幽灵 schema（`source: '${records}'` + `statistics: [{label,field,aggregation}]`——StatisticsSchema 无此二字段、渲染器零消费）→ lab 页改为 `total: '${totalCount}'` 对齐真实契约。test-first：statistics-rendering.test.tsx ×4 先红后绿（修复前 `.nop-statistics` 查询 null）。
- **[P1-2 statistics 根 marker]**：statistics-renderer.tsx 根补 `nop-statistics` marker + `data-total` 状态属性（对齐 pagination data-total 模式；Root Marker Rules + 本族 6 组件先例）。同测试锁定。
- **[P1-1 data-source 操作控制层缺注册]**：definition fields 补 `control`/`retry`（design.md §4 文档化 + source-compiler.ts:16-42 编译消费 + source-registry.ts:162-170 运行时消费；类型面核对结论——`control`/`retry` 已由共享 ActionShapeLikeFields（schema.ts:77-98）继承覆盖，**无需改 flux-core**（审计初期判断已修正））。test-first：data-renderer-definition-contracts.test.ts「operation control layer」先红后绿。
- **P2 低成本修复 ×8**：tree（TreeSchema 补 label/title 类型 + fields 补 multiple 注册——契约测试先红后绿）；chart（fields 补 componentId + eventContracts onClick/onHover（payload `{kind:'unknown'}` 文档化 DOM event）+ payload 派发参数断言锁定）；list（onLoadMore `{type,currentPage,pageSize,total}` / onPageChange `{type,currentPage,pageSize,totalPages,total}` payload 字段断言——经 evaluationBindings 流入 action args 实证）；pagination（statusPath summary 形状 `{kind:'pagination',...}` + 翻页更新 + 卸载清理 2 用例）；statistics（渲染器测试 ×4：marker/data-total/undefined→0 回退/reactive total/meta）。
- 共性裁决：全部发现为**组件单点根因**（无 ≥2 组件/跨包公共根因）→ **无 CX-n 插入**（决策记录于各卡 + 本 log）。P2 backlog ×2 显式登记卡内（list P2-3 / statistics P2-4）归 CR。
- 无复杂跨包 bug（flux-core 类型探索撤销后本 plan 未触及公共层），无需 docs/bugs/ 新记录。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c4-3-host-surfaces.spec.ts` 5/5 + 回归 99 passed + smoke 55/55）

- lab 页新增 5 场景（`data-c4c3-host.ts` 抽模块控制行数 + data-source/tree/pagination/chart 四个 lab 页注册；**无新 lab 页路由**——复用既有 6 lab 页故无 ROUTE_ASSERTIONS 更新；pagination/statistics 补入 `COMPONENT_LAB_COVERAGE_MANIFEST`（此前缺失）→ smoke.spec.ts 自动覆盖）：
  - **host-ds-list**：data-source 远程加载（env IO 边界 fetcher）→ scope `users` → list 渲染 + statistics `data-total` 回显 + `component:refresh` 再拉新批次（probe 计数 +1）→ **pass**
  - **host-ds-fail（bug 73 模式专项）**：mock fetcher 失败 → statusPath `hasError` 真机可见 + initialData 行保留（失败不抹旧数据）+ 有界重试循环 → `state:ok` + `FlakyRecovered` 恢复 → **pass**
  - **host-tree-search**：搜索 'Backend' → 匹配节点可见 + 祖先自动展开 + `tree-search-highlight` 高亮 + 非匹配隐藏；清空恢复原展开快照 → **pass**
  - **host-pagination**：独立 pagination 页码点击 → statusPath 传播 → controlled list 切片切换（`data-current-page` + Record 文本断言）→ **pass**
  - **host-chart**：scope 数据流驱动 + in-place 数据交换（chart-data-equivalent 摘要文本断言）+ 清空 → `chart-empty` 显式空态 → **pass**
- 环境发现：dev 服务器跑 React.StrictMode → data-source 双挂载消耗 fetcher 计数/失败数——宿主断言改为模式断言（regex/增量/有界循环）而非固定批次号；`${pagerState.currentPage}` 首帧求值失败（intermediate member 无 `?.` 保护）→ lab schema 改 `?.`。**均非渲染器缺陷**（StrictMode supersede 机制为设计行为），无新修复。
- 回归：tree-display-ux / w1c-list×2 / w2a-data-composition / m4-data / component-lab data-renderers / crud-demo / smoke **99 passed**；c4-3-host-surfaces 5/5；smoke 55/55（含新增 pagination/statistics 条目）。

### Phase 4 — 全卡复查 + 收口前置

- 审计卡 6 张全部 `closed`：P0 ×0、P1 ×3 fixed、P2 ×8 fixed + ×2 backlog（显式登记）、P3 ×6 keep（记录）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-data typecheck && build && lint && test` 全绿（**720 tests，+8**：statistics ×4 + data-source control 契约 ×1 + tree multiple ×1 + list payload ×2 + pagination statusPath ×2，chart payload 锁 1 处为既有用例强化）；workspace `pnpm typecheck` 31/31、`build` 31/31、`lint` 31/31、`test` **58/58（--force 实测全绿）**；本 plan 未触及公共层（flux-core 无净改动），无额外受影响包验证。
- **08-02 机制复验结论**：dim 6 全 pass——data 包 tree 无 childrenSource/searchSource（归属 form-advanced，C3.5 已闭，plan baseline 归属偏差已裁定）；data-source 请求层分类正确（args 纯 value / action 编译通道 / onSuccess/onError lifecycle action 走 compiled artifact——definition :297-304 诚实契约）+ 无 deepFields 残留；行 scope 不污染（controller 用 owner scope）。
- 共性缺陷处理（roadmap §7 裁决）：**无 CX-n 插入**（全部发现为组件单点根因，无 ≥2 组件/跨包公共层根因）。
- closure-audit 证据位置：本 plan Closure 节 + 6 审计卡 Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C4.3 行标 done 的前置即该 audit pass。
- **观察（供 CLOSURE_VERIFY 核对）**：roadmap C4.2 行当前仍为 `todo`（其父 plan `2026-08-04-0043-1` 已 completed 且 Closure 节声明机械收口已做——roadmap 表未同步，疑似上轮收口遗漏；建议 audit 时一并裁定）。

### VERIFY 复跑（mission-driver VERIFY，2026-08-04）

- `pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58（`--force` 实测，非缓存）；本族相关 e2e：c4-3-host-surfaces **5/5** + 回归 99 passed（tree-display-ux/w1c-list×2/w2a-data-composition/m4-data/data-renderers/crud-demo/smoke）+ smoke **55/55**（含新增 pagination/statistics 条目）。
- `pnpm test:e2e` 全量 **855 passed / 43 skipped / 9 failed / 7 did not run**——9 failed 全部为 C0 基线 pre-existing 裁定项（ai-chat ×1 + ai-rich-text-sender ×5 + calendar-demo ×1 + diff-perf ×1 + gantt-bars-and-links:132，successor C8.1/C9/CV），**本 plan 引入回归面零新增失败**。

### CLOSURE_VERIFY closure-audit pass（mission-driver CLOSURE_VERIFY fresh session，2026-08-04）

- C4.3 plan `2026-08-04-0043-2` 独立 closure-audit **pass**（fresh session 独立复跑，非执行缓存）：（1）4 Phase 全 `completed`、Exit Criteria 全 `[x]`、无残留 `- [ ]`；（2）live repo 落地核验——6 审计卡全 `closed`（P0×0/P1×3 fixed/P2×8 fixed+×2 backlog/P3×6 keep）、statistics-renderer.tsx:11 `nop-statistics` marker + data-total、data-renderer-definitions.ts:342-343 data-source control/retry + :389 chart componentId + :415 tree multiple、statistics-lab-page.tsx total prop 对齐、c4-3-host-surfaces.spec.ts 5 场景（含 bug 73 专项）programmatic DOM 断言；（3）anti-hollow：宿主断言真实行为（list-item 文本/data-total/probe 增量/state 恢复/chart-data-equivalent），无空壳；（4）复跑验证：`pnpm --filter @nop-chaos/flux-renderers-data test` **720/720**、c4-3-host-surfaces **5/5 真机 pass**、workspace typecheck/build/lint **31/31**、`pnpm test --force` **58/58** 全绿；（5）deferred 诚实（P2 backlog/机制依赖/e2e pre-existing 均 adjudicated + successor）；（6）机械收口：plan `Plan Status → completed`、Closure Gates 审计门禁项勾选、roadmap **C4.3 行标 `done`**。
- 观察裁定（C4.2 收口遗漏）：roadmap C4.2 行与 CX-9 行上轮未同步（C4.2 plan 已 completed 且其 Closure 节已记录 closure-audit pass）——判定为机械收口而非新审计，一并补齐标 `done`（roadmap 已同步，无遗留）。

## C4.2 crud 单组件逐组件审计执行：plan-2026-08-04-0043-1 四 Phase 全绿（18 维 + deep-audit 21-23 + P1×3/P2×2 自动修复 + 宿主场景 5/5 + 卡 closed + closure 前置）

执行 `docs/plans/2026-08-04-0043-1-c4-2-crud-audit.md`（crud 单组件 18 维逐组件审计 + deep-audit 21-23 + test-first 自动修复 + 真实浏览器组合宿主场景 + closure 前置）。**data 族 C4.2 执行完成（roadmap C4.2 行标 done 待独立 closure-audit pass 后收口，与 C2.x/C3.x/C4.1 先例一致；执行 session 不自审、不标 done）**。

### Phase 1 — 审计卡（`docs/audits/per-component/crud.md`，18 维 + 21-23 维）

- 18 维逐项核对，`文件:行` 证据；发现汇总：**P0 ×0、P1 ×3（P1-1 loadAction includeScope 契约漂移——'*' 嵌套包装 + string[] 静默失效；P1-2 dependsOn 反应式触发死路径——force 结果被反应注册表吞掉，fetch 发出但 rows 永不更新（bug 73 模式）；P1-3 phantom 声明簇——totalField/autoJumpToTopOnPagerChange JSDoc 行为主张零消费 + *Ownership 四件套 propContracts 宣称三态 vs scope-owned 组合 + syncLocation/autoGenerateQueryForm 声明未实现）、P2 ×2（loading 不透传内部 table——loadAction 加载态无 UI；crud-query-region 死代码）、P3 ×3（$crud 不进 loadAction args/stopWhen 上游消费/死代码残留类型，均文档显式 keep）**。
- 重点维度：dim 3（四态 ownership 分层 `$_crud.<id>.<slice>` + `query.*` 无包装 + T24 命名空间隔离）；dim 6（08-02 机制复验：quickSaveAction/quickSaveItemAction actionValue:true ✓ + columns fieldRules region 提取 ✓ + 无 deepFields ✓）；dim 7（loadAction 协议 evaluationBindings + `__autoPagination` + **includeScope 契约实证缺陷**）；dim 11（abort 链/竞态/失败保留/轮询/无限滚动 + **dependsOn 反应式结果被吞**）；dim 12（宿主场景 5 个 failure path）；dim 16/23（23 个既有 crud 测试断言真实行为；假绿核查发现 P1-2——reaction-regression 测试只断言 bindings 注入与 fetch 次数，从不断言反应式触发后渲染）。
- 工具脚本：`check:audit-suspects`/`styling-suspects`/`runtime-raw-schema-reads`/`fieldframe-bypasses`/`hardcoded-type-dispatch`/`missing-renderer-markers` 0 命中；`react19-optimization-candidates` 命中为既有设计（C3.x 同裁定）。

### Phase 2 — P0/P1/P2 自动修复（test-first，flux-renderers-data 705 → 712 tests；flux-runtime 1396 / flux-react 458 / flux-core 507 全绿）

- **[P1-1 includeScope 契约]**：CRUD 建 per-instance CRUD scope 投影 child scope（`helpers.createScope`，own 数据 = pagination/query/sort/filters/selection，bindings provider 每次 dispatch 前 `replace()` 刷新）；`renderer-reaction-handle.ts` 新增 `__setScopeOverride`（dispatch 漏斗 scope 覆盖）+ `ReactionHandle.dispatch` ctx 类型补可选 `scope`（flux-core，兼容增量）；`reaction-handle-proxy.ts` 转发。`'*'` 扁平暴露 5 变量、`string[]` 精确提取、无 `$_crud` 包装、无 `__crudLoadRevision` 泄漏。test-first：crud-loadaction-includescope.test.tsx 2 用例先红后绿（修复前实测 `{$_crud:{...}}` 与 `undefined`）。
- **[P1-2 dependsOn 反应式死路径]**：`renderer-reaction-handle.ts` 新增 `__setLoadCallbacks({onStart,onSettle})`——effect 路径与 force 路径共用结果 sink（rows/total/error/loading/server-page 校正/totalField 统一处理），force 结果不再被反应注册表吞掉；新增 `__setIgnoreWritesTo`（CRUD 声明自持写路径 `$_crud.<id>`/`__crudLoadRevision`/statusPath/dataStatePath）——修复后若不忽略自写会形成 fetch→setRows→revision bump→force→fetch **无限循环（实测 heap OOM）**，同时消除了无 dependsOn 时 CRUD 自写触发的冗余 fetch。test-first：crud-loadaction-depends-on-reactive.test.tsx 先红后绿（scope-writer 400ms 后写 `deptId='d7'` → RowsFord7 渲染 + args 携带 d7）。
- **[P1-3 phantom 声明簇]**：`totalField` 实现（useCrudLoadAction onSettle 消费自定义总数字段名）；`autoJumpToTopOnPagerChange` 实现（prevPageRef + 翻页 scrollIntoView，首屏不滚）；`*Ownership` 四件套契约对齐（定义 propContracts 描述 + schema JSDoc 明示 scope-owned 组合语义——design.md §4.1.2 本就文档化「scope-owned，透传 table runtime」，local/controlled 为兼容面）；`syncLocation`/`autoGenerateQueryForm`/`migrationHints` JSDoc reserved 标注。test-first：crud-c4-2-schema-contract.test.tsx 4 用例先红后绿（totalField footer 回显/autoJumpToTop/ownership local 组合语义/loading overlay）。
- **[P2-1 加载态]**：`loading: useLoadAction && paginationMode === 'pages' ? loadResult.loading : undefined` 透传内部 table（TableLoadingOverlay）；infinite 模式不叠加 overlay（infinite-status 区表达）。
- **[P2-2 死代码]**：crud-query-region.tsx 移除 CrudQueryRegion/QueryControls/countActiveFilters/`__test__countActiveFilters`（无任何导入方，仅 resolvePaginationMode/isAtLastPage 被消费）。
- 共性裁决：P1-1/P1-2 根因在公共层（renderer-reaction-handle + request 提取面）——按 roadmap §7b 当前 plan 内优先修复，**事后回写 CX-n 待收口**（见「共性缺陷处理」节）。
- bug 79 记录：`docs/bugs/79-crud-loadaction-includescope-and-reactive-dead-path-fix.md`（AGENTS.md Bug Fix Test Coverage Rule）。

### Phase 3 — 组合宿主真实浏览器场景（`tests/e2e/component-lab/c4-2-host-surfaces.spec.ts` 5/5 + 回归 32 passed/1 skipped）

- lab 页新增 5 场景（`crud-lab-page-c4c2.ts` 抽模块控制行数 + crud-lab-page.tsx 注册）：
  - **host-crud-row（bug 73 模式专项——08-02 行 scope 污染修复复验）**：CRUD 行内编辑 `Alpha`→`Alpha-edited` → 行保存条 → quickSaveItemAction probe fetcher 收到 `{name: 'Alpha-edited', id: 1}`（**编辑值，非旧行值**）→ **pass**（08-02 机制真机复验结论：pass）
  - host-crud-load：queryForm 输入 `report` → 搜索 → `Found-report` 渲染 + footer `Query: report` + 记录 fetch `data.keyword === 'report'`（查询参数进 CRUD scope 并随 loadAction payload 发出）→ **pass**
  - host-crud-include：`includeScope: '*'` 请求 data 扁平 5 变量、无 `$_crud` 包装、无 `__crudLoadRevision` 泄漏 → **pass**
  - host-crud-fail：mock fetcher 前 2 次 500 → 页面不崩无数据 → toolbar refresh ×2 → `RecoveredRow`（失败保留旧数据 + 重试可用）→ **pass**
  - host-crud-paging：页 1 选择 → 跨页 keepOnPageChange → 页 2 `Item 11` + `Sel: 1` → 回页 1 → 表头排序 → `Sort: name` 回显 → **pass**
- 回归：crud-demo 6/6、component-lab crud 系 13 全绿、dialog-dropdown-row-edit 1/1、c4-1-host-surfaces 3/3、crud-table-body-diag 1skip、performance-table 全文件绿（32 passed/1 skipped 总）。

### Phase 4 — 全卡复查 + 收口前置

- 审计卡 `docs/audits/per-component/crud.md`：P0/P1/P2 清零、P3 ×3 keep（文档显式）、状态 `closed`（open → fixing → fixed-pending-closure → closed）。
- 验证：`pnpm --filter @nop-chaos/flux-renderers-data typecheck && build && lint && test` 712 全绿；workspace `pnpm typecheck` 31/31、`build` 31/31、`lint` 31/31、`test` 58/58 全绿；`pnpm test:e2e` 全量 **844 passed / 43 skipped / 10 failed / 8 did not run**——失败逐项核对：8 项为 C0 基线 pre-existing（ai-chat ×1 + ai-rich-text-sender ×5 + calendar-demo ×1 + diff-perf ×1，successor C8.1/C9/CV）+ **gantt-bars-and-links:132（HEAD-baseline 复测同样失败——scheduling 包拖拽时序机器相关，非本 plan 引入，successor C9）** + **performance-table:349（全量跑偶发失败，隔离重跑 3/3 绿——性能诊断阈值机器相关，同 diff-perf 类，successor CV）**；本 plan 引入的 crud/table 系回归 32 passed/1 skipped 稳定。
- 共性缺陷处理（roadmap §7b 事后回写）：P1-1/P1-2 根因在 `renderer-reaction-handle.ts`（flux-runtime 公共层）+ `reaction-handle-proxy.ts`（flux-react）——`__setScopeOverride`/`__setLoadCallbacks`/`__setIgnoreWritesTo` 扩展点为 renderer-owned reaction 通用能力，影响 ≥2 组件面（任何 kind:'reaction' 字段组件：crud loadAction、content toggleViewType/setViewType 等）；决策：**当前 plan 内修复 + 事后回写 CX-n**（CX-9「共性重构：renderer-owned reaction 结果捕获与 scope 投影通道」待父 plan closure-audit 后按 §7b 标 done）。
- closure-audit 证据位置：`docs/plans/2026-08-04-0043-1-c4-2-crud-audit.md` Closure 节（mission-driver CLOSURE_VERIFY fresh session 执行；执行 session 不自审）；roadmap C4.2 行标 done 的前置即该 audit pass。

### VERIFY 复跑（mission-driver VERIFY，2026-08-04）

- `pnpm typecheck` 31/31、`pnpm build` 31/31、`pnpm lint` 31/31、`pnpm test` 58/58 全绿；crud 系相关 e2e 30/30 绿（c4-2-host-surfaces 5/5 + c4-1 3/3 + crud-demo 6/6 + component-lab crud 系 13 + dialog-dropdown-row-edit 1 + crud-list-mode 2）。
- `pnpm test:e2e` 全量 **846 passed / 43 skipped / 9 failed / 7 did not run**——9 failed 全部为 C0 基线 pre-existing 裁定项（ai-chat ×1 + ai-rich-text-sender ×5 + calendar-demo ×1 + gantt-bars-and-links:132 + diff-perf ×1，successor C8.1/C9/CV）；performance-table:349 本轮 pass（上轮 flaky 复测 3/3 绿一致）。本 plan 引入回归面稳定无引入缺陷。

---

<!-- feat-industrial-hmi branch entries (merged) -->

## industrial-hmi plan-2026-08-04-1558-3 执行：display geometry & test effectiveness 三 Phase 全绿（显示几何修正 + e2e 有效性 + 覆盖缺口闭合）+ workspace 全量验证

执行 `docs/plans/2026-08-04-1558-3-hmi-display-geometry-test-effectiveness-plan.md`（P2 backlog：Display & positioning / Test effectiveness & coverage / open-audit e2e 条目），三 Phase 全部完成（Phase 1 由前序 session 收口，本日完成 Phase 2/3）：

- **Phase 1 显示几何修正**：`use-scada-config-sync.ts` `computeSymbolBounds` 对含 `custom.points` 节点（polygon/line/arrow）从 points 数组计算 min/max 包围盒，无 width/height 但有 points 时不再退化为 0 尺寸（fit/center 不再冲 MAX_SCALE/钉画布外）；`dirty-collector.ts` `collectStates` 经 reverse-index 取 primary binding 转发 `resolveState(declaration, raw, { scale })`，按 F4 语义（判定作用于 point-store 存储值，声明级 scale 已在 `convert` 施加）只在声明级无 scale 时生效，避免双重换算；`text.ts` + instrument 数值标签（gauge/level/thermometer）以显式 width 为居中主路径（leafer-ui@2.2.9 自动宽下 autoSizeAlign 符号存在但无 layoutWidth 使 textAlign 偏移不生效，已核实 installed dist）；`scada-canvas.tsx` canvas slot 落点移到真实 canvas DOM 元素（`app.canvas.view` 面），wrapper div 保留 marker class，`design-renderer.md §10` 表同步（canvas 行 marker/slot 一致 + 永不渲染的 overlay 行注记移除）。Proof 前置 4 组（polygon bounds 精确断言 + scale 判定一致性边界断言 + Text 居中 attrs 断言 + slot DOM 断言）。
- **Phase 2 e2e 有效性**：`scada-perf.spec.ts` drag pan 断言改为读 `tree.zoomLayer.x/y` 视口变化证据（兜底直接读 zoomLayer，不依赖引擎 sync 送达性，TE-1 非恒真——未平移场景先红、真平移通过）；各 spec 家族（demo/edge/perf/pressure）补 canvas 存在性硬门禁（`helpers/scada-canvas-assert.ts`：`<canvas>` tag + 非零尺寸 + `forceRender` 帧计数 > 0 + best-effort 像素探测，SecurityError/全零像素 fallback 仅 log 不失败，F2）；移除 `scada-perf.spec.ts` 5 处 `allowConsoleErrors(100)` + `playground-entry-pages.spec.ts` `ROUTES_WITH_KNOWN_ERRORS` 的 `scada-perf-scale` 条目（open-audit live probe 证 0 console.error，m3-r2 钉死仅移除该单条）。Proof：scada e2e 家族全绿 + scada-perf-scale 路由无 console.error/pageerror 失败。
- **Phase 3 覆盖缺口闭合**：`batch-add-probe.test.ts`（断 count + 返回 shape + 双 Group.add 路径 instanceof，timing>0 断言保留在既有 e2e 真实计时处，ratio 维持观察项 F6）；`scada-canvas-lifecycle-hardening.test.tsx`（onBuildError('config-build-failed') 经 applyDiff-throw 触发 + setPointValues 销毁后 no-op，注入限于 mount 路径，stale-closure 路由 plan `{2}` F5）；`scada-engine.test.ts`（cacheImage/resolveImageUrl/exportConfig-no-config/measureAddStrategies 投影）；`serialization.test.ts`（validate 9 错误分支）；`expression-evaluator.test.ts`（错误分支矩阵）；`sensor-control-symbols.test.ts`（switch true→false OFF 位 applyProps）；`scada-canvas-smoke.test.tsx` 补 `resetLeaferMock()`（唯一漏调消费者文件对齐）。`leafer-ui-mock.ts` 补 measureText 支持自动宽 Text 居中测量。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **562 tests / 38 files** 全绿（coverage 97.29 stmts / 91.89 branches，≥90 阈值达标；`batch-add-probe.ts` 0%→100%、`switch.ts` branch 87.5→100%、`validate.ts` 91.5→99、`expression-evaluator.ts` 93.19→98.95、`scada-engine.ts` 97.53→99.38）；workspace 全量 **typecheck 32/32、build 32/32、lint 32/32、test 59/59** 全绿（verify 复跑 FULL TURBO cache 命中）；**scada-\* e2e 25/25 全绿**（demo/edge/perf/pressure）+ `playground-entry-pages.spec.ts -g scada-perf-scale` 1/1 全绿。残余 uncoverage（`validate.ts:91-92`/`expression-evaluator.ts:288,303`/`use-scada-handles.ts:118`）均为防御性死守卫，非 live defect。
- **plan 收口**：closure-audit 由独立 fresh-session 子 agent 执行判定 `APPROVED`（三 Phase diff 摘要逐项 spot-check 非 trust + 包级 562/562 + coverage 91.89 branches 复跑 + typecheck 清洁）；Phase 1–3 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`。唯一 deferred 项「leafer 自动宽 Text 居中浏览器级像素验证」分类 `watch-only residual`、`Successor Required: no`（mock↔真实漂移候选面，非 in-scope live defect）。roadmap Follow-up Backlog（Display & positioning / Test effectiveness & coverage / open-audit e2e 条目）收口注记齐备。

## industrial-hmi plan-2026-08-04-1558-2 执行：lifecycle & degradation hardening 四 Phase 全绿（生命周期清理 + 事件/动画健壮性 + flux 桥数据面 + 错误面/命令契约）+ workspace 全量验证

执行 `docs/plans/2026-08-04-1558-2-hmi-lifecycle-degradation-hardening-plan.md`（P2 backlog：State & lifecycle / Wiring & degradation 分组 + open-audit 关联 P2 项 + doc drift），四 Phase 全部完成：

- **Phase 1 生命周期清理与基线完整性**（已完成在前序 session，本日完成 final verify）：`use-scada-engine.ts` observer/rafId 提为 ref，mount cleanup 与 `destroy` 共用 `releaseRuntime`（SL-2：destroy 后容器不再被观察）；绑定域（pipeline/animator/collector）最新实例存 ref（M1：reload 后 unmount 不泄漏最新 animator 时钟）；`setPointValues` 注入闭包经 `runtimeRef.current` 取最新 pipeline（SL-3）；`use-scada-config-sync.ts` catch 内 `prevRef.current = undefined`（SL-4：构建失败强制下次 full）；`reloadBindings` 经 `PointStore.snapshotValues` + `restoreValues` 保留 props full/diff 路径 live 值（OP-1，import 路径重置为 init Decision）；`ScadaCanvasStatus` 增 `destroyed` + wrapper `data-status` 反映销毁态（OP-4）。Proof 6 例（`scada-canvas-lifecycle-hardening.test.tsx`）：destroy disconnect、reload 后注入写新 pipeline、reset 失败强制 full、reload 保留 live 值、destroy 状态可见、reload 后 unmount animator 时钟停止。
- **Phase 2 事件与动画健壮性**：`event-bridge.ts` 各 handler 顶层 try/catch（`safeRun`）+ 去重上报（`onHandlerError` 选项 + 错误码 `handler-error`），明确不升级画布 status（SL-5/m3）；`handleHover` 同符号 emit 去重（WD-4：`lastHovered === symbolId` 时不重复 emit `symbol:hover`，hover-miss 后重置）；`dirty-collector.ts` `startAlwaysAnimations` 首次全量同步遍历 `getSymbolIds()` 全部动画承载图元（SL-1/m1：覆盖无 states/无绑定图元的 always 动画）；`point-store.ts` EventHub.emit + applyValue 订阅者循环双路径 try/catch（OP-3：单订阅者异常隔离 + 经 `onSubscriberError` 去重上报）。Proof 4 例（`scada-robustness-hardening.test.ts`）：handler throw 隔离（status 不变 + 后续事件可达）、hover 同符号 emit 去重、无 states/无绑定 always 动画启动、订阅者 throw 不中断批量写入。
- **Phase 3 flux 桥接与数据面**：`use-scada-points-bridge.ts` `extractFluxScopePaths` 扩展支持复杂表达式经平台依赖收集产出订阅路径（WD-2：`extractExpressionDepsViaProbe` —— compile + 宽容 probe scope `createTolerantProbeScopeData` + `evaluateWithState` → 读 `state.root.dependencies.paths`；平台 normalizeRootPath 收敛到根段，根级订阅经 `scopeChangeHitsDependencies` 前缀匹配生效）；`compiledCache` config 变更时清空（WD-3：杜绝长会话无界增长）。Proof 3 例：复杂表达式 `${analog.temp + 1}` 随 scope 变更更新（求值 25+1=26 → 100+1=101）、`compiledCache` config 变更清空（编译次数随变更线性增长）、`extractExpressionDepsViaProbe` 单元（多标识符 / 编译失败 / 静态表达式）。禁自研表达式解析——依赖路径产出完全经平台 collector。
- **Phase 4 错误面与命令契约**：`scada-errors.ts` 错误码注册表 `SCADA_ERROR_CODES`（12 码：config-parse/config-invalid/config-build-failed/engine-create-failed/flux-compile-failed/flux-evaluate-failed/handler-error/not-visible/not-mounted/symbol-not-found/point-not-found/invalid-config）+ `scadaErrorI18nKey` 映射 + `useScadaErrorText` React hook；`scada-canvas.tsx` 错误区经 hook 渲染本地化文案（未知码 fallback 原文）；`flux-i18n` locales `{zh-CN,en-US}.ts` 补 `industrial.scada.error.*` 文案（key-parity）；`use-scada-engine.ts` width/height effect deps 补 `args.width, args.height`（WD-1/m10：props 变更触发 `engine.setSize`）；viewport policy 裁定为「仅 full/reset 路径应用」现状契约（diff 重应用会重置用户平移/缩放，§8.3 同步）；`use-scada-handles.ts` fit/center 无 bounds 失败返回 `new Error('not-visible')`（WD-5：对齐 §8.5 表）；`image.ts` 修正误导注释（loadFailed 当前无画布级消费者，design-symbols.md 注记资源加载失败占位渲染 + 画布级诊断后置 I16）。doc drift 同步：`design-renderer.md §8.3`（width/height/viewport 反应实际语义）+ `§8.5`（not-visible + 错误码注册表 + absent config 兜底契约）+ `§4.3`（绑定域重载 live 值合并语义，Phase 1）；`design-engine.md §8.1`（引擎事件表删除 `ready`/`error` 行 + 注记归属 renderer 层 action 派发）；`design-symbols.md`（image 资源加载失败契约注记）。Proof 5 例：not-visible 失败路径断言（fit/center）、absent config ready 不回归、width/height props 变更触发 setSize、错误码 i18n 解析（含 fallback）、错误码注册表单元。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **521 tests / 38 files** 全绿（Phase 1+2 Proof 11 + Phase 3 新增 3 + Phase 4 新增 5 + scada-errors/ serialize/ symbols 等覆盖性回归 6；既有 496 无弱化）；workspace 全量 **typecheck 32/32、build 32/32、lint 32/32、test 59/59** 全绿。
- **plan 收口**：Phase 1–4 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选（除 closure-audit 项需独立子 agent fresh session）；roadmap Follow-up Backlog 标记收口（State & lifecycle 4 条 + Wiring & degradation 6 条 + Documentation drift 3 条 + Open-ended audit P2 3 条）。closure-audit 由独立子 agent 待执行（plan 保持 `Plan Status: active` 直到 audit 通过）。

## industrial-hmi plan-2026-08-04-1558-1 执行：public API surface 收敛三 Phase 全绿（注册语义 + 导出面收敛 + 静态元数据/author-less schema）+ workspace 全量验证

执行 `docs/plans/2026-08-04-1558-1-hmi-public-api-surface-convergence-plan.md`（P2 backlog：注册副作用移除 + 导出面收敛到 §11 授权面 + 静态元数据补齐 + author-less schema 空场景兜底 + dist 构建卫生），三 Phase 全部完成：

- **Phase 1 注册语义与幂等**：`register-builtin.ts` 新增公开 `registerScadaSymbols()`（幂等包装 `registerBuiltinScadaSymbols`，`hasScadaSymbol` 守卫）；`index.ts` 移除模块加载副作用 `registerBuiltinScadaSymbols()` 顶层调用（历史曾致 `import type` 消费者拖入 leafer-ui canvas 运行时，repo-wide test break）；`registerScadaRenderers` 内部首调 `registerScadaSymbols()`，消费方单点接入即完成全部注册。Proof 前置 + focused 单测（`renderer-definitions.test.ts` 新增 5 例：index 加载不自动注册、registerScadaRenderers 后 24 内置图元全量清单 + 族抽样可解析、幂等重复调用无 throw、不依赖前置 registerBuiltinScadaSymbols 调用）。doc 同步：`design-symbols.md:85` 幂等措辞对齐 impl 抛错行为（同 type 重复注册抛错，`override: true` 显式替换）；`design-engine.md §8.3` + `design-symbols.md:80` 补 `ScadaTestHandle`/`SymbolCreateContext.engine` `unknown` 强类型缺失注记（循环导入约束）。
- **Phase 2 导出面收敛与构建卫生**：`index.ts` 导出面从 ~93 符号收敛到 §11 授权面——保留 `registerScadaRenderers`/`registerScadaSymbols`/符号注册表 API（register/unregister/has/get/list）+ `builtinScadaSymbolDefinitions` + `serializeScadaConfig` + 类型面；移除 ~80 零消费者内部类（engine/binding/symbols/viewport 实现，audit dim 03 实核仅 `registerScadaRenderers`/`ScadaConfig`/`ScadaSymbolNode` 有外部消费者）。内部测试走 relative path 不受影响。`package.json` build 脚本前置 `rm -rf dist`（Decision：行内 POSIX shell 而非共享 clean-dist.mjs，Out Of Scope 不做全仓改造）；build 后 `dist/` 无 `scada-canvas-placeholder.*` stale 产物。doc 同步：`design-renderer.md §11` 目录注释 + 公共导出面清单同步实际导出。
- **Phase 3 静态元数据与 author-less schema**：`renderer-definitions.ts` 补齐静态元数据（范本 `basic-renderer-definitions.ts`）——`rendererClass: 'instance-renderer'`、`propContracts`(5: config/width/height/viewport/events)、`eventContracts`(5: onSymbolClick/onSymbolDblClick/onSymbolHover/onReady/onError，含 `ScadaSymbolEventPayload` 共享 shape + onError `{code,message}`)、`componentCapabilityContracts`(9 handles: fit/center/getSymbols/getSymbol/setPointValue/getPointTable/exportConfig/importConfig/destroy，对齐 `use-scada-handles.ts` SCADA_HANDLE_METHODS 与 §8.5 表)；`defaultSchema` 同步补 `config: { version:1, variables:[], symbols:[] }`。author-less schema 收口（P3 M1 修正版）：`parseAndValidateConfig`（`scada-canvas.tsx`）对缺 config（undefined/null/''）兜底返回最小合法空场景 `EMPTY_SCADA_CONFIG` → renderer 构建空画布、状态 ready（不再永久 loading，§10 absent-config 行为归属）。TDD：focused 单测先红（`expected 'loading' to be 'ready'`）→ 实现修复 → 绿；既有 smoke/lifecycle/handles 3 例按新行为校正断言（loading region 首帧渲染后 ready 翻转、importConfig 初始 ready、empty scene exportConfig 返回空 config + fit 仍因无 bounds 失败）。`serializeScadaConfig` Decision：保留导出（design-contract 函数，host 工具链可直调）+ §4.3 doc 注记裁定。doc 同步：`docs/components/index.md` 已注册领域 renderer 清单补 `scada-canvas`。
- **工具链验证**：`check-renderer-definition-fields-only` 全包无新增违规；`check-finite-prop-contracts` ok（propContracts 有限可枚举）；`check-schema-prop-coverage` 100%（propContracts 翻转 `hasClosedPropModel` 为 closed 模型，所有授权 key 已在 fields 声明，shape-validation 无回归）。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **489 tests / 35 files** 全绿（新增 7 例：注册语义 5 + 元数据 1 + smoke author-less 1；既有 482 按新行为校正 3 例断言无弱化）；workspace 全量 **typecheck 32/32、build 32/32、lint 59/59、test 59/59** 全绿；playground `141 tests / 22 files` 全绿（消费方接入不变验证）。
- **plan 收口**：closure-audit 由独立子 agent（fresh session，task `ses_03369165affeuVbmcCvNb4fJ7R`）执行判定 `pass`（5 项 checklist 逐项 live 核对——注册语义/导出面收敛/dist 清理/元数据齐全/author-less fallback/doc sync/全量 test）；Phase 1–3 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`。roadmap Follow-up Backlog 7 条标记收口（Dependency & packaging 2 + Public API surface 4 + Documentation drift 3，含 design-symbols/engine/index.md doc drift）。审计状态不回写（本 plan 无 `> Source Audits:` 行，roadmap-sourced plan）。

## industrial-hmi plan-2026-08-04-1235-3 执行：display-math manifest 四 Phase 全绿（P1-1 manifest 门 / P1-6 视口公式 / P1-7 overlay 变换面 / P1-9 zoom 锚点空间）+ mock 建模 + workspace 全量验证 + scada e2e 25/25

执行 `docs/plans/2026-08-04-1235-3-hmi-display-math-manifest-plan.md`（4 个 confirmed live P1：multi-audit P1-1 manifest 硬门失败、P1-6 初始视口 fill/center 公式错误、P1-7 InteractionOverlay 世界坐标绘制错位、P1-9 zoomLayer 锚点内容坐标漂移且被 mock 掩蔽），四 Phase 全部完成：

- **Phase 1 manifest 门（P1-1）**：基线复跑 `pnpm check:workspace-manifest-deps` 确认本包 3 条 undeclared（`renderer-test-support.tsx:14-15` 引 flux-formula/flux-runtime、`scada-points-bridge.test.tsx:5` 引 flux-formula）；`packages/flux-renderers-industrial/package.json` devDependencies 补 `@nop-chaos/flux-formula`/`@nop-chaos/flux-runtime`（workspace:\*）+ `pnpm install --lockfile-only` 无冲突；重跑 gate：本包 0 命中（残留 5 条为 form/scheduling 既有问题，不属本包）。
- **Phase 2 初始视口公式（P1-6，use-scada-config-sync.ts `applyInitialViewport`）**：fill 分支 `x: size.width/2 - cx·s` 改为 `x: bounds.x + bounds.width/2 - size.width/(2*scale)`（y 同理，`vx = cx - sw/(2s)` 语义；fill 保留 max-scale 不委托 `engine.fit`——fit 是 min-scale/contain 语义）；center 分支委托 `engine.center(bounds)`（viewport.ts:70-78 同公式，消除双实现漂移）。Proof 前置：lifecycle 测试非巧合几何（bounds cx=155、fill scale=12、sw=800）精确断言先红后绿（fill+center、center-only scale=1、contain min-scale 回归三例，`not.toBe(0)` 弱断言升级）。
- **Phase 3 覆盖物变换面对齐（P1-7，interaction-overlay.ts + scada-engine.ts）**：覆盖物改 **screen 坐标**绘制（sky 层恒等变换即屏幕像素）——`highlight` x/y 经 `engine.getViewportPoint` 换算、宽/高乘当前 scale、rotation 不变、**strokeWidth 保持 preset 屏幕像素不除 scale**；新增 `refresh()` 按最新视口重算全部活动覆盖物，接线**双钩子**——命令路径 `applyViewportState`（zoomAt/fit/center/setViewport）+ 插件 zoom/move sync 路径（`handlePluginZoom`/`handlePluginMove`）；**覆盖物 Group `hittable: false`**（screen 对齐后若不屏蔽命中，sky 覆盖物吞掉 tree 指针事件 → click/hover 链路断裂，e2e I11 click→dialog 链实测暴露并回归）。Proof 前置：engine 测试非恒等视口（{x:100,y:0,scale:2} → screen -180,40,200×100 + strokeWidth 2）+ 命令路径/插件 zoom/move 路径刷新重定位先红后绿；既有 identity-viewport 行为（hover 跟随/applyDiff 重定位/hover-miss 清除）不回退。
- **Phase 4 zoomLayer 锚点空间修正 + mock 建模（P1-9）**：`leafer-ui-mock.ts` `MockZoomLayer.scaleOfWorld` 按 leafer-ui@2.2.9 真实语义（`zoomOfWorld` → `getTempLocal` 按 parent 世界矩阵逆变换，origin 为 **zoomLayer 外层（screen）空间点**；`zoomOfLocal`/`scaleOfOuter` 锚定反推平移）建模 x/y 副作用（`x = (x-ox)·k + ox`，此前只乘 scaleX/scaleY 掩蔽漂移）；`applyViewportState` 与 `handlePluginZoom` 兜底两处均改传 **screen 原点 `{x:0,y:0}`** 锚点（旧实现传内容坐标 `viewportToWorld(cur,{0,0})` → 被当 screen 点固定 → 内容漂移 `(vx·(1-k), vy·(1-k))`；屏幕原点锚定 = 视口 x/y 不变、仅 scale 变化，与 `move({-(Δx)·scale})` 合成精确视口态）。TDD 序：mock 先修 → 矩阵级失败用例（pan 后 zoomAt 断言 `zoomLayer.x = -viewport.x·scale` 无漂移，新 mock+旧引擎下 -450≠-300 红）→ 引擎两处修正 → 全绿；plugin clamp 兜底一致性矩阵断言（越界 25x 钳回 20x 视口 x/y 不变）+ e2e 程序化矩阵级断言（经测试句柄读视口 + zoomLayer 矩阵）。
- **e2e 补强**：`scada-demo.spec.ts` 新增 2 条——P1-7 覆盖物跨 pan/zoom 对齐（命令路径 setViewport + 插件 wheel 平移 + ctrl+wheel 缩放路径，leafer 默认 zoomMode 需 ctrl/meta 核对）、P1-9 矩阵级 zoomAt 锚点断言；`scada-edge-cases.spec.ts` 多边形覆盖物断言从世界坐标升级为 screen 坐标（页面 `viewport: {fit:'contain'}` 非恒等，旧断言掩蔽未对齐缺陷）。
- **文档同步**：`design-engine.md §4.4`（P1-9 增补：scaleOfWorld 锚点空间/screen 原点/禁内容坐标锚点 + mock 建模声明）+ `§6` 交互覆盖层行（P1-7：screen 坐标绘制 + refresh 双钩子 + hittable:false）；`design-renderer.md` 无契约文字变化（初始视口 wiring 语义不变，仅公式修正，§4.2/§8.3 无需回写）；roadmap-industrial-hmi.md 记录 P1 remediation 轮 plan `{3}` 执行完成 + mock P2 backlog 条目标记收口。审计状态不回写（本 plan 无 `> Source Audits:` 行，roadmap-sourced plan）。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **483 tests / 35 files** 全绿（新增 10 例：lifecycle 视口精确断言 3 + engine overlay 5 + engine 矩阵级 2；既有 477 无弱化，M-3/插件同步/applyDiff 覆盖物等 gate-3/gate-4 回归全保持）；workspace 全量 **typecheck 32/32、build 32/32、lint 32/32、test 59/59** 全绿；**scada-\* e2e 25/25 全绿**（demo 12 + edge 5 + pressure 3 + perf 5）。全量 e2e 连跑：除 1 例 **pre-existing** `gantt-bars-and-links resize-right` 失败（HEAD stash A/B 对照：基线同样失败，本 plan 变更面零触及 gantt）与 ai-attachments 2 例高负载 flake（isolated 复跑通过）外全绿——**unit 层 full-green；e2e 声明 scada 面 25/25 全绿 + 全量 e2e 唯一残余为既知 pre-existing 基线失败**（按 08-03 先例如实声明，不虚报全量 e2e full-green）。
- **plan 收口**：closure-audit 由独立子 agent（fresh session，task `ses_0344967f8ffeKI8LXXT4kAHd4B`）执行判定 `approved`（4 Phase Exit Criteria + Closure Gates 逐项 live 核对——manifest 门复跑 0 命中/公式源码行号/refresh 三钩子/两处锚点/mock 语义与 core.esm.js 推导一致/测试实跑 483/35/e2e 25/25；2 Minor 卫生项——daily log 与 roadmap 记录——已随本收口落地）；Phase 1–4 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（证据见 plan Closure Audit Evidence）。

## industrial-hmi mission-driver verify：workspace 四命令全绿 + scada e2e 23/23；全量 e2e 2 例已知机器态 flake（未达 full-green，不声明）

mission-driver 对 plan-2026-08-04-1235-2 变更集执行 verify：**workspace typecheck 32/32、build 32/32、lint 32/32、test 59/59（unit）全绿**；**scada-\* e2e 23/23 全绿**（demo/edge/pressure/perf，含 100k 性能档）。全量 e2e 连跑 5 轮：每轮 853–861 passed，仅间歇命中 2 例**已记录的 pre-existing 机器态 flake**（docs/logs/2026-08-03.md:719,752）——`gantt-bars-and-links resize-right`（C1.3，HEAD stash 对照同样失败）与 `w3d-editor bold`（isolated 复跑均通过；本轮机器 load avg 14–24、13 用户，高负载下时序敏感测试间歇失败），均不触及本 mission 代码面（仅 flux-renderers-industrial 变更，gantt/w3d-editor 零改动）。按 08-03 先例：**未达 full-green，不声明**。mission 四命令 + scada e2e 全绿，本变更集提交（code + docs 分提交）。

## industrial-hmi plan-2026-08-04-1235-2 执行：首帧/生命周期 wiring 五 Phase 全绿（P1-2/P1-3/P1-8/background+viewport/StateVisualApplier 修复）+ workspace 全量验证 + scada e2e 23/23

执行 `docs/plans/2026-08-04-1235-2-hmi-lifecycle-wiring-plan.md`（5 个 confirmed live P1：multi-audit P1-2 首帧无刷新、P1-3 ready 双发、P1-8 数据错误升级、open-audit P1-A background/viewport validated-but-dead、P1-B StateVisualApplier 未接线），五 Phase 全部完成：

- **Phase 1 首帧绑定刷新（P1-2，use-scada-engine.ts `reloadBindings`）**：绑定域重建后（`setRuntime(next)` 前）对新建 pipeline **无条件**调 `pipeline.requestRender(applyAttrsOf(engine))`——`synced=false` 全量首同步路径被实际执行（此前 requestRender 仅 3 处写入源触发，纯静态/表达式点配置挂载后永不 flush）。触发点内置在 reloadBindings 内部（config-sync 闭包持有旧 runtime，外部触发会打到已销毁 pipeline）；full 与非空 diff 两路径都经 reloadBindings，diff 新增静态绑定同样首同步，不做策略门控。Proof 前置：lifecycle-wiring 测试「零写入挂载 → 静态点 map 绑定 fill/text、表达式点求值、状态色、`when:'always'` 动画启动」先红后绿；`scada-handles.test.tsx` getPointTable 断言同步（表达式点 calc 挂载即求值入表 + 写入后等 rAF 合帧重算，断言确定化）。
- **Phase 2 ready 生命周期恰一次（P1-3）**：change 基准守卫代码随 plan `{1}` 已落地（full 与非空 diff 路径触发 onBuilt、空 diff 重跑仅维护 prevRef），本 Phase 补计数回归测试收口四场景——mount 恰 1、**full reset 恰 1（经 importConfig：config.version 契约恒为 1，版本变更走校验错误而非 full reset，全量路径 = mount/importConfig）**、非空 diff 恰 1、宿主每渲染传同值新对象身份 0 额外触发（open-audit identity-vs-change 触发点覆盖）。
- **Phase 3 组态 JSON background/viewport 接线（open P1-A）**：`engine.reset(config)` 应用 `config.background.color` 到 ground 层（与构造期 `ScadaEngineOptions.background` 同口径，reset 覆盖构造值，mount 期 config 不可用故接线在 reset 期）；`config.viewport {x,y,scale}` 存在时经 `engine.setViewport` 作为初始视口应用（full/reset 路径含 importConfig，props `viewport` policy 为显式首选项，两者都无保持现状，diff 路径不重应用对齐 gate-4 m-B）；`background.grid` 裁定 **watch-only 不接线**（validate 接受但无 runtime 消费面，design-renderer.md §4.2 显式注明防 author 误用，roadmap Follow-up Backlog 登记）。回归 2 例：background/viewport 挂载生效 + importConfig 全量重应用、props policy 优先级覆盖。
- **Phase 4 状态样式恢复接线（open P1-B）**：按 Decision 方案 (a)——`createBindingDomain` 内实例化 `StateVisualApplier` 并 `attachTo(pipeline)`（`state:change` 事件真实存在，dirty-collector.ts:303 发射）；**每次 pipeline 重建（mount/reloadBindings）随 createBindingDomain 重新 attach**（不能只在 mount effect 挂一次）。回归 1 例：部分状态声明（style 只在 fault）fault→run 退出恢复 base fill（此前 collectStates 只收集目标状态 style 补丁、无恢复收集，故障色永远残留；既有全三态 state-visual/device/sensor-control 用例全绿无回退）。
- **Phase 5 数据错误降级与恢复（P1-8，use-scada-points-bridge.ts + scada-canvas.tsx）**：flux 编译/求值失败 → 跳过该声明（continue）+ **lastError 式去重上报**（`lastReportedErrors` Map：同表达式同错误码仅在变化时上报一次，该声明求值成功后清空记录允许下次失败再报）；scada-canvas 切断 points-bridge `onError → handleError` 直通（数据错误不升级画布级 error、不派发 `scada:error`，§8.1 onError 仅限 config 校验/构建失败；config 错误通道不回退）；scope 数据修复后求值成功经既有注入路径自动回流（status 未被污染，无需 handleReady 复位分支）。回归 2 例：hook 级去重（失败→去重→scope 修复回流→再次失败重新上报）+ 画布级（保持 ready/该点不更新/零 scada:error 派发/scope 修复后 text 42 回流）。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **475 tests / 35 files** 全绿（新增 7 例：lifecycle-wiring 6 + bridge dedup 1；既有 468 无弱化，`scada-handles` getPointTable 1 例断言按新首帧语义更新）；workspace 全量 **typecheck 32/32、build 32/32、lint 32/32、test 59/59** 全绿；**scada-\* e2e 23/23 全绿**（demo 10 + edge 5 + pressure 3 + perf 5）。
- **文档同步**：`design-renderer.md §4.2`（background.color 接线 / grid watch-only / viewport 组态默认 + props policy 优先）+ `§8.1`（ready 按构建触发恰一次；数据错误降级不升级 error 不派发 scada:error）；roadmap-industrial-hmi.md 记录 P1 remediation 轮 plan `{2}` 执行完成 + Follow-up Backlog 2 条标记收口（flux 错误去重、onReady 语义漂移）。审计状态不回写（无 `> Source Audits:` 行且 audits 为 {1}/{2}/{3} 三 plan 共享，plan `{3}` 未执行，保持 `planned`）。
- **plan 收口**：closure-audit 由独立子 agent（fresh session，task `ses_034b1b464ffe3xTCcMoHZMckVE`）执行判定 `approved`（5 Phase Exit Criteria 逐项 live 核对 + interface-vs-semantics 抽查 + deferred 诚实 + 包级 475/35 复跑；2 Minor 卫生项——日志措辞与 roadmap 文件计数——已落地）；Phase 1–5 Status completed + 全部 items/Exit Criteria/Closure Gates 勾选 + `Plan Status: completed`（证据见 plan Closure Audit Evidence）。

## industrial-hmi plan-2026-08-04-1235-1 执行：diff 路径收敛与双写源调和三 Phase 全绿（P1-4/P1-5/type-drop 修复）+ workspace 全量验证 59/59

执行 `docs/plans/2026-08-04-1235-1-hmi-diff-path-convergence-plan.md`（3 个 confirmed live P1：multi-audit P1-4 nodeById 过期、P1-5 importConfig 双写源、open-audit P1 type-drop），三 Phase 全部完成：

- **Phase 1 diff 契约（serialization/diff.ts）**：同 id 图元 `type` 变更不再产出 `updated` patch（`applyUpdate` 只能 set 旧 leafer 节点），改产出 `removed`（旧 id）+ `added`（新节点）走 remove-then-rebuild；`children` 差异为 `undefined`（group 变叶子/子树删除）产出显式 `children: []` patch，使 `applyUpdate` 走"移除全部子树"分支而非被 `!== undefined` 守卫跳过。Proof 前置：serialization.test.ts 新增 2 用例（type-change、children-removal）先红后绿。
- **Phase 2 ConfigAdapter（engine/config-adapter.ts + scada-engine.ts plumbing）**：`applyDiff` 处理顺序改为 `removed → added → updated`（type 变更 remove+add 语义下旧节点先销毁、新节点后构建，registry/nodeById/树三者一致）；`applyUpdate` 补充 `nodeById.set(id, 新 config 节点)`（children 分支保留既有重建 + group 自身校准；非 children patch 同样刷新索引）；`applyDiff` 结束时用 nextConfig 校准所有 added/updated id 的 nodeById（engine.applyDiff 把 nextConfig 透传 adapter）。新增 2 用例：type-change 无残留（树根 children 长度 1 + tag Ellipse + getConfigNode 返回新节点）、非 children updated id 的 states/animations 声明同步（getSymbolDeclarations 返回新声明）。
- **Phase 3 importConfig 汇入 props 同步链（use-scada-config-sync.ts + scada-canvas.tsx）**：按 Decision 落地 `syncImported(config)`——置 `prevRef` + skip-next 一次性标记（计数 + 配置身份匹配，防 `reloadBindings → setRuntime` 引起的 effect 重跑用过期基线对 props config 算非空 diff 回刷 import）+ `engine.reset` + `reloadBindings` + change 基准 onBuilt（遵守 plan `{2}` Phase 2 语义：full 路径与非空 diff 路径才触发，空 diff 重跑仅维护基线）；scada-canvas.tsx `reloadConfig` 从直接 `engine.reset` 改为调用 `syncImported`（`use-scada-handles.ts` 句柄签名不变）。新增 2 用例：import-then-edit 幂等（编辑对象源自 import 场景——props config 含 import 引入 id，断言树根无重复节点 + data-status ready）、import 无 props config 时立即生效且持久 + status loading → ready 恢复。
- **回归测试 3 组**（diff type-change / diff 后声明生效 / import-then-edit 幂等）全绿入库；`design-renderer.md §4.3` diff 契约文字已同步（type-change remove+add + children undefined→`[]`）。
- **验证**：包级 `pnpm --filter @nop-chaos/flux-renderers-industrial test` **468 tests / 34 files** 全绿；workspace 全量 **typecheck 32/32、build 32/32、lint 32/32、test 59/59**（unit 层 full-green；e2e 未在本轮执行，保持既有基线）。
- **plan 收口**：Phase 1–3 Status completed + 全部 items/Exit Criteria 勾选；roadmap-industrial-hmi.md 记录 P1 remediation 轮 plan `{1}` 执行完成；审计状态不回写（无 `> Source Audits:` 行且 audits 为 {1}/{2}/{3} 三 plan 共享，plan `{2}`/`{3}` 未执行，保持 `planned`）。

## industrial-hmi mission-driver DRAFT：两份 open 审计（12 P1）→ 3 份 remediation plans（全部 draft→active）+ P2 全量入 roadmap backlog + 审计状态 planned

mission-driver DRAFT 阶段处理 `docs/audits/` 全部 open 审计（仅 2026-08-03-1506 multi + open 两份，industrial-hmi 同 session 产物；其余审计 closed/planned）：

- **P1 集合（12 项，均经 live 复核确认）**：multi-audit 9 P1（manifest 门 P1-1、首帧无刷新 P1-2、ready 双发 P1-3、nodeById 过期 P1-4、importConfig 双写源 P1-5、视口公式 P1-6、overlay sky 坐标 P1-7、数据错误升级 P1-8、zoomLayer 锚点 P1-9）+ open-audit 3 P1（config background/viewport validated-but-dead、StateVisualApplier 未接线、diff type-drop）。0 P0。
- **3 份 remediation plans（全部 `Plan Status: draft → active`，独立 fresh-session 子 agent 逐份反复审查至共识——零 Blocker/Major）**：
  - `docs/plans/2026-08-04-1235-1-hmi-diff-path-convergence-plan.md`（3 P1：P1-4/P1-5/type-drop）——diff 应用收敛 invariant（removed→added 顺序、nodeById 同步、importConfig 经 `syncImported`（prevRef + skip-next 一次性标记 + change 守卫 onBuilt）汇入 props 链防 effect 重跑回刷）；3 Phase，Proof 前置 TDD；审查 3 轮（R1 fail 1M+6m / R2 fail 1M / R3 pass）。
  - `docs/plans/2026-08-04-1235-2-hmi-lifecycle-wiring-plan.md`（5 P1：P1-2/P1-3/P1-8/background+viewport/StateVisualApplier）——首帧触发内置 `reloadBindings` 重建后（无条件）、onBuilt change 基准守卫、flux 错误按声明降级+lastError 去重+成功后清空、config JSON 字段接线（background.grid watch-only 裁定）、StateVisualApplier 随 createBindingDomain 重新 attach；5 Phase；审查 2 轮（R1 fail 2M+5m / R2 pass）。
  - `docs/plans/2026-08-04-1235-3-hmi-display-math-manifest-plan.md`（4 P1：P1-1/P1-6/P1-7/P1-9）——manifest devDeps 补齐、fill/center 公式修正（cx−sw/(2s)，禁 fit 委托）、overlay screen 坐标绘制 + 双钩子 refresh、zoomLayer 锚点 screen 空间 + mock x/y 锚定建模 + handlePluginZoom 同修；4 Phase；审查 2 轮（R1 fail 2M+3m / R2 pass）。
- **执行顺序 `{N}`**：`{1}` diff-path（最弱契约面）→ `{2}` lifecycle wiring → `{3}` display math + manifest；三 plan 交叉引用（`{2}` 守卫被 `{1}` 依赖、`{3}` 公式被 `{2}` 共用）。
- **P2 收口**：~36 条（multi ~30 + open 6）全部登记 `docs/components/roadmap-industrial-hmi.md` `## Follow-up Backlog`（新建节），每条带源审计路径；open-audit「onBuilt change 基准」P2 标记由 plan `{2}` Phase 2 收口。
- **审计状态回写**：两份审计 `Audit Status: open → planned`（+ `> Planned Into:` 三 plan 映射）。
- **验证**：纯文档阶段（无代码变更），typecheck/build/lint/test 未执行（plan 执行期收口）；draft review 证据已入各 plan `## Draft Review Record`。

## industrial-hmi mission-driver DEEP_AUDIT：多维度审计收口（docs/audits/2026-08-03-1506-multi-audit-industrial-hmi.md，9 P1 + ~30 P2）

mission-driver DEEP_AUDIT 阶段对 `packages/flux-renderers-industrial`（scada-canvas，mission 全链 I0–I16 done）执行多维度深度审计（`docs/skills/deep-audit-prompts.md`，v1 基线）：

- **机械门禁**：typecheck/lint/build PASS；unit **462/462（34 files）**、coverage 95.88% stmts / 90.57% branch；suspect 扫描 8 处 async 候选全部判定为结构化失败路径、2 处 JSON.stringify 非热路径；**`check:workspace-manifest-deps` 实测 FAIL**（本包 3 处未声明导入 + form/scheduling 5 处预存）。
- **执行方式**：12 个 deep-dive 子 agent（维度 01/03/04/06/09/14/16/21/22/23，两轮）+ 主 agent 逐条 live 复核 + 独立 fresh-session 复核 agent 对 10 条 P1 候选逐条重验（9 成立 / 1 降级 / 0 驳回）。
- **9 项 P1**（均经独立复核）：① manifest 硬门禁失败（test-support/points-bridge 缺 flux-formula/flux-runtime devDeps）；② 挂载后无初始刷新——静态/表达式点绑定值/状态样式/动画永不应用（requestRender 仅 3 处写入源触发）；③ scada:ready 每次 config 应用双派发（reloadBindings→setRuntime→空 diff 无条件 onBuilt）；④ applyDiff 后 nodeById 陈旧——states/animations 声明读旧节点；⑤ importConfig 绕过 config-sync prevRef 基线——后续 props 变更 added 节点重复构建；⑥ applyInitialViewport fill/center 居中公式错误（应 cx−sw/(2s)，实为 sw/2−cx·s）；⑦ InteractionOverlay 挂 sky 层用世界坐标——平移/缩放后 hover 覆盖物错位；⑧ 单个 flux 表达式求值失败整幅画布降级 error 且无恢复路径（onError 每轮重发）；⑨ zoomLayer===tree（leafer 2.2.9 `_zoomLayer` 从未赋值）+ 程序化缩放锚点传内容坐标（插件传屏幕坐标）→ 非零平移视口下缩放跳变，被 mock 的 scaleOfWorld 缺 x/y 副作用掩蔽（gate-3 同类 mock↔真实漂移）。
- **P2 约 30 项**（backlog）：API 面 91 符号零消费、renderer-definitions 缺静态 authoring 元数据、when:'always' 动画被 states 门槛拦截、destroy 不拆 ResizeObserver、hover 无去重、computeSymbolBounds 忽略 points、textAlign center 自动宽度失效、canvas data-slot 落空 div、perf spec 拖动 fps 口径恒真、batch-add-probe 0% 覆盖、design-\*.md 9 处漂移等。
- **结论**：0 P0；审计状态 `open`，需 remediation plan 覆盖 9 项 P1；P2 走 follow-up backlog。

## industrial-hmi I16 组态编辑器后继 mission 立项入口收口：plan-2026-08-04-0902-2 两 Phase 全绿 + 文档共识审查 2 轮达成 AGREE + roadmap I16 planned → done

执行 `docs/plans/2026-08-04-0902-2-i16-editor-initiation-entry.md`（Work Item I16，roadmap `todo → planned → done`，`planned → done` 由独立 closure-audit 核验后回写；**仅产出立项材料，不实现编辑器**）：

- **立项材料落盘** `docs/components/industrial-hmi/editor-initiation.md`（v1，8 节，经 2 轮文档共识审查达成 AGREE）：
  - **复用点三态清单（Phase 1 Proof，live 核对）**：10 项复用点逐项以 `packages/flux-renderers-industrial/src/` 源码为准核对——引擎层（18 项命令面 + applyDiff + 事件桥 + 命中解包，scada-engine.ts:103-120/:297）、ConfigAdapter nodeById O(1)（config-adapter.ts:16，I14 优化）、图元注册表（symbol-registry.ts + register-builtin.ts 24 定义）、序列化四件套（parse/validate/serialize/diff）、SCADA_HANDLE_METHODS 9 方法（use-scada-handles.ts:10-20）、InteractionOverlay（interaction-overlay.ts，points 包围盒兜底）、测试句柄（test-handle.ts 恒开）、binding 域、事件派发链、benchmark 基座——**gate-4 §10 行 241 四复用点全为「已落地+需扩展」，10 项整体无缺失，需扩展集中为 2 处**（图元属性 schema 统一抽取、编辑态交互覆盖物族与 undo 事务语义）。
  - **范围与优先级裁定（Phase 1 Decision）**：5 功能域（拖拽放置 P0/属性面板 schema P0/连线 P1/undo-redo P1/画布工具箱 P2）+ M1/M2/M3 里程碑拆分 + 编辑态/运行态分离边界约束（对齐讨论 Q8）。
  - **技术选型考量（Phase 1 Fix）**：leafer-editor 插件 vs 自研交互层——证据链（render-engines §5 能力边界/§12 #16 叠加层、download 许可体积、spike 未覆盖 Editor 的 V7 口径 + 轻量核证：@leafer-editor/partner 共装 editor+viewport、Editor extends Group、simulate.ts 挂 zoomLayer）+ 主路径 A（插件底座+组态语义适配层自研）+ 否决条件（spike 验证不成立转路径 B + 人工确认）+ **3 项「待后继 spike 验证」**（viewport+Editor 手势仲裁/Editor 事件族载荷面/编辑态覆盖物密集性能）；吸收 gate-3 §3 mock↔真实漂移教训（编辑器交互层 API 以真实 leafer spike 固化，禁以 mock 推断）。
  - **工作量与里程碑（Phase 1 Fix）**：档位化（基准定义 = 运行时实现域 I4–I15 整期；M1=L 2–3×/M2=M 1–1.5×/M3=M 1–1.5×，合计 4–6× 略高于 Q1 3–5× 档，spike 校准）+ **benchmark 包络适用性说明**（运行态包络不变、编辑态包络另立并人工确认、编辑-运行同帧仍受运行态红线）。
  - **风险与人工确认项预标记（Phase 1 Fix）**：8 项风险（R1 手势仲裁选型/R7 编辑态包络/R8 工程量进度等）+ 3 项人工确认项汇总（编辑器选型/编辑态包络数字/M1 交付边界）。
- **roadmap 回写（Phase 1 Follow-up）**：I16 `todo → planned`（激活期同步执行，I3/I15 先例，Rule 1 状态机）。
- **Phase 2 文档共识审查（Proof/Fix）**：R1（task `ses_0354c6ed7ffeDZKSpKfz9ttE1r`）`REVISE` 1 Major + 6 Minor + 1 Nit——`M-1` 更正「meta2d 无内置 undo」错误事实（本地 clone 实锤 canvas.ts:4024-4048 histories 栈 + core.ts:2512-2517 转发，改正面蓝本证据）；`m-1`/`m-2` 引注修位（scada-engine.ts :83-91→:103-120、:365-391→:403-437）；`m-3` 17→18 项命令面（逐名计数）；`m-4` maxGraph 引注 supplement §3→§4；`m-5` 三态结论「无缺失」vs「缺失面」同句矛盾消除；`m-6` §5.1「单期」口径定义；`N-1` Editor 事件名补全称——**8 项全部落地（编写者经 live 复核）**；R2（task `ses_0354540deffeWoTv8wSBBzwkmO`）确认轮 `AGREE` 零新增修正项——**共识达成（2 轮 ≤3 上限）**，材料可作后继 mission 编排输入。
- **roadmap 回写（Phase 2 Follow-up）**：I16 `planned → done`（由独立 closure-audit 核验后回写，见下）。
- **closure-audit 通过（独立子 agent fresh session，task `ses_03540a864ffeYfVV7vgePBRGY3`，不复用执行上下文）**：判定 `approved`——Phase 1/2 items 与 exit criteria 逐项 live 核对落地（roadmap I16=planned、§3 三态 10 项与 live 源码吻合、共识 2 轮 8 项修正实落盘、benchmark 数字一致、无静默降级、五处状态自洽）；2 项 Minor 卫生项关闭前处理（design-engine §12.4 引注→§12.3、本日志 Phase 1 条目旧引注修正——均已完成）。
- **验证**：纯文档计划（无代码变更），workspace 全量 `pnpm test` 59/59 全绿复跑确认（typecheck/build/lint 按 plan Closure Gates 删除项不执行，对齐 guide Minimum Rule 18）；`docs/logs/2026/08-04.md` 收口摘要（本条目）。
- **plan 收口**：Phase 1/2 全勾 + Closure Gates 全勾 + `Plan Status: completed`（Closure Audit Evidence 已回填）。

## industrial-hmi mission-driver verify 收口：全量 full-green（unit 59/59 + e2e 862 passed / 0 failed）+ route inventory 补缺 + master 修复对齐 + I16 立项入口

mission-driver 对 industrial-hmi 全链执行 verify（plan-2026-08-04-0902 系列）：**workspace typecheck/build/lint 32/32 全绿、unit 59/59 任务全绿、e2e 全量 862 passed / 0 failed / 43 skipped（full-green verification）**：

- **route inventory 收尾缺口修复**：`playground-entry-pages.spec.ts` `ROUTE_ASSERTIONS` 缺 scada 四路由断言（I13/I14/I15 新增 `scada-demo`/`scada-pressure-demo`/`scada-edge-cases`/`scada-perf-scale` 于 `DOMAIN_RENDERER_ROUTES`，spec 未同步）→ 补 4 条 h1 heading 断言（scada-demo 工艺流程组态演示页 / scada-pressure-demo 大屏·复杂组态示例页 / scada 边界用例测试页 / scada-perf-scale 性能基准测量页），`scada-perf-scale` 入 `ROUTES_WITH_KNOWN_ERRORS`（perf 页测量告警口径，对齐 calendar-perf-scale 先例）；修复后 route inventory 断言 + 4 smoke 全绿。
- **master 既有修复拉齐（cherry-pick，分支切出后 master 已修，本分支不引入）：** `1e25f554`（renderers-ai：ai-chat showTimestamp 经 message-list 接线至 bubbles，A-4 dead wiring）+ `abcecfa9`（ai/calendar/diff-perf 8 例修复：timestamp 首气泡断言 / stale selector 换 data-slot / 日历 locale-agnostic aria-label / diff-perf demo defaultCollapsedLines 15→0 + <5000ms 校准预算）——二者为预存失败，非 mission 引入，拉齐后 ai-chat/ai-rich-text-sender/calendar-demo/diff-perf 全绿。
- **新 plan 起草**：`docs/plans/2026-08-04-0902-2-i16-editor-initiation-entry.md`（I16 组态编辑器后继 mission 立项入口，仅产出立项材料不实现：功能范围/复用点盘点/选型考量/工作量评估）。

## industrial-hmi I15 测试补强、文档与收尾：plan-2026-08-04-0902-1 两 Phase 全绿 + 断言矩阵落地 + 2 个真实浏览器缺陷修复 + 文档全链收口

执行 `docs/plans/2026-08-04-0902-1-i15-test-harness-and-doc-closeout.md`（Work Item I15，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **I15.1 测试补强（断言矩阵清单落盘于 plan）**：
  - **边界用例挂载载体裁定（`edge-case-carrier`）**：空画面/非法 JSON 新增独立测试页 `#/scada-edge-cases`（`scada-edge-demo.tsx`：minimal/empty-scene/invalid-json/line-polygon 四 tab，对齐 scada-perf-scale 独立页先例——route-model DOMAIN_RENDERER_ROUTES + App switch + pages/index + home NAV_CARDS/NavigationTarget）；超大画面复用既有载体（10k pressure / 100k perf-scale）不新增；onError 观测面 = schema 级 events.onError → showToast → env.notify 落页面文本 `data-testid="scada-edge-notify"`。
  - **e2e 断言矩阵**：`scada-demo.spec.ts` +6（场景树图元类型集合 12 类、getSymbolProps leafer 属性面 m-6——text-title `fontSize`/pump-1 x/y/未命中 undefined、static 轨值变化→motor body fill `#e53935`/`#00cc66`、flux 轨渲染一致性——level-1 liquid height 随定时器变化、dblclick→navigate `#/flux-basic`、hover 覆盖物出现/清除、fit/center 视口句柄经 setViewport 打散后断言）；`scada-edge-cases.spec.ts` 新增 5（最小合法 config 无 empty region、空画面 symbols:[] ready、非法 JSON → data-status=error + empty region 可见 + onError 派发、线 hover 覆盖物 240×8、多边形 hover 覆盖物 160×120 精确断言 + A→B 切换 + 空白区清除）。
  - **i18n 接入**：flux-i18n 新增 `industrial` namespace（`industrial.scada.canvasError`：zh-CN「画布场景错误」/ en-US「Scada canvas error」）；接入面裁定——displayName 模块级静态字段不可用 t()、图元名文案无运行时消费面，仅 i18n-ize 有消费面的 empty region 缺省错误文本（scada-canvas.tsx 经 `useFluxTranslation` 解析）；i18n 单测 +2（双语解析 + key 集合一致性）。
  - **V5 体积面补强（I5 deferred 兑现）**：symbols.test.ts 新增 24 内置符号 engine 全路径加载断言（validate → engine.reset → registry 24 + 全类型覆盖，scada-group 容器经 node tag 归位）。
  - **发现并修复 2 个真实浏览器缺陷**（gate-3 类 mock↔真实漂移，e2e 断言矩阵先红后绿，记录 `docs/bugs/76-scada-hover-overlay-real-browser-drift-fix.md`）：
    - **hover-miss 永不发射**：真实 leafer 空白画布命中路径为空/defaultPath，`pointer.move` 不派发到 tree 层（`emitAppChildren` 仅放行 move/zoom/rotate/key 前缀，实测探针佐证）→ hover 覆盖物移出图元后不消失。修复：EventBridge `pointer.move`/`pointer.leave` 改挂 **App 视图面**（`moveTarget`）+ 2 个 focused 回归测试。
    - **多边形覆盖物几何 100×100 默认框**：真实 leafer Path 形状节点 width/height 为默认占位值（Polygon 100×100/Line 100×0），points 才是真实几何 → `resolveOverlayGeometry` 改 **points 优先**。
  - **验证**：包级 462 tests / 34 files 全绿（+2 新增，既有 460 不弱化）；**scada-\* e2e 23/23 全绿**（demo 10/10 + edge 5/5 + pressure 3/3 + perf 5/5，`assertTrackedPageErrors` 无 console error；perf 首轮 1 例帧钟抖动 flake 复跑全绿，I13/I14 先例口径）。
- **I15.2 文档收尾**：
  - **D-1 events 通道 drift 同步**：design-renderer.md §5 字段分类表与 §5 注回写为 prop 注册形态（`events` 整体 prop + renderer 派发，对齐 renderer-definitions.ts:27 与 renderer-boundary-audit.md D-1 裁定）；审计 D-1 影响面与例外表闭环标注（flux-compiler 点号字段支持保留为 watch-only residual）。
  - **架构文档增量**：`renderer-runtime.md` 新增「Industrial Package」节（注册面/字段分类/D-1 事件通道/引擎生命周期/组件句柄/测试句柄/性能红线）；`flux-runtime-module-boundaries.md` 新增 `@nop-chaos/flux-renderers-industrial` 边界节（契约归属/引擎依赖/事件通道/数据桥接/测试句柄/性能红线）。
  - **quick-reference.md**：Package Directory Map 增 `flux-renderers-industrial` 行 + 「Industrial Package」节（组件注册/Schema 类型/hooks/component 句柄/测试句柄/性能红线，对齐 scheduling §723 先例）。
  - **flux-guide**：`design-patterns/scada.md` 新增（组态 JSON 完整示例 + 点表三源 + 事件联动 + 画布浏览视口 + 性能红线 + 测试句柄 + 字段参考）+ README 表项 #39；`scripts/shared.mjs` REGISTER_PACKAGES 增 `{ pkg: 'flux-renderers-industrial', fn: 'registerScadaRenderers' }`；`generate-types.mjs` PACKAGE_CATEGORY/CATEGORY_COMMENTS/catOrder 增 `industrial`；**`pnpm build` 后重新生成 `flux-types/schema.d.ts`/`index.ts`**（115 定义，`ScadaCanvasSchema` 含 config/width/height/viewport/events prop + loading/empty region，FluxSchema/FluxSchemaByType 含 scada-canvas）；脚本环境适配：新增 `scripts/env-stub.mjs`（leafer-ui/sonner/html2canvas 模块级浏览器全局 stub：CanvasRenderingContext2D/Path2D/window/location/document/Image/PointerEvent/DragEvent + universal canvas ctx Proxy）+ package.json generate-types/validate 注入 `--import`；`validate.mjs` 注册 `registerScadaRenderers`。**flux-guide 局部验证通过**：scada.md 零错误零警告（validate 全量 errors=30/warnings=51 与基线逐项一致——ai.md 等既有未注册包错误，非本 plan 引入）。
  - **docs/index.md 导航核对**：industrial HMI 路由行补全实际文档路径（design-\*.md 四份 + analysis/ 目录 + 讨论文件，去除「I2 后生成」过时占位）。
  - **manifest 复核**：`examples.manifest.json` runtime 列表含 `scada-canvas`、`declaredButUnregistered` 为空——与 renderer-definitions（registerScadaRenderers 注册）及 design-renderer §5 一致，无修正。
  - **每日日志**：本条目（reverse-chronological 收口摘要）。
- **验证**：workspace 全量 typecheck 32/32、build 32/32、lint 32/32（见 Closure Gates 记录）；unit 全绿（59/59 任务；包级 462/34）；e2e 不在 mission 校验命令内，作为本 plan 交付物单独运行验证（scada-\* 23/23 全绿）。
- **closure-audit 通过（独立子 agent fresh session，task `ses_03574fdfdffegr81d1j5hX7veJ`，不复用执行上下文）**：判定 `approved`——逐项 live 复现（e2e 15/15 复跑 + i18n 28/28 + 包级 462/34 + generate-types 115 定义/再生幂等 + validate 基线一致零新增 + D-1 零残留 `events.*` + gate-3 M-1/M-2/M-3 未回退 spot check + deferred 诚实）；roadmap I15 `planned → done` 回写见 Phase Status，plan 收口（Plan Status: completed，Closure Gates 全勾）。

## industrial-hmi I14 Benchmark 与性能优化收口：plan-2026-08-04-0523-3 三 Phase 全绿 + benchmark 报告达成文档共识 + 全项达标

执行 `docs/plans/2026-08-04-0523-3-i14-benchmark-and-performance.md`（Work Item I14，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **I14.1 测量基础设施落地**：
  - **10 万级加载方式裁定**（消费 I13 `pressure-scale-drift`）：独立 perf 页/路由 `#/scada-perf-scale`（`scada-perf-scale-demo.tsx`，对齐 calendar-perf-scale 先例；不扩展 scada-pressure-demo 规模参数，I13 10k 模式零回归）+ 无 stroke 对照变体内置（生成器 `stroke` 开关）+ 默认小占位画面 + `window.__scadaPerfScale` 页面级 dev/test 投影；路由注册（route-model/App/pages index）。
  - **`perf-injection-channel` 裁定**：dev/test 专用批量方法挂测试句柄 `window.__flux_scada_<cid>.setPointValues(values)`（use-scada-engine 在 exposeTestHandle 时挂载，实现 = pointStore.setPointValues + pipeline.requestRender，与 scope-bridge 同写入路径、剔除 1 万 flux 变量订阅/求值开销）；`component:setPointValues` 不存在核实（SCADA_HANDLE_METHODS 面）；非公共契约变更，不触发人工确认阈值。
  - **测量发现并修复（拖动平移）**：viewport 插件默认不开启指针拖动平移（`move` 配置缺省仅 autoDistance）→ tree 配置补 `move: { drag: 'auto', dragEmpty: true }`（对齐 spike 拖动口径，验收包络含拖动）；另记录测量发现：插件驱动 zoomLayer 平移（指针/wheel）在 3 层 App 下不发射 tree render 事件（renderer totalTimes 递增但 render 事件不发射；画布像素 hash 实测随平移更新，视觉平移正常）——双口径落地方式：指针路径以 rAF 显示帧率计量、渲染吞吐以命令路径 render 事件计数（A4 声明内）。
  - **测量脚本** `tests/e2e/scada-perf.spec.ts`（180s 超时档位）5 项：10 万首屏创建（组态生成完成→tree render 首帧，engine.reset 驱动）、拖动 fps 双口径（指针 rAF + 命令路径吞吐，各 3 采样取 best）、内存（CDP JS heap 含无 stroke 对照组）、1 万点批量刷新端到端延迟 + 合帧断言（renderDelta=1）、m-8 batch.add 对照探针（`measureAddStrategies` 测试句柄投影）。
  - **gate-3-review §10 兑现**：1 万点全量批量合并断言（合并帧/脏属性收集路径，renderDelta=1）+ batch.add 对照（50k：逐节点 3.1ms vs 批量 551.2ms——**批量 add 反而慢 ~178x**，detached-group 构建场景下 batch.add 触发一次性整组布局；观察项口径固化，结论：不采纳 batch.add，10 万构建路径已最优形态）。
  - **baseline 实测**（Apple M4 Max / macOS 26.5.2 / Playwright Chromium 151 headless）：首屏创建 342.9–344.4ms、指针拖动 rAF best 69.9fps、吞吐 best 49.9fps、内存 stroke 128.8MB / 无 stroke 126.1MB（delta 2.7MB）、1 万点刷新 80.1–86.2ms + renderDelta=1；全项余量充分（对比 spike：165.3ms/47.5MB/16.9–19.7ms）。
- **I14.2 优化轮**：基线分解定位真实差距——直接 applyAttrs+render 18.8ms（≈spike 渲染基线）vs 全流水线 72.8ms，~54ms 为绑定层声明解析热路径（`collectStates` 每帧 10k 次 `getSymbolDeclarations` → `getConfigNode` 线性扫描 + `deepMergeInstanceProps` 深合并）；**执行优化**：ConfigAdapter `nodeById` O(1) 索引（build/applyDiff 增量维护）+ `getSymbolDeclarations` 无声明快路径（跳过深合并）；focused 单测 +6（快路径 5 + nodeById 跨 diff 语义 1），既有 453 全绿不回归；其余预设优化面（图元实例化/裁剪/脏区/数据节流/动画合帧）裁定不优化（达标且余量充分，m-8 反向证据）；**复测：1 万点刷新 86.2→42.1ms（-51%，余量 4.75x）**，其余项负载内波动。
- **I14.3 复测与结论固化**：全项重跑全绿——首屏 373.1ms <2s、指针 rAF best 70.3fps ≥45、吞吐 best 49.4fps ≥45、内存 130.6/127.8MB ≤320MB、刷新 41.9ms <200ms + renderDelta=1；**达标裁定：全项达标，不触发人工确认阈值**；结论固化入 `docs/analysis/industrial-hmi/benchmark-report.md`（测量方法/环境/基线/优化前后对比/最终复测/裁定记录/A4 双口径声明/m-8 对照）。
- **benchmark-report.md 文档共识审查 2 轮达成 AGREE**（≤3 轮上限内）：R1（task `ses_035ed10f8ffeSmBCbSeuXNaDNr`）`pass-with-minors`（2 Minor + 1 Nit 全部落地：headless 帧钟波动扩至 ~29.8fps 失败档、插件平移证据链补全、环境变量补全）；R2 确认轮 0 新增修正项——**共识达成**。
- **验证**：workspace 全量 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明，对齐既有口径）；包级 459 tests / 34 files 全绿；**perf e2e 5/5 全绿**（`scada-perf.spec.ts`）+ I13 回归 e2e 7/7 全绿（scada-demo 4/4 + scada-pressure-demo 3/3，拖动平移配置变更零回归）。
- **closure-audit 通过（独立子 agent fresh session，task `ses_035d296bbffeXECI1GcN47ZZ9H`，不复用执行上下文）**：判定 `approved`（2 Minor 均为提交前卫生项——删除 `zz-breakdown.spec.ts` 诊断探针、`prettier --write scada-engine.ts`——已全部落地）；逐项实测核验三 Phase Exit Criteria 全部 landing、Closure Gates 全通过（独立复跑 perf spec 5/5：buildMs=365.2、pointer best 75.7fps、吞吐 best 57.5fps、heap 130.6/127.8MB、latency 41.4ms renderDelta=1；包级 459/34 全绿；workspace typecheck/build/lint 32/32 + `pnpm test --force` 59/59 无缓存；测试 diff 仅新增无弱化）、文本一致性五处核对通过、接口↔语义抽查通过（声明快路径保留合并语义、nodeById 跨 diff 维护）、deferred 分类诚实（无静默降级）。**roadmap I14 `planned → done` 回写见 Phase Status，plan 收口（Plan Status: completed）**。

## industrial-hmi I13 Playground 演示页收口：plan-2026-08-04-0523-2 两 Phase 全绿 + smoke e2e 全绿 + 真实浏览器缺陷修复

执行 `docs/plans/2026-08-04-0523-2-i13-playground-demo-pages.md`（Work Item I13，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **I13.1 `scada-demo` 工艺流程演示页**（`apps/playground/src/pages/scada-demo.tsx`）：内嵌组态（motor/pump/valve/fan + 管道/pipe-junction + gauge/level/thermometer + indicator/button + 文本标签，~24 图元）+ **点表刷新双轨演示**（裁定：flux 轨 `tankLevel/flow/temp/flowOn` 声明 `source: 'flux'`，页面 `setInterval` 生成模拟数据 → SchemaRenderer `data` prop → page scope → `useScadaPointsBridge`（useScopeSelector）→ point store；static 轨 `motorState/pumpState/valveOpen/fanState/alarm` 由 schema 按钮经 `component:setPointValue` 句柄派发）+ **点击设备弹出详情**（组态内 `events` 声明 → openDialog inline `title`/`body` args）+ fit/center 按钮 + loading/empty region；I11 三链路并入（click→dialog、dblclick→navigate `#/flux-basic`、click→ajax `/api/scada-demo/points`）。
- **I13.2 `scada-pressure-demo` 大屏/复杂组态示例页**（`apps/playground/src/pages/scada-pressure-demo.tsx`）：**单页多画面 tab 切换裁定**（不新增页面级多路由，注册面最小化；切换经 SchemaRenderer `key={screen}` 重挂载——`config.version` 被 validate 严格校验为 1，不能借版本号触发 reload）+ mulberry32(42) 固定种子程序化生成 10,000 图元（矩形/线/管道/文本各 2500，2000×1300 世界）+ 大屏布局（全屏 canvas + 顶部信息栏）+ fit contain 初始视口；10 万级测量场景加载方式裁定交 I14.1（`pressure-scale-drift`，本页只做万级载体）。
- **路由/导航注册**：两页面均注册 `route-model.ts` `DOMAIN_RENDERER_ROUTES`（`#/scada-demo`、`#/scada-pressure-demo`）+ `App.tsx` domain switch + `pages/index.ts` export + `home-page.tsx` NAV_CARDS 与 `NavigationTarget`。
- **I11 验证页收敛（并入裁定）**：三链路并入 scada-demo，`scada-event-linkage-demo.tsx` 退役删除（export/路由/卡片/联合类型全清理，`apps/` 与 `tests/` 零残留引用）。
- **执行中发现并修复 2 个真实浏览器缺陷**（gate-3 类 mock↔真实漂移，均带 regression 测试）：
  - **`app.sky`/`app.ground` 缺失**：leafer App 仅当 config 含对应 key 才创建层（web.module.js `App.init`），引擎 appConfig 只声明 `tree` → `InteractionOverlay` 构造崩溃（hover 链路即挂）。修复：appConfig 补 `ground: {}`/`sky: {}`（design-engine.md §6 三层模型契约）+ `leafer-ui-mock.ts` 按真实行为建模（ground/sky 按 config 条件创建）+ A1 测试强化（断言 app.ground/app.sky defined，防 undefined===undefined 空过）。
  - **`TreeRegistry.findByNode` 只索引符号根节点**：真实 `selector.getByPoint` 返回最深命中子节点（设备 body/转子）→ 命中反查失败、symbol:click 永不发射。修复：findByNode 沿 parent 链上溯到已登记根 + deepest-hit 回归测试。
  - 另记录：I11 验证页 `openDialog args: { dialogId }` 模式在真实 flux 打开的是空 surface（`dialogId` 是 targeting 字段非 args 字段）——并入时改用 inline `title`/`body` args（fluxBasicPageSchema 既有工作模式）。
- **验证**：包级 453 tests / 34 files 全绿（新增 2 个 regression 测试）；workspace 全量 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明，对齐既有口径）；**两个 smoke e2e 全绿**：`tests/e2e/scada-demo.spec.ts` 4/4 + `tests/e2e/scada-pressure-demo.spec.ts` 3/3（程序化断言：测试句柄 `window.__flux_scada_<cid>` 读场景树/点表/视口、点表定时刷新值变化、component:setPointValue 写穿点表、点击设备弹 dialog、10k 计数 + 引擎实例替换 + `gen-5000` 确定性 + fit scale<1、无 console error；无截图断言、未引 node-canvas）。e2e 运行注记：playground 默认端口 4175 被 master worktree 的 dev server 占用，本次以 `PLAYWRIGHT_PORT=4180` 运行。
- **closure-audit 通过（独立子 agent fresh session，task `ses_03622fdc9ffeKhah0YcFqIERsV`）**：逐项实测核验零 Blocker 缺陷（audit 提出的 B-1 日志/M-1 勾选为流程项，本条目即 B-1 落地）；deferred 分类诚实（10 万级 → I14.1、完整断言矩阵 → I15.1，均 watch-only residual）；roadmap I13 `planned → done` 回写见 Phase Status，plan 收口。

## industrial-hmi I12 verify 重跑 + I13/I14 plan 落盘：workspace 全绿

- **BUILD_VERIFY 重跑**（I12 收口后工作区全量校验）：workspace `pnpm typecheck` 32/32、`pnpm build` 32/32、`pnpm lint` 32/32、`pnpm test` 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明，对齐既有口径）。
- **I13/I14 plan 文件落盘**（后续 work item 起草，未执行）：`docs/plans/2026-08-04-0523-2-i13-playground-demo-pages.md`（I13.1 scada-demo 工艺流程演示页 + I13.2 万级压力示例页，roadmap `todo`）+ `docs/plans/2026-08-04-0523-3-i14-benchmark-and-performance.md`（I14.1 固化测量方法/基线 → I14.2 优化 → I14.3 复测收口）。

## industrial-hmi I12 整体 gate 执行：plan-2026-08-04-0523-1 两 Phase 全绿 + gate-4-review 达成共识 + 修正落地

执行 `docs/plans/2026-08-04-0523-1-i12-overall-gate-review.md`（Work Item I12，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **Phase 1 前置验证通过**：roadmap I8/I9/I10/I11 = `done`；4 个上游 plan（2026-08-03-2307-3-i8 / 2026-08-04-0225-1-i9 / -2-i10 / -3-i11）均 `completed` 且各自 closure-audit 通过（I8 task `ses_03727e6a5ffeciXAp6iLWu00DT` / I9 PID 98616 / I10 task `ses_036adbdabffen6Ut0q4aHSa9ca` / I11 task `ses_03688be36ffe8jRpc6pEdUbqun`）；`renderer-boundary-audit.md` 落盘（INV-1–INV-5 全通过 + Checklist A–F + D-1 契约裁定）；无待人工裁决项。
- **roadmap 状态回写**：I12 `todo → planned`（Phase 1 Decision，plan 激活期同步执行）。
- **I12.1 独立 review（fresh session，task `ses_0366f8601ffeTHct9ttCroe5g5`，不复用 I8–I11 执行上下文）**：对照讨论文件 §八（8 条最终决策）+ 4 份 design-\*.md 终态 + I0.5 调研结论 + I10 五边界审计结论 + gate-3-review（§10/§11）审查 I8–I11 实现（功能完整性/性能/测试/文档四面），产出 `docs/analysis/industrial-hmi/gate-4-review.md`（246 行，11 节）：
  - **判定 `pass-with-minors`**：0 Blocker / 0 Major / 3 Minor（m-A 视口钳制锚点文档措辞「视口中心」vs 实现原点锚；m-B 初始视口策略每次 config 同步重应用含绑定域重建二次执行；m-C 交互覆盖物几何对无宽高语义图元 line/polygon 退化 0 尺寸）。
  - **功能完整性**：23 项核对表全绿（I8 样式细化/image 视频占位/视觉状态/复合图元、I9 24 内置符号/scada-symbol 不注册裁定回写、I10 生命周期/config 同步/完整注册/component:\* 句柄/flux 桥接、I11 事件全链路/双击合并/onReady/onError/wheel-pinch 钳制兜底/hover 接线/fit-center）；D-1 契约裁定（events 整体 prop）核对；gate-3 §11 收口动作 14 项未回退核对（M-1 event.x/y、M-2 IPickResult 解包、M-3 -(Δx)\*scale 符号全部保持）。
  - **性能路径**：合并帧 + 脏属性收集 + 动画合帧 + 禁逐点直刷四条红线全链成立（1000 点单帧 1 次 applyAttrs 断言）；batch.add 对照/1 万点断言归 I14（无静默降级）。
  - **测试覆盖**：450 passed / 34 files 复现 + coverage stmts 96.67/branches 90.53；mock↔真实漂移面核查——I7.2 三处修塑（事件形状/getByPoint 形状/zoomLayer 矩阵）全演化期保持无回退、无新漂移（I11 tap 双击合并建模与真实 leafer Interaction.ts 语义一致）。
  - **文档一致性**：m-1…m-7 修正终态与实现吻合；§12.2/§12.3 裁定回写齐全；roadmap Rule 4 gate 记录轨迹连续。
  - **gate-3-review §10 归属核对（8 项）**：前 6 项全部兑现确认（五边界审计/onReady/onError 桥接/component:\* 句柄/wheel-pinch 钳制/hover 覆盖物/flow 消费）、后 2 项（1 万点批量合并断言 + batch.add 对照 → I14、e2e 补强 → I15.1）归属记录核对无误。
  - **I12 注意项清单产出**（§10，14 行，供 I13/I14/I15 复用：I14×6/I13.1×3/I15.1×2/I15.2×2/I16×1 + 复合归属 1 行）。
  - **人工确认阈值未触发**（无范围/顺序/选型变化）。
- **gate-4-review.md 自身文档共识审查 2 轮达成 AGREE**（未超轮次上限）：R1（task `ses_03667e126ffeRLYRYlA1Xd7PxL`）REVISE——5 Nit（F1 插件交互测试计数 4→5、F2 daily log 引注 :10→:8、F3 design-symbols :145→:144、F4 design-data-binding :167→:166、F5 waitFor 措辞）；F1/F5 落地，**F2/F3/F4 经 live rg 复核证实为审查者引注错误（反向落地，对齐 gate-3 R1 F3 先例）**——原值即 live 正确行号（daily log :10 = §10 归属表 bullet、design-symbols :145 = fillStyle Decision、design-data-binding :167 = 调度器 bullet）；R2（task `ses_0365de9afffeEij1ODWQe9refo`）AGREE——0 新增修正项，F1–F5 disposition 逐项验证正确（F1 三处 5 计数、F2/F3/F4 原值精确、F5 双 waitFor 与源文件吻合）。
- **I12.2 修正落地**（每项携带 focused 回归测试，Bug Fix Test Coverage Rule）：
  - **m-A**：design-engine.md §4.4:123「以视口中心为锚」→「以视口原点世界点为锚」（与命令路径 applyViewportState/M-3 推导口径一致，选项①）；既有钳制单测保持绿。
  - **m-B**：use-scada-config-sync.ts `applyInitialViewport` 收窄至 `strategy === 'full'` 分支（选项①：diff 增量不重应用、绑定域重建后 effect 重跑为空 diff 不重复执行）+ 回归测试「does not re-apply the initial viewport policy on diff updates, only on full resets (gate-4 m-B)」（同版本 diff 后 getViewport 保持帧定值、版本变更 full 后重新帧定）。
  - **m-C**：interaction-overlay.ts 覆盖物几何按 points 包围盒兜底（选项②：line/arrow/polygon 无 width/height 语义、attrs 含 points，水平/垂直直线 0 尺寸退化最小框 MIN_OVERLAY_SIZE=8）+ design-engine.md §6 交互覆盖层行补说明 + 回归测试「shows a non-zero-size overlay for line/polygon symbols via points-bounds fallback (gate-4 m-C)」（scada-line/scada-polygon hover 覆盖物尺寸 > 0 且位置 >= 图元坐标）。
- **roadmap 回写**（Rule 4）：I12 gate 记录条目写入本文件头部「文档共识审查记录」块（对齐 I1.1/I3.1/I7 先例）；Phase Status I12 `planned → done` 由独立 closure-audit 核验后回写。
- **验证**：包级 452 tests / 34 files 全绿（新增 2 个 focused 回归测试，既有 450 未弱化），coverage 阈值 90 达标；workspace 全量验证 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明）。
- **closure-audit**：由独立子 agent（fresh session）执行，核验通过后 roadmap I12 回写 `done`、plan 收口（证据见 plan Closure Audit Evidence）。

执行 `docs/plans/2026-08-04-0225-3-i11-event-linkage-and-canvas-interaction.md`（Work Item I11，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **I11.1 图元事件→flux action 全链路**：`use-scada-events.ts` 扩展（`collectSymbolEvents` 声明索引纯函数（递归 group 子树，symbolId → ScadaSymbolEvent[]）+ 声明优先/props.events 兜底优先级链 + 非法声明 → config-invalid → onError）；`validate.ts` 补 `events` 声明形状校验（on 枚举 click/dblclick/hover + ActionSchema 对象形状）；**双击合并语义落地**：`leafer-ui-mock.ts` 按真实 leafer 交互层建模（Interaction.ts tap():321-356——path 含 `double_tap` 监听时首击延迟 tapTime(120ms) 发射（config.ts:14），tapTime+50 窗口内第二击 `tapWaitCancel` 取消首击改发 double_tap；EventBridge 恒挂 double_tap 监听故抑制生效）——双击只派发 dblclick 不派发 click，mock 面 `emit('tap')` 经合并拦截 + 既有同步断言测试升级为 waitFor；新增 `renderer/scada-event-actions.test.tsx`（声明读取/优先级链/载荷形状/双击合并/非法声明→onError）+ `scada-events-hook.test.tsx`（覆盖物守卫路径）。
- **playground 事件联动验证页**：`apps/playground/src/pages/scada-event-linkage-demo.tsx`（标注「I11 验证用，I13.1 正式 demo 取代」）——scada-canvas 最小 2 图元组态（rect-dialog：click→openDialog、dblclick→navigate `#/flux-basic`；rect-ajax：click→ajax `/api/i11/points`）+ dialog 承载 + env.fetcher/navigate/notify；注册临时路由 `#/scada-event-linkage`（DOMAIN_RENDERER_ROUTES + App domain switch + pages/index + home NAV_CARDS）。
- **触发器体系评估裁定**（I6 deferred 触发点）：以 `point:change`/`state:change` 事件源 + 既有派发链评估条件-动作扩展（meta2d dataEvents 蓝本）——**裁定不扩展**（依据：视觉条件面已由 I6.2/I6.3 value-to-state + 状态动画交付；flux 桥接点源场景可复用平台 `reaction` renderer（INV-3）；剩余 gap 无 roadmap 承诺消费场景；报警状态机属 §9.3 边界），回写 `design-data-binding.md` §12.3 + 最小扩展路径（`triggers` 声明 + RefreshPipeline 条件求值 → 复用 I11.1 派发链）记录。
- **I11.2 画布浏览交互**：
  - **wheel/pinch 核对与钳制兜底**：leafer-in viewport 源码核对——`addViewportConfig` 仅 preventDefault；缩放经 `ZoomEvent.BEFORE_ZOOM` → `zoomLayer.scaleOfWorld(e, getValidScale(e.scale))`（**getValidScale 原样返回无钳制**，display Leafer.ts:405）、平移经 `MoveEvent.BEFORE_MOVE` → `zoomLayer.move`——**插件路径绕过引擎 clampViewport**（Failure Paths `viewport-interaction-drift` 兑现，非静默接受）→ 引擎订阅 tree `zoom`/`move` 事件兜底：越界缩放以视口中心为锚重钳制回 [0.1, 20] + zoomLayer 矩阵状态（x/y/scaleX）同步回引擎视口状态（zoomLayer.x = -viewport.x\*scale，M-3 推导），保证插件交互后 getViewport/命中 world 坐标/后续命令不漂移；`scada-engine.test.ts` 新增 4 个插件交互同步测试（含越界钳制 MAX/MIN）。
  - **hover 命中反馈接线**（I8.2/m-7 兑现）：EventBridge 增发 `symbol:hover-miss` 信号（pointer.move 命中为空且前一命中存在；A→B 切换不发 miss，消费方自清前一目标）；`use-scada-events.ts` driveHover/clearHover（经引擎惰性 getter 获取 InteractionOverlay，renderer 传 `interactionLayer: true` 驱动 highlight/clear，不自行创建；lastHover 追踪 + A→B 切换清前一覆盖物 + hover-miss 清当前）；`engine.applyDiff` 覆盖物增删同步（removed 清覆盖物、updated 重定位，InteractionOverlay 补 `hasActive`）；`renderer/scada-hover-overlay.test.tsx`（出现/跟随/切换/消失/applyDiff 清理/interactionLayer 开关）+ event-bridge hover-miss 3 测试。
  - **fit/center 控制验证**：`scada-handles.test.tsx` fit/center 断言升级（引擎 getViewport 视图状态变化断言，fit 返回视口 == getViewport）。
  - **文档回写**：design-engine.md §4.1 interactionLayer 接线说明 + §4.4 视口交互核对结论（插件默认配置/钳制绕过/引擎兜底机制）+ §8.1 事件表补 `symbol:hover-miss` 行；design-data-binding.md §12.3 触发器评估裁定。
- **验证**：包级 450 tests / 34 files 全绿 + coverage 阈值 90 达标（branches 90.46）；workspace 全量 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明）。
- **closure-audit 通过（独立子 agent fresh session，task `ses_03688be36ffe8jRpc6pEdUbqun`）**：13/13 Closure Gates 与 2 Phase Exit Criteria 实测核验零 Blocker/Major（声明优先派发/双击合并/onError/hover 覆盖物出现-跟随-切换-消失/applyDiff 清理/zoom-move 同步与钳制/fit-center 视图状态断言逐项落地、既有测试无弱化、deferred 分类诚实、roadmap 状态机未跳序）；2 Minor（日志措辞过时 + applyDiff-updated 重定位缺 focused 断言）落地后 roadmap I11 `planned → done` 回写，plan 收口。

## industrial-hmi I10 React 渲染器与 flux 集成收口：plan-2026-08-04-0225-2 三 Phase 全绿 + 五边界审计落盘

执行 `docs/plans/2026-08-04-0225-2-i10-renderer-and-flux-integration.md`（Work Item I10，roadmap `todo → planned`，收口 `planned → done` 由独立 closure-audit 核验后回写）：

- **I10.1 renderer 组件与实例生命周期**（`src/renderer/`）：`scada-canvas.tsx` 主渲染器（RendererComponentProps 装配 + `nop-scada-canvas` marker + data-slot scada-canvas/canvas/loading/error，render path 无副作用）；`hooks/use-scada-engine.ts`（引擎 + 点表/反向索引/刷新流水线同生命周期创建，mount 幂等守卫防 React Compiler 重复执行，unmount destroy 幂等，ResizeObserver → setSize 防抖到帧，binding 重载重建流水线）；`hooks/use-scada-config-sync.ts`（首次/版本变更 → engine.reset 全量，同版本 → diffScadaConfig → applyDiff 增量，decideSyncStrategy 纯函数固化 diff-sync-mismatch 判定）；占位组件 `scada-canvas-placeholder.tsx` 退役删除，smoke 测试升级为真实组件断言（场景构建 + 测试句柄）。
- **I10.2 完整注册 + 组件句柄**：renderer-definitions fields 完整（config/width/height/viewport/events prop + loading/empty region）；`hooks/use-scada-handles.ts` 注册 component:\* 句柄（fit/center/getSymbols/getSymbol/setPointValue/getPointTable/exportConfig/importConfig/destroy，对齐 design-renderer.md §8.5 逐项，含 not-mounted/symbol-not-found/point-not-found/invalid-config 失败路径）；**五边界审计落盘** `docs/analysis/industrial-hmi/renderer-boundary-audit.md`（INV-1–INV-5 全通过 + Checklist A–F，作 I12 gate 输入）。
- **I10.3 点表↔flux 桥接 + 事件派发**：`hooks/use-scada-points-bridge.ts`（extractFluxScopePaths 纯函数 paths 提取 → useScopeSelector 精细化失效 → flux-formula 编译求值 → setPointValues → 刷新流水线合帧，不逐点 setState 直刷 React；私有求值子 scope 注入点表上下文，INV-4）；`hooks/use-scada-events.ts`（onSymbolEvent → createNormalizedActionEvent 单参数签名 → helpers.dispatch 派发；onReady/onError 桥接，gate-3-review §10 归属兑现；组态内图元事件声明派发留 I11.1）。
- **契约裁定（记录于审计 D-1，供 I15.2 同步）**：flux-compiler `classifyField` 仅顶层 key 精确匹配、无点号路径支持（probe 实测 `events.onClick` 规则不产生 eventPlans）→ schema 级事件 `events` 对象注册为 prop，ActionSchema 经 `helpers.dispatch`（同一 action dispatcher，raw 按需编译）派发；design-renderer.md §4.1 schema 类型不变。
- **引擎小扩展**：`engine.getSize()` 只读访问器（viewport fit:'fill' cover-fit 计算 + center 视口命令）。
- **验证**：包级 421 tests / 31 files 全绿（新增生命周期/句柄/事件/桥接组件测试 + 纯函数单测），coverage 阈值 90 达标（branches 90 恰好达成）；workspace 全量 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明）。roadmap I10 `planned → done` 由独立 closure-audit 核验后回写。
- **closure-audit 通过（独立子 agent fresh session，task `ses_036adbdabffen6Ut0q4aHSa9ca`）**：13/13 Closure Gates 与 3 Phase Exit Criteria 实测核验零 Blocker/Major（生命周期/句柄/事件/桥接测试断言逐项落地、D-1 契约裁定核对属实、deferred 分类诚实、roadmap 状态机未跳序），roadmap I10 已回写 `done`，plan 收口。
- **verify 修正（BUILD_VERIFY 强制重跑包级测试发现 1 例失败）**：`scada-handles.test.tsx` destroy 后调用句柄命令期望 `not mounted`，实测返回 `no config`——根因：`use-scada-handles.invoke` 的 not-mounted 判定读 `latest.current.runtime`（React state 未冲刷，destroy 同步执行后仍持旧 runtime），且引擎 `destroyed` 为私有字段无公开访问器。修正：`engine.isDestroyed()` 公开只读访问器（scada-engine.ts）+ invoke 前置 `method === 'destroy'` 分支（destroy 幂等契约：二次 destroy 仍返回 ok，双 destroy 测试不动）+ 其余命令经 `isDestroyed()` 判定返回 not-mounted；引擎 destroy 幂等测试补 `isDestroyed() === true` 回归断言。修正后包级 421 tests / 31 files 复测全绿、workspace typecheck/build/lint/test 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明）。

## industrial-hmi I9 工业设备图元库（Wave 4）收口：plan-2026-08-04-0225-1 四 Phase 全绿 + 24 内置符号成型

执行 `docs/plans/2026-08-04-0225-1-i9-device-symbol-library-wave4.md`（Work Item I9，roadmap `todo → planned → done`）：

- **I9.1 设备图元**（`src/symbols/device/`，内置 11 → 15）：motor/pump/valve/fan 复合图元装配（`symbols/composite.ts` 公共角色路由：body/rotor/core/liquid/bar/label，applyProps 增量按角色分发）；旋转动画装配（`when: {state:'run'}` rotate，消费 I6.3 animator + I6.2 状态联动）；默认状态声明 run=绿/stop=灰/fault=红+闪烁（design-data-binding.md §10 惯例色）；valve 阀芯开度 `custom.openRatio`（0=关 90°/1=开 0°）。
- **I9.2 仪表图元**（`src/symbols/instrument/`，15 → 19）：gauge/level/thermometer/progress；量程换算链（bindings.rotation/height/width 的 scale → applyScale → 指针角/液位/进度条）+ formatValue 文本消费；指针/液柱/进度直接属性绑定驱动（`leafer-animation-drift` 兜底路径未触发，applyProps 直写属性）。
- **I9.3 传感控制图元**（`src/symbols/sensor-control/`，19 → 23）：sensor/indicator/switch/button；状态判定（value-to-state）→ 状态样式（StateVisualApplier）→ fault 闪烁（animator blink 联动）链路；switch 拨杆位置 `custom.on` 静态区分（事件联动归 I11.1）。
- **I9.4 管道图元**（`src/symbols/pipe/`，23 → 24）：`scada-pipe-junction` 连接点语义（custom.connections 端点声明：归一化坐标/流向/目标设备）+ flow/dashOffset 经 applyProps 增量消费（gate-3-review §10 归属兑现，区别于基础 scada-pipe 直写路径）+ I6.3 flow kind 动画驱动 dash 位移 + 连接关系声明结构序列化（编辑器连线后置 I16）。
- **`scada-symbol` 图元级 type 注册契约评估落地**（I8 deferred 触发点，讨论 §九口径）：以 24 图元成型事实裁定 **不注册**（无画布外单图元真实消费场景 + renderer/组态双 schema 漂移成本，依据回写 design-symbols.md §12.2/§12.3/§1/§2/§3）。
- **扩展契约**：`ScadaSymbolNode` 补 `flow` 字段（I9.4 组态往返）+ validate.ts flow 形状校验（enabled/speed/dash）+ `engine.getSymbolDeclarations`（states/animations 经 defaults ∪ 实例深合并，符号自带默认声明对 I6.3 联动层可见）+ leafer-ui-mock 按需扩展。
- **验证**：包级 379 tests / 28 files 全绿（Phase 1 前 326/24 基线保持不弱化 + 53 个新测试），coverage 阈值 90 达标；workspace 全量 typecheck 32/32、build、lint、test 全绿（full-green verification：unit 全绿；e2e 不在 mission 校验命令内，不声明）。roadmap I9 `planned → done` 由独立 closure-audit 核验后回写。
- **closure-audit 通过（独立子 agent fresh session，opencode PID 98616）**：全部 12 项 Closure Gates 与 4 Phase Exit Criteria 实测核验零 Blocker（24 内置符号注册/13 图元族 applyProps 与状态动画装配/scada-symbol 裁定回写/roadmap 状态机未跳序），roadmap I9 已回写 `done`，plan 收口。

---

## industrial-hmi I7 实现对照 gate 执行收口：plan-2026-08-03-2307-2 两 Phase 全绿 + gate-3-review 达成共识

执行 `docs/plans/2026-08-03-2307-2-i7-implementation-gate-review.md`（Work Item I7，roadmap `planned → done`）：

- **I7.1 实现对照 review**：前置验证通过（roadmap I6 = `done`、I6 plan closure-audit 通过、I5/I6 实现与 focused 测试落盘）。独立 agent（fresh session，task `ses_037774c97ffekeQTBGto6crjFa`，不复用 I5/I6 执行上下文）对照 4 份 design-\*.md + I0.5 调研结论 + spike 工程（`~/sources/industrial-hmi-research/spike-leafer/src/demo.js`，leafer 真实 API 锚点）审查 I5/I6 实现，产出 `docs/analysis/industrial-hmi/gate-3-review.md`（322 行，11 节）：
  - **判定 `revise`**：0 Blocker / **3 Major** / 8 Minor。3 项 Major 均为「单测全绿但真实 leafer 上不可用」live defect，共同根因是 `src/test-support/leafer-ui-mock.ts` 错误建模掩蔽（I6 plan Failure Paths `leafer-event-api-drift` 触发）：`M-1` event-bridge.ts 读 `event.point`（真实 leafer 事件数据为 `x`/`y`，PointerEventHelper.convert/UIEvent 源码证实）→ symbol:click/dblclick/hover 永不发射；`M-2` hit.ts 未解包 `IPickResult.target`（getByPoint 恒返回 `{ target, path }`，spike demo.js:177-179 证实）→ 命中反查必失败；`M-3` `applyViewportState` 平移符号反（zoomLayer.x = -viewport.x*scale 推导，应传 `-(Δx)*scale`）→ setViewport/zoomAt/fit/center 反向平移且偏移翻倍。
  - **leafer 真实 API 抽查 10 项**：3 项 live-defect 漂移 + 1 项无害漂移（changedThreshold 非 leafer 配置键，Watcher 硬编码 changed<100）+ 1 项性能观察（逐节点 add vs batch.add）；A1 tree viewport 固化、lazySpeard 拼写、事件名（tap/double_tap/pointer.move）、forceRender、resize 均一致。
  - **I5 遗留事项复核**：design-renderer.md:155 `ScadaConfigDiff.added` 确认为设计笔误（实现为节点数组且语义正确——applyDiff 需完整节点实例化）；leafer 抽查结论记录作 I14 复测口径基线。
  - 差异清单 10 项裁定 + I8+ 域问题归属记录（I10.2 五边界审计/onReady/component:\* 句柄/wheel 交互/sky 覆盖物/flow 消费/1 万点断言/e2e 补强）；**无人工确认触发**。
  - **gate-3-review.md 自身文档共识审查 3 轮达成 AGREE**（未超轮次上限）：R1（task `ses_0376801a9ffebSC6hADmT6VtEd`）REVISE——2 Minor + 1 Nit（F1 m-2 引注 :79→§4.1:73+§7:141、F2 核对项 22 引注、F3 行号偏移）；R2（task `ses_0375f1128ffeSIortl6TsAo20z`）REVISE——发现 F3 反向落地（原引注即 live 正确行号），N1 恢复 12 处引注、N2 point-store 内部行号精确化、N3 event-bridge.test.ts 引注精确化，全部落地；R3（task `ses_03754ab18ffeb3fL4MvQ0Z4spZ`）AGREE——0 新增修正项。
- **I7.2 修正落地**（每项携带 focused 回归测试，Bug Fix Test Coverage Rule）：
  - **M-1**：event-bridge.ts `pointOf` 改读 `event.x`/`event.y`；mock 面事件形状改真实形状 `{ x, y }`；event-bridge.test.ts/scada-engine.test.ts 发射形状同步 + 真实形状回归断言。
  - **M-2**：hit.ts `resolveSymbolId` 解包 `IPickResult.target`（无 target 键时视原始返回为节点，null → 未命中）；mock 面 `getByPoint` 返回 `{ target, path }` 形状；新增解包成功/未命中两态回归测试。
  - **M-3**：scada-engine.ts `applyViewportState` move 参数取 `-(Δx)*scale`；回归测试断言 `setViewport({x:50,y:30,scale:2})` 触发 `move({x:-100,y:-60})`、zoomAt 后 `move({x:-100,y:-140})`。
  - **m-4**：validate.ts 补齐 bindings（至少一个字段 + point/expression/map/scale/format 类型）、animations（kind 枚举/period/loop/from/to/when 形状）、states（states 映射/ranges/booleanMap/valueMap 形状）内部结构校验 + 3 组回归用例（bindings 6 + animations 4 + states 8 = 18 个非法形态断言）。
  - **文档回写**：m-1 design-engine.md §4.1 onRender dirtyBlocks 预留字段说明；m-2 design-engine.md §4.1/§7 changedThreshold 改为自研占位键表述；m-3 design-renderer.md:155 `added: ScadaSymbolNode[]` 修正；m-5 design-symbols.md §4.2 补 `dashOffset`；m-6 design-engine.md §8.2 getSymbolProps leafer 属性面说明；m-7 design-engine.md §4.1 interactionLayer 惰性保留 + scada-engine.ts 注释；m-8 归属 I14 复测观察项（gate-3-review §10 记录）。
- **roadmap 回写**（Rule 4）：I7 gate 记录条目写入本文件头部「文档共识审查记录」块（判定/修正项摘要/审查 task id，对齐 I1.1/I3.1 先例）；Phase Status I7 `planned → done` 由独立 closure-audit 核验后回写。
- **验证**：包级 273 tests / 20 files 全绿（新增 6 个回归测试：hit 2（解包两态）+ serialization 3 组（m-4 共 18 个非法形态断言 + 3 个合法形态）+ 引擎视口符号断言 1（同名测试增强替换，断言 move 参数符号），原 268 保持不弱化），coverage 阈值 90 达标；workspace 全量验证 typecheck 32/32、build 32/32、lint 32/32、test 59/59 全绿。e2e 未纳入 mission 校验命令，不声明 full-green。
