# Round 02 — Main-agent live re-verification

> Per-round record. Main agent re-read the live code for every `certain`-rated
> candidate from round-01 and for the highest-value `likely` ones. This file
> records the verification verdicts; promoted findings land in the authoritative
> deliverable `docs/audits/2026-06-26-1859-open-audit-amis-bug-driven-improvements.md`.

## Verification verdicts

| Candidate                                                 | Verdict                                          | Note                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| NEW-T1 column-resize scope persists stale width           | **CONFIRMED, upgraded severity**                 | Worse than reported: under `scope`, `onPointerMove` is gated to `local` only (`use-column-resize.ts:197`) so the column gets **no live drag feedback at all**, and `onPointerUp:212` reads `localWidths[active.key]` which was never updated under scope → persists the **pre-drag** width. The persistence test passes only because it calls `persistWidth()` directly (`:169-179`), bypassing the real drag commit path. |
| NEW-T2 column-resize listener leak / no pointercancel     | **CONFIRMED**                                    | Listeners added at `:217-218`, removed only inside `onPointerUp:203-204`; returned cleanup (`:220`) discarded by caller; no `pointercancel`.                                                                                                                                                                                                                                                                               |
| NEW-T3 controlled ownership silent no-op                  | **CONFIRMED**                                    | `widths` returns `controlledWidths=initialWidths` only; no `onWidthsChange` callback; no dev warning; doc (`table/design.md:233`) claims "controlled 只读上游" but provides no upstream channel beyond schema.                                                                                                                                                                                                             |
| NEW-T4 controlled never reflects schema width updates     | **REJECTED**                                     | `initialWidths` is a `useMemo([columns, columnResize])` (`:111-121`), so under controlled `widths` DOES reflect schema `column.width` changes. The `useState(initialWidths)` staleness only affects `localWidths`, irrelevant under controlled. Dropped.                                                                                                                                                                   |
| NEW-T5 MemoizedDataRow hand-written memo + comparator gap | **CONFIRMED**                                    | `table-body-row-rendering.tsx:362` hand-written `React.memo`; comparator (`:363-393`) omits `fixedColumnLayout` which `DataRowView` reads ~11×. Lint passes (memo not an error under react-compiler), so this is convention+correctness, not a lint gap.                                                                                                                                                                   |
| NEW-T6 useTableExpand local-only                          | **CONFIRMED**                                    | grep: `use-table-expand.ts` has no Ownership/StatePath/controlled/scope symbols. Lone outlier in the table-state family.                                                                                                                                                                                                                                                                                                   |
| NEW-T7 pagination swallows runtime total                  | **CONFIRMED**                                    | `pagination-renderer.tsx:135` `const [total] = useState<number>(initialTotal)` — setter never destructured/used; comment (`:124-127`) justifies the pattern only for currentPage/pageSize (user-mutated), not total (server-driven).                                                                                                                                                                                       |
| NEW-T8 useCrudRuntimeState init effect every render       | held as likely                                   | `defaultQuery` literal churn plausible; not line-verified by main agent.                                                                                                                                                                                                                                                                                                                                                   |
| NEW-T9 drag handle role=button no keyboard                | **CONFIRMED**                                    | `use-row-drag-sort.ts:190-206` `dragHandleProps` has `role:'button'`, `tabIndex:0`, `aria-label`, but no `onKeyDown`.                                                                                                                                                                                                                                                                                                      |
| NEW-T10 drag handle click bubbles to row                  | held as likely                                   | no `onClick` stopPropagation in `dragHandleProps`; bubbling plausible.                                                                                                                                                                                                                                                                                                                                                     |
| NEW-T11 drag-sort no scope+no-statePath warning           | **CONFIRMED**                                    | warning block `:101-119` covers `!orderField` and `controlled&&!onReorder` only; `handleDrop:164` gates scope write on `statePath`.                                                                                                                                                                                                                                                                                        |
| NEW-T12 quick-edit save mid-save race                     | held as likely                                   | not line-verified.                                                                                                                                                                                                                                                                                                                                                                                                         |
| NEW-T13/T14 hardcoded English strings                     | held as certain (consistent i18n-bypass pattern) | not independently line-verified but pattern is unambiguous per sub-agent.                                                                                                                                                                                                                                                                                                                                                  |
| NEW-T15/T16/T17 chart/list edges                          | held as likely                                   | not line-verified.                                                                                                                                                                                                                                                                                                                                                                                                         |
| NEW-F1 date-range bypasses min/max                        | **CONFIRMED**                                    | `date-range-renderer.tsx:143-153` `commitRange`→`normalizeRange` only normalizes start≤end order; `setTimeOn:161-177` clamps the typed hour/min to 0-23/0-59 but never to minDate/maxDate.                                                                                                                                                                                                                                 |
| NEW-F2 treeConfig identity instability                    | **CONFIRMED**                                    | `tree-options.ts:45-53` `getTreeOptionConfig` returns a fresh literal; called every render at `tree-controls.tsx:50`; fed to `useTreeRemoteSearch` effect dep.                                                                                                                                                                                                                                                             |
| NEW-F5 BetweenInput discards survivor                     | **CONFIRMED**                                    | `value-input.tsx:471-475` `if (left !== undefined && right !== undefined) onChange([left,right]) else onChange(undefined)` — clearing one side emits `undefined`, destroying the survivor.                                                                                                                                                                                                                                 |
| NEW-F7 input-table lacks removeWhen                       | **CONFIRMED**                                    | grep: `removeWhen` present in `combo-renderer.tsx` + `composite-field/array-field.tsx` + `remove-when-gating.ts`; absent from `input-table-renderer.tsx`.                                                                                                                                                                                                                                                                  |
| NEW-F3/F4/F6/F8/F9/F10/F12                                | held as likely                                   | sub-agent-traced, credible, not line-verified by main agent.                                                                                                                                                                                                                                                                                                                                                               |
| NEW-R1 contract-honesty prod tests don't isolate          | **CONFIRMED, headline**                          | `flux-renderers-form/__tests__/contract-honesty.test.ts:63,67` collects ONE `formSource` blob for the whole package and passes it (string branch) to every definition; the capability resolver (`:79-82`) returns the same `runtimeSource` for every definition. So renderer A's `events.onChange`/`'save'` masks renderer B's missing one. G15 core API supports isolation; zero production harness uses it.              |
| NEW-R2 validateSubtree drops abort signal                 | held as likely                                   | not line-verified.                                                                                                                                                                                                                                                                                                                                                                                                         |
| NEW-R3 capability matcher over-broad array anchor         | **CONFIRMED likely**                             | `contract-honesty.ts:116-117` `[[,]\s*['"]h['"]` / `['"]h['"]\s\*(?:,                                                                                                                                                                                                                                                                                                                                                      | \])`matches ANY array element, not specifically a methods/listMethods array — contradicts the JSDoc promise at`:101-102`. |
| NEW-R4 cancellation→onError                               | held as interesting-guess                        |                                                                                                                                                                                                                                                                                                                                                                                                                            |
| NEW-L1 out-of-order success stale data                    | held as likely (P0-class)                        | success guard at `api-data-source-controller-runtime.ts:351` is conditional on `run && asyncGovernance`; error/finally paths use `requestSequence < latestSettledRequestSequence`. Reachability depends on governance-always-present invariant (blind spot).                                                                                                                                                               |
| NEW-L2 notice-bar no resize observer                      | **CONFIRMED**                                    | `notice-bar.tsx:64-80` `useLayoutEffect` deps `[scrollableConfig, textList, currentIndex]`, no container-width dep, no `ResizeObserver` on `contentRef` (sibling `swipe-cell.tsx` uses one for the symmetric problem).                                                                                                                                                                                                     |
| RESIDUAL-1 G13 tree fat-node only deferred                | **CONFIRMED**                                    | per sub-agent; `tree-renderer.tsx` first-50-then-all-after-0ms.                                                                                                                                                                                                                                                                                                                                                            |
| NEW-L3/L4/L5                                              | held (low-sev)                                   |                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Conclusion of round 02

17 findings promoted to **certain** (main-agent line-verified), ~16 held at **likely** (sub-agent-traced, credible), 1 rejected (NEW-T4), several low-sev. The headline themes: (1) the column-resize scope/controlled ownership paths are non-functional (data loss + no feedback), mirroring the just-fixed G10 family in a sibling hook; (2) contract-honesty production adoption is incomplete — the guard still doesn't guard for sibling masking (NEW-R1); (3) several silent data-loss edges in composite/date/condition-builder controls. Round is NOT clean. Deliverable assembled.
