# Development Log - 2026-08-30

### 2026-08-30（跨仓工具链：flux 页面产物编译验证 CLI `flux:validate-pages`——为 nop-entropy WebPageExporter 导出产物提供无浏览器批量校验）

- **背景**：跨仓计划 nop-app-erp `docs/plans/2026-08-30-1126-1-flux-page-export-and-validation.md`（配套架构契约 `docs/architecture/flux-page-export-and-validation.md`，均在该仓）。Java 侧（nop-entropy `WebPageExporter`）把应用全部页面导出为 flux 页面 JSON 后，需要 JS 侧用真实 compiler 做批量编译校验。
- **修改**：
  - `flux-guide/scripts/shared.mjs` — 新增 `buildRegistry(packages?)`（可传包清单的低层组装器，缺省全部 `REGISTER_PACKAGES`）与 `buildFullRegistry()`（全量 11 包）；`loadRegisteredDefinitions()` 改经 `buildFullRegistry()` 复用（行为不变）。
  - `flux-guide/scripts/validate.mjs` — 行内 9 包导入块替换为 `buildRegistry(VALIDATE_PACKAGES)`（**保持历史 9 包集合**，scheduling/ai 不纳入，保证该脚本校验结果与历史可比；全量 registry 归 validate-pages.mjs）。
  - `flux-guide/scripts/validate-pages.mjs`（新增）— 通用 CLI：目录/文件输入（目录恒递归，glob 末段匹配文件名）、`--level=compile|validate`（缺省 compile = `validateSchema` 诊断 + `compile()` 抛错防御，`(path,message)` 去重后标 `compile-throw`）、`--pattern`、`--report`（JSON 机器可读）、`--max-errors`；**L0 前置检查禁止静默跳过**（非对象根/未知 type 根/解析失败均 error）；`xui:*` 壳层约定键剥离并计数（消费方为 nop-chaos-next `transformPageJson`，非 compiler 已知属性）；退出码 0/1/2（错误/环境）。
  - 根 `package.json` — 新增 `flux:validate-pages`（`css-stub.mjs` loader + `env-stub.mjs` import 双桩启动，与 flux-guide `validate` script 同边界）。
- **验证**（直接 node 运行；`pnpm build` 本仓当前因预存 TS 错误失败，见下）：
  - 自举 (a) 金标集 87 文件（85 个 example.json 严格校验干净子集 + 2 个手写含 `xui:roles` 剥离用例）→ `errors=0, exit 0, strippedXui=1`。
  - 自举 (b) 坏 fixture 4 例（未知 renderer type / 坏表达式 `${1+}`（须闭合才是表达式标记，未闭合 `${1+` 为纯文本）/ 非法 JSON / 非对象根 `42`）→ 4 类 error 各一、`exit 1`。
  - `validate.mjs` 重构无回归：与 git HEAD 原版输出**完全一致**（`blocks=473 nodes=459 errors=37 warnings=76`；37 个预存 error 来自本 checkout 未提交的文档改动，非本次引入）。
  - `prettier --check`：新增文件合规；`validate.mjs`/`shared.mjs` 的 prettier 告警为**预存**（stash 对照确认原文件本就不合规，未扩大）。
- **本 checkout 预存状态（非本次引入，登记供后续跟进）**：
  1. `pnpm build` 失败：`flux-renderers-content/src/markdown.tsx(50,17)` TS2339 `Property 'ok' does not exist on type 'ApiResponse<string>'`——HEAD 提交 `01770f770`（ApiResponse 移除 `ok` 改 `status === 0`）后 content 包未同步迁移。该失败连带 `turbo` 清理了部分 dist（本次已单独 `pnpm --filter @nop-chaos/flux-renderers-industrial build` 重建恢复）。
  2. 工作区存在他人未提交改动（`docs/analysis/...04-e2e-domain-pages.md`、`flux-guide/design-patterns/content-display-components.md`），`validate.mjs` 的 37 个预存 error 疑与其相关。
  3. `docs/components/*/example.json` 严格全文件校验下存在松弛示例（kanban `$slot` 演示、gantt 工具栏 action、crud `variant="primary"`、`flex direction="col"`、calendar 空 body 等）——`flux:validate-pages` 对这些文件报 error 属工具语义（产物级严格校验），example 集的清洁化是独立跟进项。
- **临时产物**：`_tmp/validate-pages-bootstrap/`（自举 fixture 与探针），跨仓验证收口后清理。
