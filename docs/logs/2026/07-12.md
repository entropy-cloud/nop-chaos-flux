# Development Log - 2026-07-12

### 2026-07-12

- **flux-guide 文档补全（针对 complex-pages 覆盖缺口）**：比对 `apps/playground/src/complex-pages/`（14 页）与 `flux-guide/`（49 md）后，补齐 7 处 complex-pages 在用但 guide 未说明的缺口，并新增"端到端组合范例"层级。
  - **缺口修复**：
    - `design-patterns/crud.md`：修正字段名错误 `quickSaveAction`→`quickSaveItemAction`；新增 §6 行内编辑章节（`quickEdit.body`+`quickSaveItemAction`+`actionType:"quickSaveItem"` 裸 spec）。
    - `design-patterns/form.md`：新增 `valuesPath` 子作用域分区文档（过滤器发布 / wizard 分步 / 主从联动）。
    - `design-patterns/table.md`：操作列补充 `$slot.record.*` 行上下文（与 `$slot.item` 对应）。
    - `design-patterns/data-source.md`：新增 `dependsOn`/`sendOn`/`silent` 表 + 挂载即触发一次的 `__marker__` 惯用法。
    - `03-api-config.md`：新增 `selection` 字段投影串说明。
    - `11-host-integration.md`：`$Arr` 命名空间从"指针"扩展为含注册 + schema 用法的完整示例。
  - **新增组合范例** `flux-guide/examples/`（4 篇 + README）：master-detail / inline-quick-edit / business-document-formula / wizard-values-path，每篇串多个技术并标注"为什么这样接"。README.md 索引同步更新。
  - **validator**：`flux-guide/scripts/validate.mjs` 的 runtime-only 跳过从 `$slot.` 扩展到 `$Arr.`（同属"无法静态验证"的宿主命名空间）。
  - 验证：`pnpm --dir flux-guide validate` 我新增的内容 0 error / 0 parse error（剩余 3 个 `variant:"primary"` error 为 pre-existing，位于 crud.md §1/§3 + docs/components/crud，属渲染器定义枚举范畴 = Protected Area，未在本次改动）。
  - `dict` 字段经核查**已**在 `11-host-integration.md:103-130` 完整说明，不计入缺口。

- **Quick edit per-row save 架构升级**：将 quick-edit 从 per-cell save（每字段独立保存按钮）重构为 per-row save（行级 draft + 行末保存/取消按钮）。
- 新建 `useRowQuickEditDraft` hook（`packages/flux-renderers-data/src/table-renderer/use-row-quick-edit-draft.tsx`）管理整行 draft，通过 `RowQuickEditDraftContext` 共享给行内所有 `TableQuickEditCell`。
- `TableQuickEditCell` 在有 rowDraft context 且非 dialog/saveImmediately 模式时不渲染自己的保存按钮，改为通过 context 读写 draft 值。
- 行末自动渲染 `RowQuickEditSaveBar`（保存/取消按钮），仅在该行有 inline quickEdit 列且有 saveAction 时启用。
- 更新 inline-edit-table.json 示例 schema + complex-pages-model.ts 描述。
- 更新 `docs/components/table/design.md` 和 `docs/components/crud/design.md` 设计文档。
- **向后兼容**：dialog mode 和 saveImmediately 列保留 per-cell 行为，不触发行级 draft。
- 验证：typecheck ✓ (55/55 packages)、lint ✓（新文件零 error）、test ✓（656/656 passed，flux-renderers-data）。
- **修复 standard-crud 编辑弹窗 bug（bug #69）**：dialog picker `loadAction` 引用 `${pagination.currentPage}` 在 dialog scope 不可用导致 `openDialog` action 无声失败。修复 schema（移除 params）+ 修复 `reportUnhandledFailureClass` 静默逻辑 + 为 `ShowcaseSchemaHost` 添加默认 `onActionError`。
- **Picker dialog 宽度修复**：Dialog 新增 `xl` size 档位（`sm:max-w-4xl` = 896px）；Picker CRUD 模式自动使用 `xl`；`pickerDialog.size` dead config 修复（正向传递到 `DialogContent`）；`DialogBody` 加 `min-w-0` 防止 flex 子元素溢出；`PickerDialogConfig.size` 类型从 `string` 收紧为 `'sm'|'default'|'lg'|'xl'`。
- **fix(css): fieldset 默认 padding/gap**：`.nop-fieldset` 从 `padding-top:0` 改为 `var(--space-field-label-gap)`（8px），新增 flex `gap: var(--space-field-label-gap)`；`<legend>` 加上 4px `padding-block`。所有 fieldset 自动获得顶部内边距和标题/body 间距，无需调用者手动设 `bodyClassName`。
- **fix(css): fieldset 选择器 `>` 不匹配 Collapsible 包裹层**：`.nop-fieldset > legend` 和 `.nop-fieldset > [data-slot='fieldset-body']` 使用 `>`（直接子代），但 DOM 实际结构为 `fieldset > Collapsible > legend / div`，导致 CSS 完全不生效。字段间距为 0px（`getComputedStyle` 验证）、legend 样式不启用。改为后代选择器后，字段间距恢复为 16px。
- **Pagination aria-label 统一**：PaginationFirst/Last/Prev/Next 从硬编码 `aria-label="Go to next page"` 改为使用 locale key `t('flux.pagination.next')` + 纯图标（移除 visible `<span>`）；丢失 `<span>` 的 visible text → 改用 `title` + `aria-label`；同步更新 7 个测试文件中 14 处 aria-label 引用。
- **Pagination i18n fallback**：`packages/ui/src/lib/i18n.ts` 新增 `flux.pagination.previous`/`flux.pagination.next` 的 fallback messages（'Previous page'/'Next page'），修复未调用 `initFluxI18n` 测试中 locale key 无法解析的问题；`test-support.tsx` 添加 `initFluxI18n` 初始化。
- **Picker CRUD 分页参数自动注入**：`createCrudEvaluationBindings` 新增 `__autoPagination` 字段；`useCrudLoadAction` 参数新增 `pageField`/`pageSizeField`；`executeRuntimeAjaxAction` 自动从 `ctx.evaluationBindings?.__autoPagination` 合并到 `api.params`；更新 evaluationBindings 测试。
- 验证：typecheck ✓ (55/55)、flux-runtime ✓ (1312/1312)、flux-renderers-data ✓ (655/656，仅 table-responsive.test.tsx 为 pre-existing 失败)，lint ✓。
