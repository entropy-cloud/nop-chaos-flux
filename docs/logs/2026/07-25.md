# Development Log - 2026-07-25

### 2026-07-25

- **AI P1 remediation 0730-1 batch completed (Plan 2026-07-25-0730-1)**: both Phases executed + independently closure-audited. Closes all 4 P1 findings (0 P0) from the 2026-07-25-0707 multi-audit (P1-1, P1-2) + open-audit (P1-1, P1-2) against `@nop-chaos/flux-renderers-ai`. Each Fix pairs a focused regression Proof written red-first then turned green.
  - **Phase 1 — `useConversation` 会话生命周期 + 存储正确性 (3 P1)**:
    - **P1-a (multi P1-1, deleteConversation 陈旧闭包)**: post-await branch now reads `activeIdRef.current` / `conversationsRef.current` (new mirror, same pattern as `activeIdRef`) instead of closure-captured `activeId`/`conversations`. All 4 `setActiveId` call sites (create/delete/clear + switch already had it) now synchronously assign `activeIdRef.current` — closing the race between the abort microtask and the mirror effect (FP-1: deleting an active streaming conversation during abort no longer clobbers a concurrent `createConversation('Y')`).
    - **P1-b (open P1-1, clearAll 存储绕过)**: `clearAll` now fans out `storage?.deleteConversation?.(id)` per cleared id (or calls `storage.clearAll` if the host provides it — new optional `clearAll?: () => MaybePromise<void>` added to `ConversationStorageStrategy`), with per-id `reportStorageError({phase:'deleteConversation'})`. No more ghost rehydration on remount (FP-2); single-storage-reject is observable + non-blocking (FP-3). Decision P1-b: default per-id fallback (zero host migration), optional atomic clear is the optimization channel.
    - **P1-c (open P1-2, switch 驱逐丢消息)**: eviction loop gated behind `if (!storage) return;`. Without storage there is no rehydration path, so a no-storage A→B→A round-trip now reuses A's cached engine (messages preserved) instead of rebuilding it empty (FP-4). no-storage is ephemeral-by-design; host can still bound memory via `deleteConversation`.
  - **Phase 2 — `ai-citations` 内联文本渲染正确性 (1 P1)**:
    - **P1-d (multi P1-2, sanitizeHtml 双编码)**: inline path removed `sanitizeHtml(rawText)` — DOMPurify HTML-encodes (`5 < 3` → `5 &lt; 3`) and React text re-escapes the `&`, so the DOM showed literal `&lt;`/`&gt;`/`&amp;` (double-encode, FP-5). `rawText` now feeds `parseCitations` directly; React controlled text-node rendering provides the single safe escape (XSS invariant preserved — no `<script>` element can be created, FP-6). The markdown path (`ai-bubble/renderers/markdown.tsx`) is unchanged — react-markdown re-parses entities so no double-encode there. Side-benefit: the sanitize step also used to DROP citation markers inside forbidden tags (open-audit P2-3 same root) — FP-6 now asserts `[1]`/`[2]` are preserved.
  - **Tests (TDD red-first)**: new `use-conversation-clear-all.test.ts` (stateful-storage end-to-end: per-id deleteConversation count + remount no ghost rehydration + FP-3 single-reject observability), new `use-conversation-delete-during-abort.test.ts` (FP-1 concurrent create), enhanced `use-conversation-switch.test.ts` (FP-4 no-storage A→B→A messages preserved + same engine instance reused), enhanced `ai-citations.test.tsx` (FP-5 textContent literal chars + FP-6 XSS + citation preservation). All 4 went red on the buggy code, green after the fix.
  - **Lint gate clearance**: two untracked debug-residue files (`tmp-sanitize-check.mjs` + `tmp-sanitize-check.test.ts`, open-audit P2-1) were blocking `pnpm lint` via unused eslint-disable directives; deleted to satisfy the Closure Gate. This incidentally lands P2-1 (marked ✅ in roadmap).
  - **Owner docs**: `engine.md` §8.6 gained a storage/eviction contract note documenting clearAll storage-aware behavior, storage-aware eviction, and deleteConversation ref-based post-await reads.
  - **Closure**: independent fresh-session closure-audit sub-agent (`ses_0694d135cffeAdrbnh8mDIrjhV`) VERDICT: **PASS** (zero Blocker/Major; all 4 P1 verified TRUE against live code with file:line evidence; test-quality confirmed; 9 P2 backlog items all still `[ ]`; no stray build artifacts). Plan status → `completed`; both source audits `2026-07-25-0707-multi-audit-ai.md` + `2026-07-25-0707-open-audit-ai.md` `Audit Status: planned → closed`; `roadmap-ai.md` P1 reference marked ✅.
  - **Verification (full-green)**: `flux-renderers-ai` 56 files / 448 unit tests pass (was 54 / 442 baseline → +2 files net [+3 new −1 deleted tmp], +6 tests net). Full workspace `pnpm typecheck` (58) + `pnpm build` (31) + `pnpm lint` (31) + `pnpm test` (58) FULL TURBO green — **full-green verification** confirmed. No stray build artifacts.
  - **Out of scope (Non-Goals)**: 8 P2 items remain in `roadmap-ai.md` `### Follow-up Backlog` (P2-1 landed incidentally). `useConversation` hook architecture unchanged (only 3 contract/behavior wirings). `MessageEngine` public signatures unchanged.

- **AI P2 doc/test hardening 2151 batch completed (Plan 2026-07-25-0117-2)**: both Workstreams of `docs/plans/2026-07-25-0117-2-ai-p2-doc-and-test-hardening-2151.md` executed. Closes the remaining 12 doc/test P2 findings (6 doc/contract wording rot + 6 test-assertion strength) from the 2026-07-24-2151 multi-audit against `@nop-chaos/flux-renderers-ai`. Plan 1 closed the 25 code/behavior items; this plan aligns owner docs to final code state + hardens the weak assertions flagged as "green-test-hides-bug" family. No live behavior change (Non-Goal) — docs + tests + one JSDoc only.
  - **WS1 — 文档/契约措辞对齐 (6 条)**: `design.md:74` 移除「分组」（与 `renderers.md:95` + F1.4 对齐）；`design.md §13.1` marker 表补齐 `nop-ai-citations`/`nop-ai-voice-input`/`nop-ai-token-usage`/`nop-ai-suggestions`（与 §5.1 + `rg` 根集合一致）；`design.md §11.5` editing 归属改为如实描述「组件 `useState` 当前实现 + >200 消息虚拟回收限制 + 引擎无 editing-state setter」（原误写 engine 持有）；`terminology.md:511` `RendererEnv.fetcher` → `RendererEnv.stream`（与代码 + design.md §11.4 + engine.md 一致）；`use-message.ts` 补 JSDoc + `engine.md §8.5` 补热换 scope 注记（仅 `connector` 热换；`systemPrompt`/`tools`/`toolExecutor`/`maxToolRounds` mount-time-only）；`design.md §14.1` 补 `onResponseComplete` 事件契约边界（仅 active engine turn 触发，切换后 cleanup 旧订阅）。
  - **WS2 — 测试断言加固 (6 条, revert-sensitive)**: `ai-token-usage.test.tsx` clamp 改读 `stroke-dasharray` 实际值（dash===circumference、gap===0、in-bounds 不变量；移除 clamp 必红）；新增 `markdown-content.test.tsx`（8 tests：safeMarkdownSlice wiring 端到端 + rehypeRaw XSS 回归）；`use-message.test.tsx` 增 toolExecutor 转发用例（adapter → engine → tool_calls loop 全链路）；`p1-renderers.test.tsx` Timestamp 加固为 `toLocaleTimeString` label + ISO `dateTime` 属性；`ai-tool-call-hitl.test.tsx` hitl-no-handler 复断言 `data-approval` 保持 `pending`（click 后状态不变）。
  - **XSS 回归实证**: 回归套件覆盖 `<img onerror>` / `<script>` / `javascript:` href（raw HTML + markdown 语法双路径）/ `<svg xlink:href>` / 属性分裂嵌套 payload。三层闸门（`sanitizeHtml` DOMPurify + react-markdown `urlTransform` allowlist + React runtime `javascript:` URL 拦截）实证全部有效，**未发现真实 XSS 洞**，无需升级新 P1 finding。`<svg xlink:href>` 由 React runtime 替换为 throw-placeholder（safe），测试 helper 已排除该 placeholder 的 false-positive。
  - **Verification (full-green)**: `flux-renderers-ai` 54 files / 442 unit tests pass (was 53 / 433 baseline → +1 file, +9 tests net). Full workspace `pnpm typecheck` (58 task) + `pnpm build` (31 task) + `pnpm lint` (31 task) + `pnpm test` (58 task) FULL TURBO green — **full-green verification** confirmed. No stray build artifacts.
  - **Owner docs**: `roadmap-ai.md` 2151 批剩余 12 条全部标 ✅ + Plan 2 引用更新为「✅ 已收口」。两份源审计 `2026-07-24-2151-multi-audit-ai.md` + `2026-07-24-2151-open-audit-ai.md` 已是 `Audit Status: closed`（P1 收口时关闭；本 plan front matter 用 `> Source:` 非 `> Source Audits:`，按 plan-execution 指引 omit audit-closure step — 已是 closed，idempotent）。
  - **Deferred (adjudicated)**: 编辑态从组件 useState 迁引擎（successor path，待 engine state-ownership 演进）；markdown sanitize→rehypeRaw XSS 深挖（watch-only，回归套件已存在）。
  - **Closure Gates**: 9/10 已勾选（含 typecheck/build/lint/test 全绿 + 6 条 doc/test 实质 gate）；第 6 项「独立 closure-audit」保留 `[ ]`，待 fresh-session 子 agent 走 closure audit 后勾选（执行 session 不得自审，按 AGENTS.md Collaboration Discipline）。

- **AI P2 code/behavior remediation 2151 batch completed (Plan 2026-07-25-0117-1)**: all 6 Workstreams of `docs/plans/2026-07-25-0117-1-ai-p2-code-and-behavior-remediation-2151.md` executed. Closes 25 code/behavior P2 findings from the 2026-07-24-2151 multi-audit + open-audit (N-3..N-8) against `@nop-chaos/flux-renderers-ai`. Each behavior Fix pairs a focused regression Proof walking the repaired path.
  - **WS1 — 交互守卫与错误保真**: `ai-sender.tsx:60` `commit()` 入口加 `if (ctx?.isProcessing) return;`（host `senderExtensions` 不自禁用时不静默丢 draft）；`ai-attachments.tsx` 上传按钮 `disabled={ctx?.isProcessing}` + `handleUpload()` 入口守卫；`ai-tool-call.tsx` HITL 按钮 `!onApproval` 时 `disabled` + `title` 提示「未配置审批处理器」（新增 i18n key `flux.ai.approvalNoHandler`）；`tool-execution.ts:56` catch 保留原 Error 写入 `metadata.toolError`（不压平为裸 string，cause/stack/自定义字段全保）；`ai-bubble/index.tsx:221` pickRenderer 裸 `catch {}` 改 `catch (err) { console.warn(...); }`。Proofs: 新 `ai-silent-drop-guards.test.tsx`（sender commit + attachments upload 双 family），加固 `ai-tool-call-hitl.test.tsx`（hitl-no-handler 改 disabled 断言 + focus-trap 测试加 noopHandler 隔离两 concern），`engine-tool-loop.test.ts` 增 tool-error-flattened 两条用例（含 cause + 自定义字段保留 + 非 Error throw 包装）。
  - **WS2 — 显示与解析正确性**: `ai-citations.tsx` `parseCitations` 解析前剥离 fenced (``` / ~~~) + inline (`) code span（CommonMark 异种 fence 不互关）；index ≤ 0 不产生 citation segment（1-based）；`markdown-buffer.ts` `findUnclosedFenceCutoff` 按 delimiter 种类独立计数；`ai-tool-call.tsx` `highlightJson` 重写为「先 tokenize 后 escape」（每 token 单独 escape，含 `&`/`<`/`>`/`"` 字符串整段落入 `tok-str`，保留先 escape 防 XSS 不变量）。Proofs: `ai-citations.test.tsx` 增 7 条用例（fenced/inline code 不误匹配 + index 0 + CommonMark 异种 fence）；`markdown-buffer.test.ts` 增 4 条用例；`tool-call-and-content.test.tsx` 增 4 条用例 + XSS regression 加固（DOMParser 验证无 live attribute）。
  - **WS3 — 公共面与渲染器契约卫生**: `index.ts:84` 移除 `MaybePromise as AiMaybePromise` 死别名（`rg` 全仓零消费）；`index.ts:92-93` 移除 `combineDeltaData`/`generateMessageId`/`measureContentLength` 公共导出（apps/ 零消费，包内相对 import，测试已用相对路径）；`index.ts:99` 补 `InternalMessageState`/`PublicMessageState` 再导出；`ai-renderer-definitions.ts` 移除 7 个 dead field-metadata（`conversationId`/`onSend`/`itemRegion`/`avatarRegion`/`menuItems`/`trigger`/sender `actions`）；3 处 dispatch 补 `void` 前缀（`ai-tool-call.tsx:267`/`ai-citations.tsx:100`/`ai-suggestions.tsx:154`）。
  - **WS4 — React-19 生命周期稳定化**: `ai-chat.tsx` `actionProvider`/`componentHandle`/`hostScopeData` 三件套全部 `useMemo` 稳定（之前每渲染重建 → 流式 chunk 间重复 register/scope.replace）。Proof: 新 `ai-chat-lifecycle-stability.test.tsx`（hostScopeData 引用稳定 + 流式 chunk 间不重复 fire scope.replace）。
  - **WS5 — a11y 与 i18n 收敛**: `ai-sender.tsx:154` Textarea 加 `aria-label`（新增 i18n key `flux.ai.messageInput`）；`ai-conversations.tsx:67` rename Input 加 `aria-label={t('flux.ai.renameConversation')}`；`ai-tool-call.tsx:133` aria-label 拼接 status 文案（新增 i18n key `toolStatusRunning/Success/Failed/Cancelled` + `toolStatusLabel` helper）；`ai-prompts.tsx:96` 移除 `PROMPTS_DEFAULT_LABEL` 死导出（模块顶层不再求值 `t()`）；`ai-feedback.tsx:86-91` like/dislike/sources 走 `t()`（新增 i18n key `like`/`dislike`/`sources`，aria-label 与可见 label 均用文案）。Proof: 新 `p2-a11y-i18n.test.tsx`（5 个 surface 全断言）。
  - **WS6 — key / 正则 / 遮蔽健壮性**: `ai-suggestions.tsx:97,118` key 改 `${item.text}#${index}` 唯一化；`ai-prompts.tsx:49` stableKey 改纯内容派生（`label`+`badge`，去 `#${index}`）；`mention.ts:27` regex 字符类移除 `\s`（兑现「no whitespace」注释）；`user-edit.tsx:84` onChange 形参改名 `ev`（消除对引擎别名 `e` 的遮蔽）。Proofs: 加固 `ai-suggestions.test.tsx`（重复文案 3 项不碰撞）；新 `mention-regex.test.ts`（`@foo bar` 敲空格候选框关闭 + filterMentions 子串匹配不回归）；user-edit typecheck 通过（无形参遮蔽）。
  - **Verification (full-green)**: `flux-renderers-ai` 53 files / 433 unit tests pass (was 49 / 398 baseline → +4 files, +35 tests net). Full workspace `pnpm typecheck` (58 task) + `pnpm build` (31 task) + `pnpm lint` (31 task) + `pnpm test` (58 task) FULL TURBO green — **full-green verification** confirmed. No stray build artifacts.
  - **Owner docs**: `roadmap-ai.md` 2151 批 25 条全部标 ✅ + 引用本 plan；剩余 12 条（文档一致性 6 + 测试断言加固 5 + markdown XSS 实证 1）由 `2026-07-25-0117-2-ai-p2-doc-and-test-hardening-2151.md` 收口（planned）。两份源审计 `2026-07-24-2151-multi-audit-ai.md` + `2026-07-24-2151-open-audit-ai.md` 已是 `Audit Status: closed`（P1 收口时关闭；本 plan 无 `> Source Audits:` front matter，按 plan-execution 指引 omit audit-closure step）。
  - **Out of scope (Non-Goals)**: 编辑态迁引擎（design.md §11.5 归属冲突）→ Plan 2 文档裁定 + 未来增强；F3.1 残留 6 处 useMemo/useCallback（watch-only periodic re-review，已有 justification 注释）；markdown sanitize→rehypeRaw XSS 实证（下一轮 open-audit）；Closure Gates 第 6 项（独立 closure-audit）保留 `[ ]`，待 fresh-session 子 agent 走 closure audit 后勾选（执行 session 不得自审）。

- **AI P1 remediation completed (Plan 2026-07-25-0044-1)**: all 3 Workstreams of `docs/plans/2026-07-25-0044-1-ai-p1-remediation.md` executed + independently closure-audited. Closes all 7 P1 findings (0 P0) from the 2026-07-24-2151 multi-audit (P1-1..P1-5) + open-audit (N-1, N-2) against `@nop-chaos/flux-renderers-ai`. Each Fix pairs a focused regression Proof that walks the repaired path (revert-sensitive, no `.not.toThrow()`-only).
  - **WS1 — 输入面提交/清空契约 (N-1, N-2, P1-1)**: `suggestion-popup.tsx` split `onHover?(idx)` (highlight) from `onSelect` (commit); `onMouseEnter→onHover`, `onClick→onSelect` (was both bound to onSelect → hover inserted, FP-1). `tiptap-sender.tsx` keymap gained `submitAndClear()` — focused Enter/Shift-Enter/Mod-Enter now validate content + clear the editor surface directly (`setContent('',{emitUpdate:false})` + lastEmittedRef + `onChange('')`), no longer relying on the `!editor.isFocused` external-clear effect that skipped the focused-submit case (FP-2); `SenderCallbacks` carries `maxLength?`/`clearOnSubmit`; `schemas.ts` `AiSenderExtensionProps` + `ai-sender.tsx` pass `clearOnSubmit` through. `user-edit.tsx` pencil `disabled={ctx?.isProcessing}` + `resubmit()` early-returns on `isProcessing` (no silent draft drop, FP-3). Proofs: `tiptap-extensions.test.tsx` (hover≠insert + click still commits), `tiptap-sender.test.tsx` (focused Enter clears + clearOnSubmit=false keeps), new `user-edit.test.tsx` (streaming guard: sendMessage NOT called + draft preserved + idle regression).
  - **WS2 — 会话适配器异步正确性 (P1-2, P1-3)**: `use-conversation.ts` create + rename `saveConversation` now route through `Promise.resolve(...).catch(reportStorageError({phase:'saveConversation',...}))` (was bare `void storage?.saveConversation?.(...)` — silent swallow, FP-4); `switchConversation` gained per-switch `switchVersionRef` version guard (post-await bail when superseded) + `activeIdRef` eviction (evicts against current active id, not closure-captured target — a createConversation between await+evict could have displaced it, FP-5). Proofs: `use-conversation-storage.test.ts` (create + rename reject → onStorageError), `use-conversation-switch.test.ts` (A→B fast-switch with gated A loadMessages → activeEngine stays engineB).
  - **WS3 — 渲染器/引擎契约保真 (P1-4, P1-5)**: all 14 AI renderer roots emit `data-cid={props.meta.cid || undefined}` (View-delegated ones — ai-bubble/message-list/sender/tool-call/citations/token-usage/suggestions — gained `cid?: number` prop crossing the Renderer→View boundary; ai-chat's 3 branch roots each carry it); `rg -c data-cid packages/flux-renderers-ai/src` = 14 files (was 0). `create-engine.ts` connector-missing branch now writes `draft.lastError = new Error('connector-missing')` + fires `plugin.onError` (parity with tool-no-executor / connector-throw, FP-6 — was a silent error transition). Proofs: new `data-cid-contract.test.tsx` (static source guard ×14 + DOM propagation ×13), `engine.test.ts` (connector=null → lastError structured + onError cause + distinguishable from connector-throw).
  - **Closure**: independent fresh-session closure-audit sub-agent (`ses_06ae4e31effeIsNec9pYFFcwK5`) VERDICT: **PASS** (zero Blocker / Major, 1 round; per-finding live-code evidence + revert-sensitivity confirmed, no green-test-hides-bug patterns). Plan status → `completed`; both source audits `Audit Status: planned → closed`; `roadmap-ai.md` P1 reference marked ✅.
  - **Verification (full unit-green)**: `flux-renderers-ai` 49 files / 398 unit tests pass (was 47 files / 358 baseline → +2 files, +40 tests net: +7 WS1, +3 WS2, +30 WS3 incl. 14×data-cid static+DOM). Full workspace `pnpm typecheck` (58) + `pnpm build` (31) + `pnpm lint` (31) + `pnpm test` (58) FULL TURBO green. No stray build artifacts. (Note: e2e not in this run's `pnpm test` scope — unit/integration only.)
  - **Owner docs**: `renderer-runtime.md` data-cid contract (lines 76-81) already generic + correct — AI package now follows it, no edit needed. `design.md` §11.5 editing ownership unchanged (P1-1 adds a streaming guard, not an ownership change). No filler "no update" notes added per plan guidance.
  - **Out of scope (Non-Goals)**: 38 P2 items (multi-audit 32 + open-audit N-3..N-8) remain in `roadmap-ai.md` `### Follow-up Backlog (P2 from audits)` → 2151 batch. markdown `sanitize→rehypeRaw` XSS empirical check is next-round open-audit territory (self-assessed blind spot, not owned here).
