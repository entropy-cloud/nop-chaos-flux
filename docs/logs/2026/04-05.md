# Development Log — 2026-04-05

### 2026-04-05

- Started Phase 4B with the first reaction runtime baseline: added `ReactionSchema`, a `reaction` null renderer in `flux-renderers-basic`, and a runtime-owned reaction runner supporting `watch`, optional `when`, `immediate`, `debounce`, `once`, and action dispatch through the normal runtime action path.
- Link to docs and code: `packages/flux-core/src/types/schema.ts`, `packages/flux-runtime/src/reaction-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-renderers-basic/src/reaction.tsx`, `packages/flux-renderers-basic/src/index.tsx`, `packages/flux-renderers-basic/src/index.test.tsx`, `docs/architecture/api-data-source.md`
- Key decision: keep the first reaction implementation lightweight and renderer-activated, but schedule reaction execution asynchronously after writes settle so it behaves like a watch/effect substrate instead of an inline mutation hook.
- Next step: deepen Phase 4B with stronger loop guards, per-cycle dedupe, and likely a dedicated runtime-side reaction registry instead of the current per-registration runner only.

### 2026-04-05

- Added `refreshSource` as a built-in action: action dispatch now targets runtime-owned sources by id through `componentId`/`componentPath`, returning a normal `ActionResult` instead of relying on `refreshTable` or component-instance semantics for source refresh.
- Link to docs and code: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: keep the first targeting rule simple and reuse existing action fields (`componentId` / `componentPath`) rather than inventing a brand-new source-specific field before the broader action-schema cleanup lands.
- Next step: Phase 4A now has both automatic invalidation and an explicit escape hatch; the next major milestone is Phase 4B reaction runtime.

### 2026-04-05

- Added the first dependency-aware source invalidation baseline: source registry entries now subscribe to scope changes, formula sources collect dependencies from compiled runtime value state, api sources collect dependencies from evaluated request config, and matching scope-path updates automatically recompute/refresh the source without manual refresh calls.
- Link to docs and code: `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/node-runtime.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: guard against self-trigger loops by ignoring writes to a source's own published `dataPath` rather than suppressing all same-scope writes; lexical same-scope updates are valid dependencies and must still drive source recomputation.
- Next step: Phase 4A is now a credible automatic source substrate; the next major slice is Phase 4B reaction runtime, with `refreshSource` action support as a likely follow-up integration point.

### 2026-04-05

- Added explicit runtime-level source refresh semantics: `RendererRuntime.refreshDataSource({id, scope?})` now resolves registered sources through the scope-scoped registry and refreshes the matched controller without relying on unrelated page refresh behavior.
- Link to docs and code: `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: keep refresh lookup scope-scoped first and only fall back to first-match scan when no scope is provided; this preserves the lexical ownership model and avoids reintroducing a page-global source namespace by accident.
- Next step: build dependency-aware invalidation on top of the same source registry so `refreshDataSource` becomes the explicit imperative escape hatch rather than the primary update mechanism.

### 2026-04-05

- Extended Phase 4A with the first source state surface: `DataSourceController` now exposes `getState()` returning `started/loading/stale/value/error`, api sources track loading/error/stale around fetch/cached refresh, and formula sources expose the same shape with synchronous derived-value semantics.
- Link to docs and code: `packages/flux-core/src/types/runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: expose minimal state on the existing controller contract first instead of introducing a new observer API; this keeps the ownership model stable while giving Phase 4 follow-up work a concrete surface for UI and reactions.
- Next step: add dependency-aware invalidation and explicit runtime-level source refresh addressing by id so source state is useful beyond mount-time and manual controller access.

### 2026-04-05

- Extended Phase 4A to the first real unified source shape: `DataSourceSchema` is now an `api | formula` union, formula sources register through the same runtime-owned source registry, and the first publish is scheduled to a microtask so sibling consumers mounted in the same render pass observe the derived value.
- Link to docs and code: `packages/flux-core/src/types/schema.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/index.test.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/flux-renderers-data/src/index.test.tsx`, `docs/architecture/api-data-source.md`
- Key decision: ship a minimal formula-source baseline through the new registry first instead of blocking on full dependency-indexed lazy recomputation; ownership and schema unification matter more right now than finishing the entire reactive substrate in one slice.
- Next step: add explicit source state/refresh surfaces and dependency-aware invalidation, then move into Phase 4B reaction runtime on top of the same substrate.

### 2026-04-05

- Started Phase 4A with the first runtime-owned source registry cut: `RendererRuntime` now exposes `registerDataSource(...)`, registry entries are bucketed by `ScopeRef.id`, `DataSourceRenderer` only performs register/dispose lifecycle wiring, and same-scope same-id registrations replace earlier entries through runtime instead of renderer-local controller ownership.
- Link to docs and code: `packages/flux-core/src/types/runtime.ts`, `packages/flux-core/src/types/renderer-core.ts`, `packages/flux-runtime/src/source-registry.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-renderers-data/src/data-source-renderer.tsx`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: keep `createDataSourceController(...)` as a compatibility constructor for now, but make `registerDataSource(...)` the ownership boundary so source lifecycle starts moving into runtime without forcing the whole formula-source/reaction design in one step.
- Next step: extend the registry from pure lifecycle ownership into first-class source semantics by bringing `formula` sources and explicit refresh/state surfaces into the same runtime substrate.

### 2026-04-05

- Tightened the second half of Plan 37 Phase 3B: action dispatch now caches compiled top-level payload extraction and compiled `action.api` objects by identity, and ajax monitor events now report the final canonical executable request instead of the pre-preparation declarative config.
- Link to docs and code: `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: diagnostics should reflect the same request object that fetch/dedup/cache execute; otherwise debugger/monitor output drifts from real runtime behavior exactly where Phase 3 is trying to remove split semantics.
- Next step: Phase 3 can be treated as a completed baseline; the next meaningful runtime slice is either deeper schema-compiler integration for actions or Phase 4 source/reaction ownership.

### 2026-04-05

- Extended Plan 37 Phase 3 into the hot path: `executeApiObject()` now also evaluates declarative request objects before request preparation, request/response adaptor expressions are cached by source identity, and `runtime.evaluate()` now memoizes compiled object-shaped values by object identity to avoid repeated `compileValue()` work during action/request execution.
- Link to docs and code: `packages/flux-runtime/src/index.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/index.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: keep Phase 3B minimal by caching the existing execution substrate instead of widening schema/compiler contracts first; this reduces ad hoc compile work immediately while preserving current public runtime APIs.
- Next step: commit the completed Phase 3A/3B runtime work, then decide whether the next slice should deepen action-schema compile integration or move to Phase 4 source/reaction runtime ownership.

### 2026-04-05

- Completed Plan 37 Phase 3A request-path convergence: `executeApiObject()` now consumes a canonical executable request shape after `includeScope`, `params`, and `requestAdaptor` are applied, and datasource cache/dedup now key off that final request semantics instead of the raw declarative `ApiObject`.
- Link to docs and code: `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/data-source-runtime.ts`, `packages/flux-runtime/src/__tests__/request-runtime.test.ts`, `docs/architecture/api-data-source.md`
- Key decision: canonical executable requests normalize query params into the final URL and strip `params` from the fetcher-facing request so equivalent `/api/items?page=1` and `/api/items` + `params: {page: 1}` share the same dedup/cache identity.
- Next step: move into Phase 3B so action/request/source hot paths stop relying on ad hoc `runtime.evaluate()` compilation during dispatch.

### 2026-04-05

- Completed Plan 37 Phase 2 validation work: added direct `NodeRenderer` selective-invalidation coverage so unrelated scope-path updates no longer trigger unrelated node meta/props recomputation, and fixed page/form scope-owned writes to emit matching `ScopeChange` payloads.
- Link to docs and code: `packages/flux-react/src/index.test.tsx`, `packages/flux-react/src/schema-renderer.tsx`, `packages/flux-runtime/src/page-runtime.ts`, `packages/flux-runtime/src/form-runtime.ts`, `packages/flux-react/src/node-renderer.tsx`
- Key decision: scope-owned adapters that bridge page/form stores into `ScopeRef` must emit the same changed-path contract as plain scope stores, otherwise selective invalidation becomes correct only for child scopes.
- Next step: move into Phase 3 request/action hot-path convergence with the same rule that hot execution paths should stop relying on ad hoc compile-at-dispatch behavior.

### 2026-04-05

- Implemented Plan 37 Phase 0-1 baseline work: `ScopeStore` now carries `ScopeChange`, formula leaf evaluation refreshes dependency sets on every successful run, and runtime gained changed-path matching helpers plus plugin priority sorting.
- Updated React/runtime hot path so `NodeRenderer` subscriptions gate meta/props recomputation by dependency-path intersection instead of broad scope notifications.
- Link to docs and code: `packages/flux-core/src/types/scope.ts`, `packages/flux-formula/src/evaluate.ts`, `packages/flux-runtime/src/scope.ts`, `packages/flux-runtime/src/scope-change.ts`, `packages/flux-react/src/node-renderer.tsx`, `docs/architecture/flux-core.md`, `docs/architecture/renderer-runtime.md`
- Key decision: keep broad-access correctness by degrading dependency collection to wildcard when evaluation relies on `ownKeys`/materialized access; prefer conservative invalidation over false precision.
- Next step: start Phase 2 with explicit node-level selective invalidation validation and then move into request/action compile-path convergence.

### 2026-04-05

- Refined `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` Phase 4A/4B into a concrete implementation sketch: first version should keep source/reaction ownership on `RendererRuntime` using scope-local `scopeEntries: Map<scopeId, Map<entryId, Entry>>`, with `null` renderers only acting as register/release activators.
- Updated the related architecture docs to use the same concrete wording so plan and architecture now agree on the first implementation shape.
- Link to docs: `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md`, `docs/architecture/api-data-source.md`, `docs/architecture/flux-core.md`, `docs/architecture/action-scope-and-imports.md`
- Key decision: choose the simplest structure that still preserves lexical ownership, cleanup, and shadowing semantics; avoid page-global registries and avoid expanding `ScopeRef` into a behavior bag.

### 2026-04-05

- Updated architecture docs to make the boundary explicit: `data-source` and `reaction` belong semantically to the current lexical data scope, but are implemented as runtime-owned sidecar registries keyed by `ScopeRef.id` rather than as new behavior methods on `ScopeRef`.
- Link to docs: `docs/architecture/api-data-source.md`, `docs/architecture/flux-core.md`, `docs/architecture/action-scope-and-imports.md`
- Key decision: use scope-scoped source/reaction buckets to preserve lexical ownership and cleanup semantics without collapsing data scope and behavior registries into the same contract.
- Next step: when Phase 4 starts, build scope-local registration/disposal first, then layer refresh and reaction scheduling semantics on top.

### 2026-04-05

- Clarified in `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` that `data-source` and `reaction` belong semantically to the current data scope, but should be implemented as runtime-owned, scope-scoped sidecar registries keyed by `ScopeRef.id` rather than as new behavior methods on `ScopeRef`.
- Key decision: source/reaction lifecycle, dependency indexing, and disposal should follow the current lexical scope boundary, avoiding both page-global bags and behavior pollution on the pure data-scope contract.
- Next step: if implementation starts, Phase 4A/4B should build scope-local source/reaction buckets first, then layer refresh/reaction semantics on top.

### 2026-04-05

- Swept the remaining cross-plan wording in `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` so no section still implies that closed plans should carry leftover blocking work.
- Key decision: `Plan 37` is now the single live container for any blocking residual work discovered during execution, even when that residual work originally sat near the old complex-control/workbench boundary.
- Next step: keep future clarifications inside `Plan 37` instead of reintroducing dependencies on completed roadmap files.

### 2026-04-05

- Clarified the relationship between `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` and the already-completed `docs/plans/33-complex-control-platform-convergence-refactor-plan.md`.
- Key decision: `Plan 33` remains closed; if later execution of `Plan 37` exposes residual workbench/complex-control leftovers that directly block the core-runtime roadmap, those items must be recorded and continued inside `Plan 37` instead of pointing back to the completed plan.
- Next step: keep `Plan 37` as the single live container for any blocking residual work discovered during the core-runtime convergence phases.

### 2026-04-05

- Added the new execution plan `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` after re-checking the 2026-04-04 architecture analysis pair against the current codebase.
- The plan narrows the true Flux runtime core work to seven phases: dependency tracking and changed-path scope notifications, selective node invalidation, compiled action/request hot paths, first-class source/reaction runtime, minimal action control flow, contract/registry hardening, and explicit complex-renderer state ownership.
- Link to docs and code anchors: `docs/analysis/2026-04-04-flux-architecture-evaluation.md`, `docs/analysis/2026-04-04-lowcode-platform-comparison.md`, `packages/flux-formula/src/scope.ts`, `packages/flux-runtime/src/request-runtime.ts`, `packages/flux-runtime/src/action-runtime.ts`, `packages/flux-react/src/node-renderer.tsx`
- Key decision: do not put visual editor/material protocol, JSON Schema standardization, JavaScript expression syntax migration, provider-depth cleanup, or workbench/platform-layer issues into the core runtime plan; those are either higher-layer concerns or lower-priority follow-ups.
- Next step: if implementation begins, start with Phase 0/Phase 1 only, so changed-path invalidation becomes the shared substrate before expanding request/source/action capabilities.

### 2026-04-05

- Consolidated the necessary parts of `docs/plans/38-core-architecture-enhancement-plan.md` back into `docs/plans/37-flux-core-runtime-architecture-convergence-plan.md` so `Plan 37` can remain the single retained core-runtime roadmap.
- Added four missing implementation guardrails to `Plan 37`: dependency sets must refresh after each successful reevaluation, source runtime and reaction runtime are split into separate deliverables, action-control-flow work must also extend `ActionResult` and debugger/monitor contracts, and table ownership Phase 1 is intentionally limited to `pagination` plus `selection`.
- Key decision: do not absorb `Plan 38` proposals that would hard-wire `RendererDefinition.memo`, make source refresh primarily a component-handle concern, or elevate provider-depth cleanup into the main runtime execution path.
- Next step: when `Plan 38` is deleted, `Plan 37` should already contain the necessary implementation detail and risk guardrails for the retained roadmap.
