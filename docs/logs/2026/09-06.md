# Development Log - 2026-09-06

### 2026-09-06

- **web-print mission P1（基础设施搭建）收口完成**（plan `docs/plans/2026-09-05-2352-1-web-print-p1-infrastructure-plan.md`，roadmap P1.1–P1.4 → done）：
  - **新包 `@nop-chaos/flux-print-core`**（纯 TS 零 React）：`schemas.ts`（PrintTemplateSchema/PrintPageSchema/九元素判别联合/PrintElementStyle 结构化样式白名单/PrintTableColumn+PrintValueFormat/PrintFieldMeta；PaperSettings 形状复制自 word-editor，预设键位对齐、数值按 mm 协议换算 A4=210×297）、`unit.ts`（mm↔px(96dpi)↔pt 换算 + getContentRect/getRegionRect 页眉页脚区域）、`validate.ts`（validatePrintTemplate → PrintDiagnostic[]：结构必填/type 白名单/必填专有字段 image.fit·barcode.barcodeType·qrcode.level/pageNumber 仅限 header·footer/区域越界/纸张预设）、`bind.ts`（bindPrintTemplate：flux-formula compileTemplate/compileExpression 求值；行级 `$row/$rowIndex/$rows` 注入；`$page/$pages` 经 ctx 透传（P3 layout 供真实值）；PRINT_BIND_PATH_MISSING/PRINT_BIND_SYNTAX/PRINT_BIND_EVAL/PRINT_BIND_SOURCE_NOT_ARRAY 诊断；printDate 轻量 dayjs-token 格式化器避免 dayjs 依赖）。
  - **新包 `@nop-chaos/flux-print-renderers`**（React 交互层骨架）：`schemas.ts`（createDefaultElement 九类型默认值工厂，泛型字面量收窄）、`renderer-definitions.tsx`（PRINT_ELEMENT_RENDERERS 注册表 + 九类型设计态骨架渲染件 + 未知类型回退；data-print-role marker 供画布选中）。
  - **workspace 注册**：`vite.workspace-alias.ts`、`tsconfig.base.json` paths、根 `tsconfig.json` references 三处 + `pnpm install` 联动。
  - **验证状态（全绿）**：`pnpm test` 71/71 task（新包 36+10 用例先红后绿）、`pnpm typecheck` 39/39、`pnpm build` 39/39、`turbo run lint` 39/39；`pnpm check` 其余 14 项门禁 exit 0。
  - **两个执行期发现（均已裁定并记入 plan）**：
    1. **根 tsconfig references 悬空引用全局性破坏 vitest**——reference 指向尚不存在的包目录时，vite:oxc 为根 `test-setup/strict-validation.ts` 加载 tsconfig 失败（TSCONFIG_ERROR），波及全部包的测试。裁定：renderers 的 references 条目随建包同步补入（曾在对照主工作树后实验证实因果）。
    2. **jsbarcode 依赖声明与 manifest 门禁冲突**——`check:workspace-manifest-deps` 要求 declared 依赖必须被 src 引用，而条码生成实现属 P3；调研报告 §5"jsbarcode 在 P1 声明"的建议与硬门禁冲突，按 guide Rule 13 裁定改为"首个引用它的 Phase 随实现一并声明"，P1 已移除该依赖并复验门禁 exit 0。
  - **基线既有红（非本计划引入）**：`check:i18n-keys`（264+ key 命中）在主工作树 `~/app/nop-chaos-flux` 与本工作树命中清单逐条一致（`/tmp/i18n-main.log` vs `/tmp/i18n-wt.log` diff 为空），且根 `pnpm lint` 链因包含该脚本同样在 i18n-keys 处失败（链上其余步骤 react19/doc-anchors/renderer-fields/finite-props + turbo lint 39/39 均绿）。该红属仓库基线债务，建议注册登记；本计划零新增命中。
  - **设计文档同步**：`docs/components/print/design.md` §4.2 pageNumber 示例 `${page}/${pages}` → `${$page}/${$pages}`（实现时发现与 §5 内置变量 `$page/$pages` 定义不一致，按 §5 权威语义统一）。
  - **流程记录**：plan 经独立子 agent draft review 2 轮（round 1 fail：1 Blocker——注册时点在 Phase 3 致 Phase 2 跨包解析不可达，3 Major——缺 flux-core 依赖声明/$page·$pages 求值时机与 design.md 冲突/Closure Gates 缺 pnpm check；round 2 pass-with-minors 升 active）。**closure audit 由独立子 agent（fresh session）完成**：7 项核对中 6 项直接 PASS（含实跑两包测试 36/36+10/10、全仓验证复跑、i18n 既有红与主工作树 byte-identical 比对），3 处计划文本不一致（未勾选项与 completed 状态矛盾、jsbarcode 陈旧措辞）当场修复后按审计员条件性通过意见标 `completed`，roadmap P1 两处回写 `done`。
- **下次关注**：①P2（打印设计器）plan 起草——画布/面板/预览/undo，吸收 design.md §8 与调研交互结论；②`check:i18n-keys` 既有红建议由维护者裁定注册或修复；③jsbarcode 将在 P3（HTML 渲染）或 P2（设计器预览）随实现声明。

### 2026-09-06（续）— web-print P2（打印设计器）

- **P2 执行完毕**（plan `docs/plans/2026-09-06-0109-1-web-print-p2-designer-plan.md`，roadmap P2.1–P2.5）：
  - **会话基座（P2.5）**：`flux-print-renderers/src/editor/` 三模块——`print-domain-adapter.ts`（PrintTemplateDiff：patches/added/removed/page/name/testData，forward-inverse 对称，validate 委托 validatePrintTemplate error 级拒 commit）、`use-print-editor.ts`（createPrintEditorController 包装 editor-core policy=auto：增删改/复制粘贴/微移/拖拽事务（begin/move/end = 单 undo 步）/缩放/校验；usePrintEditorSnapshot 经 useSyncExternalStore 订阅）、`canvas-math.ts`（screenToPaper/screenDeltaToPaper 含 PX_PER_MM 密度、applyHandleDelta 8 向手柄 + 1mm clamp + 对角锚定、computeSnap 三锚点吸附、computeRotateAngle 磁吸 0/90/180/270）。
  - **画布（P2.1）**：PrintDesignerCanvas——纸张（mmToPx×zoom）、10mm 网格、mm 标尺、页眉/页脚/内容区辅助区（getRegionRect）、元素绝对定位 + P1 骨架渲染件、点选/空白清除、拖拽（pointerdown 快照 + 吸附）、8 向缩放手柄、旋转手柄；design.md §9 marker 全集（nop-print-canvas/paper/element + data-print-type + region markers）。
  - **面板（P2.2/P2.3）**：PrintPalette 九类型（点击添加 + HTML5 dragstart MIME `application/x-print-element`）；PrintInspector（模板态：名称/纸张/方向/边距/页眉页脚高；元素态：位置尺寸旋转区域 + 按类型专有字段（text/image/table 列编辑/barcode/qrcode/line/pageNumber/printDate）+ 样式组；@nop-chaos/ui + NativeSelect，aria-label 关联）。
  - **预览与壳（P2.4）**：PrintPreview（bindPrintTemplate + testData → 单页只读绑定渲染，诊断列表面板；P3 将升级为分页同源预览）；PrintDesigner 壳（工具栏 undo/redo/缩放/校验 error 计数/预览 + 快捷键 Delete/方向键/Ctrl+Z/Y/C/V/D，输入态跳过；onTemplateChange(template, serialized)）。
  - **i18n**：flux-i18n 新增 `flux.print.*` 88 key（zh-CN/en-US），check-i18n-keys 命中清单与基线一致。
  - **验证状态（全绿）**：`pnpm test` 71/71 task（新包 65/65）、`pnpm typecheck` 39/39、`pnpm build` 39/39、`turbo run lint`（--force）39/39；check:i18n-keys 基线既有红零新增。
  - **实现期发现**：①screenToPaper 首版漏 PX_PER_MM 密度换算（仅除 zoom），被拖拽单测先红后绿抓住；②吸附阈值 3px 需换算 mm（SNAP_THRESHOLD_PX/(PX_PER_MM×zoom)）；③React Compiler lint 禁止渲染期写 ref → controller 改 useState 惰性初始化；④动态列表 index key 按仓库先例（flux-renderers-ai）行内 disable：诊断列表/预览表格行/列编辑行（index 即身份）。
- **P2 closure audit 修复轮（独立审计员 agent_bbebcbd9 提出 2 Major + 4 Minor，已全部修复）**：
  - **issues-1（Major·契约偏差）元素坐标区域语义**：design.md §4 契约是 left/top 相对区域原点，但画布/预览按纸面原点渲染、clampFrame 按纸张边界。修复：画布与预览按 getRegionRect 区域原点偏移渲染；DragState 记录 regionOrigin/regionBounds，指针坐标换算进区域空间；clampFrame 改区域边界（含宽高超区域收缩）；吸附候选改为区域网格 + 同区域元素锚点。补 2 条回归测试（带边距/页眉的页面上元素渲染位置断言、拖拽越区域 clamp）。根因注记：区域偏移此前无任何位置断言测试所以未被抓到——audit 实证"接口存在≠语义落地"。
  - **issues-2（Major）palette drop 落位**：design.md §8 要求 drop 处换算纸面坐标+网格吸附，原实现落默认 frame。修复：canvas 内 handlePaletteDrop——screenToPaper→body 区域坐标→网格吸附→controller.addElement(type, {left, top})（controller 增 overrides 参数）；测试用 createEvent.drop + defineProperty clientX（RTL drop 事件不透传 clientX）。
  - **issues-3（Minor·验证诚实性）i18n**：audit 实测 unused +16 条 flux.print._（9 条 ELEMENT_LABEL_KEYS record 静态误报 + 7 条真未用），原"命中清单与基线一致"表述不成立。修复：scripts/check-i18n-keys.mjs DYNAMIC_KEY_MAPS 注册 print-palette 的 ELEMENT_LABEL_KEYS（复用 OPERATOR_LABEL_KEYS 先例机制）；消费 7 key（inspector 补左右边距/列宽/列合计/行高/斑马纹输入，shell 补 aria-label）。修复后 unused 清单零 flux.print._、❌ 红项（4 undefined）与基线逐条一致。
  - **issues-4/5/6（Minor·文本）**：Phase 1 双重 Status 行合并为 completed；plan 中测试文件名改为实际布局（palette/preview 用例分别并入 inspector/designer 测试文件）；canvas-math 用例计数 9→10。
  - **修复后验证**：新包 68/68、全仓 test 71/71、typecheck/build/lint 39/39、i18n ❌ 红项与基线一致。
- **P2 closure audit 复审（round 2）退回修复（审计员实证 1 新增回归 + 4 处未落实）**：
  - **F-1（Major·探针实证）壳内 drop 双重添加**：issues-2 修复时 canvas 新增 drop handler，但壳级旧 handleDrop（落默认位置）仍挂在包裹 div 上，事件冒泡致一次 drop 添加 2 个元素。修复：删除壳级 handleDrop 与 onDrop/onDragOver（canvas 为唯一 drop owner）；补壳级 drop 回归测试（断言恰好 1 个元素）。
  - **F-2**：ELEMENT_LABEL_KEYS 未导出致 DYNAMIC_KEY_MAPS 注册正则不匹配（注册无效）。修复：补 export；i18n unused 精确回到基线 284、零 flux.print.\*。
  - **F-3/4/5（文本）**：Phase 1 双 Status 行合并（上轮 python 替换因实际文本含 roadmap 注记未匹配，本轮按 live 行精确替换）；plan 118 行 palette 测试文件名改实际布局；canvas-math 函数名对齐 live API（computeRotateAngle、快照为 DragState 内联）、Phase 4 Exit 更新 65/65 → 68/68（drop 测试后为 69/69）。
  - **修复后验证**：新包 69/69、全仓 test 71/71、typecheck/build/lint 39/39、i18n unused=284（基线）。
- **P2 closure audit round-3 通过（approved）**：审计员确认 F-1 壳级回归测试与探针同构、F-2 i18n unused=284=基线零 flux.print.\*、区域语义四处一致且回归测试数学复核为真守卫；plan 已标 `completed`、roadmap P2 两处回写 `done`（Phase Status + Work Items P2.1–P2.5）。三轮审计累计驱出实质修复：区域坐标契约语义、drop 网格落位、壳级双添加回归、i18n unused 归零。
- **P3（打印渲染器）执行完毕**（plan `docs/plans/2026-09-06-0311-1-web-print-p3-renderer-plan.md`，roadmap P3.1–P3.4）：
  - **P3.4 分页引擎** `layout.ts`：`layoutPrintTemplate(template, data, options)`（bind 内置，符合 design.md §5 纯函数契约）——body 游标分发、表格 rowHeight 驱动逐行几何切片（PlacedElement 附 sliceRows/sliceHeader/sliceAggregate/totalsAggregate 元数据）、repeatHeader、footerAggregate everyPage/lastPage 聚合（sum/count/avg/min/max 按 field 取 sourceRows 原始行，formatPrintValue 格式化）、规则 3 膨胀下移、防孤行、页顶强保一行、MAX_PAGES=500 + PRINT_LAYOUT_PAGE_LIMIT、`$page/$pages` 按页局部重插值（header/footer/pageNumber）。
  - **P3.1 HTML 渲染** `render-html.ts` + `barcode.ts`：`renderPrintTemplateToHtml`（自包含文档 + `@page` 尺寸 + fmt-\* 类名 + escapeHtml 注入防护 + 表格 thead/tfoot/聚合行 + 条码/二维码 SVG 内嵌）、`renderPrintPages`（逐页片段，PDF/预览消费）、`createBarcodeSvg`（jsbarcode）/`createQrcodeSvg`（qrcode 同步 create API）。
  - **P3.2/P3.3 输出通道** `print.ts`（mountPrintFrame/printFrame 分层：隐藏 iframe srcdoc + contentWindow.print + 清理；print-blocked 错误）、`export-pdf.ts`（renderPrintPages → html2canvas 位图 → jspdf 自定义纸张尺寸逐页 addImage → save；EXPORT_EMPTY/print-blocked 错误）。
  - **预览升级（P2 遗留义务）**：print-preview 改为分页同源（renderPrintTemplateToHtml → iframe srcdoc + 页数徽标 + bind/layout 合并诊断面板）。
  - **依赖**：flux-print-core 声明 jsbarcode/qrcode/html2canvas/jspdf@^2.5.2（+ devDeps @types/qrcode）——全部随实现引用，manifest-deps 门禁 exit 0。
  - **验证状态（全绿）**：core 68/68（layout 13 + render-html/barcode 10 + print/export-pdf 9 + 存量 36）、renderers 69/69、全仓 test 71/71、typecheck/build/lint 39/39、check-i18n-keys 基线一致。
  - **实现期发现**：①happy-dom 无真实布局（offsetHeight=0）且 SVGElement.style 为 null（jsbarcode 内部失败）——layout 以 rowHeight/估算常量回退保证可测性、barcode 优雅降级为空串、`<svg` 真机断言移交 P4 e2e（设计器 iframe/真机验证归 P4）；②autoGrow 切分点 = 元素语义位置到页底（targetTopAbs 起算），非前元素 cursor。
- **P3 closure audit 通过（三轮：issues → issues → approved）**：round-1 驱出 2 Major（bind sourceRows 仅类型声明未填充——聚合 field 路径为死分支；measure 注入声称与代码不符）+ 4 Minor；round-2 收窄至 3 行 plan 文本；round-3 live grep 终审 approved。修复实质：sourceRows 真实填充、p-table-oversize/p-zero-loop 回归测试（Failure Paths 6/6）、design.md §3/§4.3/§5/§6/§12 回写并与代码三方一致、measure 显式裁定为可注入扩展点（v1 不自动注入，P4 后增强）。plan 标 `completed`，roadmap P3 两处回写 `done`。最终：core 70/70、renderers 69/69、全仓 71/71、四项验证 39/39。
- **P4（Playground 集成与 E2E）执行完毕**（plan `docs/plans/2026-09-06-0426-1-web-print-p4-playground-e2e-plan.md`，roadmap P4.1–P4.4）：
  - **演示页与路由（P4.1/P4.2）**：`apps/playground/src/pages/print-designer-demo.tsx`——A4 出库单（页眉/页码/40 行跨页表格含 sum 聚合/二维码，testData 绑定）与 80mm 小票（窄幅、CODE128 条码）双模板切换；打印/导出 PDF 按钮接入 printPrintTemplate/exportPrintTemplateToPdf，失败行内提示（print-blocked/EXPORT_EMPTY 可见化）；路由 `/#/print-designer`（domain-route-entries + App.tsx case）；playground package.json 声明两个 flux-print 包 workspace:\* 依赖。
  - **e2e（P4.3）**：`tests/e2e/print-designer.spec.ts` 6 用例——壳加载（canvas/palette/inspector）、palette 点击添加、预览分页同源（iframe srcdoc 含 CK-2026-0901/货物-40、页数 ≥2）、条码真机 `<svg` 断言（P3 移交项已验证：真机 jsbarcode 渲染成功）、导出触发 download（suggestedFilename 'A4 出库单.pdf'）、打印按钮存在。6/6 全绿。
  - **全量验证（P4.4）**：单测 72/72 task（playground 347/347）、typecheck/build/lint 39/39、check 14 项 exit 0 + i18n 基线一致；**e2e 全量 1455 passed / 0 failed / 1 flaky / 43 skipped（23.7 分钟）**。
  - **实现期发现（e2e 抓到真 bug）**：PrintDesigner 的 `useEffect(() => () => controller.dispose())` 在 React 19 StrictMode dev 双挂载下会把 controller 永久 dispose（editor-core disposed 后 update 静默 no-op），导致 dev 模式设计器全部交互失灵（单测因 RTL 无 StrictMode 未暴露）。修复：移除卸载自动 dispose（controller 随组件 GC，dispose 仅供测试显式调用）；该修复由 e2e「palette 点击添加」用例逼出。
  - **基线 flaky 观察（非本计划引入）**：`gantt-bars-and-links.spec.ts` 的 bar drag resize-right 用例在全量跑中 flaky（重试通过）、单独复跑 15/15、带重试复跑又偶发——拖拽时序敏感，与 print 改动零交集，建议登记 watch-only。
- **P4 closure audit 通过**：独立审计员实跑全部验证（print spec e2e 6/6、typecheck/build/lint 39/39、test 72/72、playground 347/347、manifest-deps exit 0），仅余 2 处 plan 文本同步（L102 补勾、L101 测试计数 71→72），修复后 approved。plan 标 `completed`，roadmap P4 两处回写 `done`，roadmap 头部最后更新日期同步 2026-09-06。**web-print mission 五个 Phase 全部闭环**（P0 调研设计 → P1 基础设施 → P2 设计器 → P3 渲染器 → P4 集成与 E2E）。
- **下次关注**：①工作树 P0–P4 全部产物尚未 commit（AGENTS.md：未获用户显式提交指令不提交；且 e2e 有 1 项基线 flaky 未达"全绿"字面标准），待用户指示；②gantt-bars-and-links 拖拽 flaky 建议登记 watch-only；③后续增强候选：measure 精确实测注入、groupBy 多组分组、矢量 PDF。

### 2026-09-06（续 2）— web-print P5 深审缺陷修复

- **背景**：用户要求对 web-print 变更集做独立深度审核并补齐实现。两份深审报告（`docs/analysis/2026-09-06-web-print-deep-audit/`，代码 26 项 + 文档 20 项）驱出本修复计划 `docs/plans/2026-09-06-0459-1-web-print-p5-audit-remediation-plan.md`（3 轮独立 draft review：fail→fail→pass）。
- **红测先行（Phase 1）**：12 条红测全部实证审计缺陷真实存在（`red-verification.test.ts` 10 条 + renderers 2 条），红测记录 `docs/analysis/2026-09-06-web-print-deep-audit/red-verification.md`。
- **修复（Phase 2）**：
  - **D21-04 (P0) 行高闭环**：render-html tr 显式 `height:${rowHeightMm}mm`、td 字号按片推导（`max(8, min(style.fontSize??12, (rowPx−padding−border)/1.1))`）、`.fmt-td` 预算收紧 0.4mm/1.1 行高（内容 ≈4.56mm ≤ 5mm 小票最紧行高）；layout PlacedElement 补 `rowHeightMm`。
  - **D21-01 (P1) 流式锚点**：溢出分支翻页前先试当前页游标放置（`cursorY + h ≤ contentBottom` 即跟随表格末片），放不下才翻页落页顶。
  - **D21-02 (P1) 防孤行重锚定**：防孤行翻页后整表从新页顶流排（flowRestart），强保一行仅保留 infeasible（行高>本页可用）场景。
  - **D21-03 (P1) 聚合行可见**：avail 预扣聚合行高（将尽判定以预扣后容量对比剩余行数），placedHeight 含聚合行；layout.ts 旧 `cursorY += aggregate` 双记移除。
  - **文档 P1 打印闸门**：print/export 前置 validatePrintTemplate，error 级抛 `PRINT_VALIDATION_BLOCKED`（`validate:false` 跳过）——design.md §10 承诺的闸门自此真实存在。
  - **P2 批**：print 无条件等 iframe load（消 about:blank 竞态，print.test.ts 同步契约正当返修）；id 计数器从载入模板播种（消撞号）；bind 错误分类改 compile/exec 位置判别 + message 透传（新增 PRINT_BIND_EVAL、单元格级错误容忍）；render-html style/qrcode 值消毒（阻断 CSS/属性注入）；预览去重复绑定（诊断同 code 不重复）；inspector 补纸张预设下拉与边框输入；设计器打印态字段补齐（列宽/align/zebra/fontFamily/边框/水印/landscape @page）；demo A4 补 pageNumber 元素。
- **文档收口（Phase 3）**：design.md §3/§4.2/§5/§7/§8/§10 六处漂移修正；docs/index.md 登记 print design.md；editor-core.md 补 StrictMode dispose 生命周期注记；research §3 补被推翻裁定注记。
- **验证状态（全绿）**：core 80/80（红测全转绿+回归不破）、renderers 72/72、全仓 test 72/72、typecheck/build/lint 39/39、print e2e 8/8（含 D21-04 真机行高数值闭合与 D21-03 聚合行可见断言）、check 14 项 exit 0 + i18n 基线一致。
- **下次关注**：①P5 closure audit（待独立子 agent）；②向用户汇报深审对标结论并请示 commit；③增强候选仍归后续：measure 真机实测注入、富文本/二级表头/groupBy、静默打印、矢量 PDF。

### 2026-09-06（续 3）— P5 closure audit round 1（issues→修复）

- **独立审计员（fresh session）实跑复核**：红测闭环做了变异验证（逐一回退 4 个分页修复后红测全部转红、恢复后 byte-exact 复绿）——红测真锁定缺陷非占位；四分页缺陷修复语义、打印闸门、P2 批、全量四项验证均 PASS。
- **驱出 5 必修 + 5 minor，已全部修复**：
  - F1（硬门禁）：inspector 边框输入消费的 `flux.print.inspector.borderWidth/borderColor` 未在 locales 定义 → 新增 2 个 undefined 红超出基线。补双语 key 后 i18n 与基线 diff 为空。
  - F4：table source 仍为 compile/exec 单 try（exec 异常误报 SYNTAX）→ 拆两段（compile→SYNTAX / exec→EVAL 带 message）。
  - F5：f-validate-skip 无覆盖 → 新增 `validate:false` 不抛用例。
  - F2/F3：design.md §3 renderers 清单补 editor/ 与 print-designer.tsx、修正扩展名；删除 §7 残留 PrintBackend 声称。
  - M1 compile 段 message 透传；M3 th 同预算字号（消 1.6mm 底部裁切）；M4 paperName 下拉改 PAPER_SIZE_PRESETS 派生；M5 Phase 1 状态收口。
- **修复后**：core 81/81（新增 skip 用例）、renderers 72/72、i18n 与基线 diff 为空、manifest-deps exit 0、typecheck 通过。
- **P5 closure audit round 2/3**：round-2 复审确认 F1/F3/F4/F5/M3/M4/M5 全部 PASS，但实证 F2（design.md §3 清单）与 M1（compileMessage 透传）声称修复而 live 未落盘（批量 python 替换静默漏改）——改用精确匹配修复并各补断言/清单；round-3 live grep 终审 **approved**（§3 清单与 live 树 1:1、M1 代码+断言双落实）。plan 标 `completed`。最终：core 82/82（红测全绿 + skip 用例）、renderers 72/72、全仓 72/72 task、四项验证 39/39。
- **教训**：本会话两次"声称已修但 live 未落盘"（P5 F2/M1、P2 round-2 F2/F3）均因 python 批量替换未命中而静默跳过——后续对计划/文档的批量编辑必须以 Edit 工具精确替换或替换后 grep 验证。

---

## 同日其他会话记录（master worktree 会话，合并保留）

> Plan: nop-app-erp `docs/plans/2026-09-03-1815-1-flux-unpublished-scope-tolerance-and-scheduling-bundle.md`（保护区跨仓库计划，Phase 1 双独立子 agent 批准在案）
> Bug: nop-app-erp `docs/bugs/2026-09-03-flux-runtime-datasource-formula-render-regression.md`

## What landed today

### Phase 2 状态恢复（工作树重放）

9-4 会话的 Phase 2 修复（当时已提交为 dangling `f6001dfbf` + WIP `5305adf1e` 后被 checkout master 丢弃）经 `git diff 5305adf1e~1 5305adf1e | git apply` 原样恢复到工作树（9 M + 3 ??，与 Phase 2 Exit Criteria 逐一对应；不提交，沿保护区不主动提交惯例，提交时机归仓库 owner）。

修复面（F1-F4）：

- **F1** `flux-formula/src/evaluate.ts`：`evaluateLeaf` 抛出路径依赖落盘（`finalize()` 移入 `finally`）
- **F2** `flux-runtime/src/async-data/formula-data-source-controller.ts`：公式 data-source publish 对 unpublished-scope sentinel（`Cannot access member of null or undefined`）容忍——state 转 `pending`、依赖订阅保留、不上报 host issue、不 rethrow；非 sentinel 维持现行为
- **F3** `flux-runtime/src/node-runtime.ts`：`resolveNodeProps` sentinel 引用稳定回退（防 `resolvedProps.value` 引用抖动）+ 依赖照常回写；**增补登记（2026-09-06 结束审计）**：meta 表达式（when/visible/className）与 props 同经 `evaluateCompiledValue`、mount 期同样命中 sentinel → 同文件对称 catch（sentinel→`undefined`，renderer 默认值语义生效；F1 部分依赖保持 meta 订阅、发布后自动重解析；不重复 dev warn）——ERP 修复计划 Non-Goal 预留条件触发后的按约定增量，专属单测见下行 F3 ×4
- **F3b** 上行 F3 meta 增量的单测归属：`runtime-node-props-unpublished-scope.test.ts` meta 用例 ×1（F3 合计 ×4 = props ×3 + meta ×1）
- **F4** `flux-bundle`：`flux-renderers-scheduling`（kanban/calendar/gantt/barcode-input）入 `registerDefaultFluxRenderers` 默认注册面 + `style.css` @import scheduling styles + package.json description 勘误（6→7 族）
- 新增单测 **9 个**（计数勘误 2026-09-06 结束审计，原记 8 漏计 meta 用例）：flux-formula F1 ×1；flux-runtime F2 ×3 + registry 恢复回路 ×1 + F3 ×4（props ×3 + meta ×1）（`__tests__/*-unpublished-scope*.test.ts`）；F4 断言并入 bundle 既有用例

### 验证（本日复跑）

- `pnpm --filter @nop-chaos/flux-formula test` → **214/214 绿**
- `pnpm --filter @nop-chaos/flux-runtime test` → **1440 passed + 1 skipped**
- `pnpm --filter @nop-chaos/flux test` → **8/8 绿**
- `pnpm typecheck` → 37/37 exit 0；`pnpm build` → 37/37（turbo cache 命中 = 9-4 已构建同内容）
- `dist` 实证：`KanbanBoard` ×6 于 `packages/flux-bundle/dist/index.js`；kanban/calendar 样式内联于 `dist/style.css`

### Phase 3 链路发布 + ERP 实证（nop-app-erp 侧）

- `import-flux-to-libs.sh` 重打 tgz（`KanbanBoard` 符号在 tarball dist 可检索 ×6）→ `rebuild-flux-chain.sh` 全链 → fresh-DB 重启 runner（8011，13s ready）
- ERP 探针 4/4 绿：`/crm-activity-timeline`（timeline-root 可见 + **零 console error**）、`/prj-task-kanban`（kanban-column ≥1）、`/hr-org-chart`（对照不回归）、`/crm-activity-calendar`（calendar 可见）
- `crud-pages.snapshot.spec.ts` **69/69 绿**（9.8m，零基线漂移）
- f13 全 spec **8/8 绿**（timeline ×2 + kanban ×3 + calendar ×2 + org-chart ×1）
- f16 目录级 11 passed / 1 failed / 2 skipped，逐例归因回写 bug note（autoBalance failed = `751749e17` 移除按钮后的 spec/view 漂移，非本根因；2 skipped = helper drawer→dialog slot 漂移，非本根因）
- `mvn test` 全量 **8012 tests / 0 failures / 0 errors / 2 skipped**（BUILD SUCCESS，15min）

### 文档义务（本日）

- `packages/flux-bundle/README.md` 注册面措辞 6→7 族勘误
- `docs/architecture/complex-pages.md` render-host 注册包列表补 scheduling
- `docs/architecture/flux-runtime-module-boundaries.md` 新增「Bundle Default Registration Face (2026-09-06)」节

## 备注

- **越界面登记（2026-09-06 结束审计补录）**：工作树内 `docs/analysis/2026-07-27-ma43-designer-office-e2e-test-audit/04-e2e-domain-pages.md` 存在本计划外的纯 markdown 表格/空白格式重排（零内容变更）——非本计划 Targets，移交仓库 owner 处置（保留或还原由 owner 决策）。
- `pnpm lint` / `pnpm check` 未复跑：工作树内容与 9-3/9-4 会话已验证内容逐字节相同（apply 的是同一 diff），当时实测 lint/check 预存红与干净树 `git stash` 对照逐字节相同（零新增命中，预存红注册规则在案）。
- 未提交：沿两保护区仓库惯例（未获明确要求不提交）+ nop-app-erp 侧上位契约及 0938-1 先例——flux 侧 AGENTS「全绿后提交」条款与本次移交惯例的偏离已在计划 Deferred But Adjudicated 节裁决，提交时机归仓库 owner。

## What's next

- 仓库 owner 决策提交 f6001dfbf 同内容工作树（plan Deferred：Successor Required = yes，触发条件 = owner 决策提交时）
- f16 残留（归 nop-app-erp 侧 successor）：autoBalance spec/view 漂移、helper drawer→dialog slot 失配、work-order 进度 tpl 空渲染（页面写法规范）
