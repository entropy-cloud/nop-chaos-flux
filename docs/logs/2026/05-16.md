# Development Log - 2026-05-16

### 2026-05-16

- Re-audited the `2026-05-16` deep-audit successor routing and split the original broad owners into single-surface plans. Updated `docs/plans/315-deep-audit-2026-05-16-owner-routing-plan.md` so the retained set now routes to `316`-`335` instead of keeping `318`-`323` as mixed umbrella buckets.
- Narrowed the original successors to one result surface each: `318` now owns spreadsheet command fidelity / edit-failure only, `320` owns bundle facade contract only, `321` owns active doc / terminology drift only, `322` owns focused proof gaps only, and `323` owns validation trigger / diagnostics fidelity only.
- Added successor plans `324`-`335` to absorb the previously mixed surfaces: spreadsheet shell styling + header semantics, compiler slot / deep-region parity, private package API surface cleanup, defensive type-boundary hygiene, Flow Designer host-page authoring contract, composite/tree accessible naming, runtime API cache identity, action error fidelity + debugger observability, the isolated Flow Designer palette perf residual, the workspace manifest hard gate, report-spreadsheet field-drop atomicity, and CRUD query submit sequencing.
- Recorded the prior-audit delta explicitly: `04-03` remains the strongest prior closure coverage gap, `06-04` is a prior out-of-scope discovery from Plan `295`, `06-01` remains under re-audit between coverage gap and sibling residual, and most remaining findings only became visible because this round added deeper lenses for host-page family parity, focused proof adequacy, accessible naming, cache identity, and public contract width.
- Routing-audit rationale: earlier 2026-05-15 closures were mostly honest on their original surfaces, but the first draft of `315`-`323` still grouped adjacent residuals by package family instead of by result surface. This pass corrected the plan ownership model without retroactively rewriting the earlier closure history.

### 2026-05-16

- Fixed the live `#/lab/array-field` regression where preloaded object rows rendered only delete buttons while `scope-debug` still showed the correct `members` data. `packages/flux-renderers-form-advanced/src/composite-field/array-field.tsx` now renders the item region with `scope: itemScope` explicitly instead of relying on ambient provider inheritance for the repeated child subtree.
- Added regression coverage in `packages/flux-renderers-form-advanced/src/composite-field/array-field.test.tsx` and `tests/e2e/component-lab/complex-form.spec.ts`, and recorded the diagnostic path plus root cause in `docs/bugs/55-array-field-playground-item-region-scope-fix.md`.

### 2026-05-16

- Fixed the live form `select` interaction path after Playwright exposed that the Base UI popup select did not commit `Role -> Admin` on the playground page. `packages/flux-renderers-form/src/renderers/input.tsx` now uses `@nop-chaos/ui` `NativeSelect` for the scalar `type: 'select'` renderer, with regression coverage in `packages/flux-renderers-form/src/__tests__/input-source-state.test.tsx` and updated E2E contracts in `tests/e2e/debugger*.spec.ts`, `tests/e2e/component-lab/simple-form.spec.ts`, and `tests/e2e/exploratory/keyboard-focus-and-teardown.spec.ts`.
- Updated `docs/components/select/design.md` to record `NativeSelect` as the current browser-interaction baseline for scalar form selects. Focused verification passed: `pnpm --filter @nop-chaos/flux-renderers-form exec vitest run src/__tests__/input-source-state.test.tsx src/__tests__/form-submit-actions.semantic.test.tsx src/__tests__/form-validation-rules.test.tsx`; focused Playwright debugger/select/domain zero-error cases passed.

### 2026-05-16

- Fixed playground surface form inputs snapping back to empty under React StrictMode by making `FormRenderer` owned form disposal defer past StrictMode effect replay; see `packages/flux-renderers-form/src/renderers/form.tsx`.
- Added focused regression coverage: `packages/flux-renderers-form/src/__tests__/form-field-handlers.test.tsx` and `tests/e2e/component-lab/surface-form-input.spec.ts`.
- Documented the rejected `modal={false}` workaround and the real StrictMode disposal root cause in `docs/bugs/54-surface-form-strictmode-dispose-fix.md`, and updated the renderer runtime cleanup baseline in `docs/architecture/renderer-runtime.md`.
- Focused validation passed: `pnpm exec vitest run "src/__tests__/form-field-handlers.test.tsx" --reporter=verbose --exclude "**/.stryker-tmp/**"` from `packages/flux-renderers-form`, and `PLAYWRIGHT_PORT=4183 npx playwright test "tests/e2e/component-lab/surface-form-input.spec.ts" --reporter=list --workers=1`.

### 2026-05-16

- Closed Plans `292`, `293`, and `294` on the live baseline after fresh independent closure audits and a full post-fix hard-gate rerun.
- Plan `292` final fixes: disabled remaining form-owner scope fallbacks in `packages/flux-renderers-form-advanced/src/{composite-field/object-field.tsx,detail-view/detail-field.tsx,detail-view/detail-view.tsx,variant-field/variant-field.tsx}` and `packages/flux-code-editor/src/code-editor-renderer/use-code-editor-binding.ts`, and fixed non-form `detail-field` overlay cleanup to clear `value-adaptation:${name}` source-local runtime overlays on success. Final focused proof passed via `pnpm --filter @nop-chaos/flux-code-editor exec vitest run src/code-editor-renderer/use-code-editor-binding.test.tsx` and `pnpm --filter @nop-chaos/flux-renderers-form-advanced exec vitest run src/detail-view/detail-revalidation.test.tsx`. Independent closure audit: `ses_1d3249dc0ffefgEavd8X1mvHE4`.
- Plan `293` final fixes: `packages/flux-runtime/src/form-runtime-validation.ts` now bridges submit `AbortSignal` into the per-field validation abort controller, `packages/flux-runtime/src/form-runtime-owner.ts` now treats `AbortError` as cancellation instead of publishing a synthetic field error, and `packages/flux-runtime/src/form-runtime.ts` / `packages/flux-core/src/types/runtime.ts` now keep `validateForm(reason, options?)` aligned on the real runtime path. Final focused proof passed via `pnpm --filter @nop-chaos/flux-runtime exec vitest run src/__tests__/owner-submit-contracts.test.ts src/__tests__/form-runtime-submit-flow.test.ts src/__tests__/owner-validation-lifecycle-contracts.test.ts`. Independent closure audit: `ses_1d3249d42ffeYYUBzLA3S8TEL4`.
- Plan `294` closure remained green on the already-landed row draft/action/subscription baseline, and the independent closure audit confirmed no remaining plan-owned blocker. Independent closure audit: `ses_1d34ca833ffeUvyKecS36FMTfA`.
- Fresh workspace hard gates all passed after the final Plan `292` / `293` closure fixes: `pnpm typecheck`, `pnpm build`, `pnpm lint`, and `pnpm test`. Final workspace test output is saved at `C:\Users\a758371\.local\share\opencode\tool-output\tool_e2ccecd63001Xvak230yPmU0l7`.
- Owner-doc decision for all three closures remains `No owner-doc update required`: the final changes converge the live implementation back to the existing documented validation-owner, submit-boundary, and data-row ownership baselines rather than introducing a new public contract.
