# Development Log - 2026-09-02

### 2026-09-02

- **批量改动拆 4 个独立 commit 入库（按 concerns 拆分，typecheck + build 验证通过）**：本日工作区累积 32 文件 modified + 2 new（1828+/499-），横跨 `format-registry` 新模块 / picker 渲染器重构 / UI button 配色变体 / 校验器扩展 / 文档类型再生 / 散落 renderer 字段等多个关注点。按独立性拆 4 commit，依依赖顺序逐个 verify 通过，无中间红状态。
  - **Commit 1 `646d16ba4` — `feat(flux-core+validation): format-registry + BASE_SCHEMA_FIELDS + url/integer/format/precision`**（13 files, +198/-3）。新增 `packages/flux-core/src/format-registry.ts`（`formatRegistry` 类 + `FormatValidator` 接口，含 i18n `messageKey` + `defaultMessage` 兜底）；`flux-core/constants.ts` 新增 `BASE_SCHEMA_FIELDS` Set（name/label/title）作为跨 renderer 共享基础字段；`flux-runtime/validation` 新增 `url`（`^https?://.+$`）/`integer`（`^-?\d+$`）内置 validator 与 `format` 注册表委派 validator；`buildValidationMessage` 分派 url/integer/format key 并从注册表解析 format 专属 messageKey；`flux-renderers-form/field-reading.tsx` formFieldContracts 新增 `format` union prop（email|url|integer）；`inputRendererDefinitions` 注册 `precision` 数字 prop；`createFieldValidation` 收集 url/integer/format 规则；`flux-renderers-basic/basic-renderer-definitions.ts` text 渲染器登记 `label`/`required`/`readOnly` 字段以承接 BASE_SCHEMA_FIELDS。`flux-compiler/shape-validation-utils.ts` 消费 BASE_SCHEMA_FIELDS。
  - **Commit 2 `18b70ec91` — `feat(ui+basic+data): semantic button variants + crud headerClassName/bodyClassName`**（4 files, +19/-0）。`packages/ui/button.tsx` 新增 `info`/`success`/`warning`/`danger`/`light`/`dark` 六个 cva 变体；`packages/tailwind-preset/index.ts` 补 `danger`（destructive 别名）与 `info` CSS 变量；`flux-renderers-basic/basic-renderer-definitions.ts` button literal union 加入六个新变体字符串；`flux-renderers-data/crud-renderer-definition.ts` 注册 `headerClassName`/`bodyClassName` prop 字段。
  - **Commit 3 `f2c033469` — `refactor(picker): extract picker-context + generalize surface via popup-config`**（12 files, +1093/-401）。新增 `packages/flux-renderers-form-advanced/picker-context.tsx`（`PickerContextProvider` + `usePickerContext` 钩子，按 cid 隔离每实例 selection/loaded-row/load in-flight 状态，替代原 `$_picker.*` scope key）；`picker-renderer.tsx` schema key 重命名 `valueKey`/`labelKey` → `valueField`/`labelField`（与 option-row/select 约定对齐），`pickerDialog` → `pickerPopup`（支持 `type: 'dialog'|'drawer'|'popover'`、size/placement/width/height/showMask/confirmText/cancelText 完整配置，默认 surfaceType='dialog'，placement 推导自 type：drawer→right、popover→bottom）；CRUD schema 工厂 `createPickerCrudSchema` → `buildDefaultPickerSchema`；`picker-dropdown.tsx` 改 surface-aware 渲染（dialog/drawer/popover 三壳共用 selection/confirm/cancel 语义，从 PickerContextProvider 取态而非 scoped selector）；`picker-helpers.ts` 删 `selectionToRowKeys`（已迁入 context 旁）、row-mapping helpers 跟随新命名；`composite-schemas.ts` PickerSchema 字段与字段规则与新命名同步；所有 picker 测试 fixture/断言同步（g1-g11 picker-upload / autofill scope dispose / crud-row isolation / label-resolve retry / picker-renderer / searchbox a11y）；`flux-guide/design-patterns/picker-transfer.md` 改写 surface config 节，文档化 popup-config 字段、drawer/popover 模式与新 per-row picker context 隔离契约。**ESLint pre-commit hook 抓到 3 处死导入**（`composite-schemas.ts` 的 `CrudColumnSchema`、`picker-renderer.tsx` 的 `getOptionLabelMap`/`rowToRecord`），就地删除并随 commit message 披露。
  - **Commit 4 `b25e3d17a` — `chore(docs+types): regenerate flux-types + sync variant/migration docs`**（5 files, +646/-81）。`flux-guide/flux-types/{index.ts, schema.d.ts}` 从当前包定义再生：捕获新注册的 BatchBarSchema/CommandPaletteSchema/KeyboardSchema/QueryFilterSchema/ResponsiveSchema/ResultSchema/SparklineSchema/StatTileSchema；FlexSchema/ContainerSchema 的 direction/align/justify/alignContent 收紧字面量 union（替换 SchemaValue）；PageSchema 加 extra/breadcrumb；ContainerSchema/FlexSchema 加 onClick。`docs/architecture/variant-vocabulary.md` 与新增 button variants（success/warning/info/danger/light/dark）及 tailwind-preset 语义令牌对齐；`flux-guide/09-amis-migration.md` 扩展迁移表（button 变体、picker valueField/labelField 重命名 + pickerPopup surface config、format-registry format 字段、url/integer validator）；`flux-guide/design-patterns/form-basic-fields.md` 文档化 input field 的 format prop + email/url/integer 字面量为 designer inspector 校验格式选择面。
- **遗留 `docs/analysis/2026-07-27-ma43-.../04-e2e-domain-pages.md` 表格重排：查实为反 prettier 格式噪音，弃置不入库**：09-01 披露的「纯 markdown 表格重排遗留」在本日拆分提交时原计划随 commit 4 收口，但实际 commit 4 只落 5 files（6 files staged → 5 files committed）。归因：该文件的改动方向是「对齐管道表格 → 紧凑管道表格」，与本仓库 prettier 配置的 markdown 表格规范化方向相反——commit 时 `prettier --fix` 直接把它格式化回与 HEAD 一致的内容，diff 归零被 lint-staged 静默剔除（ amend 重试同路径，husky 报 "prevented an empty git commit" 证实）。期间该文件一度被卷入 lint-staged automatic backup stash（commit 3 首次 lint 失败时创建），已从 stash 取出核实内容后弃置、stash drop。结论：与 09-01「不纳入提交」的处理一致，该文件保持 HEAD 态即为正确终态；后续会话不要再把它当作待提交遗留。
- **发现并发会话的半成品改动 `packages/flux-renderers-form-advanced/picker-helpers.ts`（未提交、未动）**：拆分提交收尾时工作区出现一个非本会话产生的实质性改动——`buildDefaultPickerSchema` 的 doc comment 改写并内嵌「user adjudication 2026-09-02: 行选择并不提交，最后是有一个确定才将选择的所有内容提交到下方」，行为面新增 `selection`（checkbox/radio + keepOnPageChange）、`selectionOwnership: 'scope'`、`selectionStatePath`/`dataStatePath`（`$_picker.${pickerId}.*`）、`autoClearSelectionOnRefresh: false`——把选择所有权从 commit 3 刚引入的 PickerContext（React Context 累积）改回 CRUD scope 管理的 `selectionStatePath`、Confirm 时一次性读取提交。该改动与 commit 3 的 picker-context 方案存在方向冲突，且疑似来自另一并发会话的进行中工作（含当日 adjudication 记录），本会话不代提交、不回滚，保持工作区原样留待其所属会话收口；后续收口时需同步 picker-renderer/picker-dropdown 的 Context 读取路径与 `flux-guide/design-patterns/picker-transfer.md` 的隔离契约描述。
- **验证状态**：按用户明确要求仅跑 `pnpm typecheck` 与 `pnpm build` 两个静态验证门（每 commit 提交后均 37/37 exit 0，无中间红状态）；未跑 `pnpm test`/`pnpm lint`/`pnpm check`/e2e。提交标题据实不写 `full-green verification`，仅以静态验证通过描述。
- **下次关注**：①picker 重构后既有 PickerSchema 字段重命名（valueKey/labelKey → valueField/labelField、pickerDialog → pickerPopup）属破坏性 schema 变更，需评估是否需要迁移脚手架与 changeset；②`flux-guide/design-patterns/picker-transfer.md` 同步已落，但 `flux-guide/flux-types/schema.d.ts` PickerSchema 类型未在本批次细化（仍可能为旧 `valueKey`/`labelKey` 形态），待 typecheck regen 任务统一捕获；③format-registry 当前仅 flux-runtime/validation 消费，flux-react/flux-renderers-form 侧的 `format` 字段错误展示链路尚无专门测试覆盖。
