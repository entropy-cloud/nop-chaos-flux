# Development Log — 2026-05-13

### 2026-05-13 (Plan 262 Execution)

- Executed `docs/plans/262-adversarial-review-2026-05-13-remediation-plan.md` — 6-phase fix for adversarial review confirmed live defects.
- Phase 1 (Dependency Tracking): Fixed formula-data-source-controller `start()` `.catch()` path to call `onDependenciesChange?.(undefined)`, ensuring first-publish failure doesn't permanently kill reactivity. Changed `scopeChangeHitsDependencies(change, undefined)` to return `true` (conservative, matching `docs/architecture/dependency-tracking.md`). Added focused test `formula-data-source-recovery.test.ts`.
- Phase 2 (Action Dispatch): Added `'cancelled'` to `ActionResultClass`. `classifyActionResult` now returns `'cancelled'` for cancelled/timedOut (not `'failure'`), so cancelled actions skip `onError` but still run `onSettled`. Moved `onActionStart` call inside try/catch. Wrapped `onError` dispatch in try/catch. Updated `runParallelActions` to treat cancelled results as failures for aggregate ok. Added `cancelled-class-and-error-guard.test.ts` with 5 focused tests.
- Phase 3 (Theme Token): Added `--destructive` and `--destructive-foreground` to all 5 CSS blocks in `packages/theme-tokens/src/styles.css` (`:root` + 4 theme variants), fixing invisible error text for non-playground consumers.
- Phase 4 (UI i18n): Added `setI18nGetter()` dependency injection to `packages/ui/src/lib/i18n.ts`, wired to `@nop-chaos/flux-i18n`'s `t()` in `packages/flux-bundle/src/index.tsx`. UI package remains zero-dep on `@nop-chaos/*`.
- Phase 5 (Scope Lifecycle): Added `dispose()` to host projection scope, called on old scope when `useHostScope` replaces it. Added `clear()` to `ModuleCache` interface and `createModuleCache()`. `runtime.dispose()` clears internally-owned moduleCache. Added `scope-lifecycle-leak-fix.test.ts`.
- Phase 6 (Error Boundary): Added `attemptKey` state to `SchemaRootErrorBoundary`; retry increments key and wraps children in `<Fragment key={attemptKey}>` to force `useMemo` cache bust for deterministic compilation errors.
- Verification: `pnpm typecheck` (26 packages), `pnpm build` (26 packages), `pnpm lint` (26 packages), `pnpm test` (all packages, 1048+ tests) all pass.

### 2026-05-13

- Independent closure audit `ses_1e12b2629ffe9LQ1VcHI4R6TKQ` re-reviewed Plans `254` and `255` after the successor split and confirmed they can now be marked `completed` because `09-05` was removed through the recorded 2026-05-13 renderer-contract scope change, `06-02` no longer reproduces, and the still-live retained subsets were moved into explicit successor ownership under `docs/plans/265-deep-audit-2026-05-13-reactive-owner-boundary-successor-plan.md` and `docs/plans/266-deep-audit-2026-05-13-async-lifecycle-owner-successor-plan.md`.
- Independent closure audit `ses_1e137c328ffeVBrqXeuqXvwgdD` re-reviewed Plans 256-261 after the successor split and confirmed `256`, `257`, `258`, `259`, `260`, and `261` are now consistent with `completed`; the earlier audit that only approved Plan 260 reflected the pre-reroute baseline and is now historical context only.
- Closed Plan 261 by removing whole-document spreadsheet `JSON.stringify(...)` comparisons from `packages/report-designer-renderers/src/page-renderer.tsx`, adding report-core sync-source tracking in `packages/report-designer-core/src/{core.ts,types.ts,core-dispatch.ts}`, and covering the behavior with focused tests in `packages/report-designer-core/src/__tests__/designer-core.test.ts` and `packages/report-designer-renderers/src/page-renderer-selector.test.tsx`.
- Closed the remaining low-risk live items in Plans 258 and 259 by tightening table pipeline typing in `packages/flux-renderers-data/src/table-renderer/{use-table-selection.ts,use-table-row-scope-cache.ts}` and replacing spreadsheet grid editing/row-header controls with `@nop-chaos/ui` primitives in `packages/spreadsheet-renderers/src/spreadsheet-grid.tsx`; `docs/architecture/styling-system.md` now documents the spreadsheet raw table shell as an explicit exception while keeping interactive controls on UI primitives.
- Re-routed the still-live structural/public-surface/styling residuals into explicit successor ownership under `docs/plans/262-deep-audit-2026-05-13-structural-owner-successor-plan.md`, `docs/plans/263-deep-audit-2026-05-13-public-surface-narrowing-successor-plan.md`, and `docs/plans/264-deep-audit-2026-05-13-layout-contract-and-theme-boundary-successor-plan.md`, allowing Plans 256/257/258/259/261 to reach closure-ready text state pending independent closure audit confirmation.
- Validation on the current snapshot: `pnpm typecheck`, `pnpm build`, and `pnpm lint` passed. Focused package tests for the touched areas passed, and long-run `pnpm test` surfaced an unrelated existing failure in `@nop-chaos/flux-action-core` (`classifyActionResult` expectation drift) outside the files touched in this slice.
- Re-audited successor plans `docs/plans/256-deep-audit-2026-05-12-module-boundary-and-test-hygiene-plan.md` through `docs/plans/261-deep-audit-2026-05-12-performance-redline-follow-up-plan.md` against the live repo, landed the still-live low-risk renderer/test/doc/a11y fixes, and synchronized each plan status with the current baseline.
- Closed the live contract/test drift owned by Plans 256/258/259/260: added Component Lab coverage/write proofs in `tests/e2e/component-lab/{coverage-manifest.ts,complex-form.spec.ts}`, stabilized tabs/CRUD semantic event payloads in `packages/flux-renderers-basic/src/tabs.tsx` and `packages/flux-renderers-data/src/crud-renderer.tsx`, tightened dataset/table typing in `packages/{word-editor-core,flux-renderers-data}/src`, replaced raw UI primitives in `packages/{nop-debugger,word-editor-renderers}/src`, and added explicit icon-button labels in `packages/word-editor-renderers/src/{toolbar/shared.tsx,preview/doc-preview-page.tsx}`.
- Closed the live doc-baseline drift owned by Plan 257 for `15-02`, `17-01`, `17-04`, `17-05`, and `17-07` in `docs/architecture/flux-formula.md`, `docs/architecture/flow-designer/config-schema.md`, `docs/references/flux-json-conventions.md`, `docs/components/condition-builder/design.md`, `docs/components/button/example.json`, and `packages/flux-code-editor/src/types.ts`; the broader retained items `02-15`, `03-01`, and the semantic-vocabulary adjudication for `17-03` remain outside this closure slice.
- Re-audit conclusion by plan after the successor split: `256`, `257`, `258`, `259`, `260`, and `261` are completed with explicit successor routing where needed; Plans 262-264 now own the remaining live structural/public-surface/styling residuals.
- Historical note: earlier audit `ses_1e307f9f2ffeE23XCxAh5o7whD` only approved Plan 260 before the successor-routing closure pass; that conclusion was superseded by `ses_1e137c328ffeVBrqXeuqXvwgdD` after the plans were rewritten to satisfy the closure guide.
