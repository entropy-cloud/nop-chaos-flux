### 2026-04-29

- Added two bug notes only for the hardest-to-diagnose issues from the current e2e recovery pass: a `detail-field` live dialog failure caused by a StrictMode-mounted async guard, and a report-designer demo inspector mismatch caused by missing spreadsheet-to-designer selection bridging.
- Link to docs: `docs/bugs/36-detail-field-strict-mode-mounted-guard-dialog-open-fix.md`, `docs/bugs/37-report-designer-demo-selection-bridge-inspector-stuck-on-sheet-fix.md`
- Key decision: record only bugs whose root cause lived in a different layer than the visible symptom and required live probing or cross-package comparison to isolate.
- Next step: continue Playwright closure work; add more bug notes only if later failures have similarly non-obvious causes.

### 2026-04-29

- Expanded `docs/skills/ai-tone-and-filler-review.md` after reviewing `docs/articles/` history and the attractor article's rewrite path. The checklist now covers not just sentence-level filler, but also article-level failure modes repeatedly seen in this repo: discussion residue, concept-definition lag, repeated hierarchy explanation across sections, weak grasp density, and the common mistake of treating “de-AI-ification” as “make it more formal”.
- Link to doc: `docs/skills/ai-tone-and-filler-review.md`
- Key decision: promote article-history lessons into the reusable skill itself, so future methodology/article rewrites can catch structural bad smells earlier instead of only cleaning single sentences late in the process.
- Next step: optional only — apply the expanded checklist to the attractor article again if we want one last publication-grade pass.

### 2026-04-29

- Tightened `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` after a full read focused on filler and repeated hierarchy explanation. The edit removes extra restatements around `状态空间 -> 吸引子 -> 轨迹 -> 控制`, compresses repeated `before` clarifications, and shortens several attractor/harness paragraphs that were explaining the same point twice.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: keep one clear explanation of logical priority and one clear explanation of the hierarchy, instead of restating the same order in multiple slightly different forms.
- Next step: optional only — one last read for rhythm if the article is about to be published externally.

### 2026-04-29

- Added `docs/skills/ai-tone-and-filler-review.md`, a reusable internal review checklist for identifying filler, discussion-style meta-writing, conversation residue, and obvious AI-tone patterns in publishable docs. The new skill distinguishes between lines that should be deleted, compressed, or explicitly preserved so editors do not accidentally remove theory-boundary sentences while cleaning prose.
- Link to doc: `docs/skills/ai-tone-and-filler-review.md`
- Key decision: place this under `docs/skills/` as an internal workflow/playbook rather than burying it in a single article edit. The checklist is meant to be reusable across articles, analysis notes, and discussion-to-essay rewrites.
- Next step: use this checklist on the attractor article itself and any future public-facing methodology pieces.

### 2026-04-29

- Removed another layer of discussion-style meta-writing from `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`, including lines like `这里最好先给一个正面定义` and similar author-aside phrasing. The article now states definitions and hierarchy claims directly instead of narrating the act of explaining them.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: treat phrases that comment on the writing itself as publishability noise. Keep theory and examples intact, but strip the “speaker on stage” voice wherever it is not carrying content.
- Next step: optional only — one final skim if we want to remove any remaining light discussion residue of the same kind.

### 2026-04-29

- Ran one final prose-polish pass on `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`. The edit trims several non-essential `不是……而是……` contrasts, keeps the article's Zhihu-facing rhythm tighter, and deliberately leaves the three anti-misreading attractor anchor lines untouched.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: treat this pass as surface-level rhetoric cleanup only. Do not reopen structure or theory after the positive attractor definition, repo-truth mapping, and logical-priority clarification were already landed.
- Next step: none required unless we later want title/subtitle packaging for actual external publication.

### 2026-04-29

- Tightened `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` again to close three remaining theory gaps: the article now gives `attractor` an explicit positive definition, adds a clearer bridge from the dynamical-systems metaphor into repo-level engineering terms, and states that `Attractor Before Harness` means logical priority rather than a one-time waterfall sequence.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: keep the article's existing Zhihu-facing structure and only harden the theory locally. Preserve the three anti-misreading anchor lines (`吸引子不是边界` / `不是更强的护栏` / `不是控制目标的另一种说法`) while making the positive definition and repo-truth mapping explicit earlier in the piece.
- Next step: optional only — one final prose pass if we want to reduce a few remaining non-essential `不是……而是……` contrasts without weakening the core theory.

### 2026-04-29

- Corrected the misleading “shared workbench baseline” framing in the Tiny-reference analysis and pushed the same boundary into active architecture docs. The analysis no longer sketches a pseudo-unified `DomainWorkbenchSchemaBaseline` object as if Flux lacked a definition. It now states the actual model: each `domain-host-renderer` family owns its own built-in default UI and explicit override surfaces, while shared Flux infrastructure only covers host-manifest / host-projection / namespaced-action / `WorkbenchShell` patterns. To prevent the same misread next time, `docs/architecture/renderer-runtime.md`, `docs/architecture/flow-designer/design.md`, `docs/architecture/report-designer/README.md`, and `docs/architecture/word-editor/design.md` now all state this boundary explicitly.
- Link to docs: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`, `docs/architecture/renderer-runtime.md`, `docs/architecture/flow-designer/design.md`, `docs/architecture/report-designer/README.md`, `docs/architecture/word-editor/design.md`
- Key decision: do not imply that Flux wants or needs one universal cross-designer workbench baseline protocol. If future tooling work happens, it should consume per-family override contracts rather than invent a second shared workbench schema model.
- Next step: optional only — if future planning resumes, phrase it as “stabilize per-family default UI + override-surface contracts” and avoid any new terminology that sounds like a global workbench baseline object.

### 2026-04-29

- Updated the active architecture docs that were actually behind the current host-page implementations. `docs/architecture/report-designer/design.md`, `docs/architecture/report-designer/config-schema.md`, and `docs/architecture/report-designer/api.md` now match the live `spreadsheet-page` / `report-designer-page` schema shapes and region support (`body`, `dialogs`, `statusPath`, `profile`, `adapters`, etc.). `docs/architecture/flow-designer/runtime-snapshot.md` now reflects the current `helpers.render(props.regions.*.templateNode, ...)` region render path instead of the older `region.render(...)` wording. `docs/architecture/word-editor/design.md` now records the live “built-in default UI + explicit override surfaces” behavior for toolbar/leftPanel/rightPanel instead of only mentioning a generic registry/manifest pattern.
- Link to docs: `docs/architecture/report-designer/design.md`, `docs/architecture/report-designer/config-schema.md`, `docs/architecture/report-designer/api.md`, `docs/architecture/flow-designer/runtime-snapshot.md`, `docs/architecture/word-editor/design.md`
- Key decision: only patch architecture docs that lagged behind landed code. Forward-looking docs that are intentionally ahead of implementation were left unchanged.
- Next step: optional only — later audit `docs/components/` owner docs for the same host pages to ensure their renderer-level contracts stay aligned with these architecture-doc updates.

### 2026-04-29

- Refined both Tiny-analysis documents again after checking the live host-page implementations more carefully. The previous correction still overstated the schema-driven side and could be read as if Flux wanted every visible workbench control to be declared by schema. The live code and owner docs show a more precise contract: host families use **built-in default UI plus selected schema/config override surfaces**. `designer-page`, `report-designer-page`, and `word-editor-page` all keep substantial default host UI while exposing specific override regions; Flow Designer node body/inspector/create-dialog/quick-actions remain schema-driven, but that does not mean every host control should become schema-defined. Both analysis docs now describe the model as “default UI + differential override”, not “all visible content should be schema-first”.
- Link to docs: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`, `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md`
- Key decision: the right generalization is not “schema-first visible composition everywhere”, but “clear built-in defaults plus explicit override surfaces owned by each host family”. This keeps Word Editor–style heavy product UI compatible with the same architecture without forcing everything through schema.
- Next step: if we later make implementation plans, phrase them as “stabilize default-vs-override contracts per host family” rather than “convert workbench UI to schema”.

### 2026-04-29

- Re-read `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md` end to end and did one more structural cleanup pass. This was not a broad rewrite, but it fixed the remaining high-visibility inconsistencies left by the earlier corrections: the article now says there are seven recommendation categories instead of six, section 4 no longer over-credits `host manifest` in its title, and the phase ordering now explicitly says the schema/config-first workbench-content baseline should be planned early but implemented after the earlier authoring infrastructure slices land.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`
- Key decision: the article as a whole is now structurally sound enough to keep; only surgical cleanup was needed, not a broad rewrite.
- Next step: optional only — if this article turns into execution work, split section 4 and section 6/7 into separate owner plans instead of carrying them as one umbrella recommendation set.

### 2026-04-29

- Synced the same correction into `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md`. The comparison report now explicitly states that for Flux workbench hosts, visible content is schema/config-driven, while `hostContract` / manifest only defines host boundary concerns (projection/capability/version validation). It now uses Flow Designer and Report Designer as concrete evidence: `designer-page.toolbar / inspector / dialogs`, `NodeTypeConfig.body / inspector.body / createDialog.body / quickActions`, and `report-designer-page` regions are rendered from `SchemaInput`, with hardcoded React content treated as fallback rather than the primary model.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md`
- Key decision: the comparison must not imply that Flux workbench hosts should move visible composition toward manifest/registry/preset ownership. The correct contrast is host-boundary clarity on the Flux side versus unified workbench/product assembly on the Tiny side.
- Next step: optional only — do one last repo-wide wording sweep later for any older analysis text that still loosely equates Flux host manifests with visible workbench composition.

### 2026-04-29

- Corrected the same Tiny-reference article again after re-reading the live Flow Designer contract. The earlier revision still underweighted a key existing Flux rule: visible workbench content is schema/config-driven, not manifest-driven. The article now explicitly states that `designer-page.toolbar / inspector / dialogs` and `NodeTypeConfig.body / inspector.body / createDialog.body / quickActions` are the primary visible-content path, while `hostContract`/manifest only defines the host boundary (projection/capability/version validation). It also now treats the current React fallbacks like `DesignerToolbarContent` and `DefaultInspector` as secondary bootstrap fallbacks rather than the main authoring model.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`
- Key decision: for Flux workbench hosts, “what is shown” belongs to schema/config; manifest belongs to boundary validation. Recommendations must preserve that split instead of suggesting preset/registry-driven visible composition.
- Next step: if we implement this direction, the first concrete plan should likely target replacing hardcoded default designer toolbar/inspector content with generated or loader-supplied schema/config baselines while keeping the same `designer-page` contract.

### 2026-04-29

- Corrected `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md` after a review caught a Flux-native framing error around workbench assembly. The article no longer describes the Tiny-inspired improvement as a new workbench registry/plugin model. It now maps the relevant Tiny ideas onto Flux's actual architecture split: `domain-host-renderer` + `RendererDefinition.hostContract` / manifest for static host contracts, `useHostScope` + namespaced `ActionScope` wiring for runtime host interaction, `WorkbenchShell` for visual shell reuse, and loader/workbench-layer assembly for default `toolbar / inspector / dialogs` composition.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`
- Key decision: in Flux, plugin-style workbench registry assembly is not the primary model and should not be revived by recommendation drift. Comparable Tiny ideas must be translated into Flux's existing manifest/host-boundary/workbench definitions instead of copied by name.
- Next step: if implementation follows, create narrow plans using the corrected Flux-native vocabulary: host-manifest/tooling consumption, owner-renderer shell convergence, property-panel resolver work, and Flow Designer authoring UX wiring.

### 2026-04-29

- Added `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`, a new decision-oriented article that narrows the Tiny comparison into a concrete improvement shortlist for Flux. The article filters out ideas that would violate the `Final Execution Schema` runtime boundary and keeps only implementable recommendations such as a workbench preset/registry layer, renderer-definition-based material metadata, a productized property-panel protocol built on `ResolvedAuthoringContract`, Flow Designer quick-action wiring, a tooling-only export pipeline, an iframe preview host, and selective controller-hook extraction for complex controls.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-actionable-reference-designs-from-tiny-engine-and-tiny-vue.md`
- Key decision: do not recommend “making Flux become tiny-engine”. Keep the actionable borrowings on the tooling/workbench/productization side, and keep dynamic execution, loader semantics, and core primitive boundaries unchanged.
- Next step: if one of these directions is chosen for implementation, split it into a narrow owner plan rather than bundling all six improvements into one umbrella effort.

### 2026-04-29

- Refined `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md` again to make the high-level verdict explicit when the question is specifically about design level, architecture level, and coding level. The report now states that Flux wins on architecture level, that design level splits by target (`execution-model design` vs `workbench/ecosystem/delivery design`), and that coding level should be read as roughly `flux core > tiny-vue > tiny-engine` within the visible repository evidence.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md`
- Key decision: encode the direct evaluative judgment into the report itself instead of leaving readers to infer it from the architecture/body sections, while still keeping the same evidence bounds and scope qualifiers.
- Next step: optional only — if needed, extract a shorter decision memo from the full report focused purely on architectural level and long-term platform direction.

### 2026-04-29

- Reworked the article structure in `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` so the core terms are defined before they carry argumentative weight. Moved the dynamical-system / attractor / trajectory / control framing and the Lorenz attractor figure up into the early concept section, then let the rest of the article build on that vocabulary.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: fix the article's explanatory order rather than just inserting definitions later. The Lorenz figure now supports the conceptual on-ramp instead of interrupting a repo-specific section after `attractor` had already been used heavily.
- Next step: optional only — one more polish pass if we want even tighter public-facing rhythm between sections two through five.

### 2026-04-29

- Updated `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` to add an explicit dynamical-system definition and a Lorenz attractor figure reference, aligning the article more closely with the PPT's explanatory sequence before the repo-specific mapping.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: strengthen the conceptual on-ramp with one canonical dynamical-systems example, but keep the article's main theory unchanged: the image illustrates trajectory-vs-structure, not a shift toward `boundary` or explicit target-region language.
- Next step: none required unless we also want to tune image placement or caption style for external publication.

### 2026-04-29

- Fixed the live `detail-field` dialog regression in component-lab by restoring its mounted guard after React dev/StrictMode remount cycles, and aligned the `tree-select` Playwright spec with the current `popover-trigger` DOM slot so the remaining `complex-form` e2e scenarios pass again.
- Link to code: `packages/flux-renderers-form-advanced/src/detail-view/detail-field.tsx`, `packages/flux-renderers-form-advanced/src/detail-view/detail-field-unmount.test.tsx`, `packages/flux-renderers-form-advanced/src/composite-field/object-field.tsx`, `tests/e2e/component-lab/complex-form.spec.ts`
- Key decision: keep the `detail-field` fix minimal by repairing the mount lifecycle guard instead of changing the shared dialog surface, and resolve the package lint regression by replacing render-time ref reads in `object-field` with an owner-keyed pending transform store.
- Next step: continue with the remaining non-`complex-form` Playwright failures and then rerun the full e2e suite.

### 2026-04-29

- Routed the attractor-methodology article from `docs/index.md` so it is now a first-class entry point for AI large-scale development questions about attractors, harnesses, owner-doc precedence, and convergence infrastructure.
- Link to docs: `docs/index.md`, `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: take the previously optional follow-up and make the article discoverable from the main docs router, without reopening the article's theory or structure.
- Next step: none required unless we later want a dedicated methodology subsection under `docs/articles/` or `docs/index.md`.

### 2026-04-29

- Rewrote `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` again after reviewing a stronger external draft. Kept its better Zhihu-style narrative flow, but corrected the key theory boundaries to stay faithful to the attractor discussion: `harness != guardrail`, `plan` is a local convergence device rather than a local attractor, `docs/architecture/` owner-doc precedence remains the first-order equation layer, and `state space -> attractor -> trajectory -> control` is now explicit.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: prefer the stronger public-facing narrative style from the new draft, but do not relax the first-order theory. The final article should remain readable while still resisting the same `boundary / guardrail / control-target` misreadings captured in `docs/discussions/2026-04-28-ai-development-attractor-model-discussion.md`.
- Next step: optional only — one final publication polish pass if the article will be published externally, but no more conceptual rewrites unless new source material changes the theory.

### 2026-04-29

- Finalized `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` after another independent dual review pass. The last revision reduced mid-article term density, added one general non-repo example, strengthened the repo-native "equation layer" claim for `docs/architecture/`, added the attractor-update step back into the loop, and tightened the ending for Zhihu-style publication. Two fresh subagent reviews both concluded there were no remaining blocker-level conceptual or publishability issues.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: stop revising once independent conceptual-fidelity and publishability reviews both converged on "good enough to stop"; avoid further churn that might reintroduce theory drift.
- Next step: only optional follow-up is routing the article from `docs/index.md` if the methodology piece should become a first-class docs entry point.

### 2026-04-29

- Deepened `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` after comparing it again against the PPT outline and the attractor-model discussion. The revision now explicitly adds the missing causal chain `repo truth -> generation/evaluation separation -> harness necessity`, hardens the repo-specific claim that `docs/architecture/` owner docs are the first-order attractor carrier, adds `state space -> attractor -> trajectory -> control`, and restores the point that new attractors are usually not produced naturally by current mainstream AI.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: align the article to the PPT/discussion at the level of first-order theory, not just wording. Keep the Zhihu-readable style, but make the repo mapping and concept hierarchy much harder and more explicit.
- Next step: if needed, run one final public-facing polish pass focused only on rhythm, title packaging, and example density rather than conceptual correctness.

### 2026-04-29

- Replaced the initial `tiny-engine`-only comparison with a deeper ecosystem-level report: `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md` now compares `nop-chaos-flux` against `tiny-engine + tiny-vue` together, after subagent review showed that `tiny-engine` alone under-described the real runtime/material/component stack.
- Link to doc: `docs/analysis/2026-04-29-nop-chaos-flux-vs-tiny-engine-tiny-vue-ecosystem-deep-comparison.md`
- Key decision: treat `tiny-engine` as the designer/workbench and material/codegen half, `tiny-vue` as the component/renderless/theme/runtime half, and analyze the combined ecosystem across architecture, core processing logic, dataflow/reactivity, material protocol, code generation, runtime hosting, AI friendliness, security, performance, and organizational tradeoffs.
- Next step: if needed, extract this report into a smaller decision memo focused on one narrower question such as runtime-kernel architecture, codegen strategy, or AI-operable low-code debugging.

### 2026-04-29

- Refined `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` toward a Zhihu-style article: less academic framing, stronger opening hook, clearer storytelling, and tighter explanation of the `attractor / harness / guardrail` hierarchy without relying on prior discussion context.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: treat the article as a readable methodology essay rather than a formal paper; keep the core theoretical order intact while using more direct prose, shorter argumentative loops, and concrete contrasts.
- Next step: if needed, add one more pass to tune the article for public-facing publication rhythm and title/subtitle packaging.

### 2026-04-29

- Rewrote `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md` after identifying that the previous draft did not actually explain harness engineering. The new version centers the article on the title's real claim: attractor defines direction first, harness engineering provides the execution scaffold that keeps local signals aligned with that direction.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: stop using the discussion replay as the article's backbone; instead define `attractor`, define `harness`, map both onto the repository's owner-doc and convergence mechanisms, and explicitly distinguish harness from narrower `guardrail` language.
- Next step: if needed, add this article to `docs/index.md` as the preferred entry when discussing AI methodology, owner-doc precedence, and convergence infrastructure together.

### 2026-04-29

- Added a methodology article clarifying why AI large-scale development should be framed as an attractor problem before a guardrail problem, based on `docs/discussions/2026-04-28-ai-development-attractor-model-discussion.md` and the current architecture-doc precedence chain.
- Link to doc: `docs/articles/attractor-before-harness-ai-large-scale-development-methodology.md`
- Key decision: treat `docs/architecture/` owner docs and their precedence structure as the primary carrier of the repository's attractor definition; describe `plan / lint / check / test / audit / closure` as derived convergence mechanisms rather than first-order principles.
- Next step: if needed, split `docs/architecture/` further into a clearer "attractor core" reading path versus narrower local-contract docs.

### 2026-04-29

- Executed plan 152: Tree Renderer Normalization. Replaced Button usage in `TreeNodeRenderer` (visual tree) with pure `<span>`/`<div>` elements, and added `aria-label` + `role="button"` + `tabIndex` to `TreeOptionNode` chevron spans (input-tree / tree-select) for accessibility. Updated all tree-related unit tests to work with the new DOM structure.
- Key files: `packages/flux-renderers-data/src/tree-renderer.tsx`, `packages/flux-renderers-form-advanced/src/tree-controls.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/form-tree-control-source-states.test.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/form-tree-checkbox-fields.test.tsx`, `packages/flux-renderers-form-advanced/src/__tests__/form-double-edit-regression.test.tsx`
- Key decision: expand/collapse chevrons use `<span role="button">` with `aria-label` and `tabIndex` for keyboard accessibility, matching AMIS reference structure of plain div/span without Button component overhead. Text rows use plain `<div>` instead of `<Button variant="ghost">`.
- Next step: update plan 152 status and run closure audit.
