# Development Log - 2026-08-20

### 2026-08-20

- **全仓模块实现代码检查 mission 完成（`docs/audits/check/`，37 份报告 + roadmap + SUMMARY）**：
  - 用户要求：检查各模块实现代码是否存在问题，先拟 roadmap 再逐个检查，结果存 `docs/audits/check/`，**不修改任何代码**。全程保持只读（git 状态仅 `docs/audits/check/` 新增，`packages/`、`apps/`、配置零改动）。
  - 路线图：`docs/audits/check/flux-check-roadmap.md`（8 维检查维度 D1-D8、P0-P3 严重级别、A-J 十批次覆盖 36 包 + playground、状态表全部 done）。
  - 基线工具证据（`00-baseline-tooling.md`）：typecheck/lint 全绿；**两处偏离 DV 08-09 基线**：
    - `pnpm check` exit 1 —— `flux-renderers-layout/wizard-renderer.tsx` 716 行，2026-08-12（4152e4840）引入的未注册 ERROR 级超限回归。初版误归因 playground 两个 WARN 档文件（提取日志时 grep 子串 `red` 误匹配路径中的 "sha**red**"），34 号审计质疑后按直接复跑修正。
    - `pnpm test` exit 1 —— `flux-renderers-basic` `surface-event-ctx.test.tsx` 1 用例失败，经 05/06/12/10 四轮跨包裁定，root cause 定位 `use-surface-renderer.ts:213-223`（受控 surface 字面量 `open:true` 分支 dialog 永久卸载）。
  - 总量：**P0 x22（去重后约 18 条独立链）/ P1 x113 / P2 x206 / P3 x296，共 637 条 finding**，每条含 file:line + 摘录 + 影响推理 + 修复方向。
  - 重点 P0（详见 `SUMMARY.md`）：spreadsheet `sortRange hasHeader` 清空表头行；word-editor 只读页可编辑且篡改被持久化；pivot `JSON.stringify` 签名丢函数致过滤器永久失效；formula 混合 optional 链抛错 + `$Date.now()` 编译期折叠；mobile pull-refresh 阻断长列表垂直滚动；Gantt 网格虚拟化冻结；graph 滚轮缩放失效；dashboard resize 方向反转；AI 流式渲染在 React Compiler 下整体 bail out；FormRenderer 渲染期同步写 store（tearing 风险）；flow-designer viewport 每帧污染 undo 历史；设计器 demo 尺寸硬编码两处（100×26 / 30×10）。
  - 跨包主题链（修复按链处理）：渲染期副作用、非确定性折叠、shallowEqual 语义、XSS URL 守卫、undo 污染家族、i18n 回退链死代码、受控生命周期、门禁盲区——均在 SUMMARY.md 分组并给出 root-fix 归属包。
  - 方法论：每包一个 fresh 只读子代理（general-purpose），批次 A→J 依赖层顺序推进，后批引用前批结论做跨包核实/反误报（例如 05 修正 01 的 shallowEqual 消费链、13 排除 12 的三条模式适用性）。
  - Next step：修复走独立 plan（受保护区域 plan-first），优先级建议已写入 `SUMMARY.md`；最先恢复门禁的两项 = wizard-renderer 拆分 + use-surface-renderer 受控 open 分支修复。
