# Development Log - 2026-07-06

- **flux `@dict` / `@page` loading + `xui:roles` (plan 451 completed)**: Added flux-native support for nop-entropy-backed remote loading and schema permission filtering, decoupled into two concerns (loader `url→json` + pre-compile `json→json`).
  - **Design doc**: `docs/architecture/flux-page-dict-loading-and-precompile.md` (registered in `docs/architecture/README.md`). Plan: `docs/plans/451-flux-page-dict-loading-and-precompile-plan.md` (draft → independent sub-agent review → active → executed → independent closure audit `approved`, 0 gaps → `completed`).
  - **Phase 1 — contract (`flux-core`)**: `RendererEnv` (`packages/flux-core/src/types/renderer-api.ts:64`) gained four optional hooks — `pageProvider: FluxPageProvider`, `dictProvider: FluxDictProvider`, `hasRole?(role)`, `locale?` — plus `DictBean`/`DictOptionBean` types. All default-absent so existing integrations are unaffected. Backward-compat contract test in `renderer-env.test.ts`.
  - **Phase 2 — loaders + global caches (`flux-runtime/src/special-url/`)**: `loadFluxPage` (page LRU 50, locale-keyed, in-flight de-dup) + `loadDict` (dict TTL 20s, locale-keyed). Errors not cached. `clearPageCache`/`clearDictCache`/`configure{Page,Dict}Cache` exported. 16 proof tests (TTL expiry with fake timers, LRU eviction+recency, in-flight de-dup, locale isolation, no-provider error, empty-locale fallback).
  - **Phase 3 — `@dict:` ajax dispatch**: `fetchWithSpecialUrlDispatch` in `packages/flux-runtime/src/async-data/request-runtime.ts` intercepts `@dict:` before `env.fetcher` at both fetcher sites; returns `dict.options` so `select` sources need no `responseAdaptor`. Intentionally NOT `async` (sync-first) so non-`@dict` fetcher timing is unchanged → all dedup/cancel tests still pass. Survives `runtime.setEnv()` (reads `getEnv()`). `splitSpecialPrefix` in `special-url/dispatch.ts`. 10 proof tests.
  - **Phase 4 — `xui:roles` plugin (`flux-runtime/src/plugins/xui-roles-plugin.ts`)**: `createXuiRolesPlugin({ hasRole })` returns a `RendererPlugin.beforeCompile` that prunes denied nodes + strips the key + prunes nested children. Allow-all no-op when `hasRole` absent. Reuses the existing `applyBeforeCompilePlugins` chokepoint — no compiler changes. 12 proof tests.
  - **Phase 5 — playground + docs**: `crud-demo` rewritten — removed the hand-rolled `@dict:` fetcher branch; env now carries a mock `dictProvider` + `hasRole` + `locale`; `SchemaRenderer` receives `createXuiRolesPlugin`. Added `xui:roles` demo nodes (admin-visible + superadmin-pruned). Owner docs updated: `renderer-runtime.md`, `module-cache-and-import-stack.md`, `flux-runtime-module-boundaries.md`.
  - **Verification**: `pnpm typecheck` 55/55, `pnpm build` 29/29, `pnpm lint` 29/29. `flux-runtime` 1315 passed (+1 skipped). 40 new tests total. The only `pnpm test` failure (`apps/playground/src/schema-examples.test.ts`) is pre-existing and unrelated (reads `docs/examples/user-management-schema.md`, untouched by this work; confirmed via git history by the closure auditor).
  - **Key decision**: `@page` scope = loading chain only (A), not amis-style in-schema dynamic sub-page (B). `xui:roles` lives in `beforeCompile` (covers all schema sources), not in the page loader. Caches are process-global (shared across runtimes); dict uses short TTL (DB-editable), page uses LRU (version-stable).
  - **Deferred (cross-repo successor)**: nop-chaos-next wiring — replace its bespoke `fetchFluxPage` (currently throws on non-local paths) with flux's `loadFluxPage`, and wire `pageProvider`/`dictProvider`/`hasRole`/`locale` to `@query:PageProvider__getPage` / `@query:DictProvider__getDict` / auth store / i18n. Tracked in the plan's `Deferred But Adjudicated`.
