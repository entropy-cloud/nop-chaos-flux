# Development Log - 2026-08-09

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2229-3` Cycle 2 / I2 不变式驱动审计执行收口——red list 确定性确认 + 双轮对抗探查 + cycle2-findings 零悬挂）

- **背景**：执行 plan `docs/plans/2026-08-09-2229-3-cycle2-i2-invariant-driven-audit.md`（Cycle 2 / I2：跑 ⑥-⑩ 门禁 → red list 确定性确认（== Cycle 2 / I1 注册面）+ 双轮对抗探查 + `cycle2-findings.md` 零悬挂 + W1-W4 复核，纯文档计划）。前置 Cycle 2 / I1 已收口（⑥-⑩ 门禁以 `it.fails` 预期失败 + 扫描器注册红落库）。
- **Phase 1（门禁全量运行，red list 确定性确认）**：`pnpm check:ai-engine-invariants` standalone **exit 1 = 4 注册命中**（⑧×1 `create-engine.ts:228` connector-missing 早退 + ⑥×3 `use-conversation.ts:280/:370/:427` create/delete/clearAll 不 bump）——与 Cycle 2 / I1 注册表**逐条对应，零未注册**；`pnpm --filter @nop-chaos/flux-renderers-ai test` **67 files / 544 passed + 12 expected fail**（⑥-⑩ it.fails 按预期失败套件绿、①-⑤ 全绿、零 unexpected-pass）；`pnpm check` exit 1 **仅 = 既有登记 red**（industrial 6 hits + oversized 2 exempt locale），链在工业 red 中断 → standalone 复跑补证（对齐 Cycle 1 先例）；`pnpm test` 60/60 全绿（基线佐证）。**确定性结论：门禁表达的违背面 == 注册面，零差异。**
- **Phase 2（双轮对抗探查 + 覆盖矩阵 + W1-W4 复核）**：执行 session 轮（probe A-F + 观察 E，7 项全部临时 vitest 复现，RED 6 + 记录 1，`round-01-executor.md`）+ 独立 fresh session 轮（P1-P7 全部 RED，`round-02-independent.md`，round-02 独立复核排除 ⑧ 新泄漏路径、⑥×⑦ 交错被注册成员覆盖等）——12×⑥-⑩ 覆盖矩阵零悬空格（uncovered=0，partial 格全部已探查）；**发现分类零悬挂 = K 13 条（全部已知族门禁漏覆盖 → I3 补门禁）+ N 0 条（零新族，不触发 Cycle 3 派生）+ 新 W 4 条**；Cycle 1 W1-W4 以 I6 复查结论为锚全部维持 watch-only（复触发条件无变化；cycle1 round-02 #9 create+switch exists 静默 no-op 并入 K-⑥-2 组合根因）。
- **Phase 3（findings 落档 + 收口）**：`docs/audits/ai-invariants/cycle2-findings.md` 零悬挂落档——**已知族 13 条**：K-⑩×5（abort-before-first-chunk 空残留进下一请求历史（probe A）/ onBeforeRequest rejection 留 loading 幽灵（probe B）/ 残留经 autoSave 持久化跨会话存活（P3）/ regenerate×失败销毁旧回答 + 残留（P4）/ 零 chunk connector completed 轮空残留（P6））、K-⑥×3（同 tick clearAll+create+delete 幽灵 activeId fixup（probe C）/ 同 tick delete+switch 提升已删目标（probe C2）/ bootstrap 选中 active 无引擎（P7））、K-⑦×1（bootstrap 在途 clearAll 复活（probe F））、K-K4/②×2（delete / clearAll 镜像写面 → storage 幽灵（probe D / P1））、K-K3/④×1（create 元数据不入排空链 → storage 幽灵（P2））、K-⑨×1（abort 变体 onTurnEnd rejection → host promise reject（P5））；**新族零**（双轮全部落入既有族，本轮不派生 Cycle 3）；**watch-only 新 4 条**（W-E onAfterRequest 对 aborted 轮触发 / W-⑨-a connector-missing 生命周期不对称 / W-⑨-b bootstrap-active 无 autoSave / W-⑨-c clear 不清 lastError）+ Cycle 1 W1-W4 维持；未触发候选族登记保持（streaming backpressure / tool-execution 并发）。roadmap Cycle 2 / I2 行 `todo` → `✅`（附执行证据）+ 动态状态区更新；本 daily log 落档；临时探查文件全部删除零残留（工作区仅 docs 变更）。
- **验证**：`pnpm check:docs-garbled` exit 0（新增/修改 docs——findings + roadmap + daily log + round-01/02——零新增候选，既有 likely-garbled 全为既有文件）；纯文档计划按 guide 纯文档条款，typecheck/build/lint/test 不属本 plan 门禁（AI 包 focused 面 + `pnpm test` 基线已复跑全绿）；commit `docs(ai-invariant-loop): plan-2026-08-09-2229-3 Cycle 2 I2 审计收口——red list 确定性确认 + 双轮探查 + cycle2-findings 零悬挂`（仅 stage 本 plan 相关文件）。
- **偏差**：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用）。Closure audit 由独立 fresh sub-agent 执行后勾选 Closure Gates；Plan Status 随 closure-audit 通过转 `completed`。
- **Closure Audit（独立 fresh session，2026-08-09）**：独立 closure-audit sub-agent 复核 live repo 后 APPROVED——`pnpm check:ai-engine-invariants` 复跑 4 注册命中（⑧ `create-engine.ts:228` + ⑥ `use-conversation.ts:280/:370/:427`）零未注册；AI 包测试复跑 67 files / 544 passed + 12 expected fail 与 findings 记录一致；`pnpm check` 复跑仅既有登记 red；findings `文件:行` 抽查与 live 代码一致；cycle2-findings 零悬挂（13K/0N/4W）与 roadmap 行 ✅、daily log 一致；临时探查文件零残留；docs-garbled 通过。零 Blocker / 零 Major。plan Closure Gates 7/7 [x] + Closure 节证据回填 + Plan Status → `completed`；本 daily log 收口补账。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2229-2` Cycle 2 / I1 第二批门禁 ⑥-⑩ 沉淀执行收口——it.fails + 扫描器注册红 + 登记处同步）

- **背景**：执行 plan `docs/plans/2026-08-09-2229-2-cycle2-i1-invariant-sedimentation.md`（Cycle 2 / I1：N1-N5 五个新失败族 → 第二批不变式门禁 ⑥-⑩ 沉淀）。前置 I6 已收口（roadmap Cycle 2 三行派生回写）。**本 plan 只沉淀门禁**（不变式陈述 + 参数化穷举测试 + 静态扫描器规则 + committed 回归 + 登记处同步）；N1-N5 的**代码修复 = Cycle 2 / I4 职责**（test-first 先红后绿），门禁以「预期失败」形态落库（vitest `it.fails` + 扫描器注册红），套件保持全绿。
- **Phase 1（⑥ / N1 active 位移完整性）**：RED 证据确认——5 成员场景参数化测试（switch 在途 × delete/clearAll/create 位移、同 id 快速重 switch hydration 丢失、delete active 后 next 引擎 null 悬挂）以 `it` 书写实测**全 RED**（probe-1/1b/1c/B/C 断言失败原因逐一核对：B 引擎复活 / clearAll 后提升 / X 引擎被覆盖 / 存储消息 0 条 hydrate / activeEngine null 悬挂）→ 翻转 `it.fails` × 5 套件绿；扫描器规则 `scanDisplacementVersionBumps`（位移方法 create/delete/clearAll 函数体必须含 `switchVersionRef.current` bump；初版漏 async 前缀 → deleteConversation 未命中，补 `(?:async\s+)?` 后命中 3/3）——**live 命中 = 3**（create :280 / delete :370 / clearAll :427）注册红；committed fixtures：清洁 fixture 三位移方法补 bump（防 ⑥ 规则反扫既有清洁面）+ ⑥ 违规 fixture（无 bump → exit 1）；catalog §9 ⑥ 条落档。
- **Phase 2（⑦ / N2 bootstrap 合并）**：RED 证据——gated loadConversations + create X → bootstrap resolve 后列表被覆盖为 [A]（X 出列、activeId 悬空）2 场景实测全 RED → `it.fails` × 2 套件绿；不静态化（functional updater 行为面）；catalog §9 ⑦ 条落档。
- **Phase 3（⑧ / N3 branch 戳泄漏）**：RED 证据——regenerate('branch-1') + connector-missing 早退 → 下一正常 turn assistant 被戳 `branchId:'branch-1'`（泄漏）实测 RED；消费控制场景（regenerate 有 connector → 戳被再生轮消费、下一轮干净）**live 即绿**——按 it-fails-unexpected-pass 失败路径反证，该场景以普通 `it` 提交（消费契约正向控制），泄漏场景 `it.fails`；扫描器 `scanBranchStampReset`——**静态可行性 live 验证通过**（pendingBranchId/runTurn/runOnce 同属 createMessageEngine 闭包作用域，同函数扫描 sound）；扫描/豁免面逐一裁决：isProcessing 入口早退（`create-engine.ts:206-208`）设戳路径不可达（regenerate 设戳前自有 isProcessing 守卫 + runTurn 设戳后同步调用）→ 豁免记录理由；connector-missing 早退（`:210-229`）可达 → 扫描目标——**live 命中 = 1**（:228）注册红；committed fixtures：⑧ 违规（早退无清除 → exit 1）+ ⑧ 清洁（清除存在 → exit 0；含单行 guard 形态，扫描器补同行 `if(...) { return; }` 识别）；catalog §9 ⑧ 条落档。
- **Phase 4（⑨ / N4 plugin 错误隔离）**：RED 证据——临时 vitest probe 实测 live 行为（onTurnStart rejection → sendMessage reject + `isProcessing=true` 卡死；onError 抛错 → reject + 状态写入跳过（processing 卡死）；onTurnEnd rejection → reject（遮蔽原错误，lastError 已先写入 connector-boom））与 findings §3.2 probe-4/probe-E/静态证据**逐条一致**（临时文件已删零残留）→ `it.fails` × 3 套件绿；不静态化（plugin 回调交错行为面）；catalog §9 ⑨ 条落档。
- **Phase 5（⑩ / N5 失败轮残留污染）**：RED 证据——首轮 connector 抛错（placeholder 以 `loading=false, content=''` 提交）→ 次轮请求历史携带空 assistant 实测 RED → `it.fails`；正常轮控制场景（完成的 assistant 保留在历史、无空块）live 即绿 → 普通 `it`；不静态化（buildContext 排除谓词行为面）；catalog §9 ⑩ 条落档。
- **Phase 6（登记处同步 + 全量验证 + 收口）**：`gates.md` 追加 ⑥-⑩ 五行（覆盖方法 × 检测方式 × 运行命令；棘轮状态 = 预期红（待 Cycle 2 / I4）；⑦⑨⑩ 注明不静态化理由；⑥⑧ 注明扫描器规则名 + 注册命中数）+ 新「注册红」节（4 命中逐条：⑥×3 + ⑧×1，含 live 行号）；`engine.md` §Invariants 追加 Cycle 2 节（⑥-⑩ 摘要 + 注册红说明 + 运行命令更新）+ Failure Path「已知违背面（待 I4）」节；**表完备性门禁核对**：12 方法集零漂移（完备性测试 3/3 全绿，新不变式列不新增公共方法行）；**文件拆分**（AGENTS.md 500 行评估）：conversation-invariants.test.ts 782 行超 `check:oversized-code-files` 700 错误线 → ⑥⑦ 块按职责拆出 `conversation-invariants-cycle2.test.ts`（276 行），Cycle 1 文件回 522 行，gates.md/catalog/engine.md 引用同步。
- **验证（全绿，it.fails 预期失败语义 + 注册红口径）**：`pnpm typecheck` 33/33、`pnpm build` 33/33、`pnpm lint` 33/33；`pnpm test` 60/60 tasks（AI 包 67 files / **544 passed + 12 expected fail** = 556 tests，含 invariants 套件 engine 25 + conversation 13 + cycle2 7 全绿 + 全部回归）；`pnpm test:scripts` 8 files / **39 tests**（+⑥⑧ fixtures 3 例）；`pnpm check` exit 1 **仅 = 既有登记 red**（`check:audit-event-dispatch-ctx` industrial 6 hits，2026-08-09 已登记移交 industrial workstream；`check:oversized-code-files` 2 exempt locale）+ **本 plan 注册的 ⑥⑧ 扫描器命中 4**（`check:ai-engine-invariants` 单独复跑：`create-engine.ts:228` ⑧×1 + `use-conversation.ts:280/:370/:427` ⑥×3，与注册表逐条对应）——零未注册新增；`check:docs-garbled` 候选 16 全为既有文件（本 plan 修改/新增 docs 零新增候选，含 daily log pre-existing "façade"）；`check:oversized-code-files` 无 700+ 新增（cycle2 拆分后 276 行）。
- **偏差**：⑧⑩ 的「正常消费/控制」场景按 live 实测以普通 `it` 提交而非 `it.fails`（若按字面 `it.fails` 提交将立即触发 it-fails-unexpected-pass 失败路径使套件红，违反「套件保持全绿」硬约束；`it.fails` 仅用于 live 违反的成员，I4 修复后翻转）——理由记录本 daily log + plan Phase 3/5 备注。
- **commit**：`feat(ai-invariant-loop): plan-2026-08-09-2229-2 Cycle 2 I1 第二批门禁 ⑥-⑩ 沉淀（it.fails + 扫描器注册红）`（对齐 I1/I4 commit 先例）；closure-audit 由独立 fresh sub-agent 执行后勾选 Closure Gates。

### 2026-08-09（ai-invariant-loop mission driver：REVIEW_PLANS 轮——5 份 BI 实现计划独立 fresh-session 审查通过，`draft` → `active`）

- **背景**：mission driver 流程 REVIEW_PLANS 步（visit #4，22:42-22:55）对 5 份 `draft` 状态 BI 实现计划各派独立 fresh-session review 子 agent 审查（零 Blocker / 零 Major 共识后提升）。
- **各计划审查结果**：
  - `2026-08-09-bi-kpi-filter-chart-enhance-plan.md` → verdict `pass`（3 Minor 就地修正：Closure Gates 补 `pnpm check` + `docs/logs/` 收口记录；playground 示例项落点明确为 `data-route-entries.ts`）。
  - `2026-08-09-dashboard-editor-with-editor-core-plan.md` → `pass-with-minors`（1 Major：Phase 3 palette + Phase 4 example 依赖 `pivot-table`（仍 draft）与 `stat-tile` 的跨计划前置未显式化——已加显式依赖与降级预案；1 Minor：`ScadaEditorSession` 单 `undoStack` 字段表述修正）。
  - `2026-08-09-map-openlayers-wrapper-plan.md` → `pass`（`map-data` 资源落位裁定：包内 `src/map-data/*.json` 静态模块，不另立 workspace 包；vitest.config.ts + playground 接线入 INV 审计 checklist G 硬项；INV-1 边界注明 basemap 瓦片加载属 OL 库内部管线，渲染器不直调 fetch）。
  - `2026-08-09-pivot-table-vtable-wrapper-plan.md` → `pass`（1 Major：Non-Goals 路径 A 归属引用不实——已修正为分析报告 + 独立立项，不再指向 KPI 计划）。
  - `2026-08-09-sparkline-component-plan.md` → `pass`（1 Major：Phase 1 `(Follow-up)` stat-tile 复用约定前向依赖 Phase 2 design.md——改写为 Phase 1 内可完成；1 Minor：Non-Goals 视觉复杂度表述歧义修正）。
- **计划状态**：5 plan 均 `draft` → `active`，`## Draft Review Record` 已回填。无代码变更；本 daily log 记录入档。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2229-1` Cycle 1 / I6 收口执行——统计落档 + W1 实证复查 + Cycle 2 三行派生回写 + roadmap 同步 + 独立 closure-audit）

- **背景**：执行 plan `docs/plans/2026-08-09-2229-1-cycle1-i6-closure-and-cycle2-derivation.md`（Cycle 1 / I6 循环收口与下一轮触发判定，纯文档计划）。前置 I0-I5 全 ✅（I5 full-green 基线在案）；I3 裁决的 N1-N5（5 个新失败族）触发证据齐备。
- **Phase 1（Cycle 1 统计 + W1 收敛复查）**：
  - **统计核对（对照 I1/I4 交付物逐项，数字全对应）**：新增不变式门禁 = **5 类（①-⑤）+ I4 加性扩展**（② sync 读取 / ③ 成功路径守卫 / ④ 时序守卫 / ⑤ 强制终结——扫描器 2 新规则 `scanCompletionIdentityGuard` + `scanAdapterSyncClosureReads` + 运行时参数化扩展 + committed 回归 6 用例，`gates.md` 五行全绿）；**red list = 0**（I2 实证 `check:ai-engine-invariants` exit 0 + 12×5 覆盖矩阵零 uncovered 悬空格）；**新族 = 5**（N1-N5，findings §3.2 触发证据齐备）。
  - **W1 实证复跑（临时 vitest 文件 `w1-i6-probe.tmp.test.ts`，3/3 passed，复跑后已删零残留；未触碰任何 committed 测试文件）**：abort→(clear|setMessages)→再 abort 双路径均复现——二次 abort 仍把 idle clobber 回 `'aborted'`、abortController 残留非空（**controller/processingState 窗口仍在**）；陈旧轮 unwind 后 finally 身份守卫归 null controller（**K2 已收敛 generator 残留面/自愈面**）。
  - **W1 裁决 = 维持 watch-only**（不满足移除条件）；findings §4 W1 行追加「I6 复查（2026-08-09）：K2 已收敛 generator 残留面，controller/processingState 窗口仍在」实证证据。
- **Phase 2（稳态判定 + Cycle 2 派生回写）**：稳态判定 = **非稳态**（N1-N5 五个新失败类，findings §3.2 + adjudication §3 N 表齐备）→ 不标记「稳态暂停」，按 Loop Rule 派生 Cycle 2；roadmap `ai-invariant-loop-roadmap.md` Work Item Status 表追加 3 行——**Cycle 2 / I1（不变式沉淀，第二批门禁 ⑥-⑩）** + **Cycle 2 / I2（不变式驱动审计）** + **Cycle 2 / I3（发现裁决与工作项拟制）**，各行附触发证据引用（findings §3.2 `文件:行` + adjudication §3 N 表不变式陈述，行号加注「按 cycle1 findings §3.2 原文（2026-08-09 I2 时点）」）：N1 active 位移完整性（`use-conversation.ts:293-346/:348-372/:390-423/:263-291`，5 成员 probe 全 RED）/ N2 bootstrap 列表覆盖（`:221-229`，probe-2 RED）/ N3 branch 戳泄漏（`create-engine.ts:100/:385/:542-566`，probe-3 RED，**登记候选族 branching/fork 触发**）/ N4 plugin 错误隔离（`create-engine.ts:240-242/:324-341/:459-478/:350-352`，probe-4/probe-E RED，**登记候选族 plugin 生命周期触发**）/ N5 失败轮残留污染（`create-engine.ts:459-478/:482-487`，probe-D RED）——三行触发证据与 findings §3.2 / adjudication §3 逐条核对（ID 对应、行号一致、不变式陈述原文引用）**零重复造数据**；Cycle 2 plans（`2026-08-09-2229-2` / `-3`）已在对应行关联引用（执行序 = 文件名 {N} 序）；动态状态区注释更新（Cycle 1 全闭环 + Cycle 2 已派生）+ Follow-up Backlog 节同步（W1 维持 watch-only 结论；W2-W4 维持登记）。
- **Phase 3（收口同步）**：本 daily log 落档；roadmap I6 行 `todo` → `✅`（附执行证据：plan 路径 + 本 daily log + 独立 closure-audit 引用）；`pnpm check:docs-garbled` exit 0（本 plan 新增/修改 docs——roadmap + findings + daily log——零新增候选，15 个 likely-garbled 全为既有文件，含 daily log:256 pre-existing "façade"）；`pnpm test` 60/60 tasks 全绿（纯文档零代码变更，I5 full-green 基线 cached 复验）；commit `0ae711c6` `docs(ai-invariant-loop): plan-2026-08-09-2229-1 Cycle 1 I6 收口 + Cycle 2 派生`（仅 stage 本 plan 相关 4 文件）。
- **closure-audit**（独立 fresh sub-agent，执行 session 不自审）：task `ses_018f2057dffeJmWQX182ImvLfJ`，verdict **approved**（零 Blocker / 零 Major；3 Minor 均为收口表述类——daily log 措辞随 finalization 成立 / handoff 计数笔误 / plan commit 时序——无内容影响），9 项 checklist 全 PASS（文本一致性 / 统计准确性 / 触发证据零造数 / W1 裁决诚实 / roadmap 同步 / deferred 诚实 / docs-garbled / 工作区零残留 / interface-vs-semantics）；证据落 plan Closure 节。
- **plan 状态**：Phase 1/2/3 全 completed + 全 checklist [x] + Closure Gates 7/7 [x] + Plan Status → `completed`（closure-audit 通过后 finalization 轮翻转 + commit `0ae711c6` 后续补账）。
- 偏差：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用；纯文档计划按 guide 纯文档条款，typecheck/build/lint/test 从 Closure Gates 移除，`pnpm test` 复跑仅作基线佐证）。

### 2026-08-09（ai-invariant-loop mission driver：Cycle 1 / I6 + Cycle 2 / I1 + Cycle 2 / I2 三份 plans 起草，独立 fresh-session 审查全部 pass-with-minors（零 Blocker/Major）→ 转 `active`）

- **背景**：roadmap `docs/backlog/ai-invariant-loop-roadmap.md` Cycle 1 仅剩 I6 `todo`（I0-I5 全 ✅）；I3 裁决的 N1-N5（5 个新失败族，触发证据齐备）按 Loop Rule 预授权派生 Cycle 2。本轮按执行序起草 3 份 plans（每 work item = 一 plan，roadmap Rule）：
  - `docs/plans/2026-08-09-2229-1-cycle1-i6-closure-and-cycle2-derivation.md`（Cycle 1 / I6：统计门禁 5 类 + I4 扩展 / red list 0 / 新族 5 + W1 收敛实证复查（I4 Follow-up 交接）+ 稳态判定（非稳态）→ roadmap 追加 Cycle 2 / I1 + I2 + I3 三行附触发证据 + I6 行 ✅；纯文档 plan）
  - `docs/plans/2026-08-09-2229-2-cycle2-i1-invariant-sedimentation.md`（Cycle 2 / I1：N1-N5 → 新不变式 ⑥-⑩ 沉淀——参数化穷举测试以 `it.fails` 预期失败形态落库（套件保持全绿）+ 静态扫描器规则 ⑥ `scanDisplacementVersionBumps` / ⑧ `scanBranchStampReset`（静态可行性 live 验证，降级路径在案）+ 注册红显式登记（⑥×3 + ⑧×1）+ committed fixtures + catalog §9 / gates.md / engine.md 同步；修复归 Cycle 2 / I4，本 plan 只沉淀门禁；code plan）
  - `docs/plans/2026-08-09-2229-3-cycle2-i2-invariant-driven-audit.md`（Cycle 2 / I2：跑 ⑥-⑩ 门禁 → red list 确定性确认（== 注册面零差异）+ 双轮对抗探查（执行轮 + 独立 fresh session 轮）+ `cycle2-findings.md` 零悬挂 + W1-W4 复核（以 I6 复查结论为锚）；纯文档 plan）
- **起草期 live 实证（全部核对一致）**：N1-N5 触发证据行号 post-I4 漂移复核——`use-conversation.ts`（createConversation :280-313 不 bump / switch :322 唯一 bump / delete fixup :382-388 null 悬挂 / clearAll :427-475 不 bump / bootstrap :243 整体覆盖）+ `create-engine.ts`（connector-missing 早退 :210-229 戳未消费 / onTurnStart :247-249 try 外 / onError :336-340 先于 mutate / onTurnEnd :362-364 无守卫 / runOnce catch :484-504 留空 placeholder / buildContext :511 只排 loading）；W1 窗口复核（abort :534-555 不重置 controller/processingState）→ I6 预期裁决「维持 watch-only、K2 已收敛 generator 面」以实证复跑为准。
- **Plan Review（每 plan 独立 fresh session，零 Blocker/零 Major 达成共识）**：I6 `ses_0190cb866ffeWYTIeRs1OFPdJS` pass-with-minors（4 minor：触发证据行号锚点加注 / W1 复跑钉死临时 vitest / catalog §9 待新建标注 / W2-W4 Successor Path 补键）；Cycle2-I1 `ses_0190c9a6dffeb3axQVnZeJjp44` pass-with-minors（4 minor：⑥ 豁免句归属修正 / ⑧ 扫描-豁免面显式化 / Phase 6 item-type 依据注明 / Phase 6 验证与 Closure Gates 关系说明）；Cycle2-I2 `ses_0190c8368ffek1C6pvisRvz5gh` pass-with-minors（4 minor：roadmap I2 行前置条件显式化 / catalog §9 前向引用标注 / W1 锚点改 I6 结论 / ⑧ 静态可行性前提加注）。全部 minor 已修订并回填 `## Draft Review Record`。
- **计划状态**：三 plan 均 `draft` → `active`（审查共识达成）；执行序 = 文件名 {N} 序（I6 → Cycle 2 / I1 → Cycle 2 / I2）。无代码变更；本 daily log 记录入档，commit 由各 plan 执行收口轮负责。

### 2026-08-09（plan `455-table-input-table-doc-gap-and-footer-plan` 执行收口：table.md 5 节文档补齐 + combo-input-table.md 表达式派生列范例 + input-table footer 插槽 + 独立 closure-audit 两轮 pass）

- **背景**：执行 plan `docs/plans/455-table-input-table-doc-gap-and-footer-plan.md`（Timesheet 周网格调研共识报告的「后续动作」——table 文档缺口 + input-table footer 对齐 AMIS InputTable）。
- **Phase 1（table.md 文档补齐）**：`flux-guide/design-patterns/table.md` 增 §8 树形表格（`rowChildrenField`/`childrenSource` 懒加载/展开折叠示例，注明 `defaultExpanded`/`maxDepth` 未接线 TableSchema、`expandable` 是展开行特性非树形）、§9 表头分组（`columns[].children`，colSpan/rowSpan 由 `computeHeaderRows` 推导不可手写）、§10 footer、§11 quickEdit（inline 常驻输入/dialog/`saveImmediately` 失焦提交/行级 draft 保存条/`quickSaveItemAction`+`includeScope`）、§12 合计行（`prefixRow`/`affixRow` + `TableSummaryCell{column,value,align}`，`SUM(ARRAYMAP(...))` 表达式示例），置于「table vs crud 选型」前；头部死指针 `flux-types/schema.d.ts` → `packages/flux-renderers-data/src/schemas.ts`（review 轮 minor m4）。
- **Phase 2（combo-input-table.md）**：增「派生列/只读列（item region 表达式）」节（`{type:"text", text:"${qty * price}", readOnly:true}`，注明无需 compute 机制）+ footer 文档节 + InputTable 字段参考表 footer 行。
- **Phase 3（input-table footer 插槽，Fix + Proof）**：`InputTableSchema.footer?: SchemaInput | string`（composite-schemas.ts，类型对齐 `TableSchema.footer`）；`input-table-renderer.tsx` fields 增 `{ key: 'footer', kind: 'value-or-region', regionKey: 'footer' }`（对齐 data-renderer-definitions.ts:231）+ 渲染 `hasRendererSlotContent(resolveRendererSlotContent(props, 'footer'))` → `<div data-slot="input-table-footer">` 置于根容器最末、缺失零渲染（对齐 table-renderer.tsx:121,692-693）；`__tests__/input-table-renderer.test.tsx` 增 footer 3 用例（SchemaInput 渲染/字符串渲染/缺失零渲染 + 零回归）。
- **验证（全绿）**：`pnpm typecheck` 33/33、`pnpm build` 33/33、`pnpm lint` 33/33、`pnpm test` 60/60（form-advanced 135 files / 1053 tests，含新增 footer 用例）；`pnpm check` exit 1 仅既有登记 red（`check:audit-event-dispatch-ctx` industrial 6 hits，2026-08-09 已登记移交 industrial workstream），链上其余 13 项 + `check:ai-engine-invariants` 单独复跑全 exit 0，**零新增命中**；`check:schema-prop-coverage` exit 0（footer 字段覆盖）。
- **closure-audit**（独立 fresh sub-agent，执行 session 不自审）：首轮 `ses_01929f88fffe5INEBsRITRkXO8` verdict `revise`（1 Major + 2 Minor，全部修复——M1 懒加载 action scope 为顶层 `record`（use-table-lazy-children.ts:60 `createScope({ record, rowKey })`）非 `$slot.record`，示例改 `${record.id}`；m1 footer DOM 序在分页栏之下；m2 item region 参数为 `index`/`value`（`$slot.index`），`$index` 不存在）；re-audit `ses_0191a5f8bffeWTOp1uv6yxjF9Q` verdict **pass**（零新增 finding）。证据落 plan Closure 节。
- **plan 状态**：Phase 1/2/3 全 completed + 全 checklist [x] + Closure Gates 9/9 [x] + Plan Status → `completed`。
- 偏差：无（plan front matter 无 `> Work Item:` 行、无 `> Source Audits:` 行——roadmap 翻转与审计状态翻转均不适用；工作区另有其他 session 的未提交文件（bi-control 分析 + 4 份新 plan 草稿），本计划未触碰）。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2007-3` I5 全量验证执行收口——full-green 基线记录 + roadmap I5 行 ✅ + commit 显式 full-green 声明）

- **背景**：执行 plan `docs/plans/2026-08-09-2007-3-cycle1-i5-verification.md`（Cycle 1 / I5 全量验证与门禁零命中，纯验证计划）。前置 I4 已收口（plan `2026-08-09-2007-2` completed + closure-audit PASS_WITH_MINORS），本 plan 开工。
- **Phase 1（全量静态验证）**：`pnpm typecheck` **33/33 exit 0**；`pnpm build` **33/33 exit 0**；`pnpm lint` **33/33 exit 0**；`pnpm check` **exit 1 仅既有登记 red、零未登记新命中**——`check:audit-event-dispatch-ctx` 6 hits（`flux-renderers-industrial/src/binding/` animator.ts ×4 + point-store.ts ×1 + refresh-pipeline.ts ×1，2026-08-09 已登记移交 industrial workstream）+ `check:oversized-code-files` 2 errors 全为 EXEMPT 豁免（en-US.ts/zh-CN.ts locale，2026-08-07-1053-1 裁决），链上其余 11 项检查（react19/src-artifacts/active-doc-code-anchors/package-css-exports/flux-bundle-pack/i18n-keys/workspace-manifest-deps/schema-prop-coverage/scada-symbol-keys/audit-suspects/audit-renderer-browser-io）全部通过；`check:ai-engine-invariants`（I4 扩展后）链尾被 industrial red 中断，**standalone 复跑 exit 0 零命中**（对齐 I2/I4 先例）。
- **Phase 2（全量测试验证）**：`pnpm test` **60/60 tasks exit 0，33 包 12,246 passed / 0 failed**；`pnpm --filter @nop-chaos/flux-renderers-ai test` **66 files / 542 tests exit 0**（含扩展后 invariants 套件：engine-invariants 15 + conversation-invariants 13 全绿 + 全部回归）；`pnpm test:scripts` **8 files / 36 tests exit 0**（含 I4 追加 committed 回归）；**AI 面 e2e（`tests/e2e/ai-*.spec.ts` 13 文件）47 passed / 0 failed / 0 skipped exit 0**（46.4s）；watch-only 终态清单（gantt-perf ×2 + kanban-perf ×1，60Hz 环境阈值不可达）登记核对在案、按 plan 不属本 phase 复跑面。
- **Phase 3（full-green 记录与收口）**：本 daily log 落档；roadmap `docs/backlog/ai-invariant-loop-roadmap.md` I5 行 `todo` → `✅`（附执行证据：plan 路径 + 本 daily log 引用）；`pnpm check:docs-garbled` exit 0；commit 标题显式 **`full-green verification`**（AGENTS.md 纪律原文短语，`docs(ai-invariant-loop)` 对齐 I1/I2 先例）。
- **full-green 口径（显式注明）**：本 claim 覆盖**全量单测**（typecheck/build/lint 33/33 + `pnpm test` 12,246/0 + AI 包 542/0 + `pnpm test:scripts` 36/0 + `check:ai-engine-invariants` 零命中 + `pnpm check` 零新增命中）**+ AI 面 e2e**（47 passed / 0 failed）；全仓 e2e 的 **3 条 watch-only failed（gantt-perf ×2 + kanban-perf ×1）为登记终态**（60Hz 环境阈值不可达），不属本 claim 反例。
- **偏差**：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用）。Closure audit 由独立 fresh sub-agent 执行后勾选 Closure Gates；Plan Status 随 closure-audit 通过转 `completed`。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2007-2` I4 修复执行收口——K1-K4 修复 + 门禁 ②③④⑤ 补强 + 清扫总表 + bug notes 121-124）

- **背景**：执行 plan `docs/plans/2026-08-09-2007-2-cycle1-i4-fix-execution.md`（Cycle 1 / I4 修复执行：实例 + 类别清扫 + 门禁补强）。前置 I3 已收口（K1-K4 = P0/P1 裁决 + 门禁补强契约 ③/⑤/④/②）。现场交接：Phase 1（K1）已完成并勾选；Phase 2（K2）代码+测试已在工作区但 plan 未勾选（prior run 未收账）；Phase 3/4/5 未开工。
- **Phase 2（K2）补账**：K2 代码（`activeGenerator` 句柄 + chunk 循环每迭代 `signal.aborted` 检查 + `abort()` `generator.return()`）+ 门禁 ⑤ 测试已在工作区——补 RED 证据：临时移除每迭代检查与 `generator.return()` 后 K2 测试断言 `(b)` 迟到 chunk 被 commit → RED；恢复修复 → GREEN。清理 prior run 遗留 debug `console.log`（engine.test.ts）。**两处既有测试依赖旧（有 bug）行为**：`engine.test.ts`「abort → content retained」与 `use-message.test.tsx`「abortRequest sets aborted」固定等 2 个微任务后 abort，实际 chunk 在 abort 后才被消费 → K2 检查使其内容为空而失败——按「保留意图、加固时序」修订：轮询至 chunk 实际 commit 后再 abort，断言精确 pre-abort 内容（`toBe('partial')`，同时验证迟到 chunk 被抑制）。`MessageEnginePlugin` 需 `name` 字段 + `AiConnectorChunk` 缺 import + `generator.return()` TS 参数数 → 修复（`name: 'test-*'`、import 补齐、`return({})` 空 chunk 满足签名）。K2 设计裁定「永不 yield 的 generator = connector 契约违背」记录（统一由 Phase 5 写入 engine.md）。
- **Phase 3（K3）修复**（test-first）：RED 测试两例（conversation-invariants.test.ts 不变式④ K3 块）——(a) in-flight `saveMessages`（gate 延迟落盘）先于 delete 启动、晚于删除 resolve → 断言 storage 无 ghost；(b) processing 中 `clearAll` + 迟释放 save gate → 断言 aborted 快照不落盘、storage 终态为空。修复前两例全 RED → 修复：① `pendingSavesRef` per-conversation 在途 save 串行化（attachAutoSave 链式 `.catch(()=>{}).then(save)`，错误路由 `reportStorageError` 保持）；② 按签名分机制排空——`deleteConversation`（异步）`storage.deleteConversation` 前 `await Promise.allSettled`；`clearAll`（同步 `(): void` 签名不变）storage 清空链到 `drain.then(...)` 之后（per-id fan-out 各自 `.catch`，④ 路由保持）；③ `clearAll` `detachEngine` 先于 abort 循环（abort 触发的 auto-save 回调不得再启动新 save）。修复后 GREEN。
- **Phase 4（K4）修复**（test-first）：RED 测试（同 tick create+rename → storage 保存 title 必须为 'T2'，修复前 persisted='T1' 全 RED）→ 修复：`renameConversation` 改读 `conversationsRef.current`（状态变更前）+ 同步 map 回写；`createConversation` 同步 prepend `conversationsRef.current`（对齐 `activeIdRef` 同步更新）。修复后 GREEN + **live 扫描器零命中**（此前 `renameConversation:377` 裸读被 K4 规则 flags）。committed 回归追加：清洁 fixture 扩展（K1 守卫 + K4 ref 读取 + switch 首语句豁免样例）+ ②-K4 违规 fixture（rename `setConversations` 后裸读 → exit 1）——`pnpm test:scripts` 36/36 全绿。
- **Phase 5 收口**：类别清扫总表入档（plan 新节：A 终态写入守卫 8 处逐条核对（connector-missing/tool-no-executor/abort() 三处记录无窗口理由，其余全守卫）；B generator 消费单一消费面全覆盖；C storage 全部调用点核对（saveConversation×2 记录元数据单次写不入排空链理由）；D 列表变更方法 ref 同步核对（switch :316 首语句豁免））；`invariant-catalog.md` §7 新增 ②③④⑤ 扩展契约 + §8 引用追加；`gates.md` 门禁清单 ②③④⑤ 行扩展（含扫描器规则名 + committed 回归 6 用例）；`engine.md` §Invariants 同步 + **Failure Path「永不 settle connector = 契约违背」**；bug notes **121-124** 落档（K1 完成 mutate 守卫 / K2 abort 强制终结 / K3 save-after-delete ghost / K4 rename sync 闭包）。
- **验证（full-green）**：`pnpm typecheck` 33/33、`pnpm build` 33/33、`pnpm lint` 33/33、`pnpm test` 60/60 tasks 全绿（AI 包 **66 files / 542 tests**，engine-invariants 15 + conversation-invariants 13 全绿）；`pnpm test:scripts` 8 files / 36 tests 全绿；`check:ai-engine-invariants`（扩展后）**live 零命中**；`pnpm check` 仅既有登记 red（`check:audit-event-dispatch-ctx` industrial 6 hits，2026-08-09 已登记移交 industrial workstream），**零新增命中**；`check:docs-garbled` exit 0（14 个 garbled 候选全为既有 industrial/archive 文件，本 plan 新增文档零候选）。
- **偏差**：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用）。Closure audit 由独立 fresh sub-agent 执行后勾选 Closure Gates 第 6 项；Plan Status 随 closure-audit 通过转 `completed`。

### 2026-08-09（ai-invariant-loop mission driver：build 验证轮——pre-existing 红修复 `check-package-css-exports` stale literal 17→19，`pnpm test:scripts` 34/34 全绿）

- **背景**：ai-invariant-loop mission driver「verify build passes」验证轮执行 `pnpm typecheck`/`build`/`lint`/`test`（全绿）+ `pnpm test:scripts`（I5 plan Phase 2 验证面）。
- **发现与修复**：`pnpm test:scripts` 唯一失败 = `scripts/__tests__/check-package-css-exports.test.ts:20` stale literal（断言 17 目标，live 19）——即 I1/I2 closure-audit 已登记的 F1 minor（industrial-hmi 合并引入 2 个新 CSS export，测试文件未同步，**非本 mission 引入**，clean HEAD 复现确认 pre-existing）；按 driver「诊断 → 修复 → 复验」规则修正断言 17→19（对齐 2026-08-07 同测试 11→17 附带修复先例，保留测试语义）。
- **复验**：`pnpm test:scripts` 8 files / 34 tests 全绿；`pnpm --filter @nop-chaos/flux-renderers-ai test` 66 files / 536 tests 全绿；`pnpm check:ai-engine-invariants` exit 0 零命中；`pnpm check` 仅既有登记 red（`check:audit-event-dispatch-ctx` industrial 6 hits，2026-08-09 已登记移交 industrial workstream），**零新增命中**；`pnpm check:docs-garbled` exit 0；`pnpm typecheck`/`build`/`lint`/`test` 全绿。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-2007-1` I3 发现裁决执行收口——13 条目零悬挂裁决表 + roadmap 回写意图 + 翻转待 closure-audit）

- **背景**：执行 plan `docs/plans/2026-08-09-2007-1-cycle1-i3-adjudication.md`（Cycle 1 / I3 发现裁决与工作项拟制，纯文档计划）。前置 I2 已收口（`cycle1-findings.md` 零悬挂）。裁决输入 = findings K1-K4（已知族门禁漏覆盖）/ N1-N5（新族触发证据齐备）/ W1-W4（watch-only），共 13 条目。
- **Phase 1 裁决基准核对（Proof + Decision）**：findings 全部 `文件:行` 与 live 源码逐条勾对**零不一致**（K1 `create-engine.ts:318-323` 完成 mutate 仅判状态字符串 + `:240-242` onTurnStart try 外；K2 `:417-429` chunk 循环无 per-iteration 检查 + `:509-519` abort 不持 generator；K3 `use-conversation.ts:183-196` fire-and-forget save vs `:394-398` abort 先于 `:399` detachEngine；K4 `:374-388`/`:377` 闭包读取；N1-N5/N2 触发证据行号全吻合）——findings 零改动，无修正痕迹需记录；判级标准钉死 = `component-audit-checklist.md:56-59`（P0 数据丢失/存储损坏 / P1 契约漂移/交互缺陷 / P2 UX·文档·测试 / P3 nits），裁决表格式对齐 `cr-inventory-adjudication.md` + `round2-dr-adjudication.md` 先例。
- **Phase 2 逐条裁决（Decision + Proof）**：产出 `docs/audits/ai-invariants/cycle1-adjudication.md`（新建，零悬挂）：
  - **K1-K4 → 路由 I4**（plan `2026-08-09-2007-2`，附门禁补强契约）：K1 = **P1**（1757 同型路径复活，交互缺陷）；K2 = **P1**（loading 永久幽灵 + controller 泄漏，交互缺陷）；**K3 = P0（诚实判级）**——已删会话消息被 in-flight save 重落盘 = 持久化数据幽灵，命中 checklist P0「数据丢失/存储损坏」判据；**K4 = P0（诚实判级）**——rename 静默不持久化 = 持久化数据丢失，命中 P0 判据。路由不受影响（P0/P1 均进 I4）。
  - **N1-N5 → Cycle 2 / I1**（Loop Rule 预授权派生，非延期）：每条附触发证据引用（findings §3.2 `文件:行` + 不变式陈述）+ 派生路径（I6 回写职责）；N3（branch 戳泄漏）/N4（plugin 错误隔离）为 I0 §3 登记候选族（branching/fork、plugin 生命周期）正式触发。
  - **W1-W4 → watch-only 维持**（复触发条件引用 findings §4）；W1 的 abortController 残留面注明「I4 附带收敛预期」（K2 修复面覆盖时 I5/I6 从 watch-only 移除，不作承诺）。
  - **P2/P3 = 零项**（13 条目全落入三态，无 P2/P3 区间条目 → Follow-up Backlog 无填充项）。
  - **零悬挂核对**：13 = K1-K4 ×4 + N1-N5 ×5 + W1-W4 ×4 逐条 ID 一一对应；findings §5 未触发候选族（streaming backpressure / tool-execution 并发）×2 按设计排除（保持登记）。
- **Phase 3 一致性核对与收口（Decision）**：roadmap `ai-invariant-loop-roadmap.md`——动态状态区注释记录 I3 回写意图（plan 路径 + closure-audit 引用），**I3 行 `todo` → `✅` 翻转动作按 I0/I1/I2 先例留待 closure-audit 通过后执行（不提前）**；Follow-up Backlog 节按 Phase 2 结果填充（零 P2/P3 项 + W1-W4 稳态期 watch-only 项）；本 daily log 收口记录。
- **验证**：纯文档计划（guide 纯文档条款，`pnpm typecheck/build/lint/test` 从门禁移除）；`pnpm check:docs-garbled` 待收口轮复跑；工作区仅 docs 变更（`cycle1-adjudication.md` 新建 + roadmap/log 同步）。
- **plan 状态**：Phase 1/2/3 全 completed + 全 checklist [x]；Closure Gates 1-6 核对在案、第 7 项（独立 closure-audit）由独立 fresh sub-agent 执行后勾选；Plan Status 待 closure-audit 通过后转 `completed`。
- 偏差：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用；roadmap 行翻转按计划留待 closure）。

### 新增 plan `455-table-input-table-doc-gap-and-footer-plan`（draft → active，双轮独立审查共识）

- **背景**：nop-app-erp Timesheet 周网格调研共识报告 `docs/analysis/2026-08-09-timesheet-week-grid-analysis.md`（final）的「后续动作」——table 文档缺口 + input-table footer 对齐。
- **产出**：plan 文件（3 Phase：P1 table.md 补树形/表头分组/footer/quickEdit/affixRow+prefixRow 五节；P2 combo-input-table.md 表达式派生列范例 + footer 文档；P3 input-table `footer` value-or-region 插槽 + 组件单测）。Deferred：Excel 粘贴（optimization candidate）、input-table 树形/分组（out-of-scope）、quickEdit 交互增强（watch-only，PoC 结论驱动）。
- **计划审查（Plan Review Rule 强制）**：第 1 轮 fresh sub-agent verdict=revised（2 Major：footer kind 应 `value-or-region` 非 region/prop + 渲染须 `hasRendererSlotContent(resolveRendererSlotContent(...))` 防 SchemaInput 恒空；`expandable` 语义错位实为展开行特性）+ 3 minor，全部修订；第 2 轮 fresh sub-agent verdict=pass（零 Blocker/Major，源码逐条核实）→ plan 转 `active`。
- **无代码变更**（纯文档+计划），不触发 typecheck/build/lint/test 门禁。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-1826-3` I2 不变式驱动审计执行收口——门禁零命中验证 + 双轮对抗探查 + 零悬挂发现档 + roadmap I2 行 ✅）

- **背景**：执行 plan `docs/plans/2026-08-09-1826-3-i2-invariant-driven-audit.md`（Cycle 1 / I2 不变式驱动审计，纯文档计划）。前置 I0 目录 + I1 门禁已落地（I0/I1 plans completed）。
- **Phase 1 门禁全量运行（Proof，预期零命中验证通过）**：`pnpm check` exit 1——唯一失败为**既有登记 red**（`check:audit-event-dispatch-ctx` 6 hits 于 `flux-renderers-industrial/src/binding/`，2026-08-09 已登记移交 industrial workstream；`check:oversized-code-files` 2 errors 均为已登记豁免 locale），零新增命中；`check:ai-engine-invariants` 单独复跑 **exit 0 零命中**（链尾被 industrial red 中断未跑到）；`pnpm --filter @nop-chaos/flux-renderers-ai test` **66 files / 536 tests 全绿**（含 invariants 套件 22/22 + 全部回归）。**red list = 空**（I1 门禁对 live 仓库零命中验证通过）。
- **Phase 2 对抗探查（Proof + Decision）**：双轮探查——执行 session 轮（14 项候选全部临时 vitest 复现 RED，`docs/analysis/2026-08-09-i2-cycle1-adversarial-probe/round-01-executor.md`）+ 独立 fresh-session 轮（`ses_019a8dbaeffeCjg3XMH7KqCUR2`，11 项发现，executor 复核 7 项 RED，`round-02-independent.md`）。12×5 覆盖矩阵（含 runTurn 间接行 + N/A 类）落档；uncovered 格（①×setMessageEditing、②×renameConversation）已探查不悬挂。
- **发现分类（零悬挂，`docs/audits/ai-invariants/cycle1-findings.md`）**：
  - **已知族（门禁漏覆盖 → I3 补门禁）K1-K4**：K1 ③成功路径完成 mutate 无身份守卫（`create-engine.ts:318-323`，abort 于 onTurnStart 间隙 + 新 send → 新 turn 状态被陈旧 turn clobber，复现 RED）；K2 ⑤abort 不强制终结在途 generator（`create-engine.ts:417-429/509-519`，signal-ignoring connector → loading ghost + controller 泄漏，静态证据）；K3 ④storage save-after-delete/clearAll 时序 ghost 重落盘（`use-conversation.ts:183-196` vs `:348-372/:390-423`，clearAll abort 先于 detachEngine 触发 autoSave，复现 RED）；K4 ②renameConversation 读闭包 `conversations`（`:377`，create+rename 同 tick 持久化丢失，复现 RED）。
  - **新族（→ Cycle 2 / I1 触发证据齐备）N1-N5**：N1 active 提升/复水位移完整性（switch promotion 不防 delete/clearAll/create 位移 + 同 id 快速重 switch hydration 丢失 + delete active 空引擎提升，5 成员全复现 RED，`use-conversation.ts:293-346/:348-372/:390-423/:263-291`）；N2 mount bootstrap 列表覆盖（`:221-229`，复现 RED）；N3 branch 戳泄漏（`create-engine.ts:100/:385/:542-566`，regenerate+connector-missing 后戳泄漏到下一 turn，复现 RED——**登记候选族 branching/fork 触发**）；N4 plugin 错误隔离（onTurnStart 在 try 外 rejection 卡死 `:240-242` + onError 先于状态写入抛错卡死 `:324-341/:459-478` + onTurnEnd 无守卫 `:350-352`，2 复现 RED + 1 静态——**登记候选族 plugin 生命周期触发**）；N5 失败轮空 placeholder 进后续请求（`:459-487`，复现 RED）。
  - **watch-only residual（Deferred But Adjudicated）W1-W4**：abort 窗口期 processingState/abortController 陈旧 + 二次 abort clobber（自愈）；setMessageEditing 流式期写入被覆盖（benign）；delete 的 `await removed.abort()` 在 try 外（病态 listener）；abort 于 onTurnStart 留未回答 user 消息（K1 伴生）。均带复触发条件。
  - **未触发候选族登记保持**：streaming backpressure（chunk 原地替换内存有界，无违背）、tool-execution 并发（per-call abort 检查存在）——不派生。
- **验证**：`pnpm test` **60/60 tasks exit 0**；`pnpm check:docs-garbled` exit 0（新增文件零候选）；`pnpm check:ai-engine-invariants` exit 0；临时复现测试全部删除，`packages/` 零改动（工作区仅 docs 变更）。
- **plan 状态**：Phase 1/2 全 completed + 全 checklist [x] + Closure Gates 6/6 [x] + Plan Status → `completed`；独立 closure-audit（fresh session `ses_0199e90acffektDg63TggLe6ys`，零 blocker/major，1 minor 记录于 round 文件已覆盖）证据落 plan Closure 节。roadmap I2 行 `todo` → `✅`（附执行证据）。
- 偏差：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用；全量 typecheck/build/lint 按纯文档计划条款不属本 plan 门禁，I2 零代码变更）。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-1826-2` I1 收口补账——roadmap I1 行 `todo` → ✅ + 复验全绿）

- **背景**：mission driver 复跑检查 plan `docs/plans/2026-08-09-1826-2-i1-invariant-gate-sedimentation.md`——Phase 1/2 全 completed + 全 checklist [x] + Plan Status completed + 交付物已由上轮 commit 落地（`4e5a1f4c` 门禁+测试、`d6aa09a8` gates.md/engine.md §Invariants），唯一缺口：roadmap `docs/backlog/ai-invariant-loop-roadmap.md` I1 行状态仍 `todo`（上轮只翻了 I0 行）。
- **复验（零代码改动）**：`pnpm --filter @nop-chaos/flux-renderers-ai test` **66 files / 536 tests 全绿**（含 `engine-invariants.test.ts` 12 + `conversation-invariants.test.ts` 10 + PROOF 先红后绿证据）；`pnpm check:ai-engine-invariants` **exit 0 零命中**；`pnpm test:scripts` 33/34——唯一失败为 `check-package-css-exports.test.ts` stale literal（17→19），即 plan Closure Audit 已记录的 F1 minor（industrial-hmi 合并引入，非本 plan scope，测试文件上轮未触碰），plan-owned `find-ai-engine-invariant-violations.test.ts` 4/4 全绿。
- **roadmap 同步**：I1 行 `todo` → `✅`（2026-08-09）附执行证据（plan completed + 独立 closure-audit PASS_WITH_MINOR `ses_019c50670ffeVYwPu3hesmn0GD` fresh session + 交付物清单）；「Last Updated」→ 2026-08-09；动态状态区注释更新（I0/I1 ✅、I2-I6 todo、I2 plan 已 draft active）。
- 偏差：无（plan front matter 无 `> Source Audits:` 行，审计状态翻转不适用；I1 closure-audit gate 已由上轮独立子 agent 完成并记录于 plan Closure 节）。

### 2026-08-09（ai-invariant-loop mission driver：plan `2026-08-09-1826-1` I0 执行收口 + 并行 I1 门禁落地 + industrial-hmi 合并断裂修复）

- **背景**：执行 plan `docs/plans/2026-08-09-1826-1-i0-invariant-inventory-baseline.md`（Cycle 1 / I0 不变式盘点与基线，纯文档计划）。执行期间 working tree 发生两次外部变化：① industrial-hmi 合并完成（`ad540d97` → `3efeb4c1`，249 文件 +45K 行）；② 并行 session 执行 I1 plan（`2026-08-09-1826-2`）的产出未提交落地（`check:ai-engine-invariants` 门禁脚本 + engine/adapters invariants 测试 + engine.md §invariants）。两者均非本 session 产物，以下分别处理与记录。
- **I0 执行（本 session）**：`docs/audits/ai-invariants/invariant-catalog.md` 落地（153 行）：§1 零 engine 门禁基线确认（HEAD 28 / working tree 30 项 `check:*` 均无 engine 项；`scripts/audit/` 无 engine 扫描器；`check:audit-event-dispatch-ctx` renderer 等价先例）；§2 首批 5 类不变式（异步入口 isProcessing 守卫 / await 后 ref 读取 / catch-finally controller 身份守卫 / storage 经 reportStorageError / abort 清理 controller），各含 4 字段 + live 行号证据（引用 4 轮审计 + Bug 07 note）；§2.6 三大族已修复逐行核对表（全部一致）；§3 Cycle 2+ 候选族登记（plugin 生命周期 / tool-execution 并发 / streaming backpressure / branching-fork，Cycle 1 不实现）；§4 目标集枚举（12 变更方法 engine 7 + adapter 5，各带 `文件:行`）+ runTurn 内部管道裁定 + 边界成员裁定（`setMessageEditing` 纳入 / `setConnector`/`registerPlugin`/`getMessages`/`getState`/`subscribe` 进 I1 显式白名单）；§5 交叉验证 proof。
- **Proof（零 diff）**：一次性自动化反查脚本（`/var/folders/.../T/opencode/ai-invariant-proof.mjs`）——engine 对象字面量键 12 + UseConversationReturn 函数字段 5，按变更型准则过滤后与目标集比对 **DIFF = ZERO**；运行时双保险（临时 vitest `Object.keys(createMessageEngine())` 12 键 1/1 passed，已删临时文件零残留）。
- **industrial-hmi 合并断裂修复（本 session，恢复合并前基线，非 I0 scope 的新工程）**：① `layout-renderer-definitions.ts` dropdown-button items 死字段 `icon: 'value'`（schema 无 icon、renderer 不消费，违反 C5.2 committed 契约测试）→ 移除；② `markdown-editor-renderer.tsx` 复加 `role="toolbar"`+`aria-label`（违反 20-07 committed a11y 契约决策 59eb7901，且引用的 i18n key `markdown.toolbarLabel` 不存在）→ 移除；③ 合并复活 1053-1 已拆分的 763 行 `schema-compiler-schema-definition-unified.test.ts` 单体（19 个测试名全部存在于拆分文件，零独有覆盖）→ 删除。修复后 `pnpm test` 60/60 全绿。
- **验证**：`pnpm test` **60/60 tasks exit 0**（AI 包 engine+adapters 21 files / 145 passed；flux-compiler 551、form 783、layout 110 局部全绿）；`pnpm typecheck` 33/33、`pnpm build`、`pnpm lint` 全绿。
- **`pnpm check` 残余 red（移交 industrial-hmi workstream，非本 plan 引入、非本 session scope）**：`check:audit-event-dispatch-ctx` 6 hits 于合并新增包 `flux-renderers-industrial/src/binding/`（animator.ts:97/108/116/162 `animation:start/stop`、point-store.ts:303 `point:change`、refresh-pipeline.ts:423 `state:change`，均无 `{ event, evaluationBindings, scope }` ctx）——CX-10 事件派发契约在新包的缺口，修复（ctx 透传）或豁免（类级裁定/扫描器 staged）由 industrial 团队裁决；`check:ai-engine-invariants`（I1 并行落地门禁）单独跑 **exit 0 零命中**。
- **plan 状态**：I0 plan Phase 1/2 全 completed + 全 checklist [x] + Closure Gates 7/7 [x] + Plan Status → `completed`；两轮独立 closure-audit 均 PASS（本 session `ses_019d97306ffeV5Nmu4WX3SyXSB`：2 Minor 已修——0707-open-audit 文件归属、find-\*.mjs 计数；并行 session `ses_019d61aa6ffe7PDTA69j8vwfwg`：零 finding）。roadmap `ai-invariant-loop-roadmap.md` I0 行 `todo` → `✅`（附执行证据）。
- 偏差：无（I0 交付物与 plan 定义一致；合并断裂修复均以恢复合并前 committed 基线为限，无新工程裁决）。

### 2026-08-09（ai-invariant-loop mission driver：Cycle 1 首批 3 plans 起草（I0/I1/I2）+ 两轮独立 fresh-session 审查至共识，全部转 `active`）

- **背景**：mission `ai-invariant-loop` 首次 plan 起草轮。roadmap（`docs/backlog/ai-invariant-loop-roadmap.md`）Cycle 1 七个 work item（I0-I6）全 `todo`、`docs/plans/` 无该 mission 任何 plan；本轮按依赖序起草 I0 → I1 → I2 三份（每 work item = 一 plan，roadmap Rule），未覆盖 I3-I6（内容依赖 I2 实际发现，起草过早）。
- **Plan 文件（执行序 = 文件名 {N} 序）**：
  - `docs/plans/2026-08-09-1826-1-i0-invariant-inventory-baseline.md`（I0：不变式目录 `docs/audits/ai-invariants/invariant-catalog.md` + 零门禁基线确认 + 目标集枚举；纯文档 plan，Closure Gates 按纯文档条款移除 4 项全量验证）
  - `docs/plans/2026-08-09-1826-2-i1-invariant-gate-sedimentation.md`（I1：首批 5 类不变式 → 参数化穷举测试 + 运行时枚举表完备性门禁 + `check:ai-engine-invariants` 入 `pnpm check` + committed 回归 + `gates.md` 棘轮登记处 + engine.md §invariants；code plan，4 项全量验证保留）
  - `docs/plans/2026-08-09-1826-3-i2-invariant-driven-audit.md`（I2：门禁全量运行 → red list + 对抗探查 + 方法×不变式覆盖矩阵（含 N/A 类）→ `cycle1-findings.md` 零悬挂；纯文档 plan）
- **关键起草裁定（live 实证后写入 I0）**：变更型判定准则（写入 {messages/requestState/processingState/isProcessing/lastError/abortController/activeId/conversation 列表/storage} 之一）→ `setMessageEditing`（`types.ts:320`，同步变更）**纳入**目标集，`setConnector`/`registerPlugin`/`getMessages`/`getState`/`subscribe` **不入**但进 I1 显式非变更白名单；`runTurn` 为内部管道（`create-engine.ts:193`）不进表，由 send/sendMessage/regenerate 间接覆盖。表完备性门禁机制钉死为**运行时枚举**（`Object.keys(createMessageEngine())` 对象字面量 :78-93 + `renderHook(useConversation)` 函数字段 + 嵌套 `controller` 键 ⊆ 表 ∪ 白名单）——类型级断言在 vitest/esbuild 无 typecheck 下不成立。
- **Plan Review（每 plan 两轮独立 fresh session，零 Blocker/零 Major 达成共识）**：
  - I0：R1 `ses_019ebc1f3ffeKwQgIHGId29vBj`（verdict `revised`：1 Major——Proof 操作化 + 边界成员未裁定；2 Minor——runOnce 区段标注、check:\* 计数口径）→ 修正 → R2 `ses_019e4b663ffenf8crV7jzh2LPg`（verdict `pass`，1 残余 nit `message.editing`→`message.state.editing` 已修）。
  - I1：R1 `ses_019ebacbbffe1XzAFZUPjkmUoN`（verdict `revised`：2 Major——缺 roadmap 强制的 `gates.md` 交付、表完备性门禁机制不可运行；4 Minor）→ 修正 → R2 `ses_019e4a05affeLQJ9V2fZaSnSyo`（verdict `pass`，2 Minor 已采纳：runOnce 标注、嵌套 controller 断言）。
  - I2：R1 `ses_019eb98f6ffertiUPUyPLO35SP`（verdict `revised`：1 Major——纯文档 plan 误留 4 项全量验证；3 Minor）→ 修正 → R2 `ses_019e48f8bffeinDwRAVPY6SjBk`（verdict `pass`）。
- **审查证据**：三份 plan `## Draft Review Record` 均已回填（R1/R2 task id + verdict + findings addressed）。
- **基线事实核对（起草时 live 实证，I0 catalog 将正式记录）**：三大复发族已修复（`create-engine.ts:330-346/448-472` 身份守卫、`use-conversation.ts:122-128/339/360` ref 读取、clearAll storage fan-out）；`package.json` `check:*` HEAD 28 项 / working tree 30 项（industrial-hmi 分支合并中，新增 `check:scada-symbol-keys`/`check:audit-canvas-wrapper-a11y` 均非 engine 项，零门禁基线结论不受影响；`apps/playground/` 存在 UU 冲突文件——I1/I2 开工前置已在 plan 内登记）。
- 偏差：无（三 plan 均保持 `active` 待执行队列，无自审/无 closure 声称）。

### 2026-08-09（plan `2026-08-09-1447-1` 收口：Round-2 surface 契约残项批次——fieldRules 诊断 + `=== true` 归一化 + owner-scope 解析统一 + quick-reference 同步）

- **背景**：audit-followups-2026-08-09-1114 全部「待后续批次」5 条（open-audit P2-02 + multi-audit P3-06/07/08/10）收口（plan `docs/plans/2026-08-09-1447-1-round2-surface-contract-residual-batch.md`）。全为小范围契约一致性 Fix + focused Proof，无对外新功能面。
- **Phase 1（P3-06 + P3-10）**：`BUILT_IN_ACTION_DEFINITIONS.openDialog/openDrawer.fieldRules` 各补 `closeOnSubmit: { kind: 'value', valueType: 'boolean' }`（与 `OpenDialogActionSchema`/`OwnedSurfaceStateBase` 声明对齐）；constants.test 补 openDialog 断言 + 新增 openDrawer 断言块；flux-compiler compile 回归（`schema-compiler-schema-definition-validation.test.ts`）：布尔 `true` 编译零警告、`"true"` 字符串经 `invalid-action-shape` 编译期拒绝（fail-closed，fp-schema-true-string）；quick-reference action 表两行补 `closeOnSubmit?` + 脚注指引 surface-lifecycle-callbacks §「closeOnSubmit × hook 失败交互（契约）」。
- **Phase 2（P3-07 + P3-08，Decision）**：裁定 adapter 入口 `=== true` 为唯一布尔归一化点——`action-adapter.ts` 四处恒等 cast 移除（isolate ×2 + closeOnSubmit ×2，直接读类型化 `invocation.args`）；`surface-runtime.ts` triggerHook 消费点 ×2 改 `entry.closeOnSubmit === true`；`OwnedSurfaceStateBase.closeOnSubmit` JSDoc 声明「仅布尔 `true` 生效（fail-closed）」；归一化输入矩阵测试（`true` / `"true"` / `undefined`，直接构造 invocation 绕过 compile）：仅 `true` 触发自动关闭、`"true"` fail-closed 保持打开——`surface-close-on-submit.test.ts` 11 → 12 用例。
- **Phase 3（P2-02）**：`publishClosed` 输入加可选 `ownerScope?: ScopeRef`（向后兼容，现有调用点零改型）；`surface-runtime.ts` 提取 `resolveStatusOwnerScope(ownerScope ?? scope.parent ?? scope)` 复用三处（publishSurfaceStatus/clearSurfaceStatus/publishClosedSummary）；`use-surface-renderer.ts` 三个 publishClosed 调用点回退链统一 `declarativeScope ?? node.scope`（cleanup ref 字段 `ownerScope` 更名 `nodeScope` 避免语义混淆）；focused 测试新文件 `surface-status-publish.test.ts` 4 用例（带 ownerScope 发布到 owner scope / 不带回退 `scope.parent` / 无 parent 回退自身 / 声明式关闭 statusPath 发布与 open 路径同 scope）；`surface-lifecycle-callbacks.md` 新增「Status Path Publication（状态发布 / statusPath）」节（该文档此前无此节）写明解析规则与 action-style/declarative 差异。
- **验证**：`pnpm typecheck`/`build`/`lint` 全绿（32/32）；`pnpm test` 全量 59/59 tasks（flux-runtime 1418→1422 + flux-renderers-basic 487 局部跑绿）；`pnpm check` exit 0（13 项，零新增命中）。
- **Doc-sync**：plan Phase 1-3 Status → completed + 全 checklist [x] + Plan Status → `completed` + Closure 节回填；backlog `audit-followups-2026-08-09-1114.md` 5 行回写终态「已收口」达零悬挂；quick-reference + surface-lifecycle-callbacks 同步；本 daily log 节。
- **closure-audit**（独立 fresh session `ses_01a68658effeMnp4YZgFsGd2oL`，2026-08-09）：verdict **pass**（① plan 文本一致性 ② live 代码核对（cast/`=== true`/resolveStatusOwnerScope ×3/nodeScope）③ focused 测试实测 16+1+1+5 全绿 + `pnpm check` exit 0 ④ deferred 诚实（CX-13+ 人工路由 + 3 watch-only e2e）⑤ doc 同步全对账），证据见 plan Closure 节。
- 偏差：无（matrix 测试经实测确认 `compileActions` 不 shape-validate，`"true"` 可达 adapter——直接构造 invocation 写法仍按 plan 采用，语义与 compile 期 fail-closed 双保险）。

### 2026-08-09（C1a plan Phase 3/7 宿主验收闭环：程序化验收证据 + playground 遮罩遗留规则修复 + 宿主侧联调记录）

- **背景**：C1a plan（`docs/plans/2026-08-09-0001-c1a-crud-table-dialog-visual-amis-parity-plan.md`）Phase 3 宿主 hover 人工核对项 + Phase 7 宿主人工验收项此前标注「待人工执行」；本轮以程序化验收闭环（真实浏览器 computed-style 断言，禁截图，AGENTS.md 纪律）落地全部证据并翻转 Plan Status → `completed`。
- **flux 仓验收 spec（新增 `tests/e2e/component-lab/c1a-visual-amis-parity.spec.ts`，8/8 PASS）**：① 表格密度（thead 14px/40px、body 12px、11/10px padding、16px 边列、行高 >35）；② 固定列 hover 透传（hover 行后固定列单元与中间列单元 computed background 相等、行级 hover 色生效、单元透明继承）；③ 固定边缘阴影（`nop-table-sticky-edge-left/right` `::after` inset box-shadow 实测）；④ 操作按钮 32px；⑤ stripe/bordered/edge 规则真实载入（stylesheet 断言）；⑥ dialog 全档位尺寸矩阵（xs 375/sm 350/base 500/md 800/lg 1100/xl 90%/full 1248[max-w 约束]/default 500 + data-size）；⑦ top 60px + 堆叠 +30px（嵌套 dialog 实测 y 差 30）；⑧ 遮罩 rgba(0,0,0,0.7) + 标题 14px + footer 按钮 min-width 72px。配套 `dialog-lab-page.tsx` 新增「C1a dialog size matrix」scenario（schema 驱动，零渲染器改动）。
- **验收发现并修复（playground 遗留遮罩死规则）**：`apps/playground/src/styles.css` 既有非层叠 `[data-slot='dialog-overlay'] { background: var(--nop-dialog-backdrop) }` 规则（token 定义在 `.nop-theme-root` 作用域，portal 挂 body 外不可继承 → 遮罩透明）压过 C1a `bg-[var(--dialog-overlay-bg)]`（utilities 层）——真实浏览器实测 overlay transparent。修复：该规则改走 `var(--dialog-overlay-bg)`（C1a token 面），遮罩恢复 0.7；属 C1a「遮罩 0.7」验收项的一部分（宿主侧 `amis-fix.css` 为宿主既有覆盖，见宿主日志）。
- **宿主侧联调（nop-chaos-next-master，改动清单落宿主 `docs/logs/2026/08-09.md`）**：`pnpm refresh:flux` 同步 C1a 基线（`300a413a`，flux-sync log 自动记录）；新增宿主 `tests/e2e/c1a-visual-acceptance.spec.ts` 6/6 PASS——AMIS 基线实测（thead 14px/44px、body 12px/47px、padding 11px 10px 11px 16px、modal 500px/top 60px）+ flux 密度/hover/操作列/dialog（500px/top 60/遮罩 0.7/标题 14px）+ ui 原语直接消费冒烟（master-detail Table + AddressDialog、plugins DialogContent、flow-editor Table）；宿主 flux e2e 族回归 29/30（唯一失败 `flux-real-resp` 依赖真实后端，环境项）。
- **实测偏差表（token 可调，非结构性缺陷，已记 plan Phase 7 checklist + 宿主日志）**：thead flux 40 vs AMIS 44px（`--table-row-height` 覆盖）；body 行高内容驱动（flux 32px 操作按钮 ≈55px vs AMIS link 按钮 47px）；遮罩 flux 0.7 vs 宿主 bridged AMIS 0.4（`amis-fix.css` 既有覆盖）。
- **回归**：本仓相关 e2e 全绿（c1a 8/8 + component-lab smoke/navigation/dialog/crud 相关 139/140 + fixtures-hard-gate）；typecheck 32/32；宿主侧 12/12 flux-crud 套件 + 17/18 flux 族回归。
- **Doc-sync**：plan Phase 3/7 Status → completed + 全 checklist [x] + Plan Status → `completed` + Closure Gates 两待办项落证据；宿主日志 + flux-sync log；本 daily log 节。
- 偏差：无（计划内验收项由程序化断言替代人工肉眼核对，证据强度更高；偏差仅记录于本 log）。

### 2026-08-09（plan `2026-08-09-1140-2` closeOnSubmit 契约收口：失败语义裁决 (a) + owner 架构文档双路径重写 + 顺序断言/终态同步测试加固）

- **背景**：multi-audit（`docs/audits/2026-08-09-1114-multi-audit-component-audit-round2.md`）P1-01（owner 文档 3 处无条件断言与 closeOnSubmit 矛盾 + 零提及 + Mandatory Updates 违约）、P2-02（hook 失败语义分叉）、P2-03（顺序契约断言缺失）、P2-04（50ms 假绿窗口）+ P3-05（form.tsx 死 catch）收口。
- **Phase 1 失败语义裁决 (a) AMIS 对齐 + 契约测试钉住**：先钉当前分叉基线（hook `{ok:false}` → 关闭 / 抛错 → 不关闭，+2 用例绿 1413）；裁决 **(a)**——close 只绑定 submit 成功，catch 分支也关闭，删除分叉（理由：AMIS 语义 + §6.2「提交失败不关闭」一致性 + 可预测性；用户可重试性为反方论据，提交已成功、重试入口在外层、hook 失败经 warn 可诊断，不阻断）；test-first 先红（抛错→关闭 1 failed）后绿——`surface-runtime.ts` `triggerHook` 重构为单点关闭决策（dispatch try/catch 后统一走 close 判断，两分支行为收敛）；`form.tsx` `triggerSurfaceSubmitHook` 消费 `result.ok`、失败 `console.warn`、删除死 catch（P3-05）；`surface-hooks.ts` 注释与实现对齐。契约测试全象限 11 用例绿（提交成功 × 抛错/{ok:false} × 有无 closeOnSubmit × submit:error）；`pnpm --filter @nop-chaos/flux-runtime test` = **1417 passed**（1411 基线 + 6 净增）。
- **Phase 2 owner 架构文档重写（P1-01，Mandatory Updates 履约）**：`docs/architecture/surface-lifecycle-callbacks.md`——字段表/schema 块补 `closeOnSubmit` 行；「submit hooks 不自动关闭」改为双路径口径；§Triggering Order 重写为**双路径关闭模型**（推荐 `closeOnSubmit: true`：按钮+Enter 统一、先刷新后关闭；显式编排 `submitForm.then: closeSurface`：仅按钮点击、Enter 失效，不再推荐）；§Hook Error Semantics 新增「closeOnSubmit × hook 失败交互（契约）」节（两种失败表征统一关闭、submit:error 恒不关闭、诊断路径）；「onClose 不会自动触发」改为「取决于关闭路径」（closeOnSubmit 自动关闭路径会触发 onClose hook）；标准 CRUD 示例迁移到推荐路径。`rg "closeOnSubmit"` 文档命中 10+ 处；全文无与 live 实现矛盾的断言。
- **Phase 3 顺序断言 + 失败路径终态同步（P2-03/P2-04）**：`surface-close-on-submit.test.ts` 对 `surfaceRuntime.store.remove` spy，断言 `notifySpy.mock.invocationCallOrder[0] < removeSpy.mock.invocationCallOrder[0]`（「hook 先跑后关闭」可被检测；临时改为先 close 再 dispatch 时该断言变红验证）；`dialog-close-on-submit.test.tsx` 失败路径测试删除 50ms 盲等，改为同步到失败链终态（`env.notify` 收到 error toast 为提交失败链完成信号）后再断言「OK 仍在」。`pnpm --filter @nop-chaos/flux-renderers-form test` = **777 passed**。
- **验证**：`pnpm typecheck`/`build`/`lint` 全绿（32/32）；`pnpm test` 全量 59/59 tasks = **10,754 passed / 0 failed**（10,748 基线 + 6 净增）；`pnpm check` exit 0（oversized 2 豁免维持基线）。
- **Doc-sync**：plan Phase 1-3 Status → completed + 全 checklist [x]；daily log 本节（本条目补记架构文档同步，履约 AGENTS.md Mandatory Updates）；backlog `audit-followups-2026-08-09-1114.md` P2-02/P2-03/P2-04/P3-05 去向项已按 plan 收口，P3-09 行注记「已随 plan 顺手收口」。
- **full-green 关联**：跨 `flux-runtime` + `flux-renderers-form` 代码面 + 架构文档；unit 全量 10,754/0；closure-audit（独立 fresh session `ses_01acb9f30ffeonIkanBP6fYV4b`）verdict **pass**（证据见 plan Closure 节）；Plan Status → `completed`。
- 偏差：无。

### 2026-08-09（C1a plan Phase 1-5 + 7 收口与两轮独立 closure-audit：审计证据与 minor 修复轮）

- **背景**：C1a plan（`docs/plans/2026-08-09-0001-c1a-crud-table-dialog-visual-amis-parity-plan.md`）Phase 1-5（table token 面 + ui 原语 + 固定列 + 列宽测量 + 操作列/工具栏 gap）与 Phase 7（doc-sync）已由执行轮落地；本 record 为收口轮——两轮独立 audit + 修复 + 复验 + 证据回填。
- **closure-audit 轮次**（均在执行 session 之外、fresh session）：
  - Phase 6 独立审计（task `ses_01b2a3ed5ffetlHGkjpjTSMzlb`）：PASS_WITH_MINOR（9 minor/nit）；2 条 minor 修复（footer `border-t` 残留按现场裁决移除、`crud-renderer.tsx` 遗漏 tokenize）。
  - Phase 1-5 + Phase 7 全量独立复核（task `ses_01b16284effespOPcayU8jlznP`）：verdict PASS_WITH_MINOR，4 项验证命令独立重跑全绿（theme-tokens 8/8、ui 160/160、data 790/790、typecheck 32/32）；7 条 minor/nit 发现后全部即场修复。
- **第二轮审计修复清单**（对应 plan Closure Audit Evidence）：
  - `crud-renderer.tsx:600-601`：CRUD toolbar wrapper + inner flex `gap-3` → `gap-[var(--crud-toolbar-gap)]`（消除 Phase 5 声称外的残留硬编码 gap；唯一剩余 `gap-3` 为 table-renderer.tsx:506 column-settings 面板，out-of-scope 记录）。
  - `docs/components/table/design.md`：列宽测量机制段按 live 改为 `<col>` 内联实测 width（`--table-col-{i}-width` 变量在代码中不存在，已删除 doc 中的错误表述）；回退链补齐「测量失败 → 声明宽 → 无声明 auto」+ 固定列回退 `DEFAULT_FIXED_COLUMN_WIDTH`（160px）/控制列 40px（删除不存在的 120px dead value）。
  - `design.md` + plan：stripe 机制改述「Stripe 规则带 `:not(:hover)`，hover 行恒不套条纹」（原文「cascade 在后恒胜出」机制描述错误）；plan 中 TableFooter → TableCaption 措辞校准。
  - Control-column `--table-edge-padding-x` 排除项：经审计复核裁定维持现状（首末）——与 AMIS 16px 边距同构，host 冒烟肉眼核对即可，不新增排除选择器（plan Phase 5 措辞已同步）。
  - `styles.test.ts`：补齐缺少的 `--table-header-font-weight`/`--table-header-bg`/`--table-fixed-edge-shadow`/`--table-empty-height`/`--table-row-action-gap` 断言（Phase 1「每个 token 断言」口径收紧）。
  - `--table-empty-height`（200px）为已声明未消费 token（host 可覆盖），nit 记录不改。
- **复验**：四包回归全绿——theme-tokens 8/8、ui 160/160、flux-renderers-data 790/790、root typecheck 32/32（builtin cached-fresh）、lint pass；audit 亦另行独立跑了 4 命令全绿。
- **plan 状态（终态）**：`Plan Status: completed`（2026-08-09 由宿主程序化验收闭环翻转：宿主 `c1a-visual-acceptance.spec.ts` 6/6 + flux e2e 族回归 29/30，见本日「Phase 3/7 宿主验收闭环」条目）；各 Phase Status 行 completed；Closure Gates 全 [x]（host 侧验收项已由宿主程序化闭环覆盖）；closure-audit gate 由独立子 agent 完成，证据（session id）已回填；本 record 起草时的「Plan 保持 not completed」表述已被宿主验收闭环 supersede。
- **full-green 关联**：本轮改动仅两处（crud-renderer gap tokenize + 测试断言追加 + 文档/plan 文本），无组件行为变更；验证以四包 focused 复跑为准。

### 2026-08-09（C1a plan Phase 6 收口：对话框 AMIS 化——`--dialog-*` token 面 + 尺寸映射 + 顶部阶梯定位 + 遮罩 0.7 + footer 按钮规范）

- **背景**：C1a plan（`docs/plans/2026-08-09-0001-c1a-crud-table-dialog-visual-amis-parity-plan.md`）Phase 6——flux 对话框视觉基线对齐 AMIS 宿主 default.css（R9：size 档位、`--Modal-content-startMarginTop: 60px` + `stepMarginTop: 30px`；R10：遮罩 0.4→0.7；R12：footer 按钮 min-width 72px）。
- **改动**（flux 侧，`packages/ui` + `packages/flux-react` + `packages/theme-tokens`）：
  - **token 面**（`theme-tokens/src/styles.css` `:root`）：新增 `--dialog-size-xs/sm/base/md/lg/xl`（375/350/500/800/1100/90%，未传 size 默认 `--dialog-size-base`）、`--dialog-top-offset: 60px`、`--dialog-stack-step: 30px`、`--dialog-overlay-bg: rgb(0 0 0 / 0.7)`、`--dialog-title-font-size: 14px`、`--dialog-body-padding-x: 24px`、`--dialog-footer-button-min-width: 72px`、`--dialog-content-border-radius: 6px`、`--dialog-footer-gap: 8px`。`styles.test.ts` 追加每项存在的断言（含既有 8 passed）。
  - **size 映射**（`dialog-host.tsx`）：`resolveDialogPrimitiveSize` 丢弃旧的失真拍平（xs→sm / sm|md→default / lg|xl|full→lg→672px），直接输出档名（xs→xs、sm→sm、md→base、lg→md、xl→lg；full 走既有全屏 inline 分流）；宽由 `dialog.tsx` inline `width: var(--dialog-size-<size>)` 承载；`size` prop 扩展 `'xs'|'sm'|'base'|'md'|'lg'|'xl'|'default'`（`default` 保留为兼容别名，等同 base）。
  - **定位/堆叠**（`dialog-host.tsx` + `dialog.tsx`）：`DialogContent` 新增 opt-in `topAnchored?: boolean`（默认 false）；topAnchored 时类为 `top-[var(--dialog-top-offset)] left-[50%]`，非 draggable 去 `-translate-y-1/2`，draggable 注入 `baseTransform: 'translate(-50%, 0)'`；对话框不再 50%/50% 垂直居中（对齐 AMIS 顶部定位 + 遮罩压顶）；堆叠按 surfaces 中 dialog 序号 idx 注入 inline `style.top: calc(var(--dialog-top-offset) + idx * var(--dialog-stack-step))`（位于 drag transform 之外，拖拽不清除）。
  - 遮罩：`DialogOverlay` → `bg-[var(--dialog-overlay-bg)]`（0.7）；footer 按钮 `min-width` / 间距、标题字号、body 水平 padding、content 圆角全部 token 化；footer `border-t bg-muted/50` 默认移除（AMIS footer 无边框，Decision 落点见 plan Phase 6）。
- **单测**：
  - `ui`：`dialog.test.ts` size→var 映射与 topAnchored 类/transform 断言（+2 新例）；`theme-contract.test.tsx` overlay 断言改 `bg-[var(--dialog-overlay-bg)]`；`use-dialog-drag` 基线保持绿。**ui = 160 passed**。
  - `flux-react`：`dialog-host.test.tsx` +size 档位映射与 3 dialog 堆叠 idx 步进 `style.top` 注入断言（含 full 档不进 topAnchored）、`dialog-host-responsive` 契约同步——**flux-react = 477 passed**。
  - 契约收敛：`frame-slot-meta` draggable inline transform 断言 `-50%,-50%` → `-50%, 0`（topAnchored 设计决定）；`dialog-host-responsive` 移动端显式 sm 档断言 `data-size` 'default'→'sm'（新映射）。
- **验证**：`pnpm typecheck` 32/32 绿；`ui 160`、`flux-react 477`、`theme-tokens 8`、`flux-renderers-data 790` 全绿（另修正 `table-column-width-measure.test.tsx` 的 `fixed: 'left'` 字面量类型收窄问题——按 `table-b33` 同款 `as TableColumnSchema[]` 约定修复，消除此前漏跑的类型错误）。
- **Doc-sync**：plan Phase 6 Status → completed + 全 checklist [x]；daily log 本节。
- **Phase 6 closure-audit**（独立子 agent fresh session，2026-08-09，ses_01b2a3ed5ffetlHGkjpjTSMzlb）：verdict **PASS_WITH_MINOR**；`use-dialog-drag.ts` 确认未动；两个 [minor] 裁决（in-repo 六个直连 DialogContent 消费方的视觉变化=目的一致性变更，计入 Phase 7 冒烟范围；堆叠 top 无上限=保持与 AMIS 一致，挪入 Follow-ups），一个 [minor] 测试缺口已补（ui 全档位映射矩阵 + 抽屉穿插 idx 步进 + 两个 token 断言），补测后 ui 160 / flux-react 478 / theme-tokens 8 全绿。证据落 plan Phase 6 节。
- 偏差：无（`use-dialog-drag.ts`、`use-global-z-index` 未改；`'full'` 档不进 DialogContent size prop，由 buildSurface 全屏分流——与 plan 一致）。

### 2026-08-09（Plan `2026-08-09-1140-1` 收口：table 列宽策略按列类别重构——maxWidth 过度矫正（P1-01）+ 非 sticky 控制列拉伸（P1-02）+ H10 注释对齐（P2-01）+ bug note 120）

- **Plan 执行完毕（Phase 1-3 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 Proof（test-first 先红）**：新宿主页 `apps/playground/src/pages/table-column-width-demo.tsx`（路由 `#/table-column-width`，domain-route-entries + App.tsx + pages/index 登记）+ e2e `tests/e2e/table-column-width-layout.spec.ts` 两条真实浏览器断言 + `table-column-width-strategy.test.tsx` 9 条 unit 断言（710 行 lint 上限拆分独立文件；`table-data-and-layout.test.tsx` 同步更新非 sticky 控制列契约断言）。**RED 证据**：unit 7/9 先红（无 width 列表头内联 `width/minWidth/maxWidth=120px`、`getColumnWidth` 120 fallback、非 sticky 控制列无封顶、`widths` 预填 120 写回）；e2e 2/2 先红（表头内联宽度样式 120px、selection 列实测 65.34px vs 声明 40px）。**真实浏览器发现**（记录 bug note 120）：Chromium auto 表格布局忽略 cell `max-width`——WIP 的 120 封顶未冻结像素列宽（默认表仍填满 886px 容器），但 `min-width` 生效 + 控制列拉伸为实；据此 e2e 断言 = 内联样式判别（RED on WIP）+ 像素终态（列宽合计 = 容器宽）双轨。
  - **Phase 2 Fix**：`use-column-resize.ts`——`initialWidths` 只预填有合法显式 width 的可 resize 列（`toNumericWidth(column.width, NaN)` 有限性判定）、`getColumnWidth` 返回 `widths[key] ?? column.width`（无 width 列 → undefined，类型放宽 `number | string | undefined`）、120 fallback 仅保留拖拽/步进起始值；`fixed-columns.ts`——`resolveEntry` 对 `__selection__`/`__expand__` 无条件 `width/minWidth/maxWidth = 40`（非 sticky 也封顶，新增 `createControlColumnStyle`），控制列宽度与 sticky 解耦；`table-header-row.tsx`——表头控制 cell 弃裸 `width:'40px'`、统一消费 layout props；`table-renderer.tsx` `effectiveMainColumns` 经 live 核对无需改动即收敛（widths 无 120 预填 → 无写回）。**Decision（table-layout: fixed 备选）**：驳回（改变全部表格列分配语义、跨包爆炸半径大；按列类别 maxWidth 方案已覆盖 P1-01/P1-02 全部场景）——裁决记录 plan Phase 2。**Proof（unit 矩阵）**：sticky/非 sticky × 控制列/普通列 × 有/无 width 全象限断言绿；`pnpm --filter @nop-chaos/flux-renderers-data test` = 778 passed（770 基线 + 8 净增，111 文件）。
  - **Phase 3 Fix/Proof/Follow-up**：**P2-01** `table-body-row-rendering.tsx` H10 comparator——先补比较项（unit 断言过）后真实浏览器 locality 诊断暴露破坏（`performance-table.spec.ts:349` sibling probe delta 0 → 2，layout 身份随 render 派生输入 churn）→ 撤比较项 + **改写 H10 注释**如实描述覆盖关系（comparator 按内容覆盖全部 layout 输入，内容相等无 stale 风险）并记录不采用身份比较的理由；回归测试钉住「内容相等的身份 churn 保持 locality」；**e2e 转绿** 2/2（列宽合计 886 = 886、selection 列 40.0px、非 sticky 无 position）；**bug note 120** `docs/bugs/120-table-column-width-strategy-class-split-fix.md` 行内补写 + README 索引登记；**backlog 核对** open-audit P2-02 已登记 `docs/backlog/audit-followups-2026-08-09-1114.md`（去向：待后续批次，不随本 plan 收口）。
  - **顺带收口（同批未提交 WIP 门禁障碍）**：①`useTableColumnWidths` 全部缺失测量告警改为不触发（仅部分失败告警）——jsdom/happy-dom 无布局能力或首帧不可见属预期瞬态（digest 重测自愈），修复 playground jsdom 测试（flux-basic-page.debugger ×2 先红）的告警噪音回归；②`column-width-measure.ts` + `table-renderer.tsx` 5 处预存 react-hooks lint 错误（eslint cache 掩盖的 C1a WIP 债务）清理——`digestKey` 稳定 key 依赖（useScopeSelector pathsKey 同款）+ 单行 disable 附理由（react-hooks/immutability、react-compiler 组合 ref 回调；react-hooks/set-state-in-effect 测量回填；barcode 先例同型）。
  - **验证**：`pnpm typecheck`/`build`/`lint` 全绿（32/32）；`pnpm test --force` 59/59（**10,748 passed / 0 failed**——10,703 基线 + 45 净增）；e2e 目标集全绿（table-column-width-layout 2/2 + crud-demo 6/6 + table-popover 3/3 + performance-table locality 恢复 + playground-entry-pages 65/65（新路由清单补登）+ m4-data + crud-table-body-diag + flux-basic-row-inspect）；`pnpm check` exit 0。测试全绿计数：unit 59/59 task（10,748/0）。
  - **Doc-sync**：bug note 120 + README；daily log 本节；plan Closure 节回填。plan 无 `> Work Item:` / `> Source Audits:` front-matter（audit-sourced 用 `> Source:`），roadmap ❌→✅ 与审计状态翻转不适用；open-audit 2026-08-09-1114 状态维持 `planned`（其多份 plan 同批处理，审计文件闭口由后续轮次统一）。
  - **full-green 关联**：本 plan 改动跨 `flux-renderers-data` + playground（宿主 fixture）+ e2e；unit 全量 10,748/0（DV 基线 10,703 → +45）；e2e 目标集全绿（新 spec + table 相关回归 + locality 恢复），全量 e2e 终态由后续 DV 复跑记录。
  - **closure-audit**（独立子 agent fresh session，2026-08-09）：verdict **pass**（证据见 plan Closure 节）；Plan Status → `completed`。

### 2026-08-09（修复：crud 序号列/checkbox 列在 table-layout:auto 下被剩余空间拉伸变宽）

用户反馈 NopAuthPosition crud 页面序号列"感觉过宽"。排查：生成器设置正确（`grid_crud.xpl` index 列 width=50、checkbox 列 `CONTROL_COLUMN_WIDTH=40`），问题在表格布局——flux Table 组件只有 `w-full` 无 `table-layout: fixed`（ui 包 `table.tsx:11`），浏览器默认 auto 布局下 width 只是建议值，表格占满 100% 容器时**所有列都会分到剩余空间**（无 maxWidth 无法阻止），列数少的页面拉伸明显。AMIS 表格默认 `tableLayout:'fixed'` 无此问题。

- **改动**（`flux-renderers-data` table-renderer）：
  - `fixed-columns.ts` `createStickyStyle`：width 存在时同时输出 `maxWidth: width`——所有 sticky 列（index/checkbox/expand/其他 fixed 列）统一不再被拉伸。
  - `table-body-row-rendering.tsx` index 列 cell：`width` 时补 `minWidth`+`maxWidth`（覆盖 index 列非 fixed 场景）。
  - `table-header-row.tsx`：`resolvedWidth` 分支补 `maxWidth`。
  - resize 交互验证：index/selection 列虽默认 resizable，但 header 宽度本就由静态 cellProps.style 覆盖（既有行为），maxWidth 不引入新限制；全量测试含 column-resize 套件全绿。
- **验证**：`pnpm --filter flux-renderers-data test` = 770 passed（111 文件，含新增断言：`table-data-and-layout` sticky props 断言 selection 列 width/minWidth/maxWidth=40 + fixed 列 maxWidth=120；`table-index-column` 断言 index cell width/minWidth/maxWidth=50px）；`tsc --noEmit` exit 0。
- **Doc-sync**：无（行为修复，schema 未变）。
- 偏差：无。

### 2026-08-09（openDialog/openDrawer 新增 closeOnSubmit：AMIS 语义的提交成功自动关闭）

修复"dialog 内表单按 Enter 提交后不关闭"的语义缺口——关闭动作此前只写在提交按钮 `submitForm.then: closeSurface` 里，而 Enter 回车走 form 内置提交（`form.tsx` `handleSectionKeyDown` → `ownedForm.submit()`），不经过按钮 onClick，导致提交成功但 dialog 不关。方案：runtime 增加 AMIS `Dialog.closeOnSubmit` 同款语义，把"提交成功 → 关闭"绑定到 form 提交流程本身（按钮与 Enter 两条路径统一经过 `triggerHook('submit:success')`）。

- **改动**：
  - `flux-core/types/runtime.ts`：`OwnedSurfaceStateBase` + `SurfaceRuntime.open` options 增加 `closeOnSubmit?: boolean`（action-style surface 专用）。
  - `flux-core/types/actions.ts`：`ActionContextSurfaceRuntime.open` options 同步增加。
  - `flux-runtime/surface-runtime.ts`：`open()` 透传 entry；`triggerHook` 在 submit:success 的 onSubmitSuccess nodes dispatch 完成后（含无 nodes 的 skipped 路径），若 `entry.closeOnSubmit` 则 `close(entry.id)`——先刷新（owner 侧 hook）后关闭，与 §6.2 文档化执行顺序一致；submit:error 不关闭。
  - `flux-runtime/action-adapter.ts`：openDialog/openDrawer 把 `args.closeOnSubmit === true` 传入 options。
  - 测试 `surface-close-on-submit.test.ts`（5 例）：closeOnSubmit 提交成功关闭 + hook 先跑 / 未设置不关闭 / submit:error 不关闭 / 无 onSubmitSuccess nodes 也关闭 / openDrawer 透传。既有 `surface-lifecycle-hooks.phase3` 回归通过。
  - **端到端补充（`flux-renderers-form/src/__tests__/dialog-close-on-submit.test.tsx`，5 例）**：真实渲染 dialog/drawer + form + submit 按钮走完整提交链（submitForm/Enter → form.submit() → submitAction ajax → surface submit:success hook → runtime 自动 closeSurface）——dialog 按钮提交后关闭 / **dialog 输入框 Enter 提交后关闭（核心场景）** / drawer 提交后关闭 / 未设置 closeOnSubmit 保持打开 / 提交失败保持打开。`pnpm --filter flux-renderers-form test` = 777 passed。
- **验证**：`pnpm --filter flux-runtime test` = 1411 passed；`pnpm --filter flux-core test` = 512 passed；`tsc --noEmit` exit 0。nop-entropy 生成器侧配合改动（`flux-web.xlib LoadPage` 对 dialog/drawer 输出 `closeOnSubmit: true`、`page_simple.xpl` 提交按钮去掉 then closeSurface、`action.xdef` UiDialogModel 加属性并重新 codegen `_UiDialogModel`）已在 nop-entropy 仓库完成并验证（`TestFluxWebCrudPage#testDialogCloseOnSubmitAndSubmitButton` 新增断言）。
- **Doc-sync**：`flux-guide/design-patterns/page-dialog-drawer.md` §6.1/§6.2/§6.4（closeOnSubmit 取代 `submitForm.then` 成为推荐，注明 Enter 提交差异）；`flux-guide/04-action-system.md` openDialog args 表加 closeOnSubmit 行；`flux-guide/flux-types/common.d.ts` OpenDialog/OpenDrawer args 类型；`flux-guide/examples/crud-with-dialog-and-search-form.md`、`flux-guide/01-quickstart.md` 旧示例同步。
- 偏差：无（与设计一致；`entry.closeOnSubmit` 在未设置时经 adapter 存为 `false` 而非 undefined，行为等价）。

### 2026-08-09（lesson-11 扫描器盲区类沉淀：注释/字符串剥离 + 命中面校准方法论 lesson 落地 + 0444-1 follow-up 收口）

- **Plan `2026-08-09-0917-1-round2-lesson-11-scanner-blind-spot-class.md` 执行完毕（Phase 1-2 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 lesson 11 撰写（Proof）**：live 核对 0444-1 沉淀证据全部在案——`getCodeTextForLine`（`shared.mjs:45`）、`isCodePosition`（`shared.mjs:114`）、`getCodeWindow`（`react19-rules.mjs:11-19`，注释剥离窗口 + eslint-disable 读 raw 窗口）、`isMutatedConstContainer`（`shared.mjs:451`）、`scanBareDataSlotSelectors`（`rules.mjs:325-349`）；`scripts/__tests__/find-tool-governance-gates.test.ts` 12 条用例在案；复扫计数（styling 142→141 / performance 22→21 / react19 517/5→514/4 / async 227→226 / reactive 19→19）与 0444-1 plan Closure 节一致；新建 `docs/lessons/11-scanner-false-positive-comment-string-stripping.md`（07/10 先例格式，Recommended Sections 8 节全）：① 假阳性盲区类——行级/窗口级规则必须跑在注释/字符串剥离文本上（styles.css:110 块注释实锤 + icon.tsx:23 模板字符串 + react19 三处注释触发窗口），假阳性是静默守卫噪音；② 修复模式——`getCodeTextForLine`/`isCodePosition`/`getCodeWindow` 共享 helper + committed 回归测试 12 条先红后绿（git stash 对照 7/12 先红）+ 全量复扫零新增对比；③ 命中面校准原则——判定规则以 live 命中面校准（166 粗命中 → 仅变异容器 + 泛型构造器 → 57/2 零悬挂）。
  - **Phase 2 索引与收口登记（Fix/Follow-up）**：`docs/lessons/README.md` 索引登记 lesson 11（对齐 09/10 编号行格式）；daily log 本节收口条目；0444-1 Non-Blocking Follow-ups「lessons 续写」条目注记收口（「已由 lesson-11 plan 收口（2026-08-09）」，对齐 E2a-bis 对 E2a follow-up 注记先例）。
  - **验证（本 plan 纯文档）**：按 guide 纯文档条款从 `pnpm test`/`typecheck`/`build`/`lint` 门禁移除；`pnpm check` exit 0（12 项 check:\* 链，含 `check:active-doc-code-anchors` 零命中）+ `pnpm check:docs-garbled` exit 0（零新增候选）为零新增命中依据；lesson 证据引用 live 核对无漂移。
  - **full-green 关联**：纯文档计划零代码变更；0444-1 follow-up 终态收口后该 deferred 条目不再悬挂。
  - **closure-audit**（独立子 agent fresh session `ses_01be0237affe8pbM8GT3dZAGea`，2026-08-09）：verdict **pass**（零 Blocker/Major，A-E 五项核对全 PASS，证据见 plan Closure 节）；Minor×3 已修（daily log 验证行表述 / 0444-1 Closure.Follow-up 一致性注记 / 执行记录 12 链）；Plan Status → `completed`。

### 2026-08-09（docs/bugs 重号文件名治理执行：6 重号编号 14 文件后缀去重 + README/引用三方同步 + `pnpm check` exit 0）

- **Plan `2026-08-09-0444-2-round2-bug-note-duplicate-number-filenames.md` 执行完毕（Phase 1-4 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 引用面盘点与后缀裁决（Proof/Decision）**：live `rg` 复核维护面完整文件名引用 **24 处**（15 族 16 处 = render-nodes 2 + component-level 12 + flux-runtime 2；44 族 3 处；67/74 族 round2-bug-note-gaps:37 段内 4 处 + 74b 1 处；16/17 族 0）与 baseline 一致；小写编号短语 3 处纳入消歧（04-24.md bug 15、08-03.md bug 74 ×2）；大小写变体 6 处（deep-audit-prompts:908/924、multi-audit-ai:342、remediation ×2、07-lifecycle:17）裁定 out-of-scope（语义 lesson 引用非文件路径引用，历史快照不改写）；保留区登记（plans 10 + archive 14 + plans 短语 1）；后缀方案裁决 = README 登记顺序后缀（15a-d/16a-b/17a-b/44a-b/67a-b/74a-b），被拒方案记录（整段重编号/时间戳/内容重命名）。
  - **Phase 2 重命名与索引同步（Fix）**：14 文件 `git mv` 后缀去重（15a-render-nodes / 15b-component-level / 15c-report-designer-fill-series / 15d-flux-runtime / 16a-dialog-drag / 16b-report-designer-fill-handle / 17a-json-viewer / 17b-report-designer-field-drop / 44a-performance-table / 44b-flow-designer / 67a-dialog-drag-portal / 67b-input-number / 74a-dropdown / 74b-icon-picker）；`docs/bugs/README.md` Rules 补「letter suffix 消歧」说明 + Current Entries 8 行同步终态文件名；`comm` 零缺口（README 126 条目 = live 126 编号文件）；编号唯一性按后缀语义校准为 `grep -oE '^[0-9]+[a-z]?' | uniq -d` 零输出（plan 注记）。
  - **Phase 3 跨文档引用更新（Fix）**：17 个维护面文件 + `docs/bugs/40-*.md:53`（docs/bugs 内部互引防断链）14 旧文件名全量替换；3 处短语消歧补全路径（bug 15→15d、bug 74×2→74b）；保留区 plans 10 + archive 14 维持不增；维护面 `rg` 旧名零残留；`docs/components/pull-refresh/design.md:29` 通配形态与 `15-setstate-during-render.md`（不存在路径叙述）确认无更新。
  - **Phase 4 收口验证与登记（Proof）**：`pnpm check` exit 0（12 项 check:\* 链含 check:active-doc-code-anchors；oversized 2 豁免维持基线）+ `pnpm check:docs-garbled` exit 0（9 likely-garbled 全为既有文件「facade」等法语拼写，零改动文件入候选）；README ↔ live `comm` 零缺口 + 引用新文件名全部落盘存在（三方一致）；DB plan `2026-08-08-0900-2` Non-Blocking Follow-ups 重号条目终态回写；roadmap DB 节行内注记收口。
  - **full-green 关联**：纯文档治理（docs/bugs 重命名 + README + 引用面），零代码变更；按纯文档计划条款从 `pnpm test`/`typecheck`/`build`/`lint` 门禁移除，验证以 `pnpm check` + `check:docs-garbled` + `rg` 零残留为准。
  - **closure-audit**（独立子 agent fresh session `ses_01c7e5ebfffeOROQcBOQ37AEcs`，2026-08-09）：verdict **pass**（零 Blocker/Major，10 项核对全 PASS，证据见 plan Closure 节）；Minor×1 已修——本 plan 自身与 daily log 因记录文本内法文「façade」的 ç 字符入 docs-garbled 候选，改 ASCII「facade」后两文件出候选（复跑 9 = 9 全为既有文件 exit 0）；Plan Status → `completed`。

### 2026-08-09（工具治理轮次执行：扫描器注释盲区修复 + test-global-leaks const 容器识别校准 + test-support 隐式 hook 显式化 + host 包覆盖 Proof）

- **Plan `2026-08-09-0444-1-round2-tool-governance-scanner-and-test-infra.md` 执行完毕（6 Phase 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 盲区盘点（Proof）**：逐规则核对注释/字符串剥离能力——styling `scanBareDataSlotSelectors`（`rules.mjs:311-334` 逐行 `includes('[data-slot')` 无剥离，`flux-renderers-ai/src/styles.css:110` 块注释内行**实锤假阳性**）、performance `scanJsonStringifyChangeDetection`（6 行窗口无剥离，`icon.tsx:23` 模板字符串假阳性）、broad-scope `scanBroadScopeSelectors`（正则无注释处理，结构盲区）、react19 窗口规则（`graph-renderer.tsx:285` 注释内 `useCallback(` 假阳性 + `steps-renderer.tsx:142`/`timeline-renderer.tsx:161` 注释内 `dispatch` 触发窗口假阳性）；已安全者：scanBalanced 系（自带注释状态机）+ raw-schema-reads（0150-1 已修）。
  - **Phase 2 行级规则注释剥离修复（Fix，test-first）**：**RED**——新写 `scripts/__tests__/find-tool-governance-gates.test.ts` 12 条合成夹具（临时树镜像 + `FLUX_AUDIT_SCAN_ROOT` env，对齐 DG stagedDirs 治理），git stash 对照修复前基线 **7/12 先红**；**GREEN**——`rules.mjs` 三规则复用 `getCodeTextForLine`/`isCodePosition`、react19 窗口规则新增 `getCodeWindow`（注释剥离窗口，eslint-disable 仍读 raw 窗口）、reactive-render-reads/async-failure/fieldframe/test-global-patch/hardcoded-type-dispatch 补 `isCodePosition`——12/12 全绿；`styles.css:110` 从 styling 输出消失；**全量复扫对比零新增命中**：styling 142→141（-1 注释假阳性）、performance 22→21（-1 字符串假阳性）、react19 517/5→514/4（-3 注释假阳性，start-transition 桶因全数注释触发而降为 0）、async 227→226（-1 注释假阳性）、reactive 19→19（零变化）。
  - **Phase 3 test-global-leaks const 容器识别（Fix，test-first）**：RED 合成夹具（`const arr=[]`+`push`、`const map=new Map()`+`set`、`const obj={}`+赋值 正例 + 原始值/`Object.freeze`/`as const`/静态 fixture 负例）先红 → GREEN；**判定规则以 live 命中面校准**——上一轮实现粗匹配 `^const X = [|{` 全量命中 166 条未裁决；本轮收敛为**仅变异容器**（`isMutatedConstContainer`：mutator 方法调用 push/set/add/delete/clear... + 成员/索引赋值 + `Object.assign`/`defineProperty`，证据跑在注释/字符串剥离文本上）+ 泛型构造器支持（`new Set<() => void>()` 箭头形态 `<[^>=]|=>` 匹配）——**166 条未裁决粗命中全部校准归零**，终态 **57/2 = 47 基线 - 1（`c2-5-host-surfaces.spec.ts:67` 字符串假阳性由 isCodePosition 修复）+ 11 条 landed**；11 条新命中裁决表零悬挂（全部 `landed` + 理由：vi.mock 工厂闭包必须模块顶层 / beforeEach 重置有界 / test-support mock 注册表契约，见 plan Closure 节）；committed 回归测试 3 条锁定正例/负例/let 基线。
  - **Phase 4 test-support 隐式 hook 排查与显式化（Fix/Decision）**：7 模块逐条裁决（消费面 `rg "from '<module>'"` 盘点）——**1 显式化 + 6 维持**：`flux-renderers-form-advanced/src/test-support.tsx`（81 直接消费者中 2 个非 render meta 测试——`test-dom-polyfills.test.ts` 仅消费纯 helper `installTestDomPolyfills`、`test-support-isolation.test.ts` 自管 reset）→ 新增导出 `installFormAdvancedTestHooks()`（DOM polyfills beforeAll/afterAll + i18n init + harness reset 收敛），模块顶层 beforeAll/afterAll/beforeEach/i18n init 全移除，**79 个 render importer 文件级显式调用**；2 个 meta 测试保持无 hook 消费（证明常量/helper 与 hook 副作用解耦，仿 document-io 0150-3 先例）；`rg '^beforeEach|^afterEach|^beforeAll|^afterAll' test-support.tsx` 模块顶层零命中；form-advanced 包 **135 files/1050 tests 全绿** + meta 2 tests 独立绿；其余 6 模块 keep-with-reason（全部消费者 render、模块级 hook 即契约，理由逐条记录 plan）：flux-react/test-support-core（12 消费全 render）、form/test-support（8 直接 + 58 转发全 render）、nop-debugger/controller-inspect-advanced.test-support（2 render）、report-designer/page-renderer.test-support（4 render）、flow-designer/designer-page.test-support（6 render）、condition-builder/config-test-support（6 render）。
  - **Phase 5 host 包覆盖核对（Proof）**：`testLeakRules` include 为 `isTestFile`（无包限制）+ `scanFilesWithRules` 扫 apps/packages/tests——**覆盖成立**：word-editor-renderers 9 条命中 live 在案（doc-preview-page:10、word-editor-page-actions:22/159 + test-support 系列）；其余 3 个 host 包（flow-designer/spreadsheet/report-designer renderers）经 brace-tracked 注释剥离复扫确认**零真实模块顶层 let/const 容器**（候选命中全在 `it()`/`vi.mock` 块内，braceDepth>0 正确排除）= 零命中是正确结果而非盲区；committed 夹具锁定 host 包检测（`host-package-let-positive.fixture` 于 flow-designer-renderers 下检出）→ **D3.1 登记闭合**。
  - **Phase 6 收口验证与登记**：`pnpm test:scripts` **7 files/30 tests 全绿**（原 6/18 + 新 12）；`pnpm check` **exit 0**（28 项，oversized 2 豁免维持）；`pnpm typecheck`/`build`/`lint` 32/32 全绿；`pnpm test` 59/59 全绿（form-advanced 1050 fresh 复跑）；本 plan 承接的 4 类 deferred 条目（0150-1 同类盲区 / 0150-3 const 变体 + test-support 隐式 hook / 2034-3 + D3.1 host 包作用域）全部终态回写来源 plan/daily log/roadmap 行内注记。
  - **full-green 关联**：本 plan 无产品代码变更（scripts/audit + 测试基建 + 纯文档；form-advanced test-support 显式化为测试侧结构重构，零产品行为变化）；验证以 `pnpm test:scripts` + `pnpm check` + 受影响包 focused 单测为准（plan Non-Goals 约定不重跑全量 e2e）。

### 2026-08-09（DG Guard 沉淀执行：round2-index 37 卡汇总索引 + 门禁升级（event-dispatch-ctx 14 包覆盖 + 0150-1 stagedDirs 治理）+ lessons 09/10 + checklist v2 §6 回写 + project-context 基线回写 + roadmap DG 行收口）

- **Plan `2026-08-08-2034-3-round2-dg-guard-sedimentation.md` 执行完毕（Phase 1-4 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 输入盘点与终验**：37 张 host 面卡 live 核对全 closed（fd-1..13 + ss-1..10 + rd-1..7 + we-1..7，`grep 状态:` 37/37）；DR 表 17 条 P2 路由终态逐条核对（DR-1..14 i18n fixed + DR-4/15/16 裁决 + DR-17 bug 117，零悬挂）；P3 裁决表 149 = fixed 26 / keep 99 / dismissed 24 零悬挂复核；**bug note 缺口终验**——`docs/bugs/README.md` ↔ live 126 条目 = 126 文件零缺口（`comm` 仅 README.md 自引用排除，2026-08-09 终态）；**门禁盲区清单 live 复核**——`find-event-dispatch-without-ctx.mjs` 扫描正则仅 `^packages\/flux-renderers-`（10 包）不覆盖 4 host renderer 包 + `find-event-dispatch-without-ctx.test.ts:14-32` 旧 stagedDirs 模式（写真实包目录）→ 两项均确认在案，交 Phase 2；**we-1 P3-4 双文档并存裁决**——`docs/components/word-editor-page/design.md`（单 renderer 契约 doc，manifest docsPath `word-editor-manifest.ts:188` 指向）+ `docs/architecture/word-editor/design.md`（family owner doc）**分工并存收敛为单一权威路由**（renderer 契约 → component doc / family 架构 → owner doc，docs/index.md:65-66 双向路由先例一致；owner doc 已含 DR-15/16 终态，component doc §3 fields 与 DR-15 保持终态；无需合并/去重）——结论回写 we-1 卡 P3-4 行 + 本 log。
  - **Phase 2 门禁升级（test-first 先红后绿）**：**RED 证据**——新写 `find-event-dispatch-without-ctx.test.ts`（临时目录镜像夹具 + `FLUX_AUDIT_SCAN_ROOT` env 判别，对齐 browser-io 0150-1 先例）6 用例先跑，2 失败（host 包缺 ctx 命中用例 + flux-renderers-data 缺 ctx 用例因门禁不读 env 而误绿）→ **GREEN**——`find-event-dispatch-without-ctx.mjs` 扫描正则扩展为 14 包形态 `^packages\/(?:flux-renderers-[^/]+|flow-designer-renderers|spreadsheet-renderers|report-designer-renderers|word-editor-renderers)\//`（对齐 `find-renderer-browser-io.mjs:152`）+ `FLUX_AUDIT_SCAN_ROOT` env 判别（scanRoot 覆盖 + toScanRelativePath）；**0150-1 stagedDirs 治理落地**——测试不再写入真实包目录，夹具镜像到 os.tmpdir 临时树（杀进程零残骸），3 个新夹具（flow-designer-renderers 缺 ctx 正例 / report-designer-renderers 合规负例 / flow-designer-core 范围外负例）；`pnpm test:scripts` **6 files / 18 tests 全绿**（原 15 + 新增 3）；**全仓复扫** `pnpm check:audit-event-dispatch-ctx` **零命中 exit 0**（git stash 对照旧门禁同输出，allowlist 0 = 类级裁决原生转发先例维持）——D3.1–D3.4 四连登记盲区闭合。
  - **Phase 3 round2-index 汇总索引**：新建 `docs/audits/round2-index.md`（先例 pc-index）——37 卡逐面索引（fd/ss/rd/we 分面表，每卡面身份 + 状态 + P0/P1 + 路由终态）+ **P0/P1 全量索引**（P0 零显式声明；P1 ×14 全 fixed test-first（fd-5/11 ×2 + ss-2/6/7/9 ×4 + rd-1/6 ×3（bug 111/112/113）+ we-2/4/7 ×5（bug 114/115/116））+ 低成本 P2 ×7 当场修复 + DR-1..16 路由 + DR-17 执行中发现）per-D3.x 分解核对（D3.1 2/0/DR-1,2；D3.2 4/2/DR-3..6；D3.3 3/1/DR-7..11；D3.4 5/4/DR-12..16，与 plan 注记「DR 可能再增」复核 = 17 条终态）+ **CX-n 模式索引**（CX-13+ 未插入显式声明 + D1 零共性声明 + DR Phase 4 四候选 keep 证据链）+ Audit Tool Baseline 节（门禁升级行）；`docs/audits/host-surface/README.md` §3 维护节登记 + `docs/index.md` 导航行 + Directory Roles 登记。
  - **Phase 4 lessons 09+ 续写 + checklist 修订 + 基线回写 + 收口**：`docs/lessons/09-host-surface-audit-template-and-host-scene-discipline.md`（host 面审计模板方法：18 维降维 + H1–H7 应用 + 每面 ≥1 真实浏览器宿主场景纪律，证据 = 37 卡 + H 维度命中实绩 bug 107/111/112）+ `docs/lessons/10-pattern-family-rescan-scan-scope-must-cover-host-packages.md`（模式族回扫方法 + 门禁盲区教训：扫描器正则覆盖范围必须含 host 包，证据 = D3.1–D3.4 四连登记 + DG 门禁升级）；lessons README 索引 09/10 登记；**checklist v2 §6 host 模板节有效性回写**（模板有效 + 4 条执行教训：独立宿主页优先 / 面级投影生命周期风险 / 门禁覆盖横向一致性）；**project-context 基线回写**——DV full-green 终态（typecheck/build/lint 32/32 + test 59/59 10,703/0 + e2e 1086/43/3 watch-only 50Hz + check exit 0 + test:scripts 6/15 → 6/18）+ round-2 mission 全部 work item `done` + 门禁升级基线，freshness 维持 `fresh`；**bug note 缺口终验复核**——126 = 126 零缺口维持（本 plan 无新增 bug note）；**收口登记**——roadmap DG 行 `todo → done`（附执行证据，6 列规范 + trailing 空列对齐）+ docs/index.md 登记终验（round2-index 导航行 + Directory Roles）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转。
  - **验证（本 plan 门禁升级受影响项）**：`pnpm test:scripts` 6/18 全绿 + `pnpm check:audit-event-dispatch-ctx` exit 0（全仓零命中）；`pnpm check` exit 0（28 项复核）；全量 typecheck/build/lint/test 由 DV full-green 承接（本 plan 纯文档 + 门禁脚本变更，closure 时复跑确认）。
  - **full-green 关联**：本 plan 不产生产品代码行为变更（门禁脚本 + 测试 + 纯文档）；DV full-green 终态（2026-08-09）为 project-context 回写依据。

### 2026-08-09（DV 全量验证轮执行：typecheck/build/lint 32/32 + test 59/59（10,703/0）+ e2e 1086/43/3（3 failed 全数 watch-only 50Hz）+ `pnpm check` exit 0 + D3.2 closure 正式收口 + 阻断缺陷 bug 118/119 修复）

- **Plan `2026-08-08-2034-2-round2-dv-full-verification.md` 执行完毕（4 Phase 全 completed；closure-audit 证据见 plan Closure 节）**：
  - **Phase 1 收口前一致性核对**：round-2 全部 plan ↔ roadmap 状态逐一对齐（D0/D1/D2/DB/DL/D3.1/D3.3/D3.4/DR completed ↔ `done`；DV/DG active ↔ `todo`）；**roadmap D3.3 行格式破损修复**（残留 `| todo | 同上 | ~8 卡 | D0 |` 碎片删除，恢复 6 列规范）+ D3.1 行证据单元格拆分合并（同 6 列规范）；**D3.2 收口缺口（Decision 路径 a）**——独立 fresh session 于 2026-08-09 正式审计：轮 1 verdict `fail`（1 Major：closure 流程完整性——Closure Gates 审计门禁项无证据勾选 + `Plan Status` 未翻转 + Closure Audit Evidence 空 + roadmap/daily log 过早断言；3 Minor：卡状态计数过时（6 closed + 4 fixed-pending-closure → live 10 卡全 closed）、ss-3/4/5/6/10 卡场景行「待 Phase 5」过时、10 卡 Closure 占位未填）→ 全部修复（roadmap/daily log 更正 + 5 卡场景行 pass + 10 卡 Closure 回填 + 证据回填 + status 翻转）→ 轮 2 verdict `pass`（零 Blocker/Major）——D3.2 plan `Plan Status: active → completed`，Closure Audit Evidence 回填（轮 1 + 轮 2），Closure Gates 审计门禁项以真实证据勾选；roadmap D3.2 行证据列更正（2026-08-09 正式复核收口）；daily log 08-08 D3.2 节补「closure 复核更正」披露节。
  - **Phase 2 全量静态与单测验证（fresh/零缓存）**：`pnpm typecheck`/`build`/`lint` 均 `--force` 32/32（0 cached）；`pnpm test --force` 59/59（0 cached，**10,703 passed / 0 failed**——CV 基线 10,397 + D3.x/DR 回归 306）；`pnpm check` exit 0（28 项 check:\*，27/28 exit 0 + `check:duplicates:detail` 非门禁归因维持（jscpd dump 固有 exit 1）+ oversized 仅 2 条既有 locale 豁免）；**新增命中裁决（验证阻塞性小修，留痕）**：`pnpm test:scripts` 3 条 git-scaffolding 用例在默认 5s 超时边缘超时（实测 ~5.0-5.5s，负载相关）→ `vitest.scripts.config.ts` 加 `testTimeout: 30_000`（仅 harness 配置，无门禁规则/断言变更），6/15 全绿；`check:duplicates`（阈值门禁）exit 0（3.06% < 8%）。
  - **Phase 3 全量 e2e 验证（两次全量 + 隔离归因）**：**终态 1086 passed / 43 skipped / 3 failed——3 failed 全数落入 watch-only 终态清单**（gantt-perf:21/44 scroll/drag 50Hz 阈值 49.5-50 fps vs >50 不可达 + kanban-perf:34 60fps 阈值 50Hz 不可达，主屏 50.00Hz，维持「需 60Hz 环境最终确认」）；D3.x + DR 新增 host 面 spec 全绿（flow-designer undo-clipboard 3 + slot-drag 2、spreadsheet-demo 10、report-designer-host 5、word-editor-recovery 6、entry-pages 路由断言）；component-lab **336/0**（CV 334/1/2 对照——Tiptap ×2 已由 DR bug 117 修复）、smoke+navigation **111/111**（基线一致）、host-surfaces **133/0**（CV 42/42 对照，全量 30 spec 覆盖更广）。
  - **阻断缺陷发现与修复（归因纪律纠正历史误判，全 test-first）**：
    - **bug 118 diff-view reaction 回声派发死循环**（bfff20de「1-9 触发即派发」引入）：schema reaction 的 action 解析到同一 component handle 方法（`setViewType` ↔ `component:setViewType`）→ mount 期 dependsOn 初值写入触发 reaction → invoke → 再派发 → 无限异步派发环 → lab diff-view 页渲染悬挂（c6-5 ×5 + smoke + lab-batch ×7 用例；D3.2 起被误归「环境」，实为代码回归）；修复 = `diff-view-renderer.tsx` per-key 重入 latch（echo 派发 + echo 状态变更双抑制，mount 期双 reaction 初火确定收敛）；回归测试「breaks the self-referential reaction echo」；e2e c6-5 5/5 绿。bug note 118 行内补写 + README 索引。
    - **bug 119 gantt StrictMode store.destroy() 空态**（ecb54998「P3-5」引入）：unmount cleanup 的 `store.destroy()` 在 StrictMode dev 双挂载周期清空 store（state 保留）+ re-seed effect 引用快照未变提前返回 → store 永久空 → 下次 store 驱动 re-render（dblclick/click/zoom）渲染空态（gantt-editor:9 + gantt-bars:92 + gantt-demo:59 ×3 用例）；修复 = re-seed effect store-empty 自愈（幂等重灌）；回归测试「re-seeds the store after a StrictMode double-mount destroy cycle」（StrictMode 包裹 + dblclick 驱动 + Radix portal 断言，先红后绿验证）；e2e gantt 三 spec 47/47 绿。bug note 119 行内补写 + README 索引。
  - **watch-only 终态清单（full-green 记录依据）**：gantt-perf ×2 + kanban-perf ×1（50Hz 阈值，需 60Hz 环境最终确认）；`check:duplicates:detail` 非门禁归因；w3d-editor/c3-5 ×2/ai-attachments 已 closed（DR bug 117 + D2 复核）；diff-view ×7/gantt ×3 本轮修复移除。
  - **收口登记**：roadmap `component-audit-round2-roadmap.md` DV 行 `todo → done`（附执行证据）；本 plan 无 `> Source Audits:` front-matter（roadmap-sourced），无审计状态翻转；closure-audit 证据见 plan Closure 节（独立子 agent fresh session）。
  - **full-green 验证记录（2026-08-09）**：`pnpm typecheck` 32/32 ✓（--force 零缓存）、`pnpm build` 32/32 ✓、`pnpm lint` 32/32 ✓、`pnpm test` 59/59 ✓（**10,703 passed / 0 failed**）、`pnpm test:e2e` **1086 passed / 43 skipped / 3 failed（全数 watch-only 50Hz）**、component-lab 336/0、smoke+navigation 111/111、host-surfaces 133/0、`pnpm check` exit 0、`pnpm test:scripts` 6/15 全绿。
  - **为 DG 提供的收口输入清单**：full-green 记录位置（本节）；门禁基线（28 项 check:\* 27/28 exit 0 + duplicates:detail 非门禁归因 + test:scripts 6/15 + harness testTimeout 30s）；index 素材范围（round-2 审计卡 10+7+7+7 = 31 张 host 面卡 + P0/P1 修复清单 + CX-13+ 人工确认门待裁项（ss-3 P3-2/ss-6 P3-2/ss-10 P3-4/ss-3 P3-4，DR 表 §2 已登记））；project-context 基线回写（DV full-green 终态由 DG 承接）；门禁盲区登记（`check:audit-event-dispatch-ctx` 不覆盖 4 个 host renderer 包，D3.1-D3.4 同款登记维持）。

---

<!-- feat-industrial-hmi branch entries (merged) -->

### 2026-08-09（plan 2026-08-09-1300-1 industrial SCADA 1931 re-audit P2 polish — 8 个 [P2] findings 一次性清零，mission Follow-up Backlog 清零）

- **plan `2026-08-09-1300-1`（1931 后继 re-audit §P2-1~P2-8：数据完整性 + viewport parity + 公共契约 + 死代码 + doc/test fidelity 切片）执行完成（executor pass）**——源：`docs/audits/2026-08-08-1931-multi-audit-industrial-hmi-component-audit.md` §P2-1~P2-8 + roadmap Follow-up Backlog。3 Phase 全交付，8 findings 全部落地。**收口后 industrial-hmi-component-audit mission Follow-up Backlog 清零（1712 + 1931 两轮全部 done）**。
  - **Phase 1（editor correctness + failing-first 测）**：**P2-1** `runtime-mutators.ts` `groupSymbols` 新增 `dedupAncestorDescendant` helper（从原始树建 parent→id 索引，对每个选中节点向上遍历祖先链，剔除「祖先也在选集中」的节点；后代保留在选中祖先子树内）——修祖先+后代同选 `[G1, G2]`（G2 ⊂ G1.children）时 `selectedNodes.map((c)=>({...c}))` 浅克隆致 G2 重复 id。failing-first 3 测（`runtime-mutators-nested.test.ts`）断言 collectAllSymbols + 序列化输出无重复 id + G2 不作新 group 直接 child。**P2-2** `use-editor-engine.ts` width/height effect 改 container-first（`container?.clientWidth || args.width || 0`，schema 仅 container=0 时 fallback）+ setSize 后追加 `refitViewportOnResize?.()`（与 runtime `use-scada-engine.ts:288-294` 对称）。failing-first 测（新 `use-editor-engine.test.tsx`，renderHook + 稳定 containerRef 防 mount-effect churn）spy engine.setSize 断言收到 container 400 而非 args 1000 + refit spy 被调。**两测均经 failing-first 验证**（P2-1 修复前 2 测红「duplicate id」；P2-2 临时还原 args-first → 测红「expected 1000 to be 400」）。
  - **Phase 2（public contract + dead code）**：**P2-4** `editor/index.ts` 增 `export { industrialEditorRendererDefinitions }`（与主入口 `industrialRendererDefinitions` 对称）+ editor `design-renderer.md §11` index.ts 公共面注释补该导出。**P2-5** `toolbox-panel.tsx` 3 个 tooltip 位（delete/group/ungroup）删死 `|| 'Delete'/'Group'/'Ungroup'`，对齐 :167+ bare `t(key)`（3 key 在 en/zh locale 均存在，零行为变化）。**P2-6 裁定 Option A（wire-in）**——`scada-editor-canvas.tsx` drop 路径 `t('industrial.scada.editor.error.invalid-node')` 改 `t(scadaEditorErrorI18nKey('invalid-node'))`（`src/editor` 内唯一生产解析点），import `scadaEditorErrorI18nKey` from `./renderer/editor-errors.js`；editor-errors.ts 获生产 importer + `.unknown` fallback 成真实安全网（未注册码不再渲染 raw dotted key），消除「带测试的零-importer 死代码」。Decision 记录于 plan Phase 2。editor `design-renderer.md §8.5.2` 补 Option A wire-in 记录段。
  - **Phase 3（doc + test fidelity）**：**P2-3** runtime `design-renderer.md §11` serialization/ 块补 `legacy-scan.ts`（scanLegacyAtSyntax）+ `validators/` 子目录全 9 文件（index/helpers + animation/binding/point-declaration/state-declaration/symbol-event/symbol-node，各一行职责说明）。**P2-7** `editor-state-integrity.test.ts` P1-3 选区修剪 3 测增引擎层 target 断言——undo-add / redo-remove 后 spy `engine.clearEditorSelection`（selection 空→clear，防 leafer editor 在死节点保留高亮框）；undo-live-selection 后 spy `engine.setEditorTargets`（长度未变但 reconcileEditorTargets 无条件重解析）。**P2-8** `scada-editor-canvas-reactivity.test.tsx` 删 `expect(within(root).queryByTestId('noop')).toBeNull()` cosmetic no-op + 残留 `within` import + 仅此处用的 `root` 局部变量。
- **验证**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿（industrial **117 files / 1449 tests** = 1444 baseline + 5 新 failing-first，零回归）。`src/` 无 stray build artifact（build 产物仅 dist/）。
- **plan 收口**：`2026-08-09-1300-1` Plan Status `active` → `completed`，3 Phase status + items + Exit Criteria 全勾；Closure Gates 技术项全勾（**closure-audit 项留 `[ ]`——执行 session 不得自审，待独立子 agent fresh session 执行**）；Closure section 填写 status note + 标注 audit 待办。无 `> Source Audits:` 行（plan 有 `> Source:` 单数引用，非 `> Source Audits:` 列表）→ source audit closure 步骤跳过（per mission instruction）。roadmap `industrial-hmi-component-audit-roadmap.md` 8 条 `1931-P2-*` 全部标 ✅ done（各条附 done 摘要）+ 增「1931 re-audit P2 收口轮执行进度」块（声明 mission Follow-up Backlog 清零）。
- **owner doc 同步**：runtime `docs/components/industrial-hmi/design-renderer.md §11`（serialization 文件树补 legacy-scan + validators/）；editor `docs/components/industrial-hmi-editor/design-renderer.md §11`（index.ts 公共面注释）+ §8.5.2（Option A wire-in 记录）。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 117 files / 1449 tests，1444 baseline + 5 new，零回归）。
- **successor**：closure-audit 待独立子 agent（fresh session）执行；无 plan-owned remaining work（8 findings 全部落地）。

### 2026-08-09（plan 2026-08-08-1931-4 industrial SCADA editor.target 调和 + 错误恢复一致性 — editor-correctness 切片 P1-2/P1-3/P1-4 执行）

- **plan `2026-08-08-1931-4`（multi-audit Dim 04/22 P1-2 + Dim 19/22 P1-3/P1-4 + Cross-Cutting #1/#2：editor.target 调和 + 错误恢复一致性）执行完成（executor pass + closure audit PASS fresh session）**——源：`docs/audits/2026-08-08-1931-multi-audit-industrial-hmi-component-audit.md` §P1-2/P1-3/P1-4。**这是行为正确性 plan（非机械迁移）**：闭 1910-2 A6 模式遗漏的 `syncWorkingCopy` 路径 + P1-2 修复副作用引入的 editor.target dangle。3 Phase 全交付。
  - **Phase 1（Proof：5 failing-first 测）**：新增 `packages/flux-renderers-industrial/src/editor/editor-target-reconciliation.test.ts`——5 测（P1-2a 通用编辑路径 / P1-2b 长度门控 / P1-3 catch 调和 / P1-4 catch 全量重建+target 调和 / P1-4 catch target identity）。**全断言引擎层 target / 场景状态**（对象 identity 或 `getSymbol(...).node` 等价），非 `not.toThrow` / `session.selection` 数组。**failing-first 验证**：未修改代码基线 5/5 全红（精确复现 applyDiff 半途抛错时 scene-graph 半变 + editor.target dangle）。
  - **Phase 2（Fix：helper 提取 + 4 站点对齐）**：`runtime-factories.ts` 新增 `reconcileEditorTargets` ctx-method helper（session.selection 经 `engine.getSymbol(id)?.node` 重解析到 live registry → setEditorTargets 或 clearEditorSelection）+ `syncWorkingCopy` 改造（成功路径末调 helper；catch backport 1910-2 A6 模式 `engine.build(synced.config)` 全量重建 + helper，闭合「scene 半变 + 下轮 syncWorkingCopy diff 为空 → 引擎永不愈合」永久背离）；`runtime-mutators.ts` `applyUndoRedoDiff` 改造（成功路径长度门控移除——`if (pruned.length !== session.selection.length)` 现仅包 setSessionSelection，helper 无条件调用；catch `engine.build(beforeWorking)` 后调 helper）。**Decision（helper 落点）**：ctx-method 而非独立函数或 engine method（4 站点共享；与既有 setSessionSelection/syncWorkingCopy/notifySession 同形闭包模式；EditorRuntimeContext 域内部不导出，零跨包 blast radius）。**两 catch 现同 failure-class 同防护**（rebuild scene + reconcile targets + dispatch onError）。
  - **Phase 3（owner-doc sync）**：`docs/components/industrial-hmi-editor/design-undo-redo.md §12.1` 风险表 U8 注 Phase 2 / P1-4 backport 到 `syncWorkingCopy` catch（同 failure-class 两 catch 同防护，无不对称）+ 新增 U9 记录新 `editor.target` 调和行为。`design-renderer.md §8.5.2` `editor-internal-error` 码语义经复核已涵盖两 catch（无需变更）。
- **验证**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿（industrial **116 files / 1444 tests** = 1439 baseline + 5 新 failing-first，零回归）。`src/` 无 stray build artifact。
- **plan 收口**：`2026-08-08-1931-4` Plan Status `active` → `completed`，3 Phase status + items + Exit Criteria 全勾；Closure Gates 全勾（含 closure-audit 项经独立 fresh sub-agent 完成）；Closure section 填写 evidence。无 `> Source Audits:` 行（plan 有 `> Source:` 单数引用，非 `> Source Audits:` 列表）→ source audit closure 步骤跳过（per mission instruction）。roadmap `industrial-hmi-component-audit-roadmap.md` 增「editor.target 调和 + 错误恢复一致性轮执行进度」块 + line 325 更新标注 P1-2/P1-3/P1-4 ✅ completed。
- **closure audit**：**PASS（fresh session `ses_01b24628affeDu1LRouyfkl0sm`，非执行 session 自审）** verdict=`pass`（0 Blocker / 0 Major / 1 Minor 非承重：`editor-target-reconciliation.test.ts:308` 第一断言在 OLD 代码上空真，但伴随 `not.toBe(liveBefore!.node)` 是承重 failing-first proof——测试整体在 OLD 仍正确失败）。独立 live 复核：4 站点 helper 共享非重复；length-gate 现仅包 setSessionSelection；两 catch 一致（rebuild + reconcile + onError）；synced.config 在 sync catch 中只读未重赋；engine 变量仍用于 6 站点无死闭包。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 116 files / 1444 tests，1439 baseline + 5 new，零回归）。
- **successor**：无 plan-owned remaining work（P2-7 retrofit 既有 `editor-state-integrity.test.ts:186-232` undo-prune 测到 engine-target 断言 + 其他 P2（1931-P2-1..P2-6/P2-8）显式 out-of-scope，已 routed to roadmap Follow-up Backlog）。

### 2026-08-09（plan 2026-08-08-1931-3 industrial SCADA oversized test file split — 纯机械测试拆分 + 验证收口轮）

- **plan `2026-08-08-1931-3`（multi-audit Dim 02 / P1-1：两个 >700 行硬门限超限工业测试文件机械拆分）执行完成（executor pass）+ 验证轮通过（mission-driver verify step）**——源：`docs/audits/2026-08-08-1931-multi-audit-industrial-hmi-component-audit.md` §P1-1。**纯测试代码机械搬迁，无生产代码行为变更**（Non-Goal：不动 `src/**/*.ts` 非 test）。目的：恢复 `check:oversized-code-files` 700 行硬门限 baseline 至「0 industrial files >700」（1712 audit 记录为 0，后续 remediation 测试波次将其退化为 2）。
  - **Phase 1（serialization-validate.test.ts 按 validator domain 拆分）**：原 786 行 → 8 个 sibling 文件，镜像既有生产侧 `src/serialization/validators/{symbol-node,animation,binding,point-declaration,state-declaration,symbol-event}.ts` + legacy-scan 结构。行数（`wc -l`）：`serialization-validate.test.ts` 71（保留 `validateScadaConfig` orchestrator + 共享 setup）/ `serialization-validate-symbol-node.test.ts` 38 / `serialization-validate-animation.test.ts` 45 / `serialization-validate-binding.test.ts` 100 / `serialization-validate-point-declaration.test.ts` 59 / `serialization-validate-state-declaration.test.ts` 80 / `serialization-validate-symbol-event.test.ts` 52 / `serialization-validate-error-and-dos.test.ts` 424（error branch matrix + numeric finiteness/recursion depth + legacy recursion/subshape + coverage gaps + assertShape finite + breadth/total-node DoS 合并）。共享 fixtures 无重复。
  - **Phase 2（scada-points-bridge.test.tsx 按 domain 拆分）**：原 709 行 → 3 个 sibling 文件。行数：`scada-points-bridge.test.tsx` 549（points bridge I10.3 主体 + expression）/ `scada-points-bridge-scope-path.test.tsx` 147（flux scope path extraction 订阅判定）/ `scada-points-bridge-diagnostics.test.tsx` 516（lifecycle + error-channel/diagnostic）。共享 fixture 抽至 `scada-points-bridge-test-helpers.ts`（无重复）。
- **验证（mission-driver verify step，cache-bypass 强制复核）**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿。**新增/拆分文件存在性强制复核**（turbo 默认全 cache-hit，存在「新文件未被实际编译」假绿风险）→ 对 `flux-renderers-industrial` 强制 `--force` 重跑（0 cached）：typecheck + lint 直跑绿，**test 115 files / 1439 tests 全过**（与 plan baseline ~107 files / 1439 完全一致——file 数随拆分增长、test 数不变 → 无断言在搬迁中静默丢失）。拆分产物 `wc -l` 全部 ≤ 700。
- **plan 收口**：`2026-08-08-1931-3` Plan Status `completed`（closure audit 已由独立 fresh sub-agent session `ses_01b887451ffeWgq6muZpODU9eY` 完成，PASS，非执行 session 自审）。Closure Gates 技术项全勾。
- **successor**：拆分后文件已就位，successor plan `2026-08-08-1931-4`（editor target reconciliation error recovery）的 failing-first 回归测试可注入已拆分文件而不再次撑超硬门限。

### 2026-08-09（plan 2026-08-09-0648-3 industrial SCADA collectWorldBounds 假绿测试修复 + align-distribute 坐标语义裁定 — test/adjudication 轮执行）

- **plan `2026-08-09-0648-3`（roadmap Follow-up Backlog P2-5/本轮-12：测试保真度 + 坐标语义裁定切片）执行完成（executor pass）**——2 Phase 全交付，无生产代码行为变更（P2-5 仅测试 fixture 加强；本轮-12 维持 watch-only residual 仅 owner doc 表述补充）。源：roadmap Follow-up Backlog（P2-5/本轮-12，源 audit `docs/audits/2026-08-08-1712-open-audit-...md:312` 与 multi-audit `:303` 本轮 P2 簇）。**此 plan 收口后，1712 两份 deep-audit 全部 P2 findings 收口完成**。
  - **Phase 1（P2-5 collectWorldBounds 假绿修复）**：`packages/flux-renderers-industrial/src/editor/editor-working-helpers.test.ts` 共享 fixture `configWithCustom` 的父 group `g1` 由 `x:0,y:0` 改 `x:100,y:50`（消除旧「父偏移=0 → world==local，累加逻辑无论对错都断言通过」false-green），断言子节点 `j2` world = local(5,5)+父偏移 = (105,55) + g1 自身 world=(100,50) sanity；`connection/connection-adapter.test.ts:33-42` 同型 fixture（`collectSymbolBounds` 经 `collectWorldBounds` 累加 parent offset）`withGroup` 的 g 改 `x:100,y:50`，断言 c world = local(5,6)+(100,50) = (105,56)，test name 更新为 "flattens symbols including group children with accumulated parent offset"。cloneConfigSnapshot 三测不依赖 g1 坐标，零影响。**failing-first 验证**：临时把 `editor-working-helpers.ts:56` 递归调用 `collectWorldBounds(node.children, nx, ny)` 改 `(0, 0)` 破坏累加 → editor-working-helpers 测 expected 105 got 5 失败 + connection-adapter 测 expected 105 got 5 失败（两测均红），恢复 `(nx, ny)` 后通过，证明新断言为真断言（能区分累加对错，非 0+0 trivial pass）。
  - **Phase 2（本轮-12 align-distribute 坐标语义裁定 + owner doc 同步）**：**Decision 维持「M3 局部坐标扁平算法 / T1 接受」**（不升级为 world 解析）——理由记录于 plan：① align/distribute 是基于选区包围盒的多图元相对位置算法，非对场景的命中/吸附（与 snap/hit/linkage 统一 collectWorldBounds 的诉求不同）；② 同父兄弟 local 坐标系一致，对齐在 local 空间自洽；③ 仅「跨层级混选」（顶层节点 + 嵌套 group 子节点）按各自原始 x/y 直接比较是已接受的 M3 限制，非 P1-C2/C3 回归；④ world 解析属未来 feature，非当前 contract 缺陷。`docs/components/industrial-hmi-editor/design-toolbox.md §4.2.1` 分布工具段后增「坐标语义裁定」段，显式记录「按各图元顶层 bounds 的局部 x/y/width/height 计算，不解析 group 嵌套相对坐标（M3 扁平算法 / T1 trade-off）」，并交叉引用 code docstring（`align-distribute.ts:5-6`）+ 测试注释（`align-distribute.test.ts:174-185`「documented M3 flat-algorithm, T1 trade-off」）。三处口径一致（grep 可定位 "M3 扁平算法"/"M3 flat-algorithm"/"T1 trade-off"）。`align-distribute.test.ts` 测试注释无需变更（owner doc 措辞与既有测试注释术语一致，不改测试断言行为）。
- **验证**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿（industrial **107 files / 1439 tests**，零回归——P2-5 仅 fixture 数据值变更，测试数不变；本轮-12 纯文档无测试影响）。`src/` 无 stray build artifact（build 产物仅 dist/）。
- **plan 收口**：`2026-08-09-0648-3` Plan Status `active` → `completed`（executor pass），2 Phase status + items + Exit Criteria 全勾；Closure Gates 技术项全勾（**closure-audit 项留 `[ ]`——执行 session 不得自审，待独立子 agent fresh session 执行**）；Closure section 填写 evidence + 标注 audit 待办。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。roadmap Follow-up Backlog P2-5/本轮-12 标 ✅ done + 增「test/adjudication 轮执行进度」块（并声明 1712 两份 deep-audit 全部 P2 findings 收口完成）。
- **owner doc 同步**：`docs/components/industrial-hmi-editor/design-toolbox.md §4.2.1`（坐标语义裁定段）。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 107 files / 1439 tests，零回归）。
- **successor**：closure-audit 待独立子 agent（fresh session）执行；本轮-12 align-distribute world 坐标解析升级归 out-of-scope（Deferred But Adjudicated，未来 feature plan 驱动）。

### 2026-08-09（plan 2026-08-09-0648-2 industrial SCADA 编辑器图元库/工具箱可见文案 i18n 收口 — palette+toolbox i18n 轮执行）

- **plan `2026-08-09-0648-2`（roadmap Follow-up Backlog P2-6/P2-7：编辑器两面板可见文案 i18n 收口）执行完成（executor pass）**——3 Phase 全交付（Phase 1 Decision / Phase 2 locale+palette / Phase 3 toolbox+tests）。源：roadmap Follow-up Backlog（P2-6/P2-7，源 audit `docs/audits/2026-08-08-1712-multi-audit-...md:313-314`）。
  - **Phase 1（i18n key 方案裁定，Decision）**：**D1 palette = 方案 B**（由 `def.type` 派生 `industrial.scada.symbol.<type>`，不改 `ScadaSymbolDefinition` 结构——避免 Non-Goal 的结构性变更 + 24 图元定义逐个编辑）；**D2 Group 配键**（`listScadaSymbols()` 返回且 palette 现渲染全集，过滤是行为改动超 i18n 范围）；**D3 toolbox 新增 `industrial.scada.editor.toolbox.label.*` 短 label 子命名空间**（与既有长描述 tooltip 键 `toolbox.delete='Delete selected'` 等分离，紧凑按钮不塞长串）；**D4 `'1:1'` 归 glyph**（比率符号记法，中英文通用，i18n 不适用）；**D5 fallback 经显式未命中检测**（`resolved === key ? fallback : resolved`，i18next 未命中返回 key 串非 falsy，`||` 短路永不触发会渲染 raw key）。
  - **Phase 2（locale 键 + palette i18n）**：`packages/flux-i18n/src/locales/en-US.ts` + `zh-CN.ts` 各增 `industrial.scada.symbol.*`（24 键，含 Group，en/zh 对称）+ `industrial.scada.editor.toolbox.label.*`（12 键）。`src/editor/palette/editor-palette.tsx` import `useFluxTranslation`，`title` + 可见文本经 `resolveName(type, def.name)`（key 由 type 派生，显式未命中检测回退 `def.name`）。**方案 B 不改 `ScadaSymbolDefinition` 结构、不改 24 图元定义文件**。新增 `src/editor/palette/editor-palette.test.tsx`（3 focused 用例：zh 解析后文案 '矩形'/'温度计' + title 同步 + 未命中回退 `def.name` 'FallbackWidget' + raw key 不泄漏）。
  - **Phase 3（toolbox word 按钮 i18n + tests）**：`src/editor/toolbox/toolbox-panel.tsx` 新增 `labelOr(key, fallback)` helper（显式未命中检测）；12 word 按钮（Del/Group/Ungroup/Copy/Cut/Paste/Undo/Redo/Export/Import/Fit/Center）label 改 `labelOr('...toolbox.label.<k>', '<英文短词>')`；glyph 按钮 + `1:1` (D4) label 保持原符号。**所有按钮加 `data-testid`**（使行为用例 locale 无关——word label 随 locale 变化）。`toolbox-panel.test.tsx` 增 i18n reset/init `beforeEach`（确定 zh-CN 状态）+ 2 focused 用例（① zh-CN 解析：12 word 按钮 → 中文 + glyph/1:1 保持符号 + raw key 不泄漏；② 空资源未命中：12 word 按钮 → 英文短词 fallback + raw key 不泄漏，证明显式未命中检测非 `||` 短路）。修正下游 editor 用例（`scada-editor-canvas-affordance.test.tsx` + `scada-editor-canvas-reactivity.test.tsx`）word 按钮定位 `buttonByText('Del'/...)` → `data-testid`（locale 无关）。
- **验证**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿（industrial **1439 tests** = 1434 baseline + 5 新增 focused i18n 用例（3 palette + 2 toolbox），零回归）。`src/` 无 stray build artifact。
- **plan 收口**：`2026-08-09-0648-2` Plan Status `active` → `completed`（executor pass），3 Phase status + items + Exit Criteria 全勾；Closure Gates 技术项全勾（**closure-audit 项留 `[ ]`——执行 session 不得自审，待独立子 agent fresh session 执行**）；Closure section 填写 evidence + 标注 audit 待办。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。roadmap Follow-up Backlog P2-6/P2-7 标 ✅ done + 增「i18n polish 轮执行进度」块。
- **owner doc 同步**：无（裁定：方案 B 无 `ScadaSymbolDefinition` 结构变更，文案级 i18n 改动无 documented behavior 变化 → 无 owner-doc 更新；裁定记录于 plan Phase 2 + Closure Gates）。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 107 files / 1439 tests，1434 baseline + 5 new，零回归）。
- **successor**：closure-audit 待独立子 agent（fresh session）执行；剩余 P2（本轮-12 / P2-5）留待后续 mission-driver 轮的 test/adjudication plan（与本 plan Non-Goals 一致）。

### 2026-08-09（plan 2026-08-09-0648-1 industrial SCADA 设计文档文件树/引用漂移修复 — doc 轮 N1 + N2/N3 计划起草）

- **plan `2026-08-09-0648-1`（N=2 遗留「doc 轮」：纯文档计划，收口 1712 audit P2-9/P2-10/P2-11 三处 design doc §11 文件树与 live 源码引用漂移）执行完成（executor pass）**——3 Phase 全交付，纯文档 + 1 处 source stale 注释修正。源：roadmap Follow-up Backlog（P2-9/P2-10/P2-11）。
  - **Phase 1（P2-9 design-property-panel.md）**：§11 文件树 `panel-field.tsx` → `inspector-field.tsx`（与 live 文件名一致）、删除不存在的 `panel-group.tsx` 行、`inspector-panel.tsx` 注释「消费 useEditorSession」→「消费 `runtime.session` 选中图元」（与 live 消费路径一致）。同步修正 `src/editor/inspector/inspector-panel.tsx:19` stale 注释（`useEditorSession` → `runtime.session`，补「分组折叠内聚于本组件，无独立 panel-group 模块」）。
  - **Phase 2（P2-10 design-connection.md）**：§11 文件树补 `connection-drag-controller.ts` + `connection-overlay-renderer.ts`，补齐后 6 项与 live `src/editor/connection/` 6 源文件逐项对齐。
  - **Phase 3（P2-11 design-engine.md）**：§11 文件树补 `event-bridge.ts` + `interaction-overlay.ts` + `batch-add-probe.ts`，补齐后 9 项与 live `src/engine/` 9 源文件逐项对齐。
- **验证（verification mission）**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32**（0 errors；1 预存 scheduling warning 非阻塞）· `pnpm test` workspace **59/59 任务**全绿（industrial 106 files / 1434 tests，零回归）。纯文档 + 1 注释修正，无行为变更。
- **plan 收口**：`2026-08-09-0648-1` Plan Status `completed`，3 Phase + Exit Criteria + Closure Gates 全勾；Closure audit 由独立 fresh sub-agent session 完成（PASS）。`panel-field.tsx`/`panel-group.tsx`/`useEditorSession` 全仓清零。
- **N2/N3 计划起草**：roadmap Follow-up Backlog 剩余 P2 切片经 Draft Review 起草两份后续计划（均 `active`，未执行）：`2026-08-09-0648-2`（P2-6/P2-7 palette+toolbox 可见文案 i18n 收口）+ `2026-08-09-0648-3`（P2-5 collectWorldBounds 假绿测试修复 + 本轮-12 align-distribute 坐标语义裁定）。两份均经独立 fresh-session Draft Review（N2 pass / N3 pass-with-minors）。

### 2026-08-09（plan 2026-08-08-1931-2 industrial SCADA serialization/export/contract polish — parse 守卫 / clone 统一 / 导出 parity / error code 注册 / session 投影 type 执行）

- **plan `2026-08-08-1931-2`（editor+contract polish 轮 N=2：serialization 数据管线 + 公共导出契约 6 个 P2 收口）执行完成（executor pass）**——收口 1712 两份 deep-audit 的 6 个 serialization/contract 切片 P2 findings（F5 / F6 / P2-1 / P2-2 / P2-3 / P2-8），5 Phase 全交付。源：roadmap Follow-up Backlog（与 N=1 `2026-08-08-1931-1` editor interaction 切片互补；serialization/contract 切片归本 plan）。
  - **Phase 1（F5：parse 非对象守卫 + 浅拷贝共享引用）**：`packages/flux-renderers-industrial/src/serialization/parse.ts` 重写——纯对象窄化守卫（非 `null`/非 object/array 一律抛 `Error('scada config must be an object')`，fail-closed，消除旧 `shallowCopy` 的 `as ScadaConfig` cast 类型谎言，与 validate `isPlainObject` 广度对齐）+ `structuredClone` 深隔离（与调用方不共享嵌套 symbols/variables 引用，mutate 不回流）；移除 `shallowCopy`。`serialization-parse-serialize.test.ts` 重写「非对象 pass-through」测为 throw（null/array/原始值 + 运行时 `42 as unknown as object`），增强 object 分支为 `parsed.symbols[0].x=999` 不回流断言。9 测全过。
  - **Phase 2（F6：clone 实现统一）**：以 `editor-working-helpers.cloneConfigSnapshot` 为仓库内**单一** clone 实现——加深 variables 到 `.map((v) => ({ ...v }))`（修 reviewer m-1 指出的浅 `[...config.variables]` 缺口）+ viewport 浅拷贝 + background `structuredClone` + version 经 `...config` 保留原值（非硬编码 1）。`editor-session.ts` 删除本地 `cloneConfig`/`cloneNode`，import + 调 `cloneConfigSnapshot`（createScadaEditorSession / resetSession）。**额外统一 `undo-redo/undo-redo-adapter.ts` 第三处同型 clone**（`structuredCloneSafe`/`cloneNodeDeep`/`cloneConfig` 三 helper，同硬编码 `version:1`）——核对 editor-working-helpers import 闭包不含 undo-redo-adapter，无循环依赖，一并收敛为调 cloneConfigSnapshot，满足「仓库内单一 clone 实现」closure gate。failing-first：`editor-working-helpers.test.ts` + `editor-session.test.ts` 增 variables 元素深隔离 + version 保留断言。
  - **Phase 3（P2-1 / P2-3：serialization / definitions 导出 parity）**：`packages/flux-renderers-industrial/src/index.ts` 增 `export { parseScadaConfig }` / `validateScadaConfig` / `diffScadaConfig` + `export type { ScadaValidationResult }` + `export { industrialRendererDefinitions }`（与 host 校验/审计注释承诺 + 兄弟 flux-renderers-\* 包注册模式对齐）。新增 `src/index-exports.test.ts`（3 测）断言包入口 barrel 5 serialization 符号 + industrialRendererDefinitions（数组非空）+ parse→validate host 入口校验联通。
  - **Phase 4（P2-8：editor-internal-error 码注册）**：`src/editor/renderer/editor-errors.ts` `SCADA_EDITOR_ERROR_CODES` + `SCADA_EDITOR_ERROR_CODES_M1` 同步增 `'editor-internal-error'`（M1 子集，mutator applyDiff 失败属 M1 路径）。`packages/flux-i18n/src/locales/en-US.ts` 增 `'Editor internal error'` / `zh-CN.ts` 增 `'编辑器内部错误'`。`docs/components/industrial-hmi-editor/design-renderer.md §8.5.2` 码表 + 注释块增该码（语义 + 沉默缺口闭合说明）。`editor-errors.test.ts` 增 failing-first 断言（`scadaEditorErrorI18nKey('editor-internal-error')` 命中正确 key 非 `.unknown` + M1/全集 registry 含该码）。闭合先前 prod 派发码降级 `.unknown` 的沉默缺口。i18n-contract 19 测双 locale parity 通过。
  - **Phase 5（P2-2：ScadaEditorSession 公开 type 不泄漏 UndoStack）**：Decision (a) 投影 type——`editor-session.ts` 增 `export type ScadaEditorSessionPublic = Omit<ScadaEditorSession, 'undoStack'>`（impl 接口保留含 undoStack 供域内部 relative path 消费）；`editor/index.ts:17` 改导出投影 type（移除 ScadaEditorSession 导出）。grep 复核 apps/ + 兄弟包无 `session.undoStack` 外部消费者（Draft Review 已确认）。新增 `editor/editor-public-types.test.ts` type-level `@ts-expect-error` 守卫——经 `import type { ScadaEditorSessionPublic } from './index.js'` 断言公开 barrel session type 不含 undoStack（泄漏则 directive unused → typecheck 红；tsconfig 含 src 故强制）+ runtime shape sanity。
- **验证**：`pnpm typecheck` workspace **32/32** · `pnpm build` **32/32** · `pnpm lint` **32/32** · `pnpm test` workspace **59/59 任务**全绿（industrial **1434 tests** = 1423 baseline + 11 新增 focused/export/type 测，零回归；coverage gate ≥90% branches 通过）。`src/` 无 stray build artifact（build 产物仅 dist/）。
- **plan 收口**：`2026-08-08-1931-2` Plan Status `active` → `completed`（executor pass），5 Phase status + items + Exit Criteria 全勾；Closure Gates 技术项全勾（**closure-audit 项留 `[ ]`——执行 session 不得自审，待独立子 agent fresh session 执行**）；Closure section 填写 evidence + 标注 audit 待办。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。roadmap Follow-up Backlog 增「editor+contract polish 轮 N=2 执行进度」块登记 6 findings ✅ + 剩余 P2（本轮-12, P2-5～P2-7/P2-9～P2-11）留待后续 i18n/doc/adjudication 轮。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 106 files / 1434 tests，1423 baseline + 11 new，零回归）。
- **owner doc 同步**：runtime `docs/components/industrial-hmi/design-renderer.md` §4.3 反序列化行（parse fail-closed + 深隔离）+ 序列化导出归属段（5 serialization 符号 + definitions 全导出）；editor `docs/components/industrial-hmi-editor/design-renderer.md` §8.5.2 码表（editor-internal-error）。
- **successor**：closure-audit 待独立子 agent（fresh session）执行；剩余 ~10 editor/i18n/doc 层 P2（本轮-12 / P2-5～P2-7 / P2-9～P2-11）留待后续 mission-driver 轮的 i18n/doc/adjudication plan（与本 plan Non-Goals 一致）。

### 2026-08-09（plan 2026-08-08-1931-1 industrial SCADA editor interaction polish — drop 校验 / paste id 碰撞 / 批量 undo 执行）

- **plan `2026-08-08-1931-1`（editor+contract polish 轮 N=1：editor interaction 3 个 P2 papercut 收口）执行完成**——收口 1712 两份 deep-audit 的 3 个 editor interaction 切片 P2 findings（F11 / 本轮-11 / P2-4），3 Phase 全交付。源：roadmap Follow-up Backlog（与 runtime+symbols polish 轮 `2026-08-09-0121-2` 互补——后者管 runtime+symbols，本 plan 管 editor interaction；serialization/contract 切片留给 N=2 plan `2026-08-08-1931-2`）。
  - **Phase 1（F11：drop 入口符号类型校验）**：`packages/flux-renderers-industrial/src/editor/scada-editor-canvas.tsx` `onDrop` 在 `if (type && runtime)` 内增 `hasScadaSymbol(type)` 守卫（:298），未注册 / 拼写错误 / 已注销的 type 早退（静默忽略 + 经 `dispatchEvent('scada-editor:error', {code:'invalid-node'}, onError)` 上报；不调 handleError 避免置 status='error' 破坏整个编辑器——drop 被忽略是 non-fatal）。`invalid-node` 已在 `SCADA_EDITOR_ERROR_CODES`（editor-errors.ts:16）注册，不引入新码。复用既有 `hasScadaSymbol(type)`（symbol-registry.ts）。failing-first：drop 未注册 type 时 addWorkingSymbol 不被调用 / working copy 无新增节点 + onError 收到 `invalid-node`。
  - **Phase 2（本轮-11：paste id 碰撞自增）**：`packages/flux-renderers-industrial/src/editor/toolbox/clipboard.ts` `buildClipboardPaste` 接收 `existingIds?: Set<string>`（call site toolbox-runtime.ts:204-211 传 live id 集）；顶层 id 生成（:90 起 `taken` seeded from existingIds）+ 子树 `buildIdMapRecursive`（:132-147 经 `resolveUniquePasteId` :155-166）碰撞时 while-loop 自增后缀直到不碰撞（do/while 同 `groupSymbols`/`addWorkingSymbol`/`generateConnectionId` 形，无第二套去重策略）。connection target/id 重写（plan 2026-08-08-1910-2 A7 已落地）经 idMap 用新自增 id。failing-first：working copy 已含 `foo-copy-1` fixture，paste `foo` → 新节点 id `foo-copy-2`（5 case：top-level/multi/child/zero-regression/intra-paste uniqueness）。
  - **Phase 3（P2-4：多元删除/解组单 undo entry）**：Phase 3 Decision 选 **option (a)**（侵入面最小，与 transform-move 事务式单 push 同形）。`packages/flux-renderers-industrial/src/editor/runtime-mutators.ts` `removeWorkingSymbol` 与 `ungroupSymbols` 签名升级为 `string | string[]`（:92 / :212）——`Array.isArray` 归一为 ids 数组 → 单次 `prevSnapshot = cloneConfigSnapshot(...)`（:99 / :218）→ 对 working copy 直接 mutate（remove: `detachNodesRecursive` + `pruneDanglingConnections` 单次 over idSet；ungroup: 循环 mutate 内部 `symbols` 数组，非 N 个 single-id mutator）→ 末尾单次 `undoRedo.pushOperation(...)`（:113 / :238）。三站点传完整 selection 数组：按钮路径 `toolbox-panel.tsx` `handleDelete`/`handleUngroup`（:116 / :126，传 `selection`）+ 键盘路径 `scada-editor-canvas.tsx` Delete（:340）/ Ctrl+Shift+G（:355）（传 `sel`）。执行中清理被 Phase 3 改造孤立的 dead code `removeNodeRecursive`（runtime-mutators.ts 旧 single-id 解链函数，已被 `detachNodesRecursive` 替代但定义残留，拖累 branch coverage 从 90%+ 跌到 89.94% → 删除后回到 ≥90%；同步更新 editor-working-helpers.ts:179 + runtime-mutators.ts doc-comment 引用从 `removeNodeRecursive` → `detachNodesRecursive`）。failing-first 5 case：batch delete N=3 → 单 entry + 一次 undo 全恢复；batch delete group 子树 → 单 entry + undo 恢复子树；batch ungroup N=2（键盘 Ctrl+Shift+G 路径）→ 单 entry + undo 恢复两 group；single-id delete/ungroup regression 仍 1 entry；button path（toolbox-panel.test.tsx）断言 `removeWorkingSymbol[0]===['a','b','c']` / `ungroupSymbols[0]===['a','b']`。connection 恢复经 `editor-state-integrity.test.ts:338` snapshot-based undo restores pruned connection 锁定（批量路径 prevSnapshot 同形）。
- **验证**：`pnpm typecheck` workspace 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32 · `pnpm test` workspace 59/59（industrial **1423 tests** = 1409 baseline + 14 新 failing-first，零回归；coverage branches ≥90% threshold 通过——dead code 清理后从 89.94% 回到合规态）。公共 mutator 签名（`removeWorkingSymbol`/`ungroupSymbols`）放宽为 `string | string[]`（向后兼容单 id 调用）；clipboard `buildClipboardPaste` 增 optional `existingIds` 参（向后兼容）；新增/删除均 internal-only。
- **plan 收口**：`2026-08-08-1931-1` Plan Status `active` → `completed`（closure audit PASS 后回写），3 Phase status + Exit Criteria + Closure Gates 全勾；Closure section 填写 fresh session audit evidence。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤跳过。roadmap Follow-up Backlog 增「editor+contract polish 轮 N=1 执行进度」块登记 3 findings ✅ + 剩余 P2（F5/F6, 本轮-12, P2-1～P2-3/P2-5～P2-11）留待 N=2 serialization/contract plan。
- **closure audit**：**PASS（fresh session `ses_01c9a87cbffex8daqTplfZf6Pc`，mission-driver closure-audit step，非执行 session 自审）** verdict=`approved`（0 Blocker / 0 Major / 0 Minor）。独立 live 复核：3 phase fix 全部 live & wired（非 stub）—— F11 scada-editor-canvas.tsx:298 hasScadaSymbol 守卫在 addWorkingSymbol 前；本轮-11 clipboard.ts:78-166 buildClipboardPaste existingIds + while-loop 自增 + buildIdMapRecursive 子树碰撞；P2-4 runtime-mutators.ts:92-116/212-241 单 prevSnapshot + 单 pushOperation（非 N 循环），三站点（toolbox-panel.tsx:116/126 + scada-editor-canvas.tsx:340/355）传完整 selection 数组。focused 测试均断言可观测结果。dead code `removeNodeRecursive` repo-wide grep `packages/*/src/` 零命中。owner-doc gate 裁定 not required（design-undo-redo.md:196/198 已声明单次删除/解组=1 diff；design-renderer.md:348 对 drop type validation silent；无 documented behavior 改变，Minimum Rule 17）。deferred honesty 通过。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 104 files / 1423 tests，1409 baseline + 14 new，零回归；coverage branches ≥90%）。

### 2026-08-09（plan 2026-08-09-0121-2 industrial SCADA P2 polish — runtime lifecycle & symbols author safety 执行）

- **plan `2026-08-09-0121-2`（roadmap Follow-up Backlog runtime+symbols 层 P2 收口轮）执行完成**——收口 1712 两份 deep-audit 的 14 个 P2 findings（runtime engine/binding/renderer-hooks + symbols 层），2 Workstream 全交付。源：roadmap Follow-up Backlog（显式预期的「后续 polish 轮」）。与 plan `{0121-1}`（e2e healing）独立可并行。
  - **Workstream A（Engine 生命周期 + Renderer Hooks + Binding 资源安全）9 项**：
    - **本轮-2**（`engine/scada-engine.ts`）：`interactionOverlay` getter 加 `if (this.destroyed) return undefined` 早退，防销毁后惰性重建 overlay 到已 destroy 的 app。
    - **本轮-3**（`renderer/scada-canvas.tsx`）：engineRef/runtimeRef 同步 effect 加 cleanup（`return () => { engineRef/runtimeRef.current = null/undefined }`），闭合本轮-2 可达链（unmount 后异步闭包不再触达死引擎）。
    - **本轮-1**（React Compiler 纪律对齐）：逐处裁定 scada-canvas.tsx 的 useCallback/useMemo——**移除 4 处纯优化 memo**（handleReady/handleError/reportDiagnostic/getPointValuesForLatest，消费者均经 latest-ref 转发，身份不进 effect deps）；**保留 3 处**（useMemo(parsedConfig) 身份流入 config-sync/points-bridge effect deps；handleDestroyed/reloadConfig 进 useScadaHandles effect deps）。hooks 内部 memo 链（eventsApi/reportOnce 等）裁定保留——身份流入公共返回或 effect deps，**test 环境无 React Compiler**（vitest.shared 不走 babel/react-compiler，仅 playground vite 走）故移除有 churn 风险。
    - **本轮-10**（`hooks/use-scada-config-sync.ts`）：config 身份未变时 effect 早退（`config === prevRef.current` → return），消除 reloadBindings→setRuntime(新对象身份)→effect 再跑空 diff 的冗余第二轮。
    - **本轮-13**（`hooks/use-scada-points-bridge.ts`）：compiledCache/lastReportedErrors/pointSnapshot 清理 effect deps 增 `expressionCompiler`（旧编译产物喂新 evaluator 错配）。
    - **F7**（`binding/animator.ts`）：pause 全 active 时 `stopClock`（+ `allPaused` helper）+ tick 跳过 paused 项 collect + 仅 advanced 时续帧，消除 pause 后 rAF 每帧空转重算冻结增量。
    - **F8**（`binding/refresh-pipeline.ts`）：建反向索引 `reverseDeps: Map<depPointId, Set<exprPointId>>`（`setExpressionDeps` 单一入口同步维护 lastDeps + 反向边，撤旧加新），`recomputeExpressionPoints` 由 O(n²) lastDeps 线性扫描降为 O(扇出) 查找。
    - **本轮-4/F9**（`engine/event-bridge.ts` + `binding/point-store.ts`）：去重键加 call-site 维度（event-bridge：`${site}:${message}`，point-store 已含 pointId）+ Set 256 上限（超限整体清空，report-once-until-reset）。
    - **F10**（`serialization/validators/binding.ts`）：`binding.scale` 校验从仅 `isPlainObject` 升级为与 declaration scale 同形 finite k/b 校验（复用 1809-1 `isFiniteNumber` via assertShape；expression-scale 分支校验非空字符串）。
  - **Workstream B（Symbols 作者安全 + 几何 Polish）5 项**：
    - **本轮-5**（`symbols/visual-state.ts`）：revert 仲裁的 `instanceBindings` 从读 raw 实例改为合并 `defaults.bindings ∪ instance.bindings`，防自定义符号 defaults 级 binding 经 revert 被覆盖。
    - **本轮-6**（`symbols/base-shapes/round-rect.ts` + `symbol-types.ts` + `config-types.ts` + `diff.ts`）：cornerRadius 从固定 8 改为 `min(w,h)*0.08`（100×100 仍 8 向后兼容）+ per-instance `cornerRadius` override（钳到 min(w,h)/2）；同步 ScadaSymbolProps/ScadaSymbolNode/SYMBOL_KEYS（过 check-scada-symbol-keys guard）。
    - **本轮-7**（`symbols/pipe/pipe-junction.ts`）：bidirectional 连线补 `startArrow`（旧仅 endArrow，与 'out' 同形语义误导）。
    - **本轮-8**（`symbols/composite.ts`）：`createCompositeGroup` 空 children 守卫——console.warn 结构化上报 + fallback 透明 Rect body（visible:false），防 `body=undefined` 致 applyCompositeProps BODY_FIELDS 路由 setAttrs(undefined) 崩溃。
    - **本轮-9**（`symbols/composite.ts`）：Group 节点 attrs 仅承接 root 级字段（x/y/rotation/visible/opacity/scale），过滤 width/height/fill/stroke/strokeWidth/shadow/text/fillStyle（无渲染意义，写 width/height 改变 leafer bounds 语义）；re-grep 确认 A1 P0 around:'center' 修复未触碰 toShapeAttrs。
  - **focused 测（每 Fix 配，断言正确结果非 not-throw）**：animator F7×2（pause 停时钟 + partial pause 不停）、refresh-pipeline F8（4 级链 + 50 无关点只重算受影响子集）、event-bridge/point-store 本轮-4/F9×3（异 site/pointId 不互吞 + 256 cap reset）、binding F10（NaN/Infinity/expression 非空 + 合法 linear/expression）、symbols round-rect 本轮-6（缩放 + override + cap）、pipe-junction 本轮-7（双向箭头）、composite 本轮-8/9×2（空 children fallback + Group attrs）、engine 本轮-2（destroyed getter）、visual-state 本轮-5（defaults binding revert 保留）、config-sync 本轮-10（同身份不重跑）、points-bridge 本轮-13（compiler 换身份清 cache）。2 处旧测断言对齐新行为（round-rect cornerRadius 50×50→4 / 60×40→3.2）。
- **验证**：`pnpm typecheck` 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32（check-scada-symbol-keys guard passed；1 预存 scheduling warning 非阻塞）· `pnpm --filter @nop-chaos/flux-renderers-industrial test` **1409 tests** 零回归（1394 baseline + 15 new）/ coverage 96.56%（branches 90.1%+，过 90% threshold）· `pnpm test` workspace **59/59 任务**全绿。
- **plan 收口**：`2026-08-09-0121-2` Plan Status `active` → `completed`，2 Workstream status + items + Exit Criteria 全勾；Closure Gates 全勾（含 closure-audit）。roadmap Follow-up Backlog 增「runtime+symbols polish 轮执行进度」块登记 14 findings ✅ + 剩余 P2（F5/F6/F11/本轮-11·12/P2-1～P2-11）留待 editor+contract polish plan。无 `> Source Audits:` 行（roadmap-sourced plan），source audit closure 步骤省略。
- **Full-green verification**：typecheck/build/lint 32/32、test workspace 59/59 tasks（industrial 104 files / 1409 tests，1394 baseline + 15 new，零回归）。
- **Closure audit PASS**：独立子 agent fresh session `ses_01d30f469ffe5eiZOIzZ0mkj03`，pass-with-minors，0 Blocker / 0 Major / 1 Minor（m-1 本轮-1 scope 缩窄至 canvas 级 4 处 useCallback，hooks 内部 memo 链裁定保留并记为 follow-up；非正确性缺口，认可文档化判断）。
- **successor**：no remaining plan-owned work（14/14 findings fix landed + focused 测 landed）。剩余 ~16 editor/serialization/export/docs 层 P2 留待后续 mission-driver 轮的 editor+contract polish plan（与本 plan Non-Goals 一致）。

### 2026-08-09（plan 2026-08-09-0121-1 industrial SCADA e2e residual healing 执行）

- **plan `2026-08-09-0121-1`（HCA-CV deferred e2e successor + 1809-3 P1-5 e2e 验证延期收口）执行完成**——重跑 6 scada e2e spec，诊断 + healing HCA-CV 显式「Successor Required: yes」的 8 个 e2e 残留（7 hover/click + 1 perf guard），3 Phase 全交付。源：HCA-CV plan `2026-08-08-1527-1` Deferred #1/#2 + 1809-3 plan P1-5 e2e 验证延期。
  - **Phase 1（baseline re-verification）**：重跑 scada-demo / scada-edge-cases / scada-perf。P1-5 landed 后 baseline：demo 11 pass / 4 fail（click pump-1 @world 350,278、dblclick motor-1 @136,134、hover pump-1 ×2）；edge-cases 4 pass / 2 fail（hover line @200,240、hover polygon @480,180）；perf :155 TE-1 fail（pointer-drag viewport 未变）。已知 8 失败点中 P1-5 先行修好 1（demo 5→4），baseline 见 7 fail。
  - **Phase 2（diagnose & fix）**：经 page.evaluate / elementFromPoint / getComputedStyle / 临时 console.error 探针程序化诊断（**零截图**，守 AGENTS.md e2e 诊断纪律），定位**两个独立根因**，落地 2 Fix + 1 failing-first 回归测试：
    - **Fix-1（核心）：ready 态占位 DIV 拦截 pointer 事件**（`renderer/scada-canvas.tsx:328`）。ready 分支渲染 `<div class="nop-scada-canvas-canvas" />`，leafer 也在同 container 创建 `<canvas>`（marker effect 同加该 class）；React 在 leafer mount 后重渲染该 DIV 使其叠在 leafer canvas 之上 → 拦截 pointermove/tap → EventBridge 永不收事件 → hover/click overlay/dialog/navigate 全失效。证据：`elementFromPoint(pump 目标)` 返回该 DIV（非 leafer canvas）；handlePointerMove 不触发；即便 viewport settle 后 overlay 仍 `{has:false,n:0}`。Fix：ready 占位 DIV 加 `pointer-events-none`，事件穿透到 leafer sky 层（leafer canvas 同名 class 不受影响）。**此 Fix 同时修好 scada-perf:155 TE-1**（pointer-drag 现达 leafer → zoomLayer 变 → guard pass）。
    - **Fix-2：width/height prop effect 把 schema 尺寸当 DOM 尺寸**（`renderer/hooks/use-scada-engine.ts` width/height effect）。旧 `latest.current.width ?? container.clientWidth` 把 schema width（960，world design space）当 leafer DOM 尺寸 → mount 期 setSize(960,520) 覆盖构造器 container 尺寸(303) → applyInitialViewportState 用错配 size(960) fit → scale≈1，ready 在 ResizeObserver 修正前触发 → pointer 落画布外。证据（探针）：ctor containerWidth=303→size{303,544}，紧接 effect setSize{960,520} 覆盖，fit size{960,520}→scale 1。Fix：container 优先（container-driven DOM sizing，P1-5「container 真实尺寸」），schema 仅 container=0（jsdom）fallback；setSize 后调 `refitViewportOnResize`（与 ResizeObserver handler 对称）。
    - **Fix-1 + Fix-2 合计消减 5/7**：demo click/dblclick/hover×2（4）+ perf :155 TE-1（1）。demo 15/15、perf 5/5。
    - **failing-first 回归测试**：`tests/e2e/scada-pointer-events-regression.spec.ts` 断言 ready 占位 DIV `pointer-events:none` 且 elementFromPoint 不落在该 DIV（防 Fix-1 静默回退）。
  - **Phase 3（full suite + triage）**：6 scada e2e spec 全量 + 回归 spec = **32 passed / 2 failed**（2 watch-only residual，零 unexplained、零 regression：pressure/editor 全绿；industrial unit 1394/1394；typecheck/build/lint 32/32）。scada-perf:155 裁定视口相关，随 Fix-1 收口（非 perf 包络退化）。
  - **残留 2 诚实裁定（Deferred But Adjudicated R1/R2，证据可复现）**：①**R1 edge line**（geometric，EP-3 类）— container-driven 后 edge canvas DOM=1214×288，line-poly content bounds 480×120 fit height-binding scale 2.4，line（content 底沿 world y=240）映射 screen y=288=canvas height 贴底边 sub-pixel 离屏；DIAG probe 实测 `line-overlay-after-settle=0`（geometric 非时序）；根因在 edge 页面布局（canvas 容器仅 288 高），out-of-scope。②**R2 edge polygon**（leafer 渲染时序）— screen-switch reset rebuild scene 后 leafer 渲染需一帧，ready 信号在渲染前触发，test 单次 mouse.move at ready 被 miss + 静止鼠标不重触发 hover → overlay 永不出现；DIAG probe 实测 `polygon-overlay-after-settle=1`（时序非几何）；demo fresh mount 因 mount 链路更长 test 动作前 leafer 已渲染故命中。
- **验证**：`pnpm typecheck` 32/32 · `pnpm build` 32/32 · `pnpm lint` 32/32（industrial）· `pnpm --filter @nop-chaos/flux-renderers-industrial test` **1394 tests** 零回归 · 6 scada e2e 32 pass / 2 watch-only residual。fit padding 实验（修 R1）全面 regressed perf+empty+default-geometry，已 revert（证据记录于 plan）。
- **plan 收口**：`2026-08-09-0121-1` Plan Status `active` → `completed`，3 Phase status + Exit Criteria + Closure Gates 全勾（closure-audit gate 留 [ ]——执行 session 不自审，待后续独立 fresh-session closure-audit 覆盖）。roadmap HCA-CV 行 + header 登记 e2e successor 执行（5/7 消减）；project-context.md C0 baseline 补 industrial scada e2e 残留收口记录。无 `> Source Audits:` 行（plan 无此 label），source audit closure 步骤省略；plan `> Work Item: e2e-residual-healing` 是 HCA-CV deferred successor（roadmap 无独立 ❌/✅ 行，经 HCA-CV 行 + header 注记 tracking）。
- **successor**：no remaining plan-owned work（5/7 fixed landed；2 watch-only residual Successor Required: no）。可选（非义务）：①playground edge 页面给 canvas 容器显式高度 ≥ schema（治 R1，playground 布局 mission）；②engine lifecycle reset 后 gate ready on leafer render（治 R2，独立 concern）。

### 2026-08-09

- Closed plan `docs/plans/2026-08-08-1910-3-industrial-scada-viewport-guard-meta-contract.md`（A3 + A4，second-wave remediation 第二波第 3 卡）。
- **A3 零缩放守卫自毁收口**（`packages/flux-renderers-industrial/src/engine/scada-engine.ts`）：`readZoomLayerScale` 显式拒 0（`&& scaleX !== 0` → 回退 `this.viewport.scale`），闭合 P2-8 早退守卫的「自毁环路」（守卫检测到 0 调 `syncViewportFromZoomLayer` 再读回 0 落地 `viewport.scale=0` → `viewportToWorld` 除 0 产 Infinity → EventBridge world 坐标 corrupt → zoomLayer 矩阵不可逆）；`handlePluginMove` 补与 `handlePluginZoom` 同形零守卫（defense-in-depth）。
- **A4 runtime scada-canvas 遵守 meta 契约**（`packages/flux-renderers-industrial/src/renderer/scada-canvas.tsx`）：与同包编辑器画布 `scada-editor-canvas.tsx:217` 同纪律——disabled 时不下挂图元事件回调（EventBridge 闭包经 optional chaining 早退 → dispatch 不执行、hover 覆盖物不跟随）+ wrapper `aria-disabled`/`inert`；visible:false 时 renderer 层 return null（不创建引擎）。
- 新增 failing-first 7 测：`engine/scada-engine-plugin-sync.test.ts`（+2，zoom/move scaleX=0 → viewport.scale 非零有限）+ `renderer/scada-canvas-meta-contract.test.tsx`（新文件，5 测：disabled 不派发 + sanity 派发 + aria-disabled/inert + sanity 无 aria + visible:false 不渲染）。
- Owner doc 同步：`docs/components/industrial-hmi/design-engine.md` §4.4（A3 增补）+ `docs/components/industrial-hmi/design-renderer.md` §5（A4 契约增补）；roadmap `docs/backlog/industrial-hmi-component-audit-roadmap.md` 第二波执行进度登记 1910-3 ✅ completed。
- **Full-green verification**：typecheck 32/32、build 32/32、lint 32/32、test 59/59 tasks（industrial 104 files / 1394 tests，1387 baseline + 7 new，零回归）。
- Closure audit PASS：独立子 agent fresh session `ses_01da66e23ffeKtPSwMpflIsUTB`，0 Blocker / 0 Major / 0 Minor，一轮共识。
