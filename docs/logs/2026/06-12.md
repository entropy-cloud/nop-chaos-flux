# Development Log - 2026-06-12

### 2026-06-12

- Added a manual test record for `condition-builder` AND/OR toggle lag in `docs/testing/2026/06-12.md`, documented the bug in `docs/bugs/63-condition-builder-conjunction-hover-selected-visual-lag-fix.md`, and diagnosed it with a focused regression test at `packages/flux-renderers-form-advanced/src/condition-builder/condition-builder-latency.test.tsx`.
- Verified the logic path is healthy: the pressed state updates normally and a single conjunction change stays within a bounded number of form store commits, so the reported slowness was not a store/update-latency regression.
- Fixed the perceived lag by removing the conjunction pill color transition in `packages/flux-renderers-form-advanced/src/condition-builder/condition-group.tsx`, switching the AND/OR toggle from `transition-colors` to `transition-none` so the selected state appears immediately.
- Link to doc or code path: `docs/testing/2026/06-12.md`, `docs/bugs/63-condition-builder-conjunction-hover-selected-visual-lag-fix.md`, `packages/flux-renderers-form-advanced/src/condition-builder/{condition-group.tsx,condition-builder-latency.test.tsx}`

- Tightened the automated-test baseline so Vitest and Playwright now bootstrap with strict schema validation enabled and schema compile diagnostics configured as fail-fast gates. This prevents playground/test schemas from silently compiling through invalid `component:<method>` payload contracts during automated runs.
- Added regression coverage for compile-time rejection of `component:submit` payloads when the target form capability publishes no args contract in `packages/flux-compiler/src/schema-compiler-host-action-validation.xui-actions.test.ts` and aligned `packages/flux-react/src/__tests__/schema-renderer.test.tsx` with the stricter automated-validation baseline.
- Fixed the `#/flux-basic` key-value demo submit wiring so `component:submit` no longer passes unsupported `{ method, url }` payload into the form component handle. The form now owns `/api/composite-demo` through `submitAction`, which restores normal validation-blocked behavior and prevents debugger-only action failures.
- Added a page-level regression in `apps/playground/src/pages/flux-basic-page.debugger.test.tsx` covering `Add metadata pair -> Submit key-value demo`, asserting the page surfaces `Entry 1 key is required` and does not emit `/api/composite-demo` request traffic when validation blocks submission.
- Added the manual test record at `docs/testing/2026/06-12.md` for the original user-reported repro and resolution.
- Link to doc or code path: `vitest.shared.ts`, `test-setup/strict-validation.ts`, `playwright.config.ts`, `packages/flux-compiler/src/schema-compiler.ts`, `packages/flux-compiler/src/schema-compiler-host-action-validation.xui-actions.test.ts`, `packages/flux-react/src/__tests__/schema-renderer.test.tsx`, `apps/playground/src/pages/{fluxBasicPageSchema.json,flux-basic-page.debugger.test.tsx}`, `docs/testing/2026/06-12.md`
