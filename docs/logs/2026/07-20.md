# Development Log - 2026-07-20

### 2026-07-20 (S3 — Gantt Advanced Features Complete)

- **S3 — Gantt Advanced Features (Resource Load, Baseline, Scheduling, Export)**: Implemented the full S3 feature set on top of S1+S2 Gantt baseline.
  - **Phase 1 — Design Docs**: Created 4 design docs under `docs/components/gantt/`: `design-export.md`, `design-filter-sort-group.md`, `design-responsive.md`, `design-multi-select-batch.md`. Verified S3.1–S3.4 design content in existing §12 sections.
  - **Phase 2 — Resource Load + Baseline/Compare View**: Implemented `ResourceLoad` calculation engine (`components/resource-load.ts`), dual-pane layout (`ResourceLoadGrid`, `ResourceLoadTimeline`, `ResourceLoadView`), `BaselineBars` rendering component, `criticalPath` algorithm (`components/critical-path.ts`). Added `GanttBaseline` type to `gantt.types.ts`. 20+ unit tests for load calculation edge cases, 7 critical path tests (chain/parallel/branching/lag topologies).
  - **Phase 3 — Undo/Redo + Auto-Scheduling**: Implemented `UndoStack` command pattern (`undo-stack.ts`) with `UpdateTaskCommand`, `AddLinkCommand`, `RemoveLinkCommand`, `BatchUpdateTaskCommand` — all supporting undo/redo/merge. 13 unit tests for stack lifecycle, merge behavior, limit enforcement. Implemented `SchedulerConfig` auto-scheduling panel with direction/constraint type/date picker and "Trigger Re-schedule" button.
  - **Phase 4 — Filter/Sort/Group + Responsive + Multi-Select**: Implemented `FilterBar` with 300ms debounce, column-header sort with `aria-sort`, `groupBy` select. `GanttCompact` component with `ResizeObserver`-based compact mode and Fullscreen API integration. `MultiSelect` model with Shift+Click range and Ctrl+Click toggle. 8 unit tests for multi-select model, 4 for filter/sort.
  - **Phase 5 — Export + Animations + S0.3 + Closure**: Implemented `export-handles.tsx` with dynamic import of html2canvas/jspdf/xlsx for PDF/PNG/Excel export. Enhanced visual design tests (drag ghost, selected row, hover, empty placeholder). Updated `examples.manifest.json` runtime array with barcode-input and diff-view. Updated `roadmap-scheduling.md`: S3 phase done, all 9 S3 items done, S0.3 done.
  - **Files created (components/)**: `resource-load.ts`, `resource-load-grid.tsx`, `resource-load-timeline.tsx`, `resource-load-view.tsx`, `baseline-bars.tsx`, `critical-path.ts`, `export-handles.tsx`, `filter-bar.tsx`, `gantt-compact.tsx`, `multi-select.tsx`, `scheduler-config.tsx`, `undo-stack.ts`, plus test files for each.
  - **Design docs created**: `docs/components/gantt/design-export.md`, `design-filter-sort-group.md`, `design-responsive.md`, `design-multi-select-batch.md`.
  - **Tests**: 336/336 pass (32 test files in scheduling package), typecheck ✓ (56/56), build ✓ (30/30), lint ✓ (0 errors).
  - **Execution plan**: `docs/plans/2026-07-20-2100-1-s3-gantt-advanced-plan.md` — all 5 phases complete. Closure audit gate left for independent sub-agent per AGENTS.md.

### 2026-07-20 (continued)

- **S7 — Kanban Advanced Features (Column Resize, Virtual Scroll, WIP, Tags, Activity Log, Undo/Redo, Export, Collab)**: Implemented the full S7 feature set on top of S6 Kanban Core.
  - **Phase 1 — Design Docs**: Created `docs/components/kanban/design-undo-redo.md` and `design-export-snapshot.md`. Verified all §12 design references against live S6 codebase.
  - **Phase 2 — Column Resize + Virtual Scrolling**: Implemented `useKanbanColumnResize` hook (pointer-based resize, min/max clamping, state persistence). Implemented `useKanbanVirtualizer` hook (per-column `@tanstack/react-virtual` FixedSizeList, overscan 5). Integrated resize handle into `KanbanColumnHeader` and `KanbanColumn`. Autofit mode support. Unit tests: 12 resize constraint cases, 3 virtualizer lifecycle cases.
  - **Phase 3 — WIP Limits + Tags/Colors/Members**: Created `KanbanWipBadge` (cardLimit display, red +N overflow, strict mode alert-triangle icon). Created `KanbanCardTags` (color dot, tag pills with +N collapse, member avatar stack with initials fallback). Created `KanbanTagFilter` (multi-select OR filter pills, clear button, `filterTags` integration). Updated `kanban.types.ts` with `wipStrict`, `filterTags`, `onCardUpdate`. Updated `useKanbanDnd` with `wipOverLimitColumns` canDrop rejection. Integrated tag filter and WIP into `KanbanBoard`. Unit tests: 8 WIP badge tests, 8 card-tags tests, 6 tag-filter tests.
  - **Phase 4 — Activity Log + Undo/Redo**: Implemented `kanban-undo-stack.ts` (command pattern UndoStack with push/undo/redo/canUndo/canRedo/shouldMerge/FIFO eviction at 1000). Implemented `KanbanActivityLog` (side panel, time-descending list, column/type filtering, formatted descriptions, relative timestamps). Wired undo/redo into `KanbanBoard` (Ctrl+Z/Ctrl+Shift+Z keyboard handlers, toolbar buttons with disabled state). Wired action recording via `onCardMove` event hook. Unit tests: 13 undo stack lifecycle/merge tests, 6 activity log rendering/filter tests.
  - **Phase 5 — Export/Snapshot + Collab + Closure**: Implemented `kanban-export.ts` (boardDataToJson/boardDataFromJson round-trip, exportBoardToPng with html2canvas dynamic import, downloadBlob). Implemented `useKanbanCollab` hook (WebSocket connect/disconnect/sendMessage, connection status tracking connected→reconnecting→disconnected with 3s auto-reconnect). Installed html2canvas dependency (also fixed pre-existing Gantt type errors). Updated `kanban/index.ts` with all new exports.
  - **Files created**: `hooks/use-kanban-column-resize.ts`, `hooks/use-kanban-virtualizer.ts`, `components/kanban-wip-badge.tsx`, `components/kanban-card-tags.tsx`, `components/kanban-tag-filter.tsx`, `utils/kanban-undo-stack.ts`, `components/kanban-activity-log.tsx`, `utils/kanban-export.ts`, `hooks/use-kanban-collab.ts`, plus 8 test files.
  - **Design docs created**: `docs/components/kanban/design-undo-redo.md`, `design-export-snapshot.md`.
  - **Updated**: `kanban.types.ts` (wipStrict, filterTags, onCardUpdate), `kanban-board.tsx` (all S7 features), `kanban-column-header.tsx` (resize handle, WIP styling), `kanban-column.tsx` (virtual scroll, resize, WIP), `kanban-card.tsx` (tags/colors/members), `use-kanban-dnd.ts` (wipOverLimitColumns canDrop), `index.ts` (all new exports), `docs/components/roadmap-scheduling.md` (S7 all done).
  - **Tests**: 406/406 pass (41 test files), typecheck ✓ (pre-existing Gantt jspdf/xlsx errors out of scope), build ✓ (same).
  - **Execution plan**: `docs/plans/2026-07-20-2100-2-s7-kanban-advanced-plan.md` — all 5 phases complete. Closure audit gate left for independent sub-agent per AGENTS.md.

- **S6 — Kanban Core (Read-Only Board With Drag Interaction)**: Implemented the full Kanban board renderer with flat dictionary BoardData model, pure function state helpers (moveCard/moveColumn/addCard/removeCard/changeCard/addColumn/removeColumn), board/column/card/column-header components with region support (columnHeader/columnHeaderToolbar/cardTemplate/columnFooter), drag-and-drop via `@atlaskit/pragmatic-drag-and-drop` (cross-column card move, intra-column sort, column reorder via header drag handle), card filter/search with 300ms debounce and custom filterCard function, column collapse with local state, add/delete card and column operations via useKanbanAdder hook, default visual design (loading skeleton, empty state, hover lift, drag ghost, drop indicator, collapse animation), and S10.3 Kanban playground test page with sample data (4 columns, 8 cards).
  - Files created (kanban/): `kanban.types.ts`, `kanban-helpers.ts`, `kanban-board.tsx`, `kanban-column.tsx`, `kanban-card.tsx`, `kanban-column-header.tsx`, `hooks/use-kanban-dnd.ts`, `hooks/use-column-dnd.ts`, `hooks/use-kanban-filter.ts`, `hooks/use-kanban-adder.ts`, plus test files `kanban-helpers.test.ts`, `kanban-renderer.test.tsx`, `hooks/use-kanban-filter.test.ts`, `hooks/use-kanban-dnd.test.ts`, `hooks/use-column-dnd.test.ts`, `hooks/use-kanban-adder.test.ts`.
  - Updated: `package.json` (added @atlaskit/pragmatic-drag-and-drop), `schemas.ts` (re-export KanbanSchema from kanban.types.ts), `scheduling-renderer-definitions.ts` (kanban component from `() => null` to real KanbanBoard with 30+ fields), `kanban/index.ts` (exports), `styles.css` (Kanban styles), `apps/playground/src/App.tsx` (kanban route), `apps/playground/src/pages/home-page.tsx` (nav card), `apps/playground/src/pages/kanban-demo.tsx` (new), `docs/components/roadmap-scheduling.md` (S6 done, S10.3 done).
  - Dependencies added: `@atlaskit/pragmatic-drag-and-drop@^1.5.0`.
  - Tests: 264/264 pass (20 test files, 6 new), typecheck ✓ (56/56), build ✓ (30/30), lint ✓ (0 errors).
  - Execution plan: `docs/plans/2026-07-20-2000-2-s6-kanban-core-plan.md` — all 4 phases complete. Closure audit gate left for independent sub-agent per AGENTS.md.

### 2026-07-20

- **S0 — Scheduling Package Infrastructure**: Created `packages/flux-renderers-scheduling/` with scaffolding (package.json, tsconfig, vitest, schemas.ts, scheduling-renderer-definitions.ts, index.ts, styles.css, stub directories gantt/kanban/calendar, unit test).
- **Monorepo Registration**: Added package reference to root `tsconfig.json`, paths to `tsconfig.base.json`, and aliases to `vite.workspace-alias.ts`.
- **Playground Registration**: Imported and called `registerSchedulingRenderers()` in `apps/playground/src/App.tsx`.
- **Roadmap Sync**: Updated `docs/components/roadmap-scheduling.md` S0.2/S0.3 from `proposed` to `planned`, S1.1–S1.6 from `proposed` to `done`.
- **Full verification (S0)**: typecheck ✓ (56/56).
- **Execution plan**: `docs/plans/2026-07-20-0800-1-s0-scheduling-infrastructure-plan.md` — Phase 1-3 complete.

- **S1 — Gantt Core Engine (Pure Logic Layer)**: Implemented `GanttStore`, typed data models (GanttTask/GanttLink/GanttResource/GanttAssignment), WBS tree management, time scale engine (6 zoom units: hour/day/week/month/quarter/year), zoom engine with scroll anchoring, pixel coordinate layout computation (date↔pixel round-trip, task→$x/$y/$w/$h, link→$p), and WorkCalendar with three-level fallback (resource→task→global). All modules as pure TypeScript in `packages/flux-renderers-scheduling/src/gantt/`.
  - Files created: `gantt.types.ts`, `gantt-store.ts`, `gantt-utils.ts`, `utils/date.ts`, `utils/scale.ts`, `utils/layout.ts`, `utils/worktime.ts`, plus corresponding `*.test.ts` files.
  - Updated: `schemas.ts` (Gantt/Kanban/Calendar schema types), `scheduling-renderer-definitions.ts` (3 renderer definitions).
  - Tests: 114/114 pass (7 test files), typecheck ✓, build ✓, lint ✓.
  - Execution plan: `docs/plans/2026-07-20-0800-2-s1-gantt-core-engine-plan.md` — all 4 phases complete, closure audit approved.

- **S4 — Calendar Read-Only Core (Month/Week/Day Views)**: Implemented calendar types, date utilities, state hooks, navigation, event positioning algorithms (month view packing, multi-day event splitting, week/day view vertical percentage positioning, concurrent width allocation), conflict detection, view rendering components (month/week/day views, event block, resource header, navigation header), main Calendar orchestration component with imperative handle and virtual scrolling via `@tanstack/react-virtual`, calendar CSS styles, and full renderer registration.
  - Files created (calendar/): `calendar.types.ts`, `calendar.tsx`, `utils/calendar-date-utils.ts`, `utils/calendar-layout-utils.ts`, `utils/calendar-time-utils.ts`, `hooks/use-calendar-state.ts`, `hooks/use-calendar-navigation.ts`, `hooks/use-calendar-virtualizer.ts`, `components/calendar-header.tsx`, `components/calendar-month-view.tsx`, `components/calendar-week-view.tsx`, `components/calendar-day-view.tsx`, `components/calendar-event-block.tsx`, `components/calendar-resource-header.tsx`, plus `*.test.ts` files for date-utils, layout-utils, time-utils (3 test files).
  - Updated: `schemas.ts` (CalendarSchema extended per design doc §4: CalendarView, event type/status, resource type/parent/avatar), `scheduling-renderer-definitions.ts` (Calendar component wired with proper fields: view/date/events/resources/eventTemplate/onEventClick/onDateChange/onViewChange), `styles.css` (full calendar styles), `calendar/index.ts` (exports).
  - Dependencies added: `@tanstack/react-virtual`.
  - Tests: 176/176 pass (10 test files), typecheck ✓, build ✓, lint ✓ (0 errors, 1 library compat warning).
  - Execution plan: `docs/plans/2026-07-20-0800-3-s4-calendar-core-plan.md` — all 4 phases executed. Closure audit gate left for independent sub-agent per AGENTS.md.

- **S5 — Calendar Interactive (Drag, Groups, Batch, Import/Export, Timezone, Print, Playground)**: Implemented the full S5 feature set on top of S4 calendar read-only core.
  - **Drag interactions**: `use-calendar-drag.ts` (drag to swap shifts with confirmation popup), `use-calendar-drag-create.ts` (long-press/drag to create events with shift type selector). Both wired into calendar.tsx, month/week/day views via event block onPointerDown/cell onPointerDown props.
  - **Resource groups**: `CalendarResourceGroup` component with expand/collapse, nested `resources[].resources` hierarchy, chevron toggle, sub-resource count display.
  - **Cross-day connectors**: `calendar-cross-day-lines.ts` with SVG quadratic bezier arcs between split multi-day event blocks, hover highlight support.
  - **Batch scheduling**: `CalendarBatchScheduler` component with date range picker, resource multi-select, shift type radio group, preview grid with conflict highlighting, 100-cell limit enforcement.
  - **iCal import/export**: `useCalendarICal` hook with `ical.js` integration, file parsing, serialization, Blob download, error handling. `ical.js` added as dependency.
  - **Timezone selector**: `CalendarTimezoneSelector` component with 20 common timezones, Intl.DateTimeFormat offset display, dropdown UI.
  - **Print/export**: `calendar-print.css` (`@media print` styles), `useCalendarExport` hook (window.print, html2canvas PNG export).
  - **Visual design**: S5.9 interactive states added to `src/styles.css` (drag ghost, drag-over cell highlight, nav animation, cross-day line styles, type selector/confirm overlay animations).
  - **Schema & definitions**: Updated `schemas.ts` and `scheduling-renderer-definitions.ts` with all new fields/events/handles.
  - **Playground**: Created `calendar-demo.tsx` with 10 resources, 2 months of events, registered to `scheduling-calendar` route, added navigation card to home page.
  - **Design docs**: Created `docs/components/calendar/design-batch-scheduling.md`, `design-ical.md`, `design-export.md`.
  - **Roadmap**: Updated S5 phase→`done`, S10.2→`done`.
  - **Tests**: 285/285 pass (26 test files), typecheck ✓ (56/56), build ✓ (30/30), lint ✓ (0 errors).
  - **Execution plan**: `docs/plans/2026-07-20-2000-3-s5-calendar-interactive-plan.md` — all 4 phases complete.

- **S2 — Gantt Interaction & Visual Rendering Layer**: Implemented the complete React rendering layer for the Gantt chart: resizable layout container with grid/timeline split, task grid with configurable columns/tree indent/inline editing, multi-row time scale header, background cell grid with weekend shading, task bars with type-specific styling (task/project/milestone) and progress overlays, SVG dependency lines with hover/delete, today line marker, command-mode drag-and-drop (move/resize-start/resize-end) with ghost, link drawing with temporary line and addLink on drop, bidirectional scroll sync (rAF-throttled), keyboard navigation with WAI-ARIA attributes, task editor (inline + dialog), and default visual design (loading/empty/hover/drag-ghost/zoom-transition). Created S10.1 playground demo page at `apps/playground/src/pages/gantt-demo.tsx` with sample multi-project data, registered to routes/navigation.
  - Files created (gantt/): `gantt-context.tsx`, `gantt-layout.tsx`, `gantt-header.tsx`, `gantt-grid.tsx`, `gantt-timescale.tsx`, `gantt-cellgrid.tsx`, `gantt-bars.tsx`, `gantt-links.tsx`, `gantt-markers.tsx`, `gantt-editor.tsx`, `gantt.tsx`, `hooks/use-gantt-drag.ts`, `hooks/use-gantt-link-draw.ts`, `hooks/use-gantt-scroll.ts`, `hooks/use-gantt-keyboard.ts`, plus test files `gantt-components.test.tsx`, `gantt-interactions.test.ts`, `gantt-editor.test.tsx`, `gantt-visual-states.test.tsx`.
  - Updated: `scheduling-renderer-definitions.ts` (Gantt component from `() => null` to real `Gantt`), `styles.css` (Gantt styles), `gantt/index.ts` (exports), `apps/playground/src/App.tsx` (route), `apps/playground/src/pages/index.ts` (export), `apps/playground/src/pages/home-page.tsx` (nav card), `apps/playground/src/route-model.ts` (domain route), `packages/flux-i18n/src/locales/en-US.ts`/`zh-CN.ts` (Gantt i18n keys).
  - Tests: 209/209 pass (14 test files), typecheck ✓, build ✓, lint ✓ (0 errors).
  - Execution plan: `docs/plans/2026-07-20-2000-1-s2-gantt-interactive-and-visual-plan.md` — all 4 phases complete, closure audit gate left for independent sub-agent per AGENTS.md.
  - **Build verification (S2)**: typecheck ✓ (56/56), build ✓ (30/30), lint ✓ (0 errors, 1 warning), test ✓ (all packages).

### 2026-07-20 (S8 — Barcode-input Camera Scan + S10.4 Playground)

- **S8 — Barcode-input v1 (Camera Scan, Decode, Form Integration)**: Implemented the full S8 barcode scanning input renderer in `packages/flux-renderers-scheduling/src/barcode-input/`.
  - **Phase 1 — Core Camera + Decode Hooks**: Created `prepare-wasm.ts`, `camera-utils.ts`, `barcode-detector-utils.ts`, `use-barcode-camera.ts`, `use-barcode-detect.ts` with 13 unit tests.
  - **Phase 2 — Renderer + Overlay UI + Torch**: Created `BarcodeInputSchema`, field rules, `use-barcode-torch.ts`, `barcode-scanner-overlay.tsx`, `barcode-input-renderer.tsx` with 12 unit tests.
  - **Phase 3 — Batch Queue + Offline/Degrade + S10.4 Playground**: Created `BarcodeQueue` with status lifecycle, offline detection, queue panel in overlay, camera degradation chain. Created `barcode-demo.tsx` playground page. Registered renderer in definitions/schemas/index. Updated playground routes.
  - **Updated**: `package.json` (added @zxing/library, @nop-chaos/flux-react, @nop-chaos/flux-renderers-form), `docs/components/roadmap-scheduling.md` (S8 phase→done, S8.1–S8.7→done, S10.4→done).
  - **Tests**: 454/454 pass (50 test files in scheduling package), typecheck ✓ (56/56), build ✓ (30/30), lint ✓ (0 new barcode-input errors).
  - **Execution plan**: `docs/plans/2026-07-20-2100-3-s8-barcode-input-plan.md` — all 3 phases complete. Closure audit gate left for independent sub-agent per AGENTS.md.
